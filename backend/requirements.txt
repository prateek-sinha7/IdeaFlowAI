fastapi==0.115.6
uvicorn==0.34.0
sqlalchemy==2.0.36
# Schema migrations. Production runs `alembic upgrade head` as the systemd
# unit's ExecStartPre (see docs/SIMPLE_AWS_DEPLOYMENT.md Appendix B). The
# 1.14.x series is the latest stable as of 2026-Q2 and pairs with SA 2.0.x.
alembic==1.14.0
# Postgres driver — required for the production DATABASE_URL=postgresql://...
# In dev with the SQLite default, this dependency is unused but harmless.
psycopg2-binary==2.9.12
pydantic[email]==2.12.5
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# langchain 1.x line. Bumped to satisfy deepagents 0.6.7's floor
# (langchain>=1.3.2, langchain-core>=1.4.0, langgraph>=1.2.4). Resolver-verified
# to coexist unchanged with langchain-aws 1.4.6 + langchain-anthropic 1.4.3.
langchain==1.3.4
langchain-core==1.4.0
# Anthropic direct API — used when ANTHROPIC_API_KEY is set (local dev)
langchain-anthropic==1.4.3
# LangGraph — runtime under the deepagents graphs (replaces the hand-rolled
# DeepAgent loop). deepagents 0.6.7 requires langgraph>=1.2.4,<1.3.
langgraph==1.2.4
# AWS Bedrock provider (default). On the langchain 1.x line to match
# langchain-core 1.x.
langchain-aws==1.4.6
# Pinned for reproducibility; langchain-aws depends on it transitively.
# langchain-aws 1.4.6 requires boto3>=1.42.42.
boto3==1.43.2
# LangChain deepagents — the agent runtime for ALL pipeline agents (replaces the
# custom app/agents/deep_agent.py). Pulls langgraph-checkpoint/prebuilt/sdk + langsmith.
deepagents==0.6.7
# LangGraph Postgres checkpointer — durable HITL review gates + crash/disconnect
# resume; uses the production DATABASE_URL=postgresql://... (manages its own tables).
langgraph-checkpoint-postgres==3.1.0
# Headless Chromium driver for prototype render-validation
# (app/agents/render_check.py). The browser binary itself is installed in the
# Dockerfile via `playwright install --with-deps chromium` (not in the wheel).
playwright==1.60.0
websockets==14.1
python-dotenv==1.0.1
pytest==8.3.4
hypothesis==6.122.3
httpx==0.28.1
# pytest-asyncio is required to actually run the @pytest.mark.asyncio tests
# in tests/unit/test_base_agent.py and tests/unit/test_pipeline_cancel.py.
# Without it pytest silently skips them with a PytestUnhandledCoroutineWarning
# — discovered when the local Bedrock-live walk-through caught two bugs in
# BaseAgent that the test suite had been quietly missing.
pytest-asyncio==0.24.0
# YAML frontmatter parser for AGENT.md files (folder-per-agent architecture)
python-frontmatter==1.1.0
# OpenTelemetry — the otel_tracing canonical hook (08-07 / OBS-02). API + SDK only
# (CNCF OpenTelemetry, github.com/open-telemetry/opentelemetry-python; human-verified
# on PyPI at the package-legitimacy checkpoint). The hook emits a span per fired
# lifecycle event behind a ConsoleSpanExporter by default; it auto-switches to the
# OTLP exporter only when OTEL_EXPORTER_OTLP_ENDPOINT is set, importing
# `opentelemetry-exporter-otlp` LAZILY in that branch — so the OTLP exporter is an
# OPTIONAL runtime dependency, NOT pinned here (least-privilege / supply-chain).
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
