# Shared by every prototype-* task. The verifiers are Python, and nothing else
# runs in here — the agent works on the host and uploads its deliverable, so
# this image needs no API key, no network and no project dependencies.
FROM python:3.12-slim
WORKDIR /app
