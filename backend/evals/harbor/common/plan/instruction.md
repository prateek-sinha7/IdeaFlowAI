Produce the build task plan for the specification you were given.

Output the `<tasks>` block with `## Task N:` headers and nothing else.
Every page defined in the spec must be covered by at least one task.
