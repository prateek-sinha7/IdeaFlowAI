Placeholder for the `ppt` workflow. No agents onboarded yet.

Add them here following the contract in [`../../README.md`](../../README.md),
starting from [`../../_templates/`](../../_templates/). The first file to write
is `workflow.yaml` — it declares the stage order and how each stage's input is
built from the previous stages.
