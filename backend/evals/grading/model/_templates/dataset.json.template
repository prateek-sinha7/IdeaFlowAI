{
  "_comment": "TEMPLATE — copy to <workflow>/datasets/<name>.json, drop this key. Filename is free-form; the dataset_id below is what identifies it and names the run folder. `.template` suffix so globs and JSON loaders skip it.",

  "dataset_id": "your-dataset-id",
  "for_agent": "OPTIONAL advisory guard naming the agent this dataset is meant for, validated only when present. Stops one stage's generated output being fed to the wrong agent. Omit it for a dataset that legitimately suits several stages.",
  "description": "What this dataset covers and why these rows were chosen.",

  "_row_contract": {
    "id": "REQUIRED. Names the ROW. Must be STABLE across every stage of the workflow — never suffix it per agent (billing_console_plan). The agent goes in the FILENAME. Stable ids are what let you join a row across stages and see where it degraded.",
    "industry": "REQUIRED. The domain/vertical. Used for slicing results.",
    "tags": "OPTIONAL. Free-form labels for slicing: complexity, shape, whatever you want to compare.",
    "expect": "OPTIONAL. 'pass' (default) or 'fail'. A row deliberately built to be rejected — a negative test. Without these you never learn whether your gates can say no.",
    "prompt": "REQUIRED. The input, verbatim. For a chained stage this is generated into <agent>_output.json rather than hand-written here."
  },

  "rows": [
    {
      "id": "example_row",
      "industry": "Example Industry",
      "tags": ["simple"],
      "prompt": "The input given to the agent."
    },
    {
      "id": "example_negative_row",
      "industry": "Example Industry",
      "tags": ["negative-test"],
      "expect": "fail",
      "prompt": "An input the agent SHOULD refuse or fail on."
    }
  ]
}
