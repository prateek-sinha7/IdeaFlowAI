<tasks>

## Task 1: HTML Shell & Navigation Chrome
Create prototype.html: `<!doctype html>`, `<head>` with one `<style>` block opening with the `:root` tokens (--bg:#f7f7f9, --fg:#1f2430, --accent:#3b4bd8, --surface:#ffffff, --border:#e2e4ea, --muted:#6b7280) and the full class system from the spec (`.sidebar`, `.nav-link`, `.main`, `.page`, `.page-header`, `.filter-group`, `.filter-btn`, `.table-wrap`, `.table`, `.amount`, `.badge` + pending/approved/rejected variants, `.card`, `.grid-2`, `.detail-list`, `.action-row`, `.btn`, `.btn-approve`, `.btn-reject`, `.form-group`, `.form-label`, `.form-input`, `.empty-note`). Body: a 220px sidebar (`data-od-id="sidebar"`) with brand "Ledgerline" and three nav links — Inbox `#/inbox`, Reports `#/reports`, Settings `#/settings`, each `data-page-link` — and a `<main data-od-id="main">` holding four empty `<section data-page="...">` elements: inbox (with `class="page is-active"`), expense-detail, reports, settings. Script block at the end of body: the `store` seeded with the spec's five expenses (ids 2201–2205, statuses Pending/Approved/Pending/Rejected/Approved), a `routes` array table — `{ pattern: 'inbox', page: 'inbox' }, { pattern: 'expense/:id', page: 'expense-detail' }, { pattern: 'reports', page: 'reports' }, { pattern: 'settings', page: 'settings' }` — and a hash router matching the hash through that table (exact segments, `:param` wildcards), toggling `is-active` on `section[data-page]` only and highlighting the matching `[data-page-link]`.

## Task 2: Inbox Page
Fill `section[data-page="inbox"]`: page header "Expense inbox", the four status filter buttons (All active by default) and the expense table with columns Expense, Merchant, Amount, Category, Submitted, Employee, Status and the five spec rows — amounts in an `.amount` cell, statuses as badge variants, each row navigating to `#/expense/{id}`. Rows are rendered by `renderInbox()` from `store.expenses` filtered by `store.filters.status`, so a status changed on the detail page shows here on return. When the filter matches nothing, render the single "No expenses in this state." row.

## Task 3: Expense Detail Page
Fill `section[data-page="expense-detail"]`: a `renderExpense(id)` function reads the `:id` matched by the router, looks the expense up in `store.expenses`, and renders the header ("EXP-2203 · Blue Bottle Coffee" style, with the current-status badge), the submission card (merchant, amount, category, submitted, employee, cost centre), the receipt card (payment method, receipt reference), the "← Back to inbox" link to `#/inbox`, and the action row with the Approve and Reject buttons. `approveExpense()` / `rejectExpense()` set the current expense's status in the store and re-render the header badge in place. All five ids (2201–2205) must render their own data; an unknown id renders a "No expense found for this link." card rather than throwing.

## Task 4: Reports & Settings Pages
Fill `section[data-page="reports"]`: header "Spend by category", table with columns Category, Expenses, Total and the four category rows (Travel 2 · $545.48, Software 1 · $683.76, Meals 1 · $58.42, Office 1 · $1,207.95) plus the grand-total row (5 · $2,495.61) — every figure must equal what the inbox rows sum to. Fill `section[data-page="settings"]`: one card with the three pre-filled fields (Auto-approve threshold "$75.00", Default currency "USD", Finance contact "finance@ledgerline.io") and the "Save policy" button whose handler writes "Policy saved." into an inline note under the button.

## Task 5: Final Wiring & Validation
Verify the finished file end to end: every nav href and every inbox-row target resolves through the `routes` table to a real `<section data-page>`; exactly one section carries `is-active` at any time; every `onclick` names a function defined in the script block; each filter button re-renders the inbox without console errors; each of the five expense links renders its own record; Approve and Reject flip the badge on the detail page AND on the inbox after navigating back; the Save policy button writes its confirmation; no page section is empty; all colors come from the `:root` tokens.
</tasks>
