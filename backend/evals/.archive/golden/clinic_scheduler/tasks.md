<tasks>

## Task 1: HTML Shell & Navigation Chrome
Create prototype.html: `<!doctype html>`, `<head>` with one `<style>` block opening with the `:root` tokens (--bg:#f6f7f6, --fg:#1c2b2a, --accent:#0f766e, --surface:#ffffff, --border:#dde3e2, --muted:#64748b), and the full class system from the spec (`.sidebar`, `.nav-link`, `.main`, `.page`, `.page-header`, `.toolbar`, `.search-input`, `.filter-group`, `.filter-btn`, `.table-wrap`, `.table`, `.badge` + three status variants, `.card`, `.grid-2`, `.detail-list`, `.form-group`, `.form-label`, `.form-input`, `.btn`, `.btn-primary`, `.empty-note`). Body: a 220px sidebar (`data-od-id="sidebar"`) with brand "Cedar Family Clinic" and three nav links — Schedule `#/schedule`, Providers `#/providers`, Settings `#/settings`, each `data-page-link` — and a `<main data-od-id="main">` holding four empty `<section data-page="...">` elements: schedule (with `class="page is-active"`), patient-detail, providers, settings. Script block at the end of body: the `store` object seeded with the spec's five patients (ids 101–105) and five appointments, a `routes` array table — `{ pattern: 'schedule', page: 'schedule' }, { pattern: 'patient/:id', page: 'patient-detail' }, { pattern: 'providers', page: 'providers' }, { pattern: 'settings', page: 'settings' }` — and a hash router that matches the current hash through that table (exact segments, `:param` wildcards), toggles `is-active` on `section[data-page]` only, and highlights the matching `[data-page-link]`.

## Task 2: Schedule Page
Fill `section[data-page="schedule"]`: page header "Today's schedule — Thursday 12 Mar 2026", toolbar with the search input (placeholder "Search patient or provider…", `oninput` re-render) and the four status filter buttons (All active by default), then the appointment table with columns Time, Patient, Provider, Reason, Status and the five spec rows, each status as its badge variant. Rows are rendered by a `renderSchedule()` function from `store.appointments`, filtered by `store.filters.query` (case-insensitive on patient + provider) and `store.filters.status`; each row is clickable and navigates to `#/patient/{patientId}`. When nothing matches, render the single "No appointments match." row.

## Task 3: Patient Detail Page
Fill `section[data-page="patient-detail"]`: a `renderPatient(id)` function reads the `:id` matched by the router, looks the patient up in `store.patients`, and renders the header with the patient's name, the record card (date of birth, phone, insurer, allergies) and the today's-visit card (time, provider, reason, status badge from that patient's appointment), plus the "← Back to schedule" link to `#/schedule`. All five ids (101–105) must render their own data; an unknown id renders a "No patient found for this link." card rather than throwing.

## Task 4: Providers & Settings Pages
Fill `section[data-page="providers"]`: header "Providers", table with columns Provider, Specialty, Room, Today's appointments and the two spec rows (Dr. Amara Osei — 3, Dr. Ben Kowalski — 2; the counts must agree with the schedule data). Fill `section[data-page="settings"]`: one card with the three pre-filled fields (Clinic name "Cedar Family Clinic", Working-day start "08:00", Reminder lead-time "24 hours") and the "Save preferences" `.btn-primary` whose handler writes "Preferences saved." into an inline note under the button.

## Task 5: Final Wiring & Validation
Verify the finished file end to end: every nav href and every schedule-row target resolves through the `routes` table to a real `<section data-page>`; exactly one section carries `is-active` at any time; every `onclick`/`oninput` names a function defined in the script block; typing in the search and clicking each filter re-renders without console errors; each of the five patient links renders its own record; the Save preferences button writes its confirmation; no page section is empty; all colors come from the `:root` tokens.
</tasks>
