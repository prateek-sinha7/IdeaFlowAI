<tasks>

## Task 1: HTML Shell, Store & Router
Create prototype.html: `<!doctype html>`, `<head>` with one `<style>` block opening with the `:root` tokens (--bg:#f6f7f9, --fg:#20293a, --accent:#3d47c2, --surface:#ffffff, --border:#e0e4ec, --muted:#606a7b, --ok:#15803d, --warn:#996a13, --danger:#b3261e, --info:#0b62a4) and the full class system from the spec (`.sidebar`, `.brand`, `.nav-link`, `.main`, `.page`, `.page-header`, `.page-title`, `.page-sub`, `.kpi-grid`, `.kpi`, `.kpi-value`, `.kpi-label`, `.toolbar`, `.search-input`, `.filter-group`, `.filter-btn`, `.table-wrap`, `.table`, `.row-link`, `.id-link`, `.badge` + the five semantic variants `-ok`/`-warn`/`-danger`/`-info`/`-neutral`, `.card`, `.card-title`, `.grid-2`, `.detail-list`, `.link-list`, `.back-link`, `.inline-form`, `.select-input`, `.form-group`, `.form-label`, `.form-input`, `.btn`, `.btn-primary`, `.btn-danger`, `.empty-note`, `.error-note`, `.save-note`). Body: a 220px sidebar (`data-od-id="sidebar"`) with brand "Ridgeline Residential" and six nav links — Dashboard `#/dashboard`, Units `#/units`, Tenants `#/tenants`, Work Orders `#/workorders`, Vendors `#/vendors`, Settings `#/settings`, each `data-page-link` — and a `<main data-od-id="main">` holding ten `<section data-page="...">` elements: dashboard (with `class="page is-active"`), units, unit-detail, tenants, tenant-detail, workorders, workorder-new, workorder-detail, vendors, settings. Script block at the end of body: a `TODAY = "17 Mar 2026"` constant; the `store` object seeded with the spec's 12 units, 10 tenants (T-01–T-10 with their seed payments), 6 work orders (WO-1036–WO-1041 with vendorIds, completedOn dates and activity logs), 4 vendors (V-01–V-04), the three-group `filters` object and `settings`; a `routes` array table — `{ pattern: 'dashboard', page: 'dashboard' }, { pattern: 'units', page: 'units' }, { pattern: 'unit/:id', page: 'unit-detail' }, { pattern: 'tenants', page: 'tenants' }, { pattern: 'tenant/:id', page: 'tenant-detail' }, { pattern: 'workorders', page: 'workorders' }, { pattern: 'workorders/new', page: 'workorder-new' }, { pattern: 'workorder/:id', page: 'workorder-detail' }, { pattern: 'vendors', page: 'vendors' }, { pattern: 'settings', page: 'settings' }` — and a hash router that matches the current hash through that table (exact segments, `:param` wildcards, first match wins), toggles `is-active` on `section[data-page]` only, highlights the matching `[data-page-link]`, and calls the matched page's render function so every page recomputes from the store on each visit.

## Task 2: Dashboard Page
Fill `section[data-page="dashboard"]`: page header "Operations dashboard — Tuesday 17 Mar 2026", then `renderDashboard()` computes everything from the store: the four KPI tiles (Occupancy "10 / 12" from occupied-or-notice units over all units; Rent outstanding "$3,270" as the sum over tenants of rent minus their March 2026 payments, floored at zero per tenant; Open work orders "4" as the not-Completed count; Vacant units "2"), the urgent card listing the not-Completed High-priority work orders (initially WO-1036 and WO-1038) as links to their detail, and the move-outs & vacancies card listing Rosa Ibarra's notice (link to `#/tenant/T-03`), the vacant units A-102 and B-201 (links to their unit detail) and the "Create work order →" link to `#/workorders/new`.

## Task 3: Units Page
Fill `section[data-page="units"]`: page header "Units", toolbar with the search input (placeholder "Search unit or tenant…", `oninput` re-render), the four building filter buttons (All active by default) and the four status filter buttons (All active by default), then the unit table with columns Unit, Building, Beds, Sq ft, Rent, Tenant, Status and the twelve spec rows, statuses as badges (Occupied ok, Vacant neutral, Notice warn). Rows are rendered by `renderUnits()` from `store.units`, filtered by `store.filters.units.query` (case-insensitive on unit code + tenant name), `.building` and `.status` — all three combine; each row is clickable and navigates to `#/unit/{unit code}`. When nothing matches, render the single "No units match." row.

## Task 4: Unit Detail Page
Fill `section[data-page="unit-detail"]`: a `renderUnitDetail(id)` function reads the `:id` matched by the router, looks the unit up in `store.units`, and renders the header ("A-201 — Alder building"), the facts card (building, beds, square footage, rent, status badge, and the current tenant as a link to `#/tenant/{id}` or "Vacant — ready to list"), and the work-order history card: a table [Work order, Category, Priority, Status] of this unit's work orders computed from `store.workorders`, each id linking to `#/workorder/{id}`, with the "No work orders for this unit yet." note plus a "Create work order →" link to `#/workorders/new` when there are none. All 12 unit codes must render their own data; an unknown code renders a "No unit found for this link." card with the back link rather than throwing. Include the "← Back to units" link to `#/units`.

## Task 5: Tenants & Tenant Detail Pages
Fill `section[data-page="tenants"]`: header "Tenants", toolbar with the search input (placeholder "Search tenant or unit…") and the four payment filter buttons (All / Paid / Partial / Overdue, All active by default), then the table with columns Tenant, Unit, Lease ends, Rent, Paid this month, Status rendered by `renderTenants()` — paid-this-month is COMPUTED as the sum of that tenant's payments whose date contains "Mar 2026", and the status badge is Paid (paid ≥ rent, ok), Partial (some but less, warn) or Overdue ($0, danger); search and filter combine; rows navigate to `#/tenant/{id}`; empty state "No tenants match.". Fill `section[data-page="tenant-detail"]`: `renderTenantDetail(id)` renders the header with the tenant's name, the lease & balance card (unit link, lease end, phone, rent, paid this month, balance remaining, status badge), the payment history table [Date, Amount, Method] from that tenant's payments, and the inline record-payment form (amount input with placeholder "Amount, e.g. 1920", ACH/Check/Card method select, "Record payment" button). `onRecordPayment(id)` validates the amount — non-numeric or ≤ 0 shows the inline "Enter a payment amount greater than zero." error and adds nothing — then appends {TODAY, amount, method} to the tenant's payments, clears the field and re-renders so the balance and badge update in place. All ten ids render their own record; an unknown id renders "No tenant found for this link." with the back link. Include the "← Back to tenants" link.

## Task 6: Work Orders List & New Work Order Pages
Fill `section[data-page="workorders"]`: header "Work orders", toolbar with the search input (placeholder "Search work order, unit or description…"), the four status filter buttons (All / Open / In Progress / Completed), the four priority filter buttons (All / High / Medium / Low) and the "New work order" button-styled link to `#/workorders/new`, then the table with columns Work order, Unit, Category, Priority, Opened, Vendor, Status rendered by `renderWorkOrders()` from `store.workorders` (priority badges danger/warn/neutral, status badges info/warn/ok, vendor "—" when unassigned); search and both filter groups combine; rows navigate to `#/workorder/{id}`; empty state "No work orders match.". Fill `section[data-page="workorder-new"]`: header "New work order", back link to `#/workorders`, and one form card — unit select (all 12 codes), category select (Plumbing / Electrical / HVAC / Appliance / Paint / General), priority select (High / Medium / Low), description input (placeholder "e.g. Bathroom fan rattling") and the "Create work order" `.btn-primary`. `onCreateWorkOrder()` shows the inline "Enter a description before creating." error for an empty description; otherwise it computes the next sequential id (one more than the highest existing WO number — initially WO-1042), pushes {id, the selected unit/category/priority, status "Open", vendorId null, opened TODAY, the description, log ["Created by front desk"]} onto `store.workorders`, and navigates to the new order's detail. `renderWorkOrderNew()` resets the form and clears the error on every visit.

## Task 7: Work Order Detail Page
Fill `section[data-page="workorder-detail"]`: `renderWorkOrderDetail(id)` renders the header ("WO-1036 — B-102"), the facts card (unit link, category, priority badge, opened date, vendor name or "— unassigned", status badge, description), the conditional actions area, and the activity log card from the order's `log` entries. Actions: while not Completed and unassigned, an Assign-vendor control (select of the four vendors + "Assign vendor" button — `onAssignVendor(id)` sets the vendorId, moves status Open → In Progress, appends "17 Mar 2026 — Assigned to {vendor name}" to the log and re-renders); while not Completed, a "Mark complete" `.btn-primary` (`onMarkComplete(id)` sets status Completed with completedOn TODAY, appends "17 Mar 2026 — Completed — closed by property manager", swaps the buttons for "Completed on 17 Mar 2026 — no further actions.") and a "Cancel work order" `.btn-danger` (`onCancelWorkOrder(id)` REMOVES the order from `store.workorders` and navigates to `#/workorders`); once Completed, only the completed line. Every stored id renders its own record; an unknown or cancelled id renders "No work order found for this link." with the back link rather than throwing. Include the "← Back to work orders" link.

## Task 8: Vendors & Settings Pages
Fill `section[data-page="vendors"]`: header "Vendors", table with columns Vendor, Trade, Contact, Phone, Open assignments rendered by `renderVendors()` — the four spec vendors with the open-assignment count COMPUTED as that vendor's not-Completed work orders (initially Volt Electric 0, Delta Plumbing 1, Hearthstone Handywork 0, BrightCoat Painting 1), so an assignment, completion or cancellation elsewhere changes the count here. Fill `section[data-page="settings"]`: one card with the three pre-filled fields (Office name "Ridgeline Residential — Summit Ridge Office", Rent due day "1st of the month", Work-order auto-close "14 days after completion") and the "Save preferences" `.btn-primary` whose handler writes the values back to `store.settings` and "Preferences saved." into an inline note under the button.

## Task 9: Final Wiring & Validation
Verify the finished file end to end: every nav href, table row, dashboard list entry, tenant/unit/work-order cross-link and the create link resolve through the `routes` table to a real `<section data-page>`; exactly one section carries `is-active` at any time; every `onclick`/`oninput` names a function defined in the script block; each toolbar's search plus its filter groups combine and re-render without console errors, including every empty state; all 12 unit codes, all 10 tenant ids and all 6 work-order ids render their own detail records, and an invented id on each of the three dynamic routes shows its not-found card; the full CRUD chain works — create a work order (it gets WO-1042, appears on Work Orders, its unit's history and the Dashboard count), assign a vendor to WO-1038 (Vendors count rises, status flips In Progress), mark WO-1036 complete (open counts fall, buttons swap for the completed line), cancel WO-1037 (it vanishes from every list and its old link shows the not-found card), and record a $1,350 payment for Leo Brandt (balance $0, badge flips Paid, rent outstanding drops to $1,920) — with the invalid-amount and empty-description errors both shown inline; the Save preferences button writes its confirmation; no page section is empty; all colors come from the `:root` tokens.
</tasks>
