<tasks>

## Task 1: HTML Shell, Store & Router
Create prototype.html: `<!doctype html>`, `<head>` with one `<style>` block opening with the `:root` tokens (--bg:#f5f6f8, --fg:#16212e, --accent:#0b5fad, --surface:#ffffff, --border:#dce2e9, --muted:#5d6b7a, --ok:#1a7f37, --warn:#9a6700, --danger:#b3261e) and the full class system from the spec (`.sidebar`, `.brand`, `.nav-link`, `.main`, `.page`, `.page-header`, `.page-title`, `.page-sub`, `.toolbar`, `.search-input`, `.filter-group`, `.filter-btn`, `.table-wrap`, `.table`, `.row-link`, `.trip-link`, `.badge` + the four status variants `-enroute`/`-loading`/`-delayed`/`-delivered`, `.card`, `.card-title`, `.grid-2`, `.detail-list`, `.timeline`, `.back-link`, `.delay-note`, `.form-group`, `.form-label`, `.form-input`, `.btn`, `.btn-primary`, `.empty-note`, `.save-note`). Body: a 220px sidebar (`data-od-id="sidebar"`) with brand "Harbor Line Freight" and five nav links — Board `#/board`, Drivers `#/drivers`, Vehicles `#/vehicles`, Exceptions `#/exceptions`, Settings `#/settings`, each `data-page-link` — and a `<main data-od-id="main">` holding six `<section data-page="...">` elements: board (with `class="page is-active"`), trip-detail, drivers, vehicles, exceptions, settings. Script block at the end of body: the `store` object seeded with the spec's eight trips (TRP-7300–TRP-7307, each with driverId, unit, trailer, lane, cargo, weightLbs, status, eta/deliveredAt, delay info for the two Delayed trips, and their timestamped checkins), seven drivers (DRV-01–DRV-07), eight vehicles, `filters` and `settings`; a `routes` array table — `{ pattern: 'board', page: 'board' }, { pattern: 'trip/:id', page: 'trip-detail' }, { pattern: 'drivers', page: 'drivers' }, { pattern: 'vehicles', page: 'vehicles' }, { pattern: 'exceptions', page: 'exceptions' }, { pattern: 'settings', page: 'settings' }` — and a hash router that matches the current hash through that table (exact segments, `:param` wildcards), toggles `is-active` on `section[data-page]` only, highlights the matching `[data-page-link]`, and calls the matched page's render function so every page recomputes from the store on each visit.

## Task 2: Board Page
Fill `section[data-page="board"]`: page header "Dispatch board — Tuesday 17 Mar 2026", toolbar with the search input (placeholder "Search driver or destination…", `oninput` re-render) and the five status filter buttons (All active by default), then the trip table with columns Trip, Driver, Lane, Cargo, Weight, ETA / Delivered, Status and the eight spec rows, each status as its badge variant and each weight formatted like "34,800 lbs". Rows are rendered by a `renderBoard()` function from `store.trips`, filtered by `store.filters.query` (case-insensitive on driver name + destination city) and `store.filters.status`; each row is clickable and navigates to `#/trip/{trip id}`. When nothing matches, render the single "No trips match." row.

## Task 3: Trip Detail Page
Fill `section[data-page="trip-detail"]`: a `renderTripDetail(id)` function reads the `:id` matched by the router, looks the trip up in `store.trips`, and renders the header ("TRP-7301 — Oakland, CA → Reno, NV"), the trip facts card (driver, tractor / trailer, lane, cargo, weight, ETA or delivered time, status badge), the delay note with reason and original vs current ETA when the trip is Delayed, the stops & check-ins timeline from that trip's `checkins`, and the action area: a "Mark delivered" `.btn-primary` while status is En route/Loading/Delayed, or the line "Delivered — POD on file." once Delivered. `onMarkDelivered(id)` sets the trip's status to Delivered in the store, appends the check-in "Delivered — confirmed by dispatch", and re-renders the page so the badge and button swap in place. All eight ids (TRP-7300–TRP-7307) must render their own data; an unknown id renders a "No trip found for this link." card with the back link rather than throwing. Include the "← Back to board" link to `#/board`.

## Task 4: Drivers & Vehicles Pages
Fill `section[data-page="drivers"]`: header "Drivers", table with columns Driver, CDL, Home terminal, HOS remaining, Current trip, Status and the seven spec rows, rendered by `renderDrivers()` — each driver's current trip is COMPUTED as their non-Delivered trip in `store.trips` (link to `#/trip/{id}`) and the status badge mirrors that trip's status, with "—" and "Available" when they have none (Omar Sadiq initially; any driver whose delivery was just closed). Fill `section[data-page="vehicles"]`: header "Vehicles", table with columns Unit, Model, Odometer, Next service, Trailer, Assignment and the eight spec tractors, rendered by `renderVehicles()` — trailer and assignment are computed from the active trip using that unit (link to `#/trip/{id}`), falling back to "—" / "At yard". Both tables must agree with the Board's data because they derive from the same `store.trips`, never from copied literals.

## Task 5: Exceptions & Settings Pages
Fill `section[data-page="exceptions"]`: header "Exceptions", table with columns Trip, Driver, Lane, Reason, Original ETA, Current ETA rendered by `renderExceptions()` as exactly the trips whose status is Delayed (initially TRP-7303 and TRP-7306 with the spec's reasons and ETAs), each row clickable to its trip detail; when none are Delayed render the single "No delayed trips right now." row. Fill `section[data-page="settings"]`: one card with the three pre-filled fields (Terminal name "Oakland Central Terminal", Dispatch day start "05:30", Delay alert threshold "30 minutes") and the "Save preferences" `.btn-primary` whose handler writes the values back to `store.settings` and "Preferences saved." into an inline note under the button.

## Task 6: Final Wiring & Validation
Verify the finished file end to end: every nav href, board row, exceptions row, drivers current-trip link and vehicles assignment link resolves through the `routes` table to a real `<section data-page>`; exactly one section carries `is-active` at any time; every `onclick`/`oninput` names a function defined in the script block; typing in the search and clicking each of the five filters re-renders without console errors; each of the eight trip links renders its own record and an invented id shows the not-found card; Mark delivered on TRP-7301 flips its badge in place, then the Board shows it Delivered, Exceptions still shows exactly the Delayed trips, Marcus Bell reads Available on Drivers, and HLF-T04 reads At yard on Vehicles; the Save preferences button writes its confirmation; no page section is empty; all colors come from the `:root` tokens.
</tasks>
