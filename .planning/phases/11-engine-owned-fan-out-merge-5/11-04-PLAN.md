---
phase: 11-engine-owned-fan-out-merge-5
plan: 04
type: execute
wave: 4
depends_on: ["11-01"]
files_modified:
  - backend/agents/execution_engine/budget.py
  - backend/agents/execution_engine/fanout.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/agents/execution_engine/engine.py
  - backend/agents/workflows/compiler.py
  - backend/agents/authz.py
  - backend/app/core/config.py
  - backend/tests/agents/test_budget.py
  - backend/tests/agents/test_compiler_trust.py
autonomous: true
requirements: [FANOUT-09, OBS-01]
must_haves:
  truths:
    - "BudgetManager reserves before every spawn and enforces total subagents, concurrency, depth (ctx.depth), tokens, and wall-clock; BudgetExceeded aborts gracefully with partial results"
    - "Locked defaults are module constants: max_subagents=8, max_concurrency=4, max_depth=2, wall_clock=900s"
    - "Trust-conditional Limits: file manifests may raise via Limits; user/db manifests may only lower (rejected/clamped at compile)"
    - "Per-workspace ceilings checked at reserve time against aggregate spend across the workspace's runs; default unset via a settings seam"
    - "A BudgetSnapshot (tokens, cost, subagents, depth, wall_clock spent) persists to workflow_runs.budget_snapshot_json on completion, abort, and cancel"
  artifacts:
    - path: "backend/agents/execution_engine/budget.py"
      provides: "BudgetManager.reserve() enforcing (raises BudgetExceeded) + spent()->BudgetSnapshot + per-workspace ceiling check"
      contains: "raise BudgetExceeded"
    - path: "backend/agents/workflows/compiler.py"
      provides: "Trust-conditional Limits materialization (file raises, user/db lower-only)"
      contains: "Limits"
  key_links:
    - from: "backend/agents/execution_engine/fanout.py"
      to: "ctx.budget.reserve"
      via: "reserve-before-spawn (the first thing run_fanout does)"
      pattern: "budget.reserve"
    - from: "backend/agents/execution_engine/engine.py"
      to: "workflow_runs.budget_snapshot_json"
      via: "BudgetSnapshot persisted on completion/abort/cancel"
      pattern: "budget_snapshot"
---

<objective>
Make the 11-01 budget seam ENFORCE. `BudgetManager.reserve()` (replacing the stub) raises `BudgetExceeded` when a spawn would exceed total subagents / concurrency / depth (`ctx.depth`) — reserve-before-spawn (FANOUT-09). Tokens + wall-clock are check-at-boundary + mid-flight abort (you cannot pre-reserve unknown token spend — the §6 `reserve(*, tokens=0, subagents=0)` dual shape, D-05). Locked defaults are module constants (8/4/2/900s). `Limits` becomes trust-conditional in the compiler (file manifests may raise; user/db lower-only — 08-03/10-02 ceiling precedent). Per-workspace ceilings are checked at reserve time against aggregate spend across the workspace's runs (a `ScopedStore` aggregate; default unset via a settings seam). A `BudgetSnapshot` persists to `workflow_runs.budget_snapshot_json` (the 0014 forward column finally written) on completion, abort, AND cancel (OBS-01). On `BudgetExceeded`: completed workers' artifacts preserved + surfaced, pending workers never spawn, a visible `budget_warning`/abort event reports it.

Purpose: FANOUT-09 + OBS-01 — the resource-exhaustion (fork-bomb) DoS mitigation. Reserve at the single enforcement point (`run_fanout` — the only spawn path) is bypass-proof.
Output: enforcing `BudgetManager` + the reserve call wired in `run_fanout` + trust-conditional `Limits` compiler rule + per-workspace `ScopedStore` aggregate + `BudgetSnapshot` persistence at the engine completion/abort/cancel boundaries + the settings seam + the `test_budget.py` suite.
</objective>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift verification):
- `agents/execution_engine/budget.py` — enforcing `BudgetManager.reserve` (raises `BudgetExceeded`), `BudgetManager.note_tokens`/`note_wall_clock` (check-at-boundary), `BudgetManager.warn_threshold_reached`, `BudgetManager.from_limits` (constructs from `Limits` + defaults + per-workspace ceiling)
- `agents/workflows/compiler.py` — `_compile_limits` helper (trust-conditional materialization of `Step`/workflow `Limits`)
- `agents/authz.py` — `ScopedStore.workspace_budget_spent(workspace_id)` aggregate read (default-deny, owner+workspace keyed)
- `agents/execution_engine/kernel_services.py` — `KernelServices.persist_budget_snapshot`, `KernelServices.workspace_budget_spent` handle methods
- `app/core/config.py` — `WORKSPACE_BUDGET_*` settings seam (default unset / None)
- New event: `budget_warning` (string `type` value; ≥80% of any ceiling or a failed reserve)
</artifacts_this_phase_produces>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-SPEC.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-RESEARCH.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-PATTERNS.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforcing BudgetManager (reserve-before-spawn) + trust-conditional Limits + per-workspace ceiling</name>
  <files>backend/agents/execution_engine/budget.py, backend/agents/execution_engine/fanout.py, backend/agents/workflows/compiler.py, backend/agents/authz.py, backend/app/core/config.py, backend/tests/agents/test_budget.py, backend/tests/agents/test_compiler_trust.py</files>
  <read_first>
    - backend/agents/execution_engine/budget.py (the 11-01 stub: module constants DEFAULT_MAX_SUBAGENTS=8/CONCURRENCY=4/DEPTH=2/WALL_CLOCK=900, BudgetExceeded, BudgetSnapshot, the reserve() stub to make enforcing)
    - backend/agents/execution_engine/fanout.py (the reserve() call-site already first in run_fanout; ctx.depth; the spawn loop)
    - backend/agents/workflows/compiler.py lines 190-217 (_check_trust), 218-356 (_compile_step + the privileged-grant/trust rules — the Limits trust rule mirrors), 63-90 (_ALLOWED_STEP_KEYS incl. limits/fanout)
    - backend/agents/workflows/plan.py lines 230-238 (Limits dataclass: max_tokens/max_subagents/max_depth/wall_clock_seconds)
    - backend/agents/authz.py (ScopedStore aggregate read pattern — read_exec_runs/_scope_owner_ws; the workspace-keyed aggregate)
    - backend/app/core/config.py (the settings module — where the WORKSPACE_BUDGET seam lands, default None)
    - backend/tests/agents/test_compiler_trust.py (the trust-ceiling test suite to extend for the Limits rule)
  </read_first>
  <action>
Make `BudgetManager.reserve(self, *, tokens=0, subagents=0, concurrency=0, depth=0)` ENFORCING: it raises `BudgetExceeded` (naming the breached dimension) when the requested COUNTABLE units would exceed the resolved caps — total `subagents` (running + requested > max_subagents), `concurrency` (> max_concurrency), or `depth` (`ctx.depth` + 1 > max_depth, i.e. nested fan-out beyond depth 2). The reserve is the FIRST thing `run_fanout` does, BEFORE any allocate/run_agent (Pitfall 4 — already the 11-01 call-site; this plan makes the body enforce). Tokens + wall-clock are check-at-boundary, NOT pre-reserved: add `note_tokens(n)` (accumulate, raise BudgetExceeded if over max_tokens when declared) + a wall-clock check (`note_wall_clock()` / a deadline captured at run_fanout entry; a breach mid-flight aborts gracefully). `from_limits(limits: Limits | None, *, workspace_ceiling=None)` constructs the manager resolving `Limits` over the module-constant defaults (undeclared -> defaults; tokens uncapped unless declared). Add `warn_threshold_reached(dimension)` returning True at ≥ `BUDGET_WARN_THRESHOLD` (0.8) of any ceiling — used by run_fanout to emit `budget_warning`.

Per-workspace ceiling (OBS-01): add `ScopedStore.workspace_budget_spent(self, workspace_id) -> dict` — a default-deny aggregate (owner+workspace keyed; cross-owner unaffected) summing `subagent_runs`/run spend across the workspace's runs; reached via a new `KernelServices.workspace_budget_spent` handle. `BudgetManager.reserve` checks the per-workspace aggregate against the configured workspace ceiling at reserve time. Add the settings seam to `app/core/config.py`: `WORKSPACE_BUDGET_MAX_SUBAGENTS`/etc default `None` (unset = uncapped; no UI — the seam only).

Trust-conditional `Limits` in `compiler.py`: add `_compile_limits` materializing the step/workflow `Limits` from the manifest. Under a TRUSTED (file/builtin) manifest, `Limits` may RAISE above the defaults. Under an UNTRUSTED (`user`/`db`) manifest, a `Limits` that RAISES any cap above the default ceiling is rejected with a `CompilerError` naming the offending dimension (user/db may only LOWER — the 08-03 AGENT.md-only-lowers + 10-02 ceiling precedent). The concurrency clamp at runtime (`min(declared, 4)` in run_fanout from 11-01) is the runtime backstop; this compile rule is the static gate.

Write `test_budget.py` (FANOUT-09 + OBS-01): with cap N, worker N+1 never spawns and the rejection has no side effects (reserve-before-spawn — zero extra subagent_runs rows); nested fan-out beyond depth 2 rejected (ctx.depth+1 > max_depth -> BudgetExceeded); a wall-clock breach aborts; defaults asserted as module constants (8/4/2/900); a configured-low per-workspace ceiling fails a second run's reserve with BudgetExceeded (workspace accounting keyed owner+workspace, cross-owner unaffected); partial results surfaced on abort. Extend `test_compiler_trust.py`: a user-trust manifest raising a Limits cap is rejected/clamped at compile; a file manifest raising it is accepted.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_budget.py tests/agents/test_compiler_trust.py -x -p no:cacheprovider && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - budget.py reserve() contains `raise BudgetExceeded` and enforces subagents/concurrency/depth caps
    - `grep -c "DEFAULT_MAX_SUBAGENTS = 8\|DEFAULT_MAX_CONCURRENCY = 4\|DEFAULT_MAX_DEPTH = 2\|DEFAULT_WALL_CLOCK_SECONDS = 900" agents/execution_engine/budget.py` returns 4 (defaults asserted as module constants)
    - reserve-before-spawn test: with cap N, the N+1 worker never spawns AND leaves zero new subagent_runs rows
    - depth>2 nested fan-out raises BudgetExceeded; wall-clock breach aborts
    - compiler.py contains `_compile_limits`; a user-trust manifest raising a Limits cap is a CompilerError (test_compiler_trust.py); a file manifest raising it is accepted
    - agents/authz.py contains `def workspace_budget_spent`; a configured-low workspace ceiling fails the second run's reserve
    - `/opt/homebrew/bin/lint-imports` 4 kept / 0 broken
  </acceptance_criteria>
  <done>BudgetManager enforces reserve-before-spawn (subagents/concurrency/depth) + check-at-boundary tokens/wall-clock; Limits is trust-conditional at compile; per-workspace ceiling enforced; defaults are module constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BudgetSnapshot persistence + budget_warning event + graceful abort with partial results</name>
  <files>backend/agents/execution_engine/engine.py, backend/agents/execution_engine/fanout.py, backend/agents/execution_engine/kernel_services.py, backend/tests/agents/test_budget.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py (the completion/abort paths in execute() ~419-470 + the dispatch loop where a run terminates; the single emit boundary append_event:124; the exec-workspace gated provisioning ~1200-1226 conditional-only pattern so budget snapshot writes only when fanout ran / dormant otherwise)
    - backend/agents/execution_engine/budget.py (BudgetSnapshot from spent(); the 11-01 dataclass)
    - backend/agents/execution_engine/kernel_services.py (record_exec_run None-degrade pattern — persist_budget_snapshot follows it; the run_id/scoped_store handles)
    - backend/agents/authz.py (the workflow_runs update path — budget_snapshot_json is the 0014 forward column to write; how a run row is updated owner-scoped)
  </read_first>
  <action>
Persist the `BudgetSnapshot` to `workflow_runs.budget_snapshot_json` (the 0014 forward column, finally written — OBS-01) on run completion, abort (BudgetExceeded), AND cancel. Add `KernelServices.persist_budget_snapshot(self, snapshot)` cloning the `record_exec_run` None-degrade pattern (`store is None -> return`; wrap in try/except -> `logger.warning`; audit/snapshot must NEVER abort the run). Call `ctx.budget.spent()` -> snapshot -> persist at the engine's run-termination boundaries. The write MUST be strictly conditional so existing workflows (no fanout, no budget activity) stay byte/event-identical — a run that never fanned out writes a zero/None snapshot or none (gated like the exec-workspace provisioning; the 5 characterization snapshots must not drift, Pitfall 3).

In `run_fanout`, emit a `budget_warning` event when `ctx.budget.warn_threshold_reached(dimension)` (≥80% of any ceiling) OR a reserve fails — through the single emit boundary + generic forward (zero websocket.py edits). On `BudgetExceeded`: completed workers' artifacts are preserved + surfaced (the fragment artifacts persisted in 11-03 before merge), pending workers never spawn, and the abort event reports the breach. The graceful abort returns the partial structured summary (per-worker status: completed vs never-spawned).

Extend `test_budget.py`: a `BudgetSnapshot` is persisted to `workflow_runs.budget_snapshot_json` with spent figures on BOTH completion AND abort; a `budget_warning` event appears at ≥80% consumption; on `BudgetExceeded` the partial results (completed fragment artifacts) are surfaced and pending workers left unspawned; the 5 characterization snapshots stay byte/event-identical with `SNAPSHOT_UPDATE` unset (no budget activity for existing workflows).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_budget.py tests/agents/test_characterization_prototype.py tests/agents/test_banned_patterns.py -x -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - engine.py persists a BudgetSnapshot to workflow_runs.budget_snapshot_json on completion AND abort AND cancel (grep: `budget_snapshot` in engine.py)
    - kernel_services.py contains `def persist_budget_snapshot` (None-degrading)
    - A snapshot-on-completion AND a snapshot-on-abort test both assert spent figures in budget_snapshot_json
    - A budget_warning event fires at ≥80% of a ceiling (asserted in the stream)
    - On BudgetExceeded, completed fragment artifacts are surfaced and pending workers never spawn (partial-results test)
    - 5 characterization snapshots byte/event-identical with SNAPSHOT_UPDATE unset; test_banned_patterns.py green (INV-1 — no name-branch in budget.py/fanout.py)
  </acceptance_criteria>
  <done>BudgetSnapshot persists on completion/abort/cancel; budget_warning emitted at the 80% threshold; graceful abort surfaces partial results; existing workflows stay byte/event-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest Limits -> compiler | Untrusted user/db Limits may only lower; file Limits may raise — the static budget ceiling gate |
| run_fanout -> BudgetManager.reserve | The single enforcement point; spawning outside run_fanout would bypass it |
| workspace -> per-workspace ceiling | Aggregate spend keyed owner+workspace; cross-owner runs must not interfere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Denial of Service | resource exhaustion via unbounded fan-out (fork bomb) | mitigate | BudgetManager reserve-before-spawn enforces subagents=8/concurrency=4/depth=2/wall=900s; depth>2 -> BudgetExceeded; per-workspace ceiling; reserve at the only spawn path (bypass-proof) |
| T-11-04-02 | EoP | user/db manifest raising a Limits cap above the ceiling | mitigate | trust-conditional _compile_limits rejects an untrusted manifest raising any cap (CompilerError naming the dimension); file manifests may raise (08-03/10-02 precedent) |
| T-11-04-03 | EoP / DoS | bypassing the budget by spawning outside run_fanout | mitigate | run_fanout is the ONLY spawn path (FANOUT-02 grep); reserve is the first call inside it (enforcement-point discipline, like the exec recorder) |
| T-11-04-04 | Information Disclosure | cross-owner workspace budget aggregate read | mitigate | ScopedStore.workspace_budget_spent is owner+workspace keyed default-deny; cross-owner unaffected |
| T-11-04-05 | Availability | snapshot/audit write aborts the run | mitigate | persist_budget_snapshot is None-degrading (record_exec_run pattern); a persist failure logs + continues, never aborts |
| T-11-04-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (RESEARCH audit N/A) |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_budget.py tests/agents/test_compiler_trust.py tests/agents/test_fanout.py tests/agents/test_characterization_*.py tests/agents/test_banned_patterns.py -x -p no:cacheprovider`
- `/opt/homebrew/bin/lint-imports` (4 kept / 0 broken)
- 5 characterization snapshots byte/event-identical with `SNAPSHOT_UPDATE` unset
</verification>

<success_criteria>
- BudgetManager reserves-before-spawn; subagents/concurrency/depth enforced + tokens/wall-clock check-at-boundary; BudgetExceeded aborts gracefully with partial results (FANOUT-09)
- defaults are module constants (8/4/2/900); Limits trust-conditional at compile
- per-run + per-workspace ceilings; BudgetSnapshot persisted to budget_snapshot_json on complete/abort/cancel (OBS-01)
- budget_warning at 80%; characterization snapshots byte/event-identical; import-linter 4/0
</success_criteria>

<output>
Create `.planning/phases/11-engine-owned-fan-out-merge-5/11-04-SUMMARY.md` when done
</output>
