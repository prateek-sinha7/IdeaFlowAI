---
phase: 11-engine-owned-fan-out-merge-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/execution_engine/fanout.py
  - backend/agents/execution_engine/budget.py
  - backend/agents/execution_engine/context.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/agents/execution_engine/engine.py
  - backend/agents/capabilities/strategies/fanout_batch.py
  - backend/agents/capabilities/tools/providers.py
  - backend/app/agents/tools/runner_tools.py
  - backend/agents/workflows/plan.py
  - backend/agents/workflows/compiler.py
  - backend/agents/capabilities/registry.py
  - backend/app/models/subagent_run.py
  - backend/alembic/versions/0019_subagent_runs.py
  - backend/agents/authz.py
  - specs/003-workflow-engine-decoupling/migration-ledger.md
  - backend/tests/agents/test_fanout.py
  - backend/tests/agents/test_fanout_tool.py
  - backend/tests/agents/test_subagent_runs.py
  - backend/tests/agents/test_registry_capabilities.py
  - backend/tests/agents/_scripted_model.py
autonomous: true
requirements: [FANOUT-01, FANOUT-02, FANOUT-03, FANOUT-04, FANOUT-10]
must_haves:
  truths:
    - "A granted step's spawn_subagents tool call produces a structured request that the engine fulfils via the single kernel run_fanout"
    - "A step without tools.spawn_subagents never has the tool bound"
    - "Both declarative (step.fanout via fanout_batch strategy) and runtime (tool) entry points funnel through one run_fanout"
    - "Worker selection resolves self->step agent xN and named->allowed_workers+registry; a disallowed/unknown worker is rejected before any spawn"
    - "Parallel mode is bounded by a semaphore at min(declared, 4); sequential mode runs workers in order"
    - "Each child writes one owner-scoped subagent_runs row; cross-owner read returns empty"
  artifacts:
    - path: "backend/agents/execution_engine/fanout.py"
      provides: "Kernel run_fanout: the only spawn path (worker-select, reserve stub, spawn parallel/sequential, collect)"
      contains: "async def run_fanout"
    - path: "backend/agents/execution_engine/budget.py"
      provides: "BudgetManager/BudgetExceeded/BudgetSnapshot with module-constant defaults (reserve() is a stub seam in this plan)"
      contains: "class BudgetManager"
    - path: "backend/agents/capabilities/strategies/fanout_batch.py"
      provides: "Registered fanout_batch ExecutionStrategy: parse TaskSource -> worker requests -> run_fanout"
      contains: "fanout_batch"
    - path: "backend/app/agents/tools/runner_tools.py"
      provides: "spawn_subagents concrete @tool request-emitter (returns structured-request string, spawns nothing)"
      contains: "spawn_subagents"
    - path: "backend/alembic/versions/0019_subagent_runs.py"
      provides: "Additive subagent_runs migration (0018 recipe, reversible offline)"
      contains: "subagent_runs"
  key_links:
    - from: "backend/agents/capabilities/strategies/fanout_batch.py"
      to: "ctx.runner.run_fanout"
      via: "handle method call (no fanout.py import)"
      pattern: "runner\\.run_fanout"
    - from: "backend/agents/execution_engine/engine.py"
      to: "run_fanout"
      via: "tool-result derivation of spawn_subagents request"
      pattern: "spawn_subagents"
    - from: "backend/agents/authz.py"
      to: "subagent_runs"
      via: "ScopedStore.record_subagent_run default-deny writer"
      pattern: "record_subagent_run"
---

<objective>
Land the fan-out foundation: the single kernel `run_fanout` spawn path, both entry points (declarative `fanout_batch` strategy + runtime `spawn_subagents` request-emitter tool), worker selection (self×N / named via `allowed_workers`), parallel/sequential modes with the engine-enforced concurrency cap, and the additive `0019 subagent_runs` persistence (row-per-child + owner-scoped ScopedStore reads). `BudgetManager.reserve()` lands as a no-op-returning seam (call-site present in `run_fanout`); enforcement lands in 11-04 (RESEARCH Open Question 2). Isolation in this plan uses the existing `shared_read`/per-run allocation; `sub_sandbox`/`worktree` land in 11-02. Merge in this plan is the trivial single-fragment/no-op pass-through; the `MergeStrategy` registry lands in 11-03. Typed lineage-tracked fragment-artifact persistence (`write_fragment_artifact`) is NOT in this plan — it lands in 11-03; the structured summary this plan produces carries per-worker STATUS only (no typed artifact refs).

Purpose: Make `Step.fanout`, `allowed_workers`, `ToolPermissions.spawn_subagents`, and `subagent_runs` LIVE behind ONE consumer each (INV-12), proving FANOUT-01/02/03/04/10 against scripted-model workers — the spine every later 11-* plan extends.
Output: `fanout.py`, `budget.py` (stub-reserve), `fanout_batch` strategy, `spawn_subagents` tool (capability + concrete), `0019` migration + `SubagentRun` ORM + ScopedStore writer/reader + `KernelServices.record_subagent_run`/`run_fanout`/`run_worker`, compiler materialization of `FanoutSpec`/`allowed_workers`, registry growth, and the new test suites.
</objective>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift verification):
- `agents/execution_engine/fanout.py` — `run_fanout(requests, ctx, *, step)` (async); module helpers `_select_workers`, `_spawn_parallel`, `_spawn_sequential`
- `agents/execution_engine/budget.py` — `BudgetManager` (class), `BudgetExceeded` (exception), `BudgetSnapshot` (dataclass); module constants `DEFAULT_MAX_SUBAGENTS=8`, `DEFAULT_MAX_CONCURRENCY=4`, `DEFAULT_MAX_DEPTH=2`, `DEFAULT_WALL_CLOCK_SECONDS=900`, `MERGE_AGENT_MAX_ATTEMPTS=2`, `BUDGET_WARN_THRESHOLD=0.8`
- `agents/capabilities/strategies/fanout_batch.py` — `FanoutBatchStrategy` (registered `@register("strategy","fanout_batch", user_allowed=True)`)
- `agents/capabilities/tools/providers.py` — `SpawnSubagentsToolProvider` (registered `@register("tool","spawn_subagents", user_allowed=False)`)
- `app/agents/tools/runner_tools.py` — `spawn_subagents` (`@tool` request-emitter); module constant `TOOL_SPAWN_SUBAGENTS = "spawn_subagents"`
- `agents/execution_engine/kernel_services.py` — `KernelServices.run_fanout`, `KernelServices.run_worker`, `KernelServices.record_subagent_run` (methods)
- `agents/execution_engine/context.py` — `ExecutionContext.budget` field (BudgetManager ref; depth field already exists)
- `agents/workflows/plan.py` — `FanoutSpec.agent`, `FanoutSpec.count`, `FanoutSpec.workers` fields (additive); `CompiledWorkflow.allowed_workers` field
- `agents/workflows/compiler.py` — `_compile_fanout` helper; `allowed_workers` materialization
- `app/models/subagent_run.py` — `SubagentRun` (ORM)
- `alembic/versions/0019_subagent_runs.py` — migration `revision="0019"`, table `subagent_runs`, index `ix_subagent_runs_parent`
- `agents/authz.py` — `ScopedStore.record_subagent_run`, `ScopedStore.read_subagent_runs`, `ScopedStore.update_subagent_run`
- New events: `subagent_spawned`, `subagent_result` (string `type` values)
</artifacts_this_phase_produces>

<scope_note>
This plan modifies 20 files (5 of them test files). Task 1 carries the highest context density (migration + ORM + ScopedStore + budget.py + context.py + plan.py + compiler.py + ledger). It is sized to fit, but if execution degrades past ~40% context, split Task 1 along its natural seam — each half already has an independent verify command:
- Split-A (persistence backbone): `0019` migration + `SubagentRun` ORM + `ScopedStore` writer/reader/updater + migration-ledger sync → `pytest tests/agents/test_subagent_runs.py tests/agents/test_migration_ledger.py`
- Split-B (model layer): `budget.py` stub seam + `context.py` budget field + `FanoutSpec`/`allowed_workers` + `compiler._compile_fanout` → `pytest tests/agents/test_subagent_runs.py` (the FanoutSpec/compiler assertions) plus the compiler smoke test
Do NOT pre-split; only split if the executor reports context pressure mid-task.
</scope_note>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-SPEC.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-CONTEXT.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-RESEARCH.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 + persistence backbone (0019 subagent_runs, ORM, ScopedStore, budget seam, FanoutSpec/allowed_workers)</name>
  <files>backend/alembic/versions/0019_subagent_runs.py, backend/app/models/subagent_run.py, backend/agents/authz.py, backend/agents/execution_engine/budget.py, backend/agents/execution_engine/context.py, backend/agents/workflows/plan.py, backend/agents/workflows/compiler.py, specs/003-workflow-engine-decoupling/migration-ledger.md, backend/tests/agents/test_subagent_runs.py, backend/tests/agents/_scripted_model.py</files>
  <read_first>
    - backend/alembic/versions/0018_exec_runs.py (the EXACT migration recipe to clone: header revision/down_revision, free-String status, named FK, reversible)
    - backend/app/models/exec_runs.py (ExecRun ORM shape to mirror for SubagentRun)
    - backend/agents/authz.py (ScopedStore.record_exec_run / read_exec_runs — clone for subagent; _acquire, _scope_owner_ws, owner_id/workspace_id stamping)
    - backend/agents/workflows/plan.py lines 195-238 (FanoutSpec + Limits current INERT shape) and the CompiledWorkflow dataclass definition
    - backend/agents/workflows/compiler.py lines 63-90 (_ALLOWED_STEP_KEYS), 218-300 (_compile_step), 190-217 (_check_trust)
    - backend/agents/execution_engine/context.py lines 150-175 (depth:168, cancel_event:157, runner/scoped_store/workspace_id fields — where budget ref rides)
    - backend/tests/agents/_scripted_model.py (the scripted BaseChatModel helper to extend for parallel-worker fixtures)
    - backend/tests/agents/test_migration_ledger.py (the _REQUIRED_ITEMS / expected ratchet that must be synced in lockstep)
  </read_first>
  <action>
Create the `0019_subagent_runs` migration cloning `0018_exec_runs.py` EXACTLY: `revision="0019"`, `down_revision="0018"`, `branch_labels=None`, `depends_on=None`; module docstring states ADDITIVE ONLY (Q3/INV-3) + free-String status (NO sa.Enum) + offline-reversible. Create table `subagent_runs` with columns: `id` String not-null (PK), `parent_run_id` String not-null, `owner_id` String not-null, `workspace_id` String not-null (AUTHZ-01 — every new table carries both), `parent_step` String not-null, `worker_agent` String not-null, `depth` Integer not-null, `isolation` String not-null (free String: shared_read|sub_sandbox|worktree), `status` String not-null (free String: running|complete|failed|cancelled — NO sa.Enum), `tokens` Integer nullable, `cost` JSON nullable (cost_class-weighted; € dormant), `created_at` DateTime not-null; `ForeignKeyConstraint(["parent_run_id"], ["workflow_runs.id"])` (named, matching the ORM so no alembic-check drift per the 0018/RUNTIME-02 precedent); `op.create_index("ix_subagent_runs_parent", "subagent_runs", ["parent_run_id"])`. `downgrade()` drops the index then the table.

Create `app/models/subagent_run.py::SubagentRun` mirroring `ExecRun`: register on `Base.metadata`, columns matching the migration (incl. the named FK to `workflow_runs.id`). Add `subagent_runs` to `specs/003-workflow-engine-decoupling/migration-ledger.md` as a tracked row and sync `test_migration_ledger.py`'s `_REQUIRED_ITEMS`/`expected` in lockstep so the 0018->0019 single-head chain is asserted.

Add to `agents/authz.py::ScopedStore` (clone `record_exec_run`/`read_exec_runs` at lines ~843-905): `record_subagent_run(self, parent_run_id, *, parent_step, worker_agent, depth, isolation, status, tokens=None, cost=None) -> str` (local `from app.models.subagent_run import SubagentRun`; `session, owned = self._acquire()`; build row stamping `owner_id=self._owner_id, workspace_id=self._workspace_id`; `session.add(row); session.commit(); return row.id`; `finally: if owned: session.close()`), `update_subagent_run(self, row_id, *, status, tokens=None, cost=None) -> None` (default-deny scoped lookup then field update + commit), and `read_subagent_runs(self, parent_run_id) -> list` (build query, apply `self._scope_owner_ws(query, SubagentRun)` so cross-owner read returns ∅ — the FANOUT-10 mitigation).

Create `agents/execution_engine/budget.py` with module constants `DEFAULT_MAX_SUBAGENTS=8`, `DEFAULT_MAX_CONCURRENCY=4`, `DEFAULT_MAX_DEPTH=2`, `DEFAULT_WALL_CLOCK_SECONDS=900`, `MERGE_AGENT_MAX_ATTEMPTS=2`, `BUDGET_WARN_THRESHOLD=0.8`; `BudgetExceeded(Exception)`; `@dataclass BudgetSnapshot` (fields tokens:int=0, cost:dict|None=None, subagents:int=0, depth:int=0, wall_clock_seconds:float=0.0); `class BudgetManager` constructed from `Limits | None` + defaults, holding the resolved per-run caps. In THIS plan `reserve(self, *, tokens=0, subagents=0, concurrency=0, depth=0) -> None` is a STUB SEAM that records the requested units and returns without raising (a docstring states enforcement lands 11-04 / FANOUT-09); `spent(self) -> BudgetSnapshot` returns the accumulated counts. Add `ExecutionContext.budget: "BudgetManager | None" = None` (typed forward-ref; per-run object, INV-2 — never on the engine singleton) next to the existing `depth`/`cancel_event` fields in `context.py`.

Grow `FanoutSpec` in `plan.py` ADDITIVELY (keep `mode`/`max_parallel`): add `agent: str | None = None` (worker agent; `None`/"self" = the step's own agent ×count), `count: int | None = None` (self×N), `workers: list[str] = field(default_factory=list)` (named worker ids for heterogeneous fan-out). Add `allowed_workers: list[str] = field(default_factory=list)` to `CompiledWorkflow` (pure data, INV-5). In `compiler.py` add `_compile_fanout(raw_fanout: dict) -> FanoutSpec` materializing the FanoutSpec from the step dict's `fanout` key (it is already in `_ALLOWED_STEP_KEYS` but never constructed — close that additive gap); wire it into `_compile_step` so `Step.fanout` is populated. Materialize workflow-level `allowed_workers` from the manifest top-level onto `CompiledWorkflow.allowed_workers`. Unknown step keys still rejected via `set(raw) - _ALLOWED_STEP_KEYS` (INV-5; no control flow). Write `test_subagent_runs.py` proving: 0019 upgrade->downgrade->upgrade reversible against in-memory SQLite (0018 precedent); a row written via ScopedStore round-trips with depth/isolation/status/tokens; a cross-owner ScopedStore read returns ∅. Extend `_scripted_model.py` with a parallel-worker scripted-model helper (per backend/CLAUDE.md scripted-model recipe: minimal BaseChatModel `_stream` yielding AIMessageChunk + usage_metadata, no-op bind_tools) used by later tasks.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_subagent_runs.py tests/agents/test_migration_ledger.py -x -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - alembic/versions/0019_subagent_runs.py contains `revision = "0019"` and `down_revision = "0018"`
    - `subagent_runs` migration: `grep -c "sa.Enum" alembic/versions/0019_subagent_runs.py` returns 0 (free-String status)
    - 0019 reversibility test in test_subagent_runs.py passes (upgrade->downgrade->upgrade in-memory SQLite)
    - app/models/subagent_run.py contains `class SubagentRun` with owner_id and workspace_id columns
    - agents/authz.py contains `def record_subagent_run` and `def read_subagent_runs`
    - A cross-owner read_subagent_runs returns an empty list (asserted in test_subagent_runs.py)
    - agents/execution_engine/budget.py contains `DEFAULT_MAX_SUBAGENTS = 8`, `DEFAULT_MAX_CONCURRENCY = 4`, `DEFAULT_MAX_DEPTH = 2`, `DEFAULT_WALL_CLOCK_SECONDS = 900`, `class BudgetManager`, `class BudgetExceeded`
    - agents/execution_engine/context.py ExecutionContext has a `budget` field
    - agents/workflows/plan.py FanoutSpec has `count` and `workers` fields; CompiledWorkflow has `allowed_workers`
    - test_migration_ledger.py passes (0018->0019 single head, ledger row present)
  </acceptance_criteria>
  <done>0019 migration + SubagentRun ORM + ScopedStore writer/reader/updater land reversible and owner-scoped; budget.py stub seam + FanoutSpec/allowed_workers materialization compile; migration-ledger ratchet green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kernel run_fanout + handle methods + fanout_batch strategy + spawn_subagents tool</name>
  <files>backend/agents/execution_engine/fanout.py, backend/agents/execution_engine/kernel_services.py, backend/agents/capabilities/strategies/fanout_batch.py, backend/agents/capabilities/tools/providers.py, backend/app/agents/tools/runner_tools.py, backend/agents/capabilities/registry.py, backend/tests/agents/test_fanout.py, backend/tests/agents/test_fanout_tool.py, backend/tests/agents/test_registry_capabilities.py</files>
  <read_first>
    - backend/agents/capabilities/strategies/task_loop.py (the EXACT precedent fanout_batch mirrors: lines 34-37 import-purity docstring, 135-176 class/registry/handle access, 166-190 TaskSource sourcing + parser resolve)
    - backend/agents/execution_engine/kernel_services.py (run_agent ~568-637 the worker path; run_human_gate ~475-516; record_exec_run ~358-399 the None-degrade pattern; the per-run handle object shape)
    - backend/agents/capabilities/tools/providers.py (PrototypeToolProvider ~71-83 — the thin @register tool provider returning the string KEY; TOOL_REPORT_TASK_COMPLETE ~12-14)
    - backend/app/agents/tools/runner_tools.py (report_task_complete — the store-free string @tool; FANOUT-01 spawn-free pattern)
    - backend/agents/capabilities/registry.py lines 77-134 (_KNOWN set + @register + is_user_allowed)
    - backend/tests/agents/test_registry_capabilities.py lines 130-175 (the `len(_KNOWN) == 53` lockstep drift-guard + _EXPECTED_NAMES that must be bumped together)
    - backend/CLAUDE.md "Tool Sets" + per-worker thread_id convention (`{run_id}:{agent_id}` + `:task`)
  </read_first>
  <action>
Create `agents/execution_engine/fanout.py::run_fanout(requests, ctx, *, step)` — the ONLY spawn path (FANOUT-02), an async generator yielding lifecycle events. Sequence: (1) worker selection (FANOUT-03) — for each request, `agent="self"` (or `None`) resolves to `step.agent_id`; a named agent MUST appear in `ctx`'s compiled `allowed_workers` AND resolve in the agent registry (`agents.registry.get_agent_by_id` reachable via the runner handle, NOT a direct registry import that breaks layering — read run_agent's resolution path and reuse it); a disallowed or unknown worker raises a structured error (a `ValueError`/dedicated `FanoutError` naming the bad agent) BEFORE any spawn, leaving zero `subagent_runs` rows and no workspace allocation. (2) `ctx.budget.reserve(subagents=N, concurrency=concurrency, depth=ctx.depth)` is called FIRST, before any spawn (the call site exists now; enforcement is the 11-04 stub — Pitfall 4 enforcement-point discipline). (3) Isolation in this plan = the existing per-run/`shared_read` allocation (sub_sandbox/worktree are 11-02). (4) Spawn workers (FANOUT-04): parallel mode = `asyncio.gather` under `asyncio.Semaphore(min(declared_max_parallel or DEFAULT_MAX_CONCURRENCY, DEFAULT_MAX_CONCURRENCY))` (engine enforces the cap regardless of manifest); sequential mode runs ordered awaits. Each worker runs via `ctx.runner.run_worker(...)` with per-worker `thread_id = f"{run_id}:{step_id}:{worker_i}"` (extends the build-loop convention; add a depth segment when `ctx.depth > 0` per RESEARCH A4). Each child writes a `subagent_runs` row (status `running`) via `ctx.runner.record_subagent_run(...)` + emits a `subagent_spawned` event (worker id, agent, isolation scope, depth); on completion update the row terminal + emit `subagent_result` (status, summary digest). Child agent chunk events are NOT forwarded (D-03 — lifecycle-only). (5) Merge in THIS plan is the trivial pass-through (single-fragment / collect results; real MergeStrategy is 11-03). Return/yield a structured summary carrying per-worker STATUS only (completed/failed/never-spawned + the worker id/agent) — NOT typed artifact refs (typed lineage-tracked fragment artifacts via `write_fragment_artifact` are the 11-03 FANOUT-06 deliverable; this plan's summary is the status-only foundation it extends). INV-1: zero `if pipeline_type ==`/`spec.id ==` branches.

Add the handle methods to `KernelServices` (kernel_services.py): `run_fanout(self, requests, ctx, *, step)` (async generator that calls the kernel `run_fanout` coroutine — the kernel-private fanout lives reachable from the engine; the handle is the capability-facing seam); `run_worker(self, step, ctx, *, worker_index, thread_id, agent_id, input)` wrapping `run_agent` to run ONE worker against its allocated workspace with the per-worker thread_id, re-yielding `_run_agent` events (workers carry NO gates, NO per-worker fix-loop — D-01); `record_subagent_run(self, *, parent_step, worker_agent, depth, isolation, status, tokens=None, cost=None) -> str | None` cloning `record_exec_run` EXACTLY (`store = getattr(self._ectx, "scoped_store", None); if store is None: return None`; wrap delegate in `try/except Exception` -> `logger.warning(...); return None` — audit must NEVER abort the run).

Create `agents/capabilities/strategies/fanout_batch.py` mirroring `task_loop.py`: `@register("strategy", "fanout_batch", user_allowed=True)`; module-singleton `CapabilityRegistry()`; `async def run(self, step, ctx)` sourcing the task list EXACTLY like task_loop (`task_source` decl -> `source_step` -> `runner.latest_typed_content(source_step)` -> resolve the declared `heading_tasks` parser -> `tasks = parser.parse(...)`), then `requests = [{"agent": "self", "input": t.body} for t in tasks]` and `async for event in runner.run_fanout(requests, ctx, step=step): yield event`. Import-purity: NOTHING from `agents.execution_engine` or `app.*` — reach everything through `ctx.runner` (Pitfall 2).

Add `spawn_subagents` (FANOUT-01): the capability provider in `agents/capabilities/tools/providers.py` — `@register("tool", "spawn_subagents", user_allowed=False)` class `SpawnSubagentsToolProvider` whose `provide(spec, ctx)` returns the string KEY `(["spawn_subagents"], False)` (capability layer imports no concrete tool — the `PrototypeToolProvider` precedent). The concrete `@tool spawn_subagents(tasks, mode="parallel")` in `app/agents/tools/runner_tools.py` is a STORE-FREE / SPAWN-FREE request emitter: its body returns `json.dumps({"fanout_request": tasks, "mode": mode})` ONLY — NO `asyncio`, NO `run_fanout` import, NO kernel import (Pitfall 1 / FANOUT-01 grep). The engine derives the request from the tool call/result events (the `report_task_complete` -> `task_progress` post-loop derivation pattern, engine.py ~1800-1815) and fulfils it via the SAME `run_fanout` — the runtime-entry engine dispatch wiring + tool binding via the 08-03 tool_provider/intersection path is COMPLETED in Task 3 (this task creates the tool + provider + capability tests; Task 3 wires the engine dispatch). The deepagents library `task` tool stays excluded (factory `_ToolFilterMiddleware`).

Update `agents/capabilities/registry.py` `_KNOWN` adding `("strategy","fanout_batch")` and `("tool","spawn_subagents")` AND bump the lockstep count in `test_registry_capabilities.py` (`len(_KNOWN) == 53` -> the new total) plus `_EXPECTED_NAMES` in the SAME change (Pitfall 7 — two places). `spawn_subagents` is `user_allowed=False` (CAP-03); `fanout_batch` is `user_allowed=True`.

Write `test_fanout.py` (FANOUT-02/03/04) and `test_fanout_tool.py` (FANOUT-01): funnel — both a declarative `fanout_batch` step and a tool-emitted request reach `run_fanout` (grep/assert one spawn path); worker-select — self×N spawns N; a named worker in `allowed_workers` resolves; a disallowed/unknown worker raises with ZERO subagent_runs rows; modes — instrumented parallel run observes concurrent worker count ≤ cap (4); sequential observes strict ordering; tool — a step without `tools.spawn_subagents` has no `spawn_subagents` in bound tools; a granted step's tool call yields the structured request; `grep -v '^#' app/agents/tools/runner_tools.py | grep -c 'asyncio.gather'` style assertion that the tool module body contains no spawn/gather (FANOUT-01 spawn-free). The structured-summary assertions in this task check per-worker STATUS only (no artifact-ref fields — those arrive with 11-03).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_fanout.py tests/agents/test_fanout_tool.py tests/agents/test_registry_capabilities.py -x -p no:cacheprovider && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - agents/execution_engine/fanout.py contains `async def run_fanout`
    - agents/capabilities/strategies/fanout_batch.py contains `@register("strategy", "fanout_batch"` and `runner.run_fanout`
    - `grep -c "from agents.execution_engine\|from app\." agents/capabilities/strategies/fanout_batch.py` returns 0 (import-purity)
    - app/agents/tools/runner_tools.py contains a `spawn_subagents` tool; `grep -v '^#' app/agents/tools/runner_tools.py | grep -c "asyncio\|run_fanout"` returns 0 within the spawn_subagents body region (spawn-free)
    - agents/capabilities/tools/providers.py contains `@register("tool", "spawn_subagents", user_allowed=False)`
    - kernel_services.py contains `def run_fanout`, `def run_worker`, `def record_subagent_run`
    - test_registry_capabilities.py asserts the bumped `len(_KNOWN)` count and passes
    - A self×N fan-out test spawns exactly N workers; a disallowed-worker test produces zero subagent_runs rows
    - A parallel-mode instrumented test asserts max concurrent workers ≤ 4
    - The structured summary carries per-worker status only (no artifact-ref assertions — deferred to 11-03)
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken
  </acceptance_criteria>
  <done>spawn_subagents is permission-bound (`user_allowed=False`) and spawn-free; worker selection + modes + concurrency cap enforced; run_fanout created as the kernel spawn path; fanout_batch strategy + registry lockstep green; import-linter 4/0. The runtime (tool) engine dispatch wiring that makes run_fanout the single path reached by BOTH entry points completes in Task 3 (FANOUT-02 is claimed there).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Engine dispatch wiring (fanout path + tool-result derivation) + characterization parity</name>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/test_fanout.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py lines 1249-1350 (the dispatch loop: strategy resolve:1316, post_step:1345, cancel checks:1261, the single emit boundary append_event:124) and 1761-1815 (the report_task_complete -> task_progress tool-result derivation idiom to clone for spawn_subagents)
    - backend/agents/execution_engine/engine.py lines 1200-1226 (the exec-workspace gated provisioning — the conditional-only pattern so fanout stays DORMANT for existing workflows, Pitfall 3)
    - backend/tests/agents/test_characterization_prototype.py (one of the 5 snapshots — the INV-3 parity gate; do not re-baseline)
  </read_first>
  <action>
Wire the runtime entry point (B) into the engine dispatch loop WITHOUT perturbing existing workflows (Pitfall 3 — fanout is DORMANT for every workflow that declares no `fanout` and grants no `spawn_subagents`). In the `_run_agent` astream loop, mirror the `report_task_complete` -> `task_progress` derivation (engine.py ~1800-1815): when a `tool_result` event for `spawn_subagents` fires, parse the structured request (`json.loads` of the `{"fanout_request": ..., "mode": ...}` string), call the engine-private `run_fanout(requests, ctx, step=...)`, and surface the structured summary back as the tool result (so the agent loop continues). The derivation MUST be strictly conditional on the tool name `spawn_subagents` so non-fanout runs take the identical path as today (zero new events, zero reordering). The declarative path (A) already flows through `_registry.resolve("strategy", step.strategy).run(step, ctx)` at line 1316 — `fanout_batch` plugs in with NO new engine branch (the strategy registry IS the seam). All new events (`subagent_spawned`/`subagent_result`) ride the single emit boundary (append_event:124) + the generic websocket.py forward (zero edits — 08-08). INV-1: add zero `if pipeline_type ==`/`spec.id ==` branches inside `agents/execution_engine/`.

Add an end-to-end declarative test to `test_fanout.py` driving a scripted-model `fanout_batch` step (manifest-compiled CompiledWorkflow with `step.fanout` declared + `allowed_workers`) through the real engine dispatch loop, asserting N `subagent_runs` rows + N `subagent_spawned`/`subagent_result` events in the stream and a STATUS-ONLY merged structured summary (per-worker completed/failed status). Do NOT assert typed artifact refs in this summary — `write_fragment_artifact` and the typed lineage-tracked fragment-artifact refs are the 11-03 FANOUT-06 deliverable, so the end-to-end summary asserted here is scoped to per-worker status + counts; the artifact-ref assertions are added when 11-03 lands. Run the 5 characterization snapshots with `SNAPSHOT_UPDATE` UNSET to prove byte/event-identity (fanout dormant).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_fanout.py tests/agents/test_characterization_prototype.py tests/agents/test_banned_patterns.py -x -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - engine.py derives the fanout request from a `spawn_subagents` tool_result and calls run_fanout (grep: `spawn_subagents` appears in engine.py dispatch path)
    - Both entry points (declarative fanout_batch + runtime spawn_subagents) now funnel through the single run_fanout (FANOUT-02 — the funnel test asserts one spawn path reached by both)
    - `grep -c "if pipeline_type ==\|spec.id ==" agents/execution_engine/fanout.py agents/execution_engine/budget.py` returns 0 (INV-1)
    - A declarative fanout_batch end-to-end test produces N subagent_runs rows + N subagent_spawned + N subagent_result events and a status-only structured summary (no artifact-ref assertions — deferred to 11-03)
    - The 5 characterization snapshots pass byte/event-identical with SNAPSHOT_UPDATE unset (test_characterization_prototype.py green)
    - test_banned_patterns.py passes (no 2nd create_deep_agent; no kernel name-branch; deepagents task tool still excluded)
  </acceptance_criteria>
  <done>Both entry points funnel through run_fanout in the live engine (FANOUT-02 met — the runtime tool-result derivation + the declarative strategy seam reach the single spawn path); existing workflows stay byte/event-identical (fanout dormant); banned-pattern + INV-1 gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (LLM) -> spawn_subagents tool | The model emits a fan-out REQUEST (untrusted task list/mode); the kernel — not the tool — decides what spawns |
| manifest -> compiler | Declared `fanout`/`allowed_workers` are untrusted data; compiler materializes as pure data (INV-5), rejects unknown keys |
| run -> subagent_runs store | Child rows are owner/workspace-scoped; cross-owner reads must return ∅ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Elevation of Privilege | spawn_subagents tool grantable by user/db manifest | mitigate | `@register("tool","spawn_subagents", user_allowed=False)`; compiler CAP-03 rejects user/db manifests referencing it; deepagents `task` tool stays excluded — asserted in test_fanout_tool.py + registry lockstep |
| T-11-01-02 | Denial of Service | unbounded worker spawn via run_fanout | mitigate | concurrency capped at `min(declared, 4)` via `asyncio.Semaphore` in run_fanout (the only spawn path); subagent/depth caps land enforcing in 11-04, reserve() call-site present now |
| T-11-01-03 | Information Disclosure | cross-owner subagent_runs read | mitigate | `ScopedStore.read_subagent_runs` applies `_scope_owner_ws`; owner_id+workspace_id NOT NULL; cross-owner read = ∅ asserted in test_subagent_runs.py |
| T-11-01-04 | Tampering | spawn_subagents tool body performs the spawn (bypassing run_fanout reserve/audit) | mitigate | tool body is a store-free/spawn-free request emitter (returns a JSON string only); FANOUT-01 grep asserts no asyncio/run_fanout/kernel import in the tool module |
| T-11-01-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit = N/A); brownfield on the installed stack (deepagents 0.6.7, stdlib asyncio/subprocess, repo-pinned Alembic/SQLAlchemy) |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_fanout.py tests/agents/test_fanout_tool.py tests/agents/test_subagent_runs.py tests/agents/test_migration_ledger.py tests/agents/test_registry_capabilities.py tests/agents/test_banned_patterns.py tests/agents/test_characterization_*.py -x -p no:cacheprovider`
- `/opt/homebrew/bin/lint-imports` (4 contracts kept / 0 broken)
- 5 characterization snapshots byte/event-identical with `SNAPSHOT_UPDATE` unset
</verification>

<success_criteria>
- run_fanout is the single spawn path; declarative `fanout_batch` and runtime `spawn_subagents` both funnel through it (FANOUT-02)
- spawn_subagents is permission-bound (`user_allowed=False`) and spawn-free (FANOUT-01)
- worker selection (self×N / named via allowed_workers) with disallowed-worker pre-spawn rejection (FANOUT-03)
- parallel (≤ cap 4) / sequential modes (FANOUT-04)
- one owner-scoped subagent_runs row per child + subagent_spawned/subagent_result events; 0019 reversible offline; cross-owner read = ∅ (FANOUT-10)
- the structured summary is status-only this plan (typed lineage-tracked fragment artifacts via write_fragment_artifact are the 11-03 FANOUT-06 deliverable)
- 5 characterization snapshots byte/event-identical; banned-pattern + migration-ledger + import-linter 4/0 green
</success_criteria>

<output>
Create `.planning/phases/11-engine-owned-fan-out-merge-5/11-01-SUMMARY.md` when done
</output>
</output>
