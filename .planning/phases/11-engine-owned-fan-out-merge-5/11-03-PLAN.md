---
phase: 11-engine-owned-fan-out-merge-5
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - backend/agents/capabilities/merge/__init__.py
  - backend/agents/capabilities/merge/base.py
  - backend/agents/capabilities/merge/copy_disjoint.py
  - backend/agents/capabilities/merge/git_3way.py
  - backend/agents/capabilities/merge/json_merge.py
  - backend/agents/capabilities/merge/html_fragment.py
  - backend/agents/execution_engine/fanout.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/agents/capabilities/registry.py
  - backend/app/agents/runtime/local.py
  - backend/tests/agents/test_merge.py
  - backend/tests/agents/test_merge_conflict.py
  - backend/tests/agents/test_registry_capabilities.py
autonomous: true
requirements: [FANOUT-06, FANOUT-07, FANOUT-08]
must_haves:
  truths:
    - "A MergeStrategy port (name + merge(base, fragments) -> MergeResult) is registered as a capability kind with copy_disjoint, git_3way, json, html_fragment impls"
    - "copy_disjoint over disjoint file sets produces a byte-stable deterministic result; overlapping writes produce a reported conflict (never silently overwritten)"
    - "On conflict the engine writes an owner-scoped merge_conflict artifact + emits a merge_conflict event"
    - "All 4 on_conflict policies behave per spec: human_gate pauses/resumes via the one durable HITL; merge_agent bounded at 2 then falls back to human_gate; partial keeps non-conflicting fragments; abort fails the run"
    - "Each worker's output persists as a typed lineage-tracked fragment artifact BEFORE merge; the caller receives a structured per-worker summary"
  artifacts:
    - path: "backend/agents/capabilities/merge/base.py"
      provides: "MergeStrategy Protocol port + MergeResult (conflicts + applied)"
      contains: "class MergeStrategy"
    - path: "backend/agents/capabilities/merge/copy_disjoint.py"
      provides: "Deterministic disjoint-file merge; same-path differing-content -> conflict"
      contains: "copy_disjoint"
    - path: "backend/agents/execution_engine/fanout.py"
      provides: "Merge dispatch + merge_conflict artifact/event + 4 on_conflict policies"
      contains: "on_conflict"
  key_links:
    - from: "backend/agents/capabilities/merge/git_3way.py"
      to: "ctx.runner (workspace handle)"
      via: "git merge reached through the handle, never capability-side subprocess"
      pattern: "runner\\."
    - from: "backend/agents/execution_engine/fanout.py"
      to: "run_human_gate"
      via: "merge human_gate conflict policy delegates to the one durable HITL"
      pattern: "run_human_gate"
---

<objective>
Land the merge layer: the `MergeStrategy` port (`agents/capabilities/merge/base.py`) + 4 registered impls (`copy_disjoint`/`git_3way`/`json`/`html_fragment`), the merge-conflict flow (owner-scoped `merge_conflict` artifact + `merge_conflict` event), and the 4 `on_conflict` policies (`human_gate` default via the one durable HITL, bounded `merge_agent`, `partial`, `abort`). Merge base/target = the parent run's primary workspace; fragments = the child isolated workspaces (11-02). Per-worker outputs ALSO persist as typed lineage-tracked fragment artifacts BEFORE merge (FANOUT-06), so partial results survive abort/cancel. Replaces the 11-01 trivial pass-through merge with the real dispatch.

Purpose: FANOUT-06/07/08 — deterministic fragment integration with conflict-as-first-class (never silent overwrite), the foundation for any fan-out producing more than one fragment.
Output: the `merge/` capability package (port + 4 impls), the merge dispatch + conflict-artifact/event + `on_conflict` policy switch in `run_fanout`, the `git_3way` worktree-merge handle method on `LocalWorkspace`, and the `test_merge.py`/`test_merge_conflict.py` suites.
</objective>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift verification):
- `agents/capabilities/merge/base.py` — `MergeStrategy` (Protocol port), `MergeResult` (dataclass: `applied` paths, `conflicts` list)
- `agents/capabilities/merge/copy_disjoint.py` — `CopyDisjointMerge` (`@register("merge","copy_disjoint", user_allowed=True)`)
- `agents/capabilities/merge/git_3way.py` — `Git3WayMerge` (`@register("merge","git_3way", user_allowed=True)`)
- `agents/capabilities/merge/json_merge.py` — `JsonMerge` (`@register("merge","json", user_allowed=True)`)
- `agents/capabilities/merge/html_fragment.py` — `HtmlFragmentMerge` (`@register("merge","html_fragment", user_allowed=True)`)
- `agents/execution_engine/fanout.py` — `_merge_fragments`, `_resolve_conflict` (the on_conflict policy switch); merge_agent attempt counter
- `agents/execution_engine/kernel_services.py` — `KernelServices.git_3way_merge` handle method; `KernelServices.write_fragment_artifact` (typed lineage fragment persistence)
- `app/agents/runtime/local.py` — `LocalWorkspace.merge_worktree(branch, base_commit)` (3-way merge of a worker branch back to the base via the _git owner)
- New events: `merge_conflict`, `merge_started`, `merge_completed` (string `type` values); new ArtifactRef use of the already-allowed `merge_conflict` kind
</artifacts_this_phase_produces>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-SPEC.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-RESEARCH.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-PATTERNS.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MergeStrategy port + 4 registered impls (copy_disjoint deterministic, overlap->conflict)</name>
  <files>backend/agents/capabilities/merge/__init__.py, backend/agents/capabilities/merge/base.py, backend/agents/capabilities/merge/copy_disjoint.py, backend/agents/capabilities/merge/git_3way.py, backend/agents/capabilities/merge/json_merge.py, backend/agents/capabilities/merge/html_fragment.py, backend/agents/capabilities/registry.py, backend/app/agents/runtime/local.py, backend/agents/execution_engine/kernel_services.py, backend/tests/agents/test_merge.py, backend/tests/agents/test_registry_capabilities.py</files>
  <read_first>
    - backend/agents/capabilities/base.py lines 40-60 (the ExecutionStrategy @runtime_checkable Protocol idiom MergeStrategy mirrors; the one-method-Protocol shape)
    - backend/agents/capabilities/strategies/single_shot.py (the thin @register impl shape to mirror)
    - backend/agents/capabilities/strategies/task_loop.py lines 34-37 (the import-purity docstring — NO agents.execution_engine / app import)
    - backend/agents/capabilities/registry.py lines 77-134 (_KNOWN + @register + discover() _forward_packages — the new merge/ subpackage must be wired into discover)
    - backend/tests/agents/test_registry_capabilities.py lines 130-175 (the lockstep count + _EXPECTED_NAMES to bump by 4 merge pairs)
    - backend/app/agents/runtime/local.py (LocalWorkspace._git single git owner; the clone/branch/worktree pattern git_3way's merge handle method follows)
    - backend/agents/authz.py (ScopedStore.write_ref / ArtifactRef — the typed fragment-artifact + merge_conflict-kind persistence path)
  </read_first>
  <action>
Create the `agents/capabilities/merge/` package. `base.py`: a `@runtime_checkable class MergeStrategy(Protocol)` with `name: str` and `def merge(self, base, fragments) -> "MergeResult"` (mirrors `capabilities/base.py::ExecutionStrategy` idiom); a `@dataclass MergeResult` with `applied: list[str]` (relative paths merged into base) and `conflicts: list[dict]` (per-conflict: relative path + per-path worker provenance + truncated hunks/snippets — enough to be the human_gate payload, Phase-10 D-04 discipline). 4 impls, each a thin `@register("merge", <name>, user_allowed=True)` class (A2 — engine picks them, user never names them):
- `copy_disjoint.py`: pure-stdlib file ops. Conflict = two fragments (or a fragment vs base-since-spawn) touching the SAME relative path with DIFFERING content -> append to `conflicts`, NEVER overwrite (Pitfall 6). Determinism: iterate fragments in SORTED order so a disjoint set produces a byte-stable result.
- `git_3way.py`: reaches git ONLY via `ctx.runner` (a NEW `KernelServices.git_3way_merge` handle delegating to the Task-shared `LocalWorkspace.merge_worktree` — NO capability-side subprocess; Pitfall 2 / Phase-9 D-10). Conflict = a git merge conflict (parse the conflict markers/paths into `MergeResult.conflicts`).
- `json_merge.py`: pure-stdlib key-level merge of a target JSON document. Conflict = same key, differing values.
- `html_fragment.py`: section-level composition of disjoint HTML fragments. Registers to SATISFY FANOUT-07; NOTHING routes prototype through it (Q33 / MERGE-01 v2) — a docstring states this.

Wire the new `merge/` subpackage into `registry.py::discover()` `_forward_packages` so the `@register` side-effects bind. Add the 4 `("merge", name)` pairs to `_KNOWN` AND bump `test_registry_capabilities.py`'s lockstep count + `_EXPECTED_NAMES` in the SAME change (Pitfall 7). Add `LocalWorkspace.merge_worktree(self, branch, base_commit)` (3-way merge of a worker branch back to base off the captured spawn-point commit, via `_git`; returns conflict info) + `KernelServices.git_3way_merge` handle delegating to it.

Write `test_merge.py` (FANOUT-07): ≥1 test per strategy; `copy_disjoint` over disjoint file sets produces a byte-stable deterministic result (run twice, assert identical); `copy_disjoint` overlapping writes (same path, differing content) yields a reported conflict, NOT a silent overwrite; `json` same-key-differing-value -> conflict; `git_3way` (skip/xfail if git unavailable) disjoint -> clean, overlapping -> conflict; all 4 strategies resolve via the registry (`registry.is_registered("merge", name)` for each).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_merge.py tests/agents/test_registry_capabilities.py -x -p no:cacheprovider && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - agents/capabilities/merge/base.py contains `class MergeStrategy` and `class MergeResult`
    - All 4 impls registered: `grep -c '@register("merge"' agents/capabilities/merge/*.py` returns 4
    - `grep -c "from agents.execution_engine\|from app\." agents/capabilities/merge/*.py` returns 0 (import-purity)
    - `grep -c "subprocess\|git " agents/capabilities/merge/git_3way.py` returns 0 (git reached via the handle, never capability-side)
    - copy_disjoint determinism test: two runs over a disjoint set produce byte-identical results
    - copy_disjoint overlap test: same-path differing-content yields a reported conflict (no silent overwrite)
    - test_registry_capabilities.py asserts the lockstep count bumped by 4 and passes
    - `/opt/homebrew/bin/lint-imports` 4 kept / 0 broken
  </acceptance_criteria>
  <done>MergeStrategy port + 4 registered impls land; copy_disjoint is deterministic and conflict-detecting; git_3way reaches git only via the handle; registry lockstep + import-linter green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge dispatch + merge_conflict artifact/event + 4 on_conflict policies + fragment artifacts</name>
  <files>backend/agents/execution_engine/fanout.py, backend/agents/execution_engine/kernel_services.py, backend/tests/agents/test_merge_conflict.py</files>
  <read_first>
    - backend/agents/execution_engine/fanout.py (the 11-01/11-02 run_fanout: the trivial pass-through merge to REPLACE; the collect-results step; where reserve()/spawn/teardown live)
    - backend/agents/execution_engine/kernel_services.py (run_human_gate ~475-516 the ONE durable HITL delegate; record_subagent_run; the handle pattern; write_ref/ArtifactRef path for the merge_conflict artifact + fragment artifacts)
    - backend/agents/workflows/plan.py lines 311-340 (Step.on_conflict default "human_gate"; the FanoutSpec; the merge strategy name source)
    - .planning/phases/10-safe-local-exec-gated-on-n3-4b/10-CONTEXT.md (D-02 one-durable-HITL; D-04 payload-is-what-you-sign-off — the merge_conflict payload rides review_gate_ready.data.output)
  </read_first>
  <action>
Replace the 11-01 trivial pass-through in `run_fanout` with the real merge dispatch (`_merge_fragments`). Base/target = the parent run's primary workspace (the run sandbox, or the repo working branch for has_git runs); fragments = the child isolated workspaces (11-02). BEFORE merge, persist EACH worker's output as a typed lineage-tracked fragment `ArtifactRef` (producer step/agent, parent-run linkage) via a new `KernelServices.write_fragment_artifact` handle (FANOUT-06 — partial results survive abort/cancel). Resolve the merge strategy by name (the engine selects: `git_3way` for has_git/worktree fragments, `copy_disjoint` for sub_sandbox; deterministic default — NOT manifest-controlled isolation, but the merge strategy name MAY come from the step's declared merge or the engine default — keep it engine-decided per INV-7). Emit `merge_started` -> run `MergeStrategy.merge(base, fragments)` -> on clean result emit `merge_completed`; the step's deliverable/artifact flow continues from the merged base exactly as today (deliverable resolvers keep reading the run workspace).

On a `MergeResult` with conflicts: write an owner-scoped `merge_conflict` `ArtifactRef` (the already-allowed kind; payload = conflicting relative paths + per-path worker provenance + truncated hunks — NO raw secret leak, Phase-10 D-04) + emit a `merge_conflict` event, THEN resolve per the step's `on_conflict` (`_resolve_conflict`):
- `human_gate` (default): delegate to `ctx.runner.run_human_gate(...)` -> `_run_review_gate` (the ONE durable HITL — NO sibling run_merge_gate); pass the merge_conflict payload as the `payload` dict (rides the generic `review_gate_ready.data.output`, the 10-03 approval-snapshot precedent); pause/resume round-trips on user resolution.
- `merge_agent`: spawn the designated merge worker bounded at `MERGE_AGENT_MAX_ATTEMPTS=2` (from budget.py — mirrors `_MAX_FIX_ATTEMPTS`); after 2 unresolved attempts, fall back to `human_gate` (Pitfall 8 — no oscillation). The merge worker is a named worker from `allowed_workers` (or a convention agent id — RESEARCH Open Question 3, planner-decided: source it from the step's declared merge-agent worker, else fall back to human_gate when none is designated).
- `partial`: keep the non-conflicting fragments (apply `MergeResult.applied`), mark the conflicted work failed (update those `subagent_runs` rows), continue.
- `abort`: fail the run (raise, surfacing the conflict).

INV-1: zero workflow-name/id branches.

Write `test_merge_conflict.py` (FANOUT-08): 4 policy tests (one per `on_conflict` value); a conflict writes a `merge_conflict` ArtifactRef readable owner-scoped (cross-owner read = ∅); the `merge_conflict` event appears in the stream; `merge_agent` attempt count observed ≤ 2 with no oscillation then human_gate fallback; `human_gate` pause/resume round-trips via the durable HITL; `partial` keeps non-conflicting fragments + marks conflicted rows failed; `abort` fails the run.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_merge_conflict.py tests/agents/test_merge.py tests/agents/test_fanout.py tests/agents/test_characterization_prototype.py -x -p no:cacheprovider && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - fanout.py contains `_merge_fragments`, `_resolve_conflict`, and references `run_human_gate` for the human_gate policy (no run_merge_gate sibling: `grep -c run_merge_gate` returns 0)
    - kernel_services.py contains `def write_fragment_artifact`
    - A conflict writes an owner-scoped merge_conflict ArtifactRef + emits a merge_conflict event (asserted)
    - 4 on_conflict policy tests pass; merge_agent attempt count ≤ 2 then human_gate fallback (no oscillation)
    - human_gate pause/resume round-trips; partial keeps non-conflicting fragments; abort fails the run
    - Per-worker fragment artifacts persist BEFORE merge (typed, lineage-tracked) — asserted queryable via the typed graph
    - 5 characterization snapshots byte/event-identical (fanout dormant); `/opt/homebrew/bin/lint-imports` 4/0
  </acceptance_criteria>
  <done>The merge dispatch integrates fragments deterministically; conflicts are first-class (artifact + event, never silent overwrite); all 4 on_conflict policies behave per spec; fragment artifacts persist before merge; one durable HITL preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge strategy -> base workspace | Fragments integrate into the run's primary workspace; overlap must be reported, never silently applied |
| merge_conflict artifact -> approver | The conflict payload is what a human adjudicates; it must not leak raw secrets |
| merge_agent worker -> run_fanout | A bounded retry worker; must not oscillate past 2 attempts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | merge silently overwrites instead of reporting a conflict | mitigate | copy_disjoint/json/git_3way report same-path differing-content as a conflict; overlap test asserts a reported conflict, never a silent overwrite (Pitfall 6) |
| T-11-03-02 | Information Disclosure | secret leakage in merge_conflict payload | mitigate | payload carries truncated hunks/snippets only (Phase-10 D-04 discipline); worker policy is exec/network/secrets OFF; cross-owner read of the merge_conflict ArtifactRef = ∅ (ScopedStore) |
| T-11-03-03 | Denial of Service | merge_agent unbounded retry oscillation | mitigate | bounded at MERGE_AGENT_MAX_ATTEMPTS=2 then human_gate fallback; attempt-count test asserts ≤ 2 with no oscillation (Pitfall 8) |
| T-11-03-04 | EoP | a second HITL surface for merge conflicts | mitigate | human_gate delegates to the ONE durable run_human_gate->_run_review_gate; grep run_merge_gate = 0 (Phase-10 D-02) |
| T-11-03-05 | Tampering | git_3way shells git from the capability layer | mitigate | git_3way reaches git ONLY via the ctx.runner handle (LocalWorkspace.merge_worktree); grep subprocess/git in merge/ = 0 (Phase-9 D-10, import-linter) |
| T-11-03-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (RESEARCH audit N/A); pure-stdlib merges + system git via handle |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_merge.py tests/agents/test_merge_conflict.py tests/agents/test_fanout.py tests/agents/test_registry_capabilities.py tests/agents/test_characterization_*.py tests/agents/test_banned_patterns.py -x -p no:cacheprovider`
- `/opt/homebrew/bin/lint-imports` (4 kept / 0 broken)
- 5 characterization snapshots byte/event-identical
</verification>

<success_criteria>
- MergeStrategy port + 4 registered impls; copy_disjoint deterministic, overlap->conflict (FANOUT-07)
- merge_conflict artifact + event; all 4 on_conflict policies per spec (human_gate via the one HITL, merge_agent ≤ 2, partial, abort) (FANOUT-08)
- per-worker fragment artifacts typed/lineage-tracked, persisted before merge + structured summary (FANOUT-06)
- import-linter 4/0; characterization snapshots byte/event-identical
</success_criteria>

<output>
Create `.planning/phases/11-engine-owned-fan-out-merge-5/11-03-SUMMARY.md` when done
</output>
