---
phase: 11-engine-owned-fan-out-merge-5
plan: 05
type: execute
wave: 5
depends_on: ["11-03", "11-04"]
files_modified:
  - backend/agents/execution_engine/fanout.py
  - backend/agents/execution_engine/engine.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/app/agents/runtime/local.py
  - backend/agents/workflows/sample_fanout/workflow.yaml
  - backend/agents/prompts/sample-fanout-worker/AGENT.md
  - backend/tests/agents/fixtures/sc001_fanout/
  - backend/tests/agents/test_fanout_cancel.py
  - backend/tests/agents/test_sc001_fanout.py
autonomous: true
requirements: [FANOUT-11, RESUME-01]
must_haves:
  truths:
    - "Cancelling a run mid-fan-out cancels all in-flight children, prevents pending spawns, and marks each child's subagent_runs row cancelled"
    - "cancel_event is checked before a fan-out wave, between sequential workers, and before merge"
    - "On cancel, every allocated isolated workspace is torn down (worktree remove + branch delete / sub_sandbox rmtree) — no leaked workspace dirs remain"
    - "Completed fragments' artifacts are preserved on cancel; pipeline_cancelled is emitted"
    - "A test-scoped sample fan-out workflow (manifest + AGENT.md only) runs end-to-end with zero engine edits authored for it (SC-001)"
  artifacts:
    - path: "backend/agents/execution_engine/fanout.py"
      provides: "cancel checks at fanout boundaries + finally-teardown of every allocated isolated workspace"
      contains: "cancel_event"
    - path: "backend/agents/workflows/sample_fanout/workflow.yaml"
      provides: "Test-scoped fanout manifest (fanout_batch fanning self-copies, copy_disjoint merge) proving SC-001"
      contains: "fanout"
    - path: "backend/tests/agents/test_sc001_fanout.py"
      provides: "End-to-end zero-engine-edit fan-out proof"
      contains: "def test"
  key_links:
    - from: "backend/agents/execution_engine/fanout.py"
      to: "teardown"
      via: "finally/cancel handler tears down every allocated isolated workspace"
      pattern: "teardown"
    - from: "backend/agents/workflows/sample_fanout/workflow.yaml"
      to: "fanout_batch + copy_disjoint"
      via: "registered capabilities only — zero engine edit"
      pattern: "fanout_batch"
---

<objective>
Close the phase: cancellation propagation to children (FANOUT-11) + cooperative cancel at fan-out boundaries with teardown (RESUME-01), the `subagent_runs` lifecycle completion (terminal `cancelled` status), and the SC-001 proof — a test-scoped sample fan-out workflow (manifest + AGENT.md only) that runs end-to-end with ZERO engine edits authored for it. `cancel_event` is checked before a fan-out wave, between sequential workers, and before merge; on cancel, in-flight worker `asyncio` tasks are cancelled, pending workers never spawn, each child's `subagent_runs` row is marked `cancelled`, EVERY allocated isolated workspace is torn down (worktree remove + branch delete from 11-02 / sub_sandbox rmtree) in a `finally`/cancel handler — including the cancel path (Pitfall 5), completed fragments' artifacts are preserved (persisted before merge by 11-03's `write_fragment_artifact`), and `pipeline_cancelled` is emitted.

Purpose: FANOUT-11 + RESUME-01 — no leaked worktrees/branches/dirs after cancel (resource-leak DoS mitigation), plus the SC-001 core-value proof (a brand-new fanning workflow needs zero engine edits — the kernel knows no workflow by name).
Output: the cancel-boundary checks + `finally`-teardown in `run_fanout`, the terminal `cancelled` row update, the test-scoped `sample_fanout` manifest + worker AGENT.md, and the `test_fanout_cancel.py`/`test_sc001_fanout.py` suites. This plan runs the FULL targeted offline gate as the phase exit.
</objective>

<artifacts_this_phase_produces>
New symbols this plan creates (exclude from drift verification):
- `agents/execution_engine/fanout.py` — `_check_cancel(ctx)` boundary helper; the `finally`/cancel teardown block iterating every allocated isolated workspace
- `agents/execution_engine/kernel_services.py` — `KernelServices.teardown_isolated_workspace(ws)` handle method (delegates to LocalWorkspace.remove_worktree / rmtree)
- `app/agents/runtime/local.py` — `LocalWorkspace.teardown_sub_sandbox(ws)` (rmtree the child dir) if not already covered by remove_worktree's sibling
- `agents/workflows/sample_fanout/workflow.yaml` — test-scoped fanout manifest (NEW workflow dir at the real manifest home — loaded by compile_for_run with zero engine edit, the sample_brownfield precedent)
- `agents/prompts/sample-fanout-worker/AGENT.md` — the worker agent spec for the sample fan-out (test-scoped)
- `tests/agents/fixtures/sc001_fanout/` — fixture assets for the SC-001 test
- New event use: `pipeline_cancelled` (already-emitted; now fired on fanout-boundary cancel)
</artifacts_this_phase_produces>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-SPEC.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-RESEARCH.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-PATTERNS.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-03-SUMMARY.md
@.planning/phases/11-engine-owned-fan-out-merge-5/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cancellation propagation + fanout-boundary checks + finally-teardown of every isolated workspace</name>
  <files>backend/agents/execution_engine/fanout.py, backend/agents/execution_engine/kernel_services.py, backend/app/agents/runtime/local.py, backend/tests/agents/test_fanout_cancel.py</files>
  <read_first>
    - backend/agents/execution_engine/fanout.py (the full run_fanout from 11-01..11-04: the spawn loop, the per-fanout allocated-workspace list from 11-02, the merge step from 11-03, the reserve from 11-04 — where cancel checks + finally-teardown slot in)
    - backend/agents/execution_engine/engine.py lines 1261, 1786 (the cancel_event idiom: `if cancel_event and cancel_event.is_set(): raise asyncio.CancelledError` — checked per-agent-step + per-chunk; the same check at fanout boundaries) and the pipeline_cancelled emission path
    - backend/app/agents/runtime/local.py (the 11-02 allocate_sub_sandbox/allocate_worktree/remove_worktree + the original teardown ~434 — teardown must remove worktree+branch, not just rmtree, Pitfall 5)
    - backend/agents/execution_engine/kernel_services.py (the 11-02 allocate_isolated_workspace handle; record_subagent_run/update for the cancelled terminal status)
  </read_first>
  <action>
Add cooperative cancel checks at the fan-out boundaries in `run_fanout` (RESUME-01): `_check_cancel(ctx)` (reading `ctx.cancel_event`, raising `asyncio.CancelledError` when set) is called BEFORE a fan-out wave (before any allocate/spawn), BETWEEN sequential workers (in the sequential loop), and BEFORE merge. For parallel mode, propagate cancellation to in-flight worker `asyncio` tasks — cancel the gathered worker tasks when the cancel_event fires (the `asyncio.gather` tasks are cancelled; pending workers never start). On cancel: each in-flight/spawned child's `subagent_runs` row is updated to terminal status `cancelled` (via the 11-01 `ScopedStore.update_subagent_run`); completed fragments' artifacts (persisted before merge by 11-03's write_fragment_artifact) are PRESERVED; `pipeline_cancelled` is emitted at the single boundary.

Teardown (Pitfall 5 / RESUME-01): wrap the fan-out body so a `finally`/cancel handler runs `teardown()` for EVERY allocated isolated workspace on the per-fanout list — `worktree` -> `LocalWorkspace.remove_worktree` (`git worktree remove` + branch delete, NOT just rmtree); `sub_sandbox` -> rmtree the child dir. Add `KernelServices.teardown_isolated_workspace(ws)` delegating to the LocalWorkspace owner. The teardown MUST run on the happy path AND the cancel path AND the BudgetExceeded path (the `finally` covers all three) — acceptance asserts zero isolated workspace dirs / zero orphan `git worktree list` entries remain after cancel. Ensure `LocalWorkspace.teardown` for a worktree-backed workspace removes the worktree + branch (extend the 11-02 remove_worktree to be the teardown path).

Write `test_fanout_cancel.py` (FANOUT-11 + RESUME-01): cancel mid-parallel-fan-out — in-flight workers stop, pending never start, every child's row reads `cancelled`, `teardown()` is called for every allocated isolated workspace (assert via a spy or by checking the dirs/`git worktree list` are clean), completed fragments' artifacts retained; cancel between sequential workers stops the next spawn; cancel before merge skips merge and marks the run cancelled; `pipeline_cancelled` emitted; zero isolated workspace dirs remain after cancel (the zero-residue acceptance).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_fanout_cancel.py tests/agents/test_isolation.py tests/agents/test_characterization_prototype.py -x -p no:cacheprovider && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - fanout.py checks cancel_event before the wave, between sequential workers, and before merge (grep: `cancel_event`/`_check_cancel` at >= 3 sites)
    - fanout.py has a `finally`/cancel handler that tears down every allocated isolated workspace (grep: `finally` + `teardown`)
    - kernel_services.py contains `def teardown_isolated_workspace`
    - Cancel-mid-parallel test: in-flight stop, pending never spawn, every child row reads `cancelled`, completed fragment artifacts retained
    - Zero-residue test: no isolated workspace dirs and zero orphan `git worktree list` entries remain after cancel
    - pipeline_cancelled emitted on a fanout-boundary cancel
    - 5 characterization snapshots byte/event-identical; `/opt/homebrew/bin/lint-imports` 4/0
  </acceptance_criteria>
  <done>Cancellation propagates to children at every fan-out boundary; rows marked cancelled; every isolated workspace torn down (no leaks); partial artifacts preserved; pipeline_cancelled emitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC-001 sample fan-out workflow (manifest + AGENT.md only) + phase exit gate</name>
  <files>backend/agents/workflows/sample_fanout/workflow.yaml, backend/agents/prompts/sample-fanout-worker/AGENT.md, backend/tests/agents/fixtures/sc001_fanout/, backend/tests/agents/test_sc001_fanout.py</files>
  <read_first>
    - backend/agents/workflows/sample_brownfield/workflow.yaml (the real-manifest-home precedent: a workflow dir under agents/workflows/ that compile_for_run loads with zero engine edit — D-08)
    - backend/tests/agents/test_sc001_nonprototype_task_loop.py (the SC-001 zero-engine-edit proof structure to MIRROR EXACTLY: how it asserts the deliverable AND how it proves zero engine edits were authored for THIS workflow — the 07-11 SC-001 proof pattern)
    - backend/tests/agents/fixtures/sc001_task_loop/ (the fixture-asset layout for an SC-001 test)
    - backend/agents/prompts/ (the AGENT.md frontmatter schema for a test-scoped worker — id/name/role/pipeline_type/order/max_tokens/tools, kebab-case folder == id)
    - backend/agents/workflows/compiler.py (compile_for_run path — how a manifest dir is loaded + the allowed_workers/fanout materialization from 11-01 the manifest exercises)
  </read_first>
  <action>
Author the test-scoped SC-001 proof (D-08, the sc001/sample_brownfield precedent — manifest + AGENT.md only, using ONLY registered capabilities, ZERO engine edits authored for this workflow). Create `agents/workflows/sample_fanout/workflow.yaml` at the REAL manifest home (so `compile_for_run` loads it with zero engine edit, the 09-04 sample_brownfield precedent): a workflow declaring a `fanout_batch` step that fans 3 self-copies (each producing a distinct file, e.g. `part_1.txt`/`part_2.txt`/`part_3.txt`), `mode: parallel`, merged `copy_disjoint`; declare `allowed_workers` and the worker agent; `tools.spawn_subagents` granted at the step (file manifest = trusted, so the privileged grant is allowed). Create the worker `agents/prompts/sample-fanout-worker/AGENT.md` (test-scoped worker spec — minimal, writes its assigned file). The fixture assets (scripted-model inputs) live under `tests/agents/fixtures/sc001_fanout/`.

Write `test_sc001_fanout.py` MIRRORING `test_sc001_nonprototype_task_loop.py` (the 07-11 SC-001 proof pattern — NOT a phase-start file-diff): drive the `sample_fanout` workflow end-to-end through the real engine with a scripted model, asserting — 3 workers spawn (3 `subagent_runs` rows); 3 distinct files produced in the merged base; `copy_disjoint` merged them deterministically (no conflict); the structured summary names every worker's status + artifacts. The SC-001 ACCEPTANCE is a CONCEPTUAL zero-engine-edit proof, exactly as test_sc001_nonprototype_task_loop.py proves it: assert that authoring THIS specific workflow's behavior required ONLY its manifest + AGENT.md + fixtures and no engine file was created to accommodate `sample_fanout` specifically. Make it a runnable assertion the way the task_loop SC-001 test does: (a) the workflow runs purely off registered capabilities (`fanout_batch`/`copy_disjoint`/`spawn_subagents` already in the registry — assert each `registry.is_registered(...)`), and (b) `grep -rc "sample_fanout" backend/agents/execution_engine/` returns 0 (the kernel contains NO reference to this workflow by name — INV-1; the engine infrastructure from 11-01..11-04 is workflow-agnostic, it does not name `sample_fanout`). Do NOT phrase the acceptance as a phase-start diff of engine files (11-01..11-04 legitimately modified engine files for the agnostic infrastructure); the proof is that the kernel knows no workflow by name and THIS workflow needed zero engine edit.

Run the FULL targeted offline gate as the phase exit: characterization (5 snapshots byte/event-identical, SNAPSHOT_UPDATE unset) + banned-patterns (INV-13 no 2nd create_deep_agent; INV-1 no kernel name-branch; spawn-free tool) + migration-ledger (0018->0019 single head) + compiler-trust + all the new fanout/merge/budget/isolation/cancel suites + `/opt/homebrew/bin/lint-imports` (4 kept / 0 broken).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_sc001_fanout.py tests/agents/test_fanout.py tests/agents/test_fanout_tool.py tests/agents/test_isolation.py tests/agents/test_merge.py tests/agents/test_merge_conflict.py tests/agents/test_budget.py tests/agents/test_subagent_runs.py tests/agents/test_fanout_cancel.py tests/agents/test_characterization_prototype.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py tests/agents/test_compiler_trust.py tests/agents/test_registry_capabilities.py -x -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - agents/workflows/sample_fanout/workflow.yaml exists and declares a `fanout_batch` step with `copy_disjoint` merge + `allowed_workers`
    - agents/prompts/sample-fanout-worker/AGENT.md exists (the test-scoped worker spec)
    - test_sc001_fanout.py drives the workflow end-to-end: 3 subagent_runs rows, 3 distinct files produced + copy_disjoint merged deterministically (no conflict)
    - The SC-001 proof mirrors test_sc001_nonprototype_task_loop.py: the workflow runs purely off registered capabilities (each `registry.is_registered("strategy","fanout_batch")`/`("merge","copy_disjoint")`/`("tool","spawn_subagents")` asserted) AND `grep -rc "sample_fanout" backend/agents/execution_engine/` returns 0 (the kernel names no workflow — no engine file was created to accommodate sample_fanout specifically)
    - The structured summary names every worker's status + artifacts
    - 5 characterization snapshots byte/event-identical with SNAPSHOT_UPDATE unset
    - test_banned_patterns.py + test_migration_ledger.py + test_compiler_trust.py + test_registry_capabilities.py all green
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken
  </acceptance_criteria>
  <done>A brand-new test-scoped fan-out workflow (manifest + AGENT.md only) runs end-to-end with zero engine edits authored for it — SC-001 proven by the 07-11 pattern (registered-capabilities-only + kernel names no workflow); the full targeted offline gate is green as the phase exit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cancel_event -> in-flight children | Cancellation must propagate to all in-flight worker tasks and prevent pending spawns |
| run termination -> isolated workspaces | Every allocated isolated workspace must be torn down (incl. the cancel path) — no leaks |
| file manifest -> spawn_subagents grant | The sample_fanout file manifest is trusted, so the privileged grant is allowed (user/db would be rejected) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Resource leak (DoS) | orphaned worktrees/branches/sub_sandbox dirs after cancel | mitigate | `finally`/cancel handler tears down EVERY allocated isolated workspace (git worktree remove + branch delete / rmtree); zero-residue test asserts no leaked dirs or orphan `git worktree list` entries (Pitfall 5 / RESUME-01) |
| T-11-05-02 | Denial of Service | a cancelled run keeps spawning pending workers | mitigate | cancel_event checked before the wave, between sequential workers, and before merge; in-flight asyncio tasks cancelled; pending never spawn (FANOUT-11) |
| T-11-05-03 | EoP | user/db manifest granting spawn_subagents via the sample workflow precedent | mitigate | the sample_fanout manifest is FILE-trusted; the compiler CAP-03 rule (08-01) rejects a user/db manifest referencing the `user_allowed=False` spawn_subagents — unchanged this plan |
| T-11-05-04 | Integrity | a cancelled run loses completed work | mitigate | completed fragments' artifacts (persisted before merge in 11-03) are preserved on cancel; only conflicted/incomplete rows mark cancelled |
| T-11-05-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (RESEARCH audit N/A); the sample workflow uses only registered capabilities |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_sc001_fanout.py tests/agents/test_fanout_cancel.py tests/agents/test_fanout.py tests/agents/test_isolation.py tests/agents/test_merge.py tests/agents/test_merge_conflict.py tests/agents/test_budget.py tests/agents/test_subagent_runs.py tests/agents/test_characterization_*.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py tests/agents/test_compiler_trust.py tests/agents/test_registry_capabilities.py -x -p no:cacheprovider`
- `/opt/homebrew/bin/lint-imports` (4 kept / 0 broken)
- 5 characterization snapshots byte/event-identical with `SNAPSHOT_UPDATE` unset (PHASE EXIT GATE)
</verification>

<success_criteria>
- cancellation propagates to children; cancel checked at fanout boundaries; rows marked cancelled; pipeline_cancelled emitted (FANOUT-11)
- every allocated isolated workspace torn down on cancel — zero leaked dirs/worktrees; completed fragments preserved (RESUME-01)
- a test-scoped sample fan-out workflow runs end-to-end with ZERO engine edits authored for it (SC-001 held — registered-capabilities-only + kernel names no workflow, the 07-11 pattern)
- the full targeted offline gate green: characterization byte/event-identical, banned-pattern, migration-ledger, compiler-trust, registry lockstep, import-linter 4/0
</success_criteria>

<output>
Create `.planning/phases/11-engine-owned-fan-out-merge-5/11-05-SUMMARY.md` when done
</output>
