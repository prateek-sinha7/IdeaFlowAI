---
phase: 18-custom-workflow-ux-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/workflows/plan.py
  - backend/agents/workflows/compiler.py
  - backend/agents/execution_engine/engine.py
  - backend/tests/agents/characterization/_normalize.py
  - backend/agents/capabilities/deliverables/single_file.py
  - backend/agents/capabilities/deliverables/streamed_text.py
  - backend/agents/capabilities/deliverables/serialized_sandbox.py
  - backend/agents/capabilities/deliverables/ppt.py
  - backend/tests/unit/test_deliverable_mimetype.py
autonomous: true
requirements: [ISS-021]
must_haves:
  truths:
    - "pipeline_complete emits deliverable_mimetype + deliverable_filename for EVERY pipeline (clean and degraded)"
    - "An unset DeliverableSpec.mimetype falls back to a sensible per-resolver default (single_file infers from name extension, serialized_sandbox→application/zip, streamed_text→text/markdown, ppt→text/html) so existing manifests need zero edits"
    - "The 5 characterization event goldens stay byte-identical (the new keys are stripped via _VOLATILE_STRIP_KEYS)"
    - "lint-imports stays 4 kept / 0 broken; zero new tables/migrations"
  artifacts:
    - path: "backend/agents/workflows/plan.py"
      provides: "DeliverableSpec.mimetype optional field"
      contains: "mimetype"
    - path: "backend/agents/workflows/compiler.py"
      provides: "_compile_deliverable reads raw.get('mimetype')"
      contains: "mimetype"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "pipeline_complete emits deliverable_mimetype + deliverable_filename"
      contains: "deliverable_mimetype"
    - path: "backend/tests/agents/characterization/_normalize.py"
      provides: "_VOLATILE_STRIP_KEYS includes the two new keys"
      contains: "deliverable_mimetype"
    - path: "backend/tests/unit/test_deliverable_mimetype.py"
      provides: "Unit coverage for per-resolver default mimetype + emission"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "ectx.deliverable"
      via: "getattr(ectx.deliverable, 'mimetype'/'name')"
      pattern: "ectx\\.deliverable"
    - from: "backend/tests/agents/characterization/_normalize.py"
      to: "_VOLATILE_STRIP_KEYS"
      via: "frozenset membership"
      pattern: "deliverable_mimetype"
---

<objective>
ISS-021 backend half: give every workflow's deliverable a DECLARED, type-driven shape hint so the frontend can render any custom/unknown deliverable generically (no per-workflow FE code). Add an optional `mimetype` to `DeliverableSpec`, read it through the compiler, default it sensibly per deliverable-resolver, and emit `deliverable_mimetype` + `deliverable_filename` on the `pipeline_complete` event from the already-resolved `ectx.deliverable` — with ZERO new plumbing and ZERO parity drift.

Purpose: the manifest already DECLARES the deliverable shape (`DeliverableSpec.strategy`/`name`); surfacing a mimetype on the terminal event is the additive contract the FE renderer (18-03) dispatches on. This is the only INV-3-sensitive item in the phase.

Output: `DeliverableSpec.mimetype` (optional), compiler pass-through + per-resolver default, two additive `pipeline_complete` data keys, the `_VOLATILE_STRIP_KEYS` parity guard, and a unit test pinning defaults + emission.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md
@.planning/ISSUES-REGISTER.md
@.planning/IMPLEMENTATION-REGISTER.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan ADDS (additive only — no deletions, no migrations):
- `DeliverableSpec.mimetype: str | None = None` (plan.py)
- compiler `_compile_deliverable` reads `raw.get("mimetype")`
- per-resolver default mimetype on the four deliverable resolvers
- `deliverable_mimetype` + `deliverable_filename` keys on the `pipeline_complete` data dict
- both new keys added to `_VOLATILE_STRIP_KEYS` (the INV-3 parity seam)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare DeliverableSpec.mimetype + compiler pass-through + per-resolver defaults</name>
  <files>backend/agents/workflows/plan.py, backend/agents/workflows/compiler.py, backend/agents/capabilities/deliverables/single_file.py, backend/agents/capabilities/deliverables/streamed_text.py, backend/agents/capabilities/deliverables/serialized_sandbox.py, backend/agents/capabilities/deliverables/ppt.py, backend/tests/unit/test_deliverable_mimetype.py</files>
  <read_first>
    - .planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md (the SPEC — ISS-021 BE decision 1; the per-resolver default list)
    - backend/agents/workflows/plan.py:265-286 (the DeliverableSpec dataclass — the edit target)
    - backend/agents/workflows/compiler.py:659-684 (_compile_deliverable — the edit target; mirror the existing raw.get("name")/raw.get("revises_existing") pattern)
    - backend/agents/capabilities/deliverables/single_file.py (resolver class; default = infer mimetype from deliverable.name extension)
    - backend/agents/capabilities/deliverables/streamed_text.py (default text/markdown)
    - backend/agents/capabilities/deliverables/serialized_sandbox.py (default application/zip)
    - backend/agents/capabilities/deliverables/ppt.py (default text/html)
    - backend/agents/capabilities/base.py:65-70 (DeliverableResolver Protocol — the resolve() signature; the default-mimetype hint must NOT break the Protocol)
  </read_first>
  <behavior>
    - Test: a DeliverableSpec with mimetype="text/html" round-trips through the compiler unchanged (raw {"strategy":"single_file","name":"x.html","mimetype":"text/html"} → compiled.mimetype == "text/html").
    - Test: an UNSET mimetype (no "mimetype" key in the manifest) yields compiled.mimetype is None, and the per-resolver default is applied at emission time (Task 2), NOT in the compiler — the compiler stays a thin pass-through (INV-5: no DSL, no defaulting logic in the compiler).
    - Test: the per-resolver default resolution is a pure helper — single_file with name "prototype.html"→"text/html", name "report.md"→"text/markdown", name with no/unknown extension→"application/octet-stream"; serialized_sandbox→"application/zip"; streamed_text→"text/markdown"; ppt→"text/html".
    - Test: an unknown/extra manifest key does not raise (additive-only).
  </behavior>
  <action>
    Add `mimetype: str | None = None` to the `DeliverableSpec` dataclass in plan.py:265-286 (after `revises_existing`), with a one-line docstring note that it is the DECLARED deliverable shape hint (per ISS-021); default None so existing compiled specs are byte-unchanged.
    In compiler.py `_compile_deliverable` (:680-684) add `mimetype=raw.get("mimetype")` to the returned `DeliverableSpec(...)` — a thin pass-through mirroring the existing `name=raw.get("name")` (INV-5: NO defaulting logic in the compiler; the default is computed at emission time per resolver).
    Add a per-resolver default-mimetype source on each of the four deliverable resolvers: a small `default_mimetype(deliverable)` classmethod/staticmethod (or module helper) returning single_file→infer from `deliverable.name` extension (`.html`/`.htm`→`text/html`, `.md`→`text/markdown`, unknown/none→`application/octet-stream`), serialized_sandbox→`application/zip`, streamed_text→`text/markdown`, ppt→`text/html`. Keep it import-pure (no `app.*` import — Ports & Adapters; lint-imports stays 4/0). Do NOT change the `resolve()` Protocol signature.
    REJECTED hack to avoid (from CONTEXT): do NOT content-sniff `final_output` (`startswith("<!doctype")`) — the manifest already DECLARES the shape; the default is a deterministic function of the declared strategy/name, never the bytes.
    Create test_deliverable_mimetype.py covering the behaviors above (compiler round-trip, None default, per-resolver default helper, additive-key tolerance).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_deliverable_mimetype.py -x -q</automated>
  </verify>
  <done>DeliverableSpec carries an optional mimetype; the compiler passes it through (no defaulting); each resolver exposes a deterministic default; the unit test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit deliverable_mimetype + deliverable_filename on pipeline_complete + INV-3 _VOLATILE_STRIP_KEYS guard</name>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/characterization/_normalize.py, backend/tests/unit/test_deliverable_mimetype.py</files>
  <read_first>
    - .planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md (ISS-021 BE decisions 2 + 3; the INV-3 guard is MANDATORY)
    - backend/agents/execution_engine/engine.py:1906-1943 (the _pipeline_complete_data dict + the conditional degraded keys — the emission site)
    - backend/agents/execution_engine/engine.py:953,1838 (ectx.deliverable = compiled.deliverable — the resolved spec is already on ectx; no new plumbing)
    - backend/tests/agents/characterization/_normalize.py:101-131 (_VOLATILE_STRIP_KEYS — the parity seam; model_id/estimated_cost_usd/seq/event_id are the precedent additive-but-volatile keys to mirror)
    - backend/tests/agents/test_characterization_prototype.py (one of the 5 golden drivers — the parity contract this must not break)
  </read_first>
  <behavior>
    - Test: pipeline_complete data carries deliverable_mimetype + deliverable_filename for a clean run, sourced from getattr(ectx.deliverable, "mimetype", None) (falling back to the resolver default) and getattr(ectx.deliverable, "name", None).
    - Test: when DeliverableSpec.mimetype is None, the emitted deliverable_mimetype is the resolved per-resolver default (e.g. a single_file prototype.html run emits "text/html"), NEVER null when a deliverable exists.
    - Test: the two new keys are members of _VOLATILE_STRIP_KEYS, so the normalizer strips them before the golden compare.
    - Test: a degraded run (status="degraded") still carries both keys (they are unconditional, like total_tokens — not in the strictly-conditional degraded block).
  </behavior>
  <action>
    In engine.py at the `_pipeline_complete_data` dict (:1906-1918) add two UNCONDITIONAL keys: `"deliverable_mimetype"` = the resolved mimetype (prefer `getattr(ectx.deliverable, "mimetype", None)`, else the Task-1 per-resolver default computed from the resolver strategy `compiled.deliverable.strategy or "streamed_text"` and `ectx.deliverable.name`), and `"deliverable_filename"` = `getattr(ectx.deliverable, "name", None)`. Source ONLY from `ectx.deliverable` (already set at :953/:1838) and `compiled.deliverable` — NO new cross-boundary import (Ports & Adapters; lint-imports stays 4/0). Place them alongside `final_output` so they ride EVERY pipeline_complete (clean + degraded), matching the unconditional total_tokens/model_id placement; do NOT put them in the strictly-conditional degraded block (:1937-1939).
    MANDATORY INV-3 guard: add `"deliverable_mimetype"` and `"deliverable_filename"` to the `_VOLATILE_STRIP_KEYS` frozenset in _normalize.py:101-131, with a short comment mirroring the model_id/estimated_cost_usd precedent ("additive-but-parity-neutral pipeline_complete keys, ISS-021/18-01"). `_REQUIRED_DATA_KEYS["pipeline_complete"]` is a SUBSET check so the additive keys pass without being declared required.
    Extend test_deliverable_mimetype.py with the emission + strip-membership assertions above.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_deliverable_mimetype.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q && /opt/homebrew/bin/lint-imports 2>&1 | tail -3</automated>
  </verify>
  <done>Both keys ride every pipeline_complete sourced from ectx.deliverable with the resolver default; both are in _VOLATILE_STRIP_KEYS; all 5 characterization goldens are byte-identical (green); lint-imports reports 4 kept / 0 broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → compiler | The `mimetype` field is author-declared manifest data; it crosses into the compiled `ExecutionPlan`. |
| engine → WS event | `deliverable_mimetype`/`deliverable_filename` cross from the kernel onto the `pipeline_complete` wire consumed by the FE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | manifest `mimetype` value | accept | The mimetype is a render HINT only; the FE renderer (18-03) maps unknown/unsafe values to a safe fallback (download, not execute) and sandboxes any HTML — a hostile mimetype string cannot escalate. No bytes are derived from it. |
| T-18-02 | Information disclosure | additive pipeline_complete keys | accept | `deliverable_filename` is the manifest-declared output name (e.g. `prototype.html`), not a filesystem path; no owner/workspace data added. Owner-scoping on the run is unchanged. |
| T-18-03 | Tampering (parity) | _VOLATILE_STRIP_KEYS omission | mitigate | If the new keys are NOT added to _VOLATILE_STRIP_KEYS the 5 goldens break — the Task-2 automated golden run is the gate that proves the keys are stripped and parity holds. |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NOTHING (no package-manager commands; no RESEARCH package-legitimacy table required). |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_deliverable_mimetype.py -q` green.
- All 5 characterization goldens byte-identical: `python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q`.
- `/opt/homebrew/bin/lint-imports` reports 4 kept / 0 broken.
- Zero new migrations: `git status backend/alembic/` shows no new revision files; grep confirms no `op.create_table`/`op.add_column` added in this plan's diff.
</verification>

<success_criteria>
- ROADMAP SC-1 (backend half) + SC-5 (INV-3 parity): `pipeline_complete` carries `deliverable_mimetype` + `deliverable_filename`; the 5 goldens stay byte-identical via `_VOLATILE_STRIP_KEYS`; lint-imports 4/0; zero new tables/migrations.
- An unset manifest mimetype resolves to the correct per-resolver default at emission time.
</success_criteria>

<output>
Create `.planning/phases/18-custom-workflow-ux-completeness/18-01-SUMMARY.md` when done — record the exact resolved-mimetype source expression used in engine.py, the two _VOLATILE_STRIP_KEYS additions, and the golden + lint-imports results (PROVE INV-3, not hand-wave).
</output>
