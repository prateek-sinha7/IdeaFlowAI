---
phase: 18-custom-workflow-ux-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/layout/DashboardLayout.waveMount.test.tsx
autonomous: true
requirements: [ISS-019]
must_haves:
  truths:
    - "At 1440×950 during an active wave run the 'WAVE / SUBAGENT TREE' heading is fully visible without scrolling the left column"
    - "The agent progress panel still scrolls its cards internally (a long ≥8-agent app_builder list does not push the wave panel off-screen)"
    - "A non-wave run keeps the compact 'No waves running.' empty state; no horizontal regression; nothing hard-clipped"
  artifacts:
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "Flex-budgeted left execution column (CSS-only)"
      contains: "flex-shrink-0"
  key_links:
    - from: "DashboardLayout left column wrapper"
      to: "AgentProgressPanel + WaveTreePanel siblings"
      via: "flex parent owns height; agent wrapper flex-1, wave wrapper flex-shrink-0"
      pattern: "flex-1 min-h-0"
---

<objective>
ISS-019: the `WaveTreePanel` (a headline Phase-12 capability) sits ~13px below the 1440×950 fold because `AgentProgressPanel` (designed to fill 100% and scroll internally) is the FIRST of two block siblings in an `overflow-y-auto` column — it eats the full height and pushes the wave panel past the fold; the COLUMN, not the panel, owns the only scroll. Fix the left-column flex budget (CSS-only) so the column is a height-owning flex parent: the agent panel flexes to remaining space and scrolls internally, the wave panel is a non-shrinking bottom region above the fold.

Purpose: make a built capability visible at the default viewport without changing any panel component or behavior.

Output: three className edits in `DashboardLayout.tsx:1170-1203` + a structural test asserting the new wrapper classes.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md
@.planning/ISSUES-REGISTER.md
</context>

<artifacts_this_phase_produces>
This plan MODIFIES (CSS classNames only — no component logic, no new component):
- the left execution-column wrapper: add `flex flex-col overflow-hidden` (keep `md:h-full`/widths/borders), drop the column-level `overflow-y-auto`
- the `AgentProgressPanel` wrapper: `flex-1 min-h-0 overflow-hidden`
- the `WaveTreePanel` wrapper: `flex-shrink-0 max-h-[40%] overflow-y-auto`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Flex-budget the left execution column (CSS-only)</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    - .planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md (the SPEC — ISS-019 LOCKED fix + the three REJECTED hacks)
    - frontend/src/components/layout/DashboardLayout.tsx:1170 (the left column wrapper — currently `... flex-shrink-0 h-[45vh] md:h-full ... overflow-y-auto bg-white`)
    - frontend/src/components/layout/DashboardLayout.tsx:1172-1194 (the AgentProgressPanel mount inside an ErrorBoundary)
    - frontend/src/components/layout/DashboardLayout.tsx:1195-1203 (the unconditional WaveTreePanel mount wrapper `px-3 pt-3 pb-3 border-t border-gray-200`)
  </read_first>
  <action>
    Make the left column a height-owning flex parent. On the column wrapper at :1170 add `flex flex-col overflow-hidden` and REMOVE the column-level `overflow-y-auto` (the scroll now lives inside the two child regions, not the column). Keep `w-full md:w-[340px] lg:w-[360px] flex-shrink-0 h-[45vh] md:h-full border-b md:border-b-0 md:border-r border-gray-200 bg-white`.
    Wrap the `AgentProgressPanel` ErrorBoundary region (:1171-1194) so it flexes to the remaining space and scrolls internally: the wrapper carries `flex-1 min-h-0 overflow-hidden`. Do NOT edit `AgentProgressPanel.tsx` — its existing internal `flex-1 overflow-y-auto` cards region does the scroll (CONTEXT: no panel-component change).
    Make the WaveTreePanel wrapper (:1198-1202, the `px-3 pt-3 pb-3 border-t border-gray-200` div) a non-shrinking bottom region: add `flex-shrink-0 max-h-[40%] overflow-y-auto` (keep the existing padding + `border-t`). Preserve the UNCONDITIONAL WaveTreePanel render and its props-driven pure shape (intentional Phase-12 decisions — CONTEXT "Preserve").
    REJECTED hacks to avoid (from CONTEXT): do NOT clamp the agent panel or shrink the wave panel to a fixed px height (clips content; viewport-fragile — the 122px-vs-13px drift); do NOT reorder the wave tree ABOVE the agent panel (pushes the headline agent progress below the fold instead).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>The column wrapper carries `flex flex-col` and no longer `overflow-y-auto`; the agent wrapper carries `flex-1 min-h-0`; the wave wrapper carries `flex-shrink-0`; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Structural test pinning the flex-budget wrappers</name>
  <files>frontend/src/components/layout/DashboardLayout.waveMount.test.tsx</files>
  <read_first>
    - .planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md (ISS-019 verification: the structural assertion is the offline proof; heading ≤ fold is the LIVE Playwright proof, deferred)
    - frontend/src/components/layout/DashboardLayout.waveMount.test.tsx (the existing wave-mount test — extend it; the "unrouted WorkflowComposer" comment at the top is harmless and may stay)
  </read_first>
  <action>
    Extend the existing wave-mount test with a DOM/structural assertion that the left execution column is flex-budgeted: the column wrapper element carries `flex` + `flex-col` (and no longer `overflow-y-auto`), the AgentProgressPanel wrapper carries `flex-1` + `min-h-0`, and the WaveTreePanel wrapper carries `flex-shrink-0`. Query by the stable structural anchors already used by the test (the wave heading / panel slot). This is the OFFLINE proof of the budget; add a one-line comment that the true visual proof (heading bottom ≤ 950 at 1440×950 without scroll) runs in the dedicated live Playwright pass after this phase (CONTEXT deferred item).
    Do NOT assert exact pixel positions (jsdom has no layout engine) — assert the className contract that produces the layout.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/DashboardLayout.waveMount.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>The structural test asserts the three wrapper className contracts and is green; the live-Playwright-visual deferral is noted in a test comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only layout change in an already-rendered authenticated dashboard view; no new data, no new input, no new endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Denial of service (layout regression) | left execution column | accept | A wrong flex budget could hide content rather than expose it; the Task-2 structural test + the deferred live Playwright pass are the guards. No security surface. |
| T-18-SC | Tampering | npm installs | mitigate | This plan installs NOTHING (uses the already-present `tsc`/`vitest`); no package-legitimacy table required. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- `npx vitest run src/components/layout/DashboardLayout.waveMount.test.tsx` green (asserts the three wrapper className contracts).
- Manual/live (DEFERRED to the Playwright pass): at 1440×950 during an active `sample_wave` run, the "WAVE / SUBAGENT TREE" heading bottom ≤ 950 without scrolling; an app_builder ≥8-card agent list still scrolls internally; a non-wave run keeps the compact empty state. (`/tmp/wave_panel_probe.py` measures heading-y.)
</verification>

<success_criteria>
- ROADMAP SC-3: at 1440×950 during an active wave run the wave heading is fully visible WITHOUT scrolling while the agent panel scrolls internally — CSS-only, no clipped content.
- ROADMAP SC-5 (FE health): `tsc --noEmit` + vitest green.
</success_criteria>

<output>
Create `.planning/phases/18-custom-workflow-ux-completeness/18-02-SUMMARY.md` when done — record the exact three className diffs and the test assertions; note the live-visual proof is deferred to the Playwright pass.
</output>
