---
phase: 18-custom-workflow-ux-completeness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ISSUES-REGISTER.md
  - .planning/REQUIREMENTS.md
  - .planning/IMPLEMENTATION-REGISTER.md
autonomous: true
requirements: [ISS-015, ISS-014]
must_haves:
  truths:
    - "ISS-015 is dispositioned WONTFIX (by-design) in the issues register with a crisp rationale: the wizards + the live agent-composer are the intended launch surfaces; a generic manifest-driven picker is v2/WF-DB-01 (must read /api/workflows + a user_launchable flag, NOT a hardcoded name dropdown)"
    - "REQUIREMENTS.md API-06 + the migration ledger note the capability-palette composer deletion-as-superseded (ISS-014)"
    - "IMPLEMENTATION-REGISTER Phase-8 section 846 / Phase-12 section 1206 'separate composer-routing concern' is marked resolved"
  artifacts:
    - path: ".planning/ISSUES-REGISTER.md"
      provides: "ISS-015 WONTFIX disposition + rationale"
      contains: "WONTFIX"
  key_links: []
---

<objective>
ISS-015 (doc-only) + the ISS-014 documentation reconciliation. ISS-015: there is no generic pipeline-type launch picker in the UI, and that is BY DESIGN. The product launch surface is the hardcoded 6-tile CreationHub; od_ppt/od_prototype go through the PPT/Prototype wizards (which must collect template + design-system — a bare picker structurally cannot); bare prototype/ppt are template-gated-unreachable by design; sample_* are test fixtures. The custom-workflow need is already met by the live agent-composer (ISS-014). Record WONTFIX with this rationale (NO code). Also reconcile the ISS-014 deletion docs (REQUIREMENTS.md API-06 + migration ledger note the palette-composer deletion-as-superseded; the IMPLEMENTATION-REGISTER Phase-8/Phase-12 "separate composer-routing concern" is now resolved).

Purpose: close the cluster-E paper trail so the register, requirements, and implementation register agree.

Output: register/requirements/implementation-register doc edits only — zero code.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md
@.planning/ISSUES-REGISTER.md
@.planning/REQUIREMENTS.md
@.planning/IMPLEMENTATION-REGISTER.md
</context>

<artifacts_this_phase_produces>
This plan EDITS DOCS only (no code, no tests):
- ISSUES-REGISTER.md: ISS-015 to WONTFIX (by-design) with the rationale + the v2/WF-DB-01 note (a manifest-driven picker must read /api/workflows + a user_launchable flag, never a hardcoded name dropdown)
- REQUIREMENTS.md: API-06 + migration ledger note the capability-palette composer deletion-as-superseded (ISS-014)
- IMPLEMENTATION-REGISTER.md: Phase-8 section 846 / Phase-12 section 1206 "separate composer-routing concern" marked resolved
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record ISS-015 WONTFIX (by-design) + reconcile the ISS-014 deletion docs</name>
  <files>.planning/ISSUES-REGISTER.md, .planning/REQUIREMENTS.md, .planning/IMPLEMENTATION-REGISTER.md</files>
  <read_first>
    - .planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md (ISS-015 LOCKED disposition + rationale; the ISS-014 "Reconcile docs" instruction)
    - .planning/ISSUES-REGISTER.md (the ISS-015 row near line 24 [status OPEN] + the deep-investigation row near line 55 ["WONTFIX (by-design)" in the fix column] — flip the STATUS to WONTFIX with the rationale)
    - .planning/REQUIREMENTS.md (the API-06 row near line 166 "Dynamic composer + capability palette + per-agent model picker ... additive panels"; note the palette composer is deleted-as-superseded by the live AgentsPopup)
    - .planning/IMPLEMENTATION-REGISTER.md (Phase-8 section 846 capability-palette note + Phase-12 section 1206 "separate composer-routing concern" on the dead WaveTreePanel mount in unrouted WorkflowComposer.tsx — mark resolved by Phase 18 / ISS-014)
    - .planning/phases/18-custom-workflow-ux-completeness/18-04-SUMMARY.md (the relocate-vs-delete decision for AgentModelPicker — match the REQUIREMENTS annotation to it if 18-04 has run; if not yet run, note the locked-preferred relocation and the fallback)
  </read_first>
  <action>
    In ISSUES-REGISTER.md flip the ISS-015 row STATUS to WONTFIX (by-design) and ensure the rationale is recorded crisply: the launch surface is the hardcoded 6-tile CreationHub + the PPT/Prototype wizards (template+design-system collection); bare prototype/ppt are template-gated-unreachable (D-13-06-a); sample_* are test fixtures; the custom-workflow need is met by the live agent-composer (ISS-014); a user-facing named-manifest picker = WF-DB-01 (v2). Add the guardrail note: if ever built, a manifest-driven picker MUST read /api/workflows (manifest discovery, exists, no FE consumer today) + a user_launchable manifest flag — NOT a hardcoded dropdown of names (that hack would ship broken template-gated buttons + expose test fixtures — the REJECTED approach).
    In REQUIREMENTS.md annotate API-06: the capability-palette composer (WorkflowComposer/CapabilityPalette) is deleted-as-superseded by the live AgentsPopup agent-composer (ISS-014, Phase 18); the per-agent model picker is relocated into the live composer (or v2-carried — match the 18-04 SUMMARY decision). Keep the API-02 /api/capabilities contract noted as RETAINED.
    In IMPLEMENTATION-REGISTER.md mark the Phase-8 section 846 capability-palette note and the Phase-12 section 1206 "separate composer-routing concern" (the dead WaveTreePanel mount in the unrouted WorkflowComposer.tsx) as RESOLVED by Phase 18 / ISS-014 (the unrouted composer is deleted; the live mount is in DashboardLayout.tsx — and 18-02 flex-budgets it).
    Doc-only — make NO code change. Keep edits append/annotate-style so existing history rows stay intact.
  </action>
  <verify>
    <automated>grep -iq "WONTFIX" .planning/ISSUES-REGISTER.md && grep -iq "superseded" .planning/REQUIREMENTS.md && grep -iq "ISS-014" .planning/IMPLEMENTATION-REGISTER.md && echo OK</automated>
  </verify>
  <done>ISS-015 is WONTFIX (by-design) with the rationale + the v2 guardrail; REQUIREMENTS.md API-06 + the migration ledger note the composer deletion-as-superseded; IMPLEMENTATION-REGISTER Phase-8/Phase-12 composer-routing concern is marked resolved; no code changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edits to planning artifacts; no code, no runtime surface, no input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Repudiation (stale docs) | issues/requirements/impl registers | accept | The only risk is doc drift; the grep verify pins the WONTFIX + superseded + resolved annotations. No security surface. |
| T-18-SC | Tampering | npm/pip installs | mitigate | This plan installs NOTHING (doc edits only); no package-legitimacy table required. |
</threat_model>

<verification>
- `grep -iq "WONTFIX" .planning/ISSUES-REGISTER.md` matches (ISS-015 dispositioned).
- `grep -iq "superseded" .planning/REQUIREMENTS.md` matches (API-06 deletion-as-superseded annotated).
- `grep -iq "ISS-014" .planning/IMPLEMENTATION-REGISTER.md` matches (Phase-8/12 composer-routing concern resolved).
- No code change: `git status` shows only the three .planning/*.md docs modified by this plan.
</verification>

<success_criteria>
- ROADMAP SC-4: ISS-015 dispositioned WONTFIX in the register with a crisp by-design rationale (wizards + agent-composer are the launch surfaces; generic manifest-driven picker = v2/WF-DB-01).
- The ISS-014 deletion paper trail is reconciled across REQUIREMENTS.md + IMPLEMENTATION-REGISTER.md.
</success_criteria>

<output>
Create `.planning/phases/18-custom-workflow-ux-completeness/18-05-SUMMARY.md` when done — record the ISS-015 WONTFIX disposition text and the three reconciliation edits.
</output>
