---
phase: 18-custom-workflow-ux-completeness
plan: 04
type: execute
wave: 3
depends_on: ["18-03"]
files_modified:
  - frontend/src/components/workflow/WorkflowComposer.tsx
  - frontend/src/components/workflow/CapabilityPalette.tsx
  - frontend/src/components/workflow/AgentModelPicker.tsx
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/workflow/IdeaInputPage.tsx
  - frontend/src/hooks/useWorkflow.ts
autonomous: true
requirements: [ISS-014]
must_haves:
  truths:
    - "WorkflowComposer.tsx + CapabilityPalette.tsx are proven orphaned (rename-to-.bak fault injection: tsc + vitest pass with zero broken imports) and then DELETED"
    - "AgentModelPicker is relocated into the live AgentsPopup Agents tab; its per-agent model selection threads into the run_pipeline payload as model_overrides (delivering MODEL-03 end-to-end) — OR, if relocation proves too invasive, AgentModelPicker is deleted and MODEL-03's FE-half is recorded as a v2 carry-forward (decision recorded in SUMMARY). It is NOT left orphaned."
    - "/api/capabilities + test_capabilities_api.py are RETAINED unchanged (API-02 contract + the model-catalog data source)"
    - "FE build + tests green after deletion (no broken imports)"
  artifacts:
    - path: "frontend/src/components/workflow/AgentsPopup.tsx"
      provides: "Relocated per-agent model picker wired to model_overrides (relocation branch)"
  key_links:
    - from: "frontend/src/components/workflow/AgentsPopup.tsx (model picker onChange)"
      to: "run_pipeline payload model_overrides"
      via: "IdeaInputPage onRun extraParams → DashboardLayout handleRunPipeline → startPipeline context merge"
      pattern: "model_overrides"
---

<objective>
ISS-014: delete the orphaned, unrouted capability-palette composer and stop leaving its model picker stranded. `WorkflowComposer.tsx` is mounted by NO route (pre-existing legacy); `CapabilityPalette.tsx` + `AgentModelPicker.tsx` are consumed ONLY by it (transitively dead); `/api/capabilities` is live but has ZERO FE consumer — an INV-3/12 dual-impl smell (Phase-8 hung new panels into the unrouted composer; Phase-12 logged it a "separate composer-routing concern" and never resolved it). Delete the dead composer + palette, and RELOCATE the per-agent model picker into the LIVE agent-composer (`AgentsPopup` Agents tab), wiring its selection into the `run_pipeline` payload as `model_overrides` (the backend already validates + persists `model_overrides`) — delivering MODEL-03 reachably. KEEP `/api/capabilities` + its test (API-02).

Purpose: remove the superseded dual UI (INV-3/INV-12) and make the per-agent model picker actually reachable, closing MODEL-03's FE half.

Output: two deleted files, the model picker relocated + wired (or deleted with a recorded v2 carry-forward), and a retained `/api/capabilities` contract.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md
@.planning/ISSUES-REGISTER.md
@.planning/IMPLEMENTATION-REGISTER.md
</context>

<artifacts_this_phase_produces>
This plan DELETES + RELOCATES (no new dead code):
- DELETE `WorkflowComposer.tsx` + `CapabilityPalette.tsx` (100% unreachable, superseded by the live AgentsPopup)
- RELOCATE `AgentModelPicker` into the live `AgentsPopup` Agents tab + wire its `onChange` (agentId→modelId) into the `run_pipeline` payload as `model_overrides` (delivers MODEL-03) — OR delete it + record MODEL-03 FE-half as v2 (decision in SUMMARY)
- KEEP (unchanged): `backend/app/api/capabilities.py` + `backend/tests/unit/test_capabilities_api.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Relocate AgentModelPicker into the live AgentsPopup + wire model_overrides into the run payload</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx, frontend/src/components/workflow/IdeaInputPage.tsx, frontend/src/hooks/useWorkflow.ts</files>
  <read_first>
    - .planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md (ISS-014 LOCKED decision: PREFER relocate; fallback delete + v2 carry-forward; KEEP /api/capabilities; the REJECTED hacks)
    - frontend/src/components/workflow/AgentModelPicker.tsx:1-70 (the picker — props `{ agents:{id,name}[], onChange:(modelOverrides: Record<string,string>)=>void }`, fetches /api/capabilities model_catalog, offers only user_allowed models; relocate this component/behavior into the Agents tab)
    - frontend/src/components/workflow/AgentsPopup.tsx:709-840 (AgentsPopup props + the activeTab "agents"|"skills-hooks" state at :722 + the agents-tab content at :839 — the relocation host)
    - frontend/src/components/workflow/IdeaInputPage.tsx:42,463-472 (onRun signature `(message, agentIds, resolvedType, extraParams?)` + the AgentsPopup mount — thread the picker selection up via extraParams)
    - frontend/src/hooks/useWorkflow.ts:35-92 (startPipeline; the `context` arg is merged into the payload via `Object.assign(payload, context)` at :85 — so `{ model_overrides }` passed through extraParams already lands in the run_pipeline payload)
    - backend/app/api/websocket.py:95-132 (_validate_model_overrides — the backend ALREADY accepts + validates model_overrides; the FE only needs to send it)
  </read_first>
  <action>
    Relocate the AgentModelPicker behavior into the live AgentsPopup Agents tab (the `activeTab === "agents"` content): render the per-agent model picker for the current `agents` list, populated from the existing `/api/capabilities` model_catalog (user_allowed only — keep AgentModelPicker's fetch + filter behavior; you may move the component file's logic into AgentsPopup or render the existing AgentModelPicker component inside the tab, whichever keeps the diff smallest while removing its orphan status). The picker reports `onChange(modelOverrides: Record<string,string>)` upward.
    Thread the selection out of AgentsPopup (a new `onModelOverridesChange` prop or via the existing add/remove/reorder callback shape) → IdeaInputPage holds the current `model_overrides` and includes it in the `onRun(..., extraParams)` it already builds (where `gate_agent_ids` is threaded) as `{ ...existingExtraParams, model_overrides }` ONLY when non-empty (an unselected agent emits no override — byte-identical payload when nothing is picked, preserving INV-3 for existing runs).
    Confirm the wire reaches the payload: `extraParams` flows IdeaInputPage.onRun → DashboardLayout.handleRunPipeline → onStartPipeline → useWorkflow.startPipeline, where `Object.assign(payload, context)` merges it into `run_pipeline`. If the generic merge is sufficient, useWorkflow.ts needs NO edit beyond an optional explicit `payload.model_overrides` assignment + a comment for clarity (CONTEXT cites useWorkflow.ts:47-55 as the payload site — make the model_overrides path explicit there if it improves traceability, but do NOT double-send). Keep the payload byte-identical when no override is selected.
    Fallback (record the decision in SUMMARY if taken): if relocation proves too invasive within this phase, DELETE AgentModelPicker.tsx too and record MODEL-03's FE-half as a v2 carry-forward — do NOT leave it orphaned.
    Do NOT touch /api/capabilities or its test (API-02 — KEEP). REJECTED hack: do NOT route the legacy WorkflowComposer to "make API-06 real" (resurrects a superseded dual impl).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>The per-agent model picker is reachable in the live AgentsPopup Agents tab (or deleted with a recorded v2 decision); a selected override flows into the run_pipeline payload as model_overrides; an empty selection leaves the payload byte-identical; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fault-inject orphan-ness, then DELETE WorkflowComposer.tsx + CapabilityPalette.tsx (+ AgentModelPicker.tsx if fallback)</name>
  <files>frontend/src/components/workflow/WorkflowComposer.tsx, frontend/src/components/workflow/CapabilityPalette.tsx, frontend/src/components/workflow/AgentModelPicker.tsx</files>
  <read_first>
    - .planning/phases/18-custom-workflow-ux-completeness/18-CONTEXT.md (ISS-014 LOCKED: fault-injection-confirm orphan-ness BEFORE deleting; the wave-mount test's "unrouted WorkflowComposer" comment is harmless)
    - frontend/src/components/workflow/WorkflowComposer.tsx (the dead composer — 328 lines; verify it imports CapabilityPalette + AgentModelPicker and is itself imported by NO live route)
    - frontend/src/components/workflow/CapabilityPalette.tsx (consumed only by WorkflowComposer)
  </read_first>
  <action>
    Orphan-ness fault injection FIRST (CONTEXT mandatory): rename `WorkflowComposer.tsx` and `CapabilityPalette.tsx` to `.bak` (and `AgentModelPicker.tsx` only if Task-1 took the delete fallback), then run `tsc --noEmit` + the FE vitest suite. Both MUST pass with ZERO broken imports — that PROVES no live consumer (a broken import would name the live consumer). The only references are a comment in DashboardLayout.waveMount.test.tsx ("unrouted WorkflowComposer") and a comment in lib/api.ts — both harmless prose, not imports.
    If green: DELETE the `.bak` files for real (remove WorkflowComposer.tsx + CapabilityPalette.tsx, and AgentModelPicker.tsx iff the delete-fallback path was taken in Task 1; if Task 1 RELOCATED the picker by moving its logic into AgentsPopup, AgentModelPicker.tsx is now consumed/absorbed — delete the now-dead standalone file).
    If a rename surfaces ANY broken import (a live consumer exists): STOP, restore the file, and record the live consumer in the SUMMARY (the orphan-ness premise would be falsified — do not force-delete a live file).
    Optionally tidy the stale `lib/api.ts:516` comment referencing AgentModelPicker/CapabilityPalette so the codebase has no dangling reference to deleted files (comment-only; not load-bearing).
  </action>
  <verify>
    <automated>cd frontend && (! ls src/components/workflow/WorkflowComposer.tsx src/components/workflow/CapabilityPalette.tsx 2>/dev/null) && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <done>WorkflowComposer.tsx + CapabilityPalette.tsx no longer exist; orphan-ness was fault-injection-confirmed before deletion; FE tsc + full vitest suite green with zero broken imports; /api/capabilities + its test untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE run config → run_pipeline payload | `model_overrides` (agentId→modelId) crosses from the relocated picker into the untrusted run payload. |
| (deletion) | No new boundary; removing unreachable code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering | model_overrides ingress | mitigate | The backend `_validate_model_overrides` (websocket.py:95-132) is the existing chokepoint that type-guards + allow-lists overrides and emits `invalid_model_override` — UNCHANGED by this plan; the FE only sends the field. No new server trust surface. |
| T-18-09 | Elevation of privilege (authz/endpoint regression) | deletion of composer + retained /api/capabilities | mitigate | `/api/capabilities` + `test_capabilities_api.py` are explicitly RETAINED unchanged (no endpoint/authz delta). The deleted files are FE-only, unrouted, fault-injection-proven orphaned before deletion — no route/permission scope removed. |
| T-18-10 | DoS (broken build) | force-deleting a live file | mitigate | The rename-to-.bak fault injection + tsc/vitest gate runs BEFORE deletion; a broken import aborts the delete and names the live consumer. |
| T-18-SC | Tampering | npm installs | mitigate | This plan installs NOTHING (uses the present tsc/vitest); no package-legitimacy table required. |
</threat_model>

<verification>
- Orphan-ness PROVEN: rename WorkflowComposer.tsx + CapabilityPalette.tsx → .bak, then `cd frontend && npx tsc --noEmit && npx vitest run` both green (zero broken imports) — then delete for real.
- Post-delete: `cd frontend && npx tsc --noEmit` clean + `npx vitest run` green; `ls src/components/workflow/WorkflowComposer.tsx src/components/workflow/CapabilityPalette.tsx` errors (files gone).
- model_overrides reachable: the AgentsPopup Agents tab renders the per-agent picker; a selection produces `model_overrides` in the run_pipeline payload (relocation branch) — verified by tsc + a wiring assertion or the existing run-payload test.
- API-02 retained: `cd backend && python3.11 -m pytest tests/unit/test_capabilities_api.py -q` green (unchanged).
</verification>

<success_criteria>
- ROADMAP SC-2: `WorkflowComposer.tsx` + `CapabilityPalette.tsx` deleted (proven unrouted; no broken imports; FE build + tests green); the per-agent model picker relocated into the live agent-composer with `model_overrides` reaching the `run_pipeline` payload (or deleted with MODEL-03 FE-half recorded as v2 — decision recorded); `/api/capabilities` + `test_capabilities_api.py` retained.
- ROADMAP SC-5 (FE health): `tsc --noEmit` + vitest green.
</success_criteria>

<output>
Create `.planning/phases/18-custom-workflow-ux-completeness/18-04-SUMMARY.md` when done — record: the fault-injection result (tsc/vitest green proving orphan-ness), the relocate-vs-delete decision for AgentModelPicker (and the exact model_overrides wiring path if relocated), and confirmation /api/capabilities + its test are untouched. Note the IMPLEMENTATION-REGISTER Phase-8 §846 / Phase-12 §1198 "separate composer-routing concern" is now resolved (reconciled in 18-05).
</output>
