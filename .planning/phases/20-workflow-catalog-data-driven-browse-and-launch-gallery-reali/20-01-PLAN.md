---
phase: 20-workflow-catalog-data-driven-browse-and-launch-gallery-reali
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/workflows/manifest.py
  - backend/app/api/workflows.py
  - backend/agents/workflows/app_builder/workflow.yaml
  - backend/agents/workflows/user_stories/workflow.yaml
  - backend/agents/workflows/custom/workflow.yaml
  - backend/agents/workflows/mulesoft_to_springboot/workflow.yaml
  - backend/agents/workflows/dotnet_to_azure/workflow.yaml
  - backend/agents/workflows/prototype/workflow.yaml
  - backend/agents/workflows/ppt/workflow.yaml
  - backend/tests/agents/test_manifest.py
  - backend/tests/unit/test_workflows_api.py
autonomous: true
requirements: [REUSE-MANDATE, ADDITIVE-BACKEND, INV-3, SC-001, PORTS-ADAPTERS, INV-5, ADDITIVE-ONLY]
must_haves:
  truths:
    - "GET /api/workflows returns user_launchable (+ display_name/icon/launch_surface) per workflow, sourced from the declared manifest flag (never a hardcoded name list) — SC-001"
    - "The 5 launchables (app_builder/user_stories/custom/mulesoft_to_springboot/dotnet_to_azure) report user_launchable=true; prototype/ppt report user_launchable=true + launch_surface=wizard; revisions/od_*/reverse_engineer/chat report user_launchable=false"
    - "The 5 characterization goldens (prototype/od_prototype/prototype_revision/od_ppt/app_builder) stay byte-identical — the new fields are inert (default False/None, never read by compiler/kernel) — INV-3"
    - "_ALLOWED_TOP_KEYS widened only with the 5 presentation/visibility keys; when/if/for/expr still rejected — INV-5 (no DSL)"
    - "lint-imports stays 4 kept / 0 broken — no new source→forbidden edge — PORTS-ADAPTERS"
    - "No new table, no migration, no engine/compiler/registry/kernel edit — ADDITIVE-ONLY / ADDITIVE-BACKEND"
  artifacts:
    - path: "backend/agents/workflows/manifest.py"
      provides: "WorkflowManifest gains user_launchable/display_name/description/icon/launch_surface fields + _optional_bool/_optional_str helpers; 5 keys added to _ALLOWED_TOP_KEYS"
      contains: "user_launchable: bool = False"
    - path: "backend/app/api/workflows.py"
      provides: "WorkflowSummary gains user_launchable/display_name/icon/launch_surface; list_workflows populates them off the loaded manifest"
      contains: "user_launchable"
    - path: "backend/tests/agents/test_manifest.py"
      provides: "new-field load test + default test + bool-type-reject test; strict-key rejection test preserved unchanged"
      contains: "user_launchable"
  key_links:
    - from: "backend/app/api/workflows.py:list_workflows"
      to: "backend/agents/workflows/manifest.py:load_manifest"
      via: "additive manifest read alongside compile_for_run"
      pattern: "load_manifest|manifest\\.user_launchable"
    - from: "backend/agents/workflows/<id>/workflow.yaml"
      to: "WorkflowManifest.user_launchable"
      via: "declared top-level key parsed by _build_manifest"
      pattern: "user_launchable: true"
---

<objective>
Land the ENTIRE backend surface for the data-driven workflow catalog: an additive, inert
`user_launchable` (+ `display_name`/`description`/`icon`/`launch_surface`) flag on the
`WorkflowManifest` schema, surfaced through the EXISTING authenticated `GET /api/workflows`,
and set on the 7 launchable manifest YAMLs. This is the SC-001 dividend made data: launchability
keys on a DECLARED manifest flag enumerated generically over `PIPELINE_AGENTS`, never a hardcoded
workflow-name list — a brand-new manifest setting `user_launchable: true` joins the catalog with
zero code edit.

Purpose: give the frontend catalog (Plan 20-02) a live, filterable workflow list whose
launchability is declared in data, while proving the change cannot perturb the 5 characterization
goldens (INV-3), keeps `when/if/for/expr` rejected (INV-5), and adds no source→forbidden import
edge (lint-imports 4/0). No new table, no migration, no engine/compiler/registry/kernel edit.

Output: extended `WorkflowManifest` dataclass + 2 new `_optional_*` helpers + widened allow-list;
extended `WorkflowSummary` + populated `list_workflows`; 7 edited `workflow.yaml` files; new
manifest-schema unit tests + extended workflows_api test; a proven goldens-parity + lint-imports run.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/20-workflow-catalog-data-driven-browse-and-launch-gallery-reali/20-SPEC.md
@.planning/phases/20-workflow-catalog-data-driven-browse-and-launch-gallery-reali/20-PATTERNS.md
@.planning/phases/20-workflow-catalog-data-driven-browse-and-launch-gallery-reali/20-CONTEXT.md

# Reuse analogs + edit targets (read the named lines before editing)
@backend/agents/workflows/manifest.py
@backend/app/api/workflows.py
@backend/tests/agents/test_manifest.py
@backend/tests/unit/test_workflows_api.py
</context>

<artifacts_this_phase_produces>
New symbols this PLAN introduces (frontend symbols land in 20-02):
- `WorkflowManifest.user_launchable: bool = False`
- `WorkflowManifest.display_name: str | None = None`
- `WorkflowManifest.description: str | None = None`
- `WorkflowManifest.icon: str | None = None`
- `WorkflowManifest.launch_surface: str | None = None`
- `_optional_bool(data, key, file_str, default) -> bool` (in `manifest.py`)
- `_optional_str(data, key, file_str) -> str | None` (in `manifest.py`)
- 5 new entries in `_ALLOWED_TOP_KEYS` (`manifest.py`)
- `WorkflowSummary.user_launchable` / `.display_name` / `.icon` / `.launch_surface` (`app/api/workflows.py`)
- `user_launchable: true` keys on 7 `workflow.yaml` files (+ `launch_surface: "wizard"` on prototype/ppt)
- New manifest tests: `test_loads_optional_catalog_fields`, a defaults test, a bool-reject test
- Extended `test_workflows_api.py` assertions for the new fields + the launchable set
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 5 inert catalog fields + _optional_bool/_optional_str to the manifest schema (with tests)</name>
  <files>backend/agents/workflows/manifest.py, backend/tests/agents/test_manifest.py</files>
  <read_first>
    - backend/agents/workflows/manifest.py — the WHOLE file is in context. Bind to: the "Optional with defaults" block (`version: int = 1` at :67, the forward `model`/`limits` at :70-71), `_ALLOWED_TOP_KEYS` literal (:80-94), `_build_manifest` extraction+construction (:180-201), `_optional_int` (:259-270, the bool-reject idiom to mirror — do NOT copy verbatim, see acceptance), `_optional_list`/`_optional_dict` (:230-256).
    - backend/tests/agents/test_manifest.py — the well-formed test `test_loads_well_formed_manifest` (:62-76), the `_WELL_FORMED` const (:31-46), the strict-key rejection test `test_rejects_unknown_key` (:110-120, MUST stay unchanged).
    - 20-PATTERNS.md §7 (manifest.py copy-ready excerpt) + §10 (test analog) — bind verbatim.
  </read_first>
  <behavior>
    - A manifest declaring all 5 new keys parses: user_launchable is True, display_name=="Demo Workflow", description=="A demo", icon=="rocket", launch_surface=="wizard".
    - A manifest declaring NONE of the 5 keys parses to defaults: user_launchable is False, display_name/description/icon/launch_surface are all None.
    - A manifest with `user_launchable: 1` (an int, not a bool) raises ManifestValidationError naming the field 'user_launchable' (the int subclass trap must NOT silently accept it).
    - A manifest with a `display_name:` that is not a string raises ManifestValidationError naming 'display_name'.
    - The PRESERVED strict-key test still passes: `when`/`if`/`for`/`expr` each still raise ManifestValidationError naming the key (INV-5 — the allow-list widening must not legalize control flow).
  </behavior>
  <action>
    Add to the WorkflowManifest "Optional with defaults" block (after `version: int = 1` at :67, per 20-PATTERNS.md §7): `user_launchable: bool = False`, then `display_name`, `description`, `icon`, `launch_surface` each typed `str | None = None`. Add those exact 5 key names to the `_ALLOWED_TOP_KEYS` frozenset (:80-94) — copy the existing string-literal entry style (e.g. `"version",`). Add a NEW `_optional_bool(data, key, file_str, default)` helper modeled on `_optional_int` BUT inverted: the existing `_optional_int` REJECTS bool via `isinstance(value, bool)` — `_optional_bool` must instead ACCEPT only real bools (`if not isinstance(value, bool): raise ManifestValidationError(... must be a boolean ...)`); return `default` when key absent or None. Add a `_optional_str(data, key, file_str)` helper mirroring `_optional_int`'s shape but returning `str | None` (default None; accept None/absent → None; reject non-str naming the field). Wire 5 extraction lines into `_build_manifest` (after the `version = _optional_int(...)` line at :183) and pass all 5 as constructor kwargs (after `version=version,` at :198). These fields are INERT — never read by compiler.py/the kernel (parity proof in 20-SPEC §4). Then add to test_manifest.py, copying the `test_loads_well_formed_manifest` shape (:62-76) against a `_WELL_FORMED + "user_launchable: true\n..."` doc: `test_loads_optional_catalog_fields` (all 5 parse), a defaults test (absent → False/None), and a bool-reject test feeding `user_launchable: 1` asserting the field name appears in the error. Do NOT touch `test_rejects_unknown_key`.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_manifest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.py` contains the literal line `user_launchable: bool = False`.
    - `grep -c 'user_launchable\|display_name\|description\|icon\|launch_surface' backend/agents/workflows/manifest.py` shows all 5 keys present in `_ALLOWED_TOP_KEYS` (5 new entries) AND on the dataclass.
    - `manifest.py` defines `def _optional_bool(` and `def _optional_str(`; `_optional_bool` contains `isinstance(value, bool)` as an ACCEPT gate (raises when NOT a bool) — distinct from `_optional_int`'s reject.
    - `cd backend && python3.11 -m pytest tests/agents/test_manifest.py -x -q` passes including the preserved `test_rejects_unknown_key[when]`/`[if]`/`[for]`/`[expr]` cases.
    - `cd backend && python3.11 -m pytest 'tests/agents/test_manifest.py::test_loads_optional_catalog_fields' -x -q` passes.
  </acceptance_criteria>
  <done>The schema carries 5 inert catalog fields with bool/str coercion; new tests prove parse + defaults + bool-reject; the INV-5 strict-key test is untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface the fields through GET /api/workflows + set the flags on the 7 launchable YAMLs (with API test)</name>
  <files>backend/app/api/workflows.py, backend/agents/workflows/app_builder/workflow.yaml, backend/agents/workflows/user_stories/workflow.yaml, backend/agents/workflows/custom/workflow.yaml, backend/agents/workflows/mulesoft_to_springboot/workflow.yaml, backend/agents/workflows/dotnet_to_azure/workflow.yaml, backend/agents/workflows/prototype/workflow.yaml, backend/agents/workflows/ppt/workflow.yaml, backend/tests/unit/test_workflows_api.py</files>
  <read_first>
    - backend/app/api/workflows.py — the WHOLE file is in context. Bind to: `WorkflowSummary` (:65-72), `_display_name` fallback (:114-121), `list_workflows` population loop (:176-209, the `out.append(WorkflowSummary(...))` at :200-208), the existing imports (`compile_for_run`/`PIPELINE_AGENTS`/`load_agent_spec` at :36-38). `load_manifest` lives in `agents.workflows.manifest`; the workflows dir constant must be located (grep for `_WORKFLOWS_DIR` / how `compile_for_run` finds manifests) — reuse the resolved path the engine already uses, do not invent a new one.
    - backend/tests/unit/test_workflows_api.py — `test_list_returns_all_authored_workflows` (:51-59, asserts `ids == set(PIPELINE_AGENTS.keys())` — MUST stay green), `test_list_entries_carry_metadata` (:61-71). NOTE this file is under tests/unit/, NOT tests/agents/.
    - 20-PATTERNS.md §8 (workflows.py copy-ready excerpt) + §9 (workflow.yaml DATA edits) + §5 inventory table; the sibling per-item trust flag pattern is `capabilities.py user_allowed`.
  </read_first>
  <behavior>
    - GET /api/workflows still returns one entry per PIPELINE_AGENTS id (the existing `ids == set(PIPELINE_AGENTS.keys())` assertion stays green); the response is unchanged shape PLUS 4 new optional fields.
    - app_builder/user_stories/custom/mulesoft_to_springboot/dotnet_to_azure each report user_launchable=true.
    - prototype and ppt each report user_launchable=true AND launch_surface=="wizard".
    - Every other listed id (reverse_engineer, chat, *_revision, od_ppt) reports user_launchable=false and launch_surface=None.
    - display_name falls back to `_display_name(id)` when the manifest sets none (never null for a listed row).
  </behavior>
  <action>
    Add the 4 fields to `WorkflowSummary` (:65-72) — `user_launchable: bool = False`, `display_name: str | None = None`, `icon: str | None = None`, `launch_surface: str | None = None` (per 20-PATTERNS.md §8; note the BE summary intentionally omits a separate `description` field — `description` already exists and the manifest `description` feeds it as a fallback). Import `load_manifest` from `agents.workflows.manifest` and `resolve_alias` (the alias resolver the router already implies via `compile_for_run`; if `resolve_alias` is not importable, load by `workflow_id` directly since `list_workflows` iterates real `PIPELINE_AGENTS` keys, never aliases). In the `list_workflows` loop (:176-208), additively load the manifest alongside the existing `compile_for_run(workflow_id)` (the lower-risk additive path per CONTEXT Discretion), then populate the 4 new kwargs in the `out.append(WorkflowSummary(...))` call: `user_launchable=manifest.user_launchable`, `display_name=manifest.display_name or _display_name(workflow_id)`, `icon=manifest.icon`, `launch_surface=manifest.launch_surface`; and prefer `manifest.description or _describe(...)` for the existing description field. Guard the manifest read so a load failure degrades to defaults (a single bad manifest must not break the listing — mirror the `_spec_by_id` try/except posture). Then edit the 7 YAMLs: add `user_launchable: true` as a top-level key to app_builder/user_stories/custom/mulesoft_to_springboot/dotnet_to_azure `workflow.yaml`; add BOTH `user_launchable: true` AND `launch_surface: "wizard"` to prototype/ppt `workflow.yaml`. Add NOTHING to any other manifest (defaults keep them false/parity-safe — od_prototype is an alias dir resolving to prototype, gets nothing of its own). Finally extend test_workflows_api.py with a `test_list_carries_launchable_flags` asserting the launchable set above and that *_revision/reverse_engineer report user_launchable false; keep `test_list_returns_all_authored_workflows` unchanged.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_workflows_api.py tests/agents/test_manifest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/workflows.py` `WorkflowSummary` contains `user_launchable` and `launch_surface` fields.
    - `app/api/workflows.py` `list_workflows` calls `load_manifest(` and populates `user_launchable=manifest.user_launchable`.
    - `grep -rl 'user_launchable: true' backend/agents/workflows/{app_builder,user_stories,custom,mulesoft_to_springboot,dotnet_to_azure,prototype,ppt}/workflow.yaml` lists exactly those 7 files.
    - `grep -rl 'launch_surface: .wizard' backend/agents/workflows/{prototype,ppt}/workflow.yaml` lists exactly prototype and ppt.
    - No other `workflow.yaml` under `backend/agents/workflows/` contains `user_launchable: true` (revisions/od_*/reverse_engineer/chat untouched).
    - `cd backend && python3.11 -m pytest tests/unit/test_workflows_api.py -x -q` passes including the preserved `test_list_returns_all_authored_workflows`.
  </acceptance_criteria>
  <done>The existing endpoint now reports the declared launchability for all workflows; the 7 launchables are flagged in data; the API test pins the launchable set and the unchanged id-coverage contract.</done>
</task>

<task type="auto">
  <name>Task 3: Prove INV-3 goldens byte-identical + INV-5 preserved + lint-imports 4/0</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    - 20-SPEC.md §4 "Parity proof" + §7 "Invariants" — the four reasons the new fields cannot perturb the 5 goldens (pure read-only metadata; defaults on every golden manifest; strict-key test does not freeze the allow-list; read endpoint off the golden path).
    - memory note "Offline test suite" — the full backend pytest HANGS offline; verify with the TARGETED characterization/parity suite + manifest + workflows_api, NOT whole-repo pytest. lint-imports binary is at /opt/homebrew/bin/lint-imports.
    - The characterization golden suite path (the 5: prototype/od_prototype/prototype_revision/od_ppt/app_builder). Locate it via `ls backend/tests/agents | grep -i characteriz` and the existing parity gate (`test_phase3_cutover_verify` / the snapshot suite) before running; do NOT set SNAPSHOT_UPDATE.
  </read_first>
  <action>
    Run the offline gate WITHOUT any SNAPSHOT_UPDATE env var so the run PROVES byte/event-identity rather than rewriting baselines: (1) the 5-pipeline characterization/parity suite — byte snapshots + semantic event snapshots — must be green; (2) `tests/agents/test_manifest.py` + `tests/unit/test_workflows_api.py` green (re-confirm Task 1/2); (3) `lint-imports` (binary `/opt/homebrew/bin/lint-imports`) must report 4 contracts kept / 0 broken (no new source→forbidden edge: manifest.py is pure-data, workflows.py already imports compile_for_run/registry which are permitted for `app`). If the characterization suite is gate_blocked offline (Chromium/Bedrock/Postgres), record the specific skip reason and assert byte-identity on the deterministic byte-snapshot subset that DOES run offline (the goldens are scripted-model driven per SAFE-01). Do NOT run whole-repo `pytest` (it hangs). If any golden diff appears, STOP and report — a non-default golden manifest or a compiler read of the new field would be the cause (it must not be).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_manifest.py tests/unit/test_workflows_api.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - The 5 characterization goldens (prototype/od_prototype/prototype_revision/od_ppt/app_builder) are byte-identical — the parity/characterization suite passes with NO SNAPSHOT_UPDATE set (or, if offline-gated, the deterministic byte-snapshot subset passes and the skip reason is recorded).
    - `cd backend && python3.11 -m pytest tests/agents/test_manifest.py tests/unit/test_workflows_api.py -q` exits 0.
    - `/opt/homebrew/bin/lint-imports` prints "4 contracts" with 0 broken (4 kept / 0 broken).
    - No migration file was created (the manifest is pure data; the endpoint is DB-free) — `git status` shows no new file under `backend/migrations` / `backend/alembic`.
  </acceptance_criteria>
  <done>INV-3 byte/event parity, INV-5 no-DSL preservation, and PORTS-ADAPTERS (4/0) are all proven on the targeted offline gate; additive-only confirmed (no migration).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /api/workflows` | Authenticated (JWT, `Depends(get_current_user)`) read of manifest-derived discovery metadata. The ONLY boundary this plan touches; it is DB-free and already returns every `PIPELINE_AGENTS` id today. |
| `workflow.yaml` (repo data) → `WorkflowManifest` | Engineer-authored, version-controlled YAML parsed with `yaml.safe_load` (existing T-04-03 mitigation). The 5 new keys are pure presentation/visibility data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | `GET /api/workflows` new fields | accept | The endpoint ALREADY returns every workflow id to any authenticated user; the new `user_launchable` flag REDUCES what the FE shows (it gates a subset out). No new id, secret, or PII is exposed — the fields are presentation strings (display_name/icon) + a boolean. Same authenticated read posture as today. |
| T-20-02 | Elevation of Privilege | launchability bypass | mitigate | `user_launchable` is discovery metadata ONLY — it does NOT authorize a run. Launch still goes through the EXISTING tier-gated `run_pipeline` (server-side `can_run_pipeline`). A client forging `user_launchable=true` cannot launch a workflow its tier forbids; the catalog is not a new trust boundary. Documented in 20-SPEC §2. |
| T-20-03 | Tampering | manifest YAML / DSL injection | mitigate | `_ALLOWED_TOP_KEYS` is widened ONLY with the 5 non-control-flow keys; `when/if/for/expr` stay rejected (INV-5 preserved-rejection test). `yaml.safe_load` (existing) blocks `!!python/object` RCE. New helpers type-check each field and name the offender on error. |
| T-20-04 | Tampering | INV-3 golden drift | mitigate | New fields default False/None and are never read by compiler/kernel → 5 goldens byte-identical; Task 3 PROVES it with NO SNAPSHOT_UPDATE. A drift would surface as a failing parity gate (blocking). |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (pure source/data + tests). No legitimacy gate needed; no new dependency added. |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_manifest.py tests/unit/test_workflows_api.py -q` — green.
- The 5-pipeline characterization/parity suite — byte + semantic-event identical, NO SNAPSHOT_UPDATE.
- `/opt/homebrew/bin/lint-imports` — 4 kept / 0 broken.
- `git status` — no new migration file; only the 11 declared `files_modified` touched.
- `grep` gates from the per-task acceptance criteria for the YAML flags + the manifest/summary symbols.
</verification>

<success_criteria>
- `GET /api/workflows` reports `user_launchable` (+ display_name/icon/launch_surface) per workflow, sourced from the declared manifest flag enumerated generically over `PIPELINE_AGENTS` — no hardcoded name list (SC-001).
- The 7 launchables carry the flag in data; revisions/od_*/reverse_engineer/chat stay false.
- The 5 characterization goldens are byte-identical (INV-3); `when/if/for/expr` still rejected (INV-5); lint-imports 4/0 (PORTS-ADAPTERS); no table/migration/engine edit (ADDITIVE-ONLY / ADDITIVE-BACKEND).
- Every new file/edit names its existing analog (REUSE-MANDATE): manifest fields ⟵ `version`/`model` + `_optional_int`; summary fields ⟵ `capabilities.py user_allowed`; YAML keys ⟵ existing top-key block.
</success_criteria>

<output>
Create `.planning/phases/20-workflow-catalog-data-driven-browse-and-launch-gallery-reali/20-01-SUMMARY.md` when done.
</output>
