---
phase: 05-typed-artifacts-persistence-ownership-1b
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/agents/artifacts/__init__.py
  - backend/agents/artifacts/graph.py
  - backend/tests/agents/test_artifact_graph.py
autonomous: true
requirements: [ART-01, ART-02, ART-03, ART-04]
must_haves:
  truths:
    - "A typed ArtifactRef carries producer step/agent/task, sha256 content_hash, version, parents/derived_from, visibility, retention"
    - "ArtifactGraph writes two refs (one derived) and reads back the typed lineage"
    - "consumes:[X] returns exactly the type-X refs (typed routing, not substring match)"
    - "retention defaults to run_ttl; keep/days:N overrides persist"
  artifacts:
    - path: "backend/agents/artifacts/graph.py"
      provides: "ArtifactGraph (per-run typed substrate) + ArtifactRef dataclass"
      contains: "class ArtifactGraph"
    - path: "backend/agents/artifacts/__init__.py"
      provides: "package exports for ArtifactGraph + ArtifactRef"
    - path: "backend/tests/agents/test_artifact_graph.py"
      provides: "ART-01/02/03/04 unit coverage"
  key_links:
    - from: "backend/agents/artifacts/graph.py"
      to: "stdlib only"
      via: "imports hashlib/uuid/dataclasses — NO app.models, NO app.api, NO engine internals"
      pattern: "from __future__ import annotations"
---

<objective>
Create the kernel-importable typed artifact substrate `agents/artifacts/` — the `ArtifactGraph` (per-run, typed `produces`/`consumes` routing, in-memory lineage walk) plus the `ArtifactRef` dataclass (plan §6:454–466 field set + the inline `content` field per D-01). This is the typed data that replaces the untyped `accumulated_outputs: dict[str,str]` handoff (ART-01) and is consumed by `ExecutionContext.artifacts` (wired in 05-04). It is PURE typed data with no DB/web coupling — the dataclass↔ORM-row mapping lives in the store helper (05-03), not here.

Purpose: Establish the typed contract first (interface-first ordering) so 05-03 (store helper), 05-04 (engine dual-write/read-migration) build against a fixed shape rather than discovering it.
Output: `agents/artifacts/graph.py` + `__init__.py` + the `test_artifact_graph.py` Wave 0 suite (ART-01/02/03/04).

Deviation from CONTEXT premise: SPEC ART-01 prose says `agents/execution_engine/artifacts/`; D-02 (controlling, matches plan §32) says `agents/artifacts/`. We use `agents/artifacts/` per D-02 (RESEARCH Open Question 3 / A6).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-SPEC.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-CONTEXT.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (05-01) creates the following new symbols (the plan-review source-grounding pass excludes these from drift verification):
- `agents/artifacts/` package (`__init__.py`, `graph.py`)
- `ArtifactGraph` class (per-run typed substrate; methods: `add` / `write_ref`, `get`, `list_by_kind`, `consumed_for(agent_spec)` typed routing, `lineage` / `tree`)
- `ArtifactRef` dataclass (fields per plan §6:454–466 + inline `content`): `id`, `kind`, `owner_id`, `workspace_id`, `run_id`, `producer_step`, `producer_agent`, `task_id`, `content`, `content_hash`, `location`, `version`, `parents`, `derived_from`, `visibility` (default `"private"`... see action note), `retention` (default `"run_ttl"`)
- the `kind` string vocabulary (snake_case, one name everywhere): `spec|plan|task_list|html_file|file_bundle|repo_inventory|repo_diff|context_pack|validation_report|merge_conflict|summary|patch`
(Downstream phases add the `ArtifactRef` ORM table, `Workspace`/`RunEvent`/`RunCapabilities` models, `seq`/`event_id`, and `ExecutionContext.artifacts`/`workspace_id` — those are NOT created here.)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Write failing ART-01/02/03/04 unit tests for ArtifactGraph + ArtifactRef</name>
  <files>backend/tests/agents/test_artifact_graph.py</files>
  <read_first>
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md (Phase Requirements → Test Map; "Wave 0 Gaps")
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-VALIDATION.md (Per-Task Verification Map rows ART-01/03, ART-02, ART-04)
    - specs/003-workflow-engine-decoupling/plan.md (§6 lines 454–466 — the ArtifactRef field set; §17 lines 688–695 — typed produces/consumes routing + lineage)
    - backend/agents/execution_engine/authz.py (the existing stdlib-only/pure module discipline to mirror for the new package)
  </read_first>
  <behavior>
    Write RED tests (file does not import yet — graph.py is Task 2):
    - ART-01: build an ArtifactGraph; write ref A (kind="spec"); write ref B (kind="plan") with parents=[A.id] / derived_from=A.id; read back B and assert its lineage resolves to A (graph.lineage(run_id) or graph.tree(run_id) walks B→A).
    - ART-02: a written ref records non-null producer_step + producer_agent + task_id, a sha256 content_hash == hashlib.sha256(content.encode("utf-8")).hexdigest(), and a version int.
    - ART-03 (typed routing): given an agent whose consumes=["spec"], graph.consumed_for(...) returns exactly the kind=="spec" refs and NOT a kind=="plan" ref — assert by ArtifactRef.kind equality, never substring match on ids.
    - ART-03 (revision lineage): a ref written with derived_from=<parent-run source ref id> exposes that link via .derived_from / .parents.
    - ART-04: a ref written with no retention arg defaults retention=="run_ttl"; a ref written retention="keep" persists "keep"; retention="days:30" persists verbatim.
  </behavior>
  <action>
    Create the test file with the five behaviors above as separate test functions. Import `from agents.artifacts.graph import ArtifactGraph, ArtifactRef` (the import will fail RED until Task 2). Construct refs through the graph's write method (not by hand) so the graph owns hashing/versioning. Use deterministic content strings so content_hash assertions are exact. Do NOT import any `app.models.*` or `app.api.*` in the test — this package is kernel-pure. Run the test and confirm it FAILS (RED) on the missing import. Commit: `test(tests): add failing ArtifactGraph unit tests (ART-01..04)`.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_artifact_graph.py -x 2>&1 | grep -qE "(ModuleNotFoundError|ImportError|collected)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_artifact_graph.py -x` exits non-zero (RED) with an import error for `agents.artifacts.graph`.
    - The file contains ≥5 test functions covering ART-01 (lineage), ART-02 (provenance+hash+version), ART-03 (typed consumes routing + derived_from), ART-04 (retention default + overrides).
    - `grep -n "import app" backend/tests/agents/test_artifact_graph.py` returns 0 matches (kernel-pure test).
  </acceptance_criteria>
  <done>Failing test file exists asserting the full ArtifactRef field set and ArtifactGraph routing/lineage behavior; fails only because graph.py does not exist yet.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implement agents/artifacts/graph.py (ArtifactGraph + ArtifactRef) to GREEN</name>
  <files>backend/agents/artifacts/__init__.py, backend/agents/artifacts/graph.py</files>
  <read_first>
    - backend/agents/execution_engine/context.py (lines 1–40 — the stdlib-only @dataclass import discipline + docstring mandate to copy: `from __future__ import annotations`, `from dataclasses import dataclass, field`; NO app imports)
    - backend/app/models/artifact.py (lines 1–37 — the WorkflowArtifact column shape the dataclass field names align to: producing_agent_id/version/derived_from/content)
    - specs/003-workflow-engine-decoupling/plan.md (§6 lines 454–466 — authoritative ArtifactRef field set)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-PATTERNS.md ("agents/artifacts/graph.py" section + "Content hashing / event ids" shared pattern + "No recursive CTE — in-memory walk" Don't-Hand-Roll)
  </read_first>
  <action>
    Create `agents/artifacts/__init__.py` exporting `ArtifactGraph` and `ArtifactRef`. Create `agents/artifacts/graph.py`:
    - Header docstring stating: stdlib-only imports (per the import-linter scaffold — this module is kernel-importable and MUST NOT import `app.models.*`, `app.api.*`, or any `engine`/`factory` internals; the dataclass↔ORM mapping lives in the store helper per D-02). Imports: `from __future__ import annotations`, `from dataclasses import dataclass, field`, `import hashlib`, `import uuid`.
    - `@dataclass` `ArtifactRef` with the §6:454–466 fields plus the D-01 inline `content: str` field: `id: str`, `kind: str`, `owner_id: str`, `workspace_id: str`, `run_id: str`, `producer_step: str`, `producer_agent: str`, `task_id: str | None`, `content: str`, `content_hash: str`, `location: str`, `version: int`, `parents: list[str] = field(default_factory=list)`, `derived_from: str | None = None`, `visibility: str = "private"`, `retention: str = "run_ttl"`. (Keep `visibility` default `"private"` to match plan §6:465; the store helper's default-deny read filter widens reads to `workspace`/`public` — these are consistent.)
    - `ArtifactGraph`: holds an ordered registry of refs for a run. Method `write_ref(self, *, run_id, owner_id, workspace_id, kind, producer_step, producer_agent, task_id, content, location, parents=None, derived_from=None, visibility="private", retention="run_ttl") -> ArtifactRef` that: generates `id=str(uuid.uuid4())`, computes `content_hash = hashlib.sha256(content.encode("utf-8")).hexdigest()` (D-01, deterministic), assigns `version` as `1 + count of existing refs with the same (run_id, kind)`, stores and returns the ref. Methods: `get(self, ref_id)`, `list_by_kind(self, kind)`, `consumed_for(self, consumes: list[str]) -> list[ArtifactRef]` returning refs whose `.kind in consumes` (typed routing — ART-03; the engine maps AGENT.md `consumes` strings to kinds in 05-04), `lineage(self, ref_id)`/`tree(self, run_id=None)` doing an in-memory walk over `parents`/`derived_from` (no DB recursive CTE — per-run set is small). Keep the graph itself pure in-memory; persistence (dual-write) is the engine+helper's job in 05-03/05-04.
    - Run the Task 1 tests to GREEN. Commit: `feat(engine): add agents/artifacts typed ArtifactGraph + ArtifactRef (ART-01..04)`.
    Do NOT add a `RuntimeEnvironment`/`Workspace` field, dedup-on-hash reuse, or any retention sweep — all deferred (P9/P12).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_artifact_graph.py -x && lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_artifact_graph.py -x` passes (GREEN, all ≥5 tests).
    - `cd backend && lint-imports` exits 0 (the new kernel-importable package introduces no contract violation — RESEARCH #1).
    - `grep -nE "import app\.(models|api)" backend/agents/artifacts/graph.py` returns 0 matches (kernel-pure).
    - `grep -c "content_hash" backend/agents/artifacts/graph.py` ≥ 1 and the hash is computed via `hashlib.sha256(...).hexdigest()` over `content` (verify by reading the write_ref body).
  </acceptance_criteria>
  <done>ArtifactGraph + ArtifactRef implemented as pure typed data; all ART-01..04 tests green; import-linter green; no app.* imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed in this plan) | `agents/artifacts/` is pure in-memory typed data with no DB/network/web surface; no untrusted input crosses a boundary here. Persistence + ownership enforcement land in 05-03/05-04/05-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-HASH | Tampering | `content_hash` computation in `write_ref` | accept | `content_hash` is content-addressing (dedup/lineage), NOT a security control (RESEARCH Security Domain V6 "n/a-ish"); sha256 over inline content is deterministic; no secret material. |
| T-5-PURITY | Elevation (import-direction) | new `agents/artifacts/` package | mitigate | Keep the package stdlib-only (no `app.models`/`app.api`); enforced by `lint-imports` in Task 2 verify. Prevents the kernel→web import chain (RESEARCH #1). |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH "Package Legitimacy Audit — Not applicable; zero external packages"). No install task appears in any Phase-5 plan. |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_artifact_graph.py -x` green.
- `cd backend && lint-imports` exits 0.
- No `app.*` import in `agents/artifacts/`.
</verification>

<success_criteria>
- Typed `ArtifactRef` with full lineage field set + inline content exists and is content-addressed (sha256).
- `ArtifactGraph` supports typed `consumes` routing + in-memory lineage walk.
- Wave 0 test file `test_artifact_graph.py` green; kernel import-purity preserved.
</success_criteria>

<output>
Create `.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-01-SUMMARY.md` when done.
</output>
