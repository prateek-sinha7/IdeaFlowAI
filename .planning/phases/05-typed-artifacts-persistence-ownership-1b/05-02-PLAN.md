---
phase: 05-typed-artifacts-persistence-ownership-1b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/artifact_ref.py
  - backend/app/models/workspace.py
  - backend/app/models/run_event.py
  - backend/app/models/run_capabilities.py
  - backend/app/models/workflow.py
  - backend/app/models/workflow_definition.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/0014_typed_artifacts_persistence.py
  - backend/tests/unit/test_migration_0014.py
autonomous: true
requirements: [PERSIST-01, AUTHZ-01, ART-02, ART-04, PERSIST-03, CAPRUN-01]
must_haves:
  truths:
    - "alembic upgrade head applies cleanly from 0013; alembic downgrade reverses cleanly"
    - "artifact_refs, workspaces, run_events, run_capabilities tables exist; workflow_runs gains owner_id/workspace_id/source_run_id/plan_id/budget_snapshot_json; workflows (existing) gains owner_id/workspace_id/source/manifest_json/version"
    - "every new table carries owner_id + workspace_id"
    - "artifact_refs has idx (run_id, kind) + (content_hash); run_events has idx (run_id, seq)"
    - "default-workspace backfill gives every existing run a workspace_id + owner_id=user_id"
  artifacts:
    - path: "backend/app/models/artifact_ref.py"
      provides: "ArtifactRef ORM model (table artifact_refs) replacing WorkflowArtifact"
      contains: "__tablename__ = \"artifact_refs\""
    - path: "backend/app/models/workspace.py"
      provides: "Workspace ORM model (table workspaces)"
      contains: "__tablename__ = \"workspaces\""
    - path: "backend/app/models/run_event.py"
      provides: "RunEvent ORM model (table run_events, seq + event_id)"
      contains: "__tablename__ = \"run_events\""
    - path: "backend/app/models/run_capabilities.py"
      provides: "RunCapabilities ORM model (table run_capabilities)"
      contains: "__tablename__ = \"run_capabilities\""
    - path: "backend/alembic/versions/0014_typed_artifacts_persistence.py"
      provides: "additive migration 0014 + default-workspace backfill"
      contains: "revision = \"0014\""
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "the 4 new models"
      via: "import for Base.metadata visibility (Pitfall 5)"
      pattern: "from app.models.artifact_ref import"
    - from: "backend/alembic/versions/0014_typed_artifacts_persistence.py"
      to: "0013"
      via: "down_revision chain"
      pattern: "down_revision = \"0013\""
---

<objective>
Land the additive §18 persistence schema (PERSIST-01). Add the four new ORM models (`artifact_refs`, `workspaces`, `run_events`, `run_capabilities`), EXTEND `workflow_runs` (+`owner_id`, `workspace_id`, `source_run_id`, `plan_id`, `budget_snapshot_json`) and the EXISTING `workflows` table (+`owner_id`, `workspace_id`, `source`, `manifest_json`, `version`), register all new models in `app/models/__init__.py`, and hand-author Alembic revision `0014_typed_artifacts_persistence` (chained off `0013`) with a default-workspace backfill. This is the schema substrate the store helper (05-03) queries and the engine (05-04/05-05) populates. NO drop here — the destructive thin-store DROP is `0015`, sequenced LAST in 05-06 (D-03).

Purpose: Schema-first so the store helper has real tables/columns to query and the migration up/down + backfill is provable in isolation before any engine rewiring.
Output: 4 new model files, 2 extended model files, `__init__.py` registration, `0014` migration + backfill, and the `test_migration_0014.py` Wave 0 suite (PERSIST-01 up/down/index/owner_id+workspace_id assertions).

Deviation from CONTEXT premise (workflows collision): `app/models/workflow_definition.py` ALREADY owns `__tablename__ = "workflows"` (model `WorkflowDefinition`). We EXTEND it additively — we do NOT create a second `workflows` table (RESEARCH #5). DB-authored user workflows are v2; `workflows` lands as file-backed manifest metadata only.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-SPEC.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-CONTEXT.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (05-02) creates:
- ORM models / tables: `ArtifactRef` (`artifact_refs`), `Workspace` (`workspaces`), `RunEvent` (`run_events`), `RunCapabilities` (`run_capabilities`)
- New columns on `workflow_runs`: `owner_id`, `workspace_id`, `source_run_id`, `plan_id`, `budget_snapshot_json`
- New columns on `workflows` (existing `WorkflowDefinition`): `owner_id`, `workspace_id`, `source`, `manifest_json`, `version`
- New columns on `run_events`: `seq` (monotonic per-run, populated by the engine sink in 05-04), `event_id`
- Alembic revision ids `0014` (this plan) — `0015` is created in 05-06
- `app/models/__init__.py` imports for the 4 new models
(`ExecutionContext.artifacts`/`workspace_id`, the `ArtifactGraph` dataclass, and the API endpoints are created in other plans, not here.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 4 new ORM models + extend workflow_runs & workflows + register in __init__</name>
  <files>backend/app/models/artifact_ref.py, backend/app/models/workspace.py, backend/app/models/run_event.py, backend/app/models/run_capabilities.py, backend/app/models/workflow.py, backend/app/models/workflow_definition.py, backend/app/models/__init__.py</files>
  <read_first>
    - backend/app/models/artifact.py (lines 1–37 — the EXACT WorkflowArtifact precedent to extend into ArtifactRef: PK lambda, created_at lambda, Index, relationship)
    - backend/app/models/workflow.py (lines 12–53 — WorkflowRun to extend; note existing user_id FK, session_id, parent_run_id, and the `artifacts` relationship → WorkflowArtifact that must be repointed/handled)
    - backend/app/models/workflow_definition.py (the EXISTING __tablename__="workflows" to EXTEND — RESEARCH #5)
    - backend/app/models/__init__.py (lines 1–13 — the import-registration block; new models MUST be imported here per Pitfall 5)
    - specs/003-workflow-engine-decoupling/plan.md (§18 lines 697–717 — the table-by-table column lists; §6 lines 454–466 — ArtifactRef fields)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-PATTERNS.md ("app/models/artifact_ref.py" + "ORM model conventions" shared pattern)
  </read_first>
  <action>
    Create `app/models/artifact_ref.py` — `class ArtifactRef(Base)`, `__tablename__ = "artifact_refs"`. Columns mirroring the §6:454–466 / ArtifactRef dataclass (one name everywhere, snake_case): `id` (String PK, `default=lambda: str(uuid.uuid4())`), `run_id` (String, FK `workflow_runs.id`, NOT NULL), `owner_id` (String, NOT NULL — AUTHZ-01), `workspace_id` (String, NOT NULL — AUTHZ-01), `kind` (String, NOT NULL), `producer_step` (String, NOT NULL), `producer_agent` (String, NOT NULL), `task_id` (String, nullable), `content` (Text, NOT NULL — inline, D-01), `content_hash` (String, NOT NULL), `location` (String, NOT NULL), `version` (Integer, NOT NULL), `parents` (JSON, default list — lineage), `derived_from` (String, FK `artifact_refs.id`, nullable), `visibility` (String, NOT NULL, default `"private"`, server_default `"private"`), `retention` (String, NOT NULL, default `"run_ttl"`, server_default `"run_ttl"`), `created_at` (DateTime, `datetime.now(timezone.utc)` lambda). `__table_args__`: `Index("ix_artifact_refs_run_kind", "run_id", "kind")`, `Index("ix_artifact_refs_content_hash", "content_hash")`.
    Create `app/models/workspace.py` — `class Workspace(Base)`, `__tablename__ = "workspaces"`. Columns (§18:706): `id` (String PK uuid), `owner_id` (String, NOT NULL), `workspace_id` (String, NOT NULL — self-id carries the AUTHZ-01 pair; set equal to `id` at creation for the default per-run workspace), `kind` (String, NOT NULL, default `"sandbox"`), `runtime` (String, NOT NULL, default `"local"`), `repo_id` (String, nullable — repositories is Phase 9), `ttl` (String or Integer, default representing run_ttl), `created_at` (DateTime lambda). (Note: `workspace_id` on the workspaces row equals its own `id` so the uniform default-deny filter `workspace_id = :ws` holds for workspace reads too.)
    Create `app/models/run_event.py` — `class RunEvent(Base)`, `__tablename__ = "run_events"`. Columns (§18:712 + AUTHZ-01): `id` (String PK uuid), `run_id` (String, FK `workflow_runs.id`, NOT NULL), `owner_id` (String, NOT NULL), `workspace_id` (String, NOT NULL), `seq` (Integer, NOT NULL — monotonic per run, stamped by the engine sink in 05-04), `event_id` (String, NOT NULL — generated uuid, idempotent replay), `type` (String, NOT NULL), `payload_json` (JSON, NOT NULL), `created_at` (DateTime lambda). `__table_args__`: `Index("ix_run_events_run_seq", "run_id", "seq")`.
    Create `app/models/run_capabilities.py` — `class RunCapabilities(Base)`, `__tablename__ = "run_capabilities"`. Columns (§18:713 + AUTHZ-01): `id` (String PK uuid), `run_id` (String, FK `workflow_runs.id`, NOT NULL), `owner_id` (String, NOT NULL), `workspace_id` (String, NOT NULL), `runtime` (String, NOT NULL — populated `langchain_deepagents` in 05-04), `model_overrides` (JSON, nullable — Phase 6), `skills` (JSON, nullable — Phase 8), `hooks` (JSON, nullable — Phase 8), `integrations` (JSON, nullable — Phase 9), `mcp_servers` (JSON, nullable — Phase 9), `versions` (JSON, nullable), `created_at` (DateTime lambda).
    EXTEND `app/models/workflow.py` `WorkflowRun`: additively add `owner_id` (String, nullable — backfilled), `workspace_id` (String, nullable — backfilled), `source_run_id` (String, nullable), `plan_id` (String, nullable — Phase 4 forward field), `budget_snapshot_json` (JSON, nullable — Phase 11). Leave existing `user_id`/`session_id`/`parent_run_id`/`status` untouched. Keep the existing `artifacts` relationship pointing at `WorkflowArtifact` for now (it is deleted in 05-06; do NOT break it this plan — both models coexist during dual-write).
    EXTEND `app/models/workflow_definition.py` `WorkflowDefinition` (existing `__tablename__="workflows"`): additively add `owner_id` (String, nullable — file-backed manifests have no owner yet), `workspace_id` (String, nullable), `source` (String, NOT NULL, default `"file"`, server_default `"file"`), `manifest_json` (JSON, nullable), `version` (Integer, NOT NULL, default 1, server_default `"1"`). Keep legacy `agents`/`artifact_edges`/`constitution_ref`/`user_id` (nullable-safe). Do NOT create a second `workflows` table.
    Register all four new models in `app/models/__init__.py` (import + add to `__all__`) so `Base.metadata` sees them for Alembic (Pitfall 5).
    Commit: `feat(db): add artifact_refs/workspaces/run_events/run_capabilities models + extend workflow_runs & workflows (PERSIST-01, AUTHZ-01)`.
  </action>
  <verify>
    <automated>cd backend && python3.11 -c "import app.models as m; assert m.ArtifactRef.__tablename__=='artifact_refs'; assert m.Workspace.__tablename__=='workspaces'; assert m.RunEvent.__tablename__=='run_events'; assert m.RunCapabilities.__tablename__=='run_capabilities'; print('models-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -c "import app.models"` imports without error (no duplicate `__tablename__`, no mapper resolution failure).
    - `grep -c "owner_id" backend/app/models/artifact_ref.py backend/app/models/workspace.py backend/app/models/run_event.py backend/app/models/run_capabilities.py` shows ≥1 per file (owner_id present on every new table — AUTHZ-01).
    - `grep -c "workspace_id" backend/app/models/run_event.py backend/app/models/run_capabilities.py backend/app/models/artifact_ref.py` shows ≥1 per file.
    - `grep -nE "Index\(\"ix_artifact_refs_run_kind\"|Index\(\"ix_artifact_refs_content_hash\"" backend/app/models/artifact_ref.py` returns both indexes.
    - `grep -nE "from app.models.(artifact_ref|workspace|run_event|run_capabilities) import" backend/app/models/__init__.py` returns all four imports.
    - `grep -c "__tablename__" backend/app/models/workflow_definition.py` returns exactly 1 (no second workflows table — RESEARCH #5).
  </acceptance_criteria>
  <done>Four new models created + two extended + all registered; `import app.models` clean; owner_id+workspace_id present on every new table; required indexes declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hand-author 0014 migration + default-workspace backfill + Wave 0 up/down test</name>
  <files>backend/alembic/versions/0014_typed_artifacts_persistence.py, backend/tests/unit/test_migration_0014.py</files>
  <read_first>
    - backend/alembic/versions/0013_collapse_pipeline_run_id.py (the revision header + op.batch_alter_table idiom + downgrade reversal pattern to copy)
    - backend/alembic/env.py (target_metadata = Base.metadata; compare_type/compare_server_default; the autogenerate cross-check only)
    - backend/app/models/database.py (SQLite dev FK PRAGMA vs Postgres prod — why op.batch_alter_table is mandatory for column adds)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md ("Alembic revision pattern + autogenerate vs hand-author"; Pitfall 5)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-VALIDATION.md (PERSIST-01 row — `pytest tests/unit/test_migration_0014.py -x`)
  </read_first>
  <action>
    Hand-author `alembic/versions/0014_typed_artifacts_persistence.py`: module-level `revision = "0014"`, `down_revision = "0013"`, `branch_labels = None`, `depends_on = None`.
    `upgrade()`: `op.create_table` for `artifact_refs`, `workspaces`, `run_events`, `run_capabilities` with the exact columns from Task 1 (additive — no batch needed for create_table). Create the indexes: `op.create_index("ix_artifact_refs_run_kind", "artifact_refs", ["run_id","kind"])`, `op.create_index("ix_artifact_refs_content_hash", "artifact_refs", ["content_hash"])`, `op.create_index("ix_run_events_run_seq", "run_events", ["run_id","seq"])`. Use `op.batch_alter_table("workflow_runs")` to `add_column` `owner_id`, `workspace_id`, `source_run_id`, `plan_id` (String, nullable) and `budget_snapshot_json` (JSON, nullable). Use `op.batch_alter_table("workflows")` to `add_column` `owner_id`, `workspace_id` (String, nullable), `source` (String, server_default `"file"`), `manifest_json` (JSON, nullable), `version` (Integer, server_default `"1"`). All new columns nullable/defaulted so upgrade applies over existing rows.
    Default-workspace backfill (data step inside `0014` — Claude's discretion D-03; choosing inline over a separate 0014b): after the schema ops, for every existing `workflow_runs` row, INSERT one `workspaces` row (`id=<new uuid>`, `owner_id=<that run's user_id>`, `workspace_id=<same id>`, `kind="sandbox"`, `runtime="local"`, `ttl=run_ttl`) and UPDATE the run to set `owner_id = user_id` and `workspace_id = <that workspace id>`. Implement via `op.get_bind()` + a SQLAlchemy core `select`/`insert`/`update` over the live connection (works on SQLite + Postgres); guard against zero existing rows. Every existing run MUST end with a non-null `workspace_id` + `owner_id` (Highest-Risk Behavior 3).
    `downgrade()`: reverse cleanly — drop the workflows/workflow_runs added columns (batch), drop the indexes, drop the four tables. (The backfilled workspaces rows vanish with the table drop; downgrade restores the 0013 schema.)
    Write `tests/unit/test_migration_0014.py`: programmatically run `alembic upgrade head` from a DB at `0013` on a temp SQLite DB, assert (a) the four tables exist, (b) `workflow_runs`/`workflows` have the new columns, (c) the two artifact_refs indexes + the run_events index exist, (d) every new table has `owner_id`+`workspace_id` columns, (e) a pre-seeded existing run ends with non-null `owner_id`+`workspace_id` after backfill, then run `alembic downgrade` to `0013` and assert the four tables are gone + the added columns are gone. Use the Alembic Python API (`command.upgrade`/`command.downgrade` with a `Config` pointed at the temp DB) per the existing test conventions.
    Commit: `feat(db): 0014 additive migration + default-workspace backfill (PERSIST-01)`.
    Do NOT drop `workflow_artifacts` here (that is `0015`, in 05-06).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_migration_0014.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/unit/test_migration_0014.py -x` passes (upgrade from 0013, downgrade reverses, indexes + owner_id/workspace_id asserted, backfill gives every existing run a workspace_id).
    - `grep -nE "revision = \"0014\"|down_revision = \"0013\"" backend/alembic/versions/0014_typed_artifacts_persistence.py` returns both.
    - `grep -cE "op.create_table\(" backend/alembic/versions/0014_typed_artifacts_persistence.py` returns 4 (artifact_refs, workspaces, run_events, run_capabilities).
    - `grep -c "op.drop_table(\"workflow_artifacts\")" backend/alembic/versions/0014_typed_artifacts_persistence.py` returns 0 (the DROP is 0015, not here).
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` shows no regression in pre-existing suites (additive migration; no behavior change).
  </acceptance_criteria>
  <done>0014 applies cleanly from 0013 and downgrades cleanly; every existing run is backfilled with owner_id+workspace_id; indexes present; no destructive change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → DB | The backfill writes to the production schema; an incorrect backfill could leave runs unscoped (no workspace_id), defeating default-deny. |
| ORM column defaults → reads | `owner_id`/`workspace_id` NOT NULL on new tables vs nullable (backfilled) on extended tables — a NULL owner_id on an extended row would bypass scope filters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-BACKFILL | Tampering / Info Disclosure | `0014` default-workspace backfill | mitigate | Backfill sets `owner_id = user_id` + a per-run workspace for EVERY existing run; Task 2 test asserts no existing run is left without owner_id/workspace_id (uniform scoping — Highest-Risk Behavior 3). |
| T-5-MIGRATE-DOWN | Denial of Service | `downgrade()` | mitigate | `downgrade` reverses cleanly to `0013` (asserted by Task 2 test); no irreversible op in `0014` (the one sanctioned DROP is `0015`, 05-06). |
| T-5-NULLOWNER | Elevation | new NOT NULL owner_id/workspace_id on `artifact_refs`/`run_events`/`run_capabilities` | mitigate | Columns are NOT NULL on new tables so no row can be persisted unscoped; the engine (05-04) always supplies a real principal (never None — AUTHZ-03). |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit — N/A). |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_migration_0014.py -x` green.
- `cd backend && python3.11 -c "import app.models"` clean.
- `cd backend && lint-imports` exits 0 (models are `app.*`, unaffected by kernel scaffold).
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` no new failures.
</verification>

<success_criteria>
- Additive `0014` adds the 4 tables + extends `workflow_runs` & `workflows`; every new table carries owner_id+workspace_id; required indexes present.
- `alembic upgrade head` from 0013 + `downgrade` both clean; backfill scopes every existing run.
- No destructive change (DROP deferred to 0015 / 05-06).
</success_criteria>

<output>
Create `.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-02-SUMMARY.md` when done.
</output>
