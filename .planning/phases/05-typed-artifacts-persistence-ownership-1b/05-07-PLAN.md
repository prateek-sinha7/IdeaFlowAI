---
phase: 05-typed-artifacts-persistence-ownership-1b
plan: 07
type: execute
wave: 6
depends_on: [05-06]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/agents/execution_engine/context.py
  - backend/agents/artifact_store/store.py
  - backend/app/models/artifact.py
  - backend/app/models/workflow.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/0015_drop_thin_artifact_store.py
  - specs/003-workflow-engine-decoupling/migration-ledger.md
  - backend/tests/agents/test_migration_ledger.py
autonomous: false
requirements: [PERSIST-02]
must_haves:
  truths:
    - "the parity gate is green BEFORE any deletion: prototype/od_prototype/prototype_revision/ppt/code-gen deliverables byte-identical + semantic-event parity, with ALL thin-store artifact consumers already migrated in 05-06"
    - "grep accumulated_outputs in backend/agents backend/app (non-test) returns 0; ExecutionContext.accumulated_outputs is removed"
    - "the thin-store artifact-persistence half (store/retrieve_latest/retrieve_version/list_by_type/list_lineage/_store_in_memory/_to_dict + _use_db/_mem_* fields) + WorkflowArtifact + workflow_artifacts are deleted; the in-memory HITL asyncio.Event half still works"
    - "alembic 0015 drops workflow_artifacts and is sequenced last; upgrade head + downgrade reverse cleanly"
    - "migration-ledger L15 flips to ☑ + a thin-store deletion gate is added; the ledger test flip set is updated to {L14,L15,L16} and stays green; L16 stays green"
  artifacts:
    - path: "backend/alembic/versions/0015_drop_thin_artifact_store.py"
      provides: "destructive DROP workflow_artifacts (the one sanctioned non-additive change)"
      contains: "revision = \"0015\""
    - path: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      provides: "L15 flipped to done + thin-store deletion gate row"
      contains: "L15"
  key_links:
    - from: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      to: "backend/tests/agents/test_migration_ledger.py"
      via: "L15 done arms the accumulated_outputs grep ratchet -> 0; flip-set assertion updated"
      pattern: "L15"
    - from: "backend/agents/artifact_store/store.py"
      to: "HITL asyncio.Event half"
      via: "get_resume_event/set_questionnaire_responses/get_review_event/set_review_response preserved"
      pattern: "get_resume_event"
---

<objective>
Complete the strangler cutover (PERSIST-02, D-13 step 5): once the 0A characterization parity gate is GREEN — and now that 05-06 has migrated EVERY thin-store artifact-method consumer onto the persisted typed layer — DELETE the `accumulated_outputs` mirror (flip ledger L15 to done), DELETE the thin-store artifact-persistence half + `WorkflowArtifact` + the `workflow_artifacts` table (apply `0015`), and update the migration-ledger test flip set. The in-memory `asyncio.Event` HITL half of the store is KEPT (Phase 8 owns HITL). Adding the typed graph without deleting what it supersedes means the phase is NOT done (INV-3/INV-12) — this plan is what makes the phase done.

PRECONDITION NOW TRUE (the gap that blocked the prior 05-06): all six thin-store artifact-method consumers (`_run_planner`, `_run_agent` produces-loop, `_handle_revision` reads+write, `clarify_engine` clarifications write, `websocket` reconnect read) were migrated to `ScopedStore`/`artifact_refs` in 05-06 — so deleting the thin-store artifact methods here breaks NO live consumer. Task 1 re-asserts this with a grep that the engine/clarify/websocket surface has ZERO live thin-store artifact-method calls before any deletion.

The parity gate (Task 1) is `[BLOCKING]`: if any deliverable byte or semantic-event snapshot diverges, or any consumer still calls a thin-store artifact method, STOP and fix-forward in 05-06 — NEVER ship with the mirror alive. Deletion (Tasks 2-3) only proceeds after Task 1 is green.

Purpose: One artifact implementation remains. This is the deletion-as-exit-gate (§31) that closes L15 and the thin store.
Output: mirror removed, thin store artifact half + model + table dropped, `0015` migration, ledger L15 done + thin-store gate, the updated ledger test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-SPEC.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-CONTEXT.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-PATTERNS.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-06-SUMMARY.md
@specs/003-workflow-engine-decoupling/migration-ledger.md
</context>

<artifacts_this_phase_produces>
This plan (05-07):
- DELETES `ExecutionContext.accumulated_outputs` (mirror), all non-test `accumulated_outputs` references in `backend/agents`/`backend/app`
- DELETES the thin-store artifact-persistence half (`store`, `retrieve_latest`, `retrieve_version`, `list_by_type`, `list_lineage`, `_store_in_memory`, `_to_dict`, the `_use_db`/`_mem_*` fields, `ArtifactStoreWriteError` if now unused), `app/models/artifact.py` `WorkflowArtifact`, and the `workflow_artifacts` table (alembic `0015`)
- KEEPS the HITL `asyncio.Event` half + `get_artifact_store` singleton
- creates alembic revision id `0015`
- flips ledger `L15` to done + adds a thin-store deletion gate row; updates `test_migration_ledger.py` flip-set assertion to `{L14, L15, L16}` + `_REQUIRED_ITEMS`
- removes the `WorkflowRun.artifacts` -> `WorkflowArtifact` relationship + the `WorkflowArtifact` import in `app/models/__init__.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: [BLOCKING] Parity gate — 0A characterization byte + semantic-event green AND zero live thin-store consumers, before ANY deletion</name>
  <files>(read-only gate — no production file edits)</files>
  <read_first>
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-06-SUMMARY.md (CONFIRM all six consumers migrated to ScopedStore/artifact_refs — the precondition this gate enforces)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md (Validation Architecture -> Sampling Rate phase gate; Pitfall 4)
    - backend/tests/agents/characterization/_normalize.py (confirm seq/event_id are in _VOLATILE_STRIP_KEYS from 05-04 before relying on parity)
  </read_first>
  <action>
    This is the [BLOCKING] parity gate that guards the deletion (D-13 step 5). It does TWO things:

    (a) PRECONDITION re-assert (the gap fix): grep that the engine/clarify/websocket surface has ZERO live thin-store artifact-method calls — `grep -rnE "self\._store\.(store|retrieve_latest|retrieve_version|list_by_type|list_lineage)\(" agents/execution_engine/ app/api/websocket.py` MUST return 0. If it returns ANY match, the consumers are NOT fully migrated — STOP, do NOT delete, fix-forward in 05-06. This is the precondition that the prior 05-06 plan falsely assumed.

    (b) PARITY: run the full 0A characterization suite + the migration-ledger + L16 denial + the Wave-0 suites + the migrated revision/clarifications suites. The typed reads/writes are now the SOLE artifact path on the migrated consumers (05-06); the mirror + thin-store artifact methods are still physically present but UNUSED. The gate proves the typed reads produced byte-identical deliverables + semantic-event parity. If ANY snapshot diverges (deliverable bytes OR the canonical-JSON event multiset OR seq contiguity), STOP — do NOT proceed to Task 2/3; fix-forward in 05-06 and re-run this gate. Do NOT regenerate goldens via SNAPSHOT_UPDATE here — a snapshot change at this gate means a real divergence to fix, not to rebaseline.

    Only when BOTH (a) and (b) are green does the deletion proceed.
  </action>
  <verify>
    <automated>cd backend && grep -rnE "self\._store\.(store|retrieve_latest|retrieve_version|list_by_type|list_lineage)\(" agents/execution_engine/ app/api/websocket.py | grep -v "^#" ; test $? -ne 0 && echo "CONSUMERS-MIGRATED" && cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_characterization_*.py tests/agents/test_migration_ledger.py tests/agents/test_parent_run_ownership.py tests/agents/test_artifact_graph.py tests/unit/test_migration_0014.py tests/unit/test_run_events.py tests/unit/test_run_capabilities.py tests/unit/test_runs_api_artifacts.py tests/unit/test_runs_api_events.py tests/unit/test_revision_intelligence.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -rnE "self\._store\.(store|retrieve_latest|retrieve_version|list_by_type|list_lineage)\(" agents/execution_engine/ app/api/websocket.py` returns 0 (PRECONDITION TRUE — no live thin-store artifact consumer; the prior 05-06's false premise is now disproven by grep).
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` is GREEN (deliverable byte-identity + semantic-event parity for prototype/od_prototype/prototype_revision/ppt/code-gen).
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py tests/agents/test_parent_run_ownership.py tests/unit/test_revision_intelligence.py -x` is GREEN (L16 still green; ledger parses; the migrated revision suite green).
    - All Wave-0 suites (artifact_graph, migration_0014, run_events, run_capabilities, runs_api_artifacts, runs_api_events) GREEN.
    - If any of the above is RED, the plan HALTS at this task (no deletion); the divergence/regression is fixed in 05-06 and the gate is re-run.
  </acceptance_criteria>
  <done>The precondition (zero live thin-store consumers) is proven by grep AND the full parity + correctness suite is green with the mirror still alive — the cutover is proven safe and deletion may proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the accumulated_outputs mirror + the thin-store artifact half (keep the HITL half)</name>
  <files>backend/agents/execution_engine/engine.py, backend/agents/execution_engine/context.py, backend/agents/artifact_store/store.py, backend/app/models/artifact.py, backend/app/models/workflow.py, backend/app/models/__init__.py</files>
  <read_first>
    - backend/agents/execution_engine/context.py (line ~85 — the `accumulated_outputs` field to remove)
    - backend/agents/execution_engine/engine.py (the dual-write mirror write-sites from 05-04 — remove the mirror writes now that typed reads are proven + consumers migrated; keep the typed `ctx.artifacts` writes/reads; confirm the `produces`-loop and `_handle_revision` are already off the thin store from 05-06)
    - backend/agents/artifact_store/store.py (the seam from PATTERNS "REWIRED -> DELETED files": DELETE store 50-127 / retrieve_latest 129-164 / retrieve_version 165-188 / list_by_type 190-220 / list_lineage 222-248 / _store_in_memory 264-295 / _to_dict 359-373 + _use_db/_mem_* fields in __init__ 254-262; KEEP get_resume_event 300-307 / set_questionnaire_responses 309-321 / get_questionnaire_responses 323-328 / get_review_event 334-339 / set_review_response 341-347 / get_review_response 349-353 / get_artifact_store 380-391 + the __init__ event registries 255-257)
    - backend/app/models/artifact.py (the WorkflowArtifact model to delete)
    - backend/app/models/workflow.py (line ~52 — the `artifacts` relationship -> WorkflowArtifact to remove)
    - backend/app/models/__init__.py (the WorkflowArtifact import + __all__ entry to remove)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md ("Thin-store consumer map" + the accumulated_outputs read/write site list + Pitfall 2 grep-scope)
  </read_first>
  <action>
    Remove the `accumulated_outputs` mirror entirely now that the typed graph is the proven source of truth and all consumers are migrated (05-06): delete the `accumulated_outputs` field from `ExecutionContext` (context.py ~85) and delete every remaining mirror write/read in `engine.py` (the dual-write sites added in 05-04 — writes 1500/1571/1599/1721/1756 originals; reads 1308/1331/1637/1664/1783-1784/2426/2472-2473 originals). Keep the typed `ctx.artifacts` writes/reads (the `_latest_typed_content`/`_filter_consumed_outputs` typed path becomes typed-ONLY — drop the `mirror` fallback parameter and its call-args). Ensure NO non-test reference to `accumulated_outputs` remains in `backend/agents`/`backend/app` (scrub before flipping the ledger — Pitfall 2). The build-loop scratch keys (`_build_task_number`/`_build_task_total`) were already moved off the dict in 05-04 — confirm they are not reintroduced.

    Delete the thin-store artifact-persistence half from `agents/artifact_store/store.py` per the PATTERNS seam: remove `store`/`retrieve_latest`/`retrieve_version`/`list_by_type`/`list_lineage`/`_store_in_memory`/`_to_dict` + the `_use_db`/`_mem_artifacts`/`_mem_by_type` fields (and `ArtifactStoreWriteError` if it has no remaining importer — grep first; the engine dropped its `ArtifactStoreWriteError` import path in 05-06). KEEP the in-memory HITL `asyncio.Event` half (`get_resume_event`/`set_questionnaire_responses`/`get_questionnaire_responses`/`get_review_event`/`set_review_response`/`get_review_response`) + `get_artifact_store` + the `__init__` event registries (`_resume_events`/`_questionnaire_responses`). (All artifact-store consumers were rewired to the typed graph/ScopedStore in 05-06; this only removes the now-dead methods.)

    Delete `app/models/artifact.py` (the `WorkflowArtifact` model). Remove the `artifacts` relationship -> `WorkflowArtifact` from `WorkflowRun` (workflow.py ~52) and remove the `WorkflowArtifact` import + `__all__` entry from `app/models/__init__.py`.

    Run the full suite + import-linter + vulture (vulture will flag any newly-dead code left over).
    Commit: `refactor(engine): delete accumulated_outputs mirror` and `refactor(sandbox): delete thin-store artifact half + WorkflowArtifact (keep HITL) (PERSIST-02)`.
  </action>
  <verify>
    <automated>cd backend && grep -rn "accumulated_outputs" agents app --include=*.py | grep -v "/tests/" ; test $? -ne 0 && echo "MIRROR-GONE" && cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/ tests/unit/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -rn "accumulated_outputs" agents app --include=*.py` returns 0 matches (non-test mirror gone — PERSIST-02 acceptance; SPEC scope `backend/agents backend/app`).
    - `cd backend && grep -rnE "def (store|retrieve_latest|retrieve_version|list_by_type|list_lineage)\b" agents/artifact_store/store.py` returns 0 (thin-store artifact half deleted).
    - `cd backend && grep -nE "get_resume_event|set_questionnaire_responses|get_review_event|set_review_response" agents/artifact_store/store.py` returns ≥4 (HITL half intact).
    - `test ! -f backend/app/models/artifact.py` and `cd backend && grep -nE "WorkflowArtifact" app/models/__init__.py` returns 0.
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -x` is GREEN incl. characterization (deliverables still byte-identical after the mirror is gone — typed graph is now sole source).
    - `cd backend && python3.11 -m pytest tests/agents/ -k "questionnaire or review" -x` (or the HITL/clarify suite) stays green — `submit_questionnaire`/`approve_review` still function.
    - `cd backend && lint-imports` exits 0 and `vulture app/ agents/` reports no new dead-code regression introduced by the deletions.
  </acceptance_criteria>
  <done>The mirror is gone (grep -> 0 non-test), the thin-store artifact half + WorkflowArtifact model are deleted, the HITL half + _handle_revision (now on ScopedStore) still work, and the full suite incl. characterization is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply 0015 DROP workflow_artifacts + flip ledger L15 + thin-store gate + update ledger test (landmine #1)</name>
  <files>backend/alembic/versions/0015_drop_thin_artifact_store.py, specs/003-workflow-engine-decoupling/migration-ledger.md, backend/tests/agents/test_migration_ledger.py</files>
  <read_first>
    - backend/alembic/versions/0013_collapse_pipeline_run_id.py (revision header pattern) + the 05-02 0014 file (so 0015.down_revision="0014")
    - specs/003-workflow-engine-decoupling/migration-ledger.md (the L15 row at line 63 to flip to ☑; the gate-cell grep-vs-CHECK convention at lines 25-35; where to add the thin-store deletion gate row)
    - backend/tests/agents/test_migration_ledger.py (the `_REQUIRED_ITEMS` list at lines 35-38; `_checked_grep_rows` at 90-103; and the HARD-CODED `flipped == ["L14","L16"]` assertion at 143-146 that WILL break; the grep ratchet at 114-122 runs over ALL of backend/ incl. tests — Pitfall 1/2)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-PATTERNS.md ("tests/agents/test_migration_ledger.py — the L15-flip break")
  </read_first>
  <action>
    Hand-author `alembic/versions/0015_drop_thin_artifact_store.py`: `revision = "0015"`, `down_revision = "0014"`, `branch_labels = None`, `depends_on = None`. `upgrade()`: `op.drop_table("workflow_artifacts")` (works on SQLite + Postgres). `downgrade()`: recreate the `workflow_artifacts` table with the original `WorkflowArtifact` column shape (id String PK, workflow_run_id String FK→workflow_runs.id, type, name, content Text, version Integer, schema_version String default "1.0", producing_agent_id, derived_from_artifact_id String FK→workflow_artifacts.id nullable, created_at DateTime) + the `ix_workflow_artifacts_run_type` index, so downgrade reverses cleanly per Q3. Sequenced LAST in the phase (D-03) — applied only now that the mirror is gone (Task 2) and parity is green (Task 1).

    Flip migration-ledger `L15` to ☑ — this arms the `accumulated_outputs` grep ratchet (`grep -rnE accumulated_outputs backend/ --include=*.py` -> 0). Add a thin-store deletion gate row (D-05) for the thin store: a grep row whose pattern matches the deleted artifact-persistence surface (e.g. `WorkflowArtifact|workflow_artifacts`) -> 0 in backend/. Keep the gate-cell convention (grep row, metacharacter-aware) so the parser classifies it correctly. Add the new row id to `_REQUIRED_ITEMS` in the test. Keep L16 as a CHECK row (still green).

    Update test landmine #1: in `test_migration_ledger.py` rename `test_ledger_parses_and_phase0b_flips_l14_l16` (e.g. to `..._phase1b_flips_l14_l15_l16`) and change the assertion `flipped == ["L14","L16"]` -> `flipped == ["L14","L15","L16"]` (sorted) so it passes with L15 flipped. Ensure the new thin-store gate row (if its status is ☑) is consistent with the flip-set assertion (include it in the expected sorted list since the deletion is complete this phase; update the expected list accordingly). The grep ratchet runs over ALL of `backend/` incl. tests, so confirm no non-test `accumulated_outputs` remains (already 0 from Task 2) and scrub/adjust any lingering test references that would trip the whole-backend grep before flipping (Pitfall 2).
    Commit: `feat(db): 0015 drop workflow_artifacts (thin store)` and `test(tests): flip L15 + thin-store deletion gate; update ledger flip set to {L14,L15,L16} (PERSIST-02)`.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py tests/unit/test_migration_0014.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` is GREEN with the updated flip set (`{L14, L15, L16}` and, if marked ☑, the thin-store gate) and the armed `accumulated_outputs` grep ratchet returning 0.
    - `grep -nE "revision = \"0015\"|down_revision = \"0014\"" backend/alembic/versions/0015_drop_thin_artifact_store.py` returns both.
    - `grep -c "op.drop_table(\"workflow_artifacts\")" backend/alembic/versions/0015_drop_thin_artifact_store.py` returns 1.
    - `grep -nE "L15" specs/003-workflow-engine-decoupling/migration-ledger.md` shows the row flipped to ☑ and a thin-store deletion gate row is present.
    - `grep -nE "flipped == \[\"L14\",\"L16\"\]|flipped == \[\"L14\", \"L16\"\]" backend/tests/agents/test_migration_ledger.py` returns 0 (the old hard-coded assertion is gone; replaced by the {L14,L15,L16} set).
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` no new failures; `lint-imports` exits 0.
  </acceptance_criteria>
  <done>0015 drops workflow_artifacts (reversible); ledger L15 flipped + thin-store gate added; the ledger test flip-set assertion updated to {L14,L15,L16} and green; the accumulated_outputs ratchet is armed and returns 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion vs parity | The destructive DROP + mirror removal must not ship before parity is proven AND all consumers are migrated (05-06) — a premature deletion would break a live thin-store consumer or lose the only fallback if the typed reads diverged. |
| migration runner -> DB | `0015` drops a table; an irreversible/incorrect downgrade would prevent rollback (Q3 reversibility). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-CUTOVER | Denial of Service / data loss | mirror + thin-store deletion | mitigate | Deletion is GATED on Task 1: (a) grep proves ZERO live thin-store artifact consumers (the 05-06 migration is complete — the prior plan's false premise is disproven), and (b) the 0A parity suite is BLOCKING-green; fix-forward in 05-06, never ship-with-mirror-alive (SPEC constraint); `0015` is sequenced LAST (D-03). |
| T-5-DROP-DOWN | Denial of Service | `0015` downgrade | mitigate | `downgrade()` recreates `workflow_artifacts` with the original shape + index so the DROP is reversible (Q3); the additive `0014` remains independently reversible. |
| T-5-HITL | Denial of Service | thin-store HITL half | mitigate | Only the artifact-persistence methods are removed; the `asyncio.Event` HITL half + `get_artifact_store` are explicitly preserved and asserted (Task 2) so `submit_questionnaire`/`approve_review` keep working. |
| T-5-SEED | Information Disclosure | `_handle_revision` cross-run reads (now on ScopedStore) | mitigate | The cross-owner gate is UNCHANGED by this deletion — `_handle_revision` uses `ScopedStore.assert_owns(parent_run_id)` (wired in 05-06); deleting the thin store does not touch that gate. INV-3 byte-identity preserved (same-owner reads unchanged). |
| T-5-RATCHET | Tampering (regression) | ledger L15 ratchet | mitigate | Flipping L15 arms the permanent `accumulated_outputs` grep ratchet (DEL-03) — reintroducing the mirror fails CI; the thin-store gate does the same for the deleted store surface. |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit — N/A). |
</threat_model>

<verification>
- Task 1 [BLOCKING]: zero live thin-store artifact consumers (grep) AND full characterization + ledger + denial + Wave-0 + revision suites green BEFORE deletion.
- `cd backend && grep -rn "accumulated_outputs" agents app --include=*.py` -> 0 (non-test).
- Thin-store artifact half + WorkflowArtifact + workflow_artifacts gone; HITL half + `_handle_revision` (on ScopedStore) intact.
- `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` green with flip set {L14,L15,L16}.
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` + `lint-imports` + `vulture` clean.
</verification>

<success_criteria>
- Precondition proven (zero live thin-store consumers — the 05-06 migration is complete) AND parity proven green (BLOCKING) before any deletion.
- The `accumulated_outputs` mirror AND the thin-store artifact half (+ WorkflowArtifact + workflow_artifacts) are deleted; HITL half preserved.
- `0015` drops the table (reversible), sequenced last; ledger L15 flipped + thin-store gate; ledger test updated to {L14,L15,L16} and green.
- One artifact implementation remains (INV-3/INV-12) — the phase is done.
</success_criteria>

<output>
Create `.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-07-SUMMARY.md` when done.
</output>
