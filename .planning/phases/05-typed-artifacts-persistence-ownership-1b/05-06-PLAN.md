---
phase: 05-typed-artifacts-persistence-ownership-1b
plan: 06
type: execute
wave: 5
depends_on: [05-04, 05-05]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/agents/execution_engine/clarify_engine.py
  - backend/app/api/websocket.py
  - backend/tests/unit/test_revision_intelligence.py
autonomous: true
requirements: [ART-01, ART-03, PERSIST-02]
must_haves:
  truths:
    - "every thin-store artifact-method consumer (_run_planner planning_context write, _run_agent produces-loop write, _handle_revision cross-run reads + revision write, clarify_engine clarifications write, websocket clarifications read) is migrated off agents/artifact_store/store.py onto the persisted typed layer via ScopedStore.write_ref/list_refs against artifact_refs"
    - "_handle_revision reads the PARENT run's artifacts via owner-scoped persisted ScopedStore (NOT the per-run in-memory ArtifactGraph, which cannot serve cross-run); the parent-run ownership of the caller is enforced by ScopedStore.assert_owns(parent_run_id) BEFORE the cross-run reads (T-5-SEED), so the same-owner reads return byte-identical artifacts (INV-3)"
    - "the clarifications PAYLOAD lands in artifact_refs (kind=clarifications) from clarify_engine; websocket.py reconnect reads that payload back from artifact_refs (still ownership-gated)"
    - "after this plan no engine/clarify/websocket NON-deletion path calls self._store.store / retrieve_latest / list_by_type for artifacts; the thin store keeps ONLY the HITL asyncio.Event half live (deletion of the dead artifact methods is 05-07)"
    - "tests/unit/test_revision_intelligence.py (6 tests) is rewritten to the ScopedStore API and stays green; the 0A characterization suite (incl. prototype_revision) stays byte-identical + semantic-event parity (INV-3)"
  artifacts:
    - path: "backend/agents/execution_engine/engine.py"
      provides: "_run_planner / _run_agent / _handle_revision migrated to ScopedStore-against-artifact_refs"
      contains: "write_ref"
    - path: "backend/agents/execution_engine/clarify_engine.py"
      provides: "clarifications payload dual-written into artifact_refs (kind=clarifications)"
      contains: "write_ref"
    - path: "backend/app/api/websocket.py"
      provides: "reconnect clarifications read served from artifact_refs (ownership-gated)"
      contains: "list_refs"
    - path: "backend/tests/unit/test_revision_intelligence.py"
      provides: "revision-intelligence suite rewritten to the ScopedStore API"
      contains: "ScopedStore"
  key_links:
    - from: "backend/agents/execution_engine/engine.py::_handle_revision"
      to: "backend/agents/authz.py::ScopedStore.assert_owns + list_refs"
      via: "owner-scoped cross-run parent-artifact read gated by assert_owns(parent_run_id)"
      pattern: "assert_owns"
    - from: "backend/agents/execution_engine/clarify_engine.py::_persist_qa"
      to: "backend/app/api/websocket.py reconnect read"
      via: "clarifications kind written to artifact_refs, read back via ScopedStore.list_refs"
      pattern: "clarifications"
---

<objective>
Close the FALSE precondition that blocks the 05-06 cutover. 05-04's SUMMARY (lines 50-51, 125-128) DEFERRED the thin-store artifact-method consumer migration to "05-06"; those consumers are still LIVE on `agents/artifact_store/store.py` and would break if the thin store were deleted. This plan migrates ALL of them onto the persisted typed layer (`ScopedStore` writing/reading `artifact_refs`) so that the destructive deletion (now in 05-07) rests on a TRUE precondition.

Consumers migrated (verified on disk):
1. `engine.py:1376` `_run_planner` — `planning_context` artifact write
2. `engine.py:1722` `_run_agent` — per-agent `produces`-loop artifact write
3. `engine.py:2602/2610/2613` `_handle_revision` — cross-run parent-run artifact reads (LIVE `run_revision` PPT handler)
4. `engine.py:2659` `_handle_revision` — new revision artifact write (`derived_from`)
5. `clarify_engine.py:396` — `clarifications` Q&A payload write
6. `app/api/websocket.py:558` — reconnect `clarifications` payload read

This plan is ADDITIVE and parity-safe: it changes WHERE the data is persisted/read (thin store → `artifact_refs`), not WHAT data flows. No deletion happens here — the thin store's artifact methods stay physically present (now unused on these paths) and are deleted in 05-07 behind the [BLOCKING] parity gate. The HITL `asyncio.Event` half of the store is untouched throughout.

Owner-scoping policy (LOCKED — no behavior change): `_handle_revision` reads the PARENT run's artifacts; the caller's ownership of that parent is ALREADY enforced by `ScopedStore.assert_owns(parent_run_id)` (the relocated L16 seam, D-07). For any currently-valid (same-owner) revision the owner-scoped reads return the SAME artifacts the thin store returned → INV-3 byte-identity preserved, no new cross-owner behavior introduced.

Purpose: make the cutover precondition TRUE so 05-07 can safely delete the thin-store artifact half.
Output: all six consumers persist/read via `artifact_refs`; the revision-intelligence + clarifications tests rewritten to the typed API; 0A parity still green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-SPEC.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-CONTEXT.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-PATTERNS.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-04-SUMMARY.md
@.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-05-SUMMARY.md
@backend/agents/authz.py
</context>

<artifacts_this_phase_produces>
This plan (05-06):
- MIGRATES `_run_planner` `planning_context` write (engine.py:1376) from `self._store.store(...)` to `ScopedStore.write_ref` against `artifact_refs` (kind `planning_context`)
- MIGRATES the `_run_agent` `produces`-loop write (engine.py:1720-1728) from `self._store.store(...)` to the existing typed dual-write path (one `ArtifactRef` per declared `produces` kind), removing the redundant thin-store write
- MIGRATES `_handle_revision` cross-run reads (engine.py:2602/2610/2613) to owner-scoped `ScopedStore.list_refs`/get-latest-by-kind against `artifact_refs`, gated by `assert_owns(parent_run_id)`; threads the caller `owner_id` into `_handle_revision`
- MIGRATES the `_handle_revision` revision-artifact write (engine.py:2659) to `ScopedStore.write_ref` with `derived_from` = the parent artifact id
- MIGRATES `clarify_engine.py:396` clarifications write to `ScopedStore.write_ref` (kind `clarifications`)
- MIGRATES `websocket.py:558` reconnect read from `self._store.retrieve_latest("clarifications")` to `ScopedStore.list_refs(run_id, kind="clarifications")` (latest), still ownership-gated
- REWRITES `tests/unit/test_revision_intelligence.py` (6 tests) + any clarifications test to the `ScopedStore` API
- Leaves the thin-store artifact methods physically present-but-unused (their deletion is 05-07) and the HITL half fully live
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the engine producer writes (_run_planner planning_context + _run_agent produces-loop) to ScopedStore/artifact_refs</name>
  <files>backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:1359-1393 (`_run_planner` — the `self._store.store(planning_context)` write at :1376; `ectx` is NOT in scope here — confirm how the planner is invoked from `execute()` to obtain the per-run `ScopedStore`/owner_id; the planner runs inside `execute()` where `scoped_store`/`ectx` exist)
    - backend/agents/execution_engine/engine.py:1690-1736 (`_run_agent` — the typed `_dual_write_artifact` per-run handoff at :1710, THEN the redundant `self._store.store(...)` `produces`-loop at :1720-1728)
    - backend/agents/execution_engine/engine.py:2772-2816 (`_dual_write_artifact` — the ALREADY-dual-writing helper: writes `ectx.artifacts` graph + best-effort `ectx.scoped_store.write_ref(ref)`; signature takes producer_agent/producer_step/content/kind/location/task_id/derived_from)
    - backend/agents/authz.py:133-182 (`ScopedStore.write_ref` — persists an ArtifactRef dataclass to `artifact_refs`, returns the row id; versioning = 1 + count of same (run_id, kind))
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-04-SUMMARY.md:77 (what `_dual_write_artifact` ALREADY persists — the per-run handoff artifact keyed by producer_agent/kind; the produces-loop write is REDUNDANT-or-additional thin-store data to fold into the typed path, NOT double-write)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-RESEARCH.md:182-187 (Thin-store consumer map — `self._store.store` sites)
  </read_first>
  <action>
    Migrate the two engine PRODUCER writes off the thin store onto the persisted typed layer; do NOT introduce a `v1`/`static`/`placeholder` shortcut — the data must land in `artifact_refs`.

    (1) `_run_planner` planning_context write (engine.py:1376): replace the `await self._store.store(run_id=..., artifact_type="planning_context", ...)` call with a `ScopedStore.write_ref` of an `ArtifactRef` whose `kind="planning_context"`, `run_id=pipeline_run_id`, `producer_agent=PLANNER_AGENT_ID`, `producer_step="planner"`, `content=json.dumps(planning_context)`, `owner_id`/`workspace_id` from the per-run `ScopedStore`/`ectx`, `location=f"artifact_refs/planning_context"`. Use the same `ectx.scoped_store` the engine armed at execute() entry (engine.py:721-736). If `_run_planner` does not currently receive `ectx`/`scoped_store`, thread the per-run `ScopedStore` (or `ectx`) into it from its call site inside `execute()` — confirm the call site in `read_first`. Keep the existing best-effort try/except degrade shape (a DB persist failure logs and never breaks the run — same as `_dual_write_artifact`), but DROP the `ArtifactStoreWriteError`-specific branch in favor of the best-effort pattern used by the typed path. INVESTIGATE whether the planning_context is ALREADY dual-written elsewhere; if so, remove the redundant thin-store write rather than adding a second write.

    (2) `_run_agent` produces-loop (engine.py:1720-1728): the per-run handoff for `spec.id` is ALREADY dual-written at :1710 via `_dual_write_artifact`. For each declared `artifact_type` in `spec.produces`, replace the `await self._store.store(...)` call with a typed `ScopedStore.write_ref` (or a `_dual_write_artifact` call with `kind=artifact_type`) so each declared `produces` kind lands in `artifact_refs` keyed by that kind. Preserve the failure semantics that matter for parity: if the typed persist of a `produces` artifact fails in a way the OLD code treated as fatal (it transitioned the run to `failed` and yielded `agent_error`), keep that fail-the-run behavior ONLY if the old behavior was observable in the 0A snapshots; otherwise fold into the best-effort degrade (the characterization harness has no `workflow_runs` FK row, so a hard-fail there would break parity — verify against the 0A suite, fix-forward if it diverges). Confirm no `accumulated_outputs` semantics change.

    Run the 0A characterization suite + the engine unit suites after the change.
  </action>
  <verify>
    <automated>cd backend && grep -nE "self\._store\.store\(" agents/execution_engine/engine.py | grep -vn "2659" ; cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_characterization_phase4_prototype.py tests/unit/test_execution_engine.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -nE "self\._store\.store\(" agents/execution_engine/engine.py` returns AT MOST the single `_handle_revision` write at :2659 (migrated in Task 2) — the `_run_planner` (:1376) and `_run_agent` produces-loop (:1722) thin-store writes are GONE.
    - `cd backend && grep -nE "kind=\"planning_context\"|kind='planning_context'" agents/execution_engine/engine.py` returns ≥1 (planning_context now persisted as a typed kind).
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` is GREEN (deliverable byte-identity + semantic-event parity — INV-3, the producer-write migration changes the persistence target, not the deliverable bytes/events).
    - `cd backend && python3.11 -m pytest tests/unit/test_execution_engine.py tests/agents/test_artifact_graph.py -x` is GREEN.
    - `cd backend && lint-imports` exits 0 (no kernel→app.api edge introduced — the engine reaches `app.models` only via `agents.authz.ScopedStore`).
  </acceptance_criteria>
  <done>The two engine producer writes persist via ScopedStore into `artifact_refs`; the only remaining `self._store.store(` in engine.py is the `_handle_revision` write (Task 2); 0A parity is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate _handle_revision cross-run reads + revision write to owner-scoped ScopedStore (assert_owns-gated)</name>
  <files>backend/agents/execution_engine/engine.py, backend/app/api/websocket.py, backend/tests/unit/test_revision_intelligence.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:2581-2694 (`_handle_revision` — reads `original` (:2602 `retrieve_latest`), `version_history` (:2610 `list_by_type`), `planning_context_artifact` (:2613 `retrieve_latest`), then writes the revision via `self._store.store(..., derived_from_id=original["id"])` at :2659; note dict-access `original["content"]`/`original["id"]`/`planning_context_artifact["content"]` that must become attribute access on ORM rows)
    - backend/agents/authz.py:184-242 (`ScopedStore.get_ref`/`list_refs`/`lineage` — owner+visibility-scoped reads returning ORM `ArtifactRef` ROWS, ordered by version asc; latest-by-kind = `list_refs(run_id, kind)[-1]`)
    - backend/agents/authz.py:395-435 (`ScopedStore.assert_owns(parent_run_id)` — the relocated L16 seam: looks up the parent run's TRUE owner UNSCOPED, raises `PermissionError` on mismatch; returns None on match OR absent parent — the same-owner missing/degrade path)
    - backend/app/api/websocket.py:587-660 (the `run_revision` handler — the SOLE caller of `_handle_revision`; has `user.id` (owner), `_rev_parent_run_id`, `_rev_pipeline_run_id` in scope; passes them to `_handle_revision`)
    - backend/tests/unit/test_revision_intelligence.py:1-269 (6 tests built on `ArtifactStore(use_db=False)` + `engine._store = store` + dict-shaped artifact assertions — to be rewritten to the ScopedStore API)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-04-SUMMARY.md:125-128 (the DEFERRAL evidence: `_handle_revision` reads left on the thin store; assert_owns(parent_run_id) wired above the graceful-degrade try in 05-04 Task 1 — the cross-owner gate this plan reuses)
  </read_first>
  <action>
    Migrate `_handle_revision` from the thin store to the owner-scoped PERSISTED typed layer. The per-run in-memory `ArtifactGraph` CANNOT serve this — `_handle_revision` reads the PARENT run's artifacts cross-run — so it MUST use `ScopedStore` against `artifact_refs`.

    Thread the caller principal in: change `_handle_revision` to accept `owner_id: str` (the run owner — `user.id` at the websocket call site, or `f"anon:{session_id}"` if a future anon path exists; the websocket handler has an authed `user.id`). At the websocket caller (websocket.py:643), pass `owner_id=user.id`. Inside `_handle_revision`, construct `store = ScopedStore(owner_id=owner_id)` (no workspace_id needed for cross-run reads — the parent's artifacts are owner-scoped; the ArtifactRef `visibility` default lets owner reads through `_scope_with_visibility`). FIRST call `await store.assert_owns(parent_run_id)` ABOVE the reads (T-5-SEED: a caller who does not own the parent run gets `PermissionError`, never another owner's artifacts) — this preserves the LOCKED owner-scoping policy (same-owner reads byte-identical → INV-3; no new cross-owner behavior).

    Replace the three reads:
    - `original = await self._store.retrieve_latest(parent_run_id, target_artifact_type)` → `_refs = await store.list_refs(parent_run_id, kind=target_artifact_type); original = _refs[-1] if _refs else None` (latest by version asc). Keep the `if original is None: raise ValueError("No artifact ...")` guard.
    - `version_history = await self._store.list_by_type(parent_run_id, target_artifact_type)` → `version_history = _refs` (the same scoped list — avoid a second query).
    - `planning_context_artifact = await self._store.retrieve_latest(parent_run_id, "planning_context")` → `_pc = await store.list_refs(parent_run_id, kind="planning_context"); planning_context_artifact = _pc[-1] if _pc else None`.
    Update the dict-access to ORM-row attribute access: `original["content"]` → `original.content`, `original["id"]` → `original.id`, `planning_context_artifact["content"]` → `planning_context_artifact.content`. (kind `planning_context` is now written as a typed ref by Task 1, so the planning-context-available path resolves against `artifact_refs`.)

    Replace the revision write (:2659): `new_artifact_id = await self._store.store(run_id=pipeline_run_id, artifact_type=target_artifact_type, name=..., content=revision_context, producing_agent_id="revision-agent", derived_from_id=original["id"])` → `await store.write_ref(<ArtifactRef>)` with `run_id=pipeline_run_id`, `kind=target_artifact_type`, `producer_agent="revision-agent"`, `producer_step="revision"`, `content=revision_context`, `derived_from=original.id`, `owner_id=owner_id`, `location=f"artifact_refs/{target_artifact_type}"`. Preserve the existing error-emit shape (the `state_restoration_failed` event on write failure) — wrap the `write_ref` so a persist failure emits the same event.

    Rewrite `tests/unit/test_revision_intelligence.py` (all 6 tests) to the ScopedStore API: drop the `ArtifactStore(use_db=False)` fixture; seed the parent run's artifacts by constructing `ScopedStore(owner_id="owner-x")` and `write_ref`-ing the original `spec`/`planning_context` refs into `artifact_refs` (against the unit test DB / in-memory SQLite the suite uses); call `_handle_revision(..., owner_id="owner-x")`; assert the revision ref landed via `ScopedStore.list_refs(run-rev, kind="spec")[-1]` with `derived_from == original.id` and `version == 1`; keep the three-separate-inputs / planning-context-available / planning-context-unavailable / empty-instruction / nonexistent-artifact assertions, adapted to row attributes. Add ONE cross-owner denial case: a second owner calling `_handle_revision` against the first owner's parent run raises `PermissionError` (AUTHZ-adjacent, T-5-SEED proof). If the suite needs a DB, mirror the DB-bootstrap pattern used by `tests/unit/test_runs_api_artifacts.py` (05-05).

    Migrate `websocket.py:558` reconnect clarifications READ: replace `await _store.retrieve_latest(_reconnect_run_id, "clarifications")` (the thin store) with `_refs = await _scoped.list_refs(_reconnect_run_id, kind="clarifications"); _clarifications = _refs[-1] if _refs else None` using the SAME `_scoped = ScopedStore(owner_id=user.id)` already constructed for the ownership gate (websocket.py:554) — the read stays ownership-gated (the existing `_owned_run is not None` guard collapses into the owner-scoped `list_refs`, which returns nothing for a non-owned run). Update the payload access from `_clarifications["content"]` to `_clarifications.content`. (The clarifications PAYLOAD itself is written into `artifact_refs` by Task 3.)

    Run the revision-intelligence suite + the 0A characterization (prototype_revision pipeline) + the websocket/clarify suites.
  </action>
  <verify>
    <automated>cd backend && grep -nE "self\._store\.(store|retrieve_latest|list_by_type)\(" agents/execution_engine/engine.py | grep -v "^#" | wc -l | tr -d ' ' ; cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_revision_intelligence.py tests/agents/test_characterization_phase5_revision.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -nE "self\._store\.(store|retrieve_latest|list_by_type|retrieve_version|list_lineage)\(" agents/execution_engine/engine.py` returns 0 (every engine thin-store artifact-method call — planner, produces-loop, AND `_handle_revision` reads+write — is migrated).
    - `cd backend && grep -nE "self\._store\.retrieve_latest\(" app/api/websocket.py` returns 0 (the reconnect clarifications read no longer hits the thin store).
    - `cd backend && grep -nE "assert_owns\(parent_run_id\)" agents/execution_engine/engine.py` returns ≥1 inside `_handle_revision` (T-5-SEED gate above the cross-run reads).
    - `cd backend && python3.11 -m pytest tests/unit/test_revision_intelligence.py -x` is GREEN (6+ tests on the ScopedStore API, incl. the cross-owner `PermissionError` case).
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` is GREEN (prototype_revision deliverable byte-identity + semantic-event parity — INV-3; same-owner reads return the same artifacts).
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q 2>&1 | tail -3` shows no NEW failures beyond the documented pre-existing environmental ones (`test_logout.py`, `test_pipeline_cancel.py`).
    - `cd backend && lint-imports` exits 0.
  </acceptance_criteria>
  <done>`_handle_revision` reads/writes the parent + revision artifacts via owner-scoped `ScopedStore` (assert_owns-gated), the websocket reconnect read is off the thin store, every engine thin-store artifact call is gone, and the rewritten revision-intelligence suite (incl. cross-owner denial) is green with 0A parity intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate clarify_engine clarifications write to artifact_refs (close the websocket read loop)</name>
  <files>backend/agents/execution_engine/clarify_engine.py, backend/tests/unit/test_revision_intelligence.py</files>
  <read_first>
    - backend/agents/execution_engine/clarify_engine.py:375-404 (`_persist_qa` — the `self._store.store(artifact_type="clarifications", ...)` write at :396 that `websocket.py` reads back; confirm how `clarify_engine` obtains the per-run owner/workspace — grep its `__init__`/`self._store`/`owner_id`/`run_id` context to find the principal source)
    - backend/agents/authz.py:133-182 (`ScopedStore.write_ref` target for the clarifications payload)
    - backend/app/api/websocket.py reconnect read (migrated in Task 2 to `ScopedStore.list_refs(run_id, kind="clarifications")`) — the consumer this write must satisfy
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b/05-04-SUMMARY.md:117-123 (Deviation #2: the clarifications PAYLOAD was KEPT in the thin store in 05-04; the read was ownership-gated. THIS task completes the deferred payload dual-write into artifact_refs so the thin-store clarifications write can be deleted in 05-07)
  </read_first>
  <action>
    Migrate the clarifications payload write off the thin store onto `artifact_refs` so the websocket reconnect read (Task 2) is served entirely from the typed layer.

    In `clarify_engine.py::_persist_qa` (:396): replace `await self._store.store(run_id=pipeline_run_id, artifact_type="clarifications", name=f"clarifications_round_{round_num}", content=json.dumps(qa_pairs), producing_agent_id="clarify-agent")` with `await ScopedStore(owner_id=<run owner>).write_ref(<ArtifactRef>)` where the ref has `run_id=pipeline_run_id`, `kind="clarifications"`, `producer_agent="clarify-agent"`, `producer_step=f"clarify_round_{round_num}"`, `content=json.dumps(qa_pairs)`, `location=f"artifact_refs/clarifications"`, `owner_id=<run owner>`. Obtain the run owner the same way the engine does: if `clarify_engine` already carries an `ExecutionContext`/`ectx.owner_id`/`ectx.scoped_store`, reuse it (preferred — one armed `ScopedStore` per run); else thread the `owner_id` (or the `ScopedStore`) in from the engine call site that invokes the clarify flow. Do NOT hardcode an owner or fall back to a sentinel — the clarifications must be owner-scoped so the reconnect read returns nothing for a non-owner (T-5-IDOR continuity). Keep the existing best-effort try/except degrade (a persist failure logs and does not break the clarify flow — same shape as the current `except Exception`).

    Confirm the read/write loop end-to-end: after this change, `clarify_engine` writes `kind="clarifications"` into `artifact_refs`, and `websocket.py` (Task 2) reads it back via `ScopedStore.list_refs(run_id, kind="clarifications")[-1].content`. If a clarifications unit/integration test exists (grep `tests/` for `clarifications` / `_persist_qa` / `questionnaire_ready`), rewrite it to the ScopedStore API the same way as the revision suite; otherwise add a focused round-trip test (write via clarify path → read via `ScopedStore.list_refs(kind="clarifications")`) to `tests/unit/test_revision_intelligence.py` or a sibling clarifications test file.

    Run the clarify + websocket-adjacent suites + a broad sweep.
  </action>
  <verify>
    <automated>cd backend && grep -nE "self\._store\.store\(" agents/execution_engine/clarify_engine.py | grep -v "^#" | wc -l | tr -d ' ' ; cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/ -k "clarif or revision or questionnaire" -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -nE "self\._store\.store\(" agents/execution_engine/clarify_engine.py` returns 0 (the clarifications thin-store write is migrated).
    - `cd backend && grep -nE "kind=\"clarifications\"|kind='clarifications'" agents/execution_engine/clarify_engine.py` returns ≥1 (clarifications now a typed `artifact_refs` kind).
    - `cd backend && grep -rnE "self\._store\.(store|retrieve_latest|retrieve_version|list_by_type|list_lineage)\(" agents/execution_engine/ app/api/websocket.py` returns 0 (NO non-deletion path calls a thin-store artifact method anywhere in the engine/clarify/websocket surface — the precondition for 05-07's deletion is now TRUE).
    - `cd backend && python3.11 -m pytest tests/unit/ -k "clarif or revision or questionnaire" -x` is GREEN (the clarifications round-trip resolves through `artifact_refs`).
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q 2>&1 | tail -3` shows no NEW failures beyond the documented pre-existing environmental ones; `lint-imports` exits 0.
  </acceptance_criteria>
  <done>The clarifications payload is written into `artifact_refs` (kind=clarifications) and read back via `ScopedStore`; the engine/clarify/websocket surface has ZERO live thin-store artifact-method calls — the destructive deletion in 05-07 now rests on a TRUE precondition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| revision caller → parent run | `_handle_revision` reads a DIFFERENT run's artifacts; the caller must own that parent run or it must be denied (cross-owner read of artifacts = IDOR). |
| reconnect client → clarifications payload | the reconnecting WS client reads the clarifications Q&A of a run; a non-owner must read nothing (cross-session questionnaire disclosure). |
| engine/clarify → artifact_refs (DB) | the persisted typed layer is now the SOLE artifact path on these consumers; a write/read regression silently loses revision/clarification context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-SEED | Information Disclosure / Elevation | `_handle_revision` cross-run parent read | mitigate | `await ScopedStore.assert_owns(parent_run_id)` is called ABOVE the cross-run reads (the relocated L16 seam, D-07); a non-owner gets `PermissionError` (never another owner's artifacts). Owner-scoping policy LOCKED — same-owner reads are byte-identical (INV-3), no new cross-owner behavior. Unit-proven by the cross-owner denial case (Task 2). |
| T-5-IDOR | Information Disclosure | websocket reconnect clarifications read | mitigate | The read goes through `ScopedStore.list_refs(run_id, kind="clarifications")` with the reconnecting `user.id` principal; a run the user does not own resolves to an empty scoped result → no clarifications returned (continues the 05-04 ownership gate, now serving the payload from `artifact_refs` too). |
| T-5-PARITY | Tampering (regression) | producer-write + revision-read migration | mitigate | The migration changes the persistence TARGET (thin store → `artifact_refs`), not the deliverable bytes or event stream; gated by the 0A characterization suite staying byte-identical + semantic-event parity (Tasks 1-2 acceptance). A divergence is fixed-forward, never rebaselined. |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit — N/A; zero `pip install`). |
</threat_model>

<verification>
- `cd backend && grep -rnE "self\._store\.(store|retrieve_latest|retrieve_version|list_by_type|list_lineage)\(" agents/execution_engine/ app/api/websocket.py` → 0 (all six consumers migrated; thin-store artifact methods unused on these paths).
- `cd backend && grep -nE "assert_owns\(parent_run_id\)" agents/execution_engine/engine.py` → ≥1 (T-5-SEED gate in `_handle_revision`).
- `cd backend && python3.11 -m pytest tests/unit/test_revision_intelligence.py -x` → GREEN (rewritten to ScopedStore + cross-owner denial).
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` → GREEN (INV-3 byte-identity + semantic-event parity, incl. prototype_revision).
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` + `lint-imports` → no new failures, exit 0.
</verification>

<success_criteria>
- All six thin-store artifact-method consumers (#1-#6) persist/read via `ScopedStore` against `artifact_refs`.
- `_handle_revision`'s cross-run parent reads are owner-scoped + `assert_owns`-gated (T-5-SEED); same-owner reads byte-identical (INV-3); no new cross-owner behavior.
- The clarifications payload lands in + is read from `artifact_refs`; the reconnect read stays ownership-gated.
- The revision-intelligence suite (+ clarifications round-trip) is rewritten to the typed API and green; 0A parity green.
- The engine/clarify/websocket surface has ZERO live thin-store artifact-method calls → 05-07's destructive deletion rests on a TRUE precondition. No deletion performed here.
</success_criteria>

<output>
Create `.planning/phases/05-typed-artifacts-persistence-ownership-1b/05-06-SUMMARY.md` when done.
</output>
