---
phase: 13-live-verification-gap-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/workflows/sample_fanout/workflow.yaml
  - backend/tests/agents/test_sc001_fanout.py
  - backend/tests/agents/test_phase3_token_delta_live.py
  - backend/tests/agents/test_phase8_live.py
autonomous: true
gap_closure: true
requirements: [F6, F7]
must_haves:
  truths:
    - "sample_fanout declares deliverable strategy serialized_sandbox so the deliverable resolves from the merged part files — no 'merged.txt not written by agent' single_file fallback (F6)"
    - "test_phase3_token_delta_live monkeypatches the CURRENT engine seams (_compose_context_message + the registered html_skeleton compaction capability) and collects cleanly (F7)"
    - "The phase-8 live sweep soft cost ceiling reflects the measured $5.94 clean-sweep spend (F7 / UAT test 8)"
  artifacts:
    - path: "backend/agents/workflows/sample_fanout/workflow.yaml"
      provides: "deliverable resolved via the registered serialized_sandbox strategy"
      contains: "serialized_sandbox"
    - path: "backend/tests/agents/test_sc001_fanout.py"
      provides: "deliverable assertion on the serialized_sandbox filename:-block bundle"
    - path: "backend/tests/agents/test_phase3_token_delta_live.py"
      provides: "live token-delta wrapper re-pointed at _compose_context_message + html_skeleton capability"
      contains: "_compose_context_message"
    - path: "backend/tests/agents/test_phase8_live.py"
      provides: "recalibrated LIVE_BUDGET_USD ceiling with measurement citation"
      contains: "LIVE_BUDGET_USD = 8.0"
  key_links:
    - from: "backend/agents/workflows/sample_fanout/workflow.yaml"
      to: "registered serialized_sandbox deliverable resolver"
      via: "deliverable.strategy manifest field (manifest-data-only, zero engine edits — SC-001)"
      pattern: "strategy: serialized_sandbox"
    - from: "backend/tests/agents/test_phase3_token_delta_live.py"
      to: "agents/capabilities/compaction/html_skeleton.py"
      via: "registry.resolve('compaction','html_skeleton')"
      pattern: "html_skeleton"
---

<objective>
Close UAT Gaps 6, 7 and 8 (findings F6 + F7, minor/cosmetic) — three small, independent,
fully-diagnosed repairs:

1. F6: sample_fanout still declares `deliverable: single_file name: merged.txt` which no
   step produces (workers write part_1..3.txt merged by copy_disjoint), so the live run
   fired the fallback and degraded the final output to one worker's streamed text. Same
   quirk 12-10 fixed for sample_wave (commit 8ffae37c); sample_fanout was out of scope then.
2. F7: test_phase3_token_delta_live monkeypatches engine._build_context_message, a seam
   the Phase-7 decoupling renamed to async _compose_context_message (added index param),
   and calls engine._extract_html_skeleton, relocated to the html_skeleton compaction
   capability — the test errors at setup with AttributeError.
3. UAT test 8: LIVE_BUDGET_USD=5.0 (test_phase8_live.py:104) is below the measured $5.94
   clean-sweep cost — calibration drift, not overspend.

Purpose: ROADMAP Phase 13 success criterion 6.
Output: manifest-data-only deliverable swap + two test-infra repairs. Zero engine edits.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-live-verification-gap-closure/13-UAT.md
@.planning/live-verification/REPORT.md
</context>

## Artifacts this phase produces

- `deliverable: strategy: serialized_sandbox` manifest field value in `backend/agents/workflows/sample_fanout/workflow.yaml` (replacing `single_file` + `name: merged.txt`)
- New deliverable assertions in `backend/tests/agents/test_sc001_fanout.py` (final_output is the filename:-block bundle; serialized_sandbox registered; merged.txt gone)
- Repaired monkeypatch wrapper in `backend/tests/agents/test_phase3_token_delta_live.py` targeting `_compose_context_message` and the `html_skeleton` compaction capability
- `LIVE_BUDGET_USD = 8.0` in `backend/tests/agents/test_phase8_live.py` with a measurement-citation comment
- No new production code symbols

<tasks>

<task type="auto">
  <name>Task 1: Swap sample_fanout deliverable to serialized_sandbox + update test_sc001_fanout (F6)</name>
  <files>backend/agents/workflows/sample_fanout/workflow.yaml, backend/tests/agents/test_sc001_fanout.py</files>
  <read_first>
    - backend/agents/workflows/sample_fanout/workflow.yaml (lines 32-34: the defective `deliverable: strategy: single_file / name: merged.txt` block)
    - `git show 8ffae37c` — the sample_wave mirror fix: manifest swap + the 34 test lines added to backend/tests/agents/test_sample_wave_workflow.py (the exact assertion pattern to replicate)
    - backend/agents/workflows/sample_wave/workflow.yaml lines 41-42 (the post-fix target shape: `deliverable: strategy: serialized_sandbox`, no name field)
    - backend/tests/agents/test_sc001_fanout.py (current assertions at lines 319-373 — fan-out mechanics + registered-capabilities checks that must stay green)
  </read_first>
  <action>
    1. In sample_fanout/workflow.yaml: replace the deliverable block with
       `strategy: serialized_sandbox` (drop the `name: merged.txt` line entirely —
       mirror sample_wave lines 41-42). Update the header comment block: the deliverable
       line in the capability list (line 4-5 region) and add the explanatory paragraph
       mirroring sample_wave's "deliverable: serialized_sandbox — the workers write
       disjoint part_*.txt files, copy_disjoint merges them into the base, and
       serialized_sandbox bundles that merged base into the filename:-block deliverable"
       comment (cite UAT Gap 6 / F6, 13-04).
    2. In test_sc001_fanout.py, mirror the 8ffae37c test additions for the fanout proof:
       (a) assert the run's pipeline_complete final_output is the serialized_sandbox
       filename:-block bundle containing ALL THREE part files (part_1.txt, part_2.txt,
       part_3.txt) — not a single worker's streamed text; (b) assert no fallback fired
       (final_output contains the filename: block markers); (c) in the
       registered-capabilities test (line 358+), assert
       `reg.is_registered("deliverable", "serialized_sandbox")` and that the manifest
       text contains "strategy: serialized_sandbox" and does NOT contain "merged.txt".
    3. ZERO edits to backend/agents/execution_engine/ — this is the SC-001 manifest-data
       fix; the existing fan-out mechanics assertions (3 spawned + 3 results + no
       merge_conflict + part files on disk) stay untouched.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_sc001_fanout.py -q && ! grep -q "merged.txt" agents/workflows/sample_fanout/workflow.yaml</automated>
  </verify>
  <done>sample_fanout's declared deliverable resolves from the files the workflow actually produces; the SC-001 fanout proof asserts the bundle deliverable; no engine file touched.</done>
  <acceptance_criteria>
    - `grep -c "strategy: serialized_sandbox" backend/agents/workflows/sample_fanout/workflow.yaml` returns 1
    - `grep -c "merged.txt" backend/agents/workflows/sample_fanout/workflow.yaml` returns 0
    - `python3.11 -m pytest tests/agents/test_sc001_fanout.py -q` exits 0 and the suite asserts part_1.txt, part_2.txt and part_3.txt all appear in final_output
    - `git diff --stat` for this task shows zero files under backend/agents/execution_engine/
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repair test_phase3_token_delta_live seams + recalibrate the phase-8 live budget (F7 / test 8)</name>
  <files>backend/tests/agents/test_phase3_token_delta_live.py, backend/tests/agents/test_phase8_live.py</files>
  <read_first>
    - backend/tests/agents/test_phase3_token_delta_live.py lines 140-230 (the compaction-off override: wraps engine._build_context_message at line 167, calls engine._extract_html_skeleton at 188 — both seams are gone)
    - backend/agents/execution_engine/engine.py lines 4396-4404 (the CURRENT seam: `async def _compose_context_message(self, spec, index, ordered_agents, user_message, planning_context, ectx)` — async, positional, with the added index param)
    - backend/agents/capabilities/compaction/html_skeleton.py (the relocated skeleton helper — resolve via CapabilityRegistry().resolve("compaction", "html_skeleton") and call its compact() contract; confirm the exact method name in the file)
    - backend/tests/agents/test_phase8_live.py lines 98-110 (LIVE_BUDGET_USD = 5.0 at line 104 + its guardrail comment)
  </read_first>
  <action>
    1. test_phase3_token_delta_live.py — re-point the compaction-off override:
       capture `_orig = engine._compose_context_message`; replace the wrapper with an
       ASYNC function matching the current positional shape
       (spec, index, ordered_agents, user_message, planning_context, ectx) that awaits
       the original, then performs the same skeleton-block→full-HTML swap for
       spec.id == "prototype-build". Source the skeleton via the registered capability:
       resolve("compaction", "html_skeleton") from agents.capabilities.registry
       (after discover()), using the capability's compact contract on the current HTML —
       NOT a deleted engine method. Assign the override to
       `engine._compose_context_message` (instance attribute, unbound plain async
       function — same monkeypatch style the file already documents at lines 169-173).
       Update the stale comment block referencing _build_context_message/#WR-01.
       The skeleton_block string the wrapper replaces must match what the live
       compaction path composes — read the html_skeleton capability and the engine's
       skeleton-injection site to confirm the exact "=== CURRENT PROTOTYPE" wrapper text
       still matches, and adjust the replace target if the capability output drifted.
    2. test_phase8_live.py line 104: set `LIVE_BUDGET_USD = 8.0` and extend the comment:
       recalibrated 2026-06-11 from the measured clean full sweep — $5.94 / 18.7M tokens
       (17.5M input) at Haiku 4.5 pricing after MAX_OUTPUT_TOKENS was lifted to 32768;
       5.0 was the Phase-8 calibration.
    3. Both files are RUN_LIVE_BEDROCK-gated (evidence-only, never CI gates) — offline
       verification is clean COLLECTION (the AttributeError was raised at setup, so
       collect+skip cleanly proves the repair target exists) plus a seam-existence
       assertion: getattr(ExecutionEngine, "_compose_context_message") is used, and no
       reference to _build_context_message or _extract_html_skeleton remains.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py tests/agents/test_phase8_live.py --collect-only -q && ! grep -q "_build_context_message\|_extract_html_skeleton" tests/agents/test_phase3_token_delta_live.py && grep -q "LIVE_BUDGET_USD = 8.0" tests/agents/test_phase8_live.py</automated>
  </verify>
  <done>The token-delta test targets only seams that exist on the current engine and collects cleanly; the live sweep ceiling matches measured reality with the measurement cited.</done>
  <acceptance_criteria>
    - `grep -c "_build_context_message" backend/tests/agents/test_phase3_token_delta_live.py` returns 0 and `grep -c "_extract_html_skeleton" backend/tests/agents/test_phase3_token_delta_live.py` returns 0
    - `grep -c "_compose_context_message" backend/tests/agents/test_phase3_token_delta_live.py` >= 2 (capture + assignment) and `grep -c "html_skeleton" backend/tests/agents/test_phase3_token_delta_live.py` >= 1
    - the override wrapper is declared `async def` and accepts the 6-positional shape (spec, index, ordered_agents, user_message, planning_context, ectx)
    - `grep -n "LIVE_BUDGET_USD = 8.0" backend/tests/agents/test_phase8_live.py` matches and the adjacent comment cites the 2026-06-11 $5.94 / 18.7M-token measurement
    - `python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py tests/agents/test_phase8_live.py --collect-only -q` exits 0 offline
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow.yaml manifests → compiler | file-trusted engineer-authored manifest data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Elevation of privilege | sample_fanout grants spawn_subagents | accept | unchanged grant, file-trust only (CAP-03 rejects it from user/db manifests); this plan touches only the deliverable block |
| T-13-04-02 | Tampering | budget ceiling raise masking runaway live spend | accept | LIVE_BUDGET_USD is a soft evidence-run guardrail (opt-in RUN_LIVE_BEDROCK, never CI); 8.0 stays within ~35% of the measured clean-sweep cost |
</threat_model>

<verification>
- test_sc001_fanout green offline (scripted model) with the new deliverable assertions
- Both live test files collect cleanly offline (no setup-time AttributeError)
- No file under backend/agents/execution_engine/ modified (SC-001 — manifest-data-only fix)
</verification>

<success_criteria>
- ROADMAP Phase 13 criterion 6 TRUE: sample_fanout resolves its declared deliverable with no fallback warning, test_phase3_token_delta_live targets the current engine, and the phase-8 budget reflects measured spend (F6/F7)
- Live re-runs of the two RUN_LIVE_BEDROCK suites deferred to the end-of-milestone live pass (project convention)
</success_criteria>

<output>
Create `.planning/phases/13-live-verification-gap-closure/13-04-SUMMARY.md` when done.
</output>
