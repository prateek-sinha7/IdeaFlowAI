---
phase: 13-live-verification-gap-closure
plan: 05
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/agents/artifacts/graph.py
  - backend/tests/unit/test_revision_intelligence.py
  - backend/tests/unit/test_run_revision_fe_contract.py
autonomous: true
gap_closure: true
requirements: [F2]
must_haves:
  truths:
    - "A completed run persists a kind='deliverable' artifact_ref carrying the resolved final_output (F2)"
    - "run_revision with the FE's exact payload (target_artifact_type 'ppt_output'/'od_ppt_output') against a completed parent run passes FR-014 and drives the revision — for NEW runs via the deliverable ref and for PRE-FIX runs via the summary-kind fallback (F2)"
    - "Revision seed tests seed what the run path actually persists — the artificial exact-kind seeding no longer masks the contract (F2)"
  artifacts:
    - path: "backend/agents/artifacts/graph.py"
      provides: "'deliverable' member in the closed ARTIFACT_KINDS vocabulary"
      contains: "deliverable"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "completion-time deliverable ref write + FR-014 fallback lookup chain in _handle_revision"
    - path: "backend/tests/unit/test_run_revision_fe_contract.py"
      provides: "FE-exact run_revision regression test against a realistically-persisted parent"
      min_lines: 50
  key_links:
    - from: "backend/agents/execution_engine/engine.py (execute terminal block)"
      to: "ArtifactGraph.write_ref via _dual_write_artifact"
      via: "kind='deliverable', visibility='workspace' write of final_output"
      pattern: "deliverable"
    - from: "backend/agents/execution_engine/engine.py (_handle_revision)"
      to: "store.list_refs"
      via: "fallback chain: exact target kind -> 'deliverable' -> 'summary'"
      pattern: "list_refs"
---

<objective>
Close UAT Gap 2 (finding F2, major): every `run_revision` fails FR-014 validation. The FE
(DashboardLayout.tsx:388-403) sends `target_artifact_type: "ppt_output"|"od_ppt_output"`,
the engine validates via `store.list_refs(parent_run_id, kind=target_artifact_type)`
(engine.py:3408), but NO backend code ever persists a `*_output` kind — runs persist kinds
by agent id (spec/task_list/html_file/validation_report via _AGENT_KIND_MAP, engine.py:3610)
plus summary/planning_context/clarifications. The revision characterization tests masked
this by seeding parents with whatever kind they target.

Fix (both halves of the UAT missing item): persist a generic deliverable-kind artifact_ref
at run completion, AND give the FR-014 lookup a fallback chain so the FE's exact payload
resolves against both new runs (deliverable ref) and pre-fix completed runs (summary ref).
The kernel stays workflow-agnostic — no `ppt_output` literal enters the engine (SC-001).

Purpose: ROADMAP Phase 13 success criterion 2 — PPT/user-stories/app_builder revision is
user-functional whenever a completed run exists.
Output: deliverable persist + lookup chain + de-masked seed tests + FE-exact regression test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-live-verification-gap-closure/13-UAT.md
@.planning/live-verification/REPORT.md
</context>

## Artifacts this phase produces

- `"deliverable"` — new member of the `ARTIFACT_KINDS` frozenset in `backend/agents/artifacts/graph.py`
- Completion-time deliverable persist call in `ExecutionEngine.execute()` (engine.py terminal block, ~line 1690): `_dual_write_artifact(..., kind="deliverable", producer_step="deliverable", visibility="workspace")`
- FR-014 fallback lookup chain inside `ExecutionEngine._handle_revision` (engine.py ~3405-3415): exact `target_artifact_type` kind → `"deliverable"` kind → `"summary"` kind → existing FR-014 ValueError
- New test file `backend/tests/unit/test_run_revision_fe_contract.py`
- De-masked seed scenarios added to `backend/tests/unit/test_revision_intelligence.py`
- No FE changes (the backend now accepts what DashboardLayout.tsx already sends)

<tasks>

<task type="auto">
  <name>Task 1: Persist a deliverable-kind artifact_ref at run completion</name>
  <files>backend/agents/artifacts/graph.py, backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/artifacts/graph.py lines 30-50 (ARTIFACT_KINDS closed vocabulary — note write_ref does NOT enforce membership; the addition keeps the vocabulary honest)
    - backend/agents/execution_engine/engine.py lines 1617-1710 (the execute() terminal block: completed-state transition at 1623, deliverable resolution via the declared resolver at 1653-1678, pipeline_complete emit at 1695 — the persist call lands between resolution and emit)
    - backend/agents/execution_engine/engine.py lines 3627-3680 (_dual_write_artifact signature + best-effort DB degrade semantics)
    - .planning/phases/05-typed-artifacts-persistence-ownership-1b (STATE.md decision: producer/revision/clarifications writes use visibility="workspace" so same-owner cross-run _handle_revision reads pass the owner+visibility scope filter — the deliverable ref MUST use workspace visibility for the same reason)
  </read_first>
  <action>
    1. graph.py: add "deliverable" to ARTIFACT_KINDS (additive, alphabetical placement
       irrelevant — keep the set's comment accurate).
    2. engine.py execute() terminal block: after final_output is resolved (post the
       streamed_text fallback at line 1676-1678) and BEFORE the pipeline_complete yield,
       persist the run's deliverable: await self._dual_write_artifact(ectx,
       producer_agent=<the final agent's id from ordered_agents[-1].id, falling back to
       the literal "deliverable" when ordered_agents is empty>, producer_step="deliverable",
       content=final_output, kind="deliverable", location="artifact_refs/deliverable",
       visibility="workspace"). Guard the write with `if final_output and results:` —
       a run with no completed agents or an empty deliverable writes NOTHING (this also
       keeps the write out of the all-agents-failed path 13-06 hardens, and artifact
       writes emit no WS events so the 5 characterization snapshots stay byte/event
       identical).
    3. SC-001 check: the only new literal is the generic kind "deliverable" — no
       workflow name, no agent-id literal. Do not touch the revision-side write at
       engine.py:3507 (kind=target_artifact_type) — revision-of-revision keeps working
       through the exact-match chain link added in Task 2.
    4. Run the characterization + phase5 artifact suites; if any test pins the exact SET
       of refs a completed run persists, extend its expectation with the new
       (kind="deliverable", producer_step="deliverable") row — cite F2 in the test
       comment.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_ppt.py tests/unit/test_artifact_store.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Every completed run persists exactly one new deliverable-kind ref per completion carrying final_output; characterization event streams unchanged.</done>
  <acceptance_criteria>
    - `grep -c '"deliverable"' backend/agents/artifacts/graph.py` >= 1 (ARTIFACT_KINDS member)
    - engine.py contains a `_dual_write_artifact` call with kind="deliverable" and visibility="workspace" in the execute() terminal block (grep confirms both kwargs near the call)
    - the write is guarded on non-empty final_output AND non-empty results (source assertion)
    - `python3.11 -m pytest tests/agents/test_characterization_prototype.py -q` exits 0 with no golden file modified
    - `/opt/homebrew/bin/lint-imports` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: FR-014 fallback lookup chain in _handle_revision</name>
  <files>backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py lines 3402-3450 (the FR-014 lookup: assert_owns at 3405, list_refs(kind=target_artifact_type) at 3408, the ValueError at 3411-3414, version_history = _refs at 3444, the workspace threading off `original` at 3424-3441)
    - backend/agents/execution_engine/engine.py lines 3610-3625 (_AGENT_KIND_MAP + the summary fallback kind — why non-prototype final agents persist kind="summary")
    - frontend/src/components/layout/DashboardLayout.tsx lines 388-405 (the FE-exact payload this must accept — read-only context, no FE change in this plan)
    - backend/app/api/websocket.py lines 831-910 (run_revision ingress forwarding target_artifact_type verbatim — unchanged by this plan)
  </read_first>
  <action>
    Replace the single exact-kind lookup at engine.py:3408 with a deterministic fallback
    chain, keeping assert_owns FIRST (T-5-SEED ordering untouched):

    1. Chain link 1 — exact: `list_refs(parent_run_id, kind=target_artifact_type)`
       (preserves revision-of-revision, any future exact-kind producer, and the existing
       exact-kind unit tests).
    2. Chain link 2 — deliverable: when link 1 is empty, `list_refs(parent_run_id,
       kind="deliverable")` (the Task-1 completion ref — the path every NEW run takes).
    3. Chain link 3 — legacy summary: when link 2 is empty, `list_refs(parent_run_id,
       kind="summary")` (pre-fix completed runs: the final agent's output lands as
       kind="summary" via the _AGENT_KIND_MAP fallback). Latest-by-version semantics
       stay `_refs[-1]` exactly as today.
    4. All links empty → raise the existing FR-014 ValueError unchanged (byte-identical
       message — tests pin it).
    5. `version_history` (line 3444) must be the refs list of WHICHEVER link matched
       (not unconditionally the link-1 list). The downstream use of `original`
       (workspace threading at 3424, derived_from at 3513) is kind-agnostic — no other
       change in the method.
    6. Log one INFO line naming which chain link resolved (target kind, resolved kind,
       ref id) for live diagnosability — no new WS event (stream parity).
    7. SC-001: the chain introduces only the generic kinds "deliverable"/"summary" —
       no `ppt_output` literal, no workflow-name branch in the kernel.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_revision_intelligence.py tests/agents/test_characterization_prototype_revision.py -q</automated>
  </verify>
  <done>The FR-014 lookup resolves the parent deliverable through the 3-link chain; existing exact-kind revision tests and the revision characterization suite stay green.</done>
  <acceptance_criteria>
    - engine.py _handle_revision contains three list_refs lookups (exact target, "deliverable", "summary") evaluated in that order (source assertion on the method body)
    - the FR-014 ValueError message text is byte-unchanged ("Revision MUST NOT proceed without original context (FR-014)" still greps in engine.py exactly once)
    - `python3.11 -m pytest tests/unit/test_revision_intelligence.py -q` exits 0 (existing exact-kind seeds pass via link 1)
    - `python3.11 -m pytest tests/agents/test_characterization_prototype_revision.py -q` exits 0 with no golden re-baseline
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: De-mask the seed tests + FE-exact regression test</name>
  <files>backend/tests/unit/test_revision_intelligence.py, backend/tests/unit/test_run_revision_fe_contract.py</files>
  <read_first>
    - backend/tests/unit/test_revision_intelligence.py (the _seed_ref helper + the kind="spec"/target="spec" pattern at lines 113-250 — the masking: tests seed exactly the kind they target, which no real run persists for the FE's targets)
    - backend/tests/agents/_scripted_model.py line 403 (_drive — the scripted full-run driver whose persisted-parent state the regression test can reuse, or mirror its seeding)
    - backend/agents/execution_engine/engine.py lines 3340-3545 (_handle_revision full flow: websocket_send_fn collector contract, state_restoration_failed on error at 3522, terminal pipeline_complete at 3533)
  </read_first>
  <action>
    1. In test_revision_intelligence.py add a "realistic persistence" scenario block
       (keep all existing exact-kind tests — they now cover chain link 1):
       (a) seed a parent run the way the RUN PATH actually persists — per-agent
       kind="summary" refs (one per agent id) + a kind="deliverable" ref + a
       kind="planning_context" ref; send the FE-exact target_artifact_type "ppt_output"
       → assert the revision proceeds (no FR-014 ValueError) and resolves the
       deliverable ref's content;
       (b) the same parent WITHOUT the deliverable ref (pre-fix legacy run) → assert
       the summary fallback resolves and the revision proceeds;
       (c) a parent with NO summary/deliverable/exact refs → assert the FR-014
       ValueError still raises (the guard is not weakened);
       (d) cross-owner replay of (a) → PermissionError still propagates (L16 untouched).
    2. New backend/tests/unit/test_run_revision_fe_contract.py — the WS-shaped
       regression from the UAT missing item: complete a scripted run end-to-end (drive
       engine.execute() with the scripted model, mirroring _drive scaffolding, so the
       Task-1 completion path persists the deliverable ref organically — do NOT seed it
       by hand), then call engine._handle_revision with the FE-exact frame fields
       (parent_run_id=<the completed run>, target_artifact_type="od_ppt_output",
       instruction=<text>) and a list-collector websocket_send_fn. Assert: no
       state_restoration_failed event was sent; a pipeline_complete event was sent with
       pipeline_type "od_ppt_output_revision"; and its final_output (revision_context)
       embeds the parent deliverable content. This test FAILS on pre-fix code with the
       FR-014 error — it is the contract regression for DashboardLayout.tsx:396.
    3. Comment both files' new scenarios citing F2 and 13-UAT.md Gap 2 (the masking
       removal is deliberate and in-scope per the gap's missing items).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_revision_intelligence.py tests/unit/test_run_revision_fe_contract.py -q</automated>
  </verify>
  <done>The revision suite proves the FE-exact contract against realistically-persisted parents (new + legacy) and still proves the FR-014 guard and L16 ownership.</done>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/unit/test_run_revision_fe_contract.py -q` exits 0 and the file contains the literal target "od_ppt_output" (or "ppt_output") as sent by DashboardLayout.tsx
    - test_revision_intelligence.py contains a scenario seeding kind="summary" (NOT the target kind) that asserts the revision PROCEEDS, and a no-refs scenario that asserts the FR-014 ValueError RAISES
    - the regression test's parent deliverable ref is produced by driving execute() (grep shows no manual kind="deliverable" seed in test_run_revision_fe_contract.py)
    - cross-owner scenario asserts PermissionError propagation (L16)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS client → run_revision ingress | untrusted parent_run_id + target_artifact_type + instruction |
| revision flow → parent run artifacts | cross-run read of another run's persisted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Elevation of privilege / IDOR | fallback chain reading a parent run the caller does not own | mitigate | store.assert_owns(parent_run_id) stays FIRST, before any chain link (T-5-SEED ordering pinned by acceptance criteria + the cross-owner test in Task 3) |
| T-13-05-02 | Tampering | attacker-supplied target_artifact_type as a kind filter | accept | the value is only ever an equality filter on owner-scoped list_refs (parameterized, no interpolation) and a kind label on the revision's own ref — unchanged exposure, now with two fixed generic fallbacks |
| T-13-05-03 | Information disclosure | deliverable ref visibility="workspace" exposing run output workspace-wide | accept | identical to the existing producer/summary write policy (05-06 decision); deliverable content already flows to every workspace member via the run itself |
</threat_model>

<verification>
- tests/unit/test_revision_intelligence.py (existing + de-masked scenarios) green
- tests/unit/test_run_revision_fe_contract.py green (fails on pre-fix code)
- tests/agents/test_characterization_prototype_revision.py + prototype/od_ppt characterization green, no golden re-baseline
- /opt/homebrew/bin/lint-imports green
</verification>

<success_criteria>
- ROADMAP Phase 13 criterion 2 TRUE: the FE's exact run_revision payload against a completed parent passes FR-014 and produces a revision, for both new and pre-fix parents (F2)
- Kernel remains workflow-agnostic: no *_output workflow literal in backend/agents/execution_engine/ (SC-001)
</success_criteria>

<output>
Create `.planning/phases/13-live-verification-gap-closure/13-05-SUMMARY.md` when done.
</output>
