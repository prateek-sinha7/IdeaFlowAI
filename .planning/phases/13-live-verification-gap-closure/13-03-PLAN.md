---
phase: 13-live-verification-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/prompts/app-code-compliance/AGENT.md
  - backend/agents/prompts/app-test-compliance/AGENT.md
  - backend/agents/prompts/app-sdlc-governance/AGENT.md
  - backend/agents/prompts/app-devops/AGENT.md
  - backend/agents/prompts/app-system-design/AGENT.md
  - backend/agents/prompts/app-api-design/AGENT.md
  - backend/agents/prompts/app-infra-generator/AGENT.md
  - backend/agents/prompts/app-test-implementation/AGENT.md
  - backend/agents/prompts/app-feature-implementation/AGENT.md
autonomous: true
gap_closure: true
requirements: [F5]
must_haves:
  truths:
    - "app-code-compliance, app-test-compliance and app-sdlc-governance AGENT.md prompts contain no Java/.NET/Maven/NuGet/legacy/parallel-run migration-template language (F5)"
    - "app-devops AGENT.md mandates filename:-block output and forbids narration-only responses (F5)"
    - "app-test-implementation is instructed to import/exercise only the implementation's actual exported surface from its context; app-api-design, app-infra-generator and app-devops mandate the single /api/v1 prefix (F5)"
    - "app-system-design and the re-templated agents are instructed to state assumptions and proceed — never stall asking the user clarifying questions (F5)"
  artifacts:
    - path: "backend/agents/prompts/app-code-compliance/AGENT.md"
      provides: "greenfield app_builder compliance prompt (stack derived from upstream context, Node/TS default)"
    - path: "backend/agents/prompts/app-test-compliance/AGENT.md"
      provides: "greenfield test-compliance prompt (no legacy parallel-run harness)"
    - path: "backend/agents/prompts/app-sdlc-governance/AGENT.md"
      provides: "greenfield SDLC governance prompt (no migration/decommission language)"
    - path: "backend/agents/prompts/app-devops/AGENT.md"
      provides: "explicit filename:-block output contract"
      contains: "filename:"
  key_links:
    - from: "backend/agents/prompts/app-test-implementation/AGENT.md"
      to: "app-feature-implementation output via context_from"
      via: "contract rule: import only the exported surface present in context"
    - from: "backend/agents/prompts/app-api-design/AGENT.md"
      to: "backend/agents/prompts/app-infra-generator/AGENT.md"
      via: "shared /api/v1 prefix mandate"
      pattern: "/api/v1"
---

<objective>
Close UAT Gap 5 (finding F5, major): the Opus 15-agent app_builder audit found 4 dead
agents and cross-agent contract drift. Root causes (diagnosed, prompt-level — zero engine
work): (1) app-code-compliance, app-test-compliance and app-sdlc-governance AGENT.md
prompts carry migration-pipeline template language copied from the mulesoft/dotnet
pipelines ("Java/Maven on AWS, or .NET/NuGet on Azure", "parallel-run harness against the
legacy system", legacy-decommission criteria) — live agents stall asking which stack or
emit nothing; (2) app-devops elicits narration ("Now let me create...") with zero file
blocks; (3) no cross-agent contract guidance — auth tests import a different class/method/
error surface than the implementation exports, and API design says /api/* while CI/CD
targets /api/v1/*.

Purpose: ROADMAP Phase 13 success criterion 5 — all 15 app_builder agents role-conformant
with consistent cross-agent contracts.
Output: re-templated/contracted AGENT.md prompt bodies only (frontmatter schemas
untouched); loader suite green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-live-verification-gap-closure/13-UAT.md
@.planning/live-verification/REPORT.md
</context>

## Artifacts this phase produces

- Rewritten prompt BODIES for 9 AGENT.md files under `backend/agents/prompts/` (listed in files_modified) — no new code symbols, no frontmatter field changes (id/order/pipeline_type/context_from stay as-is), no registry changes
- A `/api/v1` prefix mandate string shared across app-api-design, app-infra-generator and app-devops prompts
- An explicit `filename:`-block output-contract section in app-devops/AGENT.md

<tasks>

<task type="auto">
  <name>Task 1: Re-template the three migration-flavored prompts for app_builder scope</name>
  <files>backend/agents/prompts/app-code-compliance/AGENT.md, backend/agents/prompts/app-test-compliance/AGENT.md, backend/agents/prompts/app-sdlc-governance/AGENT.md</files>
  <read_first>
    - backend/agents/prompts/app-code-compliance/AGENT.md (current body — the "(Java/Maven on AWS, or .NET/NuGet on Azure)" stack-choice at line 29 and the Java/.NET tooling lists at 40-58 are the stall trigger)
    - backend/agents/prompts/app-test-compliance/AGENT.md (the "parallel-run harness against the legacy system" framing at lines 26-29 and 70)
    - backend/agents/prompts/app-sdlc-governance/AGENT.md (migration wrap-up mandate at 34, legacy-decommission criteria at 78, "next migration" at 82; keeps its filename:-block deliverable format shown at line 92)
    - backend/agents/prompts/app-code-generator/AGENT.md and backend/agents/prompts/app-feature-implementation/AGENT.md (the healthy greenfield app_builder voice + stack framing to align with)
    - backend/agents/prompts/app-system-design/AGENT.md (the upstream context producer these agents consume)
    - backend/CLAUDE.md "Adding an Agent" section (AGENT.md schema — bodies only, frontmatter untouched)
  </read_first>
  <action>
    Rewrite the PROMPT BODY (below the frontmatter) of each of the three agents for
    greenfield app_builder scope. Keep each agent's role identity, section structure
    style, and output format conventions; change the domain framing:

    1. app-code-compliance: the compliance target is the GREENFIELD application built by
       this pipeline. The stack is whatever the upstream design/implementation context
       specifies — default Node.js/TypeScript when the context does not pin one (per the
       F5 missing item). Replace the Java/Checkstyle/Spotless and .NET/Roslyn/dotnet-format
       tooling lists with stack-appropriate equivalents derived from context (ESLint +
       typescript-eslint, Prettier, tsc --strict for the Node/TS default), plus one line
       allowing substitution when the context names a different stack. Remove the
       "(Java/Maven on AWS, or .NET/NuGet on Azure)" choice entirely. Add an explicit
       anti-stall rule: never ask the user which stack — read it from the provided
       context, state the assumption, and proceed.
    2. app-test-compliance: remove the legacy-system/parallel-run-harness framing
       (lines 26-29 and the line-70 region). The role becomes test-strategy compliance
       for the greenfield app: coverage thresholds and gates over the test suites the
       implementation/test agents in THIS pipeline produce (unit/integration/e2e), CI
       test-failure reporting, flake policy. Tooling references move to the Node/TS
       default (vitest/jest + coverage) with the same context-substitution line. Add the
       same anti-stall rule.
    3. app-sdlc-governance: remove migration/legacy/decommission/parallel-run/"next
       migration" language. The role becomes greenfield SDLC governance: ADRs for the
       choices the upstream agents actually made (read from context), branching +
       code-review policy, release gates, environments/promotion, definition-of-done.
       Keep the existing filename:-block deliverable format. Add the same anti-stall rule.
    4. All three: keep bodies focused and roughly the current length; do NOT touch
       frontmatter (id, name, role, pipeline_type, order, context_from, tools, produces,
       consumes); do NOT rename files or agents; do NOT edit registry.py.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_loader.py -q && ! grep -liE "maven|nuget|parallel-run|legacy system" agents/prompts/app-code-compliance/AGENT.md agents/prompts/app-test-compliance/AGENT.md agents/prompts/app-sdlc-governance/AGENT.md</automated>
  </verify>
  <done>The three prompts are greenfield app_builder prompts with zero migration-template residue; every agent still loads through the schema-validation suite.</done>
  <acceptance_criteria>
    - `grep -ciE "maven|nuget|parallel-run|legacy system" backend/agents/prompts/app-code-compliance/AGENT.md` returns 0; same for app-test-compliance and app-sdlc-governance
    - `grep -ci "Java/Maven on AWS" backend/agents/prompts/app-code-compliance/AGENT.md` returns 0
    - Each of the three bodies contains an explicit proceed-without-asking rule (grep -i "state the assumption" or equivalent phrasing present in all three)
    - `python3.11 -m pytest tests/agents/test_loader.py -q` exits 0 (frontmatter untouched, all ~80 agents still load)
    - `git diff --stat` shows ONLY the three AGENT.md files changed by this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: app-devops output contract + app-system-design anti-stall</name>
  <files>backend/agents/prompts/app-devops/AGENT.md, backend/agents/prompts/app-system-design/AGENT.md</files>
  <read_first>
    - backend/agents/prompts/app-devops/AGENT.md (has a filename: example at ~line 76 but no enforcement — live output was pure narration "Now let me create the CD workflow..." with zero file blocks)
    - backend/agents/prompts/app-system-design/AGENT.md (stalled live on clarifying questions)
    - backend/agents/prompts/app-infra-generator/AGENT.md (a sibling that emits file blocks correctly — mirror its contract phrasing if it has one)
  </read_first>
  <action>
    1. app-devops: add an explicit "## Output contract" section stating: EVERY deliverable
       artifact MUST be emitted as a filename: fenced block (the exact format already
       exemplified in the body); narration about what you are "about to" create is
       FORBIDDEN — write the file block instead; a response containing zero filename:
       blocks is a failed response; any prose is limited to a brief summary AFTER the
       file blocks. Keep the existing pipeline/CD content otherwise intact.
    2. app-system-design: add an anti-stall rule near the top of the body: the agent
       never asks the user clarifying questions — upstream context (user stories, UX
       design) plus stated assumptions drive the design; when information is missing,
       pick the conventional choice, record it in an "Assumptions" subsection of the
       deliverable, and proceed to the full design.
    3. Bodies only; frontmatter untouched in both files.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_loader.py -q && grep -c "filename:" agents/prompts/app-devops/AGENT.md && grep -ci "assumption" agents/prompts/app-system-design/AGENT.md</automated>
  </verify>
  <done>app-devops carries a hard filename:-block output contract; app-system-design carries a proceed-with-assumptions rule; loader suite green.</done>
  <acceptance_criteria>
    - `grep -c "Output contract" backend/agents/prompts/app-devops/AGENT.md` >= 1 (or an equivalently-named mandatory section) and `grep -c "filename:" backend/agents/prompts/app-devops/AGENT.md` >= 2 (example + contract)
    - app-devops body contains an explicit prohibition on narration-without-file-blocks (grep -i "narration" or "about to" prohibition present)
    - `grep -ci "assumption" backend/agents/prompts/app-system-design/AGENT.md` >= 1 and the body contains a never-ask-clarifying-questions rule
    - `python3.11 -m pytest tests/agents/test_loader.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cross-agent contracts — impl/test export surface + single /api/v1 prefix</name>
  <files>backend/agents/prompts/app-test-implementation/AGENT.md, backend/agents/prompts/app-feature-implementation/AGENT.md, backend/agents/prompts/app-api-design/AGENT.md, backend/agents/prompts/app-infra-generator/AGENT.md, backend/agents/prompts/app-devops/AGENT.md</files>
  <read_first>
    - backend/agents/prompts/app-test-implementation/AGENT.md (frontmatter context_from at lines 6-15 — confirm which implementation agents' outputs it consumes; the live break: tests imported default AuthController(registerUser/loginUser, bcrypt, EmailDuplicateError) while the impl exported class AuthController(register/login, bcryptjs, DuplicateEmailError))
    - backend/agents/prompts/app-feature-implementation/AGENT.md (the implementation agent whose export surface the tests must mirror)
    - backend/agents/prompts/app-api-design/AGENT.md (live output used /api/* while CI/CD targeted /api/v1/*)
    - backend/agents/prompts/app-infra-generator/AGENT.md and backend/agents/prompts/app-devops/AGENT.md (the infra/CI consumers of the API prefix)
  </read_first>
  <action>
    1. app-test-implementation: add a "## Contract fidelity" rule block: the tests MUST
       import and exercise EXACTLY the exported surface that appears in the
       implementation code provided in your context — same module paths, same
       class/function names, same method names, same error-class names, same dependency
       package names (e.g. bcryptjs vs bcrypt: use whichever the implementation imports).
       Quote each name from the context before using it; NEVER invent or "improve" a
       name. A test that cannot compile against the implementation in context is a
       failed deliverable.
    2. app-feature-implementation: add a one-paragraph "stable export surface" rule:
       export every public class/function through explicit named exports and keep
       naming consistent with the API design context, because the downstream test agent
       imports this exact surface.
    3. API prefix: standardize on the literal prefix /api/v1 (decision recorded here —
       resolves the live /api vs /api/v1 drift). app-api-design: mandate every route is
       rooted at /api/v1 (state it once as a hard rule). app-infra-generator and
       app-devops: where health checks, route probes, ingress paths or smoke tests
       reference API paths, mandate the /api/v1 prefix consistent with the API design
       context. Use the identical literal "/api/v1" in all three bodies.
    4. Bodies only; frontmatter (incl. context_from) untouched. app-devops edits in this
       task compose with Task 2's contract section (same file — apply both).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_loader.py -q && grep -l "/api/v1" agents/prompts/app-api-design/AGENT.md agents/prompts/app-infra-generator/AGENT.md agents/prompts/app-devops/AGENT.md | wc -l | grep -q 3 && grep -qi "context" agents/prompts/app-test-implementation/AGENT.md</automated>
  </verify>
  <done>Test agent is contractually bound to the implementation's actual exports; one /api/v1 prefix mandated identically across API design, infra and CI prompts; loader suite green.</done>
  <acceptance_criteria>
    - `grep -c "/api/v1" backend/agents/prompts/app-api-design/AGENT.md` >= 1, same for app-infra-generator and app-devops (the identical literal in all three)
    - app-test-implementation body contains the exact-surface rule (grep -i "never invent" or equivalent prohibition present) and names at least: class/function names, error names, and dependency package names as surfaces to mirror
    - app-feature-implementation body contains the stable-export-surface paragraph
    - `python3.11 -m pytest tests/agents/test_loader.py -q` exits 0
    - No file outside backend/agents/prompts/ modified in this plan (`git diff --stat` confirms)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENT.md files → composed system prompts | engineer-authored, file-trusted prompt content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Spoofing / prompt injection | prompt bodies interpolating user input | accept | AGENT.md bodies are static version-controlled engineer content; no user input is interpolated by this plan; runtime user context flows through the separate skills/hooks channels unchanged |
| T-13-03-02 | Tampering | prompt edits silently changing agent tool grants | mitigate | frontmatter (tools/permissions) is explicitly out of scope — acceptance criteria pin `git diff` to prompt bodies and the loader schema suite re-validates every agent |
</threat_model>

<verification>
- tests/agents/test_loader.py green (all agents load, schema intact)
- Zero migration-language hits in the three re-templated prompts (grep gates)
- /api/v1 literal present in all three prefix-bearing prompts
- git diff confined to backend/agents/prompts/
</verification>

<success_criteria>
- ROADMAP Phase 13 criterion 5 TRUE at the prompt-contract level: the 3 migration-templated prompts are re-templated for app_builder, app-devops emits files not narration by contract, and cross-agent contract rules (export surface, /api/v1) are threaded (F5)
- Live 15-agent semantic re-audit deferred to the end-of-milestone live pass (project convention)
</success_criteria>

<output>
Create `.planning/phases/13-live-verification-gap-closure/13-03-SUMMARY.md` when done.
</output>
