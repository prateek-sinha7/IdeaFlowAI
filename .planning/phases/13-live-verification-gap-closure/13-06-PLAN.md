---
phase: 13-live-verification-gap-closure
plan: 06
type: execute
wave: 3
depends_on: [13-05]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/app/api/websocket.py
  - backend/tests/unit/test_pipeline_failure_semantics.py
  - frontend/src/types/index.ts
  - frontend/src/hooks/useWorkflow.ts
  - frontend/src/app/dashboard/page.tsx
autonomous: true
gap_closure: true
requirements: [F3]
must_haves:
  truths:
    - "A run whose agents ALL hard-fail terminates with a pipeline_failed event and state 'failed' — never pipeline_complete with final_output='' (F3)"
    - "A partially-failed run emits pipeline_complete with ADDITIVE status='degraded' + agents_failed fields, absent on clean runs (characterization byte-parity held) (F3)"
    - "A pipeline whose agents declare template injects is rejected at WS ingress with code missing_template_context when no template body is loadable — bare prototype/ppt fail fast instead of silently degrading (F3)"
    - "The FE treats pipeline_failed as a terminal failure state (F3)"
  artifacts:
    - path: "backend/agents/execution_engine/engine.py"
      provides: "all-agents-failed -> pipeline_failed terminal branch + conditional degraded fields"
      contains: "pipeline_failed"
    - path: "backend/app/api/websocket.py"
      provides: "ingress guard: template-inject-declaring agents require loadable template context"
      contains: "missing_template_context"
    - path: "frontend/src/types/index.ts"
      provides: "pipeline_failed in the WS event type unions"
      contains: "pipeline_failed"
    - path: "backend/tests/unit/test_pipeline_failure_semantics.py"
      provides: "regression tests for total-failure, partial-failure and clean-run terminal semantics"
      min_lines: 60
  key_links:
    - from: "backend/agents/execution_engine/engine.py (dispatch loop)"
      to: "terminal emit"
      via: "agent_error tracking set consulted at the terminal block"
      pattern: "pipeline_failed"
    - from: "frontend/src/hooks/useWorkflow.ts"
      to: "pipeline_failed event"
      via: "terminal failure case in the WS message switch"
      pattern: "pipeline_failed"
---

<objective>
Close UAT Gap 3 (finding F3, major): a run whose agents hard-fail still ends in
`pipeline_complete`. Live evidence: ws-prototype (bare, no od_context) — all 4 agents
emitted agent_error ("declares injects=[template,...] but no template body was loaded")
and the run ended pipeline_complete with final_output='' in 12s; ws-ppt — 2/3 agents
halted and the surviving validator improvised the whole deck. Two compounding defects:
(1) the terminal emit is unconditionally pipeline_complete regardless of failures
(engine.py ~1695); (2) WS ingress admits bare prototype/ppt whose agents declare
`injects: [template, ...]` while od_context is only assembled for od_* aliases
(websocket.py 1233-1252), so every agent is halted pre-execution by the factory.

Fix all three UAT missing items: visible terminal semantics (pipeline_failed + degraded
fields), an ingress guard keyed on declared injects (generic — no workflow-name branch),
and FE handling. Product decision (recorded): bare prototype/ppt stay admissible but
fail fast with a clear error when template context is missing — the FE always sends od_*
aliases, so no user-visible flow changes except the broken ones becoming visible.

Purpose: ROADMAP Phase 13 success criterion 3.
Output: engine terminal semantics, WS ingress guard, FE pipeline_failed handling, tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-live-verification-gap-closure/13-UAT.md
@.planning/live-verification/REPORT.md
</context>

## Artifacts this phase produces

- `pipeline_failed` — new terminal WS event type emitted by `ExecutionEngine.execute()` (data: pipeline_type, pipeline_run_id, total_duration, agents_completed=0, agents_total, agents_failed list, error, timestamp)
- Conditional ADDITIVE fields `status: "degraded"` and `agents_failed: [...]` on `pipeline_complete` data (present only when at least one agent_error occurred)
- Failed-agent tracking set inside the execute() dispatch loop (inspects yielded agent_error events)
- `missing_template_context` — new ingress rejection error code in `backend/app/api/websocket.py`
- `"pipeline_failed"` added to the FE WS event type unions in `frontend/src/types/index.ts` (both unions: the message-type literal union at line 48 and the pipeline-event union at ~line 407)
- `pipeline_failed` terminal-failure handling in `frontend/src/hooks/useWorkflow.ts` and `frontend/src/app/dashboard/page.tsx`
- New test file `backend/tests/unit/test_pipeline_failure_semantics.py`

<tasks>

<task type="auto">
  <name>Task 1: Engine terminal semantics — pipeline_failed on total collapse, degraded fields on partial failure</name>
  <files>backend/agents/execution_engine/engine.py, backend/tests/unit/test_pipeline_failure_semantics.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py lines 1496-1623 (the dispatch loop: _dispatch_step_with_retry event forwarding at 1557-1558 where agent_error events can be observed; the terminal-state guard + completed transition at 1617-1623)
    - backend/agents/execution_engine/engine.py lines 1653-1710 (deliverable resolution + the unconditional pipeline_complete emit at 1695 with agents_completed=len(results))
    - backend/agents/execution_engine/engine.py lines 2489-2525 (the agent_error yield sites in _run_agent — data carries agent_id + recoverable; errored agents do NOT append to results)
    - backend/agents/execution_engine/engine.py lines 1596-1615 (the BudgetExceeded handler — the existing precedent for a failed-terminal branch: transition to "failed" + a dedicated event + return)
    - backend/agents/execution_engine/state_machine.py (confirm "failed" is a reachable terminal state from "generating")
    - 13-05-PLAN.md Task 1 (the deliverable persist is guarded on non-empty results — the total-failure branch added here returns BEFORE deliverable resolution, so the two compose)
  </read_first>
  <action>
    1. In the execute() dispatch loop, track failures: where step events are forwarded
       (the `async for event in self._dispatch_step_with_retry(...): yield event` site
       at 1557 and any other yield path that can carry agent_error, e.g. the strategy
       fallback), record `event.get("data", {}).get("agent_id")` into a local
       `_failed_agent_ids: set[str]` when `event.get("type") == "agent_error"`. Pure
       observation — no event is modified, reordered or suppressed.
    2. Terminal branch (total collapse): at the Step-5 terminal block (after the
       cancelled/failed state guard at 1621), when `not results and _failed_agent_ids`
       — every agent that ran errored and nothing completed — transition the state
       machine to "failed" (mirror the BudgetExceeded precedent at 1604), emit a single
       `pipeline_failed` event with data {pipeline_type, pipeline_run_id,
       total_duration (same rounding as pipeline_complete), agents_completed: 0,
       agents_total: len(ordered_agents), agents_failed: sorted(_failed_agent_ids),
       error: "all agents failed", timestamp: _now()}, and RETURN before deliverable
       resolution (no empty-deliverable resolution, no deliverable ref write, no
       pipeline_complete). Keep the budget-snapshot persist call if fan-out was active
       (mirror the BudgetExceeded handler ordering).
    3. Degraded completion (partial failure): when results exist AND _failed_agent_ids
       is non-empty, add to the pipeline_complete data dict the ADDITIVE keys
       `status: "degraded"` and `agents_failed: sorted(_failed_agent_ids)`. These keys
       are STRICTLY CONDITIONAL — a clean run's pipeline_complete payload is
       byte-identical (the 5 characterization snapshots gate this; mirror the OBS-01
       conditional-snapshot pattern documented at 1687-1689).
    4. SC-001/INV-1: the branch keys on counters only — no workflow name, no agent-id
       literal, no spec.id comparison.
    5. New backend/tests/unit/test_pipeline_failure_semantics.py (scripted-model or
       stubbed-_run_agent style, offline): (a) all-agents-error run → exactly one
       pipeline_failed, NO pipeline_complete, state machine in "failed",
       agents_failed lists every agent id; (b) one-of-two agents errors → terminal
       pipeline_complete carries status="degraded" + the failed id, and agents_completed
       reflects the survivor; (c) clean run → pipeline_complete data contains NEITHER
       "status" NOR "agents_failed" key (parity guard).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_pipeline_failure_semantics.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_prototype_revision.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Total collapse emits pipeline_failed and lands in state 'failed'; partial failure is visibly degraded; clean runs byte-identical; all characterization suites green without re-baseline.</done>
  <acceptance_criteria>
    - `grep -c '"pipeline_failed"' backend/agents/execution_engine/engine.py` >= 1
    - the degraded keys are written inside a conditional on the failed set (source assertion: no unconditional "status"/"agents_failed" key in the pipeline_complete data literal at ~1697-1709)
    - `python3.11 -m pytest tests/unit/test_pipeline_failure_semantics.py -q` exits 0 with the three scenarios (a)/(b)/(c) collected
    - the 5 characterization suites pass with no file under tests/agents/characterization/golden/ modified
    - `grep -v "^\s*#" backend/agents/execution_engine/engine.py | grep -c "if pipeline_type =="` returns 0 (kernel banned-pattern gate intact)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WS ingress guard — template-inject pipelines require loadable template context</name>
  <files>backend/app/api/websocket.py, backend/tests/unit/test_pipeline_failure_semantics.py</files>
  <read_first>
    - backend/app/api/websocket.py lines 1228-1310 (od_context assembly only for od_* aliases at 1233-1274; agent resolution at 1285-1307 — the guard lands AFTER agents are resolved so it keys on the resolved specs' declared injects, and the existing error-event shape at 1278-1283 is the template to mirror)
    - backend/agents/factory.py lines 368-395 (_compose_injection raises AgentSpecError-style halt ONLY when "template" is in injects and od.get("template_body") is missing — the guard's condition must mirror this raise condition EXACTLY so ingress rejects precisely the runs the factory would halt)
    - backend/tests/unit/test_run_pipeline_validation.py (the existing ingress-validation test patterns — model_overrides rejection etc. — to mirror for the new guard's tests)
  </read_first>
  <action>
    1. In websocket.py, immediately after the agents list is resolved (post line ~1306
       "if not agents" guard), add the ingress guard: when any resolved spec declares
       "template" in (spec.injects or []) AND (od_context or {}).get("template_body")
       is falsy, send one error event mirroring the existing shape — type "error",
       chunk None, section None, data {error: a message naming the pipeline_type and
       stating that its agents declare template injection so the run requires a
       template (template_id) or an od_* alias, code: "missing_template_context",
       recoverable: False} — and return WITHOUT starting the engine. The condition is
       GENERIC (declared injects only — no pipeline-name allow/deny list), so:
       bare prototype/ppt without a template are rejected; od_* runs with a loaded
       template pass unchanged; workflows whose agents declare no template inject are
       untouched; and od_ppt_revision without a template_id is rejected ONLY if its
       resolved agents actually declare a template inject (i.e. exactly the runs the
       factory would halt anyway — fail-fast is strictly better than the silent
       per-agent collapse).
    2. Record the product decision as a comment at the guard: bare prototype/ppt remain
       admissible pipeline types (tier sets include them) but require template context;
       the FE always sends od_* aliases (decided in 13-06, F3 — see 13-UAT.md Gap 3
       missing item 3).
    3. Extend test_pipeline_failure_semantics.py (or a sibling class in the same file)
       with handler-level ingress tests mirroring test_run_pipeline_validation.py's
       driving style: (d) bare "prototype" with no template_id → exactly one error
       event with code "missing_template_context", engine never invoked; (e) the same
       pipeline with od_context carrying a template_body → guard passes (engine
       invoked / no error event); (f) a no-injects pipeline (e.g. user_stories) with no
       template → guard does not fire.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_pipeline_failure_semantics.py tests/unit/test_run_pipeline_validation.py -q</automated>
  </verify>
  <done>Runs that would silently collapse at the factory now fail fast at ingress with a typed error code; all other admissions byte-unchanged.</done>
  <acceptance_criteria>
    - `grep -c "missing_template_context" backend/app/api/websocket.py` >= 1
    - the guard condition references spec.injects membership of "template" and od_context template_body (source assertion) — no pipeline-name string comparison added for the guard
    - scenarios (d)/(e)/(f) pass: `python3.11 -m pytest tests/unit/test_pipeline_failure_semantics.py -q` exits 0
    - `python3.11 -m pytest tests/unit/test_run_pipeline_validation.py -q` exits 0 (existing ingress validations unchanged)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: FE handling for pipeline_failed</name>
  <files>frontend/src/types/index.ts, frontend/src/hooks/useWorkflow.ts, frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/types/index.ts line 48 (the WS message-type literal union — "pipeline_complete"/"pipeline_cancelled" are members) and ~line 407 (the pipeline-event union)
    - frontend/src/hooks/useWorkflow.ts line ~312 (the "pipeline_complete" case in the WS switch — the terminal-state handling to mirror for failure) and the surrounding error/cancel cases
    - frontend/src/app/dashboard/page.tsx lines ~310-360 (the terminal-event membership list at 312 including "pipeline_complete"/"pipeline_cancelled", and the pipeline_complete data handling at 353)
  </read_first>
  <action>
    1. types/index.ts: add "pipeline_failed" to BOTH unions (the line-48 message-type
       literal union and the ~line-407 pipeline-event union).
    2. useWorkflow.ts: add a "pipeline_failed" case to the WS message switch — terminal
       failure: stop run-in-progress state, mark the run/pipeline failed, surface the
       event's data.error and data.agents_failed to the user-visible error state the
       hook already maintains for agent_error/pipeline_cancelled (mirror whichever
       existing terminal case manages spinner/run-state teardown — do not invent a new
       state shape).
    3. dashboard/page.tsx: add "pipeline_failed" to the terminal-event membership list
       at line ~312 so the page-level run lifecycle treats it as terminal, and handle it
       adjacent to the pipeline_complete branch at ~353: no deliverable extraction,
       surface the failure (existing toast/error surface used for "error" events).
    4. Optionally (only if trivially supported by the existing state): render
       data.status === "degraded" on pipeline_complete as a warning note; do NOT build
       new UI surfaces for it — the contract fields are the deliverable here.
    5. Run the FE checks the repo already uses (lint + tsc via the frontend's package
       scripts) — the union additions must compile everywhere the event types are
       switched exhaustively.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The FE compiles with pipeline_failed as a first-class terminal event; a totally-failed run shows a failure state instead of an empty success.</done>
  <acceptance_criteria>
    - `grep -c "pipeline_failed" frontend/src/types/index.ts` returns 2 (both unions)
    - `grep -c "pipeline_failed" frontend/src/hooks/useWorkflow.ts` >= 1 (switch case) and `grep -c "pipeline_failed" frontend/src/app/dashboard/page.tsx` >= 1 (terminal membership + handling)
    - `npx tsc --noEmit` exits 0 in frontend/
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS client → run_pipeline ingress | untrusted pipeline_type/template_id/agent_ids admission |
| engine → WS client | failure detail (error strings, failed agent ids) emitted to the run owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Denial of service | malformed run_pipeline payloads forcing engine startup work | mitigate | the ingress guard rejects unsatisfiable inject-declaring runs BEFORE checkpointer/sandbox/engine spin-up — strictly less work on bad input than today |
| T-13-06-02 | Information disclosure | pipeline_failed.error / agents_failed leaking internals | accept | events flow only on the owner's own authenticated WS connection; payload carries the same agent_error strings the client already receives per-agent |
| T-13-06-03 | Tampering | degraded fields injected into clean-run payloads breaking FE assumptions | mitigate | fields are strictly conditional on observed agent_error events; scenario (c) test pins clean-run payload key-set; characterization snapshots gate byte-parity |
</threat_model>

<verification>
- tests/unit/test_pipeline_failure_semantics.py: scenarios (a)-(f) green
- 5 characterization suites green, no golden re-baseline (clean-run terminal payload byte-identical)
- tests/unit/test_run_pipeline_validation.py green (existing ingress contract intact)
- frontend `npx tsc --noEmit` green
- /opt/homebrew/bin/lint-imports green; kernel banned-pattern grep gate at 0
</verification>

<success_criteria>
- ROADMAP Phase 13 criterion 3 TRUE: hard-failing runs terminate as a visible failure (pipeline_failed / degraded status) or fail fast at ingress (missing_template_context) — never a silent pipeline_complete with an empty or improvised deliverable (F3)
- Bare prototype/ppt product exposure decided and recorded: admissible, template-required, fail-fast (F3 missing item 3)
</success_criteria>

<output>
Create `.planning/phases/13-live-verification-gap-closure/13-06-SUMMARY.md` when done.
</output>
