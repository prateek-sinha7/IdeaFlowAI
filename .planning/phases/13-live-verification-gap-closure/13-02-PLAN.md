---
phase: 13-live-verification-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/factory.py
  - backend/agents/capabilities/prompt/policy.py
  - backend/app/agents/deep_agent_runner.py
  - backend/app/agents/handoff/coder.py
  - backend/tests/agents/test_text_only_prompt_hygiene.py
  - backend/tests/unit/test_handoff_coder_hardening.py
autonomous: true
gap_closure: true
requirements: [F4]
must_haves:
  truths:
    - "Agents resolved to zero tools get an explicit no-tools preamble in their composed system prompt forbidding fabricated tool-call syntax (F4)"
    - "The handoff coder retries once with a corrective re-prompt on JSON parse failure and extracts JSON even when the response is polluted with fabricated function_calls XML (F4)"
    - "DeepAgentRunner strips fabricated function_calls XML spans from the terminal done output of tool-less agents (F4 defense-in-depth)"
  artifacts:
    - path: "backend/agents/factory.py"
      provides: "no-tools preamble block in _compose_system_prompt when zero tools are bound"
      contains: "tool_availability"
    - path: "backend/agents/capabilities/prompt/policy.py"
      provides: "tool_availability slot in the default PromptAssemblyPolicy order"
      contains: "tool_availability"
    - path: "backend/app/agents/handoff/coder.py"
      provides: "JSON-only prompt constraint + bounded parse-retry + XML-tolerant _extract_json"
      contains: "function_calls"
    - path: "backend/app/agents/deep_agent_runner.py"
      provides: "fabricated tool-XML sanitizer on the done output for tool-less agents"
  key_links:
    - from: "backend/agents/factory.py"
      to: "backend/agents/capabilities/prompt/policy.py"
      via: "blocks['tool_availability'] assembled by the default prompt policy"
      pattern: "tool_availability"
    - from: "backend/app/agents/handoff/coder.py"
      to: "DeepAgentRunner.astream_events"
      via: "bounded retry loop around the one-shot stream + parse"
      pattern: "for attempt in"
---

<objective>
Close UAT Gap 4 (finding F4, major): live Haiku fabricates Claude-internal tool syntax
(`<function_calls>`/`<invoke>`/`write_todos` XML) as plain text when composed prompts imply
file/tool actions without bound tools — leaking XML into prose outputs of user_stories,
custom, app_builder and dotnet agents, and breaking the handoff coder's one-shot JSON
edit-plan (1 of 2 live runs returned tool-XML instead of JSON).

The od_ppt instance of this class is already FIXED (commit 8ba8f626 seeded template files +
granted workspace tools). This plan delivers the remaining two missing items plus the
optional defense-in-depth: (1) prompt-hygiene guard for text-only composition, (2) handoff
coder hardening, (3) output sanitation of fabricated XML.

Purpose: ROADMAP Phase 13 success criterion 4.
Output: no-tools preamble in prompt composition, hardened handoff coder, runner-level
output sanitizer, tests for all three.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-live-verification-gap-closure/13-UAT.md
@.planning/live-verification/REPORT.md
</context>

## Artifacts this phase produces

- `blocks["tool_availability"]` — new named prompt block emitted by `_compose_system_prompt` in `backend/agents/factory.py` for agents resolved to zero tools
- `"tool_availability"` slot added (first) to the default `PromptAssemblyPolicy` order in `backend/agents/capabilities/prompt/policy.py`
- A `no_tools`/tool-resolution parameter threaded from `create_runner` into `_compose_system_prompt` (factory.py)
- `_strip_fabricated_tool_xml(text)` — module-level helper in `backend/app/agents/deep_agent_runner.py` applied to the terminal `done` output when `exclude_builtin_tools=True` and no custom tools are bound
- Bounded retry loop (max 2 attempts) inside `HandoffCoder.propose_edits` + a JSON-only/no-XML constraint paragraph in `_CODING_SYSTEM_PROMPT` + XML-span stripping in `_extract_json` (backend/app/agents/handoff/coder.py)
- New test files `backend/tests/agents/test_text_only_prompt_hygiene.py` and `backend/tests/unit/test_handoff_coder_hardening.py`

<tasks>

<task type="auto">
  <name>Task 1: No-tools preamble in prompt composition + runner-level XML sanitizer</name>
  <files>backend/agents/factory.py, backend/agents/capabilities/prompt/policy.py, backend/app/agents/deep_agent_runner.py, backend/tests/agents/test_text_only_prompt_hygiene.py</files>
  <read_first>
    - backend/agents/factory.py lines 100-340 (create_runner — tools resolved at line 179 via _resolve_runner_tools; _compose_system_prompt at 256-336 builds named blocks and delegates order/join to resolve("prompt","default"))
    - backend/agents/factory.py lines 480-540 (_resolve_runner_tools — (custom_tools, exclude_builtin) semantics; note the MCP flip at ~532: exclude_builtin turns off when MCP tools bind, and "planning" agents carry PLANNING_TOOLS — neither must receive the preamble)
    - backend/agents/capabilities/prompt/policy.py (the default PromptAssemblyPolicy — block order list the new slot must join)
    - backend/app/agents/deep_agent_runner.py lines 200-230 and 440-455 (exclude_builtin_tools param at 208; the terminal done event with full_output at 449)
    - backend/agents/prompts/epic-architect/AGENT.md (a flagged tools:[] agent — sanity-check the preamble lands for it)
  </read_first>
  <action>
    1. In factory.py `create_runner`: move (or duplicate-free reorder) the
       `_resolve_runner_tools(spec, ctx)` call so its result is available BEFORE
       `_compose_system_prompt` runs, and thread a derived boolean into composition —
       `no_tools = exclude_builtin_tools and not custom_tools` (True only when the agent
       has literally zero callable tools: tools:[] text-only agents; False for
       workspace/prototype/planning agents and MCP-bound agents).
    2. In `_compose_system_prompt(spec, ctx, *, no_tools=False)`: when no_tools is True,
       set `blocks["tool_availability"]` to a terse preamble block stating, concretely:
       the agent has NO tools in this session; it must NEVER emit tool-call syntax of any
       kind — no `<function_calls>`, `<invoke>`, `write_todos`, `read_file`, `write_file`,
       `edit_file`, or to-do blocks; any file content it produces must appear directly in
       the response as plain text; it must respond with prose/structured text only.
       Keep it under ~8 lines (guardrail-style terseness).
    3. In policy.py: add `"tool_availability"` FIRST in the default order (before
       `injects`) so the constraint frames everything that follows. The slot is emitted
       only when the block is present — composition for every tool-having agent is
       byte-identical (their blocks dict never contains the key).
    4. In deep_agent_runner.py: add a module-level `_strip_fabricated_tool_xml(text)`
       helper that removes `<function_calls>...</function_calls>` spans (non-greedy,
       DOTALL) and standalone fabricated `<invoke ...>...</invoke>` spans from text, and
       apply it to `full_output` in the terminal done event (line ~449) ONLY when the
       runner was constructed with `exclude_builtin_tools=True` and zero custom tools
       (mirror the no_tools condition — a tool-using agent's legitimate output is never
       touched). When the pattern is absent the function returns the input unchanged, so
       scripted-model characterization outputs (which never contain the pattern) stay
       byte-identical. Streamed chunks are intentionally NOT filtered (defense-in-depth
       targets the authoritative output that feeds downstream agent context and
       deliverables) — note this in the helper docstring.
    5. New test backend/tests/agents/test_text_only_prompt_hygiene.py:
       (a) compose the prompt for a tools:[] agent (e.g. epic-architect) and assert the
       preamble text is present and positioned before the injects/guardrails content;
       (b) compose for a "workspace" agent (e.g. app-code-generator) and a "planning"
       agent and assert the preamble is ABSENT;
       (c) unit-test _strip_fabricated_tool_xml: a string with an embedded
       function_calls span comes back without it, a clean string comes back unchanged
       (identity, same object or equal bytes).
    6. Run the targeted factory/prompt suites; if any existing test pins the default
       policy ORDER list or a full composed prompt for a tools:[] agent, update it
       additively to include the new slot — that is the intended behavior change for F4,
       not a parity break (INV-3 protects event streams; scripted models ignore prompt
       bytes, so the 5 characterization snapshots are unaffected).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_text_only_prompt_hygiene.py tests/agents/test_create_runner.py tests/agents/test_guardrails.py -q && python3.11 -m pytest tests/agents/test_characterization_prototype.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>tools:[] agents get the no-tools preamble; tool-having agents' prompts are byte-unchanged; the runner strips fabricated XML from tool-less done outputs; characterization stays green.</done>
  <acceptance_criteria>
    - `grep -c "tool_availability" backend/agents/factory.py` >= 1 and `grep -c "tool_availability" backend/agents/capabilities/prompt/policy.py` >= 1
    - `grep -c "_strip_fabricated_tool_xml" backend/app/agents/deep_agent_runner.py` >= 2 (definition + done-output call site)
    - `python3.11 -m pytest tests/agents/test_text_only_prompt_hygiene.py -q` exits 0
    - test asserts preamble PRESENT for a tools:[] agent and ABSENT for a workspace agent and a planning agent (three distinct assertions)
    - `python3.11 -m pytest tests/agents/test_characterization_prototype.py -q` exits 0 with no golden file modified
    - `/opt/homebrew/bin/lint-imports` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Handoff coder hardening — JSON-only constraint, XML-tolerant extraction, bounded retry</name>
  <files>backend/app/agents/handoff/coder.py, backend/tests/unit/test_handoff_coder_hardening.py</files>
  <read_first>
    - backend/app/agents/handoff/coder.py (whole file, 220 lines — _CODING_SYSTEM_PROMPT output-format section at 80-106, _extract_json at 112-133, propose_edits stream+parse at 154-219; the live failure: raw started with '&lt;function_calls&gt;&lt;invoke name=read_file&gt;...' and _JSON_OBJECT_RE found no object → ValueError "handoff coder returned no JSON object")
    - backend/tests/unit/ (locate the existing handoff coder tests via `grep -rln "propose_edits\|HandoffCoder" backend/tests/` — follow their DeepAgentRunner stubbing pattern)
  </read_first>
  <action>
    1. Strengthen `_CODING_SYSTEM_PROMPT`: in the "## Output format" section add an
       explicit constraint paragraph — the agent has NO tools; it must NOT emit
       `<function_calls>`, `<invoke>`, `write_todos` or any tool-call XML; the ENTIRE
       response must be the single JSON object, nothing before or after it.
    2. Harden `_extract_json`: before the existing fence-stripping, remove
       `<function_calls>[\s\S]*?</function_calls>` spans from the raw text (the
       fabricated-XML pollution case), then proceed with the existing parse → largest
       `{...}` fallback. Keeps the current behavior for clean and fenced inputs.
    3. Bounded retry in `propose_edits`: wrap the runner-stream + `_extract_json` in a
       loop of at most 2 attempts. On (json.JSONDecodeError, ValueError) from the FIRST
       attempt: log the existing warning, then re-run the one-shot with the SAME system
       prompt and the user message augmented with a corrective suffix naming the failure
       — e.g. a final paragraph stating the previous response was not a valid JSON object
       and to respond with ONLY the JSON object per the schema (no XML, no prose, no
       fences). On failure of the second attempt, re-raise exactly as today (the pipeline
       maps it to agent_error). Construct a FRESH DeepAgentRunner per attempt (the
       one-shot has no checkpointer state to reuse). Never retry on RuntimeError from a
       runner "error" event (that is a runtime fault, not a parse fault).
    4. New test backend/tests/unit/test_handoff_coder_hardening.py (stub
       DeepAgentRunner.astream_events; no model, no network):
       (a) first stream yields tool-XML garbage, second yields a valid edit-plan JSON →
       propose_edits returns the parsed plan and made exactly 2 attempts;
       (b) both streams yield garbage → raises ValueError after exactly 2 attempts;
       (c) a response wrapping the valid JSON object inside leading function_calls XML
       parses on the FIRST attempt via the hardened _extract_json (no retry);
       (d) the corrective suffix is present in the second attempt's user message.
    5. Run the existing handoff suites to confirm no contract drift (propose_edits
       signature and return shape unchanged).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_handoff_coder_hardening.py -q && python3.11 -m pytest tests/unit -k "handoff" -q</automated>
  </verify>
  <done>The handoff coder survives one fabricated-XML response per call (retry), parses XML-polluted-but-JSON-bearing responses without retry, and fails loudly after two bad attempts.</done>
  <acceptance_criteria>
    - `grep -c "function_calls" backend/app/agents/handoff/coder.py` >= 2 (prompt constraint + _extract_json strip)
    - `python3.11 -m pytest tests/unit/test_handoff_coder_hardening.py -q` exits 0 with the four scenario tests collected
    - The retry bound is 2 total attempts (test (b) asserts exactly 2 runner constructions/streams)
    - `python3.11 -m pytest tests/unit -k "handoff" -q` exits 0 (existing handoff contract intact)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → JSON edit-plan applier | untrusted LLM text parsed into file-edit operations |
| prompt composition → model | engineer/user-attached blocks composed into the system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | _extract_json fallback grabbing a {...} span from polluted text | mitigate | existing downstream path-traversal validation on edits is unchanged; the XML strip narrows (never widens) what reaches the parser; test (c) pins the behavior |
| T-13-02-02 | Denial of service | unbounded re-prompt loop on persistent parse failure | mitigate | hard 2-attempt bound asserted by test (b); RuntimeError (runtime fault) never retried |
| T-13-02-03 | Spoofing / prompt injection | preamble block weakening user-attached skill/hook instructions | accept | the preamble is static engineer-authored text containing no interpolated user input; it constrains output FORM only |
| T-13-02-04 | Denial of service | ReDoS via the XML-strip regex on adversarial output | mitigate | use a non-backtracking-prone non-greedy `[\s\S]*?` span pattern (linear on bounded model output, max_tokens-capped) |
</threat_model>

<verification>
- Composed prompts: preamble present iff zero tools bound (three-way test)
- Handoff coder: 4 scenario tests green; existing handoff suites green
- Characterization prototype suite green, no golden modified (scripted streams unaffected by prompt bytes)
- lint-imports green
</verification>

<success_criteria>
- ROADMAP Phase 13 criterion 4 TRUE at the offline-verifiable boundary: every tools:[] composition carries the anti-fabrication preamble, the runner sanitizes tool-less outputs, and the handoff coder parses-or-retries-or-fails-loudly (F4)
- Live re-verification of actual Haiku behavior deferred to the end-of-milestone live pass (project convention)
</success_criteria>

<output>
Create `.planning/phases/13-live-verification-gap-closure/13-02-SUMMARY.md` when done.
</output>
