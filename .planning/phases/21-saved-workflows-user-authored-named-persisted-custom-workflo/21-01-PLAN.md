---
phase: 21-saved-workflows-user-authored-named-persisted-custom-workflo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/workflow_definition.py
  - backend/alembic/versions/0021_saved_user_workflows.py
  - backend/app/api/user_workflows.py
  - backend/app/main.py
  - backend/agents/execution_engine/engine.py
  - backend/tests/unit/test_user_workflows.py
autonomous: true
requirements:
  - REUSE-MANDATE
  - REUSE-TABLE-INV12
  - ADDITIVE-MIGRATION
  - CRUD-OWNER-SCOPED
  - SAVE-FROM-BOTH
  - LAUNCH-EXISTING-PATH
  - INV-3
  - SC-001
  - PORTS-ADAPTERS
  - SECURITY-REVALIDATE

must_haves:
  truths:
    - "A POST to /api/user-workflows persists a source='user' row in the existing workflows table (no new table)"
    - "Every CRUD handler owner-scopes (user_id==current_user.id AND source=='user'); cross-owner read/rename/delete returns 404"
    - "POST validates base_pipeline_type, agent_ids, and model_overrides against the SAME live predicates the launch path uses; bad values return 422"
    - "The dead engine writer _persist_workflow_definition is gone (grep returns 0) and its call site is removed"
    - "The 5 characterization goldens stay byte-identical after the engine writer removal (no SNAPSHOT_UPDATE)"
    - "Migration 0021 applies and reverses cleanly (alembic upgrade head then downgrade -1) — additive, 2 nullable columns, no new table"
  artifacts:
    - path: "backend/app/models/workflow_definition.py"
      provides: "base_pipeline_type + model_overrides nullable columns on WorkflowDefinition"
      contains: "model_overrides"
    - path: "backend/alembic/versions/0021_saved_user_workflows.py"
      provides: "additive migration adding 2 nullable columns + index, down_revision=0020"
      contains: "down_revision = \"0020\""
    - path: "backend/app/api/user_workflows.py"
      provides: "owner-scoped /api/user-workflows CRUD router (POST/GET/GET{id}/PATCH{id}/DELETE{id})"
      contains: "prefix=\"/api/user-workflows\""
    - path: "backend/tests/unit/test_user_workflows.py"
      provides: "CRUD + IDOR(404) + validation(422) + duplicate-name + schema tests"
      contains: "source"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/user_workflows.py"
      via: "include_router(user_workflows_router)"
      pattern: "user_workflows_router"
    - from: "backend/app/api/user_workflows.py"
      to: "backend/app/models/workflow_definition.py"
      via: "WorkflowDefinition ORM query owner-scoped"
      pattern: "WorkflowDefinition"
    - from: "backend/app/api/user_workflows.py"
      to: "agents.registry.allowed_custom_agent_ids / model_catalog.ModelCatalog / entitlements.can_run_pipeline"
      via: "save-time validation reusing the launch predicates"
      pattern: "allowed_custom_agent_ids"
---

<objective>
Land the BACKEND spine of Saved Workflows by REUSING the dormant `workflows` table (`WorkflowDefinition`, `source="user"`) — NOT a new table. Add 2 nullable columns via additive migration `0021`, build an owner-scoped `/api/user-workflows` CRUD router that validates a saved composition with the SAME predicates the launch path enforces, and DELETE the superseded dead `_persist_workflow_definition` engine writer (INV-12 completion).

Purpose: a saved workflow is pure data `{base_pipeline_type, agent_ids, model_overrides}` persisted under owner scope; launch replays it through the existing run path (no engine run-path edit, SC-001). Finishing the half-built persistence abstraction AND removing the dead writer is what makes this phase INV-12-complete.

Output: `WorkflowDefinition` +2 cols; migration `0021`; `app/api/user_workflows.py` + registration in `main.py`; the `_persist_workflow_definition` writer removed; `tests/unit/test_user_workflows.py`; proof that the 5 goldens stay byte-identical, `lint-imports` is 4/0, and `0021` reverses.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/21-saved-workflows-user-authored-named-persisted-custom-workflo/21-SPEC.md
@.planning/phases/21-saved-workflows-user-authored-named-persisted-custom-workflo/21-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `backend/app/models/workflow_definition.py` — MODIFIED: +`base_pipeline_type` (String, nullable) +`model_overrides` (JSON, nullable).
- `backend/alembic/versions/0021_saved_user_workflows.py` — NEW: additive migration (2 nullable cols + `ix_workflows_owner_source`), `down_revision="0020"`, reversible.
- `backend/app/api/user_workflows.py` — NEW: owner-scoped CRUD router.
- `backend/app/main.py` — MODIFIED: import + `include_router(user_workflows_router)`.
- `backend/agents/execution_engine/engine.py` — MODIFIED (DELETION ONLY): `_persist_workflow_definition` method + its call site removed.
- `backend/tests/unit/test_user_workflows.py` — NEW: CRUD/IDOR/validation/duplicate-name/schema tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the 2 nullable columns + additive migration 0021 + remove the dead engine writer (INV-12)</name>
  <read_first>
    - backend/app/models/workflow_definition.py (the WorkflowDefinition class — existing column idiom at :36-40; `JSON` already imported :8; `artifact_edges` NOT NULL :29). ANALOG: the model's own existing columns (21-PATTERNS §1).
    - backend/alembic/versions/0020_wave_runs.py (head pointer + `sa.JSON()` + named index + reversible downgrade). ANALOG A (21-PATTERNS §2).
    - backend/alembic/versions/0017_repositories_repo_workspace.py:60-63,87-88 (`op.batch_alter_table(...)` portable-ALTER shape). ANALOG B (21-PATTERNS §2).
    - backend/agents/execution_engine/engine.py (the writer `_persist_workflow_definition` at :4026-4107 with its custom-only guard :4052-4056; the ONLY call site at :1380-1386). DELETION target (21-PATTERNS §5, 21-SPEC §3.3).
  </read_first>
  <action>
    Add two nullable columns to the WorkflowDefinition class beside `manifest_json`, before `created_at`: `base_pipeline_type = Column(String, nullable=True)` and `model_overrides = Column(JSON, nullable=True)` (per REUSE-TABLE-INV12 / ADDITIVE-MIGRATION; D §2 SPEC). Reuse `agents` (Text) as the ordered agent-id list and write `artifact_edges="[]"` for user rows later in the router — do NOT relax any constraint here. `JSON` is already imported; do not re-import.

    Create `backend/alembic/versions/0021_saved_user_workflows.py` copied from the 0020 header shape + the 0017 batch_alter idiom: `revision="0021"`, `down_revision="0020"`. `upgrade()` runs `op.batch_alter_table("workflows")` adding the two nullable columns, then `op.create_index("ix_workflows_owner_source", "workflows", ["owner_id", "source"])` (backs the GET list query). `downgrade()` drops the index then drops both columns inside `batch_alter_table` — fully reversible, SQLite-portable. ADDITIVE ONLY (ADDITIVE-MIGRATION / INV-3): no `create_table`, no alter of existing columns/constraints.

    DELETE the dead writer (INV-12 / REUSE-TABLE-INV12): remove the entire `async def _persist_workflow_definition` method (:4026-4107, ending just before the "Revision intelligence" section) AND its only call site (:1380-1386, the `await self._persist_workflow_definition(...)` block with its "Persist custom workflow definition (T061, FR-013)" comment). No replacement in the engine — the new router supersedes it. The writer is custom-only (guard :4052-4056 returns None for standard pipelines) so it is dormant on the 5 non-custom goldens — removal must leave them byte-identical (INV-3). Run python3.11 (no venv).
  </action>
  <acceptance_criteria>
    - `grep -c 'base_pipeline_type' backend/app/models/workflow_definition.py` >= 1 AND `grep -c 'model_overrides' backend/app/models/workflow_definition.py` >= 1.
    - `grep -c 'down_revision = "0020"' backend/alembic/versions/0021_saved_user_workflows.py` == 1; `grep -c 'create_table' backend/alembic/versions/0021_saved_user_workflows.py` == 0 (additive — no new table).
    - INV-12 proof: `grep -c '_persist_workflow_definition' backend/agents/execution_engine/engine.py` == 0 (method AND call site both gone).
    - Migration reversibility: from `backend/`, `python3.11 -m alembic upgrade head && python3.11 -m alembic downgrade -1 && python3.11 -m alembic upgrade head` exits 0 (no new table created/dropped). If the configured DB is unavailable offline, fall back to an in-memory SQLite harness in the test file (Task 3) that runs `command.upgrade`/`command.downgrade` and asserts the 2 columns appear/disappear.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -c '_persist_workflow_definition' agents/execution_engine/engine.py | grep -qx 0 && grep -q 'model_overrides' app/models/workflow_definition.py && grep -q 'down_revision = "0020"' alembic/versions/0021_saved_user_workflows.py && grep -qvx 'create_table' alembic/versions/0021_saved_user_workflows.py && echo PASS</automated>
  </verify>
  <done>The 2 nullable columns exist on WorkflowDefinition; migration 0021 is additive + reversible (`down_revision="0020"`, no `create_table`); `_persist_workflow_definition` and its call site are removed (grep==0).</done>
</task>

<task type="auto">
  <name>Task 2: Owner-scoped /api/user-workflows CRUD router with save==launch validation + register in main.py</name>
  <read_first>
    - backend/app/api/runs.py (router shell + auth import :27-36; owner-filtered list :101-129; IDOR→404 get/delete :258-297; 204 delete). ANALOG A (21-PATTERNS §3).
    - backend/app/api/mcp.py:220-234 (create-row idiom: stamp owner from current_user, add/commit/refresh). ANALOG B (21-PATTERNS §3).
    - backend/app/api/websocket.py (`_validate_model_overrides` :95-155; the launch re-validation block :1385-1406 + :1466; entitlement gate :576-581). The save-time predicates MUST mirror these (Shared Pattern §A).
    - backend/agents/registry.py:331-395 (`allowed_custom_agent_ids`); backend/agents/loader.py:29 (`SUPPORTED_PIPELINE_TYPES`); backend/agents/capabilities/model_catalog.py:128 (`ModelCatalog().ids()`); backend/app/core/entitlements.py:8-31,46-55 (`TIER_PIPELINES` — custom∈enterprise — and `can_run_pipeline`).
    - backend/app/core/dependencies.py:153-178 (`get_current_user`). backend/app/main.py:17,160 (runs_router import + include). ANALOG (21-PATTERNS §4).
  </read_first>
  <action>
    Create `backend/app/api/user_workflows.py` with `router = APIRouter(prefix="/api/user-workflows", tags=["user-workflows"])`; every handler `Depends(get_current_user)` and `Depends(get_db)` (CRUD-OWNER-SCOPED). Define a Pydantic POST body `{name: str, description: str | None, base_pipeline_type: str, agent_ids: list[str], model_overrides: dict[str,str] | None}` and a response model projecting the row (id, name, description, base_pipeline_type, agent_ids parsed from `agents`, model_overrides, created_at, updated_at).

    POST: validate (SECURITY-REVALIDATE, reusing the launch predicates VERBATIM) — `base_pipeline_type in SUPPORTED_PIPELINE_TYPES` else 422; `rejected = [a for a in agent_ids if a not in allowed_custom_agent_ids(base_pipeline_type)]` non-empty → 422; import and call `_validate_model_overrides` (or replicate its two checks: key∈agent_ids, value∈`ModelCatalog().ids()`) with `run_agent_ids=set(agent_ids)`, non-None error → 422; gate `can_run_pipeline(current_user.tier, base_pipeline_type)` (custom needs enterprise per LAUNCH-EXISTING-PATH / SAVE-FROM-BOTH), deny → 403 fail-fast (no orphan rows). Name uniqueness = API-level: reject a `name` already among the caller's `source="user"` rows → 409/422 (NOT a DB constraint — the shared table also holds source="file" rows). Insert: `source="user"`, `agents=json.dumps(agent_ids)`, `artifact_edges="[]"`, self-id stamp `user_id=owner_id=workspace_id=current_user.id`, persist `base_pipeline_type` + `model_overrides`; add/commit/refresh.

    GET "": `WHERE user_id==current_user.id AND source=="user"`, `order_by(updated_at.desc())`. GET "/{id}" / PATCH "/{id}" (rename + optional description/model_overrides edit, re-validate overrides if changed) / DELETE "/{id}": each filters `id==:id AND user_id==current_user.id`; miss or cross-owner → 404 (IDOR→404, runs.py:258-267); DELETE → 204 (`response_class=Response`, `status_code=204`).

    Register in `backend/app/main.py`: `from app.api.user_workflows import router as user_workflows_router` beside the runs import, and `app.include_router(user_workflows_router)` beside `runs_router`. Keep the new module in `app.*`; the `app → agents.registry/loader/model_catalog` import direction is already legal (PORTS-ADAPTERS) — do NOT import `agents.execution_engine`. Run `/opt/homebrew/bin/lint-imports` to confirm 4 kept / 0 broken.
  </action>
  <acceptance_criteria>
    - `grep -c 'prefix="/api/user-workflows"' backend/app/api/user_workflows.py` == 1; every handler routes through `get_current_user` (grep `Depends(get_current_user)` >= 5).
    - Owner-scope present: `grep -c 'source == "user"' backend/app/api/user_workflows.py` >= 1 AND the get/patch/delete handlers filter on `user_id == current_user.id` (grep >= 3) AND raise `HTTP_404_NOT_FOUND` on miss (grep >= 1).
    - Validation reuse present: the file references `allowed_custom_agent_ids`, `SUPPORTED_PIPELINE_TYPES`, `ModelCatalog`, and `can_run_pipeline` (grep each >= 1).
    - Self-id stamp + user-row conventions present: `grep -c 'artifact_edges' backend/app/api/user_workflows.py` >= 1 (writes `"[]"`); the insert sets owner_id/workspace_id/user_id to current_user.id.
    - Registered: `grep -c 'user_workflows_router' backend/app/main.py` == 2 (import + include).
    - Ports & Adapters: `/opt/homebrew/bin/lint-imports` reports 4 contracts kept, 0 broken; the new module does NOT import `agents.execution_engine`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -q 'prefix="/api/user-workflows"' app/api/user_workflows.py && grep -q 'allowed_custom_agent_ids' app/api/user_workflows.py && grep -q 'can_run_pipeline' app/api/user_workflows.py && grep -q 'HTTP_404_NOT_FOUND' app/api/user_workflows.py && [ "$(grep -c 'user_workflows_router' app/main.py)" = "2" ] && /opt/homebrew/bin/lint-imports && echo PASS</automated>
  </verify>
  <done>`/api/user-workflows` POST/GET/GET{id}/PATCH{id}/DELETE{id} are owner-scoped (IDOR→404), POST validates base/agents/overrides with the launch predicates + entitlement gate + per-user name uniqueness, the router is registered in main.py, and lint-imports is 4/0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Backend tests — CRUD + IDOR(404) + validation(422) + duplicate-name + schema/migration; goldens-parity proof</name>
  <read_first>
    - backend/tests/unit/test_runs_api.py:21-100 (in-memory SQLite + dependency-override harness — `_FakeUser`, `db_session`, `api` fixtures, `_make_run` helper; NO Postgres). ANALOG (21-PATTERNS §7).
    - backend/app/api/user_workflows.py (the router under test — produced in Task 2).
    - backend/tests/agents/test_characterization_prototype.py / _od_prototype / _prototype_revision / _od_ppt / _app_builder + test_manifest_parity.py (the 5 goldens + manifest parity — the INV-3 proof, must run with SNAPSHOT_UPDATE unset).
  </read_first>
  <behavior>
    - POST with valid {name, base_pipeline_type='custom', agent_ids⊆allowed, model_overrides valid} → 201/200, row persisted with source='user', artifact_edges='[]', self-id stamp.
    - POST with an agent_id NOT in allowed_custom_agent_ids → 422; POST with a model_id NOT in ModelCatalog().ids() → 422; POST with base_pipeline_type∉SUPPORTED_PIPELINE_TYPES → 422.
    - POST with a name already used by the caller's source='user' rows → rejected (409/422).
    - GET "" lists ONLY the caller's source='user' rows (a seeded source='file' row and another user's row are excluded).
    - Flip the override user to a second user → GET/PATCH/DELETE "/{id}" of the first user's row → 404 (IDOR, never 403/leak).
    - DELETE own row → 204; subsequent GET → 404.
    - Schema: loading the model exposes `base_pipeline_type` and `model_overrides` attributes/columns.
  </behavior>
  <action>
    Create `backend/tests/unit/test_user_workflows.py` by copying the `test_runs_api.py` harness (in-memory SQLite `create_engine("sqlite://")` + `Base.metadata.create_all` + `app.dependency_overrides[get_current_user]`/`[get_db]`, `TestClient`). Import `from app.api.user_workflows import router`. Add a `_make_wf(db, ...)` helper (mirror `_make_run`) seeding `WorkflowDefinition` rows with `source` parameterized. Write the tests enumerated in `<behavior>` (CRUD-OWNER-SCOPED, SECURITY-REVALIDATE, REUSE-TABLE-INV12). Add a migration/schema test: assert the model has the 2 new columns (e.g. via `WorkflowDefinition.__table__.columns`), and if running offline add an in-memory `command.upgrade`/`command.downgrade` assertion that the columns appear then disappear (covers ADDITIVE-MIGRATION reversibility without Postgres).

    Then PROVE INV-3: run the 5 characterization goldens + manifest parity with SNAPSHOT_UPDATE cleared and assert byte-identity (the engine-writer removal is the only engine edit and is dormant on these non-custom pipelines). This is a RUN, not prose — put the goldens in the verify command. Run python3.11 (no venv). The full backend pytest hangs offline — run ONLY the targeted node ids below.
  </action>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/unit/test_user_workflows.py -q` passes; the suite includes a cross-owner 404 (IDOR) test, three 422 validation tests (bad agent_id / bad model_id / bad base_pipeline_type), a duplicate-name rejection, a list-scoping test, a 204-delete test, and a 2-new-columns schema test.
    - INV-3 goldens byte-identical (SNAPSHOT_UPDATE unset): `cd backend && SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_manifest_parity.py -q` passes with ZERO snapshot regenerations.
    - Ports & Adapters: `/opt/homebrew/bin/lint-imports` reports 4 kept / 0 broken.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && SNAPSHOT_UPDATE= python3.11 -m pytest tests/unit/test_user_workflows.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_manifest_parity.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>CRUD/IDOR(404)/validation(422)/duplicate-name/list-scoping/204-delete/schema tests pass; the 5 goldens + manifest parity are byte-identical with SNAPSHOT_UPDATE unset (INV-3 proven post-writer-removal); lint-imports 4/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/user-workflows | authenticated user submits a workflow name + composition (untrusted) crossing into owner-scoped persistence |
| stored saved row → launch (run_pipeline WS) | a previously-saved agent/model set is replayed; the allow-list may have changed since save |
| new router → agents.* | app-layer router reads registry/catalog/entitlements (legal Ports&Adapters direction); MUST NOT reach into the engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure / Elevation | GET/PATCH/DELETE /api/user-workflows/{id} | mitigate | every handler filters `user_id==current_user.id AND source=='user'`; cross-owner or missing id → 404 (IDOR→404, never 403/leak), per runs.py:258-267. Tested in Task 3 (cross-owner 404). |
| T-21-02 | Tampering | POST agent_ids / model_overrides / base_pipeline_type | mitigate | save-time validation reuses the EXACT launch predicates (`SUPPORTED_PIPELINE_TYPES`, `allowed_custom_agent_ids`, `ModelCatalog().ids()`, `can_run_pipeline`); invalid → 422/403. A saved row can never carry something the launch path rejects. |
| T-21-03 | Tampering / Elevation | launching a stale saved composition | mitigate | launch path is UNCHANGED and re-validates agent_ids + model_overrides at run time (websocket.py:1385-1406,1466) against the LIVE allow-list — a since-disallowed agent/model is rejected at launch, not trusted from storage. |
| T-21-04 | Elevation | new endpoint capability surface | accept | the router adds NO capability grant — `exec`/`network`/`secrets`/`spawn_subagents` stay OFF; it only persists/reads pure data (`{base_pipeline_type, agent_ids, model_overrides}`). No code-exec path introduced. |
| T-21-05 | Tampering | persistence integrity | mitigate | self-id stamp `user_id=owner_id=workspace_id=current_user.id`; `source="user"` + `artifact_edges="[]"`; additive migration only (no constraint relaxation); name uniqueness enforced per-user at the API. |
| T-21-SC | Tampering | npm/pip installs | mitigate | no new packages installed this plan (reuses FastAPI/SQLAlchemy/alembic already in the tree); no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep -c '_persist_workflow_definition' backend/agents/execution_engine/engine.py` == 0 (INV-12 dead writer removed).
- The 5 characterization goldens + manifest parity byte-identical with SNAPSHOT_UPDATE unset (INV-3).
- `alembic upgrade head` then `downgrade -1` then `upgrade head` exits 0 (additive, reversible; no new table) — or the in-memory equivalent in the test file.
- `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken (Ports & Adapters).
- `python3.11 -m pytest tests/unit/test_user_workflows.py -q` green (CRUD/IDOR/validation/duplicate-name/schema).
</verification>

<success_criteria>
- REUSE-TABLE-INV12: saved workflows persist as `source="user"` rows in the existing `workflows` table; the dead `_persist_workflow_definition` writer is removed (grep 0).
- ADDITIVE-MIGRATION: `0021` adds exactly 2 nullable columns (+ optional index), `down_revision="0020"`, reverses cleanly, no new table.
- CRUD-OWNER-SCOPED + SECURITY-REVALIDATE: owner-scoped CRUD (IDOR→404), save validates with the launch predicates + entitlement gate + per-user name uniqueness.
- INV-3: the 5 goldens stay byte-identical post-writer-removal.
- SC-001 / PORTS-ADAPTERS: a saved workflow is pure data; the new module stays in `app.*`; lint-imports 4/0; no engine run-path edit.
</success_criteria>

<output>
Create `.planning/phases/21-saved-workflows-user-authored-named-persisted-custom-workflo/21-01-SUMMARY.md` when done.
</output>
