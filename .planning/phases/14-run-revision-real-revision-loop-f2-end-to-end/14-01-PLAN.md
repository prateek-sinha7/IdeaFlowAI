---
phase: 14-run-revision-real-revision-loop-f2-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/workflows/ppt_revision/workflow.yaml
  - backend/agents/workflows/od_ppt_revision/workflow.yaml
  - backend/tests/agents/test_manifest_parity.py
  - backend/tests/agents/test_id_alias_resolver.py
  - backend/tests/agents/_scripted_model.py
autonomous: true
requirements: [F2]
must_haves:
  truths:
    - "ppt_revision and od_ppt_revision manifests declare planner: skip — a dispatched revision run bypasses the planner LLM call AND the clarify pause (the indefinite event.wait() at clarify_engine.py:155 is never reached)"
    - "Both planner-run parity traps (test_manifest_parity.py AND test_id_alias_resolver.py) pin the new contract: exactly these two ids expect skip, every other dispatchable manifest still expects run"
    - "The settled design wrinkle is executable: a regression test asserts no agent of the two run_revision-dispatched pipelines declares a template or design_system inject (revision dispatch passes od_context=None and never traverses the 13-06 missing_template_context ingress guard)"
    - "The scripted-model harness can drive both revision pipelines deterministically: _scripts_for has dedicated branches for od-ppt-revision-agent, ppt-revision-agent, and ppt-revision-assembler emitting artifact-wrapped REVISED decks distinct from the parent deck"
  artifacts:
    - path: "backend/agents/workflows/ppt_revision/workflow.yaml"
      provides: "planner: skip (data-only flip, INV-5)"
      contains: "planner: skip"
    - path: "backend/agents/workflows/od_ppt_revision/workflow.yaml"
      provides: "planner: skip (data-only flip, INV-5)"
      contains: "planner: skip"
    - path: "backend/tests/agents/test_manifest_parity.py"
      provides: "two-id planner carve-out + injects-free design-wrinkle pin test"
      contains: "_RUN_REVISION_DISPATCHED"
    - path: "backend/tests/agents/_scripted_model.py"
      provides: "scripted revision-agent turns for deterministic dispatch tests"
      contains: "od-ppt-revision-agent"
  key_links:
    - from: "backend/agents/workflows/od_ppt_revision/workflow.yaml"
      to: "engine.py planner-skip branch (engine.py:1198-1205)"
      via: "compiled.planner == 'skip' -> gate_verdict=PROCEED, no planner events, clarify never reached"
      pattern: "planner: skip"
    - from: "backend/tests/agents/test_manifest_parity.py"
      to: "agents/prompts/{od-ppt-revision-agent,ppt-revision-agent,ppt-revision-assembler}/AGENT.md"
      via: "load_agent_spec injects assertion (design-wrinkle pin)"
      pattern: "injects"
---

<objective>
Phase 14 foundations (finding F2 follow-through): make the two run_revision-dispatched
manifests dispatchable through `execute()` without hanging, pin the planner flip in BOTH
parity traps, make the settled design wrinkle executable, and give the scripted-model
harness deterministic revision-agent turns for the Wave-2/3 test rewrites.

Why the flip: every dispatchable manifest today declares `planner: run` + `clarify.mode:
auto`. Clarify-auto pauses INDEFINITELY at `event.wait()` (clarify_engine.py:134-155)
waiting for a questionnaire round-trip the FE revision panel never sends — a dispatched
revision would hang forever (RESEARCH Pitfall 1). The INV-5-conformant fix is manifest
data: `planner: skip` makes the engine's skip branch (engine.py:1198-1205) set
`gate_verdict = "PROCEED"` and bypass planner + clarify entirely. NO engine edit.

Scope guard (RESEARCH Open Q4): ONLY `ppt_revision` and `od_ppt_revision` flip — they are
the two pipelines the FE reaches via the `run_revision` WS frame (DashboardLayout.tsx:
388-403). `prototype_revision` (characterization golden + `revises_existing: true`),
`user_stories_revision`, and `app_builder_revision` ride `run_pipeline` and are explicitly
OUT OF SCOPE — do not touch them.

Purpose: ROADMAP Phase 14 success criteria 1 and 3 preconditions.
Output: two one-key manifest flips, two updated parity tests, one new design-pin test,
three new scripted-model branches.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-RESEARCH.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-PATTERNS.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-VALIDATION.md
</context>

## Artifacts this phase produces (plan 14-01 scope)

- `planner: skip` key value in `backend/agents/workflows/ppt_revision/workflow.yaml` and `backend/agents/workflows/od_ppt_revision/workflow.yaml` (data flip — no new symbol)
- `_RUN_REVISION_DISPATCHED` — module-level frozenset constant (`{"ppt_revision", "od_ppt_revision"}`) in `backend/tests/agents/test_manifest_parity.py`, reused by `backend/tests/agents/test_id_alias_resolver.py` (its own copy or import — executor's choice, both files carve out the same two ids)
- `test_run_revision_manifests_declare_planner_skip` — new test function in `test_manifest_parity.py` (or the carve-out folded into the existing parametrized assert — either shape, the contract is what matters)
- `test_run_revision_revision_agents_declare_no_template_injects` — new design-wrinkle pin test in `test_manifest_parity.py`
- Three new `_scripts_for` branches in `backend/tests/agents/_scripted_model.py`: `od-ppt-revision-agent`, `ppt-revision-agent`, `ppt-revision-assembler` (each returning `_ScriptedTurn` lists; the last agent of each pipeline emits an `<artifact>`-wrapped REVISED deck)

<tasks>

<task type="auto">
  <name>Task 1: Flip ppt_revision + od_ppt_revision to planner: skip and update both planner parity traps + add the design-wrinkle pin</name>
  <files>backend/agents/workflows/ppt_revision/workflow.yaml, backend/agents/workflows/od_ppt_revision/workflow.yaml, backend/tests/agents/test_manifest_parity.py, backend/tests/agents/test_id_alias_resolver.py</files>
  <read_first>
    - backend/agents/workflows/ppt_revision/workflow.yaml (current: planner: run; 2 steps ppt-revision-agent -> ppt-revision-assembler; clarify block is a REQUIRED key)
    - backend/agents/workflows/od_ppt_revision/workflow.yaml (current: planner: run; 1 step od-ppt-revision-agent)
    - backend/tests/agents/test_manifest_parity.py (all 98 lines — trap #1 test_planner_run_everywhere at 70-79 parametrized over PIPELINE_AGENTS minus _INTERNAL_PIPELINES; its docstring anticipates this revisit; trap #2 clarify-defaults at 87-97 must keep passing UNCHANGED)
    - backend/tests/agents/test_id_alias_resolver.py lines 1-130 (_DISPATCHABLE at line 26 = sorted(set(PIPELINE_AGENTS) - {"chat","reverse_engineer"}) — it INCLUDES both revision ids; test_compiled_planner_is_run_everywhere at 119-125 pins planner == "run" for every _DISPATCHABLE id and MUST be carved out too — this second trap was missed by 14-RESEARCH.md, verified by grep during planning)
    - backend/agents/execution_engine/engine.py lines 1196-1210 (skip semantics: skip_planner -> _default_planning_context(user_message), gate_verdict="PROCEED", no planner events, clarify never reached)
    - backend/agents/prompts/od-ppt-revision-agent/AGENT.md, backend/agents/prompts/ppt-revision-agent/AGENT.md, backend/agents/prompts/ppt-revision-assembler/AGENT.md (frontmatter — verify NO injects key is declared; grep confirmed zero matches during planning)
  </read_first>
  <action>
    1. In BOTH workflow.yaml files change exactly one key: `planner: run` -> `planner: skip`.
       Touch NOTHING else — the `clarify:` block stays byte-identical (required manifest key;
       parity trap #2 reads clarify.defaults and must keep passing), steps/deliverable/
       context_providers/seed_files unchanged. Do NOT add `revises_existing` to the
       deliverable blocks (test_revision_gating.py pins it true ONLY on prototype_revision;
       the previous_run provider must keep no-op'ing for these dispatches).
       Update the header comment in each file to note the Phase-14 planner flip and why
       (run_revision dispatch; clarify-auto would hang at event.wait()).
    2. test_manifest_parity.py: add module constant
       `_RUN_REVISION_DISPATCHED = frozenset({"ppt_revision", "od_ppt_revision"})`.
       Update test_planner_run_everywhere: expected = "skip" for ids in
       _RUN_REVISION_DISPATCHED, "run" otherwise; update the assertion message and the
       module docstring trap #1 paragraph (the run_revision-dispatched manifests declare
       skip as of Phase 14 — clarify-auto would hang the revision panel; all other
       dispatchable manifests still declare run). Do NOT touch test_prototype_planner_runs
       (line 62) or the trap #2 clarify-defaults test.
    3. test_id_alias_resolver.py: apply the SAME two-id carve-out to
       test_compiled_planner_is_run_everywhere (expected "skip" for the two ids, "run"
       otherwise) with a comment citing Phase 14 and test_manifest_parity.py as the
       sibling trap.
    4. test_manifest_parity.py: add test_run_revision_revision_agents_declare_no_template_injects —
       for each workflow_id in _RUN_REVISION_DISPATCHED, compile the manifest (reuse
       _compile), and for each step agent_id load the spec via agents.loader.load_agent_spec;
       assert "template" not in spec.injects and "design_system" not in spec.injects.
       Docstring must record the settled design wrinkle WITH evidence (this is the
       executable pin the planning context mandates): revision dispatch passes
       od_context=None and goes _handle_revision -> execute(), which NEVER traverses the
       run_pipeline ingress where the 13-06 missing_template_context guard lives
       (websocket.py _handle_workflow_execution); factory._compose_injection raises
       TemplateMissingError ONLY for inject-declaring agents; the composed revision context
       already embeds the complete parent deck (the physical realization of the template).
       A future inject-declaring revision agent must first persist template_id on
       WorkflowRun (additive migration, Q3) — this test makes that prerequisite loud.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_manifest_parity.py tests/agents/test_id_alias_resolver.py tests/agents/test_revision_gating.py tests/agents/test_characterization_prototype_revision.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Both manifests declare planner: skip; both parity traps pass with the two-id carve-out; the injects-free pin test passes; prototype_revision golden and revision gating untouched and green.</done>
  <acceptance_criteria>
    - `grep -c "planner: skip" backend/agents/workflows/ppt_revision/workflow.yaml` returns 1 AND `grep -c "planner: run" backend/agents/workflows/ppt_revision/workflow.yaml` returns 0 (same pair for od_ppt_revision/workflow.yaml)
    - `git diff --stat backend/agents/workflows/prototype_revision/ backend/agents/workflows/user_stories_revision/ backend/agents/workflows/app_builder_revision/` shows zero changes
    - test_manifest_parity.py contains `_RUN_REVISION_DISPATCHED` and a test function whose name contains `no_template_injects`
    - `python3.11 -m pytest tests/agents/test_manifest_parity.py tests/agents/test_id_alias_resolver.py -q` exits 0
    - `python3.11 -m pytest tests/agents/test_characterization_prototype_revision.py -q` exits 0 with zero files changed under backend/tests/agents/characterization/golden/ (INV-3: prototype_revision rides run_pipeline and is NOT flipped)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add deterministic scripted-model turns for the three revision agents (Wave-0 harness gap)</name>
  <files>backend/tests/agents/_scripted_model.py</files>
  <read_first>
    - backend/tests/agents/_scripted_model.py lines 238-345 (_scripts_for structure; the od-ppt-validator branch at 322-342 is the EXACT analog — last agent's streamed text wrapped in a single artifact tag, plain markup the carousel sanitizer passes through (no translateX/vw, no .stage/.slide selectors), fixed usage tuple)
    - backend/tests/agents/_scripted_model.py lines 399-480 (_drive wiring: settings.RUNS_ROOT patch at 441, dual create_runner patch at 471-480 — the pattern the Wave-2/3 test rewrites will reuse)
    - backend/agents/capabilities/deliverables/ppt.py (PptResolver: unwrap_artifact(sanitize_carousel_deck_html(last_streamed)) — the LAST agent's streamed output IS the deliverable for strategy: ppt)
  </read_first>
  <action>
    Add three branches to _scripts_for, placed adjacent to the od-ppt block with a comment
    naming their Phase-14 role (deterministic run_revision dispatch tests):
    1. "od-ppt-revision-agent" (the ONLY step of od_ppt_revision — its streamed output is
       the deliverable): one _ScriptedTurn whose text is
       "Revised per instruction.\n<artifact>{REVISED_DECK}</artifact>" with a fixed usage
       tuple. REVISED_DECK must be a complete plain-markup HTML deck whose bytes are
       DISTINCT from the od-ppt-validator parent deck (e.g. slide sections "Revised Title"
       / "Problem" / "Solution" / "Call To Action") so unwrap + lineage assertions can
       distinguish parent vs revision content. No translateX/vw, no .stage/.slide classes
       (sanitizer pass-through).
    2. "ppt-revision-agent" (first of two ppt_revision steps — narration only, its text
       feeds the assembler): one text-only _ScriptedTurn (no artifact tag), fixed usage.
    3. "ppt-revision-assembler" (LAST ppt_revision step — its streamed output is the
       deliverable): one _ScriptedTurn emitting an <artifact>-wrapped revised deck with
       bytes distinct from BOTH the validator deck and the od-ppt-revision-agent deck.
    Keep every turn's text + usage FIXED (determinism — same discipline as the existing
    branches). Do not modify any existing branch or the generic fallback.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -c "from tests.agents._scripted_model import _scripts_for; assert _scripts_for('od-ppt-revision-agent')[0].texts[0] != _scripts_for('ppt-revision-assembler')[0].texts[0]; assert '<artifact>' in _scripts_for('od-ppt-revision-agent')[0].texts[0]; assert '<artifact>' in _scripts_for('ppt-revision-assembler')[0].texts[0]; assert '<artifact>' not in _scripts_for('ppt-revision-agent')[0].texts[0]" && python3.11 -m pytest tests/agents/test_characterization_od_ppt.py tests/unit/test_run_revision_fe_contract.py -q</automated>
  </verify>
  <done>All three revision agents have dedicated deterministic scripts; existing harness consumers (od_ppt characterization, current FE-contract test) unaffected.</done>
  <acceptance_criteria>
    - `grep -c '"od-ppt-revision-agent"' backend/tests/agents/_scripted_model.py` >= 1; same for `"ppt-revision-agent"` and `"ppt-revision-assembler"`
    - The od-ppt-revision-agent and ppt-revision-assembler turns each contain exactly one `<artifact>` opening tag; the ppt-revision-agent turn contains none (source assertion)
    - The revised decks differ byte-wise from the od-ppt-validator deck (the string "Revised" appears in both revised decks and in neither existing deck)
    - `python3.11 -m pytest tests/agents/test_characterization_od_ppt.py -q` exits 0 with zero golden files modified (additive harness change)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest data -> compiler -> engine | file-trust manifests; planner key is engine control data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | planner: skip removes the clarify gate for the two revision manifests | accept | The clarify questionnaire is a UX step, not a security control; revision input is still validated at the WS ingress (empty_revision_instruction / missing_revision_params) and ownership-gated by assert_owns. File-trust manifests only — the compiler trust ceiling for user/db manifests is unchanged. |
| T-14-01-02 | Elevation | a future od revision agent declaring template/design_system injects would dispatch without template context | mitigate | test_run_revision_revision_agents_declare_no_template_injects makes the constraint executable — an inject-declaring revision agent fails CI until template_id persistence (Q3-additive) is built. |
| T-14-01-SC | Tampering | package installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none). |
</threat_model>

<verification>
- Quick run: `cd backend && python3.11 -m pytest tests/agents/test_manifest_parity.py tests/agents/test_id_alias_resolver.py -x -q`
- Wave gate (14-VALIDATION.md battery): 5 characterization suites + test_banned_patterns.py + test_migration_ledger.py + test_revision_gating.py + `/opt/homebrew/bin/lint-imports` — all green, zero golden re-baselines.
- INV-3 scope check: `git diff --name-only` contains NO file under backend/agents/execution_engine/ and NO characterization golden.
</verification>

<success_criteria>
- Both run_revision manifests declare planner: skip; all other manifests unchanged.
- Both planner parity traps updated and green; clarify-defaults trap untouched and green.
- Design-wrinkle pin test exists and passes (settled wrinkle is executable, not prose).
- Scripted harness drives both revision pipelines deterministically (consumed by 14-03/14-04).
- Live-Bedrock confirmation of the flipped flow (ROADMAP SC4) recorded as DEFERRED to the milestone-end live pass — not blocking (project convention: defer-live-verification-to-milestone-end).
</success_criteria>

<output>
Create `.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-01-SUMMARY.md` when done.
</output>
