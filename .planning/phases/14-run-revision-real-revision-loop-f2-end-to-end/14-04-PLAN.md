---
phase: 14-run-revision-real-revision-loop-f2-end-to-end
plan: 04
type: execute
wave: 3
depends_on: [14-03]
files_modified:
  - backend/tests/unit/test_revision_intelligence.py
autonomous: true
requirements: [F2]
must_haves:
  truths:
    - "test_revision_intelligence.py pins the REAL-dispatch contract: proceed-path tests drive scripted revision pipelines through _handle_revision -> execute(); no test asserts stub semantics (context blob as final_output, _stamped_send ledger, planning_context_unavailable payload key) (ROADMAP SC3)"
    - "Every security/validation guard test survives byte-meaning-unchanged: empty/whitespace instruction ValueError, falsy-owner ValueError, FR-014 ValueError (byte-exact message), cross-owner PermissionError BEFORE any event — on both the simple and the realistic-parent variants"
    - "The FR-014 chain tests (deliverable link, summary fallback, [Error: filter) still prove WHICH link resolved — now via the resolved parent content reaching the dispatched agent input instead of the stub's echoed output"
    - "run_events persistence is asserted against execute()'s chokepoint: revision-run ledger rows exist with contiguous, non-duplicated seq — the deleted _stamped_send path has no surviving test pinning it"
    - "The full Phase-14 targeted battery is green and the live-Bedrock confirmation (ROADMAP SC4) is recorded as DEFERRED to the milestone-end live pass"
  artifacts:
    - path: "backend/tests/unit/test_revision_intelligence.py"
      provides: "real-dispatch revision intelligence suite (guards kept, proceed-paths rewritten onto scripted dispatch wiring)"
      min_lines: 500
  key_links:
    - from: "backend/tests/unit/test_revision_intelligence.py"
      to: "tests/agents/_scripted_model.py"
      via: "scripted dispatch wiring (RUNS_ROOT + dual create_runner patch + _scripts_for revision-agent branches from 14-01)"
      pattern: "create_runner"
    - from: "backend/tests/unit/test_revision_intelligence.py"
      to: "agents/execution_engine/engine.py (_handle_revision real dispatch, 14-03)"
      via: "proceed-path tests await _handle_revision and assert dispatched-agent events + exact-kind lineage refs"
      pattern: "_handle_revision"
---

<objective>
Finish ROADMAP Phase 14 success criterion 3: rewrite the 792-line stub-pinning suite
`tests/unit/test_revision_intelligence.py` to the real-dispatch contract and close the
documented intra-phase intermediate left by 14-03 (the suite is RED against the real
dispatch because it pins: `final_output` containing the three `===` section markers, the
stored ref's content being the context blob, run_events persistence through the deleted
`_stamped_send` path, and the `planning_context_unavailable` key on `pipeline_complete`).

The rewrite discipline (RESEARCH Pitfall 5): guards keep their exact meaning — they fire
BEFORE dispatch and need no scripted wiring; proceed-path tests gain the scripted dispatch
wiring and assert observable real-dispatch behavior. The three-section context contract
("three separate structured inputs, NOT concatenated") is still proven — it is now the
agents' INPUT, observable on the forwarded `agent_input` event's `context_message`, not
the run's output.

Targeting rule: every proceed-path test MUST use a target whose derived alias is one of
the two flipped manifests (`ppt_output` -> ppt_revision, `od_ppt_output` -> od_ppt_revision).
Any other `*_output` target would derive a `planner: run` manifest and hang at the clarify
gate (RESEARCH Pitfall 1) — re-target any legacy test inputs accordingly.

This plan also runs the phase-gate battery (it is the last plan of the phase).

Purpose: ROADMAP Phase 14 success criteria 2 and 3 (test half) + phase close.
Output: rewritten suite + green full targeted battery + SC4 deferral recorded.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-RESEARCH.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-PATTERNS.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-VALIDATION.md
</context>

## Artifacts this phase produces (plan 14-04 scope)

- Rewritten `backend/tests/unit/test_revision_intelligence.py` — same module, new internals:
  - a module/fixture-level scripted dispatch helper (RUNS_ROOT patch + dual `create_runner` patch + restore — the `_scripted_model.py` 471-480 idiom, reusable across proceed-path tests; suggested name `_dispatch_wiring`)
  - kept-verbatim guard tests: `test_empty_instruction_raises`, `test_whitespace_only_instruction_raises`, `test_nonexistent_artifact_raises`, `test_cross_owner_revision_denied`, `test_fe_target_cross_owner_still_denied_on_realistic_parent`, `test_fe_target_still_raises_fr014_when_no_chain_link_matches`, `test_summary_fallback_all_error_placeholders_raises_fr014`, `test_clarifications_round_trip_via_artifact_refs`
  - rewritten proceed-path tests (real-dispatch assertions): lineage/version, three-section-input, planning-context prefix presence/absence, run_events chokepoint persistence, deliverable-link + summary-fallback chain resolution
- No production-code symbol changes (test-only plan)

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the proceed-path tests onto scripted dispatch; keep every guard byte-meaning-identical</name>
  <files>backend/tests/unit/test_revision_intelligence.py</files>
  <read_first>
    - backend/tests/unit/test_revision_intelligence.py (the COMPLETE 792-line current suite — fixtures db_factory/engine, _seed_run/_seed_ref/_seed_realistic_parent helpers, and every test listed below; know exactly what each pins before touching it)
    - backend/agents/execution_engine/engine.py (the 14-03 _handle_revision — the contract under test: pre-dispatch guards, FR-014 chain, dispatch loop, guarded lineage write, state_restoration_failed on write failure)
    - backend/tests/agents/_scripted_model.py lines 399-480 (the dispatch wiring idiom: `_settings.RUNS_ROOT = _RUNS_ROOT`, patch factory_mod.create_runner AND engine_mod.create_runner with ScriptedFakeChatModel(_scripts_for(agent_id)), restore in finally; unique pipeline_run_id per test — the state-machine singleton raises StateMachineError on reuse)
    - backend/tests/unit/test_run_revision_fe_contract.py (the 14-03 rewrite — the sibling real-dispatch suite whose wiring shape this plan mirrors; avoid duplicating its E2E scenarios — this suite owns the SEEDED-parent matrix, that one owns the ORGANIC-parent E2E)
  </read_first>
  <action>
    KEEP UNCHANGED (guards fire before dispatch — no wiring needed; preserve assertions
    byte-meaning-identical, message strings byte-exact): test_empty_instruction_raises,
    test_whitespace_only_instruction_raises, test_nonexistent_artifact_raises (FR-014
    ValueError), test_cross_owner_revision_denied (PermissionError BEFORE any event),
    test_fe_target_cross_owner_still_denied_on_realistic_parent,
    test_fe_target_still_raises_fr014_when_no_chain_link_matches,
    test_summary_fallback_all_error_placeholders_raises_fr014,
    test_clarifications_round_trip_via_artifact_refs (store-level, dispatch-free).

    ADD a shared dispatch-wiring helper (fixture or context manager): sets
    settings.RUNS_ROOT to the harness temp root, patches BOTH factory_mod.create_runner
    and engine_mod.create_runner to inject ScriptedFakeChatModel(_scripts_for(agent_id)),
    restores both in finally; each test mints a uuid4 pipeline_run_id.

    REWRITE (each keeps its name where the behavior it names survives; re-target inputs to
    ppt_output/od_ppt_output ONLY — see the objective's targeting rule):
    1. test_revision_stores_new_version_with_lineage — seed a parent ref under the exact
       target kind (chain link 1 coverage), dispatch, then assert the NEW exact-kind ref on
       the revision run: content == the captured pipeline_complete final_output (the
       scripted REVISED deck, unwrapped), derived_from == the seeded original's id,
       run_id == the revision run id, visibility == "workspace", owner/workspace stamped
       from the original. Do NOT assert the three === markers in final_output (stub
       semantics — gone).
    2. test_revision_contains_three_separate_inputs — the three-section contract moved to
       the agent INPUT: capture forwarded events; assert the agent_input event's
       context_message contains "=== ORIGINAL ARTIFACT (type: ...)", "=== VERSION
       HISTORY ===", "=== REVISION INSTRUCTION ===" with the seeded original content and
       the instruction inside their sections.
    3. test_planning_context_unavailable_when_no_planning_artifact /
       test_planning_context_available_when_present — assert the "=== PLANNING CONTEXT"
       prefix is ABSENT/PRESENT in the dispatched context_message respectively. The
       planning_context_unavailable key on pipeline_complete is GONE (stub-only payload) —
       remove those assertions.
    4. test_revision_run_events_persist_and_resolve_on_real_db — the ledger now comes from
       execute()'s chokepoint: after dispatch, read the revision run's run_events
       owner-scoped and assert rows exist, seq is contiguous from 1 with NO duplicates
       (single stamping source — the Pitfall-2 regression trap), and the terminal
       pipeline_complete row is present. NOTE the workspace delta: execute() mints the
       revision run's OWN workspace (it does not inherit original.workspace_id) — resolve
       scope accordingly (owner-scoped read; recover workspace from the persisted rows,
       not from the parent).
    5. test_fe_target_resolves_deliverable_ref_on_realistic_parent /
       test_fe_target_falls_back_to_summary_on_legacy_parent /
       test_summary_fallback_skips_error_placeholder_refs — keep the _seed_realistic_parent
       matrix and the chain-link semantics; the proof of WHICH content resolved moves from
       the echoed output to the dispatched agent_input context_message ("=== ORIGINAL
       ARTIFACT" section contains the deliverable-ref content / the latest non-[Error:
       summary content respectively); keep the version_history "N version(s) exist"
       assertion via the context_message.
    Where a target was previously a non-ppt kind, re-target to ppt_output or od_ppt_output
    and note why (planner: skip applies only to the two run_revision-dispatched manifests).
    For ppt_output targets remember the deliverable is the ASSEMBLER's scripted deck (last
    agent of the 2-step ppt_revision pipeline).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_revision_intelligence.py tests/unit/test_run_revision_fe_contract.py tests/agents/test_manifest_parity.py -x -q</automated>
  </verify>
  <done>The suite passes against the real dispatch; all guards intact; no stub semantics pinned anywhere; the 14-03 intra-phase intermediate is closed.</done>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/unit/test_revision_intelligence.py -q` exits 0
    - The kept guard tests exist by name (grep all eight names listed in the action) and the FR-014 message assertion is byte-identical to engine.py's
    - `grep -c "planning_context_unavailable" backend/tests/unit/test_revision_intelligence.py` returns 0 OR only inside comments explaining the removal (no executable assertion on the deleted payload key)
    - `grep -c "_stamped_send" backend/tests/unit/test_revision_intelligence.py` returns 0
    - A seq-contiguity/no-duplicate assertion exists in the run_events test (grep: seq)
    - Proceed-path targets are limited to ppt_output/od_ppt_output: `grep -oE '"[a-z_]+_output"' backend/tests/unit/test_revision_intelligence.py | sort -u` yields only "ppt_output" and "od_ppt_output"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase-gate battery + SC-001/INV-3 grep sweep + SC4 deferral recording</name>
  <files>backend/tests/unit/test_revision_intelligence.py</files>
  <read_first>
    - .planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-VALIDATION.md (the battery definition and sign-off checklist this task executes)
  </read_first>
  <action>
    Run the complete Phase-14 targeted battery and the invariant grep sweep; fix any
    residue surfaced (test-file-only fixes belong here; an engine regression means
    re-opening 14-03 scope — stop and surface it, do not patch around). Record in the
    SUMMARY: (1) the battery results table, (2) ROADMAP SC4 (live Bedrock: FE-exact
    run_revision frame -> revised deck in the preview) as DEFERRED to the milestone-end
    live pass per project convention (defer-live-verification-to-milestone-end) together
    with the 14-02 live cancel/reconnect check and the 14-01 A1 assumption (no clarify
    questionnaire wanted on the revision panel), and (3) the IMPLEMENTATION-REGISTER
    Phase-13 "deferred feature" note (run_revision deliverable still the Phase-3 stub) as
    superseded by this phase — flag it for the register's Phase-14 entry.
    Battery (from 14-VALIDATION.md):
    - 5 characterization suites (prototype, od_prototype, od_ppt, app_builder,
      prototype_revision) — zero golden re-baselines
    - test_banned_patterns.py + test_migration_ledger.py
    - test_manifest_parity.py + test_id_alias_resolver.py + test_revision_gating.py
    - test_revision_intelligence.py + test_run_revision_fe_contract.py +
      test_run_revision_ws_dispatch.py + test_run_pipeline_validation.py +
      test_pipeline_failure_semantics.py
    - /opt/homebrew/bin/lint-imports (4 contracts kept / 0 broken baseline)
    Grep sweep:
    - `grep -rn "if pipeline_type ==" backend/agents/execution_engine/ | grep -v '^\s*#'` -> 0
    - `grep -rn "ppt_revision\|od_ppt_revision\|ppt_output" backend/agents/execution_engine/` -> 0 non-comment code matches (SC-001)
    - `git status --porcelain backend/tests/agents/characterization/golden/` -> empty (INV-3)
    - `grep -c "_stamped_send\|_rev_sink" backend/agents/execution_engine/engine.py` -> 0 (INV-12)
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py tests/agents/test_manifest_parity.py tests/agents/test_id_alias_resolver.py tests/agents/test_revision_gating.py tests/unit/test_revision_intelligence.py tests/unit/test_run_revision_fe_contract.py tests/unit/test_run_revision_ws_dispatch.py tests/unit/test_run_pipeline_validation.py tests/unit/test_pipeline_failure_semantics.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Full targeted battery green; all invariant greps clean; SC4 + live items recorded as deferred; phase ready for /gsd-verify-work.</done>
  <acceptance_criteria>
    - The battery command exits 0 (all suites green in one run — no session-pollution residue)
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken
    - `git status --porcelain backend/tests/agents/characterization/golden/` outputs nothing
    - `grep -rn "ppt_output" backend/agents/execution_engine/ --include="*.py" | grep -v "^\s*#" | grep -vc "#"` returns 0 code matches
    - The 14-04 SUMMARY lists the deferred live items (SC4 frame-to-preview, live cancel/reconnect, A1 questionnaire assumption) under a Deferred heading
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (test-only plan) | no new production surface; the suite GUARDS existing boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Elevation | regression cover for the ownership gates could be weakened by the rewrite | mitigate | Both cross-owner PermissionError tests and the falsy-owner guard are in the KEEP-verbatim list; acceptance criteria grep their presence by name. |
| T-14-04-02 | Repudiation | losing the single-stamping regression trap | mitigate | The rewritten run_events test asserts contiguous, non-duplicated seq from the execute() chokepoint — a reintroduced second counter fails it. |
| T-14-04-SC | Tampering | package installs | accept | No packages installed this phase. |
</threat_model>

<verification>
- Quick run: `cd backend && python3.11 -m pytest tests/unit/test_revision_intelligence.py tests/unit/test_run_revision_fe_contract.py tests/agents/test_manifest_parity.py -x -q`
- Phase gate: the Task-2 battery command (the 14-VALIDATION.md full battery) + lint-imports — green before /gsd-verify-work.
- SC4 and all live-environment confirmations: DEFERRED to the milestone-end live pass (recorded, not blocking).
</verification>

<success_criteria>
- ROADMAP SC3 fully TRUE: both stub-pinning suites pin the real-dispatch contract; goldens byte/event-identical; no kernel workflow-name literal.
- ROADMAP SC2 test-half TRUE: lineage + revision-of-revision + scope assertions green against the real dispatch.
- Phase-14 battery green in a single session; live items recorded as deferred (SC4).
</success_criteria>

<output>
Create `.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-04-SUMMARY.md` when done.
</output>
