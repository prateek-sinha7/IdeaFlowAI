---
phase: 14-run-revision-real-revision-loop-f2-end-to-end
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/tests/unit/test_run_revision_fe_contract.py
autonomous: true
requirements: [F2]
must_haves:
  truths:
    - "_handle_revision dispatches the registry's real revision pipeline through the normal execute() chokepoint: get_pipeline_agents(<derived alias>) agents run on the deepagents runtime, events stream stamped (seq/event_id) and persisted by execute() — no fake pipeline_start/pipeline_complete pair, no zero-duration completion (ROADMAP SC1)"
    - "The Phase-3 echo stub, the _rev_sink/_rev_counter/_stamped_send wrapper, and the CR-01 scope-writeback/sink-arm block are DELETED in this same change — execute() owns workspace mint, set_run_scope, stamping and run_events persistence (INV-3/INV-12, RESEARCH Pitfall 2)"
    - "The final revision deliverable is the revision agents' REVISED artifact (unwrapped by the declared ppt deliverable strategy), persisted under the exact target kind with derived_from=original.id, owner/workspace-scoped, visibility=workspace — revision-of-revision resolves it via FR-014 chain link 1 (ROADMAP SC2)"
    - "No workflow-name literal enters the kernel: agents/manifest resolved via the derived WR-06 alias only; banned-pattern gate green (SC-001)"
    - "Non-revision characterization goldens stay byte/event-identical: zero diffs in execute()/_execute_impl/providers/deliverable resolvers — the only kernel edit is inside _handle_revision (RESEARCH Pitfall 8)"
  artifacts:
    - path: "backend/agents/execution_engine/engine.py"
      provides: "real revision dispatch (forward-and-capture loop) + pre-dispatch unsupported-target guard + post-dispatch exact-kind derived_from lineage write inside _handle_revision"
      contains: "get_pipeline_agents(revision_pipeline_type)"
    - path: "backend/tests/unit/test_run_revision_fe_contract.py"
      provides: "FE-exact real-dispatch regression: scripted revision agents observed, revised deck as final_output, exact-kind lineage ref, revision-of-revision via chain link 1"
      min_lines: 150
  key_links:
    - from: "backend/agents/execution_engine/engine.py (_handle_revision)"
      to: "ExecutionEngine.execute()"
      via: "async for event in self.execute(agents=get_pipeline_agents(revision_pipeline_type), user_message=revision_context, ...) forwarded through the RAW websocket_send_fn"
      pattern: "self.execute"
    - from: "backend/agents/execution_engine/engine.py (_handle_revision post-dispatch)"
      to: "ArtifactGraph.write_ref + ScopedStore.write_ref"
      via: "kind=target_artifact_type, derived_from=original.id, content=final_output, visibility='workspace'"
      pattern: "derived_from"
    - from: "backend/agents/workflows/od_ppt_revision/workflow.yaml (deliverable strategy: ppt)"
      to: "agents/capabilities/deliverables/ppt.py"
      via: "unwrap_artifact(sanitize_carousel_deck_html(last_streamed)) — the agent's artifact-wrapped revised deck becomes raw HTML final_output"
      pattern: "strategy: ppt"
---

<objective>
Close live finding F2 end-to-end: replace the Phase-3 echo stub in `_handle_revision`
(engine.py:3656-3943) with a real dispatch of the registry's revision pipelines through the
normal `execute()` path. Today the handler resolves the parent via the 13-05 FR-014 chain,
composes the three-section revision context, computes the WR-06 alias — then writes that
CONTEXT back as the "revision" and emits a hand-rolled `pipeline_start`/`pipeline_complete`
pair (`total_duration: 0.0`, fake single "revision-agent"). No model ever runs.

After this plan: the composed context is the `user_message` of a real `execute()` run over
`get_pipeline_agents(revision_pipeline_type)` (deepagents runtime — INV-13 satisfied by
construction); the declared `ppt` deliverable strategy unwraps the agents' revised deck;
the post-dispatch lineage write persists the REVISED content under the exact target kind
with `derived_from`. The stub write, the fake event pair, and the `_rev_sink`/`_stamped_send`
duplicate stamping path are DELETED in the same change (INV-3/INV-12 — no branch-around,
no dual path).

Hard constraints in force: SC-001 (all strings data-derived — alias transform,
`get_pipeline_agents(<derived>)`, `compile_for_run(<derived>)` inside execute()), INV-3
(the 5 characterization goldens cover NO run_revision path — `prototype_revision` rides
`run_pipeline` and must not be touched), Q3 (no migration needed — none added).

Settled design wrinkle (pinned executable in 14-01, evidence in 14-RESEARCH.md): dispatch
with `od_context=None`. No revision agent declares any inject; the 13-06
`missing_template_context` guard lives only at the `run_pipeline` ingress which this path
never traverses; the parent deck embedded in the composed context already physically
realizes the template. Do NOT re-resolve the parent's template (impossible anyway —
WorkflowRun persists no template_id).

Purpose: ROADMAP Phase 14 success criteria 1, 2, and 3 (engine half).
Output: real-dispatch `_handle_revision` + rewritten FE-exact contract suite.

KNOWN INTERMEDIATE (vetted intra-phase gap, 05-03 precedent): after this plan,
`tests/unit/test_revision_intelligence.py` is RED — it pins stub semantics deeply
(final_output == context blob, `_stamped_send` ledger path, `planning_context_unavailable`
on pipeline_complete). Plan 14-04 (next wave) rewrites it to the real-dispatch contract.
This plan's verify commands therefore EXCLUDE that suite; everything else must be green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-RESEARCH.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-PATTERNS.md
@.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-VALIDATION.md
@.planning/phases/13-live-verification-gap-closure/13-05-SUMMARY.md
</context>

## Artifacts this phase produces (plan 14-03 scope)

- Real-dispatch body of `ExecutionEngine._handle_revision` (rewritten section between the WR-06 alias transform and the method end): pre-dispatch guard ValueError for unsupported targets, `agents = get_pipeline_agents(revision_pipeline_type)`, forward-and-capture `async for` loop over `self.execute(...)`, post-dispatch guarded exact-kind `derived_from` lineage write
- DELETED symbols/blocks (INV-12): `_rev_sink`, `_rev_counter`, `_raw_send_fn`, `_stamped_send` (engine.py:3724-3740), the CR-01 deferred sink-arm + `set_run_scope` writeback block (3798-3823), the fake `pipeline_start` (3874-3885), the stub `content=revision_context` write + fake `pipeline_complete` with `total_duration: 0.0` (3887-3943), the `"revision-agent"` / `"Revision Agent"` literals, the `planning_context_unavailable` key on `pipeline_complete` (stub-only payload field — execute() owns the terminal event now)
- Rewritten `backend/tests/unit/test_run_revision_fe_contract.py` (real-dispatch contract: scripted dispatch wiring + revision-of-revision scenario)
- No new module, no new migration, no new capability, no `_KNOWN` movement

<tasks>

<task type="auto">
  <name>Task 1: Replace the echo stub with real execute() dispatch + lineage write; delete the duplicate stamping path</name>
  <files>backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py lines 3650-3943 (the COMPLETE current _handle_revision — know every block before cutting; the KEEP list below is test-pinned and security-ordered)
    - backend/agents/execution_engine/engine.py lines 517-560 (execute() signature + its own _RunEventSink arm + itertools.count(1) — proof the chokepoint owns stamping/persistence)
    - backend/agents/execution_engine/engine.py lines 746-835 (execute()'s workspace mint + set_run_scope + run_capabilities block — proof the CR-01 writeback in _handle_revision is superseded)
    - backend/agents/execution_engine/engine.py lines 1150-1175 (the local `from agents.registry import PIPELINE_AGENTS, get_pipeline_agents` idiom + the compiled-plan vs registry membership RuntimeError — why the dispatch MUST pass exactly get_pipeline_agents(revision_pipeline_type), RESEARCH Pitfall 6)
    - .planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-PATTERNS.md (Pattern: forward-and-capture loop shape from websocket.py:1507-1587; post-dispatch lineage write shape)
  </read_first>
  <action>
    KEEP byte-unchanged (in this order — each is test-pinned): the empty-instruction guard;
    the falsy-owner ValueError ("_handle_revision requires a real owner_id (AUTHZ-03)");
    `store = ScopedStore(owner_id=owner_id)` then `await store.assert_owns(parent_run_id)`
    FIRST (T-5-SEED — before any cross-run read AND before dispatch); the FR-014 three-link
    chain with the `[Error:` filter and the byte-exact FR-014 ValueError message; the
    chain-link INFO log; version_history; the planning_context read; the three-section
    revision_context composer + planning-context prefix; the WR-06 alias transform
    `revision_pipeline_type = f"{target_artifact_type.removesuffix('_output')}_revision"`.

    DELETE (INV-12 — same change, no branch-around): the `_rev_sink`/`_rev_counter`/
    `_raw_send_fn`/`_stamped_send` block and the `websocket_send_fn = _stamped_send`
    reassignment (3724-3740) plus its WR-06 comment block (3708-3723); the CR-01 deferred-arm
    + store._workspace_id + set_run_scope writeback block (3798-3823) and its comment —
    execute() mints the revision run's own workspace and calls set_run_scope itself
    (lines 746-835); the fake pipeline_start (3874-3885); the stub try-block's
    `content=revision_context` semantics; the fake pipeline_complete with
    `total_duration: 0.0` and the `planning_context_unavailable` payload key (3931-3943).
    Keep module imports (`itertools`, `uuid`, `_RunEventSink`) — execute() still uses them;
    remove only locals this method orphaned. Update the method docstring: the revision now
    dispatches the registry revision pipeline through execute(); remove the Phase-3 stub
    commentary ("For Phase 3, we store the instruction + context...").

    ADD, after the alias transform:
    1. Local `from agents.registry import get_pipeline_agents` (mirror the engine.py:1155
       idiom); `agents = get_pipeline_agents(revision_pipeline_type)`; if empty raise
       `ValueError(f"No revision pipeline is registered for target_artifact_type "
       f"{target_artifact_type!r} (derived pipeline {revision_pipeline_type!r}).")` —
       pre-dispatch fail-fast for unmapped targets (RESEARCH Pitfall 7); the WS layer maps
       ValueError to revision_validation_error. SC-001: message interpolates data only.
    2. Forward-and-capture dispatch (PATTERNS Pattern 1):
       `final_output: str | None = None`, `terminal_failed = False`;
       `async for event in self.execute(agents=agents, user_message=revision_context,
       pipeline_run_id=pipeline_run_id, pipeline_type=revision_pipeline_type,
       user_id=owner_id, model_id=model_id, od_context=None, gate_agent_ids=[],
       parent_run_id=parent_run_id):` — capture final_output from
       pipeline_complete.data.final_output, set terminal_failed on pipeline_failed, then
       `await websocket_send_fn(event)` for EVERY event verbatim. websocket_send_fn is the
       RAW caller-supplied sender — events arrive already stamped (seq/event_id) and
       persisted by execute(); never mutate them (SAFE-03/PERSIST-03). Comment WHY
       od_context=None (settled wrinkle: no revision agent declares injects — pinned by
       test_run_revision_revision_agents_declare_no_template_injects; the guard lives only
       at the run_pipeline ingress) and why gate_agent_ids=[] (no inter-agent HITL gate on
       a panel revision; None would fall back to the static gate set).
    3. Post-dispatch lineage write, guarded `if final_output and not terminal_failed:` —
       repoint the stub's existing block: ArtifactGraph().write_ref(run_id=pipeline_run_id,
       owner_id=owner_id, workspace_id=original.workspace_id, kind=target_artifact_type,
       producer_step="revision", producer_agent=agents[-1].id (derived from the resolved
       spec list — no literal, IN-05 spirit), task_id=None, content=final_output,
       location=f"artifact_refs/{target_artifact_type}", derived_from=original.id,
       visibility="workspace") then `await store.write_ref(_rev_ref)`. Keep the
       "Revision stored" INFO log. Keep the except-Exception -> state_restoration_failed
       emit + return on write failure (RESEARCH Open Q2: preserve the event vocabulary; the
       run itself already completed — the terminal-block kind="deliverable" ref keeps
       chain link 2 functional, so do NOT fail the run). On `terminal_failed` or empty
       final_output: write NOTHING (a failed revision must not become a future revision
       parent via chain link 1 — composes with 14-02's status fidelity).
    Anti-patterns (hard-fail review): no `if revision_pipeline_type == ...` or any
    workflow-name literal (banned-pattern gate scans agents/execution_engine/); no edits
    to execute()/_execute_impl/providers/deliverable resolvers (Pitfall 8 — INV-3 scope);
    no new execute() parameter (INV-5 — planner suppression is the 14-01 manifest data).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py tests/agents/test_revision_gating.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>_handle_revision dispatches the real revision pipeline via execute(); stub/sink/fake-pair deleted; 5 goldens byte/event-identical; banned-pattern + ledger + lint green. (test_revision_intelligence + the old FE-contract assertions are red until Task 2 / 14-04 — documented intermediate.)</done>
  <acceptance_criteria>
    - engine.py contains `get_pipeline_agents(revision_pipeline_type)` and an `async for` over `self.execute(` inside _handle_revision (source assertions)
    - `grep -c "_stamped_send" backend/agents/execution_engine/engine.py` returns 0; `grep -c "_rev_sink" backend/agents/execution_engine/engine.py` returns 0; `grep -c '"revision-agent"' backend/agents/execution_engine/engine.py` returns 0; `grep -c '"total_duration": 0.0' backend/agents/execution_engine/engine.py` returns 0
    - The FR-014 ValueError message "Revision MUST NOT proceed without original context (FR-014)" greps exactly once in engine.py (byte-unchanged)
    - The lineage write is guarded on `final_output and not terminal_failed` and carries `derived_from=original.id`, `kind=target_artifact_type`, `visibility="workspace"`, `producer_agent=agents[-1].id` (source assertions)
    - `python3.11 -m pytest tests/agents/test_banned_patterns.py -q` exits 0 (SC-001: no kernel workflow-name literal) and the 5 characterization suites exit 0 with `git status --porcelain backend/tests/agents/characterization/golden/` empty (INV-3, zero re-baselines)
    - `git diff backend/agents/execution_engine/engine.py` touches ONLY the _handle_revision method body/docstring (Pitfall 8 scope assertion)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite the FE-exact contract suite to the real-dispatch contract (incl. revision-of-revision)</name>
  <files>backend/tests/unit/test_run_revision_fe_contract.py</files>
  <read_first>
    - backend/tests/unit/test_run_revision_fe_contract.py (all 116 lines — the db_factory fixture and the _drive("od_ppt") parent-run setup SURVIVE; the stub assertions at 96-116 are replaced)
    - backend/tests/agents/_scripted_model.py lines 399-480 (the dispatch wiring to replicate for the revision call: `_settings.RUNS_ROOT = _RUNS_ROOT` at 441; patch BOTH factory_mod.create_runner AND engine_mod.create_runner with ScriptedFakeChatModel(_scripts_for(agent_id)) at 471-480; restore both in finally at 542-544)
    - backend/tests/agents/_scripted_model.py (the 14-01 od-ppt-revision-agent branch — the revised deck bytes the assertions pin)
    - backend/agents/capabilities/deliverables/ppt.py (unwrap order: sanitize then unwrap — final_output is the RAW deck HTML, no artifact wrapper)
  </read_first>
  <action>
    Keep: module docstring intent (update to real-dispatch), the db_factory fixture, the
    `_drive("od_ppt")` parent run + organic-persistence discipline (seed NO artifact rows),
    OWNER = "harness-user".
    Rewrite the main test + add a second:
    1. test_fe_exact_run_revision_dispatches_real_pipeline:
       - Wire scripted dispatch for the revision call: set _settings.RUNS_ROOT to the
         harness _RUNS_ROOT; patch factory_mod.create_runner AND engine_mod.create_runner
         (engine imported it by name) to inject ScriptedFakeChatModel(_scripts_for(agent_id));
         restore both in finally. `planner: skip` (14-01) means no planner/clarify patching.
         Use a UNIQUE pipeline_run_id (uuid4 string — the state-machine singleton raises
         StateMachineError on reuse; replace the fixed "run-rev-fe-contract" literal).
       - Call engine._handle_revision with the FE-exact frame
         (target_artifact_type="od_ppt_output", a real instruction, owner_id=OWNER).
       - Assert the REAL-dispatch contract:
         (a) no state_restoration_failed event;
         (b) agent_start AND agent_complete events observed for "od-ppt-revision-agent"
             (model ran — SC1's "model runs observed");
         (c) exactly ONE pipeline_start and ONE pipeline_complete, with
             data.pipeline_type == "od_ppt_revision" on both terminal-relevant events;
         (d) final_output == the scripted REVISED deck unwrapped (raw HTML — starts with
             "<!doctype html>" and contains the revised-deck marker bytes, e.g. "Revised
             Title"; contains NO "<artifact>" wrapper) and final_output != the parent
             deliverable; do NOT assert the instruction text appears in final_output
             (RESEARCH Pitfall 5 warning);
         (e) stamping is single-sourced: every captured event's data carries seq, the seq
             sequence over all events is contiguous 1..N with no duplicates (Pitfall 2 —
             the deleted _stamped_send cannot double-stamp);
         (f) lineage: via ScopedStore(owner_id=OWNER) (workspace threading as needed for
             the read — list_refs is owner+visibility-scoped), list_refs(revision_run_id,
             kind="od_ppt_output") yields exactly one ref with content == final_output and
             derived_from == the parent's resolved deliverable-ref id (read it via
             list_refs(parent_run_id, kind="deliverable")[-1].id).
    2. test_revision_of_revision_resolves_via_exact_kind_chain_link_1 (ROADMAP SC2):
       run a SECOND _handle_revision against the FIRST revision's run id as parent
       (target "od_ppt_output", fresh uuid4 run id, same scripted wiring): assert it
       proceeds (no FR-014 ValueError, one pipeline_complete) and the agent_input/
       composed-context evidence shows the FIRST revision's deck content was resolved as
       the original (the forwarded agent_input event's context_message contains the
       revised-deck marker bytes inside the "=== ORIGINAL ARTIFACT" section) — proving
       FR-014 chain link 1 (exact kind) matched the 14-03 lineage ref. (assert_owns
       degrades gracefully for an absent same-owner run row — the existing parent run
       has no WorkflowRun row either; same-owner discipline preserved.)
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_run_revision_fe_contract.py -x -q && python3.11 -m pytest tests/agents/test_characterization_od_ppt.py tests/agents/test_banned_patterns.py -q</automated>
  </verify>
  <done>The FE-exact suite pins the real-dispatch contract end-to-end including single-source stamping, exact-kind derived_from lineage, and revision-of-revision via chain link 1.</done>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/unit/test_run_revision_fe_contract.py -q` exits 0 with >= 2 tests collected
    - The suite asserts agent_start/agent_complete for "od-ppt-revision-agent" (grep both event-type literals + the agent id in the file)
    - The suite asserts `derived_from` equality against the parent deliverable ref id and content equality against final_output (grep: derived_from in assertions)
    - The suite contains a seq-contiguity assertion (grep: seq) and a revision-of-revision test function (grep: revision_of_revision)
    - No assertion matches the instruction text inside final_output (source review: the instruction string is asserted only in context/agent_input checks if at all)
    - The file seeds no artifact rows by hand for the FIRST revision's parent: `grep -c 'kind="deliverable"' backend/tests/unit/test_run_revision_fe_contract.py` counts only READ-side lookups (no write_ref/seed helper calls writing parent refs)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE instruction -> composed context -> model prompt | untrusted user text now reaches a real model (it never did under the stub) |
| revision run -> parent run artifacts | cross-run read, owner-gated |
| lineage write -> artifact_refs | cross-tenant-sensitive persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Elevation | IDOR: revise another owner's parent run | mitigate (existing, preserved byte-identical) | `assert_owns(parent_run_id)` stays FIRST — before any cross-run read AND before dispatch; cross-owner PermissionError propagates before any event (pinned by test_cross_owner_revision_denied, kept in 14-04). |
| T-14-03-02 | Tampering | prompt injection via the revision instruction into the dispatched agents | accept (bounded) | Same composed context the stub already built now reaches a model; revision agents are tools: [] (no filesystem/exec grants) with surgical-edit prompts; no new tool surface, no gates suppressed that granted authority (gate_agent_ids=[] removes only an HITL pause). |
| T-14-03-03 | DoS | revisions now consume model tokens/runtime | mitigate | Dispatch inherits execute()'s per-run BudgetManager from compiled limits + recursion_limit backstop; revision pipelines are 1-2 single_shot steps; pre-dispatch ValueError rejects unmapped targets before any spawn. |
| T-14-03-04 | Tampering | unvalidated target_artifact_type reaching manifest loading | mitigate | Suffix transform + get_pipeline_agents over the closed registry (empty -> ValueError pre-dispatch); compile_for_run/load_manifest path-traversal already closed (T-04-09). |
| T-14-03-05 | Repudiation | failed dispatch leaving a poisoned exact-kind lineage ref (failed revision becomes a future revision parent) | mitigate | Lineage write guarded `final_output and not terminal_failed`; failure paths write nothing under the exact kind (chain link 1 stays clean); WS status fidelity (14-02) covers the run-row half. |
| T-14-03-06 | Information Disclosure | lineage ref leaking across tenants | mitigate (existing pattern) | Ref stamped owner_id + original.workspace_id, visibility="workspace"; reads go through the default-deny ScopedStore owner+visibility filter (authz.py) — cross-owner reads resolve nothing. |
| T-14-03-SC | Tampering | package installs | accept | No packages installed this phase. |
</threat_model>

<verification>
- Quick run: `cd backend && python3.11 -m pytest tests/unit/test_run_revision_fe_contract.py tests/agents/test_manifest_parity.py -x -q` (test_revision_intelligence EXCLUDED until 14-04 — documented intra-phase intermediate)
- Wave gate: 5 characterization suites + test_banned_patterns.py + test_migration_ledger.py + test_revision_gating.py + `/opt/homebrew/bin/lint-imports` — green, zero golden re-baselines, `git status --porcelain backend/tests/agents/characterization/golden/` empty.
- SC-001 greps: `grep -rn "ppt_revision\|od_ppt_revision" backend/agents/execution_engine/ | grep -v '^\s*#'` returns 0 code matches.
- Live Bedrock confirmation (FE-exact frame -> revised deck in the preview, ROADMAP SC4): DEFERRED to the milestone-end live pass per project convention — record in SUMMARY, do not block.
</verification>

<success_criteria>
- run_revision drives the real revision pipeline agents through execute(); final deliverable is the revised artifact, never the context blob (SC1, offline-proven via scripted models).
- Exact-kind derived_from lineage persisted and revision-of-revision resolves via chain link 1 (SC2).
- Stub + duplicate stamping path deleted; single-source seq proven contiguous (INV-12/SAFE-03).
- 5 goldens byte/event-identical; banned-pattern + ledger + lint green (SC3 engine half).
</success_criteria>

<output>
Create `.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/14-03-SUMMARY.md` when done.
</output>
