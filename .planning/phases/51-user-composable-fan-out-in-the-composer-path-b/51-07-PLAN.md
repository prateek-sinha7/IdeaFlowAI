---
phase: 51-user-composable-fan-out-in-the-composer-path-b
plan: 07
type: execute
wave: 5
depends_on: ["51-06"]
files_modified:
  - frontend/src/components/workflow/composer/CanvasConfigRail.tsx
  - frontend/src/components/workflow/composer/CanvasView.tsx
  - frontend/src/components/workflow/composer/CanvasConfigRail.test.tsx
  - frontend/src/components/workflow/composer/CanvasView.test.tsx
autonomous: true
requirements: [FANOUT-01, FANOUT-04, FANOUT-05, FANOUT-07]
must_haves:
  truths:
    - "The canvas config rail has a 'Fan out over a list' toggle + a 'Source list from' picker (earlier agents only), persisting {strategy, task_source} via the shared patch() reducer; disabling clears them."
    - "CanvasView threads priorAgents={pipelineAgents.slice(0, selIndex)} to the rail so the picker's options are the agents before the selected node."
    - "The toggle is disabled for the first agent (no producer) and warns on an unknown producer."
  artifacts:
    - path: "frontend/src/components/workflow/composer/CanvasConfigRail.tsx"
      provides: "canvas fan-out toggle + source picker + priorAgents prop"
      contains: "fanout_batch"
    - path: "frontend/src/components/workflow/composer/CanvasView.tsx"
      provides: "priorAgents threading"
      contains: "priorAgents"
  key_links:
    - from: "CanvasView.tsx"
      to: "CanvasConfigRail priorAgents prop"
      via: "pipelineAgents.slice(0, selIndex)"
      pattern: "priorAgents"
    - from: "CanvasConfigRail toggle"
      to: "applyLeverPatch (shared reducer via patch())"
      via: "patch({ strategy, task_source })"
      pattern: "patch\\("
---

<objective>
P4 (canvas half). Add the "Fan out over a list" toggle + "Source list from" picker to the canvas `CanvasConfigRail` (§4b), reusing the existing `Toggle` component and the shared `patch()` writer (which delegates to `applyLeverPatch`). Thread `priorAgents={pipelineAgents.slice(0, selIndex)}` from `CanvasView` (the owner of `pipelineAgents`; the selected-index local is `selIndex`, NOT `selectedIndex`). Enforce the same FE guardrails as the simple-view surface (earlier-only picker, first-agent disabled, unknown-producer warning). This plan depends on 51-06 for the widened `StepSelection` type it imports from `AgentsPopup.tsx`.

Purpose: The canvas is the primary composer surface; this brings it to parity with the simple-view. Same INSERT-A-NODE producer model (D2/D7): reuse only a known `## Task N:` producer (`prototype-plan`/`task-list-planner`), else steer to insert.
Output: Canvas toggle + source picker + `priorAgents` threading + vitest (`CanvasConfigRail.test.tsx` new, `CanvasView.test.tsx` extend).

Implements FANOUT-01 (persist on the canvas surface), FANOUT-04 (FE producer sourcing), FANOUT-05 (FE guardrails), FANOUT-07 (FE vitest). Per decisions D6, D7, D9.
</objective>

<artifacts_produced>
- `frontend/src/components/workflow/composer/CanvasConfigRail.tsx` — new `priorAgents: AgentDef[]` prop (the "`priorAgents` prop" named in the phase artifact list) + the fan-out toggle + source `<select>` + guardrails.
- `frontend/src/components/workflow/composer/CanvasView.tsx` — `priorAgents={pipelineAgents.slice(0, selIndex)}` threading.
</artifacts_produced>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-CONTEXT.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canvas rail fan-out toggle + source picker + priorAgents threading</name>
  <files>frontend/src/components/workflow/composer/CanvasConfigRail.tsx, frontend/src/components/workflow/composer/CanvasView.tsx</files>
  <read_first>
    - frontend/src/components/workflow/composer/CanvasConfigRail.tsx (props :36-49; the shared `patch()` writer :77-80 → `applyLeverPatch`; the reusable `Toggle` :82-112; existing lever rows using `patch(...)` — Validator :168-175, Review-gate :187-195, Model `<select>` :135-152, Retry :212)
    - frontend/src/components/workflow/composer/CanvasView.tsx (`pipelineAgents` :43/:63; the selected-index local `selIndex` :84-87 — NOT `selectedIndex`; the rail render :270-276)
    - frontend/src/components/workflow/AgentsPopup.tsx (the widened `StepSelection` type from 51-06 — imported here)
    - .planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md (sections 4 + 5 — verbatim analogs + the exact prop/patch shapes)
  </read_first>
  <action>
    (a) `CanvasConfigRail.tsx`: add a `priorAgents: AgentDef[]` prop. Add an "Overrides" row "Fan out over a list" using the existing `Toggle` (:82-112). On enable: `patch({ strategy: "fanout_batch", task_source: { kind: "parsed", parser: "heading_tasks", source_step: priorAgents.at(-1)?.id } })`. On disable: `patch({ strategy: undefined, task_source: undefined, fanout: undefined })`. When enabled, render a "Source list from" `<select>` (mirror the Model `<select>` at :135-152) populated from `priorAgents`, writing `patch({ task_source: { kind: "parsed", parser: "heading_tasks", source_step: e.target.value } })`. GUARDRAILS (D9/§5b): disable the toggle when `priorAgents.length === 0` (first agent) with an inline hint; when enabled with a source NOT in the known-producer allow-list `["prototype-plan", "task-list-planner"]`, render a NON-blocking warning. Keep `merge_agent`/`max_parallel`/`json_tasks`/`on_conflict` OUT (scope fence).
    (b) `CanvasView.tsx`: at the rail render (:270) add `priorAgents={pipelineAgents.slice(0, selIndex)}` (use the actual local `selIndex`, not `selectedIndex`).
    Reuse the SHARED reducer only — `patch()` already delegates to `applyLeverPatch`; do NOT fork any lever logic.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/workflow/composer/CanvasConfigRail.test.tsx src/components/workflow/composer/CanvasView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `CanvasConfigRail` accepts a `priorAgents` prop and renders a fan-out toggle + source `<select>` (options = priorAgents), disabled when `priorAgents.length === 0`, with an unknown-producer warning (`grep -n "priorAgents\|fanout_batch" frontend/src/components/workflow/composer/CanvasConfigRail.tsx` hits both).
    - `CanvasView` passes `priorAgents={pipelineAgents.slice(0, selIndex)}` (`grep -n "priorAgents=.*selIndex" frontend/src/components/workflow/composer/CanvasView.tsx` hits).
    - No forked reducer — enable/disable/source all go through `patch()`.
    - `cd frontend && npm test -- src/components/workflow/composer/CanvasConfigRail.test.tsx src/components/workflow/composer/CanvasView.test.tsx` passes.
  </acceptance_criteria>
  <done>The canvas surface persists/clears fan-out through the shared reducer with earlier-only source options threaded from CanvasView and the first-disabled/unknown-warning guardrails.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest — canvas rail persist + priorAgents threading</name>
  <files>frontend/src/components/workflow/composer/CanvasConfigRail.test.tsx, frontend/src/components/workflow/composer/CanvasView.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/composer/CanvasView.test.tsx (existing render/interaction patterns to mirror)
    - frontend/src/components/workflow/AdvancedExpander.test.tsx (the 51-06 fan-out test — mirror its toggle/persist assertions for the rail)
  </read_first>
  <action>
    Create `CanvasConfigRail.test.tsx` (new): toggling "Fan out over a list" ON calls `onSelection` with `{ strategy: "fanout_batch", task_source: { source_step: <prev> } }`; the source `<select>` defaults to the immediately-preceding agent and lists only `priorAgents`; the toggle is disabled when `priorAgents` is empty (first agent); toggling OFF clears the levers; the unknown-producer warning renders for a non-known source. Extend `CanvasView.test.tsx`: `priorAgents` is threaded to the rail (the agents before the selected node), and changing the selected node updates the available source options.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/workflow/composer/CanvasConfigRail.test.tsx src/components/workflow/composer/CanvasView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `CanvasConfigRail.test.tsx` covers: ON persists `{strategy, task_source}`; earlier-only + default-preceding source; first-agent disabled; OFF clears; unknown warning.
    - `CanvasView.test.tsx` covers: priorAgents threading + source-options update on selection change.
    - `cd frontend && npm test -- src/components/workflow/composer/CanvasConfigRail.test.tsx src/components/workflow/composer/CanvasView.test.tsx` passes.
  </acceptance_criteria>
  <done>Canvas fan-out persistence + priorAgents threading are vitest-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer canvas client → selection map | The canvas persists a fan-out selection; the backend `trust="user"` re-compile is the authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07-E | Elevation of Privilege | client-authored selection | mitigate | The canvas writes only `strategy: "fanout_batch"` + `task_source` via the shared reducer (no tools/spawn field); the backend trust-compile is the authority and rejects any user_allowed=False capability. |
| T-51-07-T | Tampering / mis-wire | source picker | mitigate | `priorAgents` (earlier only) bounds the picker; the compile guard (51-02) rejects a non-upstream source_step server-side; the unknown-producer warning + first-agent-disabled steer users to a real producer node (INSERT-A-NODE). |
| T-51-07-SC | Tampering | package installs | accept | No package installs — FE component + test edits only; nothing to audit. |
</threat_model>

<verification>
- `cd frontend && npm test -- src/components/workflow/composer/CanvasConfigRail.test.tsx src/components/workflow/composer/CanvasView.test.tsx` green.
- Shared reducer only (no forked lever logic); scope-fence knobs stay out of the UI.
</verification>

<success_criteria>
The canvas composer surface reaches parity with the simple-view: a fan-out toggle + earlier-only source picker (threaded via `priorAgents`) persisting through the shared reducer, with first-agent-disabled + unknown-producer guardrails — vitest-locked. Both composer surfaces now let a user compose a fan-out that the backend honors end-to-end.
</success_criteria>

<output>
Create `.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-07-SUMMARY.md` when done.
</output>
