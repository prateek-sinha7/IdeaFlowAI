---
phase: 51-user-composable-fan-out-in-the-composer-path-b
plan: 05
type: execute
wave: 3
depends_on: ["51-04"]
files_modified:
  - backend/tests/agents/test_composed_fanout.py
autonomous: true
requirements: [FANOUT-07, FANOUT-06]
must_haves:
  truths:
    - "A selections-driven (COMPOSED, not file-manifest) fan-out actually spawns N workers offline: N subagent_spawned + N subagent_result events + a merged deliverable."
    - "The crux carries strategy/fanout/task_source at BOTH the in-plan and absent-agent sites (the test drives an absent composed worker)."
    - "The absent synthesis-site path is PROVABLY under test (`_steps_by_agent.get(worker) is None`, C1) and the D9 upstream guard is proven to fire on the internal composed trust-compile (later/unknown source_step rejected, earlier fans out, C2)."
    - "The kernel references no composed-workflow name literal (SC-001 grep 0); banned-pattern grep 0."
    - "The 5 characterization goldens remain byte/event-identical (INV-3)."
  artifacts:
    - path: "backend/tests/agents/test_composed_fanout.py"
      provides: "offline composed-fan-out characterization (the crux proof)"
      contains: "subagent_spawned"
  key_links:
    - from: "engine.execute(selections=...)"
      to: "run_fanout"
      via: "_apply_selections carries fanout_batch + task_source.source_step to the absent worker step"
      pattern: "fanout_batch"
---

<objective>
[BLOCKING] The crux proof (D10 step 2 / scope §6b). Build an OFFLINE characterization test that drives a COMPOSED (selections-driven, NOT file-manifest) fan-out end-to-end through `engine.execute(selections=...)`: a producer agent emits a 3-task `## Task N:` list; a consumer/worker agent's SELECTION sets `strategy: fanout_batch` + `task_source.source_step = <producer>`; assert 3 `subagent_spawned` + 3 `subagent_result` events + a merged deliverable, and that the kernel contains no composed-workflow-name literal (SC-001). This is the pivotal acceptance gate that DE-RISKS the whole feature — it MUST pass before any FE work (Waves 4-5 depend on it).

Purpose: 51-04's unit tests prove the overlay mechanics; THIS proves fan-out actually runs from a composed selection. Mirror `test_sc001_fanout.py` exactly for the harness; diverge only in driving via `selections=` (composed) instead of a file manifest.
Output: New `backend/tests/agents/test_composed_fanout.py` + a re-run of the 5 goldens as the phase INV-3 gate.

Implements FANOUT-07 (the offline characterization crux) and FANOUT-06 (invariants). Per decisions D3, D10.
</objective>

<artifacts_produced>
- `backend/tests/agents/test_composed_fanout.py` — the offline composed-fan-out characterization (the "new characterization test path" named in the phase artifact list). This is the [BLOCKING] gate for the FE waves.
</artifacts_produced>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-CONTEXT.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Offline composed-fan-out characterization</name>
  <files>backend/tests/agents/test_composed_fanout.py</files>
  <read_first>
    - backend/tests/agents/test_sc001_fanout.py (THE analog, ~450 lines — copy the harness: the monkeypatch seam :214-320 patching `compile_for_run`/`resolve_alias`/`get_pipeline_agents`/`create_runner`; the `ScriptedFakeChatModel` worker turn emitting `write_file` for its `=== CURRENT TASK ===` part :89-154; the planner turn emitting the 3-task `## Task N:` plan :79-86/:192-193; the no-op `_run_planner`/`_store.store`/`_run_review_gate` :276-290; the `record_subagent_run` probe :251-262; the assertions :339-380; the SC-001 grep :408-430; the `shared_read` harness caveat)
    - .planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md (section 8 — the exact DIVERGENCE for this phase: drive via `selections=` composed, assert both sites, assert the benign hook delta)
    - backend/agents/execution_engine/engine.py (`_apply_selections` + the synthesis site — the code under proof; do NOT edit it here)
  </read_first>
  <action>
    Create `backend/tests/agents/test_composed_fanout.py` mirroring `test_sc001_fanout.py`'s harness EXACTLY, with ONE divergence (scope §6b): instead of a file manifest declaring the fan-out, pass a COMPOSED selection into `engine.execute(...)` — `selections = { "<worker_agent_id>": { "strategy": "fanout_batch", "task_source": { "kind": "parsed", "parser": "heading_tasks", "source_step": "<producer_agent_id>" } } }`. Compose the run agents as `producer → worker` where the worker is ABSENT from the base manifest (so the run exercises the synthesis-site consult path, the COMMON `custom` case) — this proves the crux at BOTH the in-plan and absent-agent sites. **Pin the absent-worker mechanism (C1 — do NOT leave this to chance):** the mocked base-manifest compile (`compile_for_run`) MUST return a plan whose `steps` EXCLUDE the worker, while the run's specs / `get_pipeline_agents` INCLUDE it — so at the synthesis site `_steps_by_agent.get(worker_id)` is `None` and the worker's fan-out step can ONLY come from `_user_steps_by_agent`. Add an EXPLICIT assertion that `_steps_by_agent.get(worker_id) is None` (or an equivalent probe proving the synthesis-site consult, not the in-plan overlay, produced the worker's step) so an executor cannot silently downgrade the test by making the worker present in the base manifest. **D9 × composed guard (C2):** add a negative sub-case — a composed worker whose `task_source.source_step` names a LATER run agent (or an unknown id) MUST be rejected by the D9 upstream guard when `_apply_selections` runs its internal `trust="user"` re-compile; assert the ACTUAL disposition the code takes (either a 422 at the LAUNCH re-validate `run_commands.py:1535` OR the `except CompilerError` degrade in `_apply_selections` that yields NO fan-out — pin whichever the real code path produces), while the happy path (`source_step` = an EARLIER run agent) compiles clean and fans out. This proves the widened `agent_ids` ordering feeds the guard correctly. The producer's scripted turn emits a 3-task `## Task N:` plan; each worker copy writes a distinct part file. Assert: exactly 3 `subagent_spawned` and 3 `subagent_result` events with `status == "complete"`; the merged deliverable bundle contains the produced files; NO `merge_conflict`; and the SC-001 grep — `grep -rl "<composed-workflow-name>" backend/agents/execution_engine/` returns "" (the kernel names no workflow). Assert the benign hook delta (default `audit_logger`/`secret_scan` on the trust-compiled absent worker step) as INTENDED (Risk #6), not a regression. Reuse the `shared_read` harness caveat comment from `test_sc001_fanout` (isolated per-worker writes + real merge are a live-Bedrock concern — the orchestrator live proof, §6d). Do NOT touch `SNAPSHOT_UPDATE`.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_composed_fanout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The test drives `engine.execute(selections=...)` (composed, NOT a file manifest) and PASSES.
    - Asserts exactly 3 `subagent_spawned` + 3 `subagent_result` (`status == "complete"`) + a merged deliverable containing the produced files + no `merge_conflict`.
    - The composed worker is ABSENT from the base manifest (exercises the synthesis-site consult) AND the assertion confirms its step carried `strategy == "fanout_batch"`.
    - **C1:** the mocked base compile EXCLUDES the worker AND the test explicitly asserts `_steps_by_agent.get(worker_id) is None` (or equivalent) — proving the ABSENT synthesis-site path is under test, not the in-plan overlay. An executor cannot make the worker present without failing this assertion.
    - **C2:** a composed worker whose `source_step` names a LATER/unknown run agent is REJECTED by the D9 guard on the internal trust-compile (assert the actual disposition — 422 at launch OR degrade-to-no-fanout), while an EARLIER `source_step` fans out — proving the guard runs on the composed internal compile.
    - SC-001 grep in the test asserts `grep -rl "<composed-name>" backend/agents/execution_engine/` == "" (kernel is name-free).
    - `cd backend && python3.11 -m pytest tests/agents/test_composed_fanout.py -q` passes.
  </acceptance_criteria>
  <done>A user-composed fan-out selection spawns N workers offline and merges — the crux is proven end-to-end. FE waves may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Phase invariant gate — goldens + import-linter + banned-pattern</name>
  <files>backend/tests/agents/test_composed_fanout.py</files>
  <read_first>
    - backend/tests/agents/test_characterization_prototype.py (+ 4 siblings), backend/tests/agents/test_banned_patterns.py
  </read_first>
  <action>
    No new code — the phase-level INV gate confirming the composed characterization introduced no regression. Run the 5 characterization goldens (SNAPSHOT_UPDATE unset), the banned-pattern gate, and lint-imports. All must be green. If any golden drifts, STOP — the composed test or a stray engine change broke the empty path; do not regenerate snapshots.
  </action>
  <verify>
    <automated>cd backend && SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_banned_patterns.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5 goldens byte/event-identical (SNAPSHOT_UPDATE unset).
    - `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -q` green (banned-pattern grep 0).
    - `/opt/homebrew/bin/lint-imports` reports 4/0.
  </acceptance_criteria>
  <done>The full offline invariant suite is green — the [BLOCKING] gate is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composed run → kernel fan-out | The test drives a user-composed fan-out through the real kernel spawn path (`run_fanout`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05-I | Information Disclosure / parity | golden drift | mitigate | Task 2 re-runs all 5 goldens byte/event-identical (SNAPSHOT_UPDATE unset) — proves the offline test + engine change introduced no regression (INV-3). |
| T-51-05-T | Tampering | SC-001 name-leak | mitigate | The test's `grep -rl <composed-name> execution_engine/` asserts the kernel names no workflow (SC-001); banned-pattern gate re-run. |
| T-51-05-D | Denial of Service | offline isolation limits | accept | The `shared_read` offline harness cannot prove per-worker isolated writes / real merge — deferred to the orchestrator live-Bedrock proof (§6d). Documented, low residual risk. |
| T-51-05-SC | Tampering | package installs | accept | No package installs — a new test file only; nothing to audit. |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_composed_fanout.py -q` green.
- 5 goldens byte/event-identical; banned-pattern green; lint-imports 4/0.
</verification>

<success_criteria>
[BLOCKING] A composed (selections-driven) fan-out spawns 3 workers offline, merges, and leaves the kernel name-free and the goldens byte-identical. This gate must be GREEN before the FE waves (51-06, 51-07) run.
</success_criteria>

<output>
Create `.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-05-SUMMARY.md` when done.
</output>
