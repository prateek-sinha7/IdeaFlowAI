---
phase: 51-user-composable-fan-out-in-the-composer-path-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/workflows/selections.py
  - backend/tests/unit/test_user_workflows_selections.py
autonomous: true
requirements: [FANOUT-03]
must_haves:
  truths:
    - "_synthesize_step emits the user-selected strategy (e.g. fanout_batch) instead of the hardcoded single_shot, keyed generically by agent_id (no workflow/agent-name literal)."
    - "An empty/absent selection still yields a bare single_shot step (parity)."
    - "A fan-out selection compiles cleanly under trust=user via synthesize_manifest + WorkflowCompiler; a smuggled user_allowed=False grant is rejected NAMING it."
  artifacts:
    - path: "backend/agents/workflows/selections.py"
      provides: "generic strategy-emit line in _synthesize_step"
      contains: "step[\"strategy\"] = sel[\"strategy\"]"
    - path: "backend/tests/unit/test_user_workflows_selections.py"
      provides: "synthesizer fan-out coverage"
  key_links:
    - from: "selections._synthesize_step"
      to: "WorkflowCompiler().compile(trust=user)"
      via: "synthesize_manifest throwaway trust-check manifest"
      pattern: "strategy"
---

<objective>
P2 — the synthesizer. Make `agents/workflows/selections._synthesize_step` emit the user-selected `strategy` (the crux prerequisite: the engine overlay in 51-04 reads `user_step.strategy`, which is only populated if the synthesizer carries the selected strategy through the `trust="user"` re-compile). Today `_synthesize_step` hardcodes `strategy: single_shot` at `selections.py:86` and never overrides it, so a composed `fanout_batch` selection never becomes a `fanout_batch` step. `fanout`/`task_source` already ride the existing projection loop (`selections.py:128-131`) — the ONLY change is one generic strategy line.

Purpose: Without this, 51-04's overlay would patch `strategy = "single_shot"` (wrong) and nothing fans out. This is build-order step 1 (D10) — isolated and unit-testable immediately.
Output: One generic line in `_synthesize_step` + extended unit coverage in `test_user_workflows_selections.py`.

Implements FANOUT-03. Per decision D5 (synthesizer emits chosen strategy, GENERIC — no name literal).
</objective>

<artifacts_produced>
- `backend/agents/workflows/selections.py` — new generic strategy-emit line in `_synthesize_step` (after the `isinstance(sel, dict)` guard, ~line 95).
- Extended tests in `backend/tests/unit/test_user_workflows_selections.py`.
</artifacts_produced>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-CONTEXT.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit the user-selected strategy in _synthesize_step</name>
  <files>backend/agents/workflows/selections.py</files>
  <read_first>
    - backend/agents/workflows/selections.py (`_synthesize_step` :78-133, hardcoded strategy :86, isinstance guard :95, projection loop :128-131)
    - .planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md (section 2 — the verbatim analog + the exact one-line insert)
  </read_first>
  <behavior>
    - When `sel` carries a non-empty string `strategy` (e.g. "fanout_batch"), the synthesized step dict has `step["strategy"]` == that value.
    - When `sel` has no `strategy` (or empty/non-string), the step keeps the safe `single_shot` default.
    - `fanout` and `task_source` in `sel` already appear on the step via the existing :128-131 loop (unchanged) — confirm they still project.
    - The insert is keyed only on the generic `strategy` key — no workflow/agent-name literal (INV-1/SC-001).
  </behavior>
  <action>
    In `_synthesize_step`, immediately AFTER the `if not isinstance(sel, dict):` guard block (~selections.py:95, before the validators/gates handling), add a generic strategy override: when `sel.get("strategy")` is a non-empty string, set `step["strategy"]` to it. Use the exact form given in PATTERNS.md section 2 (`if isinstance(sel.get("strategy"), str) and sel["strategy"]: step["strategy"] = sel["strategy"]`). Do NOT touch the `single_shot` default at :86 (it remains the absent/empty fallback). Do NOT touch the projection loop at :128-131 (`fanout`/`task_source`/`on_conflict`/`tools` already ride it). Keep it name-free per D5. The dead `_LEVER_KEYS` constant may be left as-is (it gates nothing).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'step\["strategy"\] = sel\["strategy"\]' backend/agents/workflows/selections.py` returns a hit inside `_synthesize_step` (after the isinstance guard, before line 128).
    - No workflow/agent-name string literal added: `grep -nE 'prototype|custom|fanout_batch|sample-fanout' backend/agents/workflows/selections.py` shows no NEW literal in `_synthesize_step` (the change keys only on `sel["strategy"]`).
    - `cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py -q` passes.
  </acceptance_criteria>
  <done>The synthesizer emits any user-selected strategy generically; single_shot remains the default; existing selection tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend synthesizer unit coverage for fan-out</name>
  <files>backend/tests/unit/test_user_workflows_selections.py</files>
  <read_first>
    - backend/tests/unit/test_user_workflows_selections.py (existing `_synthesize_step` / synthesize_manifest / trust-compile tests to mirror)
    - backend/agents/workflows/selections.py (`_synthesize_step`, `synthesize_manifest` :136-181, `has_selections` :218-227)
    - backend/agents/capabilities/strategies/fanout_batch.py (`user_allowed=True` :38)
  </read_first>
  <action>
    Add unit tests (mirroring the existing test style in this file): (1) `_synthesize_step(agent, {"strategy": "fanout_batch", "task_source": {"kind":"parsed","parser":"heading_tasks","source_step":"prod"}, "fanout": {"mode":"parallel","max_parallel":4}})` yields a step whose `strategy == "fanout_batch"` and that carries `task_source` + `fanout`; (2) `_synthesize_step(agent, None)` and `_synthesize_step(agent, {})` yield `strategy == "single_shot"` with no `task_source`; (3) end-to-end trust check: `synthesize_manifest(...)` + `WorkflowCompiler().compile(manifest, CapabilityRegistry(), trust="user")` ACCEPTS a fan-out selection (fanout_batch + heading_tasks are user_allowed) and produces a compiled Step with populated `.strategy`; (4) a smuggled `user_allowed=False` grant (e.g. `tools: {spawn_subagents: true}` or an exec strategy) is REJECTED by the trust compile with a CompilerError NAMING the offending capability. Do NOT touch the two existing `ExecutionEngine._apply_selections(...)` callers at :262 and :421 — those are owned by plan 51-04.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - New tests assert `strategy == "fanout_batch"` when selected and `single_shot` when absent.
    - A trust=user compile test ACCEPTS a fan-out selection and REJECTS a smuggled user_allowed=False grant (asserting the CompilerError message names the capability).
    - `cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py -q` passes with the new tests present.
  </acceptance_criteria>
  <done>Synthesizer fan-out behavior is unit-locked: strategy emission, defaulting, task_source/fanout carry, and trust-boundary accept/reject.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer selection → synthesizer → compiler | A user-authored selections map crosses into a throwaway manifest that is re-compiled under `trust="user"`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01-E | Elevation of Privilege | `_synthesize_step` strategy emission | mitigate | The synthesizer only PROJECTS the string; the authority is `WorkflowCompiler.compile(trust="user")` which rejects any `user_allowed=False` strategy/parser/tool NAMING it (verified: fanout_batch/heading_tasks are user_allowed=True). Test 2(4) asserts the reject path. |
| T-51-01-T | Tampering | generic keying | mitigate | Change keys only on `sel["strategy"]` — no workflow/agent-name literal enters the workflows layer (INV-1/SC-001); banned-pattern grep stays 0. |
| T-51-01-SC | Tampering | package installs | accept | No package installs in this plan — no new dependency added; nothing to audit. |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py -q` green.
- No new name literal in `selections.py` (`grep -nE 'fanout_batch|prototype|sample-fanout' backend/agents/workflows/selections.py` shows only the pre-existing loop keys, none inside `_synthesize_step`).
</verification>

<success_criteria>
The synthesizer emits the selected strategy generically; single_shot stays the default; fan-out selections trust-compile and smuggled grants are rejected — all unit-proven. Unblocks the 51-04 engine overlay (which reads `user_step.strategy`).
</success_criteria>

<output>
Create `.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-01-SUMMARY.md` when done.
</output>
