---
phase: 51-user-composable-fan-out-in-the-composer-path-b
plan: 06
type: execute
wave: 4
depends_on: ["51-05", "51-03"]
files_modified:
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/workflow/AdvancedExpander.test.tsx
  - frontend/src/components/workflow/IdeaInputPage.selections.test.tsx
autonomous: true
requirements: [FANOUT-01, FANOUT-04, FANOUT-05, FANOUT-07]
must_haves:
  truths:
    - "The shared StepSelection type carries the fan-out levers (strategy/task_source/fanout) so both composer surfaces persist them."
    - "The simple-view AdvancedExpander has a 'Fan out over a list' toggle + a source picker (earlier agents only); enabling writes {strategy, task_source}, disabling clears them; empty selections are omitted (INV-3)."
    - "The toggle is disabled for the first agent (no producer) and warns on an unknown (non-known-producer) source."
    - "The composed fan-out selection threads into BOTH the save (createUserWorkflow) and launch (selections) payloads, and is omitted when empty."
  artifacts:
    - path: "frontend/src/components/workflow/AgentsPopup.tsx"
      provides: "widened StepSelection type + AdvancedExpander fan-out control"
      contains: "fanout_batch"
  key_links:
    - from: "AdvancedExpander toggle"
      to: "applyLeverPatch (shared reducer)"
      via: "updateLever(agent.id, {strategy, task_source})"
      pattern: "updateLever"
---

<objective>
P3 + P4 (simple-view half). Widen the shared `StepSelection` type (D6/§4a) with the fan-out levers, then add a "Fan out over a list" toggle + source picker to the simple-view `AdvancedExpander` (§4c) driven by the existing generic `updateLever`/`applyLeverPatch` reducer (no reducer change). Enforce the FE guardrails (D9/§5b): source picker lists only EARLIER agents, toggle disabled for the first agent, non-blocking warning on an unknown producer. This plan OWNS all `AgentsPopup.tsx` edits (the type lives here alongside `AdvancedExpander`), so the canvas surface (51-07) imports the widened type without a conflict.

Purpose: Now that the backend honors the persisted fan-out selection (proven by the [BLOCKING] 51-05), the composer can persist + thread it. The INSERT-A-NODE producer model (D2/D7): the source picker offers reuse only for known `## Task N:` producers (v1 allow-list: `prototype-plan` + the new `task-list-planner` from 51-03), else steer to "Insert a producer".
Output: Widened `StepSelection` + `AdvancedExpander` fan-out control + vitest (`AdvancedExpander.test.tsx`, `IdeaInputPage.selections.test.tsx`).

Implements FANOUT-01 (persist/omit on the simple surface), FANOUT-04 (FE producer sourcing), FANOUT-05 (FE guardrails), FANOUT-07 (FE vitest). Per decisions D6, D7, D9.
</objective>

<artifacts_produced>
- `frontend/src/components/workflow/AgentsPopup.tsx` — widened `StepSelection` type (the "new StepSelection fields" named in the phase artifact list): `strategy?: "fanout_batch"`, `task_source?: { kind: "parsed"; parser: "heading_tasks" | "json_tasks"; source_step: string }`, `fanout?: { mode?: "parallel"; max_parallel?: number }` — plus the `AdvancedExpander` fan-out toggle + source picker.
</artifacts_produced>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-CONTEXT.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen StepSelection + add the AdvancedExpander fan-out control</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentsPopup.tsx (`StepSelection` type :1419-1425; `applyLeverPatch` :1452-1475 — the SINGLE generic writer, deletes undefined/""/[] keys, NO change needed; `AdvancedExpander` :1547; per-agent rows `agents.map` :1621; `updateLever` :1578/:1666 — the existing Validator lever call is the template)
    - .planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md (section 6 — verbatim type + reducer + the AdvancedExpander pattern)
    - .planning/PATH-B-FANOUT-COMPOSER-SCOPE.md (§4a, §4c, §4e — the producer reuse-or-INSERT rule + the v1 known-producer allow-list)
  </read_first>
  <action>
    (a) Widen the exported `StepSelection` type (:1419-1425) with the three optional fan-out fields (see PATTERNS §6 / artifacts_produced above). Do NOT change `applyLeverPatch` (it already deletes `undefined` keys — toggling OFF via `{strategy: undefined, task_source: undefined, fanout: undefined}` clears them).
    (b) In `AdvancedExpander`'s per-agent row (`agents.map((agent, idx) => …)` :1621), add a "Fan out over a list" toggle + a "Source list from" `<select>`, driven by the existing `updateLever(agent.id, {...})` (same shape as the Validator lever :1666). On enable: `updateLever(agent.id, { strategy: "fanout_batch", task_source: { kind: "parsed", parser: "heading_tasks", source_step: <default> } })`. On disable: `updateLever(agent.id, { strategy: undefined, task_source: undefined, fanout: undefined })`. On source change: `updateLever(agent.id, { task_source: { kind: "parsed", parser: "heading_tasks", source_step: e.target.value } })`.
    (c) GUARDRAILS (D9/§5b): the `<select>` options = the agents BEFORE this row (`agents.slice(0, idx)`); default source = the immediately-preceding agent when it is a KNOWN producer, else prompt "Insert a producer". Known-producer allow-list (v1) = `["prototype-plan", "task-list-planner"]` (the latter from 51-03). Disable the toggle when `idx === 0` with an inline hint ("Add an earlier step that outputs a task list to fan out over."). When fan-out is enabled but the chosen source is not in the known-producer allow-list, render a NON-blocking warning ("This step fans out one worker per `## Task N:` heading its source outputs"). Keep `merge_agent`/`max_parallel`/`json_tasks`/`on_conflict` OUT of the UI (scope fence).
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/workflow/AdvancedExpander.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `StepSelection` includes `strategy`, `task_source`, `fanout` optional fields (`grep -n "fanout_batch" frontend/src/components/workflow/AgentsPopup.tsx` hits the type).
    - `applyLeverPatch` is unchanged (`git diff` shows no edit to :1452-1475).
    - The AdvancedExpander row renders a fan-out toggle + source `<select>` (earlier agents only), disabled for `idx === 0`, with an unknown-producer warning.
    - `cd frontend && npm test -- src/components/workflow/AdvancedExpander.test.tsx` passes.
  </acceptance_criteria>
  <done>The shared type carries fan-out; the simple-view surface persists/clears it through the shared reducer with the earlier-only + first-agent-disabled + unknown-warning guardrails.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest — simple-view toggle/persist + save/launch payload threading</name>
  <files>frontend/src/components/workflow/AdvancedExpander.test.tsx, frontend/src/components/workflow/IdeaInputPage.selections.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AdvancedExpander.test.tsx (existing lever-toggle test patterns to mirror)
    - frontend/src/components/workflow/IdeaInputPage.selections.test.tsx (existing save/launch payload threading assertions — where `selections` reaches `createUserWorkflow` + the launch payload; mirror for fan-out)
  </read_first>
  <action>
    Extend `AdvancedExpander.test.tsx`: toggling "Fan out over a list" ON persists `{ strategy: "fanout_batch", task_source: { source_step: <prev> } }` into the agent's selection; the source picker defaults to the immediately-preceding agent and lists ONLY earlier agents; the toggle is disabled for the first agent; toggling OFF clears the levers (selection omitted when nothing else set); the unknown-producer warning renders when the source is not a known producer. Extend `IdeaInputPage.selections.test.tsx`: a composed fan-out selection threads into BOTH the `createUserWorkflow` SAVE payload AND the LAUNCH `selections` payload, and is OMITTED when empty (INV-3 short-circuit).
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/workflow/AdvancedExpander.test.tsx src/components/workflow/IdeaInputPage.selections.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AdvancedExpander tests cover: ON persists `{strategy, task_source}`; earlier-only + default-preceding source options; first-agent disabled; OFF clears; unknown warning.
    - IdeaInputPage.selections tests cover: fan-out selection in the save payload AND the launch payload; omitted when empty.
    - `cd frontend && npm test -- src/components/workflow/AdvancedExpander.test.tsx src/components/workflow/IdeaInputPage.selections.test.tsx` passes.
  </acceptance_criteria>
  <done>Simple-view fan-out persistence + save/launch threading + omit-when-empty are vitest-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer client → save/launch payload | The FE persists a fan-out selection; the backend `trust="user"` re-compile is the authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-06-E | Elevation of Privilege | client-authored selection | mitigate | The FE only writes `strategy: "fanout_batch"` + `task_source` (no tools/spawn field); the backend `trust="user"` compile (51-01/51-04) rejects any user_allowed=False capability regardless of what the client sends — the client is never the authority. |
| T-51-06-T | Tampering / mis-wire | source picker | mitigate | The picker offers only EARLIER agents; the compile guard (51-02) rejects a non-upstream source_step server-side; the unknown-producer warning steers users to a real producer (INSERT-A-NODE, D2/D7). |
| T-51-06-SC | Tampering | package installs | accept | No package installs — FE component + test edits only; nothing to audit. |
</threat_model>

<verification>
- `cd frontend && npm test -- src/components/workflow/AdvancedExpander.test.tsx src/components/workflow/IdeaInputPage.selections.test.tsx` green.
- `applyLeverPatch` unchanged; no new UI knobs beyond the toggle + source picker (scope fence honored).
</verification>

<success_criteria>
The shared `StepSelection` carries fan-out; the simple-view surface persists/clears it and threads it through save + launch with the earlier-only/first-disabled/unknown-warning guardrails — vitest-locked. Unblocks the canvas surface (51-07) which imports the widened type.
</success_criteria>

<output>
Create `.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-06-SUMMARY.md` when done.
</output>
