---
phase: 51-user-composable-fan-out-in-the-composer-path-b
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/prompts/task-list-planner/AGENT.md
  - backend/agents/registry.py
  - backend/agents/workflows/custom/workflow.yaml
  - backend/tests/agents/test_registry.py
autonomous: true
requirements: [FANOUT-04]
must_haves:
  truths:
    - "A single GENERIC task-list-planner producer agent ships whose sole output is `## Task N:` headings (an analog of prototype-plan/sample-fanout-plan, no engine capability kind)."
    - "The producer appears in the composer's user pool (get_all_agents_flat) AND is launch-allowed where fan-out compositions run (allowed_custom_agent_ids)."
    - "Adding it does NOT break the engine membership assertion or any of the 5 characterization goldens."
    - "The stale allowed_custom_agent_ids docstring (claims tightened-to-custom-pool; code unions all base agents) is corrected."
  artifacts:
    - path: "backend/agents/prompts/task-list-planner/AGENT.md"
      provides: "generic producer skill (curated prompt fragment)"
      contains: "## Task"
    - path: "backend/agents/registry.py"
      provides: "producer pool registration + corrected docstring"
  key_links:
    - from: "composer library (/api/agents/library)"
      to: "task-list-planner"
      via: "get_all_agents_flat over PIPELINE_AGENTS"
      pattern: "task-list-planner"
---

<objective>
P0 — the one content prerequisite for GENERAL fan-out (D8). Ship ONE generic `task-list-planner` producer skill: a curated `AGENT.md` (an analog of `sample-fanout-plan`/`prototype-plan`) whose SOLE output is a `## Task N:` list, so the `heading_tasks` parser turns each heading into one fan-out worker. It is a prompt fragment, NOT a new engine capability kind (no machinery). `prototype-plan` already covers the prototype domain; this makes fan-out domain-general (INSERT-A-NODE producer per D2/D7). Also correct the stale `allowed_custom_agent_ids("custom")` docstring (Risk #5).

Purpose: The composer's "Insert a producer" action (51-06/51-07) seeds a producer node from this skill; without it, general (non-prototype) fan-out has no clean producer. Independent of engine/synthesizer/FE — Wave 1.
Output: New agent folder + pool registration + docstring fix + registry test.

Implements FANOUT-04 (the generic producer content half + the "reuse-or-INSERT" allow-list target). Per decisions D2, D7, D8.
</objective>

<artifacts_produced>
- `backend/agents/prompts/task-list-planner/AGENT.md` — the generic producer skill (the "generic producer skill file path" named in the phase artifact list). Agent id: `task-list-planner` (this exact id is referenced by the FE producer-sourcing plans 51-06/51-07).
- `backend/agents/registry.py` — `task-list-planner` added to the composer pool + corrected `allowed_custom_agent_ids` docstring.
- `backend/agents/workflows/custom/workflow.yaml` — membership kept consistent (see Task 2 pitfall note).
</artifacts_produced>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-CONTEXT.md
@.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the generic task-list-planner producer AGENT.md</name>
  <files>backend/agents/prompts/task-list-planner/AGENT.md</files>
  <read_first>
    - backend/agents/prompts/prototype-plan/AGENT.md (the shipped domain producer — copy its OUTPUT CONTRACT generically: "EVERY task MUST use EXACTLY `## Task N:`", "your SOLE output is the task plan")
    - backend/tests/agents/fixtures/sc001_fanout/sample-fanout-plan/AGENT.md (the fixture producer — top-level `## Task N:` headings as the sole output; frontmatter `tools: []`)
    - .planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-PATTERNS.md (section 7 — the analog + the "prefer top-level `## Task N:` sole output" note)
    - backend/CLAUDE.md ("Adding an Agent" + AGENT.md Schema table)
  </read_first>
  <action>
    Create `backend/agents/prompts/task-list-planner/AGENT.md`. Frontmatter (mirror the schema in backend/CLAUDE.md): `id: task-list-planner`, a human name/role (e.g. "Task List Planner" / "Fan-out task decomposition"), `tools: []` (text-only producer, no disk), `guardrails: []`, `icon`, a valid `max_tokens` (1–32768), `estimated_duration`, and a `pipeline_type` + `order` consistent with the pool choice in Task 2. Body (Claude's-discretion wording per D8): instruct the agent that its SOLE output is a plain-text task plan of top-level `## Task N:` headings (one per fan-out worker), using EXACTLY the `## Task N:` header format (no `<tasks>` wrapper — prefer top-level headings since `heading_tasks` scans the whole output and slices on `##`), domain-GENERAL (decompose ANY input into independent parallelizable tasks), NEVER ask clarifying questions, NEVER emit prose/HTML/tool calls. Do NOT invent a new tool set or capability kind — it is a prompt fragment only.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_loader.py -k "schema_validation" -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/agents/prompts/task-list-planner/AGENT.md` exists; frontmatter `id` == folder name `task-list-planner`.
    - Body mandates the exact `## Task N:` header format and "SOLE output is the task plan" (grep for `## Task` and a SOLE-output directive).
    - `cd backend && python3.11 -m pytest tests/agents/test_loader.py -k "schema_validation" -q` passes (the loader validates every AGENT.md).
    - No new tool set / capability kind introduced (`tools: []`).
  </acceptance_criteria>
  <done>A schema-valid, domain-general `## Task N:` producer skill exists.</done>
</task>

<task type="auto">
  <name>Task 2: Register the producer into the composer pool + fix the stale docstring</name>
  <files>backend/agents/registry.py, backend/agents/workflows/custom/workflow.yaml, backend/tests/agents/test_registry.py</files>
  <read_first>
    - backend/agents/registry.py (`PIPELINE_AGENTS`, `get_all_agents_flat` — it iterates PIPELINE_AGENTS, NOT a filesystem scan; `allowed_custom_agent_ids` :332+ incl. the stale "custom" docstring; `get_pipeline_agents`)
    - backend/agents/workflows/custom/workflow.yaml (8 steps: market-research-agent … report-generator)
    - backend/CLAUDE.md ("Adding an Agent" Step 3 — add id to PIPELINE_AGENTS; the membership-assertion contract: `get_pipeline_agents(compiled.id)` MUST equal the compiled manifest steps or engine.py:1701 raises)
  </read_first>
  <action>
    Register `task-list-planner` so it (a) surfaces in the composer library and (b) is launch-allowed for fan-out compositions. RECOMMENDED path (verify against the live code before committing): add `task-list-planner` to `PIPELINE_AGENTS["custom"]` (the utility pool, unioned into every base pipeline AND `custom` by `allowed_custom_agent_ids`). CRITICAL PITFALL to avoid: `get_all_agents_flat()` iterates `PIPELINE_AGENTS` (not the filesystem), and the engine membership assertion (engine.py:1701-1714) requires `get_pipeline_agents("custom")` to equal the compiled `custom/workflow.yaml` steps — so if you add the id to `PIPELINE_AGENTS["custom"]` you MUST also add a matching `- agent: task-list-planner` step to `backend/agents/workflows/custom/workflow.yaml` (append it, preserving order), or a plain `pipeline_type=custom` file run raises RuntimeError. Set the AGENT.md `pipeline_type`/`order` in Task 1 to match this placement. Do NOT touch any base/revision pipeline membership (goldens must stay byte-identical). ALSO: correct the stale `allowed_custom_agent_ids` docstring — it claims the "custom" branch was "tightened to just the custom pool", but the code unions the custom pool with ALL non-revision, non-internal base-pipeline agents (Risk #5) — reword the docstring to match the code (no behavior change).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_registry.py tests/agents/test_registry_helpers.py tests/agents/test_loader.py -q && python3.11 -c "from agents.registry import get_all_agents_flat, allowed_custom_agent_ids; ids={a.id for a in get_all_agents_flat()}; assert 'task-list-planner' in ids; assert 'task-list-planner' in allowed_custom_agent_ids('custom'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `task-list-planner` ∈ `{a.id for a in get_all_agents_flat()}` (appears in the composer library) AND ∈ `allowed_custom_agent_ids("custom")` (launch-allowed) — the one-liner above prints OK from `backend/`.
    - `python3.11 -c "from agents.execution_engine.engine import compile_for_run; compile_for_run('custom'); print('custom OK')"` runs without a membership RuntimeError (custom/workflow.yaml membership kept consistent).
    - The `allowed_custom_agent_ids` docstring no longer claims "tightened to just the custom pool" — it describes the union-of-base-agents behavior.
    - The 5 goldens are unaffected (verified in 51-05); base/revision pipeline membership unchanged (`git diff` on registry.py touches only the `custom` list + the docstring).
    - `cd backend && python3.11 -m pytest tests/agents/test_registry.py tests/agents/test_registry_helpers.py tests/agents/test_loader.py -q` passes (update any registry snapshot test that enumerates the custom pool).
  </acceptance_criteria>
  <done>The generic producer is in the composer pool + launch-allowed, custom membership stays consistent, and the stale docstring is corrected — no golden/membership breakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent library → composition | A new user-selectable agent enters the pool; a user may insert it as a producer node. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03-E | Elevation of Privilege | new producer agent | mitigate | `tools: []` (text-only) — the producer has no disk/exec/spawn tools; it cannot elevate. It is a prompt fragment, not a capability kind. |
| T-51-03-T | Tampering | pool/membership consistency | mitigate | Membership kept consistent with `custom/workflow.yaml`; verified via a `compile_for_run("custom")` smoke + registry tests so no engine assertion regresses. |
| T-51-03-SC | Tampering | package installs | accept | No package installs — content + registry edits only; nothing to audit. |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_loader.py tests/agents/test_registry.py tests/agents/test_registry_helpers.py -q` green.
- Library + launch-allow one-liner prints OK; `compile_for_run("custom")` smoke passes.
</verification>

<success_criteria>
One generic `task-list-planner` producer skill ships, is discoverable in the composer pool and launch-allowed, breaks no membership/golden, and the stale docstring is corrected. Provides the FE "Insert a producer" target (id `task-list-planner`) for 51-06/51-07.
</success_criteria>

<output>
Create `.planning/phases/51-user-composable-fan-out-in-the-composer-path-b/51-03-SUMMARY.md` when done.
</output>
