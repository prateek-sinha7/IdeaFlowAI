---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/agents/capabilities/deliverables/__init__.py
  - backend/agents/capabilities/deliverables/single_file.py
  - backend/agents/capabilities/deliverables/serialized_sandbox.py
  - backend/agents/capabilities/deliverables/streamed_text.py
  - backend/agents/capabilities/deliverables/ppt.py
  - backend/agents/capabilities/context_providers/__init__.py
  - backend/agents/capabilities/context_providers/opendesign.py
  - backend/agents/capabilities/context_providers/previous_run.py
  - backend/agents/capabilities/registry.py
  - backend/tests/agents/test_deliverable_resolvers.py
  - backend/tests/agents/test_context_providers.py
autonomous: true
requirements: [PARITY-02, PARITY-03, PARITY-07]
must_haves:
  truths:
    - "single_file resolver reads deliverable.name (e.g. prototype.html) from the sandbox via the handle, with last-streamed / previous_run-seeded-original fallback — no pipeline_type branch"
    - "serialized_sandbox resolver returns serialize_sandbox_deliverable(root) when count_sandbox_deliverables(root) > 0 — the filename:-block format unchanged"
    - "streamed_text resolver returns _unwrap_artifact(last_streamed)"
    - "ppt resolver returns the carousel-sanitized + artifact-unwrapped streamed deliverable (owns both sanitize behaviors)"
    - "opendesign provider load(ctx) returns the {block-name -> content} map the L12 od/template/example branches inject today, using the physically-relocated context.py loaders (move-don't-copy, no wrapper)"
    - "previous_run provider seeds the parent run's spec/design/tasks ownership-checked via ScopedStore.assert_owns BEFORE seeding; a cross-owner PermissionError propagates and is never swallowed"
    - "all four agents/prototype/context.py importers are rewired to the opendesign provider's new home; load_ppt_od_context stays in od_context.py"
  artifacts:
    - path: "backend/agents/capabilities/deliverables/single_file.py"
      provides: "SingleFileResolver (name='single_file')"
      contains: "class SingleFileResolver"
    - path: "backend/agents/capabilities/deliverables/serialized_sandbox.py"
      provides: "SerializedSandboxResolver (name='serialized_sandbox')"
      contains: "class SerializedSandboxResolver"
    - path: "backend/agents/capabilities/deliverables/streamed_text.py"
      provides: "StreamedTextResolver (name='streamed_text')"
      contains: "class StreamedTextResolver"
    - path: "backend/agents/capabilities/deliverables/ppt.py"
      provides: "PptResolver (name='ppt') owning carousel-sanitize + artifact-unwrap"
      contains: "class PptResolver"
    - path: "backend/agents/capabilities/context_providers/opendesign.py"
      provides: "OpenDesignProvider (name='opendesign') + relocated load_prototype_context/get_template_injection_parts/get_example_html"
      contains: "class OpenDesignProvider"
    - path: "backend/agents/capabilities/context_providers/previous_run.py"
      provides: "PreviousRunProvider (name='previous_run') ownership-checked parent-run seed"
      contains: "class PreviousRunProvider"
  key_links:
    - from: "backend/agents/capabilities/context_providers/previous_run.py"
      to: "ScopedStore.assert_owns"
      via: "ownership check BEFORE seeding parent-run spec/design/tasks (INV-8)"
      pattern: "assert_owns"
    - from: "backend/agents/capabilities/deliverables/single_file.py"
      to: "ctx.runner sandbox.read(deliverable.name)"
      via: "deliverable.name from the compiled manifest, no pipeline_type branch"
      pattern: "deliverable"
---

<objective>
Implement the four `DeliverableResolver` families (`single_file`, `serialized_sandbox`, `streamed_text`, `ppt`), the two `ContextProvider` families (`opendesign`, `previous_run`), and the `seed_files`/`seed_files.from_run` materialization behavior — all behind the existing ports, reaching kernel/`app.*` primitives only through the 07-01 runner handle. The `ppt` resolver wraps the carousel-sanitize transform as a declared capability (PARITY-07). The `opendesign` provider PHYSICALLY relocates `agents/prototype/context.py`'s three live loaders (move-don't-copy, INV-12) and rewires all four importers. The `previous_run` provider seeds the parent run's spec/design/tasks, ownership-checked via the Phase-5 `ScopedStore.assert_owns` (INV-8 / L16).

This plan builds the CAPABILITIES only. The engine is NOT yet wired to use them (07-04) and NO leak is deleted (07-05). The L1–L13 leaks (`_resolve_final_output`, the carousel-sanitize sites, the revision seeding, `_build_context_message`) stay physically present and live; the new capabilities are exercised only by their Wave 0 unit tests + parity checks against the relocated loaders this plan. Strangler "wrap" step (§31).

Purpose: PARITY-02 (deliverable resolvers), PARITY-03 (providers + seed_files), PARITY-07 (ppt resolver + carousel-sanitize as a capability).
Output: `capabilities/deliverables/{single_file,serialized_sandbox,streamed_text,ppt}.py`, `capabilities/context_providers/{opendesign,previous_run}.py`, the relocated context.py loaders + rewired importers, install() bindings, and 2 Wave 0 unit test files.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-SPEC.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-CONTEXT.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-RESEARCH.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-PATTERNS.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-01-SUMMARY.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from any source-grounding drift check):
- `SingleFileResolver` (`agents/capabilities/deliverables/single_file.py`, `name="single_file"`)
- `SerializedSandboxResolver` (`agents/capabilities/deliverables/serialized_sandbox.py`, `name="serialized_sandbox"`)
- `StreamedTextResolver` (`agents/capabilities/deliverables/streamed_text.py`, `name="streamed_text"`)
- `PptResolver` (`agents/capabilities/deliverables/ppt.py`, `name="ppt"`)
- `OpenDesignProvider` (`agents/capabilities/context_providers/opendesign.py`, `name="opendesign"`) — with the relocated `load_prototype_context`/`get_template_injection_parts`/`get_example_html`
- `PreviousRunProvider` (`agents/capabilities/context_providers/previous_run.py`, `name="previous_run"`)
- New module init files: `agents/capabilities/deliverables/__init__.py`, `agents/capabilities/context_providers/__init__.py`
- Wave 0 tests: `tests/agents/test_deliverable_resolvers.py`, `tests/agents/test_context_providers.py`
NOTE: this plan RELOCATES (does not create) the three context.py loaders into `opendesign.py` (move-don't-copy). After relocation `agents/prototype/context.py` is emptied of those 3 fns; the package is NOT removed yet (07-05 removes it after `pipeline.py` is gone).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliverable resolvers (single_file / serialized_sandbox / streamed_text / ppt)</name>
  <files>backend/agents/capabilities/deliverables/__init__.py, backend/agents/capabilities/deliverables/single_file.py, backend/agents/capabilities/deliverables/serialized_sandbox.py, backend/agents/capabilities/deliverables/streamed_text.py, backend/agents/capabilities/deliverables/ppt.py, backend/agents/capabilities/registry.py, backend/tests/agents/test_deliverable_resolvers.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:447-528 (`_resolve_final_output` — the full chooser to decompose: revision branch :489-504, prototype branch :506-518, code-gen branch :520-525, text/PPT branch :527-528)
    - backend/agents/execution_engine/engine.py:363-426 (`_sanitize_carousel_deck_html`) and :427-446 (`_unwrap_artifact`) — lift into ppt.py / streamed_text.py
    - backend/agents/execution_engine/engine.py:1394 (the final-output carousel-sanitize call site) AND :1916 (the mid-stream carousel-sanitize in `_run_agent`, gated by `pipeline_type in _PPT_PIPELINE_TYPES`) — BOTH sanitize behaviors must live in the ppt resolver (Pitfall 3 / PARITY-07); this plan moves the BEHAVIOR into ppt.py (the engine deletions are 07-05)
    - backend/app/agents/sandbox.py (`RunSandbox.read`/`path_for`/`root`; `serialize_sandbox_deliverable`; `count_sandbox_deliverables` — the API the resolvers read via the handle, NOT a direct app.* import — Pitfall 4)
    - backend/agents/capabilities/base.py:64-72 (the `DeliverableResolver` port — `name` + `def resolve(self, ctx) -> Any`)
    - backend/agents/capabilities/strategies/task_loop.py (the ctx.runner handle contract from 07-01: sandbox/serialize/count access)
    - backend/agents/workflows/plan.py (the `DeliverableSpec` — `deliverable.name`/`deliverable.strategy`)
    - 07-PATTERNS.md (the four deliverable resolver sections + the deliverable-resolution decomposition)
    - backend/tests/agents/test_sandbox_deliverable.py (the resolver unit-test analog + the filename:-block byte assertion)
  </read_first>
  <behavior>
    - Test (test_deliverable_resolvers.py): with a sandbox fixture containing `prototype.html`, `SingleFileResolver().resolve(ctx)` returns the file contents when `deliverable.name == "prototype.html"`; when the file is missing it falls back to last-streamed / the `previous_run`-seeded original (set on ctx); no `pipeline_type` is read.
    - Test: with a sandbox containing 2+ code-gen deliverable files, `SerializedSandboxResolver().resolve(ctx)` returns `serialize_sandbox_deliverable(root)` byte-identical to the existing format; with 0 files it does not claim the deliverable (matches the `count > 0` guard).
    - Test: `StreamedTextResolver().resolve(ctx)` returns `_unwrap_artifact(last_streamed)` (artifact tag stripped).
    - Test: `PptResolver().resolve(ctx)` returns `_unwrap_artifact(_sanitize_carousel_deck_html(last_streamed))` — the carousel-sanitize + unwrap transform; a known carousel-deck input yields the byte-identical sanitized output the engine produces today.
  </behavior>
  <action>
    Create `capabilities/deliverables/__init__.py` + the four resolver modules, each satisfying the `DeliverableResolver` port (`name` attr + `def resolve(self, ctx) -> Any`), reaching the sandbox / serialize / count ONLY through `ctx.runner` (the 07-01 handle) — NEVER a direct `app.*` import (Pitfall 4). Keep `ctx` typed `Any`/`object`; `step`/`deliverable` may use `agents.workflows.plan` types.

    `single_file.py` — `class SingleFileResolver` (`name="single_file"`): read `deliverable.name` (default `"prototype.html"`) from the sandbox via the handle; fall back to last-streamed, then to the `previous_run`-seeded original carried on ctx (engine.py:489-518 prototype + revision branches FOLDED — NO `pipeline_type == "prototype_revision"` branch; that is L4/L8, deleted in 07-05). Absorb the L10 readback behavior (engine.py:1902 `if pipeline_type in ("od_prototype","prototype")`) into reading `deliverable.name` from the sandbox here — Pitfall 5.

    `serialized_sandbox.py` — `class SerializedSandboxResolver` (`name="serialized_sandbox"`): `if count_sandbox_deliverables(root) > 0: return serialize_sandbox_deliverable(root)` (engine.py:520-525) via the handle. Do NOT reformat the `filename:`-block UI contract.

    `streamed_text.py` — `class StreamedTextResolver` (`name="streamed_text"`): return `_unwrap_artifact(last_streamed)` (engine.py:527-528). Lift `_unwrap_artifact` (engine.py:427-446) into / alongside this module so `ppt` can share it (a small `_artifact.py` helper or a shared function — discretion; keep it import-pure).

    `ppt.py` — `class PptResolver` (`name="ppt"`): owns the carousel-sanitize + artifact-unwrap transform — return `_unwrap_artifact(_sanitize_carousel_deck_html(last_streamed))`. Lift `_sanitize_carousel_deck_html` (engine.py:363-426) verbatim. The resolver owns BOTH the final-output sanitize (engine.py:1394) AND the mid-stream sanitize (engine.py:1916) behaviors so 07-05 can delete BOTH kernel call sites (Pitfall 3 / PARITY-07).

    Update `registry.py` `install()` to bind `("deliverable","single_file")`, `("deliverable","serialized_sandbox")`, `("deliverable","streamed_text")`, `("deliverable","ppt")`.

    Write `test_deliverable_resolvers.py` per the behavior block (analog: `test_sandbox_deliverable.py`) using a temp sandbox + a fake handle.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_deliverable_resolvers.py tests/agents/test_sandbox_deliverable.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_deliverable_resolvers.py -x` exits 0 (all four resolvers from a sandbox fixture)
    - `cd backend && lint_imports` exits 0 (no resolver imports `agents.execution_engine`/`app.*`)
    - `grep -nE "from app\.|import app\.|from agents.execution_engine|import agents.execution_engine" backend/agents/capabilities/deliverables/*.py` returns 0 matches (Pitfall 4)
    - `grep -nE "pipeline_type" backend/agents/capabilities/deliverables/*.py` returns 0 matches (resolvers resolve by `deliverable.name`/`deliverable.strategy`, never by pipeline name — INV-1)
    - `grep -cE "_sanitize_carousel_deck_html" backend/agents/capabilities/deliverables/ppt.py` >= 1 (ppt owns the transform)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x` exits 0 (no regression — engine still on the legacy `_resolve_final_output` path)
  </acceptance_criteria>
  <done>The four resolvers exist behind the port, resolve by deliverable.name/strategy (no pipeline_type), ppt owns both carousel-sanitize behaviors, reach the sandbox only via the handle, and bind into install(); 0A characterization green (legacy path untouched).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: opendesign + previous_run providers (move-don't-copy relocation + ownership-checked seed)</name>
  <files>backend/agents/capabilities/context_providers/__init__.py, backend/agents/capabilities/context_providers/opendesign.py, backend/agents/capabilities/context_providers/previous_run.py, backend/agents/capabilities/registry.py, backend/agents/execution_engine/od_context.py, backend/agents/prototype/context.py, backend/agents/execution_engine/engine.py, backend/tests/agents/test_context_providers.py</files>
  <read_first>
    - backend/agents/prototype/context.py (FULL — the 3 LIVE functions to PHYSICALLY relocate: `load_prototype_context` :26-93, `get_template_injection_parts` :96-125, `get_example_html` :128-145; their dependency surface incl. `app.services.od_loader`)
    - backend/agents/execution_engine/engine.py:3243-3420 (`_build_context_message` — the L12 od/template/example injection branches the generic injector replaces; the block-name strings the opendesign provider must reproduce; note :3264 `is_first_agent`, :3321 `is_build_task_2_plus`, :3404 `spec.id == "prototype-build"`)
    - backend/agents/execution_engine/engine.py:3373,3384 (`get_template_injection_parts` call sites — rewire) and :3503-3513 (`_load_template_example` -> `get_example_html` call at :3508 — rewire)
    - backend/agents/execution_engine/engine.py:845-922 (the prototype_revision parent-run seeding L4 — `sandbox.write(REVISION_FILE_NAME, existing_html)` :845, the parent spec/design/tasks seed; the `assert_owns` ownership check site :862-922 / the L16 ratchet ScopedStore)
    - backend/agents/execution_engine/od_context.py (FULL — :19 the `load_prototype_context` re-export to rewire; :24-76 `load_ppt_od_context` which STAYS — do NOT delete od_context.py wholesale; the boundary call path from ndjson_adapter.py:25 + websocket.py:1042/1059 — Assumption A6, keep the boundary building od_context)
    - backend/agents/authz.py (the `ScopedStore` + `assert_owns` Phase-5 pattern — the ownership check the previous_run provider MUST call BEFORE seeding)
    - backend/agents/capabilities/base.py:75-83 (the `ContextProvider` port — `name` + `async def load(self, ctx) -> dict[str, str]`)
    - 07-PATTERNS.md (the opendesign + previous_run provider sections + the output contract + the heavy-dep note)
    - 07-RESEARCH.md Assumption A6 + the opendesign provider output contract + Pitfall 2 (seed_files: {})
    - backend/tests/agents/test_parent_run_ownership.py (the L16 cross-owner denial contract the previous_run provider must keep green)
  </read_first>
  <behavior>
    - Test (test_context_providers.py): `await OpenDesignProvider().load(ctx)` (with an od_context/template fixture on ctx) returns the `{block-name -> content}` map with the exact L12 block-name keys (e.g. "ACTIVE DESIGN SYSTEM: <id>", "ACTIVE TEMPLATE (SKILL.md): <id>", "TEMPLATE EXAMPLE (example.html): <id>") and the `get_template_injection_parts` blocks; the generic injector composes them in declared order (assert ordering).
    - Test: the relocated `load_prototype_context`/`get_template_injection_parts`/`get_example_html` produce output byte-identical to the pre-relocation functions (snapshot/equality against a fixture) — move-don't-copy parity.
    - Test: `await PreviousRunProvider().load(ctx)` for a same-owner parent run seeds the parent spec/design/tasks; for a CROSS-OWNER parent run, `assert_owns` raises `PermissionError` and the provider lets it PROPAGATE (never swallowed) — INV-8 / L16.
  </behavior>
  <action>
    Create `capabilities/context_providers/__init__.py` + the two provider modules satisfying the `ContextProvider` port (`name` attr + `async def load(self, ctx) -> dict[str,str]`), reaching heavy deps through the boundary `od_context` dict / the handle, keeping `ctx` typed `Any`/`object` (Pitfall 4).

    `opendesign.py` — `class OpenDesignProvider` (`name="opendesign"`): PHYSICALLY move (move-don't-copy, INV-12 — NOT a wrapper around the old location, that is the dual-impl the ledger forbids) the three live functions `load_prototype_context`, `get_template_injection_parts`, `get_example_html` from `agents/prototype/context.py` into this module (their bodies, including the `app.services.od_loader` dependency surface). `load(ctx)` composes the `{block-name -> content}` map the L12 branches inject today (engine.py:3243-3420): the design-system block, the template (SKILL.md) block, the example.html block ([:8000]), plus the `get_template_injection_parts` blocks. If the `od_loader` disk reads cannot be reached without importing `app.*` from the capability, compose the blocks FROM the pre-built boundary `od_context` dict carried on ctx (Assumption A6 — the boundary keeps building od_context and passing it into execute()). Then REWIRE all four importers: `od_context.py:19` (repoint the re-export to the new home — keep `od_context.py` and `load_ppt_od_context` :24-76 intact), `engine.py:3373`, `engine.py:3384` (`get_template_injection_parts`), `engine.py:3508` (`get_example_html`). After relocation `agents/prototype/context.py` is emptied of the 3 fns (the package stays until 07-05 removes it after `pipeline.py` is gone).

    `previous_run.py` — `class PreviousRunProvider` (`name="previous_run"`): seed the parent run's spec/design/tasks into this run's sandbox (revision; lift the engine.py:845-922 seeding behavior — `seed_files.from_run` declared surface, manifests stay `{}` per Pitfall 2). CRITICAL (V4 / INV-8 / L16): call `ScopedStore.assert_owns` on the parent run BEFORE seeding any parent context; let the cross-owner `PermissionError` PROPAGATE out — never swallow it (the L16 ratchet `test_parent_run_ownership.py` must stay green). Reach the store via `ctx.scoped_store` (the existing object-typed field) — do NOT import `app.*`.

    Update `registry.py` `install()` to bind `("context_provider","opendesign")` and `("context_provider","previous_run")`.

    Write `test_context_providers.py` per the behavior block (analog: `test_phase3_compaction.py` direct-call style) including the move-don't-copy byte-parity check and the cross-owner PermissionError propagation.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_providers.py tests/agents/test_parent_run_ownership.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_context_providers.py -x` exits 0 (provider block output + ordering + cross-owner PermissionError propagation)
    - `cd backend && python3.11 -m pytest tests/agents/test_parent_run_ownership.py -x` exits 0 (L16 stays green)
    - `grep -cE "assert_owns" backend/agents/capabilities/context_providers/previous_run.py` >= 1 (ownership check present)
    - `grep -nE "from agents.prototype.context import|import agents.prototype.context" backend/agents/execution_engine/engine.py backend/agents/execution_engine/od_context.py` shows the importers now point at the opendesign provider (no live import of the OLD `agents.prototype.context` 3-fn home except the now-emptied module / the rewired re-export)
    - `grep -cE "def load_ppt_od_context" backend/agents/execution_engine/od_context.py` >= 1 (load_ppt_od_context retained — od_context.py NOT deleted wholesale)
    - `cd backend && lint_imports` exits 0 (providers don't import `agents.execution_engine`; opendesign's `app.services.od_loader` reach is via the boundary od_context dict OR confirmed allowed)
    - `grep -cE "class OpenDesignProvider|class PreviousRunProvider" backend/agents/capabilities/context_providers/opendesign.py backend/agents/capabilities/context_providers/previous_run.py` confirms both classes
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py -x` exits 0 (no regression — the engine still injects via L12 / seeds via L4; relocation is byte-preserving)
  </acceptance_criteria>
  <done>opendesign + previous_run providers exist behind the port; the 3 context.py loaders are physically relocated (no wrapper) and all 4 importers rewired; previous_run keeps assert_owns-before-seed with propagating PermissionError; load_ppt_od_context retained; install() binds both; 0A characterization green (legacy injection/seeding path untouched).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this run -> parent run data (revision) | `previous_run` reads another run's spec/design/tasks — must be ownership-gated |
| capability -> sandbox disk | resolvers/providers read/write per-run disk via the traversal-proof RunSandbox handle |
| capability -> kernel/app | the import-linter port boundary; heavy `od_loader` reads ride the boundary od_context dict, not a direct app.* import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | `PreviousRunProvider` (cross-owner parent-run leak) | mitigate | `assert_owns` (ASVS V4 / INV-8) called BEFORE seeding any parent context; the cross-owner `PermissionError` propagates out of load(), never swallowed. Verified by `test_parent_run_ownership.py` + the assert_owns grep gate. |
| T-07-02-02 | Tampering | resolvers reading the sandbox (path traversal) | mitigate | resolvers read via the traversal-proof `RunSandbox.read`/`path_for` through the handle (ASVS V12); no `open(base/arbitrary)`; the filename:-block format is byte-preserved. |
| T-07-02-03 | Architecture erosion | `OpenDesignProvider` heavy-dep reach | mitigate | the `app.services.od_loader` disk reads ride the boundary `od_context` dict (Assumption A6) OR a confirmed-allowed import path; the capability does NOT import `agents.execution_engine` (ASVS V1). Verified by `lint_imports`. |
| T-07-02-04 | Tampering (move-don't-copy integrity) | relocated context.py loaders | mitigate | physical move (no wrapper around the old location) — the dual-impl the ledger forbids is prevented; the 3 fns have a single home (INV-12). Verified by the importer-rewire grep gate. |

No `high`-severity threat remaining: the only high-risk surface (cross-owner parent-run read) is gated by the propagating `assert_owns` and re-confirmed by the L16 ratchet.
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_deliverable_resolvers.py tests/agents/test_context_providers.py tests/agents/test_parent_run_ownership.py tests/agents/test_sandbox_deliverable.py -x` green
- `cd backend && lint_imports` green (no capability imports `agents.execution_engine`/`app.*`; opendesign heavy-dep reach via boundary or allowed path)
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x` green (legacy path untouched; relocation byte-preserving)
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -v` green (no regression; L1–L13 still present/live)
</verification>

<success_criteria>
- `single_file`/`serialized_sandbox`/`streamed_text`/`ppt` resolvers implemented; resolve by `deliverable.name`/`deliverable.strategy`, no `pipeline_type` (PARITY-02)
- `ppt` resolver owns the carousel-sanitize + artifact-unwrap transform (both behaviors) (PARITY-07)
- `opendesign` provider implemented with the 3 context.py loaders physically relocated + all 4 importers rewired; `load_ppt_od_context` retained (PARITY-03)
- `previous_run` provider seeds parent context ownership-checked via `assert_owns`-before-seed with a propagating `PermissionError` (PARITY-03 / INV-8 / L16)
- install() binds all six; import-linter green; 0A characterization green (legacy path untouched, relocation byte-preserving)
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-02-SUMMARY.md` when done
</output>
