---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/agents/prototype/context.py
  - backend/agents/prototype/pipeline.py
  - backend/agents/prototype/__init__.py
  - backend/tests/agents/test_manifest_parity.py
  - backend/tests/agents/_scripted_model.py
  - backend/tests/agents/test_banned_patterns.py
  - specs/003-workflow-engine-decoupling/migration-ledger.md
autonomous: true
requirements: [PARITY-06, PARITY-08]
must_haves:
  truths:
    - "every L1-L13 ledger grep pattern returns 0 within the kernel (agents/execution_engine/)"
    - "grep -rnE 'if pipeline_type|spec\\.id ==' agents/execution_engine/ returns 0 (INV-1); the positional spec.id == ordered_agents[0].id is rewritten to index == 0 first"
    - "the dead agents/prototype/pipeline.py no longer exists; the emptied agents/prototype/ package is removed; agents/prototype/context.py's live fns were already re-homed in 07-02"
    - "the migration-ledger ratchet test is green with L1-L13 flipped to checked using kernel-scoped patterns; L14/L15/L16 stay checked; D1 stays voided (_handle_revision retained)"
    - "the banned-pattern CI gate hard-fails on reintroduction of a kernel name/id branch (warn-only -> assert 0, kernel-scoped)"
    - "test_manifest_parity.py no longer imports SKIP_PLANNER_FOR_PROTOTYPE; _scripted_model.py no longer monkeypatches the deleted ALWAYS_CLARIFY"
    - "all 5 pipelines stay at deliverable + semantic event parity; L14/L15/L16 gates re-confirmed green; L16 cross-owner denial test passes"
  artifacts:
    - path: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      provides: "L1-L13 rows flipped to done with kernel-scoped grep patterns"
      contains: "L1"
    - path: "backend/tests/agents/test_banned_patterns.py"
      provides: "kernel-scoped INV-1 hard-fail gate"
      contains: "execution_engine"
  key_links:
    - from: "backend/tests/agents/test_migration_ledger.py"
      to: "the kernel-scoped L1-L13 grep patterns"
      via: "ratchet asserts 0 matches for each checked row"
      pattern: "execution_engine"
    - from: "backend/tests/agents/test_banned_patterns.py"
      to: "agents/execution_engine/"
      via: "kernel-scoped assert count == 0 for if pipeline_type / spec.id =="
      pattern: "assert.*== 0|assert.*count"
---

<objective>
The deletion + ratchet-flip plan — pure removal of the now-DEAD L1–L13 leaks (07-04 proved the routed path no longer reaches them; vulture confirmed orphaning). Delete L1–L13 from `agents/execution_engine/engine.py` (move-don't-copy exit gate, INV-12), drop the dead `agents/prototype/pipeline.py`, remove the emptied `agents/prototype/` package (its live `context.py` fns were re-homed into `opendesign` in 07-02), flip migration-ledger rows L1–L13 to checked with KERNEL-SCOPED grep patterns, flip the banned-pattern gate warn-only -> hard-fail (kernel-scoped), update the 3 test-coupling sites whose symbols this plan deletes, and re-verify L14/L15/L16 still closed (incl. the L16 cross-owner denial test). This is the SC-001 core-value proof landing: the kernel knows no workflow by name (INV-1).

CRITICAL: this plan deletes ONLY after 07-04's routed-path parity is green (D-05 strangler exit gate). The L4/L8 revision seeding, L10 readback, and L7 build dispatch are deleted here, AFTER the manifest path (`previous_run`/`seed_files.from_run`, `single_file` resolver, `task_loop`) was proven at parity in 07-04 — never before. Each task leaves the 0A/0C characterization suites green (§31 delete step).

PARITY-08 trap: before flipping the banned-pattern gate to hard-fail, rewrite the POSITIONAL `spec.id == ordered_agents[0].id` (engine.py:3264) to `index == 0` so the kernel grep `spec\.id ==` cleanly hits 0 and the gate does not false-match (Pitfall 1). Kernel-scope every L1–L13 grep so legit NON-kernel refs are retained: `registry.py` `REVISION_BASE_MAP`/`PIPELINE_AGENTS`, `app/core/entitlements.py`, `app/api/runs.py`, `app/api/websocket.py` display strings, tests (SPEC out-of-scope; INV-1 is a kernel property).

Purpose: PARITY-06 (delete L1–L13 + drop pipeline.py + flip ledger) + PARITY-08 (kernel zero name/id branches + banned-pattern hard-fail) + SPEC req 10 (re-verify L14/L15/L16).
Output: the leak-free `engine.py`, the removed `agents/prototype/` package, the flipped ledger, the hard-fail banned-pattern gate, the updated test-coupling sites.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-SPEC.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-CONTEXT.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-RESEARCH.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-PATTERNS.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-04-SUMMARY.md
@specs/003-workflow-engine-decoupling/migration-ledger.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
No NEW runtime symbols. This plan DELETES symbols and FLIPS gates:
- DELETED from `engine.py`: `_PPT_PIPELINE_TYPES`/`_PROTOTYPE_PIPELINE_TYPES`/`REVISION_FILE_NAME` (L1), `_resolve_final_output` (L2/L9), `_sanitize_carousel_deck_html`/`_unwrap_artifact` (L3), the prototype_revision inline seeding/post-fix (L4/L8), `SKIP_PLANNER_FOR_PROTOTYPE` usage (L5), `ALWAYS_CLARIFY` dict (L6), `spec.id == "prototype-build"` dispatch (L7), the `pipeline_type in ("od_prototype","prototype")` readback (L10), the build-loop internals `_run_build_task_loop`/`_write_build_reference_files`/`_count_plan_tasks`/`_extract_task_block`/`_run_validation_fix_loop`/`_load_template_example` (L11), `_build_context_message` per-pipeline branches (L12), `_extract_html_skeleton` (L13)
- DELETED files: `agents/prototype/pipeline.py`; the `agents/prototype/` package (incl. the emptied `context.py` + `__init__.py`) once `pipeline.py` is gone and `context.py` is empty (its 3 live fns were re-homed in 07-02)
- REWRITTEN: `spec.id == ordered_agents[0].id` (engine.py:3264) -> `index == 0` (positional, NOT a name branch — Pitfall 1)
- FLIPPED: ledger rows L1–L13 to checked (kernel-scoped patterns); the banned-pattern gate warn-only -> hard-fail (kernel-scoped)
- UPDATED test-coupling: `test_manifest_parity.py:26` (drop the SKIP_PLANNER_FOR_PROTOTYPE import), `_scripted_model.py:425` (drop the ALWAYS_CLARIFY monkeypatch)
- D1 stays VOIDED (`_handle_revision`/`run_revision` PPT-revision handler retained — NOT touched)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete L1-L13 from the kernel + drop pipeline.py + remove the prototype package + fix the positional spec.id</name>
  <files>backend/agents/execution_engine/engine.py, backend/agents/prototype/pipeline.py, backend/agents/prototype/context.py, backend/agents/prototype/__init__.py</files>
  <read_first>
    - .planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-04-SUMMARY.md (the vulture orphan list — the EXACT helpers confirmed dead/unreferenced on the routed path; delete only these)
    - backend/agents/execution_engine/engine.py (the L1–L13 definitions: L1 :153,:165,:177,:182; L2/L9 `_resolve_final_output` :447-528; L3 `_sanitize_carousel_deck_html` :363-426 + `_unwrap_artifact` :427-446; L4/L8 revision seeding :845-922 + post-fix :917-962; L5 SKIP_PLANNER_FOR_PROTOTYPE usage :1077-1105; L6 ALWAYS_CLARIFY :153,:1105-1126; L7 `spec.id == "prototype-build"` :3404 + the dispatch :1254-1269; L10 readback :1902; L11 `_run_build_task_loop` :2072 / `_write_build_reference_files` :2263 / `_count_plan_tasks` :2319 / `_extract_task_block` :2347 / `_run_validation_fix_loop` :2370 / `_load_template_example` :3503; L12 `_build_context_message` :3243; L13 `_extract_html_skeleton` :3514; the positional `spec.id == ordered_agents[0].id` :3264)
    - specs/003-workflow-engine-decoupling/migration-ledger.md:52-62 (the L1–L13 rows + their exact grep patterns)
    - backend/agents/prototype/pipeline.py (FULL — the dead module: `PROTOTYPE_PIPELINE_TYPES`, `is_prototype_pipeline`, the `prototype_revision` branch, `SKIP_PLANNER_FOR_PROTOTYPE`); confirm only `test_manifest_parity.py:26` + `__init__.py:10` reference it (no live kernel importer)
    - backend/agents/prototype/context.py (confirm the 3 live fns were already moved out in 07-02 — should be emptied of them)
    - backend/agents/execution_engine/engine.py around `_handle_revision` (D1 VOIDED — the `run_revision` PPT-revision handler is LIVE; do NOT delete it; only the prototype_revision build-path seeding L4/L8 goes)
    - 07-PATTERNS.md (the move-don't-copy / deletion-is-DoD shared pattern + the Pitfall 1 spec.id rewrite)
  </read_first>
  <action>
    Delete the L1–L13 leak definitions from `engine.py` (only the helpers the 07-04 vulture pass confirmed orphaned): the L1 constants (`_PPT_PIPELINE_TYPES`/`_PROTOTYPE_PIPELINE_TYPES`/`REVISION_FILE_NAME`), `_resolve_final_output` (L2/L9), `_sanitize_carousel_deck_html` + `_unwrap_artifact` (L3 — both, since BOTH call sites were removed by 07-04), the inline prototype_revision seeding + post-fix blocks (L4/L8 — NOT `_handle_revision`/`run_revision`, D1 voided), the `SKIP_PLANNER_FOR_PROTOTYPE` usage + the `ALWAYS_CLARIFY` dict and its overrides (L5/L6), the `spec.id == "prototype-build"` dispatch + branch (L7), the `pipeline_type in ("od_prototype","prototype")` readback (L10), the build-loop internals `_run_build_task_loop`/`_write_build_reference_files`/`_count_plan_tasks`/`_extract_task_block`/`_run_validation_fix_loop`/`_load_template_example` (L11), the `_build_context_message` per-pipeline branches/method (L12), and `_extract_html_skeleton` (L13).

    Rewrite the POSITIONAL first-agent check `is_first_agent = (len(ordered_agents) == 0 or spec.id == ordered_agents[0].id)` (engine.py:3264) to use `index == 0` (the `index` param is already threaded into the callers) so the kernel grep `spec\.id ==` cleanly returns 0 and the banned-pattern hard-fail (Task 3) does not false-match (Pitfall 1). This is NOT a name branch — it is the workflow-agnostic first-agent positional check; keep its behavior identical.

    Delete `agents/prototype/pipeline.py` (dead; only `test_manifest_parity.py:26` + `__init__.py:10` reference it — those are handled in Task 2/this task's package removal). Confirm `agents/prototype/context.py` is empty of the 3 re-homed fns (07-02); remove the `agents/prototype/` package entirely (the emptied `context.py` + `__init__.py` + `README.md` + the deleted `pipeline.py`) once nothing live imports it. Do NOT touch `od_context.py` (`load_ppt_od_context` stays). Do NOT touch `_handle_revision`/`run_revision` (D1 voided).

    Do NOT delete any NON-kernel reference (registry `REVISION_BASE_MAP`/`PIPELINE_AGENTS`, `app/core/entitlements.py`, `app/api/runs.py`, `app/api/websocket.py` display strings) — INV-1 is a kernel property; these are SPEC out-of-scope.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE '_PROTOTYPE_PIPELINE_TYPES|_PPT_PIPELINE_TYPES' backend/agents/execution_engine/` returns 0 (L1)
    - `grep -rnE '_resolve_final_output' backend/agents/execution_engine/` returns 0 (L2/L9)
    - `grep -rnE '_sanitize_carousel_deck_html|_unwrap_artifact' backend/agents/execution_engine/` returns 0 (L3)
    - `grep -rnE 'SKIP_PLANNER_FOR_PROTOTYPE' backend/agents/execution_engine/` returns 0 (L5)
    - `grep -rnE 'ALWAYS_CLARIFY' backend/agents/execution_engine/` returns 0 (L6)
    - `grep -rnE 'spec\.id == "prototype-build"' backend/agents/execution_engine/` returns 0 (L7)
    - `grep -rnE 'pipeline_type in \("od_prototype", ?"prototype"\)' backend/agents/execution_engine/` returns 0 (L10)
    - `grep -rnE '_run_build_task_loop|_write_build_reference_files|_count_plan_tasks|_extract_task_block|_run_validation_fix_loop|_load_template_example' backend/agents/execution_engine/` returns 0 (L11)
    - `grep -rnE '_build_context_message' backend/agents/execution_engine/` returns 0 (L12)
    - `grep -rnE '_extract_html_skeleton' backend/agents/execution_engine/` returns 0 (L13)
    - `grep -rnE 'spec\.id == ordered_agents\[0\].id' backend/agents/execution_engine/` returns 0 (positional check rewritten to index == 0 — Pitfall 1)
    - `test ! -e backend/agents/prototype/pipeline.py` (pipeline.py deleted) AND `test ! -d backend/agents/prototype` (package removed)
    - `grep -rnE 'def _handle_revision|def run_revision' backend/agents/execution_engine/` returns >= 1 (D1 voided — handler retained)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x` exits 0 (5-pipeline parity after deletion)
  </acceptance_criteria>
  <done>L1–L13 deleted from the kernel; the positional spec.id rewritten to index == 0; pipeline.py + the agents/prototype/ package removed; _handle_revision/run_revision retained (D1 voided); all per-leak kernel greps return 0; 5-pipeline characterization parity green.</done>
</task>

<task type="auto">
  <name>Task 2: Update the 3 test-coupling sites whose symbols were deleted</name>
  <files>backend/tests/agents/test_manifest_parity.py, backend/tests/agents/_scripted_model.py</files>
  <read_first>
    - backend/tests/agents/test_manifest_parity.py:26 (the `from agents.prototype.pipeline import SKIP_PLANNER_FOR_PROTOTYPE` import — the constant + its module are deleted in Task 1; the `is False` premise check — parity trap #1: planner: run is the AUTHORED truth, so drop the import and re-express the premise as a manifest assertion or remove it)
    - backend/tests/agents/_scripted_model.py:425 (the `engine_mod.ALWAYS_CLARIFY = False` monkeypatch — ALWAYS_CLARIFY is deleted by L6; this line raises AttributeError unless updated; replace with the manifest-sourced clarify path or remove the monkeypatch)
    - 07-RESEARCH.md Runtime State Inventory (the test-coupling section) + Wave 0 Gaps (the 3 test-coupling migrations) + parity traps #1/#2
    - backend/agents/workflows/prototype/workflow.yaml (confirm `planner: run` + `clarify.mode: auto` — the authored truth the manifest-parity test should assert instead of the deleted constant)
  </read_first>
  <action>
    Update `test_manifest_parity.py:26`: remove the `from agents.prototype.pipeline import SKIP_PLANNER_FOR_PROTOTYPE` import (the module is gone). The test's premise (SKIP_PLANNER_FOR_PROTOTYPE is False -> planner runs) is now expressed by the authored manifest `planner: run` (parity trap #1) — re-express the premise check as an assertion against the compiled/authored manifest (`compiled.planner != "skip"` for prototype) OR drop the now-redundant premise check if the manifest-parity test already asserts `planner: run`. Keep all other manifest-parity assertions intact (clarify.defaults, trap #2 revision -> custom fall-through).

    Update `_scripted_model.py:425`: remove the `engine_mod.ALWAYS_CLARIFY = False` monkeypatch (the attribute is deleted by L6). The clarify behavior is now sourced from the compiled manifest's `clarify.defaults` (Phase 4) — if any test relied on the monkeypatch to force clarify off, ensure the scripted-model path now exercises the manifest-sourced clarify path (no monkeypatch needed) and the characterization snapshots stay green.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_manifest_parity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE 'SKIP_PLANNER_FOR_PROTOTYPE' backend/tests/agents/test_manifest_parity.py` returns 0 (import dropped)
    - `grep -rnE 'ALWAYS_CLARIFY' backend/tests/agents/_scripted_model.py` returns 0 (monkeypatch removed)
    - `cd backend && python3.11 -m pytest tests/agents/test_manifest_parity.py -x` exits 0 (parity traps #1/#2 still asserted via the authored manifest)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py -x` exits 0 (scripted-model clarify path green without the monkeypatch)
  </acceptance_criteria>
  <done>test_manifest_parity.py no longer imports the deleted SKIP_PLANNER_FOR_PROTOTYPE (premise re-expressed via the authored manifest); _scripted_model.py no longer monkeypatches the deleted ALWAYS_CLARIFY; manifest-parity + prototype characterization green.</done>
</task>

<task type="auto">
  <name>Task 3: Flip ledger rows L1-L13 (kernel-scoped) + banned-pattern hard-fail + re-verify L14/L15/L16</name>
  <files>specs/003-workflow-engine-decoupling/migration-ledger.md, backend/tests/agents/test_banned_patterns.py</files>
  <read_first>
    - specs/003-workflow-engine-decoupling/migration-ledger.md:35-74 (the L1–L13 rows :52-62 with their grep patterns; the L7/L10 hard-fail note :38; L14/L15/L16 already checked; D1 voided; the CHECK rows :35; the ratchet asserts 0 matches for each checked grep row across `backend/ --include=*.py`)
    - backend/tests/agents/test_migration_ledger.py (the ratchet — it runs `grep -rnE <pattern> backend/ --include=*.py` per checked row; the L1–L13 patterns must be KERNEL-SCOPED so legit non-kernel refs survive: registry REVISION_BASE_MAP/PIPELINE_AGENTS, entitlements.py, runs.py, websocket.py, tests)
    - backend/tests/agents/test_banned_patterns.py (the INV-1 / L7-L10 reservation gate — currently warn-only over `app`+`agents` scan roots; flip the kernel name/id branch check to `assert count == 0` scoped to `agents/execution_engine/`; the existing single-file allow-list precedent `_ALLOWED_CREATE_DEEP_AGENT` is the scoping mechanism analog)
    - 07-RESEARCH.md Pitfall 1 (the banned-pattern scope: 6 kernel sites + legit non-kernel + the positional false-match now rewritten in Task 1) + the kernel-scoping discretion (refine regex / --include path / single-file allow-list)
    - backend/tests/agents/test_parent_run_ownership.py (the L16 cross-owner denial CHECK to re-confirm)
    - 07-PATTERNS.md (the PARITY-08 scoping note — kernel-scoped grep, index == 0 rewrite already done in Task 1)
  </read_first>
  <action>
    Flip migration-ledger rows L1–L13 from `☐` to `☑` (migration-ledger.md:52-62) and KERNEL-SCOPE each grep pattern so the ratchet asserts kernel-leak deletion WITHOUT false-matching legit non-kernel refs. For the bare-token leaks that legitimately appear outside the kernel (`prototype_revision` L4/L8 in registry.py REVISION_BASE_MAP/PIPELINE_AGENTS + entitlements.py + runs.py + tests), scope the pattern/path to `agents/execution_engine/` (discretion per CONTEXT: refine the regex to the leak construct, OR add `--include`/path scope, OR use the single-file allow-list precedent) — the gate must assert 0 in the kernel while retaining the non-kernel refs. Keep L14/L15/L16 checked and D1 voided (`_handle_revision` retained). Ensure the L7/L10 rows (now checked) drive the banned-pattern hard-fail per the ledger note (migration-ledger.md:38).

    Flip the banned-pattern gate (`test_banned_patterns.py`) from warn-only to HARD-FAIL for the INV-1 kernel name/id branch check: `assert count == 0` for `if pipeline_type|spec\.id ==` SCOPED to `agents/execution_engine/`. The positional `spec.id == ordered_agents[0].id` was already rewritten to `index == 0` in Task 1, so the kernel grep is clean; retain the legit non-kernel sites (websocket.py display routing, registry.py `pipeline_type == "custom"`) by scoping the assertion to the kernel root (the single-file allow-list precedent in the same file is the mechanism). Keep the INV-13 `create_deep_agent` banned-pattern check intact.

    Re-verify L14/L15/L16 still closed (SPEC req 10): run the L14 pattern (`self\._(od_context|completed_tasks|current_task_block|revision_|gate_agent_ids)`) and L15 pattern (`accumulated_outputs`) -> 0 in `backend/`; run the L16 cross-owner denial test (`test_parent_run_ownership.py`) -> pass. Confirm Task 1's `current_task_block` reclaim did NOT re-add `self._current_task_block` to the engine (L14 ratchet — Pitfall 6).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py tests/agents/test_parent_run_ownership.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` exits 0 (L1–L13 flipped to ☑ with kernel-scoped patterns; L14/L15/L16 stay ☑; D1 stays voided)
    - `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -x` exits 0 (INV-1 kernel gate hard-fails on reintroduction; create_deep_agent check intact)
    - `grep -rnE 'if pipeline_type|spec\.id ==' backend/agents/execution_engine/ --include=*.py` returns 0 (PARITY-08 / INV-1)
    - `grep -rnE 'self\._(od_context|completed_tasks|current_task_block|revision_|gate_agent_ids)' backend/` returns 0 (L14 re-confirmed; Pitfall 6 — no self._current_task_block re-added)
    - `grep -rnE 'accumulated_outputs' backend/` returns 0 (L15 re-confirmed)
    - `cd backend && python3.11 -m pytest tests/agents/test_parent_run_ownership.py -x` exits 0 (L16 cross-owner denial re-confirmed)
    - `grep -nE 'REVISION_BASE_MAP|PIPELINE_AGENTS' backend/agents/registry.py` returns >= 1 (legit non-kernel refs retained — INV-1 is a kernel property)
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -v` exits 0 AND `cd backend && lint_imports` exits 0 (full suite + import-linter green)
  </acceptance_criteria>
  <done>L1–L13 ledger rows flipped to ☑ with kernel-scoped patterns; the banned-pattern gate hard-fails on a kernel name/id branch reintroduction; the kernel INV-1 grep returns 0; L14/L15/L16 re-confirmed green; legit non-kernel refs retained; full suite + import-linter green — SC-001 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel (agents/execution_engine/) -> workflow identity | post-deletion the kernel has ZERO workflow-name/agent-id branches — INV-1 is now enforced by the hard-fail gate |
| ledger ratchet -> codebase | the CI ratchet asserts each checked leak pattern returns 0 in its scope; over-broad patterns would false-fail on legit non-kernel refs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering (regression / re-coupling) | kernel name/id branches | mitigate | the banned-pattern gate flips to HARD-FAIL kernel-scoped `assert count == 0` for `if pipeline_type|spec\.id ==` (INV-1) — any future kernel name/id branch fails CI. Verified by `test_banned_patterns.py`. |
| T-07-05-02 | Information Disclosure | L16 cross-owner parent-run denial | accept-then-verify | deletion of L4/L8 inline seeding does not weaken ownership — the routed path seeds via the 07-02 `previous_run` provider whose `assert_owns` propagates; re-confirmed here by `test_parent_run_ownership.py` (ASVS V4 / INV-8). |
| T-07-05-03 | Tampering (false-green ratchet) | over-broad ledger grep patterns | mitigate | each L1–L13 pattern is KERNEL-SCOPED (path/regex/allow-list) so it asserts kernel-leak deletion without masking via false-match or false-failing on legit non-kernel refs; the ratchet stays non-vacuous. Verified by `test_migration_ledger.py` green WITH the non-kernel refs retained (the registry/entitlements/runs/websocket grep checks). |
| T-07-05-04 | Tampering (runtime integrity) | D1 voided handler | accept | `_handle_revision`/`run_revision` (live PPT-revision handler) is retained — deleting it would break a live path; it is NOT a kernel name-branch leak (D1 voided). Verified by the D1-retained grep gate. |

No `high`-severity threat: the cross-owner surface is re-verified green; the INV-1 regression surface is now hard-failed; no new attack surface is introduced (this is pure deletion + gate-tightening).
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py tests/agents/test_parent_run_ownership.py -x` green (ledger ratchet L1–L13 ☑ kernel-scoped; banned-pattern hard-fail; L16 re-confirmed)
- `grep -rnE 'if pipeline_type|spec\.id ==' backend/agents/execution_engine/ --include=*.py` returns 0 (INV-1 / PARITY-08)
- all 13 per-leak kernel greps (L1–L13) return 0 in `agents/execution_engine/`; the legit non-kernel refs (registry/entitlements/runs/websocket) retained
- L14 + L15 patterns return 0 in `backend/`; D1 `_handle_revision`/`run_revision` retained
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -v` green; `cd backend && lint_imports` green; `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` green (5-pipeline parity holds after deletion)
</verification>

<success_criteria>
- L1–L13 deleted from the kernel; every per-leak kernel grep returns 0; `agents/prototype/pipeline.py` + the `agents/prototype/` package removed (PARITY-06)
- the positional `spec.id == ordered_agents[0].id` rewritten to `index == 0`; the kernel `if pipeline_type`/`spec.id ==` grep returns 0; the banned-pattern gate hard-fails on reintroduction (PARITY-08 / INV-1)
- migration-ledger rows L1–L13 flipped to ☑ with kernel-scoped patterns; the ratchet test green; L14/L15/L16 re-confirmed green (L16 cross-owner denial test passes); D1 stays voided
- `test_manifest_parity.py` + `_scripted_model.py` updated for the deleted symbols
- 5-pipeline deliverable + semantic event parity holds after deletion; full suite + import-linter green — SC-001 proven (the kernel knows no workflow by name)
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-05-SUMMARY.md` when done
</output>
