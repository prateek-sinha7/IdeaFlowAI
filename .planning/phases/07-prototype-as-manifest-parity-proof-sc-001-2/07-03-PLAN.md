---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - backend/agents/capabilities/compaction/__init__.py
  - backend/agents/capabilities/compaction/html_skeleton.py
  - backend/agents/capabilities/registry.py
  - backend/tests/agents/test_phase3_compaction.py
autonomous: true
requirements: [PARITY-04]
must_haves:
  truths:
    - "html_skeleton CompactionStrategy produces output byte-identical to engine._extract_html_skeleton for the same HTML input (verbatim lift, pure function)"
    - "task_loop applies the compaction via registry.resolve('compaction', step.compaction) on build task-2+ (the call site exists in task_loop from 07-01; this plan provides the registered impl)"
    - "the 0C deterministic >=50% reduction CI gate still passes against the capability"
  artifacts:
    - path: "backend/agents/capabilities/compaction/html_skeleton.py"
      provides: "HtmlSkeletonCompaction (name='html_skeleton')"
      contains: "class HtmlSkeletonCompaction"
    - path: "backend/agents/capabilities/registry.py"
      provides: "install() binding for ('compaction','html_skeleton')"
      contains: "html_skeleton"
  key_links:
    - from: "backend/agents/capabilities/strategies/task_loop.py"
      to: "registry.resolve('compaction','html_skeleton').compact()"
      via: "step.compaction on build task-2+"
      pattern: "compaction"
    - from: "backend/tests/agents/test_phase3_compaction.py"
      to: "HtmlSkeletonCompaction"
      via: "re-pointed from engine._extract_html_skeleton (>=50% reduction assertion preserved)"
      pattern: "html_skeleton|HtmlSkeletonCompaction"
---

<objective>
Re-express the Phase 0C build compaction as an `html_skeleton` `CompactionStrategy` capability, behavior-preserving (PARITY-04). Lift `engine._extract_html_skeleton` (a pure function) verbatim into the capability, register it via `install()`, and re-point the 0C `>=50%` reduction gate (`test_phase3_compaction.py`) at the capability — without losing the reduction assertion. The `task_loop` strategy (07-01) already routes task-2+ compaction through `registry.resolve("compaction", step.compaction)`; this plan provides the registered impl behind that call.

This plan builds the CAPABILITY + re-points its gate. The engine is NOT yet wired to route prototype through `task_loop` (07-04) and `_extract_html_skeleton` is NOT yet deleted from the kernel (07-05) — it stays physically present and live as the engine's inline 0C compaction until 07-04 routes through `task_loop`/the capability and 07-05 deletes it. Strangler "wrap" step (§31).

Purpose: PARITY-04 — `html_skeleton` registered as a `CompactionStrategy`, behavior-preserving vs 0C; the >=50% reduction gate preserved against the capability.
Output: `capabilities/compaction/html_skeleton.py`, the install() binding, and the re-pointed `test_phase3_compaction.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-SPEC.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-CONTEXT.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-RESEARCH.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-PATTERNS.md
@.planning/phases/03-token-trim-measured-change-0c/03-CONTEXT.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-01-SUMMARY.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from any source-grounding drift check):
- `HtmlSkeletonCompaction` (`agents/capabilities/compaction/html_skeleton.py`, `name="html_skeleton"`) — the verbatim lift of `_extract_html_skeleton`
- New module init file: `agents/capabilities/compaction/__init__.py`
- `install()` binding for `("compaction","html_skeleton")` in `registry.py`
NOTE: `test_phase3_compaction.py` is RE-POINTED (modified, not new) from `engine._extract_html_skeleton`/`engine._build_context_message` to the `HtmlSkeletonCompaction` capability; the `>=50%` reduction assertion is preserved.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: html_skeleton CompactionStrategy (verbatim lift) + register + re-point the 0C gate</name>
  <files>backend/agents/capabilities/compaction/__init__.py, backend/agents/capabilities/compaction/html_skeleton.py, backend/agents/capabilities/registry.py, backend/tests/agents/test_phase3_compaction.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:3514-3577 (`_extract_html_skeleton` — the FULL pure function: `:root` tokens, routes map, filled/empty section scan, chrome summary, total-size line — lift lines 3526-3577 unchanged; the 0C >=50% gate is calibrated to its EXACT output)
    - backend/tests/agents/test_phase3_compaction.py (FULL — the 7 tests that call `engine._extract_html_skeleton(...)` directly AND `engine._build_context_message(...)`; identify which assert the >=50% reduction and which assert the build-task-2+ injection; re-point the compaction calls at the capability, preserve the reduction assertion)
    - backend/agents/capabilities/registry.py (the `install()` from 07-01/07-02 to extend with the compaction binding; `_KNOWN` already contains `("compaction","html_skeleton")`)
    - backend/agents/capabilities/strategies/task_loop.py (the 07-01 call site `registry.resolve("compaction", step.compaction).compact(...)` on task-2+ — confirm the method name/signature the capability must expose; align the capability's `compact()` to that contract)
    - 07-PATTERNS.md (the html_skeleton compaction section — clean verbatim lift, do not alter the algorithm)
    - .planning/phases/03-token-trim-measured-change-0c/03-CONTEXT.md (the 0C behavior + the >=50% reduction gate this capability must preserve)
  </read_first>
  <behavior>
    - Test (re-pointed test_phase3_compaction.py): for the same HTML fixture, `HtmlSkeletonCompaction().compact(html)` returns output byte-identical to the pre-lift `engine._extract_html_skeleton(html)` (snapshot/equality).
    - Test: the deterministic >=50% reduction assertion holds against the capability output (the existing 0C gate measures the reduction; STATE.md records 96.9% — assert `len(skeleton) <= 0.5 * len(html)` for the calibration fixture, matching the existing gate).
    - Test: the build-task-2+ injection test (currently calling `engine._build_context_message`) is preserved — re-point its compaction expectation at the capability; the message-assembly portion stays asserted against the engine until 07-04 routes injection through the provider/injector (do NOT lose coverage; if a test mixes `_extract_html_skeleton` + `_build_context_message`, split the compaction assertion onto the capability and leave the message-assembly assertion on the engine for now).
  </behavior>
  <action>
    Create `capabilities/compaction/__init__.py` + `capabilities/compaction/html_skeleton.py` with `class HtmlSkeletonCompaction` (`name = "html_skeleton"`). Lift `_extract_html_skeleton` (engine.py:3514-3577) VERBATIM into a `compact(self, html: str) -> str` method (align the method name/signature to the `task_loop` call contract from 07-01 — `registry.resolve("compaction", step.compaction).compact(...)`); the body builds `:root` tokens, the routes map, the filled/empty section scan, the chrome summary, and the total-size line — copy lines 3526-3577 unchanged. The 0C >=50% reduction gate is calibrated to this EXACT output; DO NOT alter the algorithm (PARITY-04 behavior-preserving). The module is pure (no engine state, no `app.*`/`agents.execution_engine` import — Pitfall 4); keep `ctx` typing `Any`/`object` if any context param is added per the port.

    Update `registry.py` `install()` to bind `("compaction","html_skeleton") -> HtmlSkeletonCompaction()`.

    Re-point `test_phase3_compaction.py`: replace the direct `engine._extract_html_skeleton(...)` calls with `HtmlSkeletonCompaction().compact(...)`; PRESERVE the >=50% reduction assertion (do NOT lose it — it is the PARITY-04 gate). For tests that also call `engine._build_context_message(...)`, keep the message-assembly assertion on the engine for now (07-04 routes injection through the generic injector; 07-05 deletes `_build_context_message`) and move only the compaction expectation onto the capability. Add a byte-parity test asserting the capability output equals the (still-present, pre-deletion) `engine._extract_html_skeleton` output for the calibration fixture.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_phase3_compaction.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_phase3_compaction.py -x` exits 0 (re-pointed; >=50% reduction assertion preserved — PARITY-04 gate)
    - `cd backend && python3.11 -m pytest tests/agents/test_capability_resolution.py -x` exits 0 (resolve("compaction","html_skeleton") now returns the impl)
    - `grep -nE "from agents.execution_engine|import agents.execution_engine|from app\.|import app\." backend/agents/capabilities/compaction/html_skeleton.py` returns 0 matches (pure, import-pure — Pitfall 4)
    - `grep -cE "class HtmlSkeletonCompaction" backend/agents/capabilities/compaction/html_skeleton.py` == 1 with `name = "html_skeleton"`
    - `cd backend && lint_imports` exits 0
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -x` exits 0 (no regression — `_extract_html_skeleton` still present/live in the engine; nothing routed/deleted)
  </acceptance_criteria>
  <done>HtmlSkeletonCompaction is a verbatim lift of `_extract_html_skeleton` behind the compaction capability, registered via install(), and the 0C >=50% reduction gate is re-pointed at it and green; the engine's inline `_extract_html_skeleton` stays present/live (deleted in 07-05); 0A/ledger/banned-pattern still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability -> kernel/app | the import-linter port boundary; the compaction is a pure function with no kernel/app dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Architecture erosion | `HtmlSkeletonCompaction` | mitigate | pure function, no `app.*`/`agents.execution_engine` import (ASVS V1); import-linter stays green. Verified by `lint_imports` + the import grep gate. |

No new attack surface — behavior-preserving relocation of a pure function; the import-linter contract holds the boundary, and the >=50% reduction gate proves byte-behavior parity. No parent-run data, no agent loop, no dynamic resolution is introduced by this plan.
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_phase3_compaction.py tests/agents/test_capability_resolution.py -x` green (>=50% reduction preserved; resolve returns the impl)
- `cd backend && lint_imports` green
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -x` green (engine `_extract_html_skeleton` still present/live)
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -v` green (no regression)
</verification>

<success_criteria>
- `html_skeleton` `CompactionStrategy` implemented as a verbatim lift of `_extract_html_skeleton`; registered via install() (PARITY-04)
- the 0C `>=50%` deterministic reduction gate is re-pointed at the capability and still passes
- capability is pure/import-pure; import-linter green
- 0A characterization + migration-ledger + banned-pattern still green (engine `_extract_html_skeleton` present/live until 07-05)
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-03-SUMMARY.md` when done
</output>
