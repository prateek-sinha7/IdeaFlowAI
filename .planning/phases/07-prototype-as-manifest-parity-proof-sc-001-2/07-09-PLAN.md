---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 09
type: execute
wave: 3
depends_on: [07-08]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/agents/capabilities/context_providers/opendesign.py
  - backend/agents/capabilities/strategies/task_loop.py
  - backend/agents/capabilities/deliverables/ppt.py
  - backend/tests/agents/test_context_message_oracle.py
  - backend/tests/agents/test_context_providers.py
  - backend/tests/agents/characterization/golden/prototype.events.json
  - backend/tests/agents/characterization/golden/od_prototype.events.json
  - backend/tests/agents/characterization/golden/prototype_revision.events.json
  - backend/tests/agents/characterization/golden/od_ppt.events.json
  - backend/tests/agents/characterization/golden/app_builder.events.json
autonomous: true
gap_closure: true
requirements: [PARITY-03, PARITY-07, PARITY-09]
must_haves:
  truths:
    - "A prototype/od_prototype build prompt again carries the `=== TEMPLATE COMPLIANCE ===` block byte-exact on every build task (CR-01)"
    - "The Design-System and Template blocks are emitted only when the consuming agent declares the matching inject (`design_system` / `template` in `injects`) — the per-block gate is restored (CR-02), and example.html still never reaches tools:[] planning agents (07-06 CR-02 not re-opened)"
    - "Template injection parts are emitted RAW (no double `=== … ===` envelope) — the nested outer wrapper is gone (CR-04)"
    - "The injector END markers are BARE (`=== END ACTIVE DESIGN SYSTEM ===`, no `: {id}` suffix) (WR-03)"
    - "The build-skeleton wrapper text/position/instruction match the legacy byte-exact: standalone block after consumed outputs, `=== CURRENT PROTOTYPE (skeleton — call read_file('prototype.html') for full content before editing) ===` … `=== END CURRENT PROTOTYPE ===`, with the `[Error:` sentinel suppression (WR-01)"
    - "The ppt resolver applies sanitize→unwrap in the LEGACY order, confirmed with an <artifact>-wrapped carousel input (WR-02)"
    - "On a fix-loop iteration the typed prototype-build artifact is re-persisted when the on-disk HTML changed, so prototype-validate reads the fixed HTML (WR-05)"
    - "Every restoration is asserted byte-equal against the ORACLE (07-08), and the 5 characterization goldens are regenerated to match the corrected (oracle-equal) routed bytes"
  artifacts:
    - path: "backend/agents/execution_engine/engine.py"
      provides: "TEMPLATE COMPLIANCE re-emission; bare END markers; raw injection-part emission; legacy build-skeleton wrapper"
      contains: "TEMPLATE COMPLIANCE"
    - path: "backend/agents/capabilities/context_providers/opendesign.py"
      provides: "per-injects per-block gate restored; pre-wrapped injection parts marked raw"
      contains: "injects"
  key_links:
    - from: "backend/agents/execution_engine/engine.py (_compose_context_message)"
      to: "tests/agents/characterization/oracle/legacy_context_message.py"
      via: "the routed build-agent context_message equals the oracle bytes"
      pattern: "TEMPLATE COMPLIANCE"
    - from: "backend/agents/capabilities/strategies/task_loop.py"
      to: "runner.persist_task_html after the fix-loop"
      via: "re-persist typed artifact when on-disk HTML changed"
      pattern: "persist_task_html"
---

<objective>
Cluster C, part 2 — the seven per-finding restorations, each asserted byte-equal against
the 07-08 ORACLE (never against the drifted golden). Restore the pre-Phase-7
`context_message` contract: CR-01 (re-emit `=== TEMPLATE COMPLIANCE ===` on every build
task), CR-02 (restore the per-`injects` per-block gate without re-leaking example.html to
tools:[] planning agents), CR-04 (emit injection parts RAW, no double envelope), WR-03
(bare END markers, strip the `: {id}` suffix), WR-01 (restore the build-skeleton wrapper
text/position/`read_file` instruction + the `[Error:` suppression). Confirm-then-fix:
WR-02 (ppt sanitize/unwrap order against an `<artifact>`-wrapped carousel), WR-05
(re-persist the typed build artifact after a fix iteration). Then regenerate the five
characterization goldens to the corrected (oracle-equal) bytes and turn the 07-08
divergence xfail into a hard pass.

Purpose: these are the parity findings the 07-06 adjudication wrongly closed by pinning the
drifted prompt. The fixes are byte-restorations of pre-Phase-7 behavior (INV-3 / PARITY-03 /
PARITY-09), measured against the independent oracle so "pinned == correct" finally holds.

Output: a byte-correct routed `context_message`; a legacy-order ppt resolver; a fix-loop
that re-persists the typed artifact; oracle-equal goldens.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-REVIEW-DEEP.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-REVIEW.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-VERIFICATION.md
@backend/tests/agents/characterization/oracle/legacy_context_message.py
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore the context_message byte contract (CR-01, CR-02, CR-04, WR-03) against the oracle</name>
  <read_first>
    - backend/tests/agents/characterization/oracle/legacy_context_message.py (the ground truth — every restoration asserts against build_oracle_message)
    - backend/agents/execution_engine/engine.py (`_compose_context_message` :2851-2980 — the routed injector: the provider loop :2937-2951; the `=== {block_name} ===\n…\n=== END {block_name} ===` wrap at :2951 carrying the `: {id}` END suffix (WR-03); the missing TEMPLATE COMPLIANCE block (CR-01); the `injects` presence check at :2925 but NO per-block `injects` gate; the CURRENT TASK block :2961-2978)
    - backend/agents/capabilities/context_providers/opendesign.py (FULL — block keys `ACTIVE DESIGN SYSTEM: {ds_id}` / `ACTIVE TEMPLATE (SKILL.md): {id}` / `TEMPLATE EXAMPLE (example.html): {id}` / `TEMPLATE INJECTION PART {i}: {id}`; the builder/task gates at :57-60; the injection-part emission :113-124)
    - Legacy contract: `git show acd1636:backend/agents/execution_engine/engine.py` lines 3306-3470 (the per-injects gate, the raw `parts.append(part)`, the bare END markers, the unconditional TEMPLATE COMPLIANCE block)
  </read_first>
  <action>
    Make the routed build-agent `context_message` byte-equal to the oracle. CR-02 (per-injects
    gate): pass the consuming agent's declared `injects` to `OpenDesignProvider.load` (thread it
    onto the ExecutionContext alongside `current_spec_tools` — the same D-03 per-run-state-on-ctx
    mechanism at engine.py:2935; do NOT add a spec.id/pipeline_type branch). In `opendesign.py`,
    gate the DS block on `"design_system" in injects`, the template/example/injection-parts on
    `"template" in injects` — restoring the legacy per-block gate; KEEP the `is_builder` example
    gate exactly as-is (tools:[] planning agents still receive NO example — do NOT re-open
    07-06 CR-02). CR-04 (raw injection parts): the injection-part strings arrive PRE-WRAPPED
    (`=== TEMPLATE SEED … ===` etc.); mark these provider blocks as pre-wrapped (e.g. a sentinel
    key prefix, or a separate `raw_blocks` return channel) so the injector at engine.py:2951
    appends them RAW (`parts.append(part)`) instead of re-wrapping as `=== {block_name} ===`.
    WR-03 (bare END markers): change the injector wrap so the END line is `=== END {base_name} ===`
    with NO `: {id}` suffix — i.e. emit `=== {block_name} ===\n{content}\n=== END {base} ===`
    where `{base}` strips the `: {id}` from `{block_name}` (legacy emitted bare `=== END ACTIVE
    DESIGN SYSTEM ===`). CR-01 (TEMPLATE COMPLIANCE): re-emit the byte-exact block on EVERY
    build task — append, in the CURRENT-TASK/build region of `_compose_context_message` (or via
    a build-task-keyed block from the provider), the legacy three-line block
    `=== TEMPLATE COMPLIANCE ===\nTemplate: {template_id} — use ONLY its CSS classes from the
    TEMPLATE SEED\nDesign System: {ds_id} — use ONLY :root variables, never raw hex colors\n
    === END TEMPLATE COMPLIANCE ===` (read template_id/ds_id from od_context), gated on the
    build-agent signal (`ectx.build_task_number` set + the build tool set), NOT on a workflow
    name (INV-1). Update `test_context_providers.py` block-map assertions where the block-name /
    gate expectations changed. In `test_context_message_oracle.py`, turn the divergence xfail
    into a HARD assertion: the build-agent `context_message` from `_drive("prototype")` ==
    `build_oracle_message("prototype-build", task=...)` for task 1 AND task 2 (byte-equal).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_message_oracle.py tests/agents/test_context_providers.py -q</automated>
  </verify>
  <done>The routed build-agent context_message equals the oracle bytes for task 1 and task 2; TEMPLATE COMPLIANCE is present on every build task; DS/template emit only under the matching inject; example.html still never reaches tools:[] agents; injection parts are raw; END markers are bare; the oracle test's divergence assertion is now a hard pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restore the build-skeleton wrapper byte-exact (WR-01) against the oracle</name>
  <read_first>
    - backend/tests/agents/characterization/oracle/legacy_context_message.py (the task-2+ skeleton block the oracle pins)
    - backend/agents/capabilities/strategies/task_loop.py (:239-249 — the current NESTED `=== CURRENT PROTOTYPE SKELETON ===` injected INTO `current_task_block`, no close marker, no `read_file` directive, reads disk unconditionally)
    - backend/agents/execution_engine/engine.py (`_compose_context_message` CURRENT TASK / build region :2960-2978 — where the legacy standalone skeleton block belonged, AFTER consumed outputs)
    - Legacy contract: `git show acd1636:backend/agents/execution_engine/engine.py` lines 3424-3455 — the standalone `=== CURRENT PROTOTYPE (skeleton — call read_file('prototype.html') for full content before editing) ===` … `=== END CURRENT PROTOTYPE ===` block, positioned after the CURRENT TASK block, gated on `current_html and not current_html.startswith("[Error:")`
  </read_first>
  <action>
    Restore the skeleton block to its legacy text/position/instruction byte-exact. The legacy
    block is a STANDALONE block (not nested inside `=== CURRENT TASK ===`), emitted after the
    consumed outputs / CURRENT TASK region, with the full wrapper
    `=== CURRENT PROTOTYPE (skeleton — call read_file('prototype.html') for full content before
    editing) ===\n{skeleton}\n=== END CURRENT PROTOTYPE ===`, and is SUPPRESSED when the current
    HTML is empty OR starts with the `[Error:` sentinel (legacy read the typed graph, not the raw
    disk). Decide the cleanest seam given the strategy-owns-compaction design (Claude's discretion,
    but the BYTES + POSITION must match the oracle): either (a) have `task_loop` stop nesting the
    skeleton into `current_task_block` and instead set a dedicated `ectx` scratch
    (e.g. `ectx.current_prototype_skeleton`) that `_compose_context_message` emits as the legacy
    standalone block in the legacy position with the legacy wrapper + the `[Error:` suppression; or
    (b) keep the skeleton in the strategy but emit the FULL legacy standalone wrapper (with the
    `read_file` instruction + close marker + `[Error:` guard) at the legacy position. Source the
    skeleton from the typed-graph read (`runner.latest_typed_content("prototype-plan")`/the build
    HTML) consistent with the legacy `_latest_typed_content(... "prototype-build")` + the `[Error:`
    guard, NOT an unconditional raw disk read. Assert against
    `build_oracle_message("prototype-build", task="2")` byte-equal (the task-1 case has no prior
    HTML → no skeleton block, also asserted).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_message_oracle.py -q</automated>
  </verify>
  <done>The task-2+ routed context_message carries the legacy standalone `=== CURRENT PROTOTYPE (skeleton — call read_file('prototype.html')…) ===` block with its close marker and `[Error:` suppression, byte-equal to the oracle; task 1 has no skeleton block; the nested `=== CURRENT PROTOTYPE SKELETON ===` form is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm-then-fix ppt order (WR-02) + typed re-persist after fix (WR-05); regenerate the 5 goldens to oracle-equal bytes</name>
  <read_first>
    - backend/agents/capabilities/deliverables/ppt.py (`return unwrap_artifact(sanitize_carousel_deck_html(last_streamed))` — current NEW order)
    - backend/agents/capabilities/deliverables/_artifact.py (`sanitize_carousel_deck_html` + `unwrap_artifact` semantics)
    - Legacy order: `git show acd1636:backend/agents/execution_engine/engine.py` :504/:528 then :1393 — legacy applied `sanitize(unwrap(x))`
    - backend/agents/capabilities/strategies/task_loop.py (:266-294 — `run_agent` then `persist_task_html(task_num)` BEFORE the fix-loop; the fix-loop edits prototype.html on disk but the typed artifact is never re-persisted)
    - backend/agents/execution_engine/kernel_services.py (`persist_task_html` :258-276 — the typed dual-write; `run_validation_fix_loop` :208-255)
    - Legacy: `git show acd1636:backend/agents/execution_engine/engine.py` :2243-2255 — re-wrote the typed artifact when `fixed_html != task_html`
    - backend/tests/agents/test_characterization_*.py (the 5 golden assertions; `SNAPSHOT_UPDATE` regeneration)
  </read_first>
  <action>
    WR-02 (ppt): build the documented repro — an `<artifact>`-wrapped carousel deck input — and
    compare `unwrap_artifact(sanitize_carousel_deck_html(x))` (NEW) vs `sanitize_carousel_deck_html
    (unwrap_artifact(x))` (LEGACY). If they differ on the wrapped input, restore the LEGACY order
    `sanitize(unwrap(x))` in `ppt.py`; if identical on all relevant inputs, keep NEW and add a
    test asserting equivalence (document the finding either way). Also dedup the mid-stream
    sanitize note vs the resolver per WR-02 (the resolver owns the sanitize; confirm engine.py:1772
    region is not double-applying). WR-05 (typed re-persist): after `run_validation_fix_loop`
    completes in `task_loop.run`, re-persist the typed prototype-build artifact WHEN the on-disk
    HTML changed during the fix — read the post-fix `prototype.html` and, if it differs from the
    pre-fix content the earlier `persist_task_html` wrote, call `runner.persist_task_html(task_num,
    agent_id=agent_id)` again (or have `run_validation_fix_loop` itself re-persist on a successful
    edit). The consumer `prototype-validate` reads the typed graph, so it must see the FIXED HTML.
    Add a fix-iteration characterization/unit case asserting the typed artifact reflects the
    post-fix HTML. Finally, with the engine now byte-correct (Tasks 1+2) and these two fixes in,
    regenerate the five characterization goldens via `SNAPSHOT_UPDATE=1` and add an assertion (in
    `test_context_message_oracle.py` or the prototype characterization test) that the regenerated
    build-agent `context_message` in `golden/prototype.events.json` / `golden/od_prototype.events.json`
    equals the oracle bytes — closing the loop so the goldens now pin the ORACLE (correct), not the
    drift. Review the golden diff and confirm the only changes are the expected TEMPLATE COMPLIANCE /
    bare-END / raw-injection-part / skeleton-wrapper restorations.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_context_message_oracle.py -q</automated>
  </verify>
  <done>The ppt resolver applies the legacy-equivalent sanitize/unwrap order (confirmed with an <artifact>-wrapped carousel); the typed build artifact is re-persisted after a fix iteration so prototype-validate reads the fixed HTML; the 5 goldens are regenerated and the prototype/od_prototype build-agent context_message in the goldens equals the oracle bytes (the loop is closed).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- (engine.py) re-emitted `=== TEMPLATE COMPLIANCE ===` block; bare END markers; raw injection-part emission; legacy standalone build-skeleton block (+ optional new `ectx.current_prototype_skeleton` scratch field).
- (opendesign.py) per-`injects` per-block gate; a pre-wrapped/raw-block marking channel for injection parts.
- (ppt.py) legacy-order sanitize/unwrap (if WR-02 confirmed divergent).
- (task_loop.py) post-fix typed re-persist call.
- regenerated `golden/*.events.json` (×5) pinned to ORACLE bytes; oracle hard-equality assertions in `test_context_message_oracle.py`.
</artifacts_this_phase_produces>

<threat_model>
Internal parity-restoration refactor; no new external attack surface, no package installs.
The OD blocks composed into prompts are model-facing context only (no code execution, no
network/secrets). The CR-02 gate-restoration risk is RE-LEAKING example.html to tools:[]
planning agents (re-opening 07-06 CR-02); explicitly mitigated by keeping the `is_builder`
example gate and asserting tools:[] agents receive no example. The other risk is regenerating
goldens to the WRONG (still-drifted) bytes; mitigated by gating golden regeneration behind the
oracle hard-equality assertion (the build-agent context_message in the regenerated golden MUST
equal the oracle).
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Information disclosure | example.html leaking to planning agents | mitigate | keep the is_builder example gate; assert tools:[] agents get no example (test_context_providers) |
| T-07-09-02 | Tampering | regenerating goldens to drifted bytes | mitigate | golden regen gated on oracle hard-equality (build-agent context_message == oracle) |
| T-07-09-03 | Tampering | INV-1 regression (a name/id branch sneaks into the gate) | mitigate | gate on declared injects + opaque tool set + build_task_number only; banned-pattern gate stays green (re-run in 07-11 verify) |
</threat_model>

<verification>
- `python3.11 -m pytest tests/agents/test_context_message_oracle.py -q` — build-agent context_message == oracle (task 1 + task 2), hard pass.
- `python3.11 -m pytest tests/agents/test_context_providers.py -q` — per-injects gate + example-still-gated assertions pass.
- `python3.11 -m pytest tests/agents/test_characterization_*.py -q` — 5 pipelines green; goldens regenerated and oracle-equal.
- `grep -nE "if pipeline_type|spec\.id ==" agents/execution_engine/engine.py agents/capabilities/context_providers/opendesign.py` returns 0 (INV-1 preserved).
</verification>

<success_criteria>
The routed build-agent context_message is byte-equal to the pre-Phase-7 oracle (TEMPLATE
COMPLIANCE restored, per-injects gate restored, injection parts raw, END markers bare,
skeleton wrapper restored); the ppt order matches legacy; the typed artifact is re-persisted
after a fix; the 5 goldens are regenerated to oracle ground truth; INV-1 stays clean.
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-09-SUMMARY.md` when done
</output>
