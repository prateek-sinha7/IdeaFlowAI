---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 06
type: execute
wave: 6
depends_on: []
gap_closure: true
files_modified:
  - backend/agents/capabilities/context_providers/opendesign.py
  - backend/agents/execution_engine/engine.py
  - backend/tests/agents/test_context_providers.py
  - backend/tests/agents/characterization/_normalize.py
autonomous: true
requirements: [PARITY-03, PARITY-09]
must_haves:
  truths:
    - "the opendesign provider produces context_message blocks byte-identical to the legacy L12 injection branches (git fb55699): the DS block content is the instruction preamble + ds_body (CR-01); example.html is injected ONLY for builder agents whose tool set intersects {prototype_emit_only, prototype} (CR-02); DS body + template body + example + non-seed injection parts are SUPPRESSED on build tasks 2+ (CR-03)"
    - "all 5 pipelines (prototype/od_prototype/prototype_revision/ppt/code-gen) are at deliverable byte parity AND context_message byte parity vs the post-0C baseline — the context_message is no longer stripped blind by the characterization normalizer; a dedicated context_message parity assertion exists and is green"
  artifacts:
    - path: "backend/agents/capabilities/context_providers/opendesign.py"
      provides: "byte-identical L12 DS/template/example/injection-parts blocks: preamble restored, builder-gated example, task-2+ suppression"
      contains: "Apply these tokens to ALL colors, fonts, and spacing"
    - path: "backend/tests/agents/test_context_providers.py"
      provides: "assertions pinning the preamble (CR-01), the tools-gated example (CR-02), and the task-2+ suppression (CR-03) — the parity net is no longer blind"
      contains: "Apply these tokens"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "ectx.current_spec_tools threaded to the provider before each provider.load(ectx) in _compose_context_message (the D-03 per-run-state-on-ExecutionContext pattern; no spec.id branch)"
      contains: "current_spec_tools"
  key_links:
    - from: "backend/agents/execution_engine/engine.py _compose_context_message"
      to: "ectx.current_spec_tools"
      via: "set the consuming agent's tool set on ectx before resolve(context_provider).load(ectx)"
      pattern: "current_spec_tools"
    - from: "backend/agents/capabilities/context_providers/opendesign.py"
      to: "the legacy L12 byte contract (git fb55699 lines ~3321-3386)"
      via: "preamble + builder gate (ctx.current_spec_tools) + task-2+ suppression (ctx.build_task_number not in ('','1'))"
      pattern: "prototype_emit_only|Apply these tokens"
    - from: "backend/tests/agents/test_context_providers.py"
      to: "the composed context_message block structure"
      via: "direct assertions on preamble presence, example absence for tools:[] specs, DS/template/example absence on build task 2"
      pattern: "Apply these tokens|TEMPLATE EXAMPLE"
---

<objective>
Gap-closure for Phase 7. Phase 7 (07-01…07-05) shipped the SC-001 structural proof cleanly:
the kernel knows no workflow by name, L1–L13 are deleted, the ledger ratchet + banned-pattern
gate are locked. Verification (07-VERIFICATION.md) scored 7/9. The two FAILED truths both trace
to ONE file: `backend/agents/capabilities/context_providers/opendesign.py` is NOT a faithful
lift of the legacy L12 `_build_context_message` injection branches (git ref `fb55699`). Three
independent behavioral regressions plus a structurally-blind test net mean the `opendesign`
provider emits a context_message that diverges from the legacy byte contract (INV-3) on every
prototype/od_prototype build run — and the characterization parity net cannot see it because
`context_message` is in `_VOLATILE_STRIP_KEYS`.

This plan closes BOTH gaps with parity-only fixes (NO new behavior — restore the legacy bytes):
  - CR-01: restore the DS instruction preamble (opendesign.py:58).
  - CR-02: restore the builder gate on example.html + the tool-gated injection-parts branch —
    thread the consuming agent's tool set to the provider via `ectx.current_spec_tools`
    (the D-03 per-run-state-on-ExecutionContext pattern, the same dynamic-attr mechanism the
    engine already uses for `ectx.compiled_context_providers` at engine.py:1042).
  - CR-03: restore the build-task-2+ suppression by reading `ectx.build_task_number` (already a
    real ExecutionContext field) inside the provider — gated on the BUILDER tool set, NOT on
    `spec.id == "prototype-build"` (INV-1: the kernel must stay name/id-free; the build agent is
    identified by its tools exactly as the legacy injection-parts branch already was).
  - Test fix: correct the locked-in `test_context_providers.py:114` assertion and add the three
    missing parity assertions (preamble present; example absent for tools:[] specs; DS/template/
    example absent on build task 2).
  - Blind-spot closure: add a dedicated context_message parity assertion separate from the event
    normalizer, then remove `context_message` from `_VOLATILE_STRIP_KEYS` (with the
    characterization snapshots regenerated parity-stable, not merely made green) so context
    injection regressions can never again pass for the wrong reason.

CRITICAL — this is byte parity against the legacy L12 contract (INV-3). The AUTHORITATIVE source
for the exact bytes is `git show fb55699:backend/agents/execution_engine/engine.py` (lines
~3321-3386), NOT the abbreviated "missing" lines in 07-VERIFICATION.md. The git source uses the
preamble `"Apply these tokens to ALL colors, fonts, and spacing. Map to :root variables: --bg,
--fg, --accent, --surface, --border, --muted.\n"` (WITH spaces after each comma); copy it
verbatim. The provider remains the SINGLE home (no wrapper, no dual-impl — INV-12). 07-01…07-05
are DONE and shipped — this plan only supplements opendesign.py + the provider call site + the
two test files. Do NOT modify, replan, or re-create 07-01…07-05.

Purpose: PARITY-03 (opendesign context provider byte parity vs L12) + PARITY-09 (all-5-pipeline
context_message byte parity vs the post-0C baseline — the INV-3 full scope the normalizer was
blind to).
Output: the byte-faithful `opendesign` provider, the `ectx.current_spec_tools` thread in
`_compose_context_message`, the corrected + extended `test_context_providers.py`, the dedicated
context_message parity assertion, and the de-blinded characterization normalizer.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-VERIFICATION.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-SPEC.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-CONTEXT.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-RESEARCH.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-PATTERNS.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan adds a small amount of per-run-state plumbing + test coverage. The NEW symbols /
signatures this plan creates or changes:

- **engine.py `_compose_context_message`** — sets `ectx.current_spec_tools` (a `set[str]` of the
  consuming agent's `spec.tools`) on the ExecutionContext BEFORE the `provider.load(ectx)` loop
  (engine.py:2926-2942). This is a DYNAMIC ATTRIBUTE on `ectx` (the exact mechanism already used
  for `ectx.compiled_context_providers` at engine.py:1042) — NOT a new declared dataclass field.
  No new declared field is required because `build_task_number` already exists on
  ExecutionContext (context.py:137). (Discretion: if the executor prefers a declared
  `current_spec_tools: set[str] = field(default_factory=set)` field on `ExecutionContext` for
  type-cleanliness, that is acceptable — it mirrors the existing field pattern; but a dynamic
  attr matches the `compiled_context_providers` precedent and avoids touching context.py.)
- **opendesign.py `OpenDesignProvider.load(ctx)`** — gains two ctx reads (NO signature change —
  the port stays `async load(self, ctx)`): `ctx.current_spec_tools` (the builder gate, CR-02) and
  `ctx.build_task_number` (the task-2+ suppression, CR-03). Restores the DS preamble (CR-01) and
  the tool-gated injection-parts branch (seed-only on task 2+ for `prototype_emit_only`).
- **test_context_providers.py** — NEW/CHANGED test functions: the existing
  `test_opendesign_block_name_keys_and_content` assertion at line 114 is corrected to assert the
  preamble; NEW `test_opendesign_ds_block_has_preamble` (CR-01), `test_opendesign_example_gated_on_builder_tools`
  (CR-02 — absent for tools:[]), `test_opendesign_build_task_2_plus_suppresses_ds_template_example`
  (CR-03). (Exact function names are the executor's discretion; the assertions are pinned in the
  acceptance criteria.)
- **A dedicated context_message parity assertion** — either a new test function in
  test_context_providers.py asserting the exact composed block structure/ordering for a scripted
  prototype build-task-1 vs build-task-2 invocation, OR a new context_message reference snapshot
  consumed by a characterization-adjacent test. (Mechanism is the executor's discretion; the
  parity guarantee is pinned in the acceptance criteria.)
- **_normalize.py** — `context_message` REMOVED from `_VOLATILE_STRIP_KEYS` (line 109) once the
  dedicated parity assertion pins it AND the characterization golden snapshots are regenerated
  parity-stable (a single deterministic regeneration, not a per-run rewrite). If removing it
  proves to reintroduce genuine volatility (e.g. an unaliased run id leaking into the echo),
  keep it stripped from the normalizer BUT ensure the dedicated context_message parity assertion
  (Task 3) is the non-volatile pin — the blind spot must be closed one way or the other.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore the L12 byte contract in opendesign.py (CR-01 preamble, CR-02 builder gate, CR-03 task-2+ suppression) + thread ectx.current_spec_tools</name>
  <files>backend/agents/capabilities/context_providers/opendesign.py, backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/capabilities/context_providers/opendesign.py (FULL — the current provider: line 58 emits bare ds_body (CR-01); lines 67-74 inject example whenever runner.template_example returns content, no builder gate (CR-02); lines 77-79 inject ALL template_injection_parts unconditionally, no tool gate / no task-2+ seed-only branch; no build_task_number read at all (CR-03))
    - LEGACY CONTRACT (AUTHORITATIVE for bytes): run `git show fb55699:backend/agents/execution_engine/engine.py` and read lines ~3321-3386 — the exact L12 injection branches: the `is_build_task_2_plus = (spec.id == "prototype-build" and task_num_str not in ("", "1"))` gate; the DS block preamble string; the `_is_builder = bool(set(getattr(spec, "tools", []) or []) & {"prototype_emit_only", "prototype"})` example gate; the injection-parts branch (`prototype_emit_only` → seed-only on task 2+, all parts on task 1; `prototype` → all parts always)
    - backend/agents/execution_engine/engine.py:2851-2971 (`_compose_context_message` — the SOLE caller of provider.load; `spec` (the consuming agent) and `ectx` are both in scope; the provider loop is lines 2926-2942; `ectx.compiled_context_providers` is set as a DYNAMIC ATTR at engine.py:1042 — the precedent for setting `ectx.current_spec_tools` the same way)
    - backend/agents/execution_engine/context.py:137 (`build_task_number: str = ""` — already a real ExecutionContext field; the task_loop strategy sets it per build task; "" / "1" for non-build agents and build task 1)
    - backend/agents/execution_engine/kernel_services.py:122-135 (`template_example` + `template_injection_parts` — the runner-handle reads the provider reaches OD disk through; kernel→ports import direction honored, no app.* from the capability)
    - backend/agents/prompts/prototype-build/AGENT.md (tools: [prototype_emit_only], injects: [template, design_system, craft]) + prototype-specify/AGENT.md + prototype-plan/AGENT.md (tools: [], injects: [template, design_system]) — the builder-vs-planner tool sets the gate keys off
    - .planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-PATTERNS.md (the D-03 per-run-state-on-ExecutionContext pattern + the opendesign block-name/ordering map)
  </read_first>
  <behavior>
    - CR-01 (preamble): given od_context with ds_body="DS TOKENS", ds_id="midnight", is_design_system_required=None, the "ACTIVE DESIGN SYSTEM: midnight" block content equals the preamble string "Apply these tokens to ALL colors, fonts, and spacing. Map to :root variables: --bg, --fg, --accent, --surface, --border, --muted.\n" immediately followed by "DS TOKENS" (preamble + ds_body, byte-identical to git fb55699).
    - CR-02 (builder gate): given a template_body present + runner.template_example returning "<html>example</html>", when ctx.current_spec_tools == {"prototype_emit_only"} (or contains "prototype") the "TEMPLATE EXAMPLE (example.html): <id>" block is PRESENT; when ctx.current_spec_tools == set() (a tools:[] planning agent like prototype-specify) the "TEMPLATE EXAMPLE" block is ABSENT.
    - CR-02 (injection-parts gate): when current_spec_tools contains "prototype_emit_only" → on task 1 inject all parts, on task 2+ inject ONLY the seed part(s) (the part(s) containing "TEMPLATE SEED"); when it contains "prototype" → inject all parts always; when it is empty (planning agent) the legacy behavior is preserved (the legacy gated parts on the tool branches, so a tools:[] agent receives no injection parts).
    - CR-03 (task-2+ suppression): when ctx.build_task_number is "2" (or any value not in ("", "1")) AND the agent is a builder (current_spec_tools intersects {prototype_emit_only, prototype}), the DS block, the template body block, AND the example block are ALL ABSENT (only the seed injection part survives). When build_task_number is "" or "1", all blocks inject normally.
    - Ordering + block-name keys UNCHANGED from the current provider and from L12: ACTIVE DESIGN SYSTEM → ACTIVE TEMPLATE (SKILL.md) → TEMPLATE EXAMPLE (example.html) → TEMPLATE INJECTION PART i.
    - Empty od_context still returns {} (no regression to the existing test_opendesign_empty_when_no_od_context).
  </behavior>
  <action>
    In `engine.py _compose_context_message`, BEFORE the `for name in provider_names` loop (engine.py:2926-2942), set `ectx.current_spec_tools` to the consuming agent's tool set as a `set[str]` — derive it from `set(getattr(spec, "tools", []) or [])`. Use the SAME dynamic-attr mechanism the engine already uses for `ectx.compiled_context_providers` (engine.py:1042) — this is per-run/per-agent state on the ExecutionContext (D-03), NOT a workflow-name/id branch (INV-1: do NOT introduce any `spec.id ==`/`pipeline_type` test here; the kernel passes the OPAQUE tool set, the provider does the gating). `ectx.build_task_number` is already populated by the task_loop strategy — do not change how it is set.

    In `opendesign.py OpenDesignProvider.load(ctx)`:
    - Read `spec_tools = set(getattr(ctx, "current_spec_tools", set()) or set())` and `task_num_str = getattr(ctx, "build_task_number", "") or ""` near the top (after the existing od / runner reads). Compute `is_builder = bool(spec_tools & {"prototype_emit_only", "prototype"})` and `is_build_task_2_plus = task_num_str not in ("", "1")` — note this gates on the BUILDER TOOL SET, never on `spec.id`/`pipeline_type` (INV-1; the provider is the legitimate home for the tool-based gate the legacy L12 branch performed).
    - CR-01 (line 58): set the DS block content to the preamble string + ds_body — the EXACT bytes from git fb55699: `"Apply these tokens to ALL colors, fonts, and spacing. Map to :root variables: --bg, --fg, --accent, --surface, --border, --muted.\n"` immediately concatenated with `ds_body`. Copy the preamble verbatim from the git source (spaces after each comma) — do NOT use the comma-compressed form in the verification "missing" line.
    - CR-03 (DS): guard the DS block on `not is_build_task_2_plus` (when a builder is on task 2+, skip the DS body) — mirroring the legacy `if "design_system" in injects and od.get("ds_body") and not is_build_task_2_plus`. The `injects` membership was the kernel-side gate (already enforced by the engine's `if injects:` at engine.py:2926, which only consults the provider when the agent declares an inject) — preserve the `is_design_system_required`/`include_ds` deck-conditional logic already present.
    - CR-03 (template body) + CR-02 (example): guard the template-body block AND the example block on `not is_build_task_2_plus` (legacy nested both inside `if not is_build_task_2_plus`). Within that, CR-02: gate the example block on `is_builder` — only resolve+inject `runner.template_example(template_id)` when `is_builder` is True (a tools:[] planning agent must NOT receive example.html). Keep the existing `[:8000]` + `"...[truncated]"` truncation byte-for-byte.
    - CR-02 + CR-03 (injection parts): replace the current unconditional `for ... template_injection_parts` loop with the legacy tool-gated branch: if `"prototype_emit_only" in spec_tools` → when `is_build_task_2_plus` inject ONLY the parts containing "TEMPLATE SEED", else inject all parts; elif `"prototype" in spec_tools` → inject all parts always; else (planning agent, tools:[]) inject NO injection parts (the legacy branch had no else clause for tools:[]). Keep the `TEMPLATE INJECTION PART {i}: {template_id}` block-name + enumerate index UNCHANGED.
    - Do NOT add any `app.*` import (Pitfall 4 — the import-purity test test_opendesign_imports_no_app must stay green). Do NOT change the port signature. Do NOT introduce a second injection home (INV-12 — the provider stays the single home).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_providers.py -q && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF 'Apply these tokens to ALL colors, fonts, and spacing. Map to :root variables: --bg, --fg, --accent, --surface, --border, --muted.' backend/agents/capabilities/context_providers/opendesign.py` returns >= 1 (CR-01: the exact-byte preamble string is present in the provider, spaces after each comma — matching git fb55699)
    - `grep -nE 'prototype_emit_only.*prototype|\{"prototype_emit_only", ?"prototype"\}' backend/agents/capabilities/context_providers/opendesign.py` returns >= 1 (CR-02: the builder gate set is present)
    - `grep -nE "build_task_number|task_num_str not in \(.., ..1..\)" backend/agents/capabilities/context_providers/opendesign.py` returns >= 1 AND `grep -nE "is_build_task_2_plus" backend/agents/capabilities/context_providers/opendesign.py` returns >= 1 (CR-03: the task-2+ suppression gate is present, reading build_task_number)
    - `grep -nE 'current_spec_tools' backend/agents/execution_engine/engine.py` returns >= 1 AND that assignment occurs inside/just-before the `_compose_context_message` provider loop (the consuming agent's tools threaded to the provider per D-03)
    - `grep -rnE 'spec\.id ==|pipeline_type' backend/agents/capabilities/context_providers/opendesign.py` returns 0 (INV-1: the provider gates on tools/build_task_number, never on workflow name/id) AND `grep -rnE 'spec\.id == "prototype-build"' backend/agents/execution_engine/engine.py` returns 0 (the kernel stays name/id-free — Task did not reintroduce L7)
    - `grep -rnE 'from app\.|import app\.|agents\.execution_engine' backend/agents/capabilities/context_providers/opendesign.py` returns 0 (Pitfall 4: capability import-purity preserved)
    - `cd backend && python3.11 -m pytest tests/agents/test_context_providers.py -q` exits 0
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x -q` exits 0 (5-pipeline deliverable + event parity still green after the fix)
  </acceptance_criteria>
  <done>opendesign.py emits the DS block as preamble + ds_body (CR-01); example.html injects ONLY for builder tool sets (CR-02); DS/template/example are suppressed and only the seed part survives on build tasks 2+ (CR-03); the engine threads ectx.current_spec_tools to the provider with no spec.id/pipeline_type branch (INV-1); capability import-purity preserved; provider + characterization suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the locked-in test_context_providers.py:114 assertion + add CR-01/CR-02/CR-03 parity assertions</name>
  <files>backend/tests/agents/test_context_providers.py</files>
  <read_first>
    - backend/tests/agents/test_context_providers.py (FULL — line 114 `assert blocks["ACTIVE DESIGN SYSTEM: midnight"] == "DS TOKENS"` LOCKS IN the CR-01 regression; the `_FakeRunner` (template_example/template_injection_parts), the `_Ctx` fake (sets od_context but NOT current_spec_tools / build_task_number today), the `_OD` fixture)
    - backend/agents/capabilities/context_providers/opendesign.py (POST Task 1 — the byte contract the assertions must now pin: preamble + ds_body; example gated on current_spec_tools; DS/template/example suppressed on build_task_number not in ("","1"))
    - LEGACY CONTRACT: `git show fb55699:backend/agents/execution_engine/engine.py` lines ~3321-3386 (the exact preamble bytes the assertion must match; the tools:[] planning-agent example-exclusion comment "Planning agents ... must NOT see a full working HTML doc")
  </read_first>
  <action>
    Update the `_Ctx` fake (or add a kwarg) so tests can set `current_spec_tools` and `build_task_number` on the ctx passed to `OpenDesignProvider().load(ctx)` (mirroring how the engine threads them in Task 1). Default `current_spec_tools` to the builder set `{"prototype_emit_only"}` for the existing positive tests so their example/parts assertions keep holding (the existing tests exercised the builder path implicitly).

    Fix line 114: change `assert blocks["ACTIVE DESIGN SYSTEM: midnight"] == "DS TOKENS"` to assert the block content STARTS WITH the preamble AND ENDS WITH the ds_body — i.e. assert the block equals the exact preamble string + "DS TOKENS" (byte-identical to git fb55699), OR assert `.startswith("Apply these tokens to ALL colors, fonts, and spacing.")` AND `.endswith("DS TOKENS")`. Keep the other assertions in that test (ACTIVE TEMPLATE, TEMPLATE EXAMPLE startswith, the injection-part presence) intact.

    Add a CR-01 test: assert the "ACTIVE DESIGN SYSTEM: <ds_id>" block content begins with "Apply these tokens to ALL colors, fonts, and spacing. Map to :root variables: --bg, --fg, --accent, --surface, --border, --muted." (the full preamble line) — pin the exact bytes, not just a substring fragment.

    Add a CR-02 test: invoke the provider with `current_spec_tools = set()` (a tools:[] planning agent) AND a template_body present + a non-empty runner example; assert `"TEMPLATE EXAMPLE (example.html): web-prototype"` is NOT a key in the returned blocks (the example must NOT leak to planning agents). A companion positive case: with `current_spec_tools = {"prototype_emit_only"}`, the TEMPLATE EXAMPLE key IS present.

    Add a CR-03 test: invoke the provider with `current_spec_tools = {"prototype_emit_only"}` and `build_task_number = "2"`; assert the "ACTIVE DESIGN SYSTEM: ..." key, the "ACTIVE TEMPLATE (SKILL.md): ..." key, AND the "TEMPLATE EXAMPLE (example.html): ..." key are ALL ABSENT, and that only the seed injection part (the part containing "TEMPLATE SEED") survives. A companion case with `build_task_number = "1"` asserts all three blocks ARE present (no suppression on task 1).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_providers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF '== "DS TOKENS"' backend/tests/agents/test_context_providers.py` returns 0 (the bare-body assertion that locked in CR-01 is gone)
    - `grep -nF 'Apply these tokens to ALL colors, fonts, and spacing.' backend/tests/agents/test_context_providers.py` returns >= 1 (CR-01: the preamble is now asserted in the test, not just the key name)
    - `grep -nE 'current_spec_tools.*set\(\)|set\(\).*current_spec_tools' backend/tests/agents/test_context_providers.py` returns >= 1 AND there is an assertion that `"TEMPLATE EXAMPLE" not in` / the example key is absent for the tools:[] case (CR-02)
    - `grep -nE 'build_task_number.*=.*"2"|build_task_number="2"' backend/tests/agents/test_context_providers.py` returns >= 1 AND there is an assertion that the ACTIVE DESIGN SYSTEM / ACTIVE TEMPLATE / TEMPLATE EXAMPLE keys are absent on task 2 (CR-03)
    - `cd backend && python3.11 -m pytest tests/agents/test_context_providers.py -q` exits 0 (all CR-01/CR-02/CR-03 assertions pass against the Task-1 provider)
  </acceptance_criteria>
  <done>test_context_providers.py:114 no longer asserts the bare ds_body; the preamble (CR-01), the tools-gated example absence for tools:[] specs (CR-02), and the task-2+ DS/template/example suppression (CR-03) are each pinned by a dedicated assertion; the suite is green — the parity net is no longer blind to the three paths.</done>
</task>

<task type="auto">
  <name>Task 3: Close the structural blind spot — dedicated context_message parity assertion + de-blind the characterization normalizer</name>
  <files>backend/tests/agents/test_context_providers.py, backend/tests/agents/characterization/_normalize.py</files>
  <read_first>
    - backend/tests/agents/characterization/_normalize.py:95-124 (`_VOLATILE_STRIP_KEYS` — line 109 `"context_message"`; the comment explains WHY it was stripped: it "embeds the unaliased run text + prior-agent summaries". Determine whether, AFTER the Task-1 fix, the context_message for a SCRIPTED prototype run is deterministic once run_id/timestamps are already stripped — the OD blocks themselves are deterministic; the volatility risk is the user-brief echo + prior summaries, which the scripted-model fixtures hold constant)
    - backend/tests/agents/characterization/golden/ (prototype.events.json, od_prototype.events.json, prototype_revision.events.json — the event snapshots; if context_message is un-stripped, these regenerate to include the now-byte-correct OD blocks; the regeneration must be a SINGLE deterministic pass, parity-stable across repeated runs, NOT a per-run rewrite)
    - backend/agents/capabilities/context_providers/opendesign.py (POST Task 1 — the byte contract the dedicated parity assertion pins)
    - .planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-VERIFICATION.md (gap 2 `missing`: "add a dedicated context_message parity assertion (separate from the event normalizer) that directly compares the composed context_message ... against a reference snapshot, or assert the exact block structure/ordering at the test_context_providers level")
  </read_first>
  <action>
    Add a DEDICATED context_message parity assertion that is independent of the event normalizer and directly pins the OD block structure/ordering. Preferred (lowest-risk, no live model) location: test_context_providers.py — add a test that composes the FULL ordered block set for a scripted prototype BUILD agent on task 1 (current_spec_tools={"prototype_emit_only"}, build_task_number="1", a populated od_context + runner with example + seed/layout/checklist parts) and asserts the EXACT ordered key sequence and the exact content bytes: DS block == preamble+ds_body, ACTIVE TEMPLATE == template_body, TEMPLATE EXAMPLE == example[:8000] (+truncation marker if applicable), and the TEMPLATE INJECTION PART blocks in order. Add the task-2 counterpart asserting the suppressed ordered set (seed only). This is the non-volatile pin the characterization net lacked.

    Then de-blind the characterization normalizer: REMOVE `"context_message"` from `_VOLATILE_STRIP_KEYS` (_normalize.py:109) and regenerate the affected golden snapshots (prototype/od_prototype/prototype_revision) in a SINGLE deterministic pass so the snapshots now PIN the byte-correct context_message. Re-run the characterization suite TWICE to confirm the snapshots are parity-stable (identical bytes on repeat — not merely green once). If — and only if — removing context_message reintroduces genuine non-determinism that the scripted fixtures cannot hold constant (an unaliased run id / wall-clock leaking into the echo), revert the _normalize.py removal and instead rely solely on the dedicated test_context_providers parity assertion above as the pin (document the decision in the SUMMARY). The blind spot MUST be closed by at least one of the two mechanisms; do NOT leave both un-pinned.

    Do NOT remove any other key from `_VOLATILE_STRIP_KEYS` (timestamp/run_id/seq/event_id etc. stay stripped — they are genuinely volatile).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_providers.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - A dedicated context_message parity test exists in test_context_providers.py that asserts the EXACT ordered block-key sequence AND the byte content for a scripted prototype build-task-1 invocation (DS preamble+body, template, example, injection parts in order) and the suppressed set for build-task-2 — `cd backend && python3.11 -m pytest tests/agents/test_context_providers.py -q` exits 0
    - EITHER `grep -nF '"context_message"' backend/tests/agents/characterization/_normalize.py` returns 0 (context_message removed from _VOLATILE_STRIP_KEYS and the golden snapshots regenerated parity-stable) OR the SUMMARY documents that the dedicated test_context_providers parity assertion is the pin because un-stripping reintroduced genuine volatility — the blind spot is closed one way or the other (not left un-pinned)
    - if context_message was un-stripped: running `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py -q` TWICE in succession both exit 0 with no snapshot diff between runs (parity-stable, not a per-run rewrite)
    - `grep -nE '"timestamp"|"run_id"|"seq"|"event_id"' backend/tests/agents/characterization/_normalize.py` returns >= 1 for each (the genuinely-volatile keys remain stripped — Task 3 did not over-remove)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q` exits 0 (all 5 pipelines still at deliverable + event parity — now including context_message where un-stripped)
  </acceptance_criteria>
  <done>A dedicated context_message parity assertion pins the exact OD block structure/ordering + bytes for build task 1 and the suppressed set for build task 2; context_message is removed from _VOLATILE_STRIP_KEYS (or the dedicated assertion is documented as the sole pin) so context-injection regressions can no longer pass for the wrong reason; the characterization suite is green and parity-stable; only genuinely-volatile keys remain stripped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| context provider → model prompt | the `opendesign` provider composes the context_message bytes that reach the LLM; a faithful-vs-divergent lift determines whether prototype/od_prototype agents see the legacy contract (INV-3) |
| planning agent (tools:[]) vs builder agent (tools intersect {prototype_emit_only, prototype}) | the builder gate decides whether a full working `example.html` is injected; planning agents must NOT receive it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Information Disclosure | example.html injected into tools:[] planning agents (CR-02) | mitigate | restoring the builder gate (`current_spec_tools & {prototype_emit_only, prototype}`) STOPS leaking the full working example.html doc to prototype-specify / prototype-plan — a mild information-exposure hardening + the legacy byte contract. Verified by the CR-02 absence assertion in test_context_providers.py (Task 2) and the dedicated parity test (Task 3). |
| T-07-06-02 | Tampering (parity regression) | context_message byte divergence vs L12 (CR-01/02/03) | mitigate | the three regressions are byte-restored to the legacy contract (git fb55699) and pinned by a dedicated context_message parity assertion + the de-blinded characterization normalizer, so future context-injection drift hard-fails CI instead of passing silently (INV-3). |
| T-07-06-03 | Tampering (false-green test net) | context_message stripped from _VOLATILE_STRIP_KEYS (blind spot) | mitigate | the blind spot is closed by a non-volatile parity assertion (and/or un-stripping context_message with parity-stable regenerated snapshots); the genuinely-volatile keys (timestamp/run_id/seq/event_id) stay stripped so the multiset parity gate is not destabilized. Verified by the parity-stable double-run check (Task 3). |
| T-07-06-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan — pure parity fix to existing modules + tests; no new dependency surface, no new endpoints/secrets/exec. |

No HIGH-severity threat is introduced. This is an internal context-injection byte-parity fix with
no new external attack surface (no new endpoints, secrets, exec, or network). The one security-
adjacent effect is a hardening (CR-02 stops the example.html leak to planning agents).
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_context_providers.py -q` green (CR-01 preamble pinned; CR-02 example absent for tools:[]; CR-03 task-2+ suppression; dedicated context_message parity assertion)
- `grep -nF 'Apply these tokens to ALL colors, fonts, and spacing. Map to :root variables: --bg, --fg, --accent, --surface, --border, --muted.' backend/agents/capabilities/context_providers/opendesign.py` returns >= 1 (CR-01 exact-byte preamble)
- `grep -rnE 'spec\.id ==|pipeline_type' backend/agents/capabilities/context_providers/opendesign.py` returns 0 AND `grep -rnE 'spec\.id == "prototype-build"' backend/agents/execution_engine/engine.py` returns 0 (INV-1 — no name/id branch reintroduced; the kernel banned-pattern gate from 07-05 stays green)
- `grep -rnE 'from app\.|import app\.|agents\.execution_engine' backend/agents/capabilities/context_providers/opendesign.py` returns 0 (capability import-purity, Pitfall 4)
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q` green (5-pipeline deliverable + event + — where un-stripped — context_message parity)
- `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -q` green (the 07-05 INV-1 hard-fail gate + ledger ratchet are NOT regressed by this plan)
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -m "not requires_api_key" -q --tb=no` shows no NEW failures vs the 07-VERIFICATION baseline (548 passed agents / 486 passed unit + 8 KNOWN pre-existing unit failures)
</verification>

<success_criteria>
- opendesign.py is a byte-faithful lift of the legacy L12 injection branches (git fb55699): DS block = preamble + ds_body (CR-01); example.html injected ONLY for builder tool sets (CR-02); DS/template/example suppressed + seed-only injection parts on build tasks 2+ (CR-03) — PARITY-03 closed
- the engine threads `ectx.current_spec_tools` to the provider via the D-03 per-run-state-on-ExecutionContext pattern, with NO `spec.id`/`pipeline_type` branch in the kernel or the provider (INV-1 preserved; the 07-05 banned-pattern hard-fail gate stays green)
- test_context_providers.py:114 no longer locks in the bare-body regression; CR-01/CR-02/CR-03 each have a dedicated pinning assertion
- a dedicated context_message parity assertion exists (independent of the event normalizer) and `context_message` is no longer a structural blind spot — context-injection regressions hard-fail CI (PARITY-09 / INV-3 full scope closed)
- all 5 pipelines remain at deliverable byte + semantic event parity; the full agents + unit suites show no new failures vs the 07-VERIFICATION baseline
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-06-SUMMARY.md` when done
</output>
