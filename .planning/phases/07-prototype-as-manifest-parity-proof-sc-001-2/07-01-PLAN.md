---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/registry.py
  - backend/agents/capabilities/__init__.py
  - backend/agents/execution_engine/context.py
  - backend/agents/capabilities/strategies/__init__.py
  - backend/agents/capabilities/strategies/single_shot.py
  - backend/agents/capabilities/strategies/task_loop.py
  - backend/agents/capabilities/task_parsers/__init__.py
  - backend/agents/capabilities/task_parsers/heading_tasks.py
  - backend/tests/agents/test_capability_resolution.py
  - backend/tests/agents/test_strategies.py
  - backend/tests/agents/test_heading_tasks_parser.py
autonomous: true
requirements: [PARITY-01]
must_haves:
  truths:
    - "registry.resolve(kind, name) returns the registered impl instance for every known (kind,name) and raises on an unknown name (static dict lookup, no getattr/eval)"
    - "single_shot strategy runs one agent through ctx.runner and yields the engine's event dicts unchanged"
    - "task_loop strategy parses tasks via heading_tasks, runs one sub-agent per task through ctx.runner, applies html_skeleton compaction on task-2+, runs static_check+render_check with a bounded N=2 fix-loop, and writes spec.md/design.md/tasks.md before the loop — all via ctx.runner"
    - "heading_tasks parser turns '## Task N:' (with <tasks> fallback) plan text into Task[] byte-identically to _count_plan_tasks/_extract_task_block"
    - "ExecutionContext carries an object-typed runner handle field; context.py imports no app.* or engine internals"
    - "import-linter contract stays green: no capability module imports agents.execution_engine or app.*"
  artifacts:
    - path: "backend/agents/capabilities/strategies/single_shot.py"
      provides: "SingleShotStrategy (name='single_shot')"
      contains: "class SingleShotStrategy"
    - path: "backend/agents/capabilities/strategies/task_loop.py"
      provides: "TaskLoopStrategy (name='task_loop') with per-task loop + validation/fix + seed_files + lifted pure fix-sig helpers"
      contains: "class TaskLoopStrategy"
    - path: "backend/agents/capabilities/task_parsers/heading_tasks.py"
      provides: "HeadingTasksParser (name='heading_tasks')"
      contains: "class HeadingTasksParser"
    - path: "backend/agents/capabilities/registry.py"
      provides: "(kind,name)->impl map + resolve() + install()/_register_builtins()"
      contains: "def resolve"
    - path: "backend/agents/execution_engine/context.py"
      provides: "object-typed runner handle field on ExecutionContext"
      contains: "runner"
  key_links:
    - from: "backend/agents/capabilities/strategies/task_loop.py"
      to: "ctx.runner"
      via: "object-typed KernelServices handle (run_agent + sandbox + static_check + render_check)"
      pattern: "ctx\\.runner"
    - from: "backend/agents/capabilities/registry.py"
      to: "capability impl instances"
      via: "resolve(kind, name) static dict lookup over _KNOWN-validated names"
      pattern: "def resolve"
---

<objective>
Land the two execution strategies (`single_shot`, `task_loop`), the `heading_tasks` task parser, the D-02 name->impl resolution seam, and the D-03 runner handle on `ExecutionContext` — the foundational seams every later plan routes through. `task_loop` absorbs the per-task sub-agent loop, per-task `html_skeleton` compaction (task-2+, resolved via the registry seam), the Both-validation (`static_check`+`render_check`) + bounded N=2 fix-loop behavior, and the `seed_files` (spec.md/design.md/tasks.md) write — all reached through the object-typed runner handle so no capability imports the kernel or `app.*`.

This plan builds the SEAMS only. The engine is NOT yet wired to use them (that is 07-04) and NO leak is deleted (that is 07-05). The L1–L13 leaks stay physically present and live; the new capabilities are exercised only by their Wave 0 unit tests this plan. This is the strangler "wrap" step (§31).

Purpose: PARITY-01 — `single_shot` + `task_loop` implemented behind the `ExecutionStrategy` port; a unit test drives both strategies from a compiled `Step`; the resolution seam + runner handle exist for 07-02/03/04 to build on.
Output: `capabilities/strategies/{single_shot,task_loop}.py`, `capabilities/task_parsers/heading_tasks.py`, the extended `registry.py` (resolve + install), the `ExecutionContext.runner` handle field, and 3 Wave 0 unit test files.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-SPEC.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-CONTEXT.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-RESEARCH.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-PATTERNS.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from any source-grounding drift check — they do not exist before this plan):
- `SingleShotStrategy` (`agents/capabilities/strategies/single_shot.py`, `name="single_shot"`)
- `TaskLoopStrategy` (`agents/capabilities/strategies/task_loop.py`, `name="task_loop"`)
- `HeadingTasksParser` (`agents/capabilities/task_parsers/heading_tasks.py`, `name="heading_tasks"`)
- `CapabilityRegistry.resolve(kind, name)` + `CapabilityRegistry.install()` / `_register_builtins()` + the `(kind,name)->impl` map in `agents/capabilities/registry.py`
- `ExecutionContext.runner` field (object-typed `KernelServices` handle) in `agents/execution_engine/context.py`
- The `KernelServices` handle shape (Claude's discretion: a frozen dataclass or callable bundle attached as `ctx.runner`) — wraps `run_agent` + sandbox + `static_check`/`render_check` + any other engine internal the task-loop needs (inventory in Task 1)
- New module init files: `agents/capabilities/strategies/__init__.py`, `agents/capabilities/task_parsers/__init__.py`
- Wave 0 tests: `tests/agents/test_capability_resolution.py`, `tests/agents/test_strategies.py`, `tests/agents/test_heading_tasks_parser.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 resolution seam + D-03 runner handle + heading_tasks parser</name>
  <files>backend/agents/capabilities/registry.py, backend/agents/capabilities/__init__.py, backend/agents/execution_engine/context.py, backend/agents/capabilities/task_parsers/__init__.py, backend/agents/capabilities/task_parsers/heading_tasks.py, backend/tests/agents/test_capability_resolution.py, backend/tests/agents/test_heading_tasks_parser.py</files>
  <read_first>
    - backend/agents/capabilities/registry.py (FULL — the `_KNOWN` set :37-53, `is_registered` :64-66, `resolve_alias` :68-74 to leave untouched)
    - backend/agents/capabilities/base.py (FULL — the `TaskParser` port :27-39 and all 6 ports; impls satisfy structurally)
    - backend/agents/execution_engine/context.py:76-125 (the `scoped_store`/`model_resolver` `object|None` precedent at :82,:90 + the import-direction docstring :25-31 + the `current_task_block`/`build_task_number`/`build_task_total` fields :115-125)
    - backend/agents/execution_engine/engine.py:2319-2368 (`_count_plan_tasks` :2319 + `_extract_task_block` :2347 — the heading-parse logic to lift verbatim)
    - backend/agents/execution_engine/engine.py:1543-1700 and :2072-2262 (`_run_agent` + `_run_build_task_loop` — inventory EVERY `self.<method>` / engine internal the loop calls, to size the runner handle: A1/Open-Q1)
    - backend/agents/workflows/plan.py (the `Task` / `Step` / `TaskSource` dataclasses the parser returns / strategies consume)
    - backend/tests/agents/test_registry_capabilities.py (the membership-assertion analog to extend; confirm it stays honest — no impls at compiler import)
    - 07-PATTERNS.md (the registry.py self-extension section + the heading_tasks analog + the runner-handle template)
    - 07-RESEARCH.md Open Questions 1 & 2 (handle shape; install() invocation site)
  </read_first>
  <behavior>
    - Test (test_capability_resolution.py): `CapabilityRegistry().resolve("task_parser","heading_tasks")` returns a `HeadingTasksParser` instance with `.name == "heading_tasks"`; resolve of every `(kind,name)` in `_KNOWN` that has an impl returns an instance whose `.name` equals `name`; `resolve("strategy","does_not_exist")` raises (KeyError/ValueError); `is_registered` behavior is unchanged (still pure `_KNOWN` membership); resolve performs NO getattr/eval/dynamic-import (T-04-01 — assert by code review + a test that a name not in `_KNOWN` raises before any lookup).
    - Test (test_heading_tasks_parser.py): port the `_count_plan_tasks`/`_extract_task_block` cases — `parse("## Task 1: Foo\n...\n## Task 2: Bar\n...")` returns 2 `Task` objects; the `<tasks>...</tasks>` fallback path (no `## Task N:` headings at top level) parses the inner block; empty/no-task text returns `[]`; each task's body equals the `_extract_task_block` slice byte-for-byte.
  </behavior>
  <action>
    Extend `CapabilityRegistry` (registry.py) with a module-level `(kind,name)->impl` map plus `resolve(kind, name)` and an explicit `install()`/`_register_builtins()` per D-02. `resolve` asserts `(kind,name) in _KNOWN` AND looks up the impl map; a known name with no bound impl raises a programmer error (e.g. `KeyError`/`RuntimeError`), an unknown name raises (never silently None). Keep `resolve` a STATIC dict lookup — NO `getattr`/`eval`/dynamic import of the name (T-04-01 / V5). Leave `is_registered` (pure `_KNOWN` membership — the compiler's INV-4 path) and `resolve_alias` UNTOUCHED. `install()` imports the builtin capability modules and binds one instance per `(kind,name)`; this plan binds `("task_parser","heading_tasks")`, `("strategy","single_shot")`, `("strategy","task_loop")` (the latter two land in Task 2; import them in `install()` and add the bindings as those modules are created). Invoke `install()` lazily — at engine import or first `execute()`, NOT at compiler import — so `test_registry_capabilities.py` (no impls at Phase-4 compiler import) stays honest (Open Q2; discretion: `install()` may live in `registry.py`, `capabilities/__init__.py`, or be called engine-side — keep no `@register`/`discover()` machinery; the `registry.py` "names only" scope note stays accurate for the membership set). Update the registry module docstring to note the impl map is populated by explicit `install()` (evolution to `@register`/`discover()` is Phase 8, INV-12 not violated).

    Add the D-03 runner handle to `ExecutionContext` (context.py): a single `object | None = None` field named `runner`, documented IDENTICALLY to `scoped_store`/`model_resolver` (:82,:90) — "Typed object|None (NOT the concrete type) so this pure-data module stays import-pure; the impl lives under the kernel; capabilities reach it dynamically via ctx." Inventory (from the `read_first` reads of `_run_agent` + `_run_build_task_loop` + `_run_validation_fix_loop`) EVERY engine internal the strategies will need — at minimum: run-one-agent-yielding-events (wrapping `_run_agent` with create_runner + astream + model-fallback + output readback), the per-run sandbox (`read`/`write`/`path_for`/`root`), `static_check`, `render_check`, `count_sandbox_deliverables`, `serialize_sandbox_deliverable`. Design the handle as one frozen `KernelServices`-style bundle (discretion: dataclass of callables vs an object with methods) exposing each; the concrete `KernelServices` class lives under the kernel (`execution_engine/`) and is attached as `ctx.runner` by `execute()` in 07-04 — this plan defines the SHAPE the strategies call against (document the expected attributes/methods in `task_loop.py`/`single_shot.py` as the contract; do NOT import the concrete class into the capability). Preserve the `_run_agent` thread-id shape `f"{run_id}:{spec.id}:{task_num}"` (build) vs `f"{run_id}:{spec.id}"` (engine.py:1657-1667) inside the handle's run-agent path. Do NOT re-add `self._current_task_block` to the engine (L14 ratchet — Pitfall 6).

    Create `capabilities/task_parsers/heading_tasks.py` with `class HeadingTasksParser` (`name = "heading_tasks"`) satisfying the `TaskParser` port (`def parse(self, text) -> list[Task]`). Lift `_count_plan_tasks` (engine.py:2319-2344) + `_extract_task_block` (engine.py:2347-2368) verbatim — the `^##\s+Task\s+(\d+)` regex, the `<tasks>...</tasks>` fallback, and the per-block slicing are load-bearing (do not rewrite). Return `list[Task]` (`plan.Task` — importing `agents.workflows.plan` from a capability is ALLOWED; only `agents.execution_engine` + `app` are forbidden — Pitfall 4). Keep `ctx`/`step` typing `Any`/`object` where any context is touched; the parser itself is pure text->Task[]. Add `capabilities/task_parsers/__init__.py`.

    Write `test_capability_resolution.py` and `test_heading_tasks_parser.py` per the behavior block (analogs: `test_registry_capabilities.py`, `test_phase4_build_loop.py` `_count_plan_tasks` cases).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_capability_resolution.py tests/agents/test_heading_tasks_parser.py tests/agents/test_registry_capabilities.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_capability_resolution.py tests/agents/test_heading_tasks_parser.py -x` exits 0
    - `cd backend && python3.11 -m pytest tests/agents/test_registry_capabilities.py -x` exits 0 (membership path + "no impls at compiler import" unchanged)
    - `cd backend && lint_imports` exits 0 (no `agents.capabilities` -> `agents.execution_engine`/`app` import)
    - `grep -nE "getattr|eval\(|importlib|__import__" backend/agents/capabilities/registry.py` shows NO dynamic name resolution inside `resolve` (T-04-01)
    - `grep -cE "runner" backend/agents/execution_engine/context.py` >= 1 AND the field is typed `object | None`
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -x` exits 0 (no regression — leaks still present)
  </acceptance_criteria>
  <done>resolve()/install() seam + object-typed runner handle field + heading_tasks parser exist and pass their unit tests; the compiler membership path is unchanged; import-linter green; 0A/ledger/banned-pattern still green (nothing routed/deleted yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: single_shot + task_loop strategies (behavior lift, no kernel import)</name>
  <files>backend/agents/capabilities/strategies/__init__.py, backend/agents/capabilities/strategies/single_shot.py, backend/agents/capabilities/strategies/task_loop.py, backend/agents/capabilities/registry.py, backend/tests/agents/test_strategies.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:1254-1269 (the build-vs-else dispatch: `if getattr(spec,"id",None)=="prototype-build": _run_build_task_loop else _run_agent` — the `else` branch is single_shot, the `if` branch is task_loop)
    - backend/agents/execution_engine/engine.py:2072-2262 (`_run_build_task_loop` — the full per-task loop body: seed reference files, parse tasks, per-task sub-agent, task-2+ compaction, validation+fix dispatch)
    - backend/agents/execution_engine/engine.py:2263-2318 (`_write_build_reference_files` — the spec.md/design.md/tasks.md seeding logic; manifests are `seed_files: {}` so this rides in task_loop — Pitfall 2)
    - backend/agents/execution_engine/engine.py:2370-2440 (`_run_validation_fix_loop` — the Both-validation + bounded N=2 fix-loop, non-yielding coroutine)
    - backend/agents/execution_engine/engine.py:274-360 (the PURE fix-sig helpers `_static_issue_sigs`/`_console_sigs`/`_select_issues_to_fix` — lift verbatim, they are the byte-identical fix wording for build AND revision — do NOT rewrite)
    - backend/agents/execution_engine/engine.py:1543-1700 (`_run_agent` call shape + thread-id :1657-1667 — what the runner handle wraps)
    - backend/agents/capabilities/base.py:42-50 (the `ExecutionStrategy` port — `name` + `def run(self, step, ctx) -> AsyncIterator[dict]`)
    - backend/agents/capabilities/strategies/task_loop.py contract notes written in Task 1 (the ctx.runner attributes/methods)
    - 07-PATTERNS.md (single_shot + task_loop sections + the yield-the-engine's-event-dicts shared pattern + the current_task_block reclaim note)
    - backend/tests/agents/test_phase4_build_loop.py (the strategy unit-test analog)
  </read_first>
  <behavior>
    - Test (test_strategies.py): drive `single_shot` from a compiled `Step` against a fake `ctx.runner` whose `run_agent` yields a scripted sequence of event dicts — assert `single_shot.run(step, ctx)` re-yields the SAME `{"type","data"}` dicts in order (WS-vocabulary parity, INV-3); single_shot makes exactly one run_agent call.
    - Test (test_strategies.py): drive `task_loop` from a compiled `Step` with a 2-task plan — assert it (a) writes spec.md/design.md/tasks.md via the handle before the loop, (b) parses 2 tasks via `heading_tasks`, (c) calls `ctx.runner.run_agent` once per task with the per-task `=== CURRENT TASK ===` block set, (d) requests `html_skeleton` compaction for task-2 (resolved via the registry — stubbed/real), (e) invokes `static_check`+`render_check` and the bounded N=2 fix-loop via the handle, (f) yields the same event-dict vocabulary as the engine build loop. Use a fake handle so no real agent/Chromium is needed.
  </behavior>
  <action>
    Create `capabilities/strategies/single_shot.py` with `class SingleShotStrategy` (`name = "single_shot"`) satisfying the `ExecutionStrategy` port. `run(self, step, ctx)` is an async generator: call the run-one-agent primitive ONCE through `ctx.runner` (the handle from Task 1) and re-yield its event dicts unchanged (engine.py:1262-1269 `else` branch behavior — the non-build single-agent path used by specify/plan/validate/ppt steps). Keep `ctx` typed `Any`/`object`; access handle attributes dynamically. NEVER import `agents.execution_engine` or `app.*` (Pitfall 4).

    Create `capabilities/strategies/task_loop.py` with `class TaskLoopStrategy` (`name = "task_loop"`) satisfying the port. `run(self, step, ctx)` owns the full build loop (lift `_run_build_task_loop` engine.py:2072-2262 + `_write_build_reference_files` :2263-2318 + `_run_validation_fix_loop` :2370-2440 behavior): (1) write spec.md/design.md/tasks.md to the sandbox via `ctx.runner` BEFORE the loop (seed_files behavior rides here, manifests are `{}` — Pitfall 2); (2) parse tasks via `registry.resolve("task_parser", step.task_source.parser).parse(...)`; (3) per task, invoke `ctx.runner.run_agent(...)` yielding its events, with `current_task_block`/`build_task_number`/`build_task_total` held as STRATEGY-LOCAL scratch (reclaimed from `ExecutionContext` per D-03 — do NOT re-add `self._...` engine state, L14 Pitfall 6); (4) on task-2+ apply compaction via `registry.resolve("compaction", step.compaction).compact(...)` (the `html_skeleton` impl lands in 07-03; resolve-by-name now — in this plan's unit test stub/register a fake compaction or skip the call when no impl, but the call site routes through `resolve`); (5) after each task run the Both-validation `static_check`+`render_check` via `ctx.runner` and the bounded N=2 fix-loop. Lift the PURE fix-sig helpers `_static_issue_sigs`/`_console_sigs`/`_select_issues_to_fix` (engine.py:274-360) into / alongside `task_loop.py` verbatim (byte-identical fix wording — Don't Hand-Roll). `run()` yields the SAME `{"type","data"}` event dicts the engine build loop yields today (INV-3). Add `capabilities/strategies/__init__.py`.

    Update `registry.py`'s `install()` to bind `("strategy","single_shot") -> SingleShotStrategy()` and `("strategy","task_loop") -> TaskLoopStrategy()`.

    Write `test_strategies.py` per the behavior block using a fake `ctx.runner` (no real agent/Chromium): assert single_shot one-call re-yield + task_loop parse/seed/per-task/compaction-route/validation-fix/event-vocabulary (analog: `test_phase4_build_loop.py`).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_strategies.py tests/agents/test_capability_resolution.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_strategies.py -x` exits 0 (drives BOTH strategies from a compiled Step — PARITY-01 acceptance)
    - `cd backend && lint_imports` exits 0 (neither strategy imports `agents.execution_engine` or `app.*`)
    - `grep -nE "from agents.execution_engine|import agents.execution_engine|from app\.|import app\." backend/agents/capabilities/strategies/single_shot.py backend/agents/capabilities/strategies/task_loop.py` returns 0 matches (Pitfall 4)
    - `grep -nE "create_deep_agent" backend/agents/capabilities/strategies/task_loop.py backend/agents/capabilities/strategies/single_shot.py` returns 0 (INV-13 — strategies reach the agent loop only via the handle)
    - `grep -cE "class SingleShotStrategy|class TaskLoopStrategy" backend/agents/capabilities/strategies/single_shot.py backend/agents/capabilities/strategies/task_loop.py` confirms both classes exist with their `name` attributes
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -x` exits 0 (no regression — engine still runs the legacy path; nothing wired/deleted)
  </acceptance_criteria>
  <done>SingleShotStrategy + TaskLoopStrategy exist behind the port, drive from a compiled Step in a unit test, route compaction/parser/validation through the handle + registry, import no kernel/app, and bind into install(); 0A/ledger/banned-pattern still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled manifest name -> capability impl | `registry.resolve(kind,name)` maps a declared string to executable behavior — must not become a code-exec vector |
| capability module -> kernel/app | the import-linter port boundary; capabilities reach kernel primitives only via the object-typed `ctx.runner` handle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Elevation of Privilege | `CapabilityRegistry.resolve` | mitigate | `resolve` is a STATIC dict lookup over `_KNOWN`-validated names; NO `getattr`/`eval`/`importlib`/`__import__` of the name (T-04-01 / ASVS V5); `install()` binds a fixed set. Verified by the registry grep gate in Task 1. |
| T-07-01-02 | Tampering (runtime integrity) | `TaskLoopStrategy`/`SingleShotStrategy` | mitigate | No capability imports/calls `create_deep_agent` or re-rolls the agent loop (INV-13); the agent is invoked only through `ctx.runner.run_agent`. Verified by the `create_deep_agent` grep gate. |
| T-07-01-03 | Architecture erosion | capability modules | mitigate | import-linter contract `agents.capabilities ⊬ agents.execution_engine/app` stays green (ASVS V1); `ctx` typed `Any`/`object`, handle typed `object | None` on `ExecutionContext`. Verified by `lint_imports` + the import grep gate. |

No `high`-severity threat in this plan (seam construction; no new external attack surface; no parent-run data crosses a boundary here — that is 07-02's `previous_run`).
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_strategies.py tests/agents/test_heading_tasks_parser.py tests/agents/test_capability_resolution.py tests/agents/test_registry_capabilities.py -x` green
- `cd backend && lint_imports` green (kernel->ports boundary intact; capabilities import only `agents.workflows.plan` + stdlib)
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -x` green (L1–L13 untouched and live; engine still on the legacy path)
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -v` green (no regression)
</verification>

<success_criteria>
- `single_shot` + `task_loop` exist behind `ExecutionStrategy`; a unit test drives both from a compiled `Step` (PARITY-01 acceptance line)
- `registry.resolve(kind,name)` returns the right impl, raises on unknown, never uses getattr/eval; `is_registered` unchanged
- `heading_tasks` parser lifts `_count_plan_tasks`/`_extract_task_block` and returns `Task[]`
- `ExecutionContext.runner` object-typed handle field exists; context.py import-pure
- import-linter green; no capability imports the kernel/app; no `create_deep_agent` in a capability
- 0A characterization + migration-ledger + banned-pattern suites still green (nothing routed or deleted)
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-01-SUMMARY.md` when done
</output>
