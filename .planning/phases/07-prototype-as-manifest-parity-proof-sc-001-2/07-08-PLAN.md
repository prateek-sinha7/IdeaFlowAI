---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 08
type: execute
wave: 2
depends_on: [07-07]
files_modified:
  - backend/tests/agents/characterization/oracle/__init__.py
  - backend/tests/agents/characterization/oracle/legacy_context_message.py
  - backend/tests/agents/test_context_message_oracle.py
autonomous: true
gap_closure: true
requirements: [PARITY-03, PARITY-09]
must_haves:
  truths:
    - "An ORACLE reconstructs the TRUE pre-Phase-7 (acd1636) assembled context_message bytes for prototype/od_prototype agent classes — NOT the current (drifted) routed bytes"
    - "The oracle's ground truth is the legacy `_build_context_message` at `git show acd1636:.../engine.py` (3243-3470), captured independently of the live engine so it cannot drift with the refactor"
    - "Documented repro inputs (fixed od_context + injects + tool set + task number) drive the oracle for each agent class: prototype-specify, prototype-plan, prototype-build task-1, prototype-build task-2+"
    - "The baseline discrepancy is resolved: the oracle pins to `acd1636`; the relationship to the 07-06 `1d9234b^` reference is recorded (the _is_builder example gate is present at BOTH refs)"
    - "A test pins the oracle bytes AND demonstrates the current routed context_message DIVERGES from them — the drift the 07-06 de-blind masked; this divergence is the acceptance target 07-09 closes"
  artifacts:
    - path: "backend/tests/agents/characterization/oracle/legacy_context_message.py"
      provides: "captured pre-Phase-7 context_message bytes + per-agent-class repro inputs"
      contains: "TEMPLATE COMPLIANCE"
    - path: "backend/tests/agents/test_context_message_oracle.py"
      provides: "oracle stability + legacy-byte assertions + current-routed divergence demo; the 07-09 target"
  key_links:
    - from: "backend/tests/agents/characterization/oracle/legacy_context_message.py"
      to: "git show acd1636:backend/agents/execution_engine/engine.py (_build_context_message)"
      via: "verbatim-captured legacy assembled bytes (provenance-cited)"
      pattern: "acd1636"
---

<objective>
Cluster C, part 1 — build the ORACLE. The 07-06 gap-closure de-blinded the goldens by
removing `context_message` from `_VOLATILE_STRIP_KEYS` and regenerating with
`SNAPSHOT_UPDATE=1` — but `SNAPSHOT_UPDATE` captures the CURRENT routed bytes, which had
ALREADY drifted from the true pre-Phase-7 prompt. The goldens pin the drifted prompt as the
contract; "pinned == correct" is false (07-REVIEW-DEEP.md systemic[0]). This plan
establishes the authoritative pre-Phase-7 `context_message` bytes from the legacy
`_build_context_message` at baseline `acd1636`, captures them as an oracle INDEPENDENT of the
live engine, and writes a test that pins the oracle and demonstrates the current routed
message diverges from it.

Purpose: without an independent ground truth, every cluster-C "byte-exact restoration" is
asserted against a drifted golden and re-pins the wrong contract. The oracle is the only
correct measuring stick (07-REVIEW-DEEP.md "Execution order"; scope cluster C step 1).

Output: an `oracle/` module holding the captured legacy bytes + per-agent-class repro
inputs; a test pinning the oracle and showing the divergence. NO engine/capability source
is edited here — ground truth first.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-REVIEW-DEEP.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-RESEARCH.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-VERIFICATION.md
@backend/tests/agents/characterization/_normalize.py
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the legacy pre-Phase-7 context_message bytes as an engine-independent oracle</name>
  <read_first>
    - Legacy ground truth: `git show acd1636:backend/agents/execution_engine/engine.py`, lines 3243-3470 — the od/template/example injection branch (per-`injects` gate; `is_build_task_2_plus` suppression; the `_is_builder` example gate at ~:3367; the RAW `parts.append(part)` for injection parts; the BARE `=== END ACTIVE … ===` markers), the CURRENT TASK block, the CURRENT PROTOTYPE skeleton block (with the `read_file('prototype.html')` instruction + `=== END CURRENT PROTOTYPE ===` close + the `[Error:` suppression), the CURRENT HTML block, and the unconditional `=== TEMPLATE COMPLIANCE ===` block.
    - Legacy OD loaders: `git show acd1636:backend/agents/prototype/context.py` (`load_prototype_context`, `get_template_injection_parts`, `get_example_html`).
    - backend/tests/agents/characterization/_normalize.py (:109-117 — the 07-06 de-blind comment, the drift this corrects)
    - backend/agents/execution_engine/context.py (:121-138 — the repro-input field shapes)
  </read_first>
  <action>
    Create `backend/tests/agents/characterization/oracle/__init__.py` +
    `legacy_context_message.py`. Expose `build_oracle_message(agent_class, *, task="")`
    returning the EXACT legacy assembled `context_message` for four agent classes:
    `"prototype-specify"` (injects [template, design_system], tools [], task ""),
    `"prototype-plan"` (same, tools [], task ""),
    `"prototype-build"` task `"1"` (injects [template, design_system], tools ["prototype_emit_only"]),
    and `"prototype-build"` task `"2"`. Pick the most faithful capture mechanism
    (Claude's discretion): either PINNED-BYTES (paste the legacy block strings verbatim from
    `git show acd1636:.../engine.py:3243-3470` + the loader outputs as module constants, each
    with a `# provenance: acd1636:engine.py:<linerange>` comment, assembled with the legacy
    `"\n".join(parts)` order) or PINNED-OLD-ENGINE (load the legacy `_build_context_message`
    source via `git show acd1636:...` at test time and run it against a fixed od_context +
    fake spec/ectx). The oracle MUST honor the acd1636 gates verbatim: the per-`injects` gate;
    `is_build_task_2_plus = task not in ("","1")` suppression of DS body / template body /
    example / non-seed injection parts; the `_is_builder` example gate
    (`set(spec.tools) & {"prototype_emit_only","prototype"}` — PRESENT at acd1636, so planning
    agents with tools [] get NO example; do NOT drop it); RAW `parts.append(part)` for
    injection parts; BARE `=== END ACTIVE … ===` markers (no `: {id}` suffix); the task-2+
    skeleton wrapper `=== CURRENT PROTOTYPE (skeleton — call read_file('prototype.html') for
    full content before editing) ===` with its `=== END CURRENT PROTOTYPE ===` close + the
    `[Error:` suppression; and the unconditional `=== TEMPLATE COMPLIANCE ===` block on every
    build task. Use a SMALL deterministic od_context fixture (short bodies) so the bytes are
    reviewable. In the module docstring, resolve the baseline: pin to `acd1636`, note 07-06
    used `1d9234b^`, and record that the `_is_builder` example gate exists at BOTH refs.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_message_oracle.py -k import_and_shape -q || python3.11 -c "import tests.agents.characterization.oracle.legacy_context_message as o; print('oracle module imports')"</automated>
  </verify>
  <done>`oracle/legacy_context_message.py` exists, imports cleanly, and `build_oracle_message` returns the four legacy block sequences with the unconditional TEMPLATE COMPLIANCE block, bare END markers, and the task-2+ skeleton wrapper with its read_file instruction — each carrying acd1636 provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the oracle + demonstrate the current routed context_message diverges; re-pin the 5 goldens to oracle ground truth</name>
  <read_first>
    - backend/tests/agents/characterization/oracle/legacy_context_message.py (the artifact from Task 1)
    - backend/agents/execution_engine/engine.py (`_compose_context_message` :2851-2980 — the CURRENT routed injector: the `=== {block_name} ===\n…\n=== END {block_name} ===` wrap at :2951 that adds the `: {id}` END suffix; no TEMPLATE COMPLIANCE; no per-injects per-block gate)
    - backend/tests/agents/characterization/_normalize.py (load/write golden helpers; `context_message` now in the pinned set)
    - backend/tests/agents/test_characterization_prototype.py (the `_drive` + golden assertion pattern; `SNAPSHOT_UPDATE` regeneration)
    - backend/tests/agents/_scripted_model.py (`_drive` — how a routed run's events are produced offline)
  </read_first>
  <action>
    Create `backend/tests/agents/test_context_message_oracle.py`. Add: (1) an oracle
    STABILITY test asserting `build_oracle_message(...)` is byte-stable across calls and
    contains the legacy structural markers (TEMPLATE COMPLIANCE present on build tasks; bare
    `=== END ACTIVE DESIGN SYSTEM ===`; task-2 skeleton wrapper with `read_file('prototype.html')`;
    injection parts un-rewrapped); (2) a DIVERGENCE test that drives a prototype build via
    `_drive("prototype")`, extracts the build-agent `context_message` from the emitted
    `agent_input` events, and asserts it does NOT yet equal the oracle bytes — marked
    `xfail(strict=False)` WITH a docstring stating this xfail is the 07-09 acceptance target
    (07-09 turns it to a hard equality pass). This makes the drift the 07-06 de-blind masked
    visible + tracked. Then RE-PIN the five characterization goldens so they reflect the
    oracle ground truth rather than the drifted bytes: because the routed engine has NOT been
    corrected yet (that is 07-09), do NOT regenerate the goldens from the live engine here —
    instead, document in the test module + the 07-08 summary that the 5 goldens
    (`golden/*.events.json`) remain on the DRIFTED bytes until 07-09 corrects the engine, at
    which point 07-09 regenerates them with `SNAPSHOT_UPDATE=1` AND asserts the regenerated
    build-agent `context_message` equals the oracle (closing the loop). The authoritative
    re-pin to oracle bytes therefore happens in 07-09 after the engine is byte-correct; this
    plan's job is to make the oracle the assertion target and prove today's divergence.
    Run the new oracle test file and confirm: the stability test PASSES and the divergence
    test XFAILS (expected — the engine is still drifted).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_context_message_oracle.py -q -rx</automated>
  </verify>
  <done>`test_context_message_oracle.py` passes the oracle stability test and records the current-routed divergence as a strict-tracked xfail; the test module documents that the 5 goldens are re-pinned to oracle ground truth in 07-09 (after the engine is corrected) — this plan establishes the oracle as the measuring stick and proves the drift.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `tests/agents/characterization/oracle/__init__.py` (new package).
- `tests/agents/characterization/oracle/legacy_context_message.py` — `build_oracle_message(agent_class, *, task="")` + the per-agent-class repro od_context fixture + acd1636 provenance constants.
- `tests/agents/test_context_message_oracle.py` — the oracle stability test + the current-routed divergence xfail (the 07-09 acceptance target).
</artifacts_this_phase_produces>

<threat_model>
Additive test-only artifacts; no engine/capability source edits; no package installs; no
boundary/trust change. The risk is an INACCURATE oracle (capturing the wrong bytes), which
would mis-direct 07-09. Mitigated by (a) capturing from `git show acd1636:...` verbatim with
per-line provenance comments rather than re-deriving, and (b) the divergence test forcing the
oracle to differ from today's routed bytes (an oracle that matched today's bytes would
indicate it captured the drifted prompt and must be re-examined).
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | oracle captures drifted (not legacy) bytes | mitigate | capture verbatim from acd1636 with provenance; divergence test must XFAIL against today's routed message |
| T-07-08-02 | Information | wrong baseline chosen (acd1636 vs 1d9234b^) | accept | docstring records both; the _is_builder gate is present at both, so the example gate is unaffected by the choice |
</threat_model>

<verification>
- `python3.11 -m pytest tests/agents/test_context_message_oracle.py -q -rx` — stability PASS, divergence XFAIL.
- The oracle build-task-1 message contains `=== TEMPLATE COMPLIANCE ===` and a bare `=== END ACTIVE DESIGN SYSTEM ===` (no `: {id}` suffix).
- The oracle build-task-2 message contains `=== CURRENT PROTOTYPE (skeleton — call read_file('prototype.html')` and `=== END CURRENT PROTOTYPE ===`.
- `legacy_context_message.py` cites `acd1636` provenance for each captured block.
</verification>

<success_criteria>
The pre-Phase-7 context_message bytes are captured as an engine-independent oracle pinned to
acd1636; a test demonstrates the current routed message diverges from the oracle; the oracle
is the documented acceptance target for the 07-09 per-finding restorations.
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-08-SUMMARY.md` when done
</output>
