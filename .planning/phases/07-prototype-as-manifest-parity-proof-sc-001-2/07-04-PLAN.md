---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/tests/agents/test_routing_parity.py
autonomous: true
requirements: [PARITY-05, PARITY-09]
must_haves:
  truths:
    - "the engine's per-step dispatch is resolve(step.strategy).run(step, ctx) with NO spec.id/pipeline_type branch — the prototype build step runs via the task_loop strategy, non-build steps via single_shot"
    - "the engine resolves the final deliverable via resolve(deliverable.strategy).resolve(ctx) reading deliverable.name — no _resolve_final_output pipeline_type branch on the routed path"
    - "context injection runs through a generic injector over resolve(context_provider, ...).load(ctx) — the per-pipeline branches are no longer reached on the routed path"
    - "execute() attaches the KernelServices runner handle (run_agent + sandbox + static_check + render_check + serialize/count) to ctx.runner"
    - "prototype, od_prototype, and prototype_revision run purely from their compiled manifests; the L1-L13 leak definitions remain physically present but are now DEAD (unreferenced on the routed path, confirmed by vulture)"
    - "all 5 pipelines (prototype/od_prototype/prototype_revision/ppt/code-gen) are at deliverable byte parity (where deterministic) + semantic event parity vs the post-0C baseline"
  artifacts:
    - path: "backend/agents/execution_engine/kernel_services.py"
      provides: "KernelServices handle (the concrete object attached as ctx.runner) wrapping _run_agent + sandbox + validators"
      contains: "KernelServices"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "capability-routed per-step dispatch + deliverable resolution + generic context injector"
      contains: "resolve"
    - path: "backend/tests/agents/test_routing_parity.py"
      provides: "asserts the routed path produces the same events/deliverable as the legacy path for a prototype build"
      contains: "task_loop"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "registry.resolve('strategy', step.strategy).run(step, ctx)"
      via: "per-step dispatch (no spec.id/pipeline_type branch)"
      pattern: "resolve\\(\\s*[\"']strategy"
    - from: "backend/agents/execution_engine/engine.py"
      to: "registry.resolve('deliverable', compiled.deliverable.strategy).resolve(ctx)"
      via: "deliverable resolution by manifest strategy/name"
      pattern: "resolve\\(\\s*[\"']deliverable"
    - from: "backend/agents/execution_engine/engine.py"
      to: "ctx.runner = KernelServices(...)"
      via: "execute() attaches the runner handle"
      pattern: "runner"
---

<objective>
Wire the kernel to route `prototype`, `od_prototype`, and `prototype_revision` (and ppt/code-gen) entirely through the capabilities sourced from the `CompiledWorkflow`: per-step dispatch becomes `resolve("strategy", step.strategy).run(step, ctx)`; deliverable resolution becomes `resolve("deliverable", compiled.deliverable.strategy).resolve(ctx)` reading `deliverable.name`; context injection runs through a generic injector over the `context_provider` capabilities. `execute()` constructs and attaches the `KernelServices` runner handle to `ctx.runner` so the strategies/resolvers/providers reach `_run_agent`/sandbox/`static_check`/`render_check`/serialize/count without importing the kernel. PROVE parity (deliverable byte + semantic event) for all 5 pipelines while the L1–L13 leak definitions are STILL PHYSICALLY PRESENT but now DEAD (unreferenced on the routed path).

CRITICAL strangler constraint (D-05): NOTHING is deleted in this plan. The L4/L8 revision seeding, L10 readback, L7 build dispatch, L2/L9 `_resolve_final_output`, L12 `_build_context_message`, L13 `_extract_html_skeleton`, L3 sanitize, L1/L5/L6 constants/dicts all remain in `engine.py` — but the engine no longer CALLS them on the routed path (the call sites are swapped to the capabilities). `vulture` confirms they are orphaned BEFORE 07-05 deletes them. This is the strangler "rewire" step (§31); 07-05 is the pure "delete" step gated on this plan's green parity.

Honor the parity traps: the authored manifests use `planner: run` + `clarify.mode: auto` (NOT `planner: skip`); `prototype_revision` clarify.defaults fall through to the `custom` pipeline defaults; ALL manifests declare `seed_files: {}` (seeding rides in `task_loop`/`previous_run`, NOT manifest data — do NOT re-author manifests).

Purpose: PARITY-05 (3 prototype flavors run purely from manifests) + PARITY-09 (5-pipeline deliverable + semantic event parity vs post-0C baseline).
Output: the capability-routed `engine.py`, the `KernelServices` handle module, and a routing-parity test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-SPEC.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-CONTEXT.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-RESEARCH.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-PATTERNS.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-01-SUMMARY.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-02-SUMMARY.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-03-SUMMARY.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan (exclude from any source-grounding drift check):
- `KernelServices` (`agents/execution_engine/kernel_services.py`) — the concrete runner-handle object attached as `ctx.runner` by execute(); wraps `_run_agent` (run-one-agent-yielding-events), the per-run sandbox (read/write/path_for/root), `static_check`, `render_check`, `count_sandbox_deliverables`, `serialize_sandbox_deliverable`, and any other engine internal inventoried in 07-01 Task 1
- the generic context injector (a method/function on the engine composing `resolve(context_provider, ...).load(ctx)` block maps in declared order, replacing the per-pipeline `_build_context_message` injection on the routed path)
- Wave 0 test: `tests/agents/test_routing_parity.py`
NOTE: no leak is created or deleted here; the engine's per-step dispatch, deliverable resolution, and context injection call SITES are rewired to the capabilities while the leak DEFINITIONS remain (dead) for 07-05 to remove.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: KernelServices handle + capability-routed per-step dispatch + deliverable resolution</name>
  <files>backend/agents/execution_engine/kernel_services.py, backend/agents/execution_engine/engine.py, backend/tests/agents/test_routing_parity.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:1006-1270 (the `execute()` routing seam + the per-step dispatch :1254-1269 `if getattr(spec,"id",None)=="prototype-build": _run_build_task_loop else _run_agent` — the dispatch this task swaps for `resolve("strategy", step.strategy).run(step, ctx)`)
    - backend/agents/execution_engine/engine.py:1360-1400 (the deliverable-resolution call site + the `_resolve_final_output(pipeline_type, ...)` call :1366 + the final-output carousel-sanitize :1394 — swap for `resolve("deliverable", compiled.deliverable.strategy).resolve(ctx)`)
    - backend/agents/execution_engine/engine.py:1543-1700 (`_run_agent` — what KernelServices wraps; the thread-id shape :1657-1667; the L10 readback :1902 + the mid-stream PPT sanitize :1916 — these are behavioral and move with the strategy/resolver per Pitfall 5, but are NOT deleted here)
    - backend/agents/capabilities/registry.py (the `resolve()` + `install()` seam from 07-01/02/03 — confirm `install()` is invoked before the first resolve on the execute() path; wire the lazy install at engine import or execute() entry)
    - backend/agents/capabilities/strategies/{single_shot,task_loop}.py (the ctx.runner contract the handle must satisfy — the exact attributes/methods)
    - backend/agents/capabilities/deliverables/*.py (the ctx contract the resolvers read — last_streamed/results, sandbox via handle, previous_run-seeded original)
    - backend/agents/workflows/plan.py (`Step.strategy`/`Step.task_source`/`Step.compaction`; `CompiledWorkflow.deliverable.strategy`/`.name`/`.steps`)
    - 07-PATTERNS.md (the engine self-rewire section + the runner handle shared pattern + the yield-the-engine's-event-dicts pattern)
    - 07-RESEARCH.md Architecture diagram (target state) + Pattern 3 (routing already from CompiledWorkflow) + Open Q3 (parity against routed-but-not-deleted state)
    - backend/tests/agents/test_characterization_prototype.py (the parity harness `_drive()` the routing must keep green)
  </read_first>
  <action>
    Create `agents/execution_engine/kernel_services.py` with a `KernelServices` object (the concrete impl of the 07-01 handle contract) constructed inside `execute()` with the per-run sandbox + the bound `_run_agent` primitive + `static_check`/`render_check` (imported here in the KERNEL, NOT in the capability — the capability reaches them through `ctx.runner`) + `count_sandbox_deliverables`/`serialize_sandbox_deliverable`. Expose exactly the attributes/methods the strategies/resolvers/providers call (per the 07-01 inventory): `run_agent(spec, index, ordered_agents, ..., ectx)` returning the existing async generator (preserving the thread-id shape `f"{run_id}:{spec.id}:{task_num}"` vs `f"{run_id}:{spec.id}"`), `sandbox` (read/write/path_for/root), `static_check`, `render_check`, `count_sandbox_deliverables`, `serialize_sandbox_deliverable`. Attach it: `ectx.runner = KernelServices(...)` at execute() entry (after compile_for_run + sandbox construction). `kernel_services.py` MAY import `app.*` and engine internals (it IS kernel) — only the CAPABILITY modules may not.

    Rewire the per-step dispatch in `execute()` (engine.py:1254-1269): replace the `if getattr(spec,"id",None)=="prototype-build"` build-vs-else branch with a single capability dispatch — look up `step.strategy` (from the compiled Step for this agent/step) via `registry.resolve("strategy", step.strategy)` and `async for event in strategy.run(step, ctx): yield event`. NO `spec.id`/`pipeline_type` branch (INV-1). Ensure `install()` is invoked before the first resolve (lazy at engine import or execute() entry — Open Q2).

    Rewire the deliverable resolution (engine.py:1360-1394): replace the `_resolve_final_output(pipeline_type, ...)` call + the inline final-output carousel-sanitize with `registry.resolve("deliverable", compiled.deliverable.strategy).resolve(ctx)` (the resolver reads `deliverable.name` + owns the sanitize for ppt). The legacy `_resolve_final_output` / `_sanitize_carousel_deck_html` DEFINITIONS remain in the file (dead — 07-05 deletes them); just stop calling them on the routed path.

    Do NOT delete any L1–L13 symbol. Do NOT re-author any manifest (parity trap #3). Honor `planner: run` (parity trap #1) and the revision clarify.defaults -> custom fall-through (parity trap #2) — these already flow from `compiled` (Phase 4); the routing must not change them.

    Write `tests/agents/test_routing_parity.py`: drive a scripted prototype build through `execute()` on the ROUTED path and assert the event stream + deliverable match the legacy build (use the characterization `_drive()` harness style); assert the build step ran via `task_loop` and a non-build step via `single_shot` (no `spec.id == "prototype-build"` reached).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_routing_parity.py tests/agents/test_characterization_prototype.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_routing_parity.py -x` exits 0 (routed prototype build matches legacy events + deliverable; build via task_loop, non-build via single_shot)
    - `grep -nE "resolve\(\s*[\"']strategy" backend/agents/execution_engine/engine.py` >= 1 (per-step dispatch routes through the strategy capability)
    - `grep -nE "resolve\(\s*[\"']deliverable" backend/agents/execution_engine/engine.py` >= 1 (deliverable resolution routes through the resolver capability)
    - `grep -cE "ectx\.runner|ctx\.runner|\.runner =" backend/agents/execution_engine/engine.py` >= 1 (KernelServices handle attached)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_od_ppt.py -x` exits 0 (deliverable + event parity for prototype/code-gen/ppt on the routed path)
    - `cd backend && lint_imports` exits 0 (kernel_services may import app/engine; capabilities still don't)
  </acceptance_criteria>
  <done>execute() attaches the KernelServices handle and routes per-step dispatch + deliverable resolution through the capabilities by manifest strategy/name with no spec.id/pipeline_type branch; the legacy dispatch/resolver definitions remain (dead); routing-parity + prototype/code-gen/ppt characterization green.</done>
</task>

<task type="auto">
  <name>Task 2: Generic context injector + route od_prototype/prototype_revision; prove 5-pipeline parity + vulture-orphan the leaks</name>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/test_routing_parity.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:3243-3420 (`_build_context_message` — the per-pipeline od/ppt/build injection branches the generic injector replaces; the `is_first_agent` :3264, `is_build_task_2_plus` :3321, `spec.id == "prototype-build"` :3404 branches; the block ordering the injector must preserve)
    - backend/agents/execution_engine/engine.py:845-922 (the prototype_revision parent-run seeding L4 — now routed through the previous_run provider from 07-02; confirm the routed path seeds via the provider, not the inline block)
    - backend/agents/capabilities/context_providers/{opendesign,previous_run}.py (the `load(ctx)` block maps the generic injector composes in declared order)
    - backend/agents/workflows/plan.py (`Step.context_providers` — the declared provider list per step; the order the injector honors)
    - backend/agents/workflows/prototype/workflow.yaml + prototype_revision/workflow.yaml + ppt/workflow.yaml (the authored manifests — confirm `context_providers: [opendesign]` / `[previous_run]`, `planner: run`, `seed_files: {}`; do NOT edit them)
    - 07-RESEARCH.md Pitfall 2 (seed_files: {}) + Pitfall 5 (L10 readback in _run_agent) + Open Q3 (vulture orphan confirmation) + the opendesign provider output contract
    - backend/tests/agents/test_characterization_od_prototype.py + test_characterization_prototype_revision.py (the parity harness)
  </read_first>
  <action>
    Replace the per-pipeline injection on the routed path with a GENERIC context injector: for each step, compose the context message from `registry.resolve("context_provider", name).load(ctx)` for each name in `step.context_providers` (in declared order), merged with the user brief + prior-agent output routing (the workflow-agnostic mechanics that stay). The injector consumes the providers' `{block-name -> content}` output (07-02) and composes them in declared order — replacing the L12 od/template/example branches on the routed path. od_prototype injects template/design_system via the `opendesign` provider; prototype_revision seeds parent context via the `previous_run` provider + the `seed_files.from_run` behavior (manifests stay `{}` — Pitfall 2). The build-task-2+ compaction-skip logic + the `=== CURRENT TASK ===` injection ride in `task_loop` (07-01), not the kernel. The `_build_context_message` DEFINITION remains (dead — 07-05 deletes it); just stop calling it on the routed path.

    Confirm the routed path no longer reaches the L4 inline revision seeding, the L10 readback (folds into single_file/the strategy per Pitfall 5), or the L12 injection branches for prototype/od_prototype/prototype_revision — these are now served by previous_run/single_file/the injector. Do NOT delete them.

    Extend `test_routing_parity.py` (or add cases) to cover od_prototype (opendesign injection via provider) and prototype_revision (previous_run seed + post-edit validation behavior) on the routed path.

    Run the full 5-pipeline characterization suite to PROVE deliverable byte + semantic event parity vs the post-0C baseline, then run `vulture app/ agents/` to confirm the L1–L13 helpers are now ORPHANED (unreferenced on the routed path) — this is the gate that makes 07-05's deletion pure removal (Open Q3). Record the vulture orphan list in the SUMMARY so 07-05 knows exactly what to delete.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_routing_parity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x` exits 0 (5-pipeline deliverable byte + semantic event parity — PARITY-09)
    - `cd backend && python3.11 -m pytest tests/agents/test_routing_parity.py -x` exits 0 (od_prototype + prototype_revision routed through providers)
    - `grep -rnE "from agents.execution_engine|import agents.execution_engine|from app\.|import app\." backend/agents/capabilities/` returns 0 matches (capabilities still import no kernel/app)
    - `cd backend && vulture app/ agents/` lists the L1–L13 helpers (`_resolve_final_output`, `_sanitize_carousel_deck_html`, `_unwrap_artifact`, `_build_context_message`, `_extract_html_skeleton`, `_run_build_task_loop`, `_run_validation_fix_loop`, `_count_plan_tasks`, `_extract_task_block`, `_write_build_reference_files`, `_load_template_example`) as unused on the routed path (the orphan list recorded for 07-05) — OR they are confirmed unreferenced by a targeted grep of their call sites
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -v` exits 0 (full suite green; L1–L13 still physically present)
    - `cd backend && lint_imports` exits 0
  </acceptance_criteria>
  <done>od_prototype/prototype_revision route through the opendesign/previous_run providers + generic injector; all 5 pipelines at deliverable + semantic event parity vs post-0C; vulture confirms L1–L13 orphaned on the routed path (recorded for 07-05); manifests unedited; nothing deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS/NDJSON boundary -> execute() | the od_context dict is built at the boundary and passed into execute() (Assumption A6); the runner handle is attached inside execute() |
| this run -> parent run (revision routing) | prototype_revision now seeds via the previous_run provider — ownership stays gated (07-02) |
| kernel_services -> capabilities | the handle is the ONLY path from a capability to `_run_agent`/sandbox/validators; the import direction stays kernel->ports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Information Disclosure | prototype_revision routed parent-run seed | mitigate | the routed path seeds via `PreviousRunProvider` whose `assert_owns`-before-seed (07-02) propagates the cross-owner `PermissionError` (ASVS V4 / INV-8); the L16 ratchet stays green. Verified by `test_parent_run_ownership.py` in the wave-merge run. |
| T-07-04-02 | Tampering (runtime integrity) | KernelServices.run_agent | mitigate | the handle wraps the EXISTING `_run_agent`/`create_runner` (deepagents) — no new agent loop; capabilities never call `create_deep_agent` (INV-13). Verified by the capability `create_deep_agent` grep gate (07-01) staying clean. |
| T-07-04-03 | Architecture erosion | capability modules after routing | mitigate | only `kernel_services.py` (kernel) imports `app.*`/engine internals; the capability modules still import neither (ASVS V1). Verified by `lint_imports` + the capability import grep gate. |
| T-07-04-04 | Spoofing/Tampering (parity drift) | routed deliverable/event stream | mitigate | the 5-pipeline characterization suite (byte + semantic event multiset) proves the routed path is identical to the post-0C baseline (INV-3); vulture confirms the legacy path is dead, not silently still-reached. |

No `high`-severity threat: the only sensitive surface (cross-owner revision seed) is gated by the 07-02 provider's propagating `assert_owns`, re-confirmed in the wave run.
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_routing_parity.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -x` green (5-pipeline parity on the routed path)
- `cd backend && python3.11 -m pytest tests/agents/test_parent_run_ownership.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -x` green (L16 + ledger/banned still as-before; L1–L13 present, dead)
- `cd backend && vulture app/ agents/` reports L1–L13 helpers orphaned on the routed path (the deletion list for 07-05)
- `cd backend && lint_imports` green; `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -v` green
</verification>

<success_criteria>
- per-step dispatch + deliverable resolution + context injection route through the capabilities by manifest strategy/name with NO spec.id/pipeline_type branch on the routed path
- prototype, od_prototype, prototype_revision run purely from their compiled manifests (PARITY-05)
- all 5 pipelines at deliverable byte (where deterministic) + semantic event parity vs the post-0C baseline (PARITY-09)
- KernelServices handle attached to ctx.runner; capabilities still import no kernel/app
- L1–L13 leak definitions physically present but DEAD (vulture-orphaned, recorded for 07-05); manifests unedited; nothing deleted (strangler rewire step)
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-04-SUMMARY.md` when done (include the vulture orphan list of L1–L13 helpers for 07-05)
</output>
