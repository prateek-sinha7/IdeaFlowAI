---
phase: 07-prototype-as-manifest-parity-proof-sc-001-2
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/strategies/task_loop.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/tests/agents/test_strategies.py
autonomous: true
gap_closure: true
requirements: [PARITY-09]
must_haves:
  truths:
    - "task_loop.py holds exactly ONE validation-fix-loop implementation — the dead `else` duplicate, `_SkippedRender`, and the duplicated `_select_issues_to_fix`/`_static_issue_sigs`/`_console_sigs` no longer exist (INV-3/INV-12 no-dual-implementations)"
    - "TaskLoopStrategy.run always delegates validation+fix to runner.run_validation_fix_loop (the engine's single implementation) — there is no fallback branch"
    - "test_strategies fix-loop assertions still pass, driven by a _FakeRunner that defines run_validation_fix_loop (re-pointed off the deleted dead branch)"
    - "The KernelServices contract docstring no longer advertises run_fix_agent (an unimplemented method) — the documented surface matches the real one"
    - "The full agents suite stays green (no characterization golden change; this cluster is golden-safe)"
  artifacts:
    - path: "backend/agents/capabilities/strategies/task_loop.py"
      provides: "TaskLoopStrategy with a single validation-fix path (dead else-branch + _SkippedRender + duplicate helpers removed)"
      contains: "class TaskLoopStrategy"
    - path: "backend/tests/agents/test_strategies.py"
      provides: "fix-loop tests re-pointed onto a _FakeRunner.run_validation_fix_loop"
  key_links:
    - from: "backend/agents/capabilities/strategies/task_loop.py"
      to: "runner.run_validation_fix_loop"
      via: "the sole validation+fix delegation in run()"
      pattern: "run_validation_fix_loop"
---

<objective>
Cluster B (golden-safe). Delete the dead DUAL implementation of the validation
fix-loop inside `task_loop.py` — the `else: # pragma: no cover` branch (task_loop.py
:295-302), the in-strategy `_run_validation_fix_loop` (:368-503), the `_SkippedRender`
stand-in (:506-522), and the strategy-local duplicates of `_select_issues_to_fix` /
`_static_issue_sigs` / `_console_sigs` (:92-153) — leaving exactly ONE home for the
fix-loop behavior (the engine's `_run_validation_fix_loop`, reached via
`runner.run_validation_fix_loop`). This is the INV-3/INV-12 "no dual implementations"
fix (findings WR-07, WR-08). Also prune the unimplemented `run_fix_agent` from the
`KernelServices` contract docstring (kernel_services.py:50; the strategy docstring at
task_loop.py:50) so the documented handle surface matches the real one.

Purpose: a phase that adds an abstraction without deleting the code it supersedes is
"not done" (INV-3/INV-12). The dead else-branch + `_SkippedRender` + duplicated helpers
are the exact dual implementation the project invariant forbids.

Output: a single-implementation `task_loop.py`; a re-pointed `test_strategies.py`;
a truthful `KernelServices` contract docstring. No characterization golden changes.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-REVIEW-DEEP.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point test_strategies onto run_validation_fix_loop (RED→GREEN guard before deletion)</name>
  <read_first>
    - backend/tests/agents/test_strategies.py (the `_FakeRunner` at :90-128 — currently defines `run_fix_agent` but NOT `run_validation_fix_loop`, so it exercises the dead `else` branch; the fix-loop assertions at :281-307 read `runner.fix_calls`)
    - backend/agents/capabilities/strategies/task_loop.py (the dead `else` branch :295-302 and the in-strategy `_run_validation_fix_loop` :368-503 the test currently drives)
    - backend/agents/execution_engine/kernel_services.py (:208-255 — the REAL `run_validation_fix_loop` signature the strategy delegates to: `(step, *, task_num, total_tasks, agent_id, baseline_static, baseline_console, user_instruction, label)`)
  </read_first>
  <action>
    Re-point the `_FakeRunner` so the fix-loop tests drive the SAME path real runs take (`runner.run_validation_fix_loop`), NOT the dead in-strategy else-branch. Add an async `run_validation_fix_loop(self, step, *, task_num, total_tasks, agent_id="prototype-build", baseline_static=None, baseline_console=None, user_instruction=None, label="")` method to `_FakeRunner` that reproduces the bounded N=2 decision the tests assert: read the scripted static/render results, compute the fix list with the existing pure selection logic, and for each failing attempt (max 2) append a record to `self.fix_calls` (preserving the existing `{"task_num","attempt","agent_id","message"}` shape the assertions at :305-307 read). Keep the standalone `run_fix_agent` fake ONLY if another test still references it; otherwise remove it. The selection logic must come from the SINGLE engine home — import `_select_issues_to_fix` / `_static_issue_sigs` / `_console_sigs` from `agents.execution_engine.engine` (the same import `test_phase5_fixloop_selection.py:36-39` uses), NEVER from `task_loop`. Run `python3.11 -m pytest tests/agents/test_strategies.py -q` and confirm the fix-loop assertions (`runner.fix_calls == []` clean case; `len(runner.fix_calls) == 2` failing case) pass against the new method while the strategy STILL has its else-branch — this proves the test is re-pointed before deletion.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_strategies.py -q</automated>
  </verify>
  <done>`test_strategies.py` passes with `_FakeRunner.run_validation_fix_loop` defined; `runner.fix_calls` assertions are driven by the new method; no test imports the fix-selection helpers from `task_loop`.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead dual fix-loop + duplicate helpers from task_loop.py; prune the run_fix_agent docstring</name>
  <read_first>
    - backend/agents/capabilities/strategies/task_loop.py (FULL — the `else: # pragma: no cover` at :295-302, the in-strategy `_run_validation_fix_loop` :368-503, `_SkippedRender` :506-522, the duplicated pure helpers `_static_issue_sigs`/`_console_sigs`/`_select_issues_to_fix` :92-153, the `_MAX_FIX_ATTEMPTS` const :78, and the docstring `run_fix_agent` mention :50)
    - backend/tests/agents/test_phase5_fixloop_selection.py (:36-39 — confirms the canonical home of the pure helpers is `agents.execution_engine.engine`, so removing the task_loop copies is safe)
    - backend/agents/execution_engine/kernel_services.py (the docstring at :50-53 mentions `run_fix_agent`; the engine itself owns the fix sub-agent invocation inside `_run_validation_fix_loop`)
  </read_first>
  <action>
    In `task_loop.py`: (1) replace the `if hasattr(runner, "run_validation_fix_loop"): await runner.run_validation_fix_loop(...) else: # pragma: no cover ... self._run_validation_fix_loop(...)` block (:288-302) with the unconditional `await runner.run_validation_fix_loop(step, task_num=task_num, total_tasks=total_tasks, agent_id=agent_id)` call (the real handle ALWAYS defines this method per kernel_services.py:208 — keep the `hasattr` guard ONLY if a still-living test needs the absent-method path; Task 1 removed that need, so delete the guard). (2) Delete the in-strategy `async def _run_validation_fix_loop` (:368-503) in full. (3) Delete the `_SkippedRender` class (:506-522). (4) Delete the strategy-local `_static_issue_sigs`/`_console_sigs`/`_select_issues_to_fix` (:92-153) and the now-unused `_MAX_FIX_ATTEMPTS` const + the `time`/`_now` usages only if they become orphaned (keep `_now` if `task_loop_progress` still uses it — it does at :260, so KEEP it). (5) Remove the `run_fix_agent(...)` bullet from the strategy's `ctx.runner` contract docstring (:50-53) since the fix sub-agent is owned internally by the engine's `_run_validation_fix_loop`, never called by the strategy. In `kernel_services.py`: remove the `run_fix_agent` reference from the class/contract docstring (:50 region) — `KernelServices` defines no such method (WR-08); if you prefer to keep documenting the internal fix invocation, describe it as "owned inside `run_validation_fix_loop`," not as a public handle method. Then run `vulture agents/capabilities/strategies/task_loop.py` to confirm no newly-orphaned symbol remains, and run the full strategies + characterization suites to prove golden-safety.
  </action>
  <verify>
    <automated>cd backend && grep -c "_SkippedRender\|def _run_validation_fix_loop\|def _select_issues_to_fix\|def _static_issue_sigs\|def _console_sigs" agents/capabilities/strategies/task_loop.py | grep -qx 0 && grep -L "run_fix_agent" agents/execution_engine/kernel_services.py >/dev/null && python3.11 -m pytest tests/agents/test_strategies.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py -q</automated>
  </verify>
  <done>`task_loop.py` contains zero matches for `_SkippedRender`, the in-strategy `_run_validation_fix_loop`, or the duplicated fix-selection helpers; neither `task_loop.py` nor `kernel_services.py` docstrings mention `run_fix_agent` as a handle method; the strategies tests and the 3 prototype characterization tests pass (goldens unchanged).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- (removed) `task_loop._run_validation_fix_loop`, `task_loop._SkippedRender`, `task_loop._select_issues_to_fix`/`_static_issue_sigs`/`_console_sigs`, `task_loop._MAX_FIX_ATTEMPTS` — the dead dual implementation.
- (modified) `_FakeRunner.run_validation_fix_loop` in `test_strategies.py` — the re-pointed fix-loop test double.
- (no new public symbols)
</artifacts_this_phase_produces>

<threat_model>
Internal, parity-preserving deletion of dead code + a test-double re-point. No new
external attack surface, no new package installs, no boundary or trust-tier change. The
only behavioral risk is a parity regression masked by the deletion; mitigated by Task 1's
RED→GREEN re-point (assert the live path before deleting the dead one) and Task 2's
characterization-suite gate (goldens must stay byte-identical — cluster B is golden-safe).
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | deleting a still-reachable branch | mitigate | Task 1 re-points the only test that drove the else-branch BEFORE Task 2 deletes it; vulture confirms orphan-only removal |
| T-07-07-02 | Repudiation | silent parity drift | mitigate | characterization suite (golden-safe) must stay green in the verify gate |
</threat_model>

<verification>
- `grep -c "_SkippedRender" agents/capabilities/strategies/task_loop.py` returns 0.
- `grep -rn "from agents.capabilities.strategies.task_loop import" tests/agents/` returns no import of the fix-selection helpers.
- `python3.11 -m pytest tests/agents/test_strategies.py tests/agents/test_phase5_fixloop_selection.py -q` passes.
- `python3.11 -m pytest tests/agents/test_characterization_*.py -q` passes with NO golden modification (cluster B is golden-safe).
</verification>

<success_criteria>
task_loop.py holds exactly one validation-fix path (delegated to the engine); the dead
else-branch, `_SkippedRender`, and the duplicated pure helpers are gone; `run_fix_agent`
is no longer advertised on the KernelServices contract; the strategies + characterization
suites are green with unchanged goldens.
</success_criteria>

<output>
Create `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/07-07-SUMMARY.md` when done
</output>
