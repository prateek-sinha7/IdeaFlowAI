---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/RunChatLane.tsx
autonomous: true
requirements: [W3, "SC-001", "INV-1"]
must_haves:
  truths:
    - "On a SETTLED run (runState==complete) a free-text CHANGE request no longer auto-launches a revision; it surfaces a 'Run a refinement with this change?' confirm chip."
    - "The refinement (*_revision) launch fires ONLY after the user confirms the chip; rejecting/dismissing it launches nothing."
    - "An ASK turn is still answered by the Concierge (concierge:true) with no chip."
  artifacts:
    - path: "frontend/src/components/chat/RunChatLane.tsx"
      provides: "confirm-first refinement chip held before onRevise; reuses the existing proposal-chip render path"
      contains: "onRevise"
  key_links:
    - from: "frontend/src/components/chat/RunChatLane.tsx handleFreeText"
      to: "held refinement proposal -> confirm -> onRevise"
      via: "classifyFreeText change branch"
      pattern: "classifyFreeText"
---

<objective>
W3a — the confirm-first refinement UX (locked decision 5). Today `handleFreeText`
(`RunChatLane.tsx:686-701`) routes a settled-run CHANGE request straight to
`onRevise` (auto-launch). Replace that with a held "Run a refinement with this
change?" confirm chip; the revision launches only on explicit confirm. The
classifier stays GENERIC (INV-1 / SC-001) — keyed on free text + runState, never
a workflow-name or agent-id literal.

Purpose: users lose work to accidental auto-launched revisions; a confirm gate
makes refinement intentional. This is file-disjoint from the pipeline/command
rewire (W1/W2/W3b), so it runs in wave 1 in parallel.
Output: a confirm chip gating every settled-run refinement launch.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@frontend/src/components/chat/RunChatLane.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hold settled-run change requests behind a confirm chip</name>
  <files>frontend/src/components/chat/RunChatLane.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunChatLane.tsx (handleFreeText :686-701 — the change branch calls onRevise immediately; classifyFreeText :237; the existing proposal/confirm-chip infra: LaneProposal :113, renderProposals :719, onConfirmProposal :743; runState typing)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (decision 5, SC-001/INV-1)
  </read_first>
  <action>
    In `handleFreeText`, keep the `runState === "complete"` + `classifyFreeText(text) === "ask"`
    branch (Concierge, `{ concierge: true }`) unchanged. For the CHANGE branch, instead of
    calling `onRevise(text)` immediately, set a LOCAL held-refinement state carrying the
    instruction text and render a confirm chip labelled "Run a refinement with this change?"
    Reuse the existing confirm-chip render path (`renderProposals` / the Card+confirm
    button pattern at :719-745) rather than inventing a new component — a held refinement
    is structurally a local proposal. Confirm calls `onRevise(text)` and clears the held
    state; reject/dismiss clears it and launches nothing. Keep classification GENERIC:
    no `workflowType`/agent-id/pipeline-name literal appears in the new branch (only
    `runState` + the classifier). Preserve the existing fallbacks: when `onRevise` is
    absent a change falls back to a plain `sendMessage`; the terminal-lane
    `handleTerminalRevise` path is unchanged.
  </action>
  <acceptance_criteria>
    - `grep -n "onRevise(" frontend/src/components/chat/RunChatLane.tsx` shows `onRevise` is called ONLY from a confirm handler, never directly inside the `classifyFreeText === change` branch of `handleFreeText`.
    - The confirm chip text "Run a refinement with this change?" (or the exact copy chosen) is present and rendered when a settled-run change is held.
    - No workflow-name / agent-id literal is introduced in the changed code (source grep: the new branch references only `runState`, `classifyFreeText`, the held text — SC-001/INV-1).
    - `cd frontend && npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>A settled-run change request holds a confirm chip; onRevise fires only on confirm.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the confirm-gate behavior in the mocked lane test</name>
  <files>frontend/src/components/chat/RunChatLane.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunChatLane.tsx (the changed handleFreeText + chip render)
    - any existing RunChatLane unit/vitest spec (grep for RunChatLane.test / .spec near the component) to extend the same harness; if none exists, add a colocated vitest spec
  </read_first>
  <action>
    Add (or extend) a vitest spec asserting: (a) a settled-run ASK turn calls `sendMessage`
    with `{ concierge: true }` and renders NO refinement chip; (b) a settled-run CHANGE
    turn does NOT call `onRevise` on submit — it renders the confirm chip; (c) clicking
    confirm calls `onRevise(text)` exactly once; (d) dismiss/reject calls neither
    `onRevise` nor a revision `sendMessage`. Run the spec from the `frontend/` directory
    (cwd-sensitive vitest, per the project note). Keep the test file name aligned with the
    existing convention.
  </action>
  <acceptance_criteria>
    - The new/updated vitest spec passes (confirm launches once, dismiss launches nothing, ask stays Concierge).
    - `cd frontend && npx vitest run <spec>` green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat</automated>
  </verify>
  <done>The confirm-gate behavior is regression-locked by a green vitest spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user free-text -> revision launch | a settled-run change request that triggers a *_revision run |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Elevation of privilege | auto-launched revision | mitigate | the confirm chip forces explicit user intent before any *_revision launch — removes the accidental/auto launch surface |
| T-44-02-02 | Tampering (XSS) | held refinement text rendered in the chip | mitigate | render the instruction through React default JSX escaping (no raw-HTML sink), matching the existing proposal-chip path (T-33-04-02) |
| T-44-02-SC | Tampering | package installs | accept | no npm package installs in this plan |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- `cd frontend && npx vitest run src/components/chat` green.
- No backend change — goldens/wire-parity unaffected.
</verification>

<success_criteria>
Settled-run change requests are gated by a confirm chip; onRevise fires only on
confirm; ASK turns stay Concierge; classifier stays generic. tsc + vitest green.
</success_criteria>

## Artifacts this phase produces
- `RunChatLane.tsx`: held-refinement confirm chip gating `onRevise`.
- A vitest spec locking the confirm-gate behavior (confirm launches once / dismiss launches nothing).

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-02-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
