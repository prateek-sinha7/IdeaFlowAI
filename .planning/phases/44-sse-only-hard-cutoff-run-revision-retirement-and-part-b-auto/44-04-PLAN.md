---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 04
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/hooks/useWorkflow.ts
autonomous: true
requirements: [W2, "WR-03"]
must_haves:
  truths:
    - "Gate actions (approve+edited_content / reject / redo+instructions / update_specs) POST to /api/runs/{id}/gate — NOT /messages — so edited_content is preserved (WR-03)."
    - "Cancel POSTs to /api/runs/{id}/cancel with the run_id threaded (WS was connection-scoped and carried no run_id)."
    - "Questionnaire submit no longer 422s: it carries a message_id (or targets POST /{id}/answers) so MessageCommand validates."
    - "Cross-owner/missing run on /gate or /cancel returns 404 (IDOR->404), matching the WS owner fence."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "typed fetchers: postGate, postCancel, postAnswers (owner-scoped run commands)"
      contains: "/gate"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "gate handlers call postGate (not the WS approve_review send)"
      contains: "gate"
  key_links:
    - from: "frontend/src/app/dashboard/page.tsx onApproveReview/onRejectReview/onRedoReview/onUpdateSpecsReview"
      to: "POST /api/runs/{id}/gate"
      via: "api.ts postGate helper"
      pattern: "/gate"
    - from: "frontend/src/components/layout/DashboardLayout.tsx cancel"
      to: "POST /api/runs/{id}/cancel"
      via: "api.ts postCancel helper (run_id threaded)"
      pattern: "/cancel"
---

<objective>
W2 — rewire the un-flagged WS commands (gate / cancel / questionnaire) to REST.
These have WS as their ONLY transport today (raw `send`), so the hard cutoff
breaks HITL gates, cancel, and clarify until rewired. Gates route to
`POST /{id}/gate` (which carries `edited_content`, WR-03) — NOT `/messages`
(whose CHANNEL_GATE branch drops `edited_content` at run_commands.py:851).

Purpose: make every command ride REST so W4 can delete the WS transport.
Sequenced after 44-01 because it shares dashboard/page.tsx + DashboardLayout.tsx.
Output: gate/cancel/questionnaire over REST; edited_content preserved; owner-scoped.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@frontend/src/lib/api.ts
@backend/app/api/run_commands.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the owner-scoped REST command fetchers in api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (the `request` helper + existing run fetchers; no /gate|/cancel|/answers callers exist yet)
    - backend/app/api/run_commands.py (GateCommand :95-110 with edited_content; POST /{id}/gate :162 + set_review_response with edited_content :233,:238; POST /{id}/answers :249; POST /{id}/cancel :282)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W2, WR-03)
  </read_first>
  <action>
    Add three typed fetchers to `api.ts` mirroring the backend request bodies exactly:
    `postGate(token, runId, { gate_key, action, approved?, edited_content?, instructions?, analysis_report? })`
    -> `POST /api/runs/{runId}/gate`; `postCancel(token, runId)` -> `POST /api/runs/{runId}/cancel`;
    `postAnswers(token, runId, { responses, skip_clarification? })` -> `POST /api/runs/{runId}/answers`.
    Reuse the existing `request`/auth-header machinery. The four gate UI actions map onto
    the single `action` discriminator: approve (optional edited_content), reject
    (approved:false), redo (instructions), update_specs (analysis_report/instructions) —
    matching GateCommand. Do not invent fields the backend does not accept.
  </action>
  <acceptance_criteria>
    - `grep -n "runs/\${.*}/gate\|runs/\${.*}/cancel\|runs/\${.*}/answers" frontend/src/lib/api.ts` shows the three endpoints wired.
    - `postGate` accepts `edited_content` and forwards it in the body (source assertion).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>api.ts exposes postGate/postCancel/postAnswers targeting the owner-scoped run-command endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Route the four gate actions through POST /{id}/gate (preserve edited_content)</name>
  <files>frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/page.tsx (onApproveReview :1488-1497, onRejectReview :1498-1501, onRedoReview :1502-1510, onUpdateSpecsReview :1511-1526 — all currently `send(JSON.stringify({type:"approve_review",...}))`; reviewGateData.pipelineRunId :166 carries the target run)
    - frontend/src/lib/api.ts (postGate from Task 1)
    - backend/app/api/run_commands.py (why /gate not /messages: CHANNEL_GATE :824-851 drops edited_content)
  </read_first>
  <action>
    Replace the four `send(JSON.stringify({ type: "approve_review", ... }))` calls with
    `postGate(...)` against the gate's run id (`reviewGateData.pipelineRunId`): approve ->
    `{ gate_key, action:"approve", approved:true, edited_content }`; reject ->
    `{ gate_key, action:"reject", approved:false }`; redo ->
    `{ gate_key, action:"redo", instructions }`; update_specs ->
    `{ gate_key, action:"update_specs", analysis_report }`. Preserve the existing
    side-effects (pendingGateEditRef stash for approve-with-edits; setReviewGateData(null)
    clears) — only the transport changes. The gate-resolution ack + re-emitted
    review_gate_ready arrive over the SSE down-channel (already wired in 44-01).
    Do NOT route gates through `/messages` (it drops edited_content).
  </action>
  <acceptance_criteria>
    - `grep -c "type: \"approve_review\"" frontend/src/app/dashboard/page.tsx` returns 0 (no WS approve_review send remains).
    - The approve handler forwards `edited_content` to `postGate` (source assertion — WR-03 preserved).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>All four gate actions POST to /{id}/gate carrying edited_content; no WS approve_review send.</done>
</task>

<task type="auto">
  <name>Task 3: Cancel over REST (thread run_id) + fix the questionnaire 422</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/hooks/useWorkflow.ts</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (cancel sends :1234 and :1328 — `{type:"cancel_pipeline"}` with NO run_id; activePipelineRunId in scope)
    - frontend/src/hooks/useWorkflow.ts (submitQuestionnaire :152-168 — posts to /messages via sendCommand with NO message_id -> 422; run_pipeline payload :71-129)
    - backend/app/api/run_commands.py (POST /{id}/cancel :282; POST /{id}/answers :249; MessageCommand requires message_id :335)
  </read_first>
  <action>
    Cancel: replace both `websocketSend(JSON.stringify({ type: "cancel_pipeline" }))` sites
    (:1234, :1328) with `postCancel(token, runId)` where runId is the active pipeline run id
    (thread it explicitly — the WS frame was connection-scoped and carried no id). Preserve
    the surrounding flow (the :1228-1237 unblock-then-cancel sequence; handleStopPipeline).
    Questionnaire: fix the R4 422 by routing `submitQuestionnaire` to `postAnswers(token,
    pipelineRunId, { responses, skip_clarification })` (POST /{id}/answers) — the answers
    endpoint takes responses without a message_id, avoiding the MessageCommand 422 the
    /messages path caused. Keep the ISS-027 skip_clarification passthrough. Leave the
    run_pipeline launch on its existing sendCommand(null, payload) path (44-01 already
    made SSE the transport). Do not delete the flag branches here (W4 owns that).
  </action>
  <acceptance_criteria>
    - `grep -c "cancel_pipeline" frontend/src/components/layout/DashboardLayout.tsx` returns 0 (no WS cancel frame).
    - Cancel calls `postCancel` with a concrete run id (source assertion — run_id threaded).
    - `submitQuestionnaire` posts to `/answers` (or carries a message_id) — no bare `/messages` submit without message_id (source assertion; fixes the 422).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Cancel + questionnaire ride REST with the run_id threaded; the questionnaire 422 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> POST /api/runs/{id}/gate,/cancel,/answers | untrusted command input crosses; must be owner + terminal scoped |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04-01 | Elevation of privilege (IDOR) | /gate, /cancel, /answers for a run the caller does not own | mitigate | the FE targets the owner-scoped Phase-29 endpoints unchanged (run_commands owner/terminal fences: cross-owner/missing -> 404). Add/keep a REST-path test asserting cross-owner /gate + /cancel -> 404 (not 403) |
| T-44-04-02 | Tampering / data integrity | edited_content dropped if gates route via /messages (WR-03) | mitigate | route the four gate actions through POST /{id}/gate (carries edited_content at set_review_response :233,:238); never /messages CHANNEL_GATE (drops it :851) |
| T-44-04-03 | Denial of service | cancel targeting an arbitrary run id | mitigate | /cancel is owner+terminal scoped server-side; the FE threads only the user's own active run id |
| T-44-04-SC | Tampering | package installs | accept | no npm installs |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; no residual `approve_review`/`cancel_pipeline` WS sends (grep 0).
- Backend owner-scoping already covered by `tests/unit/test_rest_gate_commands.py` + `test_rest_answers_cancel.py` (cross-owner -> 404); confirm green.
- Mocked e2e gate/cancel/clarify flows re-pointed + green in W5 (44-09).
</verification>

<success_criteria>
Gate (with edited_content), cancel (run_id threaded), and questionnaire all ride
owner-scoped REST; the questionnaire 422 is fixed; no WS command sends remain in
these handlers. tsc clean.
</success_criteria>

## Artifacts this phase produces
- `api.ts`: `postGate` (WR-03 edited_content), `postCancel`, `postAnswers` fetchers.
- `dashboard/page.tsx`: gate handlers on /gate; `DashboardLayout.tsx`: cancel on /cancel; `useWorkflow.ts`: questionnaire on /answers.

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-04-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
