---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 07
type: execute
wave: 5
depends_on: ["44-03", "44-06"]
files_modified:
  - backend/app/api/websocket.py
  - backend/app/api/run_stream.py
  - backend/app/core/config.py
  - backend/app/main.py
autonomous: true
requirements: [W4, W3, "C.3", "D1", "CTX-04", "INV-12", "INV-3"]
must_haves:
  truths:
    - "The /ws/chat endpoint (websocket_chat receive-loop), its framing/drainer, the run_pipeline WS driver, and the run_revision WS handler + _handle_revision_execution driver are DELETED; no /ws/chat route is mounted."
    - "engine._handle_revision is KEPT (the REST /revisions byte-twin uses it); only the WS driver is removed."
    - "The SSE_TRANSPORT_ENABLED backend flag + the run_stream 404 feature-gate are removed; the SSE stream is unconditional."
    - "restore_non_terminal_runs startup wiring still works (resume registrars source from run_engine); /ws/handoff is untouched; the 5 goldens + test_wire_parity stay byte/event-identical."
  artifacts:
    - path: "backend/app/main.py"
      provides: "no websocket_router mount; resume registrars from run_engine"
      contains: "run_engine"
    - path: "backend/app/api/run_stream.py"
      provides: "SSE stream unconditional (no SSE_TRANSPORT_ENABLED 404 guard)"
  key_links:
    - from: "backend/app/main.py"
      to: "run_engine resume registrars"
      via: "restore_non_terminal_runs wiring"
      pattern: "restore_non_terminal_runs"
---

<objective>
W4 (backend half) + W3c — the INV-12 exit gate. Delete the `/ws/chat` endpoint,
its WS framing/drainer, the `run_pipeline` driver (`_handle_workflow_execution`),
and the `run_revision` WS handler (:1279) + `_handle_revision_execution` driver
(:2411). KEEP `engine._handle_revision` — the REST `/revisions` twin uses it.
Remove the backend `SSE_TRANSPORT_ENABLED` flag + the `run_stream` 404 feature
gate so the SSE stream is unconditional. `/ws/handoff` (websocket_handoff.py)
is untouched.

Purpose: no dual transport survives on the backend. Sequenced after 44-03
(relocation done) and 44-06 (FE makes zero WS connections) so deleting the
endpoint is safe. INV-3: re-run the 5 goldens + wire-parity after deletion.
Output: /ws/chat gone; run_revision retired; SSE unconditional; goldens green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/CLAUDE.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@backend/app/api/websocket.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the /ws/chat endpoint + run_pipeline/run_revision WS handlers + drivers</name>
  <files>backend/app/api/websocket.py, backend/app/main.py</files>
  <read_first>
    - backend/app/api/websocket.py (router :28; websocket_chat endpoint :704; the receive-loop dispatch: run_pipeline :828/903, cancel :924, reconnect :971, run_revision :1279, questionnaire :1342, approve_review :1367; _handle_workflow_execution :1664; _handle_revision_execution :2411; and the transitional re-import of the relocated symbols added by 44-03)
    - backend/app/main.py (websocket_router import :22; mount :190; the run_engine resume-registrar wiring from 44-03 :141-145)
    - backend/agents/execution_engine/engine.py (_handle_revision :5045 — KEEP; only the WS driver in websocket.py is deleted)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W4 R3; W3 delete list; INV-12)
  </read_first>
  <action>
    Delete from `websocket.py`: the `websocket_chat` receive-loop endpoint (:704+) and its
    `@router.websocket("/ws/chat")` route, all in-endpoint frame handlers (run_pipeline,
    cancel, reconnect, run_revision, questionnaire, approve_review), the WS framing/drainer
    helpers, and the drivers `_handle_workflow_execution` (:1664) and
    `_handle_revision_execution` (:2411). Since 44-03 already relocated the transport-neutral
    symbols to run_engine.py (websocket.py only re-imports them), after removing the endpoint
    the module has no remaining purpose — DELETE `backend/app/api/websocket.py` entirely IF it
    holds nothing but the now-dead endpoint + re-imports (confirm no other live symbol lives
    only here). In `main.py` remove the `websocket_router` import (:22) and its `include_router`
    mount (:190). Confirm the resume-registrar wiring (:141-145) already points at
    `app.api.run_engine` (44-03) so `restore_non_terminal_runs()` still runs. Do NOT touch
    `websocket_handoff.py` / the handoff router mount (:191). KEEP `engine._handle_revision`.
  </action>
  <acceptance_criteria>
    - `grep -rn "/ws/chat\|websocket_chat\|routeWebSocket" backend/app` returns 0 matches (endpoint + route gone; /ws/handoff unaffected).
    - `grep -rn "_handle_revision_execution\|_handle_workflow_execution\|\"run_revision\"" backend/app` returns 0 matches (WS drivers + handler gone).
    - `grep -c "async def _handle_revision" backend/agents/execution_engine/engine.py` returns 1 (engine._handle_revision KEPT).
    - `grep -rn "websocket_router" backend/app/main.py` returns 0 (mount removed); `grep -c "handoff_router" backend/app/main.py` is >= 1 (handoff still mounted).
    - `cd backend && python3.11 -c "import app.main"` imports cleanly (startup wiring resolves).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -c "import app.main" && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>/ws/chat + run_pipeline/run_revision WS handlers + drivers deleted; engine._handle_revision kept; handoff intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove SSE_TRANSPORT_ENABLED flag + the run_stream 404 feature gate</name>
  <files>backend/app/core/config.py, backend/app/api/run_stream.py</files>
  <read_first>
    - backend/app/core/config.py (SSE_TRANSPORT_ENABLED :126; SSE_STREAM_IDLE_TIMEOUT_SECONDS :135 — IN-01, opportunistic)
    - backend/app/api/run_stream.py (the SSE_TRANSPORT_ENABLED 404 guard :220-224; the stream endpoint :202; the two-layer owner 404 check that STAYS)
  </read_first>
  <action>
    Delete `SSE_TRANSPORT_ENABLED` from `config.py:126`. In `run_stream.py` remove the
    `if not settings.SSE_TRANSPORT_ENABLED: raise HTTPException(404 ...)` feature gate
    (:220-224) so the SSE stream is unconditional. Do NOT weaken the two-layer owner check
    (the IDOR->404 fences) that immediately follows — those STAY. Leave
    `SSE_STREAM_IDLE_TIMEOUT_SECONDS` (IN-01) as-is unless trivially enforceable in
    `_iter_sse_frames`; it is opportunistic and NOT a cutoff blocker — do not block on it.
  </action>
  <acceptance_criteria>
    - `grep -rn "SSE_TRANSPORT_ENABLED" backend` returns 0 matches (flag gone from config + consumers).
    - `run_stream.py` still enforces the two-layer owner 404 (grep shows the ScopedStore/user_id 404 checks remain).
    - `cd backend && python3.11 -c "import app.core.config, app.api.run_stream"` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_sse_stream.py tests/agents/test_attach_replay_matrix.py -q</automated>
  </verify>
  <done>The BE flag + 404 feature gate are gone; SSE is unconditional; owner fences intact.</done>
</task>

<task type="auto">
  <name>Task 3: INV-3 gate + handoff-contract after the deletion</name>
  <files>backend/app/api/run_stream.py</files>
  <read_first>
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (INV-3 — 5 goldens + test_wire_parity MUST stay byte/event-identical after the deletion; decision 2 — handoff untouched)
    - backend/tests/agents/test_wire_parity.py; backend/tests/integration/test_handoff_contract.py
  </read_first>
  <action>
    Run the post-deletion INV-3 gate: the 5 characterization goldens and
    `test_wire_parity.py` must still pass (the SSE frames still match the frozen WS
    goldens — the cutover changed transport, not engine bytes). Run
    `test_handoff_contract.py` to prove `/ws/handoff` is unaffected. Confirm the REST
    twins (`test_rest_revisions.py`, `test_rest_gate_commands.py`, `test_rest_answers_cancel.py`,
    `test_rest_run_launch.py`) still pass (the surviving command surface). Confirm NO new
    migration (chain stays 0025). Do NOT run the full suite (hangs offline). This is a
    verification task; any failure means the deletion changed behavior and must be fixed.
  </action>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/ -k characterization -q` -> 10 passed (goldens byte-identical).
    - `test_wire_parity.py` passes (SSE frames == frozen WS goldens).
    - `test_handoff_contract.py` passes (handoff unaffected).
    - The 4 `test_rest_*` twins pass (surviving command surface).
    - `ls backend/alembic/versions | wc -l` unchanged (chain 0025 — no new migration).
    - `/opt/homebrew/bin/lint-imports` 4/0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/ -k characterization tests/agents/test_wire_parity.py tests/integration/test_handoff_contract.py tests/unit/test_rest_revisions.py tests/unit/test_rest_gate_commands.py tests/unit/test_rest_answers_cancel.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Goldens + wire-parity + handoff-contract + REST twins green; migration chain 0025; lint 4/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> SSE stream / REST commands | the sole run surface after /ws/chat is deleted; owner-scoped |
| external IDE -> /ws/handoff | untouched survivor (separate module + router) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07-01 | Elevation of privilege | deleting /ws/chat removing an owner check the surviving surface relies on | mitigate | the SSE stream (run_stream two-layer 404) + REST commands (run_commands owner/terminal fences) self-enforce ownership independently of the WS endpoint; the 404 feature-gate removal does NOT touch those checks (asserted in Task 2 acceptance) |
| T-44-07-02 | Spoofing | breaking /ws/handoff auth by deleting websocket.py | mitigate | _authenticate_token/_get_db already relocated to run_engine (44-03); handoff imports from there; test_handoff_contract.py green proves auth intact |
| T-44-07-03 | Tampering | engine byte-drift during the endpoint/driver deletion | mitigate | INV-3 gate — 5 goldens + test_wire_parity must stay byte/event-identical; engine._handle_revision KEPT unchanged |
| T-44-07-SC | Tampering | package installs | accept | no pip installs (deletion only) |
</threat_model>

<verification>
- `grep -rn "/ws/chat\|websocket_chat\|_handle_revision_execution\|SSE_TRANSPORT_ENABLED" backend/app` -> 0 (only /ws/handoff survives).
- 5 characterization goldens + `test_wire_parity.py` + `test_handoff_contract.py` + the 4 `test_rest_*` twins green.
- `/opt/homebrew/bin/lint-imports` 4/0; migration chain 0025.
</verification>

<success_criteria>
/ws/chat endpoint + run_pipeline/run_revision WS drivers deleted; engine._handle_revision
kept; SSE unconditional; /ws/handoff untouched; goldens + wire-parity byte/event-identical.
</success_criteria>

## Artifacts this phase produces
- DELETED `backend/app/api/websocket.py` (endpoint + WS drivers); `SSE_TRANSPORT_ENABLED` flag + run_stream 404 gate removed.
- `main.py`: no websocket_router mount; resume registrars from run_engine.
- Ledger row D1 (run_revision) marked retired.

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-07-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
