---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 06
type: execute
wave: 4
depends_on: ["44-01", "44-04", "44-05"]
files_modified:
  - frontend/src/hooks/useHandoffSocket.ts
  - frontend/src/components/handoff/HandoffWorkflow.tsx
  - frontend/src/app/workflow/page.tsx
  - frontend/src/components/workflow/WorkflowView.tsx
  - frontend/src/lib/env.ts
  - frontend/src/providers/RunConnectionProvider.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/hooks/useWorkflow.ts
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/hooks/useWebSocket.ts
autonomous: true
requirements: [W4, "C.3", "INV-12"]
must_haves:
  truths:
    - "The frontend makes ZERO /ws/chat WebSocket connections: useWebSocket.ts is deleted and has no remaining src consumers."
    - "The NEXT_PUBLIC_SSE_TRANSPORT flag is gone entirely (resolveSseTransport/SSE_TRANSPORT, WS_URL/resolveWsUrl removed); SSE is the sole, unconditional transport."
    - "The /ws/handoff FE surface still works via a dedicated useHandoffSocket hook (HandoffWorkflow reconnects to /ws/handoff, functionally unchanged)."
    - "The vestigial dead /workflow WS connection is removed."
  artifacts:
    - path: "frontend/src/hooks/useHandoffSocket.ts"
      provides: "the D10 survivor WS hook for /ws/handoff (extracted from useWebSocket)"
      contains: "handoff"
    - path: "frontend/src/lib/env.ts"
      provides: "no WS_URL / SSE_TRANSPORT flag; SSE-only"
  key_links:
    - from: "frontend/src/components/handoff/HandoffWorkflow.tsx"
      to: "frontend/src/hooks/useHandoffSocket.ts"
      via: "handoff WS to /ws/handoff/{token}"
      pattern: "useHandoffSocket"
    - from: "frontend/src/providers/RunConnectionProvider.tsx"
      to: "SSE always-on"
      via: "enabled unconditionally true"
      pattern: "enabled"
---

<objective>
W4 (frontend half) — remove the transport flag and delete the WS `/ws/chat`
client wholesale. This is the FE INV-12 exit gate: `useWebSocket.ts` and the
`NEXT_PUBLIC_SSE_TRANSPORT` flag are DELETED, SSE becomes the sole unconditional
transport. IMPORTANT (verified, missed by the CONTEXT's W4 note):
`HandoffWorkflow.tsx` also consumes the shared `useWebSocket` for the SURVIVING
`/ws/handoff` (decision 2). So this plan first EXTRACTS a dedicated
`useHandoffSocket` for the handoff surface, then deletes `useWebSocket.ts`.

Purpose: no dual transport survives on the FE. Sequenced after 44-01/44-04/44-05
so the pipeline down-channel + all commands + revision already ride SSE/REST
before the WS client is removed.
Output: SSE-only FE; flag gone; handoff survivor intact; dead /workflow WS gone.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@frontend/src/hooks/useWebSocket.ts
@frontend/src/components/handoff/HandoffWorkflow.tsx
@frontend/src/lib/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract useHandoffSocket for /ws/handoff + remove the dead /workflow WS</name>
  <files>frontend/src/hooks/useHandoffSocket.ts, frontend/src/components/handoff/HandoffWorkflow.tsx, frontend/src/app/workflow/page.tsx, frontend/src/components/workflow/WorkflowView.tsx</files>
  <read_first>
    - frontend/src/hooks/useWebSocket.ts (the full hook — connect/backoff/JWT-expired 4001 close; ConnectionStatus type :8-13; UseWebSocketReturn :21-27)
    - frontend/src/components/handoff/HandoffWorkflow.tsx (wsUrl -> /ws/handoff/{token} :155-158; useWebSocket call :179-183; wsBridge :165-176)
    - frontend/src/app/workflow/page.tsx (:32-33 vestigial dead useWebSocket to ENV.WS_URL); frontend/src/components/workflow/WorkflowView.tsx (:89 the dead /workflow connection consumer)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (decision 2 — /ws/handoff STAYS; W4 dead /workflow removal)
  </read_first>
  <action>
    Create `frontend/src/hooks/useHandoffSocket.ts` — a minimal WS hook dedicated to the
    `/ws/handoff/{token}` survivor, carrying over ONLY what HandoffWorkflow needs from
    `useWebSocket` (connect + onMessage + token + the JWT-4001 close handling), and
    RE-EXPORT the `ConnectionStatus` type from here (DashboardLayout still imports that
    type). Point `HandoffWorkflow.tsx` at `useHandoffSocket` (behavior byte-identical: same
    URL derivation :155-158, same wsBridge, same wsActive gating). Remove the vestigial dead
    `/workflow` WS: delete the `useWebSocket` connection in `workflow/page.tsx:32-33` and its
    consumer wiring in `WorkflowView.tsx:89` (if the whole `/workflow` route/view is dead per
    the CONTEXT, remove the dead connection but keep any still-rendered UI compiling — verify
    no live caller). Do NOT delete `useWebSocket.ts` yet (Task 3, after its dashboard consumer
    is gone). The banned-pattern gate (44-10) will ban `useWebSocket`/`routeWebSocket`;
    `useHandoffSocket` + `/ws/handoff` are the sanctioned survivors and must NOT match.
  </action>
  <acceptance_criteria>
    - `useHandoffSocket.ts` exists and connects to `/ws/handoff/{token}` (grep shows `/ws/handoff`).
    - `HandoffWorkflow.tsx` imports `useHandoffSocket`, not `useWebSocket` (grep 0 for `useWebSocket` in HandoffWorkflow.tsx).
    - `workflow/page.tsx` + `WorkflowView.tsx` no longer open a `/ws/chat`/`ENV.WS_URL` WS (grep 0 for `useWebSocket`/`WS_URL` there).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The handoff survivor rides useHandoffSocket; the dead /workflow WS is gone; useWebSocket.ts still present for the dashboard until Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the NEXT_PUBLIC_SSE_TRANSPORT flag (env, provider, layout, useWorkflow)</name>
  <files>frontend/src/lib/env.ts, frontend/src/providers/RunConnectionProvider.tsx, frontend/src/app/layout.tsx, frontend/src/hooks/useWorkflow.ts</files>
  <read_first>
    - frontend/src/lib/env.ts (resolveWsUrl :38-46, WS_URL :70; resolveSseTransport :59-64, SSE_TRANSPORT :76)
    - frontend/src/providers/RunConnectionProvider.tsx (enabled = ENV.SSE_TRANSPORT :185; the if(!enabled) guards :194-196,:220-224,:230, aggregatePhase enabled arg :122, render guard :306)
    - frontend/src/app/layout.tsx (RunConnectionProvider mount :46 — remove the interim enabled prop; provider is unconditional now)
    - frontend/src/hooks/useWorkflow.ts (the ENV.SSE_TRANSPORT && runConnection.enabled branches :125-129 and :163-167 — delete the WS else-branches)
  </read_first>
  <action>
    Remove the flag entirely: in `env.ts` delete `resolveSseTransport`/`SSE_TRANSPORT` and
    `resolveWsUrl`/`WS_URL` (no WS URL is needed by any src consumer after Task 3; the
    handoff hook derives its own /ws/handoff URL from `API_URL`). In
    `RunConnectionProvider.tsx` make `enabled` unconditionally true (drop the `ENV.SSE_TRANSPORT`
    default and the `enabled` prop-override, or hardcode true) and COLLAPSE every
    `if (!enabled) return`/guard (:194,:220-224,:230,:295-296,:306) plus the `aggregatePhase`
    enabled short-circuit — SSE is always on. In `layout.tsx` drop the interim `enabled={true}`
    prop (the provider is unconditional now). In `useWorkflow.ts` delete the
    `if (ENV.SSE_TRANSPORT && runConnection.enabled) { sendCommand } else { websocketSend }`
    branches (:125-129, :163-167) — keep only the REST `sendCommand` path. Remove now-dead
    `websocketSend` params/imports in useWorkflow.
  </action>
  <acceptance_criteria>
    - `grep -rn "SSE_TRANSPORT\|resolveSseTransport\|NEXT_PUBLIC_SSE_TRANSPORT\|WS_URL\|resolveWsUrl" frontend/src` returns 0 matches.
    - `RunConnectionProvider` has no `if (!enabled)` guard remaining (grep 0 for `!enabled`).
    - `useWorkflow.ts` has no `websocketSend` branch remaining (grep 0 for `websocketSend`).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The FE flag is deleted; SSE is unconditional; useWorkflow has no WS branch.</done>
</task>

<task type="auto">
  <name>Task 3: Collapse the dashboard transport branches + delete useWebSocket.ts</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/hooks/useWebSocket.ts</files>
  <read_first>
    - frontend/src/app/dashboard/page.tsx (useWebSocket call :877-881; sseEnabled :904; chatSubscribe branch :908-918; legacyChatSend :921-926; chatWsSubscribe :135-140; the useWebSocket import :7; the pending-od_prototype connect effect keyed on connectionStatus :948-949)
    - frontend/src/components/layout/DashboardLayout.tsx (ConnectionStatus type import :36; the reconnect_pipeline WS effect :613-654; websocketSend prop threading)
    - frontend/src/hooks/useHandoffSocket.ts (the ConnectionStatus re-export from Task 1)
  </read_first>
  <action>
    In `dashboard/page.tsx` delete the `useWebSocket({...})` call (:877-881) and its import
    (:7); remove `sseEnabled` and collapse `chatSubscribe` to always use
    `runConnection.subscribe` (:908-918); delete the now-dead `chatWsSubscribe` (:135-140),
    `chatFrameListenersRef`, and `legacyChatSend` (:921-926); pass `undefined`/omit
    `legacyWsSend` to useRunChat (REST-only). Re-source any residual `connectionStatus`/
    `reconnect` consumers (e.g. the pending-od_prototype effect at :948) from
    `runConnection.phase` (a run streams via attach, so the effect keys on the SSE
    live/connected phase). In `DashboardLayout.tsx` repoint the `ConnectionStatus` type
    import from `useWebSocket` to `useHandoffSocket` (:36) and DELETE the now-dead
    `reconnect_pipeline` WS effect (:613-654) and any `websocketSend` prop it relied on.
    Finally DELETE `frontend/src/hooks/useWebSocket.ts` — it now has zero src consumers.
  </action>
  <acceptance_criteria>
    - `frontend/src/hooks/useWebSocket.ts` no longer exists (file deleted).
    - `grep -rn "useWebSocket\|chatWsSubscribe\|legacyChatSend\|sseEnabled\|reconnect_pipeline\|cancel_pipeline\|approve_review\|run_revision" frontend/src` returns 0 matches (the WS client + all WS frame sends are gone).
    - `grep -rn "/ws/chat" frontend/src` returns 0 matches (only /ws/handoff may remain, in useHandoffSocket).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>useWebSocket.ts is deleted; the dashboard has no WS transport; /ws/chat has zero FE references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external IDE -> /ws/handoff (via useHandoffSocket) | the surviving handoff WS; token-authenticated |
| browser -> SSE/REST | the sole run transport after this plan |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06-01 | Spoofing / broken auth | useHandoffSocket extraction dropping the token or JWT-4001 close handling | mitigate | carry over the token param + 4001 close-code handling verbatim from useWebSocket; HandoffWorkflow keeps its wsActive/token gating; verified against the handoff flow |
| T-44-06-02 | Elevation of privilege | removing the flag exposing a path that bypassed an auth check on the WS side | accept | the surviving SSE/REST surface self-enforces owner 404 (run_stream two-layer, run_commands fences); no auth logic lived in the deleted flag |
| T-44-06-03 | Denial of service | deleting useWebSocket while a consumer still references it | mitigate | delete the file LAST (Task 3), after all src consumers repointed; tsc proves zero dangling references |
| T-44-06-SC | Tampering | package installs | accept | no npm installs (deletion only) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- `grep -rn "useWebSocket\|WS_URL\|SSE_TRANSPORT\|/ws/chat\|routeWebSocket" frontend/src` -> 0 (only `/ws/handoff` in useHandoffSocket survives).
- The re-pointed mocked Playwright suite (44-09) is green after this plan; handoff behavior unchanged.
</verification>

<success_criteria>
useWebSocket.ts deleted; the flag removed; SSE unconditional; /ws/handoff survives
via useHandoffSocket; dead /workflow WS gone; tsc clean; zero WS-chat references.
</success_criteria>

## Artifacts this phase produces
- NEW `frontend/src/hooks/useHandoffSocket.ts` — the D10 handoff survivor hook (+ ConnectionStatus re-export).
- DELETED `frontend/src/hooks/useWebSocket.ts`; removed flag surface (env/provider/layout/useWorkflow/dashboard).

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-06-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
