---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/run_engine.py
  - backend/app/api/websocket.py
  - backend/app/api/run_commands.py
  - backend/app/api/run_stream.py
  - backend/app/api/run_files.py
  - backend/app/main.py
  - backend/app/api/websocket_handoff.py
autonomous: true
requirements: [W4, "C.3", "INV-3", "INV-12"]
must_haves:
  truths:
    - "The transport-neutral pipeline infra (queues/tasks/cancel-events, resume registrars, cleanup, ingress validators, owner/terminal fences, db + auth helpers) lives in a new transport-neutral module app/api/run_engine.py."
    - "All non-endpoint importers (run_commands, run_stream, run_files, main, websocket_handoff) resolve those symbols from run_engine.py; the /ws/chat endpoint STILL works (pure extract, not a deletion)."
    - "/ws/handoff behavior is unchanged (test_handoff_contract green); the 5 characterization goldens + test_wire_parity stay byte/event-identical."
  artifacts:
    - path: "backend/app/api/run_engine.py"
      provides: "relocated R3 transport-neutral symbols (single home)"
      contains: "_get_or_create_queue"
    - path: "backend/app/main.py"
      provides: "resume registrar wiring sources from run_engine, not the websocket module"
      contains: "run_engine"
  key_links:
    - from: "backend/app/api/run_commands.py"
      to: "backend/app/api/run_engine.py"
      via: "import of validators/fences/queues"
      pattern: "from app.api.run_engine import"
    - from: "backend/app/api/websocket_handoff.py"
      to: "backend/app/api/run_engine.py"
      via: "_authenticate_token / _get_db import"
      pattern: "run_engine import"
---

<objective>
W4a — extract the shared, transport-neutral infrastructure out of `websocket.py`
into a new `app/api/run_engine.py` BEFORE any deletion (INV-12: extract-then-delete).
The `/ws/chat` endpoint keeps working this wave — this is a pure, behavior-preserving
relocation that unblocks the endpoint deletion in a later wave (44-07) without a big
same-file change landing simultaneously with the FE cutover.

Purpose: `websocket.py` is imported by 5 modules for symbols that must survive the
endpoint deletion (R3). Moving them to a neutral module lets 44-07 delete the WS
receive-loop cleanly. Backend-only, file-disjoint from the wave-1 FE plans.
Output: `run_engine.py` owning the R3 symbols; all importers repointed; endpoint intact.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/CLAUDE.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@backend/app/api/websocket.py
@backend/app/api/run_commands.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/api/run_engine.py and relocate the R3 transport-neutral symbols</name>
  <files>backend/app/api/run_engine.py, backend/app/api/websocket.py</files>
  <read_first>
    - backend/app/api/websocket.py (symbol defs: _PIPELINE_QUEUES :39, _PIPELINE_TASKS :40, _CANCEL_EVENTS :47, _get_or_create_queue :64, _cleanup_pipeline :70, _register_resume_queue :91, _register_resume_task :100, _validate_model_overrides :121, _validate_images :184, _revalidate_selections_trust_user :270, _get_db :319, _review_gate_owned_by :324, _review_gate_run_is_terminal :351, _authenticate_token :397, _resolve_owned_parent_run_id :545)
    - backend/app/api/run_commands.py (import block :71-83 — the exact symbol set REST consumes)
    - backend/app/api/websocket_handoff.py (:26 imports _authenticate_token, _get_db)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (R3 symbol list)
  </read_first>
  <action>
    Create `backend/app/api/run_engine.py` and MOVE (cut, do not copy — INV-12 no dual
    impl) these definitions verbatim from `websocket.py`: the module-level registries
    `_PIPELINE_QUEUES`, `_PIPELINE_TASKS`, `_CANCEL_EVENTS`; the helpers
    `_get_or_create_queue`, `_cleanup_pipeline`, `_register_resume_queue`,
    `_register_resume_task`; the ingress validators/fences `_validate_model_overrides`,
    `_validate_images`, `_revalidate_selections_trust_user`, `_resolve_owned_parent_run_id`,
    `_review_gate_owned_by`, `_review_gate_run_is_terminal`; and the shared `_get_db` +
    `_authenticate_token` helpers. Carry their imports. In `websocket.py`, replace the moved
    defs with an import of the same names from `app.api.run_engine` so the still-present
    `websocket_chat` endpoint keeps compiling and behaving identically this wave. Keep the
    WS receive-loop, framing, `_handle_workflow_execution`, `_handle_revision_execution`,
    and the `router` in `websocket.py` untouched (44-07 deletes those). Respect the
    ports-and-adapters import direction — `run_engine.py` must not import `agents.execution_engine`
    in the forbidden direction; keep the same dependency edges the moved code already had.
  </action>
  <acceptance_criteria>
    - `grep -c "def _get_or_create_queue\|def _validate_images\|def _authenticate_token\|def _resolve_owned_parent_run_id" backend/app/api/run_engine.py` returns the moved-count (each def now lives in run_engine.py).
    - `grep -c "def _get_or_create_queue\|def _validate_images\|def _authenticate_token" backend/app/api/websocket.py` returns 0 (moved out, not duplicated).
    - `websocket.py` imports the moved names from `app.api.run_engine` (grep `from app.api.run_engine import`).
    - `cd backend && python3.11 -c "import app.api.run_engine, app.api.websocket"` imports cleanly.
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -c "import app.api.run_engine, app.api.websocket, app.main" && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>The R3 symbols live in run_engine.py; websocket.py re-imports them; endpoint intact.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the 5 importers (run_commands, run_stream, run_files, main, websocket_handoff) at run_engine</name>
  <files>backend/app/api/run_commands.py, backend/app/api/run_stream.py, backend/app/api/run_files.py, backend/app/main.py, backend/app/api/websocket_handoff.py</files>
  <read_first>
    - backend/app/api/run_commands.py (:71-83 import from app.api.websocket)
    - backend/app/api/run_stream.py (:62 import _PIPELINE_QUEUES, _get_or_create_queue)
    - backend/app/api/run_files.py (:59 import _get_db)
    - backend/app/main.py (:141-144 `_ws_bridge` = app.api.websocket; assigns _resume_register_queue/_resume_register_task/_resume_cleanup then restore_non_terminal_runs :145)
    - backend/app/api/websocket_handoff.py (:26 import _authenticate_token, _get_db)
  </read_first>
  <action>
    Change each importer's import SOURCE from `app.api.websocket` to `app.api.run_engine`
    for the relocated symbols, with ZERO logic change: `run_commands.py:71-83` (all 11
    symbols), `run_stream.py:62` (`_PIPELINE_QUEUES`, `_get_or_create_queue`),
    `run_files.py:59` (`_get_db`), `main.py:141` (rebind `_ws_bridge` to
    `app.api.run_engine` so `_register_resume_queue`/`_register_resume_task`/`_cleanup_pipeline`
    resolve there; the `restore_non_terminal_runs()` call is unchanged),
    `websocket_handoff.py:26` (`_authenticate_token`, `_get_db`). This is the ONLY change
    to `websocket_handoff.py` — its `/ws/handoff` route, auth flow, and reducer are
    otherwise byte-identical (decision 2: do not degrade the D10 survivor). Do NOT delete
    the `websocket_router` mount in `main.py:190` this wave (44-07 owns that).
  </action>
  <acceptance_criteria>
    - `grep -rn "from app.api.websocket import" backend/app` returns ONLY the transitional re-import inside websocket.py itself (all external importers now read `app.api.run_engine`).
    - `websocket_handoff.py` diff is exactly one import-source line (`app.api.websocket` -> `app.api.run_engine`); no other line changes.
    - `cd backend && python3.11 -m pytest tests/integration/test_handoff_contract.py -q` passes (handoff unchanged).
    - `cd backend && python3.11 -m pytest tests/unit/test_rest_gate_commands.py tests/unit/test_rest_answers_cancel.py tests/unit/test_rest_revisions.py tests/unit/test_rest_run_launch.py -q` passes (REST twins still resolve the relocated validators/fences).
    - `/opt/homebrew/bin/lint-imports` 4/0.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/integration/test_handoff_contract.py tests/unit/test_rest_gate_commands.py tests/unit/test_rest_answers_cancel.py tests/unit/test_rest_revisions.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Every non-endpoint importer resolves the relocated symbols from run_engine; handoff + REST twins green.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the relocation changed no engine bytes (INV-3 golden + wire-parity gate)</name>
  <files>backend/app/api/run_engine.py</files>
  <read_first>
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (INV-3 — 5 goldens + test_wire_parity must stay byte/event-identical)
    - backend/tests/agents/test_wire_parity.py (the parity oracle)
  </read_first>
  <action>
    Run the INV-3 gate and confirm the pure-refactor relocation is byte/event-invariant:
    the 5 characterization goldens and `test_wire_parity.py` must pass unchanged. This is
    a verification task, not a code task — if either fails, the relocation introduced a
    behavior change and must be corrected before this plan is done. Do NOT run the full
    backend suite (it hangs offline).
  </action>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/ -k characterization -q` reports 10 passed (goldens byte-identical).
    - `cd backend && python3.11 -m pytest tests/agents/test_wire_parity.py -q` passes (SSE frames still match the frozen WS goldens).
    - No new alembic migration was added (chain stays 0025): `ls backend/alembic/versions | wc -l` unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/ -k characterization tests/agents/test_wire_parity.py -q</automated>
  </verify>
  <done>Goldens + wire-parity green; migration chain unchanged — the relocation is proven byte-neutral.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> REST run commands | ingress validators/fences (images, model overrides, owner/terminal gate) guard untrusted input |
| external IDE -> /ws/handoff | the D10 survivor; reuses _authenticate_token/_get_db |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | Elevation of privilege | relocating _validate_images/_validate_model_overrides/_resolve_owned_parent_run_id/_review_gate_owned_by/_review_gate_run_is_terminal/_revalidate_selections_trust_user | mitigate | MOVE not modify; run_commands imports the SAME symbols from run_engine; re-run test_rest_gate_commands + REST twins to prove no guard dropped |
| T-44-03-02 | Spoofing | relocating _authenticate_token / _get_db used by /ws/handoff | mitigate | move not modify; test_handoff_contract.py green proves handoff auth intact |
| T-44-03-03 | Tampering | behavior drift during extraction | mitigate | INV-3 gate (5 goldens + test_wire_parity) must stay byte/event-identical |
| T-44-03-SC | Tampering | package installs | accept | no pip installs; refactor only |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/ -k characterization -q` -> 10 passed (INV-3).
- `test_wire_parity.py`, `test_handoff_contract.py`, the 4 `test_rest_*` twins green.
- `/opt/homebrew/bin/lint-imports` 4/0.
- No new migration (chain 0025).
</verification>

<success_criteria>
run_engine.py owns the R3 symbols; all importers repointed; /ws/chat endpoint
still works; /ws/handoff + goldens + wire-parity byte/event-identical.
</success_criteria>

## Artifacts this phase produces
- NEW `backend/app/api/run_engine.py` — the transport-neutral home for the R3 symbols.
- Repointed imports in run_commands.py, run_stream.py, run_files.py, main.py, websocket_handoff.py.

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-03-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
