---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 12
type: execute
wave: 7
depends_on: ["44-07", "44-09", "44-11"]
files_modified:
  - backend/tests/agents/test_iss033_count_delta_live.py
  - frontend/e2e/tests/ts-x.sse-smoke.live.spec.ts
  - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-LIVE-EVIDENCE.md
autonomous: false
requirements: [W6, "B.6b", "ISS-033"]
user_setup:
  - service: aws-bedrock
    why: "The live SSE smoke + W6 Lane-A/B require a real Bedrock run"
    env_vars:
      - name: AWS_PROFILE
        source: "SSO profile hex-ai-fe (run `aws sso login`); leave ANTHROPIC_API_KEY empty"
      - name: RUN_LIVE_BEDROCK
        source: "set to 1 to opt the live pytest in"
    dashboard_config:
      - task: "aws sso login (profile hex-ai-fe); QA login qa-enterprise@flowinqa.com / flowin-e2e-pass"
        location: "local shell + the running app"
must_haves:
  truths:
    - "With /ws/chat deleted, a real run launches via POST /api/runs, streams pipeline_start -> agent_* -> pipeline_complete over GET /api/runs/{id}/events/stream, renders a deliverable, and gate approve/reject/redo + cancel + a PPT revision (/revisions) + the Concierge all work over SSE/REST."
    - "Lane A (live pytest) asserts the ISS-033 count-delta (aux tokens in pipeline_complete vs a planner-skipped baseline) + cache_read>0 on a warm turn."
    - "Lane B (live Playwright) asserts B.6b (CompletionToast + bell badge) + P16 SC2 (Stop -> cancelled) over a real SSE run."
    - "Evidence (frame outputs, over-SSE assertions, cache_read/token values, screenshots) is recorded in 44-LIVE-EVIDENCE.md."
  artifacts:
    - path: "backend/tests/agents/test_iss033_count_delta_live.py"
      provides: "opt-in live ISS-033 count-delta + warm-cache assertion"
      contains: "RUN_LIVE_BEDROCK"
    - path: ".planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-LIVE-EVIDENCE.md"
      provides: "recorded live cutover evidence (discharges the LOCK-B follow-up)"
  key_links:
    - from: "the live app"
      to: "GET /api/runs/{id}/events/stream"
      via: "SSE-only run (no /ws/chat)"
      pattern: "events/stream"
---

<objective>
W6 (live lanes) + the end-of-phase supervised live SSE smoke. Author the opt-in
live ISS-033 pytest (Lane A) and the live Playwright SSE spec (Lane B), then run
the supervised live smoke on real Bedrock to prove the SSE-only cutover end-to-end
and record the evidence. This is the SUPERVISED tail (needs `aws sso login`); it
does NOT close the v2.0 milestone (a separate later step).

Purpose: the offline waves prove the code; this proves the LIVE cutover with
`/ws/chat` gone. Evidence in 44-LIVE-EVIDENCE.md discharges the LOCK-B
"human-validated cutover" follow-up. Independent files.
Output: green live lanes (on SSO) + recorded live smoke evidence.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@frontend/e2e/fixtures/live.ts
@backend/agents/execution_engine/engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Lane-A live ISS-033 pytest (opt-in, dual-gate)</name>
  <files>backend/tests/agents/test_iss033_count_delta_live.py</files>
  <read_first>
    - backend/tests/agents/test_phase3_token_delta_live.py (the dual-gate opt-in pattern — RUN_LIVE_BEDROCK + credential self-skip; the live-run harness)
    - backend/agents/execution_engine/engine.py (the aux-token fold :2474-2482; pipeline_complete totals)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W6 Lane-A — ISS-033 count-delta A/B + cache_read>0 warm turn)
  </read_first>
  <action>
    Author `test_iss033_count_delta_live.py` modelled on `test_phase3_token_delta_live.py`'s
    dual-gate (skip unless `RUN_LIVE_BEDROCK=1` AND creds are valid). Drive a real prototype
    run and assert (i) the `pipeline_complete` totals include the SmartPlanner + clarify aux
    tokens vs a planner-skipped baseline (the ISS-033 count-delta A/B) and (ii) `cache_read>0`
    on a warm turn. Self-skip cleanly on expired creds (no hard failure offline). Do NOT run
    it in this task (running is the supervised checkpoint) — author + import-check only.
  </action>
  <acceptance_criteria>
    - The test self-skips without `RUN_LIVE_BEDROCK=1` / valid creds (source: the skip gate).
    - `cd backend && python3.11 -m pytest tests/agents/test_iss033_count_delta_live.py -q` COLLECTS + SKIPS cleanly offline (no import/collection error).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_iss033_count_delta_live.py -q</automated>
  </verify>
  <done>The Lane-A live pytest is authored, collects, and self-skips offline.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Lane-B live SSE Playwright spec</name>
  <files>frontend/e2e/tests/ts-x.sse-smoke.live.spec.ts</files>
  <read_first>
    - frontend/e2e/fixtures/live.ts (loginLive :20; liveTest :32; the --project=live matcher)
    - frontend/e2e/tests/ts-w.model-override.live.spec.ts (an existing *.live.spec.ts for the live-harness shape)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W6 Lane-B — B.6b + P16 SC2 + opportunistic ISS-027)
  </read_first>
  <action>
    Author `ts-x.sse-smoke.live.spec.ts` in the `live` project: `loginLive`, drive one short
    real run over SSE, and assert B.6b (CompletionToast + bell badge) and P16 SC2 (Stop ->
    cancelled). Add ISS-027 (force-proceed / skip) ONLY if the button is reachable
    (opportunistic — `InlineClarifyActions.tsx:18-20` removed the inline "Skip all"; if gone,
    do not assert it here — it is offline-covered). Keep the spec resilient to real timing
    (generous waits). Do NOT run it in this task (running is the supervised checkpoint).
  </action>
  <acceptance_criteria>
    - The spec is in the `live` project (`*.live.spec.ts`, matched by playwright.config `live`).
    - It asserts B.6b (toast + badge) and P16 SC2 (Stop -> cancelled) over a real SSE run.
    - `cd frontend && npx playwright test --project=live --list` lists the new spec (collection only, no run).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test --project=live --list</automated>
  </verify>
  <done>The Lane-B live SSE spec is authored and collected in the live project.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Supervised live SSE smoke + Lane A/B on Bedrock SSO</name>
  <action>Operator runs the supervised live cutover verification below on real Bedrock (aws sso login, profile hex-ai-fe) and records the evidence in 44-LIVE-EVIDENCE.md. This is a human-verify checkpoint — the steps in how-to-verify are the executable run; do not auto-advance (workflow.auto_advance is ignored for this gate).</action>
  <what-built>
    The full SSE-only stack (W1-W5): pipeline down-channel + gate/cancel/questionnaire/revision
    over SSE/REST, /ws/chat deleted, run_revision retired, the confirm chip, the re-pointed
    mocked suite + CI gate. Plus the authored Lane-A pytest and Lane-B live spec (Tasks 1-2).
  </what-built>
  <how-to-verify>
    Prerequisite: `aws sso login` (profile hex-ai-fe; leave ANTHROPIC_API_KEY empty), backend
    uvicorn :8000 (python3.11, alembic at 0025), `next dev`, QA login qa-enterprise@flowinqa.com.
    1. In the browser Network tab, confirm NO `/ws/chat` WebSocket opens; a per-run
       `GET /api/runs/{id}/events/stream` SSE connection is present.
    2. Launch a real prototype run; confirm it streams pipeline_start -> agent_* ->
       pipeline_complete over SSE and a deliverable renders.
    3. Exercise a review gate: approve (with an edit -> confirm edited_content lands), reject,
       and redo — all over REST /gate.
    4. Stop a running pipeline -> it resolves to cancelled (over /cancel). Answer a clarify
       questionnaire -> the run resumes (no 422).
    5. Run a PPT revision from a settled deck via the confirm chip -> it launches over REST
       /revisions and streams over SSE with parent lineage.
    6. Ask the Concierge a question on a settled run -> it answers.
    7. Run Lane A: `cd backend && RUN_LIVE_BEDROCK=1 AWS_PROFILE=hex-ai-fe python3.11 -m pytest
       tests/agents/test_iss033_count_delta_live.py tests/agents/test_phase3_token_delta_live.py
       tests/agents/test_phase8_live.py -q` — assert the ISS-033 count-delta + cache_read>0.
    8. Run Lane B: `cd frontend && npx playwright test --project=live -g "sse-smoke"` — assert
       B.6b + P16 SC2.
    9. Record all evidence (Network-tab screenshot showing no /ws/chat, frame outputs,
       cache_read/token values, toast/badge screenshots, Lane-A/B pass output) in
       `44-LIVE-EVIDENCE.md` and note this discharges the LOCK-B human-validated-cutover follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" with the evidence captured in 44-LIVE-EVIDENCE.md, or describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> live SSE/REST on real Bedrock | the sole run surface; owner-scoped; no /ws/chat |
| operator shell -> Bedrock SSO | live credentials via aws sso login |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-12-01 | Information disclosure | live smoke confirming no cross-owner leakage over SSE | mitigate | the smoke uses one QA owner; the SSE stream + REST commands enforce owner 404 (verified offline in 44-04/44-07); evidence notes only the owner's run streams |
| T-44-12-02 | Spoofing | live creds mishandled | accept | SSO session only; ANTHROPIC_API_KEY empty; no secrets committed to 44-LIVE-EVIDENCE.md |
| T-44-12-SC | Tampering | package installs | accept | no installs (test authoring + supervised run) |
</threat_model>

<verification>
- Offline: the Lane-A pytest collects + self-skips; the Lane-B spec is collected in the live project.
- Supervised (SSO): the live SSE smoke passes end-to-end with /ws/chat gone; Lane-A + Lane-B green; evidence recorded in 44-LIVE-EVIDENCE.md.
</verification>

<success_criteria>
A real SSE-only run works end-to-end (launch/stream/deliverable/gate/cancel/
questionnaire/revision/Concierge, no /ws/chat); Lane-A (ISS-033 count-delta +
warm cache) and Lane-B (B.6b + P16 SC2) pass on SSO; evidence recorded.
</success_criteria>

## Artifacts this phase produces
- NEW `backend/tests/agents/test_iss033_count_delta_live.py` (opt-in live ISS-033).
- NEW `frontend/e2e/tests/ts-x.sse-smoke.live.spec.ts` (live SSE smoke: B.6b + P16 SC2).
- NEW `.planning/phases/44-.../44-LIVE-EVIDENCE.md` (recorded live cutover evidence; discharges LOCK-B follow-up).

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-12-SUMMARY.md` when done.
Do NOT run `/gsd-complete-milestone` — the v2.0 milestone close is a separate later step.
After any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
