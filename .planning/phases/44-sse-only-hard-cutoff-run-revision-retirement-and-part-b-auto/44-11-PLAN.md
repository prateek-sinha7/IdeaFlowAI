---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 11
type: execute
wave: 6
depends_on: ["44-09"]
files_modified:
  - backend/tests/agents/test_iss033_aux_token_fold_offline.py
  - frontend/e2e/tests/ts-x.notifications-b6b.spec.ts
autonomous: true
requirements: [W6, "B.6b", "ISS-033"]
must_haves:
  truths:
    - "An offline test asserts the SmartPlanner + clarify aux tokens reach the pipeline_complete totals (ISS-033 engine fold, engine.py:2474-2482)."
    - "A mocked-SSE spec drives a run to pipeline_complete and asserts the CompletionToast + bell badge (B.6b) — notifications are transport-agnostic."
    - "Both additions are offline (no Bedrock)."
  artifacts:
    - path: "backend/tests/agents/test_iss033_aux_token_fold_offline.py"
      provides: "offline proof of the aux-token fold into pipeline_complete"
      contains: "pipeline_complete"
    - path: "frontend/e2e/tests/ts-x.notifications-b6b.spec.ts"
      provides: "mocked-SSE B.6b (toast + bell badge)"
      contains: "pipeline_complete"
  key_links:
    - from: "frontend/e2e/tests/ts-x.notifications-b6b.spec.ts"
      to: "frontend/e2e/fixtures/mockSse.ts"
      via: "drive pipeline_complete then assert toast/badge"
      pattern: "pipeline_complete"
---

<objective>
W6 (offline additions) — the two offline Part-B tests that close coverage gaps:
(1) an offline test that the aux (SmartPlanner + clarify question-generation)
tokens fold into the `pipeline_complete` totals (ISS-033, `engine.py:2474-2482`);
(2) a mocked-SSE B.6b spec that drives a run to `pipeline_complete` and asserts the
CompletionToast + bell badge (notifications are transport-agnostic, derived from
`pipelineState` at `DashboardLayout.tsx:372-414`).

Purpose: ISS-033 and B.6b have no offline coverage today; both are deterministic
offline. Depends on 44-09 (the mocked-SSE B.6b needs the re-pointed harness).
Independent files — runs alongside the other wave-6 test plans.
Output: two green offline tests closing the ISS-033 + B.6b offline gaps.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@backend/agents/execution_engine/engine.py
@frontend/e2e/fixtures/mockSse.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline ISS-033 aux-token-fold test</name>
  <files>backend/tests/agents/test_iss033_aux_token_fold_offline.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py (aux_token_usage list :1199; planner usage_sink :1697; clarify._usage_sink :1810-1812; the fold into pipeline_complete totals :2474-2482)
    - backend/tests/agents/_scripted_model.py + an existing engine test (e.g. test_execution_engine.py) for the scripted-run harness shape
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W6 ISS-033 offline gap)
  </read_first>
  <action>
    Add an offline scripted-run engine test asserting that the aux tokens produced by the
    SmartPlanner + the clarify question-generation calls are INCLUDED in the
    `pipeline_complete` token totals (the fold at engine.py:2474-2482). Drive a run whose
    planner + clarify sinks emit known token counts and assert the `pipeline_complete`
    `input_tokens`/`output_tokens`/`cache_read`/`cache_write` totals equal the agent-stream
    totals PLUS the aux totals (a planner-skipped baseline should NOT include them — assert
    the delta). Use the scripted-model recipe (no Bedrock). Keep it offline and deterministic.
  </action>
  <acceptance_criteria>
    - The test asserts the pipeline_complete totals include the aux (planner + clarify) tokens vs a planner-skipped baseline (the count-delta).
    - `cd backend && python3.11 -m pytest tests/agents/test_iss033_aux_token_fold_offline.py -q` passes offline.
    - The test does NOT require Bedrock/network (no requires_api_key marker needed).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_iss033_aux_token_fold_offline.py -q</automated>
  </verify>
  <done>The aux-token fold into pipeline_complete is offline-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Mocked-SSE B.6b notifications spec (toast + bell badge)</name>
  <files>frontend/e2e/tests/ts-x.notifications-b6b.spec.ts</files>
  <read_first>
    - frontend/e2e/fixtures/mockSse.ts + dashboard.ts (the re-pointed harness from 44-09 — the pipeline emit helpers)
    - frontend/src/components/layout/DashboardLayout.tsx (the notification derivation from pipelineState :372-414 — CompletionToast + bell badge)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W6 B.6b — mocked-SSE drive to pipeline_complete)
  </read_first>
  <action>
    Add a mocked (non-live) Playwright spec that launches a run, drives the mockSse harness
    to `pipeline_complete`, and asserts the CompletionToast appears and the bell badge
    increments — B.6b. Use the re-pointed page-object/fixtures (44-09). Keep it in the
    `mocked` project (not `.live.spec.ts`). Notifications are transport-agnostic, so a
    mocked-SSE drive fully covers B.6b offline.
  </action>
  <acceptance_criteria>
    - The spec drives `pipeline_complete` via mockSse and asserts both the CompletionToast and the bell badge (source: the assertions reference the toast + badge testids).
    - `cd frontend && npx playwright test --project=mocked frontend/e2e/tests/ts-x.notifications-b6b.spec.ts` (or the spec path) is green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test --project=mocked -g "B.6b" --reporter=line</automated>
  </verify>
  <done>B.6b (toast + bell badge) is covered by a green mocked-SSE spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness -> mocked SSE / scripted engine | offline; no real transport or Bedrock |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-11-01 | Repudiation | a token-fold test that passes vacuously (baseline == folded) | mitigate | assert the DELTA vs a planner-skipped baseline so the fold must be non-zero to pass |
| T-44-11-SC | Tampering | package installs | accept | no installs (test additions) |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_iss033_aux_token_fold_offline.py -q` green.
- `cd frontend && npx playwright test --project=mocked -g "B.6b"` green.
</verification>

<success_criteria>
The ISS-033 aux-token fold is offline-proven (count-delta); B.6b (toast + bell
badge) is covered by a green mocked-SSE spec.
</success_criteria>

## Artifacts this phase produces
- NEW `backend/tests/agents/test_iss033_aux_token_fold_offline.py`.
- NEW `frontend/e2e/tests/ts-x.notifications-b6b.spec.ts` (mocked-SSE B.6b).

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-11-SUMMARY.md` when done.
Note: run FE playwright from the `frontend/` dir (cwd-sensitive). After any `state.*`
handler call, restore the project-wide progress block (total_phases:37 /
completed_phases:35 / total_plans:208 / completed_plans:207 / percent:95) — the
state.* handlers clobber it (STATE.md quirk).
</output>
