---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 05
type: execute
wave: 3
depends_on: ["44-04"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/layout/DashboardLayout.tsx
autonomous: true
requirements: [W3, "D1", "CTX-04", "WR-03"]
must_haves:
  truths:
    - "handleRevisePpt launches a revision via REST POST /api/runs/{parent_run_id}/revisions (the byte-twin of the WS run_revision -> engine._handle_revision), not the WS run_revision frame."
    - "The returned revision run streams over SSE like any other run (no extra wiring — W1 made SSE the sole down-channel)."
    - "od_ppt_revision resolves to a revise handler (the activeReviseHandler map bug is fixed); a re-revised od_ppt no longer resolves to undefined."
    - "PPT revisions supply parent_run_id explicitly (no orphaned run: source_workflow_run_id is set)."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "postRevision fetcher -> POST /api/runs/{id}/revisions"
      contains: "/revisions"
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "handleRevisePpt uses postRevision; activeReviseHandler includes od_ppt_revision"
      contains: "od_ppt_revision"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx handleRevisePpt"
      to: "POST /api/runs/{parent_run_id}/revisions"
      via: "api.ts postRevision"
      pattern: "/revisions"
---

<objective>
W3b — retire `run_revision` via Strategy A. Rewire `handleRevisePpt`
(`DashboardLayout.tsx:493-510`) off the WS `run_revision` frame onto a new REST
`POST /api/runs/{id}/revisions` fetcher. The REST endpoint (`run_commands.py:1536`)
is the byte-twin of the same server path: `_mint_revision_row` + `_drive_revision_to_queue`
-> `engine._handle_revision` — preserving the server-side artifact seed, the
planning-context prepend, and the exact-kind `derived_from` lineage. Also fix the
two verified pre-existing bugs. The BE WS handler + WS driver deletion happens in
44-07 (after the FE stops sending run_revision).

Purpose: remove the legacy WS revision path (INV-12 / D1 / CTX-04) while keeping
PPT-revision parity. Sequenced after 44-04 (shares DashboardLayout.tsx + api.ts).
Output: PPT revisions launch over REST with full parity; the 2 bugs closed.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@frontend/src/lib/api.ts
@backend/app/api/run_commands.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the postRevision REST fetcher</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts (request helper + Task-shape of the 44-04 command fetchers)
    - backend/app/api/run_commands.py (POST /{id}/revisions :1536; _mint_revision_row :1509; _drive_revision_to_queue :1591 -> engine._handle_revision :1638; the request body it expects — target_artifact_type/instruction/parent linkage)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W3 Strategy A; the byte-twin parity list)
  </read_first>
  <action>
    Add `postRevision(token, parentRunId, { target_artifact_type, instruction })` ->
    `POST /api/runs/{parentRunId}/revisions`, returning the created revision `run_id`.
    Match the endpoint's request body exactly (read `run_commands.py:1536`+ for the schema:
    the target artifact kind + instruction; parent linkage comes from the path run id).
    Reuse the existing `request`/auth machinery. Return the run_id so the caller can
    attach it (attach is free — the SSE stream picks it up via the provider's live-run
    query; call `attachRun` if the id is available synchronously).
  </action>
  <acceptance_criteria>
    - `grep -n "runs/\${.*}/revisions" frontend/src/lib/api.ts` shows the endpoint wired.
    - `postRevision` returns the created run id (source assertion).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>api.ts exposes postRevision targeting the byte-twin /revisions endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire handleRevisePpt to REST + fix the 2 pre-existing bugs</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (handleRevisePpt :493-510 sends the WS run_revision frame; the legacy text-injection fallback :512-524; activeReviseHandler map :1265-1270 — od_ppt_revision is ABSENT from the ppt branch; contentSourceRunId prop = parent run id)
    - frontend/src/lib/api.ts (postRevision from Task 1)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W3 bug (a) od_ppt_revision missing; bug (b) orphan source_workflow_run_id)
  </read_first>
  <action>
    Replace the WS `run_revision` send in `handleRevisePpt` (:499-509) with
    `postRevision(token, contentSourceRunId, { target_artifact_type: isOdPpt ? "od_ppt_output" : "ppt_output", instruction })`
    — passing `contentSourceRunId` as the explicit parent (bug (b): supplies parent_run_id
    so no orphaned run). Keep setting `setWorkflowType(...revision...)` + `onResetPipeline()`;
    the returned run streams over SSE (attach via the returned id if available). Preserve
    the legacy text-injection fallback (:512-524) for the pre-Phase-3 no-run-id case.
    Bug (a): add `od_ppt_revision` to the ppt branch of `activeReviseHandler` (:1266) so a
    re-revised od_ppt resolves to `handleRevisePpt`, not `undefined`. Do NOT delete the WS
    driver or handler on the backend here (44-07 owns that). This wave STOPS the FE from
    emitting the WS run_revision frame.
  </action>
  <acceptance_criteria>
    - `grep -c "type: \"run_revision\"" frontend/src/components/layout/DashboardLayout.tsx` returns 0 (no WS run_revision send remains).
    - `handleRevisePpt` calls `postRevision` with `contentSourceRunId` as the parent (source assertion — no orphan).
    - `grep -n "od_ppt_revision" frontend/src/components/layout/DashboardLayout.tsx` shows it in the activeReviseHandler ppt branch (:1265-1270).
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>PPT revisions launch via REST /revisions; od_ppt_revision resolves; no orphan; no WS run_revision send.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> POST /api/runs/{id}/revisions | untrusted revision request; parent run must be owner-scoped (AUTHZ-03) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05-01 | Elevation of privilege (IDOR) | /revisions against a parent run the caller does not own | mitigate | the REST endpoint mints the revision via _mint_revision_row which requires a real owner_id (engine._handle_revision AUTHZ-03 assert :5092) and resolves the owned parent; the FE passes the user's own contentSourceRunId. Covered by test_rest_revisions.py owner cases |
| T-44-05-02 | Tampering / integrity | orphaned revision (missing source_workflow_run_id) | mitigate | supply parent_run_id explicitly via the path run id (bug (b) fix) so lineage/derived_from is written (engine :5302-5331) |
| T-44-05-SC | Tampering | package installs | accept | no npm installs |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; no residual `run_revision` WS send (grep 0).
- Backend parity of the REST path already covered by `tests/unit/test_rest_revisions.py` (byte-twin -> engine._handle_revision); confirm green after 44-07 too.
- Mocked e2e revision flow re-pointed + green in W5 (44-09).
</verification>

<success_criteria>
handleRevisePpt launches revisions via REST /revisions with full parity (server
seed + planning-context + derived_from); od_ppt_revision resolves; no orphan; the
FE emits no WS run_revision frame. tsc clean.
</success_criteria>

## Artifacts this phase produces
- `api.ts`: `postRevision` fetcher.
- `DashboardLayout.tsx`: handleRevisePpt on REST /revisions; activeReviseHandler includes od_ppt_revision; parent_run_id supplied.

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-05-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
