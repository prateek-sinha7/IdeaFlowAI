---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 09
type: execute
wave: 5
depends_on: ["44-06"]
files_modified:
  - frontend/e2e/fixtures/mockSse.ts
  - frontend/e2e/fixtures/dashboard.ts
  - frontend/e2e/fixtures/test.ts
  - frontend/e2e/fixtures/mockWs.ts
  - frontend/e2e/tests/ts-sse.spec.ts
autonomous: true
requirements: [W5]
must_haves:
  truths:
    - "The mocked Playwright harness drives the app over SSE (mockSse) + a REST command mock, not routeWebSocket(/ws/chat)."
    - "The page-object (dashboard.ts) launches + waits via the SSE/REST mocks (no ws.ready()/waitForClientFrame('run_pipeline'))."
    - "The re-pointed mocked suite (the formerly-WS specs, driven through the shared page-object) is green; ts-sse.spec.ts drives the mounted app (not a synthetic fetchSse consumer)."
  artifacts:
    - path: "frontend/e2e/fixtures/mockSse.ts"
      provides: "pipeline-helper parity (emit agent_*/wave_*/pipeline_*/questionnaire/review_gate) + a REST command capture"
      contains: "pipeline"
    - path: "frontend/e2e/fixtures/dashboard.ts"
      provides: "SSE/REST-driven launch + wait helpers"
  key_links:
    - from: "frontend/e2e/fixtures/dashboard.ts"
      to: "frontend/e2e/fixtures/mockSse.ts"
      via: "SSE emit + REST command assert"
      pattern: "mockSse"
---

<objective>
W5 (frontend e2e) — re-point the mocked Playwright harness from WS to SSE. Grow
`mockSse.ts` to full pipeline-helper parity (the seq/envelope machinery is already
shared with `mockWs.ts`), add a REST command mock (`page.route` for `POST /api/runs`
+ `/{id}/{answers,cancel,gate,revisions}`) replacing `mockWs.sent`/`waitForClientFrame`,
re-point the `dashboard.ts` page-object, swap the `test.ts` auto-fixture, and upgrade
`ts-sse*.spec.ts` to drive the MOUNTED app.

Purpose: the FE is SSE-only after 44-06; the mocked suite (~132 formerly-WS specs)
must exercise the new transport. Frontend/e2e only — file-disjoint from src and from
the backend deletion (44-07), so this runs in the same wave as 44-07.
Output: a green, SSE-driven mocked Playwright suite.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@frontend/e2e/fixtures/mockSse.ts
@frontend/e2e/fixtures/mockWs.ts
@frontend/e2e/fixtures/dashboard.ts
@frontend/e2e/fixtures/test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grow mockSse to pipeline-helper parity + a REST command mock</name>
  <files>frontend/e2e/fixtures/mockSse.ts</files>
  <read_first>
    - frontend/e2e/fixtures/mockWs.ts (the pipeline emit helpers :194-287 + the seq/envelope machinery; the up-channel `sent`/`waitForClientFrame` capture)
    - frontend/e2e/fixtures/mockSse.ts (current SSE tail — lacks pipeline helpers + up-channel capture)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W5 build list)
  </read_first>
  <action>
    Port the pipeline emit helpers from `mockWs.ts:194-287` onto the SSE tail in
    `mockSse.ts` (reuse the shared seq/envelope machinery) so a test can emit
    `pipeline_start`, `agent_*`, `wave_*`, `questionnaire_ready`, `review_gate_ready`,
    `review_gate_approved`, `pipeline_cancelled`/`pipeline_failed`, and `pipeline_complete`
    over the mocked `GET /api/runs/{id}/events/stream`. Add a REST command mock: `page.route`
    handlers for `POST /api/runs` (returns `{ run_id }`) and `POST /api/runs/{id}/answers`,
    `/cancel`, `/gate`, `/revisions` — each capturing the request body so specs can assert
    the up-channel (replacing `mockWs.sent`/`waitForClientFrame`). Expose a
    `waitForCommand(kind)` helper mirroring the old `waitForClientFrame` ergonomics.
  </action>
  <acceptance_criteria>
    - `mockSse.ts` exposes pipeline emit helpers (grep for the ported helper names) + a REST command capture with a `waitForCommand`-style accessor.
    - `grep -n "POST\|page.route\|/api/runs" frontend/e2e/fixtures/mockSse.ts` shows the REST command routes for runs/answers/cancel/gate/revisions.
    - `cd frontend && npx tsc --noEmit -p e2e/tsconfig.json` (or the e2e typecheck) clean if configured; else `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>mockSse can drive a full pipeline + capture the REST up-channel.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the page-object + swap the test.ts fixture</name>
  <files>frontend/e2e/fixtures/dashboard.ts, frontend/e2e/fixtures/test.ts, frontend/e2e/fixtures/mockWs.ts</files>
  <read_first>
    - frontend/e2e/fixtures/dashboard.ts (ws.ready() :58; waitForClientFrame("run_pipeline") :99; the launch helper :84-99)
    - frontend/e2e/fixtures/test.ts (the mockWs auto-fixture :38-44; the DashboardPage construction :45-46)
    - frontend/e2e/fixtures/mockSse.ts (the new helpers from Task 1)
  </read_first>
  <action>
    Re-point `dashboard.ts`: replace `this.ws.ready()` (:58) with the SSE mock readiness and
    `waitForClientFrame("run_pipeline")` (:99) with the SSE/REST command assertion
    (`waitForCommand`/the POST /api/runs capture) — the app now launches via REST + streams
    via SSE. Swap `test.ts`: make `mockSse` the auto-fixture (replacing the `mockWs`
    auto-fixture :38-44) and construct `DashboardPage` with the SSE mock. Reduce `mockWs.ts`
    to the minimum still needed by the handoff/live surfaces or delete it if no spec depends
    on it (the banned-pattern gate in 44-10 bans `routeWebSocket`; `mockWs.ts`'s
    `page.routeWebSocket(/\/ws\/chat/)` must go). Keep the fixture API stable so the
    formerly-WS specs compile unchanged against the page-object.
  </action>
  <acceptance_criteria>
    - `grep -rn "routeWebSocket\|/ws/chat\|waitForClientFrame(\"run_pipeline\")\|ws.ready()" frontend/e2e` returns 0 matches.
    - `test.ts` provides `mockSse` as the auto-fixture; `dashboard.ts` launches/waits via the SSE/REST mock.
    - `cd frontend && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The page-object + fixture drive SSE/REST; no routeWebSocket(/ws/chat) remains.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade ts-sse spec + run the re-pointed mocked suite green</name>
  <files>frontend/e2e/tests/ts-sse.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/ts-sse.spec.ts (the current synthetic fetchSse consumer)
    - frontend/e2e/fixtures/dashboard.ts + mockSse.ts (the re-pointed harness)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (§6 DoD — the ~132 formerly-WS specs now SSE-driven green)
  </read_first>
  <action>
    Upgrade `ts-sse.spec.ts` to drive the MOUNTED app through the re-pointed page-object
    (drop the synthetic `fetchSse` consumer): launch -> assert the pipeline renders from the
    SSE emit; a gate over `/gate`; a cancel over `/cancel`; a revision over `/revisions`.
    Then run the full mocked project and fix any selector/harness drift in the formerly-WS
    specs by adjusting the SHARED page-object first (not per-spec), classifying each red as
    selector-drift (fix) vs changed-behavior (reconcile, never delete). Do NOT run the live
    project.
  </action>
  <acceptance_criteria>
    - `ts-sse.spec.ts` drives the mounted app (grep 0 for `fetchSse` synthetic consumer).
    - `cd frontend && npx playwright test --project=mocked` is green (the re-pointed formerly-WS specs pass over SSE).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx playwright test --project=mocked --reporter=line</automated>
  </verify>
  <done>The mocked Playwright suite is SSE-driven and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness -> mocked SSE/REST | no real transport; drives the mounted app |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-09-01 | Repudiation / coverage loss | re-pointing the harness silently disabling specs (test.fixme drift) | mitigate | classify each red as selector-drift (fix) vs changed-behavior (reconcile), never delete/skip a passing behavior; the suite must be green, not skipped |
| T-44-09-SC | Tampering | package installs | accept | no npm installs (test harness edits) |
</threat_model>

<verification>
- `grep -rn "routeWebSocket\|/ws/chat" frontend/e2e` -> 0.
- `cd frontend && npx playwright test --project=mocked` green.
</verification>

<success_criteria>
The mocked harness drives SSE + a REST command mock; the page-object + fixture are
re-pointed; the formerly-WS mocked suite is green; ts-sse drives the mounted app.
</success_criteria>

## Artifacts this phase produces
- `mockSse.ts`: pipeline emit helpers + REST command mock + `waitForCommand`.
- Re-pointed `dashboard.ts` + `test.ts`; `mockWs.ts` reduced/removed; `ts-sse.spec.ts` mounted-app driven.

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-09-SUMMARY.md` when done.
Note: run FE captures + vitest/playwright from the `frontend/` dir (cwd-sensitive).
After any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
