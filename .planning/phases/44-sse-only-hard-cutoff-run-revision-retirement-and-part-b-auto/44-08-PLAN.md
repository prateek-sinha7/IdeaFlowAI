---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 08
type: execute
wave: 6
depends_on: ["44-07"]
files_modified:
  - backend/tests/unit/test_rest_answers_cancel.py
  - backend/tests/unit/test_rest_revisions.py
  - backend/tests/unit/test_run_message_images.py
  - backend/tests/unit/test_pipeline_cancel.py
  - backend/tests/unit/test_run_revision_ws_dispatch.py
  - backend/tests/unit/test_ws_parent_link_ownership.py
  - backend/tests/unit/test_image_ws_ingress.py
  - backend/tests/unit/test_pipeline_failure_semantics.py
  - backend/tests/agents/test_ws_reconnect_replay.py
autonomous: true
requirements: [W5, "INV-3"]
must_haves:
  truths:
    - "The WS-only backend tests that drove deleted handlers are migrated to their REST twins or deleted; no test imports a deleted WS driver."
    - "Unique coverage from the migrated WS tests (cancel ack, revision dispatch, run-entry image ingress, terminal/missing_template_context guard) survives in the REST twins."
    - "test_wire_parity.py + test_sse_stream.py + test_attach_replay_matrix.py are KEPT and green (the parity oracle + target-state suites)."
  artifacts:
    - path: "backend/tests/unit/test_rest_answers_cancel.py"
      provides: "the migrated cancel-ack coverage (was test_pipeline_cancel FakeWebSocket)"
    - path: "backend/tests/unit/test_rest_revisions.py"
      provides: "the migrated revision-dispatch + parent-link ownership coverage"
  key_links:
    - from: "backend/tests/unit/test_rest_*.py"
      to: "the REST command endpoints"
      via: "TestClient POST (no FakeWebSocket)"
      pattern: "test_rest_"
---

<objective>
W5 (backend) — migrate the WS-only tests off the deleted `/ws/chat` handlers onto
their REST twins (which already exist from Phase 29), folding in any unique
coverage, and DELETE the obsolete WS tests. KEEP `test_wire_parity.py` (the cutover
parity oracle) and the SSE target-state suites.

Purpose: the WS handlers are gone (44-07); tests that drive `FakeWebSocket` against
them now fail to import/run. This closes them out per the CONTEXT §5 table without
losing behavioral coverage. Backend/tests only — file-disjoint from src.
Output: green REST twins carrying the migrated coverage; obsolete WS tests deleted.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@backend/tests/unit/test_rest_revisions.py
@backend/tests/unit/test_rest_answers_cancel.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold cancel + revision + image WS coverage into the REST twins</name>
  <files>backend/tests/unit/test_rest_answers_cancel.py, backend/tests/unit/test_rest_revisions.py, backend/tests/unit/test_run_message_images.py</files>
  <read_first>
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (§5 migration table — the WS-coupling column per test)
    - backend/tests/unit/test_pipeline_cancel.py (FakeWebSocket :74; the LV-01 wait_for flake :203); backend/tests/unit/test_run_revision_ws_dispatch.py (_ScriptedLoopWebSocket :534); backend/tests/unit/test_ws_parent_link_ownership.py; backend/tests/unit/test_image_ws_ingress.py (_FakeWebSocket :32)
    - backend/tests/unit/test_rest_answers_cancel.py, test_rest_revisions.py, test_run_message_images.py (the existing REST twins to extend)
  </read_first>
  <action>
    For each WS test in the §5 table, identify assertions NOT already covered by the REST
    twin and add them to the twin as REST-path cases (TestClient POST, no FakeWebSocket):
    cancel ack + terminal transition -> `test_rest_answers_cancel.py` (drop the LV-01
    `wait_for` flake); revision dispatch + the parent-link ownership cases (from
    `test_ws_parent_link_ownership.py`) -> `test_rest_revisions.py`; run-entry image
    ingress -> `test_run_message_images.py` + REST cases. Do NOT duplicate coverage the
    twin already has — only fold what is unique. Keep the twins' style/fixtures.
  </action>
  <acceptance_criteria>
    - The unique cancel/revision/image assertions from the WS tests exist in the REST twins (source: grep for the migrated case names/asserts).
    - `cd backend && python3.11 -m pytest tests/unit/test_rest_answers_cancel.py tests/unit/test_rest_revisions.py tests/unit/test_run_message_images.py -q` all pass.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_rest_answers_cancel.py tests/unit/test_rest_revisions.py tests/unit/test_run_message_images.py -q</automated>
  </verify>
  <done>The REST twins carry the migrated cancel/revision/image coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the failure-semantics guard + delete the obsolete WS tests</name>
  <files>backend/tests/unit/test_pipeline_failure_semantics.py, backend/tests/unit/test_pipeline_cancel.py, backend/tests/unit/test_run_revision_ws_dispatch.py, backend/tests/unit/test_ws_parent_link_ownership.py, backend/tests/unit/test_image_ws_ingress.py, backend/tests/agents/test_ws_reconnect_replay.py</files>
  <read_first>
    - backend/tests/unit/test_pipeline_failure_semantics.py (_FakeWebSocket :389; terminal + missing_template_context guard — verify a REST-path test exists; §5 flags this as a gap candidate)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (§5 — DELETE test_ws_reconnect_replay.py, superseded by test_sse_stream + test_attach_replay_matrix; KEEP test_wire_parity)
  </read_first>
  <action>
    Migrate the terminal + `missing_template_context` ingress-guard assertions from
    `test_pipeline_failure_semantics.py` to a REST-path test (extend the appropriate
    `test_rest_*` twin if the guard is not already REST-covered — §5 flags this as a gap;
    close it). Then DELETE the now-obsolete WS-only tests whose handlers no longer exist:
    `test_pipeline_cancel.py`, `test_run_revision_ws_dispatch.py`,
    `test_ws_parent_link_ownership.py`, the WS half of `test_image_ws_ingress.py` (delete
    the file if the REST cases fully replace it), the WS half of
    `test_pipeline_failure_semantics.py`, and `test_ws_reconnect_replay.py` (superseded).
    Do NOT touch `test_wire_parity.py`, `test_sse_stream.py`, or `test_attach_replay_matrix.py`.
  </action>
  <acceptance_criteria>
    - `grep -rln "FakeWebSocket\|_ScriptedLoopWebSocket\|routeWebSocket" backend/tests` returns 0 files (no test drives a deleted WS handler).
    - `test_ws_reconnect_replay.py` no longer exists.
    - The missing_template_context/terminal guard is asserted on a REST path (source: grep in a test_rest_* file).
    - `cd backend && python3.11 -m pytest tests/unit/ -k "rest_ or failure_semantics" -q` passes with no import errors.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/ -k "rest_ or failure_semantics" -q</automated>
  </verify>
  <done>The failure-semantics guard is REST-covered; every WS-only test is deleted; no FakeWebSocket remains.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the parity oracle + SSE target-state suites + goldens stay green</name>
  <files>backend/tests/unit/test_rest_revisions.py</files>
  <read_first>
    - backend/tests/agents/test_wire_parity.py (KEEP — the parity oracle)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (§6 DoD — targeted parity/gate suite green + 5 goldens)
  </read_first>
  <action>
    Run the kept suites to prove the migration did not regress the parity oracle or the
    SSE target-state suites, and that the 5 characterization goldens remain byte-identical
    (INV-3). Verification-only task.
  </action>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_wire_parity.py tests/unit/test_sse_stream.py tests/agents/test_attach_replay_matrix.py -q` all pass.
    - `cd backend && python3.11 -m pytest tests/agents/ -k characterization -q` -> 10 passed.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_wire_parity.py tests/unit/test_sse_stream.py tests/agents/test_attach_replay_matrix.py tests/agents/ -k "characterization or wire_parity" -q</automated>
  </verify>
  <done>Parity oracle + SSE target-state suites + 5 goldens green after the migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness -> REST endpoints | migrated tests exercise the owner-scoped REST command surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08-01 | Repudiation / coverage loss | deleting WS tests silently drops a security assertion (e.g. parent-link ownership) | mitigate | fold the cross-owner parent-link ownership cases into test_rest_revisions.py BEFORE deleting test_ws_parent_link_ownership.py; grep proves the assertion migrated |
| T-44-08-SC | Tampering | package installs | accept | no pip installs (test edits only) |
</threat_model>

<verification>
- `grep -rln "FakeWebSocket\|_ScriptedLoopWebSocket" backend/tests` -> 0 files.
- REST twins + failure-semantics + wire-parity + SSE suites + 5 goldens green.
</verification>

<success_criteria>
The §5 migration table is executed: WS tests migrated/deleted, unique coverage
preserved in REST twins, parity oracle + SSE suites + goldens green.
</success_criteria>

## Artifacts this phase produces
- Extended REST twins (`test_rest_answers_cancel.py`, `test_rest_revisions.py`, `test_run_message_images.py`) carrying migrated coverage.
- DELETED WS-only tests (`test_pipeline_cancel.py`, `test_run_revision_ws_dispatch.py`, `test_ws_parent_link_ownership.py`, `test_ws_reconnect_replay.py`, WS halves of image/failure suites).

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-08-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
