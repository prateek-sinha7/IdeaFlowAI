---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 10
type: execute
wave: 6
depends_on: ["44-06", "44-07", "44-09"]
files_modified:
  - backend/tests/agents/test_sse_cutover_banned_patterns.py
autonomous: true
requirements: [W5, "C.3", "INV-12"]
must_haves:
  truths:
    - "A CI ratchet FAILS if any of /ws/chat, routeWebSocket, useWebSocket, NEXT_PUBLIC_SSE_TRANSPORT, or SSE_TRANSPORT_ENABLED reappears in first-party source."
    - "The gate is non-vacuous: an injected bad token is proven to make the scanner fire."
    - "The sanctioned survivors (/ws/handoff, useHandoffSocket, websocket_handoff.py) are allow-listed and do NOT trip the gate."
  artifacts:
    - path: "backend/tests/agents/test_sse_cutover_banned_patterns.py"
      provides: "the machine-checkable 'hard cutoff done' ratchet"
      contains: "ws/chat"
  key_links:
    - from: "backend/tests/agents/test_sse_cutover_banned_patterns.py"
      to: "frontend/src + backend/app scan roots"
      via: "grep ratchet"
      pattern: "banned"
---

<objective>
W5 — the CI banned-pattern gate (mirrors R15 / test_banned_patterns.py). A pytest
ratchet that FAILS if the deleted WS/flag tokens reappear: `/ws/chat`,
`routeWebSocket`, `useWebSocket`, `NEXT_PUBLIC_SSE_TRANSPORT`, `SSE_TRANSPORT_ENABLED`.
This is the machine-checkable definition of "hard cutoff done."

Purpose: without a ratchet, a future edit can silently reintroduce the WS transport
or the flag. Depends on 44-06 (FE tokens gone) + 44-07 (BE tokens gone) so the gate
passes on the current tree. New test file — disjoint.
Output: a green, non-vacuous ratchet allow-listing only the /ws/handoff survivor.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@backend/tests/agents/test_banned_patterns.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SSE-cutover banned-pattern ratchet</name>
  <files>backend/tests/agents/test_sse_cutover_banned_patterns.py</files>
  <read_first>
    - backend/tests/agents/test_banned_patterns.py (the R15 ratchet pattern: _SCAN_ROOTS :56, allow-list :69, the comment/docstring-ignoring scanners, the non-vacuity injection test :30-34)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (W5 gate token list; decision 2 — /ws/handoff survivor)
    - frontend/src/hooks/useHandoffSocket.ts (the sanctioned survivor); backend/app/api/websocket_handoff.py (the sanctioned /ws/handoff module)
  </read_first>
  <action>
    Author `test_sse_cutover_banned_patterns.py` modelled on `test_banned_patterns.py`. Scan
    BOTH first-party roots: `backend/app` + `backend/agents` (for `SSE_TRANSPORT_ENABLED`,
    `websocket_chat`, `/ws/chat`) AND `frontend/src` + `frontend/e2e` (for `useWebSocket`,
    `routeWebSocket`, `NEXT_PUBLIC_SSE_TRANSPORT`, `/ws/chat`). Resolve the repo root from the
    test file path (parents up to the repo). FAIL on any match of the five banned tokens.
    ALLOW-LIST the sanctioned survivors so they do NOT trip: `/ws/handoff` (never `/ws/chat`),
    `frontend/src/hooks/useHandoffSocket.ts`, and `backend/app/api/websocket_handoff.py` — and
    scope the `useWebSocket` ban so `useHandoffSocket` does not substring-match (use a
    word-boundary/exact-identifier match, not a bare substring). Ignore comment/docstring
    mentions in planning/doc files if the scan reaches them (scope the scan to source dirs).
    Skip the frontend scan gracefully if `frontend/` is absent (defensive), but on this repo
    it must run.
  </action>
  <acceptance_criteria>
    - The test scans both backend and frontend source roots (grep shows both root sets).
    - The five banned tokens each have a scanner; `/ws/handoff`, `useHandoffSocket`, and `websocket_handoff.py` are explicitly allow-listed.
    - `cd backend && python3.11 -m pytest tests/agents/test_sse_cutover_banned_patterns.py -q` PASSES on the current tree (all tokens already deleted by 44-06/44-07).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_sse_cutover_banned_patterns.py -q</automated>
  </verify>
  <done>The ratchet passes on the cutover-complete tree and bans the five tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the ratchet is non-vacuous (fires on an injected token)</name>
  <files>backend/tests/agents/test_sse_cutover_banned_patterns.py</files>
  <read_first>
    - backend/tests/agents/test_banned_patterns.py (the injection non-vacuity test test_gate_catches_injected_hand_rolled_agent :30-34)
  </read_first>
  <action>
    Add a non-vacuity test: write a temp file containing a banned token (e.g. `useWebSocket`
    or `/ws/chat`) into a temp scan dir and assert the SAME scanner reports a violation —
    proving the ratchet actually fires on a regression (guards against a dead regex passing
    silently). Mirror the injection approach in `test_banned_patterns.py`.
  </action>
  <acceptance_criteria>
    - A non-vacuity test injects a banned token and asserts the scanner flags it (test passes because it correctly DETECTS the injected violation).
    - `cd backend && python3.11 -m pytest tests/agents/test_sse_cutover_banned_patterns.py -q` PASSES (both the clean-tree assertion and the injection assertion).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_sse_cutover_banned_patterns.py -q</automated>
  </verify>
  <done>The ratchet is proven to fire on an injected banned token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI ratchet -> source tree | machine-enforces the cutover; no runtime data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-10-01 | Tampering | a dead/never-matching scanner passing silently (false confidence) | mitigate | the non-vacuity injection test proves the scanner fires on a known-bad token |
| T-44-10-02 | Elevation of privilege | over-broad ban breaking the /ws/handoff survivor | mitigate | explicit allow-list for /ws/handoff, useHandoffSocket, websocket_handoff.py; word-boundary match so useHandoffSocket does not substring-hit the useWebSocket ban |
| T-44-10-SC | Tampering | package installs | accept | no pip installs (test only) |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_sse_cutover_banned_patterns.py -q` green (clean-tree + non-vacuity).
</verification>

<success_criteria>
A non-vacuous CI ratchet bans the five SSE-cutover tokens, allow-lists the
/ws/handoff survivor, and passes on the cutover-complete tree.
</success_criteria>

## Artifacts this phase produces
- NEW `backend/tests/agents/test_sse_cutover_banned_patterns.py` — the "hard cutoff done" ratchet.

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-10-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
