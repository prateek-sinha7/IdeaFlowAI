---
phase: 44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/providers/RunConnectionProvider.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
autonomous: true
requirements: [W1]
must_haves:
  truths:
    - "A run launched after page load streams pipeline_start -> agent_* -> wave_* -> pipeline_complete LIVE over SSE (GET /api/runs/{id}/events/stream) with no /ws/chat frame required for the down-channel."
    - "The pipeline reducer (handlePipelineMsg), the questionnaire panel, and the review-gate panel all update from the SSE fan-out (runConnection.subscribe)."
    - "connectionStatus / reconnect surfaced to DashboardLayout reflect runConnection.phase (SSE), not only the WS socket."
  artifacts:
    - path: "frontend/src/providers/RunConnectionProvider.tsx"
      provides: "sendCommand returns the created run_id; an explicit attach fires for a newly launched run (reattach wired)"
      contains: "run_id"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "runConnection.subscribe feeds handleWebSocketMessage (pipeline/questionnaire/review), plus launch->attach"
      contains: "runConnection.subscribe"
    - path: "frontend/src/app/layout.tsx"
      provides: "RunConnectionProvider mounted with SSE active for this phase"
  key_links:
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "handleWebSocketMessage"
      via: "runConnection.subscribe adapter"
      pattern: "runConnection\\.subscribe"
    - from: "frontend/src/providers/RunConnectionProvider.tsx"
      to: "RunStreamConnection (attach)"
      via: "capture run_id from POST /api/runs then reattach"
      pattern: "reattach|run_id"
---

<objective>
W1 — the PREREQUISITE. Re-source the pipeline down-channel from SSE so every
`agent_*` / `wave_* `/ `pipeline_*` / questionnaire / review-gate event flows
from `runConnection.subscribe` (the per-run SSE fan-out) instead of the WS
`useWebSocket` onMessage. Implement the launch->attach bootstrap so a run
launched after boot streams live immediately, and re-source connection status
from `runConnection.phase`. Nothing else in Phase 44 can be verified over
SSE-only until this lands.

Purpose: today the pipeline reducer at `dashboard/page.tsx:877-918` is fed
EXCLUSIVELY by `useWebSocket`; `runConnection.subscribe` feeds only the chat
transcript. Deleting the WS transport (W4) without this re-source would break
all pipeline events. This plan makes SSE the live down-channel while the WS
socket still exists (transitional — commands still ride WS until W2/W3).
Output: SSE-driven pipeline UI + a working launch->attach + phase-sourced
connection status.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md
@frontend/src/providers/RunConnectionProvider.tsx
@frontend/src/hooks/useRunStream.ts
@frontend/src/app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make RunConnectionProvider launch-aware (sendCommand returns run_id, attach the new run)</name>
  <files>frontend/src/providers/RunConnectionProvider.tsx</files>
  <read_first>
    - frontend/src/providers/RunConnectionProvider.tsx (sendCommand :275-292 discards the POST body; reattach :270-273 has ZERO callers; RunStreamConnection attach :139-175; liveRunIds/refreshLiveRuns :194-217)
    - frontend/src/hooks/useRunStream.ts (attach/afterSeq contract)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (R4 — launch->attach missing)
  </read_first>
  <action>
    Change `sendCommand` (currently `Promise&lt;void&gt;`) to parse the JSON response of the
    `POST /api/runs` create call and RETURN the created `run_id` (string | null); the
    per-run `POST /api/runs/{id}/messages` branch continues to return null. Add an
    imperative `attachRun(runId: string)` on the context value that adds the id to the
    attached set immediately (so the run streams without waiting for the next
    `refreshLiveRuns` poll) and de-dupes against `liveRunIds` — model it on
    `refreshLiveRuns`'s set-diff so it never remounts an already-attached
    RunStreamConnection. Keep `reattach()` for the boot/wake path. Do NOT remove the
    flag guards in this plan (W4 owns de-flagging); only ensure that when the provider
    is enabled the new run attaches. Update `RunConnectionContextValue`, `DEFAULT_VALUE`,
    and the `useMemo` value object to include `attachRun` and the new `sendCommand`
    return type.
  </action>
  <acceptance_criteria>
    - `sendCommand(null, payload)` resolves to the server `run_id` string (source: the fetch response is parsed and its `run_id`/`id` returned; not `void`).
    - `attachRun` exists on the context value and adds a run id to the attached set idempotently (a second call with the same id does not change `liveRunIds` identity).
    - `cd frontend && npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The provider can report a launched run's id and attach its SSE stream on demand.</done>
</task>

<task type="auto">
  <name>Task 2: Feed the pipeline/questionnaire/review router from SSE + wire launch->attach in the dashboard</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/app/layout.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/page.tsx (handleWebSocketMessage :295 routes pipeline frames to handlePipelineMsgRef :488; the WS hook :877-881; chatSubscribe/sseEnabled :904-918; the launch onStartPipeline path around :1440-1477)
    - frontend/src/providers/RunConnectionProvider.tsx (the subscribe/attachRun/sendCommand contract from Task 1)
    - frontend/src/app/layout.tsx (RunConnectionProvider mount :46 — already mounted; enabled defaults to the flag)
    - .planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-CONTEXT.md (R1, R4)
  </read_first>
  <action>
    In `layout.tsx` pass `enabled={true}` to the mounted `RunConnectionProvider` so SSE is
    the ACTIVE transport for this phase (the flag machinery is removed wholesale in W4;
    this is the interim activation, not a new flag). In `dashboard/page.tsx` subscribe to
    `runConnection.subscribe` with an adapter that maps each `RunStreamMessage` (`{type, data}`)
    into the shape `handleWebSocketMessage` consumes and invokes it — so pipeline
    (`agent_*`/`wave_*`/`pipeline_*`), `questionnaire_ready`, `review_gate_ready`,
    `review_gate_approved`, `pipeline_cancelled`/`pipeline_failed`, and `step` frames all
    route through the existing switch (which already dispatches to `handlePipelineMsgRef`
    at :488 and the setState reducers). Keep the existing WS `onMessage` wiring in place
    for now (transitional dual-listen is acceptable this wave — commands still ride WS
    until W2/W3; de-dup is handled by the reducers' idempotency and event_id). Implement
    launch->attach: at the run-launch site capture the `run_id` returned by
    `sendCommand`/`startPipeline` and call `runConnection.attachRun(run_id)` so the fresh
    run streams live (closes R4). Do NOT delete `chatWsSubscribe`/`legacyChatSend`/
    `sseEnabled` here (W4 owns that) — only ensure the pipeline down-channel now flows
    from SSE when enabled.
  </action>
  <acceptance_criteria>
    - `grep -n "runConnection.subscribe" frontend/src/app/dashboard/page.tsx` shows a subscription whose callback reaches `handleWebSocketMessage` (pipeline/questionnaire/review frames), not only the chat transcript.
    - A launched run triggers `runConnection.attachRun(...)` with the id returned from the launch (source assertion: grep shows `attachRun` called with the launch run_id).
    - `layout.tsx` mounts `RunConnectionProvider` with `enabled={true}`.
    - `cd frontend && npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Pipeline/questionnaire/review events render from SSE; a post-boot run streams live.</done>
</task>

<task type="auto">
  <name>Task 3: Re-source connectionStatus / reconnect from runConnection.phase</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (the reconnect_pipeline effect :613-654; ConnectionStatus type import :36)
    - frontend/src/app/dashboard/page.tsx (connectionStatus/reconnect from useWebSocket :877; how they thread to DashboardLayout)
    - frontend/src/providers/RunConnectionProvider.tsx (phase / RunConnectionPhase from useRunStream)
  </read_first>
  <action>
    Derive the connection status the dashboard hands to DashboardLayout from
    `runConnection.phase` (RunConnectionPhase) so the header/reconnect UI reflects the
    SSE connection when SSE is active; map the phase enum to the existing
    `ConnectionStatus` shape at the boundary (do not widen the global enum). The WS
    reconnect_pipeline effect (`DashboardLayout.tsx:613-654`) becomes redundant under
    SSE (useRunStream owns Last-Event-ID replay) — guard it so it is a no-op when the
    SSE transport is active (its full removal, plus the ConnectionStatus type migration,
    is W4's `useWebSocket` deletion). Keep behavior on the still-present WS path
    unchanged for the transition.
  </action>
  <acceptance_criteria>
    - The status DashboardLayout receives is derived from `runConnection.phase` when SSE is active (source assertion: grep for `runConnection.phase` feeding the status prop).
    - The `reconnect_pipeline` WS effect does not fire while the SSE transport is active (guarded).
    - `cd frontend && npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Connection status/reconnect reflect the SSE phase; the WS reconnect frame is dormant under SSE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser -> SSE stream (GET /api/runs/{id}/events/stream) | the run's live event down-channel; already owner-scoped (two-layer 404 in run_stream.py) |
| browser -> POST /api/runs (launch) | untrusted launch input; owner-scoped run creation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | Information disclosure | attachRun / subscribe fan-out | mitigate | attachRun only adds ids the server returned for the authed user's own launch; the SSE stream itself enforces the owner 404 fence (run_stream.py) so a forged id yields no frames |
| T-44-01-02 | Tampering | launch->attach run_id capture | accept | run_id comes from the server response to the user's own POST /api/runs; no client-chosen id is trusted for attach |
| T-44-01-SC | Tampering | package installs | accept | no npm/pip package installs in this plan (FE rewire only) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- Manual/e2e-adjacent (covered green in W5): a run launched after boot renders live agent/wave/pipeline progress sourced from SSE; questionnaire + review-gate panels open from SSE frames.
- No backend change in this plan — the 5 characterization goldens and test_wire_parity are unaffected (FE-only).
</verification>

<success_criteria>
The pipeline down-channel, questionnaire, and review-gate UI all update from
`runConnection.subscribe`; a post-boot run streams live via launch->attach;
connection status is sourced from `runConnection.phase`. `npx tsc --noEmit` clean.
</success_criteria>

## Artifacts this phase produces
- `RunConnectionProvider.tsx`: `sendCommand` returns the launched `run_id`; new `attachRun(runId)` imperative attach.
- `dashboard/page.tsx`: SSE `subscribe` -> `handleWebSocketMessage` adapter; launch->attach wiring.
- `layout.tsx`: `RunConnectionProvider` activated (`enabled={true}`) for the phase.

<output>
Create `.planning/phases/44-sse-only-hard-cutoff-run-revision-retirement-and-part-b-auto/44-01-SUMMARY.md` when done.
Note: after any `state.*` handler call, restore the project-wide progress block
(total_phases:37 / completed_phases:35 / total_plans:208 / completed_plans:207 /
percent:95) — the state.* handlers clobber it (STATE.md quirk).
</output>
