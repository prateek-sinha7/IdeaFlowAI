---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/tests/zzz-baseline.spec.ts
  - frontend/e2e/fidelity/capture-mocks.mjs
  - frontend/e2e/fidelity/assemble-gallery.mjs
  - frontend/e2e/fidelity/README.md
autonomous: true
requirements: [RUNUI-06, RUNUI-09]
must_haves:
  truths:
    - "The run-screen fidelity gallery can render OUR screen paused at planning, clarify-AWAITING, and gate-AWAITING (not just settled/live/failed) side-by-side vs the mock."
    - "The three VelocityAI run mocks are captured for the same paused/planning/failed states as TARGET cells."
    - "A reviewer can regenerate any single new state section with --state / --surface without re-capturing all."
  artifacts:
    - path: "frontend/e2e/tests/zzz-baseline.spec.ts"
      provides: "Capture of OUR run screen paused at planning/clarify-awaiting/gate-awaiting states"
      contains: "clarify-awaiting"
    - path: "frontend/e2e/fidelity/capture-mocks.mjs"
      provides: "TARGET capture of the Live/Failed mock paused states"
    - path: "frontend/e2e/fidelity/assemble-gallery.mjs"
      provides: "Side-by-side pairing incl. the new paused/planning state rows"
  key_links:
    - from: "frontend/e2e/tests/zzz-baseline.spec.ts"
      to: "frontend/e2e/fidelity/shots/current/"
      via: "page.screenshot tagged {surface}__{state}"
      pattern: "shot\\(page"
---

<objective>
Extend the Phase-39 run-screen fidelity harness (`frontend/e2e/fidelity/`) so it becomes the acceptance oracle for Phase 42's paused/planning states — the states the later waves re-align. Today `zzz-baseline.spec.ts` only PASSES THROUGH clarify/gate (it answers them immediately) and captures settled/live/failed. This plan makes it PAUSE at planning, clarify-AWAITING, and gate-AWAITING and screenshot the run screen there, and captures the matching TARGET mock states, then pairs them in `gallery.html`.

This is CONTEXT §5 Wave 0 (the oracle). It is capture-infrastructure only — no product code, no assertions that gate CI.

Purpose: every later Phase-42 wave closes each state on a side-by-side screenshot-diff vs the mock (the Phase-39/41 discipline). Without the paused-state capture there is no oracle for A/B/C/F/G/H.
Output: paused/planning/gate-awaiting/clarify-awaiting capture on both sides + gallery pairing.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/e2e/fidelity/README.md
@frontend/e2e/tests/zzz-baseline.spec.ts
@frontend/e2e/fidelity/capture-mocks.mjs
@frontend/e2e/fidelity/assemble-gallery.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture OUR run screen paused at planning / clarify-awaiting / gate-awaiting</name>
  <files>frontend/e2e/tests/zzz-baseline.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/zzz-baseline.spec.ts (the existing settled/live/failed capture flow + the mockWs driver calls: `plannerStart`, `questionnaireReady`, `reviewGateReady`, `reviewGateApproved`, the `shot()` helper, the `tab()` helper)
    - frontend/e2e/fidelity/README.md (surface/state tag conventions; `shots/current/{surface}__{state}.png`)
    - .planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md §2 (the cascade that currently OWNS the right panel during these states) + §5 W0
  </read_first>
  <action>
    Add THREE new capture tests (env-gated by the existing `FIDELITY_CAPTURE` skip) that drive our run screen into the paused/planning states and screenshot WITHOUT advancing past them — the opposite of the existing flow which answers clarify/gate immediately.
    (1) PLANNING: launch + `mockWs.start` + `seedBrief` + `mockWs.plannerStart()` and STOP (do not call `plannerComplete`); wait, then `shot(page, "full", "planning")` and a left-lane clip `leftlane__planning`. This exercises the running-with-0-agents state (§2 branch 1).
    (2) CLARIFY-AWAITING: through `plannerComplete` then `mockWs.questionnaireReady(CLARIFY_Q)` and STOP (do not click submit); wait for `chat-clarify-actions`; `shot(page,"full","clarifyawaiting")`; also click the Steps tab and `shot(page,"steps","clarifyawaiting")`; left-lane clip `leftlane__clarifyawaiting`.
    (3) GATE-AWAITING: advance to the spec agent, `mockWs.reviewGateReady({...})` and STOP (do not call `reviewGateApproved`); wait; `shot(page,"full","gateawaiting")`; Steps tab `shot(page,"steps","gateawaiting")`; left-lane clip `leftlane__gateawaiting`.
    Reuse the existing `launch`, `seedBrief`, `AGENTS`, `CLARIFY_Q`, `shot`, `tab` helpers — do NOT fork the MockApi/mockWs fixture (INV-12; one shared stub). Keep `test.setTimeout` generous as the sibling tests do. These capture the CURRENT (pre-W1) legacy full-screen takeover render — that is the correct "before" baseline; later waves regenerate the "after".
  </action>
  <acceptance_criteria>
    <automated>cd frontend && FIDELITY_CAPTURE=1 npx playwright test zzz-baseline 2>&1 | tail -5 (all capture tests pass/skip cleanly; no unhandled errors)</automated>
    <automated>ls frontend/e2e/fidelity/shots/current/ | grep -E "planning|clarifyawaiting|gateawaiting" (the new-state PNGs exist)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0 new type errors in the spec)</automated>
  </acceptance_criteria>
  <done>zzz-baseline emits `full__planning`, `full__clarifyawaiting`, `steps__clarifyawaiting`, `full__gateawaiting`, `steps__gateawaiting` plus the three left-lane clips into shots/current/.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the TARGET mock paused states + pair them in the gallery</name>
  <files>frontend/e2e/fidelity/capture-mocks.mjs, frontend/e2e/fidelity/assemble-gallery.mjs, frontend/e2e/fidelity/README.md</files>
  <read_first>
    - frontend/e2e/fidelity/capture-mocks.mjs (how it drives the `.dc.html` mock per tab/state via the local server)
    - frontend/e2e/fidelity/assemble-gallery.mjs (how target/* + current/* are paired into gallery.html; the `--surface`/state filtering)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (the `isClarify`/`isGate`/`clarAwaiting`/gate-awaiting `sc-if` blocks — the TARGET paused states)
  </read_first>
  <action>
    In `capture-mocks.mjs`: add TARGET captures of the Live mock driven into its paused variants (the `sc-if` state props for planning, clarify-awaiting, gate-awaiting) → `shots/target/{surface}__{planning|clarifyawaiting|gateawaiting}.png`, mirroring how the existing failed/live targets are captured. If the DC runtime can be state-driven only by the top-level state prop, capture the whole-screen `full__*` for each and the Steps region where separable.
    In `assemble-gallery.mjs`: register the new state tags so `target/*` and `current/*` for planning/clarifyawaiting/gateawaiting pair left/right in `gallery.html`; keep the existing ND-A..ND-H "expected — ignore" register header intact and ADD one row noting that during Wave 0 our side still shows the legacy full-screen takeover (a known pre-fix state, not an ND).
    In `README.md`: document the new state tags (`planning`, `clarifyawaiting`, `gateawaiting`) under the surface/state list + the regen command for a single new state.
  </action>
  <acceptance_criteria>
    <automated>node frontend/e2e/fidelity/assemble-gallery.mjs 2>&1 | tail -3 (assembles without error)</automated>
    <automated>grep -E "clarifyawaiting|gateawaiting|planning" frontend/e2e/fidelity/assemble-gallery.mjs (the new states are registered in the assembler)</automated>
    <automated>grep -E "clarifyawaiting|gateawaiting|planning" frontend/e2e/fidelity/README.md (documented)</automated>
    <human-check>Open gallery.html: the planning / clarify-awaiting / gate-awaiting rows render TARGET (mock) beside CURRENT (ours). Confirm the new rows are present and paired (our side legitimately still shows the legacy takeover pre-W1).</human-check>
  </acceptance_criteria>
  <done>The gallery pairs the three new paused/planning states target-vs-current; README + assembler list the new tags; single-state regen is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → filesystem | Screenshots written under git-ignored `shots/`; no product input crosses. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Tampering | zzz-baseline capture spec | accept | Test-only, env-gated by FIDELITY_CAPTURE; never runs in CI green path (README + `test.skip`). No product surface touched. |
| T-42-01-SC | Tampering | npm installs | accept | No package installs in this plan; reuses installed Playwright. |
</threat_model>

<verification>
- `FIDELITY_CAPTURE=1 npx playwright test zzz-baseline` produces the five new current-side PNGs.
- `node assemble-gallery.mjs` pairs the new states; gallery opens with the new rows.
- No product `.tsx`/backend file is modified (git diff --stat lists only e2e/fidelity files).
</verification>

<success_criteria>
The fidelity gallery is the working oracle for Phase 42's paused/planning states: our screen and the mock are captured and paired for planning, clarify-awaiting, and gate-awaiting, in addition to the existing settled/live/failed.
</success_criteria>

<artifacts_this_phase_produces>
- New capture tests in `zzz-baseline.spec.ts` emitting `full__planning`, `full__clarifyawaiting`, `steps__clarifyawaiting`, `full__gateawaiting`, `steps__gateawaiting`, and `leftlane__{planning,clarifyawaiting,gateawaiting}`.
- New TARGET captures in `capture-mocks.mjs` for the same three states.
- New state tags registered in `assemble-gallery.mjs` + documented in `README.md`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-01-SUMMARY.md` when done.
</output>
