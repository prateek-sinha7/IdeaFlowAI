---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 03
type: execute
wave: 3
depends_on: [42-02]
files_modified:
  - frontend/src/components/chat/RunChatLane.tsx
  - frontend/src/components/preview/PreviewPanel.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-08]
must_haves:
  truths:
    - "During clarify/gate the lane composer is a plain text hint input (not a duplicate answer UI); the answer surface is Steps only."
    - "On a terminal-failed run the right panel shows tabs [Steps, Audit, Files] with no Preview tab, defaulting to Audit."
    - "A failed run no longer shows the amber DegradedRunAffordance on the run screen."
  artifacts:
    - path: "frontend/src/components/chat/RunChatLane.tsx"
      provides: "FreeTextComposer hint for clarify/gate composer states"
      contains: "composerHint"
    - path: "frontend/src/components/preview/PreviewPanel.tsx"
      provides: "Failed-run tab set (no Preview, default Audit)"
  key_links:
    - from: "frontend/src/components/chat/RunChatLane.tsx"
      to: "FreeTextComposer"
      via: "clarify/gate composer cases render a hint input, not InlineClarify/InlineGate"
      pattern: "FreeTextComposer"
---

<objective>
Wave 2 (CONTEXT §5) — collapse the triple answer-UI and align the failed run. Group C: the lane composer during clarify/gate becomes a plain text hint input (the mock's composer is NOT a second answer surface — Steps is the sole answer surface). Group D: a terminal-failed run drops the Preview tab, defaults to Audit, and retires the amber DegradedRunAffordance on the RUN screen (red, not amber, per the Failed mock).

Purpose: `RunChatLane.tsx:717-746` currently mounts the FULL InlineClarifyActions/InlineGateActions in the composer, duplicating Steps (the triple-implementation of CONTEXT §2). The Failed mock (`Hexaware Run - Failed.dc.html`) shows tabs `[steps,audit,files]`, defaults `tab:'audit'`, and has NO Preview surface — failed uses red `#A33A32`.
Output: a lane composer that is a phase-hint during clarify/gate; a failed run tab set without Preview defaulting to Audit; DegradedRunAffordance retired on the run screen.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/src/components/chat/RunChatLane.tsx
@frontend/src/components/preview/PreviewPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lane composer = plain hint during clarify/gate (Group C)</name>
  <files>frontend/src/components/chat/RunChatLane.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunChatLane.tsx (`renderComposerBody` switch :717-746 — the clarify case mounts InlineClarifyActions :719-726, the gate case mounts InlineGateActions :728-746; the `FreeTextComposer` pattern already used for building/complete :754-758; the KEEP `AwaitingCard` status card :359-398)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (the composer during clarify/gate is a plain input with a phase hint, e.g. "Answer the questions above to continue…" / "Approve the plan above, or add a note…")
    - CONTEXT §C (Steps = sole answer surface; KEEP the AwaitingCard status card)
  </read_first>
  <action>
    In `renderComposerBody`, replace the `clarify` and `gate` cases' full InlineClarifyActions/InlineGateActions mounts with a `FreeTextComposer` hint input (the same component the building/complete cases use), passing a per-state `composerHint` placeholder: clarify → "Answer the questions above to continue…", gate → "Approve the plan above, or add a note…". A free-text turn in these states routes as a steering note through the existing send path (do NOT invent a new channel; reuse the building-mode `handleFreeText`/steering behavior). KEEP the `AwaitingCard` status card (:359-398, :960-991) — it already matches the mock and stays as the lane's status indicator. The Steps InlineClarifyActions/InlineGateActions (now the sole answer surface after 42-02) are untouched here. Retain the InlineClarifyActions/InlineGateActions IMPORTS only if still referenced elsewhere in the lane; remove them if the composer was their only consumer (grep before removing).
  </action>
  <acceptance_criteria>
    <automated>grep -n "case \"clarify\"\|case \"gate\"" frontend/src/components/chat/RunChatLane.tsx (both cases now render FreeTextComposer, verified by reading the block)</automated>
    <automated>grep -c "composerHint\|FreeTextComposer" frontend/src/components/chat/RunChatLane.tsx (>= 1)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run RunChatLane 2>&1 | tail -5 (green; adjust the clarify/gate composer test to assert a hint input, not the full inline actions)</automated>
    <human-check>In the clarify/gate gallery states, the LANE composer is a plain hint input (not a duplicate answer form); the AwaitingCard status card is still present; the answer form lives only in Steps.</human-check>
  </acceptance_criteria>
  <done>The lane composer is a phase-hint FreeTextComposer during clarify/gate; the AwaitingCard status card is preserved; the triple answer-UI is collapsed to Steps + status card.</done>
</task>

<task type="auto">
  <name>Task 2: Failed run — drop the Preview tab, default Audit, retire DegradedRunAffordance (Group D)</name>
  <files>frontend/src/components/preview/PreviewPanel.tsx</files>
  <read_first>
    - frontend/src/components/preview/PreviewPanel.tsx (TAB_CONFIG :351-355; the `terminalFailure`/`isCancelledTerminal`/`headerFailed` signals :593-627; the state-keyed default-tab effect added in 42-02; the DegradedRunAffordance component :374 + its Preview-tab mount :935-945; the failed FilesTab/Audit already match per KEEP)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html (tabs `[steps,audit,files]` :230, default `tab:'audit'` :194, red `#A33A32` :95, NO Preview surface)
    - CONTEXT §D + §4 KEEP (failed Steps/Audit/Files/header already match) + §8 decision 4 (RunDetailPage history reopen is OUT OF SCOPE)
  </read_first>
  <action>
    For a TERMINAL-FAILURE run (the generic `terminalFailure && !isCancelledTerminal` signal — SC-001, never a workflow-name literal), compute the tab set as [Steps, Audit, Files] with NO Preview tab (filter `preview` out of TAB_CONFIG when terminal-failed), and make the default-tab effect (42-02) resolve failed → Audit. Remove the DegradedRunAffordance mount from the Preview branch on the run screen (it was the only reason a failed Preview tab existed). Leave `DegradedRunAffordance` the exported component in place — it is still used by `RunDetailPage.tsx:296` (history reopen), which is OUT OF SCOPE (§8 decision 4); only its run-screen Preview mount is removed. Keep the failed Files "Build incomplete" banner, the Audit "governance stopped this run" verdict, and the red failed header badge (all §4 KEEP). Ensure a non-failed run is unchanged (all four tabs, default Preview).
  </action>
  <acceptance_criteria>
    <automated>grep -c "DegradedRunAffordance" frontend/src/components/preview/PreviewPanel.tsx (the component import/export may remain for RunDetailPage; verify the Preview-branch MOUNT is gone by reading the failed Preview block)</automated>
    <automated>grep -c "DegradedRunAffordance" frontend/src/components/results/RunDetailPage.tsx (>= 1 — history reopen still uses it, unchanged)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run PreviewPanel 2>&1 | tail -5 (green; assert failed → tab set has no Preview + default Audit; non-failed → four tabs default Preview)</automated>
    <human-check>The failed gallery state shows tabs [Steps, Audit, Files] with no Preview, lands on Audit, and shows no amber DegradedRunAffordance; a non-failed run still has Preview and lands on Preview.</human-check>
  </acceptance_criteria>
  <done>A terminal-failed run drops Preview, defaults Audit, and no longer mounts DegradedRunAffordance on the run screen; RunDetailPage history reopen is untouched; non-failed behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| engine events → run screen | Agent/gate output rendered in lane + tabs (model-produced). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03-01 | Tampering (XSS) | Lane hint composer / failed tabs | mitigate | FreeTextComposer + tab filtering render existing escaped-text surfaces; no dangerouslySetInnerHTML added. |
| T-42-03-02 | Denial of Service | Failed-run tab computation | accept | Pure client-side derivation from an existing terminal-failure boolean; no new fetch. |
| T-42-03-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Lane clarify/gate composer renders a hint input; Steps remains the answer surface; AwaitingCard preserved.
- Failed run: [Steps, Audit, Files], default Audit, no DegradedRunAffordance mount; RunDetailPage unchanged.
- tsc clean; PreviewPanel + RunChatLane vitest green.
- INV-3/LOCK-B: only frontend `.tsx` changed.
</verification>

<success_criteria>
The paused-composer and failed-run states match their mocks: the lane composer is a plain phase-hint during clarify/gate, and a failed run shows the Preview-less, Audit-default tab set without the amber degraded affordance.
</success_criteria>

<artifacts_this_phase_produces>
- `composerHint` per-state hint on the lane's clarify/gate composer (FreeTextComposer reuse).
- Terminal-failure tab-set derivation (Preview filtered out) + failed→Audit default in PreviewPanel.
- Removal of the run-screen DegradedRunAffordance Preview mount (component retained for RunDetailPage).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-03-SUMMARY.md` when done.
</output>
