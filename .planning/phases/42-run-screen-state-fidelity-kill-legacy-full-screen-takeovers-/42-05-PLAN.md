---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 05
type: execute
wave: 3
depends_on: [42-02]
files_modified:
  - frontend/src/components/results/artifactPreview.tsx
  - frontend/src/components/results/artifactPreview.test.tsx
  - frontend/src/components/preview/ReviewGatePanel.tsx
  - frontend/src/components/preview/ReviewGatePanel.test.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-08]
must_haves:
  truths:
    - "A single shared module exposes the name-free artifact discriminator + the Spec/Tasks/Analysis parsers/previews."
    - "The discriminator keys ONLY on artifactKind + the <spec>/<tasks>/<analysis> tags — never a workflow-name or agent-id literal (SC-001)."
    - "ReviewGatePanel.tsx is deleted; no orphaned parser logic and no second copy (INV-12)."
  artifacts:
    - path: "frontend/src/components/results/artifactPreview.tsx"
      provides: "discriminateArtifact() + SpecPreview/TasksPreview/AnalysisPreview shared module"
      exports: ["discriminateArtifact", "SpecPreview", "TasksPreview", "AnalysisPreview"]
      min_lines: 120
    - path: "frontend/src/components/results/artifactPreview.test.tsx"
      provides: "Characterization tests for the discriminator + parsers"
  key_links:
    - from: "frontend/src/components/results/artifactPreview.tsx"
      to: "(consumers 42-08 gate + 42-09 cards)"
      via: "one parser, >= 2 consumers"
      pattern: "export function discriminateArtifact"
---

<objective>
Wave 4-a (CONTEXT §5 + §9 + §A4, INV-12) — extract the shared artifact-preview module. The `<spec>/<tasks>/<analysis>` name-free discriminator and the Spec/Tasks/Analysis parsers currently live INSIDE `ReviewGatePanel.tsx` (SpecPreview :57-102, TasksPreview :104-160, AnalysisPreview :174-200, discriminator :270-273). The settled artifact cards (42-09) AND the gate plan-preview (42-08) both need them. Extract them into `components/results/artifactPreview.tsx` as a reusable module FIRST, then delete `ReviewGatePanel.tsx` (completing §A4 — the file whose branch 42-02 already removed). This is interface-first: this plan defines the contract the two W4 consumers build against.

Purpose: one implementation per behavior (INV-12); no orphaned parser logic, no second copy. Deleting ReviewGatePanel.tsx before extracting would strand the parsers; extracting is the prerequisite.
Output: `artifactPreview.tsx` (discriminator + three previews) with tests; `ReviewGatePanel.tsx` deleted.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/src/components/preview/ReviewGatePanel.tsx
@frontend/src/components/preview/ReviewGatePanel.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the shared artifactPreview module (discriminator + parsers)</name>
  <files>frontend/src/components/results/artifactPreview.tsx, frontend/src/components/results/artifactPreview.test.tsx</files>
  <read_first>
    - frontend/src/components/preview/ReviewGatePanel.tsx (SpecPreview :57-102 — parses `<spec>` → `## ` headings/sections; TasksPreview :104-160 — parses `<tasks>` → `## Task N: title` + `**Goal**` rows; AnalysisPreview :174-200 — parses `<analysis>` → `### Readiness verdict` + `### ` sections; the discriminator :270-273 — `artifactKind === "spec" || /<spec[\s>]/i.test(output)` etc.)
    - frontend/src/components/preview/ReviewGatePanel.test.tsx (the existing parser/discriminator characterization — the behavior to preserve)
    - CONTEXT §9 (the four card elements + the reuse/no-dual-impl mandate) + §6 SC-001
  </read_first>
  <behavior>
    - discriminateArtifact(output, artifactKind) → "spec" when artifactKind==="spec" OR /<spec[\s>]/i matches output.
    - discriminateArtifact → "tasks" when artifactKind==="task_list" OR /<tasks[\s>]/i matches.
    - discriminateArtifact → "analysis" when (not spec/tasks) AND (artifactKind==="summary" OR /<analysis[\s>]/i matches).
    - discriminateArtifact → null when no artifactKind and no recognized tag (an ordinary agent output renders no card — SC-001 generic degrade).
    - discriminateArtifact NEVER inspects a workflow name or agent id (input is only output + artifactKind).
    - SpecPreview(content) extracts `## ` sections from within `<spec>…</spec>` (or the raw content when unwrapped), preserving the current section split.
    - TasksPreview(content) extracts `## Task N: title` blocks + `**Goal**` lines, preserving numbering.
    - AnalysisPreview(content) extracts the `### Readiness verdict` first line + `### ` sections, preserving the READY/CAUTION/NEEDS-REVISION classification.
  </behavior>
  <action>
    Create `components/results/artifactPreview.tsx`. Move (not copy) the `SpecPreview`, `TasksPreview` (drop its `onTasksChange` edit affordance — the cards + gate preview are read-only; the editable gate path stays via InlineGateActions, which is separate), and `AnalysisPreview` presentational components verbatim from ReviewGatePanel.tsx, and add an exported pure `discriminateArtifact(output: string, artifactKind?: string): "spec" | "tasks" | "analysis" | null` factored from the ReviewGatePanel discriminator (:270-273). Export all four symbols. Write `artifactPreview.test.tsx` porting the ReviewGatePanel discriminator/parser characterization (RED first: run it against the new module before the move is wired, confirm it fails, then GREEN). Keep the module presentation-only: render parsed content as escaped React text (no dangerouslySetInnerHTML). Do NOT introduce any new fetch or data source (SC-001 — the cards consume in-state data in 42-08/42-09).
  </action>
  <acceptance_criteria>
    <automated>grep -c "export function discriminateArtifact\|export const discriminateArtifact" frontend/src/components/results/artifactPreview.tsx (== 1)</automated>
    <automated>cd frontend && grep -Ec "pipeline_type|spec\\.id ==|prototype-|/build\\|construct/i" src/components/results/artifactPreview.tsx (== 0 — SC-001, no name literal)</automated>
    <automated>cd frontend && grep -c "dangerouslySetInnerHTML" src/components/results/artifactPreview.tsx (== 0)</automated>
    <automated>cd frontend && npx vitest run artifactPreview 2>&1 | tail -5 (all discriminator + parser cases green)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
  </acceptance_criteria>
  <done>artifactPreview.tsx exports discriminateArtifact + SpecPreview/TasksPreview/AnalysisPreview; the ported characterization tests pass; no name literal; no dangerouslySetInnerHTML.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ReviewGatePanel.tsx (completes §A4)</name>
  <files>frontend/src/components/preview/ReviewGatePanel.tsx, frontend/src/components/preview/ReviewGatePanel.test.tsx</files>
  <read_first>
    - frontend/src/components/preview/ReviewGatePanel.tsx (the full-screen gate panel — its branch was removed in 42-02; the parsers are now in artifactPreview.tsx)
    - The confirmed importer state: after 42-02 the ONLY real importer (DashboardLayout.tsx:28) is gone; verify no other real `import { ReviewGatePanel }` or parser import remains.
    - CONTEXT §A4 (ReviewGatePanel.tsx file-delete moves LAST, after extraction)
  </read_first>
  <action>
    Re-grep for any real importer of ReviewGatePanel OR its parsers (`import ... ReviewGatePanel`, `SpecPreview`/`TasksPreview`/`AnalysisPreview` imported from the preview path). If any real importer remains, repoint it to `@/components/results/artifactPreview` first. Then delete `components/preview/ReviewGatePanel.tsx` and its test `ReviewGatePanel.test.tsx` (its parser/discriminator coverage now lives in artifactPreview.test.tsx — do not keep a test for a deleted file). Add a grep-ratchet note in the SUMMARY that `ReviewGatePanel` returns 0 as a live import.
  </action>
  <acceptance_criteria>
    <automated>test -f frontend/src/components/preview/ReviewGatePanel.tsx && echo FAIL || echo OK (deleted)</automated>
    <automated>cd frontend && grep -rn "ReviewGatePanel" src 2>/dev/null | grep -c "import" (== 0 — no live import; comments elsewhere are fine)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run artifactPreview 2>&1 | tail -3 (still green after the delete)</automated>
  </acceptance_criteria>
  <done>ReviewGatePanel.tsx + its test are deleted; the discriminator/parsers survive only in artifactPreview.tsx (one copy, INV-12); no live import remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent output text → parsed preview | The extracted parsers consume model-produced `<spec>/<tasks>/<analysis>` output and render it. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Tampering (XSS) | artifactPreview parsers | mitigate | Parsers output plain strings rendered as escaped React text; grep guard `dangerouslySetInnerHTML == 0` in the new module. |
| T-42-05-02 | Information Disclosure | Discriminator | mitigate | Keys only on output + artifactKind (no cross-run data, no name inference); SC-001 grep guard. |
| T-42-05-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- artifactPreview.tsx exports the discriminator + three previews; characterization tests green; SC-001 + no-innerHTML greps clean.
- ReviewGatePanel.tsx + test deleted; no live import remains; tsc clean.
- INV-12: one copy of the parser/discriminator; INV-3/LOCK-B: frontend-only.
</verification>

<success_criteria>
The name-free artifact discriminator + Spec/Tasks/Analysis parsers exist once, in a shared module the two W4 consumers (gate plan-preview, settled cards) build against, and ReviewGatePanel.tsx is deleted with no orphaned logic.
</success_criteria>

<artifacts_this_phase_produces>
- New module `components/results/artifactPreview.tsx` exporting `discriminateArtifact`, `SpecPreview`, `TasksPreview`, `AnalysisPreview`.
- New `artifactPreview.test.tsx` (ported characterization).
- Deletions: `ReviewGatePanel.tsx` + `ReviewGatePanel.test.tsx`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-05-SUMMARY.md` when done.
</output>
