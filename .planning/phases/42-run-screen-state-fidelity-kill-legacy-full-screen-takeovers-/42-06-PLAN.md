---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 06
type: execute
wave: 3
depends_on: [42-02]
files_modified:
  - frontend/src/components/results/StepsOverviewSpine.tsx
  - frontend/src/components/chat/InlineClarifyActions.tsx
autonomous: true
requirements: [RUNUI-06]
must_haves:
  truths:
    - "The Steps overview header shows the mock's phase pill (CLARIFY / REVIEW / BUILDING) + phase label, not just Running/Complete/Failed."
    - "The running spine row uses the mock's violet running-row highlight (no Zap 'Live' badge in the overview spine)."
    - "The clarify submit copy reads 'Submit answers & start the build'; the extra clarify affordances the mock omits are removed."
    - "The Cancel-Workflow affordance (added in 42-02) survives the clarify-affordance cleanup."
  artifacts:
    - path: "frontend/src/components/results/StepsOverviewSpine.tsx"
      provides: "Phase pill + label + violet running-row highlight"
    - path: "frontend/src/components/chat/InlineClarifyActions.tsx"
      provides: "Mock-matching single-submit clarify (extras removed)"
  key_links:
    - from: "frontend/src/components/results/StepsOverviewSpine.tsx"
      to: "the Steps overview header"
      via: "phase pill keyed on generic run state"
      pattern: "CLARIFY|REVIEW|BUILDING"
---

<objective>
Wave 4-d/e Steps + clarify polish (CONTEXT §5 Group G + the Steps parts of Group H; §8 decision 3). Add the mock's Steps-overview phase pill + label; give the running spine row the mock's violet running-row highlight (drop the Zap "Live" badge from the overview spine, keeping the LIVE pill in the L2 detail only); change the clarify submit copy; and REMOVE the extra clarify affordances the mock omits — all bound to LIVE data (D-15) and generic state (SC-001).

Purpose: these are the mock-fidelity gaps on the (now-reachable, post-42-02) Steps clarify/live surfaces. The mock's clarify is a single "Submit answers & start the build" with no per-question skip / "Use recommended" / "Rec." badge / "Anything else?" freeform / "Skip all".
Output: Steps overview phase pill/label + running-row highlight; single-submit clarify with the extras removed and the Cancel-Workflow affordance preserved.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/src/components/results/StepsOverviewSpine.tsx
@frontend/src/components/chat/InlineClarifyActions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Steps overview phase pill + label + violet running-row highlight</name>
  <files>frontend/src/components/results/StepsOverviewSpine.tsx</files>
  <read_first>
    - frontend/src/components/results/StepsOverviewSpine.tsx (the `statusLabel` header :133 "Run failed/Running/Run complete" + the header block :165-196; the running spine row :246 with the Zap "Live" badge; the generic run signals `isRunning`/`failed`/`laneGate`/`clarifyQuestions` already in props)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (the phase pill `CLARIFY/REVIEW/BUILDING` :981; the violet running-row highlight `background:#F4F2FB, border:#DED9F7` + node glow :924,928)
    - CONTEXT §G (phase pill + label copy: "Waiting on your answers" / "Paused for your approval" / "Pipeline running · N/M") + §H (drop Zap badge, add running-row highlight, keep LIVE pill in L2 only)
  </read_first>
  <action>
    In the Steps overview header, add a phase PILL + phase LABEL derived from the generic run state (SC-001): clarify present → pill "CLARIFY" + label "Waiting on your answers"; active gate → pill "REVIEW" + label "Paused for your approval"; running (no clarify/gate) → pill "BUILDING" + label "Pipeline running · N/M" (N/M from the live agent counts already available). Keep the existing failed/complete headers. Style the pill with the mock brand tokens (brand-fill/brand text, accent `#3C2CDA`).
    On the running spine row (:246): remove the Zap "Live" badge and instead apply the mock's violet running-row highlight (`background:#F4F2FB`, `border:#DED9F7`, subtle node glow) to the currently-running agent row. Do NOT touch the L2 detail header's LIVE pill (it stays — §H). Bind counts/labels to live pipelineState, never mock literals (ND-D).
  </action>
  <acceptance_criteria>
    <automated>grep -Ec "CLARIFY|REVIEW|BUILDING" frontend/src/components/results/StepsOverviewSpine.tsx (>= 1 — phase pill present)</automated>
    <automated>grep -Ec "F4F2FB|DED9F7" frontend/src/components/results/StepsOverviewSpine.tsx (>= 1 — running-row highlight)</automated>
    <automated>cd frontend && grep -Ec "pipeline_type|spec\\.id ==|prototype-" src/components/results/StepsOverviewSpine.tsx (== 0 — no name literal introduced)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run StepsDrilldown 2>&1 | tail -5 (green)</automated>
    <human-check>Steps overview (clarify/gate/building gallery states) shows the phase pill + label; the running spine row has the violet highlight and no Zap badge; the L2 detail LIVE pill is unchanged.</human-check>
  </acceptance_criteria>
  <done>The Steps overview header carries the mock's phase pill + label keyed on generic state; the running row uses the violet highlight; the Zap badge is gone from the spine; live counts drive N/M.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify submit copy + remove the extra clarify affordances (Group G, decision 3)</name>
  <files>frontend/src/components/chat/InlineClarifyActions.tsx</files>
  <read_first>
    - frontend/src/components/chat/InlineClarifyActions.tsx (the submit button "Send answers" :243; the extra affordances to remove — per-question Skip / "Use recommended" :191-193 / "Rec." badge :177 / "Anything else?" freeform block :215-235 / "Skip all" :245-254; the `onCancelWorkflow` affordance added in 42-02 — KEEP)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (the single "Submit answers & start the build" :229)
    - CONTEXT §G + §8 decision 3 (REMOVE the extras to match the mock's single submit)
  </read_first>
  <action>
    Change the submit label "Send answers" → "Submit answers & start the build" (:243). Remove the mock-omitted affordances: the per-question Skip toggle, the "Use recommended" button, the "Rec." recommended badge, the "Anything else?" optional freeform block, and the "Skip all" control (the `onSkipAll` path). Preserve the CORE behavior: rendering each question + collecting answers + the single submit firing the existing `onSubmitAnswers` (submit_questionnaire channel — do NOT touch it). KEEP the `onCancelWorkflow` affordance added in 42-02. If removing `onSkipAll` orphans a prop, remove the prop + its now-dead wiring at the call sites (StepsOverviewSpine mount) — but only the skip-all wiring, not the cancel wiring. Leave the navy→brand token reskin to 42-10 (this task is structure/copy only).
  </action>
  <acceptance_criteria>
    <automated>grep -c "Submit answers & start the build" frontend/src/components/chat/InlineClarifyActions.tsx (== 1)</automated>
    <automated>grep -Ec "Use recommended|Anything else|Skip all|Rec\\." frontend/src/components/chat/InlineClarifyActions.tsx (== 0 — extras removed)</automated>
    <automated>grep -c "onCancelWorkflow" frontend/src/components/chat/InlineClarifyActions.tsx (>= 1 — cancel preserved)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run InlineClarifyActions 2>&1 | tail -5 (green; update the removed-affordance assertions to expect the single submit)</automated>
    <human-check>The Steps clarify surface shows questions + one "Submit answers & start the build" button + the Cancel-Workflow affordance, with no per-question skip / recommended / freeform / skip-all controls — matching the Live mock.</human-check>
  </acceptance_criteria>
  <done>The clarify submit copy matches the mock; the five extra affordances are removed; the submit_questionnaire channel and the Cancel-Workflow affordance are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| engine events → Steps | Live phase/counts + clarify questions rendered (model/engine-produced). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06-01 | Tampering (XSS) | Steps header / clarify render | mitigate | Phase label + question text rendered as escaped React text; no dangerouslySetInnerHTML. |
| T-42-06-02 | Repudiation | Clarify submit channel | mitigate | The single submit fires the unchanged submit_questionnaire channel; removing UI extras does not alter the channel. |
| T-42-06-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Steps overview phase pill/label + violet running-row highlight present; no Zap in the spine; L2 LIVE pill unchanged.
- Clarify: single "Submit answers & start the build"; extras removed; cancel preserved; submit_questionnaire unchanged.
- tsc clean; StepsDrilldown + InlineClarifyActions vitest green; SC-001 grep 0.
</verification>

<success_criteria>
The Steps overview and clarify surfaces match their Live-mock intent — phase pill + label, violet running-row, single mock-copy submit with the extra affordances removed — on live data, with the shared submit channel and the re-homed cancel intact.
</success_criteria>

<artifacts_this_phase_produces>
- Steps-overview phase pill + label + violet running-row highlight in `StepsOverviewSpine.tsx`.
- Reworked `InlineClarifyActions.tsx`: mock submit copy + removal of Skip/Use-recommended/Rec./Anything-else/Skip-all.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-06-SUMMARY.md` when done.
</output>
