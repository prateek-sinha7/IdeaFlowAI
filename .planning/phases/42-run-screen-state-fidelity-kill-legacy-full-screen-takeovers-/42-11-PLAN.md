---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 11
type: execute
wave: 6
depends_on: [42-03, 42-04, 42-07, 42-08, 42-09, 42-10]
files_modified:
  - frontend/e2e/fidelity/assemble-gallery.mjs
  - frontend/e2e/fidelity/README.md
  - .planning/IMPLEMENTATION-REGISTER.md
  - .planning/ISSUES-REGISTER.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [RUNUI-06, RUNUI-09]
must_haves:
  truths:
    - "The full run-screen gallery is regenerated across every state (planning · live · clarify · gate · complete · failed) and each state is human-signed-off against its mock."
    - "Every intended divergence for Phase 42 is recorded in the fidelity ND register."
    - "ISS-035/036 (stale-OPEN, code landed in Phase 32) and ISS-037's TodoCard clause are reconciled; F1/F2 are recorded as deferred follow-ups; the IMPLEMENTATION-REGISTER + ROADMAP carry the Phase-42 entry."
    - "The touched mocked-e2e specs are green (RUNUI-09)."
  artifacts:
    - path: ".planning/IMPLEMENTATION-REGISTER.md"
      provides: "Phase 42 register entry (deletions ledger + ND register + F1/F2 deferrals)"
    - path: ".planning/ISSUES-REGISTER.md"
      provides: "Reconciled ISS-035/036/037 rows"
  key_links:
    - from: "frontend/e2e/fidelity/assemble-gallery.mjs"
      to: "the ND register"
      via: "Phase-42 intended divergences captioned expected-ignore"
      pattern: "ND-"
---

<objective>
Wave 5 (CONTEXT §5) — the close-out. Regenerate the full run-screen fidelity gallery across every state, get the per-state human sign-off (the acceptance oracle for the whole phase), record every Phase-42 intended divergence in the fidelity ND register, and reconcile the registers: the stale-OPEN ISS-035/036 (their code landed in Phase 32) and the ISS-037 TodoCard clause (deleted in 42-04), plus the F1/F2 deferred follow-ups from 42-09, plus the Phase-42 IMPLEMENTATION-REGISTER + ROADMAP entries. Confirm the touched mocked-e2e specs are green (RUNUI-09).

Purpose: prose does not close a state (the discipline that stopped 12 phases of drift before Phase 39). This plan is the consolidated end-of-phase human verification + bookkeeping.
Output: a regenerated, signed-off gallery; a complete ND register; reconciled ISSUES/IMPLEMENTATION/ROADMAP.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@.planning/ISSUES-REGISTER.md
@.planning/IMPLEMENTATION-REGISTER.md
@frontend/e2e/fidelity/README.md
@frontend/e2e/fidelity/assemble-gallery.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the full gallery + per-state human sign-off + ND register</name>
  <files>frontend/e2e/fidelity/assemble-gallery.mjs, frontend/e2e/fidelity/README.md</files>
  <read_first>
    - frontend/e2e/fidelity/README.md (the ND-A..ND-H register format + the regen commands)
    - frontend/e2e/fidelity/assemble-gallery.mjs (the ND array captioned "expected — ignore")
    - CONTEXT §5 W5 + §6 (screenshot-diff acceptance; intended divergences go in an ND register) + all Group notes for the divergences to record (e.g. planning shows no prep overlay — mock has none; failed drops Preview; est. values / live counts differ from the mock's fixed values = ND-D)
  </read_first>
  <action>
    Regenerate both sides of the gallery for EVERY state: `FIDELITY_CAPTURE=1 npm --prefix frontend run e2e -- zzz-baseline` → `node frontend/e2e/fidelity/capture-mocks.mjs` → `node frontend/e2e/fidelity/assemble-gallery.mjs`. Add the Phase-42 intended divergences to the ND register in assemble-gallery.mjs + README.md (continue the ND lettering from the existing register; e.g. "planning shows no prep overlay — the mock has none, planning surfaces as the lane phase-pill + Steps Running head"; "live/clarify/gate land on Steps by default"; the artifact-card pages/coverage parses are live-derived not the mock's fixed values [ND-D]; the two brittle parses F1/F2 are flagged). Then present the assembled gallery for the consolidated per-state human sign-off.
  </action>
  <acceptance_criteria>
    <automated>node frontend/e2e/fidelity/assemble-gallery.mjs 2>&1 | tail -3 (assembles without error across all states)</automated>
    <automated>grep -Ec "ND-" frontend/e2e/fidelity/assemble-gallery.mjs (the ND register carries the Phase-42 divergences)</automated>
    <human-check>Open gallery.html and sign off EACH state (planning · live · clarify · gate · complete · failed) side-by-side vs its mock: every visible difference is a registered ND divergence; no unregistered fidelity gap remains. This is the phase's acceptance gate.</human-check>
  </acceptance_criteria>
  <done>The full gallery is regenerated; every state is human-signed-off; the Phase-42 intended divergences are in the ND register.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the registers (ISS-035/036/037, F1/F2, IMPLEMENTATION-REGISTER, ROADMAP)</name>
  <files>.planning/ISSUES-REGISTER.md, .planning/IMPLEMENTATION-REGISTER.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ISSUES-REGISTER.md (ISS-035 + ISS-036 rows — stale OPEN; their fix landed in Phase 32 plan 32-05 per the IMPLEMENTATION-REGISTER; ISS-037 — the TodoCard clause deleted in 42-04)
    - .planning/IMPLEMENTATION-REGISTER.md (line ~3083 — the tracking gap note that ISS-035/036 were never reconciled; the per-phase entry format)
    - .planning/ROADMAP.md (Phase 42 entry ~:734 + Requirements line ~:1051 RUNUI-06..09)
    - the 42-09-SUMMARY deferred-items (F1 coverage aggregate + F2 sections extractor)
  </read_first>
  <action>
    Reconcile ISS-035 + ISS-036 to their true state (code landed in 32-05; if fully satisfied, mark resolved with the deciding commit/plan reference; if a residue remains, note it precisely — do not flip to resolved without evidence). Update the ISS-037 TodoCard clause to reflect the 42-04 deletion (TodoCard removed; note the remaining ISS-037 advisory items still open). Add the F1 (structured coverage/counts aggregate on `/validation-results`) + F2 (event-free `sections` extractor) follow-ups as deferred backend/additive items (OUT OF SCOPE this phase). Write the Phase-42 IMPLEMENTATION-REGISTER entry (the deletions ledger: QuestionnairePanel, ReviewGatePanel, AgentProgressPanel, WaveTreePanel, TodoCard; the artifactPreview extraction; the artifact cards; the ND register; F1/F2). Update the ROADMAP Phase-42 entry (goal filled, plans listed 42-01..42-11, requirements RUNUI-06/07/08/09; mark RUNUI-09 status per the green-e2e evidence). Docs-only edits — no code.
  </action>
  <acceptance_criteria>
    <automated>grep -Ec "ISS-035|ISS-036" .planning/ISSUES-REGISTER.md (rows present + reconciled — reading confirms status updated with evidence)</automated>
    <automated>grep -Eic "F1|F2|sections extractor|coverage" .planning/IMPLEMENTATION-REGISTER.md (F1/F2 recorded)</automated>
    <automated>grep -c "42-01-PLAN\|Phase 42" .planning/ROADMAP.md (Phase-42 entry finalized)</automated>
    <automated>cd frontend && npx playwright test --project=mocked 2>&1 | tail -8 (the touched mocked-e2e specs are green — RUNUI-09; re-anchor any selector drift from the reskin, do NOT delete a spec)</automated>
  </acceptance_criteria>
  <done>ISS-035/036/037 reconciled with evidence; F1/F2 recorded as deferred; the IMPLEMENTATION-REGISTER + ROADMAP carry the Phase-42 entry; the mocked-e2e suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Docs + gallery regeneration; no product surface changed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-11-01 | Repudiation | Register reconcile | mitigate | ISS status flips require the deciding commit/plan as evidence; no status flipped without proof. |
| T-42-11-02 | Tampering | e2e specs | mitigate | Selector drift is re-anchored, never deleted (a changed-behavior spec is reconciled, not removed). |
| T-42-11-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Full gallery regenerated + per-state human sign-off recorded; ND register carries the Phase-42 divergences.
- ISS-035/036/037 reconciled with evidence; F1/F2 deferred; IMPLEMENTATION-REGISTER + ROADMAP updated.
- `playwright --project=mocked` green (RUNUI-09).
</verification>

<success_criteria>
Every run state is human-signed-off against its mock via the regenerated gallery, all intended divergences are registered, the stale ISS rows + F1/F2 are reconciled, and the mocked-e2e suite is green — the phase closes on evidence, not prose.
</success_criteria>

<artifacts_this_phase_produces>
- Regenerated full gallery + Phase-42 ND register entries in `assemble-gallery.mjs` + `README.md`.
- Reconciled ISS-035/036/037; recorded F1/F2 deferrals; Phase-42 IMPLEMENTATION-REGISTER + ROADMAP entries.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-11-SUMMARY.md` when done.
</output>
