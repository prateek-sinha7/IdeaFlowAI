---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 09
type: execute
wave: 4
depends_on: [42-05]
files_modified:
  - frontend/src/components/results/AgentDetailPanel.tsx
  - frontend/src/components/results/AgentDetailPanel.artifactCards.test.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-07, RUNUI-08]
must_haves:
  truths:
    - "A settled spec agent's L2 detail renders a pages/sections card; a task-planner agent renders a tasks card ('N planned'); an analyzer agent renders a checks card (verdict badge) — each between the reasoning block and tool calls."
    - "Each card is selected by the shared name-free discriminator (never a workflow/agent-id literal) and renders ONLY when its artifact kind is present (single_shot / no-artifact agents render exactly as today)."
    - "A handoff line renders from dagEdges (producer→consumer + artifact label), falling back to the next agent's name."
    - "The Build-Agent construction card is unchanged (KEEP) — not duplicated."
  artifacts:
    - path: "frontend/src/components/results/AgentDetailPanel.tsx"
      provides: "Settled artifact cards (pages/tasks/checks) + handoff line, keyed on discriminateArtifact"
      contains: "discriminateArtifact"
    - path: "frontend/src/components/results/AgentDetailPanel.artifactCards.test.tsx"
      provides: "Card-type selection + conditional-render + handoff tests"
  key_links:
    - from: "frontend/src/components/results/AgentDetailPanel.tsx"
      to: "frontend/src/components/results/artifactPreview.tsx"
      via: "discriminateArtifact + SpecPreview/AnalysisPreview reused for the cards"
      pattern: "discriminateArtifact"
    - from: "frontend/src/components/results/AgentDetailPanel.tsx"
      to: "pipelineState.protoCompletedTasks / dagEdges"
      via: "tasks card + handoff line consume in-state data"
      pattern: "protoCompletedTasks|dagEdges"
---

<objective>
Wave 4-c (CONTEXT §5 Group H settled + §8 decision 2 + §9 the verified data path) — BUILD the settled agent-detail artifact cards. In `AgentDetailPanel.tsx` settled L2 detail (~:548-591), between the reasoning block and the tool-calls, render the mock's per-agent artifact card + handoff note (`Hexaware Run.dc.html:389-476, 534-559`). The card TYPE is chosen by the shared name-free discriminator (42-05) — `<spec>` → pages/sections card, `<tasks>` → tasks card, `<analysis>` → checks card. The Build-Agent construction card already exists (§4 KEEP) and is out of scope — do NOT duplicate it. All four elements are FRONTEND-ONLY on the §9-verified data path; zero backend/golden/transport change.

Purpose: user decision 2 = BUILD (not defer). This is the one net-new construction in the phase — but it is net-new UI over data that already flows (RUNUI-07/08).
Output: three conditional, generically-keyed artifact cards + a handoff line, with two brittle-parse follow-ups (F1, F2) registered OUT OF SCOPE.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/src/components/results/AgentDetailPanel.tsx
@frontend/src/components/results/artifactPreview.tsx
@frontend/src/types/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Card-type selection + data derivation (tasks / checks + handoff)</name>
  <files>frontend/src/components/results/AgentDetailPanel.tsx, frontend/src/components/results/AgentDetailPanel.artifactCards.test.tsx</files>
  <read_first>
    - frontend/src/components/results/AgentDetailPanel.tsx (the settled detail render :548-591 — reasoning :561-562, construction block :564-574 KEEP, tool-calls :577; the `AgentDetailPanelProps` :476-486; `agent.validationPassed`/`validationIssues`; the construction card pattern :359-475 as the visual analog)
    - frontend/src/components/results/artifactPreview.tsx (discriminateArtifact + the three previews from 42-05)
    - frontend/src/types/index.ts (`protoCompletedTasks{number,title,summary}` :605 + `protoCompletedTaskCount` :606; `validationPassed`/`validationIssues` :543-544; `dagEdges{from,to,artifact_type}` :603)
    - CONTEXT §9 (the four card elements + their FE sources + the BRITTLE flags for pages + coverage-%) + the mock artifact cards `Hexaware Run.dc.html:405-437` (tasks + checks) + the handoff line :555-559
  </read_first>
  <behavior>
    - Given an agent whose output discriminates to "tasks" AND pipelineState.protoCompletedTasks is non-empty → a tasks card with "N planned" = protoCompletedTaskCount (or protoCompletedTasks.length) and one row per task title.
    - Given a "tasks" agent but protoCompletedTasks empty (single_shot) → NO tasks card (correct, generic).
    - Given an agent discriminating to "analysis" → a checks card whose badge is derived from agent.validationPassed/validationIssues (pass/fail) with the verdict text parsed via the reused AnalysisPreview (flagged brittle).
    - Given an agent discriminating to "spec" → a pages/sections card via the reused SpecPreview (## headings) (flagged brittle).
    - Given an agent with no recognized artifact tag → NO card (renders exactly as today).
    - Handoff line: derived from dagEdges (matching this agent's produced edge → {to, artifact_type}); when dagEdges absent, fall back to agents[index+1].name. Never a workflow/agent-id literal beyond the live agent NAME.
  </behavior>
  <action>
    Add a pure helper (co-located, exported for the test) that, given the current agent + pipelineState + the agent list/index, returns a descriptor: the card KIND (via `discriminateArtifact(agent.output, agent.artifactKind)`), the derived data (tasks list + count from `protoCompletedTasks`; checks badge from `validationPassed`/`validationIssues`; the parsed verdict/sections handed to the reused previews), and the handoff target (from `dagEdges` producer→consumer + artifact label, else `agents[index+1].name`). Every branch keys on GENERIC state/props — do NOT copy the construction card's `findIndex(a => /build|construct/i.test(a.id))` name signal (SC-001). Write `AgentDetailPanel.artifactCards.test.tsx` covering the behavior block above (RED first: assert card-kind selection + the empty-protoCompletedTasks → no card + the handoff fallback, before wiring the render). This task is derivation + tests; the visual render is Task 2.
  </action>
  <acceptance_criteria>
    <automated>grep -c "discriminateArtifact" frontend/src/components/results/AgentDetailPanel.tsx (>= 1)</automated>
    <automated>cd frontend && grep -Ec "/build\\|construct/i|prototype-|pipeline_type|spec\\.id ==" src/components/results/AgentDetailPanel.tsx (== 0 — no name-gate for the cards; note the KEEP construction block must already avoid this — verify no NEW name signal added)</automated>
    <automated>cd frontend && npx vitest run AgentDetailPanel.artifactCards 2>&1 | tail -5 (card-selection + no-card + handoff-fallback cases green)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
  </acceptance_criteria>
  <done>A generic, tested derivation selects the card kind + data + handoff target from in-state data; empty-artifact agents yield no card; no name literal.</done>
</task>

<task type="auto">
  <name>Task 2: Render the three cards + handoff line in the settled L2 detail</name>
  <files>frontend/src/components/results/AgentDetailPanel.tsx</files>
  <read_first>
    - frontend/src/components/results/AgentDetailPanel.tsx (the insert point between reasoning :561-562 and tool-calls :577; the construction card markup :359-475 as the visual analog; the ConstructionTaskRow / progress styling for token consistency)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (pages card :389-402 [label + overview + 3-col page grid]; tasks card :405-420 [label + "N planned" in brand + `n. title` rows]; checks card :422-437 [label + green verdict badge #1F7A4D on #E7F0EA + green-check rows]; handoff line :555-559 [down-arrow + "{producer artifact} → {consumer}" muted text])
    - CONTEXT §9 (card TYPE keyed on the discriminator; tasks from protoCompletedTasks; handoff from dagEdges/order; checks badge from validation*)
  </read_first>
  <action>
    Render the descriptor from Task 1 as a card between the reasoning block and the tool-calls (NOT replacing the KEEP construction block, which stays for the build agent): the TASKS card (label + "N planned" in brand `#3C2CDA` + a row per task title from protoCompletedTasks); the CHECKS card (label + a pass/fail verdict badge from validation* using the mock green tokens `#1F7A4D`/`#E7F0EA`, plus verdict rows via the reused AnalysisPreview); the PAGES/SECTIONS card (label + section list via the reused SpecPreview, styled to the mock's 3-col grid where feasible). Each card renders ONLY when its kind is selected AND its data is present (conditional degrade). Below the cards, render the HANDOFF line (down-arrow icon + muted text "{artifact_type} → {next agent name}") from the descriptor. Reuse `SpecPreview`/`AnalysisPreview` from artifactPreview for the parsed bodies (no re-parse). Match the mock tokens (`bg-brand`/`border-brand-border`/`text-brand`, accent `#3C2CDA`). Render all text as escaped React content (no dangerouslySetInnerHTML). Do NOT touch the construction block, the Context-received panel, or the reasoning/tool/input/output sections (KEEP).
  </action>
  <acceptance_criteria>
    <automated>grep -Ec "N planned|planned" frontend/src/components/results/AgentDetailPanel.tsx (>= 1 — tasks card copy)</automated>
    <automated>grep -Ec "1F7A4D|E7F0EA" frontend/src/components/results/AgentDetailPanel.tsx (>= 1 — checks verdict badge tokens)</automated>
    <automated>grep -c "dagEdges" frontend/src/components/results/AgentDetailPanel.tsx (>= 1 — handoff line source)</automated>
    <automated>cd frontend && grep -c "dangerouslySetInnerHTML" src/components/results/AgentDetailPanel.tsx (== 0)</automated>
    <automated>cd frontend && grep -c "data-testid=\"construction-block\"" src/components/results/AgentDetailPanel.tsx (>= 1 — construction card KEEP not removed)</automated>
    <automated>cd frontend && npx vitest run AgentDetailPanel 2>&1 | tail -5 (green)</automated>
    <human-check>Regenerate the settled gallery: the Spec Writer L2 shows a pages/sections card, the Task Planner shows a tasks card ("N planned" + task rows), the analyzer shows a checks card (green verdict badge); each has the handoff line; the Build Agent still shows only its (unchanged) construction card. Compare side-by-side vs Hexaware Run.dc.html:389-476,534-559.</human-check>
  </acceptance_criteria>
  <done>The three settled artifact cards + handoff line render on live data, conditional + generically keyed, between reasoning and tool-calls; the construction card is untouched; mock tokens applied.</done>
</task>

<task type="auto">
  <name>Task 3: Register the two brittle-parse follow-ups (F1, F2) as OUT OF SCOPE</name>
  <files>frontend/src/components/results/AgentDetailPanel.tsx</files>
  <read_first>
    - CONTEXT §9 (Follow-up F1: structured `{coverage,counts{P0..P3}}` aggregate on `GET /runs/{id}/validation-results` — backend/additive; Follow-up F2: event-free `sections` extractor + `/artifacts?kind=sections` — backend/additive. Both OUT of the FE-only fence.)
    - .planning/IMPLEMENTATION-REGISTER.md (how deferred follow-ups are recorded per phase)
  </read_first>
  <action>
    Add a short code comment at the pages card and the checks-coverage derivation flagging them as BRITTLE parses of agent output (F2 = pages/sections extractor; F1 = structured coverage/counts) with a pointer that F1/F2 are registered OUT-OF-SCOPE backend/additive follow-ups (do NOT build them here — the cards parse agent output and are flagged brittle, not shipped as robust). Record F1 + F2 in the 42-09-SUMMARY.md deferred-items section so the phase reconcile (42-11) folds them into the register. Do NOT add any fetch, endpoint, or engine call (SC-001/LOCK-B — any such need means OUT OF SCOPE, flag not build).
  </action>
  <acceptance_criteria>
    <automated>cd frontend && grep -Eic "brittle|F1|F2" src/components/results/AgentDetailPanel.tsx (>= 1 — the flags are present in code comments)</automated>
    <automated>cd frontend && git diff --stat 2>/dev/null | grep -Ec "\\.py|backend/|golden|api/" (== 0 — no backend/transport touched, FE-only)</automated>
  </acceptance_criteria>
  <done>F1 (coverage aggregate) + F2 (sections extractor) are flagged in code + recorded as OUT-OF-SCOPE deferred follow-ups; no backend/transport change made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent output text → artifact cards | Spec/tasks/analysis output parsed + rendered in the L2 detail (model-produced). |
| in-run state → cards | protoCompletedTasks / dagEdges / validation* consumed from pipelineState. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09-01 | Tampering (XSS) | Artifact card render | mitigate | Cards render via artifactPreview (escaped text) + plain React text; grep guard dangerouslySetInnerHTML == 0. |
| T-42-09-02 | Information Disclosure | Card data | mitigate | Consumes only in-state, owner-scoped pipelineState fields; no new fetch (SC-001); F1/F2 explicitly deferred not built. |
| T-42-09-03 | Spoofing | Card-type selection | mitigate | Keyed on the shared name-free discriminator; SC-001 grep guard prevents a workflow/agent-name signal. |
| T-42-09-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Card kind selected by discriminateArtifact; conditional render (no card when artifact absent); handoff from dagEdges/order — all tested.
- Settled gallery shows pages/tasks/checks cards + handoff on the right agents; construction card unchanged.
- SC-001 + no-innerHTML greps clean; FE-only (git diff has no backend/golden); F1/F2 flagged + deferred.
</verification>

<success_criteria>
The settled agent-detail matches the mock's per-agent artifact cards + handoff line on live, generically-keyed data, reusing the shared artifactPreview module, without duplicating the KEEP construction card or making any backend/golden change — with the two brittle parses flagged and their robust versions deferred (F1/F2).
</success_criteria>

<artifacts_this_phase_produces>
- New exported derivation helper (card kind + data + handoff) in `AgentDetailPanel.tsx`.
- Three settled artifact cards (pages/sections · tasks · checks) + a handoff line rendered between reasoning and tool-calls.
- New test `AgentDetailPanel.artifactCards.test.tsx`.
- Registered OUT-OF-SCOPE follow-ups F1 (coverage/counts aggregate) + F2 (sections extractor).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-09-SUMMARY.md` when done.
</output>
