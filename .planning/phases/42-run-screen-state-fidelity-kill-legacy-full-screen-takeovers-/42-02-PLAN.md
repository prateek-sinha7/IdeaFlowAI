---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/src/components/chat/RunChatLane.tsx
  - frontend/src/components/chat/InlineClarifyActions.tsx
  - frontend/src/components/results/StepsOverviewSpine.tsx
  - frontend/src/components/results/AgentThinkingTab.tsx
  - frontend/src/types/index.ts
  - frontend/src/components/preview/QuestionnairePanel.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-08]
must_haves:
  truths:
    - "During planning, clarify, and review-gate, the run screen's right panel renders PreviewPanel (its Steps inline surfaces) — NOT a legacy full-screen takeover."
    - "The right panel auto-selects the correct tab per state: live/clarify/gate → Steps, failed → Audit, settled → Preview."
    - "The user can cancel a running workflow from the inline clarify (lane + Steps) via the existing handleCancelWorkflow — the Cancel affordance is not lost when QuestionnairePanel is removed."
    - "QuestionnairePanel.tsx no longer exists; the ClarifyQuestion type still resolves for every consumer."
  artifacts:
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "Right-panel cascade with the three takeover branches removed; PreviewPanel is the sole host"
    - path: "frontend/src/components/preview/PreviewPanel.tsx"
      provides: "State-keyed default tab effect"
      contains: "activeTab"
    - path: "frontend/src/types/index.ts"
      provides: "Relocated ClarifyQuestion + MCQQuestion types"
      contains: "ClarifyQuestion"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "frontend/src/components/preview/PreviewPanel.tsx"
      via: "PreviewPanel is the only right-panel branch"
      pattern: "<PreviewPanel"
    - from: "frontend/src/components/chat/InlineClarifyActions.tsx"
      to: "handleCancelWorkflow"
      via: "onCancelWorkflow prop threaded from DashboardLayout"
      pattern: "onCancelWorkflow"
---

<objective>
Wave 1 — the core unshadowing (CONTEXT §3 Group A + §Group B + §8 decision 1). Remove the three legacy full-screen right-panel takeover BRANCHES from the DashboardLayout execution cascade so the already-built mock-matching inline surfaces (Steps clarify/gate cards, RunHeader pills, Steps review-dot, planning-in-Steps) become reachable during the very states they were built for. Auto-select the correct tab per state. Re-home the Cancel-Workflow affordance onto the inline clarify so it is not lost. Delete QuestionnairePanel.tsx (no reusable logic) after relocating the shared ClarifyQuestion type. Keep ReviewGatePanel.tsx (its parsers are extracted in 42-05).

Purpose: today the takeover branches sit ABOVE PreviewPanel in a priority cascade, so PreviewPanel — the sole host of the mock-correct surfaces — never mounts during planning/clarify/gate (CONTEXT §2). This is the highest-leverage change in the phase.
Output: a right panel that is always PreviewPanel; correct tab per state; cancel re-homed; QuestionnairePanel deleted.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@.planning/IMPLEMENTATION-REGISTER.md
@frontend/src/components/layout/DashboardLayout.tsx
@frontend/src/components/preview/PreviewPanel.tsx
@frontend/src/components/preview/QuestionnairePanel.tsx
@frontend/src/components/chat/InlineClarifyActions.tsx
@frontend/src/components/results/StepsOverviewSpine.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the ClarifyQuestion type + delete QuestionnairePanel.tsx</name>
  <files>frontend/src/types/index.ts, frontend/src/components/preview/QuestionnairePanel.tsx, frontend/src/components/chat/RunChatLane.tsx, frontend/src/components/chat/InlineClarifyActions.tsx, frontend/src/components/results/StepsOverviewSpine.tsx, frontend/src/components/results/AgentThinkingTab.tsx</files>
  <read_first>
    - frontend/src/components/preview/QuestionnairePanel.tsx (defines `export interface ClarifyQuestion` :10 + `export type MCQQuestion = ClarifyQuestion` :25 — the ONLY reusable thing in this file; the rest is the legacy full-screen panel)
    - frontend/src/types/index.ts (the shared type home where WaveGroup etc. live — the relocation target)
    - CONTEXT §A4 (QuestionnairePanel has no reusable logic → delete early in W1; KEEP all state/handlers)
  </read_first>
  <action>
    Move `interface ClarifyQuestion` and `type MCQQuestion` from QuestionnairePanel.tsx into `types/index.ts` (the established shared type module). Repoint every importer of these types from `@/components/preview/QuestionnairePanel` to `@/types/index`: RunChatLane.tsx, InlineClarifyActions.tsx, StepsOverviewSpine.tsx, AgentThinkingTab.tsx, PreviewPanel.tsx (its `import("@/components/preview/QuestionnairePanel").ClarifyQuestion` inline type at :328 → `import("@/types/index").ClarifyQuestion`) and the co-located test files (InlineClarifyActions.test.tsx, RunChatLane.test.tsx, StepsDrilldown.test.tsx) — a mechanical import-path repoint. Then delete `components/preview/QuestionnairePanel.tsx`. This is a prerequisite for Task 2's branch removal (the branch is the only place the QuestionnairePanel COMPONENT is used; grep-confirmed DashboardLayout is the only mounter). Do NOT delete any state/handler in DashboardLayout (§A4 KEEP list is Task 2's concern).
  </action>
  <acceptance_criteria>
    <automated>test -f frontend/src/components/preview/QuestionnairePanel.tsx && echo FAIL || echo OK (file deleted)</automated>
    <automated>grep -rn "components/preview/QuestionnairePanel" frontend/src | grep -v "^Binary" | grep -c import (== 0 real imports remain)</automated>
    <automated>grep -c "export interface ClarifyQuestion" frontend/src/types/index.ts (== 1)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0 — every ClarifyQuestion consumer still resolves)</automated>
  </acceptance_criteria>
  <done>ClarifyQuestion/MCQQuestion live in types/index.ts; every importer + test repointed; QuestionnairePanel.tsx deleted; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the three takeover branches + auto-select the tab per state</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/preview/PreviewPanel.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (the right-panel cascade ~:1787-1869: PlanningOverlay branch :1791-1796, ReviewGatePanel branch :1797-1812, QuestionnairePanel branch :1813-1821, PreviewPanel else :1822-1868; the `PlanningOverlay` local fn :184-255; imports :27 QuestionnairePanel / :28 ReviewGatePanel; the `laneGate` mapping :1376-1388 and every §A4 state/handler — KEEP these)
    - frontend/src/components/preview/PreviewPanel.tsx (`activeTab` useState default "preview" :448; TAB_CONFIG :351-355 [preview·Steps·files·audit]; the tab id "thinking" is labelled "Steps"; `hasActiveGate`/`hasOpenClarify` :625-626; `terminalFailure`/`isCancelledTerminal` :593; the `initialTab` effect :462)
    - CONTEXT §A1/§A2/§A3 (branch removal) + §B (auto-tab: live→Steps, clarify/gate→Steps, failed→Audit, settled→Preview) + §4 KEEP + §6 SC-001
  </read_first>
  <action>
    In DashboardLayout: delete all three takeover branches from the right-panel cascade (PlanningOverlay, `<ReviewGatePanel>`, `<QuestionnairePanel>`) so the ternary collapses to render `<PreviewPanel .../>` unconditionally as the ErrorBoundary child. Remove the now-unused imports (:27 QuestionnairePanel, :28 ReviewGatePanel) and delete the local `PlanningOverlay` function definition. Keep the ReviewGatePanel.tsx FILE (42-05 extracts + deletes it). KEEP every §A4 item verbatim: `reviewGateData`, `questionnaireData`, `questionnaireQuestions`, `questionnaireLoading`, `activePipelineRunId`, the questionnaireData→questionnaireQuestions effect, the `laneGate` mapping :1376-1388, `runLaneState`/`laneClarifyOpen`, and all submit handlers (handleQuestionnaireSubmit, handleLaneSubmitAnswers, handleQuestionnaireSkip, handleRejectReview, handleCancelWorkflow, onApproveReview, onRedoReview, onUpdateSpecsReview) — they feed the inline path PreviewPanel already receives (laneGate/clarifyQuestions passthrough :1856-1863). Delete/repoint the stale comments at :1781-1784 only if they now dangle (dead-code comment cleanup is 42-04's job — leave WaveTreePanel comments).
    In PreviewPanel: add a state-keyed default-tab effect that runs when the run state resolves. Derive the generic state exactly as the existing header logic does (SC-001 — NEVER a workflow/agent-id literal): reuse `hasActiveGate`/`hasOpenClarify` (:625-626) and the `terminalFailure`/`isCancelledTerminal` signals (:593). Default: gate OR clarify OR (running with no settled deliverable) → the "thinking" (Steps) tab; terminal-failure → "audit"; settled/complete → "preview". Guard it so it fires on the STATE TRANSITION, not on every render, and so it never overrides a user's manual tab click (e.g. a `useRef` "auto-applied" latch keyed on the resolved state, mirroring the existing `initialTab` effect idiom). Do not touch TAB_CONFIG or the tab ids/testids.
  </action>
  <acceptance_criteria>
    <automated>grep -c "PlanningOverlay\|<ReviewGatePanel\|<QuestionnairePanel" frontend/src/components/layout/DashboardLayout.tsx (== 0 — all three branches + the local fn gone)</automated>
    <automated>grep -c "import.*QuestionnairePanel\|import.*ReviewGatePanel" frontend/src/components/layout/DashboardLayout.tsx (== 0)</automated>
    <automated>grep -Ecn "pipeline_type|spec\\.id ==|prototype-|/build\\|construct/" frontend/src/components/preview/PreviewPanel.tsx (auto-tab keys on generic state, not a name literal — no new name-gate introduced)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run PreviewPanel DashboardLayout.waveMount 2>&1 | tail -5 (touched-component suites green)</automated>
    <human-check>Regenerate the gallery (`FIDELITY_CAPTURE=1 npx playwright test zzz-baseline` → `node capture-mocks.mjs` → `node assemble-gallery.mjs`) and confirm CLARIFY, GATE, and PLANNING now render the PreviewPanel Steps inline surface (not the legacy full-screen panel), auto-landing on the Steps tab, matching the Live mock's `tab:'steps'` intent. Settled still lands on Preview.</human-check>
  </acceptance_criteria>
  <done>The right panel is always PreviewPanel; planning/clarify/gate land on Steps and settled on Preview; the three legacy branches, their imports, and PlanningOverlay are gone; ReviewGatePanel.tsx file retained; all §A4 state/handlers intact.</done>
</task>

<task type="auto">
  <name>Task 3: Re-home the Cancel-Workflow affordance onto the inline clarify</name>
  <files>frontend/src/components/chat/InlineClarifyActions.tsx, frontend/src/components/chat/RunChatLane.tsx, frontend/src/components/results/StepsOverviewSpine.tsx, frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    - frontend/src/components/chat/InlineClarifyActions.tsx (props: it has only `onSkipAll` :33; the submit row :238-254)
    - frontend/src/components/layout/DashboardLayout.tsx (`handleCancelWorkflow` :1290 — was wired ONLY to QuestionnairePanel; the clarify passthrough to PreviewPanel :1861-1863; the lane wiring for RunChatLane)
    - frontend/src/components/results/StepsOverviewSpine.tsx (the InlineClarifyActions mount :205 + the clarify passthrough props :47/:126) and RunChatLane.tsx renderComposerBody clarify case :719-726
    - CONTEXT §8 decision 1 (RE-HOME cancel; do NOT drop it silently) + §A2
  </read_first>
  <action>
    Add an optional `onCancelWorkflow?: () => void` prop to InlineClarifyActions and render a subtle "Cancel workflow" text affordance next to the existing skip/submit row when the prop is present (plain text button, no destructive styling beyond the existing muted skip treatment — final brand tokens land in 42-10). Thread `onCancelWorkflow` from DashboardLayout to BOTH inline-clarify mounts: (a) the lane — via RunChatLane's clarify composer case (add the prop to the InlineClarifyActions in `renderComposerBody` :721 and to RunChatLaneProps), and (b) Steps — via PreviewPanel's clarify passthrough (:1861-1863 add `onCancelClarify`/`onCancelWorkflow` passthrough) into StepsOverviewSpine's InlineClarifyActions mount. Bind it to the EXISTING `handleCancelWorkflow` (guarded `activePipelineRunId ? handleCancelWorkflow : undefined`, matching the old QuestionnairePanel wiring :1820). Do NOT change `handleCancelWorkflow` itself or the submit channels. Do NOT yet remove the OTHER extra clarify affordances (Skip all / Use recommended / Anything else) — that is 42-06 (Group G).
  </action>
  <acceptance_criteria>
    <automated>grep -c "onCancelWorkflow" frontend/src/components/chat/InlineClarifyActions.tsx (>= 1 — prop + render)</automated>
    <automated>grep -c "onCancelWorkflow\|handleCancelWorkflow" frontend/src/components/layout/DashboardLayout.tsx (>= 1 — threaded to the inline path)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run InlineClarifyActions RunChatLane 2>&1 | tail -5 (green; add/adjust a test asserting onCancelWorkflow fires)</automated>
    <human-check>In the clarify-awaiting gallery state, a "Cancel workflow" affordance is visible on BOTH the lane status card and the Steps InlineClarifyActions, and firing it calls handleCancelWorkflow.</human-check>
  </acceptance_criteria>
  <done>Cancel-Workflow is reachable from the inline clarify (lane + Steps) bound to handleCancelWorkflow; no submit channel changed; the cancel affordance was not lost with QuestionnairePanel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| engine events → run screen | Agent/gate/clarify output text rendered in the inline surfaces (model-produced, semi-trusted). |
| user → run controls | Cancel/submit actions ride the existing owner-scoped channels. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering (XSS) | PreviewPanel/Steps inline render of gate/clarify output | mitigate | Removing the takeover branches only changes WHICH already-existing component renders; all inline surfaces render output as escaped React text — no dangerouslySetInnerHTML added (grep guard). |
| T-42-02-02 | Elevation/Repudiation | Cancel/submit channels | mitigate | Re-home binds to the EXISTING owner-scoped `handleCancelWorkflow` + `submit_questionnaire` channels unchanged; no new endpoint or client-trusted action. |
| T-42-02-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `grep` proves the three branches + imports + PlanningOverlay are gone and PreviewPanel is the sole host.
- `tsc --noEmit` clean; PreviewPanel + DashboardLayout.waveMount + InlineClarifyActions + RunChatLane vitest green.
- Gallery: planning/clarify/gate render the inline Steps surface auto-tabbed to Steps; settled → Preview; Cancel-Workflow present on both inline clarify mounts.
- INV-3/LOCK-B: git diff --stat lists only frontend `.tsx`/`.ts` — no backend/transport/golden files.
</verification>

<success_criteria>
The legacy full-screen right-panel takeovers no longer pre-empt PreviewPanel; every paused/planning state renders the mock-matching inline Steps surface on the correct default tab; Cancel-Workflow is preserved on the inline clarify; QuestionnairePanel is deleted and ClarifyQuestion resolves everywhere.
</success_criteria>

<artifacts_this_phase_produces>
- Relocated `ClarifyQuestion` + `MCQQuestion` types in `types/index.ts`.
- New `onCancelWorkflow` prop on `InlineClarifyActions` + its threading through RunChatLane / PreviewPanel / StepsOverviewSpine / DashboardLayout.
- New state-keyed default-tab effect in `PreviewPanel`.
- Deletions: `QuestionnairePanel.tsx`; the PlanningOverlay/ReviewGatePanel/QuestionnairePanel cascade branches + imports + the PlanningOverlay function.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-02-SUMMARY.md` when done.
</output>
