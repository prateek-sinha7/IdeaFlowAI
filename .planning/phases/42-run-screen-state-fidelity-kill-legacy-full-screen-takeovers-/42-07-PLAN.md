---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 07
type: execute
wave: 3
depends_on: [42-02]
files_modified:
  - frontend/src/components/results/FilesTab.tsx
  - frontend/src/components/results/AuditTab.tsx
autonomous: true
requirements: [RUNUI-06]
must_haves:
  truths:
    - "While the pipeline is running, the Files hero shows a 'building' variant (spinner + task N of M · not yet validated + top progress bar) with Download/Download-All suppressed."
    - "While running, the Audit tab shows a live badge + 'in progress' elapsed + a violet 'monitoring live' banner."
    - "Settled Files + Audit are unchanged (KEEP)."
  artifacts:
    - path: "frontend/src/components/results/FilesTab.tsx"
      provides: "Building-variant Files hero keyed on pipelineState.isRunning"
    - path: "frontend/src/components/results/AuditTab.tsx"
      provides: "Live Audit badge + monitoring banner"
  key_links:
    - from: "frontend/src/components/results/FilesTab.tsx"
      to: "pipelineState.isRunning"
      via: "building hero renders only while running"
      pattern: "isRunning"
---

<objective>
Wave 4-e live-tabs polish (CONTEXT §5 Group H — the Files + Audit parts). Add the mock's LIVE states to the Files hero and Audit tab so a running run reads as in-progress: a building Files hero (spinner + "task N of M · not yet validated" + top progress bar, Download suppressed) and a live Audit banner/badge. Bound to live `pipelineState` (D-15/ND-D), generic (SC-001). The settled Files + Audit surfaces are KEEP (§4) — do not touch them.

Purpose: the Steps clarify/live surfaces are now reachable (42-02), but the Files/Audit tabs still only render their settled/failed variants; the mock shows distinct live variants while building.
Output: building Files hero + live Audit affordances, gated on the running signal, with settled unchanged.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/src/components/results/FilesTab.tsx
@frontend/src/components/results/AuditTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Files hero — building variant while running</name>
  <files>frontend/src/components/results/FilesTab.tsx</files>
  <read_first>
    - frontend/src/components/results/FilesTab.tsx (the "Final output" hero :645-681; the existing Download / Download-All actions; the failed "Build incomplete" banner :710-722 which is KEEP; the runStatus prop feed from PreviewPanel; pipelineState.isRunning availability)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (the building hero :564-575 — spinner + "task 4 of 7 · not yet validated" + top progress bar, no Download)
    - CONTEXT §H (Files building variant while `pipelineState.isRunning`)
  </read_first>
  <action>
    Add a BUILDING variant to the Files hero that renders while the run is live (`pipelineState.isRunning`, generic — SC-001): a spinner + a "task N of M · not yet validated" line (N/M from the live completed-task / planned-task counts already in pipelineState — never the mock's fixed "4 of 7"; omit the clause if counts are absent rather than fabricating) + an indeterminate top progress bar, and SUPPRESS the Download + Download-All actions in this state (nothing to download yet). When not running, render the existing settled hero unchanged (KEEP). Keep the failed "Build incomplete" banner untouched. Bind all text to live data (ND-D).
  </action>
  <acceptance_criteria>
    <automated>grep -c "isRunning" frontend/src/components/results/FilesTab.tsx (>= 1 — building variant gated on the running signal)</automated>
    <automated>cd frontend && grep -Ec "task 4 of 7|4 of 7" src/components/results/FilesTab.tsx (== 0 — no mock literal; live counts only)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run FilesTab 2>&1 | tail -5 (green; assert building hero suppresses Download while running + settled hero unchanged when done)</automated>
    <human-check>The live gallery state's Files tab shows the building hero (spinner + "task N of M · not yet validated" + progress bar, no Download); the settled Files hero is unchanged.</human-check>
  </acceptance_criteria>
  <done>The Files hero has a building variant while running (live counts, Download suppressed); settled + failed variants unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Live Audit — badge + in-progress + monitoring banner</name>
  <files>frontend/src/components/results/AuditTab.tsx</files>
  <read_first>
    - frontend/src/components/results/AuditTab.tsx (the settled/failed audit header + verdict banner + attribution card — all KEEP; the availability of a live/running signal via props/pipelineState)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (the live audit badge :623, "in progress" elapsed :649, the violet "monitoring live" banner :662)
    - CONTEXT §H (live Audit: pulsing "live" badge + "in progress" elapsed + violet "monitoring live" banner)
  </read_first>
  <action>
    Add a LIVE variant to the Audit tab while the run is running (generic running signal — SC-001): a pulsing "live" badge, an "in progress" elapsed indicator (from the live run start time already available — never a fixed mock value), and a violet "monitoring live" banner above the records. When not running, render the existing settled/failed audit surfaces unchanged (KEEP — the verdict banner, attribution card, taxonomy rows). Reuse the existing token/status styling; the violet banner uses the mock brand tokens (brand-fill/brand, `#3C2CDA`). Do NOT change the audit fetchers or the export menu (INV-12/SC-001 — presentation over the data that already flows).
  </action>
  <acceptance_criteria>
    <automated>cd frontend && grep -Eic "monitoring live|in progress|live" src/components/results/AuditTab.tsx (>= 1 — live affordances present)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run AuditTab 2>&1 | tail -5 (green; assert live variant renders the banner/badge while running + settled unchanged)</automated>
    <human-check>The live gallery state's Audit tab shows the pulsing live badge + in-progress elapsed + violet monitoring banner; the settled/failed Audit surfaces are unchanged.</human-check>
  </acceptance_criteria>
  <done>The Audit tab has a live variant (badge + in-progress + monitoring banner) while running; settled/failed variants + fetchers + export unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| engine events → Files/Audit | Live run counts + audit rows rendered (engine-produced). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07-01 | Tampering (XSS) | Files/Audit live render | mitigate | Counts/labels rendered as escaped React text; no dangerouslySetInnerHTML; audit fetchers unchanged. |
| T-42-07-02 | Information Disclosure | Audit records | mitigate | Reuses the existing owner-scoped audit fetchers unchanged; presentation-only. |
| T-42-07-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Files building hero renders while running (live counts, Download suppressed); settled hero unchanged.
- Audit live badge + in-progress + monitoring banner while running; settled/failed unchanged; fetchers/export untouched.
- tsc clean; FilesTab + AuditTab vitest green; no mock literal.
</verification>

<success_criteria>
The Files and Audit tabs render distinct, live-data-bound in-progress variants while a run is building, matching the Live mock, without disturbing the KEEP settled/failed surfaces or the audit data path.
</success_criteria>

<artifacts_this_phase_produces>
- Building-variant Files hero (live counts, Download suppressed) in `FilesTab.tsx`.
- Live Audit badge + in-progress elapsed + monitoring banner in `AuditTab.tsx`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-07-SUMMARY.md` when done.
</output>
