---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 04
type: execute
wave: 3
depends_on: [42-02]
files_modified:
  - frontend/src/components/workflow/AgentProgressPanel.tsx
  - frontend/src/components/workflow/AgentProgressPanel.test.tsx
  - frontend/src/components/workflow/WaveTreePanel.tsx
  - frontend/src/components/chat/blocks/TodoCard.tsx
  - frontend/src/components/chat/blocks/blocks.test.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/app/dashboard/page.tsx
autonomous: true
requirements: [RUNUI-06]
must_haves:
  truths:
    - "AgentProgressPanel, WaveTreePanel, and TodoCard are deleted — one implementation per behavior (INV-12)."
    - "No production surface loses a rendered feature (all three had zero JSX mounts)."
    - "The WaveTreePanel-relocation and page.tsx stale comments no longer reference deleted symbols."
  artifacts:
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "Stale relocation comments cleaned"
  key_links:
    - from: "frontend/src/components/chat/blocks/blocks.test.tsx"
      to: "(TodoCard removed)"
      via: "the dead TodoCard test reference is removed/retired"
      pattern: "TodoCard"
---

<objective>
Wave 3 (CONTEXT §5 Group E, INV-12) — delete the dead components superseded by the Phase-39 run screen: `AgentProgressPanel` (controls absorbed into RunChatLane), `WaveTreePanel` (superseded by AgentDetailPanel's inline construction tree), and `TodoCard` (no `todo` block kind is emitted — ISS-037). Verified: all three have ZERO JSX mounts in production (`grep '<WaveTreePanel'`/`'<AgentProgressPanel'`/`'<TodoCard'` = empty); the `WaveGroup` type lives in `types/index.ts` (not WaveTreePanel), so deletion drags no type.

Purpose: INV-12 forbids two implementations of one behavior; the phase's whole point is removing the superseded surfaces.
Output: three components + their tests deleted; stale relocation comments cleaned; grep ratchet green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@.planning/ISSUES-REGISTER.md
@frontend/src/components/layout/DashboardLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete AgentProgressPanel + WaveTreePanel + TodoCard and their tests</name>
  <files>frontend/src/components/workflow/AgentProgressPanel.tsx, frontend/src/components/workflow/AgentProgressPanel.test.tsx, frontend/src/components/workflow/WaveTreePanel.tsx, frontend/src/components/chat/blocks/TodoCard.tsx, frontend/src/components/chat/blocks/blocks.test.tsx</files>
  <read_first>
    - CONTEXT §E (the three dead components + their supersession; the stale comments at DashboardLayout.tsx:1781-1784 + app/dashboard/page.tsx:115)
    - .planning/ISSUES-REGISTER.md ISS-037 (TodoCard dead — no `todo` kind emitted)
    - frontend/src/components/chat/blocks/blocks.test.tsx (the only TodoCard importer — a test)
  </read_first>
  <action>
    Before deleting each file, re-verify zero real importers with a grep for the actual `import` statement and for a JSX mount `<Name` — if ANY real production importer/mount is found (not a comment or a `WaveGroup` type import from `types/index`), STOP and flag it rather than delete. Given the verified state (zero mounts), delete: `components/workflow/AgentProgressPanel.tsx` + `AgentProgressPanel.test.tsx`; `components/workflow/WaveTreePanel.tsx` (no co-located test found — confirm); `components/chat/blocks/TodoCard.tsx`. In `blocks.test.tsx`, remove the TodoCard test case + its import (it exercised a dead block kind — retire it, do NOT keep a test for deleted code). Do NOT touch the `WaveGroup` type in `types/index.ts` or its legitimate importers (AgentDetailPanel/PreviewPanel/DashboardLayout import the TYPE, not the component).
  </action>
  <acceptance_criteria>
    <automated>test -f frontend/src/components/workflow/AgentProgressPanel.tsx && echo FAIL || echo OK</automated>
    <automated>test -f frontend/src/components/workflow/WaveTreePanel.tsx && echo FAIL || echo OK</automated>
    <automated>test -f frontend/src/components/chat/blocks/TodoCard.tsx && echo FAIL || echo OK</automated>
    <automated>cd frontend && grep -rn "import.*AgentProgressPanel\|import.*WaveTreePanel\|import.*TodoCard" src 2>/dev/null | grep -v "types/index" | grep -c import (== 0)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run blocks 2>&1 | tail -5 (green without the TodoCard case)</automated>
  </acceptance_criteria>
  <done>The three dead components + their tests are deleted; no real importer remains; the WaveGroup type is untouched; tsc + blocks vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Clean the stale relocation comments</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (the WaveTreePanel-relocation comments :1781-1784 — now dangling after the component is gone)
    - frontend/src/app/dashboard/page.tsx (the stale comment near :115)
    - CONTEXT §E (fix the stale comments)
  </read_first>
  <action>
    Remove or correct the comments at DashboardLayout.tsx:1781-1784 (they describe a WaveTreePanel below-the-fold mount relocation that no longer exists) and the stale comment at app/dashboard/page.tsx:115. Do not change any live code around them — comment-only edits. Ensure no comment still references the deleted `WaveTreePanel`/`AgentProgressPanel`/`TodoCard` as if live.
  </action>
  <acceptance_criteria>
    <automated>cd frontend && grep -rn "WaveTreePanel\|AgentProgressPanel\|TodoCard" src/components/layout/DashboardLayout.tsx src/app/dashboard/page.tsx | grep -vc "WaveGroup" (== 0 — no dangling references)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run DashboardLayout.waveMount DashboardLayout.catalogHome 2>&1 | tail -5 (green — no behavior change)</automated>
  </acceptance_criteria>
  <done>The stale relocation comments no longer reference deleted symbols; no live code changed; suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure deletion of unmounted components + comment cleanup. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04-01 | Denial of Service (regression) | Deleted components | mitigate | Pre-delete grep guard requires zero real importers/mounts; tsc + touched suites gate the delete; a found importer aborts the delete. |
| T-42-04-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- The three component files (+ their tests) are gone; grep finds no real importer/mount.
- Stale comments cleaned; tsc clean; waveMount/catalogHome/blocks vitest green.
- INV-3/LOCK-B: only frontend files changed.
</verification>

<success_criteria>
The superseded AgentProgressPanel/WaveTreePanel/TodoCard are deleted with no production regression (all had zero mounts), and the dangling relocation comments are cleaned — INV-12 one-implementation-per-behavior holds on the run screen.
</success_criteria>

<artifacts_this_phase_produces>
- Deletions: `AgentProgressPanel.tsx` (+test), `WaveTreePanel.tsx`, `TodoCard.tsx`; the TodoCard case removed from `blocks.test.tsx`.
- Cleaned stale relocation comments in `DashboardLayout.tsx` + `page.tsx`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-04-SUMMARY.md` when done.
</output>
