---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 08
type: execute
wave: 4
depends_on: [42-05]
files_modified:
  - frontend/src/components/chat/InlineGateActions.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-07]
must_haves:
  truths:
    - "The review gate presents TWO primary buttons ('Approve & build' / 'Request changes') plus a task-plan preview block, matching the mock."
    - "The redo / update-specs / reject CHANNELS are preserved (collapsed under 'Request changes'), not deleted."
    - "The plan preview reuses the shared artifactPreview module (no second parser)."
  artifacts:
    - path: "frontend/src/components/chat/InlineGateActions.tsx"
      provides: "2-button gate + reused plan preview"
      contains: "Request changes"
  key_links:
    - from: "frontend/src/components/chat/InlineGateActions.tsx"
      to: "frontend/src/components/results/artifactPreview.tsx"
      via: "TasksPreview reused for the plan-preview block"
      pattern: "artifactPreview"
---

<objective>
Wave 4-b gate fidelity (CONTEXT §5 Group F). The mock's review gate is TWO buttons — "Approve & build" / "Request changes" — plus a task-plan preview block (`Hexaware Run - Live.dc.html:269-278`). We currently render 4–5 controls (Edit / Approve / Update-Specs / Redo / a 2-step Reject). Collapse redo/update-specs/reject under a single "Request changes" affordance and add the plan-preview block reusing the shared `artifactPreview` module (42-05). Preserve the underlying redo/update-specs/reject CHANNELS — restructure the UI only.

Purpose: gate fidelity to the mock while keeping the KAN-101 update_specs + redo + reject channels intact (they ride `approve_review {approved, action}`).
Output: a 2-button gate + plan preview in `InlineGateActions.tsx`, reusing artifactPreview.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/src/components/chat/InlineGateActions.tsx
@frontend/src/components/results/artifactPreview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the gate to 2 buttons + add the reused plan-preview block</name>
  <files>frontend/src/components/chat/InlineGateActions.tsx</files>
  <read_first>
    - frontend/src/components/chat/InlineGateActions.tsx (the current controls — Edit :152, Approve :181, Update-Specs :194, Redo :208, the 2-step Reject :247; the handlers handleApprove/handleReject/handleRedo/handleUpdateSpecs :111-135 and the channels they fire; `canRedo`/`canUpdateSpecs` gating :100-101; `approveLabel`)
    - frontend/src/components/results/artifactPreview.tsx (the `TasksPreview` + `discriminateArtifact` exports created in 42-05 — reuse for the plan preview)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (the gate :269-278 — shield header + "Awaiting you" badge + explanation text + a plan-preview block + two buttons "Approve & build" / "Request changes")
    - CONTEXT §F (collapse redo/update-specs/reject under one "Request changes"; add the plan-preview; preserve the channels)
  </read_first>
  <action>
    Restructure InlineGateActions to the mock's two primary buttons: "Approve & build" (fires the existing `handleApprove` / approve_review approved:true; honor `approveLabel` when provided) and "Request changes" (a single affordance that reveals the existing redo/update-specs/reject sub-controls — e.g. a small menu or expandable panel — wiring each to its EXISTING handler `handleRedo` / `handleUpdateSpecs` / `handleReject`, gated by `canRedo`/`canUpdateSpecs` as today). Do NOT delete any of those handlers or channels — only reorganize their presentation under "Request changes". Add a task-plan PREVIEW block above the buttons that renders the gate `output` through `discriminateArtifact` + the reused `TasksPreview`/`SpecPreview`/`AnalysisPreview` from artifactPreview.tsx (whichever the discriminator selects; for a task-plan gate it is TasksPreview) — reusing the shared module, NOT re-parsing. Keep the shield header + "Awaiting you" badge. Leave the navy→brand token reskin to 42-10 (structure only here).
  </action>
  <acceptance_criteria>
    <automated>grep -c "Approve & build\|Request changes" frontend/src/components/chat/InlineGateActions.tsx (>= 2 — the two primary buttons)</automated>
    <automated>grep -c "handleRedo\|handleUpdateSpecs\|handleReject" frontend/src/components/chat/InlineGateActions.tsx (>= 3 — channels preserved)</automated>
    <automated>grep -c "artifactPreview\|TasksPreview\|discriminateArtifact" frontend/src/components/chat/InlineGateActions.tsx (>= 1 — reuses the shared module, no second parser)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run InlineGateActions 2>&1 | tail -5 (green; assert Approve fires approve_review approved:true, and redo/update-specs/reject still reachable under Request changes)</automated>
    <human-check>The gate (gallery gate-awaiting state) shows the plan-preview block + exactly two primary buttons; "Request changes" reveals redo / update-specs / reject; approving still starts the build.</human-check>
  </acceptance_criteria>
  <done>The gate is two primary buttons + a reused plan-preview; the redo/update-specs/reject channels are preserved under "Request changes"; the plan preview reuses artifactPreview (INV-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user → gate action | Approve/reject/redo/update-specs ride the owner-scoped approve_review channel. |
| agent output → plan preview | Gate `output` parsed + rendered via artifactPreview. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08-01 | Tampering (XSS) | Plan-preview render | mitigate | Reuses artifactPreview (escaped text, no innerHTML); no new parser. |
| T-42-08-02 | Elevation of Privilege | Gate action channels | mitigate | Restructure preserves the exact existing approve_review {approved, action} channels + owner-scoping; no new client-trusted action. |
| T-42-08-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Gate renders two primary buttons + a reused plan-preview; redo/update-specs/reject reachable under "Request changes"; approve_review channels unchanged.
- artifactPreview reused (no second parser); tsc clean; InlineGateActions vitest green.
</verification>

<success_criteria>
The review gate matches the mock — two buttons plus a task-plan preview — while every underlying gate channel (redo / update-specs / reject / approve) is preserved and the plan preview reuses the shared artifactPreview module.
</success_criteria>

<artifacts_this_phase_produces>
- Restructured `InlineGateActions.tsx`: "Approve & build" + "Request changes" (redo/update-specs/reject collapsed) + a plan-preview block consuming `artifactPreview`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-08-SUMMARY.md` when done.
</output>
