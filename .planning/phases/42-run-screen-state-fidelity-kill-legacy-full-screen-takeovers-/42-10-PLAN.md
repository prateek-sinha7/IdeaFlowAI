---
phase: 42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-
plan: 10
type: execute
wave: 5
depends_on: [42-06, 42-08]
files_modified:
  - frontend/src/components/chat/InlineClarifyActions.tsx
  - frontend/src/components/chat/InlineGateActions.tsx
  - frontend/src/components/chat/ResultCard.tsx
autonomous: true
requirements: [RUNUI-06]
must_haves:
  truths:
    - "The inline clarify/gate/result cards use the Phase-32 brand tokens — the retired navy #1B2A4A and raw gray/blue/red/violet/amber are gone from these three files."
    - "The token-header / navy-body seam inside the Steps AwaitingCard is resolved."
    - "Behavior is unchanged — only presentation tokens change (reskin-look, keep-behavior D-15)."
  artifacts:
    - path: "frontend/src/components/chat/InlineClarifyActions.tsx"
      provides: "Brand-token clarify card"
    - path: "frontend/src/components/chat/InlineGateActions.tsx"
      provides: "Brand-token gate card"
    - path: "frontend/src/components/chat/ResultCard.tsx"
      provides: "Brand-token result card"
  key_links:
    - from: "the three inline cards"
      to: "the Phase-32 token layer"
      via: "bg-brand / border-brand-border / text-brand replace navy/raw palette"
      pattern: "bg-brand|text-brand|border-brand"
---

<objective>
Wave 4-f the inline reskin (CONTEXT §5 Group I) — route `InlineClarifyActions`, `InlineGateActions`, and `ResultCard` through the Phase-32 token layer, retiring the navy `#1B2A4A` + raw gray/blue/red/violet/amber. These render inside the Steps `AwaitingCard`, so today there is a visible token-header / navy-body seam. This runs LAST in the polish sequence so it skins the FINAL markup (after 42-06 restructured the clarify and 42-08 restructured the gate). Reskin-look, keep-behavior (D-15) — presentation only.

Purpose: the surfaces are now structurally mock-correct but still carry the retired palette; this closes the visual seam. Part of the deferred Phase-35+ palette campaign but load-bearing for THIS phase's surfaces.
Output: the three inline cards on brand tokens; navy/raw-palette grep 0 in these files.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-CONTEXT.md
@frontend/src/components/chat/InlineClarifyActions.tsx
@frontend/src/components/chat/InlineGateActions.tsx
@frontend/src/components/chat/ResultCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin InlineClarifyActions + InlineGateActions to brand tokens</name>
  <files>frontend/src/components/chat/InlineClarifyActions.tsx, frontend/src/components/chat/InlineGateActions.tsx</files>
  <read_first>
    - frontend/src/components/chat/InlineClarifyActions.tsx (retired-palette sites: navy `#1B2A4A` at :171,177(area),:217,:230,:240; raw gray/blue classes throughout) — NOTE this file was restructured in 42-06 (single submit, extras removed); reskin the CURRENT markup
    - frontend/src/components/chat/InlineGateActions.tsx (retired-palette sites: :146,:159,:169,:186,:200,:226,:268; the gradient `from-[#1B2A4A]`) — restructured in 42-08 (2 buttons + preview); reskin the CURRENT markup
    - CONTEXT §I (route through the Phase-32 token layer: `bg-brand`/`border-brand-border`/`text-brand`, mock accent `#3C2CDA`; do NOT touch TweaksPanel font/color OPTIONS) + a prior reskinned component for the exact token names (e.g. StepsOverviewSpine AwaitingCard chrome)
  </read_first>
  <action>
    Replace the retired navy `#1B2A4A` and raw gray/blue/red/violet/amber Tailwind classes in both files with the Phase-32 tokens: brand accents → `bg-brand`/`text-brand`/`border-brand`/`bg-brand-fill`/`border-brand-border` (mock `#3C2CDA`); neutral text/surfaces → the `ink-*`/`line-*`/`surface-*` tokens; status colors → the status tokens. Match the mock accent exactly where a raw hex is unavoidable (`#3C2CDA`). This is presentation-only — do NOT change any handler, prop, state, label copy, or DOM structure (D-15). The goal is that the inline body no longer clashes with the token-styled AwaitingCard header (seam resolved).
  </action>
  <acceptance_criteria>
    <automated>cd frontend && grep -Ec "1B2A4A|#2a3d5e" src/components/chat/InlineClarifyActions.tsx src/components/chat/InlineGateActions.tsx (== 0 — navy retired)</automated>
    <automated>cd frontend && grep -Ec "bg-brand|text-brand|border-brand" src/components/chat/InlineClarifyActions.tsx src/components/chat/InlineGateActions.tsx (>= 2 — brand tokens applied)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run InlineClarifyActions InlineGateActions 2>&1 | tail -5 (green — behavior unchanged; tests assert behavior not color)</automated>
  </acceptance_criteria>
  <done>Both inline cards use brand tokens; navy grep 0; behavior/handlers/labels unchanged; the AwaitingCard seam is resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin ResultCard to brand tokens</name>
  <files>frontend/src/components/chat/ResultCard.tsx</files>
  <read_first>
    - frontend/src/components/chat/ResultCard.tsx (retired-palette sites :65,:141; raw gray/blue/violet)
    - CONTEXT §I (same token layer)
  </read_first>
  <action>
    Replace the retired navy + raw palette in ResultCard.tsx with the Phase-32 tokens (`bg-brand`/`text-brand`/`border-brand-border`, ink/line/surface neutrals, status tokens; accent `#3C2CDA`). Presentation-only; no change to the card's deep-link behavior, props, or copy (D-15).
  </action>
  <acceptance_criteria>
    <automated>cd frontend && grep -Ec "1B2A4A" src/components/chat/ResultCard.tsx (== 0)</automated>
    <automated>cd frontend && grep -Ec "bg-brand|text-brand|border-brand" src/components/chat/ResultCard.tsx (>= 1)</automated>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v "mockApi" | grep -c "error TS" (0)</automated>
    <automated>cd frontend && npx vitest run ResultCard 2>&1 | tail -5 (green if a suite exists; otherwise the lane suite covering ResultCard)</automated>
    <human-check>The clarify/gate cards inside the Steps AwaitingCard and the lane ResultCards read as one brand-tokened surface — no navy-body/token-header seam — matching the mock accent #3C2CDA.</human-check>
  </acceptance_criteria>
  <done>ResultCard uses brand tokens; navy grep 0; deep-link behavior unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-token swap only; no data path change. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-10-01 | Tampering | Reskin diff | mitigate | Presentation-only class swaps; behavior tests gate that no handler/prop/DOM changed (D-15). |
| T-42-10-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Navy `#1B2A4A` grep 0 across the three files; brand tokens present.
- tsc clean; InlineClarifyActions/InlineGateActions/ResultCard vitest green (behavior unchanged).
- Gallery: the inline cards read as one brand surface (seam resolved).
</verification>

<success_criteria>
The inline clarify/gate/result cards are on the Phase-32 brand token layer with the retired navy/raw palette removed and the AwaitingCard seam resolved, with zero behavior change (D-15).
</success_criteria>

<artifacts_this_phase_produces>
- Brand-token reskin of `InlineClarifyActions.tsx`, `InlineGateActions.tsx`, `ResultCard.tsx` (navy/raw palette retired).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/42-run-screen-state-fidelity-kill-legacy-full-screen-takeovers-/42-10-SUMMARY.md` when done.
</output>
