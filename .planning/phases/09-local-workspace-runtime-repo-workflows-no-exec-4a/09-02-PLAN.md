---
phase: 09-local-workspace-runtime-repo-workflows-no-exec-4a
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/alembic/versions/0017_repositories_repo_workspace.py
  - backend/app/models/repository.py
  - backend/app/models/__init__.py
  - backend/agents/authz.py
  - backend/app/agents/sandbox.py
  - specs/003-workflow-engine-decoupling/migration-ledger.md
  - backend/tests/agents/test_migration_ledger.py
  - backend/tests/agents/test_repositories_persistence.py
autonomous: true
requirements: [RUNTIME-02, RUNTIME-03]
must_haves:
  truths:
    - "Migration 0017 (down_revision=0016) adds the repositories table; alembic upgrade head then downgrade -1 is reversible"
    - "A repo run persists exactly one repositories row + one kind=repo workspaces row linked by repo_id; both carry owner_id + workspace_id"
    - "Prototype's RunSandbox runs as Workspace(has_git=False, exec=off); the 5-pipeline characterization is byte-identical + semantic-event-identical (NO re-baseline)"
    - "No if repo: engine fork (grep -rn 'if repo' agents/execution_engine/ → 0)"
  artifacts:
    - path: "backend/alembic/versions/0017_repositories_repo_workspace.py"
      provides: "additive repositories table + workspaces.repo_id FK"
      contains: "revision = \"0017\""
    - path: "backend/app/models/repository.py"
      provides: "Repository SQLAlchemy model (owner/workspace-scoped)"
      contains: "class Repository"
    - path: "backend/app/agents/sandbox.py"
      provides: "RunSandbox refolded as a Workspace(has_git=False, exec=off) facade (move-don't-copy)"
      contains: "RunSandbox"
  key_links:
    - from: "backend/app/agents/sandbox.py"
      to: "agents.runtime.base.Workspace"
      via: "RunSandbox delegates disk internals to a has_git=False Workspace"
      pattern: "Workspace"
    - from: "backend/agents/authz.py"
      to: "repositories"
      via: "ScopedStore scoped writer persists the repo + kind=repo workspace rows"
      pattern: "repositor"
---

<objective>
Two RUNTIME deliverables, both parity-critical: (1) additive Alembic migration **0017** (per D-11) (`revision="0017"`, `down_revision="0016"`) adding the `repositories` table + wiring the `workspaces.repo_id` FK, with a `ScopedStore`-backed writer that persists exactly one `repositories` row + one `kind=repo` `workspaces` row per repo run (RUNTIME-03); and (2) the **move-don't-copy refold** of prototype's `RunSandbox` (`backend/app/agents/sandbox.py:49`) so its bespoke per-run disk internals delegate to a `Workspace(has_git=False, exec=off)` — the name survives as a thin facade so every call site is untouched, no `if repo:` engine fork (RUNTIME-02).

Purpose: one `Workspace` abstraction serves both repo and artifact workspaces (R3 — no two-engines fork), and durable repo/workspace records land in the additive schema. The hard gate is INV-3: the 5-pipeline characterization snapshots stay **byte-identical + semantic-event-identical** with `SNAPSHOT_UPDATE` UNSET.
Output: migration 0017, the `Repository` model + scoped writer, the refolded `RunSandbox`, the migration-ledger row, and Wave-0 `test_repositories_persistence.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-SPEC.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-PATTERNS.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-01-SUMMARY.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- New symbols: `Repository` model (`backend/app/models/repository.py`); a `ScopedStore` repo/workspace writer method (e.g. `create_repository`) in `backend/agents/authz.py`
- New file paths: `backend/alembic/versions/0017_repositories_repo_workspace.py`, `backend/app/models/repository.py`, `backend/tests/agents/test_repositories_persistence.py`
- DB objects: `repositories` table; `workspaces.repo_id` FK constraint; `kind='repo'` workspace rows (no enum widening — `kind` is a free `String`)
- Migration: `0017` (down_revision `0016`)
- Migration-ledger row(s): the RunSandbox refold (INV-12 move-don't-copy) entry
- The `RunSandbox` facade survives by name (NOT a new symbol — refolded in place)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): migration 0017 + Repository model + ScopedStore writer + repositories-persistence test + ledger</name>
  <files>backend/alembic/versions/0017_repositories_repo_workspace.py, backend/app/models/repository.py, backend/app/models/__init__.py, backend/agents/authz.py, backend/tests/agents/test_repositories_persistence.py, backend/tests/agents/test_migration_ledger.py, specs/003-workflow-engine-decoupling/migration-ledger.md</files>
  <read_first>
    - backend/alembic/versions/0014_typed_artifacts_persistence.py:82-99 (workspaces create: kind=sa.String server_default sandbox, repo_id sa.String nullable — the "Phase 9 forward field"), :236 (op.drop_table downgrade model)
    - backend/alembic/versions/0016_capability_hardening_tables.py:24-25 (revision/down_revision chain — 0017 sets down_revision="0016")
    - backend/app/models/handoff.py:40 (UserGithubCredential — owner-scoped/encrypted PAT model shape)
    - backend/agents/authz.py:58-79,133-211 (ScopedStore constructor + write_ref owner/workspace stamping; create_workspace/record_capabilities run-entry writers)
    - backend/tests/agents/test_migration_ledger.py:53-55 (ledger ratchet — non-kernel rows scoped to whole backend/)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md (R-G — exact repositories columns; kind needs NO enum widening; reversibility gate)
  </read_first>
  <action>
    Create `backend/alembic/versions/0017_repositories_repo_workspace.py` with `revision="0017"`, `down_revision="0016"`. `upgrade()`: `op.create_table("repositories", ...)` with columns `id`(String PK), `owner_id`(String NN), `workspace_id`(String NN), `provider`(String NN — free String holding `github`/`gitlab`/`local`, NO `sa.Enum`), `url`(String NN), `default_branch`(String NN), `auth_ref`(String nullable — the scoped-cred pointer), `created_at`(DateTime NN); then `op.create_foreign_key(None, "workspaces", "repositories", ["repo_id"], ["id"])` (repositories created FIRST in the same migration so the FK target exists). `downgrade()`: drop the FK then `op.drop_table("repositories")` — clean reverse (mirror `0014:236`). `workspaces.kind` is already a free `String` (`0014:88`) so `kind='repo'` needs NO widening; `workspaces.repo_id` is already nullable `String` (`0014:93`). Create `backend/app/models/repository.py` `class Repository` mirroring the `UserGithubCredential` owner-scoped shape (carries `owner_id`/`workspace_id`); register it in `app/models/__init__.py`. Add a `ScopedStore` writer in `agents/authz.py` (e.g. `create_repository(...)`) that stamps `owner_id`/`workspace_id` and inserts one `repositories` row + sets the `kind=repo` `workspaces` row's `repo_id` — default-deny, owner/workspace-scoped, mirroring `create_workspace`/`record_capabilities` (`engine.py:554-556`). Add the Wave-0 `tests/agents/test_repositories_persistence.py`: assert a scoped write inserts exactly ONE `repositories` row + ONE `kind=repo` `workspaces` row linked by `repo_id` (use a SQLite/in-memory or throwaway DB per backend/CLAUDE.md migration note); assert a cross-owner read of the repo via `ScopedStore` raises `PermissionError` (the default-deny denial gate). Add a migration-ledger row in `specs/003-workflow-engine-decoupling/migration-ledger.md` for the RunSandbox refold (Task 2) with a verbatim grep gate, and confirm `test_migration_ledger.py` still parses the new row.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_repositories_persistence.py tests/agents/test_migration_ledger.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -n 'revision = "0017"' alembic/versions/0017_repositories_repo_workspace.py` and `grep -n 'down_revision = "0016"' ...` both match
    - `cd backend && alembic upgrade head && alembic downgrade -1 && alembic upgrade head` reversible (run against throwaway/SQLite DB per backend/CLAUDE.md; if no DB available, the migration-ledger structural assertion is the fallback — document which path was used)
    - `repositories` table carries `owner_id` + `workspace_id` NN columns; `provider` is a free `String` (no `sa.Enum`)
    - `test_repositories_persistence.py` GREEN: one repositories row + one kind=repo workspaces row linked by repo_id; cross-owner read raises PermissionError
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` GREEN (the new refold ledger row parses)
  </acceptance_criteria>
  <done>Migration 0017 + Repository model + scoped writer land additively, reversible; persistence + ledger tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: RunSandbox refold → Workspace(has_git=False, exec=off) (move-don't-copy, parity-gated)</name>
  <files>backend/app/agents/sandbox.py</files>
  <read_first>
    - backend/app/agents/sandbox.py:49-96 (the current RunSandbox: __init__/ensure/root/path_for + traversal-rejection — the parity oracle), :183-248 (serialize_sandbox_deliverable/count_sandbox_deliverables — the raw-bytes serialization byte oracle, pinned by test_sandbox_deliverable.py)
    - backend/agents/runtime/base.py (the Workspace port from 09-01 — RunSandbox delegates to a has_git=False, exec=off instance)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md (R-B — the COMPLETE RunSandbox caller inventory: factory.py:155,172 / engine.py:48,506-507,1340,1937 / kernel_services.py:36-40,160,471 / deep_agent_runner.py:64,204 / serialized_sandbox.py:37-38 / task_loop.py:56 — the consumed surface that must stay byte-identical)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-PATTERNS.md (RunSandbox refold section + Pitfall 1 re-baseline guard)
  </read_first>
  <action>
    Refold `RunSandbox` IN-PLACE (D-02): the `RunSandbox` name + every consumed name/signature survives byte-for-byte — `__init__(user_id, run_id)`, `.ensure()`, `.root`, `.path_for`, `.read`, `.write`, `.cleanup`, plus the two module-level helpers `serialize_sandbox_deliverable`/`count_sandbox_deliverables`. Internally, the bespoke per-run disk logic is DELETED and DELEGATED to a `Workspace(has_git=False, exec=off)` obtained from the `LocalSandboxRuntime` (09-01) — move-don't-copy (INV-12), NOT a dual impl. `.root` still resolves to `<RUNS_ROOT>/<user>/<run>/`; `path_for` still rejects traversal (`sandbox.py:77-82`); `serialize_sandbox_deliverable` MUST keep its **raw-bytes** read (NOT `read_text`) to preserve CRLF byte-equivalence (`sandbox.py:233-236` — load-bearing). Do NOT introduce any `if repo:` branch in the engine — one code path serves both `has_git=False` (artifact) and `has_git=True` (repo, used by 09-04). Do NOT run `SNAPSHOT_UPDATE=1` — if a golden fails, the refold is wrong (Pitfall 1).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_sandbox_deliverable.py -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - the 5 characterization files + `test_sandbox_deliverable.py` ALL GREEN with `SNAPSHOT_UPDATE` UNSET (byte-identical + semantic-event-identical — NO re-baseline)
    - `cd backend && grep -rn "if repo" agents/execution_engine/` → 0 matches (no engine fork)
    - `cd backend && grep -n "read_text" app/agents/sandbox.py` does NOT appear inside `serialize_sandbox_deliverable` (raw-bytes read preserved)
    - the `RunSandbox` consumed surface (`__init__`/`ensure`/`root`/`path_for`/`read`/`write`/`cleanup` + the 2 module helpers) is unchanged in name/signature — `cd backend && python3.11 -m pytest tests/agents/test_local_runtime.py -x` still GREEN
    - `cd backend && /opt/homebrew/bin/lint-imports` → 4 kept / 0 broken; `python3.11 -m pytest tests/agents/test_migration_ledger.py -x` GREEN (refold ledger row grep gate = 0 bespoke-internals matches)
  </acceptance_criteria>
  <done>RunSandbox delegates to a has_git=False Workspace; 5-pipeline parity byte/event-identical; no engine fork; ledger + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run → DB (repositories/workspaces) | every row must carry owner_id + workspace_id; cross-owner reads denied |
| migration upgrade/downgrade | DDL must be additive + reversible |
| agent → RunSandbox disk | traversal must stay rejected through the refold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-ID | Information Disclosure | cross-owner repo/workspace read | mitigate | `ScopedStore` default-deny `WHERE owner_id=:owner AND workspace_id=:ws` (`authz.py:58`); `test_repositories_persistence.py` cross-owner `PermissionError` denial test (V4) |
| T-09-02-01 | Tampering | migration 0017 reversibility | mitigate | additive-only (`op.create_table` + FK); `downgrade` drops cleanly; `upgrade→downgrade -1` reversible; migration-ledger gate green |
| T-09-02-02 | Tampering | RunSandbox.path_for traversal (preserved through refold) | mitigate | the refold keeps `sandbox.py:77-82` traversal-rejection; covered by `test_local_runtime.py` + characterization (V12) |
| T-09-02-03 | Tampering | prototype parity perturbation (INV-3) | mitigate | move-don't-copy refold gated on the 5-pipeline byte+event snapshots with SNAPSHOT_UPDATE UNSET; serialize raw-bytes preserved |
</threat_model>

<verification>
- the 5 characterization files + `test_sandbox_deliverable.py` GREEN, SNAPSHOT_UPDATE UNSET (the INV-3 parity gate)
- `cd backend && grep -rn "if repo" agents/execution_engine/` → 0
- `test_repositories_persistence.py` GREEN (one repo + one kind=repo workspace row; cross-owner PermissionError)
- `alembic upgrade head && alembic downgrade -1` reversible (or migration-ledger structural assertion if no DB)
- `test_migration_ledger.py` GREEN; `lint-imports` 4/0
</verification>

<success_criteria>
- RUNTIME-02: RunSandbox runs as `Workspace(has_git=False, exec=off)`; 5-pipeline byte+event parity; no `if repo:` fork.
- RUNTIME-03: migration 0017 adds `repositories`; a repo run persists one `repositories` + one `kind=repo` `workspaces` row linked by `repo_id`; upgrade→downgrade reversible; ledger green.
- Move-don't-copy honored — the bespoke RunSandbox disk internals deleted (not dual-implemented), proven by the refold ledger grep gate.
</success_criteria>

<output>
Create `.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-02-SUMMARY.md` when done.
</output>
