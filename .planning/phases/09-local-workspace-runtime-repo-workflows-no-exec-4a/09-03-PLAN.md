---
phase: 09-local-workspace-runtime-repo-workflows-no-exec-4a
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
autonomous: false
files_modified:
  - backend/pyproject.toml
  - backend/agents/capabilities/repo_inventory/__init__.py
  - backend/agents/capabilities/repo_inventory/inventory.py
  - backend/agents/capabilities/context_pack/__init__.py
  - backend/agents/capabilities/context_pack/pack.py
  - backend/agents/capabilities/context_providers/repo.py
  - backend/app/agents/repo_index/__init__.py
  - backend/app/agents/repo_index/index.py
  - backend/agents/capabilities/registry.py
  - backend/agents/workflows/plan.py
  - backend/tests/agents/test_repo_inventory.py
  - backend/tests/agents/test_repo_index.py
  - backend/tests/agents/test_context_pack.py
requirements: [REPO-01, REPO-02, REPO-03]
must_haves:
  truths:
    - "RepoInventory lists source files, excludes .gitignore/.flowinignore + binary files, applies size caps, reports language stats + dependencies"
    - "With the index disabled search uses grep/glob; with the tree-sitter index enabled a symbol query returns file+line of a known function/class"
    - "tree-sitter is imported only behind the repo_index capability module"
    - "ContextPack returns a targeted file/snippet subset (target + selector neighbors), excludes unrelated files, lineage-tracked, surfaced via the repo ContextProvider"
  artifacts:
    - path: "backend/agents/capabilities/repo_inventory/inventory.py"
      provides: "@register('repo_inventory','default') — tree/lang/deps/ignore/binary-skip/size-cap (kernel-side, pure-stdlib)"
      contains: "repo_inventory"
    - path: "backend/app/agents/repo_index/index.py"
      provides: "@register('repo_index','tree_sitter') — symbol index app-side, tree-sitter import isolated"
      contains: "symbol_query"
    - path: "backend/agents/capabilities/context_pack/pack.py"
      provides: "@register('context_pack','default') + context_selector"
      contains: "context_pack"
    - path: "backend/agents/capabilities/context_providers/repo.py"
      provides: "@register('context_provider','repo')"
      contains: "class RepoProvider"
  key_links:
    - from: "backend/app/agents/repo_index/index.py"
      to: "tree_sitter_language_pack.get_parser"
      via: "symbol extraction behind the capability only"
      pattern: "get_parser"
    - from: "backend/agents/capabilities/context_providers/repo.py"
      to: "ctx.scoped_store.write_ref"
      via: "ContextPack lineage-tracked as a typed artifact"
      pattern: "write_ref|scoped_store"
---

<objective>
The repo-context capabilities: `repo_inventory` (kernel-side, pure-stdlib — file tree + language stats + dependency list, honoring `.gitignore` + `.flowinignore`, binary-skip, size-caps; REPO-01), the optional `repo_index` (app-side, tree-sitter symbol extractor reached via the handle, with grep/glob the default search; per D-03 prebuilt-wheels + D-04 in-memory-per-run; REPO-02), and `context_pack` + `context_selector` + the `repo` `ContextProvider` (targeted per-task subset, lineage-tracked; placement per D-10; REPO-03). Pins the two new tree-sitter dependencies behind a blocking human-verify checkpoint (they are `[ASSUMED]` — slopcheck was unavailable offline) and includes the offline-install proof for the lazy-grammar-download Open Risk.

Purpose: agents working a brownfield repo get a structural map, an optional symbol index, and a targeted context subset (the brownfield analog of `html_skeleton` compaction) — all reached via `ctx.runner`/`ctx.scoped_store` handles so the import-linter stays green.
Output: the 4 capabilities, the `repo_index` opt-in flag on the schema, the pinned deps, the offline-install proof, and Wave-0 `test_repo_inventory.py`/`test_repo_index.py`/`test_context_pack.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-SPEC.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-PATTERNS.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-01-SUMMARY.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- New symbols: `RepoInventory` (`repo_inventory/inventory.py`), `RepoIndex`/`symbol_query` (`app/agents/repo_index/index.py`), `ContextPack`/`context_selector` (`context_pack/pack.py`), `RepoProvider` (`context_providers/repo.py`); kernel-pure dataclasses `Match`/`SymbolHit`
- `@register` kinds/names: `("repo_inventory","default")`, `("repo_index","tree_sitter")`, `("context_pack","default")`, `("context_provider","repo")`
- New registry kind strings: `repo_inventory`, `repo_index`, `context_pack`
- New file paths: the 8 capability/test files above + `app/agents/repo_index/`
- New schema field: the `repo_index` opt-in flag on `RepoSpec`/`Step` (`plan.py`) + the N6 threshold config constant
- New deps (`[ASSUMED]`, gated): `tree-sitter`, `tree-sitter-language-pack`
- `discover()` import for `app.agents.repo_index` (app-side, joins `app.agents.validators`)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy checkpoint — tree-sitter + tree-sitter-language-pack (slopcheck unavailable)</name>
  <what-built>
    The two new dependencies for the symbol index are `[ASSUMED]` (slopcheck could not run offline, RESEARCH Package Legitimacy Audit). They must be human-verified on PyPI BEFORE being added to `pyproject.toml`:
    - `tree-sitter==0.25.2` — github.com/tree-sitter/py-tree-sitter (the tree-sitter core py binding)
    - `tree-sitter-language-pack==1.8.1` — github.com/Goldziher/tree-sitter-language-pack (305+ prebuilt abi3 grammar wheels incl. py/js/ts)
  </what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/tree-sitter/ — confirm it is the official `tree-sitter/py-tree-sitter` project, actively maintained, version 0.25.2 exists.
    2. Visit https://pypi.org/project/tree-sitter-language-pack/ — confirm the `Goldziher/tree-sitter-language-pack` repo, prebuilt wheels (abi3, manylinux/macOS), version 1.8.1 exists, and it bundles python/javascript/typescript grammars.
    3. Confirm neither is a typosquat of a different package name.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the install in Task 2, or name a substitute (e.g. the per-language `tree-sitter-python`/`-javascript`/`-typescript` fallback from RESEARCH R-C).</resume-signal>
</task>

<task type="auto">
  <name>Task 2 (Wave 0): pin deps + repo_index (tree-sitter, app-side) + offline-install proof + test_repo_index.py</name>
  <files>backend/pyproject.toml, backend/app/agents/repo_index/__init__.py, backend/app/agents/repo_index/index.py, backend/agents/capabilities/registry.py, backend/agents/workflows/plan.py, backend/tests/agents/test_repo_index.py</files>
  <read_first>
    - backend/agents/capabilities/registry.py:76-111 (_KNOWN), :217-227 (discover() _forward_packages — add app.agents.repo_index beside app.agents.validators)
    - backend/agents/workflows/plan.py:257-262 (RepoSpec url/ref/branch — add the index opt-in flag here, compiler only records it, INV-5)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md (R-C — tree-sitter-language-pack get_parser/get_language; symbol kinds per language; the RepoIndex port two-method shape search/symbol_query; the N6 constant; the OFFLINE-INSTALL RISK + Wave-0 smoke approach)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-PATTERNS.md (repo_index app-side section; Pitfall 2 lazy-download)
  </read_first>
  <action>
    Only after Task 1 approval: add `tree-sitter==0.25.2` + `tree-sitter-language-pack==1.8.1` to `pyproject.toml` (pinned like every dep) and install via `cd backend && python3.11 -m pip install "tree-sitter==0.25.2" "tree-sitter-language-pack==1.8.1"`. Create `backend/app/agents/repo_index/` (app-side — the `tree_sitter` import lives ONLY here, REPO-02 acceptance) with `@register("repo_index", "tree_sitter")` exposing TWO methods through one seam: `search(pattern, *, glob=None) -> list[Match]` (grep/glob — the DEFAULT, backs onto `Workspace.search` via the handle, ALWAYS available) and `symbol_query(name) -> list[SymbolHit]` (`SymbolHit(file, line, kind)` — only when the index is built). The index is built IN-MEMORY PER-RUN (no new table) on manifest opt-in OR the N6 threshold; discard at teardown. `Match`/`SymbolHit` are kernel-pure dataclasses. Use `tree_sitter_language_pack.get_parser("python"|"javascript"|"typescript")` + one tree-sitter `Query` per language extracting functions/classes/imports → `(symbol_name, kind, file, start_point.row+1)`. Define the N6 threshold as a documented module-level constant (e.g. `REPO_INDEX_FILE_THRESHOLD = 2000`) + a manifest opt-in flag on `RepoSpec` (e.g. `index: bool = False`) — the compiler only RECORDS the flag (INV-5); the build decision (`flag OR file_count > N6`) lives in the capability. Add `("repo_index","tree_sitter")` to `_KNOWN` and `"app.agents.repo_index"` to `discover()` `_forward_packages`. Create the Wave-0 `tests/agents/test_repo_index.py` including the OFFLINE-INSTALL PROOF: a smoke test that calls `get_parser("python")` + parses a fixture with the network disabled/stubbed and asserts a non-empty tree (proving grammars are wheel-bundled, NOT lazy-downloaded); plus index-off → grep/glob and index-on → `symbol_query` returns the file+line of a known function/class. If the offline proof FAILS (lazy download), fall back to per-language `tree-sitter-python`/`-javascript`/`-typescript` wheels per RESEARCH R-C and document it.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_repo_index.py -x 2>&1 | tail -20 && grep -rn "import tree_sitter" agents/ app/ | grep -v "app/agents/repo_index/"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agents/test_repo_index.py` GREEN: offline `get_parser("python")` parses without network (non-empty tree); index-off → grep/glob; index-on → `symbol_query` returns file+line of a known symbol
    - `cd backend && grep -rn "import tree_sitter" agents/ app/ | grep -v "app/agents/repo_index/"` returns 0 lines (tree-sitter imported ONLY behind the capability — REPO-02 acceptance)
    - `cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry, discover; discover(); print(CapabilityRegistry().is_registered('repo_index','tree_sitter'))"` prints `True`
    - `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -x` GREEN (tree-sitter is a LIBRARY, not a deep-agent runtime — INV-13 untouched)
    - the N6 threshold constant + the RepoSpec `index` opt-in flag exist; the compiler records the flag without control flow (INV-5)
  </acceptance_criteria>
  <done>tree-sitter deps pinned + installed (post-approval); repo_index registered app-side with tree-sitter import-isolated; offline-install proof GREEN; banned-pattern green.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 0): repo_inventory + context_pack/context_selector + repo ContextProvider (kernel-side) + tests</name>
  <files>backend/agents/capabilities/repo_inventory/__init__.py, backend/agents/capabilities/repo_inventory/inventory.py, backend/agents/capabilities/context_pack/__init__.py, backend/agents/capabilities/context_pack/pack.py, backend/agents/capabilities/context_providers/repo.py, backend/agents/capabilities/registry.py, backend/tests/agents/test_repo_inventory.py, backend/tests/agents/test_context_pack.py</files>
  <read_first>
    - backend/agents/capabilities/deliverables/serialized_sandbox.py:24-40 (resolver-via-handle idiom — `runner = ctx.runner`)
    - backend/agents/capabilities/context_providers/previous_run.py:115-125,159-164,187-218 (async load → dict[str,str]; getattr(ctx,'scoped_store'); assert_owns + propagate PermissionError; write_ref lineage)
    - backend/agents/capabilities/registry.py:135-159 (@register decorator + _KNOWN/_IMPLS/_TRUST), :193-211 (discover() _builtin_modules for kernel-side modules)
    - backend/agents/authz.py:133 (ScopedStore.write_ref — lineage-track the inventory + context pack as typed artifacts)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md (R-F — repo_inventory ignore/binary-skip/size-cap; git ls-files --exclude-standard via handle; pure-stdlib kernel-side placement)
    - backend/tests/agents/conftest.py (the local_git_fixture seeder from 09-01)
  </read_first>
  <action>
    Create three kernel-side, pure-stdlib capabilities (reach git/disk ONLY via `ctx.runner`/`ctx.scoped_store` — never an `app.*` import, Pitfall 5):
    (1) `agents/capabilities/repo_inventory/inventory.py` `@register("repo_inventory", "default")`, `name="default"` — produces file tree + language stats (by extension) + dependency list (parse `requirements.txt`/`pyproject.toml`/`package.json`). Honor ignore rules: prefer `git ls-files --cached --others --exclude-standard` via the handle (honors `.gitignore` natively), then additionally apply `.flowinignore` globs with `fnmatch`; binary-skip via null-byte sniff (read first ~8KB, skip if `b"\x00"` present — mirror `serialize_sandbox_deliverable`'s UTF-8-decode-or-skip defensive idiom); documented per-file + total size caps as module constants. Lineage-track the output via `ScopedStore.write_ref` (`authz.py:133`) as a typed artifact `kind=repo_inventory`.
    (2) `agents/capabilities/context_pack/pack.py` `@register("context_pack", "default")` + a `context_selector` — builds a TARGETED subset (files/snippets) for a task: given a target file, include it + selector-chosen neighbors, EXCLUDE unrelated files; lineage-tracked per task.
    (3) `agents/capabilities/context_providers/repo.py` `@register("context_provider", "repo")`, `name="repo"`, `async def load(self, ctx) -> dict[str, str]` — surfaces the ContextPack to agents; reach the store via `getattr(ctx,'scoped_store',None)`, `await scoped_store.assert_owns(...)` with a cross-owner `PermissionError` that PROPAGATES (never swallowed, L16). Add the three `(kind,name)` pairs to `_KNOWN` and their import lines to `discover()` `_builtin_modules` (kernel-side). Create Wave-0 `test_repo_inventory.py` (on a fixture with a binary file + a `.flowinignore` entry: inventory lists source files, excludes ignored + binary, reports lang stats + deps) and `test_context_pack.py` (pack contains target + neighbors, excludes unrelated, an `artifact_ref` is written and the `repo` provider surfaces it).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_repo_inventory.py tests/agents/test_context_pack.py -x 2>&1 | tail -20 && /opt/homebrew/bin/lint-imports 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test_repo_inventory.py` GREEN: lists source files, EXCLUDES `.gitignore`/`.flowinignore` + binary files, applies size caps, reports language stats + dependencies
    - `test_context_pack.py` GREEN: pack contains the target file + selector neighbors, EXCLUDES unrelated files, an `artifact_ref` is written (lineage-tracked), surfaced via the `repo` provider
    - `cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry, discover; discover(); r=CapabilityRegistry(); print(r.is_registered('repo_inventory','default'), r.is_registered('context_pack','default'), r.is_registered('context_provider','repo'))"` prints `True True True`
    - `cd backend && grep -rnE "import (app|agents\.execution_engine)" agents/capabilities/repo_inventory/ agents/capabilities/context_pack/ agents/capabilities/context_providers/repo.py` returns 0 lines (kernel-clean)
    - `cd backend && /opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
  </acceptance_criteria>
  <done>repo_inventory + context_pack/selector + repo provider registered kernel-side, pure-stdlib, lineage-tracked; inventory + context-pack tests GREEN; import-linter 4/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability → repo filesystem | inventory/index/pack walk untrusted repo paths — must stay repo-relative, skip binary, honor ignore |
| heavy dep (tree-sitter) → kernel | tree-sitter must stay import-isolated app-side; never leak into the kernel |
| context provider → ScopedStore | cross-owner artifact reads must be denied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | inventory/index path traversal | mitigate | reads via `Workspace`/handle under the run root; binary-skip + `.gitignore`/`.flowinignore` exclusion; size-caps; assert in `test_repo_inventory.py` (V12) |
| T-09-03-02 | Tampering | tree-sitter import leak | mitigate | `repo_index` app-side; `grep -rn "import tree_sitter"` only the capability module; import-linter 4/0 (V1) |
| T-09-03-ID | Information Disclosure | cross-owner ContextPack/artifact read | mitigate | `repo` provider `assert_owns` propagates `PermissionError`; lineage via owner/workspace-scoped `write_ref` (V4) |
| T-09-03-SC | Tampering | hallucinated/slopsquatted dep (`tree-sitter`, `tree-sitter-language-pack`) | mitigate | Task 1 `checkpoint:human-verify` (blocking-human) verifies both on PyPI BEFORE the `pyproject.toml` add; `[ASSUMED]` until verified; not auto-approvable |
</threat_model>

<verification>
- `test_repo_inventory.py` + `test_context_pack.py` + `test_repo_index.py` GREEN
- `cd backend && grep -rn "import tree_sitter" agents/ app/ | grep -v "app/agents/repo_index/"` → 0
- `cd backend && /opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
- `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -x` GREEN (tree-sitter is a lib)
- the 5 characterization snapshots untouched (09-03 is purely additive — spot-check `test_characterization_prototype.py` stays green)
</verification>

<success_criteria>
- REPO-01: `RepoInventory` lists src, excludes ignore + binary, size caps, lang stats + deps; lineage-tracked.
- REPO-02: grep/glob default; tree-sitter index returns file+line; tree-sitter imported ONLY behind the capability; offline-install proof GREEN.
- REPO-03: `ContextPack` targeted subset, excludes unrelated, lineage-tracked, surfaced via the `repo` provider.
- The 2 new deps gated by a blocking human-verify checkpoint before install.
</success_criteria>

<output>
Create `.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-03-SUMMARY.md` when done.
</output>
