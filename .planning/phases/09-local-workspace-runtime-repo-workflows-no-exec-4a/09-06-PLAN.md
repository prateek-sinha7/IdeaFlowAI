---
phase: 09-local-workspace-runtime-repo-workflows-no-exec-4a
plan: 06
type: execute
wave: 6
depends_on: ["09-05"]
autonomous: true
files_modified:
  - backend/agents/capabilities/integration_providers/__init__.py
  - backend/agents/capabilities/integration_providers/providers.py
  - backend/agents/capabilities/registry.py
  - backend/agents/factory.py
  - backend/agents/execution_engine/engine.py
  - backend/app/agents/handoff/coding_agent.py
  - backend/app/agents/handoff/__init__.py
  - backend/app/services/handoff_pipeline.py
  - backend/tests/unit/test_handoff_agents.py
  - specs/003-workflow-engine-decoupling/migration-ledger.md
  - backend/tests/agents/test_integration_providers.py
  - backend/tests/agents/test_integration_scopes.py
requirements: [INTEG-01, INTEG-02]
must_haves:
  truths:
    - "integration_provider capabilities for github/gitlab/jira/slack resolve from the unified create_runner path (not only the handoff pipeline)"
    - "integrations permission scopes default none; granting gitlab_read binds only read tools; run_capabilities records the active MCP servers + integration scopes + runtime for the run"
    - "the CodingAgent handoff bypass (build_model().ainvoke that skips create_runner) is DELETED in-plan with a migration-ledger entry; the /api/handoff routers + UserGithubCredential are RETAINED with explicit ledger justification"
    - "the 5-pipeline characterization stays byte/event-identical and the handoff contract tests stay green"
  artifacts:
    - path: "backend/agents/capabilities/integration_providers/providers.py"
      provides: "@register('integration_provider', github|gitlab|jira|slack) — the MCP-backed bridge (one mechanism)"
      contains: "integration_provider"
    - path: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      provides: "the CodingAgent-deletion ledger row + the retained-/api/handoff-endpoint justification"
      contains: "CodingAgent"
  key_links:
    - from: "backend/agents/capabilities/integration_providers/providers.py"
      to: "the mcp_server catalog + integrations scope"
      via: "the bridge binds catalog MCP tools into create_runner — no parallel SDK path"
      pattern: "mcp_server|integration"
    - from: "backend/agents/execution_engine/engine.py"
      to: "ScopedStore.record_capabilities"
      via: "run_capabilities records the run's integration scopes + MCP servers + runtime"
      pattern: "record_capabilities"
---

<objective>
Close the phase: `integration_provider` capabilities for github/gitlab/jira/slack reachable from the unified `create_runner` path — each a thin **MCP-backed bridge** (takes a catalog `mcp_server` from 09-05 + an `integrations` scope, surfaces its tools into `create_runner` via the same factory seam + async prewarm; ONE mechanism, no parallel SDK path per D-08; INTEG-01); the `integrations` tool-permission scopes (`github_read`/`gitlab_read`/`jira_read`/`slack_post`, default **none**, owner allow-list + scoped creds, persisted per run via `run_capabilities`; INTEG-02); and the **deletion of the `CodingAgent` handoff bypass** (the `build_model().ainvoke` one-shot that skips the deepagents runtime — the INV-13 gap D-09 names) with a migration-ledger entry, while **RETAINING** the live `/api/handoff` routers + `UserGithubCredential` per R-E with explicit ledger justification.

Purpose: GitHub/GitLab/Jira/Slack become reachable from the unified runner (not only the handoff pipeline), and the superseded coding bypass is deleted (INV-12 move-don't-copy). The hard gates: the 5-pipeline characterization stays byte/event-identical AND the handoff contract tests stay green (the deletion-scope guard).
Output: the integration providers, the `integrations` scope wiring + `run_capabilities` persistence, the precise `CodingAgent` deletion + ledger rows, and Wave-0 `test_integration_providers.py`/`test_integration_scopes.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-SPEC.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-PATTERNS.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-05-SUMMARY.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- New symbols: the four integration-provider classes (`integration_providers/providers.py`)
- `@register` kinds/names: `("integration_provider","github")`, `("integration_provider","gitlab")`, `("integration_provider","jira")`, `("integration_provider","slack")`
- New registry kind string: `integration_provider`
- New scope strings: `github_read`, `gitlab_read`, `jira_read`, `slack_post` (in `ToolPermissions.integrations`, default none)
- New file paths: `integration_providers/`, `tests/agents/test_integration_providers.py`, `tests/agents/test_integration_scopes.py`
- Migration-ledger rows: the `CodingAgent` deletion (with grep gate `class CodingAgent` → 0 in backend/) + the RETAINED `/api/handoff` endpoint + `UserGithubCredential` justification
- DELETIONS (not new symbols): `CodingAgent` + its references in `handoff_pipeline.py`/`handoff/__init__.py` + its unit tests
- `discover()` import for `agents.capabilities.integration_providers`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): integration_provider capabilities (MCP-backed bridge) + integrations scopes + run_capabilities persistence + tests</name>
  <files>backend/agents/capabilities/integration_providers/__init__.py, backend/agents/capabilities/integration_providers/providers.py, backend/agents/capabilities/registry.py, backend/agents/factory.py, backend/agents/execution_engine/engine.py, backend/tests/agents/test_integration_providers.py, backend/tests/agents/test_integration_scopes.py</files>
  <read_first>
    - backend/agents/capabilities/registry.py:135-159 (@register idiom + _KNOWN), :193-211 (discover() _builtin_modules — add the integration_providers import)
    - backend/agents/workflows/plan.py:70-72 (ToolPermissions.integrations slot — already present, default none), :145-184 (the perms intersection + check())
    - backend/agents/factory.py:457-499 (_resolve_runner_tools — the binding seam the integration tools enter through, the SAME seam MCP tools use in 09-05)
    - backend/agents/execution_engine/engine.py:554-556 (ScopedStore.record_capabilities — records the run's runtime + integrations + mcp_servers per CAPRUN-01)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md (R-E — the integration bridge is thin: catalog mcp_server + integrations scope → create_runner via the factory seam; ONE mechanism, no SDK path; scopes default none, recorded per run)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-05-SUMMARY.md (the mcp_server catalog names + the async-prewarm binding the bridge reuses)
  </read_first>
  <action>
    Create `agents/capabilities/integration_providers/providers.py` `@register("integration_provider", "github"|"gitlab"|"jira"|"slack")` — each a THIN bridge that takes the matching catalog `mcp_server` (09-05) + an `integrations` scope and surfaces that server's tools into `create_runner` via the SAME factory seam (`_resolve_runner_tools`/`_build_runner_tools`, `factory.py:457`) + the SAME async prewarm binding (09-05). ONE mechanism (MCP) — NO PyGithub/python-gitlab/Slack-SDK parallel path (D-08). Add the four `("integration_provider", …)` pairs to `_KNOWN` + a `discover()` import. Wire the `integrations` scopes (`github_read`/`gitlab_read`/`jira_read`/`slack_post`) on `ToolPermissions.integrations` (default none — a step with no grant binds zero integration tools; granting `gitlab_read` binds only read tools); the effective-perms intersection gates which tools bind. Record the run's active integration scopes + MCP servers + runtime per-run via `ScopedStore.record_capabilities` (`engine.py:554-556`, CAPRUN-01). Create Wave-0 `test_integration_providers.py` (a step declaring an integration resolves it from `create_runner` — the runner gains the integration's tools; the github path no longer requires the bypass) and `test_integration_scopes.py` (a step with no `integrations` grant binds zero integration tools; granting `gitlab_read` binds only read tools; assert a `run_capabilities` row records the scopes + servers + runtime).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_integration_providers.py tests/agents/test_integration_scopes.py -x 2>&1 | tail -20 && /opt/homebrew/bin/lint-imports 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test_integration_providers.py` GREEN: github/gitlab/jira/slack resolve from `create_runner` (the runner gains the integration's tools, not only the handoff pipeline)
    - `test_integration_scopes.py` GREEN: `integrations` default none; granting `gitlab_read` binds only read tools; a `run_capabilities` row records the active scopes + MCP servers + runtime
    - `cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry, discover; discover(); r=CapabilityRegistry(); print(all(r.is_registered('integration_provider',n) for n in ('github','gitlab','jira','slack')))"` prints `True`
    - `cd backend && grep -rniE "PyGithub|python-gitlab|slack_sdk|slack-sdk" agents/capabilities/integration_providers/` returns 0 (one mechanism — no parallel SDK path)
    - `cd backend && grep -rnE "import (app|agents\.execution_engine)" agents/capabilities/integration_providers/` returns 0 (kernel-clean); `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
  </acceptance_criteria>
  <done>The four integration providers bridge catalog MCP tools into create_runner via one mechanism; scopes default none + recorded per run; both tests GREEN; lint 4/0.</done>
</task>

<task type="auto">
  <name>Task 2: delete the CodingAgent handoff bypass (move-don't-copy) + ledger rows; RETAIN the /api/handoff routers + UserGithubCredential</name>
  <files>backend/app/agents/handoff/coding_agent.py, backend/app/agents/handoff/__init__.py, backend/app/services/handoff_pipeline.py, backend/tests/unit/test_handoff_agents.py, specs/003-workflow-engine-decoupling/migration-ledger.md</files>
  <read_first>
    - backend/app/agents/handoff/coding_agent.py:145-185 (CodingAgent.propose_edits — the `build_model().ainvoke` one-shot that SKIPS create_runner; the INV-13 gap; the precise deletion target), :164-170 (the build_model().ainvoke call)
    - backend/app/services/handoff_pipeline.py:30,420-422 (the CodingAgent import + instantiation in run_handoff_pipeline — re-point or remove the CodingAgent step)
    - backend/app/agents/handoff/__init__.py:10,15 (the CodingAgent export)
    - backend/app/api/handoff.py:51,172,227,265,368 (the LIVE /api/handoff router — RETAIN per R-E; registered main.py:153), backend/app/models/handoff.py:40 (UserGithubCredential PAT — RETAIN, the model for MCP/integration creds)
    - backend/tests/integration/test_handoff_contract.py (drives the REAL run_handoff_pipeline under a mock seam — the deletion-scope guard, must stay green)
    - backend/tests/agents/test_migration_ledger.py:53-55 (the ledger ratchet — non-kernel rows scoped to whole backend/)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md (R-E — the PRECISE deletion scope: DELETE CodingAgent only; RETAIN routers/UserGithubCredential/handoff_github with justification; re-point vs retain-whole gated on test_handoff_contract.py)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-PATTERNS.md (the DELETION TARGET section + Pitfall 4 do-not-delete-the-router)
  </read_first>
  <action>
    DELETE ONLY the `CodingAgent` bypass (the `build_model().ainvoke` one-shot that skips `create_runner` — the INV-13 gap, `coding_agent.py:145-185`) now that the `integration_provider` path (Task 1) produces edits through the unified runner: remove `app/agents/handoff/coding_agent.py`, its references in `handoff_pipeline.py:30,420-422` (re-point the coding step at the `integration_provider`/unified runner OR remove the step — choose whichever keeps `test_handoff_contract.py` green), its export in `handoff/__init__.py:10,15`, and its dedicated unit tests in `tests/unit/test_handoff_agents.py` (the coding cases). Move-don't-copy (INV-12) — do NOT leave a dual impl. **RETAIN** (do NOT delete — Pitfall 4): `app/api/handoff.py` + `app/api/websocket_handoff.py` (the live `/api/handoff` external IDE surface, registered `main.py:153,157`), `app/services/handoff_github.py` (the git-op layer the retained endpoint uses), and `app/models/handoff.py` incl. `UserGithubCredential:40` (the PAT precedent / MCP-cred model). Add TWO migration-ledger rows in `specs/003-workflow-engine-decoupling/migration-ledger.md`: (1) the `CodingAgent` deletion with a verbatim grep gate (`class CodingAgent` → 0 matches in `backend/`); (2) a RETAINED-with-justification row for the `/api/handoff` routers + `UserGithubCredential` (live external surface — D-09 deletion-scope guard).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && grep -rcn "class CodingAgent" . ; python3.11 -m pytest tests/integration/test_handoff_contract.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && grep -rn "class CodingAgent" .` returns 0 matches (the bypass deleted — the ledger grep gate)
    - `app/api/handoff.py`, `app/api/websocket_handoff.py`, `app/services/handoff_github.py`, and `app/models/handoff.py` (incl. `UserGithubCredential`) still exist (RETAINED — Pitfall 4)
    - `cd backend && python3.11 -m pytest tests/integration/test_handoff_contract.py -x` GREEN (the deletion-scope guard — the retained endpoint's pipeline still drives)
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` GREEN (both new ledger rows parse — the CodingAgent-deletion grep gate + the RETAINED justification)
    - `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -x` GREEN (the bypass removed closes an INV-13 gap; no hand-rolled loop reintroduced)
    - the 5 characterization snapshots stay byte/event-identical (SNAPSHOT_UPDATE UNSET) — `python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_app_builder.py -x` GREEN
  </acceptance_criteria>
  <done>The CodingAgent bypass deleted (move-don't-copy) with two ledger rows; the /api/handoff routers + UserGithubCredential RETAINED; handoff contract + migration-ledger + banned-pattern + 5-pipeline parity GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run → external integration (github/gitlab/jira/slack) | integration tools reach external surfaces — gated by `integrations` scope (default none) + scoped creds |
| owner → integration creds | per-owner scoped creds; cross-owner read denied |
| superseded bypass → runtime | the CodingAgent bypass skipped the deepagents runtime (INV-13 gap) — deleted |
| deletion → live /api/handoff surface | the live IDE router must survive the deletion (external consumers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Elevation of Privilege | unscoped integration tool binding | mitigate | `integrations` scopes default none; granting `gitlab_read` binds only read tools; the effective-perms intersection gates binding (INTEG-02; V5); write-scopes (e.g. `slack_post`) follow the 09-05 security+secrets gate |
| T-09-06-ID | Information Disclosure | cross-owner integration cred read | mitigate | scoped per-owner creds (the `UserGithubCredential`/`ScopedStore` precedent from 09-05); cross-owner read raises `PermissionError` (V2/V4) |
| T-09-06-INV13 | Elevation of Privilege | the CodingAgent bypass skipping the runtime | mitigate | DELETE the `build_model().ainvoke` bypass; integration edits flow through `create_runner`/`create_deep_agent`; banned-pattern + ledger gates (INV-12/INV-13) |
| T-09-06-Avail | Denial of Service | deleting the live `/api/handoff` router | mitigate | RETAIN the routers + `UserGithubCredential` with a ledger justification; `test_handoff_contract.py` is the deletion-scope guard (Pitfall 4) |
| T-09-06-Parity | Tampering | characterization parity perturbation | mitigate | the deletion removes only the bypass; the 5-pipeline snapshots stay byte/event-identical (SNAPSHOT_UPDATE UNSET) |
| T-09-06-SC | Tampering | (no new package this plan) | accept | 09-06 adds no dependency (the MCP dep landed gated in 09-05) |
</threat_model>

<verification>
- `test_integration_providers.py` + `test_integration_scopes.py` GREEN
- `cd backend && grep -rn "class CodingAgent" .` → 0; `app/api/handoff.py` + `UserGithubCredential` retained
- `test_handoff_contract.py` GREEN (deletion-scope guard)
- `test_migration_ledger.py` GREEN (both new rows); `test_banned_patterns.py` GREEN
- the 5 characterization snapshots byte/event-identical (SNAPSHOT_UPDATE UNSET); `cd backend && /opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
</verification>

<success_criteria>
- INTEG-01: github/gitlab/jira/slack `integration_provider` capabilities resolve from the unified `create_runner` path (not only the handoff pipeline) — one MCP mechanism, no parallel SDK.
- INTEG-02: `integrations` scopes default none; `gitlab_read` binds only read tools; `run_capabilities` records the active scopes + MCP servers + runtime.
- D-09: the `CodingAgent` bypass deleted in-plan (move-don't-copy, ledger grep gate = 0); the `/api/handoff` routers + `UserGithubCredential` RETAINED with explicit ledger justification.
- The 5-pipeline characterization + handoff contract tests stay green (the parity + deletion-scope gates).
</success_criteria>

<output>
Create `.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-06-SUMMARY.md` when done.
</output>
