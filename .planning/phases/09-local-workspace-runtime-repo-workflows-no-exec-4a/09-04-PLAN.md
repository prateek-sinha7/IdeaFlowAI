---
phase: 09-local-workspace-runtime-repo-workflows-no-exec-4a
plan: 04
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03"]
autonomous: true
files_modified:
  - backend/agents/capabilities/deliverables/repo_diff.py
  - backend/agents/capabilities/registry.py
  - backend/agents/workflows/manifests/sample_brownfield/workflow.yaml
  - backend/tests/agents/test_repo_diff.py
  - backend/tests/agents/test_sample_brownfield_workflow.py
requirements: [REPO-04, REPO-05]
must_haves:
  truths:
    - "repo_diff returns a file tree + per-file unified diff (containing the agent's edit) + a change summary; no commit/PR push occurs"
    - "the sample brownfield workflow runs end-to-end offline: clone fixture → create_branch → repo_inventory → agent reads/edits ≥1 file (write_files, no exec) → repo_diff"
    - "the surfaced diff contains the agent's edit; exec=off at every step (effective-perms intersection has exec off)"
    - "prototype characterization stays byte/event-identical (the repo path is additive)"
  artifacts:
    - path: "backend/agents/capabilities/deliverables/repo_diff.py"
      provides: "@register('deliverable','repo_diff') — reads Workspace.git_diff via handle; diff-only"
      contains: "repo_diff"
    - path: "backend/agents/workflows/manifests/sample_brownfield/workflow.yaml"
      provides: "the file-backed sample brownfield manifest (clone→branch→inventory→read/edit→diff, no exec)"
      contains: "repo_diff"
    - path: "backend/tests/agents/test_sample_brownfield_workflow.py"
      provides: "the REPO-05 end-to-end accept proof (offline, scripted model)"
  key_links:
    - from: "backend/agents/capabilities/deliverables/repo_diff.py"
      to: "ctx.runner.workspace.git_diff"
      via: "resolver reads the diff from the Workspace handle, never shells git"
      pattern: "git_diff"
    - from: "backend/agents/workflows/manifests/sample_brownfield/workflow.yaml"
      to: "repo_inventory + repo provider + repo_diff"
      via: "the manifest declares the brownfield capability chain"
      pattern: "repo"
---

<objective>
The repo deliverable + the phase accept proof: the `repo_diff` `DeliverableResolver` (kernel-side, reads the diff from `Workspace.git_diff` via the handle — never shells git directly; produces a file tree + per-file unified diff + change summary; **diff-only, no commit/PR push** per N4; REPO-04), and the file-backed **sample brownfield workflow** manifest that runs end-to-end offline: clone a local fixture → `create_branch` → `repo_inventory` → an agent reads N files and edits ≥1 (`write_files`, **no exec**) → `repo_diff` surfaces the tree + diff (REPO-05). Prototype is unaffected.

Purpose: prove the entire net-new brownfield workflow class works against a local fixture with exec OFF at every step and the diff containing the agent's edit — the REPO-05 success criterion and the first use of the `Workspace(has_git=True)` path (09-02 refold serves both).
Output: `repo_diff.py`, the sample manifest, and Wave-0 `test_repo_diff.py` + `test_sample_brownfield_workflow.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-SPEC.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-PATTERNS.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-01-SUMMARY.md
@.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-03-SUMMARY.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- New symbols: `RepoDiffResolver` (`deliverables/repo_diff.py`)
- `@register` kind/name: `("deliverable","repo_diff")`
- New file paths: `deliverables/repo_diff.py`, `manifests/sample_brownfield/workflow.yaml` (+ AGENT.md files as the manifest needs), `tests/agents/test_repo_diff.py`, `tests/agents/test_sample_brownfield_workflow.py`
- The sample brownfield workflow id (a NEW non-prototype workflow — no engine edit to run it, SC-001 discipline)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): repo_diff DeliverableResolver (reads Workspace.git_diff via handle, diff-only) + test_repo_diff.py</name>
  <files>backend/agents/capabilities/deliverables/repo_diff.py, backend/agents/capabilities/registry.py, backend/tests/agents/test_repo_diff.py</files>
  <read_first>
    - backend/agents/capabilities/deliverables/serialized_sandbox.py:21-40 (the EXACT resolver-via-handle analog — `@register("deliverable",...)`, `name=...`, `def resolve(self, ctx)`, `runner = ctx.runner`)
    - backend/agents/runtime/base.py (the Workspace port from 09-01 — `git_diff(base, work)` is the single git-subprocess owner)
    - backend/agents/capabilities/registry.py:84-87 (the deliverable entries in _KNOWN), :198 (the deliverables discover() imports)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-RESEARCH.md (R-F — repo_diff reads from Workspace.git_diff, never shells git; output = tree + per-file diff + summary; diff-only no push N4)
    - backend/tests/agents/conftest.py (the local_git_fixture from 09-01)
  </read_first>
  <action>
    Create `backend/agents/capabilities/deliverables/repo_diff.py` `@register("deliverable", "repo_diff")`, `name = "repo_diff"`, one `def resolve(self, ctx) -> Any`. Read the diff via the handle — `ctx.runner.workspace.git_diff(base_branch, working_branch)` (the branches come from the manifest-declared `base_branch`/`working_branch`, runtime-enforced) — and NEVER shell git from the resolver (D-10; the `Workspace` owns the single `git diff` subprocess). Compose the output as a file tree + per-file unified diff + a change summary (file count, lines added/removed). Strictly **diff-only — no commit/PR push** (N4): the resolver MUST NOT invoke any `git commit`/`git push`. Add `("deliverable", "repo_diff")` to `_KNOWN` (`registry.py:84-87`) and a `discover()` import line (`registry.py:198`). Create Wave-0 `tests/agents/test_repo_diff.py` (reuse `local_git_fixture`): after editing ≥1 file on the working branch, `repo_diff` returns a tree, a per-file unified diff CONTAINING the edit, and a summary; assert `git log <working_branch>` shows NO new commit beyond the branch point (no push/commit performed).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_repo_diff.py -x 2>&1 | tail -20 && grep -nE "git (commit|push)|subprocess.*git diff" agents/capabilities/deliverables/repo_diff.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_repo_diff.py` GREEN: `repo_diff` returns tree + per-file unified diff (containing the edit) + summary
    - `git log` on the working branch shows no new commit beyond the branch point (no push/commit) — asserted in the test
    - `cd backend && grep -nE "git (commit|push)" agents/capabilities/deliverables/repo_diff.py` returns 0 lines (diff-only)
    - `cd backend && grep -n "subprocess\|os.system\|Popen" agents/capabilities/deliverables/repo_diff.py` returns 0 (reads via `ctx.runner.workspace.git_diff`, never shells git)
    - `cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry, discover; discover(); print(CapabilityRegistry().is_registered('deliverable','repo_diff'))"` prints `True`
    - `cd backend && /opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
  </acceptance_criteria>
  <done>repo_diff resolver registered, reads via the handle, diff-only (no push); test GREEN; import-linter 4/0.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 0): sample brownfield workflow manifest + end-to-end accept test (no exec)</name>
  <files>backend/agents/workflows/manifests/sample_brownfield/workflow.yaml, backend/tests/agents/test_sample_brownfield_workflow.py</files>
  <read_first>
    - backend/agents/workflows/manifests/ (the existing file-backed manifest layout + an existing workflow.yaml for the schema — strategy/steps/deliverable/tools/context_providers fields)
    - backend/agents/workflows/plan.py:46-72 (ToolPermissions — exec/network/secrets default OFF; the effective-perms intersection means exec stays off), :257-262 (RepoSpec url/ref/branch + the index flag from 09-03)
    - backend/tests/agents/test_characterization_app_builder.py (the scripted-model offline end-to-end drive idiom for a code-producing workflow)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-SPEC.md (REPO-05 acceptance — clone→branch→inventory→read/edit ≥1→repo_diff; diff contains the edit; zero exec everywhere)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-03-SUMMARY.md (the repo_inventory + repo provider capability names to declare)
  </read_first>
  <action>
    Author a file-backed sample brownfield manifest `backend/agents/workflows/manifests/sample_brownfield/workflow.yaml` (+ any AGENT.md files the steps reference) declaring the chain: a `RepoSpec` binding a local fixture (`url` = the local fixture path, `base_branch`/`working_branch` declared), `repo_inventory` (09-03), the `repo` context provider, a `task_loop`/`single_shot` step whose agent reads N files and edits ≥1 (`write_files` granted, **`exec` NOT granted**), and `repo_diff` (Task 1) as the deliverable. This is a NEW non-prototype workflow — it must run with ZERO engine edit (the SC-001 discipline: all power lives in the registered capabilities). Create Wave-0 `tests/agents/test_sample_brownfield_workflow.py` (scripted model, `local_git_fixture`, fully offline): drive the manifest end-to-end and assert (a) the run completes, (b) the surfaced `repo_diff` CONTAINS the agent's edit, (c) the effective-perms intersection has `exec=off` at EVERY step (no step grants exec anywhere on the path), and (d) prototype characterization stays byte/event-identical (the repo path is additive — spot-assert the prototype snapshot is untouched in the same session or via a separate parity run).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_sample_brownfield_workflow.py tests/agents/test_characterization_prototype.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `test_sample_brownfield_workflow.py` GREEN: the run completes end-to-end offline; the surfaced diff contains the agent's edit
    - the effective-perms intersection has `exec=off` at every step (asserted in the test — zero exec permission anywhere on the path)
    - `test_characterization_prototype.py` stays GREEN in the same run (prototype unaffected — additive)
    - the manifest compiles cleanly (`repo_diff`/`repo_inventory`/`repo` provider all resolve against the registry) — no engine edit was needed to run the new workflow
    - `cd backend && /opt/homebrew/bin/lint-imports` → 4 kept / 0 broken; `python3.11 -m pytest tests/agents/test_banned_patterns.py -x` GREEN
  </acceptance_criteria>
  <done>The sample brownfield workflow runs end-to-end offline with the diff containing the edit and exec=off everywhere; prototype parity held; REPO-05 accept proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → repo working tree | the agent edits repo files — edits must stay repo-relative; the diff is the only surfaced output |
| workflow step → exec/push | the path must grant zero exec and never push/commit (diff-only, N4) |
| resolver → git | only the Workspace owns git; the resolver reads via the handle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Elevation of Privilege | exec on the brownfield path | mitigate | exec NOT granted on any step; effective-perms intersection `exec=off` asserted at every step in `test_sample_brownfield_workflow.py`; `exec_command` denied by default policy (09-01) |
| T-09-04-02 | Tampering | path traversal in edit/diff | mitigate | edits go through `Workspace.path_for` (repo-relative, traversal-rejected, 09-01/09-02); the diff reads via the handle (V12) |
| T-09-04-03 | Tampering | unintended commit/push | mitigate | `repo_diff` is diff-only — `grep -nE "git (commit|push)"` → 0; the test asserts no new commit beyond the branch point (N4) |
| T-09-04-04 | Tampering | prototype parity perturbation | mitigate | the repo path is additive; `test_characterization_prototype.py` stays green alongside the sample run |
| T-09-04-SC | Tampering | (no new package this plan) | accept | 09-04 adds no dependency |
</threat_model>

<verification>
- `test_repo_diff.py` + `test_sample_brownfield_workflow.py` GREEN
- `test_characterization_prototype.py` GREEN alongside the sample run (additive)
- `cd backend && grep -nE "git (commit|push)" agents/capabilities/deliverables/repo_diff.py` → 0
- `cd backend && /opt/homebrew/bin/lint-imports` → 4 kept / 0 broken; banned-pattern green
</verification>

<success_criteria>
- REPO-04: `repo_diff` returns a tree + per-file unified diff (containing the edit) + summary, reading from `Workspace.git_diff`; no commit/push.
- REPO-05: the sample brownfield workflow runs end-to-end offline (clone→branch→inventory→read/edit ≥1→diff); the diff contains the edit; exec=off everywhere; prototype unaffected.
- The new workflow runs with zero engine edit (SC-001 discipline — all power in registered capabilities).
</success_criteria>

<output>
Create `.planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-04-SUMMARY.md` when done.
</output>
