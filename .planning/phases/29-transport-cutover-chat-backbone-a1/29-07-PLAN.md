---
phase: 29-transport-cutover-chat-backbone-a1
plan: 07
type: execute
wave: 2
depends_on: [29-06]
files_modified:
  - frontend/src/hooks/useRunStream.ts
  - frontend/src/providers/RunConnectionProvider.tsx
  - frontend/src/hooks/useWorkflow.ts
  - frontend/src/lib/env.ts
  - frontend/e2e/tests/ts-sse-resilience.spec.ts
autonomous: true
requirements: [CHAT-07]
must_haves:
  truths:
    - "Behind a transport flag, the FE consumes the SSE stream (EventSource-style) with native Last-Event-ID resume, dedup by event_id, and a max-seen seq cursor — sending commands over REST"
    - "Connection ownership lives in an app-level provider that survives route changes (D-14a)"
    - "Reattachment is server-derived: on boot/reconnect the app queries the user's live runs and attaches each (never trusts sessionStorage alone) (D-14b)"
    - "The connection state machine (connected/reconnecting/replaying/live) is surfaced; reconnect fires on visibilitychange/online (D-14d/e)"
    - "useWebSocket.ts is NOT modified and the WS path still works when the flag is off (LOCK-B)"
  artifacts:
    - path: "frontend/src/hooks/useRunStream.ts"
      provides: "The SSE transport hook (subscribe, replay cursor, dedup, reconnect) — the WS-path twin behind the flag"
    - path: "frontend/src/providers/RunConnectionProvider.tsx"
      provides: "App-level connection provider surviving route changes + server-derived reattach"
  key_links:
    - from: "frontend/src/hooks/useWorkflow.ts"
      to: "frontend/src/hooks/useRunStream.ts"
      via: "flag-selected transport: SSE hook OR existing useWebSocket, same event-dispatch reducer"
      pattern: "useRunStream|SSE_TRANSPORT"
    - from: "frontend/src/providers/RunConnectionProvider.tsx"
      to: "backend GET /api/runs (live runs) + /events/stream"
      via: "server-derived reattach: query live runs, attach each stream"
      pattern: "events/stream"
---

<objective>
Ship the FE transport adapter (D-14 a–e) behind a transport flag, ADDITIVELY (LOCK-B): a new SSE hook (`useRunStream`) that mirrors `useWebSocket`'s reconnect/dedup/replay contract over the SSE endpoint, an app-level `RunConnectionProvider` that owns the connection across route changes with server-derived reattach, and a flag-selected transport path in `useWorkflow`. `useWebSocket.ts` is untouched and remains the transport when the flag is off.

Purpose: The down-channel must be consumed with browser-native reconnect (Last-Event-ID), surviving wizards/handoff/fullscreen navigation and page reloads — the FE half of the cutover, provably reversible by a flag.
Output: `useRunStream.ts`, `RunConnectionProvider.tsx`, flag-selected path in `useWorkflow.ts`, resilience specs.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md
@.planning/v2.0-evidence/04-frontend-anatomy.md

# The transport twin + reducer this mirrors (READ; do NOT edit useWebSocket.ts — LOCK-B)
@frontend/src/hooks/useWebSocket.ts
@frontend/src/hooks/useWorkflow.ts
# The e2e driver (built in 29-06) that verifies this
@frontend/e2e/fixtures/mockSse.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useRunStream.ts — the SSE transport hook</name>
  <files>frontend/src/hooks/useRunStream.ts, frontend/src/lib/env.ts</files>
  <action>Create `useRunStream.ts` mirroring `useWebSocket.ts`'s public contract (one onMessage callback, exponential-backoff reconnect cap 30s, drop pipeline_heartbeat/pong) but over the SSE endpoint using the browser `EventSource`/fetch-stream with native `Last-Event-ID` resume — so the dedup-by-event_id + max-seen-seq cursor that `dashboard/page.tsx` already applies keeps working unchanged. Add the transport flag to `env.ts` (e.g. `NEXT_PUBLIC_SSE_TRANSPORT`). Do NOT modify `useWebSocket.ts` (LOCK-B) — this hook is its flag-selected twin. Handle the `4001`/JWT-expiry path with silent refresh before expiry (D-14f) so there is no mid-run logout.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Hook compiles; consumes SSE with Last-Event-ID resume; reconnect/dedup contract matches the WS path; useWebSocket.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: RunConnectionProvider — app-level ownership + server-derived reattach</name>
  <files>frontend/src/providers/RunConnectionProvider.tsx</files>
  <action>Create the app-level provider (D-14a) that lifts connection ownership out of `dashboard/page.tsx` so it survives route changes. On boot/reconnect it is server-derived (D-14b): query the user's live runs (GET /api/runs filtered to non-terminal) and attach a stream per run — never trust sessionStorage alone. Surface the connection state machine connected/reconnecting/replaying/live (D-14d) and trigger reconnect on `visibilitychange`/`online` (D-14e). Persist the per-run replay cursor (D-14c; worst case = full replay, correctness by event_id dedupe). Keep it flag-gated so the legacy `dashboard/page.tsx` WS ownership path remains when the flag is off.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Provider owns the connection across routes, reattaches server-derived, exposes the state machine, reconnects on visibility/online; flag-gated.</done>
</task>

<task type="auto">
  <name>Task 3: Flag-selected transport in useWorkflow + resilience specs</name>
  <files>frontend/src/hooks/useWorkflow.ts, frontend/e2e/tests/ts-sse-resilience.spec.ts</files>
  <action>In `useWorkflow.ts` select the transport by flag: when the SSE flag is on, drive events through `useRunStream`/the provider; when off, the existing `useWebSocket` path is unchanged (additive, LOCK-B). The event-dispatch reducer (`handlePipelineMessage`) is shared and untouched in behavior. Add `ts-sse-resilience.spec.ts` (using the 29-06 `mockSse`) covering: page reload → native Last-Event-ID auto-reconnect resumes the transcript; route-change survives (provider ownership); auto-reconnect after drop; multi-tab. Keep role/text selectors (reskin-resilient).</action>
  <verify>
    <automated>cd frontend && npm run e2e -- --project=mocked -g "sse-resilience"</automated>
  </verify>
  <done>Flag on → SSE transport; flag off → WS unchanged; reload/route-change/auto-reconnect/multi-tab specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → SSE stream + REST commands | JWT-authenticated; reattach must be server-derived, not client-storage-trusted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07-1 | Spoofing | client trusts stale sessionStorage run ids | mitigate | Server-derived reattach (query live runs) — D-14b |
| T-29-07-2 | Denial of Service | mid-run 4001 logout on token expiry | mitigate | Silent JWT refresh before expiry — D-14f |
| T-29-07-SC | Tampering | package installs | accept | No installs; browser-native EventSource/fetch |
</threat_model>

<verification>
- `cd frontend && npm run e2e -- --project=mocked` → resilience specs + 123 existing specs green
- `cd frontend && npx tsc --noEmit` → clean
- LOCK-B grep: no diff to `frontend/src/hooks/useWebSocket.ts`; flag-off path exercises WS unchanged
</verification>

<success_criteria>
Behind a reversible flag, the FE consumes the SSE transport with native Last-Event-ID reconnect, an app-level provider surviving route changes, and server-derived reattach — with the WS path fully intact when the flag is off (LOCK-B).
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-07-SUMMARY.md` when done
</output>
