---
phase: 29-transport-cutover-chat-backbone-a1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/agents/characterization/_sse_projection.py
  - backend/tests/agents/test_wire_parity.py
  - backend/tests/agents/characterization/golden/prototype.wsframes.json
  - backend/tests/agents/characterization/golden/od_prototype.wsframes.json
  - backend/tests/agents/characterization/golden/od_ppt.wsframes.json
  - backend/tests/agents/characterization/golden/prototype_revision.wsframes.json
  - backend/tests/agents/characterization/golden/app_builder.wsframes.json
autonomous: true
requirements: [CHAT-07]
must_haves:
  truths:
    - "The SSE projection of a run's run_events replays the recorded /ws/chat frame sequence IDENTICALLY (volatile-stripped) for all 5 golden pipelines"
    - "The parity comparator raises on an injected frame drift (non-vacuous binding gate)"
    - "No production file is touched — the harness is offline-runnable via the scripted model"
  artifacts:
    - path: "backend/tests/agents/characterization/_sse_projection.py"
      provides: "Pure run_event->SSE-frame projection + WS-drainer-shaped wrapping + volatile-strip comparator (the contract 29-02 imports)"
      exports: ["project_events", "run_event_to_sse_frame", "ws_frame_from_engine_event", "assert_wire_parity"]
    - path: "backend/tests/agents/test_wire_parity.py"
      provides: "The binding wire-parity gate over the 5 goldens"
    - path: "backend/tests/agents/characterization/golden/prototype.wsframes.json"
      provides: "Recorded volatile-stripped WS frame sequence for prototype"
      contains: "type"
  key_links:
    - from: "backend/tests/agents/test_wire_parity.py"
      to: "backend/tests/agents/characterization/_normalize.py"
      via: "reuse _VOLATILE_STRIP_KEYS + _canonical_order (order-canonical multiset)"
      pattern: "_VOLATILE_STRIP_KEYS|_canonical_order"
    - from: "backend/tests/agents/test_wire_parity.py"
      to: "backend/tests/agents/_scripted_model.py"
      via: "drive engine.execute per golden pipeline, capture events + run_events"
      pattern: "_scripted|scripted_model"
---

<objective>
Build the wire-parity characterization harness FIRST — the binding gate for the whole transport cutover (POR §4 Wave 1, CHAT-07). Record the `/ws/chat` outbound frame sequences for the 5 golden pipelines (volatile-stripped) and prove a pure `run_events`->SSE projection replays them identically. The SSE stream endpoint (29-02) consumes this same projection, so parity holds by construction.

Purpose: Under LOCK-B the SSE transport is built ADDITIVELY; the only binding proof that it is a faithful replacement is that its frame stream is byte/event-identical (volatile-stripped) to the WS drainer's. This test is that proof and it is offline-runnable via the scripted harness (no live server).
Output: `_sse_projection.py` (the projection contract), `test_wire_parity.py` (the gate), and 5 `*.wsframes.json` goldens.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md

# Existing harness + normalizer this plan extends (read the header + helpers, do not re-read)
@backend/tests/agents/characterization/_normalize.py
@backend/tests/agents/_scripted_model.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the SSE projection + WS-frame-shape contract (_sse_projection.py)</name>
  <files>backend/tests/agents/characterization/_sse_projection.py</files>
  <behavior>
    - project_events(rows) yields one SSE frame per run_events row, in ascending seq, id line == the row seq
    - run_event_to_sse_frame(row) renders the SSE text form: an `id: {seq}` line + a `data: {json}` line + blank-line terminator; the JSON body is the row's type+payload (the exact shape 29-02 will emit)
    - ws_frame_from_engine_event(event, section) reproduces the current WS drainer wrapping `{type, chunk, section, data}` (websocket.py drainer, evidence 03 §1) so a recorded engine event can be compared to a projected SSE frame after normalization
    - assert_wire_parity(ws_frames, run_events) normalizes both sides through _VOLATILE_STRIP_KEYS + _canonical_order (the accepted order-canonical multiset framing from 01-02) and raises AssertionError on ANY residual difference
  </behavior>
  <action>Create the pure projection module. Import `_VOLATILE_STRIP_KEYS`, `_normalize`, `_canonical_order` from `characterization/_normalize.py` — never copy the strip list. The projection is the single source of truth the SSE endpoint (29-02) imports; keep it dependency-free of `app.*`/kernel so import-linter stays 4/0. The SSE frame's `id:` line carries the row `seq` (browser `Last-Event-ID` resume). Do NOT strip `stream_attached.live` or `chat_reply.card_kind` (deterministic per _normalize header). No fenced code in production — this is a test-support module so implementation is fine here.</action>
  <verify>
    <automated>python3.11 -c "import backend.tests.agents.characterization._sse_projection as p; assert hasattr(p,'project_events') and hasattr(p,'assert_wire_parity')"</automated>
  </verify>
  <done>Module imports offline; exports project_events / run_event_to_sse_frame / ws_frame_from_engine_event / assert_wire_parity; reuses the shared normalizer.</done>
</task>

<task type="auto">
  <name>Task 2: Record the 5 volatile-stripped WS frame goldens via the scripted harness</name>
  <files>backend/tests/agents/characterization/golden/prototype.wsframes.json, backend/tests/agents/characterization/golden/od_prototype.wsframes.json, backend/tests/agents/characterization/golden/od_ppt.wsframes.json, backend/tests/agents/characterization/golden/prototype_revision.wsframes.json, backend/tests/agents/characterization/golden/app_builder.wsframes.json</files>
  <action>For each of the 5 INV-3 pipelines, drive `engine.execute(...)` with the scripted model (mirror how `test_characterization_*.py` invoke it), capturing BOTH the engine event stream (wrapped via `ws_frame_from_engine_event`) and the persisted `run_events` rows from the in-memory ScopedStore. Write each golden as the volatile-stripped WS frame sequence. Chat/steering events MUST NOT appear (the scripted harness sends none — INV-3 goldens stay chat-dormant). Provide a `--snapshot-update`-style regeneration path consistent with the existing `SNAPSHOT_UPDATE` convention in `characterization/__init__.py`.</action>
  <verify>
    <automated>python3.11 -c "import json,glob; fs=glob.glob('backend/tests/agents/characterization/golden/*.wsframes.json'); assert len(fs)==5; [json.load(open(f)) for f in fs]"</automated>
  </verify>
  <done>5 non-empty *.wsframes.json goldens exist and parse; none contains chat_message/chat_reply/stream_attached.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_wire_parity.py — the binding gate (parity + non-vacuity)</name>
  <files>backend/tests/agents/test_wire_parity.py</files>
  <behavior>
    - Per golden pipeline: assert_wire_parity(recorded_ws_frames, projected_from_run_events) passes (SSE projection == WS frame sequence, volatile-stripped)
    - Negative/non-vacuity test: inject one drifted frame (extra key / changed type) and assert assert_wire_parity RAISES — the gate cannot silently pass on empty input
  </behavior>
  <action>Parametrize over the 5 goldens. Re-derive the run_events for each pipeline the same way Task 2 recorded them, project via `project_events`, and compare to the recorded `*.wsframes.json` through `assert_wire_parity`. Add an explicit non-vacuity case so the gate is proven to detect drift (mirror the `assert_seq_contiguous` non-vacuity precedent noted in the IMPLEMENTATION-REGISTER). This test is the CHAT-07 wire-parity exit gate cited by 29-02/29-03/29-04.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/agents/test_wire_parity.py -x -q</automated>
  </verify>
  <done>5 parity cases green + 1 non-vacuity case proving drift is caught; runs offline in seconds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Test-only harness; no runtime endpoint, no untrusted input crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-1 | Tampering | wire-parity gate could pass vacuously | mitigate | Task 3 non-vacuity case proves the comparator raises on injected drift |
| T-29-01-SC | Tampering | package installs | accept | No installs — `sse-starlette==3.0.2` already pinned (requirements.txt:103); test-support module only |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/agents/test_wire_parity.py -x -q` → 6 passed
- `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken (projection module imports no kernel/app)
- The 5 existing `test_characterization_*.py` remain byte/event-identical (INV-3 untouched)
</verification>

<success_criteria>
Wire-parity is a green, non-vacuous, offline binding gate: the pure `run_events`->SSE projection replays each of the 5 golden WS frame sequences identically (volatile-stripped), and `_sse_projection.py` is the reusable contract 29-02 mounts into the live endpoint.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-01-SUMMARY.md` when done
</output>
