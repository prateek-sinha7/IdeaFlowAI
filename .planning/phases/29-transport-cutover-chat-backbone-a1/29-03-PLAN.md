---
phase: 29-transport-cutover-chat-backbone-a1
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - backend/app/api/run_commands.py
  - backend/app/main.py
  - backend/tests/unit/test_rest_gate_commands.py
  - backend/tests/unit/test_rest_answers_cancel.py
autonomous: true
requirements: [CHAT-07]
must_haves:
  truths:
    - "POST /api/runs/{id}/gate resolves a paused gate with all four actions: approve / reject / redo+instructions / update_specs"
    - "A gate action on a terminal run is fenced with pipeline_not_running (recoverable:false), matching KAN-100"
    - "POST /api/runs/{id}/answers submits clarify responses (incl. skip_clarification) and resolves the clarify pause"
    - "POST /api/runs/{id}/cancel cooperatively cancels via the per-run cancel event"
    - "Every command is owner-scoped: a cross-owner target returns 404 (never 403); /ws/chat handlers are untouched (LOCK-B)"
  artifacts:
    - path: "backend/app/api/run_commands.py"
      provides: "Transport-agnostic REST command endpoints for gate/answers/cancel, thin over the existing store seams"
      exports: ["router"]
    - path: "backend/tests/unit/test_rest_gate_commands.py"
      provides: "Gate suite ported 1:1 from test_approve_review_ownership.py"
  key_links:
    - from: "backend/app/api/run_commands.py"
      to: "backend/agents/artifact_store/store.py"
      via: "store.set_review_response / set_questionnaire_responses (the same seams the WS handlers call)"
      pattern: "set_review_response|set_questionnaire_responses"
    - from: "backend/app/api/run_commands.py"
      to: "backend/app/api/websocket.py"
      via: "read-only import of _CANCEL_EVENTS + _review_gate_owned_by (no edit to websocket.py)"
      pattern: "_CANCEL_EVENTS|_review_gate_owned_by"
---

<objective>
Ship the up-channel REST command endpoints for the paused-run interactions (D-13 command mapping): `POST /api/runs/{id}/gate` (approve/reject/redo/update_specs), `POST /api/runs/{id}/answers` (clarify), `POST /api/runs/{id}/cancel`. Each is a thin, transport-agnostic wrapper over the SAME store/cancel seams the `/ws/chat` handlers use, with the inbound-handler test suites ported 1:1. `/ws/chat` stays live and untouched (LOCK-B).

Purpose: Commands must work over plain HTTP (and while the SSE stream is down), replacing the WS subprotocol command path additively.
Output: `run_commands.py` (gate/answers/cancel), two ported test suites.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md

# The WS handlers being mirrored (READ for behavior — do NOT edit, LOCK-B) + the ported test sources
@backend/tests/unit/test_approve_review_ownership.py
@backend/tests/unit/test_pipeline_cancel.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/runs/{id}/gate — four actions + terminal fence + ownership</name>
  <files>backend/app/api/run_commands.py, backend/tests/unit/test_rest_gate_commands.py</files>
  <behavior>
    - action=approve → sets review response (approved, optional edited_content) → engine resumes; action=reject → rejected (pipeline cancelled); action=redo → redo+instructions; action=update_specs → carries analysis_report in the instructions field, routing to the shipped KAN-101 spec-revision loop (do NOT rebuild it)
    - On a terminal run (pipeline_complete/failed/cancelled) any gate action returns pipeline_not_running (recoverable:false) — KAN-100
    - Gates are event-driven: a gate for an agent excluded by gate_agent_ids simply has no pending gate to resolve → clean not-found response (KAN-94), never a fabricated pause
    - Cross-owner gate_key → 404 via _review_gate_owned_by (P13), same "Unknown gate_key" non-revealing denial
  </behavior>
  <action>Create `backend/app/api/run_commands.py` with `APIRouter(prefix="/api/runs")`. Implement `POST /{id}/gate` calling `store.set_review_response(...)` — the exact seam WS `approve_review` uses (evidence 03 §2b, store.py:118). Preserve the FOUR-action contract (POR §6: approve/redo/update_specs + reject with `instructions`/`analysis_report` payloads) and reuse `_review_gate_owned_by` (imported read-only from websocket.py). Enforce the `pipeline_not_running` fence (KAN-100) and event-driven gate semantics (KAN-94). Do NOT re-implement the KAN-100 cancel-while-paused race or the KAN-101 sub-pipeline — route to them. Port every assertion from `test_approve_review_ownership.py` into `test_rest_gate_commands.py` against the REST endpoint (IDOR→404, redo payload, update_specs payload, terminal fence).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_rest_gate_commands.py -x -q</automated>
  </verify>
  <done>All four gate actions resolve the pause; terminal fence returns pipeline_not_running; cross-owner → 404; ported assertions pass 1:1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/runs/{id}/answers (clarify) + POST /api/runs/{id}/cancel</name>
  <files>backend/app/api/run_commands.py, backend/tests/unit/test_rest_answers_cancel.py</files>
  <behavior>
    - answers: body {responses:[{question_id, answer}], skip_clarification?} → store.set_questionnaire_responses(run_id, responses, skip_clarification) → clarify loop resumes; freeform maps to question_id:"freeform"
    - cancel: sets the per-run asyncio cancel event → cooperative pipeline_cancelled (suspend/persist semantics unchanged)
    - both: two-layer owner check → 404 on cross-owner
  </behavior>
  <action>Add `POST /{id}/answers` calling `store.set_questionnaire_responses` (evidence 03 §2a, store.py:62) and `POST /{id}/cancel` setting `_CANCEL_EVENTS[run_id]` (imported read-only from websocket.py, evidence 03 §1). Mirror the WS `submit_questionnaire` / `cancel_pipeline` behavior exactly. Port `test_pipeline_cancel.py` (+ the questionnaire-submit assertions) into `test_rest_answers_cancel.py` against the REST endpoints.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_rest_answers_cancel.py -x -q</automated>
  </verify>
  <done>Clarify answers resolve the pause (incl. skip); cancel fires the cooperative event; cross-owner → 404.</done>
</task>

<task type="auto">
  <name>Task 3: Register router (additive) + wire-parity regression guard</name>
  <files>backend/app/main.py</files>
  <action>Register `run_commands` router in `main.py` beside the other includes (additive; WS include stays). Confirm the up-channel adds NO new outbound event shapes (commands resolve via the existing store seams, whose resulting events are already covered by the 29-01 wire-parity goldens) — re-run the wire-parity gate as a regression check.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/agents/test_wire_parity.py backend/tests/unit/test_rest_gate_commands.py backend/tests/unit/test_rest_answers_cancel.py -x -q</automated>
  </verify>
  <done>Router mounted; wire-parity still green; command suites green offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → POST gate/answers/cancel | Untrusted run_id + gate_key + payload; a caller may target another owner's paused run |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-1 | Elevation of Privilege | resolving another owner's gate | mitigate | _review_gate_owned_by (WorkflowRun.user_id == user_id) before any write; unknown → 404 non-revealing |
| T-29-03-2 | Tampering | gate action on terminal run | mitigate | KAN-100 pipeline_not_running fence (recoverable:false) |
| T-29-03-3 | Spoofing | forged clarify/cancel target | mitigate | Two-layer owner check → 404 (IDOR→404 never 403) |
| T-29-03-SC | Tampering | package installs | accept | No installs |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/unit/test_rest_gate_commands.py backend/tests/unit/test_rest_answers_cancel.py backend/tests/agents/test_wire_parity.py -x -q` → green
- `/opt/homebrew/bin/lint-imports` → 4/0
- LOCK-B grep: no diff to websocket.py / useWebSocket.ts / websocket_handoff.py; no ratchet/ledger row
</verification>

<success_criteria>
The three paused-run REST commands mirror their `/ws/chat` counterparts byte-for-behavior (four gate actions, terminal fence, clarify resume, cooperative cancel), owner-scoped IDOR→404, with the handler test suites ported 1:1 — additively, WS intact.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-03-SUMMARY.md` when done
</output>
