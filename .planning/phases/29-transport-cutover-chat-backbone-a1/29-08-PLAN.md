---
phase: 29-transport-cutover-chat-backbone-a1
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/execution_engine/context.py
  - backend/agents/execution_engine/engine.py
  - backend/tests/agents/test_steering_seam.py
  - .planning/phases/29-transport-cutover-chat-backbone-a1/ND-11-SEAM-DECISION.md
autonomous: true
requirements: [CHAT-03]
must_haves:
  truths:
    - "ND-11 is resolved in writing FIRST: unify-vs-coexist for the three consume-once seams (redo_directive, spec_revision_context, steering_notes) + the thread-id policy (Redo :redo{N} vs KAN-101 BASE-thread reuse)"
    - "ectx.steering_notes is a consume-once queue; pending notes render as a === USER GUIDANCE === block in _compose_context_message at the next agent dispatch (the redo_directive idiom), then are cleared"
    - "Sticky (uploaded context) vs one-shot (directive) semantics hold"
    - "The seam is generic/name-free (no workflow-name branch — SC-001/INV-1) and the 5 INV-3 goldens stay byte/event-identical (steering dormant on golden runs)"
  artifacts:
    - path: ".planning/phases/29-transport-cutover-chat-backbone-a1/ND-11-SEAM-DECISION.md"
      provides: "The ND-11 seam-unification + thread-id decision record (the first design task)"
      contains: "ND-11"
    - path: "backend/agents/execution_engine/context.py"
      provides: "ectx.steering_notes consume-once carrier"
      contains: "steering_notes"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "=== USER GUIDANCE === composition in _compose_context_message"
      contains: "USER GUIDANCE"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "backend/agents/execution_engine/context.py"
      via: "consume-once read+clear of ectx.steering_notes during context composition (mirrors redo_directive set/clear)"
      pattern: "steering_notes"
---

<objective>
Resolve ND-11 (the FIRST design task of the chat backbone, POR §4 Wave 2) and implement the steering seam (D-06 / CHAT-03): a consume-once `ectx.steering_notes` queue rendered as a `=== USER GUIDANCE ===` block in `_compose_context_message`, mirroring the shipped `redo_directive` idiom. The seam is generic and name-free; the 5 INV-3 goldens stay byte/event-identical because steering is dormant on golden runs.

Purpose: A user message to a running run must land in the next agent's composed context (mid-generation injection is impossible — an agent invocation runs to completion). This is the injection seam the mechanical router (29-09) dispatches steering turns into. ND-11 must be decided before adding a third parallel seam, and the Redo-fork vs KAN-101-BASE-thread replay divergence reconciled.
Output: `ND-11-SEAM-DECISION.md`, the carrier + composition, offline fault-injection proof.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/CHAT-AND-UI-CONVERGENCE-PLAN.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/RESOLVED-DECISIONS.md

# The two shipped consume-once seams to reconcile (READ; redo at engine.py ~5916, KAN-101 sub-pipeline ~4295)
@backend/agents/execution_engine/context.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ND-11 seam-unification + thread-id decision record (FIRST)</name>
  <files>.planning/phases/29-transport-cutover-chat-backbone-a1/ND-11-SEAM-DECISION.md</files>
  <action>Write the ND-11 decision record BEFORE touching the engine. Decide unify-vs-coexist across the three consume-once injection seams — `redo_directive` (P23, engine.py ~5916), KAN-101 `spec_revision_context`/`spec_revision_pending_output` (engine.py ~4295), and the new `steering_notes` — per POR D-06/ND-11 and RESOLVED-DECISIONS.md (ND-11 = "Phase-29 first design task"). Recommended direction (justify against the evidence): `steering_notes` COEXISTS as a generic third seam sharing the same `=== ... ===` composition idiom, while the two shipped seams are left byte-stable (INV-3), and the thread-id divergence is documented — Redo forks `:redo{N}`; the KAN-101 sub-pipeline reuses BASE threads (the unaudited replay risk the sibling comment warns about). State the thread-id policy for steering turns explicitly (steering notes inject into the NEXT dispatch on the current thread — no fork — because they do not roll back a rejected turn). Also record the ND-9 tie-in (steering-state across resume is server-derived from run_events, per 29-02).</action>
  <verify>
    <automated>grep -qi "ND-11" .planning/phases/29-transport-cutover-chat-backbone-a1/ND-11-SEAM-DECISION.md && grep -qi "thread" .planning/phases/29-transport-cutover-chat-backbone-a1/ND-11-SEAM-DECISION.md</automated>
  </verify>
  <done>The record decides unify-vs-coexist, states the steering thread-id policy, and reconciles the Redo/KAN-101 divergence with evidence anchors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ectx.steering_notes carrier + === USER GUIDANCE === composition</name>
  <files>backend/agents/execution_engine/context.py, backend/agents/execution_engine/engine.py</files>
  <behavior>
    - ectx.steering_notes holds pending consume-once directives; _compose_context_message renders them as a === USER GUIDANCE === block at the next dispatch, then clears them (consume-once, mirroring redo_directive set-at/clear-at)
    - Sticky context (uploads) persists across dispatches; one-shot directives are consumed once
    - When steering_notes is empty (every golden run), composition is byte-identical to today (no === USER GUIDANCE === marker emitted) → INV-3 holds
    - The seam keys on generic state only — no `if pipeline_type ==` / `spec.id ==` (SC-001/INV-1)
  </behavior>
  <action>Implement per the ND-11 record. Add the `steering_notes` carrier to `context.py` (mirror the `redo_directive` field lifecycle, evidence 03 §2b: set at ~2687, cleared at ~2692). Render the block in `_compose_context_message` (engine.py ~5775-5976) exactly like the `=== ADDITIONAL INSTRUCTIONS (REVISE) ===` idiom but with the `=== USER GUIDANCE ===` marker (POR §6 marker family). This marker joins the injected-context marker family the chat launch surface strips. Keep it name-free (kernel INV-1). This is INV-3-sensitive: verify the block is only emitted when notes are pending (chat/steering dormant on golden runs).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/agents/test_characterization_prototype.py backend/tests/agents/test_characterization_od_ppt.py -x -q</automated>
  </verify>
  <done>steering_notes composes the guidance block consume-once; goldens byte/event-identical; no name branch added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline fault-injection proof</name>
  <files>backend/tests/agents/test_steering_seam.py</files>
  <behavior>
    - With a pending steering note, the next agent's composed context contains the === USER GUIDANCE === block with the note text; the note is cleared after one dispatch (consume-once)
    - A sticky/uploaded note persists across two dispatches; a one-shot directive does not
    - With no notes, the composed context has no === USER GUIDANCE === marker (golden-neutral)
  </behavior>
  <action>Write `test_steering_seam.py` as an offline fault-injection proof (POR §4 Wave 2 exit: "a steering note demonstrably lands in the next agent's composed context (offline fault-injection)"). Drive `_compose_context_message` directly with a scripted `ExecutionContext`; assert presence/absence + consume-once + sticky-vs-one-shot. No live model.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/agents/test_steering_seam.py -x -q</automated>
  </verify>
  <done>Fault-injection proof green: the note lands in the next dispatch, consume-once + sticky/one-shot semantics verified, golden-neutral when empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user turn → agent context injection | A steering note is untrusted text composed into the next agent prompt |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08-1 | Tampering | prompt-injection via steering text | accept | Rendered inside a delimited === USER GUIDANCE === block like existing redo_directive; same trust posture as shipped seams; owner-scoped upstream |
| T-29-08-2 | Elevation of Privilege | kernel name-branch creep | mitigate | Seam is generic/name-free; banned-pattern gate (_INV1_PATTERN) + import-linter enforce SC-001/INV-1 |
| T-29-08-SC | Tampering | package installs | accept | No installs |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/agents/test_steering_seam.py backend/tests/agents/test_characterization_*.py -x -q` → green (5 goldens byte/event-identical)
- `python3.11 -m pytest backend/tests/agents/test_banned_patterns.py -x -q` → SC-001/INV-1 clean (no name branch)
- `/opt/homebrew/bin/lint-imports` → 4/0
</verification>

<success_criteria>
ND-11 is decided in writing first; the generic consume-once `steering_notes` seam injects a `=== USER GUIDANCE ===` block at the next dispatch with sticky/one-shot semantics, offline-proven, kernel name-free, and the 5 INV-3 goldens unchanged.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-08-SUMMARY.md` when done
</output>
