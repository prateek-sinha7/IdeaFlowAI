---
phase: 29-transport-cutover-chat-backbone-a1
plan: 10
type: execute
wave: 6
depends_on: [29-09]
files_modified:
  - backend/app/agents/chat_narrator.py
  - backend/tests/unit/test_chat_narrator.py
autonomous: true
requirements: [CHAT-04]
must_haves:
  truths:
    - "Narrator chat_reply cards emit for clarify / gate / pipeline / deliverable milestones, plus a spec_revision card for KAN-101 loop-backs"
    - "Each card is a projection of an EXISTING run event (no new model call) persisted as a chat_reply run_events row, carrying the deep-link {target, nonce} seam"
    - "chat_reply never fires on the 5 golden paths (INV-3) and its card_kind discriminators stay stable (golden-neutral)"
  artifacts:
    - path: "backend/app/agents/chat_narrator.py"
      provides: "The milestone→chat_reply projection (clarify/gate/pipeline/deliverable/spec_revision cards + deep-link nonce)"
      exports: ["project_milestone_card"]
    - path: "backend/tests/unit/test_chat_narrator.py"
      provides: "Card-emission coverage across the milestone kinds + golden-neutrality"
  key_links:
    - from: "backend/app/agents/chat_narrator.py"
      to: "backend/agents/authz.py"
      via: "persist chat_reply as a run_events row via ScopedStore.append_event"
      pattern: "append_event|chat_reply"
---

<objective>
Ship the narrator (CHAT-04): app-layer projections that turn run milestones into `chat_reply` result cards — clarify / gate / pipeline / deliverable, plus a `spec_revision` card for KAN-101 loop-backs ("Revising spec — cycle N"). Each card is a pure projection of an existing run event (no model call), persisted as a `chat_reply` `run_events` row with the deep-link `{target, nonce}` seam so the card can link to the milestone it reports. Golden-neutral (chat never fires on the 5 golden paths).

Purpose: The chat lane needs assistant-side cards for run milestones, family-anchored and replay-durable — the down-channel narration half of the backbone.
Output: `chat_narrator.py`, `test_chat_narrator.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md

# The persistence seam + the messages endpoint the narrator complements (built in 29-09)
@backend/app/api/chat_router.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Milestone → chat_reply projection (5 card kinds + deep-link nonce)</name>
  <files>backend/app/agents/chat_narrator.py, backend/tests/unit/test_chat_narrator.py</files>
  <behavior>
    - project_milestone_card(event) returns a chat_reply payload for the milestone kinds: clarify (questionnaire_ready/complete), gate (review_gate_ready), pipeline (pipeline_start/complete/failed/cancelled), deliverable (deliverable produced), spec_revision (KAN-101 loop-back — "Revising spec — cycle N", revision_index = spec_revision_attempt)
    - card_kind ∈ {clarify, gate, pipeline, deliverable, spec_revision} (MOCKWS-CHAT-DRIVER-CONTRACT §2.2, + the Phase-29 spec_revision addition)
    - deep_link = {target, nonce} — a consume-once nonce linking the card to its milestone/artifact
    - No model call — pure projection of an already-emitted event
  </behavior>
  <action>Create `backend/app/agents/chat_narrator.py` (app-layer, imports only ports/models — import-linter 4/0). `project_milestone_card` maps each existing run event (evidence 03 §1 taxonomy) to a `chat_reply` card per the LIVE-STATE-CONTRACT.md milestones and the driver contract's card catalogue. Add the `spec_revision` card for the KAN-101 loop-back (LIVE-STATE-CONTRACT §2b terminology: intra-run spec-revision loop ≠ family revision run — label distinctly). Emit the deep-link `{target, nonce}` seam (single-use nonce). Cover all five kinds + the deep-link shape in `test_chat_narrator.py`.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_chat_narrator.py -x -q</automated>
  </verify>
  <done>All five card kinds project correctly from their source events with a deep-link nonce; no model call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist chat_reply as run_events + golden-neutrality</name>
  <files>backend/app/agents/chat_narrator.py</files>
  <behavior>
    - Each card persists as a type="chat_reply" run_events row via ScopedStore.append_event (family-anchored on the active run; owner+workspace default-deny) — inherits seq/event_id replay + reopen
    - chat_reply never fires on the 5 golden pipelines (INV-3); card_kind discriminators are stable (not stripped)
  </behavior>
  <action>Persist projected cards as `chat_reply` `run_events` rows through the single stamping boundary (evidence 03 §6). The type is already in `_DOCUMENTED_EVENT_TYPES` (Phase 28). Confirm the goldens stay byte/event-identical (the narrator is dormant on scripted golden runs — it only projects when the chat lane is active). Re-run the characterization + wire-parity gates as the regression proof.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_chat_narrator.py backend/tests/agents/test_wire_parity.py backend/tests/agents/test_characterization_od_ppt.py backend/tests/agents/test_characterization_prototype.py -x -q</automated>
  </verify>
  <done>Cards persist as chat_reply run_events rows, family-anchored + owner-scoped; 5 goldens byte/event-identical; wire-parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server → chat lane (narrator card) | Narrator projects server-side events; deep-link nonce must be single-use |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10-1 | Information Disclosure | card leaks cross-owner milestone | mitigate | chat_reply persisted via ScopedStore (owner+workspace default-deny); projection reads only the run's own events |
| T-29-10-2 | Elevation of Privilege | replayed deep-link nonce reuse | mitigate | Deep-link nonce is single-use (consume-once seam) |
| T-29-10-3 | Tampering | new event key breaks goldens | mitigate | Reuse Phase-28 vocab (chat_reply already documented); characterization + wire-parity regression in Task 2 |
| T-29-10-SC | Tampering | package installs | accept | No installs |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/unit/test_chat_narrator.py backend/tests/agents/test_wire_parity.py backend/tests/agents/test_characterization_*.py -x -q` → green
- `/opt/homebrew/bin/lint-imports` → 4/0 (narrator imports only ports/models)
- LOCK-B grep: no diff to websocket.py / useWebSocket.ts; zero new tables; no ratchet/ledger row
</verification>

<success_criteria>
Narrator `chat_reply` cards project for all five milestone kinds (incl. the KAN-101 spec_revision loop-back), persist as family-anchored owner-scoped `run_events` rows with a single-use deep-link nonce, with the 5 INV-3 goldens byte/event-identical.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-10-SUMMARY.md` when done
</output>
