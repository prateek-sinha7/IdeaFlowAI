---
phase: 29-transport-cutover-chat-backbone-a1
plan: 05
type: execute
wave: 5
depends_on: [29-02, 29-03, 29-04]
files_modified:
  - backend/tests/agents/test_attach_replay_matrix.py
autonomous: true
requirements: [CHAT-07]
must_haves:
  truths:
    - "The SSE attach/replay behaves correctly across all seven scenarios: fresh, mid-stream, live, terminal, cross-owner→404, restart, gate-answer-while-stream-down"
    - "A gate answered via REST while the stream is down resumes correctly on reattach (the SC-2 durability proof)"
    - "All scenarios are offline-runnable (scripted store + TestClient), no live server"
  artifacts:
    - path: "backend/tests/agents/test_attach_replay_matrix.py"
      provides: "The attach/replay matrix — the durability + resilience gate for the transport"
  key_links:
    - from: "backend/tests/agents/test_attach_replay_matrix.py"
      to: "backend/app/api/run_stream.py"
      via: "drive attach/replay against the SSE endpoint with varied Last-Event-ID + run states"
      pattern: "events/stream"
    - from: "backend/tests/agents/test_attach_replay_matrix.py"
      to: "backend/app/api/run_commands.py"
      via: "issue a gate command while no stream is attached, then reattach and assert resume"
      pattern: "/gate"
---

<objective>
Prove the transport's durability contract with the attach/replay matrix (POR §4 Wave 1): a full offline test matrix over the seven attach scenarios — fresh · mid-stream · live · terminal · cross-owner→404 · restart · gate-answer-while-stream-down. This is the binding proof for phase SC-2 ("a gate answered via REST while the stream is down resumes correctly on reattach") and the reconnect-fidelity exit.

Purpose: Browser-native reconnect/reopen must be correct under every ordering of stream drop, command, and restart. This matrix is the gate that catches a regression before a human validates live.
Output: `test_attach_replay_matrix.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md

# The endpoints under test (built in 29-02/29-03/29-04)
@backend/app/api/run_stream.py
@backend/app/api/run_commands.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The seven-scenario attach/replay matrix</name>
  <files>backend/tests/agents/test_attach_replay_matrix.py</files>
  <behavior>
    - fresh: attach to a brand-new run → replays from seq 0, then goes live
    - mid-stream: attach with Last-Event-ID = k → replays only rows seq > k, no dupes
    - live: attach to a running run → stream_attached{live:true} after tail replay
    - terminal: attach to a completed run → full replay, stream closes cleanly (no hang)
    - cross-owner: attach to another owner's run → 404
    - restart: simulate backend restart (fresh process/store), a waiting_for_user run re-emits review_gate_ready on attach (D-14g)
    - gate-answer-while-stream-down: no stream attached → POST /{id}/gate resolves the gate → reattach → the resolution + subsequent events replay in order (SC-2)
  </behavior>
  <action>Build the matrix with the offline TestClient + scripted store (mirror `test_ws_reconnect_replay.py` and `test_resume_ws_bridge.py` idioms — evidence 03 §1/§2). Each scenario asserts the SSE frame sequence (via the 29-01 projection normalization) is contiguous, deduped by event_id, and ordered by seq. The gate-answer-while-stream-down case is the SC-2 durability proof: issue the REST gate command with NO active stream, then attach and assert the gate resolution and following events replay. Keep everything offline (no live Bedrock, no running uvicorn).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/agents/test_attach_replay_matrix.py -x -q</automated>
  </verify>
  <done>All seven scenarios green offline; gate-answer-while-stream-down proves SC-2; restart re-emits review_gate_ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → attach/replay | Untrusted run_id + cursor across reconnect/restart orderings |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-1 | Information Disclosure | cross-owner attach | mitigate | Matrix asserts cross-owner → 404 (guards the 29-02 owner check) |
| T-29-05-2 | Repudiation | lost gate resolution across stream drop | mitigate | gate-answer-while-stream-down scenario proves durable resume (SC-2) |
| T-29-05-SC | Tampering | package installs | accept | No installs; test-only |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/agents/test_attach_replay_matrix.py -x -q` → 7 scenarios green
- `/opt/homebrew/bin/lint-imports` → 4/0
- LOCK-B grep: no diff to websocket.py / useWebSocket.ts; no ratchet/ledger row
</verification>

<success_criteria>
The transport's reconnect/reopen durability is proven offline across all seven attach scenarios, with the gate-answer-while-stream-down case establishing phase SC-2.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-05-SUMMARY.md` when done
</output>
