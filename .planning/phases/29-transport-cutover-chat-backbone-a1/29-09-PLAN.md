---
phase: 29-transport-cutover-chat-backbone-a1
plan: 09
type: execute
wave: 5
depends_on: [29-04, 29-08]
files_modified:
  - backend/app/api/chat_router.py
  - backend/app/api/run_commands.py
  - backend/tests/unit/test_chat_messages_endpoint.py
  - backend/tests/unit/test_mechanical_router.py
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-05]
must_haves:
  truths:
    - "POST /api/runs/{id}/messages persists the user turn as a chat_message run_events row and is idempotent by client message_id (a replayed message_id is a no-op)"
    - "The mechanical router keys on run state and delivers the turn with ZERO model calls: clarify_waiting → clarify answer; gate_paused → gate action (approve/reject/redo/update_specs); running → steering note; terminal → revision"
    - "update_specs routes to the shipped KAN-101 spec-revision loop (never rebuilt); a gate action on a terminal run is fenced with pipeline_not_running (KAN-100); gates are event-driven (KAN-94)"
    - "chat turns persist on the run active at that moment (family-anchored, D-02) — replay/reopen inherited from run_events; owner+workspace default-deny"
  artifacts:
    - path: "backend/app/api/chat_router.py"
      provides: "The mechanical intent router (run-state → command dispatch), no model calls for routable turns"
      exports: ["route_chat_turn"]
    - path: "backend/app/api/run_commands.py"
      provides: "POST /api/runs/{id}/messages endpoint (idempotent) delegating to the router"
  key_links:
    - from: "backend/app/api/chat_router.py"
      to: "backend/app/api/run_commands.py"
      via: "router dispatches gate/answers/revision turns through the SAME command handlers (29-03/29-04)"
      pattern: "gate|answers|revisions"
    - from: "backend/app/api/chat_router.py"
      to: "backend/agents/execution_engine/context.py"
      via: "steering turns append to ectx.steering_notes (the 29-08 seam)"
      pattern: "steering_notes"
    - from: "backend/app/api/run_commands.py"
      to: "backend/agents/authz.py"
      via: "ScopedStore.append_event persists chat_message (idempotent by message_id/event_id)"
      pattern: "append_event"
---

<objective>
Ship the chat backbone's up-channel (CHAT-01/CHAT-02/CHAT-05): `POST /api/runs/{id}/messages` (idempotent by client `message_id`, persists a `chat_message` `run_events` row) and the mechanical intent router (D-04) that keys on run state to deliver the turn with zero model calls — clarify answer / gate action (four actions incl. `update_specs`→KAN-101) / steering note / revision. Terminal-fenced (KAN-100), event-driven gates (KAN-94), family-anchored persistence (D-02).

Purpose: A user message reaches a run over the new transport, persists durably, routes by run state, and shapes the next agent dispatch — the core of the chat backbone.
Output: `chat_router.py`, the `/messages` endpoint, two test suites.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/CHAT-AND-UI-CONVERGENCE-PLAN.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md

# The command handlers + steering seam the router dispatches into (built in 29-03/29-04/29-08)
@backend/app/api/run_commands.py
@backend/agents/execution_engine/context.py
# The ND-11 decision (built in 29-08) governing steering thread-id policy
@.planning/phases/29-transport-cutover-chat-backbone-a1/ND-11-SEAM-DECISION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/runs/{id}/messages — persist + idempotency</name>
  <files>backend/app/api/run_commands.py, backend/tests/unit/test_chat_messages_endpoint.py</files>
  <behavior>
    - POST /{id}/messages {text, attachments[], message_id} persists a type="chat_message" run_events row via ScopedStore.append_event through the single stamping boundary (seq/event_id) — zero new tables (D-01)
    - Idempotent by client message_id: a replayed message_id does NOT create a second row (dedup at the stamping boundary via event_id derived from message_id)
    - Family-anchored: the row persists on the run active at that moment (D-02); owner+workspace default-deny (a cross-owner run → 404)
    - attachments are payload-transient refs (ND-10 "image not retained" placeholder on replay per LOCK-E) — not persisted to sandbox/DB
  </behavior>
  <action>Add `POST /{id}/messages` to `run_commands.py`. Persist the turn as a `chat_message` `run_events` row via `ScopedStore.append_event` (evidence 03 §6 — the natural home; the type is already in `_DOCUMENTED_EVENT_TYPES` and `message_id` in `_VOLATILE_STRIP_KEYS` from Phase 28). Idempotency: derive/carry `event_id` from the client `message_id` so a replay is a no-op (D-01). Two-layer owner check → 404. Then hand the turn to the router (Task 2) for state-based delivery. Do NOT reuse the legacy `chat.py::Message` table (evidence 03 §6 — no run linkage).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_chat_messages_endpoint.py -x -q</automated>
  </verify>
  <done>A turn persists as a chat_message run_events row; a replayed message_id is a no-op; cross-owner → 404; zero new tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The mechanical intent router (state → dispatch, zero model calls)</name>
  <files>backend/app/api/chat_router.py, backend/tests/unit/test_mechanical_router.py</files>
  <behavior>
    - clarify_waiting → deliver as clarify answers (POST /answers seam); gate_paused → gate action approve/reject/redo/update_specs (POST /gate seam); running → append to ectx.steering_notes (queued: "applies at the next step"); terminal → revision (POST /revisions seam)
    - update_specs routes to the shipped KAN-101 loop (never rebuilt); a gate action after terminal returns pipeline_not_running (KAN-100); a declared gate excluded by gate_agent_ids is not assumed to pause (KAN-94)
    - ZERO model calls for routable turns; free-form/ambiguous turns are left for the Concierge escalation (Phase 33) — out of scope here
    - The router keys on GENERIC run state, never a workflow name (SC-001/INV-1) — it lives in the app layer, not the kernel
  </behavior>
  <action>Create `chat_router.py` exposing `route_chat_turn(run_state, turn) -> dispatch`. Key on the live-state signal (LIVE-STATE-CONTRACT.md §1: questionnaire_ready/review_gate_ready/agent_start.../terminal) and dispatch through the SAME command handlers from 29-03/29-04 (gate/answers/revisions) and the 29-08 steering seam (`ectx.steering_notes`, thread-id policy per the ND-11 record). Preserve the four-action gate channel (D-04), the KAN-100 terminal fence, and KAN-94 event-driven semantics. No model call on any routable branch. Keep it name-free (app-layer only). Add `test_mechanical_router.py` covering each state → correct dispatch + the update_specs/terminal-fence/event-driven cases.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_mechanical_router.py -x -q</automated>
  </verify>
  <done>Each run state routes to the correct channel with zero model calls; update_specs→KAN-101; terminal fence + event-driven gates honored; no name branch.</done>
</task>

<task type="auto">
  <name>Task 3: Wire /messages → router + golden-neutrality + wire-parity regression</name>
  <files>backend/app/api/run_commands.py</files>
  <action>Connect `POST /{id}/messages` to `route_chat_turn` so a persisted turn is delivered per state. Confirm chat turns never fire on the 5 golden paths (INV-3 — the goldens send no chat_message) by re-running the characterization + wire-parity gates. Confirm no new event type/key escaped the Phase-28 vocab (chat_message/chat_reply already in `_DOCUMENTED_EVENT_TYPES`; message_id already in `_VOLATILE_STRIP_KEYS`).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_chat_messages_endpoint.py backend/tests/unit/test_mechanical_router.py backend/tests/agents/test_wire_parity.py backend/tests/agents/test_characterization_prototype.py -x -q</automated>
  </verify>
  <done>/messages delivers via the router; goldens byte/event-identical; wire-parity green; no vocab drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → POST /messages | Untrusted turn text/attachments + message_id; a caller may target another owner's run or replay a message_id |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09-1 | Information Disclosure | posting/reading another owner's run chat | mitigate | Two-layer owner check → 404; ScopedStore owner+workspace default-deny |
| T-29-09-2 | Tampering | duplicate turn injection via replay | mitigate | Idempotency by message_id→event_id at the stamping boundary (no-op on replay) |
| T-29-09-3 | Tampering | gate action after terminal | mitigate | KAN-100 pipeline_not_running fence in the router |
| T-29-09-4 | Elevation of Privilege | kernel name-branch creep | mitigate | Router is app-layer + generic-state-keyed; banned-pattern + import-linter enforce SC-001/INV-1 |
| T-29-09-SC | Tampering | package installs | accept | No installs |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/unit/test_chat_messages_endpoint.py backend/tests/unit/test_mechanical_router.py backend/tests/agents/test_wire_parity.py backend/tests/agents/test_characterization_*.py -x -q` → green
- `python3.11 -m pytest backend/tests/agents/test_banned_patterns.py -x -q` → SC-001/INV-1 clean
- `/opt/homebrew/bin/lint-imports` → 4/0
- LOCK-B grep: no diff to websocket.py / useWebSocket.ts; no migration (zero new tables); no ratchet/ledger row
</verification>

<success_criteria>
A user message reaches a run over REST, persists as an idempotent family-anchored `chat_message` `run_events` row (zero new tables), and the mechanical router delivers it per run state with zero model calls (clarify/gate/steering/revision) — terminal-fenced, event-driven, kernel name-free, goldens intact.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-09-SUMMARY.md` when done
</output>
