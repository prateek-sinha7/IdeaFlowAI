---
phase: 29-transport-cutover-chat-backbone-a1
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - backend/app/api/run_stream.py
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/tests/unit/test_sse_stream.py
autonomous: true
requirements: [CHAT-07]
must_haves:
  truths:
    - "GET /api/runs/{id}/events/stream streams the run's events as SSE with id:=seq, resumable via the Last-Event-ID request header"
    - "On attach the server emits stream_attached {live, replayed_through_seq}, replaying the durable tail after the client cursor before going live"
    - "A cross-owner stream request returns 404 (two-layer owner check), never 403"
    - "A waiting_for_user gate is re-armed on attach: review_gate_ready is re-emitted from persisted gate_events (D-14g)"
    - "The stream is behind SSE_TRANSPORT_ENABLED, built ALONGSIDE /ws/chat (LOCK-B — WS untouched)"
  artifacts:
    - path: "backend/app/api/run_stream.py"
      provides: "The per-run SSE endpoint off _PIPELINE_QUEUES (live) + ScopedStore.read_events (replay)"
      exports: ["router"]
    - path: "backend/app/core/config.py"
      provides: "SSE_TRANSPORT_ENABLED flag + SSE infra settings"
      contains: "SSE_TRANSPORT_ENABLED"
  key_links:
    - from: "backend/app/api/run_stream.py"
      to: "backend/tests/agents/characterization/_sse_projection.py"
      via: "reuse the wire-parity projection so the live stream == recorded WS frames"
      pattern: "run_event_to_sse_frame|project_events"
    - from: "backend/app/api/run_stream.py"
      to: "backend/agents/authz.py"
      via: "ScopedStore(owner_id, workspace_id).read_events(run_id, after_seq) default-deny replay"
      pattern: "read_events"
    - from: "backend/app/main.py"
      to: "backend/app/api/run_stream.py"
      via: "app.include_router(run_stream_router)"
      pattern: "run_stream"
---

<objective>
Ship the per-run SSE stream `GET /api/runs/{id}/events/stream` (D-01/D-13) off the existing per-run queue + durable log, behind the `SSE_TRANSPORT_ENABLED` flag, ALONGSIDE `/ws/chat` (LOCK-B — do not touch websocket.py). It emits `id:`=`seq`, resumes from the browser-native `Last-Event-ID` header, sends the `stream_attached {live, replayed_through_seq}` handshake (replacing `pipeline_reconnected` on the new transport), and re-arms a paused gate on attach (D-14g). Because it uses the 29-01 projection, the wire-parity gate proves it replays WS frames identically.

Purpose: The one-way event firehose becomes browser-native HTTP+SSE with `Last-Event-ID` resume — the down-channel half of the cutover.
Output: `run_stream.py` router, config flag + SSE infra, `test_sse_stream.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md

# Wire-parity projection (built in 29-01) this endpoint mounts
@backend/tests/agents/characterization/_sse_projection.py
# Reference the existing WS reconnect/replay + owner-scope pattern (READ, do NOT edit — LOCK-B)
@backend/app/api/runs.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSE stream endpoint off the per-run queue + durable log</name>
  <files>backend/app/api/run_stream.py, backend/app/core/config.py</files>
  <behavior>
    - GET /api/runs/{id}/events/stream returns text/event-stream via sse-starlette EventSourceResponse
    - Two-layer owner check (WorkflowRun.user_id == current_user.id AND ScopedStore.get_run) → 404 on miss (IDOR→404, never 403), mirroring runs.py get_run_events
    - Replay: ScopedStore.read_events(run_id, after_seq) where after_seq = Last-Event-ID header (or 0) → project each row via _sse_projection.run_event_to_sse_frame (id:=seq)
    - After the durable tail, attach to the live per-run asyncio.Queue (_PIPELINE_QUEUES via websocket.py's getter — imported, not modified) and forward new events as SSE frames
    - First frame after replay is stream_attached {live, replayed_through_seq} (live=false while replaying, then true)
    - Endpoint 404s (feature-absent) when SSE_TRANSPORT_ENABLED is false
  </behavior>
  <action>Create `backend/app/api/run_stream.py` with its own `APIRouter(prefix="/api/runs")` (FastAPI allows multiple routers on one prefix). Use `sse_starlette.EventSourceResponse` (already pinned, requirements.txt:103). Reuse the EXACT two-layer owner pattern from `runs.py::get_run_events` (evidence 03 §6: user_id-filter → 404, then ScopedStore default-deny). Import the per-run queue accessor from `app.api.websocket` (read-only import — do NOT modify websocket.py; LOCK-B). Emit `id: {seq}` on every frame so browsers resume via `Last-Event-ID`. Add `SSE_TRANSPORT_ENABLED: bool = True` to `config.py` plus the D-14h infra knobs (disable gzip for text/event-stream, set `X-Accel-Buffering: no` / `Cache-Control: no-cache`, `proxy_buffering off` documented for the ingress). This is the additive down-channel per D-13; `/ws/chat` stays live in parallel.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_sse_stream.py -x -q -k "replay or attach or owner"</automated>
  </verify>
  <done>Stream replays the durable tail from Last-Event-ID, then goes live; cross-owner → 404; flag-off → 404; frames carry id:=seq.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stream_attached handshake + gate re-arm on attach (D-14g)</name>
  <files>backend/app/api/run_stream.py</files>
  <behavior>
    - stream_attached carries {live, replayed_through_seq} where replayed_through_seq mirrors the last replayed seq (== reconnected().replayed_through_seq semantics)
    - When the run is waiting_for_user with a persisted gate, the attach path re-emits review_gate_ready (re-armed from persisted gate_events) so a reload during a paused gate resumes correctly (D-14g; closes the P23/F4 hole)
    - Re-arm reads gate state via ScopedStore (owner-scoped); it does NOT re-run any agent or mutate the artifact graph
  </behavior>
  <action>Add the handshake + gate-re-arm to the attach sequence. Reuse the existing durable gate signal already persisted by the engine (evidence 03 §2b `review_gate_ready`; restore path engine.py:4573 `restore_non_terminal_runs` branch (a) already re-arms the resume `asyncio.Event`). This task adds ONLY the stream-side re-emit of `review_gate_ready` on attach from persisted `gate_events` (D-14g) — no engine edit. Model `replayed_through_seq` on the legacy `pipeline_reconnected.replayed_through_seq` value (evidence 03 §1). Also address ND-9: a pending steering note / chat-session cursor is derivable from the replayed run_events, so reattach is server-derived (no sessionStorage trust).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_sse_stream.py -x -q -k "handshake or rearm or gate"</automated>
  </verify>
  <done>Attach emits stream_attached with the correct cursor; a paused-gate run re-emits review_gate_ready on attach; no engine/artifact mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Register router + prove wire-parity against the live projection</name>
  <files>backend/app/main.py, backend/tests/unit/test_sse_stream.py</files>
  <action>Register the new router in `main.py` beside the existing includes (after `runs_router`, line ~172) — additive, WS include untouched (LOCK-B). Finish `test_sse_stream.py` and add an assertion that the endpoint's projected frames for a scripted run match the 29-01 `assert_wire_parity` expectation (the endpoint uses the same `_sse_projection`, so parity holds by construction — this test guards against accidental divergence). Keep everything offline (TestClient / scripted store, no live Bedrock).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_sse_stream.py backend/tests/agents/test_wire_parity.py -x -q</automated>
  </verify>
  <done>Router mounted; SSE endpoint frames satisfy wire-parity; full test module green offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → GET /events/stream | Untrusted `run_id` + `Last-Event-ID`; a caller may probe another owner's run |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-1 | Information Disclosure | cross-owner event replay | mitigate | Two-layer owner check (user_id filter → 404, then ScopedStore default-deny), IDOR→404 never 403 |
| T-29-02-2 | Spoofing | forged Last-Event-ID to skip/seek | accept | Cursor only bounds replay range within the caller's OWN owner-scoped run; no cross-run leakage |
| T-29-02-3 | Denial of Service | many long-lived streams | mitigate | Reuse existing per-run single queue; uvicorn ping + proxy idle-timeout > heartbeat (D-14h); flag-gated |
| T-29-02-SC | Tampering | package installs | accept | No installs — sse-starlette already pinned |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/unit/test_sse_stream.py backend/tests/agents/test_wire_parity.py -x -q` → green
- `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
- Grep proof LOCK-B honored: `git diff --name-only` shows NO change to `backend/app/api/websocket.py`, `frontend/src/hooks/useWebSocket.ts`, or `websocket_handoff.py`; no ledger row added
- 5 INV-3 goldens unchanged
</verification>

<success_criteria>
The additive SSE down-channel is live behind a flag: resumable by Last-Event-ID, handshake + gate re-arm on attach, owner-scoped IDOR→404, and wire-parity-green against the recorded WS frame sequences — with `/ws/chat` fully intact (LOCK-B).
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-02-SUMMARY.md` when done
</output>
