---
phase: 29-transport-cutover-chat-backbone-a1
plan: 04
type: execute
wave: 4
depends_on: [29-03]
files_modified:
  - backend/app/api/run_commands.py
  - backend/tests/unit/test_rest_run_launch.py
  - backend/tests/unit/test_rest_revisions.py
  - backend/tests/unit/test_rest_user_message_shim.py
autonomous: true
requirements: [CHAT-07]
must_haves:
  truths:
    - "POST /api/runs launches a run over REST, minting the WorkflowRun and spawning the background driver onto the per-run queue (the SSE stream then attaches)"
    - "Image caps (mime allow-list, per-image + count + aggregate size, vision-model guard, IMAGE_INPUT_ENABLED) are enforced on the REST launch identically to the WS path"
    - "POST /api/runs/{id}/revisions dispatches the <base>_revision pipeline as a child run with parent_run_id + owner_id, passing the 6-scenario revision suite"
    - "user_message is ported to a POST+stream shim that drives ChatRunner directly — its frozen golden (test_chat_contract.py) stays byte-identical"
    - "/ws/chat run/chat handlers are untouched (LOCK-B); reused via read-only import"
  artifacts:
    - path: "backend/app/api/run_commands.py"
      provides: "REST launch + revision + user_message shim endpoints"
      exports: ["router"]
    - path: "backend/tests/unit/test_rest_revisions.py"
      provides: "Revision 6-scenario + parent-link ownership suite ported to REST"
  key_links:
    - from: "backend/app/api/run_commands.py"
      to: "backend/app/api/websocket.py"
      via: "read-only import of the WS-agnostic queue-spawn (_run_pipeline_to_queue) + revision/image-validation helpers"
      pattern: "_run_pipeline_to_queue|_handle_revision|_validate_images"
    - from: "backend/app/api/run_commands.py"
      to: "backend/app/agents/chat_runner.py"
      via: "user_message shim drives ChatRunner unchanged (golden untouched)"
      pattern: "ChatRunner"
---

<objective>
Ship the remaining up-channel launch commands (D-13): `POST /api/runs` (run launch, replacing `run_pipeline`), `POST /api/runs/{id}/revisions` (replacing `run_revision`), and the `user_message` POST+stream shim (ND-3 legacy free-chat ported, not dropped). The background execution machinery is REUSED from the WS-agnostic queue driver by read-only import — websocket.py is NOT modified (LOCK-B; the de-dup + WS deletion are the deferred supervised follow-up). Every inbound-handler test suite is ported 1:1, including the image caps and the revision 6-scenario suite.

Purpose: A run can be started, revised, and free-chatted entirely over REST while the SSE stream carries the results — completing the additive command surface.
Output: launch/revision/shim endpoints in `run_commands.py` + three ported test suites.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/v2.0-evidence/03-backend-chat-surface-map.md

# Ported test sources + the shim target (READ; do NOT edit websocket.py or ChatRunner golden — LOCK-B/landmine)
@backend/tests/unit/test_run_pipeline_validation.py
@backend/tests/unit/test_run_revision_ws_dispatch.py
@backend/tests/unit/test_image_ws_ingress.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/runs (launch) + image caps, reusing the WS-agnostic driver</name>
  <files>backend/app/api/run_commands.py, backend/tests/unit/test_rest_run_launch.py, backend/tests/unit/test_image_ws_ingress.py</files>
  <behavior>
    - POST /api/runs {message, pipeline_type, agent_ids, attached_skills/hooks, gate_agent_ids, model_overrides, selections, template_id, design_system_id, custom_*_body, source_workflow_run_id, images?} mints a WorkflowRun (owner_id=user) and spawns the background driver → returns {run_id}
    - The per-run queue + durable run_events are populated by the driver (decoupled from any socket) so the SSE stream (29-02) attaches and streams identically
    - Image validation identical to WS: mime allow-list, ~3.75MB/image, ≤20 images, ~8MB aggregate, vision-model guard, IMAGE_INPUT_ENABLED off → ignored; bad mime/non-vision → rejected with no execute
    - run_pipeline validation (bad payload, missing brief) rejected pre-mint
  </behavior>
  <action>Implement `POST /api/runs` in `run_commands.py`. REUSE the WS-agnostic background driver `_run_pipeline_to_queue` and the run-minting + `_validate_images` helpers by read-only import from `app.api.websocket` (evidence 03 §1 — the queue/bg-task path is already decoupled from the socket; do NOT modify websocket.py). Preserve the image caps exactly (evidence 03 CORRECTIONS #1: caps + `_validate_images` at websocket.py:1887-1910). Port `test_run_pipeline_validation.py` into `test_rest_run_launch.py` and re-point the image cases in `test_image_ws_ingress.py` to also cover the REST launch (keep the WS cases green — additive). Sanctioned LOCK-B duplication: the WS `run_pipeline` handler keeps its own copy; the de-dup is the deferred follow-up.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_rest_run_launch.py backend/tests/unit/test_image_ws_ingress.py -x -q</automated>
  </verify>
  <done>REST launch mints + drives a run to the queue; all image caps enforced identically; validation rejects bad payloads; WS image cases still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/runs/{id}/revisions — child run dispatch + ownership</name>
  <files>backend/app/api/run_commands.py, backend/tests/unit/test_rest_revisions.py</files>
  <behavior>
    - POST /{id}/revisions {target_artifact_type, instruction} creates a child WorkflowRun with parent_run_id + owner_id=user and dispatches the <base>_revision pipeline (previous_run provider seeds parent artifact + spec/design)
    - The 6 revision scenarios (from the ported suite) pass over REST; parent-link ownership: a cross-owner parent → 404
    - Terminology guard: this is a family CHILD run (D-02 "revision run"), NOT the intra-run KAN-101 spec-revision loop
  </behavior>
  <action>Implement `POST /{id}/revisions` reusing the WS-agnostic revision dispatch core (evidence 03 §2c `_handle_revision_execution`, websocket.py:2214) by read-only import; do NOT modify websocket.py. Port `test_run_revision_ws_dispatch.py`, `test_run_revision_fe_contract.py`, and `test_ws_parent_link_ownership.py` into `test_rest_revisions.py` against the REST endpoint (6-scenario suite + parent ownership → 404).</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_rest_revisions.py -x -q</automated>
  </verify>
  <done>Revision child run dispatches with parent_run_id + owner_id; 6 scenarios pass; cross-owner parent → 404.</done>
</task>

<task type="auto">
  <name>Task 3: user_message POST+stream shim (ChatRunner untouched)</name>
  <files>backend/app/api/run_commands.py, backend/tests/unit/test_rest_user_message_shim.py</files>
  <action>Add a POST endpoint that ports the legacy `user_message` free-chat to a POST+stream shim: it persists the Message rows and drives `ChatRunner` exactly as the WS handler does (evidence 03 §7), streaming ChatRunner's `phase_start/stream/phase_end/error/complete` vocab back over the SSE channel. Do NOT touch the `chat` pipeline agents, `ChatRunner`, or the frozen `test_chat_contract.py` golden (landmine, POR §7 / ND-3). Add `test_rest_user_message_shim.py` proving the shim yields the same ChatRunner output; assert `test_chat_contract.py` remains byte-identical.</action>
  <verify>
    <automated>python3.11 -m pytest backend/tests/unit/test_rest_user_message_shim.py backend/tests/agents/test_chat_contract.py -x -q</automated>
  </verify>
  <done>The shim drives ChatRunner over POST+stream; the frozen chat golden stays byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → POST /runs, /revisions, user_message | Untrusted brief/instruction/images; a caller may target another owner's parent run or over-size images |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-1 | Elevation of Privilege | revision off another owner's parent | mitigate | Parent-link ownership check (user_id) → 404 (ported test_ws_parent_link_ownership) |
| T-29-04-2 | Denial of Service | oversized/too-many images | mitigate | _validate_images caps (~3.75MB/img, ≤20, ~8MB aggregate) + vision-model guard + IMAGE_INPUT_ENABLED |
| T-29-04-3 | Tampering | malformed launch payload | mitigate | Ported run_pipeline validation rejects pre-mint |
| T-29-04-SC | Tampering | package installs | accept | No installs |
</threat_model>

<verification>
- `python3.11 -m pytest backend/tests/unit/test_rest_run_launch.py backend/tests/unit/test_rest_revisions.py backend/tests/unit/test_rest_user_message_shim.py backend/tests/unit/test_image_ws_ingress.py backend/tests/agents/test_chat_contract.py backend/tests/agents/test_wire_parity.py -x -q` → green
- `/opt/homebrew/bin/lint-imports` → 4/0
- LOCK-B grep: no diff to websocket.py / useWebSocket.ts / websocket_handoff.py / ChatRunner golden; no ratchet/ledger row
</verification>

<success_criteria>
The full additive REST command surface (POR §6 six endpoints) is complete: launch (with image caps), revision child-run dispatch, and the user_message shim — every ported handler suite green, the frozen ChatRunner golden intact, `/ws/chat` untouched.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-04-SUMMARY.md` when done
</output>
