---
phase: 29-transport-cutover-chat-backbone-a1
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/fixtures/mockSse.ts
  - frontend/e2e/fixtures/mockWs.ts
  - frontend/e2e/tests/ts-sse.spec.ts
autonomous: true
requirements: [CHAT-07]
must_haves:
  truths:
    - "The e2e harness can drive the new SSE transport (stream_attached handshake, id:=seq replay, chat frames) additively"
    - "The chat driver emits chat_message / chat_reply / stream_attached on the SAME envelope, seq counter, and event_id space as existing pipeline frames (per MOCKWS-CHAT-DRIVER-CONTRACT)"
    - "All 123 existing mocked specs stay green (additive-extension acceptance bar)"
  artifacts:
    - path: "frontend/e2e/fixtures/mockSse.ts"
      provides: "The mock-SSE transport driver (route the SSE endpoint, model attach/replay/drop) mirroring MockWs semantics"
    - path: "frontend/e2e/fixtures/mockWs.ts"
      provides: "Additive chat driver methods (chatMessage/chatReply/streamAttached/waitForChatCommand)"
      contains: "streamAttached"
  key_links:
    - from: "frontend/e2e/fixtures/mockSse.ts"
      to: "frontend/e2e/fixtures/mockWs.ts"
      via: "reuse the shared seq counter + nextEventId()/GLOBAL_EVENT_ID (one monotonic sequence)"
      pattern: "nextEventId|GLOBAL_EVENT_ID"
---

<objective>
Build the e2e mock-SSE transport driver + the additive chat driver methods per the Phase-28 `MOCKWS-CHAT-DRIVER-CONTRACT.md` (RUNUI-05). It models the Phase-29 SSE down-channel and REST up-channel ADDITIVELY (LOCK-B): the new `chat_message`/`chat_reply`/`stream_attached` frames ride the SAME envelope and monotonic seq/event_id space as the existing pipeline frames, and `useWebSocket.ts` is not touched. The 123 existing mocked specs must stay green.

Purpose: The FE transport adapter (29-07) and later chat-lane specs need a driver that can script SSE attach/replay/drop and outbound REST command assertions — without a real server.
Output: `mockSse.ts`, additive chat methods on `mockWs.ts`, a smoke spec `ts-sse`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-transport-cutover-chat-backbone-a1/29-CONTEXT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md
@.planning/v2.0-evidence/04-frontend-anatomy.md

# The existing controller this extends (READ its header INBOUND FRAME CONTRACT + emit()/seq/sent[])
@frontend/e2e/fixtures/mockWs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive chat driver methods on MockWs</name>
  <files>frontend/e2e/fixtures/mockWs.ts</files>
  <action>Add the three chat emit helpers + the outbound-command assert to `MockWs` per MOCKWS-CHAT-DRIVER-CONTRACT §2/§3: `chatMessage({messageId,text,attachments?})`, `chatReply({cardKind,text,deepLink?,messageId?})`, `streamAttached({live,replayedThroughSeq?})` (defaults replayedThroughSeq to current seq, mirroring `reconnected()`), and `waitForChatCommand(predicate?)` reusing the existing `sent[]`/`waiters` machinery. Reuse the ONE `seq` counter + `nextEventId()`/`GLOBAL_EVENT_ID` — no second envelope, no second seq space. Do NOT change the signature/behavior of any existing helper (`start`/`agent*`/`wave*`/`reviewGate*`/`questionnaire*`/`reconnected`/`drop`/`expireJwt`); keep `pipeline_reconnected` beside the new `stream_attached`.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Chat methods compile; existing helper signatures unchanged; single seq/event_id space preserved.</done>
</task>

<task type="auto">
  <name>Task 2: mockSse.ts transport driver (attach/replay/drop)</name>
  <files>frontend/e2e/fixtures/mockSse.ts</files>
  <action>Create `mockSse.ts` that intercepts `GET /api/runs/{id}/events/stream` (Playwright `page.route`) and models the SSE transport: emit `id:{seq}` framed events, honor a `Last-Event-ID` request header for replay-from-cursor, script `streamAttached`, and support `drop()`/reattach to exercise auto-reconnect. It shares the `MockWs` seq/event_id source so SSE frames interleave deterministically with any pipeline frames on one monotonic sequence (contract §1/§5). This is ADDITIVE — it does not remove or rewrite the WS route path.</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>mockSse routes the SSE endpoint, replays from Last-Event-ID, supports drop/reattach; shares the monotonic seq/event_id space.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke spec + 123-spec regression guard</name>
  <files>frontend/e2e/tests/ts-sse.spec.ts</files>
  <action>Add `ts-sse.spec.ts`: a minimal spec driving `mockSse` through attach → replay-from-cursor → drop → reattach, asserting the app dedups by event_id and resumes from seq. Then run the full mocked project to prove the additive driver kept the existing 123 specs green.</action>
  <verify>
    <automated>cd frontend && npm run e2e -- --project=mocked</automated>
  </verify>
  <done>ts-sse passes; the existing 123 mocked specs remain green (additive-extension bar).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | e2e test harness only; no runtime surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-1 | Tampering | additive driver silently breaks existing specs | mitigate | Task 3 runs the full 123-spec mocked project as the acceptance bar |
| T-29-06-SC | Tampering | package installs | accept | No installs; uses existing Playwright |
</threat_model>

<verification>
- `cd frontend && npm run e2e -- --project=mocked` → ts-sse green + 123 existing specs green
- `cd frontend && npx tsc --noEmit` → clean
- LOCK-B grep: no diff to `frontend/src/hooks/useWebSocket.ts`; `pipeline_reconnected` still present in mockWs.ts
</verification>

<success_criteria>
The e2e harness drives the SSE transport + chat frames additively per the Phase-28 contract, on one monotonic seq/event_id space, with the 123 existing mocked specs green and `useWebSocket.ts` untouched.
</success_criteria>

<output>
Create `.planning/phases/29-transport-cutover-chat-backbone-a1/29-06-SUMMARY.md` when done
</output>
