---
phase: 01-0a-safety-net-deletion-guard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/003-workflow-engine-decoupling/migration-ledger.md
  - backend/tests/agents/test_migration_ledger.py
autonomous: true
requirements: [DEL-01, DEL-02, DEL-03, DEL-04, SAFE-04]
must_haves:
  truths:
    - "specs/003-workflow-engine-decoupling/migration-ledger.md mirrors EVERY §31 row (L1, L2/L9, L3, L4/L8, L5, L6, L7, L10, L11, L12, L13, L14, L15, L16, D1, F1-F5) with the same columns and verbatim grep patterns"
    - "All ledger rows start Status = ☐ in Phase 1 (nothing deleted yet) — the guard is green/empty (D-10)"
    - "test_migration_ledger.py parses the ledger and, for each ☑ row ONLY, greps backend/ for the row's pattern and asserts 0 matches (ratchet)"
    - "The guard is proven NON-VACUOUS: a fixture ☑ row with a pattern that DOES exist in backend/ makes the test fail (false-green guard)"
  artifacts:
    - path: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      provides: "operational single-source-of-truth ledger mirroring §31"
      contains: "Status"
    - path: "backend/tests/agents/test_migration_ledger.py"
      provides: "CI ratchet asserting deleted patterns stay deleted"
      contains: "subprocess"
  key_links:
    - from: "backend/tests/agents/test_migration_ledger.py"
      to: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      via: "parse markdown table rows, filter ☑, grep backend/"
      pattern: "migration-ledger.md"
    - from: "test_migration_ledger.py grep"
      to: "backend/ tree"
      via: "subprocess grep -rnE pattern backend --include=*.py"
      pattern: "grep"
---

<objective>
Create the operational migration ledger and its CI ratchet (DEL-01..04 + SAFE-04). The standalone `specs/003-workflow-engine-decoupling/migration-ledger.md` (D-08) mirrors the plan §31 table faithfully — every `L#`/`F#`/`D#` row, same columns, verbatim grep patterns — and becomes the single source of truth CI asserts against. `test_migration_ledger.py` (D-09/D-14) parses it and enforces, for each `☑` row ONLY, that its banned grep-pattern returns **0 matches** in `backend/`.

Per the user directive ("nothing from plan.md can be missed out"), the ledger MUST include all sixteen leak rows + five factory rows + the dead-code row — copied verbatim from §31 (plan.md lines 1003-1023). In Phase 1 every row is `☐` (nothing deleted yet, D-10) → the guard is green/empty and tightens as later phases flip rows to `☑` (ratchet — DEL-03).

Purpose: make INV-12 (move-don't-copy) machine-enforceable; arm the deletion exit gate the rest of the migration depends on.
Output: `migration-ledger.md` (mirror of §31) + `test_migration_ledger.py` (the ratchet, with a non-vacuous negative-test guard).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-0a-safety-net-deletion-guard/01-CONTEXT.md
@.planning/phases/01-0a-safety-net-deletion-guard/01-PATTERNS.md
@specs/003-workflow-engine-decoupling/plan.md
</context>

<artifacts_this_phase_produces>
New files created by THIS plan (01-03):
- `specs/003-workflow-engine-decoupling/migration-ledger.md` (operational mirror of plan §31)
- `backend/tests/agents/test_migration_ledger.py` (CI ratchet)

Created by sibling plans (do NOT create here): characterization tests + golden + scripts (01-01/02); config + banned-patterns + CI wiring (01-04). NOTE: the `backend:characterization` CI job that RUNS `test_migration_ledger.py` is wired in 01-04 — this plan only creates the test + ledger.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create migration-ledger.md mirroring every §31 row (all ☐)</name>
  <files>specs/003-workflow-engine-decoupling/migration-ledger.md</files>
  <read_first>
    - specs/003-workflow-engine-decoupling/plan.md (lines 999-1025 §31 ledger table + the rules above it lines 989-997) — the AUTHORITATIVE source to mirror verbatim
    - .planning/phases/01-0a-safety-net-deletion-guard/01-PATTERNS.md (the `migration-ledger.md` section ~lines 284-297) — required columns + the full row list
  </read_first>
  <action>
    Create `specs/003-workflow-engine-decoupling/migration-ledger.md` with a header explaining it is the operational mirror of plan §31 (DEL-04) asserted by `tests/agents/test_migration_ledger.py`, the `☐ pending / ☑ done` legend, and the ratchet rule (DEL-03: once `☑`, reintroduction fails CI). Then reproduce the §31 markdown table EXACTLY — columns `| Item | Legacy (file:line) | New home | Phase | Deletion gate (grep → 0 / check) | Status |` — with all rows from plan.md lines 1004-1023: L14, L16, D1 (Phase 0B); L13 (0C→2); L1, L2/L9, L3, L4/L8, L5, L6, L7, L10, L11, L12 (Phase 2); L15 (1A→1B); F1, F2, F3, F4, F5 (Phase 3). Copy each row's grep pattern VERBATIM from §31 (e.g. L1 → `_PROTOTYPE_PIPELINE_TYPES|_PPT_PIPELINE_TYPES`; L7 → `spec.id == "prototype-build"`; L11 → the full alternation; F5 → the "create_deep_agent called only inside the adapter" check). EVERY row's Status = `☐` (D-10). Add a "Deleting commit SHA" column or note (DEL-04 wants status + deleting SHA) — leave SHA blank for `☐` rows. For non-grep rows (L16 "cross-owner denial test passes", F4 "constitution-injected-in-prod test passes", F5 "called only inside adapter") mark the gate as a CHECK (not a grep) so the parser in Task 2 can distinguish grep rows from check rows. Add a short note that the INV-1 reservation (`if pipeline_type ==` / `spec.id ==`) is enforced via L7/L10 rows (hard-fail in Phase 7) and warn-only in Phase 1 (cross-ref 01-04 banned-patterns).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin && test -f specs/003-workflow-engine-decoupling/migration-ledger.md && python3.11 -c "import re; t=open('specs/003-workflow-engine-decoupling/migration-ledger.md').read(); rows=[l for l in t.splitlines() if l.startswith('|') and ('☐' in l or '☑' in l)]; ids=[l.strip('|').split('|')[0].strip() for l in rows]; need=['L14','L16','D1','L13','L1','L2/L9','L3','L4/L8','L5','L6','L7','L10','L11','L12','L15','F1','F2','F3','F4','F5']; missing=[n for n in need if n not in ids]; assert not missing, f'missing rows: {missing}'; assert all('☐' in l for l in rows), 'some row not ☐ in Phase 1'; print('ledger rows OK:', len(rows))"</automated>
  </verify>
  <done>`migration-ledger.md` exists, contains all 20 ledger items (L1–L16 incl. combined L2/L9 + L4/L8, plus D1, F1–F5), every row Status = `☐`, and grep patterns match §31 verbatim. The parser confirms no row is missing.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_migration_ledger.py ratchet with a non-vacuous negative guard</name>
  <files>backend/tests/agents/test_migration_ledger.py</files>
  <read_first>
    - specs/003-workflow-engine-decoupling/migration-ledger.md (Task 1 output) — the table this test parses
    - .planning/phases/01-0a-safety-net-deletion-guard/01-PATTERNS.md (the `test_migration_ledger.py` section ~lines 213-251) — the parser/grep pattern to author
    - backend/tests/agents/_scripted_model.py (line 49 path-root idiom `Path(__file__).resolve().parents[...]`) — how to resolve the repo + backend roots
  </read_first>
  <action>
    Create `test_migration_ledger.py` (D-09). Resolve `_REPO = Path(__file__).resolve().parents[3]` (tests/agents → tests → backend → repo — VERIFY the depth at author time and adjust), `_LEDGER = _REPO/"specs/003-workflow-engine-decoupling/migration-ledger.md"`, `_BACKEND = _REPO/"backend"`. Write `_checked_grep_rows()` that parses table rows, skips header/separator, splits cells, and returns `(item, pattern)` for rows where Status contains `☑` AND the gate cell is a grep pattern (not a CHECK row). Parametrize `test_deleted_pattern_absent_from_backend(item, pattern)` over `_checked_grep_rows() or [("__none__", None)]`; when `pattern is None` → `pytest.skip("no ☑ grep rows yet — green/empty in Phase 1 (D-10)")`; otherwise run `grep -rnE <pattern> backend --include=*.py` via subprocess and assert returncode != 0 (0 matches) with the offending stdout in the message. CRITICAL non-vacuous guard (T-01-03 mitigation): add a SEPARATE test `test_guard_fails_on_known_present_pattern` that builds a temp ledger string containing one `☑` row whose grep pattern is a token KNOWN to exist in `backend/` (e.g. `_PROTOTYPE_PIPELINE_TYPES`, confirmed present in engine.py), runs the SAME parse+grep logic against it, and asserts the assertion WOULD fail (i.e. the pattern is found) — proving the ratchet actually greps and is not vacuously green. Also add `test_ledger_parses_and_all_phase1_rows_pending` asserting every required item id is present and all are `☐` (mirrors Task 1's check, in-suite). Keep offline/unmarked.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/agents/test_migration_ledger.py -q -rs</automated>
  </verify>
  <done>`pytest tests/agents/test_migration_ledger.py` exits 0: the parametrized deletion test SKIPS (all rows `☐`, green/empty per D-10); `test_guard_fails_on_known_present_pattern` PASSES (proving the grep machinery actually finds a present token — non-vacuous); `test_ledger_parses_and_all_phase1_rows_pending` PASSES. `-rs` output shows the intended skip reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → ledger ratchet | False-green is the core threat: in Phase 1 ALL rows are `☐`, so the deletion test legitimately skips — a poorly written test could be vacuously green forever (never actually grepping). No network/data/auth surface added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (false-green) | ratchet that never greps (vacuous skip forever) | mitigate | `test_guard_fails_on_known_present_pattern` exercises the SAME parse+grep logic against a synthetic `☑` row whose pattern exists in `backend/` and asserts a match is found — proving the grep machinery works even while real rows are `☐`. |
| T-03-02 | Tampering | ledger drifts from §31 (rows/patterns silently omitted) | mitigate | `test_ledger_parses_and_all_phase1_rows_pending` asserts all required item ids are present; Task 1 verify cross-checks the same list; SUMMARY records a §31-vs-ledger row diff = 0. |
| T-03-03 | Repudiation | wrong repo-root depth → grep targets nothing (silently 0 matches) | mitigate | The negative-guard test would FAIL if the path root were wrong (it greps a known-present token); a wrong root surfaces immediately, not as a false green. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (markdown + test code only); no `[ASSUMED]`/`[SUS]` gate needed. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/agents/test_migration_ledger.py -q -rs` exits 0 (deletion test skipped green/empty; negative guard + parse test pass).
- `migration-ledger.md` row count == §31 row count (20 items); every Phase-1 row Status = `☐`.
- The negative guard proves the grep is live (non-vacuous).
- No runtime module touched (`git diff --name-only` shows only the ledger + the new test).
</verification>

<success_criteria>
- DEL-04: `migration-ledger.md` mirrors §31 operationally (all L#/F#/D# rows, verbatim patterns, status column).
- SAFE-04 / DEL-02 / DEL-03: `test_migration_ledger.py` asserts each `☑` row's pattern → 0 in `backend/`; ratchet proven non-vacuous.
- DEL-01: ledger encodes the wrap→rewire→delete discipline (one row per legacy element, owning phase noted).
- D-10: all rows `☐` in Phase 1 → guard green/empty.
- Zero runtime behavior change.
</success_criteria>

<output>
Create `.planning/phases/01-0a-safety-net-deletion-guard/01-03-SUMMARY.md` when done. Record: the verified repo-root depth used, the exact §31-vs-ledger row diff (should be 0), which token was used for the non-vacuous negative guard, and how grep rows vs CHECK rows are distinguished by the parser.
</output>
