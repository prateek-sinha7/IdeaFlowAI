---
phase: 01-0a-safety-net-deletion-guard
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - backend/requirements-dev.txt
  - backend/pyproject.toml
  - backend/tests/agents/test_banned_patterns.py
  - .gitlab-ci.yml
autonomous: true
requirements: [SAFE-05, SAFE-06, SAFE-07]
must_haves:
  truths:
    - "import-linter and vulture are pinned in backend/requirements-dev.txt and configured in backend/pyproject.toml; `cd backend && lint-imports` exits 0 on the current tree (scaffold-green, D-12) and `vulture` runs allow-listed without false-positive failures"
    - "test_banned_patterns.py blocks hand-rolled deep agents (class DeepAgent / def deep_agent / new local deepagents|langchain_deepagents module / bespoke for _ in range(max_iterations)) while allow-listing the ONE sanctioned `from deepagents import create_deep_agent` in app/agents/deep_agent_runner.py; it passes on the current tree"
    - "The banned-pattern gate is proven NON-VACUOUS: an injected known-bad fixture (a temp module defining `class DeepAgent`) makes the scanner report a violation"
    - ".gitlab-ci.yml runs the gates in CI: backend:lint gains lint-imports + vulture; a new offline backend:characterization job runs the characterization + migration-ledger + banned-pattern tests (SAFE-07)"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "[tool.importlinter] kernel→ports contract (scaffold) + [tool.vulture] config"
      contains: "tool.importlinter"
    - path: "backend/tests/agents/test_banned_patterns.py"
      provides: "INV-13/R15 banned-pattern ratchet with allow-list + non-vacuous negative test"
      contains: "create_deep_agent"
    - path: ".gitlab-ci.yml"
      provides: "lint-imports + vulture in backend:lint; offline backend:characterization test job"
      contains: "backend:characterization"
  key_links:
    - from: ".gitlab-ci.yml backend:characterization"
      to: "tests/agents/test_characterization_*.py + test_migration_ledger.py + test_banned_patterns.py"
      via: "pytest invocation in the new job script"
      pattern: "backend:characterization"
    - from: "backend/pyproject.toml [tool.importlinter]"
      to: "agents.execution_engine.engine (the current kernel)"
      via: "forbidden-import contract, scaffold-green now, tighten in Phase 7/8"
      pattern: "tool.importlinter"
    - from: "test_banned_patterns.py allow-list"
      to: "app/agents/deep_agent_runner.py"
      via: "the only sanctioned create_deep_agent call site"
      pattern: "deep_agent_runner"
---

<objective>
Arm the static gates and wire EVERYTHING into CI — the load-bearing closer for Phase 1 (SAFE-05/06/07). Without this plan the whole safety net is false-green, because `tests/agents` is NOT run in CI today (`.gitlab-ci.yml backend:test` runs only `tests/unit`).

This plan: (1) adds `import-linter` + `vulture` to `backend/requirements-dev.txt` (pinned) and configures `[tool.importlinter]` (kernel→ports scaffold contract, D-11/D-12 — green now, designed to tighten in Phase 7/8 without rewrite) + `[tool.vulture]` (allow-listed dead-code scan, D-13) in `backend/pyproject.toml`; (2) creates `test_banned_patterns.py` (D-14/D-15) blocking hand-rolled deep agents (INV-13/R15) with the ONE sanctioned `create_deep_agent` site allow-listed + a NON-VACUOUS negative test; (3) wires all gates into `.gitlab-ci.yml` — `lint-imports` + `vulture` into `backend:lint`, and a dedicated OFFLINE `backend:characterization` test job running the characterization + ledger + banned-pattern tests (D-16/D-17).

IMPORTANT grounding correction (verify at execution): there is NO legacy `class DeepAgent` / `deep_agent.py` under `backend/app/agents/` today (already removed in the 002 migration). So the banned-pattern gate must NOT depend on allow-listing a legacy `deep_agent.py`; instead allow-list only the real `deep_agent_runner.py` import site and prove the gate non-vacuous by INJECTING a known-bad fixture in a temp dir. Re-verify before coding.

Purpose: make INV-13 and the import-direction discipline machine-enforced AND actually executed in CI.
Output: pinned dev deps + pyproject config + banned-pattern test + CI wiring that runs all four Phase-1 gate families.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-0a-safety-net-deletion-guard/01-CONTEXT.md
@.planning/phases/01-0a-safety-net-deletion-guard/01-PATTERNS.md
@.planning/phases/01-0a-safety-net-deletion-guard/01-01-SUMMARY.md
@.planning/phases/01-0a-safety-net-deletion-guard/01-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New file created by THIS plan (01-04):
- `backend/tests/agents/test_banned_patterns.py` (INV-13/R15 ratchet)

Modified by THIS plan:
- `backend/requirements-dev.txt` (add pinned `import-linter` + `vulture`)
- `backend/pyproject.toml` (add `[tool.importlinter]` + `[tool.vulture]`)
- `.gitlab-ci.yml` (extend `backend:lint`; add `backend:characterization` job)

Depends on (created by sibling plans — must exist before CI wiring references them): `test_characterization_*.py` (01-01/02), `test_migration_ledger.py` (01-03). Verify they exist before writing the CI job glob.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pin import-linter + vulture and configure [tool.importlinter] + [tool.vulture]</name>
  <files>backend/requirements-dev.txt, backend/pyproject.toml</files>
  <read_first>
    - backend/requirements-dev.txt (existing pins: ruff==0.8.4, pyright==1.1.391, pre-commit==4.0.1, plus the header "pinned exactly … Bump deliberately") — match the comment-then-exact-pin style
    - backend/pyproject.toml (lines 7-15 `[tool.pytest.ini_options]`, lines 34-87 `[tool.ruff]`) — match the rationale-first heavy-comment style; confirm there is no existing `[tool.importlinter]`/`[tool.vulture]`
    - backend/agents/execution_engine/engine.py (top imports only) — confirm the current kernel module path `agents.execution_engine.engine` and what it imports today (so the scaffold contract is green)
    - .planning/phases/01-0a-safety-net-deletion-guard/01-PATTERNS.md (the pyproject section ~lines 300-330) — the scaffold contract + vulture config shape
  </read_first>
  <action>
    In `requirements-dev.txt`, add `import-linter` and `vulture` with a comment block each (what they catch + 003 §31 / D-11 / D-13 rationale) and EXACT pins — resolve the latest stable exact versions at author time (do NOT use ranges; the PATTERNS.md values `import-linter==2.1` / `vulture==2.14` are placeholders — verify against PyPI and pin the real current versions). In `pyproject.toml`, add `[tool.importlinter]` with `root_package = "agents"` and a `forbidden` (or `layers`, discretion per D-12) contract named "kernel imports only capability ports (scaffold)" whose `source_modules = ["agents.execution_engine.engine"]` and `forbidden_modules = []` TODAY (green) — with an inline comment documenting the intended FINAL form (kernel must not import legacy `factory`/`engine` internals once `kernel.py` + `capabilities/base.py` land in Phase 2/8) so Phase 8 only tightens, never rewrites (D-12). Add `[tool.vulture]` scanning `app` + `agents`, `min_confidence = 80`, excluding `alembic/versions` + `tests`, with an allow-list approach (in-repo whitelist file OR exclude list — discretion per D-13) to avoid false positives on FastAPI routes / pytest fixtures / registry self-registration. VERIFY both tools run green on the CURRENT tree (the contract is a scaffold; vulture must not hard-fail on framework entrypoints — tune the allow-list until it exits 0).
  </action>
  <verify>
    <automated>cd backend && pip install --quiet import-linter vulture 2>/dev/null; lint-imports && vulture app/ agents/ && echo "STATIC GATES GREEN"</automated>
  </verify>
  <done>`cd backend && lint-imports` exits 0 (scaffold contract holds on the current tree). `cd backend && vulture app/ agents/` exits 0 (allow-listed; no false-positive failures on framework entrypoints). Both tools pinned in `requirements-dev.txt`; both configured in `pyproject.toml` with inline rationale + the documented tighten-later intent for the import contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_banned_patterns.py (INV-13/R15) with allow-list + non-vacuous negative test</name>
  <files>backend/tests/agents/test_banned_patterns.py</files>
  <read_first>
    - backend/app/agents/deep_agent_runner.py (line 53 `from deepagents import create_deep_agent`, line 240 `create_deep_agent(`) — the ONE sanctioned call site to allow-list
    - .planning/phases/01-0a-safety-net-deletion-guard/01-PATTERNS.md (the `test_banned_patterns.py` section ~lines 254-281, incl. the day-one false-fail caveat) — the patterns + allow-list to author
    - specs/003-workflow-engine-decoupling/plan.md (line 919 R15, line 262 INV-13) — the exact banned-pattern list
    - backend/tests/agents/_scripted_model.py (line 49 path-root idiom) — repo/backend root resolution
  </read_first>
  <action>
    Create `test_banned_patterns.py` (D-14). FIRST re-verify the tree: grep `backend/` for `class DeepAgent` / `def deep_agent` — there should be ZERO today (the legacy class was removed in 002). Author HARD-BAN scanners over `backend/app` + `backend/agents` `.py` files for: `class\s+DeepAgent\b`, `def\s+deep_agent\b`, a NEW local module/package named `deepagents` or `langchain_deepagents` under our source tree (assert no such file/dir exists outside site-packages), and bespoke `for\s+_\s+in\s+range\(\s*max_iterations` agent loops. Allow-list `from deepagents import create_deep_agent` / `create_deep_agent(` ONLY in `app/agents/deep_agent_runner.py` (assert it appears nowhere else). If grep later finds a legacy `class DeepAgent`/`def deep_agent` (e.g. a transitional file resurfaces), allow-list that exact file explicitly with an inline comment marking it a deletion target — but per current verification none exists, so the default tree must pass with NO such allow-list. Seed INV-1 (`if pipeline_type ==` / `spec.id ==`) as a WARN-ONLY/documented pattern (it still lives in engine.py — 10 matches confirmed): collect matches and either `pytest.warns`/print a documented warning OR assert the count is BELOW a recorded ceiling — do NOT hard-fail in Phase 1 (D-15; hard-fail deferred to Phase 7). CRITICAL non-vacuous guard (T-04 mitigation): add `test_gate_catches_injected_hand_rolled_agent` that writes a temp `.py` file containing `class DeepAgent:` into a `tmp_path`, runs the SAME scanner over that temp dir, and asserts a violation is reported — proving the gate actually scans and would fail on a real regression. Keep offline/unmarked.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/agents/test_banned_patterns.py -q -rw</automated>
  </verify>
  <done>`pytest tests/agents/test_banned_patterns.py` exits 0 on the current tree: hard-ban scans find no hand-rolled deep agents; the sole `create_deep_agent` import is confirmed only in `deep_agent_runner.py`; INV-1 is warn-only (no hard fail); `test_gate_catches_injected_hand_rolled_agent` PASSES (proving the scanner fires on an injected `class DeepAgent` fixture — non-vacuous).</done>
</task>

<task type="auto">
  <name>Task 3: Wire all gates into .gitlab-ci.yml (lint stage + offline backend:characterization job)</name>
  <files>.gitlab-ci.yml</files>
  <read_first>
    - .gitlab-ci.yml (lines 28-41 `backend:lint`, lines 43-62 `backend:test`) — clone image/cache/rules EXACTLY; note backend:lint currently `pip install ruff==0.7.4` separately and runs `ruff check app/`
    - backend/requirements-dev.txt (Task 1 output) — the file the lint job must install to get import-linter + vulture
    - .planning/phases/01-0a-safety-net-deletion-guard/01-PATTERNS.md (the `.gitlab-ci.yml` section ~lines 352-389) — the exact job shapes
    - Confirm the test files exist on disk before writing the glob: tests/agents/test_characterization_*.py, tests/agents/test_migration_ledger.py, tests/agents/test_banned_patterns.py
  </read_first>
  <action>
    Extend `backend:lint`: change its `before_script` to also install dev deps (`pip install --cache-dir .pip-cache -r backend/requirements-dev.txt`) so `lint-imports` + `vulture` are available (today it only pip-installs `ruff==0.7.4`); add to `script` (after `ruff check app/`): `cd backend && lint-imports` and `cd backend && vulture app/ agents/`. Add a NEW job `backend:characterization` (stage `test`) cloning the `backend:test` skeleton (same `image: python:3.13-slim`, `cache: {key: pip-py313, paths: [.pip-cache/]}`, same `rules:` merge_request_event OR main) but OFFLINE — set only `ENV: "test"` (NO DATABASE_URL/SECRET_KEY/Bedrock vars needed since these tests never touch DB/network); `before_script: pip install --cache-dir .pip-cache -r backend/requirements.txt`; `script: cd backend && python -m pytest tests/agents/test_characterization_*.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -q`. Keep the ledger + banned-pattern tests in the TEST stage (hard ratchets, D-17). Do NOT remove or weaken the existing `backend:test`/`backend:lint`. VERIFY the YAML is valid and the referenced test globs resolve to real files.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin && python3.11 -c "import yaml; d=yaml.safe_load(open('.gitlab-ci.yml')); assert 'backend:characterization' in d, 'missing backend:characterization job'; j=d['backend:characterization']; s=' '.join(j['script']) if isinstance(j['script'],list) else j['script']; assert 'test_characterization_' in s and 'test_migration_ledger' in s and 'test_banned_patterns' in s, 'char job missing a gate'; lint=' '.join(d['backend:lint']['script']); assert 'lint-imports' in lint and 'vulture' in lint, 'lint job missing static gates'; print('CI wiring OK')" && cd backend && python -m pytest tests/agents/test_characterization_*.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -q</automated>
  </verify>
  <done>`.gitlab-ci.yml` is valid YAML; `backend:characterization` exists (stage `test`, offline) and its script runs the characterization + migration-ledger + banned-pattern tests; `backend:lint` script includes `lint-imports` + `vulture` and its before_script installs `requirements-dev.txt`; the pytest invocation those jobs run exits 0 locally. Existing `backend:test`/`backend:lint` are not weakened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → gate jobs | The phase's headline threats are CI-integrity: (a) a gate that silently no-ops / is never wired into CI (false-green), (b) the deepagents allow-list being too broad and letting a real hand-rolled deep agent through. This plan is where both are closed. No network/data/auth/runtime surface is added (test + CI config only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (false-green) | characterization/ledger/banned tests not actually run in CI | mitigate | Task 3 adds the offline `backend:characterization` job whose script explicitly lists all three test families; Task 3 verify parses `.gitlab-ci.yml` and asserts each gate appears in the job script — a missing wire fails the build step. |
| T-04-02 | Elevation of Privilege | banned-pattern allow-list too broad → real hand-rolled deep agent slips through | mitigate | Allow-list is the SINGLE file `app/agents/deep_agent_runner.py` for `create_deep_agent`; no legacy `deep_agent.py` allow-list is added (verified absent). `test_gate_catches_injected_hand_rolled_agent` proves the gate fires on an injected `class DeepAgent` — the allow-list cannot suppress a NEW violation. |
| T-04-03 | Tampering (false-green) | import-linter/vulture pass vacuously (wrong root_package / everything excluded) | mitigate | `[tool.importlinter] root_package = "agents"` targets the real kernel package; `lint-imports` exits 0 only by evaluating a real contract; vulture scans `app`+`agents` (not excluded). Static-gate verify runs both tools live. |
| T-04-04 | Tampering | banned-pattern gate false-FAILS on the sanctioned import (day-one breakage) | mitigate | The allow-list explicitly permits `create_deep_agent` in `deep_agent_runner.py`; Task 2 verify proves the current tree passes (exit 0) before CI wiring. |
| T-04-SC | Tampering | npm/pip/cargo installs (import-linter, vulture) | mitigate | Two PyPI dev tools added. They are widely-used, well-known packages (import-linter by Seddonym; vulture by jendrikseipp) — NOT [ASSUMED]/[SUS]; no RESEARCH Package Legitimacy table is required for established lint tooling. Pin EXACT versions (no ranges) per the requirements-dev header; verify on PyPI (`pypi.org/project/import-linter`, `pypi.org/project/vulture`) at author time. |
</threat_model>

<verification>
- `cd backend && lint-imports` exits 0 (scaffold contract green); `cd backend && vulture app/ agents/` exits 0 (allow-listed).
- `cd backend && python -m pytest tests/agents/test_banned_patterns.py -q` exits 0; the injected-fixture negative test proves the gate is non-vacuous.
- `.gitlab-ci.yml` is valid YAML; `backend:characterization` runs the three test families offline; `backend:lint` runs `lint-imports` + `vulture`.
- `cd backend && python -m pytest tests/agents/test_characterization_*.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -q` exits 0 (the exact CI invocation, run locally).
- No runtime module touched (`git diff --name-only` shows only `requirements-dev.txt`, `pyproject.toml`, the new test, and `.gitlab-ci.yml`).
</verification>

<success_criteria>
- SAFE-05: import-linter (kernel→ports scaffold, tighten-later) + vulture pinned, configured, green.
- SAFE-06: banned-pattern gate blocks hand-rolled deep agents (INV-13/R15), allow-lists the one sanctioned import, INV-1 warn-only, non-vacuous.
- SAFE-07: all four gate families run in CI — `lint-imports` + `vulture` in `backend:lint`; characterization + ledger + banned-pattern in the offline `backend:characterization` job.
- Zero runtime behavior change.
</success_criteria>

<output>
Create `.planning/phases/01-0a-safety-net-deletion-guard/01-04-SUMMARY.md` when done. Record: the exact pinned versions of import-linter + vulture, the import-linter contract type chosen (forbidden vs layers) + the documented tighten-later intent, the vulture allow-list mechanism, confirmation that no legacy `class DeepAgent` was found (so no legacy allow-list was needed), and the final `.gitlab-ci.yml` job names/scripts.
</output>
