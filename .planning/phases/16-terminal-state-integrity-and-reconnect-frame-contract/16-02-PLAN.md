---
phase: 16-terminal-state-integrity-and-reconnect-frame-contract
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/app/api/websocket.py
  - backend/tests/unit/test_pipeline_cancel.py
  - backend/tests/unit/test_run_revision_ws_dispatch.py
autonomous: true
requirements: [ISS-007, ISS-002]
must_haves:
  truths:
    - "A real cancel_pipeline delivers pipeline_cancelled on the live wire so the FE clears all in-flight agent cards, with the DB row 'cancelled' (ISS-007, ROADMAP SC2)"
    - "test_pipeline_cancel.py passes deterministically — the ack is no longer race-dependent (ISS-002, ROADMAP SC2)"
    - "A pre-agent cancel (engine.py:1509 break) emits pipeline_cancelled, not pipeline_complete"
    - "The WebSocketDisconnect path keeps destructive task.cancel() (no socket to ack); durable replay covers reconnect"
    - "The 5 characterization goldens stay byte/event-identical — the scripted model never cancels, so the cooperative arms are dormant (ROADMAP SC5)"
  artifacts:
    - path: "backend/app/api/websocket.py"
      provides: "Per-run cancel_event created + passed to engine.execute(...); cancel_pipeline handler calls cancel_event.set() instead of current_pipeline_task.cancel()"
      contains: "cancel_event"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "pre-agent cancel break (:1509) transitions to cancelled + emits pipeline_cancelled instead of falling through to pipeline_complete"
      contains: "pipeline_cancelled"
    - path: "backend/tests/unit/test_pipeline_cancel.py"
      provides: "Deterministic cancel-ack test asserting pipeline_cancelled reaches the WS"
      contains: "pipeline_cancelled"
  key_links:
    - from: "websocket.py cancel_pipeline handler"
      to: "engine.execute cooperative cancel_event (engine.py:523) → per-chunk check (:2327) + pre-agent check (:1509) → pipeline_cancelled (:1670-1678)"
      via: "cancel_event.set()"
      pattern: "cancel_event\\.set\\(\\)"
---

<objective>
Close ISS-007 + ISS-002 (ONE root cause): the `pipeline_cancelled` ack is never delivered on the live wire after `cancel_pipeline`, so the in-flight agent card stays "RUNNING" (007) and the cancel test is flaky (002).

The WS `cancel_pipeline` handler (`websocket.py:596-615`) cancels the run via destructive `current_pipeline_task.cancel()` (`:605`) instead of the engine's cooperative `cancel_event` (`engine.py:523`, checked per-chunk; clean terminal at `engine.py:1670-1678`). On cancel, the drainer's `except asyncio.CancelledError` (`websocket.py:1718-1722`) runs `pipeline_bg_task.cancel(); raise` and DIES before the bg task yields `pipeline_cancelled` — the frame lands on a queue no one drains. DB is correctly `cancelled`; the live wire gets no terminal → the FE card stays RUNNING. Mirrored in the revision drainer (`websocket.py:1963-2050`).

`engine.execute(..., cancel_event=...)` ALREADY accepts a cooperative event and checks it per-chunk (`:2327`) and pre-agent (`:1509`); the WS call sites (`:1469`, `:1937`) just don't pass one. Wire the cooperative path: create an `asyncio.Event` per run, pass it to `execute()` / `_handle_revision`, and make `cancel_pipeline` call `cancel_event.set()` (the bg task keeps running and emits `pipeline_cancelled` through the normal persisted+drained path; the drainer sends it and breaks on the terminal). Keep destructive `task.cancel()` ONLY on the `WebSocketDisconnect` path (`:1176-1196`). Close the engine pre-agent cancel gap (`:1509-1511` → emit `pipeline_cancelled`, not fall through to `pipeline_complete`). Adopt `asyncio.timeout()` in place of `asyncio.wait_for()` at the 3 drainer sites (`:1669/:802/:1999`) as defense-in-depth.

Per CONTEXT A3: the cooperative `cancel_event` is the PRIMARY root-cause fix; the WR-01-style residual-drain (`:2069-2084`) is the FALLBACK for any drainer where cooperative wiring is awkward — DO NOT ship both for the same path.

Purpose: a stopped run must clear in-flight cards (the FE must not be left lying).
Output: cooperative cancel wiring (engine + websocket) + the deterministic cancel tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-terminal-state-integrity-and-reconnect-frame-contract/16-CONTEXT.md
@.planning/IMPLEMENTATION-REGISTER.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the cooperative cancel_event (engine pre-agent gap + websocket cancel/drainer/timeout)</name>
  <files>backend/agents/execution_engine/engine.py, backend/app/api/websocket.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py — `execute(..., cancel_event=...)` signature `:517-523` (ALREADY accepts it; threaded to `_execute_impl` :562 / :729); the per-chunk cancel check `:2327`; the outer cooperative clean-terminal `except asyncio.CancelledError` `:1670-1678` (transitions "cancelled" + yields `pipeline_cancelled`); the PRE-AGENT cancel break `:1509-1511` (currently just `break`s and falls through to `pipeline_complete` at :1741 — THIS is the gap to close); the WR-03 gate-block→cancel precedent at the review-gate reject `:2647-2654` (emits pipeline_cancelled on a non-CancelledError terminal — mirror this for the pre-agent break).
    - backend/app/api/websocket.py — the `cancel_pipeline` handler `:596-615` (calls `current_pipeline_task.cancel()` at :605 — REPLACE with `cancel_event.set()`); the `_handle_workflow_execution` engine.execute call `:1469-1486` (does NOT pass cancel_event — ADD it); the main drainer `except asyncio.CancelledError` `:1718-1722` (`pipeline_bg_task.cancel(); raise` — the destructive path that dies before the terminal); the 3 `asyncio.wait_for(...get(), timeout=10.0)` drainer sites `:802` / `:1669` / `:1999` (swap to `asyncio.timeout()`); the WebSocketDisconnect path `:1176-1196` (KEEP destructive `task.cancel()` — no socket to ack); the revision drainer `:1963-2050` + the `_handle_revision` call `:1937-1945`; the WR-01 residual-drain precedent `:2059-2084`.
    - .planning/phases/16-terminal-state-integrity-and-reconnect-frame-contract/16-CONTEXT.md — decision A3 (the 5-step LOCKED fix + the "no dual mechanism per path" rule + the REJECTED hacks: "just swap wait_for→timeout and stop", sync ack in the cancel handler).
    - .planning/ISSUES-REGISTER.md — the ISS-007 / ISS-002 deep root-cause rows + the `/tmp/iss007_repro.py` three-layer repro reference.
  </read_first>
  <behavior>
    - cancel_pipeline (websocket.py:596-615): on a live run, call `cancel_event.set()` (cooperative) — NOT `current_pipeline_task.cancel()`. The bg task keeps running, the engine observes the event per-chunk / pre-agent, emits `pipeline_cancelled` through the normal persisted+drained path; the drainer forwards it and breaks on the terminal. Keep the idempotent no-active-pipeline ack branch (:606-614) unchanged.
    - _handle_workflow_execution + _handle_revision_execution: create one `asyncio.Event` per run, store a per-run handle (so cancel_pipeline can reach it), and pass `cancel_event=ev` into `engine.execute(...)` / `_handle_revision(...)`.
    - engine pre-agent cancel gap (engine.py:1509-1511): on the pre-agent `cancel_event.is_set()` break, transition the run to "cancelled" and emit `{"type":"pipeline_cancelled", "data":{"pipeline_run_id": ...}}` BEFORE leaving the loop — do not fall through to `pipeline_complete` (:1741). Mirror the outer cooperative terminal at :1670-1678 / the gate-reject precedent at :2650-2654.
    - The 3 drainer get-with-timeout sites use `asyncio.timeout()` (the correct CPython-3.11 primitive) in place of `asyncio.wait_for()` — defense-in-depth, removes the latent result-vs-cancel swallow.
    - WebSocketDisconnect path (:1176-1196): UNCHANGED — keep destructive `current_pipeline_task.cancel()` (durable replay covers reconnect; `test_disconnect_path_cancels_running_task` stays green).
    - SC-001: every cancel branch keys on the generic cooperative event / cancel signal — NO workflow/model/provider name. No new tables/migrations.
    - NO dual mechanism per path: choose the cooperative `cancel_event` as the single fix for both the main and revision drainers; only fall back to the WR-01 residual-drain for a specific drainer if cooperative wiring there proves awkward — and then do NOT also keep the destructive cancel for that same path.
  </behavior>
  <action>
    (1) In `_handle_workflow_execution` (websocket.py ~:1201) and `_handle_revision_execution` (~:1787), construct an `asyncio.Event()` per run before starting the bg task, hold it on the per-run handle the `cancel_pipeline` handler can resolve (alongside `current_pipeline_task` / `_PIPELINE_TASKS`), and pass `cancel_event=ev` into the `engine.execute(...)` call (:1469) and the `_handle_revision(...)` call (:1937). (2) In the `cancel_pipeline` handler (:596-615) replace `current_pipeline_task.cancel()` (:605) with `cancel_event.set()` for the resolved live run; leave the rejected-sync-ack comment intent honored (do NOT send a sync ack here — the bg task owns the ack); keep the idempotent no-pipeline branch. (3) In `engine.py` at the pre-agent break (:1509-1511), transition to "cancelled" via `self._state_machine.transition(...)` and `yield {"type":"pipeline_cancelled", ...}` before exiting — mirror :1670-1678; guard the transition like the gate-reject precedent (:2647-2649) so a double terminal can't throw StateMachineError. (4) Swap the 3 drainer `asyncio.wait_for(queue.get(), timeout=10.0)` sites (:802/:1669/:1999) to `async with asyncio.timeout(10.0): event = await queue.get()` (catch `asyncio.TimeoutError` exactly as today's heartbeat branch). (5) Leave the WebSocketDisconnect path (:1176-1196) destructive. Pick the cooperative path for BOTH drainers; do NOT also wire the WR-01 residual-drain on the cancel path (no dual mechanism). Avoid REJECTED hacks: "swap wait_for→timeout and stop" (insufficient — ack still 0/10), and a sync ack inside the cancel handler (races the bg task's own ack/DB write).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_pipeline_cancel.py -q</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n 'cancel_event' backend/app/api/websocket.py` shows an `asyncio.Event` created in BOTH `_handle_workflow_execution` and `_handle_revision_execution`, passed into `engine.execute(...)` / `_handle_revision(...)`, and `cancel_event.set()` called in the `cancel_pipeline` handler; `grep -n 'current_pipeline_task.cancel()' backend/app/api/websocket.py` shows it ONLY on the WebSocketDisconnect/error paths (:1180-1196), NOT in the `cancel_pipeline` handler.
    - SOURCE: the 3 drainer get sites use `asyncio.timeout(` (`grep -n 'asyncio.timeout(' backend/app/api/websocket.py` returns ≥3); the cancel-relevant `asyncio.wait_for(` at the queue-get drainers is gone (the post-drain `await asyncio.wait_for(bg_task, timeout=5.0)` join may remain — it is not a queue-get drainer).
    - SOURCE: `engine.py` pre-agent cancel break (:1509 region) now transitions to "cancelled" and yields `pipeline_cancelled` (`grep -n 'pipeline_cancelled' backend/agents/execution_engine/engine.py` shows a match in the pre-agent-break path in addition to the existing :1677 / :2650).
    - SOURCE (SC-001): no workflow/model/provider literal added to either cancel path; `grep -nE 'if pipeline_type ==|spec.id ==' backend/agents/execution_engine/engine.py` returns 0 (kernel banned-pattern unchanged).
    - SOURCE (no dual mechanism): the cancel path uses EITHER cooperative `cancel_event` OR a residual-drain per drainer, never both for the same path (manual diff review — documented in the SUMMARY).
    - TEST: `python3.11 -m pytest tests/unit/test_pipeline_cancel.py -q` PASSES deterministically (run 3x — `for i in 1 2 3; do python3.11 -m pytest tests/unit/test_pipeline_cancel.py -q || exit 1; done`); the previously-failing ack assertion now holds.
    - INV-3 parity proof (mandatory): `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -q` exits 0 (5 goldens byte/event-identical, SNAPSHOT_UPDATE UNSET); `/opt/homebrew/bin/lint-imports` (from backend/) exits 0 = 4 kept / 0 broken; `git status --porcelain` shows no new alembic/migration file.
  </acceptance_criteria>
  <done>cancel_pipeline sets the cooperative event; the engine emits pipeline_cancelled on both the per-chunk and pre-agent paths; the drainer forwards it before breaking; the disconnect path stays destructive; the 3 drainers use asyncio.timeout(); the cancel test passes deterministically; 5 goldens + banned + ledger + lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cancel-delivery tests — main ack + revision row 'cancelled' tightening</name>
  <files>backend/tests/unit/test_pipeline_cancel.py, backend/tests/unit/test_run_revision_ws_dispatch.py</files>
  <read_first>
    - backend/tests/unit/test_pipeline_cancel.py:170-191 — the current cancel scenario (drives `_handle_workflow_execution`, cancels the outer task, asserts exactly one `pipeline_cancelled` reached the FakeWebSocket + DB row `cancelled`); currently fails at the ack assertion. With the cooperative fix, restructure the trigger to a `cancel_pipeline` WS message (or `cancel_event.set()`) rather than `task.cancel()` so it mirrors the real Stop click; keep the `wr.status == "cancelled"` + ack assertions.
    - backend/tests/unit/test_run_revision_ws_dispatch.py:366 — `test_cancellation_lands_row_cancelled` (currently accepts the `pipeline_cancelled` frame stuck in-queue — tighten to REQUIRE it in the FakeWebSocket `sent` types); the drainer-frame contract assertions at :194-200 / :505-509 (section = target_artifact_type) for the wrapper shape.
    - /tmp/iss007_repro.py — the three-layer cancel repro (its structure: bg engine task → queue → drainer → WS; mirror this layering into the assertion that `pipeline_cancelled` REACHES the WS, not just the queue).
    - .planning/phases/16-terminal-state-integrity-and-reconnect-frame-contract/16-CONTEXT.md — the ISS-007/002 test note ("the ack now deterministic; tighten test_cancellation_lands_row_cancelled to require it in sent_types").
  </read_first>
  <behavior>
    - test_pipeline_cancel.py main ack test: triggering a cancel via the cooperative path delivers exactly one `pipeline_cancelled` to the WS (the FakeWebSocket `sent` list), the DB row is `cancelled`, deterministically across repeated runs.
    - test_run_revision_ws_dispatch.py::test_cancellation_lands_row_cancelled: assert `pipeline_cancelled` appears in the FakeWebSocket `sent` types (the live wire), not merely on the in-queue residue; the row is `cancelled`; the frame rides the drainer wrapper (section = target_artifact_type).
  </behavior>
  <action>
    Update `test_pipeline_cancel.py` so the cancel is triggered through the cooperative mechanism the fix introduces (send a `cancel_pipeline` message via the FakeWebSocket message loop, or set the per-run cancel_event) instead of `task.cancel()` on the outer drainer — this makes the test exercise the real ISS-007 path. Keep + green the existing assertions: exactly one `pipeline_cancelled` in `ws.sent`, `wr.status == "cancelled"`, `wr.completed_at is not None`. Tighten `test_run_revision_ws_dispatch.py::test_cancellation_lands_row_cancelled` so it requires `pipeline_cancelled` in the FakeWebSocket sent-types (delivered on the live wire), in addition to the row being `cancelled`; reuse the existing drainer-frame wrapper assertions. Do not modify production code in this task — it pins Task 1's behavior. Keep the `test_disconnect_path_cancels_running_task` test (if present) green (the disconnect path still destroys the task).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_pipeline_cancel.py tests/unit/test_run_revision_ws_dispatch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - TEST: `python3.11 -m pytest tests/unit/test_pipeline_cancel.py tests/unit/test_run_revision_ws_dispatch.py -q` PASSES; both the main cancel-ack test and `test_cancellation_lands_row_cancelled` assert `pipeline_cancelled` REACHES the WS sent-list (not the in-queue residue) AND the DB row is `cancelled`.
    - DETERMINISM: `for i in 1 2 3; do cd backend && python3.11 -m pytest tests/unit/test_pipeline_cancel.py -q || exit 1; done` passes all three iterations (no result-vs-cancel race).
    - The disconnect-path test (destructive cancel, no ack expected) stays green.
    - INV-3 parity proof (mandatory): `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -q` exits 0 (5 goldens byte/event-identical); `/opt/homebrew/bin/lint-imports` (from backend/) exits 0 = 4 kept / 0 broken.
  </acceptance_criteria>
  <done>The cancel tests assert live-wire delivery of pipeline_cancelled deterministically; the revision cancel test requires the frame in sent-types; the disconnect path stays destructive; 5 goldens + banned + ledger + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → WS (cancel_pipeline) | The cancel request crosses from an authenticated client; it must only cancel the requesting owner's run. |
| WS → engine | The cooperative `cancel_event` is an in-process signal; no untrusted payload crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-TENANT | EoP / Tampering | `cancel_pipeline` owner/workspace scoping | mitigate | The cooperative wiring must PRESERVE the existing per-connection run handle resolution — `cancel_event.set()` only fires for the run owned by THIS authenticated WS connection (the `current_pipeline_task` / per-run handle is already connection-scoped). Do NOT introduce a cross-connection lookup keyed on a client-supplied run id that could cancel another owner's run. Confirm the scoping is unchanged (no regression). |
| T-16-02-AUTHZ | Tampering | run-row authz on the pipeline_cancelled path | accept | The change moves WHEN/HOW `pipeline_cancelled` is emitted; it does NOT change the run-row write authz (the bg task's CancelledError handler / cooperative terminal already owns the owner-scoped `cancelled` persist). No authz delta. |
| T-16-02-DOS | DoS | repeated cancel_pipeline messages | accept | `cancel_event.set()` is idempotent; a second set is a no-op. The idempotent no-active-pipeline ack branch is preserved. |
| T-16-02-SC | Tampering | no package installs | accept | Zero new dependencies; zero migrations. |
</threat_model>

<verification>
- ISS-007 closed: a real cancel delivers `pipeline_cancelled` on the live wire (FE clears in-flight cards); DB row `cancelled`.
- ISS-002 closed: `test_pipeline_cancel.py` passes deterministically (no result-vs-cancel race).
- Pre-agent cancel gap closed: an `engine.py:1509` break emits `pipeline_cancelled`, not `pipeline_complete`.
- INV-3 parity (ROADMAP SC5): 5 goldens byte/event-identical; banned-patterns + migration-ledger green; lint-imports 4/0; zero new tables/migrations.
- SC-001: cancel branches key on the generic cooperative signal; no workflow/model/provider literal.
- Owner/workspace scoping on `cancel_pipeline` is preserved (no cross-tenant cancel).
</verification>

<success_criteria>
- ROADMAP SC2: a real `cancel_pipeline` delivers `pipeline_cancelled` on the live wire so the FE clears all in-flight agent cards, with the DB row `cancelled`; `test_pipeline_cancel.py` passes deterministically.
- ROADMAP SC5: INV-3 parity holds (5 goldens byte-identical, lint-imports 4/0, zero new tables/migrations).
</success_criteria>

<artifacts_this_phase_produces>
NEW:
- A per-run `asyncio.Event` cancel handle created in `_handle_workflow_execution` + `_handle_revision_execution`, plumbed to `engine.execute(...)` / `_handle_revision(...)`.
- The pre-agent `pipeline_cancelled` emission branch in `engine.py` (the closed cancel gap).

MODIFIED:
- `backend/app/api/websocket.py` — `cancel_pipeline` handler (cooperative `cancel_event.set()`); 3 drainer sites (`asyncio.timeout()`); the engine.execute / _handle_revision call sites (pass cancel_event). WebSocketDisconnect path unchanged.
- `backend/agents/execution_engine/engine.py` — pre-agent cancel break emits `pipeline_cancelled`.
- `backend/tests/unit/test_pipeline_cancel.py` — cancel-ack test now exercises the cooperative path, deterministic.
- `backend/tests/unit/test_run_revision_ws_dispatch.py` — `test_cancellation_lands_row_cancelled` requires the frame in sent-types.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/16-terminal-state-integrity-and-reconnect-frame-contract/16-02-SUMMARY.md` when done.
</output>
