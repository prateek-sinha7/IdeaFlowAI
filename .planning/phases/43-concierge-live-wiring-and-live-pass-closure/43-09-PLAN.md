---
phase: 43-concierge-live-wiring-and-live-pass-closure
plan: 09
type: execute
wave: 6
depends_on: ["43-07", "43-08"]
files_modified:
  - backend/app/api/websocket.py
  - frontend/src/hooks/useWebSocket.ts
  - frontend/src/lib/env.ts
  - .planning/ISSUES-REGISTER.md
  - .planning/FIX-REGISTER.md
  - .planning/IMPLEMENTATION-REGISTER.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [C.1, C.2, C.3, C.4]
user_setup:
  - service: aws-bedrock
    why: "The WS→SSE deletion is executed ONLY after the live SSE cutover (43-06) + all Part-B (43-07/08) validate; a final regression pass runs live"
    env_vars:
      - name: AWS_PROFILE (SSO)
        source: "aws sso login — hex-ai-fe SSO profile"
    dashboard_config:
      - task: "Confirm 43-06 cutover + all 43-07/43-08 Part-B checks passed with recorded evidence BEFORE deleting /ws/chat"
        location: "43-LIVE-EVIDENCE.md"
must_haves:
  truths:
    - "The legacy /ws/chat run-handler transport is DELETED (the INV-12 exit gate) ONLY after the SSE cutover + all Part-B validate"
    - "After deletion the 5 characterization goldens + wire-parity are re-run and stay green"
    - "The ISSUES / FIX / IMPLEMENTATION registers reconcile every live result (pass/issue) recorded in this phase"
    - "The deferred-items.md sweep across every phase reconciles items closed/carried by this live pass"
    - "The v2.0 milestone is completed (and the v1.0 close-out reconciled) with no un-run Part-B item marked done"
  artifacts:
    - path: ".planning/ISSUES-REGISTER.md"
      provides: "Reconciled issue dispositions from the live pass (ISS-003/004/005/006/011/027/029/031/032/033/034)"
      contains: "ISS-033"
  key_links:
    - from: "the live SSE cutover + Part-B evidence"
      to: "/ws/chat deletion (INV-12 exit gate)"
      via: "supervised gate — delete only after validation"
      pattern: "ws/chat"
    - from: "43-LIVE-EVIDENCE.md pass/issue records"
      to: "ISSUES / FIX / IMPLEMENTATION registers + ROADMAP"
      via: "reconciliation sweep"
      pattern: "register"
---

<objective>
Close the milestone: the WS→SSE deletion (C.3, the big INV-12/INV-3 exit gate), the register
reconciliation (C.1), the deferred-items sweep (C.2), and `/gsd-complete-milestone` for v2.0
(C.4). Per the A.0 decision + LOCK-B, `/ws/chat` is deleted ONLY after the live SSE cutover
(43-06) AND all Part-B checks (43-07/43-08) validate the SSE path holds — a risk-reduction
sequencing of "delete after the cutover validates." This is the milestone's exit gate.

Purpose: Deliver the INV-12 exit gate (no dual transport), reconcile every live result into the
registers, sweep the deferred-items across all phases, and complete v2.0. Traces the POR
close-out (§7). Autonomous:false — the WS deletion is a supervised gate contingent on recorded
live evidence, and a final live regression pass runs after deletion.

Output: `/ws/chat` deleted + goldens/wire-parity re-green; reconciled ISSUES/FIX/IMPLEMENTATION
registers + ROADMAP; a deferred-items sweep; and the v2.0 milestone completed.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: C.3 — delete /ws/chat (INV-12 exit gate) after the SSE cutover + Part-B validate</name>
  <what-built>The SSE transport (43-06) validated live (B.3) + all Part-B (43-07/08) recorded pass. This gate deletes the legacy WS run-handler transport and re-proves parity.</what-built>
  <read_first>
    - backend/app/api/websocket.py (the /ws/chat run-handlers — the transport being deleted; CHAT-07 scope)
    - frontend/src/hooks/useWebSocket.ts + frontend/src/lib/env.ts:48-77 (the FE legacy transport + the flag being retired)
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §3 (C.3) + §7 (INV-12/INV-3/LOCK-B) + §5 (CHAT-07 grep ratchets + ledger row)
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md (confirm B.3 + all Part-B passed BEFORE deleting)
  </read_first>
  <action>
    GATE: verify in 43-LIVE-EVIDENCE.md that the 43-06 cutover (B.3) AND every 43-07/43-08 Part-B check
    recorded PASS with evidence. Only then: delete the legacy `/ws/chat` run-handler transport in
    websocket.py (the CHAT-07 scope — the run-pipeline/message inbound handlers; keep `websocket_handoff.py`
    + inbound MCP untouched per CHAT-07), retire the FE legacy `useWebSocket` chat wiring + the
    `NEXT_PUBLIC_SSE_TRANSPORT` flag branch (SSE is now the sole transport), add the grep ratchets that assert
    the deleted symbols are gone, and flip the migration-ledger row for CHAT-07/INV-12. Re-run the 5
    characterization goldens + the wire-parity suite AFTER deletion and confirm green (INV-3 — any live-pass
    code change re-runs the goldens). Then run a final live smoke over SSE (a run + a Concierge ask) to prove
    the deletion did not regress the live path. This is supervised — do NOT delete on offline evidence alone.
  </action>
  <acceptance_criteria>
    - GATE evidence: 43-LIVE-EVIDENCE.md shows B.3 + all Part-B PASS before deletion (cited in the SUMMARY).
    - /ws/chat run-handlers deleted: grep the CHAT-07 deleted symbols in websocket.py → 0 (ratchet added); handoff WS + inbound MCP untouched.
    - FE legacy chat transport + SSE flag branch retired (grep the retired symbols → 0); SSE is the sole path.
    - 5 characterization goldens + wire-parity green AFTER deletion (`git diff --stat golden/` empty; test_wire_parity green).
    - Final live smoke (a run + a Concierge ask over SSE) recorded PASS.
    - `lint-imports` 4 kept / 0 broken.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_wire_parity.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <resume-signal>Type "approved" (deletion validated + goldens green + live smoke passed), or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: C.1 + C.2 — reconcile ISSUES/FIX/IMPLEMENTATION registers + sweep deferred-items across all phases</name>
  <files>.planning/ISSUES-REGISTER.md, .planning/FIX-REGISTER.md, .planning/IMPLEMENTATION-REGISTER.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md (all Part-B pass/issue records)
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §3 (C.1/C.2) + §5 (the exhaustive live-deferred register to reconcile)
    - .planning/IMPLEMENTATION-REGISTER.md (consult at the ISS/live-deferred anchors cited in §5 — L2317-2332, L2593-2595 — NOT wholesale)
  </read_first>
  <action>
    C.1: For every live result recorded in 43-LIVE-EVIDENCE.md, update the registers: ISSUES-REGISTER.md
    (dispose ISS-003/004/005/006/011/027/029/031/032/033/034 — close, carry, or re-open with the live
    evidence), FIX-REGISTER.md (append any fixes filed+landed during the live pass), IMPLEMENTATION-REGISTER.md
    (record the A.1/A.3/A.4/A.x wiring + the WS deletion as the INV-12 exit gate), REQUIREMENTS.md (mark
    CONC-01 + LIVE-02 + UPLD-02 complete). C.2: Sweep every phase's deferred-items.md — reconcile the items
    this live pass closed (DEF-29-09-1, DEF-30-03-1, WR-02, ISS-033/034, the Phase-32/37/38 live-deferred
    halves, P16 SC1/SC2 default re-confirm, ISS-004 live re-confirm) vs those explicitly carried beyond the
    milestone (LOCK-E: ND-4/7/10/12/13.6/7, the Handoff screen). Update ROADMAP: mark Phase 43 complete and
    Phase 34 superseded-by-43. Do NOT close any item whose 43-LIVE-EVIDENCE.md record is an unresolved issue.
  </action>
  <acceptance_criteria>
    - Every ISS-* named in CONTEXT §5 has a disposition line referencing the 43-LIVE-EVIDENCE.md evidence.
    - REQUIREMENTS.md CONC-01 / LIVE-02 / UPLD-02 marked complete (grep the status markers).
    - The deferred-items sweep records each closed item + each LOCK-E carried item (no silent gaps).
    - ROADMAP: Phase 43 marked complete; Phase 34 marked superseded-by-43.
    - No item with an unresolved 43-LIVE-EVIDENCE.md issue is marked closed.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "43-LIVE-EVIDENCE\|ISS-033\|ISS-034\|LIVE-02" .planning/ISSUES-REGISTER.md .planning/REQUIREMENTS.md</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: C.4 — complete the v2.0 milestone + reconcile the v1.0 close-out</name>
  <what-built>The registers + ROADMAP reconciled (Task 2); the exit gate landed (Task 1). This runs the milestone completion.</what-built>
  <action>
    Confirm every Part-B item is PASS in 43-LIVE-EVIDENCE.md (no un-run/open-issue item), then run /gsd-complete-milestone for v2.0 and reconcile the v1.0 close-out per POR §7; update STATE.md + ROADMAP. Do NOT complete the milestone on any offline-only or unresolved-issue item.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §3 (C.4) + §9 (do NOT mark the milestone complete on any un-run Part-B item)
    - .planning/STATE.md (milestone: v2.0)
  </read_first>
  <how-to-verify>
    Confirm ALL Part-B items (B.1-B.7 + default re-confirms) recorded PASS in 43-LIVE-EVIDENCE.md (no un-run
    or unresolved-issue item). Then run `/gsd-complete-milestone` for v2.0 and reconcile the v1.0 close-out
    per POR §7. Verify STATE.md + ROADMAP reflect the milestone completion.
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE: every Part-B item is PASS in 43-LIVE-EVIDENCE.md before completion (no un-run item, no open issue).
    - v2.0 milestone completed; v1.0 close-out reconciled; STATE.md/ROADMAP updated.
    - The milestone is NOT marked complete on any offline-only or unresolved-issue Part-B item.
  </acceptance_criteria>
  <resume-signal>Type "approved" (all Part-B PASS, milestone completed), or describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transport deletion | Deleting /ws/chat while SSE is the sole live path — a premature deletion would strand live users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-09-PREMATURE | Denial of service | /ws/chat deletion | mitigate | Deletion gated on recorded 43-06 (B.3) + all Part-B PASS evidence; goldens + wire-parity re-run + a live smoke after deletion |
| T-43-09-PARITY | Tampering | post-deletion behavior | mitigate | 5 goldens + wire-parity re-run green after deletion (INV-3) |
| T-43-09-EXPOSE-SSE | Elevation | SSE as sole transport | mitigate | The SSE path's owner-gating + confirm-hold (43-01 H1) + nonce hardening (43-03) already landed and validated before deletion |
| T-43-09-SC | Tampering | package installs | accept | No new packages; deletion + doc reconciliation only |
</threat_model>

<verification>
- /ws/chat deleted only after the recorded live validation; goldens + wire-parity green after deletion; live smoke PASS.
- Registers + deferred-items + ROADMAP reconciled against 43-LIVE-EVIDENCE.md.
- v2.0 milestone completed with no un-run/open-issue Part-B item.
- `lint-imports` 4/0.
</verification>

<success_criteria>
- C.3: the INV-12 exit gate — /ws/chat deleted, SSE the sole transport, parity re-proven.
- C.1/C.2: registers + deferred-items reconciled with the live evidence.
- C.4: v2.0 milestone completed; v1.0 close-out reconciled.
</success_criteria>

## Artifacts this phase produces
- The /ws/chat deletion + grep ratchets + the CHAT-07/INV-12 ledger-row flip (the INV-12 exit gate).
- Reconciled ISSUES / FIX / IMPLEMENTATION registers + REQUIREMENTS (CONC-01/LIVE-02/UPLD-02 complete) + ROADMAP (Phase 43 complete, Phase 34 superseded).
- The cross-phase deferred-items sweep record.
- v2.0 milestone completion + v1.0 close-out reconciliation.

<output>
Create `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-09-SUMMARY.md` when done.
</output>
