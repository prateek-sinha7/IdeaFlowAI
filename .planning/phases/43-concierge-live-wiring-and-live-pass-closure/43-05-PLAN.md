---
phase: 43-concierge-live-wiring-and-live-pass-closure
plan: 05
type: execute
wave: 2
depends_on: ["43-01", "43-03"]
files_modified:
  - backend/app/api/run_commands.py
  - backend/agents/execution_engine/engine.py
  - backend/app/api/chat_router.py
  - backend/tests/unit/test_run_message_images.py
  - backend/tests/agents/test_steering_seam.py
autonomous: true
requirements: [A.3]
must_haves:
  truths:
    - "A running run registers its live in-process ExecutionContext so _live_ectx_for_run resolves it (not None)"
    - "A queued steering note is drained into the NEXT agent dispatch as a === USER GUIDANCE === block"
    - "A per-turn image sent to an already-running run is delivered onto that run's next dispatch (closes DEF-30-03-1 / DEF-29-09-1)"
    - "The steering + per-turn-image delivery keys on the generic run/ectx registry only (SC-001/INV-1)"
    - "The 5 characterization goldens stay byte-identical (steering/images fire on zero golden paths)"
  artifacts:
    - path: "backend/app/api/run_commands.py"
      provides: "live-ectx registry — _live_ectx_for_run resolves the running in-process ectx"
      contains: "_live_ectx_for_run"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "steering_notes + pending_turn_images drain onto the next dispatch"
      contains: "USER GUIDANCE"
  key_links:
    - from: "run_commands.py::post_message (CHANNEL_STEERING / concierge steering)"
      to: "the running run's live ectx"
      via: "_live_ectx_for_run registry lookup"
      pattern: "_live_ectx_for_run"
    - from: "engine.py::_compose_context_message (next dispatch)"
      to: "ectx.steering_notes / ectx.pending_turn_images"
      via: "consume-once drain"
      pattern: "steering_notes"
---

<objective>
Land the live-ectx registry so mid-run steering and per-turn images reach the running run
(A.3). Today `_live_ectx_for_run` (run_commands.py:361) unconditionally `return None` (:373),
so `apply_steering` and `apply_turn_images` no-op against a live run — the durable
`chat_message` row is written but nothing reaches the next agent dispatch. Register each
running run's in-process `ExecutionContext` so `_live_ectx_for_run` resolves it, and drain the
queued `steering_notes` (as a `=== USER GUIDANCE ===` block) and `pending_turn_images` onto the
next agent dispatch. This ONE seam closes both the steering deferral (DEF-29-09-1) and the
per-turn-image deferral (DEF-30-03-1).

Purpose: B.2 (mid-run steering lands in the next agent's live prompt) and the per-turn-image
half of B.1 both depend on this live handle. Traces REQUIREMENTS CHAT-03 (consume-once steering
seam) + UPLD-02 (per-turn image carrier live end-to-end). This plan edits run_commands.py AND
engine.py, both touched by earlier plans — depends_on 43-01 (run_commands.py) and 43-03
(engine.py) to serialize the file ownership (no same-wave conflict).

Output: A live-ectx registry + the engine-side drain, proven offline (the seam is already
proven at the router by test_run_message_images / test_steering_seam; this plan proves the live
handle resolves and the drain fires), goldens byte-identical, lint 4/0.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md
@.planning/ROADMAP.md
@backend/CLAUDE.md
@backend/app/api/run_commands.py
@backend/app/api/chat_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-ectx registry — _live_ectx_for_run resolves the running in-process ectx</name>
  <files>backend/app/api/run_commands.py, backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/app/api/run_commands.py:361-373 (_live_ectx_for_run — return None) + :764-781 (CHANNEL_STEERING apply_turn_images) + :815-820 (concierge ectx = _live_ectx_for_run)
    - backend/app/api/chat_router.py:325-360 (apply_steering appends to ectx.steering_notes; apply_turn_images enqueues onto ectx.pending_turn_images)
    - backend/agents/execution_engine/engine.py (execute() run setup — where the per-run ExecutionContext is created; the 12-09 injected engine→WS live-task bridge pattern for a process-local registry)
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §A.3 + §4.3 (steering + per-turn images share one seam)
  </read_first>
  <action>
    Introduce a process-local live-ectx registry keyed by run_id (a module-level dict guarded for the
    single-process runtime, mirroring the existing _PIPELINE_TASKS / _CANCEL_EVENTS registries in
    run_commands.py). At run start the engine REGISTERS the run's in-process ExecutionContext into this
    registry (via an injected register/unregister callback so no engine → app import edge is created —
    reuse the 12-09 injected-bridge pattern); at terminal/teardown it UNREGISTERS (finally-block, no leak).
    Rewrite `_live_ectx_for_run` to return the registered ectx for run_id, or None if the run is not live
    in this process (degrade-safe — the durable chat_message row remains the record). Keying is generic
    (run_id only) — no workflow-name branch (SC-001/INV-1). Do NOT change the durable-persist ordering in
    post_message (the chat_message row is still written first).
  </action>
  <acceptance_criteria>
    - `_live_ectx_for_run` returns a registered ectx (grep it no longer unconditionally returns None; a
      registry lookup is present).
    - Source assertion: the engine registers/unregisters via an injected callback — grep
      `import.*app\.api\.run_commands` inside agents/execution_engine/ → 0 (no engine→app import).
    - Test: after registering a fake ectx for a run_id, `_live_ectx_for_run(run_id)` returns it; after
      unregister (or terminal teardown) it returns None (no leak).
    - `lint-imports` 4 kept / 0 broken.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_run_message_images.py tests/agents/test_steering_seam.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Engine drain — steering_notes (=== USER GUIDANCE ===) + per-turn images onto the next dispatch</name>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/test_steering_seam.py, backend/tests/unit/test_run_message_images.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py (_compose_context_message / the next-dispatch context builder — where a === USER GUIDANCE === block and turn images should be consumed)
    - backend/app/api/chat_router.py:325-360 (the queue shapes: ectx.steering_notes list, ectx.pending_turn_images list; the one-shot turn_images_once carrier)
    - backend/CLAUDE.md (the === CURRENT TASK === injection idiom the build loop already uses — mirror for === USER GUIDANCE ===)
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §A.3
  </read_first>
  <action>
    At the next agent dispatch's context assembly, DRAIN the run's `ectx.steering_notes` (consume-once) and
    render them as a `=== USER GUIDANCE ===` block prepended/appended per the existing redo idiom, then clear
    the queue so a note is applied exactly once (ND-9/ND-11). In the SAME drain, move `ectx.pending_turn_images`
    onto the one-shot `ectx.turn_images_once` carrier the composer already reads (rendered once at the next
    dispatch, then cleared — payload-transient, ND-10). Both drains key on the generic ectx queues only
    (SC-001/INV-1 — no workflow-name branch). Guard against an empty/absent queue (no block emitted when
    nothing is queued — so non-steering runs are byte-identical, INV-3). This makes the offline-proven
    apply_steering / apply_turn_images seams actually reach the model once the live handle (Task 1) resolves.
  </action>
  <acceptance_criteria>
    - Source assertion: the next-dispatch context builder emits a `=== USER GUIDANCE ===` block when
      steering_notes is non-empty and clears the queue (grep `USER GUIDANCE` + a queue-clear).
    - Test: a steering note queued on a fake ectx appears in the composed next-dispatch context exactly once,
      and is absent on the dispatch after (consume-once).
    - Test: a per-turn image queued on pending_turn_images is moved to turn_images_once and rendered on the
      next dispatch, then cleared (payload-transient).
    - Test: with no queued steering/images, the composed context is byte-identical to the pre-change baseline
      (no spurious block) — reuse the steering-seam parity assertion.
    - 5 characterization goldens byte-identical (`git diff --stat backend/tests/agents/characterization/golden/` empty).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_steering_seam.py tests/unit/test_run_message_images.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py -q</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat steering/image turn → running run's ectx | Untrusted user text/images injected into the next agent prompt |
| live-ectx registry | Process-local run→ectx map; a wrong-run lookup would cross-inject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05-XINJECT | Tampering | _live_ectx_for_run | mitigate | Registry keyed by run_id; post_message already owner-gated the run (404) BEFORE resolving the ectx, so a caller only reaches their own run's ectx |
| T-43-05-IMGCAP | Denial of service | per-turn images | mitigate | Images already cap-validated by _validate_images at ingress (run_commands.py:685-693) BEFORE queueing; payload-transient (no persistence) |
| T-43-05-GUIDANCE | Tampering | === USER GUIDANCE === block | accept | Steering text is the user's own guidance to their own run; consume-once bounds replay; no privilege change |
| T-43-05-IMPORT | Tampering | engine↔registry wiring | mitigate | Registry register/unregister injected as callbacks; import-linter asserts no engine→app edge |
| T-43-05-LEAK | Denial of service | registry growth | mitigate | Unregister in the run teardown finally-block; no unbounded growth |
| T-43-05-SC | Tampering | package installs | accept | No new packages introduced this plan |
</threat_model>

<verification>
- `_live_ectx_for_run` resolves the running ectx; registry unregisters on teardown (no leak).
- Steering note drained as === USER GUIDANCE === exactly once; per-turn image delivered once.
- No-steering runs byte-identical; 5 goldens byte-identical.
- No engine→app import edge; `lint-imports` 4/0.
</verification>

<success_criteria>
- Mid-run steering reaches the next agent's live prompt (B.2 enabled).
- Per-turn image delivered to an already-running run (DEF-30-03-1 / DEF-29-09-1 closed; B.1 per-turn enabled).
- Goldens byte-identical; SC-001/INV-1 generic keying; lint 4/0.
</success_criteria>

## Artifacts this phase produces
- A process-local live-ectx registry (run_id → ExecutionContext) in run_commands.py + injected register/unregister callbacks on the engine.
- The rewritten `_live_ectx_for_run` (resolves the running ectx).
- The engine next-dispatch drain: `=== USER GUIDANCE ===` steering block + `turn_images_once` per-turn image carrier consumption.

<output>
Create `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-05-SUMMARY.md` when done.
</output>
