---
phase: 43-concierge-live-wiring-and-live-pass-closure
plan: 07
type: execute
wave: 4
depends_on: ["43-06"]
files_modified:
  - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md
autonomous: false
requirements: [B.1, B.2, B.4]
user_setup:
  - service: aws-bedrock
    why: "B.1/B.2/B.4 run against real Haiku on Bedrock over the live SSE transport"
    env_vars:
      - name: AWS_PROFILE (SSO)
        source: "aws sso login — hex-ai-fe SSO profile (leave ANTHROPIC_API_KEY empty)"
      - name: BEDROCK_PROMPT_CACHE_ENABLED
        source: "ON since P26"
    dashboard_config:
      - task: "alembic upgrade head; uvicorn app.main:app (127.0.0.1:8000, python3.11); next dev with NEXT_PUBLIC_SSE_TRANSPORT=1; QA login qa-enterprise@flowinqa.com / flowin-e2e-pass"
        location: "local dev (CONTEXT §8)"
must_haves:
  truths:
    - "B.1: a live multi-turn chat with images (per-turn + document image paths) reaches the model on a real run"
    - "B.2: a mid-run steering instruction lands in the NEXT agent's live prompt === USER GUIDANCE === block"
    - "B.4: cache_read > 0 is observed, including multi-turn cache-point placement, and the ISS-033 direct-call agents' tokens are now counted"
    - "No live check is marked done without recorded evidence (frames / cache_read values / agent_input fields)"
  artifacts:
    - path: ".planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md"
      provides: "Recorded evidence for B.1 / B.2 / B.4 (pass / issue per check)"
      contains: "cache_read"
  key_links:
    - from: "live Bedrock run"
      to: "recorded evidence (frames, cache_read, agent_input)"
      via: "supervised live check"
      pattern: "cache_read"
---

<objective>
Drive the live Part-B checks that ride the run/chat/steering/cache seams wired in Part A:
B.1 (multi-turn chat WITH images), B.2 (mid-run steering in the next agent's live prompt), and
B.4 (`cache_read>0` incl. multi-turn cache-point placement + the ISS-033 direct-call agents now
counted). Each is a SUPERVISED live check on a real Bedrock run over the SSE transport (flipped
ON in 43-06); each records EVIDENCE proving pass, or files an issue.

Purpose: The offline halves are proven (43-01..43-05); these are the live confirmations that the
milestone-end pass requires. Traces REQUIREMENTS LIVE-02 (multi-turn chat with images, mid-run
steering observed in next dispatch, cache_read>0 with multi-turn cache placement) + UPLD-02
(per-turn image live) + CHAT-03 (steering seam). Autonomous:false — needs Bedrock SSO + running
uvicorn/next/alembic (CONTEXT §8). Do NOT mark any item done on offline evidence.

Output: `43-LIVE-EVIDENCE.md` with a pass/issue record + captured evidence for B.1/B.2/B.4.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: B.1 — live multi-turn chat with images (per-turn + document image paths)</name>
  <what-built>The per-turn image live-delivery seam (43-05) + the chat backbone over live SSE (43-06). Run-entry image was already live-proven (quick-260710-ftq, image_count=1); this confirms the per-turn + document image paths.</what-built>
  <action>
    Drive the supervised B.1 live check per the steps below and record the agent_input / image content-block evidence in 43-LIVE-EVIDENCE.md. Any miss → file an issue, root-cause, fix, re-run.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §2 (B.1) + §5 (Phase-30 per-turn/document image)
    - .planning/REQUIREMENTS.md UPLD-02
  </read_first>
  <how-to-verify>
    On a live run (prerequisites per CONTEXT §8): 1. Send a multi-turn chat where a LATER turn attaches an
    image (per-turn image path) to an already-running run. 2. Confirm the image reaches the model on the
    next dispatch (the agent's input carries the image content block). 3. Repeat with a document-attached
    image path. Capture the agent_input / HumanMessage content-block evidence showing the image arrived
    (e.g. image_count>0 in the spec-writer/next agent input, as with the run-entry proof).
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE: the next agent's live input shows the per-turn image arrived (image content block / image_count>0), recorded in 43-LIVE-EVIDENCE.md.
    - EVIDENCE: the document-image path similarly delivered.
    - Any failure filed as an issue (root-caused, fixed, re-run) — not marked pass.
  </acceptance_criteria>
  <resume-signal>Type "approved" with evidence, or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: B.2 — mid-run steering lands in the next agent's live === USER GUIDANCE === block</name>
  <what-built>The live-ectx registry + engine steering drain (43-05).</what-built>
  <action>
    Drive the supervised B.2 live check per the steps below and record the next agent's === USER GUIDANCE === block evidence in 43-LIVE-EVIDENCE.md. Any miss → file an issue, fix, re-run.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §2 (B.2)
    - .planning/REQUIREMENTS.md CHAT-03
  </read_first>
  <how-to-verify>
    On a live multi-agent run: 1. While agent N is running (or between agents), send a steering instruction
    in the chat lane. 2. Confirm the NEXT agent's live prompt contains a `=== USER GUIDANCE ===` block with
    that instruction. 3. Confirm it is consumed ONCE (not repeated on the agent after). Capture the next
    agent's composed input (the block text) as evidence.
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE: the next agent's live input contains the === USER GUIDANCE === block with the sent instruction (captured in 43-LIVE-EVIDENCE.md).
    - EVIDENCE: consume-once confirmed (absent on the following dispatch).
    - Failure → filed as issue, fixed, re-run.
  </acceptance_criteria>
  <resume-signal>Type "approved" with evidence, or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: B.4 — cache_read>0 (multi-turn cache placement) + ISS-033 agents counted</name>
  <what-built>The shared cached-invoke helper (43-04) routing SmartPlanner/ClarifyEngine/handoff Test+Compliance through cache-eligible, token-counted calls.</what-built>
  <action>
    Drive the supervised B.4 live check per the steps below and record the cache_read values + per-agent token counts in 43-LIVE-EVIDENCE.md; record the ISS-031/032/033/034 disposition. Any miss → file an issue, fix, re-run.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §2 (B.4) + §5 (ISS-031/032/033/034)
    - .planning/REQUIREMENTS.md LIVE-02
  </read_first>
  <how-to-verify>
    On a live run with BEDROCK_PROMPT_CACHE_ENABLED ON: 1. Observe the usage/telemetry and confirm
    `cache_read > 0` on repeated-prefix turns (the P26 cache actually fires on Bedrock). 2. Confirm multi-turn
    cache-point PLACEMENT (a later turn reads the cached prefix). 3. Confirm the SmartPlanner / ClarifyEngine /
    handoff Test+Compliance token usage now appears in the run's cost accounting (previously uncounted, ISS-033/034).
    Capture the cache_read numbers + the per-agent token counts as evidence.
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE: recorded cache_read>0 values incl. a multi-turn read (43-LIVE-EVIDENCE.md).
    - EVIDENCE: the previously-uncounted direct-call agents' tokens now appear in the run cost.
    - Disposition recorded for ISS-031/032/033/034 (close or re-disposition with the live evidence).
    - Failure → filed as issue, fixed, re-run.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the cache_read + token-count evidence, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live user chat/images → running run | Untrusted content injected into live prompts (bounded by the Part-A mitigations) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07-IMGCAP | Denial of service | live per-turn images | mitigate | Caps enforced at ingress (_validate_images) — verified live that an over-cap image is rejected |
| T-43-07-EVID | Repudiation | live-check claims | mitigate | Every pass requires recorded evidence in 43-LIVE-EVIDENCE.md; no un-evidenced "done" |
| T-43-07-SC | Tampering | package installs | accept | No new packages; live verification only |
</threat_model>

<verification>
- B.1/B.2/B.4 each recorded PASS with captured evidence, or filed as an issue.
- ISS-031/032/033/034 disposition recorded with live evidence.
- No offline-only completion of any live item.
</verification>

<success_criteria>
- B.1 (multi-turn chat + per-turn/document images) live-proven with evidence.
- B.2 (mid-run steering in next agent's live prompt) live-proven with evidence.
- B.4 (cache_read>0 + multi-turn placement + ISS-033 agents counted) live-proven with evidence.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md` — the recorded evidence log for B.1 / B.2 / B.4 (shared with 43-08).

<output>
Create `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-07-SUMMARY.md` when done, citing the evidence entries.
</output>
