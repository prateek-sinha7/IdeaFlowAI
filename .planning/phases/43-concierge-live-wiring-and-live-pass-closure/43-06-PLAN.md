---
phase: 43-concierge-live-wiring-and-live-pass-closure
plan: 06
type: execute
wave: 3
depends_on: ["43-01", "43-02", "43-03", "43-04", "43-05"]
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/.env.local
autonomous: false
requirements: [A.0, A.2, B.3]
user_setup:
  - service: aws-bedrock
    why: "The live Concierge Q&A check (B.3) runs against real Haiku on Bedrock"
    env_vars:
      - name: AWS_PROFILE (SSO)
        source: "aws sso login — the hex-ai-fe SSO profile (leave ANTHROPIC_API_KEY empty)"
      - name: NEXT_PUBLIC_SSE_TRANSPORT
        source: "set to 1/true to flip the FE onto the SSE transport (the supervised cutover)"
      - name: BEDROCK_PROMPT_CACHE_ENABLED
        source: "ON since P26"
    dashboard_config:
      - task: "Run alembic upgrade head, then uvicorn app.main:app (127.0.0.1:8000, python3.11) + next dev; QA login qa-enterprise@flowinqa.com / flowin-e2e-pass"
        location: "local dev environment (see CONTEXT §8 prerequisites)"
must_haves:
  truths:
    - "RunConnectionProvider is mounted in app/layout.tsx behind NEXT_PUBLIC_SSE_TRANSPORT (A.2)"
    - "With the flag ON, useRunChat.sendMessage routes to POST /messages (the SSE up-channel), reaching the Concierge"
    - "B.3: a live settled-run ask receives a grounded, non-hallucinated answer from the real run data, verified with recorded evidence"
    - "Consequential proposals execute ONLY through the existing channels behind confirm chips (nothing auto-executes)"
    - "This is the ONE supervised transport flip (LOCK-B); /ws/chat is NOT deleted in this plan (that is the 43-09 exit gate)"
  artifacts:
    - path: "frontend/src/app/layout.tsx"
      provides: "RunConnectionProvider mount behind the SSE flag (A.2)"
      contains: "RunConnectionProvider"
  key_links:
    - from: "app/layout.tsx"
      to: "RunConnectionProvider (flag-gated)"
      via: "provider mount enabling the SSE up/down channel"
      pattern: "RunConnectionProvider"
    - from: "useRunChat.sendMessage({concierge:true})"
      to: "POST /api/runs/{id}/messages → CHANNEL_CONCIERGE → converse"
      via: "SSE transport ON"
      pattern: "concierge"
---

<objective>
Execute the ONE supervised SSE transport cutover (A.0(a) realized + A.2) and verify the live
Concierge Q&A (B.3). Per the A.0 RESOLVED decision, Part A wired the Concierge offline-correct
against the `POST /messages` contract with the SSE path DARK; this plan mounts
`RunConnectionProvider` in `app/layout.tsx` behind `NEXT_PUBLIC_SSE_TRANSPORT` and enables the
flag so `useRunChat.sendMessage` routes to `POST /messages` — the ONLY path that reaches the
`chat:concierge` subsystem. Then, on a real Bedrock run, verify the Concierge answers a settled-
run ask with grounded, non-hallucinated data and that proposals execute only behind confirm chips.

Purpose: This is the supervised transport flip (LOCK-B) that unblocks the live Part-B checks.
B.3 is verified AS PART OF this cutover per the A.0 decision. The `/ws/chat` deletion (INV-12
exit gate, C.3) is deliberately NOT here — it is the final 43-09 step, after ALL of Part B
validates the SSE path holds (a risk-reduction refinement of "delete after the cutover validates").

Output: The provider mount + flag flip, and a recorded B.3 evidence capture (the live Concierge
answer + proof it grounded in real run data + proof no proposal auto-executed).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md
@.planning/ROADMAP.md
@frontend/src/app/layout.tsx
@frontend/src/providers/RunConnectionProvider.tsx
@frontend/src/lib/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount RunConnectionProvider behind the SSE flag (A.2)</name>
  <files>frontend/src/app/layout.tsx, frontend/.env.local</files>
  <read_first>
    - frontend/src/app/layout.tsx (current provider tree — SkillsHooksProvider at :35; RunConnectionProvider NOT mounted)
    - frontend/src/providers/RunConnectionProvider.tsx:55-90 (the provider contract + the inert DEFAULT_VALUE when unmounted)
    - frontend/src/lib/env.ts:48-77 (SSE_TRANSPORT flag resolution)
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §A.0 + §A.2 (the supervised cutover)
  </read_first>
  <action>
    Mount `RunConnectionProvider` in the app/layout.tsx provider tree so the SSE up/down channel is available
    app-wide. Gate the ACTIVE behavior on `ENV.SSE_TRANSPORT` (the provider already returns an inert
    DEFAULT_VALUE when disabled, so mounting it unconditionally is safe — when the flag is OFF the WS path is
    unchanged; INV-3/LOCK-B). Set `NEXT_PUBLIC_SSE_TRANSPORT=1` in `.env.local` to FLIP the transport ON for
    the supervised cutover. Do NOT delete or alter the WS path (it stays intact behind the flag until 43-09).
    Confirm the mount does not break SSR (the provider is a client component).
  </action>
  <acceptance_criteria>
    - `RunConnectionProvider` appears in app/layout.tsx's provider tree (grep present).
    - `NEXT_PUBLIC_SSE_TRANSPORT` is set to 1/true in .env.local.
    - `cd frontend && npx tsc --noEmit` clean.
    - The WS path is untouched (grep confirms no deletion of the useWebSocket/legacy transport wiring).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: B.3 — live Concierge Q&A (grounded, confirm-gated) — SUPERVISED</name>
  <what-built>
    The SSE transport is flipped ON (Task 1) so useRunChat.sendMessage posts to POST /messages, reaching the
    Concierge (43-01 backend defects fixed, 43-02 FE re-routing wired). This checkpoint verifies the live
    behavior on real Bedrock.
  </what-built>
  <action>
    Drive the supervised B.3 live check on real Bedrock per the steps below, then record the captured evidence (grounded answer + confirm-gated proposals) in the SUMMARY. On any hallucination/auto-execute/mis-route, file an issue instead of marking done.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §2 (B.3) + §8 (prerequisites) + §7 (INV-13, confirm-gated proposals)
  </read_first>
  <how-to-verify>
    Prerequisites (user runs — CONTEXT §8): `aws sso login`; `alembic upgrade head`; backend
    `uvicorn app.main:app --host 127.0.0.1 --port 8000` (python3.11); `next dev`; QA login
    qa-enterprise@flowinqa.com / flowin-e2e-pass; NEXT_PUBLIC_SSE_TRANSPORT=1, BEDROCK_PROMPT_CACHE_ENABLED ON.
    1. Launch a real prototype (or any) run to completion (settled).
    2. In the run chat lane, ask a STATUS/CONTENT question grounded in the run ("what did the run produce?",
       "which agents ran?", "what's in the deliverable?"). CONFIRM it is ANSWERED by the Concierge (a
       chat_reply), NOT launched as a *_revision run.
    3. Verify the answer is GROUNDED in the real run data (names/artifacts/gate history that actually exist)
       and NON-HALLUCINATED — cross-check against the run's Steps/Files/Audit tabs.
    4. Trigger a consequential proposal (ask for a change / gate action). CONFIRM it surfaces as a HELD
       proposal behind a confirm chip and executes ONLY on confirm (nothing auto-executes).
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE captured: the Concierge answer text + a side-by-side of the run's real data proving grounding
      (screenshot or transcript). A wrong/hallucinated answer → FILE AS ISSUE (do not mark done).
    - EVIDENCE: the settled-run ask produced a chat_reply, NOT a new revision run row.
    - EVIDENCE: a consequential proposal was HELD (confirm chip shown) and executed only after confirm.
    - Record PASS / ISSUE with the evidence in the SUMMARY. Do NOT mark B.3 done without recorded evidence.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the evidence attached, or describe the issue to file.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE (SSE up-channel) → POST /messages | Now the live transport; all confirm/concierge/steering turns cross here |
| Concierge model ← run content | Untrusted run content grounds answers; must not follow embedded instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06-EXPOSE | Elevation | SSE transport flip | mitigate | The cutover is supervised (checkpoint gate); the backend confirm-hold (43-01 H1) + owner-gating (404) already enforce that a live turn cannot forge an action |
| T-43-06-HALLUC | Tampering | Concierge answer | mitigate | B.3 evidence check requires grounding in real run data; a hallucinated answer is filed as an issue, not accepted |
| T-43-06-AUTOEXEC | Elevation | proposals | mitigate | B.3 evidence check confirms consequential proposals execute only behind confirm chips (INV-13/CONC-01) |
| T-43-06-INJ | Tampering | run content → prompt | accept | Untrusted-content preamble + proposal-only tools bound the blast radius to a confirm-gated proposal |
| T-43-06-SC | Tampering | package installs | accept | No new packages introduced this plan |
</threat_model>

<verification>
- RunConnectionProvider mounted behind the flag; flag ON in .env.local; `tsc --noEmit` clean.
- B.3 verified live with recorded evidence (grounded answer + confirm-gated proposals).
- WS path intact (deletion deferred to 43-09).
- LOCK-B: this is the single supervised transport flip.
</verification>

<success_criteria>
- SSE transport flipped ON via the supervised cutover; the Concierge is reachable live.
- B.3 passes with recorded evidence (grounded, non-hallucinated, confirm-gated).
- No un-evidenced live claim; /ws/chat NOT yet deleted.
</success_criteria>

## Artifacts this phase produces
- The `RunConnectionProvider` mount in app/layout.tsx (A.2) — the SSE provider now available app-wide behind the flag.
- The B.3 live evidence record (in the SUMMARY).

<output>
Create `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-06-SUMMARY.md` when done, including the B.3 evidence capture.
</output>
