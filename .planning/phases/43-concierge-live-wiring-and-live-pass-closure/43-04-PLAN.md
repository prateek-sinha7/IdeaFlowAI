---
phase: 43-concierge-live-wiring-and-live-pass-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/agents/cached_invoke.py
  - backend/agents/planner/smart_planner.py
  - backend/agents/execution_engine/clarify_engine.py
  - backend/app/agents/handoff/classifier.py
  - backend/app/agents/handoff/compliance_agent.py
  - backend/tests/unit/test_cached_invoke.py
autonomous: true
requirements: [A.x]
must_haves:
  truths:
    - "SmartPlanner, ClarifyEngine, and the handoff Test/Compliance calls route their model call through ONE shared cached-invoke helper"
    - "The shared helper places Bedrock prompt-cache points so a repeated system/context prefix is cache-eligible"
    - "The tokens consumed by those direct-call agents are counted (surfaced in the run's usage accounting), not silently dropped"
    - "No direct build_model().ainvoke / self._llm.ainvoke bypass remains at the routed sites"
    - "INV-13 respected: the helper reaches Bedrock via the sanctioned model path, not a hand-rolled agent loop"
    - "The 5 characterization goldens stay byte-identical"
  artifacts:
    - path: "backend/app/agents/cached_invoke.py"
      provides: "Shared cached-invoke helper (cache-point placement + token counting) — ISS-033"
      contains: "def cached_invoke"
  key_links:
    - from: "smart_planner.py::SmartPlanner.plan"
      to: "cached_invoke"
      via: "routed model call (was self._llm.ainvoke)"
      pattern: "cached_invoke"
    - from: "clarify_engine.py (LLM branch)"
      to: "cached_invoke"
      via: "routed model call (was build_model().ainvoke)"
      pattern: "cached_invoke"
---

<objective>
Build the shared cached-invoke helper (A.x — RESOLVED 2026-07-15 → BUILD NOW; ISS-033) and
route the direct model calls through it. Today SmartPlanner (smart_planner.py:~389,
`self._llm.ainvoke`), ClarifyEngine (clarify_engine.py:~492, `build_model().ainvoke`), and the
handoff Test/Compliance agents call the model DIRECTLY — so they get NO Bedrock prompt-caching
(the P26 cache-points middleware lives only in the DeepAgentRunner/deepagents path) and their
tokens are UNCOUNTED in run cost. Route all of them through ONE helper that places cache points
and counts tokens.

Purpose: These direct calls are the standing `cache_read>0` gap and a cost-accounting hole.
Fixing them feeds the B.4 live check (cache_read>0 incl. multi-turn cache-point placement) and
closes ISS-033 (ISS-034 dollar-display rides alongside). INV-13: the helper reaches Bedrock via
the sanctioned model factory path — it does NOT hand-roll an agent loop or call create_deep_agent.

Output: `app/agents/cached_invoke.py` (cache-point placement + token counting), the three
call-sites routed through it, and the tokens surfaced in the run's usage accounting — proven
offline (targeted tests, goldens byte-identical, lint 4/0).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md
@.planning/ROADMAP.md
@backend/CLAUDE.md
@backend/agents/planner/smart_planner.py
@backend/agents/execution_engine/clarify_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared cached-invoke helper (cache-point placement + token counting)</name>
  <files>backend/app/agents/cached_invoke.py, backend/tests/unit/test_cached_invoke.py</files>
  <read_first>
    - backend/app/agents/model_factory.py (build_model — the sanctioned provider select; where Bedrock is built)
    - backend/app/agents/deep_agent_runner.py (how the P26 cache-points middleware places cache markers today — mirror that placement so a repeated prefix is cache-eligible)
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §A.x + §2 (B.4 cache_read>0)
    - .planning/IMPLEMENTATION-REGISTER.md (consult ONLY at the ISS-031/032/033 anchor L2593-2595 for the prior cache-point disposition — do not read wholesale)
  </read_first>
  <action>
    Create `app/agents/cached_invoke.py` exposing an async `cached_invoke(...)` that takes a system/context
    prefix + the per-call message(s) + an optional model id + an optional usage sink, builds the model via
    the sanctioned `build_model` (INV-13 — no create_deep_agent, no hand-rolled loop), places Bedrock
    prompt-cache points on the STABLE prefix (the same cache-marker mechanism the DeepAgentRunner uses so a
    repeated system/context prefix across turns is cache-eligible — multi-turn cache-point placement), invokes
    the model, and RETURNS both the raw text and the usage metadata. Route the returned usage into the run's
    usage accounting via the injected sink (so these tokens are COUNTED, closing ISS-034's counting half).
    Degrade-safe: when caching is unavailable/off, it still invokes and counts (no crash). Keep it provider-
    agnostic (works for the local ChatAnthropic dev path and ChatBedrockConverse prod path). Do NOT re-add a
    summarizer. This is a thin invoke helper, not an agent.
  </action>
  <acceptance_criteria>
    - `cached_invoke` exists and calls `build_model` (grep `build_model` in cached_invoke.py); grep for
      `create_deep_agent` in cached_invoke.py → 0 (INV-13).
    - Test: with a scripted model recording its received messages, `cached_invoke` places a cache marker on
      the stable prefix (assert the marker/`cache_control`-style field is present on the prefix block) and
      returns `(text, usage)` with the usage metadata populated.
    - Test: the injected usage sink receives the call's token counts (counting proven offline).
    - `lint-imports` 4 kept / 0 broken.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_cached_invoke.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Route SmartPlanner + ClarifyEngine + handoff Test/Compliance through the helper</name>
  <files>backend/agents/planner/smart_planner.py, backend/agents/execution_engine/clarify_engine.py, backend/app/agents/handoff/classifier.py, backend/app/agents/handoff/compliance_agent.py, backend/tests/unit/test_cached_invoke.py</files>
  <read_first>
    - backend/agents/planner/smart_planner.py:383-400 (plan() — self._llm.ainvoke direct call)
    - backend/agents/execution_engine/clarify_engine.py:488-500 (build_model().ainvoke direct call)
    - backend/app/agents/handoff/classifier.py + compliance_agent.py (grep the direct ainvoke/build_model calls — the Test/Compliance model calls)
    - backend/CLAUDE.md (import-linter: smart_planner/clarify_engine live under agents/ — verify routing to app.agents.cached_invoke does not break the 4 contracts; if it would, inject the helper rather than importing app.* from the kernel package)
  </read_first>
  <action>
    Replace each direct `self._llm.ainvoke([...])` / `build_model(...).ainvoke([...])` at the SmartPlanner,
    ClarifyEngine, and handoff Test/Compliance sites with a call to the shared `cached_invoke`, passing the
    stable system/context prefix separately from the per-call content so the prefix is cache-eligible, and
    threading the run's usage sink so tokens are counted. Preserve each site's existing JSON-extraction /
    tolerant-parse / static-fallback behavior byte-for-byte (INV-3 — the parsed OUTPUT must be identical for
    the same model text; only the invoke path changes). CRITICAL import-linter guard: `agents/planner` and
    `agents/execution_engine` are kernel-side packages that must not import `app.*` directly. If a direct
    import of `app.agents.cached_invoke` from those packages would add a forbidden edge, inject the helper as
    a dependency (constructor/param) the app layer supplies — mirror how the engine already receives app-side
    services. Verify with `lint-imports` after wiring. Delete the now-dead direct-invoke lines (no dual path).
  </action>
  <acceptance_criteria>
    - Source assertion: grep the routed sites for a remaining direct `self._llm.ainvoke` / `build_model(`
      immediately-followed-by-`.ainvoke` at those specific call points → 0 (routed through cached_invoke or an
      injected helper).
    - Test: SmartPlanner.plan and the ClarifyEngine LLM branch produce the SAME parsed dict/array for a given
      scripted model output as before (parity — reuse/extend the existing planner/clarify tests).
    - Test: the run usage sink accumulates the tokens from these calls (counting closed).
    - 5 characterization goldens byte-identical (`git diff --stat backend/tests/agents/characterization/golden/` empty).
    - `lint-imports` 4 kept / 0 broken (the routing did NOT add a kernel → app edge).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_cached_invoke.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brief/clarify input → model prompt | Untrusted user text forms the per-call content (already handled by the existing prompts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04-INV13 | Tampering | cached_invoke | mitigate | Helper builds the model via the sanctioned build_model path; grep asserts no create_deep_agent / hand-rolled loop |
| T-43-04-IMPORT | Tampering | kernel→app routing | mitigate | Route via injected helper if a direct import would break import-linter; `lint-imports` asserts 4/0 |
| T-43-04-PARITY | Tampering | planner/clarify output | mitigate | Parsed output byte-identical for the same model text (INV-3); goldens byte-identical |
| T-43-04-SC | Tampering | package installs | accept | No new packages introduced (reuses langchain-core / model_factory) |
</threat_model>

<verification>
- All three direct-call sites routed through the shared helper; no direct-invoke bypass remains.
- Cache points placed on the stable prefix; tokens counted via the usage sink.
- Parsed outputs byte-identical (INV-3); 5 goldens byte-identical.
- INV-13 respected; `lint-imports` 4/0.
</verification>

<success_criteria>
- SmartPlanner / ClarifyEngine / handoff Test+Compliance route through the shared cached-invoke helper.
- Prefix is cache-eligible; tokens are counted (feeds B.4).
- Goldens byte-identical; INV-13 held; lint 4/0.
</success_criteria>

## Artifacts this phase produces
- `app/agents/cached_invoke.py` with `cached_invoke(...)` — the shared cache-point-placing, token-counting model-invoke helper (ISS-033).
- The routed call-sites (SmartPlanner, ClarifyEngine, handoff Test/Compliance) now cache-eligible + token-counted.

<output>
Create `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-04-SUMMARY.md` when done.
</output>
