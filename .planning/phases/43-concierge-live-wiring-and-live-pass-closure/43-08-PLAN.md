---
phase: 43-concierge-live-wiring-and-live-pass-closure
plan: 08
type: execute
wave: 5
depends_on: ["43-06", "43-07"]
files_modified:
  - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md
autonomous: false
requirements: [B.5, B.6, B.7]
user_setup:
  - service: aws-bedrock
    why: "B.5/B.6/B.7 + default-profile re-confirms run against real Haiku on Bedrock over live SSE"
    env_vars:
      - name: AWS_PROFILE (SSO)
        source: "aws sso login — hex-ai-fe SSO profile (leave ANTHROPIC_API_KEY empty)"
    dashboard_config:
      - task: "alembic upgrade head; uvicorn app.main:app (127.0.0.1:8000, python3.11); next dev with NEXT_PUBLIC_SSE_TRANSPORT=1; QA login qa-enterprise@flowinqa.com / flowin-e2e-pass"
        location: "local dev (CONTEXT §8)"
must_haves:
  truths:
    - "B.5: the unified LaunchWizard live-launches prototype + ppt through /dashboard (Phase-41 single-screen Configure was reverted; they route back to LaunchWizard)"
    - "B.6: GET /api/analytics/summary returns a live HTTP round-trip AND a notification PUSHes over the real connection"
    - "B.7: the live Playwright chat suite is green (the mocked run-screen suite is already 132/0/42)"
    - "The default-profile live re-confirms (ISS-003/004/011/027, P16 SC1/SC2) pass on the default model profile"
    - "No live check is marked done without recorded evidence"
  artifacts:
    - path: ".planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md"
      provides: "Recorded evidence for B.5 / B.6 / B.7 + the default-profile re-confirms"
      contains: "analytics"
  key_links:
    - from: "live Bedrock run + Playwright"
      to: "recorded evidence (launch, analytics round-trip, notification push, suite green)"
      via: "supervised live check"
      pattern: "analytics"
---

<objective>
Drive the remaining live Part-B checks whose offline halves already landed: B.5 (unified
LaunchWizard live launch of prototype + ppt), B.6 (live analytics round-trip + notification
PUSH), B.7 (live Playwright chat suite green), plus the `default`-profile live re-confirms
(ISS-003/004/011/027, P16 SC1/SC2). Each is a SUPERVISED live check recording EVIDENCE.

Purpose: These complete the milestone-end live pass. Note (B.5): the Phase-41 single-screen
Configure was BUILT then REVERTED (quick-260713-rcf) because a bare `prototype` fails the backend
`missing_template_context` guard — so prototype/ppt route back to `LaunchWizard`; verify the
LaunchWizard path, not the reverted Configure. Traces REQUIREMENTS LIVE-02 (Playwright live
suite) + the Phase-37/38 offline-done items (SHELL-04 launch, analytics/notifications).
Autonomous:false — needs Bedrock SSO + running servers (CONTEXT §8).

Output: `43-LIVE-EVIDENCE.md` appended with pass/issue records + evidence for B.5/B.6/B.7 + the
default-profile re-confirms.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: B.5 — unified LaunchWizard live launch (prototype + ppt)</name>
  <what-built>The Phase-37 LaunchWizard (offline byte-identical, WR-01). Phase-41 single-screen Configure was reverted; prototype/ppt route back to LaunchWizard.</what-built>
  <action>
    Drive the supervised B.5 live check per the steps below and record the launched run ids + state in 43-LIVE-EVIDENCE.md. Any missing_template_context/launch failure → file an issue, fix, re-run.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §2 (B.5) + §5 (Phase-37 live create→launch; register L3519-3527 the revert)
  </read_first>
  <how-to-verify>
    On live dev: 1. From /dashboard launch a prototype via the unified LaunchWizard → confirm it actually
    starts a real run (not a missing_template_context failure). 2. Launch a ppt the same way → confirm it runs.
    Capture the launched run ids + the run reaching a non-failed running/complete state.
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE: prototype + ppt each launched via LaunchWizard reach a real running/complete run (run ids recorded in 43-LIVE-EVIDENCE.md), no missing_template_context failure.
    - Failure → filed as issue, fixed, re-run.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the run-id evidence, or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: B.6 — live analytics round-trip + notification PUSH</name>
  <what-built>Phase-38 analytics/notifications (offline half done).</what-built>
  <action>
    Drive the supervised B.6 live check per the steps below and record the analytics HTTP response + the pushed notification in 43-LIVE-EVIDENCE.md. Any miss → file an issue, fix, re-run.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §2 (B.6) + §5 (Phase-38 live notification PUSH + analytics round-trip L3475)
  </read_first>
  <how-to-verify>
    On live dev: 1. Load the Analytics page → confirm `GET /api/analytics/summary` returns a live HTTP 200
    with real data (not seeded/mock). 2. Trigger an event that PUSHes a notification (e.g. a run completes)
    → confirm the notification arrives over the real connection (SSE/live), not a poll. Capture the HTTP
    response + the pushed notification.
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE: GET /api/analytics/summary live 200 with real values (recorded).
    - EVIDENCE: a notification PUSHed over the live connection on a real event (recorded).
    - Failure → filed as issue, fixed, re-run.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the analytics + notification evidence, or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: B.7 — live Playwright chat suite green + default-profile re-confirms</name>
  <what-built>The mocked run-screen suite is already 132/0/42 (Phase 42). This confirms the LIVE chat suite + the default-profile re-confirms.</what-built>
  <action>
    Drive the supervised B.7 live check + the default-profile re-confirms per the steps below and record the suite result + each re-confirm outcome in 43-LIVE-EVIDENCE.md. Classify each spec red (fix / reconcile / flake) — never delete a spec. Any miss → file an issue, fix, re-run.
  </action>
  <read_first>
    - .planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-CONTEXT.md §2 (B.7) + §2 default-profile re-confirms (ISS-003/004/011/027, P16 SC1/SC2) + §5 (register L3515-3517 the ts-a/b/c/e reds + ts-l fixme)
  </read_first>
  <how-to-verify>
    On live dev: 1. Run the LIVE Playwright chat suite against the live backend → confirm green (or triage
    each red as selector-drift-fix vs changed-behavior-reconcile vs harness-flake; never delete a spec).
    2. Re-confirm on the `default` model profile: ISS-003 (token-delta A/B), ISS-004 (0 tool-XML in live
    sdlc-governance chunks), ISS-011 (Phase-8 HITL 3 cases), ISS-027 ("Skip all & run directly"), P16 SC1/SC2
    (ValidationException→pipeline_failed / Stop→pipeline_cancelled). Capture the suite result + each
    re-confirm outcome.
  </how-to-verify>
  <acceptance_criteria>
    - EVIDENCE: the live Playwright chat suite result (green, or a triaged red list with dispositions) recorded.
    - EVIDENCE: each default-profile re-confirm (ISS-003/004/011/027, P16 SC1/SC2) recorded pass/issue.
    - Failure → filed as issue, fixed, re-run (spec reds classified, never deleted).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the suite + re-confirm evidence, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live analytics/notification endpoints → client | Owner-scoped reads; a cross-owner leak would be a defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08-ANALYTICS-IDOR | Information disclosure | GET /api/analytics/summary | mitigate | Verify live that the summary is owner-scoped (no cross-owner data); endpoint unchanged since Phase 38, re-confirm live |
| T-43-08-EVID | Repudiation | live-check claims | mitigate | Every pass requires recorded evidence; no un-evidenced "done" |
| T-43-08-SC | Tampering | package installs | accept | No new packages; live verification only |
</threat_model>

<verification>
- B.5/B.6/B.7 each recorded PASS with evidence, or filed as an issue.
- Default-profile re-confirms (ISS-003/004/011/027, P16 SC1/SC2) recorded.
- No offline-only completion of any live item.
</verification>

<success_criteria>
- B.5 (LaunchWizard live launch), B.6 (analytics round-trip + notification push), B.7 (live Playwright chat suite) live-proven with evidence.
- Default-profile re-confirms recorded pass/issue.
</success_criteria>

## Artifacts this phase produces
- Appended entries in `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-LIVE-EVIDENCE.md` for B.5 / B.6 / B.7 + the default-profile re-confirms.

<output>
Create `.planning/phases/43-concierge-live-wiring-and-live-pass-closure/43-08-SUMMARY.md` when done, citing the evidence entries.
</output>
