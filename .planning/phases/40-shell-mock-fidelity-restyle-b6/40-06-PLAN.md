---
phase: 40-shell-mock-fidelity-restyle-b6
plan: 06
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/components/history/WorkflowHistory.tsx
  - frontend/src/components/history/WorkflowHistory.test.tsx
  - frontend/e2e/tests/ts-t.history.spec.ts
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]
must_haves:
  truths:
    - "The History surface keeps the title 'Run History' (ND-W — app vocabulary), not the mock's 'Workflow History'"
    - "It renders the mock's type-filter chips + Sort tabs + TODAY/EARLIER/OLDER date groups with status/token/version badges"
    - "The populated, filter-empty, and zero states each match the mock (captured via the seeded runs from 40-01)"
    - "All rows are live/seeded run data (ND-D); the family grouping + sort behavior is preserved"
  artifacts:
    - path: "frontend/src/components/history/WorkflowHistory.tsx"
      provides: "Restyled History (Run History title + filter chips + Sort tabs + date groups + status badges)"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/history/WorkflowHistory.tsx"
      to: "/api/runs"
      via: "live run list → date-bucketed family groups"
      pattern: "api/runs"
---

<intended_divergence_note>
Inherit ND-A..D + ND-W..Z from `40-UI-SPEC.md`. This surface exercises: **ND-W** — the title stays "Run History" (matches the app's profile-menu label), NOT the mock's "Workflow History"; and ND-D — all rows are live/seeded run data, never the mock's fixed 9-run list. The filter/sort/date-group skeleton already exists; this is a restyle to the mock's chip/tab/group/badge composition. Any OTHER visual departure (beyond the title + live rows) is a fidelity bug.
</intended_divergence_note>

<objective>
Restyle the Workflow History surface to the mock (CONTEXT specifics #5), keeping the title "**Run History**" (ND-W). Bring the type-filter chips, the Sort tabs, the TODAY/EARLIER/OLDER date-grouped rows, and the status/token/version badges to mock parity, over the live/seeded `/api/runs` data (ND-D). Capture the **populated**, **filter-empty**, and **zero** states. Preserve the existing family grouping, sort, search, delete, and revision behavior. Re-anchor the `ts-t.history` mocked-e2e spec to the restyled surface.

Purpose: History is a high-traffic list surface — its chips/sort/groups/badges must match the mock.
Output: `WorkflowHistory` restyled to the mock across its three states; ts-t re-anchored.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-CONTEXT.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-UI-SPEC.md
@frontend/src/components/history/WorkflowHistory.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle History to the mock (keep 'Run History' — ND-W) + re-anchor ts-t.history</name>
  <files>frontend/src/components/history/WorkflowHistory.tsx, frontend/src/components/history/WorkflowHistory.test.tsx, frontend/e2e/tests/ts-t.history.spec.ts</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (History `sc-if isHistory` :361 — the type-filter chips + Sort tabs + TODAY/EARLIER/OLDER grouped rows with status/token/version badges; the filter-empty `histFilterEmpty` :423 + zero `histZero` :426 states)
    - frontend/src/components/history/WorkflowHistory.tsx (the "Run History" title :767 — KEEP per ND-W; `filterType`/`searchQuery`/`sortKey` :123-127; the family-grouped date buckets via `RevisionFamilyView` `bucketAndSortFamilies`/`FamilyGroupCard` :26; the `/api/runs` fetch; the empty-state copy)
    - frontend/src/components/history/RevisionFamilyView.tsx (the `FamilyGroupCard` + `bucketAndSortFamilies` the restyle composes over — do NOT change the grouping contract, only its presentation)
    - frontend/e2e/tests/ts-t.history.spec.ts (the stale History locators to re-anchor to the restyled chips/tabs/groups)
  </read_first>
  <action>
    Restyle `WorkflowHistory` to the mock's History composition, KEEPING the "Run History" title (:767 — ND-W, do NOT change to "Workflow History"). Restyle the type-filter chips (from `filterType`), the Sort tabs (from `sortKey`), and the TODAY/EARLIER/OLDER date-grouped rows (the existing `bucketAndSortFamilies` buckets) to the mock's chip/tab/group chrome; restyle each run row to the mock's composition — title, humanized type, relative time, a status badge, token count, and the revision/version chip — reusing the existing `FamilyGroupCard`/family data (do NOT change the grouping contract, only its presentation). Restyle the filter-empty + zero empty-states to the mock (`histFilterEmpty` / `histZero`). Route all color/type through the existing tokens; keep the search, delete, revise, and reopen behaviors. Then RE-ANCHOR `ts-t.history.spec.ts` locators to the restyled chips/tabs/groups (the Phase-32/39 re-anchor pattern) — only update locators/assertions to the new layout, do NOT weaken the behavioral assertions. NO fenced code in this action — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/history__shell.png` + `history__shellfull.png` (populated, over the 40-01 seeded runs) vs the `Hexaware Workspace v2.dc.html` History surface, plus the filter-empty + zero captures, via the 40-01 oracle, closed to ND-W (title) + ND-D (live rows). This autonomous task generates the current/* captures mechanically; the human approval is the trailing checkpoint.
    - The title renders "Run History" (grep → present; "Workflow History" absent; ND-W); the chips/Sort tabs/date groups render from live state (vitest: seeded runs → grouped rows + status/token/version badges).
    - `ts-t.history.spec.ts` PASSES against the restyled surface (mocked project) — the re-anchored locators resolve, the filter/sort/group assertions hold.
    - `npx --prefix frontend tsc --noEmit` clean; `WorkflowHistory.test.tsx` green (title + chips/sort/groups + empty-state cases).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "Run History" src/components/history/WorkflowHistory.tsx | grep -vqx 0 && npx vitest run src/components/history/WorkflowHistory.test.tsx && npx playwright test --project=mocked ts-t.history</automated>
  </verify>
  <done>History matches the mock across populated/filter-empty/zero with the "Run History" title kept (ND-W); ts-t re-anchored green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of Workflow History (3 states) vs the mock (closed to ND-A..D + ND-W..Z)</name>
  <files>frontend/e2e/fidelity/gallery-shell.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (D40-5; ND-D live/seeded rows make a pixel-diff-vs-mock invalid). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/history*, over the seeded runs), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-shell-gallery.mjs --surface history`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Workflow History surface across populated / filter-empty / zero — the "Run History" title (ND-W), type-filter chips, Sort tabs, TODAY/EARLIER/OLDER date groups, and status/token/version badges — with the prior task's mechanical gates already green (npx tsc --noEmit clean, WorkflowHistory vitest green, ts-t re-anchored green).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-shell.html and find the `history` sections (populated + empty states). Compare the mock (left) against ours (right). Confirm every visible difference is a registered divergence — ND-W (our title "Run History" vs the mock's "Workflow History", captioned "expected — app vocabulary"), ND-D (our seeded live rows vs the mock's fixed 9); any OTHER visual departure (chip/tab/group/badge chrome, row composition, spacing) is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if Workflow History matches its mock across the three states closed to the intended-divergence register ("Run History" kept per ND-W), or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| run history data → list rows | server-provided run titles/errors rendered in the UI |
| delete / revise → REST | authenticated owner-scoped mutations |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06-01 | Tampering (XSS) | run-row titles / error text | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new History code (grep → 0) |
| T-40-06-02 | Spoofing | delete / revise / reopen | accept | reuses the existing owner-scoped mutations (IDOR→404 already enforced); no new authority path |
| T-40-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs (reuses lucide-react + tokens). |
</threat_model>

<verification>
- History matches the mock across populated/filter-empty/zero per the shell-gallery oracle, closed to ND-W (title) + ND-D (live rows).
- The family grouping/sort/search/delete behavior is preserved; ts-t re-anchored green.
- `npx tsc --noEmit` clean; WorkflowHistory vitest green.
</verification>

<success_criteria>
SHELL-01 (SC-1): the History surface (3 states) matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
SHELL-02 (SC-2): the filter chips / Sort tabs / date groups / badges land.
SHELL-03 (SC-3): live data (ND-D); title kept (ND-W); existing behavior reused.
SHELL-04 (SC-4): ts-t.history re-anchored green.
</success_criteria>

<artifacts_produced>
- Restyled `WorkflowHistory` (Run History title kept + filter chips + Sort tabs + date groups + status/token/version badges; 3 states).
- Re-anchored `ts-t.history` mocked-e2e spec.
</artifacts_produced>

<output>
Create `.planning/phases/40-shell-mock-fidelity-restyle-b6/40-06-SUMMARY.md` when done.
</output>
