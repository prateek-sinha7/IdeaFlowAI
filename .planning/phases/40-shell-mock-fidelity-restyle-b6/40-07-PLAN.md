---
phase: 40-shell-mock-fidelity-restyle-b6
plan: 07
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx
  - frontend/src/components/savedworkflows/SavedWorkflowsPage.test.tsx
  - frontend/e2e/tests/ts-z2.saved-workflows.spec.ts
  - frontend/e2e/tests/ts-z.catalog.spec.ts
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]
must_haves:
  truths:
    - "The Catalogue / My Workflows surface renders the mock's grid (title 'My Workflows' kept — ND-B) over the live/stubbed /api/user-workflows data (ND-D)"
    - "The populated grid, the search-filtered state, and the zero/empty state each match the mock (via the 40-01 /api/user-workflows stub)"
    - "The existing kebab actions (rename/delete), search, and saved-workflow launch are preserved"
    - "The surface no longer crashes in mocked mode (depends on the 40-01 stub)"
  artifacts:
    - path: "frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx"
      provides: "Restyled My Workflows grid to the mock's Catalogue composition"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx"
      to: "/api/user-workflows"
      via: "live saved-workflow list → card grid"
      pattern: "user-workflows"
---

<intended_divergence_note>
Inherit ND-A..D + ND-W..Z from `40-UI-SPEC.md`. This surface exercises: **ND-B** — the title/nav stays "My Workflows", NOT the mock's "Workflow Catalogue" (D-11 — Catalogue reserved for the future marketplace); and ND-D — the grid is the live/stubbed `/api/user-workflows` list, never the mock's fixed saved-workflow set. Any OTHER visual departure (beyond the title + live rows) is a fidelity bug. This plan DEPENDS on the 40-01 `/api/user-workflows` stub (the surface crashes without it in mocked mode).
</intended_divergence_note>

<objective>
Restyle the Catalogue / My Workflows surface to the mock's "Workflow Catalogue" grid (CONTEXT specifics #6), keeping the "**My Workflows**" title (ND-B). Bring the pipelines + saved-workflow card grid, the search-filtered state, and the zero/empty state to mock parity, over the live/stubbed `/api/user-workflows` data (ND-D — the 40-01 stub unblocks this). Preserve the existing kebab actions (rename/delete), search, and saved-workflow launch. Re-anchor the `ts-z2.saved-workflows` (+ `ts-z.catalog`) mocked-e2e specs.

Purpose: My Workflows is the user's own-workflow surface — currently blocked/crashing; unblock + restyle to the mock.
Output: `SavedWorkflowsPage` restyled to the mock across populated/filtered/empty; ts-z2/ts-z re-anchored.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-CONTEXT.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-UI-SPEC.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-01-PLAN.md
@frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle My Workflows to the mock's Catalogue grid (keep 'My Workflows' — ND-B) + re-anchor ts-z2/ts-z</name>
  <files>frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx, frontend/src/components/savedworkflows/SavedWorkflowsPage.test.tsx, frontend/e2e/tests/ts-z2.saved-workflows.spec.ts, frontend/e2e/tests/ts-z.catalog.spec.ts</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (Catalogue `sc-if isCatalogue` :434 — the "Workflow Catalogue" header + the pipelines + saved-workflows card grid; card composition, badges, actions)
    - frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx (the "My Workflows" h1 :222 — KEEP per ND-B; the `/api/user-workflows` fetch + `userWorkflows.filter` :150-170; the count header :229-247; the search :248-257; the empty state :273-296; the card grid :297-320; the `KebabMenu` rename/delete :87)
    - frontend/e2e/tests/ts-z2.saved-workflows.spec.ts + ts-z.catalog.spec.ts (the stale locators to re-anchor to the restyled grid)
  </read_first>
  <action>
    Restyle `SavedWorkflowsPage` to the mock's Catalogue grid composition, KEEPING the "My Workflows" title (:222 — ND-B, do NOT change to "Workflow Catalogue"). Restyle the header/count line, the search, and the saved-workflow **card grid** (:297-320) to the mock's card chrome — name (initials/avatar), deliverable type, agent count, relative updated time, and the kebab (rename/delete) — reusing the existing `UserWorkflowSummary` data + `KebabMenu` (do NOT change the CRUD contract, only its presentation). If the mock's Catalogue also shows a "pipelines" section alongside saved workflows, render our launchable-pipeline equivalent only from live data (do NOT fabricate — ND-D); otherwise keep the saved-workflows grid as the surface. Restyle the empty state (:273-296) + the search-filtered state to the mock. Route all color/type through the existing tokens; keep the saved-workflow launch (`onLaunchSaved`) + search + kebab behaviors. Then RE-ANCHOR `ts-z2.saved-workflows.spec.ts` (+ `ts-z.catalog.spec.ts`) locators to the restyled grid (the Phase-32/39 re-anchor pattern) — only update locators/assertions to the new layout, do NOT weaken the behavioral assertions. NO fenced code in this action — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/catalogue__shell.png` + `catalogue__shellfull.png` (populated, over the 40-01 `/api/user-workflows` stub) vs the `Hexaware Workspace v2.dc.html` Catalogue surface, plus the filtered + empty captures, via the 40-01 oracle, closed to ND-B (title) + ND-D (live rows). This autonomous task generates the current/* captures mechanically; the human approval is the trailing checkpoint.
    - The surface renders with NO `userWorkflows.filter` crash (depends on the 40-01 stub); the title reads "My Workflows" (grep → present; "Workflow Catalogue" absent; ND-B); the grid renders from the stubbed rows (vitest: seeded `/api/user-workflows` → N cards; empty → the empty state).
    - `ts-z2.saved-workflows.spec.ts` (+ `ts-z.catalog.spec.ts`) PASS against the restyled surface (mocked project) — the re-anchored locators resolve, the rename/delete/launch assertions hold.
    - `npx --prefix frontend tsc --noEmit` clean; `SavedWorkflowsPage.test.tsx` green (grid + filtered + empty + kebab cases).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "My Workflows" src/components/savedworkflows/SavedWorkflowsPage.tsx | grep -vqx 0 && npx vitest run src/components/savedworkflows/SavedWorkflowsPage.test.tsx && npx playwright test --project=mocked ts-z2.saved-workflows ts-z.catalog</automated>
  </verify>
  <done>My Workflows renders the mock's Catalogue grid across populated/filtered/empty with the "My Workflows" title kept (ND-B), no crash; ts-z2/ts-z re-anchored green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of Catalogue / My Workflows (3 states) vs the mock (closed to ND-A..D + ND-W..Z)</name>
  <files>frontend/e2e/fidelity/gallery-shell.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (D40-5; ND-D live/stubbed rows make a pixel-diff-vs-mock invalid). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/catalogue*, over the stubbed /api/user-workflows), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-shell-gallery.mjs --surface catalogue`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Catalogue / My Workflows surface across populated / filtered / empty — the mock's Catalogue card grid with the "My Workflows" title (ND-B), over the stubbed live data — with the prior task's mechanical gates already green (npx tsc --noEmit clean, SavedWorkflowsPage vitest green, ts-z2/ts-z re-anchored green, no crash).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-shell.html and find the `catalogue` sections (populated + filtered + empty). Compare the mock (left) against ours (right). Confirm every visible difference is a registered divergence — ND-B (our title "My Workflows" vs the mock's "Workflow Catalogue", captioned "expected — D-11"), ND-D (our stubbed live rows vs the mock's fixed set); any OTHER visual departure (card chrome, grid geometry, badges, kebab, empty state) is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if Catalogue / My Workflows matches its mock across the three states closed to the intended-divergence register ("My Workflows" kept per ND-B), or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| saved-workflow data → grid cards | server-provided names/briefs rendered in the UI |
| rename / delete / launch → REST | authenticated owner-scoped mutations |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07-01 | Tampering (XSS) | card name / brief text | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new Catalogue code (grep → 0) |
| T-40-07-02 | Spoofing | rename / delete / launch | accept | reuses the existing owner-scoped CRUD (IDOR→404 already enforced); no new authority path |
| T-40-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs (reuses lucide-react + tokens). |
</threat_model>

<verification>
- Catalogue / My Workflows matches the mock across populated/filtered/empty per the shell-gallery oracle, closed to ND-B (title) + ND-D (live rows).
- No crash (40-01 stub); the CRUD + search + launch behavior preserved; ts-z2/ts-z re-anchored green.
- `npx tsc --noEmit` clean; SavedWorkflowsPage vitest green.
</verification>

<success_criteria>
SHELL-01 (SC-1): the Catalogue / My Workflows surface (3 states) matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
SHELL-02 (SC-2): the Catalogue card grid lands.
SHELL-03 (SC-3): live/stubbed data (ND-D); title kept (ND-B); existing CRUD/launch reused.
SHELL-04 (SC-4): the surface is unblocked (40-01 stub) + ts-z2/ts-z re-anchored green.
</success_criteria>

<artifacts_produced>
- Restyled `SavedWorkflowsPage` (My Workflows title kept + the mock's Catalogue card grid; 3 states).
- Re-anchored `ts-z2.saved-workflows` (+ `ts-z.catalog`) mocked-e2e specs.
</artifacts_produced>

<output>
Create `.planning/phases/40-shell-mock-fidelity-restyle-b6/40-07-SUMMARY.md` when done.
</output>
