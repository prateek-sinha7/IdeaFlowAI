---
phase: 40-shell-mock-fidelity-restyle-b6
plan: 05
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/components/settings/AccountSettings.tsx
  - frontend/src/components/settings/AccountSettings.test.tsx
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03]
must_haves:
  truths:
    - "The Account Settings tabs read Profile / AI Model / Usage & Limits / Constitution (the 'Limits' tab is relabelled 'Usage & Limits' to match the mock)"
    - "Each of the four tabs matches its mock composition + styling (captured per tab)"
    - "The Profile tab renders a richer form bound only to real user fields (email, plan/tier) — no fabricated name/role/organization fields (ND-Y)"
    - "The AI Model tab lists the live available models (ND-D), never the mock's hardcoded model list"
  artifacts:
    - path: "frontend/src/components/settings/AccountSettings.tsx"
      provides: "Restyled Settings tabs (Usage & Limits relabel + richer real-data profile form)"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/settings/AccountSettings.tsx"
      to: "/api/settings/preferences + /api/auth/me"
      via: "live profile + model data"
      pattern: "settings/preferences"
---

<intended_divergence_note>
Inherit ND-A..D + ND-W..Z from `40-UI-SPEC.md`. This surface exercises: ND-C (tab underline active-state, kept), ND-D (the AI-Model tab lists live models, never the mock's fixed list), and **ND-Y** — the Profile tab renders ONLY the profile fields backed by real user data (email, plan/tier); the mock's fabricated name / role / organization fields are NOT reproduced (SC-001 — never fabricate unpersisted data). Planner ruling (not a ND): the "Limits" tab is ALIGNED to the mock's "Usage & Limits" label (a pure fidelity fix). Any OTHER visual departure across the four tabs is a fidelity bug.
</intended_divergence_note>

<objective>
Restyle the Account Settings surface to the mock (CONTEXT specifics #4). Relabel the "Limits" tab to "**Usage & Limits**" (align to the mock — a pure fidelity fix). Build the richer Profile form the mock shows, but bound to REAL user fields only (email, plan/tier) — do NOT fabricate name/role/organization (ND-Y). Restyle all four tabs (Profile / AI Model / Usage & Limits / Constitution) to mock parity, captured per tab. Keep the live model-preference save, the change-password flow, and all existing behavior.

Purpose: Account Settings is the user's control surface — its tabs + forms must match the mock without fabricating data.
Output: `AccountSettings` with the relabelled tab + a real-data richer profile form + four mock-parity tabs.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-CONTEXT.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-UI-SPEC.md
@frontend/src/components/settings/AccountSettings.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Settings — relabel Usage & Limits, richer real-data Profile form (ND-Y), four mock-parity tabs</name>
  <files>frontend/src/components/settings/AccountSettings.tsx, frontend/src/components/settings/AccountSettings.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (Account Settings `sc-if isSettings` :548 — tabs `stProfile` / `stModel` / `stLimits` (labelled "Usage & Limits") / `stConstitution`; the richer profile form fields; the model tab; the usage/limits + constitution tabs)
    - frontend/src/components/settings/AccountSettings.tsx (the h1 :175; the `Tabs` items :155-158 — the "limits" label to relabel; the Profile tab email-only form :62/97; the AI-Model tab live models :79-104; the change-password + save-model handlers :110-122; the Limits + Constitution tab bodies)
    - frontend/src/lib/api.ts (the user shape from `/api/auth/me` — which profile fields actually exist: email, tier/plan, is_admin — the ND-Y boundary of what is real vs fabricated)
  </read_first>
  <action>
    Restyle `AccountSettings` to the mock's four-tab composition. Relabel the `limits` tab item (:157) from "Limits" to "**Usage & Limits**" (align to the mock — the internal id "limits" + any testids stay stable). Restyle the Profile tab into the mock's richer form LAYOUT, but populate it ONLY with real user data (email + plan/tier from `/api/auth/me`/preferences) — render the mock's name/role/organization fields ONLY if the user model actually carries them; otherwise omit them (do NOT add fabricated inputs — ND-Y/SC-001). Keep the change-password flow. Restyle the AI-Model tab (live models from `/api/settings/preferences` + `/api/capabilities`, the save-preference handler — ND-D, never the mock's fixed model list), the Usage & Limits tab, and the Constitution tab to the mock's chrome. Keep the ND-C underline active-state on the tabs. Route all color/type through the existing tokens. NO fenced code in this action — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/settings__shell.png` + the per-tab captures (`settings-model`, `settings-limits`, `settings-constitution`) vs the `Hexaware Workspace v2.dc.html` Settings surface + tabs, via the 40-01 oracle, closed to ND-A..D + ND-W..Z (esp. ND-Y profile fields, ND-D model list, ND-C underline). This autonomous task generates the current/* captures mechanically; the human approval is the trailing checkpoint.
    - The tab label reads "Usage & Limits" (vitest: the tablist contains "Usage & Limits", not a bare "Limits"); the internal id/testid is stable.
    - The Profile form renders NO fabricated name/role/org inputs beyond what the real user model carries (vitest: with a mock user of {email,tier}, only real fields render; grep for a hardcoded org/role string → absent; ND-Y).
    - `npx --prefix frontend tsc --noEmit` clean; `AccountSettings.test.tsx` green (tab label + real-data profile + model-list cases).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/settings/AccountSettings.test.tsx</automated>
  </verify>
  <done>Settings shows Profile / AI Model / Usage & Limits / Constitution at mock parity; the profile form is real-data only (ND-Y); model list live (ND-D).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of Account Settings + each tab vs the mock (closed to ND-A..D + ND-W..Z)</name>
  <files>frontend/e2e/fidelity/gallery-shell.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (D40-5; ND-D/ND-Y live data make a pixel-diff-vs-mock invalid). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/settings*), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-shell-gallery.mjs --surface settings`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Account Settings surface — the four tabs Profile / AI Model / Usage & Limits / Constitution (each captured), the relabelled tab, and the real-data-only richer profile form (ND-Y) — with the prior task's mechanical gates already green (npx tsc --noEmit clean, AccountSettings vitest green).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-shell.html and find the `settings`, `settings-model`, `settings-limits`, `settings-constitution` sections. For EACH tab, compare the mock (left) against ours (right). Confirm every visible difference is a registered divergence — ND-Y (the mock's fabricated name/role/org fields absent in ours, captioned "expected — no fabricated data"), ND-D (live model list), ND-C (underline); any OTHER visual departure (tab chrome, form layout, spacing) is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if Account Settings + each tab matches its mock closed to the intended-divergence register (profile real-data-only per ND-Y), or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user profile / preferences → Settings forms | owner-scoped account data rendered + editable |
| change-password / save-model → REST | authenticated mutations |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05-01 | Information disclosure | Profile form | mitigate | render ONLY real owner-scoped fields (ND-Y); never fabricate/echo fields the user model does not carry |
| T-40-05-02 | Spoofing | change-password / save-model | accept | reuses the existing authenticated owner-scoped mutations; no new authority path |
| T-40-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs (reuses lucide-react + tokens). |
</threat_model>

<verification>
- Settings tabs (Profile / AI Model / Usage & Limits / Constitution) match the mock per the shell-gallery oracle, closed to ND-A..D + ND-W..Z (ND-Y, ND-D, ND-C).
- The profile form is real-data only (ND-Y/SC-001); the model list is live (ND-D); the "Usage & Limits" relabel lands.
- `npx tsc --noEmit` clean; AccountSettings vitest green.
</verification>

<success_criteria>
SHELL-01 (SC-1): Account Settings + each tab matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
SHELL-02 (SC-2): the richer profile form + relabelled tab land.
SHELL-03 (SC-3): real/live data (ND-Y/ND-D); divergences preserved; existing save flows reused.
</success_criteria>

<artifacts_produced>
- Restyled `AccountSettings` (Usage & Limits relabel + real-data richer profile form + four mock-parity tabs).
</artifacts_produced>

<output>
Create `.planning/phases/40-shell-mock-fidelity-restyle-b6/40-05-SUMMARY.md` when done.
</output>
