---
phase: 40-shell-mock-fidelity-restyle-b6
plan: 04
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/components/analytics/AnalyticsPage.tsx
  - frontend/src/components/analytics/AnalyticsPage.test.tsx
autonomous: false
requirements: [SHELL-01, SHELL-03]
must_haves:
  truths:
    - "The Analytics surface matches the mock's composition + styling: KPI row, daily-activity bars, success-rate donut, by-pipeline, recent runs, token/model breakdown"
    - "Number/date formatting (tokens, cost, durations, dates) matches the mock's format"
    - "All values are bound to the live /api/analytics/summary payload (ND-D — seeded for the diff), never the mock's hardcoded numbers"
  artifacts:
    - path: "frontend/src/components/analytics/AnalyticsPage.tsx"
      provides: "Mock-parity Analytics styling + number-format over the live endpoint"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/analytics/AnalyticsPage.tsx"
      to: "/api/analytics/summary"
      via: "endpoint payload → KPI + charts"
      pattern: "analytics/summary"
---

<intended_divergence_note>
Inherit ND-A..D + ND-W..Z from `40-UI-SPEC.md`. This surface exercises ND-D only: every KPI/chart value is bound to the live `/api/analytics/summary` payload (seeded by 40-01 for a fair diff), never the mock's hardcoded figures. The structure already matches (Phase 38 built AnalyticsPage to this endpoint); this plan is a styling + number-format parity pass. Any structural/styling departure from the mock (beyond ND-D values) is a fidelity bug.
</intended_divergence_note>

<objective>
Bring the Analytics surface to mock parity (CONTEXT specifics #3). AnalyticsPage already has the mock's structure (KPI row · daily-activity bars · success-rate donut · by-pipeline · recent runs · token/model breakdown — Phase 38), so this is a **styling + number-format** pass: align spacing/typography/card chrome/chart styling and the number/date formatting to the mock, on the live `/api/analytics/summary` payload (ND-D, seeded by 40-01). Do NOT re-architect the endpoint or the charts' data contracts.

Purpose: Analytics is a data surface that already matches structurally — close the styling gap to the mock.
Output: `AnalyticsPage` restyled to mock parity over the live endpoint.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-CONTEXT.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-UI-SPEC.md
@frontend/src/components/analytics/AnalyticsPage.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics styling + number-format parity pass over the live endpoint</name>
  <files>frontend/src/components/analytics/AnalyticsPage.tsx, frontend/src/components/analytics/AnalyticsPage.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (Analytics `sc-if isAnalytics` :473 — KPI cards, Daily Activity bars, Success-rate donut, By-pipeline, Recent runs, Token/Model breakdown; the exact card chrome, spacing, number/date formats)
    - frontend/src/components/analytics/AnalyticsPage.tsx (h1 :249; KPI row :309; daily-activity `BarChart` :347; success `DonutChart` :372; by-pipeline :413; the `formatTokens`/`formatCost`/`formatDate` helpers :53-63; the endpoint-bound derived data :169+)
    - frontend/src/components/analytics/charts/{BarChart,DonutChart}.tsx (the token-styled chart primitives — adjust styling props only if the mock requires; do NOT change their data contracts)
  </read_first>
  <action>
    Restyle `AnalyticsPage` to the mock's Analytics composition — align the KPI card chrome, section spacing/typography, chart framing, legend/label styling, and the by-pipeline/recent-runs/token-model rows to the mock. Align the number + date formatting to the mock's format via the existing `formatTokens`/`formatCost`/`formatDate` helpers (:53-63) — adjust the helpers if the mock's format differs (e.g. thousands/compact notation, currency, relative dates); tabular-nums where the mock uses aligned figures. Route all color/type through the existing tokens; if the mock needs a chart-styling tweak, pass a styling prop into `BarChart`/`DonutChart` rather than changing their data contracts (INV-3). Keep the endpoint binding (`/api/analytics/summary`) + the derived-data math exactly (ND-D — values stay live/seeded). Do NOT re-architect the endpoint or add a new fetch. NO fenced code in this action — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/analytics__shell.png` + `analytics__shellfull.png` vs the `Hexaware Workspace v2.dc.html` Analytics surface, via the 40-01 oracle (over the seeded `/api/analytics/summary`), closed to ND-D (values differ; structure/styling must match). This autonomous task generates the current/* captures mechanically; the human approval is the trailing checkpoint.
    - The KPI/chart values render from the endpoint payload (grep AnalyticsPage for the mock's hardcoded figures → absent; ND-D); number/date formatting matches the mock (vitest on the format helpers).
    - The charts' data contracts are unchanged (only styling props / container chrome adjusted; grep the BarChart/DonutChart prop shapes → unchanged).
    - `npx --prefix frontend tsc --noEmit` clean; `AnalyticsPage.test.tsx` green (format + endpoint-binding cases).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/analytics/AnalyticsPage.test.tsx</automated>
  </verify>
  <done>Analytics matches the mock's composition + number/date formatting over the live endpoint; chart data contracts untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of the Analytics surface vs the mock (closed to ND-A..D + ND-W..Z)</name>
  <files>frontend/e2e/fidelity/gallery-shell.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (D40-5; ND-D live/seeded values make a pixel-diff-vs-mock invalid). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/analytics__*, over the seeded endpoint), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-shell-gallery.mjs --surface analytics`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Analytics surface — KPI row, daily-activity bars, success-rate donut, by-pipeline, recent runs, token/model breakdown — restyled to mock parity over the seeded live endpoint, with the prior task's mechanical gates already green (npx tsc --noEmit clean, AnalyticsPage vitest green).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-shell.html and find the `analytics__shell` / `analytics__shellfull` sections. Compare the mock (left) against ours (right). Confirm the ONLY differences are ND-D (the numbers/charts show our seeded live values, not the mock's fixed figures — captioned "expected — ignore"); any structural/styling/format departure (card chrome, spacing, chart framing, number/date format) is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the Analytics surface matches its mock closed to the intended-divergence register (values live per ND-D), or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| analytics endpoint payload → charts/KPIs | server-aggregated numeric data rendered in the UI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04-01 | Tampering (XSS) | recent-runs labels / pipeline names | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new Analytics code (grep → 0) |
| T-40-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs (reuses the existing charts + tokens). |
</threat_model>

<verification>
- Analytics matches the mock's composition + number/date format per the shell-gallery oracle, closed to ND-D.
- Values stay live/seeded (SC-001/ND-D); the endpoint + chart data contracts are unchanged.
- `npx tsc --noEmit` clean; AnalyticsPage vitest green.
</verification>

<success_criteria>
SHELL-01 (SC-1): the Analytics surface matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
SHELL-03 (SC-3): live data (ND-D); endpoint + chart contracts reused, not re-architected.
</success_criteria>

<artifacts_produced>
- Restyled `AnalyticsPage` (mock-parity chrome + number/date format over the live endpoint).
</artifacts_produced>

<output>
Create `.planning/phases/40-shell-mock-fidelity-restyle-b6/40-04-SUMMARY.md` when done.
</output>
