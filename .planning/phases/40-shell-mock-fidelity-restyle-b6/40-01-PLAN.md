---
phase: 40-shell-mock-fidelity-restyle-b6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/fixtures/mockApi.ts
  - frontend/e2e/fixtures/dashboard.ts
  - frontend/e2e/tests/zzz-shell-baseline.spec.ts
  - frontend/e2e/fidelity/assemble-shell-gallery.mjs
  - frontend/e2e/fidelity/README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SHELL-01, SHELL-04]
must_haves:
  truths:
    - "The Catalogue / My Workflows surface renders in mocked mode (no `userWorkflows.filter is not a function` crash) — `/api/user-workflows` is stubbed in the shared MockApi"
    - "History, Analytics, and Home-recents render POPULATED in mocked mode from seeded capture scaffolding (not empty), so the fidelity diff is fair"
    - "The shell fidelity oracle writes repo-relative outputs and assembles a per-surface-filterable side-by-side gallery with the finalized ND-A..D + ND-W..Z register captioned as 'expected — ignore'"
    - "SHELL-01..04 are defined in REQUIREMENTS.md and the Phase 40 ROADMAP carries a **Requirements:** line (owned, not floating)"
  artifacts:
    - path: "frontend/e2e/fixtures/mockApi.ts"
      provides: "Stub for /api/user-workflows + seeded History/Analytics scaffolding + setters"
      contains: "api/user-workflows"
    - path: "frontend/e2e/fidelity/assemble-shell-gallery.mjs"
      provides: "Per-surface-filterable shell gallery assembler carrying the finalized ND register"
      contains: "ND-W"
  key_links:
    - from: "frontend/e2e/fixtures/mockApi.ts"
      to: "/api/user-workflows"
      via: "route handler returning a saved-workflow array"
      pattern: "api/user-workflows"
    - from: "frontend/e2e/fidelity/assemble-shell-gallery.mjs"
      to: "shots/target + shots/current"
      via: "side-by-side gallery with --surface filter"
      pattern: "assemble-shell-gallery"
---

<objective>
Stand up everything the six surface plans diff against — FIRST (Wave 1). (1) Stub the blocked `/api/user-workflows` in the SHARED MockApi so Catalogue stops crashing, and SEED representative History / Analytics / Home-recents scaffolding so those surfaces render populated for a fair diff (D40-4). (2) FORMALIZE the committed shell oracle (D40-3): repo-relative outputs + env gate on the current-side spec, a finalized ND register (ND-A..D carried + ND-W..Z) with a `--surface` filter in the gallery assembler, and a README. (3) OWN the requirement-traceability docs (SHELL-01..04) + the Phase 40 ROADMAP `**Requirements:**` line.

This lands FIRST because no surface plan can close its fidelity checkpoint until the gallery renders every in-scope surface (including the currently-crashing Catalogue and the currently-empty History/Analytics).

Purpose: makes the D40-5 acceptance oracle real + mechanically-regenerable and unblocks every surface plan's fidelity checkpoint.
Output: a Catalogue that renders in mocked mode + seeded populated shell surfaces + a formalized `frontend/e2e/fidelity/` shell gallery + the SHELL-01..04 docs.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-CONTEXT.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-UI-SPEC.md
@frontend/e2e/fixtures/mockApi.ts
@frontend/e2e/tests/zzz-shell-baseline.spec.ts
@frontend/e2e/fidelity/assemble-shell-gallery.mjs
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
The full register lives in `40-UI-SPEC.md` (ND-A..D carried from Phase 39 + ND-W..Z new). Task 2 makes `assemble-shell-gallery.mjs`'s ND array the CANONICAL source of truth: finalize the scoping candidates (ND-E?..H?) into the shipped register — ND-A brand, ND-B "My Workflows", ND-C nav underline, ND-D live/seeded data, ND-W "Run History" title, ND-X no Voice affordance, ND-Y no fabricated profile fields, ND-Z Library drawer deferred to Phase 41 — captioned "expected — ignore". Delete the Configure/Composer candidates (ND-F?/ND-G?) from the shell gallery caption: those surfaces are DEFERRED (D40-2) and are not diffed in Phase 40. Do NOT wire an automated pixel-diff (ND-D live data ≠ the mock's fixed values).
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: Stub /api/user-workflows + seed History/Analytics/Home-recents scaffolding in the shared MockApi</name>
  <files>frontend/e2e/fixtures/mockApi.ts, frontend/e2e/fixtures/dashboard.ts</files>
  <read_first>
    - frontend/e2e/fixtures/mockApi.ts (the route dispatcher — the `/api/workflows` handler :234 + `/api/analytics/summary` :237 + the audit-seeding idiom :243-297 to mirror; the `DEFAULT_WORKFLOWS` shape :105-112; the `setRuns`/`setWorkflows` setters :176-181)
    - frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx (`UserWorkflowSummary` fields the grid reads — id/name/agent_ids/brief/updated_at; the fetch at :158-170 that throws when the payload is not an array)
    - frontend/src/components/history/WorkflowHistory.tsx (binds `/api/runs`; the family-grouped date buckets need ≥1 completed + ≥1 failed run across TODAY/EARLIER/OLDER)
    - frontend/src/components/analytics/AnalyticsPage.tsx (the `/api/analytics/summary` payload keys it binds — KPI totals, daily-activity series, success donut, by-pipeline, recent runs, token/model breakdown)
  </read_first>
  <action>
    In MockApi add a `GET /api/user-workflows` route handler (mirror the `/api/runs` json() idiom) returning a configurable saved-workflow array — read `SavedWorkflowsPage.tsx` for the exact `UserWorkflowSummary` shape and seed ≥2 representative rows (id, name, deliverable/type, agent_ids, brief, created_at/updated_at) PLUS an empty-array variant path so the Catalogue empty-state can be captured; expose a `setUserWorkflows(rows)` setter. This alone fixes the `userWorkflows.filter is not a function` crash (`SavedWorkflowsPage.tsx:170`). Then SEED the currently-empty surfaces as test-only capture scaffolding (mirroring the existing audit-seeding comment block at :243-251 — clearly labelled NOT production data): (a) a representative `/api/runs` history set (≥3 runs spanning completed / failed / cancelled and TODAY / EARLIER / OLDER `created_at` buckets, with `token_usage` + revision `title`s so the family grouping + status/token/version chips render); (b) a populated `/api/analytics/summary` payload (fill the keys AnalyticsPage binds — KPI totals, daily-activity series, success counts, by-pipeline, recent runs, token/model — with representative non-zero values). In `dashboard.ts`, add helpers/setters so the shell-capture spec can install the seeded MockApi (a `seededShell()` option or setters called before `goto()`); do NOT change the existing default-empty behavior other specs rely on (gate the seeding behind the new option/setters — tsc identity for existing specs). This is capture scaffolding under SC-001/ND-D: production still returns only real data.
  </action>
  <acceptance_criteria>
    - `GET /api/user-workflows` returns an ARRAY (seeded ≥2 rows by default for the shell capture; an empty variant available) — the Catalogue renders in mocked mode with NO `userWorkflows.filter is not a function` crash.
    - The seeded History set produces ≥1 populated group across the date buckets; the seeded analytics summary renders non-zero KPIs/charts — verified by the shell capture spec reaching those surfaces without an empty-state.
    - Seeding is OPT-IN (new setter/option) — existing specs that expect empty defaults are byte-unchanged; `npx --prefix frontend tsc --noEmit` clean on the e2e tsconfig (identity vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "api/user-workflows" e2e/fixtures/mockApi.ts && grep -q "setUserWorkflows" e2e/fixtures/mockApi.ts && npx tsc --noEmit -p e2e/tsconfig.json 2>&1 | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <done>Catalogue no longer crashes in mocked mode; History/Analytics/Home-recents render populated from opt-in seeded scaffolding; existing specs unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: Formalize the shell oracle — repo-relative outputs, env gate, finalized ND register + --surface filter, README</name>
  <files>frontend/e2e/tests/zzz-shell-baseline.spec.ts, frontend/e2e/fidelity/assemble-shell-gallery.mjs, frontend/e2e/fidelity/README.md</files>
  <read_first>
    - frontend/e2e/tests/zzz-shell-baseline.spec.ts (the committed scoping spec — currently writes under `$PHASE40_OUT`; the two capture tests + the `menu()` / `clickNav()` drivers to keep)
    - frontend/e2e/fidelity/assemble-shell-gallery.mjs (the committed assembler — the `ND` array :26-35 to FINALIZE; no `--surface` filter yet)
    - frontend/e2e/fidelity/capture-shell-mocks.mjs (the TARGET side — already committed; its `{surface}__{shell|shellfull}` tag convention to keep)
    - frontend/e2e/fidelity/README.md (Phase-39 fidelity README — extend, don't fork)
  </read_first>
  <action>
    Make the committed shell oracle a RETAINED, repo-relative harness (mirroring how 39-07 formalized the run oracle). In `zzz-shell-baseline.spec.ts`: keep the two capture tests + the nav drivers; default the output dir to a repo-relative `frontend/e2e/fidelity/shots-shell/current/` when `PHASE40_OUT` is unset (keep the env override for scratch runs); keep the `SHELL_CAPTURE=1` gate so a normal `npm run e2e` skips it; ensure the seeded MockApi (Task 1) is installed for the History/Analytics/Catalogue captures (call the new `dashboard.ts` seeding option before `goto()`). In `assemble-shell-gallery.mjs`: FINALIZE the `ND` array to the shipped register — remove the `?` scoping-candidate suffixes and the Configure/Composer candidates (ND-F?/ND-G? — those surfaces are DEFERRED, not diffed here), keeping ND-A brand · ND-B "My Workflows" · ND-C nav underline · ND-D live/seeded data · ND-W "Run History" title · ND-X no Voice affordance · ND-Y no fabricated profile fields · ND-Z Library drawer deferred to Phase 41; add an optional `--surface <name>` filter so a surface checkpoint regenerates only its section (the output HTML then contains only that surface's tags); default the ROOT to a repo-relative `frontend/e2e/fidelity/shots-shell` when `PHASE40_OUT` is unset. Extend `README.md` with the shell capture + assemble commands, the `shots-shell/` layout, how each surface checkpoint regenerates + reviews its section, and the finalized ND list to ignore. Do NOT commit screenshot binaries — git-ignore `shots-shell/` + `gallery-shell.html`. Do NOT rebuild `capture-shell-mocks.mjs` (reuse as-is).
  </action>
  <acceptance_criteria>
    - `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` writes `shots-shell/current/{surface}__{shell|shellfull}.png` for all six in-scope surfaces (Home/Library/Analytics/Settings/History/Catalogue) with the seeded data, without error; a normal `npm run e2e` SKIPS the spec (gated).
    - `node frontend/e2e/fidelity/assemble-shell-gallery.mjs` writes a self-contained `gallery-shell.html`; the ND caption lists ND-A..D + ND-W..Z (grep → `ND-W`, `ND-Z` present; `ND-F?`/`ND-G?` absent); `--surface history` filters the output to only the history tags.
    - `README.md` documents the shell commands + the ND list; `shots-shell/` and `gallery-shell.html` are git-ignored.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "ND-W" e2e/fidelity/assemble-shell-gallery.mjs && grep -q "ND-Z" e2e/fidelity/assemble-shell-gallery.mjs && grep -q "surface" e2e/fidelity/assemble-shell-gallery.mjs && git check-ignore e2e/fidelity/gallery-shell.html && grep -Eiq "shell|run history|velocityai" e2e/fidelity/README.md</automated>
  </verify>
  <done>The shell oracle writes repo-relative, is env-gated, carries the finalized ND register with a per-surface filter, and is documented; shots/gallery git-ignored.</done>
</task>

<task type="auto">
  <name>Task 3: Own the requirement-traceability docs (SHELL-01..04)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the "Run-Screen Mock Fidelity (Phase 39)" block RUNUI-06..09 :236-241 — the insertion anchor + entry style)
    - .planning/ROADMAP.md (the Phase 40 section added at planning time — the `**Requirements:**` / `**Success Criteria:**` lines)
  </read_first>
  <action>
    Ensure (idempotent create-if-absent) that `REQUIREMENTS.md` carries a "Shell Mock Fidelity (Phase 40)" subsection after the RUNUI block, defining: SHELL-01 (SC-1 — each in-scope shell surface + every sub-view/sub-tab/state matches its mock to the ND register, proven by the side-by-side shell gallery + a HUMAN sign-off, not a prose claim); SHELL-02 (SC-2 — the net-new as-is affordances land on live data: Home 3×2 card grid + recents, Library card grids, Settings richer profile form, History filter chips/sort/date-groups, the Catalogue grid); SHELL-03 (SC-3 — live/real data everywhere (SC-001/ND-D); intended divergences ND-A..D + ND-W..Z preserved; existing components reused, chrome/layout rebuilt; no fabricated data); SHELL-04 (SC-4 — the shell fidelity oracle is formalized (blocked `/api/user-workflows` stubbed + data seeded + per-surface gallery regenerates), and each touched surface's mocked-e2e spec is re-anchored green). Add a `**Requirements:** SHELL-01, SHELL-02, SHELL-03, SHELL-04` line to the Phase 40 ROADMAP section. Verify every SHELL id in the 7 plans' `requirements` frontmatter resolves to a REQUIREMENTS.md entry (no dangling id).
  </action>
  <acceptance_criteria>
    - `REQUIREMENTS.md` defines SHELL-01, SHELL-02, SHELL-03, SHELL-04 (grep → 4 present).
    - `ROADMAP.md` Phase 40 section has a `**Requirements:** SHELL-01, SHELL-02, SHELL-03, SHELL-04` line.
    - Every SHELL id in the Phase 40 plans' `requirements` frontmatter resolves to a REQUIREMENTS.md entry (no dangling id).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Requirements:\*\* SHELL-01" .planning/ROADMAP.md && for id in SHELL-01 SHELL-02 SHELL-03 SHELL-04; do grep -q "$id" .planning/REQUIREMENTS.md || exit 1; done</automated>
  </verify>
  <done>SHELL-01..04 are defined + owned in REQUIREMENTS.md and referenced by the Phase 40 ROADMAP; no dangling requirement ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MockApi → app under test | fabricated fixtures only; test-only, never shipped |
| shell capture script → local mock server | reads .dc.html from a fixed local path; no untrusted network input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Information disclosure | seeded MockApi scaffolding | accept | test-only fixtures (labelled NOT production data); never bundled into the app; production endpoints return only real owner-scoped data |
| T-40-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs — reuses the already-installed Playwright + Node built-ins; no new dependency. |
</threat_model>

<verification>
- Catalogue renders in mocked mode (no `userWorkflows.filter` crash); History/Analytics/Home-recents render populated from opt-in seeded scaffolding.
- The shell oracle writes repo-relative, is `SHELL_CAPTURE`-gated, carries the finalized ND-A..D + ND-W..Z register with a `--surface` filter, and is documented; shots/gallery git-ignored.
- SHELL-01..04 defined + owned; no dangling ids. No new npm dependency; no shipped-code change (fixtures/spec/scripts/docs only).
</verification>

<success_criteria>
SHELL-04 (SC-4): the blocked Catalogue is stubbed + the empty surfaces seeded, the shell fidelity oracle is formalized (repo-relative, per-surface gallery), and the requirement docs are owned.
</success_criteria>

<artifacts_produced>
- `frontend/e2e/fixtures/mockApi.ts`: `/api/user-workflows` stub + `setUserWorkflows` + seeded History/Analytics scaffolding (opt-in).
- `frontend/e2e/fixtures/dashboard.ts`: a seeded-shell install option/setters for the capture spec.
- `frontend/e2e/tests/zzz-shell-baseline.spec.ts`: retained env-gated shell capture spec with repo-relative outputs.
- `frontend/e2e/fidelity/assemble-shell-gallery.mjs`: finalized ND-A..D + ND-W..Z register + `--surface` filter + repo-relative default.
- `frontend/e2e/fidelity/README.md`: shell capture + assemble commands + the ND list.
- `.planning/REQUIREMENTS.md` SHELL-01..04 + `.planning/ROADMAP.md` `**Requirements:**` line.
</artifacts_produced>

<output>
Create `.planning/phases/40-shell-mock-fidelity-restyle-b6/40-01-SUMMARY.md` when done.
</output>
