---
phase: 40-shell-mock-fidelity-restyle-b6
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/components/library/LibraryPage.tsx
  - frontend/src/components/library/LibraryPage.test.tsx
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03]
must_haves:
  truths:
    - "Library leads with an 'Library' h1 + item count + search, then the Agents/Skills/Hooks tab chrome, matching the mock composition"
    - "Each of the Agents, Skills, and Hooks tabs renders its reference catalog as the mock's card grid"
    - "The agent-detail keeps the shared AgentCapabilitiesModal (the mock's right-drawer rebuild defers to Phase 41 — ND-Z), captured + captioned as expected"
    - "No shared composer-owned component (AgentsPopup / AgentCapabilitiesModal) is restyled in this plan (INV-3 — no cross-phase edit)"
  artifacts:
    - path: "frontend/src/components/library/LibraryPage.tsx"
      provides: "Restyled Library header + Agents/Skills/Hooks card grids to the mock"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/library/LibraryPage.tsx"
      to: "Tabs (Agents/Skills/Hooks)"
      via: "restyled tab chrome + card grids"
      pattern: "Tabs"
---

<intended_divergence_note>
Inherit ND-A..D + ND-W..Z from `40-UI-SPEC.md`. This surface exercises: ND-C (nav underline active-state on the tab chrome, kept), and **ND-Z** — the mock's right-side agent-detail DRAWER (Overview/Skills/Hooks/Config) is NOT rebuilt here; Phase 40 keeps our shared `AgentCapabilitiesModal` (composer-owned) and the drawer rebuild lands with the Composer rebuild in Phase 41. The agent-detail is captured + captioned as an expected (ND-Z) divergence. Library reference data is a static curated catalog (not live-fetched), which is faithful to the mock's reference intent — not a ND-D concern. Any OTHER visual departure on the Agents/Skills/Hooks catalog is a fidelity bug.
</intended_divergence_note>

<objective>
Restyle the Library surface to the mock (CONTEXT specifics #2): the "Library" h1 + item count + search, the Agents/Skills/Hooks **tab chrome**, and each tab's **card grid** — captured per tab. The agent-detail drawer parity is DEFERRED to Phase 41 (ND-Z) because the detail reuses the composer-owned `AgentCapabilitiesModal`; this plan does NOT fork or restyle that shared component. Keep all existing behavior (tab switching, search filters, skill/hook detail modals, the agent-capabilities open path).

Purpose: Library is the reference catalog surface — its header + tab grids must match the mock.
Output: `LibraryPage` renders the mock's header + Agents/Skills/Hooks card grids; the agent-detail deferral (ND-Z) is captured.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-CONTEXT.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-UI-SPEC.md
@frontend/src/components/library/LibraryPage.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the Library header + Agents/Skills/Hooks tab chrome + card grids to the mock</name>
  <files>frontend/src/components/library/LibraryPage.tsx, frontend/src/components/library/LibraryPage.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (Library `sc-if isLibrary` :300 — the "Library" h1 + count + search; the Agents/Skills/Hooks tabs `isLibAgents` :310 / `isLibSkills` :327 / `isLibHooks` :342; the card grids per tab; and the agent-detail drawer `drawerOpen` :709 — CAPTURE only, do NOT rebuild, ND-Z)
    - frontend/src/components/library/LibraryPage.tsx (the `Tabs` triad + counts :356-360; the agents grid :497-529, skills grid :531-563, hooks grid :565-587; the header lead-in that currently leads with the tabs; `selectedAgent` → `AgentCapabilitiesModal` :348 — the composer-owned modal to LEAVE untouched)
  </read_first>
  <action>
    Restyle `LibraryPage` chrome + grids to the mock's Library composition. Add/adjust the header so it leads with a "Library" h1 + an item-count line + the search input (per the mock), ABOVE the Agents/Skills/Hooks tab row (the mock leads with the h1; ours currently leads with the tabs). Restyle the tab chrome to the mock (keep the `Tabs` primitive + the ND-C purple-underline active-state — do NOT adopt the mock's pill-fill; ND-C). Restyle each tab's card grid (agents :497-529, skills :531-563, hooks :565-587) to the mock's card composition (avatar/initials, name, role/summary, count line) — route all color/type through the existing tokens (ink ramp, brand, surface-card, line-border), no raw hex. Keep the existing search/filter behavior + the skill/hook detail modals + the agent-capabilities open path. Do NOT touch `AgentsPopup` / `AgentCapabilitiesModal` (composer-owned — ND-Z); the agent-detail drawer parity defers to Phase 41. NO fenced code in this action — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/library__shell.png` + the per-tab captures (`library-agents`, `library-skills`, `library-hooks`) vs the `Hexaware Workspace v2.dc.html` Library surface + tabs, via the 40-01 oracle, closed to ND-A..D + ND-W..Z (esp. ND-C underline, ND-Z agent-detail). This autonomous task generates the current/* captures mechanically; the human approval is the trailing checkpoint.
    - The Library header leads with the h1 + count + search above the tabs (vitest: the h1 renders before the tablist in the DOM order); all three tabs render their card grids.
    - No edit touches `AgentsPopup.tsx` / `AgentCapabilitiesModal` (grep the plan diff → those files unchanged; ND-Z / INV-3).
    - `npx --prefix frontend tsc --noEmit` clean; `LibraryPage.test.tsx` asserts the header order + the three tab grids render.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/library/LibraryPage.test.tsx</automated>
  </verify>
  <done>Library leads with the mock's h1 + count + search and restyled Agents/Skills/Hooks card grids; the shared agent-capabilities modal is untouched (ND-Z).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of the Library surface + each tab vs the mock (closed to ND-A..D + ND-W..Z)</name>
  <files>frontend/e2e/fidelity/gallery-shell.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (D40-5; ND-D live data makes a pixel-diff-vs-mock invalid). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/library*), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-shell-gallery.mjs --surface library`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Library surface — the h1 + count + search header, the restyled Agents/Skills/Hooks tab chrome + card grids (each tab captured), and the agent-detail (kept as the shared modal, ND-Z) — with the prior task's mechanical gates already green (npx tsc --noEmit clean, LibraryPage vitest green, AgentsPopup untouched).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-shell.html and find the `library`, `library-agents`, `library-skills`, `library-hooks`, and `agent-detail-drawer` sections. For EACH tab, compare the mock (left) against ours (right). Confirm every visible difference is a registered divergence — ND-C (underline active-state), ND-Z (the mock's right-drawer agent-detail vs our shared modal, captioned "expected — deferred to Phase 41"); any OTHER visual departure on the header/tabs/card grids is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the Library surface + each tab matches its mock closed to the intended-divergence register (agent-detail drawer accepted as ND-Z), or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| catalog reference data → card grids | curated static reference content rendered in the UI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Tampering (XSS) | card grids + skill/hook detail | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new Library code (grep → 0) |
| T-40-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs (reuses lucide-react + tokens). |
</threat_model>

<verification>
- Library header + Agents/Skills/Hooks card grids match the mock per the shell-gallery oracle, closed to ND-A..D + ND-W..Z (ND-C underline, ND-Z agent-detail).
- The shared composer-owned `AgentCapabilitiesModal` is untouched (ND-Z / INV-3).
- `npx tsc --noEmit` clean; LibraryPage vitest green.
</verification>

<success_criteria>
SHELL-01 (SC-1): the Library surface + each tab matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
SHELL-02 (SC-2): the restyled card grids land.
SHELL-03 (SC-3): divergences preserved (ND-C, ND-Z); the shared modal reused, not forked.
</success_criteria>

<artifacts_produced>
- Restyled `LibraryPage` (h1 + count + search header + Agents/Skills/Hooks card grids).
- ND-Z captured (Library agent-detail drawer deferred to Phase 41; shared modal untouched).
</artifacts_produced>

<output>
Create `.planning/phases/40-shell-mock-fidelity-restyle-b6/40-03-SUMMARY.md` when done.
</output>
