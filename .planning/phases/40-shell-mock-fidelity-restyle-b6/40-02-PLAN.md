---
phase: 40-shell-mock-fidelity-restyle-b6
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/components/catalog/HomeLaunchGrid.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/catalog/HomeLaunchGrid.test.tsx
  - frontend/e2e/tests/ts-b.selection.spec.ts
  - frontend/e2e/tests/ts-c.input-trigger.spec.ts
autonomous: false
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]
must_haves:
  truths:
    - "The Home prompt renders UNDER the h1 with an Attach + Build action row (no Voice — ND-X), replacing the 'Start with a prompt' box that was above the h1"
    - "The deliverables render as a 3×2 CARD GRID (from the live /api/workflows list — ND-D), not a vertical row list"
    - "A 'Jump back in' recents strip renders from live recent runs (ND-D); it is absent/graceful when there are no recents"
    - "Selecting a deliverable card still launches its run (the existing launch fork is preserved); the Build action submits the prompt"
    - "All Home data is live (SC-001/ND-D); the mock's fixed card labels/recents are never cloned"
  artifacts:
    - path: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      provides: "The 3×2 deliverable card grid + Jump-back-in recents, from live data"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      to: "/api/workflows"
      via: "live launchable-row fetch → card grid"
      pattern: "api/workflows"
    - from: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      to: "recent runs"
      via: "Jump-back-in recents strip"
      pattern: "recent"
---

<intended_divergence_note>
Inherit ND-A..D + ND-W..Z from `40-UI-SPEC.md` (canonical in `assemble-shell-gallery.mjs`). This surface exercises: ND-A (brand eyebrow "VelocityAI" not "HEXAWARE"), ND-D (the deliverable cards + recents come from LIVE `/api/workflows` + real recent runs, never the mock's fixed 6 labels / hardcoded recents), and **ND-X** (the mock's prompt "Voice" affordance is NOT reproduced — there is no product voice-input capability; Attach IS reproduced as it is a real image-input feature). Any OTHER visual departure from the Home mock is a fidelity bug.
</intended_divergence_note>

<objective>
Restyle the Home landing to the mock's composition (CONTEXT specifics #1): the prompt moves UNDER the h1 with an Attach + **Build** action row (no Voice — ND-X); the deliverables become a **3×2 card grid** bound to the live `/api/workflows` list (ND-D), replacing the vertical row list; and a **"Jump back in" recents** strip renders from live recent runs. Keep every existing behavior — the SC-001 data-driven launch fork, the per-card real estimates (Phase 38), the locked/allowed gating, saved-workflow launch. Re-anchor the two Home mocked-e2e specs to the new card-grid layout.

Purpose: Home is the product's front door — it must match the mock as-is.
Output: `HomeLaunchGrid` renders the card grid + recents from live data; the DashboardLayout prompt is relocated + restyled to the mock; ts-b/ts-c re-anchored.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-CONTEXT.md
@.planning/phases/40-shell-mock-fidelity-restyle-b6/40-UI-SPEC.md
@frontend/src/components/catalog/HomeLaunchGrid.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle Home — prompt under the h1 (Attach + Build, no Voice) + 3×2 deliverable card grid from live rows</name>
  <files>frontend/src/components/catalog/HomeLaunchGrid.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/catalog/HomeLaunchGrid.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (Home `sc-if isHome` :91 — eyebrow + "What would you like to build" h1, the prompt UNDER the h1 with the Attach/Voice/Build action row, the "Or start from a deliverable" 3×2 card grid, the "Jump back in" recents)
    - frontend/src/components/catalog/HomeLaunchGrid.tsx (eyebrow :150, h1 :153, subtitle :158, "Create workflow" :167, the vertical row list :186-256 to become a card grid; the live `/api/workflows` fetch :59-136; the per-card estimate at :239-249 to keep)
    - frontend/src/components/layout/DashboardLayout.tsx (the fused-Home region :1488-1519 — the "Start with a prompt" `home-launch-prompt` textarea :1502-1510 ABOVE the grid mount :1519; `handleHomeSelectFeature` + `handleLaunchSaved` wiring)
  </read_first>
  <behavior>
    - Home renders (top→bottom): eyebrow (ND-A "VelocityAI") → h1 → the prompt input WITH an Attach button + a Build submit button (NO Voice button) → the "Or start from a deliverable" card grid → the recents strip (recents land in Task 2).
    - The deliverable cards render as a responsive 3-column grid (3×2 for the 6 live rows) from `/api/workflows` — count/labels are DATA-DRIVEN (a 7th live row makes 7 cards), never the mock's fixed 6.
    - Clicking a deliverable card triggers the SAME launch path as the current row (`onSelectFeature(row.id)`); the Build button submits the prompt text down the existing launch fork; locked rows keep the lock affordance.
  </behavior>
  <action>
    Relocate + restyle the Home prompt to the mock: in `DashboardLayout.tsx`, move the `home-launch-prompt` textarea from ABOVE the h1 (:1502-1510) to UNDER the h1 (either by passing it into `HomeLaunchGrid` as a slot/prop or by re-ordering the fused-Home block so the grid's h1 renders first), and restyle it to the mock's input shell with an **Attach** affordance + a primary **Build** submit button — do NOT add a Voice button (ND-X). Keep the existing prompt→launch wiring (the brief text still flows into the chosen deliverable's launch, `handleHomeSelectFeature`). In `HomeLaunchGrid.tsx`, convert the vertical `divide-y` row list (:186-256) into the mock's responsive **card grid** (Tailwind `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`, matching the SavedWorkflowsPage grid idiom :298 for token consistency) — each card carries the row's `display_name` (italic serif per the mock), description, the Phase-38 real estimate (`~N agents · ~Xm`, keep :239-249), and the lock/allowed state; the whole card is the click target → `onSelectFeature(row.id)`. Keep the "Create workflow" affordance (relabel/position per the mock's Custom-workflow card if cleaner). Route ALL color/type through the existing tokens (ink ramp, brand, surface-card, line-border) — no raw hex. Do NOT clone the mock's fixed card set — the grid is the live `/api/workflows` list (ND-D). If the legacy `components/home/CreationHub.tsx` twin is provably unused, delete it (INV-3) — otherwise leave it and note why. NO fenced code in this action — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/home__shell.png` + `home__shellfull.png` vs the `Hexaware Workspace v2.dc.html` Home surface, via the 40-01 shell oracle, closed to ND-A..D + ND-W..Z (esp. ND-D live cards, ND-X no Voice). This autonomous task generates the current/* capture(s) mechanically (tsc clean + vitest green + the banned-literal greps); the human fidelity approval is this plan's trailing checkpoint task.
    - The deliverable rows render as a grid, not a `divide-y` list (grep HomeLaunchGrid → the row-list `divide-y` idiom is gone; a `grid-cols` class present); the cards derive from the live fetch (a mock-injected 7-row `/api/workflows` renders 7 cards — vitest).
    - NO Voice button renders on Home (grep for a voice/mic affordance → absent; ND-X); the Attach + Build affordances render.
    - `npx --prefix frontend tsc --noEmit` clean; `HomeLaunchGrid.test.tsx` asserts the grid card count from live rows + the Build submit + no-Voice.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/catalog/HomeLaunchGrid.test.tsx</automated>
  </verify>
  <done>Home renders the mock's prompt-under-h1 (Attach + Build, no Voice) + a live-data 3×2 card grid; existing launch fork + estimates preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: "Jump back in" recents from live runs + re-anchor the Home mocked-e2e specs</name>
  <files>frontend/src/components/catalog/HomeLaunchGrid.tsx, frontend/src/components/catalog/HomeLaunchGrid.test.tsx, frontend/e2e/tests/ts-b.selection.spec.ts, frontend/e2e/tests/ts-c.input-trigger.spec.ts</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (the "Jump back in" recents strip on Home — card composition: title, type, relative time, status)
    - frontend/src/components/history/WorkflowHistory.tsx (the `/api/runs` fetch + `WorkflowRun` shape to reuse for recents — do NOT invent a new endpoint; recents = the N most-recent runs)
    - frontend/e2e/tests/ts-b.selection.spec.ts + ts-c.input-trigger.spec.ts (the stale Home locators from the pre-grid layout — per Phase-39 deferred-item D-39-07-1: ts-b needs the per-row "Inspect" strict-mode fix + the migration NEW-pill / 2-tile behavior on the new grid; ts-c needs the input→execution swap on the server `pipeline_start`)
  </read_first>
  <behavior>
    - A "Jump back in" strip renders the N most-recent runs from live data (title · type · relative time · status), each deep-linking to its run; when there are no recent runs the strip is absent (graceful), never a fabricated placeholder.
    - ts-b.selection + ts-c.input-trigger pass against the new card-grid Home (card selection launches; the Build/prompt trigger swaps input→execution on the server frame).
  </behavior>
  <action>
    Add the mock's "Jump back in" recents strip to `HomeLaunchGrid` (or a small `HomeRecents` sub-component in the same file/dir), sourced from the live recent runs (reuse the `/api/runs` list + `WorkflowRun` shape WorkflowHistory already uses — do NOT add a new endpoint; take the N most recent). Each recent card shows title · humanized type · relative time · status chip, and clicking it opens that run (reuse the existing run-open/deep-link path). Render nothing when there are no recents (ND-D — never a cloned/fabricated recent). Then RE-ANCHOR the two Home mocked-e2e specs to the new grid layout (the Phase-32/39 re-anchor pattern): update `ts-b.selection.spec.ts` locators to the card-grid cards (fix the per-card "Inspect" strict-mode ambiguity; keep the migration NEW-pill / 2-tile assertion against the new card), and `ts-c.input-trigger.spec.ts` to the relocated prompt + Build submit (the view swaps input→execution on the server `pipeline_start`). Only re-anchor locators/assertions to the redesigned layout — do NOT weaken the behavioral assertions.
  </action>
  <acceptance_criteria>
    - The recents strip renders the live recent runs (vitest: seeded runs → N recent cards; empty runs → strip absent); each card deep-links to its run.
    - `ts-b.selection.spec.ts` + `ts-c.input-trigger.spec.ts` PASS against the new Home (mocked project) — the re-anchored locators resolve, the selection→launch + prompt→execution assertions hold.
    - SCREENSHOT-DIFF re-run of `home__shellfull.png` shows the recents strip, closed to ND-D. This autonomous task generates the current/* capture(s) mechanically; the human approval is the trailing checkpoint.
    - `npx --prefix frontend tsc --noEmit` clean; `HomeLaunchGrid.test.tsx` recents cases green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/catalog/HomeLaunchGrid.test.tsx && npx playwright test --project=mocked ts-b.selection ts-c.input-trigger</automated>
  </verify>
  <done>Home shows live recents; the two Home e2e specs are re-anchored green to the card-grid layout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fidelity checkpoint — human review of the Home surface vs the mock (closed to ND-A..D + ND-W..Z)</name>
  <files>frontend/e2e/fidelity/gallery-shell.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (that is the exact prose-claim drift D40-5 exists to prevent; a pixel-diff-vs-mock is invalid because ND-D live data never equals the mock's hardcoded values). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/home__*), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-shell-gallery.mjs --surface home`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Home surface — prompt-under-h1 (Attach + Build, no Voice), the live-data 3×2 deliverable card grid, and the "Jump back in" recents — with the prior autonomous tasks' mechanical gates already green (npx tsc --noEmit clean, HomeLaunchGrid vitest green, ts-b/ts-c re-anchored green, no-Voice grep 0).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-shell.html and find the `home__shell` / `home__shellfull` sections. Compare the mock (left — Hexaware Workspace v2 Home) against ours (right). Confirm every visible difference is one of the registered ND-A (brand) / ND-D (live cards + recents) / ND-X (no Voice) divergences (captioned "expected — ignore"); any OTHER visual departure (ordering, grid geometry, card composition, prompt placement) is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the Home surface matches its mock closed to the intended-divergence register, or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| run/workflow data → Home cards + recents | server-provided titles/labels rendered in the UI |
| prompt text → launch | user input carried into the run launch |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Tampering (XSS) | card + recents rendering | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new Home code (grep → 0) |
| T-40-02-02 | Spoofing | recents deep-link / launch | accept | reuses the existing owner-scoped run-open + launch paths; no new authority path |
| T-40-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs (reuses lucide-react + tokens). |
</threat_model>

<verification>
- Home matches the mock per the shell-gallery oracle, closed to ND-A..D + ND-W..Z (esp. ND-D live cards/recents, ND-X no Voice).
- Cards + recents derive from live data (SC-001/ND-D); the launch fork + Phase-38 estimates preserved.
- `npx tsc --noEmit` clean; HomeLaunchGrid vitest green; ts-b/ts-c re-anchored green.
</verification>

<success_criteria>
SHELL-01 (SC-1): the Home surface matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
SHELL-02 (SC-2): the net-new card grid + recents land on live data.
SHELL-03 (SC-3): live data; divergences preserved; existing launch/estimate behavior reused.
SHELL-04 (SC-4): the Home mocked-e2e specs (ts-b/ts-c) are re-anchored green.
</success_criteria>

<artifacts_produced>
- Restyled `HomeLaunchGrid` (3×2 live-data card grid + "Jump back in" recents).
- Relocated + restyled Home prompt in `DashboardLayout` (under the h1, Attach + Build, no Voice).
- Re-anchored `ts-b.selection` + `ts-c.input-trigger` mocked-e2e specs.
- (Possibly) deleted the unused `CreationHub` twin (INV-3).
</artifacts_produced>

<output>
Create `.planning/phases/40-shell-mock-fidelity-restyle-b6/40-02-SUMMARY.md` when done.
</output>
