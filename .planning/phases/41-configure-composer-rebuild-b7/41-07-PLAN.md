---
phase: 41-configure-composer-rebuild-b7
plan: 07
type: execute
wave: 5
depends_on: ["41-04"]
files_modified:
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/library/LibraryPage.tsx
  - frontend/src/components/library/LibraryPage.test.tsx
autonomous: false
requirements: [CMPUI-05]
must_haves:
  truths:
    - "The Library agent-detail opens as a right-side DRAWER (Overview / Skills / Hooks / Config) matching the mock's drawerOpen surface — no longer a centered modal (closes the Phase-40 ND-Z deferral + SHELL-04's Agent-drawer clause)"
    - "The drawer is the restructured shared AgentCapabilitiesModal (Overview/Skills/Hooks/Config tabs incl. AgentPromptSection + AdvancedExpander) — the shared inspector is restructured in place, not forked"
    - "The Library surface + all other Phase-40-signed-off Library composition (h1 + count + search + Agents/Skills/Hooks card grids) is unchanged except the agent-detail affordance now opens the drawer"
  artifacts:
    - path: "frontend/src/components/library/LibraryPage.tsx"
      provides: "Library agent-detail opens the right-side drawer (Overview/Skills/Hooks/Config)"
      contains: "drawer"
  key_links:
    - from: "frontend/src/components/library/LibraryPage.tsx"
      to: "AgentCapabilitiesModal (restructured as a right drawer)"
      via: "agent-detail → right-side drawer"
      pattern: "AgentCapabilitiesModal|Drawer"
---

<intended_divergence_note>
Inherit ND-A..D + ND-AE..AJ from `41-UI-SPEC.md`. This plan CLOSES the Phase-40 ND-Z deferral (the Library agent-detail drawer was deferred to Phase 41 because `AgentCapabilitiesModal` is composer-owned). After this plan, ND-Z no longer applies to the Library agent-detail (the drawer is built). Carried divergences on the rest of the Library surface (ND-A/B/C/D) are unchanged; the Phase-40-signed-off Library composition is otherwise untouched. Any visual departure of the drawer from the mock's `drawerOpen` surface is a fidelity bug.
</intended_divergence_note>

<objective>
Build the Library agent-detail **right-side drawer** (Overview / Skills / Hooks / Config) to the mock's `drawerOpen` surface — closing the Phase-40 ND-Z deferral and SHELL-04's Agent-drawer clause (CMPUI-05). Restructure the shared `AgentCapabilitiesModal` (the composer-owned inspector Phase 40 kept as a centered modal) into a right-side drawer form, and open it from the Library agent card. The shared inspector is restructured IN PLACE (reused by the composer's per-agent inspector too) — not forked. Leave the rest of the Phase-40-signed-off Library composition (h1 + count + search + Agents/Skills/Hooks card grids) unchanged.

Purpose: complete the Library surface to the mock now that the composer rebuild owns the shared inspector.
Output: the Library agent-detail opens as the mock's right drawer, captured for the fidelity gate.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-configure-composer-rebuild-b7/41-CONTEXT.md
@.planning/phases/41-configure-composer-rebuild-b7/41-UI-SPEC.md
@frontend/src/components/library/LibraryPage.tsx
@frontend/src/components/workflow/AgentsPopup.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure AgentCapabilitiesModal into a right-side drawer + open it from the Library agent card</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx, frontend/src/components/library/LibraryPage.tsx, frontend/src/components/library/LibraryPage.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (the Library agent-detail `drawerOpen` :709 — the right-side drawer composition: Overview / Skills / Hooks / Config tabs, the avatar/name/role header, the slide-in-from-right chrome)
    - frontend/src/components/workflow/AgentsPopup.tsx (`AgentCapabilitiesModal` :410 — the EXPORTED 4-tab inspector [Overview/Skills/Hooks/Config; Config = `AgentPromptSection` :209 + `AdvancedExpander` :1412; `surfaceOnly` in the drawer]; restructure its shell from a centered modal into a right-side drawer while keeping the tab bodies)
    - frontend/src/components/library/LibraryPage.tsx (the agent card → `selectedAgent` → `AgentCapabilitiesModal` :348 — the open path to repoint at the drawer; the Phase-40-signed-off h1 + count + search + card grids to LEAVE unchanged)
  </read_first>
  <action>
    Restructure the shared `AgentCapabilitiesModal` shell from a centered modal into a right-side DRAWER (slide-in-from-right, Overview/Skills/Hooks/Config tab bar, avatar/name/role header) matching the mock's `drawerOpen` surface — KEEP the tab bodies (Overview/Skills/Hooks + the Config tab's `AgentPromptSection` + `AdvancedExpander`) intact; this is a shell restructure, not a fork (the composer's per-agent inspector keeps using the same component). Repoint the Library agent card's open path (`selectedAgent` → the drawer) so clicking an agent opens the right drawer instead of the centered modal. Leave the rest of the Library composition (h1 + count + search + Agents/Skills/Hooks card grids — Phase-40-signed-off) unchanged. Route all color/type through the existing tokens. Update `LibraryPage.test.tsx` to assert the agent-detail opens the drawer (a right-side panel with the four tabs). NO fenced code — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/library-agent-detail__shell.png` vs the `Hexaware Workspace v2.dc.html` `drawerOpen` Library agent-detail surface, via the 41-01 oracle, closed to ND-A..D (ND-Z now RESOLVED — the drawer is built). This autonomous task generates the current/* capture; the human approval is the trailing checkpoint.
    - The Library agent-detail opens as a right-side drawer with Overview/Skills/Hooks/Config tabs (vitest: the drawer + four tabs render on agent-select); the rest of the Library composition is unchanged.
    - `AgentCapabilitiesModal` is restructured in place (not forked — grep: still a single shared component reused by the composer); `npx tsc --noEmit` clean; `LibraryPage.test.tsx` green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/library/LibraryPage.test.tsx</automated>
  </verify>
  <done>The Library agent-detail opens as the mock's right drawer (Overview/Skills/Hooks/Config), restructured from the shared inspector (not forked); the rest of the Library surface is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of the Library agent-detail drawer vs the mock (ND-Z resolved)</name>
  <files>frontend/e2e/fidelity/gallery-phase41.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match. Regenerate this surface's section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/library-agent-detail*), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-phase41-gallery.mjs --surface library` (or the agent-detail tag). Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Library agent-detail right-side drawer (Overview/Skills/Hooks/Config), restructured from the shared AgentCapabilitiesModal — with the prior task's mechanical gates already green (tsc clean, LibraryPage vitest green, the inspector restructured in place not forked). This closes the Phase-40 ND-Z deferral.</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-phase41.html and find the Library `agent-detail` drawer section. Compare the mock (left) against ours (right). Confirm the drawer chrome (slide-in-from-right, the Overview/Skills/Hooks/Config tab bar, the avatar/name/role header) matches the mock's `drawerOpen` surface; ND-Z is now RESOLVED (the drawer is built, no longer deferred). Any visual departure of the drawer from the mock is a fidelity gap to fix before approving; confirm the rest of the Library composition (Phase-40-signed-off) is unchanged.</how-to-verify>
  <resume-signal>Type "approved" if the Library agent-detail drawer matches its mock (ND-Z resolved), or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| /api/agents + /api/capabilities → the drawer | live owner-scoped agent + capability data rendered in the drawer |
| Config tab prompt edits → AgentPromptSection | owner-scoped custom-prompt mutations (existing PUT/DELETE) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-01 | Tampering (XSS) | drawer tab bodies | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the restructured drawer (grep → 0) |
| T-41-07-02 | Spoofing | Config tab prompt PUT/DELETE | accept | reuses the existing authenticated owner-scoped `AgentPromptSection` mutations; no new authority path |
| T-41-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs — shell restructure + tokens; no new dependency. |
</threat_model>

<verification>
- The Library agent-detail drawer matches the mock's `drawerOpen` surface per the Phase-41 gallery oracle (ND-Z resolved); the rest of the Library composition is unchanged.
- `AgentCapabilitiesModal` restructured in place (not forked); the composer's inspector still reuses it.
- `npx tsc --noEmit` clean; LibraryPage vitest green.
</verification>

<success_criteria>
CMPUI-05: the Library agent-detail right-drawer is rebuilt from AgentCapabilitiesModal — closing SHELL-04's Agent-drawer clause + the Phase-40 ND-Z deferral, screenshot-diff-proven + human-signed-off.
</success_criteria>

<artifacts_produced>
- `AgentCapabilitiesModal` restructured into a right-side drawer (shared, reused by the composer inspector).
- Library agent-detail opens the drawer; ND-Z resolved.
</artifacts_produced>

<output>
Create `.planning/phases/41-configure-composer-rebuild-b7/41-07-SUMMARY.md` when done.
</output>
</content>
