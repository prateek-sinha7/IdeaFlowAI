---
phase: 41-configure-composer-rebuild-b7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/fixtures/mockApi.ts
  - frontend/e2e/fixtures/dashboard.ts
  - frontend/e2e/tests/zzz-shell-baseline.spec.ts
  - frontend/e2e/fidelity/assemble-phase41-gallery.mjs
  - frontend/e2e/fidelity/README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [HARN-01]
must_haves:
  truths:
    - "The Configure Templates + Design System accordions/overlays render POPULATED in mocked mode — `/api/prototype/templates`, `/api/prototype/design-systems`, `/api/ppt/templates` are stubbed with representative rows in the shared MockApi (D-02)"
    - "The Phase-41 fidelity oracle writes repo-relative outputs, filters by `--surface`, points its current-side tags at the real new surfaces (config / composer-simple / composer-canvas), and carries the finalized ND-AE..AJ register captioned 'expected — ignore'"
    - "The current-side capture spec has drivers that reach the unified Configure + the Composer Simple + the Composer Canvas surfaces once built (guarded so a normal run skips), plus a canvas capture target for the design-match gate"
    - "The B7 requirement family (CFGUI-01/02 · CMPUI-01..05 · HARN-01) is defined in REQUIREMENTS.md, SHELL-04 is marked closed-by-Phase-41, and the Phase 41 ROADMAP carries a **Requirements:** line"
  artifacts:
    - path: "frontend/e2e/fixtures/mockApi.ts"
      provides: "Stubs for /api/prototype/templates + /api/prototype/design-systems + /api/ppt/templates (seeded) + setters"
      contains: "prototype/templates"
    - path: "frontend/e2e/fidelity/assemble-phase41-gallery.mjs"
      provides: "Formalized Phase-41 gallery assembler: repo-relative, --surface filter, ND-AE..AJ, real current-side tags"
      contains: "ND-AJ"
  key_links:
    - from: "frontend/e2e/fixtures/mockApi.ts"
      to: "/api/prototype/templates + /api/prototype/design-systems + /api/ppt/templates"
      via: "route handlers returning representative arrays"
      pattern: "prototype/(templates|design-systems)"
    - from: "frontend/e2e/fidelity/assemble-phase41-gallery.mjs"
      to: "shots-shell/target + shots-shell/current"
      via: "side-by-side gallery with --surface filter + the canvas proposal iframe"
      pattern: "assemble-phase41-gallery"
---

<objective>
Stand up everything the Configure + Composer surface plans diff against — FIRST (Wave 1), mirroring how 40-01 unblocked the shell surfaces. (1) Stub the three unstubbed Configure data APIs (`/api/prototype/templates`, `/api/prototype/design-systems`, `/api/ppt/templates`) in the SHARED MockApi with representative seed rows so the Templates + Design-System accordions/overlays render populated for a fair fidelity gate (D-02 / D-CFG-STUBS). (2) FORMALIZE the already-scaffolded `assemble-phase41-gallery.mjs`: repo-relative default, a `--surface` filter, current-side tags pointed at the real NEW surfaces (config / composer-simple / composer-canvas), and the finalized ND-AE..AJ register; extend the current-side capture spec with guarded drivers for those surfaces + a canvas capture target. (3) OWN the B7 requirement docs (CFGUI/CMPUI/HARN) + mark SHELL-04 closed-by-Phase-41 + add the Phase 41 ROADMAP `**Requirements:**` line.

This lands FIRST because no Configure/Composer surface plan can close its fidelity checkpoint until the gallery renders every in-scope surface with populated data.

Purpose: makes the acceptance oracle real + mechanically-regenerable and unblocks every surface plan's fidelity checkpoint (per D-02).
Output: a Configure surface that renders its template/DS overlays in mocked mode + a formalized Phase-41 gallery + the B7 requirement docs.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-configure-composer-rebuild-b7/41-CONTEXT.md
@.planning/phases/41-configure-composer-rebuild-b7/41-UI-SPEC.md
@frontend/e2e/fixtures/mockApi.ts
@frontend/e2e/tests/zzz-shell-baseline.spec.ts
@frontend/e2e/fidelity/assemble-phase41-gallery.mjs
@frontend/e2e/fidelity/assemble-shell-gallery.mjs
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
The full register lives in `41-UI-SPEC.md` (ND-A..D carried + ND-AE..AJ new). Task 2 makes `assemble-phase41-gallery.mjs`'s ND array the CANONICAL Phase-41 source of truth: finalize the scaffolded ND-AE..AI candidates AND add **ND-AJ** (Composer Canvas reference = the approved proposal, not a `.dc.html` mock — a design-match gate). Caption them "expected — ignore". Do NOT wire an automated pixel-diff (ND-D live data ≠ the mock's fixed values). The Phase-40 ND-A..AD register in `assemble-shell-gallery.mjs` is UNTOUCHED — Phase 41 continues at ND-AE in the Phase-41 assembler only.
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: Stub the Configure template/DS/ppt APIs + seed representative rows in the shared MockApi (D-02)</name>
  <files>frontend/e2e/fixtures/mockApi.ts, frontend/e2e/fixtures/dashboard.ts</files>
  <read_first>
    - frontend/e2e/fixtures/mockApi.ts (the route dispatcher — mirror the 40-01 `/api/user-workflows` stub + the audit-seeding idiom; the existing `setRuns`/`setWorkflows`/`setUserWorkflows` setters to parallel)
    - frontend/src/lib/api.ts (`listPrototypeTemplates`, `listDesignSystems`, `listPPTTemplates` — the exact response shapes the Configure accordions bind: template id/name/preview/kind (DESKTOP/MOBILE) + design-system id/name/category/swatches + ppt-template id/name)
    - frontend/src/components/workflow/ConfigureScreen.tsx (the template/DS fetch :146-149 + `acceptsTemplateDs` gate :132 — what a populated accordion needs)
    - frontend/src/components/prototype/{TemplateGallery,DesignSystemPicker}.tsx + frontend/src/components/ppt/PPTTemplateGallery.tsx (the card/grid shapes the overlays render)
  </read_first>
  <action>
    In MockApi add `GET /api/prototype/templates`, `GET /api/prototype/design-systems`, `GET /api/ppt/templates` route handlers (mirror the `/api/user-workflows` json() idiom that 40-01 added). Read `frontend/src/lib/api.ts` for the EXACT `listPrototypeTemplates` / `listDesignSystems` / `listPPTTemplates` response shapes and seed ≥3 representative rows each (a "No template / BLANK" option + 2 real web templates with DESKTOP/MOBILE tags for prototype templates; ≥1 grouped category with swatch dots for design systems — enough for the `overlayDs` grouped-chips + the `overlayTemplate` grid to render; ≥2 deck templates for ppt). Expose `setPrototypeTemplates(rows)` / `setDesignSystems(rows)` / `setPPTTemplates(rows)` setters + an empty-variant path so the "None selected" / empty-state (ND-AF) can be captured. In `dashboard.ts`, extend the existing seeding option (the 40-01 `seededShell()`/setters) so the Phase-41 capture spec installs the seeded template/DS/ppt data before `goto()` — do NOT change the default-empty behavior other specs rely on (gate behind the option/setters; tsc identity for existing specs). This is capture scaffolding under SC-001/ND-D: production still returns real registry data; label it NOT production data.
  </action>
  <acceptance_criteria>
    - `GET /api/prototype/templates`, `/api/prototype/design-systems`, `/api/ppt/templates` each return a representative ARRAY by default (empty variant available) — the Configure Templates + Design-System accordions/overlays render populated in mocked mode.
    - Seeding is OPT-IN (new setters/option) — existing specs that expect empty defaults are byte-unchanged; `npx tsc --noEmit -p e2e/tsconfig.json` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "prototype/templates" e2e/fixtures/mockApi.ts && grep -q "design-systems" e2e/fixtures/mockApi.ts && grep -q "ppt/templates" e2e/fixtures/mockApi.ts && grep -q "setPrototypeTemplates" e2e/fixtures/mockApi.ts && npx tsc --noEmit -p e2e/tsconfig.json 2>&1 | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <done>The three Configure data APIs are stubbed + seeded (opt-in); the Templates/Design-System accordions render populated in mocked mode; existing specs unregressed.</done>
</task>

<task type="auto">
  <name>Task 2: Formalize the Phase-41 oracle — repo-relative, --surface filter, ND-AE..AJ, real current-side tags + capture drivers + a canvas target</name>
  <files>frontend/e2e/fidelity/assemble-phase41-gallery.mjs, frontend/e2e/tests/zzz-shell-baseline.spec.ts, frontend/e2e/fidelity/README.md</files>
  <read_first>
    - frontend/e2e/fidelity/assemble-phase41-gallery.mjs (the scaffolded assembler — the `ND` array with ND-AE..AI :81-90 to FINALIZE + extend with ND-AJ; the current-side tags that currently point at placeholders `agent-detail__shell` for composer-simple + `config-*` for configure; the canvas proposal iframe path :44)
    - frontend/e2e/fidelity/assemble-shell-gallery.mjs (the 40-01 formalized sibling — the `--surface` filter + repo-relative default + git-ignore pattern to mirror; the ND-A..AD register that STAYS untouched)
    - frontend/e2e/tests/zzz-shell-baseline.spec.ts (the shell capture spec + the `menu()`/`clickNav()` nav drivers + the 40-01 seeding install to reuse for the new surfaces)
    - frontend/e2e/fidelity/capture-shell-mocks.mjs (the TARGET side — already captures `config`/`config-full` + `composer`/`composer-full`; reuse, do NOT rebuild)
  </read_first>
  <action>
    Make the scaffolded `assemble-phase41-gallery.mjs` the RETAINED, repo-relative Phase-41 oracle (mirroring how 40-01 formalized the shell assembler). In `assemble-phase41-gallery.mjs`: default the shots ROOT + OUT to repo-relative `frontend/e2e/fidelity/shots-shell` + `frontend/e2e/fidelity/gallery-phase41.html` when `PHASE41_OUT` is unset (keep the env override); FINALIZE the `ND` array — keep ND-AE..AI, ADD **ND-AJ** (Composer Canvas reference = the approved proposal, a design-match gate — see 41-UI-SPEC); add a `--surface <name>` filter (config / composer / composer-canvas) so a checkpoint regenerates only its section; POINT the current-side tags at the real NEW surfaces — `config__shell` (the unified Configure screen), `config-settings__shellfull` (the Workflow-Settings overlay), `composer-simple__shell` (the Simple view), `composer-canvas__shell` (our built canvas) — instead of the `agent-detail__shell` placeholders (keep the target/mock side + the canvas proposal iframe as-is). In `zzz-shell-baseline.spec.ts`: add GUARDED capture drivers (behind `SHELL_CAPTURE=1`, tolerant of a not-yet-built surface so a normal run + the earlier surfaces still pass) that install the seeded MockApi (Task 1) then navigate to + screenshot the unified Configure surface, the Composer Simple view, and the Composer Canvas view under the `{surface}__shell` / `__shellfull` tag convention; write to the repo-relative `shots-shell/current/`. Extend `README.md` with the Phase-41 capture + assemble commands, the new tags, and the finalized ND-AE..AJ list to ignore. Git-ignore the Phase-41 gallery output (mirror the shell git-ignore). Do NOT rebuild `capture-shell-mocks.mjs`; do NOT touch `assemble-shell-gallery.mjs`'s ND-A..AD register.
  </action>
  <acceptance_criteria>
    - `node frontend/e2e/fidelity/assemble-phase41-gallery.mjs` writes a self-contained `gallery-phase41.html`; the ND caption lists ND-AE..AJ (grep → `ND-AJ` present); `--surface composer` filters the output to only the composer tags; the current-side tags reference `config__shell` / `composer-simple__shell` / `composer-canvas__shell` (not `agent-detail`).
    - `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` runs green (the new Configure/Composer drivers are guarded — they capture when the surface exists and no-op otherwise, so this plan's run does not fail on the not-yet-built surfaces); a normal `npm run e2e` SKIPS the spec.
    - `README.md` documents the Phase-41 commands + the ND-AE..AJ list; `gallery-phase41.html` is git-ignored.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "ND-AJ" e2e/fidelity/assemble-phase41-gallery.mjs && grep -q "composer-simple" e2e/fidelity/assemble-phase41-gallery.mjs && grep -q "surface" e2e/fidelity/assemble-phase41-gallery.mjs && git check-ignore e2e/fidelity/gallery-phase41.html && grep -Eiq "phase.?41|composer|canvas" e2e/fidelity/README.md && node e2e/fidelity/assemble-phase41-gallery.mjs >/dev/null 2>&1; test $? -le 1</automated>
  </verify>
  <done>The Phase-41 oracle writes repo-relative, is `--surface`-filterable, points its current side at the real new surfaces, carries ND-AE..AJ, has guarded capture drivers + a canvas target, and is documented; gallery git-ignored.</done>
</task>

<task type="auto">
  <name>Task 3: Own the B7 requirement docs (CFGUI/CMPUI/HARN) + close SHELL-04 + ROADMAP Requirements line</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the "Shell Convergence" block SHELL-01..05 :267-273 + the status-index table :491-499 — the insertion anchor + entry style; SHELL-04 "Generic Configure surface + Agent drawer + Workflow dialog" is the requirement this phase CLOSES)
    - .planning/ROADMAP.md (the Phase 41 section added at planning time — the `**Requirements:**` / `**Success Criteria:**` lines)
  </read_first>
  <action>
    Ensure (idempotent create-if-absent) that `REQUIREMENTS.md` carries a "Configure Unification + Composer Rebuild (Phase 41 [B7])" subsection after the Shell Convergence block, defining: CFGUI-01 (the unified one-screen Configure — Step-1 brief + four accordions + overlays — matches the mock, screenshot-diff + HUMAN sign-off, closed to the ND register); CFGUI-02 (ConfigureScreen revived as the single screen with a real onLaunch through the existing onStartPipeline seam + in-app nav; the overlapping Configure code in IdeaInputPage + LaunchWizard + WizardStepper DELETED — no dual Configure implementation survives, INV-3); CMPUI-01 (the Composer is a full-page surface with entry from Home + edit-from-My-Workflows and a Simple⇄Canvas toggle, bound to the AgentsPopup shared data model, the modal shell replaced — INV-3); CMPUI-02 (the Simple view matches Hexaware Composer.dc.html — identity card + reorderable agent rows + model picker + override chips + custom prompt + capability palette + skills & hooks + summary rail, screenshot-diff + HUMAN sign-off); CMPUI-03 (the hand-rolled node-graph Canvas view matches the approved proposal composer-canvas-proposal.html — design-match HUMAN sign-off, ND-AJ); CMPUI-04 (Run-once launches through the existing onStartPipeline seam [functional test] + Save-to-catalogue reuses createUserWorkflow; no engine/backend/manifest change, no fabricated cost — ND-AG); CMPUI-05 (the Library agent-detail right-drawer rebuilt from AgentCapabilitiesModal — closes SHELL-04's Agent-drawer clause, ND-Z); HARN-01 (the Configure template/DS/ppt APIs stubbed + seeded + the Phase-41 fidelity oracle formalized, per-surface regenerable, carrying ND-AE..AJ). Mark SHELL-04 as closed-by-Phase-41 (note it in the entry + flip its status-index row to reference Phase 41). Add a `**Requirements:** CFGUI-01, CFGUI-02, CMPUI-01, CMPUI-02, CMPUI-03, CMPUI-04, CMPUI-05, HARN-01` line to the Phase 41 ROADMAP section. Verify every requirement id in the 7 plans' frontmatter resolves to a REQUIREMENTS.md entry (no dangling id).
  </action>
  <acceptance_criteria>
    - `REQUIREMENTS.md` defines CFGUI-01/02, CMPUI-01..05, HARN-01 (grep → all present) and notes SHELL-04 closed-by-Phase-41.
    - `ROADMAP.md` Phase 41 section has the `**Requirements:**` line listing all eight ids.
    - Every requirement id in the Phase 41 plans' `requirements` frontmatter resolves to a REQUIREMENTS.md entry (no dangling id).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Requirements:\*\* CFGUI-01" .planning/ROADMAP.md && for id in CFGUI-01 CFGUI-02 CMPUI-01 CMPUI-02 CMPUI-03 CMPUI-04 CMPUI-05 HARN-01; do grep -q "$id" .planning/REQUIREMENTS.md || exit 1; done</automated>
  </verify>
  <done>The B7 requirement family is defined + owned in REQUIREMENTS.md, SHELL-04 is closed-by-Phase-41, and the Phase 41 ROADMAP references all eight ids; no dangling ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MockApi → app under test | fabricated template/DS/ppt fixtures only; test-only, never shipped |
| capture script → local mock server | reads .dc.html + the approved proposal from fixed local paths; no untrusted network input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Information disclosure | seeded template/DS/ppt scaffolding | accept | test-only fixtures (labelled NOT production data); never bundled into the app; production endpoints return only real registry data |
| T-41-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs — reuses the already-installed Playwright + Node built-ins; no new dependency (D-04 hand-roll extends to the harness). |
</threat_model>

<verification>
- The Configure Templates/Design-System overlays render populated in mocked mode (three APIs stubbed + seeded, opt-in).
- The Phase-41 oracle writes repo-relative, is `SHELL_CAPTURE`-gated + `--surface`-filterable, points its current side at the real new surfaces, carries ND-AE..AJ + a canvas target, and is documented; gallery git-ignored.
- CFGUI/CMPUI/HARN defined + owned; SHELL-04 closed-by-Phase-41; no dangling ids. No new npm dependency; no shipped-code change (fixtures/spec/scripts/docs only).
</verification>

<success_criteria>
HARN-01: the blocked Configure data APIs are stubbed + seeded, the Phase-41 fidelity oracle is formalized (repo-relative, per-surface, ND-AE..AJ, canvas target), and the B7 requirement docs are owned.
</success_criteria>

<artifacts_produced>
- `frontend/e2e/fixtures/mockApi.ts`: `/api/prototype/templates` + `/api/prototype/design-systems` + `/api/ppt/templates` stubs + setters (seeded, opt-in).
- `frontend/e2e/fixtures/dashboard.ts`: the seeded-template/DS install option for the capture spec.
- `frontend/e2e/fidelity/assemble-phase41-gallery.mjs`: formalized (repo-relative, `--surface`, ND-AE..AJ, real current-side tags).
- `frontend/e2e/tests/zzz-shell-baseline.spec.ts`: guarded Configure + Composer-Simple + Composer-Canvas capture drivers.
- `frontend/e2e/fidelity/README.md`: Phase-41 capture + assemble commands + the ND-AE..AJ list.
- `.planning/REQUIREMENTS.md` CFGUI/CMPUI/HARN + SHELL-04 closure + `.planning/ROADMAP.md` `**Requirements:**` line.
</artifacts_produced>

<output>
Create `.planning/phases/41-configure-composer-rebuild-b7/41-01-SUMMARY.md` when done.
</output>
</content>
