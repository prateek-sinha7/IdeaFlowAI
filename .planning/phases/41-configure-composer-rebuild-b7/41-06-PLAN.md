---
phase: 41-configure-composer-rebuild-b7
plan: 06
type: execute
wave: 6
depends_on: ["41-04", "41-05"]
files_modified:
  - frontend/src/components/workflow/composer/ComposerPage.tsx
  - frontend/src/components/workflow/composer/SummaryRail.tsx
  - frontend/src/components/workflow/composer/CanvasConfigRail.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/e2e/tests/composer-run.spec.ts
autonomous: true
requirements: [CMPUI-04]
must_haves:
  truths:
    - "The Composer's 'Run once' (in the Simple Summary rail + the Canvas docked summary) launches the composed workflow through the EXISTING onStartPipeline → startPipeline seam (base_pipeline_type + brief + composed agent ids + selections as extraParams) and lands on the run screen"
    - "Run wiring is ADDITIVE FE only — no engine/backend/manifest/migration change, no new launch contract, no fabricated cost (ND-AG); est. duration stays live"
    - "Save-to-catalogue reuses createUserWorkflow (via NameWorkflowModal) with base_pipeline_type fixed at entry (ND-AH)"
    - "A functional mocked-e2e proves Run-once fires onStartPipeline with the composed type + agent ids and transitions to the run screen"
  artifacts:
    - path: "frontend/e2e/tests/composer-run.spec.ts"
      provides: "Functional test: Composer Run-once launches through onStartPipeline and lands on the run screen"
      contains: "onStartPipeline"
  key_links:
    - from: "frontend/src/components/workflow/composer/ComposerPage.tsx"
      to: "onStartPipeline (DashboardLayout → dashboard/page.tsx startPipeline)"
      via: "Run once → onStartPipeline(base_pipeline_type, brief, agentIds, …)"
      pattern: "onStartPipeline"
---

<intended_divergence_note>
Inherit ND-A..D + ND-AE..AJ from `41-UI-SPEC.md`. This plan is the ONE functional (non-presentation) piece. It exercises ND-AG (Run routes through the REAL launch seam — no fabricated cost, est. duration live) + ND-AH (base_pipeline_type fixed at entry, carried onto both the run launch and the Save-to-catalogue payload). No visual change beyond making the Run-once/Save buttons live — the composer composition was signed off in 41-04/41-05.
</intended_divergence_note>

<objective>
Wire the Composer's "Run once" through the EXISTING launch seam and confirm Save-to-catalogue (D-05 / D-CMP-RUN + D-06 / D-CMP-SAVE). Make the Run-once action (in the Simple Summary rail + the Canvas docked summary) call `onStartPipeline(base_pipeline_type, brief, composedAgentIds, attachedSkills, attachedHooks, extraParams)` — the SAME seam the Configure launch and the revision sites use — so the composed workflow lands on the run screen. This is ADDITIVE FE wiring to an existing seam: NO engine/backend/manifest/migration change, NO new contract, NO fabricated cost (ND-AG). Confirm Save-to-catalogue reuses `createUserWorkflow` (via `NameWorkflowModal`) with `base_pipeline_type` fixed at entry (ND-AH). Prove it with a functional mocked-e2e.

Purpose: the composer must be able to actually RUN what it composes, minimally + additively, without touching the engine.
Output: a live Run-once through the real seam + a functional test; Save-to-catalogue confirmed.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-configure-composer-rebuild-b7/41-CONTEXT.md
@.planning/phases/41-configure-composer-rebuild-b7/41-UI-SPEC.md
@frontend/src/components/workflow/composer/ComposerPage.tsx
@frontend/src/app/dashboard/page.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Run-once → onStartPipeline (Simple + Canvas) + confirm Save-to-catalogue reuses createUserWorkflow</name>
  <files>frontend/src/components/workflow/composer/ComposerPage.tsx, frontend/src/components/workflow/composer/SummaryRail.tsx, frontend/src/components/workflow/composer/CanvasConfigRail.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (the `onStartPipeline` prop :66/:281 + the revision launch call-sites :589-678 + the configure-surface onLaunch wiring added in 41-03 — mirror the exact arg convention; thread a Run callback down to ComposerPage)
    - frontend/src/app/dashboard/page.tsx (`onStartPipeline` :1451 → `startPipeline(type, message, agentIds, skills, hooks, extraParams)` :1475 — the seam; the run transitions the surface to mainView='execution')
    - frontend/src/components/workflow/composer/ComposerPage.tsx + SummaryRail.tsx + CanvasConfigRail.tsx (the Run-once buttons made inert in 41-04/41-05; the composed state — pipelineAgents → agent ids, the brief, base_pipeline_type, SelectionsMap → extraParams)
    - frontend/src/components/workflow/AgentsPopup.tsx (`createUserWorkflow` :1781 + `NameWorkflowModal` — the Save-to-catalogue path to confirm the composer reuses)
  </read_first>
  <action>
    Thread a Run callback from `DashboardLayout` down to `ComposerPage` that calls the EXISTING `onStartPipeline(base_pipeline_type, brief, composedAgentIds, attachedSkills, attachedHooks, extraParams)` — reuse the exact arg convention of the revision launch sites (:589-678) and the 41-03 configure onLaunch (map `pipelineAgents` → agent ids, the composer brief → the message, `SelectionsMap`/gates/selections → `extraParams`). Wire the Run-once buttons in `SummaryRail.tsx` (Simple) + `CanvasConfigRail.tsx` (Canvas docked summary) to that callback; the run then lands on the run screen (mainView='execution' via the unchanged startPipeline path). Do NOT add a new launch contract, a new endpoint, or a fabricated cost — est. duration stays live (ND-AG). Confirm Save-to-catalogue reuses `createUserWorkflow` via `NameWorkflowModal` with `base_pipeline_type` fixed at entry (ND-AH) — wire it if 41-04 left it as a stub. Keep the change minimal + additive. NO fenced code — name identifiers only.
  </action>
  <acceptance_criteria>
    - Run-once in both the Simple Summary rail + the Canvas docked summary calls `onStartPipeline` with the composed base_pipeline_type + agent ids (grep: `onStartPipeline` referenced from the composer Run path).
    - No engine/backend/manifest/migration file is touched (grep the diff → only frontend/ + the e2e spec); no new endpoint / launch contract; no fabricated cost string in the composer summary.
    - Save-to-catalogue calls `createUserWorkflow` with a fixed base_pipeline_type; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "onStartPipeline" src/components/workflow/composer/ComposerPage.tsx && grep -q "createUserWorkflow" src/components/workflow/AgentsPopup.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && [ -z "$(git -C .. diff --name-only HEAD | grep -vE '^(frontend|\.planning)/')" ]</automated>
    <!-- diff-scope gate (BLOCKER-1 fix): every changed path must be under frontend/ or .planning/ — any agents/ backend/ manifest/ migrations/ *.py change fails this plan, machine-enforcing the additive-FE-only isolation the Run-wiring demands. -->
    <notes>Wave 5 sequencing: run 41-05 (Canvas) and 41-07 (Library drawer) SEQUENTIALLY, not concurrently — they share AgentsPopup.tsx as a read-surface (41-07 writes it, 41-05 imports AdvancedExpander/AgentPromptSection from it). 41-04 is file-heavy (~9); the executor should keep the modal-wrapper deletion + ts-e re-anchor tight and lean on the tsc/vitest gates + the checkpoint.</notes>
  </verify>
  <done>Run-once launches the composed workflow through the unchanged onStartPipeline seam (both views); Save-to-catalogue reuses createUserWorkflow; additive FE only.</done>
</task>

<task type="auto">
  <name>Task 2: Functional mocked-e2e — Composer Run-once launches through the seam and lands on the run screen</name>
  <files>frontend/e2e/tests/composer-run.spec.ts</files>
  <read_first>
    - frontend/e2e/tests/*.spec.ts (the mocked-e2e harness idiom — how a spec installs MockApi/MockWs [dashboard.ts], navigates a mainView, asserts a run screen appears; the `ts-e.model-picker` re-anchor from 41-04 for the composer nav pattern)
    - frontend/e2e/fixtures/{mockApi,mockWs,dashboard}.ts (the run-launch mock — asserting a run starts + the execution surface renders)
  </read_first>
  <action>
    Add `composer-run.spec.ts` (mocked project): navigate to the full-page Composer (mainView='composer'), compose/observe a small agent list, click "Run once", and assert the run launches through the seam — the harness observes the launch (the composed base_pipeline_type + agent ids reach the run start) and the surface transitions to the run/execution screen. Cover BOTH the Simple Summary-rail Run-once and (lightly) the Canvas docked Run-once. Keep it a focused functional test of the ONE functional piece; do NOT re-test presentation (the fidelity gates own that). Run it green in the mocked project.
  </action>
  <acceptance_criteria>
    - `composer-run.spec.ts` asserts Run-once from the Composer fires the launch through the seam with the composed type + agent ids and lands on the run/execution screen; it passes in the mocked project.
    - The spec is additive (does not regress the existing mocked suite); `npx tsc --noEmit -p e2e/tsconfig.json` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && test -f e2e/tests/composer-run.spec.ts && grep -q "onStartPipeline\|Run once\|startPipeline\|execution" e2e/tests/composer-run.spec.ts && npx tsc --noEmit -p e2e/tsconfig.json 2>&1 | grep -c "error TS" | grep -qx 0 && npx playwright test --project=mocked composer-run</automated>
  </verify>
  <done>A functional mocked-e2e proves Composer Run-once launches through the existing seam and lands on the run screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Composer Run-once → onStartPipeline → startPipeline | user-composed run crosses into the run engine via the existing owner-scoped seam |
| Save-to-catalogue → createUserWorkflow | user-composed workflow persisted via the existing owner-scoped save |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06-01 | Elevation of privilege | Run-once → onStartPipeline wiring | accept | reuses the EXISTING owner-scoped `onStartPipeline` → `startPipeline` seam (same arg convention as the revision + configure launch sites); no new authority path, no new contract, no engine edit (SC-001) |
| T-41-06-02 | Tampering | composed extraParams | mitigate | pass only the shared-data-model fields (agent ids, selections/gates) the existing launch already accepts; no fabricated cost/metering field |
| T-41-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs — additive FE wiring + one e2e spec; no new dependency. |
</threat_model>

<verification>
- Run-once (Simple + Canvas) launches the composed workflow through the unchanged `onStartPipeline` seam and lands on the run screen (functional mocked-e2e green).
- Additive FE only: no engine/backend/manifest/migration change, no new contract, no fabricated cost (ND-AG); Save-to-catalogue reuses `createUserWorkflow` (ND-AH).
- `npx tsc --noEmit` clean; the mocked suite unregressed.
</verification>

<success_criteria>
CMPUI-04: Run-once launches through the existing onStartPipeline seam (functional test) + Save-to-catalogue reuses createUserWorkflow; no engine/backend/manifest change, no fabricated cost.
</success_criteria>

<artifacts_produced>
- Live Run-once (Simple + Canvas) → onStartPipeline; confirmed Save-to-catalogue → createUserWorkflow.
- `frontend/e2e/tests/composer-run.spec.ts` — the functional launch test.
</artifacts_produced>

<output>
Create `.planning/phases/41-configure-composer-rebuild-b7/41-06-SUMMARY.md` when done.
</output>
</content>
