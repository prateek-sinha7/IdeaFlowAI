---
phase: 41-configure-composer-rebuild-b7
plan: 05
type: execute
wave: 5
depends_on: ["41-04"]
files_modified:
  - frontend/src/components/workflow/composer/CanvasView.tsx
  - frontend/src/components/workflow/composer/CanvasNode.tsx
  - frontend/src/components/workflow/composer/CanvasConfigRail.tsx
  - frontend/src/components/workflow/composer/ComposerPage.tsx
  - frontend/src/components/workflow/composer/CanvasView.test.tsx
autonomous: false
requirements: [CMPUI-03]
must_haves:
  truths:
    - "The Composer's Canvas view is a hand-rolled node-graph: an <svg> edge layer (bezier paths + arrow markers) UNDER absolute-positioned agent node divs, a left→right sequential chain (Brief → agents), with NO graph/dnd library (D-04)"
    - "Each node maps to an agent (avatar · name · role · Core badge · model pill · Validator/Gate/Retry chips · l/r ports); clicking a node opens the right config rail (Model / Overrides Validator·Review-gate·Retry / Custom prompt) bound to SelectionsMap + AgentPromptSection"
    - "Edges reflect the sequential agent order; + insert affordances on edges + at the chain end call the same add-agent path; a docked Run summary sits under the config rail"
    - "The view matches the APPROVED proposal composer-canvas-proposal.html (ND-AJ — a design-match gate, not a .dc.html mock-fidelity gate)"
  artifacts:
    - path: "frontend/src/components/workflow/composer/CanvasView.tsx"
      provides: "The hand-rolled node-graph Canvas view (SVG bezier edges + absolute nodes + config rail + docked run summary)"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/workflow/composer/CanvasView.tsx"
      to: "SelectionsMap + AgentPromptSection (via CanvasConfigRail)"
      via: "click-node → config rail bound to the per-agent levers"
      pattern: "SelectionsMap|AdvancedExpander|AgentPromptSection"
---

<intended_divergence_note>
Inherit ND-A..D + ND-AE..AJ from `41-UI-SPEC.md`. This surface is governed by **ND-AJ**: the Canvas view has NO shipped `.dc.html` mock — its acceptance reference is the APPROVED PROPOSAL `composer-canvas-proposal.html` (a design reference). The gate is a design-match human sign-off (our built canvas side-by-side with the proposal), NOT a mock-fidelity screenshot-diff. Carried: ND-AG (the docked Run summary omits fabricated est. cost — the proposal shows "$3.20" as an illustration; ours omits it, est. duration live), ND-AH (deliverable-type fixed), ND-A/B/C.
</intended_divergence_note>

<objective>
Build the Composer's **Canvas view** — a hand-rolled node-graph visual designer bound to the same shared data model as the Simple view (D-04 / D-CMP-CANVAS; CMPUI-03). Render an `<svg>` edge layer (bezier `path`s + arrow markers) UNDER absolute-positioned agent node divs in a left→right sequential chain (Brief → agents), with a dark Brief trigger pill, per-node ports, + insert affordances on edges + at the chain end, and zoom/Fit controls. Clicking a node opens the right config rail (Model / Overrides / Custom prompt) bound to `SelectionsMap` + `AgentPromptSection`; a docked Run summary sits under the rail. NO graph/dnd library (`package.json` has none; the approved proposal is itself hand-rolled). Acceptance = match the approved proposal `composer-canvas-proposal.html` (ND-AJ — a design-match gate, not a mock-fidelity gate).

Purpose: deliver the user-approved Canvas designer as the second Composer view, hand-rolled per the stack mandate.
Output: the Canvas view wired to the header toggle, bound to the shared data model, captured for the design-match gate.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-configure-composer-rebuild-b7/41-CONTEXT.md
@.planning/phases/41-configure-composer-rebuild-b7/41-UI-SPEC.md
@frontend/src/components/workflow/composer/ComposerPage.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-roll the Canvas view (SVG bezier edges + absolute nodes + config rail + docked run summary) wired to the header toggle</name>
  <files>frontend/src/components/workflow/composer/CanvasView.tsx, frontend/src/components/workflow/composer/CanvasNode.tsx, frontend/src/components/workflow/composer/CanvasConfigRail.tsx, frontend/src/components/workflow/composer/ComposerPage.tsx, frontend/src/components/workflow/composer/CanvasView.test.tsx</files>
  <read_first>
    - /Users/1000060523/.claude/jobs/e660aea4/tmp/phase41-scope/composer-canvas-proposal.html (THE build target — the `:root` token map [brand #3C2CDA, paper #F0EEE7, card #FCFBF7, warm #F6F4EE, amber gate #9A6B1E], the dot-grid `.canvas`, the `svg.edges` bezier `path`s + `marker#arw`/`marker#arwb` arrow markers + the active edge tinted brand, the dark Brief `.pill`, the 178px `.node` cards [avatar · name · role · `.core` badge · `.model` pill · `.ovs` Validator/Gate/Retry chips with on/gate states · l/r `.port`s], the `.ins` + insert affordances on edges + at the chain end, the `.controls` zoom/Fit, and the right `.rail` [`.cfg` selected-node config: Model field · Overrides rowtogs Validator/Review-gate/Retry stepper · Custom-prompt textarea] + the docked `.summary` [Run summary · 4 stat cards Agents/Review-gate/Est-duration/Est-cost · capability chips · Save-to-catalogue/Run once])
    - frontend/src/components/workflow/composer/ComposerPage.tsx (the Simple⇄Canvas toggle + the shared data model props — pipelineAgents order + SelectionsMap + declaredCapabilities + the summary; the Simple view built in 41-04 to mirror the data binding)
    - frontend/src/components/workflow/AgentsPopup.tsx (`AdvancedExpander` :1412 [SelectionsMap levers] + `AgentPromptSection` :209 [custom prompt] — REUSE these inside CanvasConfigRail; the native drag-reorder pattern for node reorder; `getRole` :132 → the Core badge)
  </read_first>
  <behavior>
    - The Canvas branch of the header toggle renders the node-graph; each agent in `pipelineAgents` is a node positioned left→right in sequence, preceded by a dark Brief trigger pill.
    - Edges connect consecutive nodes as bezier `<svg>` paths with arrow markers; the edge into the selected node is tinted brand.
    - Clicking a node marks it selected (brand ring) and the right config rail shows that node's Model + Overrides (Validator / Review-gate / Retry) + Custom prompt, bound to `SelectionsMap[id]` + `AgentPromptSection` — editing a lever updates the same shared state the Simple view reads.
    - A + affordance on an edge (and at the chain end) inserts an agent at that index via the same add-agent path; a node's remove takes it out of `pipelineAgents`.
    - The docked Run summary shows agent count + review-gate count + live est. duration and NO est. cost (ND-AG).
    - No graph/dnd library is imported (hand-rolled SVG + absolute divs).
  </behavior>
  <action>
    Build `composer/CanvasView.tsx` as the hand-rolled node-graph, rendered by the ComposerPage header toggle's Canvas branch. Lay out one `CanvasNode.tsx` per agent in `pipelineAgents`, absolute-positioned left→right in a dot-grid `.canvas`, preceded by a dark Brief trigger pill. Render an `<svg>` edge layer UNDERNEATH the nodes: a bezier `path` between each consecutive node's ports with `marker#arw` (grey) / `marker#arwb` (brand) arrow markers, the edge into the selected node tinted brand — compute the path control points from the node positions (linear sequential chain; NO graph library — D-04). Each `CanvasNode` shows avatar · name · role · a Core badge (`getRole`) · a model pill · Validator/Gate/Retry chips (on/gate states from `SelectionsMap`) · l/r ports; clicking it selects it. Build `CanvasConfigRail.tsx` for the selected node: Model + Overrides (Validator switch · Review-gate switch · Retry stepper) + Custom prompt — REUSE `AdvancedExpander` (SelectionsMap) + `AgentPromptSection`, do NOT re-implement the levers; below it dock the Run summary (agents · review-gates · live est. duration · declared-cap chips · Save-to-catalogue · Run once [inert here; wired in 41-06]) — OMIT est. cost (ND-AG). Add + insert affordances on the edges + at the chain end (call the same add-agent path the Simple view uses) + zoom/Fit controls (`motion` is available for interaction polish). Bind everything to the SAME shared data model as the Simple view (no data rework). Write `CanvasView.test.tsx` FIRST asserting the `<behavior>` cases (node-per-agent, edge count = agents, click-select → rail binds SelectionsMap, no graph-lib import). Route all color/type through the existing tokens (map the proposal's `:root` to the token layer). NO fenced code — name identifiers only.
  </action>
  <acceptance_criteria>
    - DESIGN-MATCH capture: `shots-shell/current/composer-canvas__shell.png` captured via the 41-01 canvas driver, for the design-match gate against the approved proposal (ND-AJ). This autonomous task generates the capture; the human approval is the trailing checkpoint.
    - The Canvas renders one node per agent + bezier edges between consecutive nodes; click-select binds the config rail to `SelectionsMap` + `AgentPromptSection`; + insert + remove mutate `pipelineAgents` (vitest asserts the `<behavior>` cases).
    - NO graph/dnd dependency is imported (grep the composer dir → no reactflow/@xyflow/dagre/elkjs/cytoscape/d3/dnd-kit; only hand-rolled SVG + absolute divs + optionally `motion`).
    - `npx tsc --noEmit` clean; `CanvasView.test.tsx` green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && ! grep -Erq "reactflow|@xyflow|dagre|elkjs|cytoscape|dnd-kit" src/components/workflow/composer/ && grep -q "AgentPromptSection\|AdvancedExpander\|SelectionsMap" src/components/workflow/composer/CanvasConfigRail.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/workflow/composer/CanvasView.test.tsx</automated>
  </verify>
  <done>The hand-rolled Canvas node-graph is wired to the header toggle, bound to the shared data model via the reused levers, with no graph library.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Design-match checkpoint — human review of the Canvas view vs the APPROVED PROPOSAL (ND-AJ, not a mock)</name>
  <files>frontend/e2e/fidelity/gallery-phase41.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — the reference is the APPROVED PROPOSAL, not a `.dc.html` mock (ND-AJ). Regenerate this surface's section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/composer-canvas*), then `node frontend/e2e/fidelity/assemble-phase41-gallery.mjs --surface composer-canvas` (the assembler embeds the approved proposal `composer-canvas-proposal.html` live in a data: iframe next to our capture). Then stop and hand the assembled side-by-side to the reviewer.</action>
  <what-built>The hand-rolled Canvas view — the dot-grid canvas, the `<svg>` bezier edge layer with arrow markers (active edge brand-tinted), the dark Brief trigger pill, the absolute-positioned 178px agent nodes (avatar/name/role/Core/model-pill/override-chips/ports), the + insert affordances + zoom/Fit controls, the right config rail (Model / Overrides / Custom prompt bound to SelectionsMap), and the docked Run summary (no est. cost) — with the prior task's mechanical gates already green (tsc clean, CanvasView vitest green, no graph library).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-phase41.html and find the `composer ▸ Canvas view (APPROVED PROPOSAL)` section. Compare the APPROVED PROPOSAL (left, embedded live) against our built canvas (right). This is a DESIGN-MATCH review (ND-AJ), not a mock-fidelity diff: confirm the composition, node layout, edge routing + arrow markers, the config rail, and the docked run summary match the approved proposal. Confirm the only deliberate divergence is ND-AG (no fabricated est. cost — the proposal shows "$3.20" as an illustration; ours omits it, est. duration live). Any OTHER departure from the proposal (node card composition, edge style, rail fields, control chrome) is a gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the Canvas view matches the approved proposal (closed to ND-AG/AH), or list the specific design-match gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| node config-rail edits → SelectionsMap | user per-agent model/override/prompt edits into the shared config state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05-01 | Tampering (XSS) | node cards + config rail + custom-prompt | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the canvas code (grep → 0); custom prompt is a controlled textarea |
| T-41-05-02 | Elevation of privilege | reused AdvancedExpander levers in the rail | accept | reuses the existing tested `SelectionsMap` levers (same `user_allowed` gating); no new authority path |
| T-41-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs — hand-rolled SVG + absolute divs (D-04); only the already-installed `motion` for interactions; no new dependency. |
</threat_model>

<verification>
- The Canvas view matches the approved proposal (ND-AJ design-match gate) — hand-rolled SVG edges + absolute nodes + config rail + docked run summary, bound to the shared data model.
- No graph/dnd dependency added (D-04); the config rail reuses the exported levers (not forked).
- `npx tsc --noEmit` clean; CanvasView vitest green.
</verification>

<success_criteria>
CMPUI-03: the hand-rolled node-graph Canvas view matches the approved proposal `composer-canvas-proposal.html`, design-match human-signed-off (ND-AJ).
</success_criteria>

<artifacts_produced>
- `composer/CanvasView.tsx` (+ `CanvasNode` / `CanvasConfigRail`) — the hand-rolled node-graph Canvas view wired to the header toggle.
</artifacts_produced>

<output>
Create `.planning/phases/41-configure-composer-rebuild-b7/41-05-SUMMARY.md` when done.
</output>
</content>
