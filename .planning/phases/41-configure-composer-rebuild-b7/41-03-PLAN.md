---
phase: 41-configure-composer-rebuild-b7
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/catalog/HomeLaunchGrid.tsx
  - frontend/src/app/workflow/configure/page.tsx
  - frontend/src/app/workflow/create/page.tsx
  - frontend/src/components/workflow/IdeaInputPage.tsx
  - frontend/src/components/workflow/LaunchWizard.tsx
  - frontend/src/components/workflow/WizardStepper.tsx
autonomous: true
requirements: [CFGUI-02]
must_haves:
  truths:
    - "The unified ConfigureScreen is mounted as a mainView='configure' surface in DashboardLayout with a REAL onLaunch that funnels the composed ComposedLaunchCommand into the existing onStartPipeline → startPipeline seam (no new contract)"
    - "In-app nav reaches the unified Configure: Home deliverable cards + the od_prototype/od_ppt entries route to mainView='configure' (not the old mainView='input' / /workflow/create)"
    - "The overlapping Configure code is DELETED — IdeaInputPage's brief-launch Configure flow + LaunchWizard + WizardStepper are gone; no dual Configure implementation survives (INV-3, no shadow)"
    - "The launch fires a real run through the unchanged onStartPipeline seam — proven by a mocked-e2e that Start-run from the unified Configure lands on the run screen"
  artifacts:
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "mainView='configure' surface mounting ConfigureScreen with a real onLaunch → onStartPipeline"
      contains: "configure"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "onStartPipeline (dashboard/page.tsx → startPipeline)"
      via: "ConfigureScreen onLaunch composes → onStartPipeline"
      pattern: "onStartPipeline"
---

<intended_divergence_note>
Inherit ND-A..D + ND-AE..AJ from `41-UI-SPEC.md`. This plan is a wiring + deletion refactor — it does NOT change the Configure screen's composition (signed off in 41-02); re-capturing `config__shell` must still match the mock. The only divergences in play are carried (ND-AE/AF/AI, unchanged). The INV-3 deletion is the load-bearing outcome: no second Configure implementation may survive.
</intended_divergence_note>

<objective>
Wire the unified Configure screen live and DELETE the superseded split implementations (D-01 / D-CFG-ROUTE — the UNIFY + DELETE half). Mount `ConfigureScreen` as a `mainView="configure"` surface in `DashboardLayout` with a REAL `onLaunch` that funnels the composed `ComposedLaunchCommand` into the EXISTING `onStartPipeline` → `startPipeline` seam (additive wiring, no new contract). Repoint the in-app nav (Home deliverable cards + the od_prototype/od_ppt entries) to `mainView="configure"`. Then DELETE the overlapping Configure code — IdeaInputPage's brief-launch flow (+ the `mainView="input"` surface if it is wholly Configure) + `LaunchWizard.tsx` + `WizardStepper.tsx` + the `/workflow/create` route — so no dual Configure implementation survives (INV-3). Reconcile the touched mocked-e2e specs.

Purpose: unify three impls/two routes into ONE reachable screen driven by the real launch seam, and remove the shadows (INV-3/INV-12).
Output: the unified Configure is nav-reachable + launches real runs; the split impls are gone; e2e re-anchored green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-configure-composer-rebuild-b7/41-CONTEXT.md
@.planning/phases/41-configure-composer-rebuild-b7/41-UI-SPEC.md
@frontend/src/components/layout/DashboardLayout.tsx
@frontend/src/app/dashboard/page.tsx
@frontend/src/components/workflow/ConfigureScreen.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount ConfigureScreen as mainView='configure' with a real onLaunch → onStartPipeline + repoint in-app nav</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/app/dashboard/page.tsx, frontend/src/components/catalog/HomeLaunchGrid.tsx, frontend/src/app/workflow/configure/page.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (the `MainView` union :173 + the surface switch :1479-1660 — add a `configure` member + surface; `onStartPipeline` prop :66/:281 + the existing revision launch call-sites :589-678 to mirror the wiring; `onSelectFeature` :849/:929 currently sets `mainView="input"`)
    - frontend/src/app/dashboard/page.tsx (the `onStartPipeline` prop wiring :1451 → `startPipeline(type, message, agentIds, skills, hooks, extraParams)` :1475 — the seam ConfigureScreen's onLaunch funnels into)
    - frontend/src/components/workflow/ConfigureScreen.tsx (the `ComposedLaunchCommand` :60 shape + `onLaunch` :81 — map its fields to the onStartPipeline args)
    - frontend/src/components/catalog/HomeLaunchGrid.tsx (the deliverable cards + od entries currently routing to /workflow/create or mainView=input)
  </read_first>
  <action>
    Add a `configure` member to the `MainView` union and a `mainView === "configure"` surface in `DashboardLayout` that mounts `ConfigureScreen` (threading the selected deliverable's `workflowId`) with a REAL `onLaunch={(command) => …}` that maps the `ComposedLaunchCommand` fields (deliverable/pipeline type, brief message, composed agent ids, selections/gates, template/DS choices as `extraParams`) into the EXISTING `onStartPipeline(type, message, agentIds, attachedSkills, attachedHooks, extraParams)` call — reuse the exact arg convention of the revision launch sites (:589-678). Do NOT add a new launch contract; `onStartPipeline` → `startPipeline` (dashboard/page.tsx:1451/1475) is unchanged. Repoint the in-app nav: `onSelectFeature` (:849/:929) and the Home deliverable cards (`HomeLaunchGrid`) route to `setMainView("configure")` (threading the chosen deliverable), replacing the old `mainView="input"` (non-od) + the `/workflow/create` push (od_prototype/od_ppt). Reduce `app/workflow/configure/page.tsx` to a thin redirect into the `/dashboard` configure surface (or delete it if nothing else references the route) — the canonical Configure now lives on the shell surface. Route all new chrome through the existing tokens. NO fenced code — name identifiers only.
  </action>
  <acceptance_criteria>
    - `DashboardLayout` has a `mainView === "configure"` surface mounting ConfigureScreen; its onLaunch funnels into `onStartPipeline` (grep: `onStartPipeline` referenced from the configure surface).
    - Home deliverable cards + od entries route to the configure surface (grep: no remaining `mainView="input"` Configure entry / `/workflow/create` push for the launch path).
    - `npx tsc --noEmit` clean; the config capture still matches (re-run `SHELL_CAPTURE=1 … zzz-shell-baseline` config* — no visual regression vs the 41-02 sign-off).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q '"configure"' src/components/layout/DashboardLayout.tsx && grep -q "onStartPipeline" src/components/layout/DashboardLayout.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <done>The unified Configure is a nav-reachable mainView='configure' surface that launches real runs through the unchanged onStartPipeline seam.</done>
</task>

<task type="auto">
  <name>Task 2: DELETE the superseded Configure impls (IdeaInputPage brief-launch + LaunchWizard + WizardStepper + /workflow/create) + reconcile e2e (INV-3)</name>
  <files>frontend/src/components/workflow/IdeaInputPage.tsx, frontend/src/components/workflow/LaunchWizard.tsx, frontend/src/components/workflow/WizardStepper.tsx, frontend/src/app/workflow/create/page.tsx, frontend/src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    - frontend/src/components/workflow/IdeaInputPage.tsx (the brief-launch Configure flow — `handleRun` :347, the Advanced→AgentsPopup mount :795, the inline ReviewGatesSection :789, `handleSaveWorkflow` :397, migration sub-tiles :717 — determine which responsibilities are wholly Configure [delete] vs. anything non-Configure that must migrate to the configure surface first)
    - frontend/src/components/workflow/LaunchWizard.tsx + WizardStepper.tsx (the od_ Template→DS→Discovery wizard — wholly Configure, delete outright once the configure surface + the 41-02 accordions cover od_prototype/od_ppt)
    - frontend/src/components/layout/DashboardLayout.tsx (the `mainView="input"` surface :1627 mount of IdeaInputPage — remove once nothing routes to it)
    - frontend/e2e/tests/*.spec.ts (the mocked specs touching the old launch/wizard/model-picker paths — reconcile to the unified Configure; the Phase-39/40-deferred `ts-e.model-picker` re-anchor is Composer territory → it lands in 41-04, NOT here)
  </read_first>
  <action>
    DELETE the superseded Configure code (INV-3, no shadow): remove `LaunchWizard.tsx` + `WizardStepper.tsx` + the `/workflow/create` route (redirect it into the configure surface or delete the page); remove IdeaInputPage's brief-launch Configure flow and the `mainView="input"` surface mount — if IdeaInputPage is wholly the Configure brief path, delete the component and the `input` MainView member; if any non-Configure responsibility remains, migrate it onto the configure surface FIRST, then delete the Configure half. Ensure the deleted components/exports have no remaining importers (grep 0). Reconcile the mocked-e2e specs that drove the old launch/wizard paths onto the unified Configure surface (re-anchor selectors/nav); leave the `ts-e.model-picker` re-anchor for 41-04 (Composer/agent-config). Run the targeted mocked-e2e for the launch/configure path green. This closes the INV-3 exit gate: exactly one Configure implementation survives.
  </action>
  <acceptance_criteria>
    - `LaunchWizard.tsx`, `WizardStepper.tsx` are deleted; `/workflow/create` no longer renders the wizard (redirect or removed); IdeaInputPage's Configure brief-launch flow is gone (grep: no importer of the deleted components/exports; no second Configure launch path).
    - No dangling imports/routes; `npx tsc --noEmit` clean.
    - The launch/configure mocked-e2e path is re-anchored green (Start-run from the unified Configure lands on the run screen); a normal `npm run e2e` for the touched specs passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && test ! -f src/components/workflow/LaunchWizard.tsx && test ! -f src/components/workflow/WizardStepper.tsx && ! grep -rq "LaunchWizard\|WizardStepper" src/ && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <done>The split Configure impls (IdeaInputPage brief-launch + LaunchWizard + WizardStepper + /workflow/create) are deleted; exactly one Configure implementation survives (INV-3); e2e re-anchored green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Configure onLaunch → onStartPipeline → startPipeline | user-composed launch crosses into the run engine via the existing owner-scoped seam |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Elevation of privilege | the new onLaunch → onStartPipeline wiring | accept | reuses the EXISTING owner-scoped `onStartPipeline` → `startPipeline` seam with the same arg convention as the shipped revision launch sites; no new authority path, no new contract |
| T-41-03-02 | Tampering | dead-code deletion (dangling imports) | mitigate | grep-verify no remaining importer of the deleted components/exports; `tsc --noEmit` clean gate |
| T-41-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs — deletion + wiring only; no new dependency. |
</threat_model>

<verification>
- The unified Configure is a nav-reachable `mainView="configure"` surface that launches real runs through the unchanged `onStartPipeline` seam (mocked-e2e green).
- `LaunchWizard`/`WizardStepper` deleted + IdeaInputPage Configure flow removed + `/workflow/create` retired; no dual Configure implementation survives (INV-3); no dangling imports.
- `npx tsc --noEmit` clean; the config capture still matches the 41-02 sign-off (no visual regression).
</verification>

<success_criteria>
CFGUI-02: ConfigureScreen revived as the single screen with a real onLaunch through the existing seam + in-app nav; the overlapping Configure code deleted — no dual Configure implementation survives (INV-3).
</success_criteria>

<artifacts_produced>
- `DashboardLayout` `mainView="configure"` surface + real onLaunch → onStartPipeline; repointed Home/od nav.
- Deleted: `LaunchWizard.tsx`, `WizardStepper.tsx`, IdeaInputPage's Configure brief-launch flow, the `/workflow/create` wizard route.
- Re-anchored launch/configure mocked-e2e.
</artifacts_produced>

<output>
Create `.planning/phases/41-configure-composer-rebuild-b7/41-03-SUMMARY.md` when done.
</output>
</content>
