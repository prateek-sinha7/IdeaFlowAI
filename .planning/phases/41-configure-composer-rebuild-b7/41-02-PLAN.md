---
phase: 41-configure-composer-rebuild-b7
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - frontend/src/components/workflow/ConfigureScreen.tsx
  - frontend/src/components/workflow/ConfigureScreen.test.tsx
  - frontend/src/components/workflow/configure/TemplatesAccordion.tsx
  - frontend/src/components/workflow/configure/DesignSystemAccordion.tsx
  - frontend/src/components/workflow/configure/ReviewGatesAccordion.tsx
  - frontend/src/components/workflow/configure/WorkflowSettingsAccordion.tsx
autonomous: false
requirements: [CFGUI-01]
must_haves:
  truths:
    - "The Configure screen is ONE 'Configure your run' screen: a top header bar (back · eyebrow · title · Save draft · Start run →), an always-open Step-1 'Describe what you're building' brief card (Attach, no Voice — ND-AI), and the four ADVANCED CONFIGURATION accordions below"
    - "Each accordion (Templates · Design System · Review Gates · Workflow Settings) shows a live 'Currently using: X' summary line + a Browse/Open control that opens the matching overlay (Templates grid · DS grouped-chips · Review-gate manager · Workflow-Settings tab modal)"
    - "The Templates + Design System accordions render ONLY when the deliverable declares opendesign (ND-AE); selections bind to the live registries and read 'None selected' until picked (ND-AF)"
    - "The shipped bodies (TemplateGallery / DesignSystemPicker / ReviewGatesSection / AdvancedExpander) are reused verbatim — not re-implemented"
  artifacts:
    - path: "frontend/src/components/workflow/ConfigureScreen.tsx"
      provides: "The unified one-screen Configure: header bar + Step-1 brief + four summary-line accordions + overlays"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/workflow/ConfigureScreen.tsx"
      to: "TemplateGallery / DesignSystemPicker / ReviewGatesSection / AdvancedExpander"
      via: "reused accordion bodies + Browse/Open overlays"
      pattern: "TemplateGallery|DesignSystemPicker|ReviewGatesSection|AdvancedExpander"
---

<intended_divergence_note>
Inherit ND-A..D + ND-AE..AJ from `41-UI-SPEC.md`. This surface exercises: ND-AE (Templates + Design-System accordions gated on the declared `opendesign` signal — a user_stories/custom run shows only Review Gates + Workflow Settings), ND-AF (selections bind to live registries + read "None selected" until picked; the mock's fabricated "Analytics Hub / Ink & Alabaster / 150 systems / 8 capabilities" are NOT cloned), and ND-AI (the Step-1 brief box shows Attach only, no Voice). Any OTHER visual departure on the header / Step-1 brief / accordions / overlays is a fidelity bug.
</intended_divergence_note>

<objective>
Rebuild the DORMANT `ConfigureScreen.tsx` into the mock's ONE "Configure your run" screen (D-01 / D-CFG-ROUTE — the SCREEN build; the nav-wiring + deletion of the split impls is 41-03). Promote the "Describe" accordion to an always-open Step-1 brief card; add each accordion's live "Currently using: X" summary line + a Browse/Open overlay (Templates grid · DS grouped-chips · Review-gate manager · Workflow-Settings tab modal); move Save-draft + Start-run into a top header bar; reuse the shipped accordion bodies verbatim. Keep the ConfigureScreen `onLaunch` contract intact (it is wired live in 41-03). Runs against the 41-01 template/DS/ppt stubs (D-02).

Purpose: Configure is the pre-run setup surface — its one-screen composition must match the mock before it is wired + the split impls deleted.
Output: `ConfigureScreen` renders the mock's header + Step-1 brief + four summary-line accordions + overlays, captured for the fidelity gate.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-configure-composer-rebuild-b7/41-CONTEXT.md
@.planning/phases/41-configure-composer-rebuild-b7/41-UI-SPEC.md
@frontend/src/components/workflow/ConfigureScreen.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild ConfigureScreen into the one screen — header bar + Step-1 brief + four summary-line accordions + overlays</name>
  <files>frontend/src/components/workflow/ConfigureScreen.tsx, frontend/src/components/workflow/ConfigureScreen.test.tsx, frontend/src/components/workflow/configure/TemplatesAccordion.tsx, frontend/src/components/workflow/configure/DesignSystemAccordion.tsx, frontend/src/components/workflow/configure/ReviewGatesAccordion.tsx, frontend/src/components/workflow/configure/WorkflowSettingsAccordion.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Workspace v2.dc.html (the `isConfig` surface ~:166 — the header bar composition + the Step-1 "① Describe what you're building" brief card + the four "ADVANCED CONFIGURATION" accordions with their "Currently using: X" summary lines; the overlays `overlayTemplate` :827 "Choose a template" grid, `overlayDs` :858 "Choose a design system · N systems" grouped chips, `overlayGates` :876 "Review gates" per-agent toggle, `modalWorkflow` :776 "Workflow configuration" Overview/Skills&Hooks/Capabilities/Context tab-rail)
    - frontend/src/components/workflow/ConfigureScreen.tsx (the current accordions Describe :273 / Templates :289 / Design System :297 / Review Gates :309 / Workflow Settings :312; `acceptsTemplateDs` gate :132; the template/DS fetch :146-149; `ComposedLaunchCommand` :60 + `onLaunch` :81 + `handleLaunch` :238 + `handleLaunch` button :335 — KEEP the onLaunch contract intact)
    - frontend/src/components/prototype/{TemplateGallery,DesignSystemPicker,DiscoveryForm}.tsx + frontend/src/components/workflow/ReviewGatesSection.tsx + frontend/src/components/workflow/AgentsPopup.tsx AdvancedExpander (:1412) — the shipped bodies to REUSE verbatim inside the accordions/overlays
    - frontend/src/components/workflow/IdeaInputPage.tsx (the Step-1 brief textarea :512 + the Attach footer :559-695 — the Attach control pattern to lift into the Step-1 card; do NOT reproduce Voice — ND-AI)
  </read_first>
  <behavior>
    - Given a deliverable that declares `opendesign`, the Configure screen renders all four accordions (Templates, Design System, Review Gates, Workflow Settings).
    - Given a deliverable that does NOT declare `opendesign` (e.g. user_stories/custom), only Review Gates + Workflow Settings render (ND-AE) — Templates + Design System are absent.
    - The Step-1 brief card is always open (not an accordion) and shows an Attach control but NO Voice control (ND-AI).
    - With no selection made, the Templates + Design System summary lines read "None selected" (ND-AF), never a fabricated value.
    - Each accordion's Browse/Open control opens the matching overlay; the overlay renders the reused body (TemplateGallery grid / DesignSystemPicker grouped-chips / ReviewGatesSection / AdvancedExpander).
    - The header bar renders back · eyebrow · title "Configure your run" · Save draft · Start run →; Start run invokes the existing `onLaunch` with the composed `ComposedLaunchCommand`.
  </behavior>
  <action>
    Rebuild `ConfigureScreen` to the mock's one-screen composition. Move Save-draft + Start-run into a top header bar (back · eyebrow · title "Configure your run" · Save draft · Start run →) — Start run keeps invoking the existing `handleLaunch`/`onLaunch` (do NOT change the `ComposedLaunchCommand` contract; 41-03 wires it live). Promote "Describe" from an accordion to an always-open Step-1 brief card (numbered "① Describe what you're building", textarea + an Attach control lifted from IdeaInputPage's footer pattern + a ⌘Enter send affordance) — NO Voice (ND-AI). Extract each of the four accordions into a small `components/workflow/configure/*Accordion.tsx` wrapper that renders: the icon + title, a live "Currently using: X" summary line (bound to the live selection — "None selected" until picked, ND-AF), and a Browse/Open control that opens the matching overlay (Templates → the `overlayTemplate` grid via the reused TemplateGallery; Design System → the `overlayDs` grouped-chips via the reused DesignSystemPicker; Review Gates → the `overlayGates` per-agent manager via the reused ReviewGatesSection; Workflow Settings → the `modalWorkflow` Overview/Skills&Hooks/Capabilities/Context tab modal composed from the reused AdvancedExpander + SkillsHooksTab + CapabilityPaletteSection). Keep the `acceptsTemplateDs` declared-gate (ND-AE) — Templates + Design System only render when the deliverable declares `opendesign`. Reuse the shipped bodies verbatim (do NOT re-implement TemplateGallery/DesignSystemPicker/ReviewGatesSection/AdvancedExpander). Route all color/type through the existing tokens (no raw hex). Write `ConfigureScreen.test.tsx` FIRST asserting the `<behavior>` cases (gated accordions, no-Voice, "None selected", header Start-run → onLaunch). NO fenced code in this action — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/config__shell.png` + the overlay captures (`config-templates`, `config-designsystem`, `config-gates`, `config-settings`) vs the `Hexaware Workspace v2.dc.html` `isConfig` surface + overlays, via the 41-01 oracle, closed to ND-A..D + ND-AE..AJ (esp. ND-AE gating, ND-AF "None selected", ND-AI no-Voice). This autonomous task generates the current/* captures; the human approval is the trailing checkpoint.
    - The Step-1 brief card is always open with Attach + no Voice; the four accordions each show a summary line + a Browse/Open overlay (vitest asserts the `<behavior>` cases).
    - Templates/Design-System render only for an opendesign-declaring deliverable (vitest: a non-opendesign deliverable renders only Review Gates + Workflow Settings — ND-AE); unset selections read "None selected" (ND-AF).
    - `npx tsc --noEmit` clean; `ConfigureScreen.test.tsx` green; the reused bodies are imported, not re-implemented (grep: TemplateGallery/DesignSystemPicker/ReviewGatesSection/AdvancedExpander imported).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/workflow/ConfigureScreen.test.tsx</automated>
  </verify>
  <done>ConfigureScreen is the mock's one screen (header + Step-1 brief + four summary-line accordions + overlays), gated (ND-AE) + live (ND-AF) + no-Voice (ND-AI), reusing the shipped bodies; the onLaunch contract is intact for 41-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of the unified Configure screen + each overlay vs the mock (closed to ND-A..D + ND-AE..AJ)</name>
  <files>frontend/e2e/fidelity/gallery-phase41.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (ND-D/ND-AF live data make a pixel-diff-vs-mock invalid). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/config*), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-phase41-gallery.mjs --surface config`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The unified "Configure your run" screen — the top header bar, the always-open Step-1 brief card (Attach, no Voice), the four ADVANCED CONFIGURATION accordions with live "Currently using: X" summary lines, and the four overlays (Templates grid · DS grouped-chips · Review-gate manager · Workflow-Settings tab modal) — with the prior task's mechanical gates already green (tsc clean, ConfigureScreen vitest green, shipped bodies reused).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-phase41.html and find the `config`, `config ▸ Templates overlay`, `config ▸ Design System overlay`, `config ▸ Review Gates overlay`, `config ▸ Workflow Settings modal` sections. Compare the mock (left) against ours (right). Confirm every visible difference is a registered divergence — ND-AE (Templates/DS absent for a non-opendesign deliverable), ND-AF ("None selected" vs the mock's fabricated selections), ND-AI (no Voice on the Step-1 brief); any OTHER visual departure on the header / Step-1 brief / accordions / overlays is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the unified Configure screen + each overlay matches its mock closed to the intended-divergence register, or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| brief textarea + attachments → the composed launch command | user free-text + files staged for a run |
| template/DS registries → the accordions/overlays | live owner-scoped registry data rendered in the UI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Tampering (XSS) | Step-1 brief + accordion/overlay bodies | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new Configure code (grep → 0) |
| T-41-02-02 | Information disclosure | "Currently using" summary lines | mitigate | bind to live owner-scoped registry data only; never fabricate/clone the mock's fixed values (ND-AF) |
| T-41-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs (reuses lucide-react + tokens + the shipped bodies). |
</threat_model>

<verification>
- The unified Configure screen + each overlay match the mock per the Phase-41 gallery oracle, closed to ND-A..D + ND-AE..AJ (ND-AE gating, ND-AF live, ND-AI no-Voice).
- The shipped bodies are reused verbatim (not re-implemented); the `onLaunch`/`ComposedLaunchCommand` contract is unchanged (wired in 41-03).
- `npx tsc --noEmit` clean; ConfigureScreen vitest green.
</verification>

<success_criteria>
CFGUI-01: the unified one-screen Configure (Step-1 brief + four accordions + overlays) matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
</success_criteria>

<artifacts_produced>
- Rebuilt `ConfigureScreen` (header bar + Step-1 brief + four summary-line accordions + overlays), reusing the shipped bodies.
- `components/workflow/configure/*Accordion.tsx` wrappers (summary line + Browse/Open overlay).
</artifacts_produced>

<output>
Create `.planning/phases/41-configure-composer-rebuild-b7/41-02-SUMMARY.md` when done.
</output>
</content>
