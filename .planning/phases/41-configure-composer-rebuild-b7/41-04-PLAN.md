---
phase: 41-configure-composer-rebuild-b7
plan: 04
type: execute
wave: 4
depends_on: ["41-03"]
files_modified:
  - frontend/src/components/workflow/composer/ComposerPage.tsx
  - frontend/src/components/workflow/composer/AgentRow.tsx
  - frontend/src/components/workflow/composer/IdentityCard.tsx
  - frontend/src/components/workflow/composer/SummaryRail.tsx
  - frontend/src/components/workflow/composer/ComposerPage.test.tsx
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/catalog/HomeLaunchGrid.tsx
  - frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx
autonomous: false
requirements: [CMPUI-01, CMPUI-02]
must_haves:
  truths:
    - "The Composer is a full-page mainView='composer' surface reached from Home's 'Compose a custom workflow' card + edit-from-My-Workflows, with a Simple ⇄ Canvas view toggle in its header (Simple active here; Canvas mounts in 41-05)"
    - "The Simple view matches Hexaware Composer.dc.html: an identity card (Name / Deliverable-type read-only [ND-AH] / Description) + reorderable agent ROWS (index · avatar · name · Core badge · role · inline model picker · Validator/Gate/Retry override chips · Custom prompt → · remove) + a Capability palette card + a Skills & hooks card + a sticky Summary rail"
    - "The surface is bound to the AgentsPopup shared data model (pipelineAgents order + SelectionsMap + declaredCapabilities + AgentPromptSection) via the EXPORTED sub-components (AdvancedExpander / CapabilityPaletteSection / SkillsHooksTab) — the sub-components are reused, not forked"
    - "The AgentsPopup MODAL wrapper is RETAINED — its callers (LaunchWizard + IdeaInputPage) remain, since the wizard/input flow was kept when Configure was reverted; the full-page Composer is ADDITIVE and only REUSES the exported sub-components, so no dual composer exists (INV-3: shared sub-components + distinct entry/purpose, not a duplicate)"
  artifacts:
    - path: "frontend/src/components/workflow/composer/ComposerPage.tsx"
      provides: "The full-page Composer surface: header + Simple⇄Canvas toggle + Simple view bound to the shared data model"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/workflow/composer/ComposerPage.tsx"
      to: "AdvancedExpander / CapabilityPaletteSection / SkillsHooksTab (AgentsPopup exports)"
      via: "reused per-agent + palette + skills sub-components"
      pattern: "AdvancedExpander|CapabilityPaletteSection|SkillsHooksTab"
---

<intended_divergence_note>
Inherit ND-A..D + ND-AE..AJ from `41-UI-SPEC.md`. This surface exercises: ND-AH (the identity card's Deliverable-type is read-only — `base_pipeline_type` is fixed at composer entry, not an in-composer dropdown), ND-AG (the Summary rail omits fabricated est. cost + its primary action is Save-to-catalogue; est. duration IS derived live — Run wiring lands in 41-06), and the carried ND-A/B/C (brand / My-Workflows nav / underline). Any OTHER visual departure on the identity card / agent rows / palette / skills / summary rail is a fidelity bug.
</intended_divergence_note>

<objective>
Lift the AgentsPopup MODAL content into a full-page Composer surface with a Simple ⇄ Canvas view toggle (D-03 / D-CMP-ENTRY), and build the mock-fidelity **Simple view** (CMPUI-02). Mount a `mainView="composer"` surface in `DashboardLayout` (additive — build fresh; the 41-03 configure surface was reverted), reached from Home's "Compose a custom workflow" card (`HomeLaunchGrid`) + edit-from-My-Workflows (`SavedWorkflowsPage` `onLaunchSaved`); the header carries a Simple⇄Canvas toggle (Simple active here; the Canvas view mounts in 41-05). Build the Simple view to `Hexaware Composer.dc.html`: identity card (Name / Deliverable-type read-only [ND-AH] / Description) + reorderable agent rows + Capability palette + Skills & hooks + a sticky Summary rail — all bound to the AgentsPopup shared data model via the EXPORTED sub-components. KEEP the AgentsPopup MODAL wrapper (LaunchWizard + IdeaInputPage still use it for inline per-run editing) — the Composer is ADDITIVE and only reuses its sub-components (INV-3: not a dual implementation).

Purpose: add the mock's full-page Composer as a NEW authoring surface, reusing the shared data model + sub-components (the inline modal editor stays for the wizard/input flow).
Output: the full-page Composer Simple view, nav-reachable + captured for the fidelity gate; the AgentsPopup modal retained + reused (not deleted).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-configure-composer-rebuild-b7/41-CONTEXT.md
@.planning/phases/41-configure-composer-rebuild-b7/41-UI-SPEC.md
@frontend/src/components/workflow/AgentsPopup.tsx
@frontend/src/components/layout/DashboardLayout.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full-page Composer surface + entry + Simple⇄Canvas toggle + the Simple view (bound to the shared data model) + reuse the modal's sub-components (modal retained)</name>
  <files>frontend/src/components/workflow/composer/ComposerPage.tsx, frontend/src/components/workflow/composer/AgentRow.tsx, frontend/src/components/workflow/composer/IdentityCard.tsx, frontend/src/components/workflow/composer/SummaryRail.tsx, frontend/src/components/workflow/composer/ComposerPage.test.tsx, frontend/src/components/workflow/AgentsPopup.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/catalog/HomeLaunchGrid.tsx, frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Composer.dc.html (the full-page Composer — the dark top nav + header [back · "CUSTOM WORKFLOW · COMPOSER" · {{wfName}} · Save draft · Save workflow]; the identity card [Name / Deliverable type / Description]; the agent pipeline ROW composition [up/down · NN index · avatar · name · Core lock badge · role · model picker · Overrides Validator/Gate/Retry chips · "Custom prompt →" · remove]; the Capability palette card; the Skills & hooks card; the sticky Summary rail [agents · gates · strategy · est. duration · est. cost · declared caps · Save-to-catalogue · Run once])
    - frontend/src/components/workflow/AgentsPopup.tsx (the MODAL `AgentsPopup` :1747 is RETAINED — do NOT delete, its callers remain; REUSE the EXPORTED sub-components — `AdvancedExpander` :1412 [per-agent Validator/Gate/Model/Retry → SelectionsMap], `CapabilityPaletteSection` :1098, `SkillsHooksTab` :790, `AgentCapabilitiesModal` :410, `AgentPromptSection` :209; `getRole` :132 → the Core badge; the native HTML5 drag-reorder pattern :1821-1843; `createUserWorkflow` :1781; the summary counts + `estimated_duration` sum)
    - frontend/src/components/layout/DashboardLayout.tsx (the `MainView` union :173 + surface switch — add a `composer` member + surface (additive; the 41-03 configure surface was reverted — build fresh); the `onSelectFeature` custom-entry path)
    - frontend/src/components/catalog/HomeLaunchGrid.tsx (the "custom" / "Compose a custom workflow" card — repoint it to the composer surface) + frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx (`onLaunchSaved` :359 — edit-from-My-Workflows re-targets the composer surface, pre-loaded)
  </read_first>
  <behavior>
    - The Composer mounts as a full page (mainView='composer'), reachable from Home's "Compose a custom workflow" card and from edit-from-My-Workflows (pre-loaded with the saved workflow's agents + selections).
    - The header shows a Simple ⇄ Canvas toggle; Simple is active and renders the agent-row list (Canvas is a stub/placeholder until 41-05).
    - The identity card shows Name + Description (editable) and Deliverable-type as READ-ONLY (ND-AH) — no in-composer deliverable dropdown.
    - Each agent row renders index + avatar + name + a Core badge for a locked/required agent (getRole) + role + an inline model picker + Validator/Gate/Retry override chips (bound to SelectionsMap via AdvancedExpander) + a Custom-prompt affordance + a remove control; reorder moves the agent within pipelineAgents.
    - The Summary rail shows agent count + review-gate count + strategy + a LIVE est. duration (from estimated_duration) and NO est. cost (ND-AG); its primary action is Save-to-catalogue (Run once is wired in 41-06).
    - The AgentsPopup MODAL wrapper is RETAINED (LaunchWizard + IdeaInputPage still import it); the Composer reuses its exported sub-components (does NOT delete the modal).
  </behavior>
  <action>
    Build `composer/ComposerPage.tsx` as the full-page surface: a header (back · "CUSTOM WORKFLOW · COMPOSER" · workflow name · Save draft · Save workflow) with a Simple⇄Canvas segmented toggle (per-session state; Simple active — the Canvas branch renders a placeholder until 41-05). Compose the Simple view from small sub-components: `IdentityCard.tsx` (Name + Description editable; Deliverable-type rendered READ-ONLY from the fixed `base_pipeline_type` — ND-AH), `AgentRow.tsx` (reorderable row: up/down + index + avatar + name + Core badge via `getRole` + role + an inline model picker + the Validator/Gate/Retry override chips + Custom-prompt + remove — bind the per-agent levers by REUSING the exported `AdvancedExpander` [SelectionsMap] and `AgentPromptSection` [custom prompt], do NOT re-implement them; reuse the AgentsPopup native drag-reorder pattern for reordering), the Capability palette card (REUSE `CapabilityPaletteSection`), the Skills & hooks card (REUSE `SkillsHooksTab`), and `SummaryRail.tsx` (agents · review-gates · strategy · live est. duration from `estimated_duration` · declared caps · Save-to-catalogue [reuse the NameWorkflowModal → `createUserWorkflow` path] · a Run-once button that is inert here, wired in 41-06) — OMIT est. cost (ND-AG). Bind everything to the shared data model (the caller's `pipelineAgents` order + `SelectionsMap` + `declaredCapabilities`). Mount a `mainView="composer"` surface in `DashboardLayout` (additive new surface; the 41-03 configure surface was reverted — build fresh) and repoint Home's "Compose a custom workflow" card + `SavedWorkflowsPage.onLaunchSaved` to it (edit pre-loads the saved workflow). KEEP the `AgentsPopup` MODAL wrapper function (:1747) — LaunchWizard + IdeaInputPage still use it; only REUSE its exported sub-components (do NOT delete the modal). Re-anchor the Phase-39/40-deferred `ts-e.model-picker` mocked-e2e onto the composer's inline model picker (Composer/agent-config territory). Write `ComposerPage.test.tsx` FIRST asserting the `<behavior>` cases. Route all color/type through the existing tokens. NO fenced code — name identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `shots-shell/current/composer-simple__shell.png` vs `Hexaware Composer.dc.html` via the 41-01 oracle, closed to ND-A..D + ND-AE..AJ (esp. ND-AH read-only deliverable-type, ND-AG no cost / Save-first). This autonomous task generates the current/* capture; the human approval is the trailing checkpoint.
    - The full-page Composer is reachable from Home + edit-from-My-Workflows; the header toggle shows Simple⇄Canvas (Simple active); the Simple view renders the identity card + agent rows + palette + skills + summary rail (vitest asserts the `<behavior>` cases).
    - The exported sub-components are REUSED (grep: AdvancedExpander / CapabilityPaletteSection / SkillsHooksTab / AgentPromptSection imported into composer/*), not re-implemented; the AgentsPopup modal is RETAINED (still imported by LaunchWizard + IdeaInputPage) — the Composer only reuses its exported sub-components.
    - `npx tsc --noEmit` clean; `ComposerPage.test.tsx` green; the `ts-e.model-picker` spec re-anchored green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "AdvancedExpander" src/components/workflow/composer/ComposerPage.tsx && grep -q '"composer"' src/components/layout/DashboardLayout.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/workflow/composer/ComposerPage.test.tsx</automated>
  </verify>
  <done>The full-page Composer Simple view is nav-reachable, mock-composed, bound to the shared data model via the reused sub-components; the modal wrapper is retained + reused (INV-3: additive, not a dual implementation).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity checkpoint — human review of the Composer Simple view vs the mock (closed to ND-A..D + ND-AE..AJ)</name>
  <files>frontend/e2e/fidelity/gallery-phase41.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (ND-D/ND-AG live data make a pixel-diff-vs-mock invalid). Regenerate this surface's gallery section: refresh our side via `SHELL_CAPTURE=1 npm --prefix frontend run e2e -- zzz-shell-baseline` (current/composer-simple*), the target side via `node frontend/e2e/fidelity/capture-shell-mocks.mjs` (if not already captured), then `node frontend/e2e/fidelity/assemble-phase41-gallery.mjs --surface composer`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The full-page Composer Simple view — the header + Simple⇄Canvas toggle, the identity card (deliverable-type read-only), the reorderable agent rows (model picker + Validator/Gate/Retry chips + Custom prompt), the Capability palette + Skills & hooks cards, and the sticky Summary rail (no est. cost) — with the prior task's mechanical gates already green (tsc clean, ComposerPage vitest green, modal wrapper retained, sub-components reused).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery-phase41.html and find the `composer ▸ Simple view` section. Compare the mock (left) against ours (right). Confirm every visible difference is a registered divergence — ND-AH (Deliverable-type read-only vs the mock's editable dropdown), ND-AG (no "Est. cost" in the Summary rail + Save-to-catalogue as the primary action), ND-A/B/C (brand / My-Workflows / underline); any OTHER visual departure on the identity card / agent rows / palette / skills / summary rail is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the Composer Simple view matches its mock closed to the intended-divergence register, or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent/model/override edits → SelectionsMap → createUserWorkflow | user-composed workflow config persisted via the existing owner-scoped save |
| /api/capabilities + /api/agents → the palette/rows | live owner-scoped capability + agent data rendered |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Tampering (XSS) | identity card + agent rows + palette | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new composer code (grep → 0) |
| T-41-04-02 | Elevation of privilege | reused AdvancedExpander model/override levers | accept | reuses the existing tested `SelectionsMap` levers with the same `user_allowed`-gated capability lists; no new authority path |
| T-41-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs — reuses the exported sub-components + tokens + `motion`; no new dependency (D-04 hand-roll). |
</threat_model>

<verification>
- The full-page Composer Simple view matches `Hexaware Composer.dc.html` per the Phase-41 gallery oracle, closed to ND-A..D + ND-AE..AJ (ND-AH, ND-AG).
- Bound to the shared data model via the REUSED exported sub-components (not forked); the AgentsPopup MODAL wrapper is retained + reused (INV-3: additive).
- `npx tsc --noEmit` clean; ComposerPage vitest green; `ts-e.model-picker` re-anchored green.
</verification>

<success_criteria>
CMPUI-01: the Composer is a full-page surface with entry from Home + edit-from-My-Workflows and a Simple⇄Canvas toggle, bound to the shared data model; the modal is retained for inline per-run editing while the Composer is an additive authoring surface reusing shared sub-components (INV-3: not a dual implementation).
CMPUI-02: the Simple view matches its mock, screenshot-diff-proven + human-signed-off, closed to the register.
</success_criteria>

<artifacts_produced>
- `composer/ComposerPage.tsx` (+ `AgentRow` / `IdentityCard` / `SummaryRail`) — the full-page Simple view.
- `DashboardLayout` `mainView="composer"` surface + Home/My-Workflows entry.
- Retained + reused: the AgentsPopup modal (sub-components shared; wrapper kept for the wizard/input flow); re-anchored `ts-e.model-picker`.
</artifacts_produced>

<output>
Create `.planning/phases/41-configure-composer-rebuild-b7/41-04-SUMMARY.md` when done.
</output>
</content>
