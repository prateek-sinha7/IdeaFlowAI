---
phase: 30-uploads-multimodal-a2
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/IMPLEMENTATION-REGISTER.md
autonomous: true
requirements: [UPLD-02, UPLD-04]
must_haves:
  truths:
    - "IMPLEMENTATION-REGISTER.md contains an entry for the already-landed image-input cluster (waves edw/frv/gvq)"
    - "The entry records the shipped artifacts: run_images input_provider, _validate_images ingress caps + vision guard, prototype input_providers:[run_images] opt-in + injects:[images], FE picker/preview, the 7 tests"
    - "The entry records the ND-10 image-transience disposition (payload-transient; images do not survive reopen/replay) so a future reader does not re-plan durable image storage"
    - "The entry follows the register's existing per-phase/cluster format and links to the source files (index-not-source discipline)"
  artifacts:
    - path: ".planning/IMPLEMENTATION-REGISTER.md"
      provides: "the image-input cluster section (register-first discipline — closes the STATE.md note that it was missing)"
      contains: "image-input"
  key_links:
    - from: ".planning/IMPLEMENTATION-REGISTER.md"
      to: "backend/agents/capabilities/input_providers/run_images.py + app/api/websocket.py _validate_images"
      via: "pointer references (exact file paths) to the shipped image-input code"
      pattern: "run_images"
---

<objective>
Add the IMPLEMENTATION-REGISTER.md entry for the already-landed image-input cluster (waves
`edw`/`frv`/`gvq`). STATE.md flags this explicitly: "the image-input cluster is NOT yet in
IMPLEMENTATION-REGISTER — add its entry when Phase 30 opens." Per the register-first discipline
(memory: implementation-register), the code-free pointer index must record shipped work so no
future bug-fix/feature re-writes it or contradicts a locked decision.

This is the register's durable record of the IMAGE realization of the UPLD-02 (per-turn/run-entry
images) and UPLD-04 (attach + resize) families — hence those requirement tags.

Purpose: close the known register gap; make the image spine discoverable before anyone touches
multimodal again.
Output: one register entry (pure docs) covering the shipped image-input artifacts + the ND-10
transience disposition.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-uploads-multimodal-a2/30-CONTEXT.md

# The register format to match + the shipped image-input code to index (read, do not re-derive):
@.planning/IMPLEMENTATION-REGISTER.md    # match the existing per-phase 8-part / cluster section format; append after Phase 27
@backend/agents/capabilities/input_providers/run_images.py   # the run_images input_provider (Wave-1 spine)
@backend/agents/capabilities/registry.py                     # (input_provider, run_images) lockstep + the drift-guard count history
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the image-input cluster (edw/frv/gvq) + ND-10 transience disposition</name>
  <files>.planning/IMPLEMENTATION-REGISTER.md</files>
  <action>
    Append a new cluster section to IMPLEMENTATION-REGISTER.md (after the Phase 27 section, matching
    the existing per-phase/cluster heading + prose format — index, not source; point to exact files).
    Record the SHIPPED image-input cluster (waves edw/frv/gvq):

    - Capability: `input_provider:run_images` (`backend/agents/capabilities/input_providers/run_images.py`)
      — shapes the transient `ectx.run_images` base64 carrier into multimodal content-blocks;
      registry lockstep `(input_provider, run_images)` + the `_KNOWN` drift-guard count (the
      65-count reconciliation incl. the KAN-73 audit_logger drift).
    - Ingress caps + guard: `_validate_images` in `backend/app/api/websocket.py` (mime allow-list
      {png,jpeg,webp,gif}, ~3.75 MB/image, <=20 images, ~8 MB aggregate, vision-model guard,
      `IMAGE_INPUT_ENABLED` flag); the launch (`POST /api/runs`) + WS `run_pipeline` paths validate
      pre-mint. Engine seams: `_normalize_run_images`, `_dispatch_payload` (split-transport),
      `_compose_input_blocks` (the per-agent `injects:[images]` gate).
    - Workflow opt-in: prototype declares `input_providers: [run_images]` + `prototype-specify`
      `injects: [images]` (the dormant-by-default gate — no other workflow opts in).
    - FE: the picker/preview chips + out-of-band `images` payload on the 3 launch surfaces
      (`IdeaInputPage.tsx`, dashboard, templates) — base64 NEVER inlined into the brief (D3).
    - Tests (the 7): `test_input_providers_run_images.py`, `test_image_input_wiring.py`,
      `test_prototype_image_optin.py`, `test_image_ingress_validation.py`, `test_image_ws_ingress.py`
      (+ the FE `IdeaInputPage.imageInput.test.tsx` / `useWorkflow.imagePayload.test.ts`).
    - Locked disposition (do-not-resurrect): D-07 as-built — images are PAYLOAD-TRANSIENT; they ride
      the message → `ectx.run_images` → base64 blocks and NEVER touch the sandbox/DB/run_events.
      Consequence + decision: ND-10/LOCK-E — images do NOT survive reopen/replay/resume; the honest
      "image not retained" placeholder (Phase 30) is the disposition, NOT durable image storage.
    - Cross-reference Phase 30's remaining scope (this milestone): `POST /api/runs/{id}/files`
      (30-01), `context_provider:uploaded_files` (30-02), the per-turn image carrier (30-03),
      client resize + reopen placeholder (30-04).

    If the register carries a phase-navigation table, add the row for this cluster consistently.
    Keep it code-free (pointers to files/headings only). No source files change — docs only.
  </action>
  <verify>
    <automated>grep -c "run_images\|_validate_images\|image-input\|ND-10\|payload-transient" .planning/IMPLEMENTATION-REGISTER.md | (read n; test "$n" -ge 4 && echo "register entry present: $n hits")</automated>
  </verify>
  <done>The register has an image-input cluster entry indexing the shipped run_images provider, ingress caps + vision guard, prototype opt-in, FE picker/preview, the 7 tests, and the ND-10 payload-transient disposition — closing the STATE.md "not yet in register" note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | documentation-only change; no code, no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-14 | Repudiation | inaccurate register (mis-indexed shipped code) | mitigate | every claim points to an exact file path verified against the tree (index-not-source discipline); no behavioral claims beyond the shipped tests |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | docs-only change — no dependencies, no install task |
</threat_model>

<verification>
- IMPLEMENTATION-REGISTER.md contains the image-input cluster entry (run_images, _validate_images, prototype opt-in, FE, 7 tests).
- The ND-10 payload-transient disposition is recorded (no durable image storage).
- Entry follows the register format and references exact source files (code-free index).
</verification>

<success_criteria>
The register-first gap is closed: the already-landed image-input cluster (edw/frv/gvq) is indexed
in IMPLEMENTATION-REGISTER.md with its artifacts, tests, and the ND-10 transience disposition, so
future multimodal work does not re-plan the image spine or resurrect a durable-storage design.
</success_criteria>

<output>
Create `.planning/phases/30-uploads-multimodal-a2/30-05-SUMMARY.md` when done.
</output>
