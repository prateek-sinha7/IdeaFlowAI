---
phase: 30-uploads-multimodal-a2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/run_commands.py
  - backend/app/api/chat_router.py
  - backend/agents/execution_engine/engine.py
  - backend/tests/unit/test_run_message_images.py
  - backend/tests/unit/test_mechanical_router.py
autonomous: true
requirements: [UPLD-02]
must_haves:
  truths:
    - "A chat turn that carries images on the Phase-29 POST /api/runs/{id}/messages path delivers those images into the NEXT dispatch's HumanMessage content blocks"
    - "Per-turn images are validated with the SAME image-ingress caps (_validate_images) before they are queued"
    - "The per-turn carrier reuses the Phase-29 chat_message path — the /messages endpoint is NOT rebuilt"
    - "ND-10/LOCK-E: per-turn images are payload-transient — never written to sandbox/DB/run_events; the chat_message row keeps attachment refs stamped retained:false (no bytes)"
    - "Dormant on golden runs: no chat-turn images → run_images carrier empty → agent_input + goldens byte-identical (INV-3)"
    - "The carrier keys on the generic run-state + steering queue only — never a workflow name (SC-001/INV-1)"
  artifacts:
    - path: "backend/app/api/chat_router.py"
      provides: "apply_turn_images() seam + ChatTurn.images — the per-turn image analogue of apply_steering"
      contains: "apply_turn_images"
    - path: "backend/app/api/run_commands.py"
      provides: "MessageCommand.images + validation + threading onto the next-dispatch image carrier"
      contains: "images"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "drain of pending per-turn images onto ectx.run_images before _compose_input_blocks (consume-once, dormant by default)"
      contains: "pending_turn_images"
  key_links:
    - from: "backend/app/api/run_commands.py post_message"
      to: "_validate_images"
      via: "reuse the websocket.py image-ingress caps before queueing per-turn images"
      pattern: "_validate_images"
    - from: "backend/app/api/chat_router.py apply_turn_images"
      to: "ectx.pending_turn_images"
      via: "append validated images to the per-run pending carrier (mirrors apply_steering → steering_notes)"
      pattern: "pending_turn_images"
    - from: "backend/agents/execution_engine/engine.py"
      to: "ectx.run_images"
      via: "drain pending_turn_images into run_images before _compose_input_blocks at the next dispatch"
      pattern: "run_images"
---

<objective>
Build UPLD-02's remaining residue: the PER-TURN image carrier. The run-entry image spine
already shipped (waves edw/frv/gvq — `run_images` input_provider, WS ingress caps, prototype
opt-in). What's left is letting a user attach images to an in-flight chat turn: the images ride
the ALREADY-BUILT Phase-29 `POST /api/runs/{id}/messages` steering path and land in the NEXT
agent dispatch's `HumanMessage` content blocks — exactly where run-entry images already flow
(via `ectx.run_images` → `_compose_input_blocks`).

Per POR §2 (D-07) + §3 (LOCK-E / ND-10): images are PAYLOAD-TRANSIENT. They ride the message →
`ectx.run_images` → base64 blocks and NEVER touch the sandbox, DB, or run_events. The
`chat_message` row that Phase-29 already persists keeps its attachment refs stamped
`retained: false` (no bytes) — this plan locks that disposition; it does NOT build durable image
storage (ND-10 deferred).

Purpose: mid-run multimodal steering without any persistence surface.
Output: `MessageCommand.images` + `_validate_images` reuse + an `apply_turn_images` seam + a
dormant-by-default engine drain onto `ectx.run_images`, plus the ND-10 no-persistence lock.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-uploads-multimodal-a2/30-CONTEXT.md

# The Phase-29 chat path + the image plumbing to REUSE (read, do not rebuild):
@backend/app/api/run_commands.py     # MessageCommand (already has attachments + retained:false), post_message two-layer owner check, the CHANNEL_STEERING branch
@backend/app/api/chat_router.py      # ChatTurn/Dispatch, route_chat_turn (PHASE_RUNNING → steering), apply_steering(ectx, note) → ectx.steering_notes (the seam to mirror)
@backend/agents/execution_engine/engine.py   # _normalize_run_images (~L410), _dispatch_payload (~L431), run_images carrier set at run entry (~L1018), _compose_input_blocks (~L6350, the images gate)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-turn image carrier — MessageCommand.images → validate → apply_turn_images → next dispatch run_images</name>
  <files>backend/app/api/run_commands.py, backend/app/api/chat_router.py, backend/agents/execution_engine/engine.py</files>
  <behavior>
    - A RUNNING-phase chat turn with valid images: post_message validates them via _validate_images, then apply_turn_images appends normalized {mime_type,data} entries to ectx.pending_turn_images.
    - The engine drains pending_turn_images onto ectx.run_images before _compose_input_blocks at the next dispatch → an injects:[images] agent's HumanMessage carries the image content blocks.
    - A turn with images that violate the caps (count/size/aggregate/mime) → 400 from the SAME _validate_images path; nothing queued.
    - A turn with NO images → pending_turn_images stays empty → run_images empty → dispatch payload byte-identical (INV-3 dormant).
    - Images are never persisted: _persist_chat_message still stamps attachment refs retained:false with NO bytes.
  </behavior>
  <action>
    Add `images: list | None = None` to `MessageCommand` (run_commands.py). In `post_message`,
    after the existing two-layer owner check + persist, when `body.images` is present AND
    settings.IMAGE_INPUT_ENABLED: validate via `_validate_images(body.images,
    effective_model_ids=...)` (import the existing validator from websocket.py exactly as the
    launch path does; a violation → 400, before queueing). This REUSES the Phase-29 endpoint — do
    NOT add a new route.

    Add the seam to chat_router.py: `ChatTurn.images: list = field(default_factory=list)` and
    `apply_turn_images(ectx, images)` — the image analogue of `apply_steering`: normalize each
    entry to {mime_type, data} (reuse engine._normalize_run_images shape) and append to a generic
    per-run `ectx.pending_turn_images` queue; best-effort no-op when the attribute is absent
    (mirrors apply_steering's ectx-missing guard; the durable chat_message row remains the record,
    ND-9 re-derive on resume). Route unchanged: PHASE_RUNNING already → CHANNEL_STEERING; the
    steering branch in post_message additionally calls apply_turn_images for the live ectx handle
    (same live-handle lookup the steering note uses). Keyed on the generic queue only — no
    workflow name (SC-001/INV-1).

    Engine drain (engine.py): before `_compose_input_blocks(spec, ectx)` at each dispatch, drain
    `getattr(ectx, "pending_turn_images", None)` — if non-empty, extend `ectx.run_images` with the
    normalized entries and clear the pending queue (consume-once, so each attached image reaches
    exactly the next dispatch's HumanMessage). Initialize `pending_turn_images` alongside the
    existing run-entry `run_images`/`steering_notes` scratch so it always exists. DORMANT by
    default: empty queue → run_images unchanged → `_dispatch_payload` returns the bare
    context_message str (INV-3: agent_input + the 5 goldens byte-identical). Do NOT touch the
    _validate_images caps or the run-entry image path.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_run_message_images.py tests/unit/test_mechanical_router.py -x -q</automated>
  </verify>
  <done>A chat-turn image is cap-validated, queued via apply_turn_images, drained onto ectx.run_images, and appears in the next dispatch's content blocks; a capless/empty turn stays dormant (byte-identical); images are never persisted.</done>
</task>

<task type="auto">
  <name>Task 2: Tests — per-turn image delivery, ND-10 no-persistence lock, INV-3 dormancy</name>
  <files>backend/tests/unit/test_run_message_images.py, backend/tests/unit/test_mechanical_router.py</files>
  <action>
    New `test_run_message_images.py`: (a) a RUNNING-phase /messages turn with a valid image →
    apply_turn_images queues it → the engine drain moves it to ectx.run_images → an injects:[images]
    dispatch's input_blocks contain the base64 image block (assert content-block shape from
    RunImagesProvider). (b) an image violating a cap → 400 (reuse _validate_images), nothing
    queued. (c) ND-10 lock: after a turn with images, assert NO image bytes exist in the sandbox /
    DB / run_events, and the persisted chat_message row's attachment refs are stamped
    retained:false with no data field — images do not survive replay/reopen. (d) INV-3 dormancy: a
    turn with no images leaves run_images empty and the dispatch payload byte-identical to the
    no-image baseline.
    Extend `test_mechanical_router.py`: assert apply_turn_images appends to ectx.pending_turn_images
    (and is a no-op on an ectx lacking the attribute), mirroring the existing apply_steering test.
    These two files only (full suite hangs offline).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_run_message_images.py tests/unit/test_mechanical_router.py -q && /opt/homebrew/bin/lint-imports 2>/dev/null | tail -3</automated>
  </verify>
  <done>Per-turn image delivery proven end-to-end; ND-10 no-persistence proven (retained:false, no bytes anywhere); INV-3 dormancy byte-identical; router seam covered; lint-imports 4/0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/runs/{id}/messages | untrusted per-turn image payload (mime, base64) crosses here |
| endpoint → live ectx handle | queued images reach the in-flight run's next dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Denial of service | per-turn image flood (decompression / count / aggregate) | mitigate | reuse _validate_images caps (mime allow-list, ~3.75 MB/image, <=20, ~8 MB aggregate, vision-model guard) → 400 before queueing |
| T-30-10 | Information disclosure | injecting images into another user's run | mitigate | the /messages endpoint's existing two-layer owner check (ORM user_id → 404, then default-deny ScopedStore.get_run → 404) gates the turn before any queueing |
| T-30-11 | Information disclosure | images persisted then leaked on replay/reopen | mitigate | ND-10/LOCK-E: images are payload-transient — never written to sandbox/DB/run_events; chat_message refs stamped retained:false, no bytes (locked by test) |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies — reuses _validate_images + _normalize_run_images; no install task |
</threat_model>

<verification>
- A per-turn image rides the Phase-29 /messages path into the next dispatch's HumanMessage blocks.
- Per-turn images are _validate_images-capped (400 on violation) before queueing; endpoint not rebuilt.
- ND-10: no image bytes in sandbox/DB/run_events; chat_message refs retained:false (locked).
- INV-3: no-image turn → run_images empty → dispatch payload + 5 goldens byte-identical.
- `python3.11 -m pytest tests/unit/test_run_message_images.py tests/unit/test_mechanical_router.py -q` green; lint-imports 4/0.
</verification>

<success_criteria>
UPLD-02 residue delivered: images attached to a chat turn ride the existing Phase-29 chat_message
path (validated by the shared image caps) into the next dispatch's HumanMessage content blocks,
staying payload-transient (ND-10 — never persisted), with the run-entry image path and the 5
goldens byte-identical.
</success_criteria>

<output>
Create `.planning/phases/30-uploads-multimodal-a2/30-03-SUMMARY.md` when done.
</output>
