---
phase: 08-capabilities-hardened-registry-gates-tool-perms-runtime-3
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/agents/workflows/plan.py
  - backend/agents/workflows/compiler.py
  - backend/agents/capabilities/tools/__init__.py
  - backend/agents/capabilities/tools/providers.py
  - backend/agents/factory.py
  - backend/tests/agents/test_tool_permissions.py
  - backend/tests/agents/test_create_runner.py
autonomous: true
requirements: [TOOLPERM-01, TOOLPERM-02, TOOLPERM-03, AGENTRT-04]
user_setup: []

must_haves:
  truths:
    - "Effective tool perms = intersection(owner_allow_list, workflow_ceiling, step_grant); an AGENT.md default may only lower a permission, never raise it"
    - "A step without write_files binds no write tool; exec/network/secrets/spawn remain unbindable by default"
    - "A tool_provider registry resolves tool sets by name; the closed _build_runner_tools switch is deleted (grep → 0)"
    - "Existing agents bind identical tool sets (parity) — test_create_runner.py green"
    - "The Workspace.ExecutionPolicy enforcement point denies exec/network/secrets even though the LocalSandboxRuntime is Phase 9"
    - "The mcp/integrations permission slots exist (default none) — slots only, no client/servers"
  artifacts:
    - path: "backend/agents/capabilities/tools/providers.py"
      provides: "tool_provider capabilities (workspace/prototype/planning sets) resolved by name"
      contains: "register(\"tool\""
    - path: "backend/agents/factory.py"
      provides: "_build_runner_tools deleted; tool binding via tool_provider registry + ToolPermissions intersection"
    - path: "backend/agents/workflows/plan.py"
      provides: "ToolPermissions grant fields confirmed/extended; ExecutionPolicy seam"
  key_links:
    - from: "backend/agents/factory.py"
      to: "tool_provider registry resolve"
      via: "grant-driven tool binding"
      pattern: "resolve\\(.tool."
    - from: "compiler intersection"
      to: "Step.tools effective perms"
      via: "intersection(owner, workflow, step)"
      pattern: "intersection"
---

<objective>
Make the inert `ToolPermissions` grant set live (D-07): resolve effective perms as `intersection(owner_allow_list, workflow_ceiling, step_grant)` where an AGENT.md default may only LOWER a permission (never raise), and enforce least-privilege at the two enforcement points — `factory._build_runner_tools` (via a `tool_provider` registry that binds only granted tool sets) and the `Workspace.ExecutionPolicy` (runtime exec/network/secrets gate, default-deny, enforcement point wired even though the `LocalSandboxRuntime` is Phase 9). Replace the closed `_build_runner_tools` switch (F2) with the `tool_provider` registry and DELETE it (INV-12; F2 ledger row → ☑) — only after the routed path binds byte-identical tool sets for every existing agent (parity gate).

Purpose: TOOLPERM-01/02/03 enforce INV-9 least-privilege before any exec/runtime work ships; AGENTRT-04 deletes the F2 leak. The `mcp`/`integrations` slots land as default-`none` placeholders (no client/servers this phase).
Output: live `ToolPermissions` intersection in the compiler, the `tool_provider` registry, `factory._build_runner_tools` deleted, the `ExecutionPolicy` default-deny seam, and the perm-intersection + parity tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-SPEC.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-CONTEXT.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-RESEARCH.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-PATTERNS.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToolPermissions intersection in the compiler + ExecutionPolicy seam + mcp/integrations slots</name>
  <files>backend/agents/workflows/plan.py, backend/agents/workflows/compiler.py, backend/tests/agents/test_tool_permissions.py</files>
  <behavior>
    - Test (test_tool_permissions.py): effective = intersection(owner_allow_list, workflow_ceiling, step_grant); an AGENT.md declaring write_files where the step grant lacks it does NOT raise it (effective stays off).
    - Test: defaults — read_files ON; write_files/git/spawn_subagents OFF; exec/network OFF; secrets/mcp/integrations none — yield the documented effective set for an ungranted step.
    - Test: an AGENT.md may only LOWER a default (turning read_files off lowers it); attempting to raise exec is a no-op (stays OFF).
    - Test: the ExecutionPolicy denies exec/network/secrets by default (the enforcement point returns deny) even with no runtime host attached.
  </behavior>
  <read_first>
    - backend/agents/workflows/plan.py — the INERT `ToolPermissions` dataclass (plan.py:45-61: read_files ON, rest OFF/none) + `Step.tools` (plan.py:214-241); confirm the grant fields are present (RESEARCH: they are, inert) — extend only if a field is genuinely missing
    - backend/agents/workflows/compiler.py — the compile path that produces CompiledWorkflow/Step; the intersection resolution slots in where Step.tools is finalized
    - 08-RESEARCH.md § "Pitfall 5: ToolPermissions parity" — defaults must yield the SAME bindings the switch produced for existing manifests
    - 08-RESEARCH.md § "Assumptions Log" A5 — the ExecutionPolicy enforcement point can be wired without the LocalSandboxRuntime (Phase 9); SPEC says wire the point only
  </read_first>
  <action>
    Per D-07/TOOLPERM-01/02/03: confirm the `ToolPermissions` dataclass (plan.py:45-61) carries the grant fields (`read_files`, `write_files`, `git`, `spawn_subagents`, `exec`, `network`, `secrets`, `mcp`, `integrations`) with the §8 defaults (read_files ON; write_files/git/spawn_subagents OFF; exec/network OFF; secrets/mcp/integrations none) — add only any genuinely-missing field (e.g. confirm `mcp`/`integrations` slots exist as default-`none`; add them as slots ONLY, no client/servers). In the compiler, resolve effective perms as `effective = intersection(owner_allow_list, workflow_ceiling, step_grant)` and apply the rule that an AGENT.md default may only LOWER (never raise) — implement intersection (a permission is effective only if granted at ALL three levels; AGENT.md can drop a granted perm but not add one). Source the owner_allow_list from the per-owner caps seam (Phase-5 run_capabilities / ScopedStore) — a missing owner allow-list defaults to the workflow ceiling this phase (DB user manifests are later-phase). Add the `Workspace.ExecutionPolicy` enforcement-point seam (a thin policy object on the workspace/runtime boundary) that default-denies `exec`/`network`/`secrets` — wire the POINT only (the LocalSandboxRuntime is Phase 9); a step requesting exec is denied regardless. Write `test_tool_permissions.py` covering the four behaviors.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_tool_permissions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_tool_permissions.py -x` exits 0
    - Behavior: an AGENT.md declaring a permission its step did not grant does NOT raise it (test asserts effective = intersection)
    - Behavior: `exec`/`network`/`secrets`/`spawn_subagents` are unbindable by default (effective off for an ungranted step)
    - `grep -c "mcp\|integrations" backend/agents/workflows/plan.py` returns ≥1 (the slots exist, default none)
    - Behavior: the ExecutionPolicy seam returns deny for exec/network/secrets with no runtime host attached
  </acceptance_criteria>
  <done>Effective perms compute by intersection with AGENT.md only able to lower; defaults are least-privilege; the ExecutionPolicy seam default-denies exec/network/secrets; mcp/integrations are default-none slots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tool_provider registry binding the granted tool sets (parity)</name>
  <files>backend/agents/capabilities/tools/__init__.py, backend/agents/capabilities/tools/providers.py, backend/tests/agents/test_create_runner.py</files>
  <behavior>
    - Test (test_create_runner.py): for one agent of each class (text-only, workspace/code-gen, prototype/prototype_emit_only, planning), the tool_provider registry binds the IDENTICAL `(custom_tools, exclude_builtin)` the closed switch produced.
    - Test: a tool set resolves via `resolve("tool", <set_name>)` after discover().
    - Test: binding is grant-driven — a step whose effective perms lack write_files binds no write-capable set.
  </behavior>
  <read_first>
    - backend/agents/factory.py — the F2 `_build_runner_tools` closed switch (factory.py:384-446; the `workspace`/`prototype`/`prototype_emit_only`/`planning` branches mapping to `(custom_tools, exclude_builtin)`, raising on unknown) — the impl being lifted into providers
    - backend/agents/planner/tools.py — `PLANNING_TOOLS`; backend/app/agents/tools/runner_tools.py — `report_task_complete` (the custom tools the sets bind)
    - 08-PATTERNS.md § "agents/factory.py F2 → tool_provider registry" — the parity bar (existing manifests + default ToolPermissions bind the SAME sets) + the exact branch mapping
    - backend/tests/agents/test_create_runner.py — the parity gate (one agent of each class binds identical tool sets)
  </read_first>
  <action>
    Create `agents/capabilities/tools/` with `tool_provider` capabilities (one per tool set: `workspace`, `prototype`, `prototype_emit_only`, `planning`, and the text-only `[]` case), each `@register("tool", <set_name>, user_allowed=...)` and satisfying the `ToolProvider` port (`provide(spec, ctx) -> (custom_tools, exclude_builtin)`). Reproduce the EXACT `(custom_tools, exclude_builtin)` the F2 switch produced per set (workspace → `([], exclude_builtin=False)`; prototype/prototype_emit_only → `([report_task_complete], False)`; planning → `(PLANNING_TOOLS, True)`; `[]` text-only → `([], True)`) so existing agents bind identically (parity per Pitfall 5). Privileged sets (those that would grant write/exec — none in the existing 4, but the structure must support it) register `user_allowed=False`. Extend test_create_runner.py to assert the provider path binds identically for one agent of each class.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_create_runner.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'register("tool"' backend/agents/capabilities/tools/` returns ≥4 (the tool sets registered)
    - `cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry, discover; discover(); r=CapabilityRegistry(); [r.resolve('tool',n) for n in ('workspace','prototype','planning')]; print('tool providers resolve')"` prints `tool providers resolve`
    - `cd backend && python3.11 -m pytest tests/agents/test_create_runner.py -x` exits 0 (existing agents bind identical tool sets via the provider path)
    - Behavior: a step whose effective perms lack write_files binds no write-capable set
  </acceptance_criteria>
  <done>The tool_provider registry resolves each tool set by name and binds the identical (custom_tools, exclude_builtin) the switch produced; binding is grant-driven; parity holds.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point factory tool binding onto the provider registry; PROVE parity; DELETE F2</name>
  <files>backend/agents/factory.py, backend/tests/agents/test_create_runner.py</files>
  <read_first>
    - backend/agents/factory.py — `_build_runner_tools` (factory.py:384-446) and its single call site in `create_runner`; the deletion target
    - specs/003-workflow-engine-decoupling/migration-ledger.md — the F2 row (grep gate `_build_runner_tools` → 0; phase 3; ☐ → ☑)
    - backend/tests/agents/test_create_runner.py + backend/tests/agents/test_migration_ledger.py — the parity gate + the F2 ratchet
    - 08-CONTEXT.md `<deep_work_rules>` — the deletion-plan sequencing: (a) build+register [done Task 2], (b) rewire the call site, (c) run the 5 characterization snapshots to PROVE parity, (d) delete the inline original + flip the ledger row; (c) MUST pass before (d)
  </read_first>
  <action>
    Rewire `create_runner`'s tool-binding call site from `_build_runner_tools(spec, ctx)` onto the `tool_provider` registry (resolve each declared tool set, apply the effective-perms intersection from Task 1, bind the union of granted sets). Run the 5 characterization snapshots + test_create_runner.py to PROVE the routed path binds byte-identical tool sets for every existing agent. ONLY after parity is green: DELETE the `_build_runner_tools` function entirely (INV-12) and flip the migration-ledger F2 row to ☑. The deletion is this plan's exit gate.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_create_runner.py tests/agents/test_migration_ledger.py tests/agents/test_characterization_*.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_build_runner_tools" backend/agents/factory.py` returns 0 (F2 deleted — the ledger grep gate)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (5 pipelines byte/event-identical — parity PROVEN before deletion)
    - `cd backend && python3.11 -m pytest tests/agents/test_create_runner.py -x` exits 0 (identical tool sets via the provider path)
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` exits 0 AND shows the F2 row ☑
    - `cd backend && lint_imports` exits 0
  </acceptance_criteria>
  <done>_build_runner_tools is deleted (grep → 0), the tool_provider registry binds identical sets, the 5 snapshots are green, and the F2 ledger row is ☑.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENT.md grant → effective perms | An AGENT.md may attempt to raise a permission |
| step → ExecutionPolicy | A step may request exec/network/secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-EoP1 | Elevation of Privilege | over-privileged tool binding | mitigate | effective = intersection(owner, workflow, step); AGENT.md may only LOWER; enforced at _build_runner_tools replacement + ExecutionPolicy (Task 1/2/3) |
| T-08-03-EoP2 | Elevation of Privilege | exec/network/secrets accidental enable | mitigate | defaults OFF/none; ExecutionPolicy default-denies; exec stays OFF until N3/Phase 10 (Task 1) |
| T-08-03-parity | Tampering (regression) | F2 switch replacement | mitigate | parity proven against the 5 characterization snapshots + test_create_runner BEFORE deletion (Task 3) |
| T-08-03-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_tool_permissions.py tests/agents/test_create_runner.py -x` exits 0
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py tests/agents/test_migration_ledger.py -x` exits 0 (parity + F2 ☑) in a clean session
- `cd backend && lint_imports` exits 0; banned-pattern green
</verification>

<success_criteria>
- Effective perms = intersection(owner, workflow, step); AGENT.md only lowers; exec/network/secrets/spawn unbindable by default
- tool_provider registry binds identical tool sets for existing agents (parity)
- `_build_runner_tools` deleted (grep → 0); F2 ledger row ☑
- ExecutionPolicy enforcement point default-denies exec/network/secrets; mcp/integrations default-none slots present
</success_criteria>

## Artifacts this phase produces (08-03)
- Capabilities: tool_provider impls for `workspace`/`prototype`/`prototype_emit_only`/`planning`/text-only (each `@register("tool", ...)`)
- New files: `agents/capabilities/tools/{__init__,providers}.py`
- ToolPermissions confirmed/extended fields incl. `mcp`/`integrations` default-none slots (plan.py)
- ExecutionPolicy enforcement-point seam (Workspace boundary)
- Intersection resolution function `effective = intersection(owner, workflow, step)` (compiler)
- Deleted: `factory._build_runner_tools` (F2 ledger row ☑)
- New file: `backend/tests/agents/test_tool_permissions.py`

<output>
Create `.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-03-SUMMARY.md` when done
</output>
