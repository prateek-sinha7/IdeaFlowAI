---
phase: 08-capabilities-hardened-registry-gates-tool-perms-runtime-3
plan: 08
type: execute
wave: 7
depends_on: ["08-01", "08-02", "08-04"]
files_modified:
  - backend/app/api/capabilities.py
  - backend/app/main.py
  - backend/tests/unit/test_capabilities_api.py
  - frontend/src/components/workflow/CapabilityPalette.tsx
  - frontend/src/components/workflow/AgentModelPicker.tsx
  - frontend/src/components/results/ValidatorIssuePanel.tsx
  - frontend/src/components/workflow/WorkflowComposer.tsx
autonomous: false
requirements: [API-02, API-03, API-06]
user_setup: []

must_haves:
  truths:
    - "GET /api/capabilities returns the registry palette (kind, name, user_allowed, config schema) incl. runtimes/skills/hooks/model catalog with required auth + permission scopes"
    - "The run stream emits the new validator_result/validation_warning/gate_* events additively; existing workflows stay at semantic parity (no event renamed/removed)"
    - "The frontend renders the capability palette + per-agent model picker + validator/issue panel from LIVE /api/capabilities + WS data"
    - "Subagent/wave tree + repo-diff viewers are NOT added (deferred — no backing data)"
    - "The existing characterization snapshots remain byte-identical (deliverables) + semantic event parity; new events additive only (no re-baseline)"
  artifacts:
    - path: "backend/app/api/capabilities.py"
      provides: "GET /api/capabilities palette endpoint (auth via get_current_user)"
      contains: "/api/capabilities"
    - path: "frontend/src/components/workflow/CapabilityPalette.tsx"
      provides: "Capability palette panel (live /api/capabilities data)"
    - path: "frontend/src/components/workflow/AgentModelPicker.tsx"
      provides: "Per-agent model picker (model catalog from the palette)"
    - path: "frontend/src/components/results/ValidatorIssuePanel.tsx"
      provides: "Validator/issue panel (validator_result/validation_warning from the WS stream)"
  key_links:
    - from: "frontend/src/components/workflow/CapabilityPalette.tsx"
      to: "GET /api/capabilities"
      via: "fetch on mount"
      pattern: "/api/capabilities"
    - from: "backend/app/main.py"
      to: "capabilities_router"
      via: "router registration"
      pattern: "capabilities"
---

<objective>
Surface the hardened capability layer to the API + frontend (D-11): `GET /api/capabilities` returns the registry palette (kind, name, `user_allowed`, config schema) incl. runtimes/skills/hooks/model catalog with required auth + permission scopes (API-02); the run stream emits the new `validator_result`/`validation_warning`/`gate_*` events additively while existing workflows stay at semantic parity (API-03); and the frontend adds the three data-bearing panels — capability palette, per-agent model picker, validator/issue panel — reusing existing composer surfaces (API-06, partial). The subagent/wave tree + repo-diff viewers are DEFERRED (no backing data until P9/11/12). Existing workflows stay at strict INV-3 parity (no snapshot re-baseline; new events additive only).

Purpose: API-02/03/06 — the palette endpoint + additive event contract + the three composer panels complete the user-facing surface of the hardened registry/gates/validators.
Output: the `/api/capabilities` endpoint + registration, the additive event contract proof, the three React panels wired to live data, and the API + parity tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-SPEC.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-CONTEXT.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-RESEARCH.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-PATTERNS.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-01-SUMMARY.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-02-SUMMARY.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/capabilities palette endpoint + additive WS event proof</name>
  <files>backend/app/api/capabilities.py, backend/app/main.py, backend/tests/unit/test_capabilities_api.py</files>
  <behavior>
    - Test (test_capabilities_api.py): `GET /api/capabilities` requires auth (401 without a token; 200 with) and returns the palette: each entry has kind, name, user_allowed, config schema; runtimes/skills/hooks/model catalog kinds are present.
    - Test: the palette reflects the registry (a newly @register'd capability appears without an endpoint edit).
    - Test (additive events): the run stream emits validator_result/validation_warning/gate_* additively — the existing-workflow event contract is unchanged (no event renamed/removed); the characterization multiset for existing workflows is unchanged.
  </behavior>
  <read_first>
    - backend/app/api/workflows.py — the auth + router idiom (workflows.py:160-163: `@router.get("")`, `current_user: User = Depends(get_current_user)`); the APIRouter prefix/tags pattern
    - backend/app/main.py — `main.py:148` workflows_router registration (where capabilities_router registers alongside)
    - backend/agents/capabilities/registry.py — the registry to enumerate (kinds + names + user_allowed via is_user_allowed; the config schema per kind); ModelCatalog (model_catalog kind) for the model catalog palette
    - backend/app/api/websocket.py — the generic event forward (websocket.py:595 `{"type": event["type"], ...}`) — new event TYPES flow through unchanged, NO websocket.py edit needed for new event types (RESEARCH)
    - 08-RESEARCH.md § "Code Examples" → "/api/capabilities matches the existing auth pattern (D-11)" + § "Don't Hand-Roll" → WS event forwarding
    - 08-PATTERNS.md § "app/api/capabilities.py" — the auth + router + registration idiom
  </read_first>
  <action>
    Create `app/api/capabilities.py`: `router = APIRouter(prefix="/api/capabilities", tags=["capabilities"])` with `@router.get("")` taking `current_user: User = Depends(get_current_user)` (JWT, same posture as every endpoint), returning the registry palette — for each registered `(kind, name)`: kind, name, `user_allowed` (via `registry.is_user_allowed`), and the config schema; include the `runtime`/`skill`/`hook`/`model_catalog` kinds (the model catalog from `ModelCatalog`) plus permission scopes. Register `capabilities_router` in `app/main.py` alongside `workflows_router` (main.py:148). Confirm the new `validator_result`/`validation_warning`/`gate_*` events flow through the GENERIC websocket forward (websocket.py:595) with NO websocket.py edit for new event types — and that the existing-workflow event contract is unchanged (no event renamed/removed). Write `test_capabilities_api.py` covering the three behaviors (palette + auth, registry-reflective, additive events).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_capabilities_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/capabilities" backend/app/api/capabilities.py` returns ≥1; `grep -c "capabilities" backend/app/main.py` returns ≥1 (router registered)
    - `grep -c "get_current_user" backend/app/api/capabilities.py` returns ≥1 (auth required)
    - `cd backend && python3.11 -m pytest tests/unit/test_capabilities_api.py -x` exits 0 (palette + auth + scopes; runtimes/skills/hooks/model catalog present; registry-reflective)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (existing-workflow event contract unchanged; new events additive only; no re-baseline)
    - `grep -c "validator_result\|gate_" backend/app/api/websocket.py` consistent with NO new-event-type edit (the generic forward handles them)
  </acceptance_criteria>
  <done>GET /api/capabilities returns the auth-gated registry palette incl. runtimes/skills/hooks/model catalog; new events flow additively; existing snapshots unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend capability palette + per-agent model picker + validator/issue panel (live data)</name>
  <files>frontend/src/components/workflow/CapabilityPalette.tsx, frontend/src/components/workflow/AgentModelPicker.tsx, frontend/src/components/results/ValidatorIssuePanel.tsx, frontend/src/components/workflow/WorkflowComposer.tsx</files>
  <read_first>
    - frontend/src/components/workflow/WorkflowComposer.tsx — the composer surface the palette + model picker extend (reuse, not rebuild)
    - frontend/src/components/workflow/AgentLibrary.tsx + AgentLibraryData.ts — the existing palette/library pattern the CapabilityPalette mirrors; SkillManager.tsx for the per-attachment UI idiom
    - frontend/src/components/workflow/ReviewGatesSection.tsx + AgentProgressPanel.tsx — the run-surface event-consumption pattern the ValidatorIssuePanel mirrors (how WS events are read into a panel)
    - frontend/src/components/results/PrototypePipelineView.tsx + FilesTab.tsx — the results-surface panel pattern
    - 08-CONTEXT.md D-11 + Claude's Discretion — extend existing composer components vs new sibling panels (planner's call: new sibling panels reusing the existing fetch/WS hooks); subagent/wave tree + repo-diff viewers DEFERRED
    - 08-RESEARCH.md § "Recommended Project Structure" (frontend) + § Metadata "Frontend (D-11)" — reuse AgentLibrary/ReviewGatesSection/results surfaces
  </read_first>
  <action>
    Add three data-bearing panels reusing the existing composer/run surfaces (D-11 — reuse, do not rebuild; new sibling components wired into `WorkflowComposer.tsx`):
    - `CapabilityPalette.tsx`: fetches `GET /api/capabilities` on mount and renders the palette grouped by kind (showing name + `user_allowed` + config schema), mirroring the `AgentLibrary.tsx` list pattern.
    - `AgentModelPicker.tsx`: a per-agent model picker populated from the palette's model catalog (the `model_catalog` kind), reusing the per-agent selection idiom; wired so a per-agent model selection is captured (it feeds the existing model_overrides path).
    - `ValidatorIssuePanel.tsx`: a validator/issue panel that consumes the new `validator_result`/`validation_warning` WS events from the run stream (mirroring `ReviewGatesSection.tsx`/`AgentProgressPanel.tsx` event consumption) and renders issues by severity (CRITICAL/HIGH/MEDIUM/LOW).
    Wire all three into `WorkflowComposer.tsx`. Do NOT add subagent/wave tree or repo-diff viewers (deferred — no backing data). Keep the panels additive (no change to existing composer behavior).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5 || npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/capabilities" frontend/src/components/workflow/CapabilityPalette.tsx` returns ≥1 (fetches live palette data)
    - `grep -c "validator_result\|validation_warning" frontend/src/components/results/ValidatorIssuePanel.tsx` returns ≥1 (consumes the live WS events)
    - `grep -c "CapabilityPalette\|AgentModelPicker\|ValidatorIssuePanel" frontend/src/components/workflow/WorkflowComposer.tsx` returns ≥3 (the three panels wired in)
    - `cd frontend && npm run build` (or `npm run typecheck`) exits 0 (no type/compile errors)
    - The three panels render from live API/WS data (verified in Task 3 checkpoint); subagent/wave/diff viewers are absent
  </acceptance_criteria>
  <done>The capability palette, per-agent model picker, and validator/issue panel are wired into the composer from live /api/capabilities + WS data; deferred viewers are absent; the frontend builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the three composer panels render from live data (API-06)</name>
  <action>Pause for the human to run the dev backend + frontend and confirm the capability palette, per-agent model picker, and validator/issue panel populate from live /api/capabilities + WS data, and that the deferred subagent/wave/diff viewers are absent.</action>
  <what-built>
    The three additive composer panels — capability palette (live `/api/capabilities`), per-agent model picker (model catalog), and validator/issue panel (live `validator_result`/`validation_warning` WS events) — wired into `WorkflowComposer.tsx`. API-06 is a frontend visual render with no headless DOM harness in the parity suite, so a human confirms the panels populate from live data.
  </what-built>
  <how-to-verify>
    1. Run the dev backend (`cd backend && python3.11 -m uvicorn app.main:app --reload`) and the dev frontend.
    2. Open the workflow composer. Confirm the CAPABILITY PALETTE populates from `/api/capabilities` — entries show kind/name/`user_allowed`, including runtimes/skills/hooks and the model catalog (not a hardcoded list).
    3. Confirm the PER-AGENT MODEL PICKER lists models from the palette's model catalog and a selection is captured per agent.
    4. Run a prototype build and confirm the VALIDATOR/ISSUE PANEL populates from live `validator_result`/`validation_warning` events (issues grouped by CRITICAL/HIGH/MEDIUM/LOW).
    5. Confirm NO subagent/wave tree or repo-diff viewer appears (correctly deferred).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the panels.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/capabilities | An unauthenticated client must not read the palette |
| run stream → frontend panels | New events must not break the existing-workflow event contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-auth | Spoofing/Access Control | /api/capabilities | mitigate | `Depends(get_current_user)` (JWT) — same posture as every endpoint; 401 without a token (Task 1) |
| T-08-08-ID | Information Disclosure | palette exposes privileged caps | mitigate | the palette surfaces `user_allowed` so the UI can gate user-grantable caps; privileged kinds (exec/secrets/spawn/runtimes) are user_allowed=False (08-01) |
| T-08-08-parity | Tampering (regression) | new WS events | mitigate | new events are additive only (no rename/remove); existing characterization event contract unchanged; generic forward handles new types (Task 1) |
| T-08-08-SC | Tampering | npm/pip installs | accept | No new backend package; frontend uses existing deps (no new dependency) |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_capabilities_api.py -x` exits 0
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 (existing event contract unchanged; new events additive) in a clean session
- `cd frontend && npm run build` (or typecheck) exits 0; `cd backend && lint_imports` exits 0
</verification>

<success_criteria>
- GET /api/capabilities returns the auth-gated registry palette (kind, name, user_allowed, config schema) incl. runtimes/skills/hooks/model catalog + permission scopes
- The run stream emits validator_result/validation_warning/gate_* additively; existing workflows at semantic parity (no re-baseline)
- The capability palette + per-agent model picker + validator/issue panel render from live API/WS data; deferred viewers absent
- The existing characterization snapshots remain byte-identical + semantic event parity
</success_criteria>

## Artifacts this phase produces (08-08)
- New API route: `GET /api/capabilities` (app/api/capabilities.py) + `capabilities_router` registration (app/main.py)
- New WS events surfaced: `validator_result`, `validation_warning`, `gate_*` (additive; flow through the generic forward)
- New frontend components: `CapabilityPalette.tsx`, `AgentModelPicker.tsx`, `ValidatorIssuePanel.tsx` (wired into `WorkflowComposer.tsx`)
- New file: `backend/tests/unit/test_capabilities_api.py`

<output>
Create `.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-08-SUMMARY.md` when done
</output>
