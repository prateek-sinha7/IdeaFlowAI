---
phase: 08-capabilities-hardened-registry-gates-tool-perms-runtime-3
plan: 06
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - backend/agents/factory.py
  - backend/agents/execution_engine/engine.py
  - backend/tests/agents/test_constitution_prod.py
autonomous: true
requirements: [AGENTRT-06]
user_setup: []

must_haves:
  truths:
    - "A Postgres/DB-stored Constitution is injected into the composed prompt under a running event loop (production) — the no-op is fixed"
    - "The constitution-injected-in-prod test passes (DB-stored Constitution appears in the composed prompt under a running event loop)"
    - "The existing characterization snapshots stay byte-identical (the characterization runs carry NO Constitution, so the fix is invisible to them)"
    - "The _inject_constitution event-loop-running _mem-only branch (R12) is deleted; injection is sync-safe / pre-warmed via the PromptAssemblyPolicy path"
    - "The F4 ledger row is ☑ and all F1–F5 rows are ☑ (migration-ledger ratchet green)"
  artifacts:
    - path: "backend/tests/agents/test_constitution_prod.py"
      provides: "AGENTRT-06 constitution-injected-in-prod test (NEW)"
      contains: "constitution"
    - path: "backend/agents/factory.py"
      provides: "Constitution sync-safe / pre-warmed injection; R12 _mem-only running-loop branch deleted"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "constitution pre-warm at run entry"
      via: "async pre-warm before the sync create_runner calls"
      pattern: "constitution"
    - from: "backend/agents/factory.py"
      to: "pre-warmed constitution cache"
      via: "sync read of the pre-warmed value"
      pattern: "constitution"
---

<objective>
Fix the R12 constitution no-op (F4 per D-08, AGENTRT-06): today `_inject_constitution` (factory.py:258-306) reads only the in-process `_mem` dict when an event loop is running, so a Postgres-stored Constitution is silently NOT injected in production. Make injection sync-safe / pre-warmed (the lower-risk pre-warm-at-run-entry approach per RESEARCH A3) so a DB-stored Constitution is injected under a running event loop, and DELETE the `_mem`-only running-loop branch (F4 ledger row → ☑). The existing characterization runs carry NO Constitution, so the fix is invisible to the snapshots (byte-identical preserved); the constitution-injected-in-prod test is a NEW test, not a re-baseline.

Purpose: AGENTRT-06 — the only R12 bug fix this phase (prompt-content changes are deferred per Q37). It completes the F1–F5 deletion set (F4 was the last factory leak; F1/F3/F5 landed in 08-05, F2 in 08-03).
Output: the pre-warm seam at run entry, the sync-safe constitution read in factory, the deleted R12 branch, and the new in-prod test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-SPEC.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-CONTEXT.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-RESEARCH.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-PATTERNS.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-warm the Constitution at run entry + sync-safe read; NEW in-prod test</name>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/test_constitution_prod.py</files>
  <behavior>
    - Test (test_constitution_prod.py, NEW): with a DB-stored Constitution set for an owner and an event loop running, the composed prompt for a governed agent CONTAINS the Constitution block (`## Constitution (Governing Principles...`).
    - Test: with NO Constitution set, the composed prompt is unchanged (graceful no-op; matches the characterization runs).
    - Test: the pre-warm runs once at run entry (before the sync create_runner calls) — no `RuntimeError: event loop already running`.
  </behavior>
  <read_first>
    - backend/agents/factory.py — `_inject_constitution` (factory.py:258-306); the running-loop branch (factory.py:282-292) that reads only `memory._mem` (R12); `create_runner` is SYNC (factory.py:69) but called from the async engine
    - backend/agents/execution_engine/engine.py — `_run_agent` (the async caller of the sync create_runner, ~engine.py:1381 per RESEARCH); the run-entry point where the pre-warm fits (before the per-agent create_runner calls)
    - backend/agents/workflow_memory/memory.py — `get_workflow_memory()` + `get_constitution(user_id)` (async) + the `_mem` cache the R12 branch reads
    - 08-RESEARCH.md § "Pitfall 3: Constitution fix changes existing snapshots" + Assumptions A3/A4 — pre-warm-at-run-entry is lower-risk than await-in-running-loop; the characterization runs carry NO Constitution (verify before — grep the characterization fixtures for constitution) so snapshots stay byte-identical
    - 08-PATTERNS.md § "F1/F3/F4 → ..." — F4/R12 fix: pre-warm the constitution at run entry; the constitution-injected-in-prod test is NEW
  </read_first>
  <action>
    Per AGENTRT-06 / RESEARCH A3: at the async engine run entry (before the sync `create_runner` calls in `_run_agent`), pre-warm the owner's Constitution by awaiting `get_constitution(user_id)` ONCE and caching it on `ExecutionContext` (a pre-warmed value the sync factory can read without awaiting). In `_inject_constitution`, read the pre-warmed value from the context (sync) instead of the `_mem`-only running-loop branch — so a DB-stored Constitution injects in production under a running event loop. FIRST verify the characterization fixtures set NO Constitution (grep — RESEARCH A4) so the snapshots are unaffected; if any do, surface it before proceeding. Write the NEW `test_constitution_prod.py` covering the three behaviors (the test drives a governed agent under a running loop with a DB-stored Constitution and asserts the block appears). Do NOT delete the R12 branch yet (Task 2 deletes after the new test + snapshots prove parity).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_constitution_prod.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_constitution_prod.py -x` exits 0 (DB Constitution injected under a running loop; no-Constitution path unchanged; no event-loop RuntimeError)
    - Behavior: the composed prompt for a governed agent with a DB Constitution contains `## Constitution (Governing Principles`
    - `grep -rc "constitution" backend/tests/agents/characterization/ 2>/dev/null | grep -v ':0'` returns empty OR the SUMMARY records that the characterization fixtures set no Constitution (A4 verified)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (snapshots byte-identical — the pre-warm is invisible without a Constitution set)
  </acceptance_criteria>
  <done>The Constitution is pre-warmed at run entry and read sync-safely so it injects in prod; the new test passes; the characterization snapshots are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: DELETE the R12 _mem-only running-loop branch; flip F4 ledger row</name>
  <files>backend/agents/factory.py, backend/tests/agents/test_constitution_prod.py</files>
  <read_first>
    - backend/agents/factory.py — `_inject_constitution` (factory.py:258-306), specifically the running-loop branch (factory.py:282-292) reading `memory._mem` — the R12 deletion target; ensure the sync-safe pre-warmed read (Task 1) fully supersedes it
    - specs/003-workflow-engine-decoupling/migration-ledger.md — the F4 row (CHECK: constitution-injected-in-prod test passes; phase 3; ☐ → ☑)
    - backend/tests/agents/test_migration_ledger.py — the F4 ratchet
    - 08-CONTEXT.md `<deep_work_rules>` — deletion sequencing: the new in-prod test + snapshots PROVE the fix before deleting the R12 branch
  </read_first>
  <action>
    Now that the pre-warmed sync-safe read (Task 1) supersedes the broken branch and the new in-prod test passes: DELETE the `_mem`-only event-loop-running branch from `_inject_constitution` (the R12 construct, factory.py:282-292) and any now-dead async-in-running-loop scaffolding, leaving the single sync-safe pre-warmed read path (INV-12 — no dual path). Flip the migration-ledger F4 row to ☑ (its CHECK is "constitution-injected-in-prod test passes" — now true). Re-run the new test + the 5 snapshots + the ledger ratchet. With F4 ☑, ALL of F1–F5 are now deleted (F2=08-03, F1/F3/F5=08-05, F4=08-06).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_constitution_prod.py tests/agents/test_migration_ledger.py tests/agents/test_characterization_*.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_mem.get" backend/agents/factory.py` returns 0 (the R12 _mem-only running-loop branch deleted)
    - `cd backend && python3.11 -m pytest tests/agents/test_constitution_prod.py -x` exits 0 (the F4 CHECK gate: constitution-injected-in-prod passes)
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` exits 0 AND shows the F4 row ☑ (and F1/F2/F3/F5 all ☑)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (snapshots byte-identical)
    - `cd backend && lint_imports` exits 0
  </acceptance_criteria>
  <done>The R12 branch is deleted, the single sync-safe path remains, the F4 ledger row is ☑, and all F1–F5 rows are now ☑.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Constitution store → composed prompt | A governing Constitution must inject in production (not silently dropped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-R | Repudiation/Integrity | constitution silently dropped in prod | mitigate | pre-warm at run entry + sync-safe read injects the DB Constitution under a running loop; the new in-prod test is the gate (Task 1/2) |
| T-08-06-parity | Tampering (regression) | F4 fix changing existing snapshots | mitigate | characterization runs carry no Constitution (A4 verified) → snapshots byte-identical; the new test is additive (Task 1) |
| T-08-06-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_constitution_prod.py -x` exits 0
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py tests/agents/test_migration_ledger.py -x` exits 0 (parity + F1–F5 all ☑) in a clean session
- `cd backend && lint_imports` exits 0; banned-pattern green
</verification>

<success_criteria>
- A DB-stored Constitution is injected under a running event loop (production); the constitution-injected-in-prod test passes
- The R12 _mem-only running-loop branch is deleted; injection is single-path sync-safe / pre-warmed; F4 ledger row ☑
- The 5 characterization snapshots stay byte-identical (no Constitution set in those runs)
- All F1–F5 ledger rows are now ☑
</success_criteria>

## Artifacts this phase produces (08-06)
- Pre-warm seam: owner Constitution awaited once at engine run entry, cached on ExecutionContext
- Sync-safe constitution read in factory `_inject_constitution`
- Deleted: the R12 `_mem`-only event-loop-running branch (factory.py:282-292); F4 ledger row ☑
- New file: `backend/tests/agents/test_constitution_prod.py`

<output>
Create `.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-06-SUMMARY.md` when done
</output>
