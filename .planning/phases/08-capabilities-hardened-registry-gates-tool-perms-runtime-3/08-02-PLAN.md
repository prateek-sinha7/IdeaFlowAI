---
phase: 08-capabilities-hardened-registry-gates-tool-perms-runtime-3
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/agents/capabilities/gates/__init__.py
  - backend/agents/capabilities/gates/human.py
  - backend/agents/capabilities/gates/validation.py
  - backend/agents/capabilities/gates/approval.py
  - backend/agents/capabilities/gates/security.py
  - backend/agents/execution_engine/engine.py
  - backend/app/models/validation_results.py
  - backend/app/models/gate_events.py
  - backend/app/models/hook_runs.py
  - backend/alembic/versions/0016_capability_hardening_tables.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/tests/agents/test_gates.py
  - backend/tests/unit/test_migrations.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]
user_setup: []

must_haves:
  truths:
    - "All four gate kinds (human/validation/approval/security) resolve from the registry as GateHandler impls"
    - "A step declaring gates:[security] that requests exec is blocked (exec OFF)"
    - "A step declaring gates:[approval] pauses for sign-off"
    - "A step with gates:[validation] + a validator producing a P0 issue blocks; with only a P2 issue emits validation_warning and proceeds"
    - "The validation gate maps issues through the single canonical map_severity from 08-01 (validators/severity.py) — imported, not a local re-definition or a stub"
    - "The human gate routes through the existing _run_review_gate with identical review_gate_* sequences (no snapshot re-baseline)"
    - "Each gate firing writes a gate_events row carrying owner_id + workspace_id"
    - "Additive migration 0016 adds validation_results, gate_events, hook_runs (each owner_id + workspace_id), head 0015→0016"
  artifacts:
    - path: "backend/agents/capabilities/gates/validation.py"
      provides: "Real Validation_Gate (runs declared validators, applies block-critical/warn-non-critical policy)"
      contains: "register(\"gate\", \"validation\")"
    - path: "backend/agents/capabilities/gates/security.py"
      provides: "security gate (default-denies exec/network/secrets)"
    - path: "backend/agents/capabilities/gates/approval.py"
      provides: "approval gate (explicit sign-off → wait_human)"
    - path: "backend/agents/capabilities/gates/human.py"
      provides: "human gate routing to engine._run_review_gate (parity)"
    - path: "backend/alembic/versions/0016_capability_hardening_tables.py"
      provides: "Additive migration: validation_results, gate_events, hook_runs"
      contains: "down_revision = \"0015\""
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "registry.resolve(\"gate\", name)"
      via: "step-boundary gate evaluation in the dispatch loop"
      pattern: "resolve\\(.gate."
    - from: "backend/agents/capabilities/gates/validation.py"
      to: "registry.resolve(\"validator\", name)"
      via: "step.validators loop — GATE-02 behavioral wiring"
      pattern: "resolve\\(.validator."
    - from: "backend/agents/capabilities/gates/validation.py"
      to: "gate_events row write"
      via: "ScopedStore writer through KernelServices"
      pattern: "gate_events"
---

<objective>
Stand up the `GateHandler` registry (D-03): register `human`/`validation`/`approval`/`security` gates as real `GateHandler` impls, evaluated in declared order at the step boundary by the kernel; outcome ∈ `pass | block | wait_human`. Make the declared-but-dead `Validation_Gate` real (GATE-02): it runs the step's declared validators and applies the failure policy (block-critical / warn-non-critical), emitting `validation_warning` on residuals. Preserve `human`-gate parity (GATE-03) by routing the registered `human` gate to the existing `engine._run_review_gate`. Front-load the additive `0016` migration here with ALL THREE §18 tables (`validation_results`/`gate_events`/`hook_runs`) so 08-04 and 08-07 only write rows.

Purpose: Gates become first-class registry-driven handlers (GATE-01/02/03); the `security` gate builds the default-deny enforcement the trust model needs; the `0016` migration is the shared persistence substrate for validators (08-04) and hooks (08-07).
Output: four gate impls under `agents/capabilities/gates/`, the engine step-boundary gate seam, the three SQLAlchemy models + `0016` Alembic migration, a `gate_events` ScopedStore writer, and the gate + migration tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-SPEC.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-CONTEXT.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-RESEARCH.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-PATTERNS.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 0016 migration + three models + gate_events ScopedStore writer</name>
  <files>backend/app/models/validation_results.py, backend/app/models/gate_events.py, backend/app/models/hook_runs.py, backend/alembic/versions/0016_capability_hardening_tables.py, backend/agents/execution_engine/kernel_services.py, backend/tests/unit/test_migrations.py</files>
  <behavior>
    - Test (test_migrations.py): `alembic upgrade head` creates all three tables; each has `owner_id` and `workspace_id` columns (both NOT NULL).
    - Test: each model row write goes through the ScopedStore (owner/workspace sourced from ExecutionContext) — a cross-owner read returns nothing (default-deny precedent).
    - Test: the migration's down_revision is "0015" (head chain unbroken).
  </behavior>
  <read_first>
    - backend/app/models/run_capabilities.py — the EXACT §18 additive-table precedent: UUID PK + run_id FK + owner_id + workspace_id (both nullable=False) + payload cols; its skills/hooks JSON cols reserved "Phase 8"
    - backend/alembic/versions/0015_drop_thin_artifact_store.py — the head migration; new migration's `down_revision = "0015"`
    - backend/agents/authz.py — the ScopedStore (authz.py:58) default-deny owner/workspace-scoped writer; mirror its write pattern; owner_id/workspace_id from ExecutionContext at write time
    - 08-PATTERNS.md § "Additive owner/workspace-scoped table" + § "app/models/validation_results.py..." — the model shape + index list (§18: validation_results (run_id, step); gate_events (run_id); hook_runs (run_id))
    - 08-RESEARCH.md § "Code Examples" → "Additive migration mirrors RunCapabilities (D-10)"
  </read_first>
  <action>
    Create three SQLAlchemy models mirroring `run_capabilities.py` (D-10): `ValidationResult` (`validation_results`: id UUID PK, run_id FK→workflow_runs.id, owner_id+workspace_id NOT NULL, step, validator name, severity in {CRITICAL,HIGH,MEDIUM,LOW}, attempt int, issues JSON); `GateEvent` (`gate_events`: id, run_id FK, owner_id+workspace_id, step, gate name, outcome in {pass,block,wait_human}, detail JSON); `HookRun` (`hook_runs`: id, run_id FK, owner_id+workspace_id, hook name, event, outcome in {continue,warn,block}, detail JSON). Write a SINGLE additive Alembic migration `0016_capability_hardening_tables.py` with `down_revision = "0015"`, `revision = "0016"`, creating all three tables additively (no destructive alter) with the §18 indexes (`validation_results` (run_id, step); `gate_events` (run_id); `hook_runs` (run_id)). Add ScopedStore writer methods (or extend the existing scoped writer) for the three tables, sourcing owner_id/workspace_id from ExecutionContext at write time, reached by capabilities via the `KernelServices` handle (NO kernel→app import — the writer lives where ScopedStore lives; capabilities call it via `ctx.runner`). Expose a `gate_events` write on the KernelServices handle for this plan; `validation_results`/`hook_runs` writes are consumed in 08-04/08-07. Write `test_migrations.py` covering the three behaviors.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_migrations.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'down_revision = "0015"' backend/alembic/versions/0016_capability_hardening_tables.py` returns 1
    - `grep -c "owner_id" backend/app/models/validation_results.py backend/app/models/gate_events.py backend/app/models/hook_runs.py` returns ≥3 (every table carries owner_id)
    - `grep -c "workspace_id" backend/app/models/validation_results.py backend/app/models/gate_events.py backend/app/models/hook_runs.py` returns ≥3
    - `cd backend && python3.11 -m pytest tests/unit/test_migrations.py -x` exits 0 (alembic upgrade head creates the 3 tables; each owner+workspace)
    - Behavior: a `gate_events` write through the ScopedStore is owner/workspace-scoped; a cross-owner read returns nothing
  </acceptance_criteria>
  <done>0016 lands additively with the three owner/workspace-scoped tables and an unbroken head chain; gate_events writes through ScopedStore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four GateHandler impls (human/validation/approval/security)</name>
  <files>backend/agents/capabilities/gates/__init__.py, backend/agents/capabilities/gates/human.py, backend/agents/capabilities/gates/validation.py, backend/agents/capabilities/gates/approval.py, backend/agents/capabilities/gates/security.py, backend/tests/agents/test_gates.py</files>
  <behavior>
    - Test (test_gates.py): all four gate kinds resolve via `CapabilityRegistry().resolve("gate", name)` after discover().
    - Test: the `security` gate evaluating a step that requests `exec` returns outcome `block` (exec OFF this phase).
    - Test: the `approval` gate returns outcome `wait_human` (pauses for sign-off) and is exercised by a test manifest step `gates:[approval]`.
    - Test: the `validation` gate run against a target with a P0 issue returns `block`; with only a P2 issue returns `pass` AND emits a `validation_warning` (residual), proceeding.
    - Test: the `validation` gate resolves the step's declared `validators: [...]` from the registry (`resolve("validator", name)`) and maps their issues through the imported `map_severity` from `agents.capabilities.validators.severity` (08-01) — no local severity table.
    - Test: each gate firing writes a `gate_events` row with the outcome.
  </behavior>
  <read_first>
    - backend/agents/capabilities/base.py — the `GateHandler` Protocol (base.py:105: `async def evaluate(self, step, ctx) -> Any`); copy the idiom
    - backend/agents/capabilities/validators/severity.py — the single canonical `map_severity` (created in 08-01); the validation gate IMPORTS it (`from agents.capabilities.validators.severity import map_severity`) — do NOT re-define a severity table here, and do NOT stub it for 08-04 to fill (it already exists)
    - backend/agents/capabilities/post_steps/revision_validation.py — the closest capability-impl analog (role + self-registration shape)
    - backend/agents/execution_engine/engine.py — `_run_review_gate` (the human gate behavior to route to) + `_should_gate`; the `review_gate_*` events; the per-step dispatch loop seam where gates evaluate
    - 08-RESEARCH.md § "Code Examples" → "The engine gate seam (D-03)" — the pre-step (security/approval) vs post-step (validation) placement; human → existing _run_review_gate unchanged
    - 08-RESEARCH.md § "Security Domain" — the security gate default-denies exec/network/secrets; defaults OFF
  </read_first>
  <action>
    Create `agents/capabilities/gates/` with four `GateHandler` impls, each `@register("gate", <name>, user_allowed=...)` (per D-01) and satisfying the port (`name`, `async def evaluate(step, ctx) -> outcome`); outcome ∈ `pass | block | wait_human`:
    - `human.py` (`@register("gate","human", user_allowed=True)`): routes to the existing `engine._run_review_gate` behavior via the KernelServices handle so `review_gate_*` events are byte/event-identical — GATE-03 parity, NO new behavior. Do NOT duplicate the HITL logic; delegate.
    - `validation.py` (`@register("gate","validation", user_allowed=True)`): runs the step's declared `validators: [...]` (resolved from the registry via `resolve("validator", name)`, reached through the handle), maps each issue's internal P0–P3 severity through the SHARED `map_severity` IMPORTED from `agents.capabilities.validators.severity` (the single canonical function created in 08-01 — NOT a local copy, NOT a stub for 08-04 to fill; it is already present and importable at 08-02 execution time), applies block-critical / warn-non-critical, emits `validation_warning` on residual non-critical issues, returns `block` on a critical issue else `pass`. Write a `gate_events` row.
    - `approval.py` (`@register("gate","approval", user_allowed=False)`): requires explicit sign-off → returns `wait_human` (no sensitive action exists to trigger it until P9/P10, so it is exercised by a test manifest). Write a `gate_events` row.
    - `security.py` (`@register("gate","security", user_allowed=False)`): default-denies a step requesting `exec`/`network`/`secrets` (which stay OFF this phase) → returns `block`; else `pass`. Write a `gate_events` row.
    Write `test_gates.py` covering the six behaviors + two test manifest stubs declaring `gates:[approval]` and `gates:[security]`. NOTE: at 08-02 execution time no `validator` impls are registered yet (those land in 08-04), so the validation-gate tests use a tiny in-test fake validator registered against the registry to drive the policy + the imported `map_severity` — the gate's WIRING (resolve("validator", …) + map_severity import + policy) is proven here; the real html_static/html_render/Tier validators are wired through this same path in 08-04.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_gates.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'register("gate"' backend/agents/capabilities/gates/` returns 4 (human/validation/approval/security registered)
    - `grep -c "from agents.capabilities.validators.severity import map_severity\|validators.severity" backend/agents/capabilities/gates/validation.py` returns ≥1 (imports the single canonical mapping from 08-01 — no local severity table, no stub)
    - `cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry, discover; discover(); r=CapabilityRegistry(); [r.resolve('gate',n) for n in ('human','validation','approval','security')]; print('4 gates resolve')"` prints `4 gates resolve`
    - `cd backend && python3.11 -m pytest tests/agents/test_gates.py -x` exits 0
    - `cd backend && python3.11 -m pytest tests/agents/test_gates.py -k validation -x` exits 0 (P0 blocks; P2 emits validation_warning + proceeds; resolves validators + maps via the imported map_severity)
    - Behavior: a `gates:[security]` step requesting exec returns `block`; a `gates:[approval]` step returns `wait_human`
  </acceptance_criteria>
  <done>All four gates resolve from the registry, evaluate to pass|block|wait_human, and write gate_events rows; the validation gate resolves declared validators and maps severity through the single imported map_severity; the policy is block-critical/warn-non-critical.</done>
</task>

<task type="auto">
  <name>Task 3: Wire gate evaluation at the engine step boundary (additive; human parity)</name>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/test_gates.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py — the per-step dispatch loop (the `resolve("strategy", name).run(step, ctx)` seam) + the existing `_should_gate`/`_run_review_gate` (human gate) call site; the post_step invocation site
    - 08-RESEARCH.md § "Code Examples" → "The engine gate seam (D-03)" — pre-step gates (security/approval) block BEFORE the strategy runs; post-step gates (validation) evaluate AFTER; human stays the existing path
    - 08-RESEARCH.md § "Pitfall 6" + § "Common Pitfalls" — gates must be additive; new gate_* events are additive only; existing review_gate_* sequences unchanged
    - backend/tests/agents/test_characterization_prototype.py — the human-gate parity snapshot (review_gate_* sequence) that must stay byte/event-identical
  </read_first>
  <action>
    At the engine's per-step dispatch loop, evaluate a step's declared `gates: [...]` in order at the step boundary (D-03): pre-step gates (`security`/`approval`) evaluate BEFORE `resolve("strategy").run(...)` and a `block`/`wait_human` outcome halts the step (additively — a blocked step emits the new `gate_*` event, not a renamed existing one); post-step gates (`validation`) evaluate AFTER the strategy completes. The existing `human` gate path (`_should_gate` → `_run_review_gate` → `review_gate_*`) stays EXACTLY as-is for parity; the registered `human` gate delegates to it (no second HITL mechanism). New `gate_*` events are additive only — emit them via the generic event path so they flow through `websocket.py:595` unchanged. Do NOT move existing prototype validation into a gate (that stays inline in task_loop per D-06). Add engine-level test cases to test_gates.py proving a `gates:[security]` step halts and a gated prototype agent still emits identical `review_gate_*` events.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_gates.py tests/agents/test_characterization_prototype.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (human-gate review_gate_* sequences byte/event-identical; NO re-baseline; new gate_* events additive only)
    - Behavior: a step declaring `gates:[security]` requesting exec halts at the step boundary with a `block` outcome event
    - `cd backend && python3.11 -m pytest tests/agents/test_gates.py -x` exits 0 (engine-level gate evaluation cases pass)
    - `grep -c "resolve(.gate." backend/agents/execution_engine/engine.py` returns ≥1 (gates resolved from the registry at the step boundary)
  </acceptance_criteria>
  <done>Gates evaluate in declared order at the step boundary; security/approval block/pause additively; the human gate keeps identical review_gate_* parity with no snapshot re-baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| step request → security gate | A step may request a privileged permission (exec/network/secrets) |
| capability → gate_events table | Gate firings write owner/workspace-scoped audit rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-EoP | Elevation of Privilege | security gate | mitigate | security gate default-denies exec/network/secrets (returns `block`); defaults OFF this phase (Task 2/3) |
| T-08-02-ID | Information Disclosure | gate_events cross-owner read | mitigate | gate_events carries owner_id+workspace_id; writes/reads via ScopedStore default-deny (Task 1) |
| T-08-02-parity | Tampering (regression) | human gate re-route | mitigate | human gate delegates to the unchanged _run_review_gate; characterization snapshots gate parity (Task 3 acceptance) |
| T-08-02-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_gates.py tests/unit/test_migrations.py -x` exits 0
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 (5 pipelines parity; human gate unchanged) in a clean session
- `cd backend && lint_imports` exits 0 (gates import the port + handle, no kernel→app edge); banned-pattern + migration-ledger green
</verification>

<success_criteria>
- Four GateHandler kinds resolve from the registry; outcome pass|block|wait_human
- Validation_Gate is real (block-critical / warn-non-critical, validation_warning on residuals); resolves declared validators and maps severity via the single imported map_severity from 08-01
- human gate at semantic parity (identical review_gate_*; no re-baseline)
- 0016 adds validation_results/gate_events/hook_runs additively (each owner+workspace); gate_events rows written via ScopedStore
</success_criteria>

## Artifacts this phase produces (08-02)
- Classes/Capabilities: `HumanGate`, `ValidationGate`, `ApprovalGate`, `SecurityGate` (each `@register("gate", ...)`)
- New files: `agents/capabilities/gates/{__init__,human,validation,approval,security}.py`
- Models: `ValidationResult`, `GateEvent`, `HookRun` (app/models/{validation_results,gate_events,hook_runs}.py)
- Migration: `alembic/versions/0016_capability_hardening_tables.py` (revision "0016", down_revision "0015")
- New tables: `validation_results`, `gate_events`, `hook_runs` (each owner_id + workspace_id)
- KernelServices handle method: `gate_events` write (ScopedStore-backed)
- Consumes (does NOT create): `map_severity` from `agents/capabilities/validators/severity.py` (created in 08-01)
- New WS event names: `gate_*` (additive), `validation_warning` (additive)
- New files: `backend/tests/agents/test_gates.py`, `backend/tests/unit/test_migrations.py`

<output>
Create `.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-02-SUMMARY.md` when done
</output>
