---
phase: 08-capabilities-hardened-registry-gates-tool-perms-runtime-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/registry.py
  - backend/agents/capabilities/base.py
  - backend/agents/capabilities/validators/__init__.py
  - backend/agents/capabilities/validators/severity.py
  - backend/tests/agents/test_registry_capabilities.py
  - backend/tests/agents/test_strategies.py
  - backend/tests/agents/test_compiler_trust.py
  - backend/tests/agents/test_severity.py
  - backend/agents/workflows/compiler.py
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, VALID-03]
user_setup: []

must_haves:
  truths:
    - "A capability module with @register(kind, name) is resolvable with zero edits to registry.py (no central if/elif)"
    - "discover() imports every built-in capability subpackage AND app/agents/validators/, is idempotent, and never runs at compiler import"
    - "install()/_register_builtins() no longer exists in registry.py (deleted, single successor is discover())"
    - "The new tool/skill/hook/runtime capability KINDS are registrable and resolve from the registry"
    - "A user-trust compile referencing a user_allowed=False capability raises a compile error naming the capability; a file/built-in manifest referencing the same capability compiles"
    - "test_strategies.py no longer pollutes the global _IMPLS across a same-session run (autouse save/restore reset fixture)"
    - "A single canonical map_severity P0–P3→CRITICAL/HIGH/MEDIUM/LOW function exists in severity.py (VALID-03 single source) and is importable by the 08-02 validation gate before 08-04 runs"
  artifacts:
    - path: "backend/agents/capabilities/registry.py"
      provides: "@register decorator + discover() + user_allowed trust flag; install() deleted"
      contains: "def register"
    - path: "backend/agents/capabilities/base.py"
      provides: "New Protocol ports: PromptAssemblyPolicy, AgentRuntimeAdapter, HookHandler, ToolProvider, SkillProvider, HookProvider"
      contains: "class PromptAssemblyPolicy"
    - path: "backend/agents/capabilities/validators/severity.py"
      provides: "The single canonical P0–P3→CRITICAL/HIGH/MEDIUM/LOW mapping function (VALID-03 single source)"
      contains: "def map_severity"
    - path: "backend/tests/agents/test_compiler_trust.py"
      provides: "CAP-03 trust enforcement test"
    - path: "backend/tests/agents/test_strategies.py"
      provides: "autouse _IMPLS save/restore reset fixture (D-12 fold)"
  key_links:
    - from: "backend/agents/workflows/compiler.py"
      to: "registry.is_user_allowed"
      via: "trust check at the per-reference validation seam"
      pattern: "is_user_allowed"
    - from: "@register"
      to: "_KNOWN / _IMPLS"
      via: "import-time binding at module load"
      pattern: "@register\\("
---

<objective>
Convert the Phase-7 explicit `install()`/`_register_builtins()` registry into a self-registering `CapabilityRegistry`: a `@register(kind, name)` decorator that binds `(kind,name)→impl` at module import, a startup `discover()` that explicitly imports the known capability subpackages (including the app-side `app/agents/validators/`), per-capability `user_allowed` trust flags, the new `tool`/`skill`/`hook`/`runtime` capability kinds, and the new Protocol ports those kinds + the F1–F5 lift will satisfy. Delete `install()`/`_register_builtins()` (INV-12 — `discover()` is its single successor). Add the compiler trust check (CAP-03). Create the single canonical `map_severity` function in `agents/capabilities/validators/severity.py` (VALID-03 single source) so the 08-02 validation gate can import it cleanly BEFORE 08-04 runs (08-04 imports — never recreates — it). Fold the Phase-7 test-isolation fix (D-12).

This is the Wave-1 substrate everything in Waves 2–8 registers into and resolves from.

Purpose: CAP-01/CAP-02 make the registry self-registering with no central dispatch; CAP-03 adds the trust seam that keeps `exec`/`secrets`/`spawn_subagents` off the user palette; VALID-03's single severity function lands here so it is a stable import for the 08-02 gate (eliminating the forward reference to 08-04). The new ports unblock 08-02 (gates), 08-03 (tools), 08-04 (validators), 08-05 (runtime/prompt/skill/hook providers), 08-07 (hooks).
Output: `registry.py` (decorator + discover() + trust + new kinds, install() deleted), `base.py` (6 new ports), `validators/severity.py` (the single `map_severity` function), the compiler trust check, the new trust + severity tests, and the test-isolation reset fixture.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-SPEC.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-CONTEXT.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-RESEARCH.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-PATTERNS.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the six new capability ports to base.py</name>
  <files>backend/agents/capabilities/base.py</files>
  <read_first>
    - backend/agents/capabilities/base.py — the current ports (TaskParser, ExecutionStrategy, Validator, DeliverableResolver, ContextProvider, PostStep, GateHandler); copy the EXACT one-method-Protocol idiom (`@runtime_checkable`, `name: str`, ONE method, `...` body, stdlib `typing` only, runtime objects typed `Any`)
    - 08-PATTERNS.md § "Capability port (one-method Protocol)" — the idiom every new port copies (base.py:42-112)
    - 08-RESEARCH.md § "Architecture Patterns" → Recommended Project Structure — the new kinds (PromptAssemblyPolicy, AgentRuntimeAdapter, HookHandler, ToolProvider, SkillProvider, HookProvider)
  </read_first>
  <action>
    Add six new `@runtime_checkable` Protocol ports to base.py, each with `name: str` and one method, typing all runtime objects as `Any`, importing ONLY stdlib `typing` (no kernel/app import — keeps the import-linter kernel→ports direction one-way):
    - `PromptAssemblyPolicy` — `def assemble(self, blocks: dict[str, Any], ctx: Any) -> str` (transform a named-block mapping into the composed prompt str; the default order constant lives with the impl in 08-05).
    - `AgentRuntimeAdapter` — `def create(self, agent_id: str, ctx: Any) -> Any` (wraps/selects a runner; INV-13 — the impl wraps `DeepAgentRunner`, never re-implements it).
    - `HookHandler` — `async def handle(self, event: Any, ctx: Any) -> Any` (returns a hook outcome `continue|warn|block`; carries `events: list[str]` and `required_permission: str | None` attributes alongside `name`).
    - `ToolProvider` — `def provide(self, spec: Any, ctx: Any) -> Any` (returns the `(custom_tools, exclude_builtin)` tuple for a tool set).
    - `SkillProvider` — `async def provide(self, ctx: Any) -> list[Any]` (returns versioned skill blocks; carries a `version` notion in the returned items).
    - `HookProvider` — `async def provide(self, ctx: Any) -> list[Any]` (returns hook descriptors, incl. the `behavioral` non-executable sub-type block).
    Match the existing docstring style. Do NOT bind any impl here (ports are interface-only). Do NOT add the kinds to `_KNOWN` here (that is Task 2 / per-plan).
  </action>
  <verify>
    <automated>cd backend && python3.11 -c "from agents.capabilities.base import PromptAssemblyPolicy, AgentRuntimeAdapter, HookHandler, ToolProvider, SkillProvider, HookProvider; print('ports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class PromptAssemblyPolicy\|class AgentRuntimeAdapter\|class HookHandler\|class ToolProvider\|class SkillProvider\|class HookProvider" backend/agents/capabilities/base.py` returns 6
    - `grep -c "import" backend/agents/capabilities/base.py | head` shows only stdlib `typing` imports (no `from agents.execution_engine` / `from app`)
    - `cd backend && python3.11 -m pytest tests/agents/test_registry_capabilities.py -x` exits 0 (ports add no impl bindings)
    - Each new port is `@runtime_checkable`, has a `name: str` attribute, exactly one declared method, and a `...` body
  </acceptance_criteria>
  <done>The six ports import cleanly, are interface-only (stdlib typing only), and break no existing test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: @register decorator + discover() + user_allowed + new kinds; delete install()</name>
  <files>backend/agents/capabilities/registry.py, backend/tests/agents/test_registry_capabilities.py</files>
  <behavior>
    - Test: a module that calls `@register("strategy","single_shot")` is resolvable via `CapabilityRegistry().resolve("strategy","single_shot")` after `discover()`, with zero edits to registry's resolution code.
    - Test: `discover()` is idempotent (two calls bind the same set, no error).
    - Test: `is_registered("strategy","single_shot")` returns True at compiler import time with NO impl bound (impl-free membership preserved).
    - Test: resolving a new kind (e.g. `resolve("runtime","langchain_deepagents")` once registered in 08-05) works through the same path; the new kinds `tool`/`skill`/`hook`/`runtime` are accepted KIND strings.
    - Test: `register(...)` records `user_allowed` and `CapabilityRegistry().is_user_allowed(kind, name)` reflects it.
  </behavior>
  <read_first>
    - backend/agents/capabilities/registry.py — the FULL current file: `_KNOWN` (16 pairs), `install()`/`_register_builtins()` (the explicit binding being deleted), `resolve`/`is_registered`/`resolve_alias`, and the lazy-bind-on-first-resolve in `resolve` (registry.py:173-184). The docstring (:9-16) already names Phase 8 as owner.
    - backend/tests/agents/test_registry_capabilities.py — `_EXPECTED_NAMES` (:30-49) and `test_registered_count_is_exactly_sixteen` (:73) — the drift guards to update in lockstep; the impl-free-at-compiler-import assertion that MUST stay green
    - 08-PATTERNS.md § "agents/capabilities/registry.py" — the `@register`/`discover()`/`user_allowed` idiom, NO pkgutil, NO entry-points; discover() imports the known subpackages + `app/agents/validators/`; never at compiler import
    - 08-RESEARCH.md § "Pattern 3: discover() must NOT run at compiler import" + Pitfall 1 — the impl-free membership invariant
  </read_first>
  <action>
    Per D-01/D-02: add a module-level `register(kind, name, *, user_allowed=False)` decorator to registry.py that binds `(kind,name)→impl_instance` into `_IMPLS` at import AND records `user_allowed` into a `_TRUST: dict[tuple[str,str], bool]` map; it also adds `(kind,name)` into `_KNOWN` (so `_KNOWN` is decorator-populated — the single source). Add `discover()` that explicitly imports the known capability subpackages — `agents/capabilities/{strategies,deliverables,context_providers,task_parsers,compaction,post_steps,validators,gates,tools,skills,hooks,runtimes,prompt}/` AND `app/agents/validators/` (the only app-side capability package) — so each `@register` fires; guard with an idempotency flag mirroring the current `_INSTALLED` (registry.py:77,93-95). `discover()` takes the lazy slot in `resolve` (registry.py:176) — invoked at engine import / first `execute()`, NEVER at compiler import. DELETE `install()` and the `_register_builtins = install` alias (INV-12 — `discover()` is the single successor); convert the existing builtin bindings into `@register` decorators on their impl classes (do that wiring as those modules are touched in later plans; for THIS plan, register the already-existing Phase-7 impls — strategies/deliverables/context_providers/task_parsers/compaction/post_steps — by decorating their classes so `discover()` reproduces the exact `_IMPLS` set `install()` produced). Add `is_user_allowed(self, kind, name) -> bool` to `CapabilityRegistry`. Accept the new KIND strings `tool`/`skill`/`hook`/`runtime` (no central if/elif — KINDs are free strings keyed in `_KNOWN`). Update `test_registry_capabilities.py` `_EXPECTED_NAMES` + the count assertion in lockstep (expected in-scope edit; keep the impl-free-at-compiler-import assertion). Built-in capabilities register `user_allowed=False` by default; mark the safe ones (e.g. validators, model_catalog) `user_allowed=True` per the trust intent — the privileged kinds (tool sets granting exec/secrets/spawn, runtimes) stay `user_allowed=False`.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_registry_capabilities.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def install\|_register_builtins" backend/agents/capabilities/registry.py` returns 0 (install() deleted)
    - `grep -c "def register\|def discover\|def is_user_allowed" backend/agents/capabilities/registry.py` returns 3
    - `grep -c "pkgutil\|iter_modules\|walk_packages\|entry_points" backend/agents/capabilities/registry.py` returns 0 (explicit discover, no auto-walk)
    - `cd backend && python3.11 -m pytest tests/agents/test_registry_capabilities.py -x` exits 0 (impl-free membership at compiler import still holds; count assertion updated)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (discover() reproduces the install() _IMPLS set — strategies/deliverables/providers/parsers/compaction/post_steps all resolve identically)
    - `cd backend && lint_imports` exits 0 (discover() importing app/agents/validators/ does not create a kernel→app edge from the registry — registry only IMPORTS the package to trigger its @register; the validators import the port, not the reverse)
  </acceptance_criteria>
  <done>discover() self-registers all existing built-ins idempotently; install() is gone; user_allowed is recorded; the new kinds are accepted; the 5 characterization snapshots stay byte/event-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compiler trust check (CAP-03) + test_strategies.py reset fixture (D-12 fold)</name>
  <files>backend/agents/workflows/compiler.py, backend/tests/agents/test_compiler_trust.py, backend/tests/agents/test_strategies.py</files>
  <behavior>
    - Test (test_compiler_trust.py): compiling a manifest with a user/db trust context that references a `user_allowed=False` capability raises a CompilerError whose message NAMES the offending `(kind, name)`.
    - Test: the SAME manifest compiled with a file/built-in trust context (the default for the 15 file-backed manifests) compiles successfully — Phase-4/7 compile parity holds (no existing manifest newly fails).
    - Test (test_strategies.py): after the autouse reset fixture, running test_strategies in the same session as the characterization suite leaves `_IMPLS` unpolluted (the global registry is restored).
  </behavior>
  <read_first>
    - backend/agents/workflows/compiler.py — the INV-4 per-reference validation seam (compiler.py:110-211; the `is_registered("strategy"/"gate"/"validator", ...)` calls at ~:161/:166/:171); the trust check slots in at the SAME per-reference site
    - 08-RESEARCH.md § "Code Examples" → "Trust check slots into the compiler (D-02)" — the exact seam + the default-trusted-for-file-manifests rule
    - backend/tests/agents/test_strategies.py — the global-registry mutation at :333-334 (`install()` + `_IMPLS[("compaction","html_skeleton")]`) and the finally at :360-361 that pops only ONE key (the pollution; deferred-items.md:49-68)
    - 08-CONTEXT.md § "Folded Items" — the autouse registry save/restore reset fixture requirement (D-12)
  </read_first>
  <action>
    Thread a trust context into the compile call per D-02: a manifest `source` / trust flag (`file|db`, defaulting to `file`/trusted for the existing file-backed manifests so Phase-4/7 parity holds). At the EXISTING per-reference validation site in compiler.py (where `is_registered` is called), when the trust context is user/db, ALSO check `registry.is_user_allowed(kind, name)` (and the owner allow-list seam — thread the owner but a missing allow-list defaults to the `user_allowed` flag only this phase, since DB manifests are later-phase) — a not-user-allowed reference raises the existing CompilerError construct, NAMING the `(kind, name)`. Do NOT fork the validation path; add the check inline at the same site. Write `test_compiler_trust.py` covering the three behaviors above. Add an autouse `_IMPLS` save/restore reset fixture to test_strategies.py (snapshot `registry._IMPLS` + `registry._INSTALLED`/the new idempotency flag + `_TRUST` before each test, restore after) so the global-registry mutation no longer leaks across a same-session run (D-12 fold) — with `@register` import-time binding the pollution surface is larger, so this is now required.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_compiler_trust.py tests/agents/test_strategies.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_compiler_trust.py -k trust -x` exits 0 (user-trust non-user-allowed → naming CompilerError; file manifest compiles)
    - Behavior: the CompilerError message for a non-user-allowed reference contains the literal `(kind, name)` of the offending capability
    - `cd backend && python3.11 -m pytest tests/agents/test_compiler*.py -x` exits 0 (no existing file-backed manifest newly fails — compile parity)
    - `cd backend && python3.11 -m pytest tests/agents/test_strategies.py tests/agents/test_characterization_*.py -x` exits 0 in ONE session (no global-registry pollution — D-12 fold proven)
    - `grep -c "autouse=True" backend/tests/agents/test_strategies.py` returns ≥1
  </acceptance_criteria>
  <done>The trust check rejects non-user-allowed refs under a user trust context while every existing manifest still compiles; the reset fixture removes the same-session pollution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: The single canonical map_severity function (VALID-03 single source)</name>
  <files>backend/agents/capabilities/validators/__init__.py, backend/agents/capabilities/validators/severity.py, backend/tests/agents/test_severity.py</files>
  <behavior>
    - Test (test_severity.py): `map_severity("P0") == "CRITICAL"`, `map_severity("P1") == "HIGH"`, `map_severity("P2") == "MEDIUM"`, `map_severity("P3") == "LOW"` — a single tested function (VALID-03 acceptance: "a single tested function").
    - Test: an unknown internal severity raises (or maps to a documented default) consistently — one deterministic behavior, no second mapping anywhere.
    - Test: `from agents.capabilities.validators.severity import map_severity` succeeds with NO impl/discover side-effect (importing the function does not trigger registry binding) — so the 08-02 validation gate can import it cleanly before 08-04 runs.
  </behavior>
  <read_first>
    - 08-SPEC.md § Requirements item 7 (VALID-01/02/03) — "internal P0–P3 severities with one function mapping to CRITICAL/HIGH/MEDIUM/LOW for the UI"; acceptance "the P0–P3→CRITICAL/HIGH/MEDIUM/LOW mapping is a single tested function"
    - 08-CONTEXT.md D-04 (Q24) — severity is internal P0–P3 with ONE mapping function to CRITICAL/HIGH/MEDIUM/LOW; the single-source rule
    - backend/agents/capabilities/__init__.py — the package init idiom for the new `validators/` subpackage (kept import-light so importing severity does not pull heavy deps)
  </read_first>
  <action>
    Per VALID-03 (the single-source rule): create `agents/capabilities/validators/` (kernel-side, pure-stdlib, NO heavy-dep import) with `severity.py` containing exactly ONE function `map_severity(internal: str) -> str` mapping `P0→CRITICAL`, `P1→HIGH`, `P2→MEDIUM`, `P3→LOW` (and a single, deterministic behavior for an unknown input). This is THE canonical mapping the whole phase shares: the 08-02 validation gate imports it (eliminating the forward reference the checker flagged), 08-04's validators import it (08-04 MUST NOT recreate it), and the API/frontend severity labels derive from it. Keep `severity.py` impl-free of any `@register`/discover side-effect so a bare `from agents.capabilities.validators.severity import map_severity` is safe at any import time (including from the gate before discover() runs). Add a minimal `validators/__init__.py` (package marker only; the registered validator impls land in 08-04 — this plan creates ONLY the package + the severity function). Write `test_severity.py` covering the three behaviors. Do NOT add `map_severity` to 08-04's creation scope — 08-04 imports this function.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_severity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def map_severity" backend/agents/capabilities/validators/severity.py` returns 1 (the single source; this is the ONLY definition in the tree)
    - `grep -rc "def map_severity" backend/ | grep -v ':0'` lists ONLY `severity.py` (no second definition anywhere — VALID-03 single-source)
    - `cd backend && python3.11 -c "from agents.capabilities.validators.severity import map_severity; print(map_severity('P0'), map_severity('P3'))"` prints `CRITICAL LOW`
    - `cd backend && python3.11 -m pytest tests/agents/test_severity.py -x` exits 0
    - Behavior: importing `map_severity` triggers no registry binding / discover() side-effect (safe to import from the 08-02 gate before 08-04)
  </acceptance_criteria>
  <done>A single `map_severity` P0–P3→CRITICAL/HIGH/MEDIUM/LOW function exists in `validators/severity.py`, is the only definition in the tree, and imports cleanly with no side-effect so the 08-02 gate can consume it before 08-04 runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest author (user/DB) → compiler | An untrusted user/DB manifest may reference any capability name |
| capability module import → global registry | @register binds impls into a process-global singleton at import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-EoP | Elevation of Privilege | compiler trust check (CAP-03) | mitigate | `user_allowed=False` on privileged caps + compiler raises a naming CompilerError under a user/db trust context (Task 3); the seam keeps exec/secrets/spawn off the user palette |
| T-08-01-T | Tampering | registry resolve() static lookup | mitigate | resolve() stays a static dict lookup over `_KNOWN`-validated names (no eval/exec, no dynamic import); @register binds only at known-subpackage import via explicit discover() — no pkgutil walk (Task 2) |
| T-08-01-arch | Tampering (architecture) | discover() importing app/agents/validators/ | mitigate | discover() IMPORTS the app-side package to trigger its @register; the validators import the kernel PORT (legal direction), never the reverse; `lint_imports` is the gate (Task 2 acceptance) |
| T-08-01-SC | Tampering | npm/pip installs | mitigate | No package installs in this plan; OTel install is gated in 08-07 behind a blocking human checkpoint |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_registry_capabilities.py tests/agents/test_compiler_trust.py tests/agents/test_strategies.py tests/agents/test_severity.py -x` exits 0
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 (5 pipelines byte/event-identical) in a clean session
- `cd backend && lint_imports` exits 0; `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -x` exits 0
</verification>

<success_criteria>
- `@register`/`discover()` self-register all built-ins; `install()`/`_register_builtins` deleted (grep → 0); no central if/elif in resolution
- `user_allowed` recorded per capability; compiler raises a naming error for a non-user-allowed reference under a user/db trust context; file manifests compile unrestricted
- The new `tool`/`skill`/`hook`/`runtime` kinds + the six new ports exist and resolve/import cleanly
- The single canonical `map_severity` function exists in `validators/severity.py` (VALID-03 single source) and imports cleanly for the 08-02 gate before 08-04 runs
- The 5 characterization snapshots stay byte/event-identical; test_strategies no longer pollutes the registry same-session
</success_criteria>

## Artifacts this phase produces (08-01)
- Decorator: `register(kind, name, *, user_allowed=False)` (registry.py)
- Functions: `discover()` (registry.py), `CapabilityRegistry.is_user_allowed(kind, name)`, `map_severity(internal)` (validators/severity.py — the single P0–P3→CRITICAL/HIGH/MEDIUM/LOW source)
- Maps: `_TRUST: dict[tuple[str,str], bool]` (registry.py)
- Ports (base.py): `PromptAssemblyPolicy`, `AgentRuntimeAdapter`, `HookHandler`, `ToolProvider`, `SkillProvider`, `HookProvider`
- New capability KINDs accepted: `tool`, `skill`, `hook`, `runtime`
- New package: `backend/agents/capabilities/validators/` (package marker + severity.py; validator impls land in 08-04)
- New files: `backend/tests/agents/test_compiler_trust.py`, `backend/tests/agents/test_severity.py`
- New fixture: autouse `_IMPLS` save/restore in `test_strategies.py`
- Deleted: `install()`, `_register_builtins` (registry.py)

<output>
Create `.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-01-SUMMARY.md` when done
</output>
