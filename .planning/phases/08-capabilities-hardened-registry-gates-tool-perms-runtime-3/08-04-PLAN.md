---
phase: 08-capabilities-hardened-registry-gates-tool-perms-runtime-3
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - backend/agents/capabilities/validators/__init__.py
  - backend/agents/capabilities/validators/severity.py
  - backend/agents/capabilities/validators/task_done_when.py
  - backend/agents/capabilities/validators/spec_plan_coverage.py
  - backend/app/agents/validators/__init__.py
  - backend/app/agents/validators/html_static.py
  - backend/app/agents/validators/html_render.py
  - backend/app/agents/validators/design_quality.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/agents/capabilities/strategies/task_loop.py
  - backend/tests/agents/test_validators.py
autonomous: true
requirements: [VALID-01, VALID-02, VALID-03, VALID-04, VALID-05]
user_setup: []

must_haves:
  truths:
    - "A Validator registry runs a step's declared validators: [...] via resolve; DeliverableContext carries path/content/Workspace/task-meta"
    - "html_static/html_render are registered Validators wrapping static_check/render_check, reached app-side via the KernelServices handle (no kernel→app import)"
    - "The fix-loop is generic — parameterized by deliverable name + max_attempts + FixPolicy template (not hardcoded prototype.html); default warn-non-critical / block-critical, then validation_warning with residuals"
    - "A single P0–P3→CRITICAL/HIGH/MEDIUM/LOW mapping function exists and is the source of truth"
    - "Each validator run/attempt writes a validation_results row"
    - "spec_plan_coverage, task_done_when, design_quality are registered validators; design_quality is warnings-first/non-blocking (P2/P3 only); each exercised by ≥1 test manifest"
    - "task_loop's inline validation routes through the registered validators + generic loop at byte/event parity (5 snapshots unchanged)"
  artifacts:
    - path: "backend/agents/capabilities/validators/severity.py"
      provides: "The single P0–P3→CRITICAL/HIGH/MEDIUM/LOW mapping function"
      contains: "def map_severity"
    - path: "backend/app/agents/validators/html_static.py"
      provides: "html_static registered Validator wrapping static_check"
      contains: "register(\"validator\", \"html_static\")"
    - path: "backend/app/agents/validators/html_render.py"
      provides: "html_render registered Validator wrapping render_check"
    - path: "backend/app/agents/validators/design_quality.py"
      provides: "Tier#6 design_quality validator (warnings-first, non-blocking)"
    - path: "backend/agents/capabilities/validators/task_done_when.py"
      provides: "Tier#5 task_done_when validator"
    - path: "backend/agents/capabilities/validators/spec_plan_coverage.py"
      provides: "Tier#4 spec_plan_coverage validator"
  key_links:
    - from: "backend/app/agents/validators/html_static.py"
      to: "target.runner.static_check"
      via: "KernelServices handle (no kernel→app import)"
      pattern: "runner\\.static_check"
    - from: "backend/agents/capabilities/strategies/task_loop.py"
      to: "runner.run_validation_fix_loop (generic, registered validators)"
      via: "unconditional delegation"
      pattern: "run_validation_fix_loop"
---

<objective>
Stand up the `Validator` registry + generic fix-loop + the single severity mapping (D-04/D-05/D-06): migrate `html_static`/`html_render` to registered `Validator`s wrapping `static_check`/`render_check` (living app-side in `app/agents/validators/`, reached via the `KernelServices` handle — never a kernel→`app.*` import); make the fix-loop generic (deliverable name + `max_attempts` + `FixPolicy` fix-prompt template, replacing the HTML-hardcoded internals of `KernelServices.run_validation_fix_loop`); add the single P0–P3→CRITICAL/HIGH/MEDIUM/LOW mapping function; land Tier#4/5/6 validators (`spec_plan_coverage`, `task_done_when`, `design_quality` warnings-first); and re-point `task_loop`'s inline validation onto the registered validators at strict INV-3 parity. Each run/attempt writes a `validation_results` row (uses the `0016` table from 08-02).

Purpose: VALID-01..05 — validators become declarative + registered, the fix-loop generic, severity unified, prototype's checks persist. The strictest parity risk in the phase (re-pointing task_loop validation) is gated by the 5 characterization snapshots.
Output: the app-side + kernel-side validator packages, the severity function, the generic fix-loop, the task_loop re-point, and the validator tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-SPEC.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-CONTEXT.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-RESEARCH.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-PATTERNS.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-01-SUMMARY.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Severity mapping + DeliverableContext + html_static/html_render registered validators (app-side)</name>
  <files>backend/agents/capabilities/validators/__init__.py, backend/agents/capabilities/validators/severity.py, backend/app/agents/validators/__init__.py, backend/app/agents/validators/html_static.py, backend/app/agents/validators/html_render.py, backend/tests/agents/test_validators.py</files>
  <behavior>
    - Test (test_validators.py): `map_severity(P0)==CRITICAL`, `P1==HIGH`, `P2==MEDIUM`, `P3==LOW` — a single tested function.
    - Test: `resolve("validator","html_static")` and `resolve("validator","html_render")` return registered validators after discover().
    - Test: html_static.validate(DeliverableContext) reaches static_check via the handle (target.runner.static_check) and returns Issues with mapped severity; html_render reaches render_check (degrades to available=False offline).
    - Test: a validator run writes a validation_results row carrying owner_id+workspace_id.
  </behavior>
  <read_first>
    - backend/agents/capabilities/base.py — the `Validator` Protocol (base.py:54: `async def validate(self, target) -> list`)
    - backend/app/agents/static_check.py + backend/app/agents/render_check.py — the heavy-dep checks being wrapped (render_check degrades to available=False offline)
    - backend/agents/execution_engine/kernel_services.py — the handle: `static_check`/`render_check` (kernel_services.py:126-132), `run_validation_fix_loop` (:274); the validation_results writer added in 08-02 (gate_events writer is the sibling pattern)
    - 08-PATTERNS.md § "app/agents/validators/html_static.py..." + 08-RESEARCH.md § "Validator wraps the heavy check, reached via the handle (D-04)" — the import-linter-legal app→port direction; `@register("validator", "html_static")`; `target.runner.static_check(target.path)`
    - 08-CONTEXT.md D-04 — DeliverableContext carries path/content/Workspace/task-meta and may request browser/compile/test/static-analysis runners; Q24 severity P0–P3
  </read_first>
  <action>
    Create the single severity function in `agents/capabilities/validators/severity.py`: `map_severity(internal: str) -> str` mapping P0→CRITICAL, P1→HIGH, P2→MEDIUM, P3→LOW (VALID-03 — the one source of truth; the validation gate in 08-02 consumes THIS). Define `DeliverableContext` (path/content/Workspace handle/task-meta + a `runner` handle reference) — kernel-pure, lives kernel-side, typed so capabilities receive it. Create `app/agents/validators/` (new package) with `html_static.py` (`@register("validator","html_static", user_allowed=True)`, wraps `static_check`) and `html_render.py` (`@register("validator","html_render", user_allowed=True)`, wraps `render_check`) — each imports the `Validator` PORT (`agents.capabilities.base.Validator`) + the `register` decorator + the heavy check, self-registers, and reaches the heavy dep ONLY via `target.runner.static_check`/`render_check` (the KernelServices handle — NO kernel→app import; the import-linter forbids only the reverse). `.validate` returns Issues with severity mapped through `map_severity`. Each run/attempt writes a `validation_results` row via the ScopedStore writer (from 08-02), owner/workspace from ExecutionContext. Write test_validators.py covering the four behaviors. Ensure `discover()` (08-01) imports `app/agents/validators/` so these self-register.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_validators.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def map_severity" backend/agents/capabilities/validators/severity.py` returns 1 (single mapping function)
    - `grep -rc 'register("validator", *"html_static"\|register("validator", *"html_render"' backend/app/agents/validators/` returns ≥2
    - `grep -rc "from app\.\|import app\." backend/app/agents/validators/html_static.py backend/app/agents/validators/html_render.py | grep -v ':0'` shows the validators do NOT import the kernel (they import the PORT `agents.capabilities.base`); `cd backend && lint_imports` exits 0 (no kernel→app edge)
    - `cd backend && python3.11 -m pytest tests/agents/test_validators.py -x` exits 0
    - Behavior: a validator run writes a validation_results row with owner_id+workspace_id
  </acceptance_criteria>
  <done>html_static/html_render are registered, reach the heavy deps via the handle, map severity through the single function, and persist validation_results rows; import-linter stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generic FixPolicy fix-loop + Tier#4/5/6 validators</name>
  <files>backend/agents/execution_engine/kernel_services.py, backend/agents/capabilities/validators/task_done_when.py, backend/agents/capabilities/validators/spec_plan_coverage.py, backend/app/agents/validators/design_quality.py, backend/tests/agents/test_validators.py</files>
  <behavior>
    - Test: the fix-loop drives off FixPolicy config (deliverable name + max_attempts + fix-prompt template) — running it with deliverable="app.py" operates on app.py (not hardcoded prototype.html).
    - Test: default policy = warn-non-critical / block-critical; residual non-critical issues emit validation_warning.
    - Test: `spec_plan_coverage`, `task_done_when`, `design_quality` each `resolve("validator", name)` and run against a target; each exercised by ≥1 test manifest.
    - Test: `design_quality` only ever emits P2/P3 (warnings-first / non-blocking) — never blocks.
  </behavior>
  <read_first>
    - backend/agents/execution_engine/kernel_services.py — `run_validation_fix_loop` (:274); the HTML-hardcoded internals being made generic; the `filename` parameter Phase-7 threaded (07-11) so the loop is already deliverable-parameterized at the boundary
    - 08-RESEARCH.md § "Don't Hand-Roll" → Fix-loop — the engine's single `_run_validation_fix_loop` made generic via FixPolicy; task_loop already delegates unconditionally (task_loop.py:292) — do NOT build a new loop
    - 08-CONTEXT.md D-05 — Tier#4/5/6 placement: design_quality needs heavy/render-style checks → app side; task_done_when/spec_plan_coverage likely pure-stdlib → agents/capabilities/validators/; design_quality warnings-first → P2/P3 only
    - backend/app/agents/static_check.py — the stdlib-style check idiom design_quality/the stdlib validators mirror
  </read_first>
  <action>
    Make the fix-loop generic (D-06): refactor the internals of `KernelServices.run_validation_fix_loop` to drive off a `FixPolicy` (deliverable name + `max_attempts` + fix-prompt template + the step's declared registered validators resolved from the registry) — behavior-identical to Phase 7 but config-driven, not hardcoded `prototype.html`. Default policy = warn-non-critical / block-critical; after `max_attempts`, emit `validation_warning` with residual non-critical issues. Keep the loop where `task_loop` invokes it (NOT moved into the gate — Pitfall 4). Land the three Tier validators: `spec_plan_coverage` (Tier#4, pre-build analyze; pure-stdlib → `agents/capabilities/validators/spec_plan_coverage.py`), `task_done_when` (Tier#5, per-task acceptance; pure-stdlib → `agents/capabilities/validators/task_done_when.py`), `design_quality` (Tier#6, tokens/placeholder/a11y; heavier → `app/agents/validators/design_quality.py`, WARNINGS-FIRST — emits only P2/P3, never blocks). Each `@register("validator", <name>, user_allowed=True)`, returns Issues with severity via `map_severity`, writes a `validation_results` row, and is exercised by ≥1 test manifest in test_validators.py.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_validators.py -k "tier or fixloop" -x</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: the fix-loop runs against a deliverable named other than prototype.html (e.g. app.py) — drives off FixPolicy, not a hardcoded filename
    - `cd backend && python3.11 -m pytest tests/agents/test_validators.py -k tier -x` exits 0 (the three Tier validators register, run, ≥1 test manifest each)
    - Behavior: `design_quality` issues are non-blocking (only P2/P3 severities emitted; no critical/block)
    - `grep -rc 'register("validator"' backend/agents/capabilities/validators/ backend/app/agents/validators/` returns ≥5 (html_static/html_render + 3 Tier)
    - `cd backend && python3.11 -m pytest tests/agents/test_validators.py -x` exits 0
  </acceptance_criteria>
  <done>The fix-loop is generic (FixPolicy-driven); the three Tier validators register, run, and persist; design_quality is warnings-first.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point task_loop validation onto registered validators; PROVE INV-3 parity</name>
  <files>backend/agents/capabilities/strategies/task_loop.py, backend/tests/agents/test_validators.py</files>
  <read_first>
    - backend/agents/capabilities/strategies/task_loop.py — the unconditional `runner.run_validation_fix_loop` delegation (task_loop.py:292); the inline validation re-point target
    - 08-PATTERNS.md § "agents/capabilities/strategies/task_loop.py — validation re-point" — the generic FixPolicy loop replaces the internals; task_loop drives registered html_static/html_render; byte/event-identical (5 snapshots gate it)
    - 08-RESEARCH.md § "Pitfall 4: Validation gate vs strategy ownership" + § "Pitfall 2" — keep validation inline in task_loop; the validation gate is additive for non-build steps; do NOT move validation into the gate
    - backend/tests/agents/test_characterization_prototype.py + test_characterization_prototype_revision.py — the parity gates for the re-point
  </read_first>
  <action>
    Re-point `task_loop`'s inline validation so it drives the REGISTERED `html_static`/`html_render` validators + the generic FixPolicy loop (from Tasks 1–2) instead of the Phase-7 hardcoded path — keeping the delegation unconditional and the loop where task_loop invokes it (D-06). Run the 5 characterization snapshots to PROVE the routed validation produces byte/event-identical output to Phase 7. Per the D-06 directive, evaluate whether the Phase-7 `revision_validation` post_step should become a `gates:[validation]` step — DECIDE WITH SNAPSHOT EVIDENCE: re-express it as a `validation` gate ONLY if `test_characterization_prototype_revision.py` proves byte/event-identical; otherwise leave it a post_step (record the decision in the SUMMARY). Do NOT re-baseline any snapshot.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_validators.py tests/agents/test_characterization_*.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (5 pipelines byte/event-identical — re-point parity PROVEN; NO re-baseline)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py -x` exits 0 (prototype build runs html_static/html_render via the registry; validation_results rows written per attempt; deliverable + event parity)
    - Behavior: the revision_validation gate-vs-post_step decision is recorded in the SUMMARY with the snapshot evidence (parity result)
    - `cd backend && lint_imports` exits 0 (task_loop reaches validators via resolve + the handle, no kernel→app import)
  </acceptance_criteria>
  <done>task_loop validation routes through the registered validators + generic loop at strict INV-3 parity (5 snapshots green, no re-baseline); the revision post_step decision is recorded with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel ↔ app heavy-dep validators | Validators live app-side; the kernel must not import app |
| validator run → validation_results table | Each run persists owner/workspace-scoped rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-arch | Tampering (architecture) | kernel reaching into app for heavy deps | mitigate | validators import the PORT + reach static_check/render_check via the KernelServices handle only; lint_imports forbids agents.capabilities→app (Task 1 acceptance) |
| T-08-04-ID | Information Disclosure | validation_results cross-owner read | mitigate | validation_results carries owner_id+workspace_id; writes via ScopedStore default-deny (Task 1/2) |
| T-08-04-parity | Tampering (regression) | task_loop validation re-point | mitigate | parity proven against the 5 characterization snapshots; no re-baseline (Task 3) |
| T-08-04-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_validators.py -x` exits 0
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 (5 pipelines parity; validation re-point byte/event-identical) in a clean session
- `cd backend && lint_imports` exits 0 (validators app-side, reached via handle); banned-pattern + migration-ledger green
</verification>

<success_criteria>
- Validator registry runs declared validators; html_static/html_render registered + reached via the handle; each attempt writes a validation_results row
- Generic FixPolicy fix-loop replaces the HTML-hardcoded loop; single P0–P3→severity mapping function
- spec_plan_coverage/task_done_when/design_quality registered + run; design_quality warnings-first/non-blocking; each ≥1 test manifest
- task_loop validation routes through registered validators at strict INV-3 parity (5 snapshots unchanged)
</success_criteria>

## Artifacts this phase produces (08-04)
- Function: `map_severity(internal) -> str` (severity.py) — the single P0–P3→CRITICAL/HIGH/MEDIUM/LOW mapping
- Class: `DeliverableContext` (kernel-side; path/content/Workspace/task-meta/runner handle)
- Validator capabilities: `html_static`, `html_render`, `design_quality` (app-side), `task_done_when`, `spec_plan_coverage` (kernel-side) — each `@register("validator", ...)`
- New packages: `agents/capabilities/validators/`, `app/agents/validators/`
- Generic FixPolicy fix-loop (kernel_services.run_validation_fix_loop internals, config-driven)
- validation_results rows written per validator run/attempt
- New file: `backend/tests/agents/test_validators.py`

<output>
Create `.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-04-SUMMARY.md` when done
</output>
