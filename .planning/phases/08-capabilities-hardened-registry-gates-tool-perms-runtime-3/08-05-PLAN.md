---
phase: 08-capabilities-hardened-registry-gates-tool-perms-runtime-3
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-03"]
files_modified:
  - backend/agents/capabilities/prompt/__init__.py
  - backend/agents/capabilities/prompt/policy.py
  - backend/agents/capabilities/skills/__init__.py
  - backend/agents/capabilities/skills/providers.py
  - backend/agents/capabilities/hooks/__init__.py
  - backend/agents/capabilities/hooks/behavioral.py
  - backend/agents/capabilities/runtimes/__init__.py
  - backend/agents/capabilities/runtimes/langchain_deepagents.py
  - backend/agents/factory.py
  - backend/app/agents/deep_agent_runner.py
  - backend/tests/agents/test_guardrails.py
  - backend/tests/agents/test_create_runner.py
  - backend/tests/agents/test_banned_patterns.py
autonomous: true
requirements: [AGENTRT-01, AGENTRT-02, AGENTRT-03, AGENTRT-05, SKILL-01]
user_setup: []

must_haves:
  truths:
    - "create_deep_agent is imported/called ONLY inside the langchain_deepagents adapter's allow-listed module (F5 CHECK gate); banned-pattern + import-linter green"
    - "A PromptAssemblyPolicy (default order injects→guardrails→skills→hooks→constitution→prompt_body) drives prompt assembly; the inline blocks.append order is deleted (F1 grep → 0)"
    - "Composed prompts are byte-identical to today for existing agents"
    - "skill_provider (ui/disk/template/repo) with a provider interface + versioning replaces the flattened skill list; hook_provider replaces inline hook injection (F3 grep → 0)"
    - "The legacy prompt-only hook survives as a kind: behavioral non-executable sub-type — the ## Active Behavioral Hooks block still renders"
    - "create_runner selects the runtime via the AgentRuntimeAdapter; future claude_code_cli/custom_runner slot in without kernel edits"
  artifacts:
    - path: "backend/agents/capabilities/prompt/policy.py"
      provides: "PromptAssemblyPolicy impl (default block order) registered"
      contains: "register(\"prompt"
    - path: "backend/agents/capabilities/runtimes/langchain_deepagents.py"
      provides: "AgentRuntimeAdapter wrapping DeepAgentRunner (INV-13)"
      contains: "register(\"runtime\", \"langchain_deepagents\")"
    - path: "backend/agents/capabilities/skills/providers.py"
      provides: "skill_provider capabilities (ui/disk/template/repo) with versioning"
    - path: "backend/agents/capabilities/hooks/behavioral.py"
      provides: "behavioral hook_provider (non-executable; renders ## Active Behavioral Hooks)"
  key_links:
    - from: "backend/agents/factory.py"
      to: "PromptAssemblyPolicy.assemble"
      via: "registry-resolved policy drives block order"
      pattern: "assemble\\("
    - from: "backend/app/agents/deep_agent_runner.py"
      to: "create_deep_agent (allow-listed, only call site)"
      via: "AgentRuntimeAdapter wraps it"
      pattern: "create_deep_agent"
---

<objective>
Lift three factory leaks behind declared abstractions and delete the inline originals (D-08, INV-12): F1 → `PromptAssemblyPolicy` (the hardcoded `blocks.append` order becomes a declared, registry-resolved policy with the default order `injects→guardrails→skills→hooks→constitution→prompt_body`); F3 → `skill_provider`/`hook_provider` (inline skills/hooks injection becomes provider capabilities with a provider interface + versioning; the legacy prompt-only hook survives as a `kind: behavioral` non-executable sub-type); F5 → `AgentRuntimeAdapter` (`create_deep_agent` reached only inside the `langchain_deepagents` adapter; `create_runner` selects the runtime via the adapter). Each deletion is gated on byte-identical parity (composed prompts unchanged; the 5 characterization snapshots unchanged).

Purpose: AGENTRT-01/02/03/05 + SKILL-01 — the factory's prompt-order/skills-hooks/runtime become declared capabilities, deleting F1/F3/F5. (F4 constitution is 08-06; F2 tools was 08-03.)
Output: the PromptAssemblyPolicy + skill/hook providers + AgentRuntimeAdapter capabilities, the rewired factory, F1/F3 deleted from factory.py, F5 reached only via the adapter, and the parity/banned-pattern tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-SPEC.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-CONTEXT.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-RESEARCH.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-PATTERNS.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-01-SUMMARY.md
@.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentRuntimeAdapter (F5) — create_deep_agent reached only via the adapter</name>
  <files>backend/agents/capabilities/runtimes/__init__.py, backend/agents/capabilities/runtimes/langchain_deepagents.py, backend/app/agents/deep_agent_runner.py, backend/agents/factory.py, backend/tests/agents/test_banned_patterns.py</files>
  <behavior>
    - Test (test_banned_patterns.py): `create_deep_agent` is imported/called ONLY inside the allow-listed module; the banned-pattern allow-list stays `{"app/agents/deep_agent_runner.py"}`.
    - Test: `resolve("runtime","langchain_deepagents")` returns the AgentRuntimeAdapter after discover().
    - Test: create_runner builds the runner via the adapter and produces an identical DeepAgentRunner for existing agents (parity).
  </behavior>
  <read_first>
    - backend/app/agents/deep_agent_runner.py — the F5 hardcoded `create_deep_agent` (deep_agent_runner.py:240); INV-13 — the canonical `from deepagents import create_deep_agent`; the adapter WRAPS, never replaces
    - backend/tests/agents/test_banned_patterns.py — `_ALLOWED_CREATE_DEEP_AGENT = {"app/agents/deep_agent_runner.py"}` (test_banned_patterns.py:70); the F5 CHECK gate; INV-13 ratchet
    - 08-RESEARCH.md § "Open Questions" Q2 + § "Anti-Patterns" — KEEP the create_deep_agent CALL inside deep_agent_runner.py so the allow-list stays stable; the runtime capability selects/wraps DeepAgentRunner via the handle, never calling create_deep_agent itself nor importing app from the kernel
    - 08-PATTERNS.md § "F5 → AgentRuntimeAdapter" — wrap never replace; CHECK gate
  </read_first>
  <action>
    Create `agents/capabilities/runtimes/langchain_deepagents.py`: an `AgentRuntimeAdapter` impl `@register("runtime","langchain_deepagents", user_allowed=False)` satisfying the port (`create(agent_id, ctx) -> runner`). The adapter SELECTS/WRAPS `DeepAgentRunner` — it must NOT itself call `create_deep_agent` and must NOT import `app.*` from the kernel side; it reaches `DeepAgentRunner` construction via the KernelServices handle / factory seam so the `create_deep_agent` CALL stays inside the allow-listed `deep_agent_runner.py` (keeping `_ALLOWED_CREATE_DEEP_AGENT` stable per RESEARCH Q2). Rewire `factory.create_runner` to select the runtime via `resolve("runtime", <id>)` (defaulting to `langchain_deepagents`) instead of hardcoding the DeepAgentRunner build — future `claude_code_cli`/`custom_runner` slot in via the same port with no kernel edit. Run the F5 CHECK gate (banned-pattern) + import-linter + the 5 snapshots to prove parity; the adapter routing must produce an identical runner for existing agents. The F5 ledger row flips ☑ when the CHECK gate confirms `create_deep_agent` is called only in the allow-listed module.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py tests/agents/test_create_runner.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "create_deep_agent" backend/agents/ | grep -v ':0'` returns empty (no kernel-side call); `grep -c "create_deep_agent" backend/app/agents/deep_agent_runner.py` returns ≥1 (the only call site)
    - `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -x` exits 0 (F5 CHECK gate: create_deep_agent only in the allow-listed module; allow-list unchanged)
    - `cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry, discover; discover(); print(CapabilityRegistry().resolve('runtime','langchain_deepagents'))"` resolves the adapter
    - `cd backend && python3.11 -m pytest tests/agents/test_create_runner.py tests/agents/test_characterization_*.py -x` exits 0 in a clean session (identical runner; parity)
    - `cd backend && lint_imports` exits 0 (the runtime capability does not import app from the kernel)
  </acceptance_criteria>
  <done>create_deep_agent is reached only inside the allow-listed adapter module; create_runner selects the runtime via the registry; banned-pattern + import-linter + snapshots green; F5 row ☑.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PromptAssemblyPolicy (F1) + skill_provider/hook_provider (F3) capabilities</name>
  <files>backend/agents/capabilities/prompt/__init__.py, backend/agents/capabilities/prompt/policy.py, backend/agents/capabilities/skills/__init__.py, backend/agents/capabilities/skills/providers.py, backend/agents/capabilities/hooks/__init__.py, backend/agents/capabilities/hooks/behavioral.py, backend/tests/agents/test_guardrails.py</files>
  <behavior>
    - Test: PromptAssemblyPolicy with the default order assembles a block mapping into the SAME composed string the inline blocks.append path produced (byte-identical) for each existing agent class.
    - Test: skill_provider resolves skills with version metadata (not a flattened content list); a ui/disk/template/repo provider returns versioned blocks.
    - Test: the behavioral hook_provider renders the `## Active Behavioral Hooks` block identically for attached behavioral hooks (non-executable sub-type).
  </behavior>
  <read_first>
    - backend/agents/factory.py — `_compose_system_prompt` (factory.py:174-255): the `blocks.append` order (injects→guardrails→skills→hooks→constitution→prompt_body, joined with "\n\n"); the inline skills (factory.py:220-225) + hooks (factory.py:226-245, the `## Active Behavioral Hooks` block); the constitution block site (08-06 owns the F4 fix — leave _inject_constitution call intact this plan)
    - backend/CLAUDE.md § "Skills vs Guardrails vs Hooks" — the fixed injection order + the three sources
    - 08-PATTERNS.md § "F1/F3/F4 → PromptAssemblyPolicy + skill/hook providers" — the default order MUST be injects→guardrails→skills→hooks→constitution→prompt_body; "\n\n" join; byte-identical; behavioral hook survives
    - 08-RESEARCH.md § "Pitfall 2: Snapshot re-baseline temptation on the prompt-assembly lift" — reproduce the join byte-for-byte; NEVER re-baseline
    - backend/agents/capabilities/base.py — the PromptAssemblyPolicy/SkillProvider/HookProvider ports (added in 08-01)
  </read_first>
  <action>
    Create `agents/capabilities/prompt/policy.py`: a `PromptAssemblyPolicy` impl `@register("prompt","default", user_allowed=True)` (or as a config object on the compiled workflow per Claude's discretion D-08 — choose the registered-capability form for consistency with the other lifts) whose default `order = ["injects","guardrails","skills","hooks","constitution","prompt_body"]` and whose `assemble(blocks, ctx)` joins the present blocks with `"\n\n"` EXACTLY as the inline path did (byte-identical). Create `agents/capabilities/skills/providers.py`: `skill_provider` capabilities (`ui`/`disk`/`template`/`repo`) `@register("skill", <kind>, ...)` with a provider interface + version metadata replacing the flattened skill content list (SKILL-01). Create `agents/capabilities/hooks/behavioral.py`: a `hook_provider` `@register("hook","behavioral", user_allowed=True)` non-executable sub-type that renders the `## Active Behavioral Hooks` block identically (the legacy prompt-only hook survives — F3). Write tests (extend test_guardrails.py) proving byte-identical composition + versioned skills + the behavioral block render. Do NOT yet rewire factory to use these (Task 3 does the rewire+delete).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_guardrails.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'register("prompt"' backend/agents/capabilities/prompt/policy.py` returns ≥1
    - `grep -rc 'register("skill"' backend/agents/capabilities/skills/` returns ≥1 (ui/disk/template/repo providers, with versioning)
    - `grep -rc 'register("hook", *"behavioral"' backend/agents/capabilities/hooks/` returns ≥1 (behavioral non-executable sub-type)
    - Behavior: PromptAssemblyPolicy.assemble produces the byte-identical composed string vs the inline path for each existing agent class
    - `cd backend && python3.11 -m pytest tests/agents/test_guardrails.py -x` exits 0 (skill version metadata + behavioral block render)
  </acceptance_criteria>
  <done>PromptAssemblyPolicy reproduces the block order byte-for-byte; skill_provider returns versioned skills; the behavioral hook_provider renders the legacy block; none of it is wired into factory yet.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire factory onto the policy/providers; PROVE byte parity; DELETE F1 + F3</name>
  <files>backend/agents/factory.py, backend/tests/agents/test_create_runner.py, backend/tests/agents/test_guardrails.py</files>
  <read_first>
    - backend/agents/factory.py — `_compose_system_prompt` (factory.py:174-255): the `blocks.append` lines (F1) + the inline skills/hooks injection (factory.py:220-245, F3); the call site in create_runner
    - specs/003-workflow-engine-decoupling/migration-ledger.md — the F1 row (grep `blocks\.append` → 0) + F3 row (grep `_inject_skills|_inject_hooks` → 0); phase 3; ☐ → ☑
    - backend/tests/agents/test_migration_ledger.py — the F1/F3 ratchet
    - 08-CONTEXT.md `<deep_work_rules>` — deletion sequencing: build+register [Task 2] → rewire call site → run the 5 snapshots to PROVE parity → delete inline original + flip ledger; (c) before (d)
    - 08-RESEARCH.md § "Pitfall 2/3" — keep _inject_constitution intact (08-06 fixes F4); only F1 (block order) + F3 (skills/hooks) delete here
  </read_first>
  <action>
    Rewire `_compose_system_prompt` so block ORDER comes from the `PromptAssemblyPolicy` (resolve("prompt", ...).assemble(blocks, ctx)) and the skills/hooks blocks come from the `skill_provider`/`hook_provider` capabilities (Task 2) instead of the inline `blocks.append` + inline skill/hook loops. Keep the `_inject_constitution` CALL exactly as-is (F4 is 08-06's deletion — do NOT touch the constitution branch here beyond consuming its output as the "constitution" block in the policy order). Run the 5 characterization snapshots + test_create_runner.py + test_guardrails.py to PROVE composed prompts are byte-identical for every existing agent. ONLY after parity is green: DELETE the inline `blocks.append` ordering (F1) and the inline `_inject_skills`/`_inject_hooks` injection (F3) from factory.py, and flip the migration-ledger F1 + F3 rows to ☑. The deletions are this plan's exit gate.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_guardrails.py tests/agents/test_create_runner.py tests/agents/test_migration_ledger.py tests/agents/test_characterization_*.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "blocks\.append" backend/agents/factory.py` returns 0 (F1 deleted)
    - `grep -cE "_inject_skills|_inject_hooks" backend/agents/factory.py` returns 0 (F3 deleted)
    - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x` exits 0 in a clean session (composed prompts byte-identical — parity PROVEN before deletion; NO re-baseline)
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -x` exits 0 AND shows the F1 + F3 rows ☑
    - `cd backend && python3.11 -m pytest tests/agents/test_guardrails.py -x` exits 0 (behavioral block still renders; skills versioned)
    - `cd backend && lint_imports` exits 0
  </acceptance_criteria>
  <done>F1 (blocks.append) and F3 (_inject_skills/_inject_hooks) are deleted (greps → 0); the policy + providers drive assembly; composed prompts are byte-identical; F1/F3 ledger rows ☑.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel → deepagents runtime | create_deep_agent must stay behind the allow-listed adapter (INV-13) |
| prompt assembly → composed system prompt | Re-ordering must not change bytes (parity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-runtime | Tampering | hand-rolled deep agent / runtime swap | mitigate | AgentRuntimeAdapter WRAPS create_deep_agent; banned-pattern gate keeps the call in the allow-listed module; INV-13 (Task 1) |
| T-08-05-arch | Tampering (architecture) | runtime capability importing app | mitigate | the runtime capability reaches DeepAgentRunner via the handle, not a kernel→app import; lint_imports green (Task 1) |
| T-08-05-parity | Tampering (regression) | F1/F3 prompt-assembly lift | mitigate | composed-prompt byte parity proven against the 5 snapshots BEFORE deletion (Task 3) |
| T-08-05-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py tests/agents/test_guardrails.py tests/agents/test_create_runner.py -x` exits 0
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py tests/agents/test_migration_ledger.py -x` exits 0 (parity + F1/F3/F5 ☑) in a clean session
- `cd backend && lint_imports` exits 0
</verification>

<success_criteria>
- create_deep_agent only inside the langchain_deepagents adapter's allow-listed module (F5 CHECK); banned-pattern + import-linter green; F5 ☑
- PromptAssemblyPolicy drives block order; blocks.append grep → 0; composed prompts byte-identical; F1 ☑
- skill_provider (versioned) + hook_provider replace inline injection; _inject_skills|_inject_hooks grep → 0; behavioral block renders; F3 ☑
- create_runner selects the runtime via the adapter; future runtimes slot in with no kernel edit
</success_criteria>

## Artifacts this phase produces (08-05)
- Capabilities: `PromptAssemblyPolicy` default impl (`@register("prompt","default")`), skill_provider impls (ui/disk/template/repo, `@register("skill",...)`), behavioral hook_provider (`@register("hook","behavioral")`), `AgentRuntimeAdapter` (`@register("runtime","langchain_deepagents")`)
- New packages: `agents/capabilities/{prompt,skills,hooks,runtimes}/`
- Default prompt block order constant `["injects","guardrails","skills","hooks","constitution","prompt_body"]`
- Deleted: factory `blocks.append` order (F1), inline `_inject_skills`/`_inject_hooks` (F3); F1/F3/F5 ledger rows ☑
- create_runner runtime selection via `resolve("runtime", ...)`

<output>
Create `.planning/phases/08-capabilities-hardened-registry-gates-tool-perms-runtime-3/08-05-SUMMARY.md` when done
</output>
