---
phase: 38-analytics-estimates-notifications-b4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/analytics/charts/DonutChart.tsx
  - frontend/src/components/analytics/charts/BarChart.tsx
  - frontend/src/components/analytics/charts/DonutChart.test.tsx
  - frontend/src/components/analytics/charts/BarChart.test.tsx
autonomous: true
requirements: [SC-1, SC-2, SHELL-05]
must_haves:
  truths:
    - "SC-1: the reusable DonutChart + BarChart are the self-contained token-styled SVG primitives the reskinned AnalyticsPage (38-04) renders instead of its inline copies."
    - "a11y: every chart SVG carries role=\"img\" + an aria-label text alternative summarising the data (screen-reader readable)."
    - "token gate: charts consume Phase-32 @theme tokens (var(--brand) one-chroma base; --status-* ONLY when a datum is a run-status); per-file retired-palette grep = 0."
    - "SC-001/INV-1: charts are generic presentation primitives — they take data props, they do NOT branch on any workflow name."
    - "no external chart lib: pure inline SVG (CSP-safe); package.json stays chart-library-free."
  artifacts:
    - path: "frontend/src/components/analytics/charts/DonutChart.tsx"
      provides: "token-styled SVG donut (strokeDasharray sweep) with role=img + aria-label"
      min_lines: 25
    - path: "frontend/src/components/analytics/charts/BarChart.tsx"
      provides: "token-styled SVG/flex bar chart with role=img + aria-label"
      min_lines: 25
    - path: "frontend/src/components/analytics/charts/DonutChart.test.tsx"
      provides: "render + a11y aria-label assertion"
    - path: "frontend/src/components/analytics/charts/BarChart.test.tsx"
      provides: "render + a11y aria-label assertion"
  key_links:
    - from: "frontend/src/components/analytics/charts/DonutChart.tsx"
      to: "globals.css @theme tokens"
      via: "brand/status CSS vars"
      pattern: "var\\(--brand|--status-|color-brand"
    - from: "frontend/src/components/analytics/charts/DonutChart.tsx"
      to: "assistive tech"
      via: "role + label"
      pattern: "role=\"img\""
---

<objective>
Extract the two chart primitives currently inlined in `AnalyticsPage.tsx` — the flexbox/SVG `BarChart` (:96-130) and the `<circle>` donut (:432-448) — into reusable, parametrized, token-styled, a11y-labelled components under `frontend/src/components/analytics/charts/`. They hardcode `#1B2A4A` today; the extracted versions consume Phase-32 `@theme` tokens (`var(--brand)` one-chroma base; `--status-*` ONLY where a datum IS a run-status) and carry `role="img"` + an `aria-label` data summary.

Purpose: pure, dependency-free presentation primitives (only Phase-32 tokens) — buildable in parallel with the backend (38-01). 38-04 imports them and deletes the inline copies (INV-3 no-dual-impl).
Output: `DonutChart.tsx` + `BarChart.tsx` + their `.test.tsx` a11y/render tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-PATTERNS.md
@frontend/src/styles/globals.css
</context>

<artifacts_produced>
NEW symbols (38-04 consumes these):
- `frontend/src/components/analytics/charts/DonutChart.tsx` — `export function DonutChart(props)`: parametrized SVG donut. Props include the fraction/segments to render, a base `chroma` (defaults to `var(--brand)`), an optional status-datum flag, and an `ariaLabel` string. Renders `role="img"` + `aria-label`.
- `frontend/src/components/analytics/charts/BarChart.tsx` — `export function BarChart(props)`: parametrized bar chart. Props: `data` (values + labels), base `chroma` (defaults `var(--brand)`), `ariaLabel`. Renders `role="img"` + `aria-label`.
- Their `.test.tsx` files.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 failing render + a11y tests for DonutChart and BarChart</name>
  <files>frontend/src/components/analytics/charts/DonutChart.test.tsx, frontend/src/components/analytics/charts/BarChart.test.tsx</files>
  <read_first>
    - frontend/src/components/analytics/AnalyticsPage.tsx:96-130 (inline BarChart to model props on) + :431-448 (inline donut SVG + the existing role/aria idiom in RESEARCH Pattern 2 L190-191).
    - An existing `*.test.tsx` under frontend/src/components/ui/ or frontend/src/components/analytics/ for the vitest + @testing-library render idiom (render, `getByRole`, `toHaveAttribute`).
    - .planning/phases/38-analytics-estimates-notifications-b4/38-VALIDATION.md row 52 (the `npx vitest run src/components/analytics/charts` command).
  </read_first>
  <behavior>
    - DonutChart.test: renders → `getByRole("img")` present; its `aria-label` contains the numeric summary passed in (e.g. a success-rate string); does NOT throw with a 0% or 100% fraction.
    - BarChart.test: renders with a small data array → `getByRole("img")` present; `aria-label` reflects the data summary; renders one bar element per datum.
    - Both tests fail now (components do not exist) — RED.
  </behavior>
  <action>
    Create both `.test.tsx` files asserting `render(<DonutChart .../>)` / `render(<BarChart .../>)` expose a `role="img"` element whose `aria-label` includes the passed-in numeric summary, and that BarChart emits one bar per datum. Import from `./DonutChart` / `./BarChart` (not-yet-created → RED).
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/analytics/charts` FAILS with unresolved import of `./DonutChart` / `./BarChart` (RED observed).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analytics/charts 2>&1 | tail -8</automated>
  </verify>
  <done>Both a11y/render tests exist and fail because the components are not yet created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract + tokenize DonutChart and BarChart (a11y-labelled, one-chroma)</name>
  <files>frontend/src/components/analytics/charts/DonutChart.tsx, frontend/src/components/analytics/charts/BarChart.tsx</files>
  <read_first>
    - frontend/src/components/analytics/AnalyticsPage.tsx:96-130 (BarChart source), :431-448 (donut source — `<circle>` + `motion.circle` + `strokeDasharray`/`strokeDashoffset` + `transform="rotate(-90 44 44)"`).
    - frontend/src/styles/globals.css (`@theme inline` + `:root`) — the exact vars: `--brand` (#3C2CDA), `--brand-fill`, ink ramp `--ink-*`, surfaces, `--status-running/-done/-failed/-amber/-queued` (+ `-fill`). 38-PATTERNS.md Mapping 6 lists them with line anchors.
    - frontend/src/components/ui/Badge.tsx (token-only primitive style — no raw hex — to match the neighbourhood convention).
  </read_first>
  <behavior>
    - Makes both Task-1 tests GREEN.
    - DonutChart: base sweep stroke = `var(--brand)`; track = a neutral token (e.g. `var(--status-queued-fill)`); when the caller flags a segment as a run-status datum (success/fail), that segment uses `--status-done`/`--status-failed`. Always renders `role="img"` + `aria-label`.
    - BarChart: bars use `var(--brand)` (single chroma) unless a datum is flagged status; `role="img"` + `aria-label`.
    - Zero retired hex; motion/react animation preserved (heights / strokeDashoffset), matching the extracted originals.
  </behavior>
  <action>
    Create `DonutChart.tsx` and `BarChart.tsx` as `export function` components taking the props in `<artifacts_produced>`. Port the exact SVG geometry from AnalyticsPage (donut `cx=44 cy=44 r=36`, `strokeDasharray = 2*Math.PI*36`, `motion.circle` sweep, `rotate(-90 44 44)`; BarChart flex bars with `motion.div` height animation). Replace every hardcoded `#1B2A4A`/`#F3F4F6`/`#8AAEC8`/navy-ramp value with the Phase-32 token var per the Mapping-6 list (`var(--brand)` base; `--status-*` ONLY for status data). Add `role="img"` + an `aria-label` composed from the numeric props. Keep them generic (data-prop driven) — NO workflow-name branch.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/analytics/charts` PASSES (render + a11y) — GREEN observed.
    - Retired-palette per file = 0: `grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" frontend/src/components/analytics/charts/DonutChart.tsx frontend/src/components/analytics/charts/BarChart.tsx` → 0 for each.
    - Positive token usage present: `grep -Ec "var\(--|--brand|--status-|color-brand" frontend/src/components/analytics/charts/DonutChart.tsx` → > 0 (and same for BarChart).
    - Each renders `role="img"`: `grep -c 'role="img"' frontend/src/components/analytics/charts/DonutChart.tsx` → ≥ 1 (and BarChart).
    - No chart library import: `grep -REc "recharts|chart.js|d3|visx|victory|nivo" frontend/src/components/analytics/charts/` → 0.
    - `cd frontend && npx tsc --noEmit` → no NEW errors vs baseline (identity).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analytics/charts 2>&1 | tail -8 && grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" src/components/analytics/charts/</automated>
  </verify>
  <done>Both charts render, pass a11y tests, carry role="img"+aria-label, retired-palette grep = 0, no chart lib, tsc identity holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data props → SVG render | Numeric aggregate values from the analytics endpoint are rendered; no untrusted HTML/exec surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Tampering / design | chart primitives | mitigate | pure data-prop-driven SVG; no workflow-name branch (SC-001); no `dangerouslySetInnerHTML`. |
| T-38-CSP | Elevation (script injection) | chart rendering | mitigate | hand-rolled inline SVG, zero external chart lib → CSP-safe; grep asserts no recharts/d3/visx import. |
| T-38-A11Y | Accessibility gap | chart SVG | mitigate | every chart carries `role="img"` + `aria-label` text alternative (a11y gate). |
| T-38-SC | Tampering | package installs | accept | zero installs (locked decision: no chart lib). |
</threat_model>

<verification>
OFFLINE, BY DELTA:
- `cd frontend && npx vitest run src/components/analytics/charts` → green (render + a11y).
- Per-file retired-palette grep = 0 (DonutChart.tsx, BarChart.tsx).
- No chart-lib import grep = 0.
- `cd frontend && npx tsc --noEmit` → identity (no new errors vs baseline).
- Mocked Playwright / visual verification → LIVE-DEFERRED to Phase 34.
</verification>

<success_criteria>
- Two reusable, parametrized, token-styled, a11y-labelled SVG chart components exist under `analytics/charts/`.
- Zero retired palette, zero chart library, `role="img"`+`aria-label` present, tests green, tsc identity holds.
</success_criteria>

<output>
Create `.planning/phases/38-analytics-estimates-notifications-b4/38-02-SUMMARY.md` when done.
</output>
