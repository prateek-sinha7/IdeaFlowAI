---
phase: 38-analytics-estimates-notifications-b4
plan: 05
type: execute
wave: 3
depends_on: [38-01, 38-04]
files_modified:
  - frontend/src/components/catalog/HomeLaunchGrid.tsx
  - frontend/src/components/catalog/HomeLaunchGrid.test.tsx
autonomous: true
requirements: [SC-2, SHELL-05]
must_haves:
  truths:
    - "SC-2: each Home deliverable card shows a REAL '~N agents · ~Xm' estimate derived from step_count/agent_count (already on the WorkflowSummary wire) + the per-type history average duration from /api/analytics/summary — NOT a hardcoded literal."
    - "SC-001/INV-1: the estimate keys on the generic row (step_count/agent_count + type_avg_duration_sec by the row's generic id) — NEVER a workflow-name branch; WORKFLOW_LABELS stays display-only."
    - "owner-scoped: the history average comes from the owner-scoped /api/analytics/summary (WHERE user_id == current_user.id); no cross-owner duration leaks."
    - "additive: no new field on WorkflowSummary and no new table — the avg-duration is the additive type_avg_duration_sec already on /api/analytics/summary (38-01)."
    - "token gate: the estimate line uses Phase-32 tokens (text-ink-*); HomeLaunchGrid per-file retired-palette grep = 0."
  artifacts:
    - path: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      provides: "real per-deliverable estimate line on each card"
      contains: "agents"
    - path: "frontend/src/components/catalog/HomeLaunchGrid.test.tsx"
      provides: "estimate-line render test (agents always; time only when history exists; not hardcoded)"
  key_links:
    - from: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      to: "/api/analytics/summary type_avg_duration_sec"
      via: "getAnalyticsSummary(token, 'all')"
      pattern: "getAnalyticsSummary|type_avg_duration_sec"
    - from: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      to: "WorkflowSummary.step_count"
      via: "row.step_count / row.agent_count"
      pattern: "step_count|agent_count"
---

<objective>
Add a REAL per-deliverable estimate line — `~{agents} agents · ~{minutes}m` — to each Home deliverable card in `HomeLaunchGrid.tsx`. The agent count is `row.step_count`/`row.agent_count`, ALREADY on the `WorkflowSummary` wire (`api.ts:681` / `workflows.py:78`). The time is `round(type_avg_duration_sec[row.id] / 60)` from the owner-scoped `/api/analytics/summary` (38-01, via `getAnalyticsSummary` added in 38-04). NOT the mock's hardcoded `~24m / 5 agents`.

Purpose: SC-2 — "Home deliverable cards show real time/agent estimates." Depends on 38-01 (the history-average field) + 38-04 (the `getAnalyticsSummary` client). Discretion A2 resolved: use the additive `type_avg_duration_sec` on `/api/analytics/summary` — NO new `WorkflowSummary` field, NO new table.
Output: estimate line on HomeLaunchGrid cards + `HomeLaunchGrid.test.tsx`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-PATTERNS.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-01-SUMMARY.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-04-SUMMARY.md
@frontend/src/components/catalog/HomeLaunchGrid.tsx
</context>

<artifacts_produced>
Changed symbols:
- `frontend/src/components/catalog/HomeLaunchGrid.tsx` — each card gains an estimate line reading `row.step_count`/`row.agent_count` + `type_avg_duration_sec[row.id]` (fetched once via `getAnalyticsSummary(token, "all")`).
- `frontend/src/components/catalog/HomeLaunchGrid.test.tsx` — NEW (or extended) test.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 failing estimate-line test for HomeLaunchGrid</name>
  <files>frontend/src/components/catalog/HomeLaunchGrid.test.tsx</files>
  <read_first>
    - frontend/src/components/catalog/HomeLaunchGrid.tsx:53-93 (fetch-shell), :172-231 (card render — `label:172`, `subtitle:173`), and the row source `getWorkflowDefinitions` (returns `WorkflowSummary[]` with `step_count`/`agent_count`).
    - frontend/src/lib/api.ts (`getAnalyticsSummary` + `AnalyticsSummary.type_avg_duration_sec` from 38-04; `WorkflowSummary.step_count:681`).
    - 38-VALIDATION.md row 54 + 38-RESEARCH.md Pattern 3 / Pitfall 5 (agents always; time clause only when a history avg exists; NOT hardcoded).
  </read_first>
  <behavior>
    - Card for a workflow WITH a history entry → renders "~N agents" (from step_count/agent_count) AND "~Xm" (from `round(type_avg_duration_sec[id]/60)`).
    - Card for a workflow WITHOUT a history entry → renders "~N agents" but OMITS the "~Xm" time clause (no fabricated/hardcoded time).
    - No hardcoded `24m` / `5 agents` literal appears.
    - Fails now (no estimate line) — RED.
  </behavior>
  <action>
    Create/extend `HomeLaunchGrid.test.tsx` mocking `@/lib/api` (`getWorkflowDefinitions` → rows with known `step_count`; `getAnalyticsSummary` → a `type_avg_duration_sec` map covering ONE of the rows). Assert the two render cases in `<behavior>` and that no hardcoded estimate literal is present. RED against the current card (no estimate line).
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/catalog/HomeLaunchGrid` FAILS (RED — no estimate line yet).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/catalog/HomeLaunchGrid 2>&1 | tail -8</automated>
  </verify>
  <done>Estimate-line test exists and fails because the card has no estimate yet.</done>
</task>

<task type="auto">
  <name>Task 2: Render the real estimate line on each HomeLaunchGrid card</name>
  <files>frontend/src/components/catalog/HomeLaunchGrid.tsx</files>
  <read_first>
    - frontend/src/components/catalog/HomeLaunchGrid.tsx:68-93 (fetch-shell to extend with a second fetch), :172-231 (card body — insert the estimate line near `subtitle:200`), :77-80 (`getWorkflowDefinitions(jwt).then(rows => setWorkflows(rows.filter(user_launchable)))`).
    - frontend/src/lib/api.ts (`getAnalyticsSummary`, `AnalyticsSummary.type_avg_duration_sec`, `WorkflowSummary.step_count`/`agent_count`).
    - 38-PATTERNS.md Mapping 5 (the estimate derivation) + the Phase-32 `text-ink-*` token for the line.
  </read_first>
  <behavior>
    - Makes the Task-1 test GREEN.
    - Fetches `getAnalyticsSummary(token, "all")` once (alongside the existing definitions fetch, same `cancelled`-guarded effect); builds a `type_avg_duration_sec` lookup.
    - Per card: `agents = row.step_count ?? row.agent_count`; renders "~{agents} agents" ALWAYS. If `type_avg_duration_sec[row.id]` exists → also render "~{round(sec/60)}m"; else omit the time clause. Keyed on the generic `row.id` — NO workflow-name branch.
    - Estimate line styled with `text-ink-*` (no raw hex).
  </behavior>
  <action>
    Extend the mount effect to also call `getAnalyticsSummary(getToken()-or-jwt, "all")` (guarded by `cancelled`, tolerant `.catch` → empty map so the estimate degrades gracefully if analytics is unavailable). Insert an estimate line into the card body near `subtitle` (:200): agents from `row.step_count ?? row.agent_count` ("~N agents" always); minutes from `round(type_avg_duration_sec[row.id] / 60)` ("~Xm") ONLY when a history entry exists, else omit the time clause. Use a `text-ink-*` token for styling. Do NOT branch on any workflow name (WORKFLOW_LABELS/display_name stay display-only — SC-001). No new hardcoded estimate literal.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/catalog/HomeLaunchGrid` PASSES (both render cases) — GREEN observed.
    - `grep -Ec "getAnalyticsSummary|type_avg_duration_sec" frontend/src/components/catalog/HomeLaunchGrid.tsx` → ≥ 1; `grep -Ec "step_count|agent_count" frontend/src/components/catalog/HomeLaunchGrid.tsx` → ≥ 1.
    - Not hardcoded: `grep -Ec "~ ?24 ?m|~ ?5 agents|24m|estimated.*=.*['\"]24" frontend/src/components/catalog/HomeLaunchGrid.tsx` → 0.
    - SC-001: `grep -nE "if .*(row\.id|type) *=== *['\"](prototype|od_ppt|ppt|user_stories|app_builder)" frontend/src/components/catalog/HomeLaunchGrid.tsx` → 0.
    - Token gate = 0: `grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" frontend/src/components/catalog/HomeLaunchGrid.tsx` → 0.
    - `cd frontend && npx tsc --noEmit` → identity (no new errors vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/catalog/HomeLaunchGrid 2>&1 | tail -8 && grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" src/components/catalog/HomeLaunchGrid.tsx</automated>
  </verify>
  <done>Each card shows a real, generic-keyed "~N agents · ~Xm" estimate (time omitted when no history); not hardcoded; token gate = 0; tsc identity holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HomeLaunchGrid → /api/analytics/summary | JWT-authenticated fetch for the owner's own per-type history average (owner-scoped in 38-01). |
| workflow row → estimate | `step_count`/`agent_count` are non-sensitive manifest metadata already on the wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information disclosure | history-avg source | mitigate | avg-duration comes from the owner-scoped `/api/analytics/summary` (WHERE user_id); no cross-owner duration surfaces. |
| T-38-04 | Tampering / design | estimate derivation | mitigate | keyed on generic `row.id` + `type_avg_duration_sec`; grep asserts no workflow-name branch (SC-001/INV-1). |
| T-38-08 | (data integrity) | analytics unavailable | mitigate | tolerant `.catch` → empty map; estimate degrades to "~N agents" (no fabricated time), never crashes the grid. |
| T-38-SC | Tampering | package installs | accept | zero installs. |
</threat_model>

<verification>
OFFLINE, BY DELTA:
- `cd frontend && npx vitest run src/components/catalog/HomeLaunchGrid` → green (agents-always + time-when-history + not-hardcoded).
- HomeLaunchGrid retired-palette grep = 0.
- SC-001 no name-branch; additive (no new WorkflowSummary field, no table).
- `cd frontend && npx tsc --noEmit` → identity.
- Live round-trip / visual verification → LIVE-DEFERRED to Phase 34. Mocked Playwright e2e → LIVE-DEFERRED to Phase 34.
</verification>

<success_criteria>
- Every Home card shows a real, generic-keyed "~N agents · ~Xm" estimate (time omitted when no history); not hardcoded; owner-scoped history source; token gate = 0; tsc identity; tests green.
</success_criteria>

<output>
Create `.planning/phases/38-analytics-estimates-notifications-b4/38-05-SUMMARY.md` when done.
</output>
