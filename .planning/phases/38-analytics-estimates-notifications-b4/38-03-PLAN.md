---
phase: 38-analytics-estimates-notifications-b4
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/useNotifications.ts
  - frontend/src/components/ui/NotificationPanel.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/hooks/useNotifications.test.tsx
autonomous: true
requirements: [SC-2, SHELL-05]
must_haves:
  truths:
    - "SC-2: the notifications feed produces a notification for each of the four kinds gate / running / done / failed (today only markCompleted fires; failed/cancelled are destructured but never called and there is no gate kind)."
    - "SC-001/INV-1: terminal + gate transitions key OFF the GENERIC pipelineState markers (failed / cancelled / reviewGateData) — NEVER a workflow-name branch."
    - "owner-scoped: the feed reflects the owner's own run-state (the pipelineState the owner's session drives); no cross-owner run surfaces."
    - "token gate: the new gate kind renders via the Phase-32 --status-amber ramp; NotificationPanel per-file retired-palette grep = 0."
    - "a11y: the feed stays keyboard-operable + aria-labelled (Phase-35 role=menu / Escape-close preserved; the gate row inherits it)."
    - "LOCK-B: NO transport touch — the derivation is client state off pipelineState; live PUSH over the real connection is LIVE-DEFERRED to Phase 34."
  artifacts:
    - path: "frontend/src/hooks/useNotifications.ts"
      provides: "gate kind in the status union + markGatePaused transition"
      contains: "gate"
    - path: "frontend/src/components/ui/NotificationPanel.tsx"
      provides: "StatusIcon + Badge branch for the gate kind (--status-amber)"
      contains: "gate"
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "wired markFailed / markCancelled / markGatePaused off generic pipelineState markers"
      contains: "markGatePaused"
    - path: "frontend/src/hooks/useNotifications.test.tsx"
      provides: "gate-kind + failed/cancelled transition tests"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "useNotifications transitions"
      via: "generic terminal + gate markers"
      pattern: "markFailed|markCancelled|markGatePaused"
    - from: "frontend/src/hooks/useNotifications.ts"
      to: "PipelineNotification.status union"
      via: "gate kind"
      pattern: "\"gate\""
---

<objective>
Make the notifications feed honest: today `PipelineNotification.status` is `running|completed|failed|cancelled` with NO `gate` kind, and `DashboardLayout` destructures `markFailed`/`markCancelled` (:365-366) but only ever fires `markCompleted` (:456) — so failed/cancelled runs produce no notification and there is no gate notification at all. Add the `gate` kind (amber `--status-amber`) + a `markGatePaused` transition, and WIRE the terminal-failed / terminal-cancelled / gate transitions off the GENERIC `pipelineState` markers.

Purpose: SC-2's "notifications feed live (gate/running/done/failed)" — the derivation logic, built + unit-tested OFFLINE. Independent of the analytics endpoint → parallelizable in Wave 1.
Output: `gate` kind + `markGatePaused` in the hook; gate StatusIcon/Badge branch in the panel; wired transitions in DashboardLayout; a hook test. Live PUSH over the real connection → LIVE-DEFERRED to Phase 34.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-PATTERNS.md
@frontend/src/hooks/useNotifications.ts
@frontend/src/components/ui/NotificationPanel.tsx
</context>

<artifacts_produced>
NEW / changed symbols (Phase 34 live push consumes these):
- `useNotifications.ts`: `PipelineNotification.status` union GAINS `"gate"`; NEW `markGatePaused(id)` transition (mirrors `markFailed` at :78, sets `status:"gate"`).
- `NotificationPanel.tsx`: `StatusIcon` gains a `gate` branch (amber icon via `--status-amber`); `Badge status="gate"` supported.
- `DashboardLayout.tsx`: the terminal branch now calls `markFailed`/`markCancelled` off `pipelineState.failed`/`.cancelled`, and `markGatePaused` off the gate marker (`reviewGateData && isPipelineRunning`), all keyed on `currentPipelineNotifId.current`.
- `useNotifications.test.tsx`: NEW test file.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 failing tests for the gate kind + failed/cancelled transitions</name>
  <files>frontend/src/hooks/useNotifications.test.tsx</files>
  <read_first>
    - frontend/src/hooks/useNotifications.ts:6-17 (`PipelineNotification` type + `status` union at :11), :41-108 (transitions: `addRunningNotification:41`, `markCompleted:69`, `markFailed:78`, `markCancelled:87`).
    - An existing hook test under frontend/src/hooks/ (or `@testing-library/react` `renderHook`/`act` idiom) for the pattern.
    - .planning/phases/38-analytics-estimates-notifications-b4/38-VALIDATION.md rows 55-56.
  </read_first>
  <behavior>
    - addRunning → markGatePaused(id): item status becomes `"gate"` (RED now — markGatePaused does not exist).
    - addRunning → markFailed(id): status `"failed"`, completedAt set.
    - addRunning → markCancelled(id): status `"cancelled"`, completedAt set.
    - The status union accepts `"gate"` at the type level (compiles once added).
  </behavior>
  <action>
    Create `useNotifications.test.tsx` using `renderHook(() => useNotifications())` + `act`. Assert the four transitions listed in `<behavior>`. The `markGatePaused` + `"gate"` assertions fail now (RED).
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/hooks/useNotifications` FAILS on the missing `markGatePaused` / `"gate"` kind (RED observed).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useNotifications 2>&1 | tail -8</automated>
  </verify>
  <done>Hook test exists and fails on the not-yet-added gate kind / markGatePaused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add gate kind + markGatePaused (hook) and gate StatusIcon/Badge (panel)</name>
  <files>frontend/src/hooks/useNotifications.ts, frontend/src/components/ui/NotificationPanel.tsx</files>
  <read_first>
    - frontend/src/hooks/useNotifications.ts:11 (status union), :78-95 (`markFailed`/`markCancelled` to mirror for `markGatePaused`).
    - frontend/src/components/ui/NotificationPanel.tsx:30-34 (`StatusIcon` branches → `text-status-*`), :196 (`<Badge status={n.status} />`), :179 (`--status-running-fill` usage).
    - frontend/src/components/ui/Badge.tsx (status→token mapping — confirm/extend a `gate`/amber branch).
    - 38-PATTERNS.md Mapping 4 (gate uses `--status-amber`, globals.css:103 "cancelled + gate/review").
  </read_first>
  <behavior>
    - Makes the Task-1 gate assertions GREEN.
    - `PipelineNotification.status` union includes `"gate"`; `markGatePaused(id)` sets `status:"gate"` (no completedAt — it is a paused, not terminal, state).
    - `StatusIcon` renders an amber gate icon (e.g. a pause/gate lucide icon) via `text-[color] --status-amber`; `Badge` renders a `gate` status without falling through.
  </behavior>
  <action>
    In `useNotifications.ts`: add `"gate"` to the `status` union (:11) and a `markGatePaused` `useCallback` mirroring `markFailed` (:78) but setting `status:"gate" as const` and `read:false`; export it from the hook's return object. In `NotificationPanel.tsx`: add a `status === "gate"` branch to `StatusIcon` (:30) using an amber token (`--status-amber`), and ensure `Badge status="gate"` maps to the amber ramp (extend `Badge.tsx` if it lacks a gate case). Map `completed`→Badge `done` where the panel already does. Do NOT touch any SSE/WS transport (LOCK-B).
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/hooks/useNotifications` PASSES (gate + failed + cancelled) — GREEN observed.
    - `grep -c '"gate"' frontend/src/hooks/useNotifications.ts` → ≥ 1; `grep -c 'markGatePaused' frontend/src/hooks/useNotifications.ts` → ≥ 2 (defn + export).
    - `grep -c 'gate' frontend/src/components/ui/NotificationPanel.tsx` → ≥ 1 and `grep -Ec "status-amber|--status-amber" frontend/src/components/ui/NotificationPanel.tsx` (or Badge.tsx) → ≥ 1.
    - NotificationPanel retired-palette grep = 0: `grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" frontend/src/components/ui/NotificationPanel.tsx` → 0.
    - a11y preserved: `grep -Ec "role=\"menu\"|aria-label|aria-haspopup" frontend/src/components/ui/NotificationPanel.tsx` → > 0 (unchanged).
    - No transport touch: `grep -REc "EventSource|WebSocket|/events/stream|/ws/" frontend/src/hooks/useNotifications.ts frontend/src/components/ui/NotificationPanel.tsx` → 0.
    - `cd frontend && npx tsc --noEmit` → identity.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useNotifications 2>&1 | tail -8 && grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" src/components/ui/NotificationPanel.tsx</automated>
  </verify>
  <done>Gate kind + markGatePaused exist and pass; panel renders the amber gate branch; a11y + token gate hold; no transport touch.</done>
</task>

<task type="auto">
  <name>Task 3: Wire markFailed / markCancelled / markGatePaused off generic pipelineState markers in DashboardLayout</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx:361-369 (`useNotifications` destructure — `markCompleted`/`markFailed`/`markCancelled` already pulled), :401-466 (the effect that adds the running notification + only fires `markCompleted:456`, `currentPipelineNotifId.current`), :1305-1316 (the GENERIC terminal/gate markers: `reviewGateData && isPipelineRunning ? "gate"`; `pipelineState?.failed || pipelineState?.cancelled || pipelineState?.degraded ? "terminal"`).
    - 38-PATTERNS.md Mapping 4 (Pitfall 4: key OFF generic pipelineState, NEVER a workflow name).
  </read_first>
  <behavior>
    - When the run reaches terminal-failed (`pipelineState.failed`) → `markFailed(currentPipelineNotifId.current)`.
    - When terminal-cancelled (`pipelineState.cancelled`) → `markCancelled(...)`.
    - When a review gate opens (`reviewGateData && isPipelineRunning`, the existing `runLaneState==="gate"` condition) → `markGatePaused(...)`.
    - `markCompleted` still fires on all-done (unchanged). All keyed on the generic markers — grep proves no workflow-name literal in the wiring.
  </behavior>
  <action>
    Pull `markGatePaused` from the `useNotifications()` destructure (:365-366 block). In the terminal-handling effect (the one near :442-466 that currently only calls `markCompleted`), branch on the GENERIC markers: `pipelineState?.failed` → `markFailed(currentPipelineNotifId.current)`; `pipelineState?.cancelled` → `markCancelled(...)`; the review-gate-open condition (`reviewGateData && isPipelineRunning`, matching the `runLaneState:1310` gate arm) → `markGatePaused(...)`. Guard each on `currentPipelineNotifId.current` being set. Do NOT branch on `pipeline_type`/workflow name. Do NOT touch transport (LOCK-B). Live PUSH confirmation → deferred.
  </action>
  <acceptance_criteria>
    - `grep -Ec "markFailed\(|markCancelled\(|markGatePaused\(" frontend/src/components/layout/DashboardLayout.tsx` → ≥ 3 (all three now CALLED, not just destructured).
    - SC-001: the wiring keys on generic markers — `grep -nE "pipelineState\?\.(failed|cancelled)|reviewGateData" frontend/src/components/layout/DashboardLayout.tsx` shows the guard uses generic state; `grep -nE "(markFailed|markCancelled|markGatePaused)\([^)]*(prototype|od_ppt|ppt|user_stories|app_builder)" DashboardLayout.tsx` → 0.
    - No transport touch in the diff: `git diff --unified=0 frontend/src/components/layout/DashboardLayout.tsx | grep -Ec "EventSource|WebSocket|/events/stream|/ws/"` → 0.
    - `cd frontend && npx tsc --noEmit` → identity (no new errors vs baseline).
    - `cd frontend && npx vitest run src/hooks src/components/ui/NotificationPanel` → green (regression check on the feed surface).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error; grep -Ec "markFailed\(|markCancelled\(|markGatePaused\(" src/components/layout/DashboardLayout.tsx</automated>
  </verify>
  <done>All three terminal/gate transitions are wired off generic pipelineState markers; tsc identity holds; no transport touch; no name-branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipelineState (owner session) → feed | The feed derives from the owner's own run-state; no cross-owner run is addressable. |
| transport (SSE/WS) → NOT touched | LOCK-B: no transport code in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Tampering / design | DashboardLayout wiring | mitigate | transitions key on generic `pipelineState.failed/.cancelled` + `reviewGateData`; grep asserts no workflow-name literal in the wiring (SC-001/INV-1). |
| T-38-06 | Information disclosure | feed source | mitigate | feed reflects only the owner's own session run-state; no cross-owner run id is fetched here. |
| T-38-07 | Tampering | transport | accept (out of scope) | LOCK-B — no SSE/WS touch; live PUSH LIVE-DEFERRED to Phase 34; grep asserts no transport symbols in the diff. |
| T-38-SC | Tampering | package installs | accept | zero installs. |
</threat_model>

<verification>
OFFLINE, BY DELTA:
- `cd frontend && npx vitest run src/hooks/useNotifications src/components/ui/NotificationPanel` → green (gate kind + transitions).
- NotificationPanel retired-palette grep = 0; a11y (role=menu/aria) preserved.
- `grep -Ec "markFailed\(|markCancelled\(|markGatePaused\(" DashboardLayout.tsx` → ≥ 3 (wired, not just destructured).
- No transport symbols in the DashboardLayout / hook diff (LOCK-B).
- `cd frontend && npx tsc --noEmit` → identity.
- **Live notification PUSH over the real connection → LIVE-DEFERRED to Phase 34.** Mocked Playwright e2e → LIVE-DEFERRED to Phase 34.
</verification>

<success_criteria>
- The feed supports gate/running/done/failed; failed/cancelled/gate transitions are wired off generic pipelineState markers.
- Amber gate rendering via `--status-amber`; a11y + token gate hold; no transport touch; tsc identity; hook tests green.
</success_criteria>

<output>
Create `.planning/phases/38-analytics-estimates-notifications-b4/38-03-SUMMARY.md` when done.
</output>
