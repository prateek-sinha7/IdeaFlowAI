---
phase: 38-analytics-estimates-notifications-b4
plan: 04
type: execute
wave: 2
depends_on: [38-01, 38-02]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/analytics/AnalyticsPage.tsx
  - frontend/src/components/analytics/AnalyticsPage.test.tsx
autonomous: true
requirements: [SC-1, SHELL-05]
must_haves:
  truths:
    - "SC-1: the dashboard date/pipeline/model filters RECOMPUTE by re-querying the server date-scoped /api/analytics/summary endpoint (38-01) instead of re-filtering an in-memory array of up-to-500 raw runs."
    - "SC-001/INV-1: AnalyticsPage binds to the endpoint's generic type/model/status rollups; PIPELINE_LABELS-style maps stay display-only — no workflow-name control flow."
    - "token gate: AnalyticsPage is fully reskinned onto Phase-32 @theme tokens; per-file retired-palette grep = 0 (this is the worst-offender file — ~100 hits today)."
    - "INV-3 / no-dual-impl: the inline BarChart (:96) and inline donut (:432) are DELETED and replaced by the extracted DonutChart/BarChart (38-02) — a single implementation, no shadow copy."
    - "owner-scoped: AnalyticsPage renders only the caller's own aggregated numbers (the endpoint filters WHERE user_id == current_user.id)."
    - "a11y: the charts rendered on the page carry role=img + aria-label (via the 38-02 primitives)."
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "getAnalyticsSummary(token, range) client + AnalyticsSummary TS type mirroring the backend Pydantic"
      exports: ["getAnalyticsSummary", "AnalyticsSummary"]
    - path: "frontend/src/components/analytics/AnalyticsPage.tsx"
      provides: "reskinned, endpoint-backed analytics dashboard (filter→refetch→re-render)"
      contains: "getAnalyticsSummary"
    - path: "frontend/src/components/analytics/AnalyticsPage.test.tsx"
      provides: "filter→refetch→re-render + retired-palette-absent test"
  key_links:
    - from: "frontend/src/components/analytics/AnalyticsPage.tsx"
      to: "/api/analytics/summary"
      via: "getAnalyticsSummary on filter change"
      pattern: "getAnalyticsSummary"
    - from: "frontend/src/components/analytics/AnalyticsPage.tsx"
      to: "analytics/charts primitives"
      via: "import DonutChart/BarChart"
      pattern: "from \"\\.\\./analytics/charts|from \"\\./charts|charts/(Donut|Bar)Chart"
---

<objective>
Rewire `AnalyticsPage.tsx` (683 lines, ~100 retired-palette hits) from client-side aggregation (`getWorkflows(limit:500)` + `useMemo` rollups) to the server date-scoped `GET /api/analytics/summary` endpoint (38-01): the date/pipeline/model filters RECOMPUTE by re-querying instead of re-filtering an in-memory array. Simultaneously reskin the page onto Phase-32 `@theme` tokens and replace the two inline chart copies with the extracted `DonutChart`/`BarChart` (38-02), deleting the inline versions (INV-3).

Purpose: SC-1 — "date-scoped analytics aggregations power the dashboard (filters actually recompute)." Also owns the FE `getAnalyticsSummary` client + `AnalyticsSummary` type that 38-05 (Home estimates) reuses.
Output: `getAnalyticsSummary` + `AnalyticsSummary` in `api.ts`; reskinned endpoint-backed `AnalyticsPage.tsx`; `AnalyticsPage.test.tsx`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-PATTERNS.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-01-SUMMARY.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-02-SUMMARY.md
</context>

<artifacts_produced>
NEW / changed symbols (38-05 consumes `getAnalyticsSummary`):
- `frontend/src/lib/api.ts`:
  - `export interface AnalyticsSummary` — mirrors the backend Pydantic field-for-field (`kpis`, `daily[]`, `pipelines[]`, `models[]`, `spend`, `token_totals`, `type_avg_duration_sec`).
  - `export async function getAnalyticsSummary(token, range)` — mirrors the `getWorkflows` fetch idiom (`request<AnalyticsSummary>('/api/analytics/summary?range=' + range, { headers: authHeaders(token) })`).
- `frontend/src/components/analytics/AnalyticsPage.tsx` — endpoint-backed, reskinned, imports the 38-02 charts (inline copies deleted).
- `frontend/src/components/analytics/AnalyticsPage.test.tsx` — NEW.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add getAnalyticsSummary + AnalyticsSummary type to api.ts</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts:80-97 (`request<T>` + `authHeaders`), :316-330 (`getWorkflows` — the exact fetch idiom to mirror).
    - .planning/phases/38-analytics-estimates-notifications-b4/38-01-SUMMARY.md (the `AnalyticsSummary` response field list produced by 38-01) — mirror it field-for-field. Fallback source if the summary is thin: backend/app/api/analytics.py `AnalyticsSummary` Pydantic model.
  </read_first>
  <behavior>
    - `getAnalyticsSummary(token, "7d")` issues `GET /api/analytics/summary?range=7d` with `authHeaders(token)`, typed `Promise<AnalyticsSummary>`.
    - `AnalyticsSummary` is a structural mirror of the backend model (kpis / daily / pipelines / models / spend / token_totals / type_avg_duration_sec).
  </behavior>
  <action>
    Add `export interface AnalyticsSummary` mirroring the 38-01 backend Pydantic response (field-for-field, generic type/model keys). Add `export async function getAnalyticsSummary(token: string, range: string): Promise<AnalyticsSummary>` using the `request` + `authHeaders` idiom exactly like `getWorkflows` (:316). Do not add any other endpoint.
  </action>
  <acceptance_criteria>
    - `grep -c "getAnalyticsSummary" frontend/src/lib/api.ts` → ≥ 1; `grep -c "interface AnalyticsSummary" frontend/src/lib/api.ts` → 1.
    - `grep -c "/api/analytics/summary" frontend/src/lib/api.ts` → ≥ 1.
    - `cd frontend && npx tsc --noEmit` → identity (no new errors vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error; grep -c "getAnalyticsSummary\|interface AnalyticsSummary" src/lib/api.ts</automated>
  </verify>
  <done>Typed client + type exist, mirror the backend model, tsc identity holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 failing AnalyticsPage test (filter→refetch→re-render + palette-absent)</name>
  <files>frontend/src/components/analytics/AnalyticsPage.test.tsx</files>
  <read_first>
    - frontend/src/components/analytics/AnalyticsPage.tsx:187-235 (`dateFilter` state + filter handlers), :324-355 (the filter pill row + pipeline/model selects).
    - An existing component test that mocks `@/lib/api` (vi.mock) for the render + interaction idiom.
    - 38-VALIDATION.md rows 51, 53.
  </read_first>
  <behavior>
    - Mount AnalyticsPage with `getAnalyticsSummary` mocked → assert it is called once on mount with the default range.
    - Change the date filter (click a different pill) → assert `getAnalyticsSummary` is called again with the NEW range (server recompute), and a KPI/number re-renders from the second payload.
    - Assert the rendered charts expose `role="img"` (a11y via 38-02 primitives).
    - Fails now (page still calls `getWorkflows` + client rollup) — RED.
  </behavior>
  <action>
    Create `AnalyticsPage.test.tsx` with `vi.mock("@/lib/api")` stubbing `getAnalyticsSummary` (two distinct payloads). Assert call-on-mount, call-on-filter-change with the new range, and re-render of a number from the new payload; assert `getAllByRole("img").length > 0`. RED against the current getWorkflows-based page.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/analytics/AnalyticsPage` FAILS (RED observed — page does not yet call getAnalyticsSummary).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analytics/AnalyticsPage 2>&1 | tail -8</automated>
  </verify>
  <done>Test exists and fails because the page still uses client-side aggregation.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire AnalyticsPage to the endpoint + full Phase-32 reskin + swap in extracted charts</name>
  <files>frontend/src/components/analytics/AnalyticsPage.tsx</files>
  <read_first>
    - frontend/src/components/analytics/AnalyticsPage.tsx — the WHOLE file: data source `getWorkflows(limit:500):200`; the `useMemo` rollups `filteredRuns:211`, `tokenStats:249`, `pipelineBreakdown:265`, `dailyUsage:282`, `successRate:262`; the KPI tiles / Daily bars / Success donut / By-Pipeline bars / Token Breakdown / Model Details / spend chip; the retired palette (`PIPELINE_PALETTE:54-62`, inline `BarChart:96`, donut `:433-444`, and every `#1B2A4A`/`#8AAEC8`/`#E8EDF5`/`#F3F4F6`/`bg-white`/`gray-*` at :55,:65,:96,:173,:328,:336,:347,:393,:396,:408,:436,:582,:595,:670-672).
    - frontend/src/components/analytics/charts/DonutChart.tsx + BarChart.tsx (38-02 — the components to import).
    - frontend/src/styles/globals.css + 38-PATTERNS.md Mapping 6 (the exact token substitutions: `#1B2A4A`→`var(--brand)`/`text-brand`, navy ramp→brand/status, `bg-white`→`bg-surface-card`/`-white`, `text-gray-*`→`text-ink-*`, `border-gray-*`→`border-line-*`, `#F3F4F6`/`#E8EDF5` fills→token fills).
    - frontend/src/components/catalog/HomeLaunchGrid.tsx:68-93 (the fetch-shell idiom: mount `useEffect` + `cancelled` guard + `getToken()` fallback + loading/error/finally) to model the refetch.
  </read_first>
  <behavior>
    - Makes the Task-2 test GREEN.
    - Data source is `getAnalyticsSummary(token, dateFilter)`; changing `dateFilter` (and pipeline/model filters where they map to server params) REFETCHES and re-renders — no in-memory reduce of 500 runs.
    - The KPI tiles / daily bars / success donut / pipeline bars / token split / spend bind to the `AnalyticsSummary` fields; the exact KPI math is ported from the endpoint payload (not re-derived in JS).
    - The inline `BarChart` fn and inline donut SVG are DELETED; the page imports `DonutChart`/`BarChart` from `analytics/charts` (INV-3 no dual impl).
    - Fully reskinned: zero retired hex; one-chroma `var(--brand)` except status-data donut segments use `--status-*`.
  </behavior>
  <action>
    Replace `getWorkflows(limit:500)` + all client `useMemo` rollups with a fetch to `getAnalyticsSummary(token, dateFilter)` using the HomeLaunchGrid fetch-shell idiom (mount effect + `cancelled` guard + `getToken()` fallback + loading/error/finally); make `setDateFilter`/`setPipelineFilter`/`setModelFilter` trigger a refetch (server recompute). Bind KPI tiles / Daily Activity / Success donut / By-Pipeline / Token Breakdown / Model Details / spend to the `AnalyticsSummary` fields. DELETE the inline `BarChart` (:96) and inline donut (:432) and import `DonutChart`/`BarChart` from `analytics/charts` instead. Replace EVERY retired hex/`bg-white`/`gray-*`/`PIPELINE_PALETTE` navy value with the Phase-32 token per the Mapping-6 list (`var(--brand)` base; `--status-*` ONLY for status data; `PIPELINE_LABELS` stays display-only — SC-001). Keep `motion/react` animations.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/analytics/AnalyticsPage` PASSES (filter→refetch→re-render + role=img) — GREEN observed.
    - Token gate = 0: `grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" frontend/src/components/analytics/AnalyticsPage.tsx` → 0. (Also confirm the broader navy set: `grep -Ec "#1B2A4A|#8AAEC8|#2E4A7A|#3D6B9E|#5B8DB8|bg-white|text-gray-|bg-gray-" frontend/src/components/analytics/AnalyticsPage.tsx` → 0.)
    - Positive token usage: `grep -Ec "var\(--|--brand|--status-|text-brand|text-ink-|bg-surface|border-line" frontend/src/components/analytics/AnalyticsPage.tsx` → > 0.
    - No dual impl: `grep -Ec "function BarChart|<circle " frontend/src/components/analytics/AnalyticsPage.tsx` → 0 (inline chart definitions removed); `grep -Ec "charts/(Donut|Bar)Chart|from \"\\./charts|from \"\\.\\./analytics/charts" frontend/src/components/analytics/AnalyticsPage.tsx` → ≥ 1 (imports the extracted ones).
    - SC-001 no name-branch: `grep -nE "if .*(pipeline|type) *=== *['\"](prototype|od_ppt|ppt|user_stories|app_builder)" frontend/src/components/analytics/AnalyticsPage.tsx` → 0 (PIPELINE_LABELS display-only is fine).
    - `getWorkflows(.*limit.*500)` removed: `grep -c "getWorkflows" frontend/src/components/analytics/AnalyticsPage.tsx` → 0.
    - `cd frontend && npx tsc --noEmit` → identity (no new errors vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/analytics/AnalyticsPage 2>&1 | tail -8 && grep -REc "#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains" src/components/analytics/AnalyticsPage.tsx && grep -Ec "function BarChart|<circle " src/components/analytics/AnalyticsPage.tsx</automated>
  </verify>
  <done>Page is endpoint-backed (filter→refetch→re-render), fully reskinned (retired-palette=0), imports the extracted charts (inline deleted), no name-branch, tsc identity holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AnalyticsPage → /api/analytics/summary | JWT-authenticated fetch; server enforces owner-scope + range allow-list (38-01). |
| filter input → range param | `dateFilter` is a fixed UI enum; the client only sends allow-listed range values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information disclosure | data source | mitigate | data now comes from the owner-scoped endpoint (38-01), not a 500-row client dump; the server WHERE user_id gate is authoritative. |
| T-38-03 | Tampering (input) | range param | mitigate | `dateFilter` is a fixed UI enum; only allow-listed range strings are sent; server also allow-lists (defense in depth). |
| T-38-04 | Tampering / design | rollup binding | mitigate | binds to generic type/model/status rollups; `PIPELINE_LABELS` display-only; grep asserts no name-branch (SC-001). |
| T-38-A11Y | Accessibility gap | charts | mitigate | rendered via the 38-02 `role="img"`+aria-label primitives. |
| T-38-SC | Tampering | package installs | accept | zero installs. |
</threat_model>

<verification>
OFFLINE, BY DELTA:
- `cd frontend && npx vitest run src/components/analytics` → green (AnalyticsPage + charts regression).
- AnalyticsPage retired-palette grep = 0 (this is the worst-offender file); positive token usage present.
- No inline chart defs (`function BarChart`/`<circle`) remain; extracted charts imported (INV-3).
- `getWorkflows` removed from AnalyticsPage; `getAnalyticsSummary` wired.
- `cd frontend && npx tsc --noEmit` → identity.
- Visual reskin verification + live endpoint round-trip → LIVE-DEFERRED to Phase 34. Mocked Playwright e2e → LIVE-DEFERRED to Phase 34.
</verification>

<success_criteria>
- Dashboard filters recompute against the server endpoint (SC-1); page fully reskinned (retired-palette=0); inline charts deleted and replaced by extracted primitives; no name-branch; tsc identity; tests green.
</success_criteria>

<output>
Create `.planning/phases/38-analytics-estimates-notifications-b4/38-04-SUMMARY.md` when done.
</output>
