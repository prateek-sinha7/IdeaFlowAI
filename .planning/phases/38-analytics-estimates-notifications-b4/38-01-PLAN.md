---
phase: 38-analytics-estimates-notifications-b4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/analytics.py
  - backend/app/main.py
  - backend/tests/unit/test_analytics_api.py
autonomous: true
requirements: [SC-1, SHELL-05]
must_haves:
  truths:
    - "SC-1: GET /api/analytics/summary returns date-scoped aggregations (daily series + per-type/model rollups + spend) computed server-side over the caller's own runs."
    - "owner-scoped (user_id→404): user A's aggregate includes ONLY A's runs; a cross-owner row never enters the aggregate (the runs.py:250 WHERE user_id == current_user.id idiom, NEVER the nullable owner_id:58)."
    - "token gate / enum allow-list: the range param is an allow-listed enum (today|3d|7d|30d|90d|all); an unknown value falls back to the default window, never an unbounded or injected scan."
    - "DoS guard: a malformed token_usage blob degrades to an empty per-run aggregate via try/except→{}, never a 500."
    - "SC-001/INV-1: aggregation keys on generic type/status/model_id — NEVER a workflow-name branch; PIPELINE_LABELS-style maps stay display-only."
    - "additive migrations only / INV-3: additive read-only over existing WorkflowRun columns — NO new table, NO migration; the 5 characterization goldens stay byte-identical by construction."
  artifacts:
    - path: "backend/app/api/analytics.py"
      provides: "owner-scoped /api/analytics/summary aggregation router"
      exports: ["router", "get_analytics_summary", "AnalyticsSummary"]
      min_lines: 90
    - path: "backend/tests/unit/test_analytics_api.py"
      provides: "owner-isolation / recompute / malformed-guard / range-scoping tests"
      min_lines: 80
    - path: "backend/app/main.py"
      provides: "analytics router registration beside runs_router"
      contains: "analytics"
  key_links:
    - from: "backend/app/api/analytics.py"
      to: "WorkflowRun"
      via: "owner-scoped base query"
      pattern: "WorkflowRun\\.user_id == current_user\\.id"
    - from: "backend/app/main.py"
      to: "analytics router"
      via: "include_router"
      pattern: "analytics_router|include_router\\(analytics"
---

<objective>
Add ONE additive, read-only, owner-scoped aggregation endpoint — `GET /api/analytics/summary?range=<today|3d|7d|30d|90d|all>` — on a NEW `backend/app/api/analytics.py` router, registered beside `runs_router` in `main.py`. It aggregates the caller's OWN `WorkflowRun` rows (daily series, per-type rollup, per-model rollup, spend, per-type average duration) so the browser stops downloading up to 500 raw run rows to reduce in JS (today's `AnalyticsPage` client-side rollup).

Purpose: this is the SC-1 foundation — the endpoint the reskinned AnalyticsPage (38-04) re-queries on filter change, and the history-average source the Home estimates (38-05) consume.
Output: `analytics.py` router + `AnalyticsSummary` Pydantic schema + `test_analytics_api.py` (Wave-0 tests) + one `include_router` line in `main.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-RESEARCH.md
@.planning/phases/38-analytics-estimates-notifications-b4/38-PATTERNS.md
</context>

<artifacts_produced>
NEW symbols this plan produces (downstream plans + Phase 34 consume these):
- `backend/app/api/analytics.py` — NEW router module.
  - `router = APIRouter(prefix="/api/analytics", tags=["analytics"])`
  - `get_analytics_summary(range, current_user, db)` — the `GET /api/analytics/summary` handler.
  - `AnalyticsSummary` — Pydantic response model (the FE `AnalyticsSummary` TS type in 38-04 mirrors this field-for-field).
  - `_cutoff_for(range)` — allow-listed enum → datetime|None helper.
  - `_aggregate(runs)` — pure roll-up helper (daily series / per-type / per-model / spend / type_avg_duration_sec).
- Endpoint path: `GET /api/analytics/summary?range=<enum>`.
- Response fields (generic, name-free): `kpis` (total/completed/failed/success_rate), `daily[]` (date + counts + tokens), `pipelines[]` (per-`type` count/tokens/cost/avg_duration), `models[]` (per-`model_id` count/tokens/cost), `spend` (sum of persisted `estimated_cost_usd`), `token_totals` (input/output/cache), `type_avg_duration_sec` (Record[type→seconds], the Home-estimate source).
- `backend/tests/unit/test_analytics_api.py` — NEW test file (owner_isolation / recompute / malformed / range cases).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 failing tests for the analytics summary endpoint</name>
  <files>backend/tests/unit/test_analytics_api.py</files>
  <read_first>
    - backend/tests/unit/ — an existing owner-scoped endpoint test in this dir for the seed/client fixtures (two-user seeding + auth token idiom). Mirror its `TestClient` + DB-session setup exactly.
    - backend/app/api/runs.py:236-255 (`list_runs` owner-scoped base query — the aggregation's owner-scope analog) and :1116-1123 (tolerant `json.loads`→`{}` guard).
    - backend/app/models/workflow.py:19-41 (WorkflowRun columns: `user_id:20`, `type:22`, `status:23`, `agent_count:33`, `duration:34`, `token_usage:36`, `model_id:37`, `created_at:38`, `completed_at:41`).
    - .planning/phases/38-analytics-estimates-notifications-b4/38-VALIDATION.md rows 47-50 (the exact `-k` case names).
  </read_first>
  <behavior>
    - test_owner_isolation: seed user A (3 runs) + user B (2 runs, distinct token/cost); assert A's `/api/analytics/summary` kpis.total == 3 and spend/token_totals sum ONLY A's runs (B never leaks).
    - test_recompute: seed A with runs inside and outside a 7d window; `?range=7d` kpis/spend == the hand-computed sum of the in-window runs only.
    - test_malformed: seed A with one run whose `token_usage` is a non-JSON string ("{bad"); `/summary` returns 200 with that run contributing an empty token aggregate (spend unaffected by the bad blob), never a 500.
    - test_range: `?range=all` returns every A run; `?range=today` returns only runs with `created_at` today; unknown `?range=zzz` falls back to the default window (no 4xx/5xx crash).
    - Auth: an unauthenticated call → 401 (inherited `Depends(get_current_user)`).
  </behavior>
  <action>
    Create `test_analytics_api.py` with the four `-k`-selectable cases named `owner_isolation`, `recompute`, `malformed`, `range` (so `pytest -k owner_isolation` etc. select exactly one). Seed `WorkflowRun` rows directly via the DB session (set `user_id`, `type`, `status`, `duration`, `token_usage` as a `json.dumps({...estimated_cost_usd...})` string, `model_id`, `created_at`). Assert against the `AnalyticsSummary` fields listed in this plan's `<artifacts_produced>`. Tests MUST fail now (endpoint does not exist yet) — this is RED.
  </action>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/unit/test_analytics_api.py -x` FAILS with a collection/404/ImportError against the missing endpoint (RED observed, not assumed).
    - The file defines exactly four test fns matching `-k owner_isolation|recompute|malformed|range`.
    - No new external package imported (grep the new file for `import recharts|chart|d3` → 0).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_analytics_api.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>Four named failing tests exist and fail for the right reason (no endpoint), NOT for a fixture/import error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Owner-scoped analytics aggregation router + register in main.py</name>
  <files>backend/app/api/analytics.py, backend/app/main.py</files>
  <read_first>
    - backend/app/api/runs.py:236-255 (`list_runs` — clone the `db.query(WorkflowRun).filter(WorkflowRun.user_id == current_user.id)` owner-scope), :1080-1130 (`get_run_summary` — the `Depends(get_current_user)` + `Depends(get_db)` signature + the try/except JSON parse at :1116-1123), :1010 (`RunSummaryResponse` Pydantic template).
    - backend/app/api/workflows.py:66-78 (`WorkflowSummary` Pydantic style) for the response-model idiom.
    - backend/app/main.py:12-31 (router imports) + :170-183 (`app.include_router(runs_router)` at :175 — register the new router right beside it).
    - .planning/phases/38-analytics-estimates-notifications-b4/38-RESEARCH.md L268-291 (`_cutoff_for` allow-list table + the `getAnalyticsSummary` client contract 38-04 will mirror) and 38-PATTERNS.md Mapping 1 (token_usage JSON shape: `estimated_cost_usd`, `total_input_tokens`, `total_output_tokens`, cache keys).
  </read_first>
  <behavior>
    - Makes all four Task-1 tests GREEN.
    - `_cutoff_for(range)` maps the allow-listed enum {today,3d,7d,30d,90d,all}→datetime|None, unknown→default (30d) — NO exception on a bad value.
    - Base query is owner-scoped: `WorkflowRun.user_id == current_user.id`, then `.filter(WorkflowRun.created_at >= cutoff)` when cutoff is not None.
    - `token_usage` parsed per-run in try/except→`{}`; spend = Σ of the persisted `estimated_cost_usd` key (default 0.0 when absent) — NOT re-derived from pricing math.
    - Rollups key on generic `type` / `model_id` / `status` — no `if type == "prototype"` branch anywhere.
  </behavior>
  <action>
    Create `backend/app/api/analytics.py`: `router = APIRouter(prefix="/api/analytics", tags=["analytics"])`; `get_analytics_summary(range: str = "30d", current_user: User = Depends(get_current_user), db: Session = Depends(get_db))` returning `AnalyticsSummary`. Implement `_cutoff_for` (allow-list `.get(range, default)`), `_aggregate(runs)` producing the fields in `<artifacts_produced>`: `kpis` (total/completed/failed/success_rate off generic `status`), `daily[]` bucketed by `date(created_at)`, `pipelines[]` grouped by generic `type` (count + summed tokens + summed `estimated_cost_usd` + `avg_duration` over completed runs), `models[]` grouped by `model_id`, `token_totals`, `spend`, and `type_avg_duration_sec` (Record[type→avg completed `duration`]). Parse `token_usage` via the runs.py:1116-1123 try/except→`{}` idiom. Return NUMBERS ONLY — never `input`/`output`/`agent_outputs` bodies. In `main.py`, add `from app.api.analytics import router as analytics_router` beside the other imports and `app.include_router(analytics_router)` beside `include_router(runs_router)` at ~:175.
  </action>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/unit/test_analytics_api.py -x` PASSES all four cases (owner_isolation, recompute, malformed, range) — GREEN observed.
    - `grep -nE "if .*type *== *['\"](prototype|od_ppt|ppt|user_stories|app_builder)" backend/app/api/analytics.py` → 0 (SC-001: no workflow-name branch).
    - `grep -nE "owner_id" backend/app/api/analytics.py` → 0 (owner-scope is `user_id`, never the nullable `owner_id`).
    - `grep -nE "agent_outputs|\.output|input_prompt|thinking_text" backend/app/api/analytics.py` → 0 (numbers-only, no agent-body leak).
    - `grep -n "analytics" backend/app/main.py` shows both the import and the `include_router` line.
    - `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken.
    - The 5 goldens byte-identical: `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_prototype_revision.py` → all pass.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_analytics_api.py -x 2>&1 | tail -5 && /opt/homebrew/bin/lint-imports 2>&1 | tail -3</automated>
  </verify>
  <done>All four analytics tests green; router registered; no name-branch / owner_id / agent-body grep hits; lint-imports 4/0; goldens byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/analytics/summary | JWT-authenticated caller supplies `range`; must only ever see their OWN aggregated run numbers. |
| stored token_usage → aggregate | Persisted JSON Text blob (attacker-influenceable via prior runs) parsed at read time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information disclosure | `get_analytics_summary` base query | mitigate | `WHERE WorkflowRun.user_id == current_user.id` (runs.py:250 idiom); `test_owner_isolation` proves B's runs never enter A's aggregate. NEVER the nullable `owner_id`. |
| T-38-02 | Denial of service | `token_usage` parse | mitigate | `json.loads` in try/except → `{}` fallback (runs.py:1116-1123); `test_malformed` proves 200-not-500. |
| T-38-03 | Tampering (input) | `range` param | mitigate | allow-listed enum via `_cutoff_for(...).get(range, default)`; `test_range` proves unknown→default, no crash. |
| T-38-Leak | Information disclosure | response body | mitigate | return numbers only (counts/tokens/cost/durations); grep asserts no `agent_outputs`/`output`/`input_prompt` echoed (V7 / `_SUMMARY_SAFE_AGENT_KEYS` posture). |
| T-38-04 | Tampering / design | rollup logic | mitigate | key on generic `type`/`status`/`model_id`; grep asserts no workflow-name branch (SC-001/INV-1). |
| T-38-SC | Tampering | package installs | accept | zero installs this phase (charts hand-rolled, backend reuses vendored FastAPI/SQLAlchemy). No new package → no legitimacy gate. |
</threat_model>

<verification>
OFFLINE, BY DELTA:
- Backend: `cd backend && python3.11 -m pytest tests/unit/test_analytics_api.py -x` (owner_isolation / recompute / malformed / range all green). **NEVER full pytest (hangs offline).**
- `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken.
- 5 characterization goldens byte-identical (command in Task 2 acceptance).
- Live round-trip of the endpoint against a running server → LIVE-DEFERRED to Phase 34.
</verification>

<success_criteria>
- `GET /api/analytics/summary?range=` exists, owner-scoped, additive read-only, no new table/migration.
- All four Wave-0 tests green; no name-branch / owner_id / agent-body grep hits.
- Router registered in main.py; lint-imports 4/0; goldens byte-identical.
</success_criteria>

<output>
Create `.planning/phases/38-analytics-estimates-notifications-b4/38-01-SUMMARY.md` when done.
</output>
