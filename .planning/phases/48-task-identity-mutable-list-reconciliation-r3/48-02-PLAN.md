---
phase: 48-task-identity-mutable-list-reconciliation-r3
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/agents/capabilities/strategies/task_loop.py
  - backend/tests/agents/test_restart_resume.py
  - backend/tests/agents/test_task_list_gate_lineage.py
autonomous: true
requirements: [RESUME-15, RESUME-16]
must_haves:
  truths:
    - "A user edit to the task list mints a NEW task_list version with derived_from lineage to the prior version; max(version) wins; no new table/endpoint (RESUME-15)"
    - "On resume/re-run-after-edit a cumulative task_loop skips the longest common prefix of matching keys IN ORDER and re-runs everything at/after the first divergence (RESUME-16 cumulative)"
    - "Re-materialization for a cumulative resume restores the deliverable version AS OF the common-prefix boundary task, not the global max"
    - "Immutability preserved: no artifact_refs row is deleted or mutated; reconciliation is read/order-side only"
  artifacts:
    - path: "backend/tests/agents/test_task_list_gate_lineage.py"
      provides: "Proof that both gate-edit sites stamp derived_from on the edited task_list and max-version is what re-parses"
      contains: "derived_from"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "Ordered completed-key emission + boundary-version re-materialization"
      contains: "_compute_upstream_context_hash"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "_dual_write_artifact"
      via: "both gate-edit sites pass derived_from=<prior task_list ref id>"
      pattern: "derived_from="
    - from: "backend/agents/capabilities/strategies/task_loop.py"
      to: "common-prefix skip"
      via: "current_keys vs completed_ordered positional divergence"
      pattern: "resume_completed_task_ids"
---

<objective>
Close the ONE RESUME-15 gap and deliver the cumulative half of RESUME-16.

RESUME-15 gap-close: both gate-edit sites (`_gate_edited` engine.py:2971-2979 and
`_apply_declared_gate_edit` engine.py:4566-4575) already mint a NEW `task_list` version through
the sole writer and the strategy already re-parses `max(version)` — the ONLY missing piece is
`derived_from` lineage. Stamp it in place at both sites.

RESUME-16 (cumulative): a completed-key SET is insufficient for `task_loop` because each task
edits the same evolving file. Extend the Phase-46 cursor to ALSO emit completed keys in original
production ORDER (by `min(version)` per task_id), extend the task_loop skip to the ORDER-based
common-prefix rule, and extend `_rematerialize_artifacts_to_disk` to restore the deliverable's
version AS OF the boundary task (not the global max).

Purpose: makes the versioned task list safely editable (RESUME-15) and makes cumulative
single-file builds reconcile correctly after a mid-list delete/edit/insert (RESUME-16) — never
skipping a suffix task whose basis changed (Pitfall 3), never restoring a file that embeds
deleted-task work (Pitfall 4).

Output: `derived_from` at both gate-edit sites; an ordered completed-keys emission on the cursor;
the common-prefix skip in task_loop; boundary-version selection in re-materialization.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-CONTEXT.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-RESEARCH.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-PATTERNS.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-VALIDATION.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-01-SUMMARY.md

# The exact regions to modify (read the quoted code in PATTERNS §4, §5)
@backend/agents/capabilities/strategies/task_loop.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RESUME-15 — stamp derived_from at BOTH gate-edit sites</name>
  <read_first>
    - backend/agents/execution_engine/engine.py:3695-3705 — the redo path `_dual_write_artifact(... derived_from=_iter_derived)` at :3704 — the ONLY gate action that stamps lineage today; the pattern to REPLICATE (PATTERNS §5 Analog)
    - backend/agents/execution_engine/engine.py:2968-2981 — inline `_gate_edited` write (2972-2979, NO derived_from) — gap site 1
    - backend/agents/execution_engine/engine.py:4531-4579 — declared `_apply_declared_gate_edit`; the edit write at 4566-4575 (NO derived_from) — gap site 2
    - backend/agents/execution_engine/engine.py:5517-5573 — `_dual_write_artifact` accepts `derived_from` (:5527) and plumbs to write_ref (:5552); :5575-5603 `_latest_typed_content` (max-version selection to resolve the prior ref id)
    - backend/agents/execution_engine/engine.py:5488-5515 — `_artifact_kind_for` (`prototype-plan → "task_list"`) — the kind whose lineage this closes
    - 48-RESEARCH §"Gate-Edit / task_list Status & Gaps" (the gap is derived_from ONLY — everything else already holds)
  </read_first>
  <files>backend/tests/agents/test_task_list_gate_lineage.py, backend/agents/execution_engine/engine.py</files>
  <behavior>
    - Seed a v1 `task_list` artifact, drive a gate Edit (the declared `_apply_declared_gate_edit` path and, in a second case, the inline `_gate_edited` path) → a v2 `task_list` is written whose `derived_from` == the v1 ref id.
    - `_latest_typed_content(source_step)` returns the EDITED (v2) content (max-version wins) — proving the strategy re-parses the edit.
    - No new table, no new endpoint: the edit rides the existing `/gate` write path (WR-03); only the existing versioned `task_list` artifact is the substrate.
    - Goldens never gate-edit → the change is INV-3 neutral.
  </behavior>
  <action>
    RED FIRST: create `tests/agents/test_task_list_gate_lineage.py` seeding a v1 `task_list`
    then invoking each gate-edit site, asserting (a) a v2 ref exists with `derived_from` == the
    v1 id, and (b) `_latest_typed_content` == the edited content. Run → RED (derived_from is None
    today). Then at BOTH sites resolve the prior ref id via a `_latest_typed_content`-style
    max-version lookup for that kind/agent BEFORE writing, and pass `derived_from=<that id>` into
    `_dual_write_artifact` — copy the redo path idiom at :3704 (PATTERNS §5 Copy/deviate delta).
    Small, additive, event-free. Do NOT introduce a new table/endpoint/status (Pitfall 10 — this
    is the ONLY RESUME-15 gap; resist re-architecting). Cite RESUME-15.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_task_list_gate_lineage.py tests/agents/test_redo_gate_safety.py -q &amp;&amp; SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_prototype.py -q</automated>
  </verify>
  <done>Both gate-edit sites stamp `derived_from`; the new lineage test is GREEN; redo_gate_safety holds at the pre-existing 4/3 (untouched); prototype golden dormancy unbroken; no new table/endpoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RESUME-16 cumulative — ordered completed keys + common-prefix skip</name>
  <read_first>
    - backend/agents/execution_engine/engine.py:6198-6272 — `_compute_resume_completed_task_ids`; the task_loop branch (6251-6259) builds a distinct-`task_id` SET; keep the set (waves + completeness use it) and ADD an ordered emission (PATTERNS §4 Analog A, Pitfall 3)
    - backend/agents/artifacts/graph.py:162-163 — `version = 1 + count prior (run_id, kind)` — monotonic per kind → order-by-`min(version)` per task_id == build order
    - backend/agents/capabilities/strategies/task_loop.py:233-259 — the current set-membership skip (`str(task_num) in _completed_task_nums`) to replace with the common-prefix rule
    - 48-RESEARCH §"Cumulative (task_loop) — the common-prefix rule" (the 4-step rule + the delete-in-the-middle worked example)
  </read_first>
  <files>backend/agents/execution_engine/engine.py, backend/agents/capabilities/strategies/task_loop.py, backend/tests/agents/test_restart_resume.py</files>
  <behavior>
    - The cursor ALSO returns, per task_loop step, `completed_ordered[agent_id]: list[str]` = the distinct completed `task_id`s ordered by `min(version)` (build order). The existing distinct-set return is preserved unchanged (byte-neutral for waves + completeness).
    - The strategy parses the CURRENT (max-version) task list, computes `current_keys[]`, and skips the longest prefix `p` where `current_keys[0:p] == completed_ordered[0:p]` positionally; it RUNS everything at index >= p — even a suffix task whose own key matches a completed key (its predecessor changed → its basis changed). Pitfall 3.
    - Delete-in-the-middle: `[A,B,C]` completed, current `[A,C]` → position 0 A==A ✓, position 1 C!=B divergence → p=1 → re-run C (B's work never merged forward).
    - Fail-safe: any read failure/ambiguity leaves the step out of the completed dict → whole step re-runs. Dormant on a normal run (empty cursor ⇒ byte/event-identical dispatch).
  </behavior>
  <action>
    RED FIRST: add `test_task_loop_reconcile_common_prefix` to `test_restart_resume.py` using the
    seed-durable harness — seed completed keys for tasks in a known order, edit the current list
    (delete a middle task), assert the strategy dispatches exactly the divergence-onward suffix
    (not set-membership). Run → RED. Then extend `_compute_resume_completed_task_ids` to also emit
    `completed_ordered` (order the task_loop `html_file` refs by `min(version)` per distinct
    task_id — graph.py:163 monotonicity; keep the existing set) and stamp it onto `ectx`
    alongside the existing cursor (INV-2 — no engine per-run state; INV-12 — extend, do not fork).
    Replace the task_loop set-membership skip with the ORDER-based common-prefix rule per RESEARCH
    §Cumulative. Keep the wave branch of the cursor and the completeness count untouched. Cite
    RESUME-16.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_restart_resume.py -k "reconcile_common_prefix or resume_completed_task_ids or skips_completed" -q</automated>
  </verify>
  <done>Common-prefix reconcile GREEN (delete-middle re-runs the divergence suffix, never set-membership); the cursor emits both the set and the ordered list; wave + completeness paths unchanged; dormant on normal runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RESUME-16 cumulative — boundary-version re-materialization</name>
  <read_first>
    - backend/agents/execution_engine/engine.py:5975-6039 — `_rematerialize_artifacts_to_disk`; the per-location global `max(version)` selection at 6032-6034 (the behavior to make boundary-aware); `_FILE_KINDS` at 6023, `.uploads/` fence at 6030, best-effort per-row guard at 6038 (all KEEP)
    - backend/agents/artifacts/graph.py:162-163 — version monotonicity (restore-by-`max(version)` per task_id; Pitfall 9: a task_id has multiple versions from the fix-loop re-persist)
    - backend/agents/execution_engine/engine.py:1370-1383 — the resume hook call sites (`_is_resume`-gated) where the boundary/allow-set is threaded from the reconciler
    - 48-RESEARCH §"Cumulative" step 4 + Pitfall 4 (the single most important behavior change)
  </read_first>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/test_restart_resume.py</files>
  <behavior>
    - `_rematerialize_artifacts_to_disk` accepts an OPTIONAL boundary selector (e.g. `boundary_task_id: str | None` and/or a `current_key_allow_set`) with a default of None that preserves today's global-max behavior exactly (dormant on non-cumulative / non-edited resume).
    - When a cumulative boundary is supplied, the deliverable is restored to the `max(version)` `html_file` ref whose `task_id == current_keys[p-1]` (the file AFTER the boundary task's fixes), NOT the global max (which may embed deleted-task work).
    - The `.uploads/` fence (6030), `_FILE_KINDS` (6023), and best-effort per-row degrade (6038) are unchanged; immutable rows are only READ.
  </behavior>
  <action>
    RED FIRST: add `test_task_loop_reconcile_boundary_version` to `test_restart_resume.py` — seed
    multiple `html_file` versions across task_ids (including a later task's version that would win
    under global-max), supply a common-prefix boundary, and assert re-materialization writes the
    boundary task's version to disk (not the global max). Run → RED. Then add the optional
    boundary selector to `_rematerialize_artifacts_to_disk` per PATTERNS §4 Analog B (Pitfall 4),
    thread it from the reconciler at the `_is_resume`-gated hook (engine.py:1383), and keep the
    default None path byte-identical. Order-by-`min(version)`, restore-by-`max(version)` both
    derive from the existing version rows (Pitfall 9). Do NOT delete/mutate any row (T-48-04).
    Cite RESUME-16.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_restart_resume.py -k "reconcile_boundary_version or reconcile_common_prefix" -q &amp;&amp; SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_ppt.py tests/agents/test_characterization_user_stories.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_code_gen.py -q</automated>
  </verify>
  <done>Boundary-version restore GREEN (boundary task's version on disk, not global max); default None path byte-identical (goldens 10/10, `SNAPSHOT_UPDATE` unset); `.uploads/` fence + best-effort degrade intact; no row mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `/gate` edit ingress | `edited_content` mints a new `task_list` version — the ONLY task-edit ingress (WR-03) |
| durable store → reconciler read/order | completed refs read + ordered on resume; must stay owner-scoped and immutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Tampering | gate-edit ingress | mitigate | Edits ride the existing owner-gated `POST /{id}/gate` path (WR-03) only; no new endpoint/table; `derived_from` records lineage; `max(version)` wins — a client cannot forge an out-of-band version |
| T-48-02 | Information Disclosure | ordered-key cursor + boundary re-materialization reads | mitigate | Reads the SAME owner-scoped `ectx.scoped_store.tree(run_id)` / `read_subagent_runs` the Phase-46 cursor uses — default-deny, no scope widening; a foreign row can never enter the completed set or reach disk |
| T-48-04 | Repudiation / Integrity | boundary re-materialization vs immutable rows | mitigate | Reconciliation is READ/ORDER-side only — `_rematerialize_artifacts_to_disk` selects a version to restore, never deletes/mutates an `artifact_refs` row (immutability contract); deleted-task work is superseded by versioning, not erased |
| T-48-05 | DoS / data-loss | common-prefix mis-skip | mitigate | Fail-safe = re-run: any cursor read failure/ambiguity leaves the step out of the completed dict (whole step re-runs); the ORDER rule never skips a suffix whose predecessor changed (Pitfall 3) |
| T-48-SC | Tampering (supply chain) | dependencies | accept | Zero new packages — no npm/pip/cargo install in this plan; no package-legitimacy gate applies |
</threat_model>

<verification>
Run from `backend/` (ABSOLUTE cd; python3.11 no venv; NO live Bedrock):

1. RESUME-15 lineage: `python3.11 -m pytest tests/agents/test_task_list_gate_lineage.py -q` → green; both gate-edit sites stamp `derived_from`; max-version re-parse proven.
2. RESUME-16 cumulative: `python3.11 -m pytest tests/agents/test_restart_resume.py -k "reconcile_common_prefix or reconcile_boundary_version" -q` → green (delete-middle re-runs the divergence suffix; boundary version restored).
3. Delta floor (unchanged): `python3.11 -m pytest tests/agents/test_restart_resume.py tests/agents/test_per_task_capture.py tests/agents/test_wave_scheduler.py -q` → the KAN-88 test remains the SOLE pre-existing red; per_task_capture 4; wave 10.
4. Goldens (INV-3): `SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_*.py -q` → **10 passed** (both the gate-edit lineage and the reconciler are dormant on non-resume/non-edit runs).
5. redo_gate_safety held: `python3.11 -m pytest tests/agents/test_redo_gate_safety.py -q` → pre-existing **4 passed / 3 failed** (untouched — never fix/delete/re-enable).
6. INV-1 + INV-12 + imports: `grep -cE 'pipeline_type ==|spec.id ==' agents/execution_engine/engine.py` → **0**; `grep -c 'def _compute_upstream_context_hash' agents/execution_engine/engine.py` → **1** (single home held from 48-01); `/opt/homebrew/bin/lint-imports` → **4 kept / 0 broken**.
</verification>

<success_criteria>
- RESUME-15 delivered: `derived_from` stamped at BOTH gate-edit sites; edited `task_list` is a new version won by `max(version)` and is what the strategy re-parses; no new table/endpoint (12-era lock held).
- RESUME-16 cumulative delivered: cursor emits completed keys in build order; task_loop uses the ORDER-based common-prefix skip; re-materialization restores the boundary-task version (not global max).
- Immutability preserved (read/order-side only); fail-safe = re-run everywhere; dormant on normal runs.
- Goldens 10/10 (`SNAPSHOT_UPDATE` unset), INV-1 grep 0, single upstream-hash home (count 1), lint 4/0, redo held at 4/3, KAN-88 sole restart red untouched.
</success_criteria>

<output>
Create `.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-02-SUMMARY.md` when done.
</output>
