---
phase: 48-task-identity-mutable-list-reconciliation-r3
plan: 03
type: execute
wave: 3
depends_on: ["48-02"]
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/tests/agents/test_restart_resume.py
  - backend/tests/agents/test_sc001_reconcile.py
autonomous: true
requirements: [RESUME-16]
must_haves:
  truths:
    - "On resume/re-run a wave_scheduler step skips completed workers by key and RUNS new/edited/rotated workers (RESUME-16 independent)"
    - "A completed key absent from the current list (orphan) is excluded from BOTH merge and assembly at read time — the merge implementations are untouched and no row is deleted"
    - "A spec edit (update_specs) rotates the upstream hash → all build task_keys rotate → every build task re-runs (no v1-key skipped)"
    - "A SYNTHETIC non-prototype workflow exercises the whole reconcile path with ZERO edits under agents/execution_engine/ (SC-001)"
  artifacts:
    - path: "backend/tests/agents/test_sc001_reconcile.py"
      provides: "SC-001 synthetic non-prototype reconcile proof (complete 2/4 → delete-completed + edit-pending + add → resume → exact set + orphan excluded)"
      contains: "def test_"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "Orphan-fragment exclusion via the re-materialization allow-set"
      contains: "_rematerialize_artifacts_to_disk"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "_rematerialize_artifacts_to_disk allow-set"
      via: "current-key allow-set skips restoring orphaned fragment locations before merge/assembly"
      pattern: "_rematerialize_artifacts_to_disk"
    - from: "backend/tests/agents/test_sc001_reconcile.py"
      to: "agents/execution_engine/"
      via: "git-diff / grep containment proving zero engine name-literals"
      pattern: "grep -rc"
---

<objective>
Deliver the independent (wave) half of RESUME-16 and the phase's SC-001 proof.

Wave per-key skip already FALLS OUT of 48-01 (wave `requests[].task_id` and the cursor's
`subagent_runs.task_id` are now keys) — this plan's real production work is ORPHAN EXCLUSION:
a completed key absent from the current list must not reach the assembled deliverable, but rows
are immutable. Gate the orphan at the single upstream point — `_rematerialize_artifacts_to_disk`
(extended in 48-02 to accept an allow-set) — so it never reaches disk and therefore never reaches
merge or assembly, with ZERO edits to the merge implementations.

Then prove composition and name-freedom: an `update_specs` spec edit rotates the upstream hash so
all build keys rotate and every build task re-runs (no new code — the rotation composes), and a
SYNTHETIC non-prototype `task_loop` workflow drives the full reconcile path with zero engine
name-literals (the ROADMAP SC4 proof).

Purpose: completes RESUME-16 (skip/run/exclude) and discharges the SC-001 core-value obligation.

Output: the orphan allow-set wiring for wave steps; the update_specs compose test; the sc001
synthetic reconcile fixture + proof test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-CONTEXT.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-RESEARCH.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-PATTERNS.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-VALIDATION.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-01-SUMMARY.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-02-SUMMARY.md

# The SC-001 idiom to clone (read fully)
@backend/tests/agents/test_sc001_nonprototype_task_loop.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RESUME-16 independent — orphan-fragment exclusion at re-materialization</name>
  <read_first>
    - backend/agents/execution_engine/engine.py:5975-6039 — `_rematerialize_artifacts_to_disk` (the optional allow-set param added in 48-02 T3); §6023 `_FILE_KINDS` includes `file_bundle` (fragments), §6030 `.uploads/` fence
    - backend/agents/execution_engine/kernel_services.py:713-751 — `write_fragment_artifact` writes fragments with `task_id=str(worker_index)` (:750) — NOT the key; the join to the key is via `subagent_runs` (worker_index ↔ task_id==key)
    - backend/agents/execution_engine/engine.py:6198-6272 — the wave branch of the cursor (`subagent_runs.task_id` where status=="complete"); the current-key set for the wave step comes from re-parsing the current (max-version) task list + key_by_id
    - 48-RESEARCH §"Independent (wave_scheduler) — per-key + orphan exclusion" (exclusion POINT = re-materialization gate; the fragment→key join is the one MEDIUM-confidence wrinkle — prefer restricting re-materialization to fragment locations whose owning worker's `subagent_runs.task_id` ∈ current-key set)
  </read_first>
  <files>backend/agents/execution_engine/engine.py, backend/tests/agents/test_restart_resume.py</files>
  <behavior>
    - For a wave step on resume-after-edit, the reconciler computes the current-key allow-set (keys of the current max-version task list) and passes it to `_rematerialize_artifacts_to_disk`.
    - A fragment whose owning worker's `subagent_runs.task_id` (the key) is NOT in the current-key allow-set (a completed key absent from the current list = orphan) is NOT restored to disk → it reaches NEITHER `_merge_fragments` NOR the terminal `serialized_sandbox` assembly (both read only disk). Merge/assembly implementations are UNTOUCHED.
    - Non-orphan fragments (current, completed) ARE restored (skip still works). Rows are never deleted (immutability). Default None allow-set preserves today's behavior (dormant on non-edited resume).
  </behavior>
  <action>
    RED FIRST: add `test_wave_reconcile_orphan_excluded` to `test_restart_resume.py` — seed a
    wave step's completed subagent_runs + fragment `file_bundle` refs where ONE completed worker's
    key is absent from the current (edited) task list; supply the current-key allow-set to
    re-materialization; assert the orphan fragment's location is NOT written to the sandbox while
    the surviving fragment IS. Run → RED. Then wire the wave step's current-key allow-set through
    the `_is_resume`-gated re-materialization call so orphaned fragment locations are skipped
    (join fragments→key via `subagent_runs` worker_index↔task_id per RESEARCH — the exclusion
    point is HIGH confidence, the join is the MEDIUM wrinkle; pick the simplest correct join).
    Do NOT edit `_merge_fragments` or the `serialized_sandbox` resolver (zero merge edits — the
    exclusion is entirely read-side at the re-materialization gate). Do NOT delete/mutate any row
    (T-48-04). Cite RESUME-16.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_restart_resume.py -k "reconcile_orphan_excluded or wave" tests/agents/test_wave_scheduler.py tests/agents/test_subagent_runs.py -q</automated>
  </verify>
  <done>Orphan fragment excluded from disk (hence merge + assembly) GREEN; surviving fragment restored; merge/assembly impls untouched; wave 10 + subagent 18 unchanged; no row deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RESUME-16 — update_specs rotation composes (proof, no new production code)</name>
  <read_first>
    - backend/agents/execution_engine/engine.py:4585-4700 — `_run_spec_revision_sub_pipeline` (update_specs re-runs specify→plan→analyze, minting new spec/task_list versions); :2986-2989 `_gate_update_specs` sets `spec_revision_pending_output`
    - backend/agents/execution_engine/engine.py — `_compute_upstream_context_hash` (from 48-01): the build step consumes spec/plan; a new spec version rotates its upstream digest → all task_keys rotate
    - 48-RESEARCH §"update_specs Composition (Phase 27)" (the proof: new spec version → new content_hash → upstream rotates → every task_key rotates → all build tasks re-run; add a test)
  </read_first>
  <files>backend/tests/agents/test_restart_resume.py</files>
  <behavior>
    - Seed a completed build under spec-v1 keys (task_ids = keys computed against spec-v1's upstream hash). Bump the spec to v2 (a new `spec` version with different content_hash), so `_compute_upstream_context_hash` for the build step rotates.
    - Re-parse the build task list + recompute current keys under the v2 upstream hash: NONE of the v1 completed keys appear in the current-key set → the reconciler skips nothing → every build task re-runs. Assert no v1-key is in the skip set and all current tasks dispatch.
    - This must hold with NO new production code — the rotation is a property of the substrate (48-01 upstream-namespacing).
  </behavior>
  <action>
    Add `test_update_specs_reconcile_composes` to `test_restart_resume.py` per RESEARCH
    §update_specs Composition: seed the completed spec-v1 build, mint a spec-v2 version (rotating
    the upstream hash), and assert (a) every current build task re-runs and (b) no v1 completed
    key is skipped. This is a PROOF/regression test — it should pass GREEN once 48-01/48-02 land
    (the composition falls out of upstream-namespacing). If it fails, the gap is in the upstream
    hash rotation or the reconciler wiring — gap-close there (do NOT add a bespoke update_specs
    branch — INV-12; the rotation must be generic). Cite RESUME-16.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_restart_resume.py -k "update_specs_reconcile_composes" -q</automated>
  </verify>
  <done>update_specs compose test GREEN — spec-v2 rotates all build keys, every task re-runs, no v1-key skipped; no bespoke update_specs branch added (rotation is generic).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC-001 synthetic non-prototype reconcile proof</name>
  <read_first>
    - backend/tests/agents/test_sc001_nonprototype_task_loop.py (full file) — the SC-001 idiom: fixture at a REAL home `agents/workflows/<synthetic>/` (or reuse `tests/agents/fixtures/sc001_task_loop/`), per-agent scripted models, `compile_for_run`/`get_pipeline_agents` pointed at the fixture, planner/store/review-gate neutralised, the two-part acceptance (registry.is_registered all True + `grep -rc "<name>" agents/execution_engine/ == 0`)
    - backend/tests/agents/test_sc001_fanout.py:1-55 — the sibling fan-out idiom
    - backend/tests/agents/test_restart_resume.py:1449-1490 — the seed-durable idiom (`_make_session`, `_seed_workflow_run`, `ScopedStore`, `_seed_ref`, `record_subagent_run`) for driving the reconcile
    - 48-RESEARCH §"Validation Architecture" (the SC-001 row) + Pitfall 8 (name-freedom; the drive script: complete 2 of 4 → gate-edit list: delete one completed + edit one pending + add one → resume → exact re-run set + orphan excluded from context/assembly)
  </read_first>
  <files>backend/tests/agents/test_sc001_reconcile.py</files>
  <behavior>
    - A NON-prototype `task_loop` workflow (deliverable NOT `prototype.html`; task/spec source steps DECLARED, not the `prototype-plan`/`prototype-specify` literals) using ONLY already-registered capabilities runs OFFLINE end-to-end.
    - Drive: complete 2 of 4 tasks → gate-edit the task list (delete one COMPLETED task, edit one PENDING task, add one NEW task) → resume.
    - Assert the reconciler re-runs EXACTLY the right set (the edited/new/divergence-onward tasks under the cumulative rule; completed-and-still-present skipped) and the DELETED completed task's artifact is EXCLUDED from context injection and from the assembled deliverable.
    - Two-part SC-001 acceptance: (a) every capability the fixture uses `registry.is_registered(...)` is True; (b) `grep -rc "<synthetic-name>" backend/agents/execution_engine/` == 0 (the kernel is workflow-agnostic in NAME).
  </behavior>
  <action>
    Create `tests/agents/test_sc001_reconcile.py` cloning the `test_sc001_nonprototype_task_loop.py`
    scaffolding (reuse `fixtures/sc001_task_loop/` or add a minimal `sc001_reconcile` fixture — pick
    the simplest that proves the path). Drive the complete-2-of-4 → gate-edit (delete-completed +
    edit-pending + add) → resume ladder per Pitfall 8, asserting the exact re-run set + orphan
    exclusion from BOTH context injection and assembly. Include the two-part SC-001 acceptance
    (registry.is_registered + the engine-directory grep == 0). NO production code changes in this
    task — it is the SC4 proof that the whole 48-01/48-02/48-03 reconcile path needs ZERO engine
    name-literals. Cite RESUME-16 / SC-001.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_sc001_reconcile.py tests/agents/test_sc001_nonprototype_task_loop.py tests/agents/test_banned_patterns.py -q &amp;&amp; grep -cE 'pipeline_type ==|spec.id ==' agents/execution_engine/engine.py</automated>
  </verify>
  <done>Synthetic reconcile proof GREEN (exact re-run set + orphan excluded from context/assembly); registry.is_registered all True; engine-directory name grep == 0; banned_patterns + existing sc001 suite still green; INV-1 grep prints 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable fragments → assembled deliverable | orphaned worker fragments must not reach merge/assembly on a resume-after-edit |
| synthetic workflow → kernel | proves no workflow/agent name literal is required anywhere in the reconcile path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Integrity / Repudiation | orphan-fragment exclusion vs immutable rows | mitigate | Exclusion is READ-side only at the re-materialization gate — an orphaned fragment is simply not restored to disk (never merged/assembled); its `artifact_refs`/`file_bundle` row is never deleted or mutated (immutability contract); history preserved |
| T-48-02 | Information Disclosure | current-key allow-set + subagent_runs join reads | mitigate | The allow-set and the fragment→key join read the SAME owner-scoped `ectx.scoped_store` rows (default-deny); no cross-owner fragment can enter the allow-set or reach disk |
| T-48-01 | Tampering | update_specs rotation | mitigate | Rotation is driven by the canonical-JSON upstream-context-hash (48-01) — a new spec content_hash deterministically rotates every affected key; a stale-spec task can never be spuriously skipped |
| T-48-05 | DoS / data-loss | orphan-join ambiguity | mitigate | Fail-safe = restore/re-run on ambiguity — an unmappable fragment defaults to the existing global-max restore (worst case a wasteful but CORRECT re-run), never a wrong exclusion of live work |
| T-48-SC | Tampering (supply chain) | dependencies | accept | Zero new packages — tests use existing offline scripted-model scaffolding; no npm/pip/cargo install; no package-legitimacy gate applies |
</threat_model>

<verification>
Run from `backend/` (ABSOLUTE cd; python3.11 no venv; NO live Bedrock):

1. RESUME-16 independent: `python3.11 -m pytest tests/agents/test_restart_resume.py -k "reconcile_orphan_excluded or update_specs_reconcile_composes" -q` → green (orphan excluded from disk/merge/assembly; update_specs rotation composes).
2. SC-001 proof: `python3.11 -m pytest tests/agents/test_sc001_reconcile.py tests/agents/test_sc001_nonprototype_task_loop.py tests/agents/test_sc001_fanout.py tests/agents/test_banned_patterns.py -q` → green; `grep -rc "<synthetic-name>" agents/execution_engine/` == 0.
3. Delta floor (unchanged): `python3.11 -m pytest tests/agents/test_restart_resume.py tests/agents/test_wave_scheduler.py tests/agents/test_subagent_runs.py tests/agents/test_per_task_capture.py tests/agents/test_json_tasks.py -q` → KAN-88 remains the SOLE pre-existing red; wave 10, subagent 18, per_task_capture 4, json 12.
4. Goldens (INV-3): `SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_*.py -q` → **10 passed** (all reconcile work dormant on non-resume/non-edit runs).
5. redo_gate_safety held: `python3.11 -m pytest tests/agents/test_redo_gate_safety.py -q` → pre-existing **4 passed / 3 failed** (untouched).
6. INV-1 + INV-12 + imports: `grep -cE 'pipeline_type ==|spec.id ==' agents/execution_engine/engine.py` → **0**; `grep -c 'def _compute_upstream_context_hash' agents/execution_engine/engine.py` → **1**; `/opt/homebrew/bin/lint-imports` → **4 kept / 0 broken**.
</verification>

<success_criteria>
- RESUME-16 independent delivered: wave per-key skip (falls out of 48-01) + orphan-fragment exclusion at the re-materialization gate; merge/assembly impls untouched; no row deleted.
- update_specs composes: a spec-v2 edit rotates all build keys and every build task re-runs, with no bespoke update_specs branch (rotation generic — INV-12).
- SC-001 proven: a synthetic non-prototype workflow drives the full complete→edit→resume reconcile with exact re-run set + orphan exclusion and ZERO engine name-literals (registry.is_registered all True; engine-dir name grep 0).
- Goldens 10/10 (`SNAPSHOT_UPDATE` unset), INV-1 grep 0, single upstream-hash home (count 1), lint 4/0, redo held at 4/3, KAN-88 sole restart red untouched.
</success_criteria>

<output>
Create `.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-03-SUMMARY.md` when done.
</output>
