---
phase: 48-task-identity-mutable-list-reconciliation-r3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/task_identity.py
  - backend/agents/execution_engine/engine.py
  - backend/agents/execution_engine/kernel_services.py
  - backend/agents/capabilities/strategies/task_loop.py
  - backend/agents/capabilities/strategies/wave_scheduler.py
  - backend/tests/agents/test_task_identity.py
  - backend/tests/agents/test_restart_resume.py
autonomous: true
requirements: [RESUME-14]
must_haves:
  truths:
    - "task_key = sha256(upstream_context_hash · normalized_task_content · occurrence_ordinal) is reorder-safe, insert-safe, duplicate-text-safe, and upstream-aware (RESUME-14)"
    - "The upstream-context-hash has exactly ONE implementation home, reused by input_hash AND the task key (INV-12)"
    - "Task-granular durable writes carry the task_key in the existing task_id slots; goldens stay byte/event-identical (INV-3)"
    - "Legacy positional rows remain readable and can never be mistaken for a key (fail-safe = re-run)"
  artifacts:
    - path: "backend/agents/capabilities/task_identity.py"
      provides: "Pure normalize/ordinal/compute_task_key functions (stdlib-only, capability-package sibling)"
      contains: "def compute_task_key"
    - path: "backend/tests/agents/test_task_identity.py"
      provides: "Pure key/normalize/ordinal unit cases (reorder/insert/dup/upstream-rotate/cross-restart)"
      contains: "def test_"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "_compute_upstream_context_hash"
      via: "_compute_step_input_hash calls the extracted helper (one home)"
      pattern: "_compute_upstream_context_hash"
    - from: "backend/agents/capabilities/strategies/task_loop.py"
      to: "backend/agents/capabilities/task_identity.py"
      via: "compute_task_key threaded into persist_task_html task_key kwarg"
      pattern: "compute_task_key|task_key="
---

<objective>
Establish content-addressed task identity (RESUME-14): a new pure module
`agents/capabilities/task_identity.py` computing
`task_key = sha256(upstream_context_hash · normalized_task_content · occurrence_ordinal)`,
factor the upstream-context-hash out of `_compute_step_input_hash` into ONE reusable engine
helper (INV-12), and switch the three physical write sites that stamp the `task_id` slot from
POSITIONAL values to keys — proven byte/event-neutral on goldens.

Purpose: This is the identity substrate the reconciler (48-02/48-03) keys on. Without a stable,
upstream-namespaced, duplicate-safe key, a user-edited task list cannot reconcile cleanly on
resume or re-run-after-edit. Per RESUME-14 the hash discipline is inherited from `input_hash`
(sorted inputs, no timestamp/uuid → cross-restart stable).

Output: `task_identity.py` (pure), `_compute_upstream_context_hash` engine helper + a
`runner.upstream_context_hash(step)` handle, key writes at `persist_task_html` (both branches)
and wave `requests[].task_id`, and updated identity-contract tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-CONTEXT.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-RESEARCH.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-PATTERNS.md
@.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-VALIDATION.md

# Analogs to copy (read the exact quoted regions in PATTERNS §1, §2, §3)
@backend/agents/capabilities/model_pricing.py
@backend/agents/capabilities/strategies/task_loop.py
@backend/agents/capabilities/strategies/wave_scheduler.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pure task_identity module (normalize · ordinals · compute_task_key)</name>
  <read_first>
    - backend/agents/capabilities/model_pricing.py (lines 1-32) — the pure capability-package module-header + "Scope guards" discipline to copy verbatim-in-spirit (PATTERNS §1 Analog A)
    - backend/agents/capabilities/validators/severity.py (lines 1-16) — the "one home, everyone imports, never re-derives" doctrine (PATTERNS §1 Analog B)
    - backend/agents/execution_engine/engine.py:5852-5857 — the canonical-JSON hash discipline to mirror (sort_keys=True, separators=(",",":"), sha256().hexdigest())
    - backend/agents/capabilities/task_parsers/heading_tasks.py:58-122 — heading `Task.body` INCLUDES the `## Task N:` header line (why normalization must strip the ordinal token); json_tasks.py:69-146 for the json field shape
  </read_first>
  <files>backend/tests/agents/test_task_identity.py, backend/agents/capabilities/task_identity.py</files>
  <behavior>
    - normalize_task_content: strips a leading `^##\s+Task\s+\d+\b:?` ordinal token from the first line (keeps the title text after the colon), then NFC-normalizes, converts `\r\n`→`\n`, and collapses whitespace via `re.sub(r"\s+"," ",text).strip()`. Reordering a heading task (`## Task 3:` vs `## Task 1:` with identical title/body) yields IDENTICAL normalized content. Editing the task body changes it.
    - occurrence_ordinals(tasks): returns per-index the 0-based count of PRIOR tasks whose normalize_task_content equals this one (two identical "Fix styling" tasks → ordinals 0 and 1). Left-to-right over the current list; used ONLY for tie-breaking identical text.
    - compute_task_key(upstream_context_hash, normalized_content, ordinal): `sha256(json.dumps({"u":..,"c":..,"o":..}, sort_keys=True, separators=(",",":")).encode("utf-8")).hexdigest()` — a 64-char hex string. Deterministic across processes (no `hash()`, no timestamp/uuid). Same triple → same key; any of the three changing → different key.
    - Cross-restart stability: computing the key twice for the same inputs (fresh call) is byte-equal; a positional value like "1" can never equal a produced key (length + charset).
  </behavior>
  <action>
    RED FIRST: write `tests/agents/test_task_identity.py` with cases covering every bullet in
    the behavior block — reorder-stable normalization, ordinal-strip for heading bodies, edit
    sensitivity, occurrence-ordinal duplicate disambiguation, canonical-JSON determinism,
    key≠positional. Run it and SEE it fail (module import error) before writing the module.
    Then create `agents/capabilities/task_identity.py` as a PURE stdlib module (`from __future__
    import annotations`; imports limited to `hashlib`, `json`, `re`, `unicodedata`, `logging`).
    Copy the model_pricing "Scope guards" header discipline (MUST NOT import `app.*`,
    `agents.execution_engine`, or `agents.workflows` — the kernel/strategies import THIS module,
    never the reverse). Implement `normalize_task_content`, `occurrence_ordinals`,
    `compute_task_key` exactly per RESEARCH §"Normalization & Ordinals" and PATTERNS §1.
    Pitfall 1 (cross-restart stability) and Pitfall 2 (heading ordinal in body) are the two
    highest-risk normalization bugs — the RED cases must pin both. Do NOT reference any
    workflow/agent name literal (SC-001/INV-1). This is RESUME-14's identity core.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_task_identity.py -q</automated>
  </verify>
  <done>test_task_identity.py passes GREEN; the module is pure (no `app`/`execution_engine`/`workflows`/`hash()`/timestamp imports); reorder-stable + edit-sensitive + ordinal-disambiguated + cross-restart-deterministic all proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract _compute_upstream_context_hash (one home, INV-12) + runner handle</name>
  <read_first>
    - backend/agents/execution_engine/engine.py:5799-5857 — `_compute_step_input_hash`; the upstream half to LIFT is 5819-5846 (spec/ordered_agents resolution + the upstream_hashes build), sorted-hash canonical at 5852-5856
    - backend/agents/execution_engine/kernel_services.py:1141-1172 — the runner-handle method shape (e.g. `latest_typed_content`); §"KernelServices public methods" — this is where a new `upstream_context_hash(step)` handle lands so strategies reach it via `ctx.runner`
    - backend/agents/capabilities/strategies/task_loop.py:190-210 and wave_scheduler.py:169-195 — where each strategy has `step`, `ctx`, `runner` in scope (per-step, before the task loop)
  </read_first>
  <files>backend/agents/execution_engine/engine.py, backend/agents/execution_engine/kernel_services.py, backend/tests/agents/test_restart_resume.py</files>
  <behavior>
    - `_compute_upstream_context_hash(self, step, ectx) -> str` contains the spec/ordered_agents resolution + the upstream_hashes build (lifted from 5819-5846) + `sha256(json.dumps({"upstream": sorted(upstream_hashes)}, sort_keys=True, separators=(",",":")).encode()).hexdigest()`.
    - `_compute_step_input_hash` is rewritten to CALL the new helper for its upstream half, then fold the `input`/`task_block` into its own canonical payload — the upstream scan appears EXACTLY ONCE in the file (no second scheme).
    - The input_hash value for a given (step, ectx) is UNCHANGED versus HEAD (step-reuse keys stay stable → goldens neutral).
    - `runner.upstream_context_hash(step)` returns the same digest the engine helper produces (the handle delegates to `self._engine._compute_upstream_context_hash(step, self._ectx)`).
  </behavior>
  <action>
    RED FIRST: add a unit case to `test_restart_resume.py` (e.g. `test_upstream_context_hash_single_home_stable`) that (a) asserts the engine exposes `_compute_upstream_context_hash` and it returns a stable 64-char hex for a seeded ectx, and (b) asserts `runner.upstream_context_hash(step)` equals the engine helper's output. Run it → RED (AttributeError). Then factor engine.py:5819-5846 into `_compute_upstream_context_hash(self, step, ectx)` per PATTERNS §2 (Copy/deviate delta), rewrite `_compute_step_input_hash` to call it (Pitfall 6 / INV-12 — do NOT leave a second upstream scan), and add the `upstream_context_hash(step)` handle to KernelServices delegating to the engine. The upstream hash is PER-STEP (identical for every task in the step) — the handle is called ONCE per `strategy.run`. Keep the owner-scoped `ectx.artifacts.tree(run_id)` read exactly as-is (no scope widening — T-48-02). This closes the INV-12 "one home" discretion item.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_restart_resume.py -k upstream_context_hash -q &amp;&amp; SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_ppt.py tests/agents/test_characterization_user_stories.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_code_gen.py -q</automated>
  </verify>
  <done>The new unit case passes; goldens stay 10/10 (`SNAPSHOT_UPDATE` unset) proving input_hash unchanged; the upstream scan is single-home (INV-12 enumerate-pin 1 — see verification section).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write-path switch (positions→keys) + identity-contract test updates + neutrality proof</name>
  <read_first>
    - backend/agents/execution_engine/kernel_services.py:1321-1462 — `persist_task_html`; branch-1 write task_id=str(task_num) at :1384, branch-2 sibling ArtifactRef task_id=str(task_num) at :1442; :750 `write_fragment_artifact` task_id=str(worker_index) — LEAVE UNCHANGED (PATTERNS §3 Site C)
    - backend/agents/capabilities/strategies/task_loop.py:246-319 — the per-task loop + the `persist_task_html(task_num, agent_id=agent_id, filename=filename)` call at :316-319
    - backend/agents/capabilities/strategies/wave_scheduler.py:263-271 — the `requests`/`task_ids` build; PATTERNS §3 Site B (keep `t.id` for build_waves/depends_on/dup-guard — Pitfall 7)
    - backend/tests/agents/test_restart_resume.py:1288-1490 — the three identity-contract tests that MUST update: `test_task_loop_skips_completed_tasks_on_resume_cursor` (seeds {"1","2"} at :1314), `test_wave_scheduler_skips_completed_workers_on_resume_cursor` (:1393), `test_kernel_computes_resume_completed_task_ids_cursor` (seeds task_id="1"/"2" at :1459-1467)
    - 48-RESEARCH §"Test-Contract Changes" + §"Golden neutrality — PROVEN" — the exact update-vs-untouched split and the grep proof that task_id is in NO event payload
  </read_first>
  <files>backend/agents/execution_engine/kernel_services.py, backend/agents/capabilities/strategies/task_loop.py, backend/agents/capabilities/strategies/wave_scheduler.py, backend/tests/agents/test_restart_resume.py</files>
  <behavior>
    - task_loop computes `keys[i] = compute_task_key(runner.upstream_context_hash(step), normalize_task_content(tasks[i]), ordinals[i])` once after parse; threads `task_key=keys[task_num-1]` into `persist_task_html`, which writes it into BOTH :1384 and :1442 task_id slots.
    - wave_scheduler builds `key_by_id = {t.id: compute_task_key(upstream, normalize, ordinal) for t in tasks}` once after parse; emits `"task_id": key_by_id[t.id]` on each request (:270) and the skip compares `key_by_id[t.id]` (Pitfall 7: `build_waves`/`depends_on`/dup-guard keep reading `t.id`).
    - The three identity-contract tests seed/assert KEYS instead of positions (behavior preserved: skip completed, run rest — values become keys).
    - Goldens byte/event-identical (task_id in no event payload); INV-1 grep stays 0; lint 4/0; `write_fragment_artifact` unchanged.
  </behavior>
  <action>
    RED FIRST: update the three identity-contract tests named in read_first to seed keys (or a
    key-aware companion) per RESEARCH §"MUST update". Run them → they exercise the new write
    path; where the strategy/cursor still writes positions they fail. Then: thread a keyword
    `task_key: str` into `persist_task_html(self, task_num, *, agent_id, filename, task_key)`
    and write it into both :1384 and :1442 slots (leave branch-2 content_hash dedup and the
    `.uploads/` fence untouched). In task_loop, compute the per-task keys after parse
    (task_loop.py:210) using `runner.upstream_context_hash(step)` + `task_identity`
    functions, and pass `task_key=` at the persist call (:316-319). In wave_scheduler, build
    `key_by_id` after parse (:185) and switch the request `task_id` + the skip comparison to the
    wrapped key (KEEP `t.id` everywhere in `build_waves`/DAG/dup-guard — Pitfall 7). LEAVE
    `write_fragment_artifact` (:750) alone (documented, scope-minimal — the wave cursor reads
    `subagent_runs.task_id`, now the key via fanout.py:388, NOT fragment task_id). Backward-compat
    fail-safe: a 64-char key never equals a legacy positional "1" → legacy rows re-run and are
    superseded by versioning; do NOT migrate or mutate old rows (Pitfall 5). Then prove
    golden-neutrality: goldens 10/10 with `SNAPSHOT_UPDATE` unset. This completes RESUME-14.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_restart_resume.py tests/agents/test_per_task_capture.py tests/agents/test_wave_scheduler.py tests/agents/test_json_tasks.py tests/agents/test_subagent_runs.py -q &amp;&amp; SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_ppt.py tests/agents/test_characterization_user_stories.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_code_gen.py -q &amp;&amp; grep -cE 'pipeline_type ==|spec.id ==' agents/execution_engine/engine.py</automated>
  </verify>
  <done>Contract tests GREEN with keys; goldens 10/10 (`SNAPSHOT_UPDATE` unset); INV-1 grep prints 0; per_task_capture 4, wave 10, json 12, subagent 18 unchanged; `write_fragment_artifact` untouched; lint 4/0 (see verification).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable store → reconciler read | `artifact_refs`/`subagent_runs` rows read on resume; must stay owner-scoped |
| task content → key computation | parsed task text + upstream content_hashes feed the sha256 key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | `task_identity.compute_task_key` | mitigate | Canonical-JSON sha256 (sort_keys, separators), NFC, ordinal disambiguation, no `hash()`/timestamp/uuid — cross-restart stable (Pitfall 1); no key collision across reorder/duplicate |
| T-48-02 | Information Disclosure | `_compute_upstream_context_hash` durable tree read | mitigate | Reads the SAME owner-scoped `ectx.artifacts.tree(run_id)` as the existing input_hash — no scope widening; a foreign row can never enter the digest |
| T-48-05 | DoS / data-loss | legacy positional ↔ key coexistence | mitigate | Fail-safe = re-run: a 64-char key never equals a positional `"1"` → unmappable legacy row re-runs and is superseded via versioning; no row mutation/migration (Pitfall 5) |
| T-48-SC | Tampering (supply chain) | dependencies | accept | Zero new packages — `task_identity.py` is pure stdlib (`hashlib`/`json`/`re`/`unicodedata`); no npm/pip/cargo install in this plan, so no package-legitimacy gate applies |
</threat_model>

<verification>
Run from `backend/` (ABSOLUTE cd; python3.11 no venv; NO live Bedrock):

1. Pure module: `python3.11 -m pytest tests/agents/test_task_identity.py -q` → all green.
2. Delta floor (must equal the 48-VALIDATION baseline): `python3.11 -m pytest tests/agents/test_restart_resume.py tests/agents/test_per_task_capture.py tests/agents/test_wave_scheduler.py tests/agents/test_json_tasks.py tests/agents/test_subagent_runs.py -q` → restart_resume holds at its baseline (KAN-88 `test_waiting_for_user_run_is_rearmed_not_driven` remains the SOLE pre-existing red — untouched), per_task_capture 4, wave 10, json 12, subagent 18.
3. Goldens (INV-3): `SNAPSHOT_UPDATE= python3.11 -m pytest tests/agents/test_characterization_*.py -q` → **10 passed**, `SNAPSHOT_UPDATE` MUST be unset.
4. INV-1: `grep -cE 'pipeline_type ==|spec.id ==' agents/execution_engine/engine.py` → **0**.
5. INV-12 enumerate-pin 1: `grep -c 'def _compute_upstream_context_hash' agents/execution_engine/engine.py` → **1**; and the upstream-scan token appears once — `grep -c 'set(getattr(upstream, "produces", \[\])) &amp; consumes' agents/execution_engine/engine.py` → **1** (the scan lives ONLY in the extracted helper).
6. Imports: `/opt/homebrew/bin/lint-imports` → **4 kept / 0 broken** (task_identity.py must not import the engine/app — the contract at pyproject.toml:165-169).
7. redo_gate_safety untouched by this plan: `python3.11 -m pytest tests/agents/test_redo_gate_safety.py -q` → hold at pre-existing **4 passed / 3 failed** (do NOT fix/delete/re-enable — the 3 reds are scripted-model harness drift, not this phase).
</verification>

<success_criteria>
- RESUME-14 delivered: `task_key = sha256(upstream · normalized · ordinal)` — reorder/insert/duplicate/upstream-rotate correct, cross-restart stable.
- Single upstream-hash home (INV-12): `_compute_upstream_context_hash` extracted, `_compute_step_input_hash` calls it, `runner.upstream_context_hash(step)` delegates, upstream scan count == 1.
- Write-path switched at `persist_task_html` (:1384 + :1442) and wave `requests[].task_id` (:270); `write_fragment_artifact` and `build_waves`/`t.id` untouched.
- Golden-neutral (10/10, `SNAPSHOT_UPDATE` unset), INV-1 grep 0, lint 4/0, redo held at 4/3, KAN-88 sole restart red untouched.
- The three identity-contract tests updated to keys (contract change, not weakening); the byte-neutral suites (per_task_capture, wave, json, subagent) unchanged.
</success_criteria>

<output>
Create `.planning/phases/48-task-identity-mutable-list-reconciliation-r3/48-01-SUMMARY.md` when done.
</output>
