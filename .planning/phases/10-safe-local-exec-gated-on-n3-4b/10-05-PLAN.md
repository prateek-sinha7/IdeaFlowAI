---
phase: 10-safe-local-exec-gated-on-n3-4b
plan: 05
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - backend/agents/capabilities/mcp_servers/catalog.py
  - backend/agents/capabilities/deliverables/repo_diff.py
  - backend/tests/agents/test_repo_diff.py
  - specs/003-workflow-engine-decoupling/migration-ledger.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: false
requirements: [EXEC-01, EXEC-02]
must_haves:
  truths:
    - "The IN-01 MCP-04 sign-off is documented near the slack_post user_allowed=True gating (post-only scope, same gate/audit path)"
    - "IN-03: repo_diff._split_per_file assigns a synthetic key to a block under an unparseable diff --git header so no changed file is silently dropped"
    - "A unit test with a quoted/rename diff --git header asserts the block appears under a synthetic key"
    - "The IN-02 shell=True closure is recorded as a migration-ledger ratchet row (grep shell=True over runtime+agents = 0)"
    - "The N3 threat-model blocker is flipped to resolved in STATE.md and PROJECT.md citing 10-SPEC.md"
    - "The STATE.md IN-01/02/03 pending-todo block is cleared (all three closed in Phase 10)"
    - "5 characterization snapshots are byte/event-identical with SNAPSHOT_UPDATE unset; lint-imports 4 kept/0 broken; banned-pattern + migration-ledger gates green"
    - "All 14 SPEC acceptance criteria are green across the phase"
  artifacts:
    - path: "backend/agents/capabilities/mcp_servers/catalog.py"
      provides: "IN-01 MCP-04 sign-off documentation near slack_post user_allowed=True"
      contains: "slack"
    - path: "backend/agents/capabilities/deliverables/repo_diff.py"
      provides: "IN-03 synthetic-key fallback in _split_per_file/_flush"
      contains: "_split_per_file"
    - path: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      provides: "IN-02 shell=True closure ratchet row"
      contains: "shell=True"
    - path: ".planning/STATE.md"
      provides: "N3 resolved + IN-01/02/03 pending-todo cleared"
      contains: "N3"
    - path: ".planning/PROJECT.md"
      provides: "N3 exec threat-model flipped from open to resolved"
      contains: "N3"
  key_links:
    - from: "backend/agents/capabilities/deliverables/repo_diff.py"
      to: "synthetic key for an unparseable header block"
      via: "_flush assigns __unparsed_N__ when current_path is None but current_lines exist"
      pattern: "__unparsed"
    - from: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      to: "shell=True ratchet (grep = 0 over runtime + agents)"
      via: "a ☑ grep row enforced by test_migration_ledger.py"
      pattern: "shell=True"
---

<objective>
Close the Phase 9 review trio and flip N3 to resolved (SPEC requirement 9: DEBT + PARITY).

- IN-01: document the MCP-04 sign-off near the `slack_post` `user_allowed=True` gating — `post_message` is the ONE user-grantable WRITE on the user palette, accepted because the scope is post-only and rides the same gate/audit path as every MCP call (comment-only, no behavior change).
- IN-02: subsumed by 10-01 (argv/no-shell). This plan records the closure as a migration-ledger ratchet row so `shell=True` can never reappear over `backend/app/agents/runtime/` + `backend/agents/` (the existing banned-pattern test does not scan `shell=True` — RESEARCH A5).
- IN-03: `repo_diff._split_per_file` falls back to a synthetic key (`__unparsed_N__`) for an unparseable/quoted/rename `diff --git` header so no changed file is silently dropped.
- N3 resolution record: STATE.md's N3 ⚠️ blocker row + the IN-01/02/03 pending-todo block flip to resolved/cleared, and PROJECT.md's open-decisions line drops N3 — citing 10-SPEC.md (this SPEC IS the N3 decision record).
- Phase parity gate: confirm the 5 characterization snapshots are byte/event-identical, lint-imports 4/0, banned-pattern + migration-ledger green, and all 14 SPEC acceptance criteria pass.

Purpose: closes EXEC-01/EXEC-02's DEBT+PARITY requirement and resolves the open N3 decision so the phase can transition.

Output: IN-01 sign-off doc, IN-03 synthetic-key fix + test, IN-02 ledger row, N3-resolved flips, and the green phase-gate parity proof.
</objective>

<artifacts_this_phase_produces>
Symbols/paths CREATED or MODIFIED by this plan (exclude created symbols from drift verification):
- CREATED: an IN-01 MCP-04 sign-off comment/docstring near `SlackMcpServer` `user_allowed=True` in `backend/agents/capabilities/mcp_servers/catalog.py`
- CREATED: the synthetic-key fallback branch in `backend/agents/capabilities/deliverables/repo_diff.py:_split_per_file._flush` (assigns `__unparsed_N__` when `current_path is None` but `current_lines` exist) + an idx counter
- CREATED: a new test case in `backend/tests/agents/test_repo_diff.py` (unparseable/quoted/rename header -> synthetic key)
- CREATED: a migration-ledger grep ratchet row for the IN-02 `shell=True` closure
- MODIFIED: STATE.md (N3 resolved, pending-todo cleared) + PROJECT.md (N3 dropped from open decisions) — planning docs
</artifacts_this_phase_produces>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-SPEC.md
@.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-RESEARCH.md
@.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-PATTERNS.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IN-01 sign-off doc + IN-03 repo_diff synthetic-key fallback + IN-02 ledger row</name>
  <files>backend/agents/capabilities/mcp_servers/catalog.py, backend/agents/capabilities/deliverables/repo_diff.py, backend/tests/agents/test_repo_diff.py, specs/003-workflow-engine-decoupling/migration-ledger.md</files>
  <read_first>
    - backend/agents/capabilities/mcp_servers/catalog.py (~line 86 the `SlackMcpServer` entry: `@register("mcp_server","slack", user_allowed=True)`, `exposed_tools = frozenset({"post_message","list_channels"})` — the IN-01 sign-off site)
    - backend/agents/capabilities/deliverables/repo_diff.py (~109-148 `_split_per_file` + the inner `_flush` reading `current_path`/`current_lines`; `_path_from_header` returning None for quoted/rename headers — the silent-drop bug)
    - backend/tests/agents/test_repo_diff.py (the existing repo_diff test style — how a unified diff string is fed and per-file output asserted)
    - .planning/phases/09-local-workspace-runtime-repo-workflows-no-exec-4a/09-REVIEW.md (§Info — the exact IN-01/IN-02/IN-03 finding text + fix guidance)
    - .planning/phases/10-safe-local-exec-gated-on-n3-4b/10-RESEARCH.md (§Code Examples: the IN-03 synthetic-key fallback + the IN-01 sign-off comment text; A5 — shell=True needs a ledger row, the banned-pattern test does not scan it)
    - .planning/phases/10-safe-local-exec-gated-on-n3-4b/10-PATTERNS.md (§catalog.py IN-01; §repo_diff.py IN-03)
    - specs/003-workflow-engine-decoupling/migration-ledger.md (~65-78 the table header + sample grep-row format; the ratchet rule — a ☑ grep row returns 0 matches in its scope)
  </read_first>
  <behavior>
    - Test (IN-03 quoted header): feed a unified diff whose `diff --git "a/has space.py" "b/has space.py"` header is unparseable by `_path_from_header` (returns None); assert `_split_per_file` returns the block under a synthetic `__unparsed_*` key (NOT dropped).
    - Test (IN-03 rename header): feed a rename-style header that `_path_from_header` cannot resolve; assert the block survives under a synthetic key.
    - Test (IN-03 parity — normal diff): a normal `diff --git a/x.py b/x.py` still keys under `x.py` (no synthetic key; existing behavior unchanged).
    - Test (IN-03 multiple unparseable): two unparseable blocks get distinct synthetic keys (no collision/overwrite).
  </behavior>
  <action>
    IN-01 (catalog.py): add a comment/docstring near the `SlackMcpServer` `user_allowed=True` line documenting the MCP-04 sign-off (RESEARCH §IN-01 example): `post_message` is the ONE user-grantable WRITE on the user palette, accepted because the scope is post-only (no read of other channels' history beyond `list_channels`) and it rides the same gate/audit path as every MCP call; reviewed in Phase 10. Comment-only — NO behavior change (`exposed_tools` unchanged). IN-03 (repo_diff.py): fix `_split_per_file._flush` so a block under an unparseable header is NOT dropped. Today `_flush` only writes when `current_path is not None`. Change it so when `current_lines` exist but `current_path is None`, it assigns a synthetic key `f"__unparsed_{idx}__"` with a per-call incrementing `idx` (so multiple unparseable blocks get distinct keys), then writes the block under that key (RESEARCH §IN-03 example — use a small mutable counter / nonlocal idx pattern). Keep the normal path (parseable `current_path`) byte-identical. Add the IN-03 test cases to `backend/tests/agents/test_repo_diff.py`. IN-02 (migration-ledger): add ONE grep ratchet row to `specs/003-workflow-engine-decoupling/migration-ledger.md` for the `shell=True` closure: Item = IN-02 shell=True exec surface; Legacy = `local.py exec_command shell=True`; New home = argv-list `shell=False` (app/agents/runtime/local.py); Phase = 10; Deletion gate = the verbatim grep pattern `shell=True` (scoped so the parser greps `backend/` — note in the row that the live exec surface is `backend/app/agents/runtime/` + `backend/agents/`); Status = ☑; Deleting SHA blank. The pattern must make `test_migration_ledger.py` grep for `shell=True` and assert 0 matches in the enforced scope.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_repo_diff.py tests/agents/test_migration_ledger.py -x -p no:cacheprovider && test $(grep -rn "shell=True" app/agents/runtime/ agents/ | grep -v '^#' | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - catalog.py contains the MCP-04 sign-off text near the slack `user_allowed=True` line; `exposed_tools` for slack is unchanged (no behavior change)
    - repo_diff.py `_split_per_file` assigns a `__unparsed_*` key when `current_path is None` and `current_lines` exist; the normal parseable path is unchanged
    - test_repo_diff.py exits 0: quoted/rename headers survive under synthetic keys; normal diffs key by path; multiple unparseable blocks get distinct keys
    - migration-ledger.md contains a ☑ row whose deletion gate is the `shell=True` grep pattern; test_migration_ledger.py passes (the row greps 0 matches in scope)
    - grep `shell=True` over backend/app/agents/runtime/ + backend/agents/ returns 0 (excluding comment lines)
  </acceptance_criteria>
  <done>IN-01 signed off, IN-03 no longer drops files under unparseable headers, IN-02 closure ratcheted — the Phase 9 review trio is closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 10 exec stack is complete across plans 10-01..10-05: hardened argv exec_command + audited exec_runs (10-01), trust-conditional compiler ceiling + INV-12 forward-surface deletion (10-02), profile-conditional security gate + approval HITL + §15 workspace wiring (10-03), code_compile/code_test/code_lint validators + EXEC-02 sample manifest (10-04), and the IN-01/02/03 closure (this plan, Task 1). This task flips N3 to resolved in STATE.md + PROJECT.md and runs the phase parity gate.
  </what-built>
  <how-to-verify>
    Before approving, the executor must FIRST apply the planning-doc flips, THEN run the parity gate and report results:
    1. STATE.md: flip the N3 ⚠️ blocker row (line ~187) to RESOLVED citing 10-SPEC.md as the decision record; CLEAR the IN-01/02/03 pending-todo block (line ~181) noting all three closed in Phase 10; update the Last-updated line.
    2. PROJECT.md: remove N3 from the "Open (confirm before phase)" decisions line (line ~123) and mark the Phase 4B safe-local-exec open-decision (line ~56) appropriately; the N3 ties-until lines (~75, ~99) may stay as historical context or be updated to "resolved Phase 10".
    3. Run the phase parity gate and confirm ALL green:
       - `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x -p no:cacheprovider` — 5 snapshots byte/event-identical with SNAPSHOT_UPDATE unset
       - `/opt/homebrew/bin/lint-imports` — 4 kept / 0 broken
       - `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -x -p no:cacheprovider` — green
       - `cd backend && python3.11 -m pytest tests/agents/test_local_runtime.py tests/agents/test_exec_runs.py tests/agents/test_compiler_trust.py tests/agents/test_gates.py tests/agents/test_code_validators.py tests/agents/test_repo_diff.py -x -p no:cacheprovider` — green (the phase's behavior suite)
    4. Walk the 14 SPEC acceptance criteria (10-SPEC.md §Acceptance Criteria) and confirm each maps to a green test/grep.
    Report: the snapshot diff result, lint-imports count, the phase suite result, and any of the 14 criteria not yet provably green.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 snapshots are byte/event-identical, lint-imports is 4/0, banned-pattern + migration-ledger are green, the phase suite passes, and all 14 SPEC criteria map to green checks — or describe the failing criterion/gate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user palette -> slack_post WRITE (MCP-04) | The one user-grantable write; sign-off documents the accepted post-only scope on the same gate/audit path |
| repo diff parser -> per-file output | An unparseable header must not silently drop a changed file (data-integrity boundary) |
| banned-pattern ratchet -> shell=True reintroduction | The ledger grep row is the permanent ratchet preventing IN-02 from recurring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Repudiation / Tampering | shell=True reintroduced after the IN-02 fix | mitigate | migration-ledger ☑ grep row for `shell=True` (test_migration_ledger.py asserts 0 matches) — permanent ratchet (per N3: argv-only, no shell) |
| T-10-05-02 | Information Disclosure / Tampering | a changed file silently dropped from a repo diff (IN-03) | mitigate | synthetic-key fallback ensures every block under an unparseable header is retained under `__unparsed_N__` |
| T-10-05-03 | Elevation of Privilege | the slack_post user-grantable write scope misunderstood/widened | mitigate | IN-01 sign-off documents the post-only scope + shared gate/audit path near the user_allowed gating; exposed_tools unchanged |
| T-10-05-04 | Tampering | the exec stack perturbing existing deterministic runs (parity break) | mitigate | the phase parity gate (5 snapshots byte/event-identical, lint 4/0, banned-pattern + ledger green) is a blocking-human checkpoint |
| T-10-05-SC | Tampering | npm/pip/cargo installs | mitigate | zero external packages (comment + parser fix + planning-doc edits); RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_repo_diff.py tests/agents/test_migration_ledger.py -x -p no:cacheprovider`
- `grep -rn "shell=True" backend/app/agents/runtime/ backend/agents/ | grep -v '^#'` returns nothing
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py tests/agents/test_banned_patterns.py -x -p no:cacheprovider`
- `/opt/homebrew/bin/lint-imports` (4 kept / 0 broken)
- the full phase behavior suite (local_runtime, exec_runs, compiler_trust, gates, code_validators, repo_diff) green
</verification>

<success_criteria>
- IN-01 sign-off present near slack_post; IN-03 synthetic-key fallback proven by a unit test; IN-02 closure ratcheted in the migration-ledger
- N3 flipped to resolved in STATE.md + PROJECT.md citing 10-SPEC.md; IN-01/02/03 pending-todo cleared
- 5 characterization snapshots byte/event-identical (SNAPSHOT_UPDATE unset); lint-imports 4/0; banned-pattern + migration-ledger green
- all 14 SPEC acceptance criteria green
</success_criteria>

<output>
Create `.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-05-SUMMARY.md` when done.
</output>
