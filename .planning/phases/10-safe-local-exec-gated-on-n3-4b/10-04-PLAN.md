---
phase: 10-safe-local-exec-gated-on-n3-4b
plan: 04
type: tdd
wave: 4
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - backend/agents/capabilities/validators/code_compile.py
  - backend/agents/capabilities/validators/code_test.py
  - backend/agents/capabilities/validators/code_lint.py
  - backend/tests/agents/test_registry_capabilities.py
  - backend/tests/agents/test_code_validators.py
  - backend/tests/agents/fixtures/sample_python_repo/
  - backend/tests/agents/fixtures/sample_exec_workflow/
autonomous: true
requirements: [EXEC-02]
must_haves:
  truths:
    - "code_compile, code_test, code_lint are registered Validator capabilities reachable by name"
    - "The capability registry count is 53 (was 50) and the lockstep drift-guard asserts == 53"
    - "code_compile + code_test PASS against the local Python fixture repo offline with zero edits under backend/agents/execution_engine/ (SC-001)"
    - "code_lint flags a seeded lint error as issues with severity mapped via the single map_severity"
    - "Each validator run writes validation_results rows via the handle"
    - "Under an exec-denied policy each code validator returns >=1 explicit refusal Issue, spawns zero processes, and lets no PermissionError escape (VALIDATOR-DENY)"
    - "An exec-granting sample manifest compiles (file trust + gates: [security, approval]) proving EXEC-02 offline"
    - "Validators reach exec only via target.runner/workspace handle and never import app.* (import-linter 4/0)"
  artifacts:
    - path: "backend/agents/capabilities/validators/code_compile.py"
      provides: "code_compile validator (python -m py_compile via the workspace handle)"
      contains: "code_compile"
    - path: "backend/agents/capabilities/validators/code_test.py"
      provides: "code_test validator (pytest -p no:cacheprovider via the handle, capped)"
      contains: "code_test"
    - path: "backend/agents/capabilities/validators/code_lint.py"
      provides: "code_lint validator (ruff check via the handle)"
      contains: "code_lint"
    - path: "backend/tests/agents/fixtures/sample_python_repo/"
      provides: "tiny compile+test fixture + a seeded lint error for code_lint"
      contains: ""
    - path: "backend/tests/agents/fixtures/sample_exec_workflow/"
      provides: "exec-granting sample manifest (file trust, gates: [security, approval], tools.exec: true) proving EXEC-02"
      contains: ""
  key_links:
    - from: "backend/agents/capabilities/validators/code_compile.py"
      to: "workspace.exec_command(argv) via target.runner"
      via: "the repo_diff._workspace handle reach (getattr(runner, 'workspace', None))"
      pattern: "exec_command"
    - from: "backend/agents/capabilities/validators/code_compile.py"
      to: "validation_results write"
      via: "target.runner.record_validation_result best-effort"
      pattern: "record_validation_result"
---

<objective>
Build the VALIDATOR layer (tier 3 of 3, the EXEC-02 payoff): the three code validators that drive gated exec, plus the fixture repo and the exec-granting sample manifest that prove the whole stack offline.

`code_compile` (`python -m py_compile`), `code_test` (`pytest`), and `code_lint` (`ruff check`) are kernel-side registered `Validator` capabilities modeled on `spec_plan_coverage.py`. They reach exec ONLY via the runner/workspace handle (`target.runner.workspace.exec_command(argv)`, the `repo_diff._workspace` reach) — never importing `app.*`, never spawning directly (the recorder audits + the policy enforces). When exec is ungranted, each returns an explicit refusal `Issue` without spawning (VALIDATOR-DENY). Registry membership grows 50->53 with the lockstep drift-guard bumped.

An exec-granting sample manifest (file trust, `gates: [security, approval]`, `tools.exec: true`) + a tiny `sample_python_repo` fixture prove EXEC-02: `code_compile` + `code_test` PASS offline with zero edits under `backend/agents/execution_engine/` (SC-001 discipline — a new workflow gains exec by manifest only).

Purpose: closes EXEC-02 (VALIDATORS + VALIDATOR-DENY).

Output: three registered code validators, the fixture repo, the exec-granting sample manifest, the bumped registry guard, and offline pass/deny proofs.
</objective>

<artifacts_this_phase_produces>
Symbols/paths CREATED by this plan (exclude from drift verification — they are new):
- File `backend/agents/capabilities/validators/code_compile.py` — class registered `("validator","code_compile", user_allowed=True)`
- File `backend/agents/capabilities/validators/code_test.py` — class registered `("validator","code_test", user_allowed=True)`
- File `backend/agents/capabilities/validators/code_lint.py` — class registered `("validator","code_lint", user_allowed=True)`
- Module-level `_workspace`/`_exec_granted`/`_target_py_files`/`_record`/`Issue` helpers in each validator (modeled on spec_plan_coverage + repo_diff)
- File `backend/tests/agents/test_code_validators.py` (new test file)
- New fixture dir `backend/tests/agents/fixtures/sample_python_repo/` (a compilable+testable module + test + a seeded lint error)
- New fixture dir `backend/tests/agents/fixtures/sample_exec_workflow/` (exec-granting manifest + AGENT.md, modeled on sc001_task_loop)
- Changed assertion in `backend/tests/agents/test_registry_capabilities.py` (`len(_KNOWN) == 50` -> `== 53`)
</artifacts_this_phase_produces>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-SPEC.md
@.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-RESEARCH.md
@.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-PATTERNS.md
@.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-01-PLAN.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: code_compile / code_test / code_lint validators + fixture repo + VALIDATOR-DENY + registry bump</name>
  <files>backend/agents/capabilities/validators/code_compile.py, backend/agents/capabilities/validators/code_test.py, backend/agents/capabilities/validators/code_lint.py, backend/tests/agents/fixtures/sample_python_repo/, backend/tests/agents/test_code_validators.py, backend/tests/agents/test_registry_capabilities.py</files>
  <read_first>
    - backend/agents/capabilities/validators/spec_plan_coverage.py (full — the kernel-side Validator template: imports `Validator` port + `register` + the single `map_severity`; the `@register("validator", name, user_allowed=True)` decorator; the `Issue` dataclass; the `_record(...)` -> `runner.record_validation_result(...)` best-effort writer)
    - backend/agents/capabilities/validators/severity.py (`map_severity` — the SINGLE P0-P3 -> CRITICAL/HIGH/MEDIUM/LOW source; never re-derive)
    - backend/agents/capabilities/deliverables/repo_diff.py (~90-105 `_workspace(ctx)` — the handle reach `getattr(runner, "workspace", None)` then `runner` itself; copy verbatim for `_workspace` in each validator)
    - backend/agents/capabilities/base.py (the `Validator` port contract) and kernel_services.py (~99-125 `DeliverableContext` — `target.runner`, `target.path`, `target.content`, `target.task_meta`; how validators find the run-root path)
    - backend/app/agents/runtime/local.py (the `LocalWorkspace.exec_command(argv)` signature + `LocalExecutionPolicy.allows("exec")` + the exec_allow list, all from 10-01 — what `_exec_granted(ws)` checks and what argv the validators pass)
    - backend/tests/agents/fixtures/sc001_task_loop/ + fixtures/sample_brownfield/ (the fixture-dir precedent for the NEW sample_python_repo)
    - backend/tests/agents/test_registry_capabilities.py (~line 132 `assert len(_KNOWN) == 50` — the lockstep drift-guard to bump to 53)
    - .planning/phases/10-safe-local-exec-gated-on-n3-4b/10-RESEARCH.md (§Pattern 3 the validator body verbatim; Pitfall 2 pytest-inside-pytest — tiny fixture, `-p no:cacheprovider`, capped; §VALIDATORS/VALIDATOR-DENY acceptance)
    - .planning/phases/10-safe-local-exec-gated-on-n3-4b/10-PATTERNS.md (§validators — copy verbatim list + the three deltas; argv targets per validator)
  </read_first>
  <behavior>
    - RED then GREEN per validator. Test (compile PASS): `code_compile` over `sample_python_repo` (all files valid) under an exec-granted workspace returns zero P0/P1 issues; writes a validation_results row.
    - Test (compile FAIL): a fixture file with a syntax error yields a P0 issue naming the file.
    - Test (test PASS): `code_test` runs the fixture's tiny passing test (pytest `-p no:cacheprovider`, capped by the policy) and returns no failure issues; writes a validation_results row.
    - Test (lint flags seeded error): `code_lint` over a seeded ruff violation returns >=1 issue whose severity came through `map_severity` (assert the severity label is one map_severity produces, not a locally-derived string).
    - Test (VALIDATOR-DENY compile): under an exec-denied policy (or `ws is None`), `code_compile.validate` returns >=1 refusal Issue naming the missing exec grant, spawns ZERO processes (patch subprocess.run to fail the test if reached, or assert via the recorder that no allowed/killed outcome occurred), and lets no `PermissionError` escape.
    - Test (VALIDATOR-DENY test): same assertions for `code_test`.
    - Test (VALIDATOR-DENY lint): same assertions for `code_lint`.
    - Test (registry membership): `("validator","code_compile")`, `("validator","code_test")`, `("validator","code_lint")` are all in `_KNOWN`; `len(_KNOWN) == 53`.
  </behavior>
  <action>
    Create the three validators in `backend/agents/capabilities/validators/`, each modeled VERBATIM on `spec_plan_coverage.py`: import `Validator` (port) + `register` + `map_severity` (single source); `@register("validator", "<name>", user_allowed=True)`; an `Issue` dataclass; a `_record(target, name, issues)` -> `target.runner.record_validation_result(...)` best-effort writer. Reach exec via a `_workspace(target.runner)` helper copied from `repo_diff._workspace` (`getattr(runner, "workspace", None)` then `runner`). Add `_exec_granted(ws)` checking `ws is not None and ws.policy.allows("exec")`. VALIDATOR-DENY: when `ws is None` or not `_exec_granted(ws)`, build a refusal `Issue` (mapped severity via map_severity, message naming the missing exec grant), `_record`, and RETURN — NO spawn (never `import subprocess` to spawn; reach exec only through `ws.exec_command(argv)`). argv per validator: `code_compile` -> `["python","-m","py_compile",*py_files]`; `code_test` -> `["pytest","-p","no:cacheprovider", <fixture-root>]`; `code_lint` -> `["ruff","check", <target>]`. Resolve target `.py` files via a stdlib glob over the run-root reached from `target` (do not hardcode paths). Wrap `ws.exec_command(...)` in try/except: `CalledProcessError`/non-zero output -> parse into P0/P1 `Issue`s; `PermissionError` -> a P1 refusal Issue (never escapes); `subprocess.TimeoutExpired` -> a P1 issue noting the cap. Map every severity through `map_severity` (VALID-03 — never a local ladder). Create `backend/tests/agents/fixtures/sample_python_repo/` (modeled on sc001_task_loop): one tiny importable module that compiles, one passing `test_*.py`, and a separate seeded file/line carrying a ruff violation for the lint test. Keep it tiny (Pitfall 2). Bump `backend/tests/agents/test_registry_capabilities.py` `assert len(_KNOWN) == 50` -> `== 53` (lockstep drift-guard). Write `backend/tests/agents/test_code_validators.py` covering all behaviors above; run validators' subprocesses bounded by the policy caps and pytest with `-p no:cacheprovider`; run via the targeted offline suite, never full pytest.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_code_validators.py tests/agents/test_registry_capabilities.py -x -p no:cacheprovider && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - the three validator files exist and each contains `@register("validator", "<name>", user_allowed=True)` for code_compile / code_test / code_lint
    - each validator imports `map_severity` from validators/severity.py and uses it (no local P0->CRITICAL dict)
    - no validator file contains `import subprocess` used to SPAWN (they reach exec only via `ws.exec_command`); grep over the three files for a direct `subprocess.run(`/`subprocess.Popen(` returns 0
    - test_code_validators.py exits 0: compile+test PASS on the fixture; lint flags the seeded error with a map_severity label; each validator under exec-denied returns >=1 refusal with zero spawns and no escaping PermissionError
    - test_registry_capabilities.py asserts `len(_KNOWN) == 53` and passes
    - lint-imports 4 kept / 0 broken (validators never import app.* or agents.execution_engine)
  </acceptance_criteria>
  <done>Three code validators land, reach exec only through the handle, refuse explicitly when ungranted, and prove compile+test PASS on the fixture; registry guard is 53.</done>
</task>

<task type="auto">
  <name>Task 2: exec-granting sample manifest proving EXEC-02 offline (SC-001)</name>
  <files>backend/tests/agents/fixtures/sample_exec_workflow/, backend/tests/agents/test_code_validators.py</files>
  <read_first>
    - backend/tests/agents/fixtures/sc001_task_loop/workflow.yaml + its AGENT.md files (the file-trust manifest precedent: how a manifest declares steps, strategy, gates, tools; the sc001 SC-001 proof shape — a non-prototype workflow replicating behavior by manifest only)
    - backend/tests/agents/test_sc001_nonprototype_task_loop.py (how an SC-001 fixture is compiled + asserted offline with zero engine edits)
    - backend/agents/workflows/compiler.py (the compile entry the test calls — what trust value to pass for file trust; the GRANT-PATH + D-01 behavior added in 10-02 that this manifest must satisfy: `tools.exec: true` requires `gates: [security, approval]`)
    - .planning/phases/10-safe-local-exec-gated-on-n3-4b/10-SPEC.md (requirement 7 VALIDATORS acceptance: compile+test validators PASS against the fixture with ZERO edits under backend/agents/execution_engine/ — SC-001 discipline; the Core Value: a custom workflow replicates by manifest + AGENT.md only)
    - .planning/phases/10-safe-local-exec-gated-on-n3-4b/10-PATTERNS.md (§test_code_validators fixture + sample-manifest precedent)
  </read_first>
  <behavior>
    - Test (manifest compiles): the exec-granting `sample_exec_workflow` manifest compiles at file trust to a `CompiledWorkflow` with the exec step's `tools.exec is True` and `security`+`approval` in its gates (proving GRANT-PATH + D-01 accept the legitimate authoring path).
    - Test (EXEC-02 end-to-end offline): compiling + running the exec step's validators (code_compile + code_test) against `sample_python_repo` under the exec-granted workspace PASSES with zero edits under `backend/agents/execution_engine/` (assert no engine-source modification is required — the capability + manifest carry it).
  </behavior>
  <action>
    Create `backend/tests/agents/fixtures/sample_exec_workflow/` modeled on `sc001_task_loop/`: a `workflow.yaml` (file-trust manifest) declaring a step that grants `tools: {exec: true}` and declares `gates: [security, approval]` and lists `validators: [code_compile, code_test]` (and optionally code_lint), plus the minimal AGENT.md file(s) the manifest references. The manifest MUST satisfy the 10-02 compiler rules (file trust so exec survives; both required gates present so D-01 passes). Add test cases to `backend/tests/agents/test_code_validators.py` (or a sibling test if cleaner): one asserting the manifest compiles to `Step.tools.exec is True` with security+approval gates; one asserting the EXEC-02 offline proof — the code validators PASS against `sample_python_repo` via the exec-granted workspace with ZERO edits under `backend/agents/execution_engine/` (this is the SC-001 discipline check: a brand-new exec-capable workflow is authored by manifest + AGENT.md only). Keep the fixture tiny and offline (Pitfall 2 — `-p no:cacheprovider`, capped). Do NOT add the manifest to any production registry/catalog — it is a test fixture proving the authoring path, not a shipped workflow.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_code_validators.py -x -p no:cacheprovider && git -C .. diff --quiet -- backend/agents/execution_engine/ && echo "SC-001: no engine edits"</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/agents/fixtures/sample_exec_workflow/workflow.yaml grants `tools.exec: true` and declares `gates: [security, approval]`
    - a test asserts the manifest compiles at file trust to `Step.tools.exec is True` with security+approval gates
    - a test proves EXEC-02 offline: code_compile + code_test PASS against sample_python_repo via the exec-granted workspace
    - SC-001 discipline holds: no edits under backend/agents/execution_engine/ are required for the exec-capable workflow (the verify command's git diff over that dir is clean for this plan's work)
  </acceptance_criteria>
  <done>A brand-new exec-granting workflow is authored by manifest + AGENT.md only and its compile/test validators pass offline — EXEC-02 proven, SC-001 held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validator capability -> workspace.exec_command | Validators reach process spawn only through the handle so the policy enforces allow/deny and the recorder audits |
| ungranted validator run -> refusal vs spawn | An exec-denied validator must refuse explicitly, never spawn, never crash (VALIDATOR-DENY) |
| test fixture manifest -> compiler | The exec-granting manifest exercises GRANT-PATH + D-01; it must not weaken the production palette |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Elevation of Privilege | a validator spawning subprocess directly (bypassing policy + audit) | mitigate | validators reach exec ONLY via `ws.exec_command`; grep for direct `subprocess.run(`/`Popen(` in the three files returns 0; import-linter keeps them off app.* |
| T-10-04-02 | Tampering | a validator silently passing when exec is ungranted (false green) | mitigate | VALIDATOR-DENY: explicit refusal Issue, zero spawns, no PermissionError escape — never a silent pass |
| T-10-04-03 | DoS | pytest-inside-pytest hang / runaway fixture | mitigate | tiny fixture; `pytest -p no:cacheprovider`; bounded by the 10-01 policy caps (cpu=60s, wall=120s) |
| T-10-04-04 | Information Disclosure | the sample manifest leaking into a shipped/user-grantable palette | accept | fixture lives under tests/agents/fixtures only; exec validators are engineer-authored (user_allowed on the validator is the registry default for kernel validators; exec itself stays gate+trust-bound) |
| T-10-04-SC | Tampering | npm/pip/cargo installs | mitigate | zero external packages (ruff/pytest already installed offline, py_compile stdlib); RESEARCH Package Legitimacy Audit: N/A |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_code_validators.py tests/agents/test_registry_capabilities.py -x -p no:cacheprovider`
- `/opt/homebrew/bin/lint-imports` (4 kept / 0 broken — validators stay kernel-pure)
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_*.py -x -p no:cacheprovider` (5 snapshots byte/event-identical — new validators are additive registry entries, not wired into any existing manifest)
- grep the three validator files for a direct spawn (`subprocess.run(`/`subprocess.Popen(`) returns 0
</verification>

<success_criteria>
- code_compile / code_test / code_lint registered; registry guard == 53
- compile+test PASS on sample_python_repo offline with zero engine edits (SC-001); lint flags the seeded error via map_severity
- each validator refuses explicitly under exec-denied (>=1 issue, zero spawns, no escaping PermissionError)
- the exec-granting sample manifest compiles (file trust + security+approval gates) proving EXEC-02 offline
- import-linter 4/0; 5 characterization snapshots byte/event-identical
</success_criteria>

<output>
Create `.planning/phases/10-safe-local-exec-gated-on-n3-4b/10-04-SUMMARY.md` when done.
</output>
