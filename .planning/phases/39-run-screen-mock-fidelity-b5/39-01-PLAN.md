---
phase: 39-run-screen-mock-fidelity-b5
plan: 01
type: execute
wave: 2
depends_on: ["39-07"]
files_modified:
  - frontend/src/components/chat/RunChatLane.tsx
  - frontend/src/components/chat/ChatPanel.tsx
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/chat/LaneRunHeader.tsx
  - frontend/src/components/chat/RunChatLane.test.tsx
  - frontend/src/components/chat/LaneRunHeader.test.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-08]
must_haves:
  truths:
    - "The left lane is a structured transcript with a run header (Back to history · type · status · elapsed · agents · tokens), not a 'What can I help you with?' greeting"
    - "User/assistant bubbles match the mock (user #ECEAFC 14/14/4/14 bubble; assistant square near-black avatar with a rotated brand diamond)"
    - "Inline 'N clarifying questions' and 'PIPELINE · N agents' cards render from live run state and deep-link into Steps"
    - "Attachment chips and an 'Ask for a change…' composer render at the foot of the lane"
    - "Live states show a running phase pill + 'Awaiting you' clarify/gate status cards + a building pipeline mini; failed shows the What-went-wrong card + Resume options"
    - "All lane data is live (SC-001) — no cloned mock strings"
  artifacts:
    - path: "frontend/src/components/chat/LaneRunHeader.tsx"
      provides: "The left-lane run header (back link, type eyebrow, status pill, meta row)"
      min_lines: 40
    - path: "frontend/src/components/chat/RunChatLane.tsx"
      provides: "Structured-transcript composition + per-state lane bodies"
  key_links:
    - from: "frontend/src/components/chat/RunChatLane.tsx"
      to: "pipelineState (totalDuration/agents/totalTokens/pipeline_type)"
      via: "LaneRunHeader meta derivation"
      pattern: "pipelineState"
    - from: "frontend/src/components/chat/RunChatLane.tsx"
      to: "onRequestOpenTab('thinking')"
      via: "inline clarifying-questions / pipeline card deep-link"
      pattern: "onRequestOpenTab"
---

<intended_divergence_register>
## INTENDED-DIVERGENCE REGISTER (per D39-1 / D39-5 — the whole Phase-39 plan set inherits this)

For this phase we INVERT D-15's default and clone the mock's composition exactly (D39-1). Every deliberate departure from the mock is registered here. A screenshot-diff that flags one of these is CLOSED, not a failure. Any OTHER visual departure is a fidelity bug.

| # | Divergence | Mock says | We ship | Why |
|---|-----------|-----------|---------|-----|
| ND-A | Brand wordmark | "HEXAWARE" | "VelocityAI" | product brand (SC-001 / D39-5) |
| ND-B | Nav label | "Catalogue" | "My Workflows" | D-11 (Catalogue reserved for the future marketplace) |
| ND-C | Nav active-state | mock's treatment | purple underline | ND-13.1 |
| ND-D | All run data | hardcoded ("14.8M tokens", the fixed Apple transcript, "150 design systems", the fixed audit rows) | LIVE data from `pipelineState` / real endpoints | SC-001 (D39-4: the data already flows) |
| ND-E | Left-lane width | 390px | responsive `md:w-[340px] lg:w-[360px]` | keep our responsive shell; adopt the mock's INTERNAL composition only (UI-SPEC geometry) |
| ND-F | Prototype scrubbing controls | left-lane "Preview" Clarify/Gate/Building segmented scrubber; live-Preview `image-slot` "drop a screenshot" placeholder | NOT reproduced | demo-only affordances in the mock, not product features |
| ND-G | Deliverable renderers | mock's hardcoded website/deck/doc | REUSE the existing `UserStoryPreview`/`PPTPreview`/`PrototypePreview`/`AppBuilderPreview`/`MarkdownPreview` | D39-3 (reuse, don't rebuild) |

Downstream plans reference this register by number.
</intended_divergence_register>

<objective>
Replace the run screen's LEFT column greeting-state with the mock's **structured transcript**, and add the **lane run header** and **per-state lane bodies** — across settled / live-streaming / failed. This is the biggest fidelity gap (CONTEXT specifics #1). Keep every existing behavior (revision send, suggestions, gate/clarify quick-actions, Stop, terminal cards, deep-links) — the mock composition wraps the SAME data + callbacks (D39-4, SC-001).

Folds the proposed W6 States into this surface: the lane's live and failed variants are delivered here (no separate States plan touches these files).

Purpose: the left lane is where users read the run — it must match the mock as-is.
Output: `RunChatLane` renders the transcript composition; new `LaneRunHeader`; mock-faithful bubbles/cards/composer across three states.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@frontend/src/components/chat/RunChatLane.tsx
@frontend/src/components/chat/ChatPanel.tsx
@frontend/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LaneRunHeader + settled structured transcript (replace the greeting)</name>
  <files>frontend/src/components/chat/LaneRunHeader.tsx, frontend/src/components/chat/RunChatLane.tsx, frontend/src/components/chat/ChatPanel.tsx, frontend/src/components/chat/MessageBubble.tsx, frontend/src/components/chat/LaneRunHeader.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (settled left lane — header :51-64; bubbles :67-113; clarifying-questions card :80-86; PIPELINE·N-agents card :88-102; deliverable card :104-112; attachment chips :116-131; composer :132-137)
    - frontend/src/components/chat/RunChatLane.tsx (the composition root — header :575-610, ChatPanel mount :613-623, composer :625-635)
    - frontend/src/components/chat/ChatPanel.tsx (the greeting empty-state :215-324 to suppress in run context; hideComposer path already used)
    - frontend/src/components/chat/MessageBubble.tsx (bubble rendering to restyle)
  </read_first>
  <behavior>
    - LaneRunHeader renders: a "Back to history" link (chevron-left, ink-500), a type eyebrow (uppercase, ink-400) · dot · a status token ("Done" check when complete), the run title (Manrope 300 20px), and a meta row (elapsed · N/M agents · tokens, tabular-nums) — all derived from props (pipeline_type, runState, pipelineState).
    - In a run context ChatPanel does NOT render the "What can I help you with?" greeting/orbs/suggestion-chips; the transcript renders instead (or an empty transcript body when no messages yet).
    - User bubble: right-aligned, bg #ECEAFC, border #DED9F7, radius 14/14/4/14; assistant row: 22px near-black square avatar with a rotated 7px brand diamond + prose.
  </behavior>
  <action>
    Create `LaneRunHeader.tsx` reproducing the settled mock header composition (`Hexaware Run.dc.html:46-57`): back link → `onBackToHistory` (new OPTIONAL prop on RunChatLane; renders the link only when supplied — DashboardLayout wires it in 39-05); type eyebrow from `pipelineState.pipeline_type` (humanized) or a `runType` prop; status chip driven by `runState` ("Done" + check when complete/idle-with-deliverable; the live/failed variants are Task 2/3); title from a `runTitle` prop (fallback to the run's brief) ; meta row = `formatDuration(pipelineState.totalDuration)` · `${completedCount}/${agents.length} agents` · `${formatTokenCount(totalTokens)} tokens` reusing the existing `src/lib/runStats.ts` helpers (INV-12 single source). Route ALL color/type through Phase-32 tokens (near-black ink, warm ink ramp, brand) — no raw hex except where a token is genuinely absent (add a token instead). In RunChatLane, mount `LaneRunHeader` at the top of the lane (above the transcript), and pass a `inRun` signal (or reuse the presence of `pipelineState`/messages) into ChatPanel so its greeting empty-state is suppressed on the run screen (guard the greeting block behind `!hideComposer ? … : structured`), keeping the greeting for the standalone chat usage. Restyle the transcript bubbles (`MessageBubble`) to the mock: user bubble tokens/radius above; assistant avatar = the 22px near-black rounded-square with the rotated brand-diamond. Render the settled inline cards as GENERIC transcript adornments driven by live state (do NOT hardcode "6 clarifying questions"): a "N clarifying questions" row (count from `pipelineState.clarifications`) and a "PIPELINE · N agents" mini (count from `pipelineState.agents`), each `onClick`→`onRequestOpenTab("thinking")`; a "Delivered as v{n} · open in preview →" card when a deliverable exists → `onRequestOpenTab("preview")`. Reuse the existing attachment tray for the composer attachment chips, and restyle the composer shell to the mock's white rounded input with attach/voice/send affordances (the existing send/attach wiring stays). NO fenced code in this action — name the identifiers only.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `current/settled__leftlane.png` vs `target/` `Hexaware Run.dc.html` left lane, via the 39-07 harness, closed to ND-A..ND-G. A human signs off the images.
    - The greeting string "What can I help you with?" does NOT render in the run-screen lane (grep the rendered transcript testid → absent); the structured transcript + LaneRunHeader render instead.
    - `npx --prefix frontend tsc --noEmit` clean (identity vs baseline).
    - Touched-component vitest green: `LaneRunHeader.test.tsx` asserts header meta derivation; existing `RunChatLane`/`ChatPanel`/`MessageBubble` suites stay green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/chat/LaneRunHeader.test.tsx src/components/chat/RunChatLane.test.tsx</automated>
  </verify>
  <done>The settled lane is a structured transcript with the mock's header, bubbles, inline cards, attachment chips and composer; live data only; screenshot-diff signed off.</done>
</task>

<task type="auto">
  <name>Task 2: Live / streaming lane states (running pill, Awaiting-you clarify/gate cards, building pipeline mini)</name>
  <files>frontend/src/components/chat/RunChatLane.tsx, frontend/src/components/chat/LaneRunHeader.tsx, frontend/src/components/chat/RunChatLane.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (lane header running pill :70-80; bot line :85-88; CLARIFY status card :91-97; GATE status card :99-107; BUILDING pipeline mini :110-128; composer hint :148; phase state model :874-988)
    - frontend/src/components/chat/RunChatLane.tsx (renderComposerBody switch :375-566 — clarify/gate/building already wire InlineClarifyActions/InlineGateActions/steering; keep those)
  </read_first>
  <action>
    Add the live variants to `LaneRunHeader` + the transcript body, driven by the GENERIC `runState` (clarify | gate | building) + `pipelineState` (SC-001, ND-D — never a workflow name, never the mock's fixed "3/5"/"2.1M"). Header: a phase pill (brand fill, pulsing dot) reading "Clarifying" / "Awaiting approval" / "Running" per `runState`; meta row shows live elapsed/agents/tokens. In the transcript, above the composer, render the mock's STATUS cards (NOT the full clarify/gate UI — that stays in the composer body and in Steps): a "Paused — N questions for you · Awaiting you" clarify card (count from `clarifyQuestions`) → `onRequestOpenTab("thinking")`; a "Paused — task plan needs approval · Awaiting you" gate card → `onRequestOpenTab("thinking")`; and a "Pipeline · N agents · k/N" building mini with per-agent dot styles (done check / running pulse / queued) derived from `pipelineState.agents[].status` → `onRequestOpenTab("thinking")`. Set the composer placeholder/hint per state to the mock copy ("Answer the questions…", "Approve the plan above, or add a note…", "Steer the run — add a note…"). Do NOT reproduce ND-F (the left-lane Clarify/Gate/Building scrubber). Keep the existing `renderComposerBody` clarify/gate/building bodies intact.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `current/live__*` lane vs `target/` `Hexaware Run - Live.dc.html` left lane in each of clarify/gate/building, via the 39-07 harness, closed to ND-A..ND-G (esp. ND-D live data, ND-F no scrubber). Human sign-off.
    - Counts/labels are derived from `pipelineState`/`clarifyQuestions`/`runState` (grep RunChatLane for the mock literals "3/5", "2.1M", "6 questions" → absent; SC-001/ND-D).
    - `npx --prefix frontend tsc --noEmit` clean; `RunChatLane.test.tsx` gains cases asserting the running pill + Awaiting-you cards render per runState and deep-link to Steps.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -Rn "2.1M\|3 / 5\|14.8M" src/components/chat/RunChatLane.tsx src/components/chat/LaneRunHeader.tsx | grep -vc "//" | grep -qx 0 && npx vitest run src/components/chat/RunChatLane.test.tsx</automated>
  </verify>
  <done>Live/streaming lane matches the mock across clarify/gate/building on live data; scrubber (ND-F) intentionally absent.</done>
</task>

<task type="auto">
  <name>Task 3: Failed lane state (What-went-wrong card + Resume options + failed header)</name>
  <files>frontend/src/components/chat/RunChatLane.tsx, frontend/src/components/chat/LaneRunHeader.tsx, frontend/src/components/chat/RunChatLane.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html (failed header pill :48-56; assistant explanation :61-64; What-went-wrong card :66-73; Resume options :75-79; composer :82-89)
    - frontend/src/components/chat/RunChatLane.tsx (existing `terminal` state: cancelled :462-479, failed "What went wrong" :482-516, degraded :519-542 — RESTYLE these to the mock, do not add a parallel path; INV-12)
  </read_first>
  <action>
    Restyle the EXISTING terminal-`failed` branch to the mock's failed composition (`Hexaware Run - Failed.dc.html`): header status pill "Failed" (red status tokens) in `LaneRunHeader`; the assistant explanation line; a "What went wrong" card (red-tinted, alert-triangle header, bullet list from the sanitized server error + failed-agent names — reuse `resolveAgentNames`/`sanitizeError`, ND-D live data, never the mock's ".env/exec" fiction unless the real error says so); a "Resume options" card with two actions — a primary "Reopen & fix from the failed step" (→ `onRelaunch`, red primary token) and a secondary "Edit brief & run again" (→ `onRelaunch` or the relaunch path). Keep the cancelled + degraded terminal variants (restyle to the same token family for consistency). Do NOT introduce a second terminal renderer — edit the existing `renderComposerBody` `terminal` branch in place. The Steps "Pipeline halted" banner + failed-node reason live in 39-02 (AgentThinkingTab), not here.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `current/failed__*` lane vs `target/` `Hexaware Run - Failed.dc.html` left lane, via the 39-07 harness, closed to ND-A..ND-G. Human sign-off.
    - The failed card renders the SANITIZED live error + real failed-agent names (grep RunChatLane for ".env"/"exec is off" literals → absent; ND-D).
    - INV-12: exactly one terminal renderer (grep for a duplicated "What went wrong" block → 1).
    - `npx --prefix frontend tsc --noEmit` clean; `RunChatLane.test.tsx` failed-state case green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "What went wrong" src/components/chat/RunChatLane.tsx | grep -qx 1 && npx vitest run src/components/chat/RunChatLane.test.tsx</automated>
  </verify>
  <done>Failed lane matches the mock (What-went-wrong + Resume options + red header) on live error data; single terminal renderer preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server error string → lane "What went wrong" card | untrusted server text rendered in the UI |
| chat message content → transcript bubbles | user/assistant content rendered |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Information disclosure | failed-state error card | mitigate | reuse `sanitizeError` (first line, capped, no stack frames) — never echo raw server internals (LIVE-STATE-CONTRACT §1) |
| T-39-01-02 | Tampering (XSS) | transcript bubbles + inline cards | mitigate | render via React JSX escaping only; no `dangerouslySetInnerHTML` in the new lane code (grep → 0) |
| T-39-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan (uses existing lucide-react + tokens). |
</threat_model>

<verification>
- Lane matches the three mocks (settled/live/failed) per the screenshot-diff oracle, closed to the register.
- All lane data derives from live `pipelineState`/callbacks (SC-001/ND-D); no mock strings.
- `npx tsc --noEmit` clean; touched-component vitest green; single terminal renderer (INV-12).
</verification>

<success_criteria>
RUNUI-06 (SC-1): the left lane matches its mock across the three states, screenshot-diff-proven, closed to the register.
RUNUI-08 (SC-3): live/real data; intended divergences preserved; renderers untouched.
</success_criteria>

<artifacts_produced>
- `frontend/src/components/chat/LaneRunHeader.tsx` (NEW): the left-lane run header component.
- `RunChatLane` new OPTIONAL props: `onBackToHistory?`, `runTitle?`, `runType?` (wired by 39-05).
- Restyled `MessageBubble` user/assistant composition; suppressed greeting in run context (`ChatPanel`).
- Inline transcript adornments: clarifying-questions row, PIPELINE·N-agents mini, deliverable card, Awaiting-you clarify/gate cards, building pipeline mini.
- The INTENDED-DIVERGENCE REGISTER (ND-A..ND-G) — inherited by the whole plan set.
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-01-SUMMARY.md` when done.
</output>
