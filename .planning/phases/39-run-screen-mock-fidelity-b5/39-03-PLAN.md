---
phase: 39-run-screen-mock-fidelity-b5
plan: 03
type: execute
wave: 2
depends_on: ["39-07"]
files_modified:
  - frontend/src/components/results/FilesTab.tsx
  - frontend/src/components/results/FilesTab.test.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-08]
must_haves:
  truths:
    - "Files opens on a header ('Files · N files available · 1 deliverable') + a Download All button"
    - "A dark 'Final output' hero card names the deliverable, its type/size/validated, with Preview + Download actions"
    - "Agent outputs render as a timeline spine (rail + per-agent avatar nodes) of downloadable .md rows"
    - "Run-input cards (prompt.md / clarifications.md) render as a bordered pair"
    - "Failed run shows the 'Build incomplete' amber banner + only the planning-artifact files"
    - "Every row is a real file from live data (SC-001) — no cloned mock file list"
  artifacts:
    - path: "frontend/src/components/results/FilesTab.tsx"
      provides: "Final-output hero + agent-outputs timeline + Run-input cards, token-reskinned"
      contains: "Final output"
  key_links:
    - from: "frontend/src/components/results/FilesTab.tsx"
      to: "agentOutputs / deriveDeliverableFiles"
      via: "hero (final deliverable) + timeline (agent outputs)"
      pattern: "agentOutputs"
---

<objective>
Bring the **Files** tab to the mock's composition (CONTEXT specifics #6): a header with **Download All**, a dark **"Final output" hero** card, a **per-agent outputs timeline spine**, and the **Run-input cards** — plus the failed-run "Build incomplete" banner. The current `FilesTab` already has the right STRUCTURE (final output → agent outputs → run input) and derivation logic; this is chrome + token reskin (it still uses the retired `gray-*` palette and a hardcoded `#f5f5f0` background). Reuse every derivation helper (`deriveDeliverableFiles`, `runInputFileRows`, the app-builder split) — do not rebuild them.

Folds W6 States: the failed-run "Build incomplete — only planning artifacts" banner is delivered here.

Purpose: Files is the run's deliverable-download surface — it must match the mock's hero + timeline.
Output: `FilesTab` reskinned to the mock with the dark hero, timeline spine, run-input cards, and failed banner.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-01-PLAN.md
@frontend/src/components/results/FilesTab.tsx
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
Inherit the INTENDED-DIVERGENCE REGISTER (ND-A..ND-G) from `39-01-PLAN.md`. ND-D: file names, sizes, counts and the "Final output" deliverable come from live `agentOutputs`/`deriveDeliverableFiles`/`genericDeliverable` — never the mock's fixed "apple-reference-prototype.html · 151.6 KB" list.
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: Header + dark Final-output hero + Run-input cards (token reskin)</name>
  <files>frontend/src/components/results/FilesTab.tsx, frontend/src/components/results/FilesTab.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (Files header + Download All :615-622; dark Final-output hero :624-638 — `#0C0D12` bg, radial brand glow, 52px icon tile, "FINAL OUTPUT" eyebrow, name, "type · size · validated", Preview + Download actions; Run input pair :658-669)
    - frontend/src/components/results/FilesTab.tsx (header row :612-625, run-input section :631-636, final-output/agent-outputs sections :677-705; retired `#f5f5f0` bg :611 and `gray-*` throughout; `deriveDeliverableFiles`/`runInputFileRows` reused)
  </read_first>
  <action>
    Reskin the FilesTab container off Phase-32 tokens (replace the inline `style={{background:'#f5f5f0'}}` with `bg-surface-paper`; retire `gray-*`). Header: "Files" title (Manrope 300 22px) + a "N files available · 1 deliverable" subline + a right-aligned "Download All" button (token-styled, reuse the existing download-all handler). Add a dark **Final-output hero** card matching the mock (`Hexaware Run.dc.html:624-638`): `bg-[#0C0D12]` (add an `--surface-ink-black` token if missing — prefer a token) with the radial brand glow, a 52px translucent icon tile, the "FINAL OUTPUT" eyebrow (blue-on-dark `#8E88E8`/token), the deliverable NAME + "{format} · {size} · validated" line (from the live final `FileItem` — the first `files[]` row / `deriveDeliverableFiles` result / `genericDeliverable`), and two actions: "Preview" (→ a new optional `onOpenPreview` prop, wired later, or omit if unavailable) and "Download" (reuse `handleDownload` on the final file). Reskin the "Run input" section into the mock's bordered card pair (prompt.md / clarifications.md) from the existing `runInputFileRows`. Keep ALL derivation + download wiring intact (INV-12 — no new file-derivation code).
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: the Files header + Final-output hero + Run-input region of `current/settled__files.png` vs `Hexaware Run.dc.html` Files, via the 39-07 harness, closed to ND-A..ND-G. Human sign-off.
    - The hero names the LIVE deliverable (grep `FilesTab.tsx` for the mock literal "apple-reference-prototype.html"/"151.6 KB" → absent; ND-D).
    - Retired palette gone: `grep -v '^\s*//' src/components/results/FilesTab.tsx | grep -c "text-gray-\|bg-gray-\|border-gray-\|#f5f5f0"` == 0.
    - `npx --prefix frontend tsc --noEmit` clean; `FilesTab.test.tsx` asserts the hero renders the derived final deliverable + Download All fires the existing handler.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -v '^\s*//' src/components/results/FilesTab.tsx | grep -c "text-gray-\|bg-gray-\|#f5f5f0" | grep -qx 0 && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/results/FilesTab.test.tsx</automated>
  </verify>
  <done>Files header + dark Final-output hero + Run-input cards match the mock on live data; palette retired; derivation wiring intact.</done>
</task>

<task type="auto">
  <name>Task 2: Agent-outputs timeline spine + failed 'Build incomplete' banner</name>
  <files>frontend/src/components/results/FilesTab.tsx, frontend/src/components/results/FilesTab.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (Agent outputs timeline :640-656 — a `position:absolute` vertical rail + per-agent 38px avatar nodes with the agent code initials + the file row card + download button)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html (Files "Build incomplete" amber banner :175 + the reduced planning-only file list :176-182)
    - frontend/src/components/results/FilesTab.tsx (genericAgentFiles / appBuilderDocFiles rows :688-701, renderFileRow :536-568)
  </read_first>
  <action>
    Reskin the "Agent outputs (N)" section into the mock's TIMELINE SPINE: a left vertical rail (absolute-positioned 2px divider) with a 38px circular avatar node per agent (the agent code initials from `agent.name`) and the existing file-row card beside it (name · "{role} · {size}" · a token-styled download button). Keep the section header "Agent outputs (N)" + the "Intermediate work-in-progress…" subline. Reskin `renderFileRow` off tokens (retire `gray-*`). FAILED state: when the run is failed/degraded (thread the run status into FilesTab via a new optional `runStatus?` prop, defaulting to undefined → no banner), render the amber "Build incomplete — only the planning artifacts were produced before the run stopped. No application code was written." banner ABOVE the file list (the file list itself already reflects the reduced live outputs — ND-D, do not fabricate rows). Keep every download/derivation path unchanged.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: the Agent-outputs timeline of `current/settled__files.png` vs `Hexaware Run.dc.html`, and `current/failed__files` (drive the failed run) vs `Hexaware Run - Failed.dc.html` Files, via the 39-07 harness, closed to ND-A..ND-G. Human sign-off.
    - The timeline renders one node per LIVE agent output (grep for a hardcoded 3/5-file mock list → absent).
    - Failed banner renders only when `runStatus` is failed/degraded (asserted in vitest; default undefined → no banner, zero regression for completed runs).
    - `npx --prefix frontend tsc --noEmit` clean; `FilesTab.test.tsx` timeline + failed-banner cases green; retired-palette grep on the file == 0.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -v '^\s*//' src/components/results/FilesTab.tsx | grep -c "text-gray-\|bg-gray-\|#f5f5f0" | grep -qx 0 && npx vitest run src/components/results/FilesTab.test.tsx</automated>
  </verify>
  <done>Agent outputs render as the mock's timeline spine and the failed run shows the Build-incomplete banner over the live (reduced) file set; download wiring intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deliverable/agent-output content → download blobs | model-generated content downloaded by the user |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03-01 | Tampering | file download path | accept | reuses the existing `downloadBlob`/`handleDownload` (blob with declared mime, `a.download`); no new execution path |
| T-39-03-02 | Tampering (XSS) | file-name / hero text render | mitigate | React JSX escaping; no `dangerouslySetInnerHTML` added (grep → 0) |
| T-39-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan. |
</threat_model>

<verification>
- Files matches the settled + failed mocks per the screenshot-diff oracle, closed to the register.
- Live file data only (ND-D); derivation + download wiring reused (INV-12); retired palette gone.
- `npx tsc --noEmit` clean; touched-component vitest green.
</verification>

<success_criteria>
RUNUI-06 (SC-1): Files matches its mock (settled + failed), screenshot-diff-proven.
RUNUI-08 (SC-3): live/real file data; derivation helpers reused, not rebuilt.
</success_criteria>

<artifacts_produced>
- `FilesTab` reskinned: token container, header + Download All, dark Final-output hero, agent-outputs timeline spine, Run-input card pair.
- `FilesTab` new OPTIONAL props: `runStatus?` (failed banner), `onOpenPreview?` (hero Preview action — wired by 39-05/06 if adopted).
- (Optional) `--surface-ink-black` token if not already present.
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-03-SUMMARY.md` when done.
</output>
