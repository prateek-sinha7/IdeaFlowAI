---
phase: 39-run-screen-mock-fidelity-b5
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/fixtures/mockApi.ts
  - frontend/e2e/fixtures/dashboard.ts
  - frontend/e2e/fixtures/scenarios.ts
  - frontend/e2e/tests/zzz-baseline.spec.ts
  - frontend/e2e/fidelity/serve-mocks.mjs
  - frontend/e2e/fidelity/capture-mocks.mjs
  - frontend/e2e/fidelity/assemble-gallery.mjs
  - frontend/e2e/fidelity/README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [RUNUI-09]
must_haves:
  truths:
    - "`npm run e2e` (mocked project) exits 0 against feat/ui-2 — no home-grid, launch-flow, or run-family crash"
    - "A reusable fidelity harness drives our run screen through settled / live / failed and screenshots every tab (current/*)"
    - "The target mocks render over a local HTTP server (support.js + fonts load) and screenshot per tab/state (target/*)"
    - "One command assembles target/* + current/* into a single self-contained side-by-side gallery with the ND-A..ND-H register captioned as 'expected — ignore' — the artifact each surface checkpoint reviews"
    - "RUNUI-06..09 are defined in REQUIREMENTS.md and the Phase 39 ROADMAP carries a **Requirements:** line (owned, not floating)"
  artifacts:
    - path: "frontend/e2e/fixtures/mockApi.ts"
      provides: "Shared stubs for /api/workflows, /api/runs/{id}/family, /api/analytics/summary, and the audit endpoints"
      contains: "api/workflows"
    - path: "frontend/e2e/fidelity/assemble-gallery.mjs"
      provides: "Pairs target/* vs current/* into ONE self-contained mock-vs-current HTML gallery"
    - path: "frontend/e2e/fidelity/serve-mocks.mjs"
      provides: "Local HTTP server for the .dc.html target mocks"
    - path: "frontend/e2e/fidelity/capture-mocks.mjs"
      provides: "Playwright capture of the target mocks per tab/state"
  key_links:
    - from: "frontend/e2e/fixtures/mockApi.ts"
      to: "/api/workflows"
      via: "route handler returning a launchable-row array"
      pattern: "api/workflows"
    - from: "frontend/e2e/fidelity/assemble-gallery.mjs"
      to: "shots/target + shots/current"
      via: "side-by-side HTML gallery"
      pattern: "assemble-gallery"
---

<objective>
Repair the stale mocked-e2e harness so `npm run e2e` exits 0 against `feat/ui-2`, and stand up the **fidelity ORACLE** (D39-6) the whole phase diffs against: (1) our-side capture of the run screen across settled/live/failed, (2) a local-HTTP render + capture of the three target mocks, and (3) a **side-by-side gallery assembler** that pairs them into ONE self-contained HTML file with the intended-divergence register captioned — the exact artifact each surface plan's human-verify checkpoint reviews.

This lands FIRST (Wave 1) because no surface plan can close its fidelity checkpoint until the gallery exists. It also OWNS the requirement-traceability docs (RUNUI-06..09).

Three known-red harness breakages to fix (scope fence), promoting them from per-spec route-stubs into the SHARED fixtures: `GET /api/workflows` (home-grid `rows.filter` crash), the "Provide the brief" launch flow (stale `dashboard.runWith`), and `GET /api/runs/{id}/family` (`[...runFamily.members]` not iterable).

Purpose: makes the D39-6 acceptance oracle real and mechanically-verifiable, and unblocks every surface plan's fidelity checkpoint.
Output: green mocked e2e suite + a `frontend/e2e/fidelity/` two-sided harness with a one-command gallery + the RUNUI-06..09 docs.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-01-PLAN.md
@frontend/e2e/tests/zzz-baseline.spec.ts
@frontend/e2e/fixtures/dashboard.ts
@frontend/e2e/fixtures/mockApi.ts
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
The full INTENDED-DIVERGENCE REGISTER lives in `39-01-PLAN.md` (per D39-5). The gallery must CAPTION the registered divergences (ND-A brand "VelocityAI" vs "HEXAWARE"; ND-B "My Workflows" vs "Catalogue"; ND-C nav underline; ND-D live data vs the mocks' hardcoded values; ND-E responsive width; ND-F prototype scrubber/image-slot not reproduced; ND-G reused renderers; ND-H client-only Share) as "expected — ignore", so a human reviewer does not mistake them for fidelity gaps. Do NOT wire an automated pixel-diff against the mock — ND-D (live data ≠ the mock's fixed values) would make a pixel comparison always "fail"; the fidelity judgment is a HUMAN review of the assembled gallery (the surface checkpoints in 39-01..39-06).
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: Move the three harness stubs into the shared MockApi + repair the dashboard fixture launch flow</name>
  <files>frontend/e2e/fixtures/mockApi.ts, frontend/e2e/fixtures/dashboard.ts, frontend/e2e/fixtures/scenarios.ts</files>
  <read_first>
    - frontend/e2e/fixtures/mockApi.ts (the route dispatcher — see the `/api/runs` handler at :164 for the idiom to mirror)
    - frontend/e2e/fixtures/dashboard.ts (stale `homeHeading` :28, `runWith` :50, `thinkingTab` :99)
    - frontend/e2e/tests/zzz-baseline.spec.ts (the per-spec `stubHome`/`launch`/`stubAudit` route-stubs :29-84 — the exact payloads to promote into MockApi)
  </read_first>
  <action>
    In MockApi add route handlers, mirroring the existing `/api/runs` json() idiom, for: (a) `GET /api/workflows(?...)` → an array of at least one launchable row `{id:"user_stories", display_name:"Generate product requirements", description, user_launchable:true, step_count:3}` (fixes the home-grid `rows.filter` crash); (b) `GET /api/runs/{id}/family` → a well-formed `RunFamily` `{root_id, members:[{id,type,title,status,revision_index:0,parent_run_id:null,created_at,completed_at}]}` (fixes `[...runFamily.members]` not-iterable); (c) `GET /api/analytics/summary` → `{type_avg_duration_sec:{}}`; (d) the three audit reads `GET /api/runs/{id}/gate-events|validation-results|exec-runs` → the envelope shapes from `zzz-baseline.spec.ts:66-83`. Make the family + workflows rows configurable via a MockApi setter so specs can vary them. In `dashboard.ts`: update `homeHeading()` to the CURRENT fused-Home heading (verify the live label by grep on the Home component — Phase 36 fused Home; do NOT assume the old copy); rewrite `runWith()` to the real flow (click the deliverable row → wait for the "Provide the brief" heading → fill the brief screen textarea AFTER it mounts → click "Run workflow" → wait for the `run_pipeline` client frame), matching `zzz-baseline.spec.ts:49-61`; relabel `thinkingTab()` from "Thinking" to "Steps". Delete the now-redundant per-spec `stubHome`/`stubAudit` route-stubs from `zzz-baseline.spec.ts` (superseded by MockApi — INV-12 no dual stub). Add an `od_prototype` scenario helper to `scenarios.ts` only if the existing `AGENTS.od_prototype` is insufficient.
  </action>
  <acceptance_criteria>
    - AUTOMATED whole-suite gate: `npm --prefix frontend run e2e` EXITS 0 (the mocked project is fully green), AND its output contains zero occurrences of `rows.filter is not a function`, `not iterable`, or a `Provide the brief` timeout (grep the run log → 0). A mechanical exit-code + grep gate, not a manual "someone looked at the summary".
    - No spec references a locally-defined `stubHome`/`stubAudit` (grep `frontend/e2e` → the promoted stubs live in `mockApi.ts` only; INV-12).
    - `npx --prefix frontend tsc --noEmit` clean on the e2e tsconfig (identity vs baseline).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run e2e > /tmp/39-07-e2e.log 2>&1; code=$?; grep -Ec "rows.filter is not a function|not iterable|Provide the brief.*timeout" /tmp/39-07-e2e.log | grep -qx 0 && test $code -eq 0</automated>
  </verify>
  <done>The whole mocked e2e suite exits 0; the three known-red breakages are fixed in the shared fixtures, not per-spec.</done>
</task>

<task type="auto">
  <name>Task 2: Two-sided fidelity harness — capture + the side-by-side gallery assembler</name>
  <files>frontend/e2e/tests/zzz-baseline.spec.ts, frontend/e2e/fidelity/serve-mocks.mjs, frontend/e2e/fidelity/capture-mocks.mjs, frontend/e2e/fidelity/assemble-gallery.mjs, frontend/e2e/fidelity/README.md</files>
  <read_first>
    - frontend/e2e/tests/zzz-baseline.spec.ts (the settled/live/failed drivers to keep + de-throwaway)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (target — the inline `<script src="./support.js">` + Google-fonts links require a LOCAL HTTP origin to load)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html
  </read_first>
  <action>
    Convert `zzz-baseline.spec.ts` into a RETAINED, env-gated (`FIDELITY_CAPTURE=1`) fidelity capture spec: keep the three drivers (settled / live-streaming / failed) + per-tab screenshots, but write OUR-side outputs under `frontend/e2e/fidelity/shots/current/` (repo-relative, not the hard-coded scratch path), tag each file `{surface}__{state}` (e.g. `steps__settled`, `steps__live`, `audit__failed`, `leftlane__settled`), and do NOT run as an assertion during the normal `npm run e2e` pass. Add `serve-mocks.mjs`: a tiny static HTTP server (Node built-in `http` — NO new dependency) serving `/Users/1000060523/Documents/Work/VelocityAI-New-UI/` so `support.js` + fonts resolve. Add `capture-mocks.mjs`: a Playwright script that opens each `.dc.html` mock over that server, clicks through each tab/sub-state via the mock's own `sc-for`/`onClick` controls, and screenshots into `shots/target/` with the matching `{surface}__{state}` tags. Add `assemble-gallery.mjs`: pairs `shots/target/{tag}.png` with `shots/current/{tag}.png` into ONE self-contained `frontend/e2e/fidelity/gallery.html` — a two-column layout per tag (mock LEFT, ours RIGHT, tag heading), with a header block that lists ND-A..ND-H as "expected — ignore" and accepts an optional `--surface <name>` filter (so a surface checkpoint can regenerate just its section). Add `README.md`: the capture + assemble commands, the `shots/` layout, how each surface checkpoint regenerates + reviews its gallery section, and the ND list to ignore. (Optional, non-required regression-vs-self: note that after a surface is human-approved, `toHaveScreenshot` of OUR approved surface can be committed as a self-baseline so later waves catch unintended regressions — distinct from fidelity-vs-mock; leave this as a documented option, do not build it here.) Do NOT commit screenshot binaries — only scripts + README; git-ignore `shots/` and `gallery.html`.
  </action>
  <acceptance_criteria>
    - `node frontend/e2e/fidelity/serve-mocks.mjs` serves the three mocks over HTTP and `support.js` + Manrope/Heebo fonts load (a one-off capture shows the rendered top bar, not raw markup).
    - `FIDELITY_CAPTURE=1 npm --prefix frontend run e2e -- zzz-baseline` produces `current/{surface}__{state}.png` across settled/live/failed without error; `node frontend/e2e/fidelity/capture-mocks.mjs` produces the matching `target/*.png`.
    - `node frontend/e2e/fidelity/assemble-gallery.mjs` writes a self-contained `gallery.html` pairing target vs current per tag, with the ND-A..ND-H "expected — ignore" caption; `--surface steps` filters to one surface (mechanical: the output HTML contains only the steps tags).
    - `README.md` documents the commands + the ND list; `shots/` and `gallery.html` are git-ignored.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && test -f e2e/fidelity/serve-mocks.mjs && test -f e2e/fidelity/capture-mocks.mjs && test -f e2e/fidelity/assemble-gallery.mjs && grep -Eiq "velocityai|my workflows|underline|expected .*ignore" e2e/fidelity/README.md && git check-ignore e2e/fidelity/gallery.html</automated>
  </verify>
  <done>Both sides render + screenshot on demand and one command assembles the side-by-side gallery (per-surface filterable). Scripts + README committed; shots/gallery git-ignored.</done>
</task>

<task type="auto">
  <name>Task 3: Own the requirement-traceability docs (RUNUI-06..09)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the Run-Screen Redesign block RUNUI-01..05 :228-234 — the insertion anchor + entry style; the RUNUI-06..09 subsection added at planning time)
    - .planning/ROADMAP.md (Phase 39 section — the `**Depends on:**` / `**Requirements:**` / `**Success Criteria:**` lines)
  </read_first>
  <action>
    Ensure (idempotent create-if-absent) that `REQUIREMENTS.md` carries a "Run-Screen Mock Fidelity (Phase 39)" subsection defining RUNUI-06 (SC-1 fidelity, screenshot-diff gallery + human sign-off), RUNUI-07 (SC-2 net-new Share/Version/Renders-as/fuller-Audit), RUNUI-08 (SC-3 live data + reuse renderers + preserved divergences), RUNUI-09 (SC-4 e2e green + fidelity harness/gallery) — one entry each, mapped to the Phase 39 Success Criteria — and that the Phase 39 ROADMAP section carries a `**Requirements:** RUNUI-06, RUNUI-07, RUNUI-08, RUNUI-09` line. These were added at planning time; this task OWNS them — verify they exist and match the plan-frontmatter IDs, creating any that are missing so traceability is never floating.
  </action>
  <acceptance_criteria>
    - `REQUIREMENTS.md` defines RUNUI-06, RUNUI-07, RUNUI-08, RUNUI-09 (grep → 4 present).
    - `ROADMAP.md` Phase 39 section has a `**Requirements:** RUNUI-06, RUNUI-07, RUNUI-08, RUNUI-09` line.
    - Every RUNUI id in the 7 plans' `requirements` frontmatter resolves to a REQUIREMENTS.md entry (no dangling id).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Requirements:\*\* RUNUI-06" .planning/ROADMAP.md && for id in RUNUI-06 RUNUI-07 RUNUI-08 RUNUI-09; do grep -q "$id" .planning/REQUIREMENTS.md || exit 1; done</automated>
  </verify>
  <done>RUNUI-06..09 are defined + owned in REQUIREMENTS.md and referenced by the Phase 39 ROADMAP; no dangling requirement ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → local mock server | reads .dc.html from a fixed local path; no untrusted network input |
| MockApi → app under test | fabricated fixtures only; test-only, never shipped |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07-01 | Information disclosure | capture script serving a local dir | accept | test-only; serves the read-only mock dir on localhost; never bundled into the app |
| T-39-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan — the harness uses the already-installed Playwright and Node's built-in `http`; no new dependency. |
</threat_model>

<verification>
- `npm run e2e` (mocked) exits 0 against feat/ui-2 (automated).
- The two-sided fidelity harness renders both sides and assembles a per-surface-filterable side-by-side gallery on demand.
- RUNUI-06..09 defined + owned; no dangling ids. No new npm dependency; no shipped-code change (fixtures/spec/scripts/docs only).
</verification>

<success_criteria>
RUNUI-09 (SC-4): the mocked e2e suite is green again and the fidelity screenshot harness + side-by-side gallery run under `frontend/e2e`.
</success_criteria>

<artifacts_produced>
- `frontend/e2e/fixtures/mockApi.ts`: new route stubs (`/api/workflows`, `/api/runs/{id}/family`, `/api/analytics/summary`, the 3 audit endpoints) + setters.
- `frontend/e2e/fixtures/dashboard.ts`: repaired `homeHeading`, `runWith` (Provide-the-brief flow), `thinkingTab`→Steps.
- `frontend/e2e/tests/zzz-baseline.spec.ts`: retained env-gated fidelity capture spec (`{surface}__{state}` tags, repo-relative outputs).
- `frontend/e2e/fidelity/{serve-mocks,capture-mocks,assemble-gallery}.mjs` + `README.md`: two-sided harness + the side-by-side gallery assembler (the checkpoint artifact).
- `.planning/REQUIREMENTS.md` RUNUI-06..09 + `.planning/ROADMAP.md` `**Requirements:**` line.
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-07-SUMMARY.md` when done.
</output>
