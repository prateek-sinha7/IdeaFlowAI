---
phase: 39-run-screen-mock-fidelity-b5
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/fixtures/mockApi.ts
  - frontend/e2e/fixtures/dashboard.ts
  - frontend/e2e/fixtures/scenarios.ts
  - frontend/e2e/tests/zzz-baseline.spec.ts
  - frontend/e2e/fidelity/serve-mocks.mjs
  - frontend/e2e/fidelity/capture-mocks.mjs
  - frontend/e2e/fidelity/README.md
autonomous: true
requirements: [RUNUI-09]
must_haves:
  truths:
    - "`npm run e2e` (mocked project) is green against feat/ui-2 — no home-grid, launch-flow, or run-family crash"
    - "A reusable fidelity harness drives our run screen through settled / live / failed and screenshots every tab"
    - "The target mocks render over a local HTTP server (support.js + fonts load) and screenshot per tab/state"
    - "A single command regenerates the mock-vs-current baseline gallery for a human sign-off"
  artifacts:
    - path: "frontend/e2e/fixtures/mockApi.ts"
      provides: "Shared stubs for /api/workflows, /api/runs/{id}/family, /api/analytics/summary, and the audit endpoints"
      contains: "api/workflows"
    - path: "frontend/e2e/tests/zzz-baseline.spec.ts"
      provides: "Formalized fidelity capture spec (our-side render, all tabs × 3 states)"
    - path: "frontend/e2e/fidelity/serve-mocks.mjs"
      provides: "Local HTTP server for the .dc.html target mocks"
    - path: "frontend/e2e/fidelity/capture-mocks.mjs"
      provides: "Playwright capture of the target mocks per tab/state"
  key_links:
    - from: "frontend/e2e/fixtures/mockApi.ts"
      to: "/api/workflows"
      via: "route handler returning a launchable-row array"
      pattern: "api/workflows"
    - from: "frontend/e2e/fixtures/dashboard.ts"
      to: "Provide the brief launch flow"
      via: "runWith fills the brief screen textarea after it mounts"
      pattern: "Provide the brief"
---

<objective>
Repair the stale mocked-e2e harness so `npm run e2e` is green against `feat/ui-2`, and formalize the throwaway `zzz-baseline.spec.ts` capture into a reusable **fidelity harness** — the acceptance ORACLE (D39-6) every other Phase-39 plan diffs against. This plan lands FIRST (Wave 1) because no surface plan can prove fidelity until the screenshot-diff oracle exists.

Three known-red harness breakages to fix (scope fence): `GET /api/workflows` (home grid `rows.filter` crash), the "Provide the brief" launch flow (stale `dashboard.runWith` fills the wrong textarea before the brief screen mounts), and `GET /api/runs/{id}/family` (`[...runFamily.members]` not iterable). All three currently work only via per-spec route-stubs inside `zzz-baseline.spec.ts`; move them into the SHARED `MockApi` + `dashboard` fixtures so the WHOLE suite is green.

Purpose: makes the D39-6 acceptance oracle real and unblocks every surface plan's screenshot-diff.
Output: green mocked e2e suite + a `frontend/e2e/fidelity/` harness that renders both sides (our run screen + the three target mocks) and regenerates the baseline gallery.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@frontend/e2e/tests/zzz-baseline.spec.ts
@frontend/e2e/fixtures/dashboard.ts
@frontend/e2e/fixtures/mockApi.ts
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
The full INTENDED-DIVERGENCE REGISTER lives in `39-01-PLAN.md` (per D39-5). This harness must NOT flag the four registered divergences as fidelity failures: brand "VelocityAI" (mock "HEXAWARE"), nav "My Workflows" (mock "Catalogue"), nav purple underline, and live/real data (never the mocks' hardcoded "14.8M tokens" / fixed Apple transcript / fixed audit rows). The capture script and the gallery caption must annotate these as expected.
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: Move the three harness stubs into the shared MockApi + repair the dashboard fixture launch flow</name>
  <files>frontend/e2e/fixtures/mockApi.ts, frontend/e2e/fixtures/dashboard.ts, frontend/e2e/fixtures/scenarios.ts</files>
  <read_first>
    - frontend/e2e/fixtures/mockApi.ts (the route dispatcher — see the `/api/runs` handler at :164 for the idiom to mirror)
    - frontend/e2e/fixtures/dashboard.ts (stale `homeHeading` :28, `runWith` :50, `thinkingTab` :99)
    - frontend/e2e/tests/zzz-baseline.spec.ts (the per-spec `stubHome`/`launch`/`stubAudit` route-stubs :29-84 — the exact payloads to promote into MockApi)
  </read_first>
  <action>
    In MockApi add route handlers, mirroring the existing `/api/runs` json() idiom, for: (a) `GET /api/workflows(?...)` → an array of at least one launchable row `{id:"user_stories", display_name:"Generate product requirements", description, user_launchable:true, step_count:3}` (fixes the home-grid `rows.filter` crash — the grid expects an ARRAY, MockApi currently returns nothing); (b) `GET /api/runs/{id}/family` → a well-formed `RunFamily` `{root_id, members:[{id,type,title,status,revision_index:0,parent_run_id:null,created_at,completed_at}]}` (fixes `[...runFamily.members]` not-iterable); (c) `GET /api/analytics/summary` → `{type_avg_duration_sec:{}}`; (d) the three audit reads `GET /api/runs/{id}/gate-events|validation-results|exec-runs` → the envelope shapes from `zzz-baseline.spec.ts:66-83` (`{workflow_id, gate_events:[...]}` etc.). Make the family + workflows rows configurable via a MockApi setter so specs can vary them. In `dashboard.ts`: update `homeHeading()` to the CURRENT fused-Home heading (verify the live label by grep on the Home component — Phase 36 fused Home; do NOT assume "What would you like to build today?"); rewrite `runWith()` to the real flow (click the deliverable row → wait for the "Provide the brief" heading → fill the brief screen's textarea AFTER it mounts → click "Run workflow" → wait for the `run_pipeline` client frame), matching `zzz-baseline.spec.ts:49-61`; relabel `thinkingTab()` locator from "Thinking" to "Steps" (Phase-32 relabel, PreviewPanel `TAB_CONFIG`). Delete the now-redundant per-spec `stubHome`/`stubAudit` route-stubs from `zzz-baseline.spec.ts` (they are superseded by MockApi — no dual stub). Add an `od_prototype` scenario helper to `scenarios.ts` only if the existing `AGENTS.od_prototype` is insufficient for the capture spec.
  </action>
  <acceptance_criteria>
    - `npm --prefix frontend run e2e` runs the mocked project with ZERO failures attributable to `rows.filter is not a function`, the wrong-textarea launch, or `runFamily.members` not iterable (grep the run log for those three strings → 0).
    - No spec references a locally-defined `stubHome`/`stubAudit` (grep `frontend/e2e` → the promoted stubs live in `mockApi.ts` only; INV-12 no dual stub).
    - `npx --prefix frontend tsc --noEmit` clean on the e2e tsconfig (identity vs baseline).
    - A human confirms the suite summary shows the previously-red specs now passing.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run e2e 2>&1 | grep -Ec "rows.filter is not a function|not iterable|Provide the brief.*timeout" | grep -qx 0</automated>
  </verify>
  <done>The whole mocked e2e suite is green; the three known-red breakages are fixed in the shared fixtures, not per-spec.</done>
</task>

<task type="auto">
  <name>Task 2: Formalize the fidelity harness (our-side capture spec + target-mock renderer + gallery regen)</name>
  <files>frontend/e2e/tests/zzz-baseline.spec.ts, frontend/e2e/fidelity/serve-mocks.mjs, frontend/e2e/fidelity/capture-mocks.mjs, frontend/e2e/fidelity/README.md</files>
  <read_first>
    - frontend/e2e/tests/zzz-baseline.spec.ts (the settled/live/failed drivers to keep + de-throwaway)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (target — note the inline `<script src="./support.js">` + Google-fonts links that require a LOCAL HTTP origin to load)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html
  </read_first>
  <action>
    Convert `zzz-baseline.spec.ts` from a throwaway into a RETAINED fidelity capture spec: keep the three drivers (settled / live-streaming / failed) and the per-tab screenshots, but write outputs under a repo-relative dir (`frontend/e2e/fidelity/shots/current/`) instead of the hard-coded `/Users/.../jobs/...` scratch path, and gate the capture behind an env flag (e.g. `FIDELITY_CAPTURE=1`) so it does NOT run as an assertion in the normal `npm run e2e` pass. Update its tab locators to `getByRole("tab", {name})` (Tabs primitive `role=tab`) and add the "detail drill" + left-lane clips per the target surfaces. Add `frontend/e2e/fidelity/serve-mocks.mjs`: a tiny static HTTP server that serves `/Users/1000060523/Documents/Work/VelocityAI-New-UI/` (so `support.js` + fonts resolve) on a local port. Add `frontend/e2e/fidelity/capture-mocks.mjs`: a Playwright script that opens each of the three `.dc.html` mocks over that server, clicks through each tab/sub-state (using the mock's own `sc-for`/`onClick` controls), and screenshots into `frontend/e2e/fidelity/shots/target/`. Add `README.md` documenting: the two capture commands, the shots layout (`target/` vs `current/`), how each surface plan diffs its surface, and the four registered divergences the reviewer must IGNORE (see the intended-divergence note above). Do NOT commit any screenshot binaries — only the scripts + README.
  </action>
  <acceptance_criteria>
    - `node frontend/e2e/fidelity/serve-mocks.mjs` serves the three mocks over HTTP and `support.js` + Manrope/Heebo fonts load (verified by a one-off screenshot showing the rendered top bar, not raw markup).
    - `FIDELITY_CAPTURE=1 npm --prefix frontend run e2e -- zzz-baseline` produces `current/settled__{preview,steps,files,audit,leftlane}.png` + `live__*` + `failed__*` without error.
    - `node frontend/e2e/fidelity/capture-mocks.mjs` produces the matching `target/*.png` set for all three mocks.
    - `README.md` lists the two commands, the diff workflow, and the four intended divergences to ignore.
    - `.gitignore` (or a fidelity-local ignore) excludes `frontend/e2e/fidelity/shots/`.
  </acceptance_criteria>
  <verify>
    <automated>test -f frontend/e2e/fidelity/serve-mocks.mjs && test -f frontend/e2e/fidelity/capture-mocks.mjs && grep -Eiq "velocityai|my workflows|underline|live data|hardcoded" frontend/e2e/fidelity/README.md</automated>
  </verify>
  <done>Both sides render and screenshot on demand; the gallery can be regenerated and a human can sign off per surface. The harness is committed (scripts + README), shots are git-ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → local mock server | reads .dc.html from a fixed local path; no untrusted network input |
| MockApi → app under test | fabricated fixtures only; test-only, never shipped |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07-01 | Information disclosure | capture script serving a local dir | accept | test-only script; serves the read-only mock dir on localhost; never bundled into the app |
| T-39-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan (harness uses the already-installed Playwright). If a static-server dep is needed, prefer Node's built-in `http` — no new dependency. |
</threat_model>

<verification>
- `npm run e2e` (mocked) fully green against feat/ui-2.
- Fidelity harness renders both sides and regenerates the gallery on demand.
- No new npm dependency; no shipped-code change (fixtures/spec/scripts only).
</verification>

<success_criteria>
RUNUI-09 (SC-4): the mocked e2e suite is green again and the fidelity screenshot harness runs under `frontend/e2e`.
</success_criteria>

<artifacts_produced>
- `frontend/e2e/fixtures/mockApi.ts`: new route stubs — `/api/workflows`, `/api/runs/{id}/family`, `/api/analytics/summary`, `/api/runs/{id}/gate-events|validation-results|exec-runs` (+ setters).
- `frontend/e2e/fixtures/dashboard.ts`: repaired `homeHeading`, `runWith` (Provide-the-brief flow), `thinkingTab`→Steps.
- `frontend/e2e/tests/zzz-baseline.spec.ts`: retained fidelity capture spec (env-gated), repo-relative outputs.
- `frontend/e2e/fidelity/serve-mocks.mjs`, `capture-mocks.mjs`, `README.md`: the two-sided fidelity harness.
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-07-SUMMARY.md` when done.
</output>
