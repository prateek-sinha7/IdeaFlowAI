---
phase: 39-run-screen-mock-fidelity-b5
plan: 06
type: execute
wave: 4
depends_on: ["39-05"]
files_modified:
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/src/components/preview/PreviewChrome.tsx
  - frontend/src/components/preview/PreviewPanel.test.tsx
autonomous: true
requirements: [RUNUI-06, RUNUI-07, RUNUI-08]
must_haves:
  truths:
    - "The Preview tab wraps the deliverable in a browser chrome: traffic-light dots, a URL/file bar with the deliverable name + version chip, and a zoom/open affordance"
    - "A 'Renders as' deliverable-type switch (segmented pills) sits under the chrome and forces a typed renderer for the current deliverable"
    - "The existing deliverable renderers are reused unchanged inside the chrome (D39-3) — not rebuilt"
    - "The streaming preview shows the animated progress bar + a 'building …' URL; failed preview keeps the degraded affordance"
  artifacts:
    - path: "frontend/src/components/preview/PreviewChrome.tsx"
      provides: "The browser-chrome frame (dots + URL bar + zoom) wrapping the renderer"
    - path: "frontend/src/components/preview/PreviewPanel.tsx"
      provides: "Preview tab mounts the chrome + the reskinned Renders-as switch"
  key_links:
    - from: "frontend/src/components/preview/PreviewChrome.tsx"
      to: "FIRST_PARTY_RENDERERS / renderDeliverable"
      via: "chrome wraps the reused renderer output"
      pattern: "renderDeliverable"
---

<objective>
Bring the **Preview** tab to the mock's composition (CONTEXT specifics #5): wrap the deliverable in the mock's **browser chrome** (traffic-light dots + a URL/file bar carrying the deliverable name + version chip + a zoom/open affordance) and reskin the existing renderer switcher into the mock's **"Renders as" segmented deliverable-type switch** — all WRAPPING the EXISTING deliverable renderers unchanged (D39-3, ND-G — do NOT rebuild them from the mock's hardcoded website). Add the streaming-preview affordance (animated progress bar + "building …" URL); keep the failed degraded affordance.

This plan touches `PreviewPanel.tsx` after 39-05 (same file) — hence Wave 4.

Purpose: Preview is where the deliverable is shown — the mock frames it in browser chrome with a type switch.
Output: new `PreviewChrome`; a mock-faithful "Renders as" switch; streaming preview; renderers reused.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-01-PLAN.md
@frontend/src/components/preview/PreviewPanel.tsx
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
Inherit ND-A..ND-H. Especially ND-G: the deliverable renderers (`UserStoryPreview`/`PPTPreview`/`PrototypePreview`/`AppBuilderPreview`/`MarkdownPreview`) are REUSED as-is inside the chrome; the mock's hardcoded Apple website/deck/doc content is NOT cloned. ND-D: the "Renders as" switch offers the typed renderers available for the ACTUAL deliverable (the existing `rendererOptions` set), NOT the mock's fixed 5-way Prototype/User-Stories/Deck/App/Doc list; the URL bar shows the REAL deliverable filename.
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: PreviewChrome (browser frame) wrapping the reused renderers + reskinned "Renders as" switch</name>
  <files>frontend/src/components/preview/PreviewChrome.tsx, frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/preview/PreviewPanel.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (Preview browser chrome :184-206 — traffic-light dots :188, centered URL bar with lock icon + filename + version chip :189-195, zoom "100%" + open-in-new :196-199; the "Renders as" switch row :201-205; the deliverable surface bg :206)
    - frontend/src/components/preview/PreviewPanel.tsx (Preview tab body :848-883, renderDeliverable :713-740, FIRST_PARTY_RENDERERS :632-649, the existing RendererSwitcher :957-984 + rendererOptions :678-693 [reuse/reskin these — do NOT rebuild the dispatch], hasContent/showFailureAffordance :569-592)
  </read_first>
  <action>
    Create `PreviewChrome.tsx`: the mock browser frame (`Hexaware Run.dc.html:184-206`) — a card with a top bar (three traffic-light dots, a centered URL/file bar showing a lock icon + the REAL deliverable filename + a version chip, and a right-side "100%" + open-in-new-tab affordance), a "Renders as" switch row beneath it, and a scrollable content surface (`#EEECE5`/token bg) into which the CHILDREN (the reused renderer output) are slotted. In PreviewPanel's Preview tab body, wrap the existing `renderDeliverable()` output in `PreviewChrome` (children = the current renderer — ND-G, the renderer itself is unchanged). Reskin the existing `RendererSwitcher` into the mock's "Renders as" SEGMENTED pill row: keep the existing `rendererOptions` derivation (Auto + the typed renderers genuinely available for THIS deliverable — SC-001/ND-D, not the mock's fixed 5-way) and the existing `rendererOverride` state + `renderRendererOverride` dispatch; only change the presentation to the segmented pills (active = ink fill, inactive = white/line). Pass the real filename (from the deliverable / `genericDeliverable.filename` / the derived name) + the active version label into the chrome URL bar. Keep the failure affordance (`DegradedRunAffordance`) and the empty state unchanged (they render WITHOUT the chrome, or inside it per the mock — state which; the failed mock shows the degraded card, not a chrome). Do NOT alter the deliverable dispatch table or the renderers.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `current/settled__preview.png` (deliverable area) vs `Hexaware Run.dc.html` Preview, via the 39-07 harness, closed to ND-A..ND-H (esp. ND-G reused renderer, ND-D real filename + type switch). Human sign-off.
    - The reused renderers are unchanged: no edits to `UserStoryPreview`/`PPTPreview`/`PrototypePreview`/`AppBuilderPreview`/`MarkdownPreview` (grep the diff — those files are NOT in `files_modified`).
    - The "Renders as" switch offers only the typed renderers for the current deliverable (grep PreviewPanel for the mock literals "User Stories","Deck","App code","Doc" as a FIXED 5-way array → absent; options come from `rendererOptions`).
    - The URL bar shows the real deliverable filename (asserted in vitest).
    - `npx --prefix frontend tsc --noEmit` clean; `PreviewPanel.test.tsx` chrome + switch cases green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && git diff --name-only | grep -Ec "UserStoryPreview|PPTPreview|PrototypePreview|AppBuilderPreview|MarkdownPreview" | grep -qx 0 && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/preview/PreviewPanel.test.tsx</automated>
  </verify>
  <done>The Preview tab frames the reused deliverable renderer in the mock browser chrome with a real-filename URL bar and a live "Renders as" switch; renderers untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Streaming preview (progress bar + building URL) and failed-state pass</name>
  <files>frontend/src/components/preview/PreviewChrome.tsx, frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/preview/PreviewPanel.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (streaming Preview :177-197 — chrome with a "building …html…" URL :184, the top indeterminate progress bar `@keyframes bar` :191, the content surface; note the mock's `image-slot` "drop a screenshot" placeholder :193 is ND-F and is NOT reproduced)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html (no dedicated Preview — the failed run surfaces the degraded/what-went-wrong affordance)
    - frontend/src/components/preview/PreviewPanel.tsx (isStreaming plumbing :580, the streaming header label :756, DegradedRunAffordance :857-863)
  </read_first>
  <action>
    Add the streaming variant to `PreviewChrome`: when `isStreaming`/`pipelineState.isRunning` and there is no settled deliverable yet, the URL bar reads "building {deliverable-name}…" and an indeterminate progress bar animates across the top of the content surface (a token-styled brand bar; add a `@keyframes` for the sweep in the component's scoped styles or globals). Do NOT reproduce the mock's `image-slot` "drop a screenshot" placeholder (ND-F) — the content surface shows the live renderer output (which already streams) or a calm building state. FAILED: keep the existing `showFailureAffordance` → `DegradedRunAffordance` path exactly (the failed mock shows the degraded card, not a chromed preview); confirm it still renders when the run is terminal-failed with no content. Verify the settled/live/failed Preview transitions do not regress the existing renderer dispatch, the read-only version override banner, or the copy/download controls.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `current/live__full.png` Preview area vs `Hexaware Run - Live.dc.html` Preview (progress bar + building URL, no image-slot per ND-F), via the 39-07 harness, closed to ND-A..ND-H. Human sign-off.
    - Failed run still renders `DegradedRunAffordance` (asserted in vitest with a terminal-failed, no-content pipelineState); no chrome wraps the failure card.
    - The mock's `image-slot` placeholder is NOT present (grep PreviewPanel/PreviewChrome for `image-slot` → 0; ND-F).
    - `npx --prefix frontend tsc --noEmit` clean; `PreviewPanel.test.tsx` streaming + failed cases green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -Rc "image-slot" src/components/preview/PreviewPanel.tsx src/components/preview/PreviewChrome.tsx | grep -qE ":0$" && npx vitest run src/components/preview/PreviewPanel.test.tsx</automated>
  </verify>
  <done>Streaming Preview shows the mock progress bar + building URL (no image-slot); failed Preview keeps the degraded affordance; no regression to the renderer dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deliverable content → renderer (inside chrome) | model-generated HTML/markdown rendered — the existing renderers own the sandbox contract |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06-01 | Tampering (XSS) | chrome wrapping HTML deliverables | mitigate | the chrome is a passive frame; the existing renderers keep their sandboxed-iframe contract (`GenericDeliverablePreview` sandbox="allow-scripts", no allow-same-origin) — unchanged by this plan (renderers not edited) |
| T-39-06-02 | Tampering (XSS) | URL bar filename render | mitigate | filename rendered as escaped text; no `dangerouslySetInnerHTML` in chrome (grep → 0) |
| T-39-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan. |
</threat_model>

<verification>
- Preview matches the settled + live mocks per the screenshot-diff oracle, closed to the register; failed keeps the degraded affordance.
- Deliverable renderers reused unchanged (ND-G); type switch + URL bar are live (ND-D); image-slot not reproduced (ND-F).
- `npx tsc --noEmit` clean; touched-component vitest green; renderer sandbox contract intact.
</verification>

<success_criteria>
RUNUI-06 (SC-1): Preview matches its mock (settled + live), screenshot-diff-proven.
RUNUI-07 (SC-2): the net-new "Renders as" deliverable-type switch lands.
RUNUI-08 (SC-3): the deliverable renderers are reused, not rebuilt; live data only.
</success_criteria>

<artifacts_produced>
- `frontend/src/components/preview/PreviewChrome.tsx` (NEW): browser-chrome frame (dots + URL bar + zoom + streaming progress bar) wrapping the reused renderer.
- Reskinned "Renders as" segmented switch (over the existing `rendererOptions`/`rendererOverride` dispatch, unchanged logic).
- Streaming preview affordance; preserved degraded/failed affordance; renderer dispatch untouched.
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-06-SUMMARY.md` when done.
</output>
