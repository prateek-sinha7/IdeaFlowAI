---
phase: 39-run-screen-mock-fidelity-b5
plan: 04
type: execute
wave: 2
depends_on: ["39-07"]
files_modified:
  - frontend/src/components/results/AuditTab.tsx
  - frontend/src/components/results/AuditTab.test.tsx
  - frontend/src/lib/exporters/auditExporter.ts
autonomous: true
requirements: [RUNUI-06, RUNUI-07, RUNUI-08]
must_haves:
  truths:
    - "Audit opens on an 'Audit trail' header (icon · title · N records) + an Export menu (CSV / JSON / report)"
    - "An attribution + compliance summary block shows run/owner/workspace/started/duration + a 6-stat grid (Checks/Passed/Warnings/Blocked/Denied/Secret-scans)"
    - "Coverage chips + a green 'passed all governance gates' banner render for a clean run; a red blocked/denied summary for a failed run"
    - "Rows carry a category chip (Governance/Security/Activity + finer sub-labels incl. secret-scan/performance/behavioral), a severity chip, a verdict badge, and a collapsible 'What is this?' body"
    - "Every stat/row is derived from the real gate-events/validation-results/exec-runs fetches (SC-001) — no cloned mock rows"
  artifacts:
    - path: "frontend/src/components/results/AuditTab.tsx"
      provides: "6-stat grid + coverage banner + fuller categories over the real fetches"
      contains: "governance gates"
  key_links:
    - from: "frontend/src/components/results/AuditTab.tsx"
      to: "getRunGateEvents / getRunValidationResults / getRunExecRuns"
      via: "real audit fetches feeding the grid/rows"
      pattern: "getRunGateEvents"
---

<objective>
Bring the **Audit** tab to the mock's composition (CONTEXT specifics #7) — a **6-stat compliance grid**, **coverage chips**, a green **"passed all governance gates" banner** (red for a failed run), an **attribution row**, an **Export menu**, and the **fuller category taxonomy** (Governance / Security / Activity with finer sub-labels: gate, validation, secret-scan, exec/denied, performance, behavioral-hook) — layered on top of the EXISTING real fetches (`getRunGateEvents`/`getRunValidationResults`/`getRunExecRuns`). The tab is already real, not a stub (D39-4); this is presentation + a fuller category mapping, plus the net-new categories (RUNUI-07 / SC-2).

Folds W6 States: the failed/blocked audit variant (blocked+denied+secrets-hit stats, red banner, blocked-only filter) is delivered here.

Purpose: Audit is the governance/compliance surface — it must match the mock's grid + banner + taxonomy.
Output: `AuditTab` reskinned to the mock with the stat grid, coverage banner, attribution, export menu, and fuller categories over real data.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-01-PLAN.md
@frontend/src/components/results/AuditTab.tsx
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
Inherit the INTENDED-DIVERGENCE REGISTER (ND-A..ND-G) from `39-01-PLAN.md`. ND-D: every stat/row/attribution value comes from the real gate/validation/exec fetches — never the mock's fixed rows ("Behavioral guideline active — Quality Gate", "09:00:57", "f3a1c9…e42"). If a category (e.g. behavioral-hook / performance) has NO real source for a given run, its stat is 0 and the row group is empty — never fabricated.
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: Header + attribution + 6-stat compliance grid + coverage banner + export menu</name>
  <files>frontend/src/components/results/AuditTab.tsx, frontend/src/components/results/AuditTab.test.tsx, frontend/src/lib/exporters/auditExporter.ts</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (Audit header + Export menu :677-699; attribution + compliance summary :701-725 — the run/owner/workspace/started/duration line, the 6-stat grid Checks/Passed/Warnings/Blocked/Denied/Secret-scans :710-717, coverage chips :718-720, the green "0 blocked · 0 denied · 0 critical — run passed all governance gates" banner :721-724; filters :727-735)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html (failed audit 5-stat grid incl. "Secrets hit" :136-142, "Blocked only" toggle :147, red-tinted stats)
    - frontend/src/components/results/AuditTab.tsx (existing real fetches :230-295, counts memo :304-320, header/coverage/counters :368-430, export bar :432-457)
  </read_first>
  <action>
    Reskin the AuditTab header to the mock (`Hexaware Run.dc.html:677-699`): an "Audit trail" title (Manrope 300 22px) with the shield icon + an "N records" pill + the subline. Replace the plain CSV/JSON export bar with the mock's brand "Export ▾" MENU (CSV — flattened rows / JSON — raw hook_runs / Compliance report) reusing the existing `exportAuditCSV`/`exportAuditJSON` for the first two (the report option may be a disabled/"coming soon" entry — do NOT fabricate a PDF path; a client-only print/JSON is acceptable, state which). Add the attribution + compliance summary CARD (`:701-725`): an attribution row (Run id / Owner / Workspace / Started / Duration — from the run + summary data available; thread a small `runMeta?` prop or reuse `workflowRunId` + what the fetches carry, ND-D live values, elide fields with no source rather than fabricate), the **6-stat grid** (Checks, Passed, Warnings, Blocked, Denied, Secret-scans) computed from the existing `counts` memo (extend it to add warnings + secret-scan totals — secret-scans = the security/secret-scan category count; warnings = the amber verdict count), the **coverage chips** (one per category present), and the **verdict banner**: green "0 blocked · 0 denied · 0 critical — run passed all governance gates." when clean, else the red blocked/denied/critical summary (drive off the computed counts, ND-D). Route all colors through the status/severity tokens (the SOLE one-chroma exception already documented in the file). Keep the fetch logic + severity filters untouched.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: the Audit header + attribution + 6-stat grid + coverage banner of `current/settled__audit.png` vs `Hexaware Run.dc.html` Audit, via the 39-07 harness, closed to ND-A..ND-G. Human sign-off.
    - Stats/banner derive from the real fetches (grep `AuditTab.tsx` for the mock literals "14.8M"/"f3a1c9"/"09:00:57" → absent; the banner text is computed from `counts`, not a constant string).
    - The green "passed all governance gates" banner renders for a 0-blocked/0-denied/0-critical run and the red summary for a blocked run (both asserted in vitest with stubbed fetch data).
    - `npx --prefix frontend tsc --noEmit` clean; `AuditTab.test.tsx` grid + banner + export-menu cases green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "getRunGateEvents" src/components/results/AuditTab.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && npx vitest run src/components/results/AuditTab.test.tsx</automated>
  </verify>
  <done>Audit header + attribution + 6-stat grid + coverage banner + export menu match the mock on real fetch data (green pass / red fail).</done>
</task>

<task type="auto">
  <name>Task 2: Fuller category taxonomy + collapsible 'What is this?' rows (net-new categories)</name>
  <files>frontend/src/components/results/AuditTab.tsx, frontend/src/components/results/AuditTab.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (row list + collapsible body :737-766 — the outcome icon-wrap, category chip, severity chip, verdict badge, and the "What is this?" violet explainer + key/value rows; the CAT/SEV/OUT maps in the script :1020-1039; integrity footer :768-772)
    - frontend/src/components/results/AuditTab.tsx (AuditRowCard :175-210, CATEGORY_META :158-162, verdict/severity chips :99-154, category filters :406-429)
  </read_first>
  <action>
    Extend the audit taxonomy to the mock's fuller category set while staying keyed on the REAL fetch data (SC-001/ND-D). Map each real row to a finer category label — gate→"Gate", validation→"Validation", exec→"Exec", and derive the net-new sub-categories from the real data where the signal exists: secret-scan (a gate/validation row whose kind/label denotes a secret scan → "Security"), performance (an exec/otel-span row → "Perf"), behavioral-hook (a hook/guideline row if `hookRuns` is present → "Governance"). Group these into the mock's three filter groups (Governance / Security / Activity) — extend the filter row to the mock's All/Governance/Security/Activity chips with live counts (in ADDITION to the existing severity filters, or replacing them per the mock — keep severity filtering reachable). Reskin `AuditRowCard` to the mock row (`:737-766`): the circular outcome icon-wrap (pass green check / warn amber / block red), the title, the "{agent} · {target} · {time}" meta, a category chip, an optional severity chip, a verdict badge, a chevron, and a COLLAPSIBLE body with the violet "What is this?" explainer (a short static description per category — a UI affordance, not fabricated run data) + the key/value detail rows from the row's `detail`. Add the "Blocked / denied only" toggle and the dashed integrity footer. Do NOT invent rows for categories with no real source — an empty category shows 0 and no rows (ND-D).
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: the Audit filter row + row list + expanded "What is this?" body of `current/settled__audit.png` (and a blocked run for the failed variant) vs the Audit mocks, via the 39-07 harness, closed to ND-A..ND-G. Human sign-off.
    - The fuller categories (secret-scan/performance/behavioral) render ONLY when the real data carries a matching row (grep for hardcoded mock rows like "Behavioral guideline active — Quality Gate" as literals → absent; the categories are derived).
    - Rows collapse/expand with a "What is this?" explainer + key/value detail (asserted in vitest).
    - `npx --prefix frontend tsc --noEmit` clean; `AuditTab.test.tsx` category-grouping + expand cases green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "Behavioral guideline active — Quality Gate" src/components/results/AuditTab.tsx | grep -qx 0 && npx vitest run src/components/results/AuditTab.test.tsx</automated>
  </verify>
  <done>Audit rows carry the fuller category taxonomy + severity + verdict + collapsible explainer, all derived from real fetch data; net-new categories appear only when the data supports them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| audit fetch endpoints → tab | owner-scoped reads (404→[] on cross-owner) — unchanged |
| row detail (validator/exec output) → row body | model/tool-derived text rendered |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04-01 | Information disclosure | attribution row (owner/workspace) | mitigate | render only fields the owner-scoped fetches already return for the current user's own run; elide unknown fields, never guess |
| T-39-04-02 | Tampering (XSS) | row detail / "What is this?" | mitigate | React escaping; the explainer text is static UI copy; no `dangerouslySetInnerHTML` (grep → 0) |
| T-39-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan. |
</threat_model>

<verification>
- Audit matches the settled + failed mocks per the screenshot-diff oracle, closed to the register.
- Every stat/row derives from the real gate/validation/exec fetches (ND-D); net-new categories appear only with real data.
- `npx tsc --noEmit` clean; touched-component vitest green.
</verification>

<success_criteria>
RUNUI-06 (SC-1): Audit matches its mock (settled + failed), screenshot-diff-proven.
RUNUI-07 (SC-2): the fuller Audit categories (secret-scan / performance / behavioral) land.
RUNUI-08 (SC-3): live/real fetch data; no fabricated rows.
</success_criteria>

<artifacts_produced>
- `AuditTab` reskinned: header + Export menu, attribution + 6-stat compliance grid, coverage chips + verdict banner, fuller category taxonomy (Governance/Security/Activity + secret-scan/performance/behavioral), collapsible "What is this?" rows, integrity footer.
- Extended `counts` memo (warnings + secret-scan totals); category-derivation from the real fetch data.
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-04-SUMMARY.md` when done.
</output>
