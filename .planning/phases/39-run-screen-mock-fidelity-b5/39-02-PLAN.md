---
phase: 39-run-screen-mock-fidelity-b5
plan: 02
type: execute
wave: 2
depends_on: ["39-07"]
files_modified:
  - frontend/src/components/results/AgentThinkingTab.tsx
  - frontend/src/components/results/StepsOverviewSpine.tsx
  - frontend/src/components/results/AgentDetailPanel.tsx
  - frontend/src/components/results/TaskDetailPanel.tsx
  - frontend/src/components/results/AgentThinkingTab.test.tsx
autonomous: false
requirements: [RUNUI-06, RUNUI-08]
must_haves:
  truths:
    - "Steps opens on a glanceable overview: status line + progress + Clarifications card + a one-line navigable agent spine with inline gate strips"
    - "Clicking an agent drills into a 2-column detail: full execution (reasoning/artifact/tools/input/output) on the left, a sticky 'Context received' panel (from contextSources) on the right"
    - "The Build/construction agent drills a third level to a single task detail"
    - "Live shows running node + streaming caret + inline Awaiting-you gate/clarify; failed shows the failed node + reason card + 'Pipeline halted' banner"
    - "Every value is derived from live agents/pipelineState (SC-001) — no cloned mock transcript"
  artifacts:
    - path: "frontend/src/components/results/StepsOverviewSpine.tsx"
      provides: "The L1 overview spine (status line, progress, navigable agent rows, gate strips)"
    - path: "frontend/src/components/results/AgentDetailPanel.tsx"
      provides: "The L2 2-column agent detail with the sticky Context-received panel"
    - path: "frontend/src/components/results/TaskDetailPanel.tsx"
      provides: "The L3 single-task detail"
  key_links:
    - from: "frontend/src/components/results/AgentDetailPanel.tsx"
      to: "agent.contextSources"
      via: "sticky 'Context received' panel"
      pattern: "contextSources"
    - from: "frontend/src/components/results/StepsOverviewSpine.tsx"
      to: "InlineGateActions / InlineClarifyActions"
      via: "inline gate strips + clarify (reused, not rebuilt)"
      pattern: "InlineGateActions"
---

<objective>
Reorganize the **Steps** tab (currently a flat vertical list of agent cards) into the mock's three-level navigation: **overview spine → 2-column agent detail with a sticky "Context received" panel → task detail** (CONTEXT specifics #4). The data already flows — `AgentRunState` carries `inputPrompt`, `contextSources`, `toolCalls`, `thinkingText`, `output`; construction fan-out comes via `waves`/`protoCompletedTaskCount` (D39-4). This is presentation reorganization, not new data.

Folds W6 States into this surface: live (running node, streaming caret, inline Awaiting-you gate/clarify — reuse the existing `InlineGateActions`/`InlineClarifyActions`) and failed (failed node + reason card + "Pipeline halted" banner) are delivered here.

Purpose: Steps is the run's explainability surface — it must match the mock's drill-down.
Output: `AgentThinkingTab` restructured into overview/detail/task views with the sticky Context panel, all token-reskinned, across three states.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-01-PLAN.md
@frontend/src/components/results/AgentThinkingTab.tsx
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
Inherit the INTENDED-DIVERGENCE REGISTER (ND-A..ND-G) from `39-01-PLAN.md`. Especially ND-D: every step value (durations, tokens, "context received" sizes, reasoning, tool args) is LIVE from `AgentRunState`/`pipelineState` — never the mock's fixed "97s / 30.1k / 14.8M / spec.md 36.4 KB" transcript.
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: L1 overview spine + L2 2-column agent detail with the sticky Context-received panel</name>
  <files>frontend/src/components/results/StepsOverviewSpine.tsx, frontend/src/components/results/AgentDetailPanel.tsx, frontend/src/components/results/AgentThinkingTab.tsx, frontend/src/components/results/AgentThinkingTab.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (STEPS overview :294-354 — status line+progress, Clarifications collapsible :312-332, navigable agent rows + gate strips :335-352; L2 detail 2-col grid :356-580 — agent card body reasoning/artifact/tools/input/output, the sticky "Context received" side panel :562-579)
    - frontend/src/components/results/AgentThinkingTab.tsx (current flat list — PipelineHeader :90-142, ContextSourcesRow :310-345, ToolCallsSection :348-404, InputPromptSection/OutputPreviewSection :406-473, AgentTimelineCard :483-610, main :700-879; the reused InlineGateActions/InlineClarifyActions :802-829, StartingPointCard/ClarificationsCard mounts :783-795)
    - frontend/src/components/results/ClarificationsCard.tsx and StartingPointCard.tsx (reused as-is)
  </read_first>
  <action>
    Introduce a view-state to `AgentThinkingTab` (`overview | agentDetail | taskDetail`) with a selected agent id, mirroring the mock's `stepView`/`taskView`. Extract `StepsOverviewSpine.tsx`: the L1 overview = a status line ("Run complete" / running / failed) + a segmented progress bar (per-agent segments from `pipelineState.agents[].status`, brand fill), the existing `ClarificationsCard` (reused, styled to the mock's collapsible Clarifications card), and a COMPACT navigable agent spine — one row per visible agent (done check / running pulse / idle ring node · name · `dur · tok` meta · chevron) that on click opens L2. Between rows, render the inline **gate strips** ("Review gate — {gate} · approved by you") derived from the run's gate events / `laneGate` (reuse the existing gate data; do not invent). Extract `AgentDetailPanel.tsx`: the L2 = a `grid-template-columns:1fr 288px` layout — LEFT is the full agent execution (reuse the existing reasoning/artifact/tools/input/output sub-sections: `ToolCallsSection`, `InputPromptSection`, `OutputPreviewSection`, plus a reasoning block from `thinkingText`; token-reskin them to the mock's violet reasoning / paper input / violet output cards), RIGHT is a `position:sticky` "Context received" panel built from `agent.contextSources` (source name + size/compression meta, "N sources fed in", the "The kernel assembled these into this agent's prompt before it ran." footer). Reuse `ContextSourcesRow`'s derivation logic for names/sizes/compression (INV-12 — factor the shared source-formatting into a helper rather than duplicate). In the main component, render `StartingPointCard` + `PlannerCard` + `ClarificationsCard` + the overview spine in overview mode; render `AgentDetailPanel` (with a breadcrumb "Steps / {agent}") in agentDetail mode. Retire the flat `AgentTimelineCard` list (its content moves into `AgentDetailPanel`; delete the superseded flat-list render — INV-3, no dual list). Token-reskin ALL of this off Phase-32 tokens (retire the `gray-*` palette in the touched code).
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `current/settled__steps.png` (overview) and `current/settled__steps-detail.png` (L2) vs `target/` `Hexaware Run.dc.html` Steps overview + agent detail, via the 39-07 harness, closed to ND-A..ND-G (esp. ND-D live values). This autonomous task generates the current/* capture(s) mechanically (tsc clean + vitest green + the banned-literal greps); the human fidelity approval is this plan's trailing checkpoint task.
    - The sticky "Context received" panel renders from `agent.contextSources` (grep `AgentDetailPanel.tsx` for `contextSources` → present; grep for the mock literal "spec.md 36.4 KB" → absent).
    - INV-3: the flat `AgentTimelineCard` vertical-list render is removed (grep `AgentThinkingTab.tsx` for a `.map(AgentTimelineCard)`-style flat list → 0; content lives in the drill-down).
    - Retired palette gone from touched files: `grep -v '^\s*//' | grep -c "text-gray-\|bg-gray-\|border-gray-"` on the three touched components == 0.
    - `npx --prefix frontend tsc --noEmit` clean; `AgentThinkingTab.test.tsx` asserts overview→detail navigation + the Context panel from `contextSources`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && grep -q contextSources src/components/results/AgentDetailPanel.tsx && npx vitest run src/components/results/AgentThinkingTab.test.tsx</automated>
  </verify>
  <done>Steps opens on the overview spine and drills to the 2-column agent detail with a live sticky Context-received panel; flat list retired; screenshot-diff signed off.</done>
</task>

<task type="auto">
  <name>Task 2: L3 task detail + live (streaming) and failed Steps states</name>
  <files>frontend/src/components/results/TaskDetailPanel.tsx, frontend/src/components/results/AgentThinkingTab.tsx, frontend/src/components/results/StepsOverviewSpine.tsx, frontend/src/components/results/AgentThinkingTab.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (construction waves + navigable task rows :439-476; TASK DETAIL L3 :584-609 — single task card with reasoning + tool calls)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (running node/pill :262/900-937; streaming caret in reasoning :319; awaiting gate card :269-278; clarifications-awaiting inline answer UI :222-232; running progress segs :892-893)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html (failed node + reason card :110-124; "Pipeline halted" banner :124)
    - frontend/src/components/results/AgentThinkingTab.tsx (ConstructionDrilldown :624-694 + WaveTreePanel mount; the reused InlineGateActions/InlineClarifyActions :802-829)
  </read_first>
  <action>
    Extract `TaskDetailPanel.tsx` (L3): a single construction task's detail (status node, "Task N · {title}", duration, a reasoning card, and the task's tool-call rows) sourced from the `waves`/construction data already threaded (`ConstructionDrilldown` inputs). Wire the L2 construction block's per-task rows to open L3 (a "Build Agent / task detail" breadcrumb + back). LIVE state: in `StepsOverviewSpine`, the running agent row uses the pulsing brand node + a "LIVE" pill and the progress segment animates; in `AgentDetailPanel`, the running agent's reasoning shows the streaming caret (reuse the existing `▌` streaming affordance) and a "Reasoning (live)" label; keep the reused `InlineClarifyActions` (clarify-awaiting: inline answer buttons + submit) and `InlineGateActions` (gate-awaiting: approve/request-changes) mounted inline in the Steps trace — restyle their wrappers to the mock's "Awaiting you" card chrome (brand-tinted, focus-ring box-shadow) WITHOUT changing their submit channels (SC-001/INV-12). FAILED state: the failed agent row uses the red node + "Failed" badge, `AgentDetailPanel` shows a red reason card (from the agent's `error`, sanitized), skipped agents render as "Not run" grey nodes, and a "Pipeline halted — N agents did not run. Reopen to resume…" banner renders at the foot of the overview spine (derived from the failed/degraded pipeline state — ND-D, never the mock's fixed "Security Agent" text unless the real run says so). Do NOT rebuild `WaveTreePanel`/`ConstructionDrilldown` internals — reuse and restyle.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: `current/live__steps.png` vs `Hexaware Run - Live.dc.html` Steps, and `current/failed__steps.png` vs `Hexaware Run - Failed.dc.html` Steps, via the 39-07 harness, closed to ND-A..ND-G. This autonomous task generates the current/* capture(s) mechanically (tsc clean + vitest green + the banned-literal greps); the human fidelity approval is this plan's trailing checkpoint task.
    - L3 task detail opens from an L2 construction task row and back-navigates (asserted in vitest).
    - Live: the running agent shows the streaming caret + LIVE pill; the awaiting gate/clarify cards reuse `InlineGateActions`/`InlineClarifyActions` (grep `AgentThinkingTab.tsx`/`StepsOverviewSpine.tsx` → both imports present; no forked clarify/gate submit).
    - Failed: the "Pipeline halted" banner + failed reason card derive from pipeline state (grep for the mock literal "Security Agent" hardcoded → absent).
    - `npx --prefix frontend tsc --noEmit` clean; vitest green for the new L3 + state cases.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && grep -q "InlineGateActions\|InlineClarifyActions" src/components/results/AgentThinkingTab.tsx && npx vitest run src/components/results/AgentThinkingTab.test.tsx</automated>
  </verify>
  <done>Steps drills to L3 task detail and matches the mock across live (streaming/awaiting) and failed (halted) on live data; reused gate/clarify/wave components preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fidelity checkpoint — human review of the Steps tab vs the mocks (closed to ND-A..ND-H)</name>
  <files>frontend/e2e/fidelity/gallery.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (that is the exact prose-claim drift D39-6 exists to prevent, and a pixel-diff-vs-mock is invalid here because ND-D live data never equals the mock's hardcoded values). Regenerate this surface's side-by-side gallery section: refresh our side via `FIDELITY_CAPTURE=1 npm --prefix frontend run e2e -- zzz-baseline` (current/steps__*), the target side via `node frontend/e2e/fidelity/capture-mocks.mjs`, then `node frontend/e2e/fidelity/assemble-gallery.mjs --surface steps`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The Steps tab across settled (overview → agent detail → task detail), live (streaming/awaiting), and failed (halted), with the prior autonomous tasks' mechanical gates already green (npx tsc --noEmit clean, touched-component vitest green, banned-literal greps 0).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery.html and find the steps section. For each of settled (overview → agent detail → task detail), live (streaming/awaiting), and failed (halted), compare the mock (left — Hexaware Run.dc.html / Hexaware Run - Live.dc.html / Hexaware Run - Failed.dc.html (Steps)) against ours (right). Confirm every visible difference is one of the registered ND-A..ND-H divergences (captioned "expected — ignore"); any OTHER visual departure is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the Steps tab matches its mock across settled (overview → agent detail → task detail), live (streaming/awaiting), and failed (halted) closed to the intended-divergence register, or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent output/error/tool-args → Steps detail | untrusted model/tool text rendered |
| gate/clarify submit → resume channel | user actions cross into the run engine (unchanged transport) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Tampering (XSS) | reasoning/output/tool-arg rendering | mitigate | render as text via React escaping / existing `<pre>` paths; no new `dangerouslySetInnerHTML` (grep → 0) |
| T-39-02-02 | Elevation of privilege | inline gate approve/reject | accept | reuses the existing `InlineGateActions` submit channel + its KAN-100 terminal fence; no new authority path |
| T-39-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan. |
</threat_model>

<verification>
- Steps matches the three mocks (overview/detail/task × settled/live/failed) per the screenshot-diff oracle, closed to the register.
- Live data only (ND-D); reused gate/clarify/wave components not forked (INV-12); flat list retired (INV-3).
- `npx tsc --noEmit` clean; touched-component vitest green.
</verification>

<success_criteria>
RUNUI-06 (SC-1): Steps matches its mock across the three states + all sub-navigation, screenshot-diff-proven.
RUNUI-08 (SC-3): live/real data; renderers/gate/clarify reused, not rebuilt.
</success_criteria>

<artifacts_produced>
- `frontend/src/components/results/StepsOverviewSpine.tsx` (NEW): L1 overview spine + gate strips + halted banner.
- `frontend/src/components/results/AgentDetailPanel.tsx` (NEW): L2 2-column agent detail + sticky Context-received panel.
- `frontend/src/components/results/TaskDetailPanel.tsx` (NEW): L3 task detail.
- `AgentThinkingTab` restructured into overview/agentDetail/taskDetail views; flat `AgentTimelineCard` list retired; shared context-source formatting helper.
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-02-SUMMARY.md` when done.
</output>
