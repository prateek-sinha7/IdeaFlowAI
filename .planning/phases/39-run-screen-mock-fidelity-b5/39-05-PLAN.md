---
phase: 39-run-screen-mock-fidelity-b5
plan: 05
type: execute
wave: 3
depends_on: ["39-01", "39-07"]
files_modified:
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/src/components/preview/RunHeader.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/preview/RunHeader.test.tsx
  - frontend/src/components/preview/PreviewPanel.test.tsx
autonomous: false
requirements: [RUNUI-06, RUNUI-07]
must_haves:
  truths:
    - "The right column has a run-header row above the tabs: a Version menu (from runFamily), a Share button, and a Download button"
    - "The tab order is Preview then Steps then Files then Audit (was Preview, Files, Steps, Audit)"
    - "The header carries a live status badge (streaming / Awaiting you / Paused / Failed) and the Steps tab shows a pulsing review dot when the run is paused"
    - "Share copies a shareable link to the run (client-only in v1, no backend); disabled while running/failed per the mock"
    - "The lane Back-to-history is wired and run metadata flows to both the lane header and the run header"
  artifacts:
    - path: "frontend/src/components/preview/RunHeader.tsx"
      provides: "The right-column run header (Version menu / Share / Download / status badge)"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/preview/RunHeader.tsx"
      to: "runFamily (sorted members) + handleSelectVersion"
      via: "Version menu"
      pattern: "runFamily"
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "RunChatLane onBackToHistory + run meta"
      via: "lane wiring"
      pattern: "onBackToHistory"
---

<objective>
Assemble the mock's right-column **run header** (CONTEXT specifics #2): a **Version dropdown** (from the existing `runFamily`), a **Share** button, and a **Download** button, above the tab row — and fix the **tab order** to Preview then Steps then Files then Audit (D39 specifics #3). Add the header **status badge** (streaming / Awaiting you / Paused / Failed) and the **Steps review dot** (Live mock). Land the net-new **Share** affordance (RUNUI-07 / SC-2). Wire `DashboardLayout` so the lane `onBackToHistory` (defined in 39-01) and the run metadata reach both the lane header and the run header.

**Share decision (Claude's discretion, D39-2 / scope fence):** Share is **client-only in v1** — it copies a deep link to the run (the owner-auth-gated run route) to the clipboard. NO new backend endpoint, NO persistence, NO `ArtifactRef` write, NO migration. Rationale: the mock depicts only a Share BUTTON (no recipient view / share-settings surface), so a functional copy-link is a faithful as-is reproduction; a true public/unauth share token would add a new unauthenticated read surface not shown in the mock and is out of the visual scope. Registered as ND-H below (an additive, minimal interpretation — NOT a scope reduction of anything the mock shows).

Folds W6 States: the header status badge + Steps review dot for live/paused/failed are delivered here.

Purpose: the run header is the run top-level action bar — it must match the mock.
Output: new `RunHeader`; corrected tab order; Version menu / Share / Download; status badge; lane + header wiring.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-CONTEXT.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-UI-SPEC.md
@.planning/phases/39-run-screen-mock-fidelity-b5/39-01-PLAN.md
@frontend/src/components/preview/PreviewPanel.tsx
@frontend/src/components/preview/LiveVersionChip.tsx
@frontend/CLAUDE.md
</context>

<intended_divergence_note>
Inherit ND-A..ND-G from `39-01-PLAN.md`. This plan adds ND-H: Share ships as a client-only "copy run link" (no backend, no token persistence) rather than a public share token — additive-only + no new unauth surface (D39-2, scope fence), faithful to the button the mock shows. Version menu + Download + status badge are LIVE (ND-D): versions from `runFamily`, download of the real deliverable, status from `runState`/`pipelineState`.
</intended_divergence_note>

<tasks>

<task type="auto">
  <name>Task 1: RunHeader — Version menu (from runFamily) + client-only Share + Download + status badge + tab order</name>
  <files>frontend/src/components/preview/RunHeader.tsx, frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/preview/RunHeader.test.tsx, frontend/src/components/preview/PreviewPanel.test.tsx</files>
  <read_first>
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run.dc.html (run header row :144-173 — Version button + dropdown :145-165, Share :167-169, Download :170-172; tab row :175-179)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Live.dc.html (header status badge streaming/awaiting/paused :159-166, disabled Share :165, "v1 draft" chip :164, Steps tab review dot :170)
    - /Users/1000060523/Documents/Work/VelocityAI-New-UI/Hexaware Run - Failed.dc.html (failed header badge "Run failed at the security gate" :94-98, "v1 · partial" chip)
    - frontend/src/components/preview/PreviewPanel.tsx (header :751-843 — LiveVersionChip mount :762-769, TAB_CONFIG :331-339 which MUST reorder to Preview·Steps·Files·Audit, the family/version derivation sortedMembers/handleSelectVersion :468-506, tab bar :804-843)
    - frontend/src/components/preview/LiveVersionChip.tsx (the existing version-menu logic to reuse — do not duplicate)
  </read_first>
  <action>
    Reorder `TAB_CONFIG` to Preview, Steps, Files, Audit (move the "thinking"/Steps entry before "files"; keep the internal id "thinking" + testids stable per the existing in-file comment). Create `RunHeader.tsx`: the mock header ROW (`Hexaware Run.dc.html:144-173`) rendered ABOVE the tab row inside PreviewPanel's right column — a Version button + dropdown built on the EXISTING `runFamily` derivation (`sortedMembers`, `activeIdx`, `handleSelectVersion`, `handleBackToLatest` already computed in PreviewPanel — pass them in as props / lift the minimal state; reuse `LiveVersionChip`'s menu internals rather than rebuild — extract a shared `VersionMenu` if cleaner, INV-12), a "Share" button, and a primary "Download" button (to the primary-deliverable download; reuse the existing content/PPT download path or a passed `onDownload`). Add the STATUS BADGE at the left of the header driven by `runState`/`pipelineState` (SC-001/ND-D): streaming/"Running" (brand, spinner) while building, "Waiting on you"/"Paused · review gate" for clarify/gate, a generic "Run failed" (red) when failed, plus a version chip ("v1" / "v1 draft" / "v1 · partial") per state. Add the pulsing review DOT to the Steps tab when paused (clarify/gate) — thread a `paused` flag into the Tabs mount or render it adjacent to the Steps tab. Share = CLIENT-ONLY: an `onShare` handler that copies the run deep link (the current run route + run id) to the clipboard and shows a transient "Link copied" affordance; DISABLED while running/failed (mock `cursor:not-allowed`). NO backend call, NO new endpoint. Relocate the existing LiveVersionChip out of the small PreviewPanel header (its logic now lives in RunHeader) so there are NOT two version affordances (INV-3). Fold or drop the old Copy/Collapse controls per the mock (state which); route all styling through Phase-32 tokens.
  </action>
  <acceptance_criteria>
    - SCREENSHOT-DIFF: the run header + tab row of `current/settled__preview.png` vs `Hexaware Run.dc.html`, and the live/failed header of `current/live__full.png` / `current/failed__full.png` vs the Live/Failed mocks, via the 39-07 harness, closed to ND-A..ND-H. This autonomous task generates the current/* capture(s) mechanically (tsc clean + vitest green + the banned-literal greps); the human fidelity approval is this plan's trailing checkpoint task.
    - Tab order is Preview, Steps, Files, Audit (assert the rendered tab sequence in vitest; the "thinking" testid still resolves to the Steps tab).
    - Version menu lists the live `runFamily` members and selecting one drives `handleSelectVersion` (vitest, ≥2-member family); exactly ONE version affordance exists (grep PreviewPanel for a remaining `LiveVersionChip` mount is 0; INV-3).
    - Share copies a link client-side (clipboard spy) and is disabled while running; NO fetch/endpoint call from RunHeader (ND-H).
    - `npx --prefix frontend tsc --noEmit` clean; `RunHeader.test.tsx` + `PreviewPanel.test.tsx` green.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "LiveVersionChip" src/components/preview/PreviewPanel.tsx | grep -qx 0 && grep -Ec "fetch\(|/api/" src/components/preview/RunHeader.tsx | grep -qx 0 && npx vitest run src/components/preview/RunHeader.test.tsx src/components/preview/PreviewPanel.test.tsx</automated>
  </verify>
  <done>The run header (Version menu / client-only Share / Download / status badge) matches the mock across states, tab order is corrected, and a single version affordance remains.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardLayout wiring — lane Back-to-history + run metadata to the lane and run headers</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/preview/PreviewPanel.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx (RunChatLane mount :1707-1746, PreviewPanel mount :1783-1830, runLaneState :1344-1350, laneGate :1355-1367, runFamily fetch :384-390, handleGoHome :973, submittedBrief :303/1811)
    - frontend/src/components/chat/RunChatLane.tsx (the 39-01 OPTIONAL props: onBackToHistory, runTitle, runType)
    - frontend/src/components/preview/PreviewPanel.tsx (the RunHeader mount added in Task 1)
  </read_first>
  <action>
    Wire the DashboardLayout run shell so the two new headers get their data. RunChatLane mount: pass `onBackToHistory={() => setMainView("history")}` (or the existing history-nav path), `runTitle` (from `submittedBrief` / the run title), and `runType` (from `workflowType` / `pipelineState.pipeline_type`) — these are the 39-01 optional props; the lane already receives `pipelineState` for elapsed/agents/tokens. PreviewPanel mount: ensure `runFamily`, `liveRunId`, `runState`/`pipelineState`, and a `runTitle`/`runType` reach the new RunHeader (add pass-throughs on PreviewPanel's props as needed, all optional/default-undefined so history + test renders are unchanged — tsc identity). Provide the `onDownload` for the header (reuse the primary deliverable download already available in PreviewPanel, or the FilesTab download-all path) and the `onShare` (compose the run deep link from `liveRunId`/`contentSourceRunId`). Do NOT change `runLaneState`/`laneGate`/`runFamily` derivation — only thread existing values into the new headers. Keep all Phase-32/33 wiring (gate/clarify/waves/deep-link) intact.
  </action>
  <acceptance_criteria>
    - The lane Back-to-history link renders and navigates to the history view (vitest or e2e assertion); run type/title/meta render in both headers from live values (ND-D).
    - PreviewPanel new pass-through props are optional/default-undefined — existing history + test renders are byte-unchanged (`npx --prefix frontend tsc --noEmit` identity vs baseline; existing PreviewPanel/DashboardLayout suites stay green).
    - SCREENSHOT-DIFF re-run of `current/settled__leftlane.png` (lane header now shows Back-to-history + live meta) closed to ND-A..ND-H. This autonomous task generates the current/* capture(s) mechanically (tsc clean + vitest green + the banned-literal greps); the human fidelity approval is this plan's trailing checkpoint task.
    - No change to `runLaneState`/`laneGate`/`runFamily` logic (grep the diff — only additive prop passing).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "onBackToHistory" src/components/layout/DashboardLayout.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <done>Both headers receive live run metadata; the lane Back-to-history works; existing renders unregressed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fidelity checkpoint — human review of the run header + tab order vs the mocks (closed to ND-A..ND-H)</name>
  <files>frontend/e2e/fidelity/gallery.html (generated)</files>
  <action>Run automation FIRST, then PAUSE for a human — do NOT self-certify the fidelity match (that is the exact prose-claim drift D39-6 exists to prevent, and a pixel-diff-vs-mock is invalid here because ND-D live data never equals the mock's hardcoded values). Regenerate this surface's side-by-side gallery section: refresh our side via `FIDELITY_CAPTURE=1 npm --prefix frontend run e2e -- zzz-baseline` (current/preview__*), the target side via `node frontend/e2e/fidelity/capture-mocks.mjs`, then `node frontend/e2e/fidelity/assemble-gallery.mjs --surface preview`. Then stop and hand the assembled gallery to the reviewer.</action>
  <what-built>The run header + tab order across settled, live (streaming/awaiting/paused), and failed, with the prior autonomous tasks' mechanical gates already green (npx tsc --noEmit clean, touched-component vitest green, banned-literal greps 0).</what-built>
  <how-to-verify>Open frontend/e2e/fidelity/gallery.html and find the preview section. For each of settled, live (streaming/awaiting/paused), and failed, compare the mock (left — Hexaware Run.dc.html / Hexaware Run - Live.dc.html / Hexaware Run - Failed.dc.html (right-column header + tab row)) against ours (right). Confirm every visible difference is one of the registered ND-A..ND-H divergences (captioned "expected — ignore"); any OTHER visual departure is a fidelity gap to fix before approving.</how-to-verify>
  <resume-signal>Type "approved" if the run header + tab order matches its mock across settled, live (streaming/awaiting/paused), and failed closed to the intended-divergence register, or list the specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Share link → clipboard | a run deep link (owner-auth-gated route) copied client-side |
| Version select → read-only fetch | reuses the existing owner-scoped `getWorkflow` version fetch |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | Information disclosure | Share (client-only link) | mitigate | copies the OWNER-AUTH-GATED run route only; no public/unauth token, no new endpoint (ND-H); recipient still needs to authenticate as the owner |
| T-39-05-02 | Spoofing | Download / version select | accept | reuses existing owner-scoped download + `getWorkflow` version fetch (IDOR→404 already enforced); no new authority path |
| T-39-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs in this plan; no backend/migration change (Share is client-only). |
</threat_model>

<verification>
- The run header (Version / Share / Download / status badge) + corrected tab order match the settled/live/failed mocks per the screenshot-diff oracle, closed to ND-A..ND-H.
- Share is client-only (no endpoint); single version affordance (INV-3); live data only (ND-D).
- `npx tsc --noEmit` clean (identity for the additive pass-throughs); touched-component vitest green.
</verification>

<success_criteria>
RUNUI-06 (SC-1): the run header + tab order match the mock across the three states.
RUNUI-07 (SC-2): the net-new Share and Version menu affordances land (Share client-only, Version from runFamily).
</success_criteria>

<artifacts_produced>
- `frontend/src/components/preview/RunHeader.tsx` (NEW): run header (Version menu / Share / Download / status badge / version chip).
- Reordered `TAB_CONFIG` (Preview · Steps · Files · Audit); Steps review dot when paused.
- Client-only `onShare` (copy run link) + `onDownload` wiring; DashboardLayout lane Back-to-history + run-meta pass-throughs.
- (Optional) shared `VersionMenu` extracted from LiveVersionChip (INV-12).
</artifacts_produced>

<output>
Create `.planning/phases/39-run-screen-mock-fidelity-b5/39-05-SUMMARY.md` when done.
</output>
