---
phase: 02-executioncontext-ownership-0b
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/agents/execution_engine/engine.py
  - specs/003-workflow-engine-decoupling/migration-ledger.md
autonomous: true
requirements: [CTX-04, CTX-05]
must_haves:
  truths:
    - "The dead _handle_revision method is removed entirely from engine.py"
    - "The D1 grep _handle_revision over backend/ returns 0"
    - "Migration-ledger rows L14 and D1 are flipped to ☑ (their grep ratchets are now enforced and green)"
    - "The migration-ledger CI guard (test_migration_ledger.py) passes with L14 + D1 armed"
    - "Phase 0A characterization snapshots stay byte-identical and at semantic-event parity"
  artifacts:
    - path: "backend/agents/execution_engine/engine.py"
      provides: "engine with _handle_revision deleted (D1)"
    - path: "specs/003-workflow-engine-decoupling/migration-ledger.md"
      provides: "L14 + D1 rows marked ☑ with deleting commit reference"
      contains: "☑"
  key_links:
    - from: "specs/003-workflow-engine-decoupling/migration-ledger.md (L14 row)"
      to: "backend/tests/agents/test_migration_ledger.py"
      via: "flipping ☐→☑ arms the parametrized grep ratchet for the L14 pattern"
      pattern: "L14.*☑"
    - from: "specs/003-workflow-engine-decoupling/migration-ledger.md (D1 row)"
      to: "backend/tests/agents/test_migration_ledger.py"
      via: "flipping ☐→☑ arms the grep ratchet for _handle_revision"
      pattern: "D1.*☑"
---

<objective>
Delete the dead `_handle_revision` method from `engine.py` (CTX-04 / D1 — superseded by the inline revision handling at `engine.py:551-644`, never reached), then flip migration-ledger rows **L14** and **D1** from `☐` to `☑`. Flipping those rows arms their grep ratchets automatically (the existing `test_migration_ledger.py` guard parses the ledger and, for each `☑` grep row, asserts the pattern returns 0 in `backend/`). After this plan, reintroducing the L14 per-run-state pattern OR `_handle_revision` fails CI permanently (DEL-03 ratchet). Zero behavior change (CTX-05).

Purpose: D1 removes dead code (INV-12 — a phase that supersedes code must delete it). Flipping L14 + D1 to `☑` is the operational proof that Phase 0B's two grep-gated deletions are complete and locked.
Output: `engine.py` with `_handle_revision` gone; `migration-ledger.md` with L14 + D1 at `☑`; the ledger CI guard green with both ratchets live; 0A suite green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-executioncontext-ownership-0b/02-SPEC.md
@.planning/phases/02-executioncontext-ownership-0b/02-CONTEXT.md
@.planning/phases/02-executioncontext-ownership-0b/02-PATTERNS.md
@.planning/phases/02-executioncontext-ownership-0b/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan REMOVES a symbol and updates the ledger; it creates no new code symbols:
- `_handle_revision` (engine.py:2138-2251) is DELETED — downstream grounding must not flag its disappearance as drift.
- Migration-ledger rows L14 and D1 transition `☐`→`☑` (status + deleting commit SHA filled).

(Plan 02-01 produced `ExecutionContext`/`context.py` and the `ectx` params; plan 02-03 produces `authz.py`/`assert_owns`/the denial exception/the denial test.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead _handle_revision method (D1)</name>
  <files>backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py — READ the current `_handle_revision` body (verified range `:2138-2251` per 02-PATTERNS.md / migration-ledger.md; confirm the exact current start/end since line numbers shifted after 02-01's edits — locate it by `grep -n 'async def _handle_revision' backend/agents/execution_engine/engine.py`). It is superseded by the INLINE revision handling at `:551-644` and is never reached. Note it contains a dead `raise ValueError("Revision instruction must not be empty.")` and dead `self._store.retrieve_latest(...)` calls — the deletion is TOTAL (method body + signature + its docstring).
    - .planning/phases/02-executioncontext-ownership-0b/02-PATTERNS.md (D1 section, lines 198-201) — confirms total deletion, dead validation included.
    - specs/003-workflow-engine-decoupling/migration-ledger.md (D1 row) — the verbatim gate `_handle_revision`.
    - backend/tests/agents/_scripted_model.py (`_drive`) — re-run the 0A suite after deletion to prove the method was truly dead (no caller).
  </read_first>
  <action>
    Locate `async def _handle_revision(self, ...)` via grep (its line number shifted after 02-01; do NOT trust a stale absolute number). Delete the ENTIRE method — signature, docstring, and full body (the `if not instruction...raise ValueError`, the `self._store.retrieve_latest(...)` retrieval, and everything down to its last line before the next `def`/method boundary). Verify NO call site references it first: `grep -rn '_handle_revision' backend/` must show ONLY the definition before deletion (it is dead — superseded by inline revision at `:551-644`). After deletion, re-run the full 0A suite to prove nothing relied on it. Do NOT touch the inline revision handling — only the dead method is removed. No behavior change (CTX-05): the snapshots must stay byte-identical and at semantic parity.
  </action>
  <verify>
    <automated>cd backend && grep -rn '_handle_revision' . ; test $? -ne 0 && cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '_handle_revision' backend/` returns 0 matches (D1 ratchet target — exits non-zero / no output).
    - `cd backend && python3.11 -m pytest tests/agents/ -v` exits 0 (0A snapshots green — the method was provably dead; deliverables byte-identical, events at semantic parity).
    - No inline revision handling was altered: `grep -nE 'REVISION REQUEST|prototype_revision' backend/agents/execution_engine/engine.py` still matches the inline path (unchanged).
  </acceptance_criteria>
  <done>`_handle_revision` is gone; the D1 grep returns 0; the 0A suite is green (proving the method was dead).</done>
</task>

<task type="auto">
  <name>Task 2: Flip migration-ledger rows L14 and D1 to ☑ (arm the grep ratchets)</name>
  <files>specs/003-workflow-engine-decoupling/migration-ledger.md</files>
  <read_first>
    - specs/003-workflow-engine-decoupling/migration-ledger.md — READ the full ledger table (lines 47-72). The L14 row (line 49) and D1 row (line 51) are currently `☐`. Note the EXACT column layout: `| Item | Legacy | New home | Phase | Deletion gate (grep → 0 / check) | Status | Deleting SHA |`. The Status cell (`☐`/`☑`) and the trailing `Deleting SHA` cell are what change.
    - backend/tests/agents/test_migration_ledger.py — READ the guard (lines 40-125): `_parse_rows` finds the Status cell as the last cell carrying `☐`/`☑`; `_checked_grep_rows` yields `(item, pattern)` only for `☑` rows; `test_deleted_pattern_absent_from_backend` greps each `☑` grep-row's pattern over `backend/` and asserts 0 matches. ALSO note `test_ledger_parses_and_all_phase1_rows_pending` asserts ALL rows are `☐` — that test is Phase-1-specific and WILL now fail once L14/D1 flip; update its intent (see action) so it does not block Phase 0B.
    - .planning/phases/02-executioncontext-ownership-0b/02-SPEC.md (Acceptance Criteria) — "Migration-ledger rows L14 and D1 flipped to ☑ (grep ratchets green); L16 row recorded with its denial test" (L16 itself is plan 02-03).
  </read_first>
  <action>
    In `specs/003-workflow-engine-decoupling/migration-ledger.md`, change the L14 row Status cell from `☐` to `☑` and fill its `Deleting SHA` cell (use the short SHA of this plan's deletion commit, or `(this PR)` as a placeholder the commit step replaces). Do the SAME for the D1 row. Leave the L16 row `☐` (it is a CHECK row owned by plan 02-03). Leave every OTHER row `☐` (L13, L1-L12, L15, F1-F5 belong to later phases). Then RECONCILE the now-stale Phase-1 invariant test: `backend/tests/agents/test_migration_ledger.py::test_ledger_parses_and_all_phase1_rows_pending` asserts every row is `☐` — that was true only in Phase 1. Update that test so it no longer asserts blanket-pending: keep the "every required §31 item is present" assertion (the `_REQUIRED_ITEMS` membership check), but replace the `not_pending == []` assertion with one that whitelists the Phase-0B-flipped rows — i.e. assert that the ONLY `☑` rows are exactly `{L14, D1}` (and that L16/all later rows remain `☐`). Do NOT weaken the ratchet (`test_deleted_pattern_absent_from_backend` must keep firing on the live L14 + D1 patterns). Run the ledger guard to confirm L14's `self\._(od_context|completed_tasks|current_task_block|revision_|gate_agent_ids)` pattern and D1's `_handle_revision` pattern both return 0 (they must, after 02-01 + Task 1 here) and the non-vacuity guard (`test_guard_fails_on_known_present_pattern`) still passes.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\| L14 .*☑' specs/003-workflow-engine-decoupling/migration-ledger.md` returns 1 match (L14 flipped).
    - `grep -nE '^\| D1 .*☑' specs/003-workflow-engine-decoupling/migration-ledger.md` returns 1 match (D1 flipped).
    - `grep -nE '^\| L16 .*☐' specs/003-workflow-engine-decoupling/migration-ledger.md` returns 1 match (L16 still pending — owned by 02-03).
    - `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -v` exits 0 (the L14 + D1 grep ratchets are now live and green; the non-vacuity guard and CHECK-row classification tests still pass; the updated phase-status test passes).
    - `grep -rnE 'self\._(od_context|completed_tasks|current_task_block|revision_|gate_agent_ids)' backend/` AND `grep -rn '_handle_revision' backend/` both return 0 (the armed ratchets have nothing to fire on).
  </acceptance_criteria>
  <done>L14 and D1 are `☑` in the ledger; the ledger CI guard passes with both grep ratchets live and green; L16 and all later rows stay `☐`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger ↔ CI ratchet | The migration-ledger is the source of truth `test_migration_ledger.py` asserts against; a wrong status cell either fails to enforce (false green) or fires on legitimate code (false red). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | reintroduction of relocated per-run state (`self._*`) or the dead `_handle_revision` in a later edit | mitigate | Flipping L14 + D1 to `☑` arms permanent grep ratchets (DEL-03): any reappearance of the L14 pattern or `_handle_revision` in `backend/` fails CI. The ratchet is proven non-vacuous by the existing `test_guard_fails_on_known_present_pattern`. |
| T-02-06 | Repudiation | a deletion landing without ledger record (silent scope drift) | mitigate | The ledger row's `Deleting SHA` cell records the deleting commit; `test_ledger_parses_and_all_phase1_rows_pending` (updated here) asserts the exact set of `☑` rows, so an undocumented flip or an un-flipped deletion is caught. |
| T-02-07 | Tampering | deleting `_handle_revision` when it is actually live (breaking revision) | mitigate | Pre-deletion `grep -rn '_handle_revision' backend/` confirms only the definition (no caller), and the full 0A suite — including `test_characterization_prototype_revision.py` — is re-run as a blocking gate. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no audit required. |

(The CTX-03/L16 cross-owner seeding threat is owned by plan 02-03.)
</threat_model>

<verification>
- D1 grep `_handle_revision` over `backend/` returns 0.
- L14 grep `self\._(od_context|completed_tasks|current_task_block|revision_|gate_agent_ids)` over `backend/` returns 0 (carried from 02-01; re-confirmed with the ratchet now live).
- Ledger rows L14 + D1 are `☑`; L16 + all later rows are `☐`.
- `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -v` exits 0 (ratchets live, non-vacuity guard green).
- Full 0A suite green: `cd backend && python3.11 -m pytest tests/agents/ -v` exits 0.
</verification>

<success_criteria>
- Dead `_handle_revision` deleted (CTX-04); D1 grep → 0.
- Ledger L14 + D1 at `☑` with their ratchets enforced and green; L16 recorded as still-pending CHECK (filled by 02-03).
- Deliverables byte-identical; events at semantic parity (CTX-05).
</success_criteria>

<output>
Create `.planning/phases/02-executioncontext-ownership-0b/02-02-SUMMARY.md` when done. Record: the deletion of `_handle_revision` (with the confirmed no-caller grep), the L14/D1 ledger flips, and the reconciliation of the Phase-1 `all rows pending` test into a Phase-0B `only {L14, D1} flipped` assertion.
</output>
