---
phase: 33-concierge-compaction-a5
plan: 05
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - backend/agents/workflows/manifest.py
  - backend/tests/agents/test_sc001_lane_router_concierge.py
  - backend/tests/agents/test_chat_event_neutrality.py
  - backend/tests/agents/test_phase3_cutover_verify.py
  - backend/tests/agents/characterization/_normalize.py
autonomous: true
requirements: [SC-001, SC-4, D-05, INV-1, INV-3, INV-5]

must_haves:
  truths:
    - "A brand-new throwaway custom workflow manifest gets lane + router + Concierge with ZERO engine/FE/orchestrator code: its runs route through route_chat_turn identically, registry.resolve(\"chat\",\"concierge\") returns the shared impl, and grep for the throwaway workflow name in the engine/router/concierge returns 0 (SC-001 / INV-1, ROADMAP SC-4)."
    - "The manifest chat: key compiles as DATA — an optional dict field carried into the compiled manifest, with no compiler control-flow keyed off it (INV-5)."
    - "The 5 characterization goldens stay byte/event-identical with SNAPSHOT_UPDATE unset, and chat/concierge events (incl. concierge_proposal) fire on NONE of the golden paths (INV-3)."
  artifacts:
    - path: "backend/agents/workflows/manifest.py"
      provides: "optional chat: data field on WorkflowManifest (INV-5)"
      contains: "chat"
    - path: "backend/tests/agents/test_sc001_lane_router_concierge.py"
      provides: "throwaway-manifest lane+router+concierge proof + grep gate"
    - path: "backend/tests/agents/test_chat_event_neutrality.py"
      provides: "extended concierge-dormant golden guard"
  key_links:
    - from: "backend/tests/agents/test_sc001_lane_router_concierge.py"
      to: "route_chat_turn + registry.resolve(chat,concierge)"
      via: "throwaway manifest routes identically + resolves the shared impl"
      pattern: "resolve\\(\\s*[\"']chat[\"']"
    - from: "backend/tests/agents/test_chat_event_neutrality.py"
      to: "the 5 goldens"
      via: "chat/concierge event types absent from every golden stream"
      pattern: "concierge_proposal"
---

<objective>
Prove the milestone core value (SC-001, ROADMAP SC-4): a brand-new custom workflow gets lane + router + Concierge with ZERO engine/FE/orchestrator code. Land the manifest `chat:` DATA key (INV-5) that a custom workflow authors, then a throwaway-manifest fixture test + grep gate that empirically proves no new workflow-name branch exists. Finally, extend the INV-3 golden guards and prove the 5 goldens stay byte-identical with the Concierge dormant.

Purpose: This is the phase's non-negotiable proof — the kernel knows no workflow by name; all power lives in registered capabilities + declared data. Also closes INV-3 (goldens byte/event-identical) and INV-5 (manifest chat: compiles as data).
Output: `manifest.py` gains an optional `chat` data field; a new SC-001 fixture test with a grep gate; the concierge-dormant guard + event-type registries extended; a byte-identical goldens proof.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-concierge-compaction-a5/33-CONTEXT.md
@.planning/phases/33-concierge-compaction-a5/33-RESEARCH.md
@.planning/phases/33-concierge-compaction-a5/33-PATTERNS.md
@.planning/phases/33-concierge-compaction-a5/33-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: manifest chat: data key (INV-5 — compiles as data)</name>
  <files>backend/agents/workflows/manifest.py</files>
  <read_first>
    - backend/agents/workflows/manifest.py:44-102 (`WorkflowManifest` dataclass — optional-with-defaults fields like `context_providers: list = field(default_factory=list)` :61, the field-name list ~:102), :128-240 (`load_manifest` — `context_providers = _optional_list(data, "context_providers", file_str)` :203, assembled into the dataclass :226), :280 (`_optional_dict(data, key, file_str, default_factory)`)
  </read_first>
  <action>
    Add an OPTIONAL `chat: dict = field(default_factory=dict)` field to `WorkflowManifest` (INV-5 — pure DATA: suggestions + concierge notes; NO control-flow key). Load it in `load_manifest` via `_optional_dict(data, "chat", file_str, dict)` and pass it into the dataclass construction (~:226). If the manifest has a `__slots__`/field-name allow-list (~:102), add `"chat"` there too. There must be NO `if manifest.chat ...`-style compiler branch that changes execution flow — the key is READ as data by the Concierge (`getattr(compiled, "chat", {})`) and the FE (suggestions), never a control switch. Do NOT special-case any workflow name.
  </action>
  <acceptance_criteria>
    - `python3.11 -c "import sys; sys.path.insert(0,'backend'); from agents.workflows.manifest import WorkflowManifest; print('chat' in {f.name for f in __import__('dataclasses').fields(WorkflowManifest)})"` prints `True` (from repo root).
    - `grep -nE "if .*\\.chat\\b|chat ==|manifest\\.chat and" backend/agents/workflows/manifest.py backend/agents/workflows/compiler.py` returns 0 lines (no control-flow keyed off chat: — INV-5).
    - Existing manifest tests still pass: `python3.11 -m pytest tests/agents -k manifest -x` (from backend/).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SC-001 throwaway-manifest proof + grep gate</name>
  <files>backend/tests/agents/test_sc001_lane_router_concierge.py</files>
  <read_first>
    - backend/tests/agents/test_sc001_gate_flag.py:1-40 (the name-free-discriminator precedent + offline-safe harness + the `grep`/`Path`-based source gate shape — mirror the "if pipeline_type / spec.id == → 0" gate)
    - backend/app/api/chat_router.py (from 33-03 — `route_chat_turn` returns CHANNEL_CONCIERGE for free-form, CHANNEL_GATE for gate turns)
    - backend/agents/capabilities/registry.py:393-413 (`resolve(kind, name)` — static dict lookup, no workflow branch)
    - backend/agents/workflows/manifest.py (from Task 1 — the chat: data key the throwaway manifest authors)
  </read_first>
  <action>
    Create `test_sc001_lane_router_concierge.py` (offline). Author a THROWAWAY custom workflow manifest fixture (a minimal manifest dict/yaml with a `chat:` data block + a generic agent sequence + a distinctive throwaway workflow name, e.g. `sc001_throwaway_wf`) under the test. Assert:
    (1) A free-form turn against that workflow's run routes through `route_chat_turn` to `CHANNEL_CONCIERGE`, and a gate turn routes to `CHANNEL_GATE` — with NO code referencing the throwaway workflow name (the router keys only on generic RunState).
    (2) `registry.resolve("chat","concierge")` returns the SAME shared impl regardless of the workflow (a static dict lookup).
    (3) The chat: data compiles (via Task 1's key) and is carried as data (assert the compiled manifest exposes the `chat` block).
    (4) GREP GATE: assert `grep` for the throwaway workflow name across `backend/agents/execution_engine/`, `backend/app/api/chat_router.py`, and `backend/app/agents/chat/concierge.py` returns 0 matches (the SC-001 grep gate — mirror `test_sc001_gate_flag.py`'s source-gate). Also assert NO new `if pipeline_type == / spec.id == / workflow name ==` branch was introduced anywhere in the chat surface (0 matches).
  </action>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/agents/test_sc001_lane_router_concierge.py -x` passes (from backend/).
    - The grep gate inside the test asserts 0 matches for the throwaway name in engine/router/concierge (the test itself encodes and runs the gate).
    - Independent confirmation: `grep -rc "sc001_throwaway_wf" backend/agents/execution_engine/ backend/app/api/chat_router.py backend/app/agents/chat/concierge.py` returns 0 in each non-test source path.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: INV-3 golden neutrality — extend guards + prove 5 goldens byte-identical</name>
  <files>backend/tests/agents/test_chat_event_neutrality.py, backend/tests/agents/test_phase3_cutover_verify.py, backend/tests/agents/characterization/_normalize.py</files>
  <read_first>
    - backend/tests/agents/test_chat_event_neutrality.py:37-45 (the 5 goldens: `prototype`, `od_prototype`, `od_ppt`, `prototype_revision`, `app_builder`), :60-89 (`test_chat_events_absent_from_golden` — asserts `{chat_message, chat_reply, stream_attached}` appear in NONE of the golden streams)
    - backend/tests/agents/test_phase3_cutover_verify.py:182 (`_DOCUMENTED_EVENT_TYPES`), :241-243 (existing `chat_message`/`chat_reply`/`stream_attached` entries)
    - backend/tests/agents/characterization/_normalize.py:95-174 (`_VOLATILE_STRIP_KEYS` :101 — additive `pipeline_complete` keys stripped for parity)
    - 33-03-SUMMARY.md (the new `concierge_proposal` event type + any new `pipeline_complete` keys the concierge/compaction path introduced)
  </read_first>
  <action>
    Extend the INV-3 guards to admit the new Concierge vocabulary WITHOUT changing the goldens:
    (1) `test_phase3_cutover_verify.py:182` `_DOCUMENTED_EVENT_TYPES` — add `"concierge_proposal"` (and any other NEW concierge event type introduced in 33-03) so the forward-vocabulary guard accepts it.
    (2) `test_chat_event_neutrality.py:60-89` — extend the guarded set in `test_chat_events_absent_from_golden` to include `"concierge_proposal"` (and any new type), asserting it fires on NONE of the 5 golden streams (concierge dormant on golden paths).
    (3) `characterization/_normalize.py:101` `_VOLATILE_STRIP_KEYS` — add any NEW `pipeline_complete` keys the concierge/compaction path adds, so additive keys stay parity-neutral.
    (4) PROVE byte-identity: with `SNAPSHOT_UPDATE` UNSET, run the characterization suite for the 5 pipelines against the UNCHANGED `golden/*` fixtures. Do NOT regenerate fixtures. Do NOT touch `test_chat_contract.py` (frozen ChatRunner golden — INV-12; the Concierge does not run through ChatRunner).
    If 33-03 introduced NO new event type (answers reuse `chat_reply` and proposals reuse an existing type), document that in the SUMMARY and skip (1)/(2)'s additions — but still run the byte-identity proof.
  </action>
  <acceptance_criteria>
    - `SNAPSHOT_UPDATE` is UNSET and `python3.11 -m pytest tests/agents/test_chat_event_neutrality.py tests/agents/test_phase3_cutover_verify.py -x` passes (from backend/) — goldens byte/event-identical, concierge dormant.
    - The characterization suite for the 5 pipelines passes against the unchanged fixtures (no `SNAPSHOT_UPDATE` in the command): `python3.11 -m pytest tests/agents/characterization -x`.
    - `git diff --stat backend/tests/agents/characterization/golden/` shows NO changes to any `golden/*.events.json` (goldens untouched).
    - `grep -c "concierge_proposal" backend/tests/agents/test_chat_event_neutrality.py` matches the count of new concierge event types added (or 0 if 33-03 reused existing types — documented in SUMMARY).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| custom manifest → compiler | a user-authored `chat:` block is untrusted manifest data compiled into the run |
| new workflow name → engine/router/concierge | a new workflow must not be able to inject a name-keyed code branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | Tampering | manifest `chat:` block as a control-flow injection surface | mitigate | `chat:` compiles as DATA only (INV-5) — read via `getattr`, never a compiler branch. Verified by the "no control-flow keyed off chat" grep in Task 1. |
| T-33-05-02 | Elevation / INV-1 | a workflow-name branch sneaks into the chat surface | mitigate | The SC-001 grep gate (Task 2) asserts 0 matches for the throwaway name + 0 new `pipeline_type/spec.id ==` branches in engine/router/concierge. |
| T-33-05-03 | Tampering | golden regression (byte drift) hides a behavioral change | mitigate | Byte-identity proven with `SNAPSHOT_UPDATE` unset + `git diff --stat` on `golden/*` showing no change; concierge events dormant on all 5 goldens. |
| T-33-05-SC | Tampering | pip installs | accept | No new package installs (test + manifest-data only). Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- SC-001 proof green: `python3.11 -m pytest tests/agents/test_sc001_lane_router_concierge.py -x`.
- INV-3 goldens byte/event-identical (SNAPSHOT_UPDATE UNSET): `python3.11 -m pytest tests/agents/test_chat_event_neutrality.py tests/agents/test_phase3_cutover_verify.py tests/agents/characterization -x` + `git diff --stat backend/tests/agents/characterization/golden/` clean.
- INV-5 no control-flow keyed off chat: grep clean.
- Import gate: `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken.
- Full register/parity suite (final wave gate): `python3.11 -m pytest tests/agents/test_registry_capabilities.py tests/agents/test_chat_event_neutrality.py tests/agents/test_phase3_cutover_verify.py -x`.
</verification>

<success_criteria>
- The throwaway-manifest test proves lane + router + Concierge with ZERO code; the grep gate returns 0 for the throwaway name + 0 new name-keyed branches (SC-001 / ROADMAP SC-4).
- The manifest `chat:` key compiles as data with no compiler control-flow (INV-5).
- The 5 goldens are byte/event-identical with SNAPSHOT_UPDATE unset; concierge events fire on none of them (INV-3).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (33-05)

New manifest field:
- `WorkflowManifest.chat: dict` (optional, `default_factory=dict`) loaded via `_optional_dict(data, "chat", ...)` — INV-5 data-only

New test file:
- `backend/tests/agents/test_sc001_lane_router_concierge.py` (throwaway-manifest lane+router+concierge proof + grep gate)

Test-infra registries extended (not new symbols):
- `_DOCUMENTED_EVENT_TYPES` += `"concierge_proposal"` (and any new concierge type)
- `test_chat_events_absent_from_golden` guarded set += `"concierge_proposal"`
- `_VOLATILE_STRIP_KEYS` += any new `pipeline_complete` keys

Throwaway fixture (test-scoped, not shipped): a custom workflow manifest named `sc001_throwaway_wf` with a `chat:` data block.

Goldens: UNCHANGED (proven byte-identical).
</artifacts_produced>

<output>
Create `.planning/phases/33-concierge-compaction-a5/33-05-SUMMARY.md` when done.
</output>
