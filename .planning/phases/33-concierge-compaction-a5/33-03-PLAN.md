---
phase: 33-concierge-compaction-a5
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - backend/app/api/chat_router.py
  - backend/app/api/run_commands.py
  - backend/tests/unit/test_concierge_escalation.py
  - backend/tests/unit/test_concierge_proposal_channels.py
autonomous: true
requirements: [D-04, D-05, D-02, SC-1, SC-3, INV-12]

must_haves:
  truths:
    - "route_chat_turn returns CHANNEL_CONCIERGE for genuinely free-form turns and its existing channel (answers/gate/steering/revision) for routable turns — routable turns stay ZERO-model-call, the router stays a pure structural predicate (D-04, INV-12)."
    - "The app layer disposes each Concierge propose_* intent through the SAME existing seams the router uses: propose_gate_action→set_review_response (incl. update_specs→KAN-101), propose_steering_note→apply_steering, propose_revision→_mint_revision_row+_drive_revision_to_queue (D-05); consequential proposals held behind a confirm chip."
    - "Post-run free-form turns produce revision runs stitched into the family transcript via the existing revision seam — no forked revision path (D-02, SC-3)."
  artifacts:
    - path: "backend/app/api/chat_router.py"
      provides: "CHANNEL_CONCIERGE constant + pure free-form escalation branch"
      contains: "CHANNEL_CONCIERGE"
    - path: "backend/app/api/run_commands.py"
      provides: "post_message concierge invocation + proposal→channel disposal + confirm-chip hold"
    - path: "backend/tests/unit/test_concierge_escalation.py"
      provides: "free-form→concierge, routable→zero-model proof"
    - path: "backend/tests/unit/test_concierge_proposal_channels.py"
      provides: "propose_*→existing-seam disposal proof"
  key_links:
    - from: "backend/app/api/run_commands.py"
      to: "backend/app/agents/chat/concierge.py"
      via: "post_message invokes the concierge when dispatch.channel == CHANNEL_CONCIERGE"
      pattern: "CHANNEL_CONCIERGE"
    - from: "backend/app/api/run_commands.py"
      to: "set_review_response / apply_steering / _mint_revision_row"
      via: "propose_* intent disposal through existing seams"
      pattern: "_mint_revision_row|set_review_response|apply_steering"
---

<objective>
Wire the Concierge into the live chat surface: add the router's free-form→Concierge escalation (a pure structural branch, routable turns stay zero-model — D-04) and dispose the Concierge's `propose_*` intents through the SAME existing Phase-29 seams the mechanical router uses (D-05), including post-run revision-run stitching into the family transcript (D-02).

Purpose: Delivers ROADMAP SC-1 (proposals execute only through existing channels) and SC-3 (post-run chat → revision runs stitched into the family). Reuses the Phase-29 seams verbatim — no fork of ChatRunner, no new revision path, no new execution seam (INV-12). Consequential proposals are held behind a confirm chip (the FE confirm UX lands in 33-04).
Output: `chat_router.py` gains `CHANNEL_CONCIERGE` + a pure escalation branch; `run_commands.py::post_message` gains a `CHANNEL_CONCIERGE` invocation + proposal disposal + confirm-chip hold; two offline unit tests.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-concierge-compaction-a5/33-CONTEXT.md
@.planning/phases/33-concierge-compaction-a5/33-RESEARCH.md
@.planning/phases/33-concierge-compaction-a5/33-PATTERNS.md
@.planning/phases/33-concierge-compaction-a5/33-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: router escalation — CHANNEL_CONCIERGE + pure free-form branch + escalation test</name>
  <files>backend/app/api/chat_router.py, backend/tests/unit/test_concierge_escalation.py</files>
  <read_first>
    - backend/app/api/chat_router.py:49-52 (channel constants `CHANNEL_ANSWERS/GATE/STEERING/REVISION`), :154 (`Dispatch` dataclass), :235-295 (`route_chat_turn` — pure, zero model; keys only on generic `RunState.phase`; the running-turn fallthrough at :291-295 currently maps ALL free-form running turns to `CHANNEL_STEERING`), :32-33 (docstring reserving free-form for the Concierge escalation)
    - backend/app/api/chat_router.py:301-316 (`apply_steering(ectx, note)` — the steering seam, referenced by the escalation design)
    - existing `route_chat_turn` unit tests (grep `tests/unit` for the router test file) — mirror their structure for the new escalation cases
  </read_first>
  <action>
    Add `CHANNEL_CONCIERGE = "concierge"` alongside the existing channel constants (`chat_router.py:49-52`). Add a branch in `route_chat_turn` that returns a `Dispatch(channel=CHANNEL_CONCIERGE, ...)` ONLY for genuinely free-form turns (RESEARCH A2: a PURE structural predicate — e.g. a running/terminal turn with no `action` and no structured clarify/steering discriminator, question-shaped by structure). CRITICAL: preserve the zero-model guarantee — every ROUTABLE turn (structured clarify response, `turn.action in GATE_ACTIONS`, post-terminal revision, steering-with-`sticky`) MUST still resolve to its EXISTING channel with no model call; only the genuinely free-form residual escalates. NEVER key the predicate on a workflow name / `pipeline_type` / `spec.id` (INV-1) — it is a generic structural check. `route_chat_turn` stays pure: it only CLASSIFIES to `CHANNEL_CONCIERGE`; the model INVOCATION happens in `post_message` (Task 2).
    Create `test_concierge_escalation.py` (offline): assert a genuinely free-form running turn and a free-form terminal turn → `CHANNEL_CONCIERGE`; assert each routable turn (clarify answer, gate action, post-terminal revision, sticky steering) still routes to its pre-existing channel; assert no model is called on any routable path (the router has no model handle — the test asserts the dispatch is a pure classification, mirroring existing router tests).
  </action>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/unit/test_concierge_escalation.py -x` passes (from backend/).
    - `grep -c "CHANNEL_CONCIERGE" backend/app/api/chat_router.py` >= 2 (constant + branch).
    - `grep -nE "pipeline_type|spec\\.id|workflow[_ ]?name ==|== \"prototype\"|== \"od_" backend/app/api/chat_router.py` shows no NEW workflow-name branch introduced by this change (INV-1 — the escalation predicate is generic/structural).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: post_message concierge invocation + proposal→channel disposal + confirm-chip hold</name>
  <files>backend/app/api/run_commands.py, backend/tests/unit/test_concierge_proposal_channels.py</files>
  <read_first>
    - backend/app/api/run_commands.py:419-614 (`post_message` dispatch), :525-609 (the existing per-channel execution seams), :526-558 (`set_review_response(gate_key, approved=..., action=..., instructions=...)` — 4 actions incl. `update_specs`→KAN-101 at :550-554), :505 (`_gate_is_pending` KAN-94 armed ground truth), :577-609 (`_mint_revision_row` :584 + `asyncio.create_task(_drive_revision_to_queue(...))` :595), :350-362 (`_live_ectx_for_run` returns `None` today — steering is best-effort, DEF-29-09-1)
    - backend/app/api/run_commands.py:365-416 (chat turns append to `run_events` `type="chat_message"` idempotently via `ScopedStore.append_event_next_seq`, `event_id=chat:{message_id}`; `chat_reply` is the documented answer-projection type), :446-472 (two-layer owner check), :474-489 (`_validate_images` cap-validation)
    - backend/app/api/chat_router.py:301-316 (`apply_steering(ectx, note)` → `ectx.steering_notes`)
    - backend/app/agents/chat/concierge.py (from 33-02 — the `propose_*` intents + the async entry method to invoke)
  </read_first>
  <action>
    In `post_message`, add a `CHANNEL_CONCIERGE` branch (alongside the existing `CHANNEL_GATE`/`CHANNEL_STEERING`/`CHANNEL_REVISION` branches at :525-609): invoke the Concierge capability (resolved via `registry.resolve("chat","concierge")`, per-run instance) with the turn text + the owner-scoped ctx. Project its answer back to the lane as a `chat_reply` `run_events` row (reuse the existing append path — additive, no new table; owner_id+workspace_id stamped by `ScopedStore`).
    Dispose each returned `propose_*` intent through the SAME EXISTING seams — NO new execution path:
    - `propose_gate_action` → `art_store.set_review_response(gate_key, approved=..., action=..., instructions=...)` (route `action == update_specs` to the shipped KAN-101 loop — do NOT rebuild spec-revision; respect `_gate_is_pending`/KAN-94 + the KAN-100 terminal fence).
    - `propose_steering_note` → `apply_steering(ectx, note)` → `ectx.steering_notes` (best-effort; the durable `chat_message`/`chat_reply` row is the record — ND-9).
    - `propose_revision` → `_mint_revision_row(...)` + `asyncio.create_task(_drive_revision_to_queue(...))` — the revision-family child; live stitching rides the existing per-run queue + `run_events` (D-02, SC-3). Reuse `REVISION_BASE_MAP` — do NOT fork.
    CONSEQUENTIAL proposals (gate action + revision) are HELD behind a confirm chip: emit a `concierge_proposal` `run_events` row carrying the pending intent and return WITHOUT executing; execution happens on a subsequent confirmed turn (the FE confirm UX + the confirm round-trip land in 33-04). Non-consequential answers project immediately as `chat_reply`. Do NOT touch `ChatRunner`, `test_chat_contract.py`'s golden, `user_message` semantics, or `_INTERNAL_PIPELINES` (INV-12).
    Create `test_concierge_proposal_channels.py` (offline): with the seams mocked/spied, assert each `propose_*` intent disposes through its matching seam EXACTLY once (`propose_gate_action`→`set_review_response`; `propose_steering_note`→`apply_steering`; `propose_revision`→`_mint_revision_row`+`_drive_revision_to_queue`); assert a consequential proposal is HELD (emits `concierge_proposal`, does NOT call the seam until confirmed); assert a fabricated gate resolution on a terminal run is refused (KAN-100 fence).
  </action>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/unit/test_concierge_proposal_channels.py -x` passes (from backend/).
    - `grep -nE "set_review_response|apply_steering|_mint_revision_row|_drive_revision_to_queue" backend/app/api/run_commands.py` shows all four disposal seams referenced from the CHANNEL_CONCIERGE path (reuse, not fork).
    - `grep -c "concierge_proposal" backend/app/api/run_commands.py` >= 1 (confirm-chip hold event).
    - `grep -nE "class ChatRunner|def user_message|_INTERNAL_PIPELINES" backend/app/api/run_commands.py` shows NO edits to ChatRunner/user_message/_INTERNAL_PIPELINES semantics (INV-12 — this task adds a branch, does not fork).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → post_message | free-form chat `text` (untrusted) crosses into routing + Concierge invocation |
| Concierge proposal → execution seam | a proposed intent must not execute without owner consent for consequential actions |
| turn → gate/revision on a terminal run | a resolution/revision must respect the terminal fence + armed-gate ground truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Elevation / Tampering | Concierge executes a consequential proposal without consent | mitigate | Consequential proposals (gate action, revision) are HELD behind a confirm chip — emit `concierge_proposal`, execute only on a confirmed turn. Verified by the "held, not executed" assertion in `test_concierge_proposal_channels.py`. |
| T-33-03-02 | Tampering | fabricated gate resolution / revision on a stopped run | mitigate | KAN-100 terminal fence + KAN-94 armed-gate ground truth (`_gate_is_pending`, `run_commands.py:505`) already enforced in `post_message`; the CHANNEL_CONCIERGE path routes through the SAME `set_review_response` seam. Verified by the terminal-fence refusal assertion. |
| T-33-03-03 | Information Disclosure | Concierge invocation reads cross-owner run data | mitigate | Two-layer owner check in `post_message` (`run_commands.py:446-472`) + the Concierge's own ScopedStore reads (33-02); the invocation passes only the invoking owner+workspace ctx. |
| T-33-03-04 | Spoofing / Tampering | prompt injection in run content escalates to auto-execution | mitigate | The router only CLASSIFIES to CHANNEL_CONCIERGE (pure, no model); the Concierge is proposal-only; consequential intents need a confirm chip → injected text cannot self-execute. |
| T-33-03-SC | Tampering | pip installs | accept | No new package installs (reuses existing seams + the 33-02 concierge). Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- Offline unit suites green: `python3.11 -m pytest tests/unit/test_concierge_escalation.py tests/unit/test_concierge_proposal_channels.py -x`.
- Chat-contract golden untouched: `python3.11 -m pytest tests/unit/test_chat_contract.py -x` still passes (INV-12 — ChatRunner not forked).
- Import gate: `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken.
- LIVE-DEFERRED (Phase-34, mark `human_needed`): live mid-run steering delivery (needs the live in-process `ectx` handle — `_live_ectx_for_run` returns `None` today, `run_commands.py:350-362`). Do NOT attempt offline.
</verification>

<success_criteria>
- `route_chat_turn` returns `CHANNEL_CONCIERGE` for free-form turns and existing channels (zero-model) for routable turns.
- Each `propose_*` intent disposes through its matching existing seam exactly once; consequential proposals held behind a confirm chip.
- Post-run free-form turns mint revision-family children via the existing seam (no fork); the chat-contract golden is untouched.
- import-linter 4 kept / 0 broken.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (33-03)

New router constant:
- `CHANNEL_CONCIERGE = "concierge"` (in `chat_router.py`) + a pure free-form escalation branch in `route_chat_turn`

New event type:
- `concierge_proposal` — a `run_events` row carrying a pending consequential proposal held behind a confirm chip (added to `_DOCUMENTED_EVENT_TYPES` + the golden-neutrality guard in 33-05)

New app-layer behavior (in `run_commands.py::post_message`):
- a `CHANNEL_CONCIERGE` dispatch branch invoking the concierge + projecting `chat_reply`
- proposal→channel disposal reusing `set_review_response` / `apply_steering` / `_mint_revision_row` + `_drive_revision_to_queue`

New test files:
- `backend/tests/unit/test_concierge_escalation.py`
- `backend/tests/unit/test_concierge_proposal_channels.py`

Reused (NOT new): `set_review_response`, `apply_steering`, `_mint_revision_row`, `_drive_revision_to_queue`, `REVISION_BASE_MAP`, `run_events` (`chat_reply`).
</artifacts_produced>

<output>
Create `.planning/phases/33-concierge-compaction-a5/33-03-SUMMARY.md` when done.
</output>
