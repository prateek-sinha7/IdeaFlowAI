---
phase: 33-concierge-compaction-a5
plan: 04
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - frontend/src/components/chat/RunChatLane.tsx
  - frontend/src/components/chat/ChatTokenWidget.tsx
autonomous: true
requirements: [D-05, D-08, SC-1]

must_haves:
  truths:
    - "RunChatLane renders a confirm-chip affordance for consequential Concierge proposals (gate action / revision) — the user confirms before the app executes (D-05, SC-1)."
    - "RunChatLane exposes a 'compact' affordance and surfaces composed-context/token usage via the existing ChatTokenWidget — FE only DISPLAYS usage, no FE compression (D-08)."
    - "The FE EXTENDS the existing suggestion-chip + ChatTokenWidget paths — no parallel widget is created (INV-12)."
  artifacts:
    - path: "frontend/src/components/chat/RunChatLane.tsx"
      provides: "confirm-chip UX + compact affordance on the existing lane"
      contains: "confirm"
    - path: "frontend/src/components/chat/ChatTokenWidget.tsx"
      provides: "composed-context/token usage display (extended)"
  key_links:
    - from: "frontend/src/components/chat/RunChatLane.tsx"
      to: "the concierge_proposal confirm round-trip"
      via: "confirm chip onClick → post_message confirm"
      pattern: "confirm"
    - from: "frontend/src/components/chat/RunChatLane.tsx"
      to: "frontend/src/components/chat/ChatTokenWidget.tsx"
      via: "import ChatTokenWidget (existing)"
      pattern: "ChatTokenWidget"
---

<objective>
Extend the existing Phase-31 chat lane FE (D-05/D-08 display half): add a confirm-chip affordance for consequential Concierge proposals, a "compact" affordance, and composed-context/token usage display via the existing `ChatTokenWidget`. FE only DISPLAYS and CONFIRMS — no FE compression (D-08).

Purpose: Completes ROADMAP SC-1's user-facing half — the user sees and confirms consequential proposals before the app executes them (the confirm-chip hold from 33-03). EXTEND `RunChatLane.tsx` (which already renders suggestion chips and imports `ChatTokenWidget`) — do NOT create a parallel widget (INV-12/FE-extend mandate).
Output: `RunChatLane.tsx` gains a confirm-chip render path (modeled on the existing suggestion-chip path) + a compact affordance; `ChatTokenWidget.tsx` surfaces composed-context usage.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-concierge-compaction-a5/33-CONTEXT.md
@.planning/phases/33-concierge-compaction-a5/33-PATTERNS.md
@.planning/phases/33-concierge-compaction-a5/33-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: confirm-chip UX + compact affordance on RunChatLane</name>
  <files>frontend/src/components/chat/RunChatLane.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunChatLane.tsx:74 (`LaneSuggestion` type + "never a workflow-name literal" note), :104-105 (`suggestions?: LaneSuggestion[]` prop), :287-305 (the existing "Suggested next steps" quick-reply chip render path with `data-testid="chat-suggestion-chip"` / `data-suggestion-id`), :38 (`import { ChatTokenWidget }`)
    - 33-03-SUMMARY.md (the `concierge_proposal` event shape + the confirm round-trip contract — what the FE must POST to confirm/reject a pending proposal)
  </read_first>
  <action>
    EXTEND `RunChatLane.tsx` — do NOT create a new component. Add a confirm-chip render path modeled on the existing suggestion-chip path (:287-305): when a `concierge_proposal` (pending consequential intent from 33-03) is present, render a confirm/reject chip pair (`data-testid="chat-proposal-confirm"` / `data-testid="chat-proposal-reject"`, carrying the proposal id) whose confirm handler POSTs the confirm turn back through the existing chat send path (the same `post_message` seam) so the app executes the held proposal; reject dismisses it. Keep the chips GENERIC — never a workflow-name literal (INV-1), mirroring the existing `LaneSuggestion` discipline. Add a "compact" affordance (a control/button, `data-testid="chat-compact"`) that signals the backend compaction is available/usage is high — FE only DISPLAYS/triggers; it does NOT compress (D-08). Wire it to the existing token/usage state so it surfaces when composed-context usage is high.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` (or `npm run build`) succeeds — no type errors from the new props/render paths.
    - `grep -cE "chat-proposal-confirm|chat-proposal-reject|chat-compact" frontend/src/components/chat/RunChatLane.tsx` >= 3 (the new testids present).
    - `grep -nE "\"prototype\"|\"od_|pipeline_type|workflowName ===" frontend/src/components/chat/RunChatLane.tsx` shows NO new workflow-name literal introduced by the chips (INV-1 — generic).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: composed-context/token usage display via existing ChatTokenWidget</name>
  <files>frontend/src/components/chat/ChatTokenWidget.tsx, frontend/src/components/chat/RunChatLane.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatTokenWidget.tsx (the EXISTING P26 token/context-usage widget — already imported by RunChatLane; extend it to surface composed-context usage, do NOT create a second widget)
    - frontend/src/components/chat/RunChatLane.tsx:38 (existing `ChatTokenWidget` import + its current usage site)
  </read_first>
  <action>
    EXTEND `ChatTokenWidget.tsx` to surface the composed-context usage (the bounded conversation-context size relative to budget) alongside the existing P26 token telemetry — so the user can SEE when history is near budget (the trigger for the compact affordance in Task 1). Do NOT create a parallel widget and do NOT add FE compression logic (D-08 — display only). If the composed-context usage value is not yet supplied by the run stream, degrade gracefully (hide the sub-display) rather than error. Keep the token/context values sourced from the existing P26 telemetry the widget already consumes.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` (or `npm run build`) succeeds.
    - `grep -c "ChatTokenWidget" frontend/src/components/chat/RunChatLane.tsx` >= 1 (still the single widget — no parallel token widget created).
    - No new token widget file: `ls frontend/src/components/chat/ | grep -iE "token" ` shows only `ChatTokenWidget.tsx` (INV-12 — extend, not fork).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → confirm chip → post_message | the confirm action is the ONLY path that executes a consequential proposal |
| run content → chat render | free-form chat/answer text is rendered in the lane (XSS surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Elevation | consequential proposal executes without an explicit confirm | mitigate | Execution is gated on the confirm chip click → a confirm turn POSTed through `post_message`; the backend hold (33-03) does not execute until then. Verified by the confirm-testid presence + the 33-03 held-not-executed backend assertion. |
| T-33-04-02 | Tampering (XSS) | free-form chat / Concierge answer text rendered in the lane | mitigate | Render text through React's default escaping (no `dangerouslySetInnerHTML` for chat/answer/proposal text). Verified by `grep -c "dangerouslySetInnerHTML" RunChatLane.tsx` staying 0 for the new paths (V5 Input Validation — sanitize at render). |
| T-33-04-SC | Tampering | npm installs | accept | No new npm packages — extends existing components only. Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- FE type-check/build green: `cd frontend && npx tsc --noEmit` (or `npm run build`).
- Existing mocked chat e2e still green if present: `cd frontend && npm run e2e` for the RunChatLane spec (mocked) — extend it to click the confirm chip if the harness supports it; otherwise assert testid presence.
- `grep -c "dangerouslySetInnerHTML" frontend/src/components/chat/RunChatLane.tsx` == 0 for the new chat/proposal text paths (XSS-safe render).
- LIVE-DEFERRED (Phase-34, mark `human_needed`): live visual confirmation of the confirm-chip flow + compact affordance against a real run. Build + offline-prove here.
</verification>

<success_criteria>
- RunChatLane renders confirm/reject chips for consequential Concierge proposals and a compact affordance, modeled on the existing suggestion-chip path (no parallel widget).
- ChatTokenWidget surfaces composed-context usage; FE displays only (no FE compression).
- FE type-checks/builds clean; no new workflow-name literal; chat text rendered XSS-safe.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (33-04)

New FE testids / render paths (in `RunChatLane.tsx`):
- `data-testid="chat-proposal-confirm"`, `data-testid="chat-proposal-reject"` (confirm-chip pair for consequential proposals)
- `data-testid="chat-compact"` (compact affordance)

Extended (NOT new) components:
- `RunChatLane.tsx` — confirm-chip render path modeled on the existing suggestion-chip path
- `ChatTokenWidget.tsx` — composed-context usage sub-display added to the existing P26 widget

No new files, no new npm packages (FE-extend mandate).
</artifacts_produced>

<output>
Create `.planning/phases/33-concierge-compaction-a5/33-04-SUMMARY.md` when done.
</output>
