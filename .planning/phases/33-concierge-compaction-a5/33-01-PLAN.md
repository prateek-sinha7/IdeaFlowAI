---
phase: 33-concierge-compaction-a5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/compaction/chat_history.py
  - backend/agents/capabilities/context_providers/conversation.py
  - backend/agents/capabilities/registry.py
  - backend/tests/agents/test_chat_history_compaction.py
  - backend/tests/agents/test_conversation_provider.py
  - backend/tests/agents/test_registry_capabilities.py
  - backend/tests/agents/test_input_providers_run_images.py
  - backend/tests/agents/test_uploaded_files_provider.py
autonomous: true
requirements: [D-08, SC-2]

must_haves:
  truths:
    - "A long chat transcript compacted by compaction:chat_history stays under the configured budget with the most-recent turns byte-verbatim and older turns summarized (SC-2, D-08)."
    - "context_provider:conversation returns a single {\"conversation_context\": ...} block composed from compacted run_events when its inject token is declared, and {} otherwise (dormant on golden paths — INV-3)."
    - "The registry reports len(_KNOWN)==68 with the 2 new kernel capabilities registered and discover()-bound (registry lockstep, POR §97)."
  artifacts:
    - path: "backend/agents/capabilities/compaction/chat_history.py"
      provides: "compaction:chat_history pure compact() capability"
      contains: "@register"
      min_lines: 40
    - path: "backend/agents/capabilities/context_providers/conversation.py"
      provides: "context_provider:conversation ContextProvider clone"
      contains: "conversation_context"
      min_lines: 40
    - path: "backend/tests/agents/test_chat_history_compaction.py"
      provides: "under-budget + recent-verbatim fidelity proof"
    - path: "backend/tests/agents/test_conversation_provider.py"
      provides: "provider load + dormant proof"
  key_links:
    - from: "backend/agents/capabilities/context_providers/conversation.py"
      to: "compaction:chat_history"
      via: "registry.resolve(\"compaction\",\"chat_history\").compact(...)"
      pattern: "resolve\\(\\s*[\"']compaction[\"']"
    - from: "backend/agents/capabilities/registry.py"
      to: "the 2 new kernel capability modules"
      via: "_builtin_modules import + _KNOWN literal seed"
      pattern: "context_providers\\.conversation"
---

<objective>
Land the two kernel-pure capabilities that bound composed chat history — `compaction:chat_history` (a pure `compact()` transform) and `context_provider:conversation` (a `ContextProvider` clone that composes compacted `run_events` into a context block) — and complete the registry lockstep bumping the drift-guard count 66 → 68.

Purpose: These form the bounded-history substrate the Concierge consumes (forced dependency order — RESEARCH §Dependency Ordering). Compaction is a pure function provable fully offline (analog `html_skeleton.compact`); the conversation provider is a near-verbatim clone of `uploaded_files.py` reading owner-scoped `run_events`. Proves ROADMAP SC-2 offline.
Output: 2 new capability modules, 2 new offline test files, registry lockstep (2 pairs seeded in `_KNOWN` + `_EXPECTED_NAMES`, 2 module paths in `_builtin_modules`, count 66→68 in three drift-guard files).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-concierge-compaction-a5/33-CONTEXT.md
@.planning/phases/33-concierge-compaction-a5/33-RESEARCH.md
@.planning/phases/33-concierge-compaction-a5/33-PATTERNS.md
@.planning/phases/33-concierge-compaction-a5/33-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: compaction:chat_history pure compact() capability + offline under-budget test</name>
  <files>backend/agents/capabilities/compaction/chat_history.py, backend/tests/agents/test_chat_history_compaction.py</files>
  <read_first>
    - backend/agents/capabilities/compaction/html_skeleton.py:16-45 (the analog: purity docstring :16-19, `@register("compaction","html_skeleton",...)` :29, duck-typed `def compact(self, html) -> str` :45 — the `compaction` kind has NO formal Protocol port)
    - backend/tests/agents/test_phase3_compaction.py:1-138 (clone target: `from tests.agents import _scripted_model` FIRST for offline env, `_FILLER`-style long fixture, `assert len(skeleton) <= 0.5*len(source)` reduction gate + fidelity gate)
  </read_first>
  <action>
    Create `HtmlSkeletonCompaction`'s sibling class `ChatHistoryCompaction` decorated `@register("compaction", "chat_history", description="Summarize chat turns beyond budget and keep the most-recent turns verbatim to bound the composed conversation context.")` with `name = "chat_history"`. Signature: `def compact(self, transcript, *, budget, keep_recent) -> str` (parameterize budget + keep_recent per RESEARCH Open-Q2 — assert shape, not a magic number). Behavior: keep the `keep_recent` most-recent turns byte-verbatim; replace older turns with a shorter summary marker so the whole block fits under `budget` (measure with `count_tokens_approximately` from langchain or `len` — pick one and assert against the same). Import-PURE: registry decorator + stdlib ONLY — mirror html_skeleton.py:16-19 purity docstring; NO `app.*`, NO `agents.execution_engine`, NO deepagents import here (in-graph message growth is separately handled by the base-stack SummarizationMiddleware already wired in deep_agent_runner.py:346 — do NOT re-add it). Do NOT add a `base.py` port (duck-typed, like html_skeleton).
    Create `test_chat_history_compaction.py` cloning `test_phase3_compaction.py`: import `_scripted_model` first (offline `RUNS_ROOT`→temp), build a long `_FILLER`-repeated transcript exceeding a realistic budget, call `ChatHistoryCompaction().compact(transcript, budget=..., keep_recent=...)`, assert (a) under budget (analog of the `<= 0.5*len(source)` reduction gate) and (b) a specific recent turn's exact text is present AND a specific old turn's verbatim body is absent (summarized).
  </action>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/agents/test_chat_history_compaction.py -x` passes (from backend/).
    - `grep -nE "import app|from app|agents\.execution_engine|from deepagents" backend/agents/capabilities/compaction/chat_history.py` returns 0 lines (import purity).
    - `/opt/homebrew/bin/lint-imports` reports 4 kept / 0 broken.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: context_provider:conversation clone + offline load/dormant test</name>
  <files>backend/agents/capabilities/context_providers/conversation.py, backend/tests/agents/test_conversation_provider.py</files>
  <read_first>
    - backend/agents/capabilities/context_providers/uploaded_files.py:58-138 (clone target: self-gate on `ctx.current_spec_injects` :74-79, reach the run handle via `getattr(ctx, "runner", ...)` :81-84, degrade-not-crash → `{}`, single named block return `{"uploaded_files_context": body}` :110)
    - backend/agents/capabilities/base.py:76-83 (the `ContextProvider` Protocol: `name: str` + `async def load(self, ctx) -> dict[str, str]`)
    - backend/agents/authz.py:314 (`ScopedStore.read_events(run_id, after_seq)` — owner+workspace scoped, cross-owner → nothing → 404; the owner-scoped read surface)
    - backend/tests/agents/test_uploaded_files_provider.py:1-60 (clone shape: `SimpleNamespace` ctx fakes, `.name` assert, gated vs un-gated `load`)
  </read_first>
  <action>
    Clone `uploaded_files.py` into `context_providers/conversation.py` as `class ConversationProvider` decorated `@register("context_provider", "conversation", description="Surface the run's compacted chat history as sticky agent/steering context.")` with `name = "conversation"`. Self-gate on the DECLARED inject token `"conversation"` in `ctx.current_spec_injects` (never on `spec.id`/`pipeline_type` — INV-1); return `{}` when un-gated (this keeps it DORMANT on golden runs, so INV-3 holds). When gated: reach the owner-scoped read surface via the `ctx` handle (mirror uploaded_files' `getattr(ctx, "runner", ...)` — resolve the exact scoped-read accessor against uploaded_files.py's ctx usage + the ScopedStore construction; NEVER raw ORM — IDOR→404 lives in ScopedStore), read this run's chat `run_events`, pass them through `registry.resolve("compaction","chat_history").compact(...)`, and return a single block `{"conversation_context": body}`. Degrade-not-crash → `{}` on any miss. Import-PURE: registry decorator + stdlib ONLY (NO `app.*`, NO `agents.execution_engine`).
    Create `test_conversation_provider.py` cloning `test_uploaded_files_provider.py`: assert `ConversationProvider().name == "conversation"`; a gated ctx with fake compacted events → `load` returns a dict whose only key is `"conversation_context"`; an un-gated ctx (token absent) → `{}`; an empty/error read → `{}` (dormant).
  </action>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/agents/test_conversation_provider.py -x` passes (from backend/).
    - `grep -nE "import app|from app|agents\.execution_engine" backend/agents/capabilities/context_providers/conversation.py` returns 0 lines.
    - `grep -c "conversation_context" backend/agents/capabilities/context_providers/conversation.py` >= 1 and `grep -c "current_spec_injects" backend/agents/capabilities/context_providers/conversation.py` >= 1 (self-gate present).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: registry lockstep for the 2 kernel capabilities (66 → 68)</name>
  <files>backend/agents/capabilities/registry.py, backend/tests/agents/test_registry_capabilities.py, backend/tests/agents/test_input_providers_run_images.py, backend/tests/agents/test_uploaded_files_provider.py</files>
  <read_first>
    - backend/agents/capabilities/registry.py:81 (`_KNOWN` literal — seed pairs here), :180-218 (`register` side-effect auto-adds to `_KNOWN`), :221-344 (`discover()`), :252 (`_builtin_modules` tuple — add the 2 kernel module paths), :312 (`_forward_packages` — NOT touched here; the concierge lands in 33-02)
    - backend/tests/agents/test_registry_capabilities.py:38 (`_EXPECTED_NAMES`), :150-164 (the running tally comment ending "= 66" + `assert len(_KNOWN) == 66` :163 + `assert set(_KNOWN) == set(_EXPECTED_NAMES)` :164)
    - backend/tests/agents/test_input_providers_run_images.py:132 (`assert len(_KNOWN) == 66`)
    - backend/tests/agents/test_uploaded_files_provider.py:200 (`assert len(_KNOWN) == 66`)
  </read_first>
  <action>
    Do the mechanical lockstep for the 2 kernel capabilities from Tasks 1-2 (the concierge is NOT registered here — it is app-side in 33-02, keeping its own 68→69 bump):
    (1) `registry.py:81` `_KNOWN` literal — add `("compaction", "chat_history")` and `("context_provider", "conversation")` with `# 33 / D-08` comments (seed the literal so manifest refs validate impl-free at compiler import — INV-4 pure-membership path).
    (2) `registry.py:252` `_builtin_modules` — add `"agents.capabilities.compaction.chat_history"` and `"agents.capabilities.context_providers.conversation"` (kernel-side, imported eagerly by `discover()`).
    (3) `test_registry_capabilities.py:38` `_EXPECTED_NAMES` — append the same 2 pairs (house comment style).
    (4) Bump `assert len(_KNOWN) == 66` → `68` in ALL THREE files: `test_registry_capabilities.py:163`, `test_input_providers_run_images.py:132`, `test_uploaded_files_provider.py:200`. Append 2 lines to the running tally comment (`test_registry_capabilities.py:150-162`) continuing "...= 66, plus the two 33 D-08 capabilities (compaction:chat_history + context_provider:conversation, bounded chat history) = 68." in house style.
    Bump ALL THREE count asserts + `_EXPECTED_NAMES` in this single task/commit — a partial bump red-fails `test_registered_count_is_exactly_fifty` (asserts count AND `set(_KNOWN)==set(_EXPECTED_NAMES)`).
  </action>
  <acceptance_criteria>
    - `python3.11 -m pytest tests/agents/test_registry_capabilities.py -x` passes (count 68, `_KNOWN`==`_EXPECTED_NAMES`).
    - `python3.11 -m pytest tests/agents/test_registry_capabilities.py -k discover` passes (`resolve()` binds both new impls).
    - `python3.11 -m pytest tests/agents/test_input_providers_run_images.py tests/agents/test_uploaded_files_provider.py -x` passes (count 68 in both).
    - `grep -rn "len(_KNOWN) == 66" backend/tests/agents/` returns 0 lines; `grep -rn "len(_KNOWN) == 68" backend/tests/agents/` returns 3 lines.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run content → conversation provider | chat `run_events` bodies are untrusted user text composed into an agent context block |
| owner/workspace scope → run_events read | the conversation provider must read only THIS owner+workspace's events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Information Disclosure | `context_provider:conversation` reading `run_events` | mitigate | All reads via the owner-scoped read surface reached through `ctx` (ScopedStore `read_events`, `authz.py:314`) — NEVER raw ORM; cross-owner → nothing → 404. Verified by the clone-of-uploaded_files import-purity + no raw-ORM grep. |
| T-33-01-02 | Tampering | untrusted run text summarized by `compact()` | accept | `compact()` is a pure string transform with no execution/eval; malicious text is only truncated/summarized, never interpreted. Low risk (no code path executes the content). |
| T-33-01-SC | Tampering | npm/pip installs | accept | No new package installs in this plan (compaction is stdlib + registry decorator; no deepagents import). Package Legitimacy Gate N/A. |
</threat_model>

<verification>
- Full targeted register/parity suite green: `python3.11 -m pytest tests/agents/test_registry_capabilities.py tests/agents/test_chat_history_compaction.py tests/agents/test_conversation_provider.py -x`.
- Import gate: `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken (both new modules kernel-pure).
- NEVER run the full backend pytest suite (it HANGS — Chromium/Bedrock/Postgres-gated).
</verification>

<success_criteria>
- `compaction:chat_history` and `context_provider:conversation` are registered, discover()-bound, and resolvable; `len(_KNOWN)==68` across all three drift-guard files.
- Offline compaction proof: a long transcript compacts under budget with recent turns byte-verbatim and old turns summarized.
- The conversation provider returns `{"conversation_context": ...}` when gated and `{}` when un-gated (dormant → INV-3 preserved).
- Both new modules import-pure (lint-imports 4/0).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (33-01)

New capability registry names:
- `("compaction", "chat_history")` — `@register` in `backend/agents/capabilities/compaction/chat_history.py`
- `("context_provider", "conversation")` — `@register` in `backend/agents/capabilities/context_providers/conversation.py`

New classes / methods:
- `ChatHistoryCompaction` (class) with `name = "chat_history"` and `compact(self, transcript, *, budget, keep_recent) -> str`
- `ConversationProvider` (class) with `name = "conversation"` and `async def load(self, ctx) -> dict[str, str]`

New context block key:
- `"conversation_context"` (returned by `ConversationProvider.load`)

New inject token:
- `"conversation"` (self-gate token for the conversation provider)

New test files:
- `backend/tests/agents/test_chat_history_compaction.py`
- `backend/tests/agents/test_conversation_provider.py`

Registry constants changed (not new symbols): `_KNOWN` literal +2 pairs, `_builtin_modules` +2 paths, `_EXPECTED_NAMES` +2 pairs, count 66→68.
</artifacts_produced>

<output>
Create `.planning/phases/33-concierge-compaction-a5/33-01-SUMMARY.md` when done.
</output>
