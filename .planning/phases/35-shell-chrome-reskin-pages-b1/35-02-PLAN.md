---
phase: 35-shell-chrome-reskin-pages-b1
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - frontend/src/components/settings/AccountSettings.tsx
  - frontend/src/components/settings/AccountSettings.render.test.tsx
autonomous: true
requirements: [B1-02]
user_setup: []

must_haves:
  truths:
    - "Account Settings is fully reskinned onto Phase-32 tokens/primitives (Tabs/Card/Button/Badge) with NO per-page palette fork (D-15) and retired-palette grep=0."
    - "Every existing wired control still works: email display via getMe, password change via changePassword, model preference via getPreferences/updatePreferences, constitution via /api/settings/constitution GET/PUT/DELETE (D-15 keep-richer-behavior; INV-3 no backend change)."
    - "No unbacked fields are added — Full name / Role / Organization are mock fiction with no endpoint and MUST NOT appear; email stays read-only; usage-limit numbers stay as-is (NO new backend)."
  artifacts:
    - path: "frontend/src/components/settings/AccountSettings.tsx"
      provides: "Token-reskinned settings surface with preserved wiring"
      contains: "getPreferences"
    - path: "frontend/src/components/settings/AccountSettings.render.test.tsx"
      provides: "Behavior-parity render test asserting existing api fns still called"
  key_links:
    - from: "frontend/src/components/settings/AccountSettings.tsx"
      to: "/api/settings/preferences + /api/auth/change-password"
      via: "getPreferences/updatePreferences/changePassword"
      pattern: "getPreferences|updatePreferences|changePassword"
---

<objective>
Reskin `AccountSettings.tsx` (759 lines, 21 retired-palette lines — the heaviest single file) onto the Phase-32 token layer and `ui/*` primitives, preserving all four already-wired sections. This is a RESKIN, not a static→real build: the mock's "static fields" are mock fiction; the product controls are already real.

Purpose: Deliverable B1-02. Complete the token migration for the settings surface without regressing any endpoint call and without adding unbacked fields.

Output: Reskinned `AccountSettings.tsx` + a behavior-parity render test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-RESEARCH.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/styles/globals.css
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/results/AuditTab.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Tabs.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Card.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Button.tsx
</context>

## Artifacts this phase produces (plan 35-02)

- `frontend/src/components/settings/AccountSettings.render.test.tsx` (NEW) — mounts the component with mocked `@/lib/api`, asserts `getMe`/`getPreferences`/`getCapabilities` load calls fire, the password form calls `changePassword`, model save calls `updatePreferences`, and the constitution section fetches `/api/settings/constitution`. No new runtime symbols.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write AccountSettings behavior-parity render test (RED/GREEN scaffold)</name>
  <files>frontend/src/components/settings/AccountSettings.render.test.tsx</files>
  <read_first>
    - frontend/src/components/settings/AccountSettings.tsx (call sites: getMe/getPreferences/getCapabilities L102-116, changePassword form L253-276, updatePreferences save L121-130, constitution fetch L640-686)
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (vitest + testing-library + api-mock conventions to mirror)
  </read_first>
  <behavior>
    - On mount, the component invokes the existing loaders (`getMe`, `getPreferences`, `getCapabilities`).
    - Submitting the password form (with a valid ≥8-char new password) invokes `changePassword(token, current, new)`.
    - Saving a changed model invokes `updatePreferences(token, modelId)`.
    - The constitution section reads `/api/settings/constitution`.
    - The rendered profile section shows email read-only and does NOT render Full name / Role / Organization inputs (fiction guard).
  </behavior>
  <action>Author a vitest + @testing-library render test that mocks `@/lib/api` (vi.mock) and asserts the behavior contract above. Mirror the mocking/render conventions in `ReviewGatesSection.test.tsx`. Because the wiring already exists, the load-call assertions should PASS immediately; the fiction-guard assertion (no name/role/org inputs) also encodes the scope boundary. Do NOT edit `AccountSettings.tsx` in this task.</action>
  <acceptance_criteria>
    - `cd frontend && npm run test -- AccountSettings.render` RUNS; load-call + fiction-guard assertions define the parity contract.
    - `cd frontend && npx tsc --noEmit` shows no NEW type errors from the test file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- AccountSettings.render 2>&1 | tail -5</automated>
  </verify>
  <done>Behavior-parity + fiction-guard test exists and encodes the wiring contract that the reskin must not break.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reskin AccountSettings to tokens/primitives, preserve all wiring</name>
  <files>frontend/src/components/settings/AccountSettings.tsx</files>
  <read_first>
    - frontend/src/components/settings/AccountSettings.tsx (the file being reskinned; 21 retired lines incl. TIER_DETAILS L37-50)
    - frontend/src/components/results/AuditTab.tsx (chip-select idiom L411-422 active = bg-brand-fill border-brand-border; card + ink ramp)
    - frontend/src/components/ui/Tabs.tsx (section tab bar), ui/Card.tsx, ui/Button.tsx, ui/Badge.tsx (tier chip)
    - frontend/src/styles/globals.css (token utilities + radius/elevation vars)
  </read_first>
  <action>Reskin `AccountSettings.tsx` — swap every `#1B2A4A`/`gray-*`/`slate-*`/raw hex (incl. `TIER_DETAILS` L37-50) to token utilities: text → `text-ink-900/700/500`, surfaces → `bg-surface-card/warm/white`, borders → `border-line-border/divider/control`, brand accents → `text-brand`/`bg-brand`/`bg-brand-fill`. Replace the section tab bar (`setTabs` for profile|model|limits|constitution) with the `Tabs` primitive (`role=tablist`, `data-testid=tab-*`). Replace ad-hoc panels/cards with `Card`; primary/secondary actions (Save model, Change password, constitution Save/Reset) with `Button`; tier/plan chips with `Badge` (tier). Model-selection radio cards → AuditTab chip-select idiom (active = `bg-brand-fill border-brand-border`). Fonts: headings → `font-sans` (Manrope), not `font-serif`. PRESERVE every endpoint call and control: `getMe`/`getPreferences`/`getCapabilities` loaders, `changePassword` password form (keep pw≥8 client validation), `updatePreferences` model save (`pendingModel`/`handleSaveModel`), constitution `fetch('/api/settings/constitution')` GET/PUT/DELETE, `TIER_PIPELINES`/`TIER_LABELS` limits display. Do NOT add Full name / Role / Organization inputs (no endpoint — mock fiction); email stays read-only display; leave the hardcoded usage-limit numbers (L566-574) as-is (no usage API — NO new backend, INV-3). No fenced hex; no new fetch code.</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/settings/AccountSettings.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-' frontend/src/components/settings/AccountSettings.tsx` returns nothing; file positively uses `text-ink-`/`bg-surface-`/`border-line-` and imports Tabs/Card/Button.
    - Wiring intact: file still contains `getPreferences`, `updatePreferences`, `changePassword`, and `/api/settings/constitution`.
    - Fiction guard: no new `<input` labelled name/role/organization added (grep -i 'organization\|full name' shows no new input field).
    - Behavior GREEN: `cd frontend && npm run test -- AccountSettings.render` passes; `npx tsc --noEmit` no NEW errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- AccountSettings.render && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-gray-|bg-gray-' src/components/settings/AccountSettings.tsx && echo PASS</automated>
  </verify>
  <done>AccountSettings fully tokenized via Tabs/Card/Button/Badge, all four wired sections preserved, no unbacked fields added, parity test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client settings form ↔ existing /api/settings/* + /api/auth/* | Reskin re-styles inputs; all reads/writes flow through unchanged api.ts functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | Tampering | password-change form | mitigate | Keep the existing pw≥8 client validation + `changePassword` call unchanged; parity test asserts `changePassword` still called. |
| T-35-02-02 | Information Disclosure | new profile fields | mitigate | Do NOT add name/role/org inputs (no backing endpoint) — fiction guard in the render test prevents introducing unbacked PII fields. |
| T-35-02-SC | Tampering | npm installs | accept | Zero package installs (Legitimacy Audit N/A). |

All threats LOW / satisfied-by-construction: reskin-only, no auth/session logic change, no new endpoint (INV-3).
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + retired-palette grep=0 + token-authority self-check on AccountSettings.tsx.
- Wave 2 merge: `npm run test` green by delta + `npm run e2e` (mocked) diffed vs `frontend/e2e/.baseline-35.txt` — no new failures. Live-only checks → live-deferred.
</verification>

<success_criteria>
- AccountSettings fully tokenized (Tabs/Card/Button/Badge), retired-palette grep=0, no stray stock palette.
- Every wired control preserved (email/password/model/constitution/limits); no unbacked fields; parity test green.
</success_criteria>

<output>
Create `.planning/phases/35-shell-chrome-reskin-pages-b1/35-02-SUMMARY.md` when done.
</output>
