---
phase: 35-shell-chrome-reskin-pages-b1
plan: 06
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - frontend/src/app/login/page.tsx
  - frontend/src/app/register/page.tsx
  - frontend/src/app/login/login.reskin.test.tsx
autonomous: true
requirements: [B1-05, B1-06]
user_setup: []

must_haves:
  truths:
    - "Login is reskinned onto Phase-32 tokens with a NEW ~46% dark brand panel (bg-surface-near-black + Manrope wordmark) added on the left and the form on the right — reskin + restructure, NOT a face-value rebuild (ND-12, D-15)."
    - "The wired form is fully preserved: login() → redirect by data.user.is_admin (admin→/admin else→/dashboard), 401 → 'Invalid email or password', loading disable, invite-only footer (ND-12 keep-richer-behavior; INV-3 no backend change)."
    - "Identity traps DROPPED (no SSO button, no Forgot link — no backend exists for them); e2e selectors preserved: id=email, id=password, submit accessible name 'Sign in', heading 'Welcome back'."
    - "Register stays a redirect-to-/login stub (backend register → 403) — only the #f5f5f0 literal is tokenized for gate compliance; behavior confirmed unchanged (B1-06)."
  artifacts:
    - path: "frontend/src/app/login/page.tsx"
      provides: "Reskinned two-column login with dark brand panel + preserved wired form"
      contains: "bg-surface-near-black"
    - path: "frontend/src/app/login/login.reskin.test.tsx"
      provides: "Render test: brand panel present + selectors preserved + no SSO/Forgot"
  key_links:
    - from: "frontend/src/app/login/page.tsx"
      to: "router.push(is_admin ? /admin : /dashboard)"
      via: "login() success"
      pattern: "is_admin"
---

<objective>
Reskin `app/login/page.tsx` onto the Phase-32 token layer, add the ~46% dark brand panel, and preserve the fully-wired form. Confirm `app/register/page.tsx` is a correct redirect stub and tokenize its single retired literal. DROP the identity traps (SSO / Forgot) — they have no backend.

Purpose: Deliverables B1-05 (login) + B1-06 (register no-work confirm). Complete the token migration for auth pages while keeping the redirect-by-is_admin flow and all e2e selectors intact.

Output: Reskinned two-column `login/page.tsx` + tokenized `register/page.tsx` + a render test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-RESEARCH.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/styles/globals.css
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/results/AuditTab.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Button.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/e2e/tests/ts-a.auth.spec.ts
</context>

## Artifacts this phase produces (plan 35-06)

- `frontend/src/app/login/login.reskin.test.tsx` (NEW) — asserts: the dark brand panel is present (an element with the near-black surface / brand wordmark), `#email` + `#password` inputs exist, the submit button has accessible name "Sign in", the heading text "Welcome back" is present, and NO "Continue with SSO" / "Forgot" affordances render. No new runtime symbols. The brand panel is added INLINE in `login/page.tsx` (no new component file).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write login reskin render test (brand panel + selector-preservation contract)</name>
  <files>frontend/src/app/login/login.reskin.test.tsx</files>
  <read_first>
    - frontend/src/app/login/page.tsx (wired form: login() L22, redirect by is_admin L23, error L25-31, ids L90/L107, submit "Sign in" L126, heading "Welcome back" L65)
    - frontend/e2e/tests/ts-a.auth.spec.ts (selectors: #email/#password L18-19, "Sign in" L20, "Welcome back" L13)
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (test conventions)
  </read_first>
  <behavior>
    - Renders `#email` and `#password` inputs.
    - Submit control has accessible name exactly "Sign in".
    - Heading text "Welcome back" is present.
    - A dark brand panel element is present (near-black surface + wordmark).
    - No "Continue with SSO" button and no "Forgot" link render (traps dropped).
  </behavior>
  <action>Author a vitest + @testing-library render test encoding the behavior contract above. The brand-panel and no-SSO/no-Forgot assertions describe the post-reskin state (RED now, GREEN after task 2); the selector assertions guard against regression. Mirror `ReviewGatesSection.test.tsx` conventions; stub `next/navigation` router and `@/lib/api` login as needed. Do NOT edit `login/page.tsx` in this task.</action>
  <acceptance_criteria>
    - `cd frontend && npm run test -- login.reskin` RUNS; assertions encode brand-panel + selector-preservation + no-traps contract.
    - `cd frontend && npx tsc --noEmit` no NEW type errors from the test file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- login.reskin 2>&1 | tail -5</automated>
  </verify>
  <done>Render test exists encoding the brand-panel, selector-preservation, and dropped-traps contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reskin login (two-column + dark brand panel), preserve form; tokenize register</name>
  <files>frontend/src/app/login/page.tsx, frontend/src/app/register/page.tsx</files>
  <read_first>
    - frontend/src/app/login/page.tsx (the file being reskinned)
    - frontend/src/app/register/page.tsx (redirect stub L10-20, #f5f5f0 literal)
    - frontend/src/components/results/AuditTab.tsx (token idioms)
    - frontend/src/components/ui/Button.tsx (primary submit)
    - frontend/src/styles/globals.css (surface-near-black, brand, input-focus util L249, radius/elevation)
  </read_first>
  <action>Reskin `login/page.tsx` into a two-column layout: LEFT ~46% dark brand panel `bg-surface-near-black` with a Manrope (`font-sans`) wordmark (+ a `#3C2CDA`/`text-brand`/`bg-brand` dot), eyebrow, headline, subhead (mirror evidence 02/10 §2; the "SSO & SCIM / audit trail" tags render as STATIC decoration only — wire nothing); RIGHT column keeps the form. Swap tokens: page bg `style={{background:"#f5f5f0"}}` (L40) → `bg-surface-paper`; eyebrow `var(--font-inter)` (L57) and H1 `var(--font-fraunces)` (L63) → `font-sans`; input focus `#1B2A4A` ring (L96/L113) → `focus:border-brand` + the `.input-focus` util; submit `bg-[#1B2A4A] hover:bg-[#243456]` (L124) → the `Button` primitive (`variant="primary"`, `type="submit"`); all `text-gray-*`/card `border-gray-200 bg-white` → token utilities. PRESERVE: `login()` call, redirect-by-`data.user.is_admin` (L23), 401→"Invalid email or password" + generic ApiError handling, loading disable, invite-only footer (L132), and the exact selectors — `id="email"`, `id="password"`, submit accessible name "Sign in", heading "Welcome back". DROP the SSO button and "Forgot?" link (no backend — ND-12). Then tokenize `register/page.tsx`: swap the `#f5f5f0` literal → `bg-surface-paper`; keep the `useEffect(router.replace("/login"))` redirect stub + "Redirecting…" exactly (confirmed correct — B1-06). No fenced hex; NO new backend/auth route (INV-3).</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/app/login/page.tsx frontend/src/app/register/page.tsx` returns nothing (incl. `var(--font-inter)`/`var(--font-fraunces)` removed).
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-|#243456' frontend/src/app/login/page.tsx` returns nothing; file positively contains `bg-surface-near-black` and uses `Button`.
    - Selectors preserved: `login/page.tsx` still contains `id="email"`, `id="password"`, the string `Sign in`, the string `Welcome back`, and `is_admin`.
    - Traps dropped: `grep -Eic 'SSO|forgot' frontend/src/app/login/page.tsx` = 0.
    - Register unchanged behavior: `register/page.tsx` still contains `router.replace("/login")`.
    - Behavior GREEN: `cd frontend && npm run test -- login.reskin` passes; `npx tsc --noEmit` no NEW errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- login.reskin && grep -q 'id="email"' src/app/login/page.tsx && grep -q 'Welcome back' src/app/login/page.tsx && grep -q 'is_admin' src/app/login/page.tsx && ! grep -rEn '#1B2A4A|#f5f5f0|Fraunces|Inter|bg-blue-|text-gray-' src/app/login/page.tsx src/app/register/page.tsx && echo PASS</automated>
  </verify>
  <done>Login is a tokenized two-column layout with a dark brand panel, wired form + all selectors preserved, SSO/Forgot dropped; register tokenized and confirmed as a redirect stub; login.reskin test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client ↔ /api/auth/login (existing) | Reskin re-styles the form; the login POST + redirect-by-is_admin flow is unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06-01 | Spoofing | new auth entry points | mitigate | Explicitly DROP SSO/Forgot — no new unauthenticated route added; acceptance grep asserts no SSO/forgot affordance. |
| T-35-06-02 | Repudiation | login behavior regression | mitigate | Preserve `login()` + redirect-by-is_admin + selectors; render test + existing `ts-a.auth` mocked spec stay green (by delta). |
| T-35-06-03 | Information Disclosure | credential handling | mitigate | Password input keeps `type="password"`; no credential logging added; no new field. Satisfied by construction. |
| T-35-06-SC | Tampering | npm installs | accept | Zero package installs (Legitimacy Audit N/A). |

All threats LOW / satisfied-by-construction: reskin-only, form wiring unchanged, no new auth surface (ND-12/INV-3).
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + retired-palette grep=0 + token-authority self-check + selector-preservation greps on login/register.
- Wave 2 merge: `npm run test` green by delta + `npm run e2e` (mocked, esp. `ts-a.auth.spec.ts`) diffed vs `frontend/e2e/.baseline-35.txt` — auth spec must NOT newly fail. Live auth redirect against a real server → live-deferred.
</verification>

<success_criteria>
- Login is a tokenized two-column layout with a dark brand panel; retired-palette grep=0, no stray stock palette.
- Wired form + redirect-by-is_admin + all e2e selectors preserved; SSO/Forgot dropped.
- Register tokenized + confirmed redirect stub; login.reskin test + auth spec green.
</success_criteria>

<output>
Create `.planning/phases/35-shell-chrome-reskin-pages-b1/35-06-SUMMARY.md` when done.
</output>
