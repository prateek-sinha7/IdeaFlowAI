---
phase: 35-shell-chrome-reskin-pages-b1
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - frontend/src/components/workflow/prototype/TemplateGallery.tsx
  - frontend/src/components/workflow/prototype/TemplateDetailModal.tsx
  - frontend/src/components/workflow/prototype/CustomTemplateModal.tsx
  - frontend/src/components/workflow/prototype/TemplateCard.tsx
  - frontend/src/components/workflow/prototype/TemplateGallery.reskin.test.tsx
autonomous: true
requirements: [B1-03]
user_setup: []

must_haves:
  truths:
    - "The template picker (gallery + detail modal + custom-template modal + card) is fully reskinned onto Phase-32 tokens/primitives (Tabs/Card/Pill/Button) with retired-palette grep=0 and no per-page palette fork (D-15)."
    - "Real behavior preserved: live search filtering, category tab filtering, custom-template upload + localStorage persistence, `has_preview` gating, detail modal, and real `onSelect` wiring — the mock's inert cards are NOT copied (D-15 keep-richer-behavior)."
    - "Picker is keyed on generic template data, never a workflow name (SC-001/INV-1); no transport/backend touched (INV-3)."
  artifacts:
    - path: "frontend/src/components/workflow/prototype/TemplateGallery.tsx"
      provides: "Token-reskinned template gallery with preserved search/filter/select"
      contains: "font-sans"
    - path: "frontend/src/components/workflow/prototype/TemplateGallery.reskin.test.tsx"
      provides: "Render test: search filters + grep-clean + real onSelect"
  key_links:
    - from: "frontend/src/components/workflow/prototype/TemplateGallery.tsx"
      to: "onSelect(template)"
      via: "card click"
      pattern: "onSelect"
---

<objective>
Reskin the template picker flow onto the Phase-32 token layer + `ui/*` primitives: `TemplateGallery.tsx` (505 lines, the grid + category tabs + real search) and its sibling modals `TemplateDetailModal.tsx`, `CustomTemplateModal.tsx`, `TemplateCard.tsx`. Preserve every wired behavior — the picker is already richer than the mock (real selection vs the mock's inert cards).

Purpose: Deliverable B1-03 (template side). Complete the token migration for the template-selection surface without regressing search, filtering, custom-upload, or selection.

Output: Reskinned template gallery + 3 sibling modal/card files + a render test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-RESEARCH.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/styles/globals.css
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/results/AuditTab.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Tabs.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Card.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Pill.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Button.tsx
</context>

## Artifacts this phase produces (plan 35-03)

- `frontend/src/components/workflow/prototype/TemplateGallery.reskin.test.tsx` (NEW) — asserts: typing in the search box filters the rendered templates, category tabs use `role=tab`, a card click calls `onSelect`, and no retired palette in the rendered classNames. No new runtime symbols.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reskin TemplateGallery grid + tabs + search, add render test</name>
  <files>frontend/src/components/workflow/prototype/TemplateGallery.tsx, frontend/src/components/workflow/prototype/TemplateGallery.reskin.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/prototype/TemplateGallery.tsx (CATEGORIES L34, search `query` L37, getBucket L23, filtered/filteredCustom L53-73, tab count `bg-[#1B2A4A]` L118, onSelect wiring)
    - frontend/src/components/results/AuditTab.tsx (card grid L182, chip/filter idiom L406-428)
    - frontend/src/components/ui/Tabs.tsx, ui/Card.tsx, ui/Pill.tsx, ui/Button.tsx
    - frontend/src/styles/globals.css
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (test conventions)
  </read_first>
  <behavior>
    - Typing a query into the search input reduces the rendered template set (real filter, not inert).
    - Category tabs render with `role=tab` and switching a category changes the filtered set.
    - Clicking a template card calls `onSelect` with that template.
  </behavior>
  <action>Reskin `TemplateGallery.tsx` to tokens: category tabs → the `Tabs` primitive (or the AuditTab chip idiom with active = `bg-brand-fill border-brand-border`); tab count badge `bg-[#1B2A4A]` (L118) → `bg-brand` or a `<Pill>`; template cards → `Card` + AuditTab row idiom; search input + upload button → token classes + `Button`; all `text-gray-*`/`bg-gray-*`/raw hex → `text-ink-*`/`bg-surface-*`/`border-line-*`; headings → `font-sans`. PRESERVE: the real `query` search filtering, `getBucket` scenario→bucket mapping, `filtered`/`filteredCustom` memos over real `PrototypeTemplate[]`, `has_preview` gating, custom-template upload + localStorage, `onSelect` wiring, and the `TemplateDetailModal`/`CustomTemplateModal` mounts. Do NOT make search inert or copy the mock's unwired cards (D-15). Then author `TemplateGallery.reskin.test.tsx` (vitest + testing-library) asserting the behavior contract above. No fenced hex; no backend (INV-3).</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/workflow/prototype/TemplateGallery.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-' frontend/src/components/workflow/prototype/TemplateGallery.tsx` returns nothing; file positively uses token utilities + a `ui/` primitive.
    - Wiring intact: file still contains `onSelect` and the search `query` filter path.
    - Behavior GREEN: `cd frontend && npm run test -- TemplateGallery.reskin` passes; `npx tsc --noEmit` no NEW errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- TemplateGallery.reskin && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-gray-|bg-gray-' src/components/workflow/prototype/TemplateGallery.tsx && echo PASS</automated>
  </verify>
  <done>TemplateGallery grid/tabs/search tokenized, real search/filter/select preserved, render test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reskin template sibling modals (detail + custom + card)</name>
  <files>frontend/src/components/workflow/prototype/TemplateDetailModal.tsx, frontend/src/components/workflow/prototype/CustomTemplateModal.tsx, frontend/src/components/workflow/prototype/TemplateCard.tsx</files>
  <read_first>
    - frontend/src/components/workflow/prototype/TemplateDetailModal.tsx (1 retired line)
    - frontend/src/components/workflow/prototype/CustomTemplateModal.tsx (3 retired lines)
    - frontend/src/components/workflow/prototype/TemplateCard.tsx (2 retired lines)
    - frontend/src/components/results/AuditTab.tsx (token idioms)
    - frontend/src/styles/globals.css (scrim var `--scrim`, elevation-modal, radius-card)
    - frontend/src/components/ui/Card.tsx, ui/Button.tsx, ui/Pill.tsx
  </read_first>
  <action>Reskin the three template sibling files to tokens: modal scrims → `bg-[var(--scrim)]`; modal panels → `Card` or `bg-surface-white border-line-border rounded-[var(--radius-card)] shadow-[var(--elevation-modal)]`; all `#1B2A4A`/`gray-*`/raw hex/`text-gray-*` → token utilities; primary/secondary buttons → `Button`; meta/category chips → `Pill`; headings → `font-sans`. PRESERVE all props, handlers, form logic (custom-template create/edit), and `onSelect`/`onClose` wiring. No fenced hex; no backend.</action>
  <acceptance_criteria>
    - Retired-palette grep = 0 across all three: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/workflow/prototype/TemplateDetailModal.tsx frontend/src/components/workflow/prototype/CustomTemplateModal.tsx frontend/src/components/workflow/prototype/TemplateCard.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-' <the three files>` returns nothing; files positively use tokens/primitives.
    - Behavior preserved: `npx tsc --noEmit` no NEW errors; component prop signatures unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-gray-|bg-gray-' src/components/workflow/prototype/TemplateDetailModal.tsx src/components/workflow/prototype/CustomTemplateModal.tsx src/components/workflow/prototype/TemplateCard.tsx && echo PASS</automated>
  </verify>
  <done>Template detail/custom/card modals tokenized, all form + select wiring preserved, tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client picker state ↔ local template data + localStorage | Selection/search/custom-upload are client-local; no server call added by the reskin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03-01 | Tampering | onSelect / search wiring | mitigate | Preserve real `onSelect` + `query` filter (do not copy inert mock cards); render test asserts search filters + onSelect fires. |
| T-35-03-02 | Tampering | picker keying (SC-001) | accept | Picker keyed on generic `PrototypeTemplate[]`, never a workflow name. Satisfied by construction. |
| T-35-03-SC | Tampering | npm installs | accept | Zero package installs (Legitimacy Audit N/A). |

All threats LOW / satisfied-by-construction: reskin-only, no new backend/endpoint (INV-3).
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + retired-palette grep=0 + token-authority self-check on touched files.
- Wave 2 merge: `npm run test` green by delta + `npm run e2e` (mocked) diffed vs `frontend/e2e/.baseline-35.txt` — no new failures. Live-only checks → live-deferred.
</verification>

<success_criteria>
- Template gallery + 3 sibling files fully tokenized, retired-palette grep=0, no stray stock palette.
- Real search/filter/custom-upload/select preserved (D-15); render test green.
</success_criteria>

<output>
Create `.planning/phases/35-shell-chrome-reskin-pages-b1/35-03-SUMMARY.md` when done.
</output>
