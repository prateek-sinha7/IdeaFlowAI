---
phase: 35-shell-chrome-reskin-pages-b1
plan: 07
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - frontend/src/app/admin/page.tsx
  - frontend/src/app/admin/admin.reskin.test.tsx
autonomous: true
requirements: [B1-07]
user_setup: []

must_haves:
  truths:
    - "The Admin page (header bar, stat cards, users table, tier-dropdown, create-user + delete-confirm modals, toast) is fully reskinned onto Phase-32 tokens/primitives (Card/Button/Badge/Pill) with retired-palette grep=0 and no per-page palette fork (D-15)."
    - "The product's RICHER wiring is preserved (ND-12): the is_admin access gate, adminListUsers/UpdateTier/CreateUser/DeleteUser, optimistic tier update + revert, wired search, self-delete guard, and the Runs/Role/Joined columns + password-create modal (email+password+plan+grant-admin) — a face-value mock rebuild would regress these."
    - "DEFERRED (additive backend, NOT this phase): the mock's Status + Last-active columns and the email-invite flow — the AdminUser type has no display_name/status/last_active_at, so no columns are added for fields the API does not return (ND-12, INV-3)."
  artifacts:
    - path: "frontend/src/app/admin/page.tsx"
      provides: "Token-reskinned admin console with preserved richer wiring + is_admin gate"
      contains: "is_admin"
    - path: "frontend/src/app/admin/admin.reskin.test.tsx"
      provides: "Render test: Runs/Role/Joined columns + pw-create modal + TierDropdown aria + gate"
  key_links:
    - from: "frontend/src/app/admin/page.tsx"
      to: "getMe().is_admin gate + adminUpdateTier"
      via: "access guard + TierDropdown"
      pattern: "is_admin|adminUpdateTier"
---

<objective>
Reskin `app/admin/page.tsx` (504 lines, 11 retired-palette lines) onto the Phase-32 token layer + `ui/*` primitives, preserving every wired behavior and the admin access gate. Keep the product's richer columns (Runs/Role/Joined) and password-create modal; defer the mock's Status/Last-active columns and email-invite (no backing fields).

Purpose: Deliverable B1-07. Complete the token migration for the admin console without regressing user-management wiring or the is_admin gate, and without building unbacked columns.

Output: Reskinned `admin/page.tsx` + a render test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-RESEARCH.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/styles/globals.css
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/results/AuditTab.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Card.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Badge.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Button.tsx
</context>

## Artifacts this phase produces (plan 35-07)

- `frontend/src/app/admin/admin.reskin.test.tsx` (NEW) — with `@/lib/api` mocked, asserts: the users table renders the Runs / Role / Joined columns; the create-user modal contains a password field (`type="password"`); the `TierDropdown` exposes `aria-expanded`/`role=menu`; and the is_admin access gate remains (non-admin → redirect). Also asserts NO Status / Last-active column header renders (defer guard). No new runtime symbols.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write admin reskin render test (columns + pw-create + gate + defer guard)</name>
  <files>frontend/src/app/admin/admin.reskin.test.tsx</files>
  <read_first>
    - frontend/src/app/admin/page.tsx (admin gate getMe().is_admin L123-126, admin api fns L11, TierDropdown L33-95, TierBadge L25, search L101, create-user modal w/ password, delete-confirm)
    - frontend/src/lib/api.ts (AdminUser type ~L496-503: email/tier/is_admin/workflow_run_count/created_at — NO display_name/status/last_active_at)
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (api-mock + render conventions)
  </read_first>
  <behavior>
    - Table shows Runs (workflow_run_count), Role (is_admin), and Joined (created_at) columns.
    - Create-user modal includes a password input (`type="password"`) plus email + plan + grant-admin.
    - TierDropdown trigger exposes `aria-expanded`; the open dropdown carries `role=menu` and closes on Escape.
    - Non-admin user (getMe().is_admin false) is redirected (gate preserved).
    - No "Status" or "Last active" column header renders (defer guard).
  </behavior>
  <action>Author a vitest + @testing-library render test that mocks `@/lib/api` (adminListUsers returns a fixture with workflow_run_count/created_at/is_admin; getMe returns is_admin variants) and encodes the behavior contract above. Mirror `ReviewGatesSection.test.tsx` conventions; stub `next/navigation`. The TierDropdown `role=menu`/Escape and the defer-guard assertions describe the post-reskin state (some RED now); the column + pw-field + gate assertions guard against regression. Do NOT edit `admin/page.tsx` in this task.</action>
  <acceptance_criteria>
    - `cd frontend && npm run test -- admin.reskin` RUNS; assertions encode columns + pw-create + gate + TierDropdown-aria + defer-guard.
    - `cd frontend && npx tsc --noEmit` no NEW type errors from the test file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- admin.reskin 2>&1 | tail -5</automated>
  </verify>
  <done>Render test exists encoding the richer-wiring preservation + gate + defer contract for the admin reskin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reskin admin console to tokens/primitives, preserve wiring + gate</name>
  <files>frontend/src/app/admin/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/page.tsx (the file being reskinned; TierBadge TIER_STYLES L19-23, TierDropdown L33-95, delete copy L466, header bar, stat cards, table)
    - frontend/src/components/results/AuditTab.tsx (card + status-chip idioms)
    - frontend/src/components/ui/Card.tsx, ui/Badge.tsx, ui/Button.tsx, ui/Pill.tsx
    - frontend/src/styles/globals.css (surface-near-black header, scrim, elevation-menu/modal, radius ladder)
  </read_first>
  <action>Reskin `admin/page.tsx` to tokens: header bar `bg-white` → `bg-surface-near-black` + Manrope (`font-sans`) wordmark + an "Admin" `Badge`/`Pill` chip; the 5 stat cards → `Card`; users table → token surfaces/borders (`bg-surface-card`, `border-line-divider`, `text-ink-*`); `TierBadge` (`TIER_STYLES` retired hex L19-23) → the `Badge` primitive (tier/role); `TierDropdown` menu → `shadow-[var(--elevation-menu)]` + `rounded-[var(--radius-menu)]`; delete-confirm modal scrim → `bg-[var(--scrim)]` + `Card`/token panel; toast → tokens; all `#1B2A4A`/`gray-*`/`#E8EDF5`/`#F4F5F7`/raw hex/`text-gray-*` → token utilities; buttons → `Button`. Add a11y to `TierDropdown`: `aria-expanded`/`role=menu`/Escape-to-close (menu-button pattern). PRESERVE (do NOT regress — ND-12): the `getMe().is_admin` access gate + redirect (L123-126), `adminListUsers`/`adminUpdateTier`/`adminCreateUser`/`adminDeleteUser`, optimistic tier update + revert-on-error, wired search, the Runs/Role/Joined columns, the create-user modal (email+password+plan+grant-admin — keep password `type="password"`), delete-confirm + self-delete guard, and the stronger delete copy "permanently delete the user and all their data" (L466 — do NOT weaken to the mock's "run history retained"). DEFER: do NOT add Status / Last-active columns or an email-invite flow (AdminUser has no display_name/status/last_active_at). May add trivial client-side tier filter chips (`Pill`) over the existing `tier` field. No fenced hex; NO new backend/endpoint/column (INV-3).</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/app/admin/page.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-|#E8EDF5|#F4F5F7' frontend/src/app/admin/page.tsx` returns nothing; file positively uses Card/Badge/Button + tokens.
    - Wiring + gate intact: file still contains `is_admin`, `adminUpdateTier`, `adminCreateUser`, `workflow_run_count`, `created_at`, and a password input `type="password"`.
    - Defer guard: no new "Status"/"Last active"/"last_active_at"/"display_name"/invite column or field added (grep -i shows none introduced).
    - Behavior GREEN: `cd frontend && npm run test -- admin.reskin` passes; `npx tsc --noEmit` no NEW errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- admin.reskin && grep -q 'is_admin' src/app/admin/page.tsx && grep -q 'workflow_run_count' src/app/admin/page.tsx && grep -q 'type="password"' src/app/admin/page.tsx && ! grep -rEn '#1B2A4A|#f5f5f0|bg-blue-|text-gray-|bg-gray-|#E8EDF5|#F4F5F7' src/app/admin/page.tsx && echo PASS</automated>
  </verify>
  <done>Admin console tokenized via Card/Badge/Button, is_admin gate + all user-mgmt wiring + Runs/Role/Joined + pw-create preserved, TierDropdown a11y added, Status/Last-active/invite deferred, render test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client ↔ /api/admin/* (existing) | Reskin re-styles the console; the is_admin gate + admin CRUD flow through unchanged api.ts functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07-01 | Elevation of Privilege | admin access gate | mitigate | Keep the `getMe().is_admin` redirect during the chrome reskin; render test asserts non-admin is redirected. |
| T-35-07-02 | Tampering | user-mgmt wiring | mitigate | Preserve adminUpdateTier optimistic-revert, pw-create (type=password), self-delete guard, stronger delete copy; render test asserts columns + pw field. |
| T-35-07-03 | Information Disclosure | unbacked columns | accept | Do NOT add Status/Last-active/display_name columns (no API fields) — defer guard prevents surfacing fields the server does not return. |
| T-35-07-SC | Tampering | npm installs | accept | Zero package installs (Legitimacy Audit N/A). |

All threats LOW / satisfied-by-construction: reskin-only, admin gate + CRUD wiring unchanged, no new endpoint/column (ND-12/INV-3).
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + retired-palette grep=0 + token-authority self-check + wiring/gate greps on admin/page.tsx.
- Wave 2 merge: `npm run test` green by delta + `npm run e2e` (mocked) diffed vs `frontend/e2e/.baseline-35.txt` — no new failures. Live admin API checks → live-deferred.
</verification>

<success_criteria>
- Admin console fully tokenized (Card/Badge/Button), retired-palette grep=0, no stray stock palette.
- is_admin gate + all user-mgmt wiring + Runs/Role/Joined + pw-create preserved; Status/Last-active/invite deferred; TierDropdown a11y; render test green.
</success_criteria>

<output>
Create `.planning/phases/35-shell-chrome-reskin-pages-b1/35-07-SUMMARY.md` when done.
</output>
