---
phase: 35-shell-chrome-reskin-pages-b1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppHeader.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/ui/NotificationPanel.tsx
  - frontend/src/components/layout/AppHeader.a11y.test.tsx
  - frontend/src/components/ui/NotificationPanel.a11y.test.tsx
autonomous: true
requirements: [B1-01]
user_setup: []

must_haves:
  truths:
    - "Nav reads Home · Library · My Workflows; the third item shows the visible label 'My Workflows' and still routes to page key `saved-workflows` (D-11 — 'Catalogue' is gone as a label, no component/page-key rename this phase)."
    - "The top bar is the dark near-black shell (`bg-surface-near-black` = #111114) on Phase-32 tokens with NO per-page palette fork (D-15)."
    - "Nav active state = purple-underline (brand), not white-fill pill; nav/profile-menu are keyed on generic routes/page keys, never a workflow name (SC-001/INV-1)."
    - "Profile menu and notifications bell are keyboard-operable with `aria-haspopup`/`aria-expanded`/`role=menu`/Escape-to-close-and-refocus (a11y invariant), asserted by tests."
    - "The running-pipeline badge, profile identity block (email + Plan tier + basic-tier Upgrade), notification progress bar and status-CTA wiring are all preserved (D-15 keep-richer-behavior); no transport/backend touched (INV-3/LOCK-B)."
  artifacts:
    - path: "frontend/src/components/layout/AppHeader.tsx"
      provides: "Token-reskinned dark shell + nav pill (My Workflows) + a11y profile menu"
      contains: "bg-surface-near-black"
    - path: "frontend/src/components/ui/NotificationPanel.tsx"
      provides: "Token-reskinned notifications panel chrome + a11y bell"
      contains: "bg-surface"
    - path: "frontend/src/components/layout/AppHeader.a11y.test.tsx"
      provides: "a11y assertions for nav aria-current + profile menu aria/role/Escape"
    - path: "frontend/src/components/ui/NotificationPanel.a11y.test.tsx"
      provides: "a11y assertions for bell aria-haspopup/aria-expanded/Escape"
  key_links:
    - from: "frontend/src/components/layout/AppHeader.tsx"
      to: "onNavigate('saved-workflows')"
      via: "My Workflows nav button click"
      pattern: "onNavigate\\(\"saved-workflows\"\\)"
    - from: "frontend/src/components/layout/AppHeader.tsx"
      to: "frontend/src/components/ui/NotificationPanel.tsx"
      via: "import + render with existing props"
      pattern: "NotificationPanel"
---

<objective>
Reskin the app SHELL to the Workspace-v2 idiom on the already-landed Phase-32 token/primitive layer, and close the a11y gaps on the interactive chrome. Three surfaces: `AppHeader.tsx` (dark bar + centered nav pill + profile menu), `NotificationPanel.tsx` (panel chrome only — live feed is Phase 38), and a light touch on `DashboardLayout.tsx` (body surface only).

This is the FOUNDATION plan (Wave 1): it also captures the pre-reskin mocked-Playwright baseline so every later plan verifies BY DELTA (DEF-29-06-1), and it writes the two a11y test scaffolds first (RED) so the reskin makes them pass (GREEN).

Purpose: Deliverable B1-01. The shell converges visually with the run subtree, keying nav on generic routes (SC-001), relabeling Catalogue→My Workflows (D-11), and adding keyboard/ARIA to the menu + bell — while preserving every wired behavior (D-15).

Output: Reskinned `AppHeader.tsx`, `NotificationPanel.tsx`, light-touched `DashboardLayout.tsx`, two new a11y test files, and a captured baseline artifact.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-CONTEXT.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-RESEARCH.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/styles/globals.css
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/results/AuditTab.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Tabs.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Badge.tsx
</context>

## Artifacts this phase produces (plan 35-01)

- `frontend/src/components/layout/AppHeader.a11y.test.tsx` (NEW) — nav `aria-current="page"` on active item + profile-menu `aria-haspopup`/`aria-expanded`/`role=menu`/`role=menuitem`/Escape-refocus.
- `frontend/src/components/ui/NotificationPanel.a11y.test.tsx` (NEW) — bell `aria-haspopup`/`aria-expanded` + dropdown `role`/Escape-close.
- Captured pre-reskin mocked-Playwright baseline (saved pass/fail set to diff against — e.g. `frontend/e2e/.baseline-35.txt`, git-ignored or committed per repo norm).
- NO new runtime components, NO new hook, NO new backend symbol. a11y added via inline `onKeyDown`/aria props on existing triggers (do NOT add a menu library — RESEARCH Don't-Hand-Roll).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture baseline + write failing a11y tests (RED)</name>
  <files>frontend/src/components/layout/AppHeader.a11y.test.tsx, frontend/src/components/ui/NotificationPanel.a11y.test.tsx, frontend/e2e/.baseline-35.txt</files>
  <read_first>
    - frontend/src/components/layout/AppHeader.tsx (current triggers: profile button L161-172, nav L89-123)
    - frontend/src/components/ui/NotificationPanel.tsx (bell L69-80, dropdown L83-91)
    - frontend/src/components/ui/Tabs.tsx (reference a11y idiom: role=tablist/tab, aria-selected, data-testid)
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (existing vitest render-test conventions to mirror)
  </read_first>
  <behavior>
    - AppHeader a11y: active nav item exposes `aria-current="page"`; profile trigger exposes `aria-haspopup="menu"` + `aria-expanded` toggling false→true on open; open dropdown has `role="menu"` with `role="menuitem"` rows (Account Settings / Analytics / Workflow History / Log out); pressing Escape closes the menu and returns focus to the trigger.
    - NotificationPanel a11y: bell exposes `aria-haspopup="menu"` (or `dialog`) + `aria-expanded` toggling on open; open dropdown carries a `role`; Escape closes it. Preserve existing `aria-label="Notifications"`.
    - Nav-label assertion: header renders visible text "My Workflows" and NOT "Catalogue"; clicking it fires `onNavigate("saved-workflows")`.
  </behavior>
  <action>Capture the pre-reskin baseline FIRST: run the mocked e2e suite and save the pass/fail set to `frontend/e2e/.baseline-35.txt` so later waves diff by delta (DEF-29-06-1). Then author two vitest+@testing-library render tests that encode the a11y + nav-label contract in `behavior` above. These tests describe NOT-YET-IMPLEMENTED behavior and MUST fail now (RED) — that is expected. Mirror the render-test conventions in `ReviewGatesSection.test.tsx` (vitest `describe`/`it`, `render`, `screen`, `fireEvent`/`userEvent`). Query the profile trigger and bell by accessible role/name; assert aria attributes + Escape handling. Do NOT edit AppHeader/NotificationPanel in this task.</action>
  <acceptance_criteria>
    - `frontend/e2e/.baseline-35.txt` exists and lists the pre-reskin mocked pass/fail set (evidence the baseline was captured before any reskin edit).
    - `cd frontend && npm run test -- AppHeader.a11y NotificationPanel.a11y` RUNS and the new tests FAIL (RED) with assertion errors about missing aria/role/Escape — not import/compile errors.
    - `cd frontend && npx tsc --noEmit` shows no NEW type errors introduced by the test files.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- AppHeader.a11y NotificationPanel.a11y 2>&1 | grep -Eq 'fail|✕|expected' && echo RED-as-expected</automated>
  </verify>
  <done>Baseline captured; both a11y test files exist and fail on assertions (RED), ready for the reskin tasks to turn GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reskin AppHeader dark shell + nav (My Workflows) + profile-menu a11y; light-touch DashboardLayout</name>
  <files>frontend/src/components/layout/AppHeader.tsx, frontend/src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    - frontend/src/components/layout/AppHeader.tsx (the file being reskinned)
    - frontend/src/components/results/AuditTab.tsx (canonical token idioms: header row, ink ramp, brand icon)
    - frontend/src/styles/globals.css (@theme token utilities + :root radius/elevation vars)
    - frontend/src/components/ui/Tabs.tsx (active = `text-ink-900 border-brand` underline idiom for nav)
    - frontend/src/components/ui/Button.tsx (variant primary/secondary for action buttons)
    - frontend/src/components/ui/Badge.tsx + ui/Pill.tsx (running badge, plan chip)
  </read_first>
  <action>Reskin `AppHeader.tsx` to tokens: dark bar `bg-[#111827] border-[#1f2937]` (L77) → `bg-surface-near-black` + a token divider (e.g. `border-white/10`); logo tile `bg-blue-600` → `bg-brand`; wordmark `font-serif italic` → `font-sans` (Manrope); nav container drop the `bg-[#1f2937]` pill fill; each nav `<button>` active state changes from white-fill (`bg-white text-gray-900`) to PURPLE-UNDERLINE per the Tabs idiom (`text-white`/`text-brand-on-dark` active + a 2px `border-brand` underline; inactive `text-ink-300`/`text-white/60`), keeping icons. Relabel the third nav item's VISIBLE TEXT from "Catalogue" (L121) to "My Workflows" per D-11 — the `onNavigate("saved-workflows")` handler and page key stay UNCHANGED (the `WorkflowCatalog`→`HomeLaunchGrid` rename is Phase 36). Convert the running-pipeline badge `bg-[#1B2A4A]` (L137) and profile dropdown `border-gray-200 bg-white` / all `text-gray-*` / `text-[#1B2A4A]` Upgrade link / `hover:bg-gray-50` / `text-red-600` logout to tokens (`bg-surface-card`, `border-line-border`, `text-ink-700/500/900`, `text-brand`, `hover:bg-surface-warm`, `text-status-failed`) and `shadow-md`→`shadow-[var(--elevation-menu)]`, dropdown radius→`rounded-[var(--radius-menu)]`. Avatar = rounded-square (`rounded-[var(--radius-button)]`, not `rounded-full`) per resolved cosmetics. Add a11y to the profile menu: `aria-haspopup="menu"` + `aria-expanded={profileOpen}` on the trigger; `role="menu"` on the dropdown container; `role="menuitem"` on each item button; an `onKeyDown` handler that closes on Escape and refocuses the trigger (use a `useRef` on the trigger). Add `aria-current="page"` to the active nav button. PRESERVE all wiring: `onNavigate` union, `currentPage` active detection, running-badge props, identity block (email + `TIER_LABELS[userTier]` + basic-tier Upgrade), outside-click + AnimatePresence, the `<NotificationPanel .../>` mount with all its props. Then LIGHT-TOUCH `DashboardLayout.tsx`: only ensure the page-shell body background uses `bg-surface-paper` where an ad-hoc literal exists; do NOT restructure routing or page keys (SC-001). Use NO fenced hex; every color routes through a token. No new backend, no transport touch (INV-3/LOCK-B).</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/layout/AppHeader.tsx frontend/src/components/layout/DashboardLayout.tsx` returns nothing.
    - Token authority (floor+ceiling): `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-|#111827|#1f2937' frontend/src/components/layout/AppHeader.tsx` returns nothing (dark-bar hex #111827/#1f2937 and all stock gray/blue eliminated); file positively contains `bg-surface-near-black` and `border-brand`.
    - Nav label: `grep -c 'Catalogue' frontend/src/components/layout/AppHeader.tsx` = 0 AND the file contains the string `My Workflows` AND still contains `onNavigate("saved-workflows")`.
    - a11y GREEN: `cd frontend && npm run test -- AppHeader.a11y` passes.
    - Behavior preserved by delta: `cd frontend && npm run test -- DashboardLayout.catalogHome` still passes; `npx tsc --noEmit` shows no NEW errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- AppHeader.a11y DashboardLayout.catalogHome && grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b|#111827|#1f2937|bg-blue-|text-gray-' src/components/layout/AppHeader.tsx | grep -q . && echo FAIL-stock-remains || echo PASS</automated>
  </verify>
  <done>AppHeader is dark-shell tokenized, nav reads "My Workflows" routing to saved-workflows with purple-underline active + aria-current, profile menu is keyboard/ARIA-complete, DashboardLayout body uses paper surface, all wiring intact, a11y + catalogHome tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reskin NotificationPanel chrome + bell a11y (chrome only; feed = Phase 38)</name>
  <files>frontend/src/components/ui/NotificationPanel.tsx</files>
  <read_first>
    - frontend/src/components/ui/NotificationPanel.tsx (the file being reskinned)
    - frontend/src/components/results/AuditTab.tsx (empty-state disc idiom L353-361, list-row idiom L182)
    - frontend/src/components/ui/Badge.tsx (status label via normalizeStatus keys)
    - frontend/src/styles/globals.css (status ramp :root vars, elevation-notif, radius-menu)
  </read_first>
  <action>Reskin `NotificationPanel.tsx` to tokens — CHROME ONLY, no feed/data logic changes (live feed is Phase 38). Convert: bell `bg-[#1f2937]`/`hover:bg-[#1f2937]`/`text-gray-400` → dark-bar-compatible tokens (`text-ink-300 hover:text-white hover:bg-white/10`); unread badge `bg-[#1B2A4A] border-[#111827]` → `bg-brand` + token border; dropdown `border-gray-200 bg-white shadow-xl` → `bg-surface-white border-line-border shadow-[var(--elevation-notif)] rounded-[var(--radius-menu)]`; header `bg-gray-50 border-gray-100` + `text-gray-*` count chip → `bg-surface-warm border-line-divider` + a `<Pill>` or token chip; empty-state `bg-gray-100`/`text-gray-300` disc → AuditTab idiom `rounded-[var(--radius-menu)] bg-surface-warm` + `text-ink-400`; per-notification `StatusIcon` colors (`text-[#1B2A4A]`, `text-emerald-600`, `text-red-500`, `text-gray-400`) → status tokens (`text-status-running`, `text-status-done`, `text-status-failed`, `text-status-queued`); running progress bar `bg-[#1B2A4A]` + `bg-gray-100` track → `bg-status-running`/`bg-[var(--status-running-fill)]`; status label pill block (L178-187) → use the `Badge` primitive keyed on `n.status` (running→blue, completed→done, failed→red, cancelled→amber; Badge normalizes); CTA links `text-[#1B2A4A]` → `text-brand`; `bg-blue-50/30` unread row + `bg-blue-50` → `bg-brand-fill`/token. Add a11y to the bell: `aria-haspopup="menu"` + `aria-expanded={open}`; give the dropdown a `role` (`menu` or `dialog`); add `onKeyDown` Escape→close + refocus bell (add a ref). PRESERVE: `formatRelativeTime`, mark-all-read-on-open, progress-bar math, indeterminate pulse, status-driven CTA routing (`onGoToPipeline`/`onViewResults`), all props/handlers, existing `aria-label="Notifications"`. No fenced hex; no new data source; no backend (INV-3).</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/ui/NotificationPanel.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo|emerald|red)-|text-(gray|slate|emerald|red)-|#111827|#1f2937' frontend/src/components/ui/NotificationPanel.tsx` returns nothing; file positively uses `bg-surface`/`text-status-`/`text-ink-` tokens and imports/uses the `Badge` primitive.
    - a11y GREEN: `cd frontend && npm run test -- NotificationPanel.a11y` passes.
    - Behavior preserved: `npx tsc --noEmit` shows no NEW errors; props signature of `NotificationPanel` unchanged (still `notifications`/`unreadCount`/`onMarkAllRead`/`onClearAll`/`onGoToPipeline`/`onViewResults`).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- NotificationPanel.a11y && grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-emerald-|text-red-|#111827|#1f2937' src/components/ui/NotificationPanel.tsx | grep -q . && echo FAIL-stock-remains || echo PASS</automated>
  </verify>
  <done>NotificationPanel chrome is fully tokenized, status via Badge, bell keyboard/ARIA-complete, all feed wiring and props preserved, a11y test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client React chrome ↔ existing app data/handlers | Shell reskin re-styles presentation only; all data (notifications feed, tier, nav routing) flows through unchanged props/handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | Information Disclosure | NotificationPanel | accept | No new data surface — panel renders the SAME `notifications[]` props; live feed is Phase 38. Satisfied by construction (INV-3, no transport touch). |
| T-35-01-02 | Tampering | nav routing (SC-001) | mitigate | Nav stays keyed on generic page keys (`saved-workflows`), never a workflow name; label-only change. Asserted: `onNavigate("saved-workflows")` present + grep no workflow-name branch. |
| T-35-01-03 | Elevation of Privilege | shell auth surface | accept | Shell exposes no auth decision; profile menu items call existing `onNavigate`/`onLogout` handlers unchanged. No new endpoint. Satisfied by construction. |
| T-35-01-SC | Tampering | npm/pip installs | accept | ZERO package installs in this phase (RESEARCH: Package Legitimacy Audit N/A). No new dependency introduced. |

All threats LOW and satisfied-by-construction: FE-only reskin, no new backend/endpoint/auth surface, no transport touch (LOCK-B/INV-3).
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + the per-file retired-palette grep=0 + token-authority self-check on touched files.
- Wave 1 merge (before Wave 2 starts): `cd frontend && npm run test` (vitest full) green by delta + `cd frontend && npm run e2e` (mocked) diffed against `frontend/e2e/.baseline-35.txt` — NO newly-failing spec (esp. auth/catalog/saved-workflows). Any live-server-only check → mark live-deferred, do not block.
</verification>

<success_criteria>
- Dark near-black shell + purple-underline nav (Home · Library · My Workflows) + a11y profile menu + tokenized notifications panel, all on Phase-32 tokens with retired-palette grep=0 and no stray stock palette.
- Nav label "Catalogue" removed; route/page key `saved-workflows` unchanged (D-11); no component rename (SC-001).
- Profile menu + notifications bell keyboard/ARIA-complete (a11y tests green).
- All richer behavior preserved (running badge, identity block, notification progress/CTA); baseline captured for delta verification.
</success_criteria>

<output>
Create `.planning/phases/35-shell-chrome-reskin-pages-b1/35-01-SUMMARY.md` when done.
</output>
