---
phase: 35-shell-chrome-reskin-pages-b1
plan: 05
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - frontend/src/components/library/LibraryPage.tsx
  - frontend/src/components/library/LibraryPage.reskin.test.tsx
autonomous: true
requirements: [B1-04]
user_setup: []

must_haves:
  truths:
    - "The Library page (Agents / Skills / Hooks) is fully reskinned onto Phase-32 tokens/primitives (Tabs/Card/Pill/Button) with retired-palette grep=0 and no per-page palette fork (D-15)."
    - "Real controls preserved and, where the mock was static, wired against EXISTING local data only: the Agents/Skills/Hooks tab model, real catalog counts, category filtering, skill/agent detail modals with copy-to-clipboard — no inert search, no invented backend (D-15, NO-new-backend)."
    - "Library is keyed on generic catalog data (agents/skills/hooks), never a workflow name (SC-001/INV-1); no transport/backend touched (INV-3)."
  artifacts:
    - path: "frontend/src/components/library/LibraryPage.tsx"
      provides: "Token-reskinned Agents/Skills/Hooks catalog with real controls"
      contains: "font-sans"
    - path: "frontend/src/components/library/LibraryPage.reskin.test.tsx"
      provides: "Render test: Tabs roles + search filters + grep-clean"
  key_links:
    - from: "frontend/src/components/library/LibraryPage.tsx"
      to: "ui/Tabs"
      via: "Agents/Skills/Hooks tab bar"
      pattern: "role=\"tab\"|Tabs"
---

<objective>
Reskin `LibraryPage.tsx` (624 lines, 8 retired-palette lines) onto the Phase-32 token layer + `ui/*` primitives. Replace the ad-hoc tab bar with the `Tabs` primitive, agent/skill/hook cards with `Card`, chips with `Pill`. Where the mock had static/decorative controls but a real filter is cheap on the existing local data, wire it — without inventing any backend.

Purpose: Deliverable B1-04. Complete the token migration for the Library and make real controls where the mock had static text, using local catalog constants only.

Output: Reskinned `LibraryPage.tsx` + a render test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-RESEARCH.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/styles/globals.css
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/results/AuditTab.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Tabs.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Card.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Pill.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Button.tsx
</context>

## Artifacts this phase produces (plan 35-05)

- `frontend/src/components/library/LibraryPage.reskin.test.tsx` (NEW) — asserts: the Agents/Skills/Hooks tabs render with `role=tab` (Tabs primitive); switching tabs changes the rendered catalog; typing in the search filters the visible items (real, not inert); grep-clean of retired palette. No new runtime symbols.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write LibraryPage reskin render test (contract scaffold)</name>
  <files>frontend/src/components/library/LibraryPage.reskin.test.tsx</files>
  <read_first>
    - frontend/src/components/library/LibraryPage.tsx (tabs, LIBRARY_AGENTS/CUSTOM_AGENTS L15, SKILLS/SKILL_CATEGORIES L11, HOOKS/HOOK_EVENTS L12, SkillDetailModal L53 copy L56, AgentCapabilitiesModal L10, ICON_STYLES L39)
    - frontend/src/components/ui/Tabs.tsx (role=tab / data-testid=tab-*)
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (test conventions)
  </read_first>
  <behavior>
    - The Agents/Skills/Hooks tabs render with `role=tab`; clicking a tab switches the visible catalog section.
    - Typing into the search input filters the visible agents/skills (real filter over local data).
    - The catalog counts reflect the local constant lengths (real data).
  </behavior>
  <action>Author a vitest + @testing-library render test encoding the behavior contract above (Tabs roles, tab switching, real search filter, real counts). Mirror `ReviewGatesSection.test.tsx` conventions. The tab-role + search assertions describe the post-reskin state; if the current tab bar is ad-hoc (no `role=tab`), that assertion fails now (RED) and the reskin turns it GREEN. Do NOT edit `LibraryPage.tsx` in this task.</action>
  <acceptance_criteria>
    - `cd frontend && npm run test -- LibraryPage.reskin` RUNS; assertions define the Tabs-role + real-search contract.
    - `cd frontend && npx tsc --noEmit` no NEW type errors from the test file.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- LibraryPage.reskin 2>&1 | tail -5</automated>
  </verify>
  <done>Render test exists encoding Tabs-role + real-search contract for the Library reskin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reskin LibraryPage to Tabs/Card/Pill, wire real controls on local data</name>
  <files>frontend/src/components/library/LibraryPage.tsx</files>
  <read_first>
    - frontend/src/components/library/LibraryPage.tsx (the file being reskinned; ICON_STYLES retired hex L39; libTabBtn ad-hoc tabs)
    - frontend/src/components/results/AuditTab.tsx (card grid L182, chip idiom)
    - frontend/src/components/ui/Tabs.tsx, ui/Card.tsx, ui/Pill.tsx, ui/Button.tsx
    - frontend/src/styles/globals.css
  </read_first>
  <action>Reskin `LibraryPage.tsx` to tokens: the Agents/Skills/Hooks tab bar → the `Tabs` primitive; agent/skill/hook cards → `Card` + AuditTab row idiom; category/filter chips → `Pill` (active = `bg-brand-fill border-brand-border`); the `ICON_STYLES` tint array (L39, retired hex) → token-based tints (`text-brand`/`text-ink-*` on `bg-brand-fill`/`bg-surface-warm`); all `#1B2A4A`/`gray-*`/raw hex/`text-gray-*` → token utilities; headings → `font-sans`. PRESERVE: the tab model, real catalog data + counts (`LIBRARY_AGENTS`/`CUSTOM_AGENTS`/`SKILLS`/`HOOKS`), category filtering, `SkillDetailModal` (markdown + copy-to-clipboard) and `AgentCapabilitiesModal`. Where a control is currently decorative and a real filter is cheap on the EXISTING local data (e.g. wire the search box to filter agents/skills), wire it — but do NOT invent any backend or per-agent/per-hook fields with no data source (mock fiction: `dur`/`trigger` strings, `showAgentTint` editor toggle, agent drawer are out of scope). No fenced hex; NO new backend (INV-3).</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/library/LibraryPage.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-' frontend/src/components/library/LibraryPage.tsx` returns nothing; file positively uses Tabs/Card/Pill + tokens.
    - Behavior GREEN: `cd frontend && npm run test -- LibraryPage.reskin` passes (Tabs roles + real search filter); `npx tsc --noEmit` no NEW errors.
    - No fiction: no new backend fetch added (grep shows no new `fetch(`/api call introduced in this file).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- LibraryPage.reskin && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-gray-|bg-gray-' src/components/library/LibraryPage.tsx && echo PASS</automated>
  </verify>
  <done>LibraryPage tokenized via Tabs/Card/Pill, real controls preserved/wired on local data, no invented backend, render test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client Library UI ↔ local catalog constants | Agents/skills/hooks are local constants; reskin adds no server call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05-01 | Tampering | search/filter wiring | mitigate | Wire only over existing local data; render test asserts real filtering (no inert mock search, no invented backend). |
| T-35-05-02 | Tampering | Library keying (SC-001) | accept | Keyed on generic catalog data, never a workflow name. Satisfied by construction. |
| T-35-05-SC | Tampering | npm installs | accept | Zero package installs (Legitimacy Audit N/A). |

All threats LOW / satisfied-by-construction: reskin-only, no new backend/endpoint (INV-3).
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + retired-palette grep=0 + token-authority self-check on LibraryPage.tsx.
- Wave 2 merge: `npm run test` green by delta + `npm run e2e` (mocked, incl. `ts-f.skills-hooks`) diffed vs `frontend/e2e/.baseline-35.txt` — no new failures. Live-only checks → live-deferred.
</verification>

<success_criteria>
- Library fully tokenized (Tabs/Card/Pill), retired-palette grep=0, no stray stock palette.
- Real controls preserved/wired on local data; no invented backend; render test green.
</success_criteria>

<output>
Create `.planning/phases/35-shell-chrome-reskin-pages-b1/35-05-SUMMARY.md` when done.
</output>
