---
phase: 35-shell-chrome-reskin-pages-b1
plan: 04
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - frontend/src/components/workflow/prototype/DesignSystemPicker.tsx
  - frontend/src/components/workflow/prototype/DesignSystemDetailModal.tsx
  - frontend/src/components/workflow/prototype/CustomDesignSystemModal.tsx
  - frontend/src/components/workflow/ReviewGatesSection.tsx
  - frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx
autonomous: true
requirements: [B1-03]
user_setup: []

must_haves:
  truths:
    - "The DS picker (grid + detail modal + custom modal) and the Review-gates section are fully reskinned onto Phase-32 tokens/primitives (Pill/Card/Button/Badge) with retired-palette grep=0 and no per-page palette fork (D-15)."
    - "LOCK-F: the DS picker renders the REAL ~14 count from the actual `systems` prop — the mock's fabricated '150 systems' header MUST NOT appear; no catalog expansion."
    - "Review-gates preserves the load-bearing `onChange(gateAgentIds, touched)` contract byte-for-byte (gate_agent_ids sent only when touched; untouched omits → backend static default) and its `aria-expanded`/`sr-only` a11y — reskin classNames only (INV-3 byte-identical, D-15)."
  artifacts:
    - path: "frontend/src/components/workflow/prototype/DesignSystemPicker.tsx"
      provides: "Token-reskinned DS picker with real ~14 count"
      contains: "font-sans"
    - path: "frontend/src/components/workflow/ReviewGatesSection.tsx"
      provides: "Token-reskinned review-gates with preserved onChange contract"
      contains: "aria-expanded"
    - path: "frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx"
      provides: "Render test: real count from prop (not 150) + grep-clean + real select"
  key_links:
    - from: "frontend/src/components/workflow/ReviewGatesSection.tsx"
      to: "onChange(gateAgentIds, touched)"
      via: "checkbox toggle"
      pattern: "onChange\\("
    - from: "frontend/src/components/workflow/prototype/DesignSystemPicker.tsx"
      to: "onSelect / onSelectCustom"
      via: "system chip click"
      pattern: "onSelect"
---

<objective>
Reskin the design-system picker flow (`DesignSystemPicker.tsx` + `DesignSystemDetailModal.tsx` + `CustomDesignSystemModal.tsx`) and the `ReviewGatesSection.tsx` popover onto the Phase-32 token layer + `ui/*` primitives. Enforce LOCK-F (real ~14 count, no expansion) and preserve the Review-gates `onChange` contract exactly.

Purpose: Deliverable B1-03 (DS + review-gates side). Complete the token migration for the DS-selection and review-gates surfaces without regressing search, grouping, custom DS CRUD, selection, or the gate-selection contract.

Output: Reskinned DS picker + 2 sibling modals + review-gates section + a render test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-RESEARCH.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/styles/globals.css
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/results/AuditTab.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Pill.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Card.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Badge.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/ui/Button.tsx
</context>

## Artifacts this phase produces (plan 35-04)

- `frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx` (NEW) — mounts the picker with a fixture `systems` prop of N entries and asserts the rendered count reflects N (the real prop length, ~14), NOT a hardcoded "150"; a system chip click calls `onSelect`; grep-clean of retired palette. No new runtime symbols.
- ReviewGatesSection reuses the EXISTING `ReviewGatesSection.test.tsx` for behavior preservation (contract unchanged).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reskin DesignSystemPicker (LOCK-F real ~14 count), add render test</name>
  <files>frontend/src/components/workflow/prototype/DesignSystemPicker.tsx, frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/prototype/DesignSystemPicker.tsx (allCategories L48 from real systems, search L27, grouped chips L54-76, custom DS L35/L87, DesignSystemDetailModal L29, onSelect/onSelectCustom)
    - frontend/src/components/results/AuditTab.tsx (filter-chip cloud idiom L406-428)
    - frontend/src/components/ui/Pill.tsx, ui/Card.tsx, ui/Button.tsx
    - frontend/src/styles/globals.css
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (test conventions)
  </read_first>
  <behavior>
    - Any count/header the picker shows is derived from the real `systems` prop length (render with a fixture of ~14 → shows ~14), NEVER a hardcoded "150".
    - Real search filters the grouped chips; category grouping preserved.
    - Clicking a system chip calls `onSelect` (and custom DS calls `onSelectCustom`).
  </behavior>
  <action>Reskin `DesignSystemPicker.tsx` to tokens: grouped system chips + swatch trio → the AuditTab filter-chip idiom + `Pill` (active = `bg-brand-fill border-brand-border`); category tabs → `Tabs`-style token underline; search input + create button → tokens + `Button`; all `#1B2A4A`/`gray-*`/raw hex/`text-gray-*` → token utilities; headings → `font-sans`. LOCK-F: ensure any count/header is computed from `systems.length` (real ~14) — do NOT introduce or keep a fabricated "150 systems" string (mock overstatement). PRESERVE: real `search`, `allCategories` grouping, custom DS create/edit/delete + localStorage, `DesignSystemDetailModal` mount, `onSelect`/`onSelectCustom` wiring. Then author `DesignSystemPicker.reskin.test.tsx` asserting the behavior contract (count from prop, real select, grep-clean). No fenced hex; no backend (INV-3).</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/workflow/prototype/DesignSystemPicker.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-' frontend/src/components/workflow/prototype/DesignSystemPicker.tsx` returns nothing; file positively uses tokens + a `ui/` primitive.
    - LOCK-F: `grep -c '150' frontend/src/components/workflow/prototype/DesignSystemPicker.tsx` = 0 (no fabricated count); the render test proves the count follows the prop length.
    - Wiring intact: file still contains `onSelect` and the search filter path.
    - Behavior GREEN: `cd frontend && npm run test -- DesignSystemPicker.reskin` passes; `npx tsc --noEmit` no NEW errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- DesignSystemPicker.reskin && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-gray-|bg-gray-' src/components/workflow/prototype/DesignSystemPicker.tsx && echo PASS</automated>
  </verify>
  <done>DS picker tokenized with real ~14 count (LOCK-F), search/grouping/custom/select preserved, render test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reskin DS sibling modals (detail + custom)</name>
  <files>frontend/src/components/workflow/prototype/DesignSystemDetailModal.tsx, frontend/src/components/workflow/prototype/CustomDesignSystemModal.tsx</files>
  <read_first>
    - frontend/src/components/workflow/prototype/DesignSystemDetailModal.tsx (1 retired line)
    - frontend/src/components/workflow/prototype/CustomDesignSystemModal.tsx (5 retired lines)
    - frontend/src/components/results/AuditTab.tsx
    - frontend/src/styles/globals.css (scrim, elevation-modal, radius-card)
    - frontend/src/components/ui/Card.tsx, ui/Button.tsx, ui/Pill.tsx
  </read_first>
  <action>Reskin the two DS sibling modals to tokens: scrims → `bg-[var(--scrim)]`; panels → `Card`/`bg-surface-white border-line-border rounded-[var(--radius-card)] shadow-[var(--elevation-modal)]`; all `#1B2A4A`/`gray-*`/raw hex/`text-gray-*` → token utilities; buttons → `Button`; swatch/meta chips → `Pill`; headings → `font-sans`. PRESERVE all props, custom-DS form logic (create/edit), swatch rendering, and `onSelect`/`onClose` wiring. No fenced hex; no backend.</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/workflow/prototype/DesignSystemDetailModal.tsx frontend/src/components/workflow/prototype/CustomDesignSystemModal.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-' <both files>` returns nothing; files positively use tokens/primitives.
    - Behavior preserved: `npx tsc --noEmit` no NEW errors; prop signatures unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-gray-|bg-gray-' src/components/workflow/prototype/DesignSystemDetailModal.tsx src/components/workflow/prototype/CustomDesignSystemModal.tsx && echo PASS</automated>
  </verify>
  <done>DS detail/custom modals tokenized, all form + select wiring preserved, tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reskin ReviewGatesSection, preserve onChange contract + a11y</name>
  <files>frontend/src/components/workflow/ReviewGatesSection.tsx</files>
  <read_first>
    - frontend/src/components/workflow/ReviewGatesSection.tsx (docstring contract L11-24, disclosure L102-121, checkbox list L130-167, default logic L40, aria-expanded L109, sr-only checkbox L150, re-seed on agentsKey L63-74)
    - frontend/src/components/workflow/ReviewGatesSection.test.tsx (EXISTING behavior test — must stay green)
    - frontend/src/components/results/AuditTab.tsx (header row idiom L370)
    - frontend/src/components/ui/Badge.tsx (default chip)
    - frontend/src/styles/globals.css
  </read_first>
  <action>Reskin `ReviewGatesSection.tsx` CLASSNAMES ONLY: header row → AuditTab header idiom; `#1B2A4A`/`#F1F4FB` hover/`gray-*` → token utilities (`text-ink-*`, `bg-surface-*`, `border-line-*`, checkbox active → `bg-brand`/`border-brand`); the "default" chip → `Badge` or `Pill`; heading → `font-sans`. DO NOT TOUCH the logic: preserve the `onChange(gateAgentIds, touched)` contract exactly (gate_agent_ids sent ONLY when touched; `[]` means no gates; untouched omits → backend static default, byte-identical Q3/INV-3), the `gate === "Human_Gate"` default-gating, `initialGateIds` seeding, re-seed on `agentsKey` change, `aria-expanded` disclosure, and the `sr-only` checkbox a11y. No fenced hex; no backend.</action>
  <acceptance_criteria>
    - Retired-palette grep = 0: `grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' frontend/src/components/workflow/ReviewGatesSection.tsx` returns nothing.
    - Token authority: `grep -rEn 'bg-\[#|text-\[#|#[0-9A-Fa-f]{6}|bg-(blue|gray|slate|indigo)-|text-(gray|slate)-|#F1F4FB' frontend/src/components/workflow/ReviewGatesSection.tsx` returns nothing; file positively uses tokens.
    - Contract preserved: file still contains `onChange(` and `aria-expanded`; EXISTING `cd frontend && npm run test -- ReviewGatesSection` stays green (by delta).
    - `npx tsc --noEmit` no NEW errors.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test -- ReviewGatesSection && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|bg-blue-|text-gray-|bg-gray-|#F1F4FB' src/components/workflow/ReviewGatesSection.tsx && echo PASS</automated>
  </verify>
  <done>ReviewGatesSection tokenized (classNames only), onChange contract + a11y byte-preserved, existing test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client picker/gate state ↔ existing run-config payload | Reskin re-styles chips/checkboxes; the gate_agent_ids payload contract is unchanged (proven byte-identical by the golden guards) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04-01 | Tampering | ReviewGates onChange contract | mitigate | Reskin classNames only; preserve `onChange(gateAgentIds, touched)` + touched-gating; EXISTING ReviewGatesSection.test.tsx stays green (byte-identical default, INV-3/Q3). |
| T-35-04-02 | Information Disclosure | DS count (LOCK-F) | accept | Render real `systems.length` (~14), never a fabricated 150; no catalog expansion. Satisfied by construction. |
| T-35-04-03 | Tampering | picker keying (SC-001) | accept | Picker keyed on generic system data, never a workflow name. Satisfied by construction. |
| T-35-04-SC | Tampering | npm installs | accept | Zero package installs (Legitimacy Audit N/A). |

All threats LOW / satisfied-by-construction: reskin-only, no new backend/endpoint, gate payload byte-identical (INV-3).
</threat_model>

<verification>
- Per task: `npx tsc --noEmit` + retired-palette grep=0 + token-authority self-check on touched files.
- Wave 2 merge: `npm run test` green by delta (incl. existing ReviewGatesSection.test.tsx and `ts-g.review-gates`/`ts-n.review-gate` e2e) + `npm run e2e` (mocked) diffed vs `frontend/e2e/.baseline-35.txt` — no new failures. Live-only checks → live-deferred.
</verification>

<success_criteria>
- DS picker + 2 modals + review-gates fully tokenized, retired-palette grep=0, no stray stock palette.
- LOCK-F real ~14 count enforced; DS search/grouping/custom/select preserved.
- Review-gates onChange contract + a11y byte-preserved (existing test green).
</success_criteria>

<output>
Create `.planning/phases/35-shell-chrome-reskin-pages-b1/35-04-SUMMARY.md` when done.
</output>
