---
phase: 31-chat-lane-mvp-a3
plan: 07
type: execute
wave: 4
depends_on: ["31-04"]
files_modified:
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/e2e/tests/ts-chat.spec.ts
  - frontend/e2e/tests/ts-chat-cards.spec.ts
autonomous: true
requirements: [CHATUI-01, CHATUI-02, CHATUI-03]
must_haves:
  truths:
    - "The RunChatLane is mounted in the execution-surface left column, replacing the AgentProgressPanel controls it absorbed"
    - "The lane is fed the family-anchored transcript from useRunChat wired to whichever transport is active (SSE flag ON = RunConnectionProvider.subscribe; flag OFF = legacy WS dispatch) — flag-OFF path unchanged"
    - "A result-card deep-link opens the correct PreviewPanel tab via the nonce'd seam"
    - "New chat e2e specs pass under --project=mocked and introduce NO NEW breakage vs the pre-existing feat/ui-2 baseline (delta-verify)"
  artifacts:
    - path: "frontend/e2e/tests/ts-chat.spec.ts"
      provides: "mocked chat-lane e2e (send/receive, streaming, aria-live, gate/clarify quick-actions)"
    - path: "frontend/e2e/tests/ts-chat-cards.spec.ts"
      provides: "mocked result-card + deep-link + attachment e2e"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "frontend/src/components/chat/RunChatLane.tsx"
      via: "mount in the execution left column"
      pattern: "RunChatLane"
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "useRunChat + RunConnectionProvider.subscribe / legacy WS dispatch"
      via: "transport-agnostic transcript feed + deep-link seam wiring"
      pattern: "useRunChat|subscribe"
    - from: "frontend/src/components/preview/PreviewPanel.tsx"
      to: "useTabDeepLink consume -> setActiveTab"
      via: "nonce'd deep-link target (all tabs, not just preview/files)"
      pattern: "initialTab|deepLink"
---

<objective>
Integrate the chat lane into the live run screen: mount `RunChatLane` in the execution-surface LEFT column (absorbing the `AgentProgressPanel` controls), wire `useRunChat` to whichever transport is active (Phase-29 SSE when `NEXT_PUBLIC_SSE_TRANSPORT` is ON, legacy WS unchanged when OFF), make `PreviewPanel` the nonce'd deep-link target for all run tabs, and prove it with new mocked e2e specs verified BY DELTA against the pre-existing baseline — plus the codebase's first `data-testid`-driven chat specs.

Purpose: This is the wiring plan. The lane (plan 04) and its parts are built; this mounts them without breaking the flag-OFF legacy path (LOCK-B additive), preserves the FIX-039 accumulator-reset ordering in `useWorkflow.ts` (CONTEXT: do not disturb), and threads the D-12 gate/clarify/revise/stop callbacks. e2e discipline: NEW chat specs PASS + NO NEW breakage vs the feat/ui-2 baseline (DEF-29-06-1 pre-existing red) + tsc-identity — NOT absolute-green e2e (CONTEXT e2e-BASELINE caveat).
Output: edits to `DashboardLayout.tsx`, `page.tsx`, `PreviewPanel.tsx` + two mocked chat specs.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-chat-lane-mvp-a3/31-CONTEXT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md
# Mount point + right-panel single-slot (execution surface):
@frontend/src/components/layout/DashboardLayout.tsx
# Transport ownership + frame dispatch + threaded props (messages/streamingContent/onSendMessage):
@frontend/src/app/dashboard/page.tsx
@frontend/src/providers/RunConnectionProvider.tsx
# Deep-link target (has initialTab/onTabSelect/setActiveTab already):
@frontend/src/components/preview/PreviewPanel.tsx
# The mock drivers ALREADY carry chat frames (Phase 29-06): chatMessage/chatReply/streamAttached:
@frontend/e2e/fixtures/mockWs.ts
@frontend/e2e/fixtures/mockSse.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useRunChat transport-agnostically + deep-link target; preserve FIX-039</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/components/preview/PreviewPanel.tsx</files>
  <action>
    In `page.tsx`: instantiate the plan-03 `useRunChat({ runId: activePipelineRunId, subscribe, sendCommand, legacyWsSend })` and the `useTabDeepLink` seam. Feed frames transport-agnostically: when the SSE flag is ON, pass `RunConnectionProvider`'s `subscribe`/`sendCommand`; when OFF, route the existing WS `handleWebSocketMessage` chat frames (`chat_message`/`chat_reply`/`stream_attached`) into `useRunChat` and use the legacy `user_message` send — SAME transcript either way (LOCK-B additive; flag-OFF path otherwise UNCHANGED). Thread the resulting `messages`/`sendMessage` + the existing gate/clarify/revise/stop callbacks + the `useTabDeepLink.requestOpenTab` down toward the lane. DO NOT modify the `useWorkflow.ts` reducer — the transcript is a separate hook; the FIX-039 unconditional `agent_start` accumulator reset stays byte-for-byte (CONTEXT invariant; assert `useWorkflow.ts` is not in the diff).
    In `PreviewPanel.tsx`: make it the deep-link CONSUMER — accept the nonce'd `{tab, nonce}` (extend the existing `initialTab` handling, which today only honors `preview`/`files`, to accept ALL tabs and re-trigger `setActiveTab` on nonce change via `useTabDeepLink.consume` or an effect keyed on the nonce). A card deep-link to Steps/Preview/Files/Audit switches the tab exactly once per click (borrow #6).
  </action>
  <verify>
    <automated>cd frontend && git diff --name-only | grep -qx "src/hooks/useWorkflow.ts" && echo FIX039_VIOLATION || echo FIX039_PRESERVED; npx tsc --noEmit 2>&1 | grep -v "e2e/fixtures/mockApi.ts" | grep -c "error TS" | grep -qx 0 && echo TSC_IDENTITY_OK</automated>
  </verify>
  <done>useRunChat fed by both transports (flag-OFF unchanged); PreviewPanel consumes the nonce seam for all tabs; useWorkflow.ts NOT in diff (FIX-039 preserved); tsc identity.</done>
</task>

<task type="auto">
  <name>Task 2: Mount RunChatLane in the execution left column (absorb AgentProgressPanel controls)</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx</files>
  <action>
    In the execution 2-panel layout (`DashboardLayout.tsx`, the `mainView === "execution"` block, left column ~340-360px), mount `RunChatLane` in the LEFT column, passing the plan-07 Task-1 wiring (`messages`, `sendMessage`, `runState` derived from pipelineState, the gate/clarify callbacks — `onApprove`/`onReject`/`onRedo`/`onUpdateSpecs`/`onSubmitAnswers`, the revise callbacks already computed here, `onCancelPipeline` for Stop, the chain/suggestions callback, and `requestOpenTab`). The lane ABSORBS the `AgentProgressPanel` Stop/revise/suggestions controls (CONTEXT deliverable 1) — remove those control responsibilities from the left column's AgentProgressPanel mount (keep per-agent progress available via the right-panel Thinking tab / D-12 Building mini-card; do NOT delete AgentProgressPanel — Phase 32 relocates it into Steps). Keep `WaveTreePanel` where it is or below the lane. This is CURRENT skin (D-15) — no token/palette change. Add `data-testid="execution-chat-lane"` on the mounted lane container.
  </action>
  <verify>
    <automated>cd frontend && grep -q "RunChatLane" src/components/layout/DashboardLayout.tsx && npx tsc --noEmit 2>&1 | grep -v "e2e/fixtures/mockApi.ts" | grep -c "error TS" | grep -qx 0 && echo MOUNT_AND_TSC_OK</automated>
  </verify>
  <done>RunChatLane mounted in the execution left column with the absorbed controls wired; AgentProgressPanel retained (not deleted); tsc identity.</done>
</task>

<task type="auto">
  <name>Task 3: Mocked chat e2e specs — DELTA-verified (new pass, no new breakage)</name>
  <files>frontend/e2e/tests/ts-chat.spec.ts, frontend/e2e/tests/ts-chat-cards.spec.ts</files>
  <action>
    Write two mocked Playwright specs (default `--project=mocked`) driving the EXISTING chat driver already in `mockWs.ts`/`mockSse.ts` (Phase 29-06: `chatMessage`/`chatReply`/`streamAttached`/`waitForChatCommand`) — no driver rewrite (MOCKWS-CHAT-DRIVER-CONTRACT §4 additive). Select via the NEW `data-testid`s (the codebase's first — `run-chat-lane`, `chat-transcript`, `chat-composer`, `chat-message`, `chat-result-card`, `chat-gate-actions`, `chat-clarify-actions`, `chat-attachments`, `chat-token-widget`) so the specs survive the Phase-32 reskin.
    `ts-chat.spec.ts`: (a) send a turn → `waitForChatCommand` asserts the outbound command → `chatMessage` echo renders one bubble (optimistic reconcile, no dupe); (b) streamed assistant content renders markdown + the transcript region exposes `role="log"` + `aria-live`; (c) gate state → `chat-gate-actions` visible, Approve fires the command, terminal event hides the actions (KAN-100 fence); (d) clarify state → `chat-clarify-actions` chips submit answers.
    `ts-chat-cards.spec.ts`: (a) a `chatReply({cardKind:'deliverable', deepLink})` renders a `chat-result-card` labeled "Deliverable" (LOCK-F); clicking it switches the Preview tab (deep-link seam); (b) a `spec_revision` card renders "Revising spec — cycle N"; (c) an attachment drag-drop/paste renders a `chat-attach-chip`.
    Do NOT chase absolute-green: verify BY DELTA — the two new specs PASS and no PREVIOUSLY-GREEN spec regresses.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --project=mocked e2e/tests/ts-chat.spec.ts e2e/tests/ts-chat-cards.spec.ts</automated>
  </verify>
  <done>Both new chat specs pass under --project=mocked; assertions ride data-testids; no rewrite of the shared mock driver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| active transport frames -> transcript | frames arrive over SSE (flag ON) or legacy WS (flag OFF); the lane must not trust a stale/duplicated frame |
| lane callbacks -> run commands | mounted lane dispatches gate/clarify/revise/stop commands into the live run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07-T | Tampering | dual-transport frame feed | mitigate | one transcript, event_id-deduped (plan 03); flag-OFF path is byte-unchanged (LOCK-B additive) so no new attack surface on the legacy route |
| T-31-07-E | Elevation of Privilege | mounted gate/clarify actions | mitigate | reuse existing owner-scoped, terminal-fenced callbacks (KAN-100); no new command path |
| T-31-07-R | Repudiation | FIX-039 reducer ordering | mitigate | integration does NOT touch `useWorkflow.ts` — the accumulator-reset ordering is preserved by construction (diff-asserted) |
| T-31-07-SC | Tampering | npm installs | accept | zero new packages (react-markdown/motion/lucide already present) — no install/legitimacy surface |
</threat_model>

<verification>
- `cd frontend && npx playwright test --project=mocked e2e/tests/ts-chat.spec.ts e2e/tests/ts-chat-cards.spec.ts` — new specs PASS.
- DELTA baseline: run the pre-existing mocked suite once BEFORE (capture the already-red set) and once AFTER; assert NO previously-green spec regressed (CONTEXT e2e-BASELINE — do not require absolute green; DEF-29-06-1 baseline is pre-existing red).
- `cd frontend && npx tsc --noEmit` — IDENTITY (no new errors vs `e2e/fixtures/mockApi.ts` baseline).
- FIX-039: `git diff --name-only | grep -x "frontend/src/hooks/useWorkflow.ts"` returns nothing (untouched).
- LOCK-B: flag-OFF legacy WS path unchanged (`useWebSocket.ts` not in diff); no `/ws/chat` handler edit, no deletion ratchet.
- INV-3: no backend, no golden fixture change.
</verification>

<success_criteria>
The chat lane is live in the execution surface: mounted in the left column with absorbed controls, fed transport-agnostically (flag-OFF unchanged, LOCK-B), deep-linking into PreviewPanel tabs via the nonce seam — proven by two new mocked chat specs verified BY DELTA (new pass, no new breakage) on the codebase's first data-testids, with tsc-identity and FIX-039 preserved (INV-3, SC-001).
</success_criteria>

<output>
Create `.planning/phases/31-chat-lane-mvp-a3/31-07-SUMMARY.md` when done.
</output>
