---
phase: 31-chat-lane-mvp-a3
plan: 06
type: execute
wave: 2
depends_on: ["31-03"]
files_modified:
  - frontend/src/hooks/useChatAttachments.ts
  - frontend/src/hooks/useChatAttachments.test.ts
  - frontend/src/components/chat/ChatAttachments.tsx
  - frontend/src/components/chat/ChatAttachments.test.tsx
  - frontend/src/components/chat/ChatTokenWidget.tsx
  - frontend/src/components/chat/ChatTokenWidget.test.tsx
autonomous: true
requirements: [CHATUI-03]
must_haves:
  truths:
    - "The chat composer accepts attachments via file picker AND clipboard paste AND drag-drop, each routed through the same intake"
    - "Oversized images are downscaled client-side via Phase-30 resizeImage before base64, staying under the server cap"
    - "Attachment preview chips are removable; a non-retained image shows the ND-10 'image not kept on reopen' placeholder"
    - "A token-usage widget surfaces the P26 telemetry fields (input/output/total tokens, est cost, cache-read %) from the live run state"
  artifacts:
    - path: "frontend/src/hooks/useChatAttachments.ts"
      provides: "attachment intake (picker/paste/drag-drop) + resize + preview chip state"
      exports: ["useChatAttachments"]
    - path: "frontend/src/components/chat/ChatAttachments.tsx"
      provides: "drop zone + paste handler + preview chips + ND-10 placeholder"
      exports: ["ChatAttachments"]
    - path: "frontend/src/components/chat/ChatTokenWidget.tsx"
      provides: "P26 token-usage widget for the lane"
      exports: ["ChatTokenWidget"]
  key_links:
    - from: "frontend/src/hooks/useChatAttachments.ts"
      to: "frontend/src/lib/resizeImage.ts"
      via: "resizeImage(file) before base64 (UPLD-04)"
      pattern: "resizeImage"
    - from: "frontend/src/components/chat/ChatTokenWidget.tsx"
      to: "pipelineState token_usage fields (P26)"
      via: "read cacheReadTokens/totalTokens/estimatedCostUsd (mirror TokenUsageSummary)"
      pattern: "cacheRead|totalTokens"
---

<objective>
Deliver the chat-lane attachment UI (CHATUI-03 + the UPLD-04 residue: paste + drag-drop + client resize) and the P26 token-usage widget. The launch-composer picker + base64 + preview chips ALREADY exist (image waves, `IdeaInputPage.tsx`); this plan builds the chat-lane's own attachment intake ADDING paste + drag-drop and wiring Phase-30's `resizeImage.ts`, plus a token widget reading P26 telemetry.

Purpose: CONTEXT deliverable 4 — "picker + preview EXIST — ADD paste + drag-drop + wire resizeImage; token-usage widget from P26". Images are payload-transient (ND-10 / D-07): they ride the message payload out-of-band, never persisted, so a reopened turn shows the "image not retained" placeholder. The widget mirrors the existing `TokenUsageSummary` (P26) fields but sized for the lane.
Output: `useChatAttachments.ts`, `ChatAttachments.tsx`, `ChatTokenWidget.tsx` + suites. Composed by the lane (plan 04); the `ChatAttachment` type comes from plan 03.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-chat-lane-mvp-a3/31-CONTEXT.md
@frontend/src/lib/resizeImage.ts
@frontend/src/components/workflow/TokenUsageSummary.tsx
# The existing launch-composer image intake to mirror (picker + readAsDataURL + chips + resizeImage):
@frontend/src/components/workflow/IdeaInputPage.tsx
# ChatAttachment type is defined in plan 03 (frontend/src/types/index.ts) — treat as pinned.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useChatAttachments — picker + paste + drag-drop intake with client resize (UPLD-04)</name>
  <files>frontend/src/hooks/useChatAttachments.ts, frontend/src/hooks/useChatAttachments.test.ts</files>
  <behavior>
    `useChatAttachments()` returns `{ attachments, addFiles(files), onPaste(event), onDrop(event), remove(id), clear() }`:
    - Test: `addFiles([imageFile])` produces one image attachment whose base64 came THROUGH `resizeImage` (mock resizeImage; assert it was called for an image, and the resized `data`/`mime_type` land on the attachment).
    - Test: a NON-image file (e.g. .pdf) is added WITHOUT resize (resizeImage not called), carrying name/mime/size.
    - Test: `onPaste` with a clipboard image item extracts the file and routes it through `addFiles` (same resize path).
    - Test: `onDrop` with dataTransfer files routes through `addFiles`.
    - Test: client-side cap — files over the size/type allow-list are rejected before base64 (mirror the server cap: image mimes png/jpeg/webp/gif, per-image ~3.75MB post-resize).
    - Test: `remove(id)` drops one chip; `clear()` empties.
  </behavior>
  <action>
    Implement the attachment intake hook. Reuse the launch-composer pattern (`IdeaInputPage.tsx` image picker: `readAsDataURL` + strip prefix + `resizeImage(f).then(...)`) but ADD the two missing intakes flagged in evidence 04 §5 CORRECTIONS #5: clipboard `onPaste` (extract image items) and `onDrop` (dataTransfer files). Wire Phase-30 `frontend/src/lib/resizeImage.ts` (`resizeImage(file) -> {mime_type, data, width, height}`) for images BEFORE base64 (UPLD-04). Produce `ChatAttachment` records (plan-03 type) marked `retained:false` (ND-10 payload-transient — images never persist). Enforce a client cap mirroring the server allow-list. Do NOT inline base64 into message text — attachments ride as a structured payload (the lane's sendMessage passes them to the transport; server validation still applies).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useChatAttachments.test.ts</automated>
  </verify>
  <done>Picker + paste + drag-drop all route through resize (images) / passthrough (files) with a client cap; retained:false set (ND-10).</done>
</task>

<task type="auto">
  <name>Task 2: ChatAttachments preview chips (+ ND-10 placeholder) and ChatTokenWidget (P26)</name>
  <files>frontend/src/components/chat/ChatAttachments.tsx, frontend/src/components/chat/ChatAttachments.test.tsx, frontend/src/components/chat/ChatTokenWidget.tsx, frontend/src/components/chat/ChatTokenWidget.test.tsx</files>
  <action>
    `ChatAttachments.tsx`: a composer sub-surface wrapping `useChatAttachments` — a drop zone (`onDrop`/`onDragOver` with a visible drag-active state), a paste handler bound to the composer, a hidden multi-file `<input type=file accept="image/*,.pdf,.txt,.md,.docx">`, and removable preview chips (image thumbnail from the resized base64; file → name + size chip). For an attachment that is `retained:false` AND rendered in a REOPENED/replayed context, show the ND-10 "image not kept on reopen" placeholder chip (LOCK-E / ND-10 disposition — the placeholder IS the disposition, not durable storage). `data-testid="chat-attachments"`, `chat-attach-chip`, `chat-attach-dropzone`. Current skin (D-15), raw Tailwind.
    `ChatTokenWidget.tsx`: a compact lane widget mirroring `TokenUsageSummary.tsx` (P26) — read `totalTokens / totalInputTokens / totalOutputTokens / estimatedCostUsd / cacheReadTokens` off the live run state and render "N tokens · $cost · ⚡ M cached (P%)". Every figure PINNED to its P26 field (FIGURE-TO-FIELD: `agent_complete`/`pipeline_complete` token totals; cache split from cache telemetry) — no invented number. `data-testid="chat-token-widget"`.
    Tests: chips render + remove; drop zone toggles drag-active; ND-10 placeholder shows for a non-retained attachment in reopened mode; the token widget shows the cached % only when cacheRead>0 and renders the pinned totals.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/ChatAttachments.test.tsx src/components/chat/ChatTokenWidget.test.tsx && npx tsc --noEmit 2>&1 | grep -v "e2e/fixtures/mockApi.ts" | grep -c "error TS" | grep -qx 0 && echo TSC_IDENTITY_OK</automated>
  </verify>
  <done>Preview chips + drop zone + ND-10 placeholder render; token widget shows pinned P26 fields; tsc identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file/clipboard/drop -> composer | attachment bytes (image/file) enter the client from the user's device |
| resized base64 -> transport payload | attachment payload is sent to the run over the message up-channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-D | Denial of Service | oversized image/file intake | mitigate | client-side resize (resizeImage, longest-edge 1568) + a size/type allow-list reject BEFORE base64; server caps still apply (defense in depth) |
| T-31-06-T | Tampering | attachment mime/type | mitigate | client allow-lists mimes (png/jpeg/webp/gif + doc types); the authoritative validation stays server-side (Phase 30 caps), client cap is UX-only |
| T-31-06-I | Information disclosure | ND-10 payload-transient images | accept | images are never persisted (retained:false); a reopened turn shows the "image not kept" placeholder rather than a stale/leaked byte reference (ND-10/LOCK-E) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/hooks/useChatAttachments.test.ts src/components/chat/ChatAttachments.test.tsx src/components/chat/ChatTokenWidget.test.tsx` — green.
- `cd frontend && npx tsc --noEmit` — IDENTITY (no new errors vs `e2e/fixtures/mockApi.ts` baseline).
- resizeImage wired: `grep -q "resizeImage" frontend/src/hooks/useChatAttachments.ts` (UPLD-04).
- INV-3 + FIX-039: no backend, no golden, no `useWorkflow.ts`, no `frontend/e2e/**` change.
- data-testids on attachment + token surfaces (CONTEXT: first testids).
</verification>

<success_criteria>
Chat-lane attachment UI adds paste + drag-drop + client resize (UPLD-04) on top of the existing picker/preview pattern, marks images payload-transient with the ND-10 placeholder, and the P26 token widget surfaces pinned telemetry fields (CHATUI-03). tsc-identity; zero backend/e2e/useWorkflow change (INV-3, FIX-039).
</success_criteria>

<output>
Create `.planning/phases/31-chat-lane-mvp-a3/31-06-SUMMARY.md` when done.
</output>
