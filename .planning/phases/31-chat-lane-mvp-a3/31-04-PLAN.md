---
phase: 31-chat-lane-mvp-a3
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03", "31-05", "31-06"]
files_modified:
  - frontend/src/components/chat/RunChatLane.tsx
  - frontend/src/components/chat/RunChatLane.test.tsx
  - frontend/src/components/chat/ResultCard.tsx
  - frontend/src/components/chat/ResultCard.test.tsx
  - frontend/src/components/chat/MessageBubble.tsx
  - frontend/src/components/chat/ChatPanel.tsx
autonomous: true
requirements: [CHATUI-01]
must_haves:
  truths:
    - "The revived chat lane renders the family-anchored transcript with streaming markdown (react-markdown) and a live cursor on the streaming turn (D-02)"
    - "The streaming region carries aria-live=polite + role=log (open-design's documented a11y failure, NOT repeated)"
    - "Narrator result cards (clarify/gate/pipeline/deliverable/spec_revision) render from chat_reply and a card click fires the nonce'd deep-link seam"
    - "The lane absorbs the AgentProgressPanel controls — Stop, revise-as-chat composer, suggestions-as-chips — driven by the D-12 composer mode per run state"
  artifacts:
    - path: "frontend/src/components/chat/RunChatLane.tsx"
      provides: "the run chat lane composition root (transcript + composer + cards + blocks + controls)"
      exports: ["RunChatLane"]
      min_lines: 80
    - path: "frontend/src/components/chat/ResultCard.tsx"
      provides: "narrator result card with deep-link + spec_revision/deliverable kinds (LOCK-F 'Deliverable')"
      exports: ["ResultCard"]
  key_links:
    - from: "frontend/src/components/chat/RunChatLane.tsx"
      to: "frontend/src/hooks/useRunChat.ts + useTabDeepLink.ts"
      via: "transcript state + deep-link seam"
      pattern: "useRunChat|useTabDeepLink"
    - from: "frontend/src/components/chat/RunChatLane.tsx"
      to: "buildBlocks + tool-renderers + ThinkingBlock + useMeasuredVirtualWindow"
      via: "render the ChatBlock union (borrow #3/#4/#5/#7)"
      pattern: "buildBlocks|renderToolBlock"
    - from: "frontend/src/components/chat/ResultCard.tsx"
      to: "onRequestOpenTab (nonce'd seam, borrow #6)"
      via: "deep-link into run tabs"
      pattern: "onRequestOpenTab|deepLink"
---

<objective>
Compose the revived chat lane (CHATUI-01) — the run-screen left-column conversational surface. It renders the family-anchored transcript (D-02) with streaming markdown + `aria-live`/`role="log"`, projects narrator `chat_reply` cards (5 kinds) that deep-link into run tabs via the nonce'd seam (borrow #6), composes the plan-01/02 borrow pieces (buildBlocks, tool-renderer registry, ThinkingBlock/todo/file-ops, measured virtualizer), and absorbs the `AgentProgressPanel` controls (Stop, revise-as-chat, suggestions-as-chips) with a D-12 composer-mode-per-state. Mounts the plan-05 inline gate/clarify actions and the plan-06 attachment UI + token widget.

Purpose: D-09 says revive the in-repo kit (`ChatPanel`/`MessageBubble`/`ChatInput`), keep react-markdown, ship `aria-live` from day one (evidence 06 anti-lessons). This is the composition root that turns the dead kit + the borrow mechanisms + the data layer into one working lane in the CURRENT skin (D-15 — behavior over restyle; Phase 32 owns the reskin). SC-001: cards + affordances key on generic event kinds (`card_kind`), never a workflow name.
Output: `RunChatLane.tsx` (root), `ResultCard.tsx`, and the adapted revived kit (`MessageBubble.tsx`, `ChatPanel.tsx`) + suites.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-chat-lane-mvp-a3/31-CONTEXT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
@.planning/v2.0-evidence/06-open-design-teardown.md
# Revived kit to adapt (streaming bubbles, markdown, cursor, auto-scroll — already implemented):
@frontend/src/components/chat/ChatPanel.tsx
@frontend/src/components/chat/MessageBubble.tsx
@frontend/src/components/chat/ChatInput.tsx
# Controls to absorb (Stop / revise composer / suggestions-as-chips):
@frontend/src/components/workflow/AgentProgressPanel.tsx
# Consumed contracts from waves 1-2 (treat as pinned):
#   useRunChat / useTabDeepLink / ChatMessage        (plan 03)
#   buildBlocks / ChatBlock / partial-json           (plan 01)
#   tool-renderers / ThinkingBlock / virtualizer     (plan 02)
#   InlineGateActions / InlineClarifyActions         (plan 05)
#   ChatAttachments / ChatTokenWidget                (plan 06)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ResultCard — narrator cards (5 kinds) + nonce'd deep-link (borrow #6, LOCK-F)</name>
  <files>frontend/src/components/chat/ResultCard.tsx, frontend/src/components/chat/ResultCard.test.tsx</files>
  <action>
    Build `ResultCard.tsx` rendering a narrator `chat_reply` message by its GENERIC `cardKind` (`clarify | gate | pipeline | deliverable | spec_revision`) — a switch on the card kind, NOT a workflow name (SC-001). Each card shows the narrator text + a "deep-link" affordance that calls `onRequestOpenTab(tab)` (the plan-03 `useTabDeepLink.requestOpenTab`) to open the milestone's run tab (deliverable → Preview, clarify/gate → Steps, etc.) via the nonce'd seam (borrow #6). The `deliverable` card labels the output "Deliverable" (LOCK-F) and links "open in Preview →". The `spec_revision` card renders the KAN-101 loop-back label "Revising spec — cycle N" (distinct from a family revision run — LIVE-STATE-CONTRACT §2b terminology rule; never conflate). `data-testid="chat-result-card"` + `data-card-kind={cardKind}`. Current skin (D-15), raw Tailwind + lucide-react. Tests: each of the 5 kinds renders its label/testid; clicking the deep-link fires `onRequestOpenTab` with the expected tab; the deliverable card says "Deliverable" (LOCK-F); no workflow-name literal in the file (SC-001 grep).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/ResultCard.test.tsx && grep -cE "\"prototype\"|od_ppt|app_builder|user_stories|ppt_revision" src/components/chat/ResultCard.tsx | grep -qx 0 && echo SC001_OK</automated>
  </verify>
  <done>5 card kinds + deep-link fire proven; "Deliverable" label (LOCK-F); SC-001 clean.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt the revived kit — streaming markdown + aria-live/role=log + agent-block rendering</name>
  <files>frontend/src/components/chat/MessageBubble.tsx, frontend/src/components/chat/ChatPanel.tsx</files>
  <action>
    Adapt the two dead-kit components for the run context (D-09 revive, D-15 keep behavior/current skin — do NOT restyle to Phase-32 tokens). `MessageBubble.tsx`: keep its existing react-markdown + remark-gfm body + blinking `streaming-cursor`; ADD rendering of the plan-01 `ChatBlock[]` (via `buildBlocks`) for assistant turns — thinking (plan-02 `ThinkingBlock`), tool blocks (plan-02 `renderToolBlock` registry), todo/file-ops, and usage — so an agent turn shows reasoning/tools/progress, not just prose. A narrator turn (has `cardKind`) renders `ResultCard` instead of a bubble. `ChatPanel.tsx`: put `aria-live="polite"` + `role="log"` + `aria-relevant="additions text"` on the scrollable transcript region (evidence 06 §7 / POR §7 a11y landmine — open-design's documented failure; ship it from day one); keep the existing auto-scroll but delegate to the plan-02 measured virtualizer when `messages.length > VIRTUALIZE_THRESHOLD` (80). Add `data-testid="chat-transcript"` on the log region and `data-testid="chat-message"` per bubble. Tests (in ResultCard/RunChatLane suites or a bubble test): streaming turn shows the cursor + markdown; the log region has `role="log"` + `aria-live`; a narrator message renders a ResultCard.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'role="log"' src/components/chat/ChatPanel.tsx && grep -q 'aria-live' src/components/chat/ChatPanel.tsx && npx tsc --noEmit 2>&1 | grep -v "e2e/fixtures/mockApi.ts" | grep -c "error TS" | grep -qx 0 && echo A11Y_AND_TSC_OK</automated>
  </verify>
  <done>Transcript region has role=log + aria-live; assistant turns render agent blocks + markdown; narrator turns render ResultCard; tsc identity.</done>
</task>

<task type="auto">
  <name>Task 3: RunChatLane composition root — transcript + composer modes + absorbed controls</name>
  <files>frontend/src/components/chat/RunChatLane.tsx, frontend/src/components/chat/RunChatLane.test.tsx</files>
  <action>
    Build `RunChatLane.tsx` as the left-column composition root. It: (1) takes `messages`, `sendMessage`, `runState`, transport/command props + the gate/clarify/revise/stop callbacks (threaded by plan 07); (2) renders `ChatPanel` (the transcript + aria-live region) with the plan-06 `ChatTokenWidget` in the header/footer; (3) renders a UNIFIED composer that ABSORBS the AgentProgressPanel controls — a Stop button (visible while running), the revise-as-chat textarea (the old `AgentProgressPanel` revise composer), and the "Suggested next steps" chain buttons rendered as quick-reply CHIPS; (4) switches COMPOSER MODE per the D-12 live-state (LIVE-STATE-CONTRACT §1): clarify → answer channel (mount plan-05 `InlineClarifyActions`), gate → gate-action mode (mount plan-05 `InlineGateActions`), building → steering ("guidance applies at the next step" hint), complete → revision mode (free text = revision turn), terminal → relaunch. Free-text send goes through `sendMessage` (transport-agnostic, plan 03); quick-actions go through the SAME typed callbacks (plan 05). Mount plan-06 `ChatAttachments` in the composer. `data-testid="run-chat-lane"`, `chat-composer`, `chat-stop`. SC-001: composer mode keys off the generic `runState`, never a workflow name. Tests: composer mode flips with runState (clarify shows InlineClarifyActions, gate shows InlineGateActions, complete shows revision affordance); Stop fires its callback while running and hides on terminal; a free-text send calls `sendMessage`; a suggestion chip fires the chain callback; no workflow-name literal (SC-001 grep).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/RunChatLane.test.tsx && grep -cE "\"prototype\"|od_ppt|app_builder|user_stories" src/components/chat/RunChatLane.tsx | grep -qx 0 && echo SC001_OK</automated>
  </verify>
  <done>Composer mode-per-state proven; Stop/revise/suggestions absorbed; free-text via sendMessage; quick-actions via shared callbacks; SC-001 clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server chat/narrator text -> react-markdown render | narrator + streamed content is rendered as markdown in the transcript |
| result card -> tab navigation | a card deep-links into a run tab via the nonce seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-T | Tampering (XSS) | markdown render of server content | mitigate | react-markdown default (no `rehype-raw`, no `dangerouslySetInnerHTML`) — server text cannot inject executable HTML; keep the kit's existing safe renderer (evidence 06 anti-lesson: don't hand-roll) |
| T-31-04-T2 | Tampering | card renderer selection | mitigate | ResultCard switches on generic `card_kind`; unknown kind → inert generic card, no workflow-name branch (SC-001) |
| T-31-04-D | Denial of Service | large/streaming transcript | mitigate | plan-02 measured virtualizer engages >80 msgs; streaming turn re-renders bounded to the last bubble |
| T-31-04-I | Information disclosure | deep-link nonce | mitigate | single-use nonce (plan 03) — a stale card click cannot re-navigate; tabs are same-run only |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/` — the lane + card + kit suites green.
- `cd frontend && npx tsc --noEmit` — IDENTITY (no new errors vs `e2e/fixtures/mockApi.ts` baseline).
- a11y: `grep -q 'role="log"' frontend/src/components/chat/ChatPanel.tsx && grep -q 'aria-live' frontend/src/components/chat/ChatPanel.tsx` (POR §7 landmine).
- SC-001: no workflow-name literal in `RunChatLane.tsx` / `ResultCard.tsx`.
- INV-3 + FIX-039: no backend, no golden, no `useWorkflow.ts`, no `frontend/e2e/**` change (mount + e2e are plan 07).
- data-testids on lane/transcript/composer/cards (CONTEXT: first testids).
</verification>

<success_criteria>
The revived chat lane composes the borrow mechanisms + data layer into a working CURRENT-SKIN surface (CHATUI-01): family-anchored transcript with streaming markdown + `aria-live`/`role="log"`, 5-kind narrator cards deep-linking via the nonce seam, absorbed Stop/revise/suggestions controls, and D-12 composer-mode-per-state mounting the plan-05 quick-actions + plan-06 attachments/token widget. SC-001 clean, tsc-identity, zero backend/e2e change (INV-3).
</success_criteria>

<output>
Create `.planning/phases/31-chat-lane-mvp-a3/31-04-SUMMARY.md` when done.
</output>
