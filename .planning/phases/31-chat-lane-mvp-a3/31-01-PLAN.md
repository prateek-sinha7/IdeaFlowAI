---
phase: 31-chat-lane-mvp-a3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/runtime/partial-json.ts
  - frontend/src/components/chat/runtime/partial-json.test.ts
  - frontend/src/components/chat/runtime/streaming-json.ts
  - frontend/src/components/chat/runtime/streaming-json.test.ts
  - frontend/src/components/chat/runtime/buildBlocks.ts
  - frontend/src/components/chat/runtime/buildBlocks.test.ts
  - frontend/src/components/chat/runtime/blocks.types.ts
  - frontend/NOTICE
  - frontend/THIRD-PARTY-NOTICES.md
autonomous: true
requirements: [CHATUI-01]
must_haves:
  truths:
    - "partial-json repair parses a truncated JSON prefix without throwing"
    - "extractStreamingJsonString pulls a single named string field out of an open JSON fragment"
    - "buildBlocks coalesces an ordered agent-event array into merged render blocks (consecutive text/thinking merged, same-family tools grouped)"
    - "A NOTICE + THIRD-PARTY-NOTICES file credits nexu-io/open-design (Apache-2.0) for the borrowed mechanisms"
  artifacts:
    - path: "frontend/src/components/chat/runtime/partial-json.ts"
      provides: "repairJsonPrefix + parsePartialJson (borrow #1)"
      exports: ["repairJsonPrefix", "parsePartialJson"]
    - path: "frontend/src/components/chat/runtime/streaming-json.ts"
      provides: "extractStreamingJsonString (borrow #2)"
      exports: ["extractStreamingJsonString"]
    - path: "frontend/src/components/chat/runtime/buildBlocks.ts"
      provides: "buildBlocks events->blocks reducer (borrow #4)"
      exports: ["buildBlocks"]
    - path: "frontend/src/components/chat/runtime/blocks.types.ts"
      provides: "ChatBlock discriminated union + AgentEvent input types"
      exports: ["ChatBlock", "AgentEvent"]
    - path: "frontend/NOTICE"
      provides: "Apache-2.0 attribution to open-design"
      contains: "open-design"
  key_links:
    - from: "frontend/src/components/chat/runtime/buildBlocks.ts"
      to: "frontend/src/components/chat/runtime/blocks.types.ts"
      via: "import type ChatBlock/AgentEvent"
      pattern: "blocks\\.types"
---

<objective>
Land the pure, dependency-light open-design borrow-list PRIMITIVES (D-09 items #1, #2, #4) as owned, tested modules under a new `frontend/src/components/chat/runtime/` directory, plus the mandatory Apache-2.0 attribution/NOTICE. These are the reusable logic pieces the revived chat lane (plan 04) renders against — shipped first, interface-first, so downstream plans import stable contracts.

Purpose: The evidence teardown (`06-open-design-teardown.md` §8b) verdict is "revive our kit, BORROW these mechanisms with attribution". This plan owns the three framework-neutral pure functions and pins the `ChatBlock` render contract the lane consumes. No mounting, no transport — pure logic, maximally testable (opportunistic TDD per `tdd_mode=false`).
Output: `partial-json.ts`, `streaming-json.ts`, `buildBlocks.ts`, `blocks.types.ts` + their vitest suites, and `NOTICE`/`THIRD-PARTY-NOTICES.md` crediting nexu-io/open-design (Apache-2.0, D-09).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-chat-lane-mvp-a3/31-CONTEXT.md
@.planning/v2.0-evidence/06-open-design-teardown.md
@.planning/CHAT-AND-UI-CONVERGENCE-PLAN.md

# Existing FE conventions (React 19, TS strict, vitest ^4, path alias @/*)
@frontend/src/hooks/useRunStream.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: partial-json + streaming-json repair primitives (borrow #1, #2) + Apache-2.0 NOTICE</name>
  <files>frontend/src/components/chat/runtime/partial-json.ts, frontend/src/components/chat/runtime/partial-json.test.ts, frontend/src/components/chat/runtime/streaming-json.ts, frontend/src/components/chat/runtime/streaming-json.test.ts, frontend/NOTICE, frontend/THIRD-PARTY-NOTICES.md</files>
  <behavior>
    partial-json (`repairJsonPrefix(prefix: string): string` + `parsePartialJson<T>(prefix: string): T | undefined`):
    - Test: a complete valid JSON string parses to the same object as JSON.parse.
    - Test: a truncated object `{"a":1,"b":"tex` repairs+parses to `{a:1}` (drops the incomplete trailing key/value, never throws).
    - Test: a truncated array `[1,2,` repairs+parses to `[1,2]`.
    - Test: an unterminated string value closes the quote (`{"a":"he` -> `{a:"he"}`).
    - Test: garbage / empty input returns undefined from parsePartialJson (never throws).
    streaming-json (`extractStreamingJsonString(fragment: string, field: string): string | undefined`):
    - Test: `{"content":"partial cod` with field `content` -> `partial cod`.
    - Test: field appearing after other keys `{"path":"a.ts","content":"x` -> `x` for `content`.
    - Test: field absent -> undefined.
    - Test: escaped quotes inside the value are preserved (`{"content":"a \" b`).
  </behavior>
  <action>
    Implement `partial-json.ts` and `streaming-json.ts` as ZERO-dependency pure TS, reimplementing the behavior described in `06-open-design-teardown.md` §5 + §8b (open-design `runtime/partial-json.ts` lines 16-93 `repairJsonPrefix`/`parsePartialJson`, and the `extractStreamingJsonString` single-field extractor at `AssistantMessage.tsx:2786`). This is a clean-room reimplementation from the behavioral spec — do NOT depend on any open-design source at build time; these functions power live streaming tool-arg / result-card previews later. Because the logic is DERIVED from Apache-2.0 code, create `frontend/NOTICE` (product name + "This product includes software derived from nexu-io/open-design (https://github.com/nexu-io/open-design), Apache License 2.0.") and `frontend/THIRD-PARTY-NOTICES.md` listing each borrowed mechanism (#1 partial-json repair, #2 extractStreamingJsonString, #3 tool-renderer registry, #4 buildBlocks reducer, #5 scroll/virtualizer, #6 nonce'd deep-link seam, #7 ThinkingBlock/todo/file-ops) with the upstream file:line and the local file that reimplements it. Per D-09 (no external chat framework; borrow WITH attribution). Add a short file-header block-comment in each borrow module: `Adapted from nexu-io/open-design (Apache-2.0) — see /THIRD-PARTY-NOTICES.md`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/runtime/partial-json.test.ts src/components/chat/runtime/streaming-json.test.ts</automated>
  </verify>
  <done>Both suites green; NOTICE + THIRD-PARTY-NOTICES exist and name open-design + Apache-2.0; each borrow module carries the attribution header comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatBlock contract + buildBlocks events->blocks reducer (borrow #4)</name>
  <files>frontend/src/components/chat/runtime/blocks.types.ts, frontend/src/components/chat/runtime/buildBlocks.ts, frontend/src/components/chat/runtime/buildBlocks.test.ts</files>
  <behavior>
    `blocks.types.ts` defines `AgentEvent` (discriminated union: text | thinking | tool_use | tool_result | usage | status | file_op input events) and `ChatBlock` (the coalesced render union: `{kind:'text',text}` | `{kind:'thinking',text,durationMs?}` | `{kind:'tool',name,status,args?,result?,isError?}` | `{kind:'usage',inputTokens,outputTokens,costUsd?,durationMs?}` | `{kind:'file_ops',files}`). All keyed on GENERIC event kinds — NEVER a workflow/agent name (SC-001).
    `buildBlocks(events: AgentEvent[]): ChatBlock[]`:
    - Test: consecutive `text` deltas merge into ONE text block (accumulated string).
    - Test: consecutive `thinking` deltas merge into one thinking block; a `thinking` then `text` yields two blocks in order.
    - Test: a `tool_use` + its matching `tool_result` collapse into one `tool` block with status resolved (pending -> success/error via isError).
    - Test: two same-name tool runs group order-preserved; different-name tools stay separate.
    - Test: a `usage` event becomes a trailing `usage` block; ordering of non-mergeable kinds is stable/preserved.
    - Test: empty input -> [].
  </behavior>
  <action>
    Implement `buildBlocks.ts` reimplementing open-design's `AssistantMessage.buildBlocks()` coalescing model (`06-open-design-teardown.md` §5 + §8b, upstream `AssistantMessage.tsx:3148`) as a pure reducer over the typed `AgentEvent[]`. Define the `AgentEvent`/`ChatBlock` unions in `blocks.types.ts` — this is the interface-first contract plans 02 (tool-renderer registry, blocks) and 04 (lane) render against, so keep it minimal, generic, and free of any workflow-name discriminator (SC-001). Attribution header comment as in Task 1 (Apache-2.0 / open-design). Pure TS, no React import in this file.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/runtime/buildBlocks.test.ts && npx tsc --noEmit 2>&1 | grep -v "e2e/fixtures/mockApi.ts" | grep -c "error TS" | grep -qx 0 && echo TSC_IDENTITY_OK</automated>
  </verify>
  <done>buildBlocks suite green; ChatBlock/AgentEvent exported; tsc shows ZERO new errors outside the known pre-existing `e2e/fixtures/mockApi.ts` baseline (identity).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server frame -> client parser | streamed JSON fragments (partial-json / buildBlocks) originate from run events; untrusted/partial by nature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-D | Denial of Service | partial-json on a pathological truncated fragment | mitigate | bounded single-pass repair (no unbounded backtracking); parsePartialJson returns undefined instead of throwing/looping |
| T-31-01-T | Tampering | buildBlocks keyed on event kind | mitigate | union discriminated on GENERIC kinds only; no workflow-name branch (SC-001) so a crafted name cannot select a privileged renderer |
| T-31-01-SC | Tampering | npm/pip installs | accept | ZERO new packages — pure TS reimplementation of Apache-2.0 mechanisms; no install surface |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/runtime/` — all borrow-primitive suites green.
- `cd frontend && npx tsc --noEmit` — IDENTITY: zero new `error TS` beyond the known pre-existing `e2e/fixtures/mockApi.ts` baseline (delta-verify per CONTEXT e2e-BASELINE caveat).
- INV-3: no backend file, no golden fixture, no `frontend/e2e/**` spec touched by this plan (FE-only, by construction).
- SC-001: `grep -rnE "prototype|od_ppt|app_builder|user_stories" frontend/src/components/chat/runtime/` returns 0 (no workflow-name literals in the borrow primitives).
</verification>

<success_criteria>
Three pure borrow modules (#1/#2/#4) + the ChatBlock contract land under `frontend/src/components/chat/runtime/` with green vitest suites, tsc-identity, Apache-2.0 NOTICE/THIRD-PARTY-NOTICES crediting open-design (D-09), and no workflow-name literals (SC-001). Zero backend / e2e-spec changes (INV-3).
</success_criteria>

<output>
Create `.planning/phases/31-chat-lane-mvp-a3/31-01-SUMMARY.md` when done.
</output>
