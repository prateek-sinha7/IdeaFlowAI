---
phase: 31-chat-lane-mvp-a3
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/hooks/useRunChat.ts
  - frontend/src/hooks/useRunChat.test.ts
  - frontend/src/hooks/useTabDeepLink.ts
  - frontend/src/hooks/useTabDeepLink.test.ts
autonomous: true
requirements: [CHATUI-01]
must_haves:
  truths:
    - "A chat transcript hook folds chat_message / chat_reply / stream_attached frames into an append-only, family-anchored, event_id-deduped transcript (D-02)"
    - "Sending a turn optimistically renders it, POSTs via the transport up-channel, and reconciles the server chat_message echo by message_id"
    - "A nonce'd deep-link seam turns a result-card target into a single-use {tab, nonce} the Preview panel consumes exactly once (borrow #6)"
    - "ChatMessage carries attachments + narrator cardKind + deepLink without breaking the existing type consumers"
  artifacts:
    - path: "frontend/src/hooks/useRunChat.ts"
      provides: "family-anchored transcript state + sendMessage + card projection"
      exports: ["useRunChat"]
    - path: "frontend/src/hooks/useTabDeepLink.ts"
      provides: "nonce'd deep-link seam (borrow #6)"
      exports: ["useTabDeepLink"]
    - path: "frontend/src/types/index.ts"
      provides: "extended ChatMessage (attachments/cardKind/deepLink) + ChatAttachment + DeepLinkTarget"
      contains: "ChatAttachment"
  key_links:
    - from: "frontend/src/hooks/useRunChat.ts"
      to: "chat_message / chat_reply / stream_attached frames"
      via: "reducer over the RunStreamMessage / WS frame envelope {type,data:{event_id,seq}}"
      pattern: "chat_reply"
    - from: "frontend/src/hooks/useRunChat.ts"
      to: "POST /api/runs/{id}/messages (SSE up-channel) OR user_message (legacy WS)"
      via: "sendMessage transport shim"
      pattern: "messages"
---

<objective>
Own the chat lane's DATA layer, interface-first: the `useRunChat` transcript hook, the `ChatMessage` type extension, and the `useTabDeepLink` nonce'd seam (borrow #6). Downstream plans (04 lane, 05 quick-actions, 06 attachments, 07 integration) import these pinned contracts instead of re-deriving them.

Purpose: The transcript must be an append-only, family-anchored (D-02) accumulation that rehydrates from `run_events` replay and stitches across parent/child runs — NOT a per-phase content swap (LIVE-STATE-CONTRACT §0). The frame vocabulary (`chat_message`/`chat_reply`/`stream_attached`) and envelope (`{type, data:{event_id, seq}}`) already exist from Phase 29 (mock drivers in `e2e/fixtures/mockSse.ts` + `mockWs.ts`); this hook consumes them transport-agnostically so it works under the Phase-29 SSE flag (`NEXT_PUBLIC_SSE_TRANSPORT`) AND the flag-OFF legacy WS path unchanged.
Output: `useRunChat.ts`, `useTabDeepLink.ts`, extended `ChatMessage`, + suites.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-chat-lane-mvp-a3/31-CONTEXT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
@frontend/src/hooks/useRunStream.ts
@frontend/src/providers/RunConnectionProvider.tsx
@frontend/src/types/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ChatMessage type + useRunChat transcript reducer (D-02 family-anchored)</name>
  <files>frontend/src/types/index.ts, frontend/src/hooks/useRunChat.ts, frontend/src/hooks/useRunChat.test.ts</files>
  <behavior>
    Types (`ChatAttachment {kind:'image'|'file', name, mimeType?, sizeBytes?, retained:boolean}`, `DeepLinkTarget {tab, nonce}`, `ChatMessage` extended with optional `attachments?`, `cardKind?: 'clarify'|'gate'|'pipeline'|'deliverable'|'spec_revision'`, `deepLink?: DeepLinkTarget`). Extensions are OPTIONAL so existing consumers (dead kit MessageBubble/ChatPanel) still compile.
    `useRunChat({ runId, subscribe, sendCommand, legacyWsSend? })` returns `{ messages, sendMessage, streamAttached }`:
    - Test: a `chat_message` frame appends a user turn keyed by `message_id`.
    - Test: a `chat_reply` frame appends an assistant NARRATOR message carrying `cardKind` + `deepLink` from `data.card_kind`/`data.deep_link`.
    - Test: the transcript ACCUMULATES — a second run state's frames APPEND, never replace/clear a prior turn (LIVE-STATE-CONTRACT §0 accumulation rule); a `pipeline_complete`/state-change frame does not wipe the transcript.
    - Test: duplicate frames (same `event_id`) are deduped (mirrors dashboard `shouldApplyEvent`).
    - Test: `sendMessage(text, attachments?)` optimistically appends a user turn with a client `message_id`, and the later server `chat_message` echo with the same `message_id` reconciles (no duplicate bubble).
    - Test: family anchoring — messages carry their `run_id`/`thread_id`; a child run's frames stitch into the SAME transcript array (D-02).
  </behavior>
  <action>
    Extend `ChatMessage` in `frontend/src/types/index.ts` with the three OPTIONAL fields + add `ChatAttachment`/`DeepLinkTarget` (do not remove/rename any existing field — INV-3 posture / non-breaking). Implement `useRunChat` as a pure-ish reducer over the shared frame envelope `{type, data:{event_id, seq, ...}}`. Keep it TRANSPORT-AGNOSTIC: it accepts a `subscribe(fn)` (the Phase-29 `RunConnectionProvider.subscribe` when the SSE flag is ON) and a `sendCommand(runId, payload)` up-channel; `sendMessage` posts `{text, attachments, message_id}` via `sendCommand` (SSE → `POST /api/runs/{id}/messages`) OR, when a `legacyWsSend` is provided (flag-OFF), emits the legacy `user_message` WS frame — SAME transcript either way ("single backend channel", CONTEXT). Dedup by `event_id`; reconcile optimistic sends by `message_id`. Do NOT touch `useWorkflow.ts` (preserve its FIX-039 ordering — the pipeline reducer is separate; the transcript is its own hook).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useRunChat.test.ts && npx tsc --noEmit 2>&1 | grep -v "e2e/fixtures/mockApi.ts" | grep -c "error TS" | grep -qx 0 && echo TSC_IDENTITY_OK</automated>
  </verify>
  <done>Transcript accumulates + dedupes + reconciles optimistic sends; ChatMessage extension is non-breaking (tsc identity); no useWorkflow.ts edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useTabDeepLink nonce'd seam (borrow #6)</name>
  <files>frontend/src/hooks/useTabDeepLink.ts, frontend/src/hooks/useTabDeepLink.test.ts</files>
  <behavior>
    `useTabDeepLink()` returns `{ pending, requestOpenTab(tab), consume() }` (a `{tab, nonce}` pending target):
    - Test: `requestOpenTab('preview')` sets a pending `{tab:'preview', nonce}` with a fresh nonce.
    - Test: two successive requests to the SAME tab produce DIFFERENT nonces (so a repeat deep-link re-triggers the consumer effect).
    - Test: `consume()` clears pending after read (single-use — a second consume yields nothing).
    - Test: a stale nonce (already consumed) does not re-open the tab.
  </behavior>
  <action>
    Implement the nonce'd deep-link-into-tabs seam from `06-open-design-teardown.md` §5 (`onRequestOpenFile(tabId)` -> `{name, nonce}` -> `setActiveTab`, string tab ids, borrow #6). The nonce makes the target CONSUME-ONCE and RE-TRIGGERABLE (a second click on the same-tab card still fires the consumer `useEffect`). This is the seam the result cards (plan 04) call and the Preview panel (plan 07) consumes to `setActiveTab`. Generic string tab ids only — no workflow-name coupling (SC-001). Attribution header (Apache-2.0 / open-design).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useTabDeepLink.test.ts</automated>
  </verify>
  <done>Fresh nonce per request, same-tab re-trigger, single-use consume all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server chat frame -> transcript | chat_message/chat_reply payloads are server-controlled, folded into rendered transcript |
| deep-link nonce -> tab switch | a card-issued deep link drives a panel navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-S | Spoofing | optimistic-send reconciliation | mitigate | reconcile strictly by client-minted `message_id`; a server echo with a mismatched id appends as a distinct turn rather than overwriting an unrelated optimistic bubble |
| T-31-03-T | Tampering | deep-link target | mitigate | nonce is single-use + generated client-side per request; `consume()` clears it so a replayed/stale nonce cannot re-navigate |
| T-31-03-I | Information disclosure | family-anchored transcript | mitigate | messages carry only the run/thread ids the server already scoped (owner-scoped run_events, Phase 29); the hook never fetches cross-owner runs |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/hooks/useRunChat.test.ts src/hooks/useTabDeepLink.test.ts` — green.
- `cd frontend && npx tsc --noEmit` — IDENTITY (ChatMessage extension is additive/optional; zero new errors vs `e2e/fixtures/mockApi.ts` baseline).
- FIX-039 preserved: `git diff --name-only` must NOT include `frontend/src/hooks/useWorkflow.ts` (the transcript is a separate hook).
- INV-3: no backend, no golden, no `frontend/e2e/**` change.
</verification>

<success_criteria>
The transcript data layer + type contract + nonce'd seam ship interface-first: `useRunChat` accumulates a family-anchored (D-02) transcript from the Phase-29 frame vocabulary transport-agnostically, `ChatMessage` gains non-breaking optional fields, `useTabDeepLink` provides the single-use seam (borrow #6). tsc-identity; useWorkflow.ts untouched (FIX-039 preserved); zero backend/e2e change (INV-3).
</success_criteria>

<output>
Create `.planning/phases/31-chat-lane-mvp-a3/31-03-SUMMARY.md` when done.
</output>
