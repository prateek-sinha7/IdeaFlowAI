---
phase: 31-chat-lane-mvp-a3
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/runtime/tool-renderers.tsx
  - frontend/src/components/chat/runtime/tool-renderers.test.tsx
  - frontend/src/components/chat/runtime/useMeasuredVirtualWindow.ts
  - frontend/src/components/chat/runtime/useMeasuredVirtualWindow.test.ts
  - frontend/src/components/chat/blocks/ThinkingBlock.tsx
  - frontend/src/components/chat/blocks/TodoCard.tsx
  - frontend/src/components/chat/blocks/FileOpsSummary.tsx
  - frontend/src/components/chat/blocks/blocks.test.tsx
autonomous: true
requirements: [CHATUI-01]
must_haves:
  truths:
    - "A tool-renderer registry dispatches registry -> family-card -> generic fallback, try/catch isolated so one bad renderer never breaks the transcript"
    - "ThinkingBlock renders collapsed-by-default with a 'Thought for Xs' timer; TodoCard renders plan progress; FileOpsSummary renders a files-this-turn strip"
    - "A measured virtual window returns only the visible slice of a >80-message list from a scroll ref + measured heights"
  artifacts:
    - path: "frontend/src/components/chat/runtime/tool-renderers.tsx"
      provides: "tool-renderer registry + dispatch (borrow #3)"
      exports: ["registerToolRenderer", "renderToolBlock", "GenericToolCard"]
    - path: "frontend/src/components/chat/runtime/useMeasuredVirtualWindow.ts"
      provides: "ResizeObserver-measured virtual window (borrow #5)"
      exports: ["useMeasuredVirtualWindow", "VIRTUALIZE_THRESHOLD"]
    - path: "frontend/src/components/chat/blocks/ThinkingBlock.tsx"
      provides: "collapsed reasoning block with elapsed timer (borrow #7)"
      exports: ["ThinkingBlock"]
  key_links:
    - from: "frontend/src/components/chat/runtime/tool-renderers.tsx"
      to: "frontend/src/components/chat/runtime/blocks.types.ts"
      via: "import type ChatBlock (tool variant)"
      pattern: "blocks\\.types"
---

<objective>
Land the open-design borrow-list RENDERING pieces (D-09 items #3 tool-renderer registry, #5 scroll/measured-virtualizer, #7 ThinkingBlock/todo/file-ops blocks) as small, isolated React components/hooks under `frontend/src/components/chat/`. These render the `ChatBlock` union produced by plan 01's `buildBlocks`, and are composed by the lane in plan 04.

Purpose: These are the agent-rendering mechanics the dead in-repo kit lacks (`06-open-design-teardown.md` §8 "Net"). Owning them as attributed, tested, framework-neutral pieces keeps the lane (plan 04) a thin composition. The registry pattern (#3) is the SC-001-safe extension point: renderers key on GENERIC tool names, never a workflow name.
Output: `tool-renderers.tsx`, `useMeasuredVirtualWindow.ts`, and the three block components + suites. Attribution headers per plan 01's NOTICE.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-chat-lane-mvp-a3/31-CONTEXT.md
@.planning/v2.0-evidence/06-open-design-teardown.md

# The ChatBlock/AgentEvent contract this plan renders (from plan 01 — same wave; treat as the pinned interface)
# frontend/src/components/chat/runtime/blocks.types.ts  (kinds: text | thinking | tool | usage | file_ops)
# Existing motion + lucide-react conventions:
@frontend/src/components/chat/MessageBubble.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tool-renderer registry + dispatch (borrow #3)</name>
  <files>frontend/src/components/chat/runtime/tool-renderers.tsx, frontend/src/components/chat/runtime/tool-renderers.test.tsx</files>
  <behavior>
    `registerToolRenderer(name: string, render: (props: {status,name,args?,result?,isError?}) => ReactNode)` + `renderToolBlock(block)`:
    - Test: a registered renderer for tool `read_file` is used when a tool block with that name renders.
    - Test: an UNregistered tool name falls back to `GenericToolCard` (shows name + status).
    - Test: a renderer that THROWS is caught (try/catch isolation) and the dispatch falls back to GenericToolCard instead of crashing — assert no throw propagates.
    - Test: status derivation — pending (no result) vs success vs error (isError:true) — reflected in the rendered output (role/text).
    - Test: dispatch keys ONLY on the generic `name` string; no branch references a workflow name (SC-001).
  </behavior>
  <action>
    Implement the three-tier dispatch from `06-open-design-teardown.md` §5 (`runtime/tool-renderers.ts` + `ToolCard.tsx:37-77` dispatch; `deriveToolStatus`/`toRenderProps` are pure): a `Map`-based renderer registry -> (empty by default, extension point) -> `GenericToolCard` fallback, each call try/catch-isolated so a bad renderer degrades to the fallback. Consume the `ChatBlock` `tool` variant from plan 01. Do NOT pre-populate a family-card vocabulary here (the lane can register renderers later); ship the registry + generic fallback + status derivation. Render with raw Tailwind + lucide-react (matching kit stack); no shadcn/Radix (none in repo). Attribution header comment (Apache-2.0 / open-design, see /THIRD-PARTY-NOTICES.md). SC-001: renderers are looked up by generic tool name only.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/runtime/tool-renderers.test.tsx</automated>
  </verify>
  <done>Registry + generic fallback + try/catch isolation green; no workflow-name branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: measured virtual window hook (borrow #5)</name>
  <files>frontend/src/components/chat/runtime/useMeasuredVirtualWindow.ts, frontend/src/components/chat/runtime/useMeasuredVirtualWindow.test.ts</files>
  <behavior>
    `useMeasuredVirtualWindow({ itemCount, scrollRef, estimateHeight })` returns `{ startIndex, endIndex, topSpacer, bottomSpacer, measure(index, height) }` + `VIRTUALIZE_THRESHOLD = 80`:
    - Test: below the threshold (itemCount <= 80) it returns the FULL range [0, itemCount) with zero spacers (virtualization disengaged).
    - Test: above the threshold with a mocked scrollTop/clientHeight, only a windowed slice is returned and topSpacer/bottomSpacer sum ~= off-window height.
    - Test: `measure(index, height)` updates the running height model so a subsequent window uses the measured (not estimated) height.
    - Test: pure math is deterministic given the same scroll inputs (no DOM required beyond the injected ref shape).
  </behavior>
  <action>
    Implement the measured virtual window from `06-open-design-teardown.md` §5 + §8b (`useMeasuredVirtualWindow` at `ChatPane.tsx:3059`, `CHAT_MESSAGE_VIRTUALIZE_THRESHOLD = 80`). Depend only on a scroll ref + item count + a per-index measured-height map (ResizeObserver in the component that mounts it; the hook exposes `measure()` for that). Keep the windowing math pure/unit-testable by reading scrollTop/clientHeight off the passed ref (mock it in tests). Export `VIRTUALIZE_THRESHOLD`. Attribution header (Apache-2.0 / open-design). This is the scroll/anchor + virtualizer the lane engages at >80 messages; the kit's naive `scrollIntoView` auto-scroll stays for small transcripts.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/runtime/useMeasuredVirtualWindow.test.ts</automated>
  </verify>
  <done>Threshold disengage + windowed slice + measure() update all green.</done>
</task>

<task type="auto">
  <name>Task 3: ThinkingBlock + TodoCard + FileOpsSummary block components (borrow #7)</name>
  <files>frontend/src/components/chat/blocks/ThinkingBlock.tsx, frontend/src/components/chat/blocks/TodoCard.tsx, frontend/src/components/chat/blocks/FileOpsSummary.tsx, frontend/src/components/chat/blocks/blocks.test.tsx</files>
  <action>
    Build three small presentational block components reimplementing open-design's `ThinkingBlock` (`AssistantMessage.tsx:2603` — collapsed-by-default reasoning with a client-measured "Thought for Xs" timer), the TodoWrite-backed `TodoCard` (`runtime/todos.ts` + `TodoCard`), and `FileOpsSummary` ("files this turn" strip, `runtime/file-ops.ts`), per `06-open-design-teardown.md` §5. Render from the plan-01 `ChatBlock` variants (`thinking`, and a todo/file_ops projection). CURRENT SKIN per D-15 (behavior over restyle): raw Tailwind + motion + lucide-react, reuse the existing kit's collapsible/`<thinking>` idiom seen in `MessageBubble.tsx` (do not introduce a new design token set — Phase 32 owns the reskin). Add a `data-testid` on each block root (`chat-thinking-block`, `chat-todo-card`, `chat-file-ops`) — the codebase's first testids, per CONTEXT. Attribution header (Apache-2.0 / open-design). `blocks.test.tsx`: render each with a fixture block and assert the testid + collapsed/expand toggle (ThinkingBlock) + the "Thought for Xs" label appears once a duration is provided.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/blocks/blocks.test.tsx && npx tsc --noEmit 2>&1 | grep -v "e2e/fixtures/mockApi.ts" | grep -c "error TS" | grep -qx 0 && echo TSC_IDENTITY_OK</automated>
  </verify>
  <done>Three blocks render with data-testids; ThinkingBlock collapse/expand + timer label proven; tsc identity (no new errors vs baseline).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server tool/result payload -> renderer | tool args/results in blocks are server-streamed, rendered as UI |
| large transcript -> DOM | an unbounded message list can exhaust the DOM/CPU |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-T | Tampering | tool-renderer dispatch | mitigate | try/catch isolation per renderer + generic-name-only lookup (SC-001) — a crafted tool name cannot select a privileged/unknown renderer, falls back to GenericToolCard |
| T-31-02-D | Denial of Service | long transcript render | mitigate | measured virtual window engages at >80 messages, renders only the visible slice + spacers |
| T-31-02-I | Information disclosure | tool args/result render | mitigate | rendered as inert text via block components (no raw HTML/eval); markdown rendering is plan 04's react-markdown (no dangerouslySetInnerHTML) |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/chat/runtime/ src/components/chat/blocks/` — green.
- `cd frontend && npx tsc --noEmit` — IDENTITY (zero new errors vs the `e2e/fixtures/mockApi.ts` baseline).
- SC-001: `grep -rnE "prototype|od_ppt|app_builder|user_stories" frontend/src/components/chat/runtime/tool-renderers.tsx` returns 0.
- INV-3: no backend, no golden, no `frontend/e2e/**` change (FE-only).
- First `data-testid`s land on chat block surfaces (CONTEXT: codebase's first testids).
</verification>

<success_criteria>
The three borrow-list rendering mechanisms (#3/#5/#7) ship as attributed, tested, SC-001-safe React pieces that render plan 01's `ChatBlock` union, with data-testids on block surfaces and tsc-identity. Zero backend/e2e-spec change (INV-3).
</success_criteria>

<output>
Create `.planning/phases/31-chat-lane-mvp-a3/31-02-SUMMARY.md` when done.
</output>
