---
phase: 03-token-trim-measured-change-0c
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/tests/agents/characterization/golden/prototype.html
  - backend/tests/agents/characterization/golden/od_prototype.html
  - backend/tests/agents/test_phase3_parity.py
  - backend/tests/agents/test_phase3_token_delta_live.py
autonomous: true
requirements: [COMPACT-02, COMPACT-03]
must_haves:
  truths:
    - "The prototype + od_prototype deliverable byte-goldens are re-recorded under SNAPSHOT_UPDATE=1 to reflect the compacted-prompt output (the sanctioned INV-3 0C change); no other pipeline's golden changes."
    - "The prototype + od_prototype semantic event snapshots stay green with NO golden edit; seq stays contiguous."
    - "A dedicated test proves the produced data-page ID set + routes map keys are identical before/after compaction and validation is equal-or-better (zero net-new failures)."
    - "A real multi-task build's token/cost delta (a measured reduction) is recorded in the phase SUMMARY with a reproduction command, via an opt-in live-gated test."
  artifacts:
    - path: "backend/tests/agents/test_phase3_parity.py"
      provides: "Pages/routes + validation-pass parity assertion driven offline via _drive('prototype') and _drive('od_prototype')"
      min_lines: 50
    - path: "backend/tests/agents/test_phase3_token_delta_live.py"
      provides: "Opt-in (RUN_LIVE_BEDROCK=1 + creds) live token-delta evidence test mirroring test_deep_agent_runner_hitl_live.py gating"
      min_lines: 40
    - path: "backend/tests/agents/characterization/golden/prototype.html"
      provides: "Re-baselined prototype deliverable golden (may be a no-op diff if the scripted model writes fixed HTML)"
    - path: "backend/tests/agents/characterization/golden/od_prototype.html"
      provides: "Re-baselined od_prototype deliverable golden (may be a no-op diff)"
  key_links:
    - from: "backend/tests/agents/test_phase3_parity.py"
      to: "_drive('prototype') / _drive('od_prototype')"
      via: "offline scripted drive + final-output extraction"
      pattern: "_drive\\("
    - from: "backend/tests/agents/test_phase3_parity.py"
      to: "data-page IDs + routes keys"
      via: "regex on the produced deliverable matching _extract_html_skeleton's own regexes"
      pattern: "data-page"
    - from: "backend/tests/agents/test_phase3_token_delta_live.py"
      to: "RUN_LIVE_BEDROCK opt-in gate"
      via: "skip unless env set AND AWS creds resolve"
      pattern: "RUN_LIVE_BEDROCK"
---

<objective>
Prove semantic parity for the now-compacted prototype build and capture the real-world token
delta. Re-baseline the `prototype` + `od_prototype` deliverable byte-goldens via
`SNAPSHOT_UPDATE=1` (the sanctioned INV-3 0C exception — D-03), hold the `_normalize`d semantic
event snapshots green with no golden edit (Req 4), add a focused offline assertion that the
produced `data-page` ID set + `routes` map keys are identical before/after compaction and the
validation pass is equal-or-better (Req 6 / D-03), and add an opt-in live-gated test that
records the real multi-task-build token/cost delta into the phase SUMMARY as COMPACT-03
evidence (D-04).

Purpose: This is the parity + measurement increment for COMPACT-02 (gated on semantics, not
byte-identity) and the measured-reduction half of COMPACT-03 (real-run evidence). It depends on
the engine edit from 03-01 already being live.

Output: Re-recorded prototype/od_prototype goldens + a parity test + an opt-in live evidence
test, plus the recorded delta in the SUMMARY.
</objective>

<phase_goal>
**As a** Flowin engineer landing the one sanctioned non-byte-identical change, **I want to**
re-baseline the prototype deliverable goldens, keep the semantic snapshots green, and prove the
same pages/routes + equal-or-better validation, **so that** the compaction ships with semantic
parity (not byte-identity) and a recorded real-world token reduction as evidence.
</phase_goal>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-token-trim-measured-change-0c/03-SPEC.md
@.planning/phases/03-token-trim-measured-change-0c/03-CONTEXT.md
@.planning/phases/03-token-trim-measured-change-0c/03-PATTERNS.md
@backend/CLAUDE.md
@.planning/phases/03-token-trim-measured-change-0c/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the offline pages/routes + validation parity assertion</name>
  <files>backend/tests/agents/test_phase3_parity.py</files>
  <read_first>
    - backend/tests/agents/test_characterization_prototype.py — the deliverable-snapshot test shape (:36-42: `_drive("prototype")` then `extract_final_output(events)`) and the event-snapshot required-keys + `assert_seq_contiguous` pattern (:45-88) to mirror.
    - backend/tests/agents/test_characterization_od_prototype.py — `test_od_prototype_deliverable_byte_snapshot` (:37) + `test_od_prototype_event_snapshot` (:46) — the alias path to also drive.
    - backend/tests/agents/characterization/__init__.py — `extract_final_output` (:60-75) pulls the deliverable off `pipeline_complete.final_output`.
    - backend/tests/agents/_scripted_model.py — `_drive` signature (:325) + the od alias map `_OD_ALIAS_FOR_LOOKUP` (:354) so `_drive("od_prototype")` works; the fixed 84-byte scripted `prototype-build` HTML (:269-281) — note the produced deliverable has exactly one `data-page='dashboard'` section and no `const routes` map, so the parity assertion must be written against what the SCRIPTED build actually emits, not a hand-imagined multi-page doc.
    - backend/agents/execution_engine/engine.py — `_extract_html_skeleton`'s routes regex (`const routes\s*=\s*\{([^}]+)\}`, :2645) and data-page regex (`<section[^>]+data-page=["']([^"']+)["']`, :2652) so the parity test derives "page"/"route" identically to the helper.
    - .planning/phases/03-token-trim-measured-change-0c/03-SPEC.md — Requirement 6 (pages/routes + validation parity) + Requirement 4 (semantic snapshot) acceptance lines.
    - .planning/phases/03-token-trim-measured-change-0c/03-CONTEXT.md — D-03 (re-baseline + hold semantic snapshot + focused pages/routes assertion; ONLY two goldens may change).
    - .planning/phases/03-token-trim-measured-change-0c/03-PATTERNS.md — the "pages/routes + validation parity" section (reuse the helper's regexes; drive both prototype + od_prototype; equal-or-better validation = zero net-new failures).
  </read_first>
  <action>
    Create `backend/tests/agents/test_phase3_parity.py` (commit scope `tests`). Import `_scripted_model` so the offline env is set. Drive `events = await _drive("prototype")` and `_drive("od_prototype")` (the alias rides the same path). From each event stream, extract the deliverable via `extract_final_output(events)`; derive the `data-page` ID set using the SAME regex `_extract_html_skeleton` uses (`<section[^>]+data-page=["']([^"']+)["']`) and the `routes` map keys using the SAME routes regex (`const routes\s*=\s*\{([^}]+)\}`). Assert the produced `data-page` ID set is non-empty and equals the post-0B (pre-compaction) reference set — capture the expected set as an explicit literal derived from the committed `golden/prototype.html` (which the scripted model writes regardless of prompt, so it is the stable reference), NOT a freshly-driven "before" run (the engine edit is already live in this wave). Assert the `routes` key set matches the same reference. For validation parity, scan the event stream for validation-related events (static_check / render_check / validation outcomes surfaced in the `_drive` events) and assert zero net-new validation FAILURE events vs the reference (equal-or-better = the failure count does not increase). Mark async tests with `@pytest.mark.asyncio`. Keep the assertion robust to the scripted model's single-section output: if the scripted deliverable has exactly one `data-page` and no `const routes` block, assert exactly that stable shape (the falsifiable claim is "the set is unchanged", which still fails if compaction were to drop or rename the section). Use `pytest.skip` with a clear message ONLY if Chromium-backed render_check is unavailable for the render half, mirroring the existing render_check degrade-to-skip convention.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_phase3_parity.py -v</automated>
  </verify>
  <acceptance_criteria>
    - The test drives both `_drive("prototype")` and `_drive("od_prototype")` fully offline (no DB/Bedrock/API key) and is green.
    - It asserts the produced `data-page` ID set and `routes` key set equal the pre-0C reference set (derived from the committed golden), using the same regexes `_extract_html_skeleton` uses.
    - It asserts zero net-new validation-failure events vs the reference (validation pass equal-or-better).
    - The assertion is falsifiable: if the engine edit dropped or renamed a `data-page` section, the test would FAIL.
  </acceptance_criteria>
  <done>An offline parity test proves identical data-page IDs + routes keys and equal-or-better validation for both prototype and od_prototype, using the helper's own regexes.</done>
</task>

<task type="auto">
  <name>Task 2: Re-baseline prototype + od_prototype deliverable goldens; hold the semantic event snapshots green</name>
  <files>backend/tests/agents/characterization/golden/prototype.html, backend/tests/agents/characterization/golden/od_prototype.html</files>
  <read_first>
    - backend/tests/agents/characterization/__init__.py — `assert_deliverable_snapshot` (:78-108): under `SNAPSHOT_UPDATE` it WRITES the golden (after the non-empty guard); otherwise it asserts byte-for-byte. `SNAPSHOT_UPDATE` flag at :34.
    - backend/tests/agents/test_characterization_prototype.py / test_characterization_od_prototype.py — the deliverable tests to re-record + the event-snapshot tests to KEEP GREEN (no golden edit).
    - backend/CLAUDE.md — the `SNAPSHOT_UPDATE=1` regen recipe + commit scope `tests`.
    - .planning/phases/03-token-trim-measured-change-0c/03-SPEC.md — Requirement 5 (deliverable re-baseline; ONLY two goldens change) + Requirement 4 (event snapshots pass with no golden edit) acceptance lines.
    - .planning/phases/03-token-trim-measured-change-0c/03-PATTERNS.md — the "deliverable re-baseline" section AND its Caveat: the scripted build writes a FIXED single-section HTML regardless of prompt, so the re-baseline may be a NO-OP byte diff — that is expected; the real byte change is proven by the live delta (D-04), not the scripted golden.
  </read_first>
  <action>
    Run the sanctioned re-baseline (commit scope `tests`):
    `cd backend && SNAPSHOT_UPDATE=1 python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py -v`.
    Then inspect `git diff --name-only tests/agents/characterization/golden/`. EXPECT one of two outcomes and handle BOTH: (a) if the scripted deliverable bytes changed, the diff touches EXACTLY `golden/prototype.html` + `golden/od_prototype.html` — review the diff and keep it; (b) if the scripted model produced byte-identical HTML (the documented likely case — the scripted `prototype-build` writes a fixed 84-byte doc), the diff is EMPTY — that is acceptable and expected; record in the SUMMARY that the scripted golden is a no-op and the real byte change is evidenced by the live token-delta test (D-04). In EITHER case, confirm the diff does NOT touch `golden/prototype_revision.html`, `golden/od_ppt.html`, `golden/app_builder.txt`, or ANY `*.events.json` file. Do NOT pass `SNAPSHOT_UPDATE` to the event-snapshot tests — re-run them WITHOUT the env var to confirm `test_prototype_event_snapshot` + `test_od_prototype_event_snapshot` pass with no golden edit. Do NOT re-record any other pipeline's golden.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `test_prototype_deliverable_byte_snapshot` + `test_od_prototype_deliverable_byte_snapshot` pass against the (possibly re-recorded) goldens.
    - `test_prototype_event_snapshot` + `test_od_prototype_event_snapshot` pass with NO `*.events.json` golden edit; `assert_seq_contiguous` holds.
    - `git diff --name-only tests/agents/characterization/golden/` lists ONLY `prototype.html` and/or `od_prototype.html` (or is empty); it NEVER lists `prototype_revision.html`, `od_ppt.html`, `app_builder.txt`, or any `*.events.json`.
    - If the diff is empty (scripted no-op), the SUMMARY explicitly records that and points to the live delta (D-04) as the real byte-change evidence.
  </acceptance_criteria>
  <done>The prototype + od_prototype deliverable goldens are re-recorded (or confirmed no-op) with the diff scoped to exactly those two files; the semantic event snapshots pass with no golden edit; no other pipeline's golden moved.</done>
</task>

<task type="auto">
  <name>Task 3: Add the opt-in live token-delta evidence test and record the delta in the SUMMARY</name>
  <files>backend/tests/agents/test_phase3_token_delta_live.py</files>
  <read_first>
    - backend/tests/agents/test_deep_agent_runner_hitl_live.py — the opt-in/SSO gating to copy VERBATIM: the `_RUN_LIVE_HINT` message (:59-64), the `_aws_creds_resolve()` STS probe (:67+), and the dual gate "skip unless `RUN_LIVE_BEDROCK=1` AND creds resolve".
    - backend/CLAUDE.md — the data-flow note that token totals come from the runner's `usage` events; `usage_metadata` accumulation; commit scope `tests`.
    - backend/tests/agents/_scripted_model.py — `usage_metadata` shape (:128-135) and `_drive` (:325) for the offline harness reference (the live test runs a REAL model, not scripted, but mirrors the multi-task build shape).
    - .planning/phases/03-token-trim-measured-change-0c/03-SPEC.md — Requirement 3 (real-run token-delta evidence; NOT a CI gate) acceptance lines.
    - .planning/phases/03-token-trim-measured-change-0c/03-CONTEXT.md — D-04 (opt-in live-gated test logs the real token delta → SUMMARY; evidence-only; fallback = documented manual-run procedure if no live run available).
    - .planning/phases/03-token-trim-measured-change-0c/03-PATTERNS.md — the "opt-in live delta" section (two gates both required; log accumulated input_tokens with vs without compaction; copy delta + reproduction command into SUMMARY; NEVER a CI gate).
  </read_first>
  <action>
    Create `backend/tests/agents/test_phase3_token_delta_live.py` (commit scope `tests`). Copy the dual opt-in gate from `test_deep_agent_runner_hitl_live.py` verbatim: a module-level `_RUN_LIVE_HINT` string giving the exact run command (`aws sso login --profile personal-sso` then `RUN_LIVE_BEDROCK=1 AWS_PROFILE=personal-sso python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py -v -s`), an `_aws_creds_resolve()` STS probe, and a skip at the top of the test unless BOTH `os.environ.get("RUN_LIVE_BEDROCK") == "1"` AND `_aws_creds_resolve()` returns ok. Inside the test, run the multi-task prototype build against a REAL model TWICE: once with compaction ON (the live engine edit) and once with it forced OFF (temporarily monkeypatch the `is_build_task_2_plus` branch back to the full-HTML path, or set `_build_task_number` handling to force the full-HTML branch for the baseline run — whichever is the least-invasive way to drive the pre-compaction prompt). Accumulate `input_tokens` from the `usage` events for each run, compute the delta and percentage reduction, and `print(...)` them (run with `-s` to see output) plus the reproduction command. This test is EVIDENCE-ONLY and must NEVER be a CI gate (the CI gate is 03-01's >=50% test). After running it live (or, if no live run is available at execution time, after documenting the manual-run procedure), copy the measured token/cost delta figure + the reproduction command into `03-02-SUMMARY.md` under a COMPACT-03 evidence heading.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py -v</automated>
  </verify>
  <acceptance_criteria>
    - With no env set, the test SKIPS cleanly (default/CI path) printing `_RUN_LIVE_HINT` — it never runs a real model or fails in CI.
    - The skip requires BOTH gates: `RUN_LIVE_BEDROCK=1` AND `_aws_creds_resolve()` ok (a typo'd env or expired SSO still skips, never errors mid-model-call).
    - When run live, it logs accumulated `input_tokens` for compaction-on vs compaction-off and a computed reduction.
    - `03-02-SUMMARY.md` contains a concrete token/cost delta figure (a measured reduction) with the documented reproduction command; if no live run was available, it instead documents the exact manual-run procedure (D-04 fallback).
  </acceptance_criteria>
  <done>An opt-in, dual-gated live test logs the real multi-task-build token delta and skips cleanly in CI; the SUMMARY records the measured reduction (or the documented manual procedure) as COMPACT-03 evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → AWS Bedrock (live test only) | The opt-in live test makes a real model call when explicitly enabled. It uses the developer's own SSO creds via the existing `build_model()` Bedrock path — no new credential surface, identical to `test_deep_agent_runner_hitl_live.py`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | golden re-baseline diff | mitigate | The re-record is scoped by an explicit `git diff --name-only` check to ONLY `prototype.html`/`od_prototype.html`; no other pipeline's deliverable bytes or any `*.events.json` may change — verified before commit. |
| T-03-04 | Elevation of Privilege / cred misuse | live token-delta test (Bedrock) | accept | The live path is double-gated (`RUN_LIVE_BEDROCK=1` AND STS-probed creds) and uses the developer's own SSO session via the existing Bedrock factory; it is opt-in, never CI, and adds no new credential handling beyond the established live-test pattern. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — tests reuse existing harness imports (`_scripted_model`, characterization helpers, boto3 already in deps for the existing live test). No RESEARCH.md package-legitimacy gate applies. |

**Security analysis (per security_requirement):** No new persistent threats. The parity tests are
offline and read-only against the existing harness. The golden re-baseline is the sanctioned INV-3
0C change, scoped by a diff-name check to exactly two files. The only external-call surface is the
opt-in live token-delta test, which reuses the established double-gated SSO/Bedrock pattern and
never runs in CI — no new inputs, secrets handling, or exec surface.
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_phase3_parity.py -v` is green (offline).
- `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py -v` is green (deliverable + event snapshots).
- `git diff --name-only tests/agents/characterization/golden/` lists ONLY `prototype.html` and/or `od_prototype.html` (or is empty) — never `prototype_revision.html`, `od_ppt.html`, `app_builder.txt`, or any `*.events.json`.
- `cd backend && python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py -v` SKIPS cleanly with no env set (CI-safe).
- `cd backend && python3.11 -m pytest tests/agents/test_migration_ledger.py -v` stays green; L13 in the ledger is still `☐`.
- `03-02-SUMMARY.md` records the COMPACT-03 token/cost delta (or the documented manual procedure).
</verification>

<success_criteria>
- `prototype` + `od_prototype` deliverable byte-goldens are re-recorded (or confirmed no-op); `prototype_revision`/`od_ppt`/`app_builder` deliverable goldens are unchanged (COMPACT-02 / Req 5).
- `prototype` + `od_prototype` semantic event snapshots pass with no golden edits; `seq` contiguous (COMPACT-02 / Req 4).
- A test asserts identical `data-page` ID set + `routes` keys and equal-or-better validation pass (zero net-new failures) before/after compaction (COMPACT-02 / Req 6).
- The phase SUMMARY records a real multi-task build's token/cost delta (a measured reduction) with a documented reproduction (COMPACT-03 / Req 3).
- Migration ledger row L13 remains `☐`; the Phase-1 ledger ratchet stays green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-02)

**Re-baselined goldens (modify existing files; may be no-op diffs):**
- `backend/tests/agents/characterization/golden/prototype.html`
- `backend/tests/agents/characterization/golden/od_prototype.html`

**New test files:**
- `backend/tests/agents/test_phase3_parity.py`
- `backend/tests/agents/test_phase3_token_delta_live.py`

**New test functions (names are the executor's choice; suggested):**
- `test_prototype_pages_routes_parity` — data-page IDs + routes keys unchanged for prototype (COMPACT-02 / Req 6).
- `test_od_prototype_pages_routes_parity` — same for the od alias.
- `test_validation_pass_equal_or_better` — zero net-new validation failures.
- `test_token_delta_live` — opt-in, dual-gated live token-delta evidence (COMPACT-03 / Req 3).

**New fixtures/constants (test-local):**
- `_RUN_LIVE_HINT` (live-test run-command string) + `_aws_creds_resolve()` (STS probe), copied from `test_deep_agent_runner_hitl_live.py`.
- A `_REFERENCE_PAGE_IDS` / `_REFERENCE_ROUTE_KEYS` literal (or equivalent) derived from the committed golden, used as the pre-0C parity reference.

**Documentation artifact:**
- A COMPACT-03 evidence block in `03-02-SUMMARY.md` (the measured token/cost delta + reproduction command, or the documented manual-run procedure fallback).

**Explicitly NOT produced (Phase 7 / out of scope):** no `CompactionStrategy` capability, no manifest, no deletion of `_extract_html_skeleton`, no flip of ledger row L13.
</artifacts_produced>

<output>
Create `.planning/phases/03-token-trim-measured-change-0c/03-02-SUMMARY.md` when done.
</output>
