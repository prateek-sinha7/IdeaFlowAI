---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 05
type: execute
wave: 2
depends_on: ["22-02"]
files_modified:
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/workflow/CapabilityPaletteSection.test.tsx
autonomous: true
requirements: [SURF-01, SURF-03, EMP-02]
must_haves:
  truths:
    - "An embedded capability palette inside AgentsPopup fetches GET /api/capabilities at runtime and renders every capability kind, grouped by kind"
    - "Each palette row shows name, description, user_allowed/trust flag, security-gated flag, and a config-schema affordance — driven entirely by the fetched payload"
    - "user_allowed=False capabilities render visible-but-locked (engineer-only/upgrade affordance), never user-composable"
    - "The palette source carries NO hardcoded capability-name array (SC-001 grep target = 0)"
    - "Adding a capability to the registry (test fixture payload) makes it appear with no FE source edit"
    - "The composer shows a launchable workflow's declared per-step capabilities (SURF-03)"
  artifacts:
    - path: "frontend/src/components/workflow/AgentsPopup.tsx"
      provides: "an embedded grouped-by-kind capability palette section reusing the AgentModelPicker fetch shell; locked rows for user_allowed=False"
      contains: "getCapabilities"
    - path: "frontend/src/components/workflow/CapabilityPaletteSection.test.tsx"
      provides: "render test: grouped rows, locked rows for user_allowed=False, no-hardcoded-array, fixture-cap-appears"
  key_links:
    - from: "frontend/src/components/workflow/AgentsPopup.tsx palette"
      to: "GET /api/capabilities (api.ts getCapabilities)"
      via: "runtime fetch of palette.capabilities"
      pattern: "getCapabilities\\("
---

<objective>
Build the live-registry-driven capability palette EMBEDDED inside `AgentsPopup` (D-01) — one grouped-by-kind panel that renders EVERY capability kind from `GET /api/capabilities` (SURF-01), shows each workflow's declared capabilities (SURF-03), and renders `user_allowed=False` capabilities visible-but-locked (EMP-02 UI affordance) — reusing the `AgentModelPicker` fetch shell (D-02) with NO hardcoded capability-name array (SC-001).

Purpose: This cashes the SC-001 dividend at the UX layer — every registered capability becomes visible, sourced from the live registry (22-02's `description`/`security_gated`/`config_schema`). The palette is the surface SURF-01/03 and the EMP-02 lock affordance share. CRITICAL LANDMINE: do NOT recreate the standalone `CapabilityPalette.tsx` (P18/ISS-014 deleted it as an INV-12 dual-impl) — embed inside `AgentsPopup`.
Output: an embedded grouped palette section in `AgentsPopup`; locked rows for privileged caps; `CapabilityPaletteSection.test.tsx` (new) proving grouped rows + locked rows + no-hardcoded-array + fixture-cap-appears.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-PATTERNS.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-UI-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies these NEW surfaces (22-06's Advanced expander selects from the same palette data):
- `AgentsPopup.tsx` — an embedded "Capabilities" palette section (NOT a new top-level file/route): a fetch over `getCapabilities(jwt)` reusing the `AgentModelPicker.tsx:54-90` loading/error/empty/cancel state machine; consumes `palette.capabilities` (the 22-02 array with `description`/`security_gated`/`config_schema`), NOT `model_catalog`. Rows are grouped by the payload `kind` (group header title-cased from `kind` — no hardcoded kind list, SC-001). Each row: name (label) + description (body) + trust state + security-gated lock + a config-schema affordance (expandable iff `config_schema` non-empty). `user_allowed=false` → a recessed locked row (`bg-gray-50 border-gray-100`, `text-gray-400`, `Lock` icon, `cursor-not-allowed`, `aria-disabled="true"`, engineer-only/upgrade microcopy per the UI-SPEC copywriting contract).
- SURF-03 projection consumption: the composer shows the opened launchable workflow's declared per-step capabilities (from `GET /api/workflows/{id}` or the compiled-capability projection — check `app/api/workflows.py` `WorkflowSummary` during execution; if it lacks per-step caps, the projection is additive and registry-driven, but PREFER the existing `GET /api/workflows/{id}` if it already returns declared capabilities per API-01).
- `CapabilityPaletteSection.test.tsx` (NEW) — render the palette against a fixture `/api/capabilities` payload with all kinds; assert ≥1 row per kind, a locked row for a `user_allowed=False` cap, that a fixture-only capability appears (no FE source edit needed → SC-001), and the no-hardcoded-array grep.
- NOTE: the local FE helper `getCapabilities(agent: AgentDef)` at AgentsPopup.tsx:142 is a NAME COLLISION — the palette must call the API fetcher `getCapabilities` imported from `lib/api.ts`. Resolve the collision (alias the import or rename the local helper) without changing the local helper's behavior.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 — author the palette render test (grouped rows, locked rows, no-hardcoded-array, fixture-cap-appears)</name>
  <files>frontend/src/components/workflow/CapabilityPaletteSection.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentModelPicker.test.tsx (the analog render test + the getCapabilities mock pattern)
    - frontend/src/components/workflow/AgentModelPicker.tsx:54-146 (the fetch/loading/error/empty shell + the grouped-eyebrow + lever-row chrome to reuse)
    - frontend/src/lib/api.ts:512-525 (the `CapabilitiesPalette` interface — confirm 22-02 added `description`/`security_gated`/`config_schema`)
    - 22-UI-SPEC.md § Interaction & State Contract (palette) + § Copywriting Contract (locked-row + group-header + empty/loading/error copy) + § Accessibility (aria-disabled, role=group)
    - 22-VALIDATION.md Wave-0 (FE palette render test: no-hardcoded-array + grouped rows + locked rows)
  </read_first>
  <behavior>
    - Rendering the palette against a fixture payload with ≥1 capability per kind shows ≥1 row per kind, grouped under a per-kind header (title-cased from `kind`).
    - A `user_allowed=False` fixture capability renders a locked row (`Lock` icon + engineer-only/upgrade copy + `aria-disabled="true"`), non-selectable.
    - A fixture-only capability (a kind/name not referenced anywhere in FE source) renders → proves no hardcoded list (SC-001).
    - Loading/error/empty states match the `AgentModelPicker` copy from the UI-SPEC ("Loading capabilities…", "Failed to load capabilities.", "No capabilities available").
  </behavior>
  <action>
    Author `CapabilityPaletteSection.test.tsx` mocking `getCapabilities` (from `lib/api.ts`) to return a fixture `CapabilitiesPalette` with multiple kinds incl. at least one `user_allowed=False` cap and one fixture-only cap name. Assert grouped rendering (a header per kind, ≥1 row per kind), the locked-row affordance + `aria-disabled`, that the fixture-only cap appears, and the loading/error/empty copy. Include the no-hardcoded-array grep assertion as a separate test-or-script step: `grep -rnE "\[.*('strategy'|\"strategy\").*('validator'|\"validator\")" frontend/src/components/workflow/` → 0 in the palette source. RED until Task 2.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/workflow/CapabilityPaletteSection.test.tsx 2>&1 | tail -20 || echo "RED expected pre-Task-2"</automated>
  </verify>
  <acceptance_criteria>
    - The render test exists and binds: grouped rows, locked row for `user_allowed=False`, fixture-cap-appears, state copy.
    - The no-hardcoded-array grep assertion is present.
    - The test is RED before Task 2 (proves it binds behavior).
  </acceptance_criteria>
  <done>The palette render test exists, asserts grouped + locked + fixture-cap + no-hardcoded-array, and is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embed the grouped capability palette inside AgentsPopup (SURF-01/03 + EMP-02 lock) — GREEN</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentsPopup.tsx:1-50 (imports — note the local `getCapabilities(agent)` helper at :142 name collision) + :800-1000 (the "Advanced" section at :816 + the AgentModelPicker mount at :985 — where the palette section sits)
    - frontend/src/components/workflow/AgentModelPicker.tsx:54-146 (the fetch shell + grouped eyebrow + row chrome to reuse verbatim)
    - 22-RESEARCH.md § Pitfall 1 (do NOT recreate standalone CapabilityPalette.tsx — embed in AgentsPopup) + § Open Q2 (SURF-03 projection source)
    - 22-UI-SPEC.md § Component Inventory + § Spacing/Typography/Color (the dense inspector tokens: text-[10px]/[11px], gap-1.5, bg-gray-50 border-gray-100, #1B2A4A accent, Lock icon)
    - 22-PATTERNS.md § "AgentsPopup.tsx — palette + Advanced expander"
  </read_first>
  <action>
    Inside `AgentsPopup` (NOT a new component/route — embed, per D-01/Pitfall 1), add a "Capabilities" palette section using the `AgentModelPicker` fetch shell pattern: `getCapabilities(jwt)` (the API fetcher from `lib/api.ts` — resolve the name collision with the local `getCapabilities(agent)` helper at :142 by aliasing the import e.g. `getCapabilities as fetchCapabilities`, leaving the local helper untouched). Consume `palette.capabilities` (NOT `model_catalog`). Group rows by the payload `kind` (derive the group header by title-casing `kind` — NO hardcoded kind list; D-03). Render each row from the payload: name, description, the trust state, the `security_gated` lock, and a config-schema affordance (expandable only when `config_schema` is non-empty, reusing the skills/hooks sub-panel pattern). Render `user_allowed=false` rows as recessed locked rows per the UI-SPEC (D-04: Lock icon, engineer-only/upgrade copy, `cursor-not-allowed`, `aria-disabled="true"`, `role="group"`/`aria-label={kind}` on groups). For SURF-03, surface the opened launchable workflow's declared per-step capabilities from `GET /api/workflows/{id}` (or the compiled projection) — drive the display from the projection, not a hardcoded description. NO hardcoded capability-name array anywhere in the palette source (SC-001).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/workflow/CapabilityPaletteSection.test.tsx && npx tsc --noEmit && grep -rnE "\[.*('strategy'|\"strategy\").*('validator'|\"validator\")" src/components/workflow/AgentsPopup.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The palette render test passes (GREEN): grouped rows per kind, locked rows for `user_allowed=False`, fixture-cap-appears.
    - The no-hardcoded-array grep over the palette source returns 0 (SC-001); `npx tsc --noEmit` clean.
    - The palette is embedded in `AgentsPopup` — no new `CapabilityPalette.tsx`/`WorkflowComposer.tsx` file, no new top-level route (Pitfall 1): `ls src/components/workflow/CapabilityPalette.tsx 2>/dev/null` → absent.
    - SURF-03: the composer shows the opened workflow's declared per-step caps sourced from the compiled projection (not a hardcoded description).
  </acceptance_criteria>
  <done>An embedded grouped-by-kind palette renders every kind from the live registry with locked rows for privileged caps and no hardcoded list; SURF-03 shows declared caps; no standalone palette resurrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /api/capabilities` | authenticated fetch of the registry payload (the palette data source) |
| palette UI lock affordance | advisory only — the authoritative trust gate is server-side (22-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Elevation | a user composing a `user_allowed=False` capability by bypassing the FE lock | mitigate (defense-in-depth; authoritative gate is 22-04) | The palette renders privileged caps visible-but-locked + non-selectable (`aria-disabled`), but the FE lock is ADVISORY — the authoritative rejection is the server `_check_trust` at SAVE+LAUNCH (22-04, T-22-04-01/02). This plan's locked-row test asserts the affordance; 22-04 asserts the server backstop. |
| T-22-05-02 | (SC-001 erosion) | a hardcoded capability-name list creeping into the palette source | mitigate | The palette renders entirely from the fetched payload; the no-hardcoded-array grep → 0 (asserted); a fixture-only cap appears with no FE edit. |
| T-22-05-03 | Information Disclosure | the palette fetched without auth | mitigate | `getCapabilities` sends the JWT (the AgentModelPicker shell pattern); the endpoint enforces `Depends(get_current_user)` (22-02 / API-02); "Not authenticated." on a missing token. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/workflow/CapabilityPaletteSection.test.tsx` + `npx tsc --noEmit`.
- SC-001: `grep -rnE "\[.*('strategy'|\"strategy\").*('validator'|\"validator\")" frontend/src/components/workflow/` → 0; `ls frontend/src/components/workflow/CapabilityPalette.tsx` → absent (Pitfall 1).
- FE regression: `npm test -- --run src/components/workflow/AgentModelPicker.test.tsx` (the reused shell stays green).
</verification>

<success_criteria>
- [ ] An embedded palette inside `AgentsPopup` renders every capability kind from a live `GET /api/capabilities` fetch, grouped by kind, each showing name/description/trust/security-gated/config-schema.
- [ ] `user_allowed=False` caps render visible-but-locked (engineer-only/upgrade); SURF-03 shows a workflow's declared per-step caps.
- [ ] No hardcoded capability-name array in the palette source (SC-001 grep 0); a fixture cap appears with no FE edit.
- [ ] No standalone `CapabilityPalette.tsx` recreated (Pitfall 1); `npx tsc --noEmit` clean.
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-05-SUMMARY.md` when done.
</output>
