---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx
  - frontend/src/components/layout/DashboardLayout.catalogHome.test.tsx
  - backend/agents/workflows/manifest.py
  - backend/tests/agents/test_manifest.py
autonomous: true
requirements: [UXFIX-01, UXFIX-03, UXFIX-04]
must_haves:
  truths:
    - "The data-driven WorkflowCatalog is the default home landing; CreationHub.WORKFLOWS no longer drives the default landing"
    - "Each launchable workflow renders its authored display_name in the catalog (the P20 BE coalesce fix stays intact)"
    - "The generic mimetype-dispatched renderer is the PRIMARY deliverable dispatch path"
    - "The 4 first-party types (user_stories/app_builder/ppt/prototype) route through the same dispatch table and render identically (no visual regression)"
    - "A custom/unknown deliverable renders via the generic path as the primary route, not fallback-only"
  artifacts:
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "default landing renders WorkflowCatalog; CreationHub.WORKFLOWS no longer the default home"
    - path: "frontend/src/components/preview/PreviewPanel.tsx"
      provides: "generic-primary mimetype-dispatch table; first-party types are routed entries"
    - path: "backend/agents/workflows/manifest.py"
      provides: "authored display_name on launchable manifests (P20 field; BE coalesce fix intact)"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx default mainView"
      to: "WorkflowCatalog (GET /api/workflows)"
      via: "default landing mounts the data-driven catalog"
    - from: "frontend/src/components/preview/PreviewPanel.tsx dispatch"
      to: "GenericDeliverablePreview (mimetype-primary)"
      via: "mimetype-dispatch table routing first-party + generic"
---

<objective>
Land the three remaining UX data-faithfulness fixes: make the data-driven `WorkflowCatalog` the default home landing (UXFIX-03/D-20) so "no hardcoded name list" holds on the default view, render the authored `display_name` in the catalog (UXFIX-01/D-18, keeping the P20 BE coalesce fix), and make the generic mimetype renderer the PRIMARY deliverable dispatch path (UXFIX-04/D-21) so the SC-001 dividend isn't fallback-only.

Purpose: These close the v1.0 audit's UX warnings. UXFIX-03 resolves the confirmed home-vs-CreationHub dual surface (P20 SC-1). UXFIX-04 restructures `PreviewPanel` from "4 first-party branches + generic fallback" into a mimetype-dispatch table where the first-party types are routed entries — generic-primary, no visual regression. UXFIX-02 (22-03) supplies the persisted mimetype that drives reopen through this primary path.
Output: catalog-as-home in `DashboardLayout`; authored `display_name` on launchable manifests; generic-primary dispatch in `PreviewPanel`; `DashboardLayout.catalogHome.test.tsx` (new) + extended `PreviewPanel.genericDeliverable.test.tsx` + extended `test_manifest.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-PATTERNS.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-UI-SPEC.md
@.planning/phases/20-workflow-catalog-data-driven-browse-and-launch-gallery-reali/20-02-SUMMARY.md
@.planning/phases/18-prototype-and-deliverable-adherence/18-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies these surfaces:
- `DashboardLayout.tsx` — the default landing renders `WorkflowCatalog` (the data-driven `GET /api/workflows` gallery, already mounted at the `catalog` tab at :1084-1093) instead of `CreationHub` (:1070-1079); the default `mainView` initializer (:256) returns `"catalog"` (or the home block mounts `WorkflowCatalog`). The hardcoded `CreationHub.WORKFLOWS` array no longer drives the default landing (D-20).
- `manifest.py` — authored `display_name:` on the launchable manifests (the P20 field at manifest.py:75-76); leave the BE fallback null only where intentionally unauthored. Keep the P20 BE coalesce fix that lets the FE fallback work (UXFIX-01/D-18).
- `PreviewPanel.tsx` — restructure the dispatch (:557-570) from "4 first-party branches + `hasGenericDeliverable` fallback-last" into a mimetype-dispatch TABLE where the generic path is PRIMARY and the 4 first-party types (`user_stories`/`app_builder`/`ppt`/`prototype`) are registered routed entries the dispatcher routes to (D-21). Keep the CR-01 fix (`custom` already removed from the MarkdownPreview branch). NO visual regression — first-party outputs render byte-identically.
- `DashboardLayout.catalogHome.test.tsx` (NEW) — default `mainView === "catalog"`/catalog mount; CreationHub.WORKFLOWS no longer the default home.
- Extended `PreviewPanel.genericDeliverable.test.tsx` — custom deliverable via the generic-primary path + first-party types render identically (regression guard).
- Extended `test_manifest.py` (BE) — `display_name` authored where intended; BE fallback null only where intentionally unauthored.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the data-driven catalog the home landing + author display_name (UXFIX-03 + UXFIX-01)</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/layout/DashboardLayout.catalogHome.test.tsx, backend/agents/workflows/manifest.py, backend/tests/agents/test_manifest.py</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx:254-258 (the default `mainView` initializer returning "home") + :1068-1093 (the `home`=CreationHub block + the `catalog`=WorkflowCatalog block to make default)
    - frontend/src/components/layout/DashboardLayout.waveMount.test.tsx (the analog FE test for DashboardLayout)
    - backend/agents/workflows/manifest.py:75-76 (`display_name`/`user_launchable` fields — P20)
    - backend/app/api/workflows.py (the `WorkflowSummary` `display_name` population + the P20 BE coalesce fix to KEEP intact — UXFIX-01/D-18)
    - 20-02-SUMMARY.md (the P20 data-driven catalog + the WR-01 display_name fallback fix to preserve)
    - 22-SPEC.md UXFIX-01 + UXFIX-03 acceptance
  </read_first>
  <behavior>
    - The default landing (fresh load, no execution in flight) renders `WorkflowCatalog` (data-driven, `GET /api/workflows`), NOT `CreationHub`.
    - The hardcoded `CreationHub.WORKFLOWS` array no longer drives the default landing.
    - Each launchable workflow renders its authored `display_name` in the catalog; the BE fallback is null only where intentionally unauthored.
  </behavior>
  <action>
    In `DashboardLayout.tsx`, make the data-driven catalog the default landing: either change the default `mainView` initializer (:256) to `"catalog"`, or mount `WorkflowCatalog` in the `home` view block (:1070-1079) — choose the change that keeps the existing tab/nav semantics consistent (the catalog tab content becomes home; CreationHub is no longer the default mount). Preserve `handleSelectFeature`/`handleLaunchSaved` wiring (P21). Author `DashboardLayout.catalogHome.test.tsx` asserting the default landing renders `WorkflowCatalog` (catalog mount) and that `CreationHub.WORKFLOWS` no longer drives the default home. In `manifest.py`, author `display_name:` on the launchable manifests where a friendly name is intended (leave null only where deliberately unauthored); KEEP the P20 BE coalesce fix in `workflows.py` intact (do not re-introduce a coalesce that defeats the FE fallback). Extend `test_manifest.py` to assert `display_name` is authored where intended and the BE fallback is null only where intentionally unauthored.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/layout/DashboardLayout.catalogHome.test.tsx && npx tsc --noEmit && cd ../backend && python3.11 -m pytest tests/agents/test_manifest.py -k display_name -q</automated>
  </verify>
  <acceptance_criteria>
    - The default landing renders the data-driven `WorkflowCatalog` (sourced from `GET /api/workflows`); `CreationHub.WORKFLOWS` no longer drives the default home (test asserts the catalog mount).
    - Each launchable workflow renders its authored `display_name`; the BE fallback is null only where intentionally unauthored (test/assertion on the label source); the P20 coalesce fix is intact.
    - `npx tsc --noEmit` clean; `test_manifest.py -k display_name` green.
  </acceptance_criteria>
  <done>The data-driven catalog is the home landing showing friendly display_names; the hardcoded CreationHub.WORKFLOWS no longer drives the default view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the generic mimetype renderer the PRIMARY deliverable dispatch (UXFIX-04, no regression)</name>
  <files>frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx</files>
  <read_first>
    - frontend/src/components/preview/PreviewPanel.tsx:553-572 (the current dispatch — 4 first-party branches + `hasGenericDeliverable` fallback-last; the CR-01 note: `custom` already removed from MarkdownPreview — KEEP)
    - frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx + PreviewPanel.degraded.test.tsx + MarkdownPreview.security.test.tsx (the per-type render tests that must stay green — the regression guards)
    - frontend/src/components/preview/ (the `GenericDeliverablePreview` component built P18/ISS-021 — promote to primary)
    - 18-03-SUMMARY.md (the generic mimetype-dispatched renderer + the sandboxed-iframe security contract that must survive — sandbox="allow-scripts", NO allow-same-origin)
    - 22-PATTERNS.md § "PreviewPanel.tsx — UXFIX-04 generic-primary (D-21)"
  </read_first>
  <behavior>
    - A custom/unknown mimetype deliverable renders via the generic path as the PRIMARY route (not reached only after the 4 first-party branches fail).
    - The 4 first-party types (`user_stories`/`app_builder`/`ppt`/`prototype`) route through the same dispatch table and render identically (byte-identical output — no visual regression; the per-type tests stay green).
    - The P18 sandboxed-iframe security contract survives (text/html → `sandbox="allow-scripts"`, NO `allow-same-origin`).
  </behavior>
  <action>
    Restructure the `PreviewPanel` dispatch (:557-570) from "4 first-party branches + generic fallback-last" into a mimetype-dispatch table where the generic mimetype-dispatched renderer is the PRIMARY route and the 4 first-party types become registered entries the dispatcher routes to (D-21) — e.g. a dispatch map keyed on the deliverable mimetype/renderType where the first-party renderers are routed entries and the generic renderer is the default/primary path. Preserve the CR-01 fix (`custom` not routed to MarkdownPreview). Preserve the P18 sandboxed-iframe security contract. Drive the dispatch generically (mimetype-keyed, NOT a workflow-name branch — SC-001). Extend `PreviewPanel.genericDeliverable.test.tsx`: a custom deliverable renders via the generic path as primary; each of the 4 first-party types renders identically through the dispatch table (regression guard). The existing per-type tests (degraded, MarkdownPreview.security) must stay green.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/preview/PreviewPanel.genericDeliverable.test.tsx src/components/preview/PreviewPanel.degraded.test.tsx src/components/preview/MarkdownPreview.security.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The generic mimetype renderer is the PRIMARY dispatch (a custom deliverable renders via the generic path as the primary route, not fallback-only).
    - The 4 first-party types render identically through the dispatch table (no visual regression — the per-type tests stay green, incl. the sandboxed-iframe security test).
    - The dispatch is mimetype-keyed (SC-001 — no new workflow-name branch); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The generic mimetype-dispatched renderer is the primary deliverable dispatch path; the first-party types are routed entries rendering identically; the P18 security contract survives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deliverable content → renderer | untrusted deliverable bytes cross into the preview renderer (esp. text/html) |
| `GET /api/workflows` → catalog landing | the data-driven catalog list drives the default landing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07-01 | Tampering / XSS | a malicious text/html deliverable executing in the same origin via the generic renderer | mitigate | The P18 sandboxed-iframe contract is PRESERVED through the dispatch refactor — text/html renders in `sandbox="allow-scripts"` with NO `allow-same-origin` (asserted by `MarkdownPreview.security.test.tsx` staying green). The refactor must not weaken the sandbox. |
| T-22-07-02 | (visual regression / parity) | the dispatch restructure changing a first-party render | mitigate | The 4 first-party types route through the same table and render identically; the per-type render tests are the regression guard (must stay green). |
| T-22-07-03 | (SC-001 erosion) | a workflow-name branch in the dispatch table | mitigate | The dispatch is mimetype/renderType-keyed (generic), never a workflow name; SC-001 grep on `PreviewPanel.tsx` → 0. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/preview/ src/components/layout/DashboardLayout.catalogHome.test.tsx` + `npx tsc --noEmit`.
- `cd backend && python3.11 -m pytest tests/agents/test_manifest.py tests/unit/test_workflows_api.py -k display_name -q`.
- SC-001: `grep -rnE "== ['\"]prototype|== ['\"]ppt|spec\.id ==" frontend/src/components/preview/PreviewPanel.tsx` → only renderType comparisons routed via the generic table, no workflow-name kernel branch.
- Manual visual no-regression of the 4 first-party types is a deferred eyeball check (22-VALIDATION Manual-Only) — the unit regression tests are the offline gate.
</verification>

<success_criteria>
- [ ] The data-driven catalog is the home landing; `CreationHub.WORKFLOWS` no longer drives the default view.
- [ ] Each launchable workflow renders its authored `display_name`; the P20 BE coalesce fix is intact.
- [ ] The generic mimetype renderer is the PRIMARY dispatch; the 4 first-party types render identically (no regression); the P18 sandbox survives.
- [ ] SC-001: no new workflow-name branch; `npx tsc --noEmit` clean; FE + manifest tests green.
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-07-SUMMARY.md` when done.
</output>
