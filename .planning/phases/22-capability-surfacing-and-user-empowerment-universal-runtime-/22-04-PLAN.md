---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 04
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - backend/app/api/user_workflows.py
  - backend/app/api/websocket.py
  - backend/tests/unit/test_user_workflows_selections.py
  - backend/tests/agents/test_compiler.py
autonomous: true
requirements: [EMP-02, EMP-03, EMP-01]
must_haves:
  truths:
    - "A saved workflow persists a compact per-step capability-selections map in workflow_definition.manifest_json, owner-scoped"
    - "The selections round-trip: save then list then reopen shows the same selections"
    - "A cross-owner GET on a saved workflow returns 404 (IDOR, never 403/leak)"
    - "A save payload that injects a user_allowed=False capability is rejected server-side by compiling the selections with trust=user (CAP-03)"
    - "A launch re-validates the persisted selections with trust=user (a tampered row is rejected at launch, not only at save)"
    - "A user-selected validator + non-default model + retry, persisted and launched, demonstrably reach execution through the EXISTING run path (no new endpoint, no kernel branch)"
  artifacts:
    - path: "backend/app/api/user_workflows.py"
      provides: "save synthesizes a WorkflowManifest from the selections + compiles trust=user (CAP-03 backstop) + persists the compact map in manifest_json"
      contains: "trust=\"user\""
    - path: "backend/app/api/websocket.py"
      provides: "launch loads manifest_json + re-compiles selections trust=user before execution (no new run endpoint)"
      contains: "trust=\"user\""
    - path: "backend/tests/unit/test_user_workflows_selections.py"
      provides: "round-trip + IDOR→404 + save-reject + launch-reject + selection-reaches-execution tests"
  key_links:
    - from: "backend/app/api/user_workflows.py save"
      to: "backend/agents/workflows/compiler.py _check_trust (compiler.py:299-324)"
      via: "WorkflowCompiler.compile(manifest, registry, trust=\"user\")"
      pattern: "trust=\"user\""
    - from: "backend/app/api/websocket.py launch"
      to: "backend/agents/workflows/compiler.py _check_trust"
      via: "re-compile persisted selections trust=\"user\" before execute"
      pattern: "trust=\"user\""
---

<objective>
Activate the built-but-dormant `trust="user"` compile path as the server-side authoritative gate for user-composed workflows (EMP-02), persist the richer per-step capability selections in the reused `manifest_json` column owner-scoped (EMP-03), and prove a user-selected lever (validator + non-default model + retry) reaches execution through the EXISTING run path (EMP-01) — with NO new run endpoint and NO kernel workflow-name branch.

Purpose: This phase is the FIRST caller of `compiler.compile(trust="user")` (`_check_trust`, compiler.py:299-324 — fully built, BE-CMP-07 green, never invoked). Re-validating at BOTH save AND launch is the security boundary — the FE lock affordance (22-05) is advisory only. The selections persist via the dormant `manifest_json` column (zero migration, D-11). This is the backend half of EMP-01; the FE composer half is 22-06.
Output: save-side synth+compile(trust=user)+persist; launch-side re-compile(trust=user); `test_user_workflows_selections.py` (new) proving round-trip + IDOR→404 + save/launch rejection + selection-reaches-execution.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-PATTERNS.md
@.planning/phases/21-saved-workflows-user-authored-named-persisted-custom-workflo/21-01-SUMMARY.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-01-SUMMARY.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies these NEW symbols (the FE composer in 22-05/22-06 produces the selections this consumes):
- The compact selections map shape (D-11 / RESEARCH Open Q1 — settled compact map): `manifest_json` stores `{<agent_id>: {validators?: [...], gates?: [...], model?: <id>, retry?: {max_attempts: N}, ...}}` — a per-step (agent ≈ step) selections map. Document this shape in the save endpoint docstring (it is the contract the FE composer + launch synth both read).
- `user_workflows.py` — on save (create + the PATCH-when-selections-change path), synthesize a `WorkflowManifest` from `{base_pipeline_type, agent_ids, selections}` and call `WorkflowCompiler.compile(manifest, registry, trust="user")` (the CAP-03 server backstop — `_check_trust` rejects not-`user_allowed` refs); on success persist the compact selections map into `manifest_json` (owner-scoped row, reuse `_owned()` IDOR→404). Existing rows with `manifest_json IS NULL` mean "no selections" (parity).
- `websocket.py` — at the launch site (the `_run_pipeline` path that loads a saved row), load `manifest_json`, synthesize the manifest, and re-compile `trust="user"` BEFORE execute (re-validate a possibly-tampered row at launch, not only save — Pitfall 3). NO new run endpoint; the existing run path is extended with pure data (SC-001).
- `tests/unit/test_user_workflows_selections.py` (NEW) — round-trip (save→list→reopen same selections); cross-owner GET→404; save-side reject of a smuggled `user_allowed=False` cap; launch-side reject of a tampered row; the EMP-01 selection-reaches-execution proof (a persisted validator fires / chosen model resolves / retry activates under an injected transient fault, driven save→launch).
- Extended `tests/agents/test_compiler.py` — the `trust="user"` rejection cases (smuggled exec/spawn/security-gate/filesystem-MCP → `CompilerError` naming `(kind,name)`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Save-side — synthesize manifest, compile(trust=user) as CAP-03 backstop, persist selections in manifest_json</name>
  <files>backend/app/api/user_workflows.py, backend/tests/unit/test_user_workflows_selections.py, backend/tests/agents/test_compiler.py</files>
  <read_first>
    - backend/app/api/user_workflows.py (the full file: `_owned()` IDOR→404 at :155; `create_user_workflow` POST at :177; the PATCH path; `_validate_model_overrides` at :99; the `{base_pipeline_type, agent_ids, model_overrides}` save body at :62-91 — extend with `selections`)
    - backend/agents/workflows/compiler.py:299-324 (`_check_trust` — the CAP-03 rejection this is the FIRST `trust="user"` caller of) + the `WorkflowCompiler.compile(...)` signature + the `_compile_limits` untrusted ceiling at :279-289
    - backend/agents/workflows/manifest.py (the `WorkflowManifest` shape to synthesize from the selections + agent list)
    - backend/app/models/workflow_definition.py:34-45 (the dormant `manifest_json` column at :39 + owner_id/workspace_id at :36-37 — the persistence target, D-11)
    - 21-01-SUMMARY.md (the P21 owner-scoped CRUD + IDOR→404 conventions this EXTENDS, never forks)
    - 22-PATTERNS.md § "compiler.py — trust re-validation (EMP-02/03, D-12)" + § "workflow_definition.py — EMP-03 persistence (D-11)"
    - 22-RESEARCH.md § Open Q1 (compact selections map, synthesize manifest at save AND launch) + Pitfall 3
  </read_first>
  <behavior>
    - Save with a clean selections map (only `user_allowed=True` caps) → 201/200, `manifest_json` holds the compact map; reopen returns the same selections.
    - Save with a smuggled `user_allowed=False` capability (e.g. `gate: security`, `tool: filesystem-MCP`, an exec cap) → 4xx; the underlying `compile(trust="user")` raised `CompilerError` naming the `(kind, name)` (CAP-03).
    - Save with a Limits cap raising above the untrusted ceiling → 4xx (`_compile_limits` rejects raises for untrusted trust).
    - A cross-owner GET on a saved workflow → 404 (IDOR via `_owned()`, never 403/leak).
    - `model_overrides` continues to validate via the existing `_validate_model_overrides` two-check allow-list (no regression).
  </behavior>
  <action>
    Extend the save body (user_workflows.py:62-91) with an optional `selections: dict[str, dict] | None` (the compact per-step map, agent_id → {validators, gates, model, retry, ...}). In `create_user_workflow` (and the PATCH-when-selections-change path), synthesize a `WorkflowManifest` from `{base_pipeline_type, agent_ids, selections}` (the agent list is already in `agent_ids`; map each agent's selections onto its step), then call `WorkflowCompiler.compile(manifest, registry, trust="user")` BEFORE persisting — this is the CAP-03 server backstop (`_check_trust` rejects not-`user_allowed` refs; `_compile_limits` rejects ceiling raises). On compile success, persist the compact `selections` map into `manifest_json` on the owner-scoped row (reuse the existing `_owned()` / owner_id+workspace_id scope — IDOR→404 unchanged). Translate `CompilerError` into a 4xx with the offending `(kind,name)` message. Keep the existing `_validate_model_overrides` + `can_run_pipeline` + `SUPPORTED_PIPELINE_TYPES` predicates intact (no regression). Author `test_user_workflows_selections.py`: round-trip (save→list→reopen same selections), cross-owner GET→404, save-reject of a smuggled `user_allowed=False` cap, save-reject of a ceiling-raising Limits cap. Extend `test_compiler.py` with the `trust="user"` rejection cases for at least one privileged cap per family (exec / spawn_subagents / security-gate / filesystem-MCP).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py tests/agents/test_compiler.py -k "trust or user_allowed or selections or idor or round_trip" -q</automated>
  </verify>
  <acceptance_criteria>
    - Save persists the compact selections map in `manifest_json`; round-trip (save→list→reopen) returns identical selections.
    - A save payload smuggling a `user_allowed=False` capability is rejected (4xx) because `compile(trust="user")` raised `CompilerError` naming `(kind,name)` — asserted at the save endpoint (CAP-03).
    - A ceiling-raising Limits cap is rejected (`_compile_limits` untrusted ceiling).
    - Cross-owner GET → 404 (IDOR via `_owned()`); `_validate_model_overrides` still enforced.
  </acceptance_criteria>
  <done>Save synthesizes + compiles(trust=user) + persists selections owner-scoped; smuggled privileged caps and ceiling raises are rejected at save; IDOR→404.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Launch-side — re-compile persisted selections trust=user + prove the selection reaches execution (EMP-01)</name>
  <files>backend/app/api/websocket.py, backend/tests/unit/test_user_workflows_selections.py</files>
  <read_first>
    - backend/app/api/websocket.py:539-603 (the `run_pipeline` message dispatch — `agent_ids`/`model_overrides`/`gate_agent_ids` extraction + `can_run_pipeline`) and :1302-1340 (the `_run_pipeline` function the saved row launches through — the existing run path, NO new endpoint)
    - backend/app/api/websocket.py:95-153 (`_validate_model_overrides` — the launch-side untrusted-input mirror; the trust re-validation sits at the same boundary)
    - backend/agents/workflows/compiler.py:299-324 (`_check_trust`) — re-invoke `compile(trust="user")` at launch
    - backend/agents/execution_engine/engine.py:4485-4582 (the RESUME-02 retry wrapper gated on `step.retry.max_attempts>0` — the WIRE-02 consumer; 22-01 materializes retry) + model_policy.py:90-118 (ModelResolver tiers — the WIRE-01 consumer)
    - 22-01-SUMMARY.md (confirm model/retry/injects are now materialized by the compiler — the EMP-01 lever path)
    - 22-RESEARCH.md § Pitfall 3 (LAUNCH-side re-validation is mandatory — a tampered row must be rejected at launch, not only save)
  </read_first>
  <behavior>
    - Launching a saved row loads `manifest_json`, synthesizes the manifest, and re-compiles `trust="user"` before execute; a row tampered AFTER save (manifest_json mutated to reference a `user_allowed=False` cap) is REJECTED at launch (not executed).
    - A saved workflow with a selected validator + non-default model + retry, when launched, reaches execution: the chosen validator fires (a `validation_*` event / audit row), the chosen model is the one `ModelResolver` resolves, and retry activates under an injected transient fault.
    - The launch flows through the EXISTING `_run_pipeline` path — no new run endpoint, no `if pipeline_type ==`/`spec.id ==` branch added (SC-001).
  </behavior>
  <action>
    At the saved-workflow launch site in `websocket.py` (the `_run_pipeline` path that resolves a saved row's `{base_pipeline_type, agent_ids, model_overrides}` + now `manifest_json` selections), load the persisted selections, synthesize the same `WorkflowManifest` (reuse the save-side synth helper — do NOT duplicate the synthesis logic), and re-compile `WorkflowCompiler.compile(manifest, registry, trust="user")` BEFORE execute — rejecting a tampered row (Pitfall 3). Thread the compiled selections (which now carry the materialized `Step.model`/`Step.retry`/validators from 22-01) into the existing execute call — pure data, no new endpoint, no kernel branch (D-13 / SC-001). Author the EMP-01 integration proof in `test_user_workflows_selections.py` (the `compose_launch`/`user_workflow_launch` test): drive save→launch with a selections map declaring a validator + a non-default model + `retry:{max_attempts:N}`, inject a transient fault, and assert (a) the validator fired (event/audit row), (b) `ModelResolver` resolved the chosen model, (c) the retry wrapper activated. Add the launch-side rejection test (tamper `manifest_json` to a `user_allowed=False` cap → launch raises/rejects).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py -k "launch or compose_launch or reaches or tamper" -q && grep -nE "if pipeline_type ==|spec\.id ==" app/api/websocket.py | grep -v "run_pipeline\b" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Launch re-compiles persisted selections with `trust="user"`; a tampered row (manifest_json referencing a `user_allowed=False` cap) is REJECTED at launch (the EMP-02 launch-side acceptance).
    - The EMP-01 proof passes: a selected validator fires + the chosen model resolves + retry activates under an injected fault, driven save→launch through the existing run path.
    - No new run endpoint added; SC-001 grep over `websocket.py` shows no new workflow-name/`spec.id ==` branch on the routed launch path (the existing `run_pipeline` message-type dispatch is the message router, not a workflow-name branch).
  </acceptance_criteria>
  <done>Launch re-validates trust=user (tampered rows rejected); a user-selected validator+model+retry demonstrably reaches execution via the existing run path; no kernel branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client save payload → `manifest_json` persistence | untrusted user-composed selections cross into owner-scoped storage |
| persisted `manifest_json` → launch execution | a possibly-tampered stored row crosses into the run path |
| saved-workflow GET → another owner | cross-owner access attempt (IDOR) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Elevation | smuggled privileged capability (exec/spawn_subagents/security-gate/filesystem-MCP) in a save payload | mitigate | `compile(trust="user")` → `_check_trust` (compiler.py:299-324) raises `CompilerError` naming `(kind,name)` at SAVE; test asserts the save endpoint 4xx. NEVER trust the FE lock alone. Block-severity. |
| T-22-04-02 | Elevation | a row tampered AFTER save launching with a smuggled privileged cap | mitigate | Re-compile `trust="user"` at LAUNCH before execute (Pitfall 3); test asserts launch rejection of a tampered `manifest_json`. Block-severity. |
| T-22-04-03 | Elevation | ceiling-raising Limits in a user selections map | mitigate | `_compile_limits` (compiler.py:279-289) rejects raises above `_LIMITS_DEFAULT_CEILING` for untrusted trust; test asserts a ceiling-raise is rejected at save. |
| T-22-04-04 | Information Disclosure / Tampering | IDOR — cross-owner read/write of a saved workflow | mitigate | `_owned()` filters by owner → 404 (never 403/leak); cross-owner GET test asserts 404 (V4 access control; owner_id+workspace_id scope on the row). |
| T-22-04-05 | (architecture erosion / SC-001) | a kernel workflow-name branch added to wire the saved-row launch | mitigate | The launch flows through the existing `_run_pipeline` path with pure data; `test_banned_patterns.py` INV-1 grep (`if pipeline_type ==`/`spec.id ==`) → 0, CI hard-fail. |
| T-22-04-06 | Tampering | untrusted `model_overrides` in the save/launch payload | mitigate | The existing `_validate_model_overrides` two-check allow-list (agent_id ∈ run set + model_id allow-listed) stays enforced at both save (user_workflows.py:99) and launch (websocket.py:95). |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_user_workflows_selections.py tests/agents/test_compiler.py -q`.
- INV-3: `cd backend && python3.11 -m pytest tests/agents/test_characterization_{prototype,od_prototype,prototype_revision,od_ppt,app_builder}.py -q` (file-backed goldens compile with the default trusted path — unaffected; byte/event-identical).
- SC-001: `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -q` + `grep -nE "if pipeline_type ==|spec\.id ==" app/api/websocket.py agents/workflows/compiler.py` → 0 on routed paths.
- `lint-imports` 4/0; full targeted suite (~35s) on wave-merge.
</verification>

<success_criteria>
- [ ] Saved workflows persist a compact per-step selections map in `manifest_json` owner-scoped; selections round-trip (save→list→reopen).
- [ ] A smuggled `user_allowed=False` capability is rejected server-side at BOTH save AND launch (CAP-03, `_check_trust`); cross-owner GET → 404.
- [ ] A user-selected validator + non-default model + retry, saved and launched, demonstrably reaches execution through the existing run path (EMP-01) — no new endpoint, no kernel branch.
- [ ] INV-3 goldens byte-identical; SC-001 grep 0; lint-imports 4/0; zero new migrations (manifest_json reuse, D-11).
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-04-SUMMARY.md` when done.
</output>
