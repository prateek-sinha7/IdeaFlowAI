---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/registry.py
  - backend/app/api/capabilities.py
  - backend/tests/unit/test_capabilities_api.py
  - frontend/src/lib/api.ts
autonomous: true
requirements: [SURF-02]
must_haves:
  truths:
    - "Each registered capability self-describes a description + config_schema via the @register decorator (single source of truth in the registry)"
    - "GET /api/capabilities returns description + security_gated + a populated (per-capability) config_schema for every capability — additive, no removed keys"
    - "config_schema is no longer the literal {} stub; it is populated per capability for caps that take config and legitimately {} for caps that take none"
    - "The API-02 contract holds: Depends(get_current_user) auth + permission scopes + all existing response keys preserved"
  artifacts:
    - path: "backend/agents/capabilities/registry.py"
      provides: "_META dict sibling to _TRUST + description=/config_schema= kwargs on register() + a describe(kind,name) accessor"
      contains: "_META"
    - path: "backend/app/api/capabilities.py"
      provides: "CapabilityEntry extended with description + security_gated; the {} stub replaced by registry.describe(...)['config_schema']"
      contains: "security_gated"
    - path: "backend/tests/unit/test_capabilities_api.py"
      provides: "assertions that each entry carries description + security_gated + non-trivial config_schema where applicable, and the auth gate + existing keys remain"
  key_links:
    - from: "backend/app/api/capabilities.py _KNOWN loop"
      to: "backend/agents/capabilities/registry.py describe()"
      via: "registry.describe(kind, name) replacing the {} stub"
      pattern: "describe\\("
---

<objective>
Make `/api/capabilities` supply the display metadata the embedded palette needs — `description`, a derived `security_gated` flag, and a populated per-capability `config_schema` — sourced from the registry itself (D-08), additively (D-09/D-10), with the API-02 contract intact.

Purpose: The palette (22-05) renders entirely from this payload (SC-001 — no FE hardcoded metadata). The metadata MUST live in the registry (`@register` kwargs → `_META`), NOT in a static map in the API layer, so a newly-registered capability appears WITH metadata and no FE edit. This plan is the API-side foundation the FE palette consumes.
Output: `_META` + `describe()` on the registry; `CapabilityEntry` extended with `description` + `security_gated`; the `{}` stub at capabilities.py:116 replaced; extended `test_capabilities_api.py`; FE type follows.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies these NEW symbols (the palette in 22-05 + the FE types depend on them):
- `registry.py` — `_META: dict[tuple[str, str], dict] = {}` (sibling to `_TRUST` at registry.py:160); `register(kind, name, *, user_allowed=False, description: str = "", config_schema: dict | None = None)` records `_META[(kind, name)] = {"description": description, "config_schema": config_schema or {}}` in `_decorate` (registry.py:185-189); a `describe(self, kind, name) -> dict` accessor on `CapabilityRegistry` mirroring `is_user_allowed` (lazy-`discover()`-guarded, defaults `{"description": "", "config_schema": {}}` for unrecorded names).
- `capabilities.py` — `CapabilityEntry` gains `description: str = ""` and `security_gated: bool = False` (derived: `not user_allowed`, per RESEARCH discretion A2 — no separate stored boolean); the `config_schema={}` stub (capabilities.py:116) becomes `registry.describe(kind, name)["config_schema"]`, `description=registry.describe(...)["description"]`, `security_gated=not registry.is_user_allowed(kind, name)`.
- `frontend/src/lib/api.ts` — the `CapabilitiesPalette`/capability-entry TS interface (api.ts:512-525) gains `description: string`, `security_gated: boolean`, `config_schema: Record<string, unknown>` (type follows the BE additions; the palette in 22-05 reads these).
- A registry-driven metadata pass for the capabilities that take config (populate `config_schema=` on their `@register` decorators where the capability accepts config; leave `{}` where it takes none — D-09).
- Extended `tests/unit/test_capabilities_api.py` (195 lines, exists).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the registry with _META + describe() and populate config_schema/description on capabilities that take config</name>
  <files>backend/agents/capabilities/registry.py</files>
  <read_first>
    - backend/agents/capabilities/registry.py:158-192 (the `register()` decorator + `_TRUST`/`_decorate` — the EXACT mechanism to mirror for `_META`)
    - backend/agents/capabilities/registry.py:334-348 (`is_user_allowed` + the lazy `discover()` guard — the accessor pattern to mirror for `describe()`)
    - backend/agents/capabilities/registry.py:79-143 (`_KNOWN` — the ~60 (kind,name) pairs / ~20 kinds that will surface)
    - 22-PATTERNS.md § "registry.py — SURF-02 metadata seam (D-08)"
    - 22-RESEARCH.md § Pattern 1 (the registered seam) + Claude's Discretion (decorator kwargs settled; security_gated derived from user_allowed)
  </read_first>
  <behavior>
    - `register(kind, name, *, user_allowed=False, description="", config_schema=None)` records `_META[(kind,name)] = {"description": description, "config_schema": config_schema or {}}`.
    - `registry.describe("validator", "spec_plan_coverage")` returns `{"description": <non-empty>, "config_schema": <dict>}` for a capability that authored metadata.
    - `registry.describe(<unrecorded name>)` returns `{"description": "", "config_schema": {}}` (safe default — never KeyError).
    - `describe()` triggers `discover()` if not yet discovered (so `_META` is bound), mirroring `is_user_allowed`.
  </behavior>
  <action>
    Add `_META: dict[tuple[str, str], dict] = {}` beside `_TRUST` (registry.py:160). Extend the `register()` signature with `description: str = ""` and `config_schema: dict | None = None` (mirroring the existing `user_allowed` kwarg) and record into `_META` inside `_decorate` (registry.py:185-189). Add `describe(self, kind, name) -> dict` on `CapabilityRegistry` mirroring `is_user_allowed` (registry.py:336-346): lazy-`discover()` then `_META.get((kind, name), {"description": "", "config_schema": {}})`. Then populate `description=` and (where the capability accepts config) `config_schema=` on the `@register(...)` decorators of the in-repo capability impls — author concise one-line descriptions per capability and a JSON-Schema-lite `config_schema` object for caps that take config (D-09; most caps legitimately keep `{}`). Do NOT add a static `CAPABILITY_META` map anywhere (D-08 anti-pattern — that is a second hardcoded source eroding SC-001).
  </action>
  <verify>
    <automated>cd backend && python3.11 -c "from agents.capabilities.registry import CapabilityRegistry; r=CapabilityRegistry(); d=r.describe('validator','spec_plan_coverage'); assert 'description' in d and 'config_schema' in d, d; print('describe OK', d)"</automated>
  </verify>
  <acceptance_criteria>
    - `_META` exists as a sibling dict to `_TRUST`; `register()` accepts `description=`/`config_schema=`; `describe()` returns the dict (lazy-discover-guarded, safe default).
    - At least the config-taking capabilities carry a non-`{}` `config_schema` authored on their `@register` decorator; descriptions are authored (non-empty) for the capabilities the palette surfaces.
    - No static metadata map in `capabilities.py` or elsewhere in the API layer (D-08) — `grep -rn "CAPABILITY_META\|CAP_META" backend/app/api/` → 0.
  </acceptance_criteria>
  <done>The registry self-describes capability metadata via `@register` kwargs → `_META`; `describe()` reads it; the single-source-of-truth invariant holds (no API-layer map).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project description + security_gated + populated config_schema through /api/capabilities (additive) + FE type</name>
  <files>backend/app/api/capabilities.py, backend/tests/unit/test_capabilities_api.py, frontend/src/lib/api.ts</files>
  <read_first>
    - backend/app/api/capabilities.py:56-64 (`CapabilityEntry` — preserve ALL existing keys, add additively)
    - backend/app/api/capabilities.py:90-118 (the `@router.get` + `Depends(get_current_user)` auth gate + the `_KNOWN` loop with the `config_schema={}` stub at :116)
    - backend/tests/unit/test_capabilities_api.py (the 195-line existing contract test to extend — find the entry-shape + auth assertions)
    - frontend/src/lib/api.ts:512-525 (the `CapabilitiesPalette` interface + `getCapabilities` fetch — DRIFTED from CONTEXT 522-529)
    - 22-PATTERNS.md § "capabilities.py — SURF-02 projection (D-09/D-10)"
  </read_first>
  <behavior>
    - Each `CapabilityEntry` in the `GET /api/capabilities` response carries: `kind`, `name`, `user_allowed` (existing), PLUS `description` (from `registry.describe`), `config_schema` (populated from `registry.describe`, no longer literal `{}`), `security_gated` (derived: `not user_allowed`).
    - The auth dependency `Depends(get_current_user)` and the `model_catalog` projection are unchanged (additive — no removed keys, API-02 intact).
    - At least one config-taking capability returns a non-empty `config_schema`; at least one privileged capability (`user_allowed=False`) returns `security_gated=true`.
  </behavior>
  <action>
    Extend `CapabilityEntry` (capabilities.py:56-64) additively with `description: str = ""` and `security_gated: bool = False`. In the `_KNOWN` enumeration loop (capabilities.py:106-118) replace the `config_schema={}` stub (capabilities.py:116) with `config_schema=registry.describe(kind, name)["config_schema"]`, add `description=registry.describe(kind, name)["description"]` and `security_gated=not registry.is_user_allowed(kind, name)` (derive from `user_allowed`, per RESEARCH discretion A2 — no second stored source). Preserve the `model_catalog` skip and the auth gate verbatim. Extend `test_capabilities_api.py` to assert: every non-model_catalog entry carries `description` + `security_gated` + a `config_schema` key; at least one entry has a non-`{}` `config_schema`; a `user_allowed=False` entry has `security_gated=true`; the existing keys (`kind`/`name`/`user_allowed`) and the `Depends(get_current_user)` 401-without-auth behavior remain. In `api.ts`, extend the capability-entry TS interface (api.ts:512-525) with `description: string`, `security_gated: boolean`, `config_schema: Record<string, unknown>` to match the BE shape.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_capabilities_api.py -q && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test_capabilities_api.py` passes and asserts `description` + `security_gated` + populated `config_schema` per entry + the auth gate + existing keys (additive, no breaking change).
    - `frontend` `npx tsc --noEmit` is clean (the new fields are typed; only pre-existing unrelated TS errors remain).
    - The `config_schema={}` literal stub at capabilities.py:116 is removed (replaced by `registry.describe(...)`).
  </acceptance_criteria>
  <done>`/api/capabilities` supplies description + security_gated + populated config_schema additively, registry-sourced; the contract test proves it; the FE type matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → `GET /api/capabilities` | authenticated read of the capability registry projection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Information Disclosure | `/api/capabilities` leaking the registry to an unauthenticated caller | mitigate | `Depends(get_current_user)` auth dependency PRESERVED verbatim (API-02); `test_capabilities_api.py` asserts the auth gate (401 without a token) still fires. The extension is additive metadata only. |
| T-22-02-02 | Tampering | a second metadata source (static API map) drifting from the registry → a registered cap appears without metadata (SC-001 erosion) | mitigate | Metadata is registry-owned (`@register` → `_META`); the API projects via `registry.describe`. `grep -rn "CAPABILITY_META" backend/app/api/` → 0. Single source of truth (D-08). |
| T-22-02-03 | Information Disclosure | `config_schema`/`description` leaking secrets or PII | accept | Config schemas + descriptions are repo-authored capability config shapes — no secrets, no PII (V8 data protection; the registry holds no credentials). |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_capabilities_api.py -q`.
- Wave-merge gate: full targeted suite (~35s) + `lint-imports` 4/0 + SC-001 grep 0.
- `cd frontend && npx tsc --noEmit` clean.
</verification>

<success_criteria>
- [ ] Each capability entry carries `description` + `security_gated` + a populated `config_schema` (no `{}` stub for config-taking caps), additive over the existing keys.
- [ ] Metadata is registry-sourced (`@register` → `_META` → `describe`), with NO static API-layer map (D-08).
- [ ] API-02 contract intact: `Depends(get_current_user)` + permission scopes + existing keys preserved; `test_capabilities_api.py` green.
- [ ] FE capability type matches the BE shape; `npx tsc --noEmit` clean.
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-02-SUMMARY.md` when done.
</output>
