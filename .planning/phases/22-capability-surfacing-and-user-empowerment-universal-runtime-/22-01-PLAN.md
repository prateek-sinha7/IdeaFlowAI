---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/workflows/compiler.py
  - backend/agents/factory.py
  - backend/tests/agents/test_allowed_step_keys.py
  - backend/tests/unit/test_factory_injects.py
  - backend/tests/agents/test_compiler.py
autonomous: true
requirements: [WIRE-01, WIRE-02, WIRE-03]
must_haves:
  truths:
    - "A manifest declaring per-step model: changes the model ModelResolver resolves for that step"
    - "A manifest declaring top-level model: changes the workflow-default model"
    - "A manifest declaring per-step retry:{max_attempts:N} activates the RESUME-02 retry wrapper under an injected transient fault"
    - "A manifest declaring per-step injects: has those injects merged into the composed system prompt at the factory seam"
    - "No _ALLOWED_STEP_KEYS entry is accepted-but-dropped — each is reflected on the compiled Step/CompiledWorkflow or raises CompilerError"
    - "The 5 characterization goldens stay byte-identical and event-identical (INV-3)"
  artifacts:
    - path: "backend/agents/workflows/compiler.py"
      provides: "model=/retry=/injects= pass-through in the Step(...) constructor and model= in the CompiledWorkflow(...) constructor"
      contains: "model="
    - path: "backend/agents/factory.py"
      provides: "order-stable merge of step.injects with spec.injects (AGENT.md) at the _compose_system_prompt injection seam"
    - path: "backend/tests/agents/test_allowed_step_keys.py"
      provides: "parametrized test enumerating every _ALLOWED_STEP_KEYS entry asserting consumed-or-raises (D-17)"
  key_links:
    - from: "backend/agents/workflows/compiler.py Step(...)"
      to: "backend/agents/workflows/plan.py Step.model/retry/injects"
      via: "constructor kwarg pass-through"
      pattern: "Step\\(.*model="
    - from: "backend/agents/factory.py _compose_system_prompt"
      to: "compiled step.injects"
      via: "generic merge keyed on step.injects (no workflow-name branch)"
---

<objective>
Materialize the three accepted-but-dropped compiler step keys — `model:` (WIRE-01, top-level + per-step), `retry:` (WIRE-02, per-step), and `injects:` (WIRE-03, per-step, materialize-and-consume per D-16) — by passing them through the two compiler constructors and merging `step.injects` into the factory injection seam, then add the durable parametrized guard proving NO `_ALLOWED_STEP_KEYS` entry is silently dropped (D-17).

Purpose: Cash the v1.0 audit's latent-wiring finding — the fields exist on the dataclasses (`plan.py:362/368/369/408`), `ModelResolver` already reads tiers 2 & 4, and the RESUME-02 wrapper is already gated on `step.retry`; the compiler is the only gap. This is the backend foundation EMP-01 stands on (a user-selected non-default model + retry must reach execution).
Output: Compiler materialization for model/retry/injects; a generic factory merge for injects; `test_allowed_step_keys.py` (new) + extended `test_factory_injects.py` + extended `test_compiler.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-PATTERNS.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies these NEW symbols and surfaces (downstream plans + executors depend on them):
- `compiler.py` — `Step(...)` constructor (compiler.py:505-517) gains `model=`, `retry=`, `injects=` kwargs (coerced from `raw.get(...)`); `CompiledWorkflow(...)` constructor (compiler.py:231-244) gains `model=` (coerced from the manifest top-level `model:`).
- `factory.py` — a generic order-stable injects merge in `_compose_system_prompt` (factory.py:310-315): `effective = list(spec.injects) + [i for i in step_injects if i not in spec.injects]`, keyed on the compiled `step.injects` (NO workflow/agent-name branch — SC-001).
- `tests/agents/test_allowed_step_keys.py` (NEW) — the parametrized `_ALLOWED_STEP_KEYS` consumed-or-raises guard (D-17), analog `tests/agents/test_compiler.py`.
- Extended `tests/unit/test_factory_injects.py` (the merge consume side) and `tests/agents/test_compiler.py` (WIRE-01 model + WIRE-02 retry materialization).
NOTE: NO edit to `plan.py` — `Step.model`/`Step.retry`/`Step.injects`/`CompiledWorkflow.model` fields already exist (plan.py:362/368/369/408); the compiler simply stops dropping the kwargs.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the WIRE-03 parametrized guard + WIRE-01/02 compiler tests (Wave-0 RED gates)</name>
  <files>backend/tests/agents/test_allowed_step_keys.py, backend/tests/agents/test_compiler.py, backend/tests/unit/test_factory_injects.py</files>
  <read_first>
    - backend/tests/agents/test_compiler.py (the analog test module + existing fixtures/manifest builders)
    - backend/tests/unit/test_factory_injects.py (the existing AGENT.md injects merge test to extend)
    - backend/agents/workflows/compiler.py:60-90 (the `_ALLOWED_STEP_KEYS` frozenset — the parametrize source: agent, strategy, gates, hooks, validators, compaction, task_source, post_step, tools, model, fix, fanout, on_conflict, retry, injects, depends_on)
    - backend/agents/workflows/plan.py:358-372 + :405-412 (Step.model/retry/injects + CompiledWorkflow.model target fields)
    - 22-RESEARCH.md § "WIRE-03 Decision" (the INV-3-safety proof) + § Validation Architecture
  </read_first>
  <behavior>
    - test_allowed_step_keys.py: `@pytest.mark.parametrize("key", sorted(_ALLOWED_STEP_KEYS))` — for each key, compile a minimal manifest declaring that key with a representative value; assert EITHER the value is reflected on the compiled `Step`/`CompiledWorkflow` (consumed) OR a `CompilerError` is raised (rejected). NO key may both compile-clean AND be absent from the compiled object (accepted-but-dropped is the failure). Cover every current key incl. `depends_on`, `fix`, `tools` (document each key's consumed-or-raises disposition in the assertion).
    - test_compiler.py (WIRE-01): a manifest with per-step `model: {model: <id>}` → `compiled.steps[i].model.model == <id>`; a top-level `model: {model: <id>}` → `compiled.model.model == <id>`. A manifest with NO model: → `step.model is None` / `compiled.model` is the default `ModelPolicy` (parity).
    - test_compiler.py (WIRE-02): a manifest with per-step `retry: {max_attempts: 2}` → `compiled.steps[i].retry.max_attempts == 2`; no retry: → `step.retry is None` (wrapper dormant).
    - test_factory_injects.py (WIRE-03 consume): with `step.injects == []` the composed system prompt is byte-identical to the AGENT.md-only path (the goldens no-op proof); with `step.injects == ["craft"]` for an agent whose AGENT.md declares `["template"]`, the merged effective list is order-stable `["template", "craft"]` with no duplication.
  </behavior>
  <action>
    Author the three test additions as failing-first gates. The parametrized `test_allowed_step_keys.py` is the durable D-17 guard — drive it off `from agents.workflows.compiler import _ALLOWED_STEP_KEYS` so a future key addition automatically forces a consumed-or-raises decision. Build minimal manifests with the existing test helpers in `test_compiler.py` (do not hand-roll a new manifest builder). For the factory merge test, reuse the `test_factory_injects.py` AgentSpec/AgentContext fixtures and assert byte-identity for the empty-injects case (this IS the mechanical INV-3 proof per 22-RESEARCH.md WIRE-03 Decision step 5). These tests are RED until Task 2 lands the materialization.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_allowed_step_keys.py tests/agents/test_compiler.py -k "model or retry or allowed_step" tests/unit/test_factory_injects.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agents/test_allowed_step_keys.py` exists and is parametrized over `_ALLOWED_STEP_KEYS` (one case per key).
    - The new model/retry cases in `test_compiler.py` and the merge case in `test_factory_injects.py` exist and FAIL before Task 2 (RED), proving they bind the behavior.
    - No test asserts a loosened allow-list (INV-5: strict-key rejection preserved — the test asserts raises for non-allow-listed keys remains).
  </acceptance_criteria>
  <done>The three test additions exist; the model/retry/injects-merge behavior cases are RED; the parametrized key enumeration runs one case per `_ALLOWED_STEP_KEYS` entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize model/retry/injects in the two compiler constructors + merge injects at the factory seam (GREEN)</name>
  <files>backend/agents/workflows/compiler.py, backend/agents/factory.py</files>
  <read_first>
    - backend/agents/workflows/compiler.py:485-520 (the `Step(...)` constructor + the `fanout`/`on_conflict` materialization precedent to MIRROR — "declared-but-inert field made live")
    - backend/agents/workflows/compiler.py:228-245 (the `CompiledWorkflow(...)` constructor — no `model=` today)
    - backend/agents/workflows/plan.py:358-372 + :405-412 (the `ModelPolicy`/`RetryPolicy` field types to coerce raw dicts into)
    - backend/agents/factory.py:305-320 + :398-415 (the `_compose_system_prompt` injection seam + `_compose_injection`)
    - backend/agents/model_policy.py:90-118 (ModelResolver tiers 2/4 — the WIRE-01 consumer; confirm Step.model/CompiledWorkflow.model are what it reads)
    - 22-PATTERNS.md § "compiler.py — WIRE-01/02/03 materialization" + § "factory.py — WIRE-03 consume side"
  </read_first>
  <action>
    In `compiler.py`, mirror the existing `fanout`/`on_conflict` inert-field-made-live pattern (compiler.py:487-503): coerce `raw.get("model")` (WIRE-01 / D-14) → `ModelPolicy` (reuse the existing model-policy coercion helper if one exists in this file or in `plan.py`; do NOT invent a parallel parser), `raw.get("retry")` (WIRE-02 / D-15) → `RetryPolicy`, `raw.get("injects") or []` → `list[str]`, then pass `model=`, `retry=`, `injects=` into the `Step(...)` constructor (compiler.py:505-517). In the `CompiledWorkflow(...)` constructor (compiler.py:231-244) pass `model=` coerced from the manifest top-level `model:` (default to the existing `ModelPolicy` default when absent — parity). Do NOT touch `_ALLOWED_STEP_KEYS` (INV-5 — the keys are already allow-listed; only the drop is fixed). In `factory.py`, at the injection seam (factory.py:310-315) compute `effective = list(spec.injects) + [i for i in step_injects if i not in spec.injects]` where `step_injects` is read off the compiled step's context generically (NO workflow/agent-name literal — SC-001), and feed `effective` into `_compose_injection`. When `step_injects == []` this returns exactly `spec.injects` (the byte-identical goldens no-op). Confirm the engine threads the compiled `step.injects` to the factory at the existing seam; if a generic plumbing field is needed, add it without a name branch.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_allowed_step_keys.py tests/agents/test_compiler.py -k "model or retry or allowed_step" tests/unit/test_factory_injects.py -q && python3.11 -m pytest tests/agents/test_characterization_{prototype,od_prototype,prototype_revision,od_ppt,app_builder}.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 tests now PASS (GREEN): per-step + top-level model resolved; retry materialized; injects merge order-stable.
    - The 5 characterization goldens are byte-identical AND event-identical with NO `SNAPSHOT_UPDATE` (INV-3) — run them, never re-baseline.
    - `grep -nE "if pipeline_type ==|spec\.id ==|== \"prototype\"|== \"od_" agents/agents/factory.py agents/workflows/compiler.py 2>/dev/null` (and the routed paths) returns 0 — SC-001 (the injects merge keys on generic `step.injects`, never a name).
    - `_ALLOWED_STEP_KEYS` is unchanged (INV-5 — no key added/removed, no silent-accept introduced).
  </acceptance_criteria>
  <done>model/retry/injects are materialized by the compiler and consumed (ModelResolver / RESUME-02 wrapper / factory merge); goldens byte-identical; the D-17 parametrized guard is green; SC-001 grep 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest dict → compiled Step/CompiledWorkflow | authored manifest keys cross into typed plan objects |
| compiled step.injects → composed system prompt | step-declared injection names cross into the agent prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | DSL/control-flow smuggled into a step key | mitigate | `_ALLOWED_STEP_KEYS` strict-key rejection is PRESERVED unchanged (INV-5); WIRE-03 materializes the already-allow-listed `injects:`, it does NOT loosen the allow-list into silent-accept. The D-17 parametrized test asserts every key is consumed OR raises — proving no silent-accept path exists. |
| T-22-01-02 | Elevation | injects merge double-applies or reorders → prompt divergence (INV-3 erosion as a side channel) | mitigate | Order-stable merge `spec.injects + [i for i in step.injects if i not in spec.injects]`; goldens declare no per-step injects so `step.injects==[]` → exact `spec.injects` → byte-identical. Proven by the 5 characterization goldens (the mechanical proof). |
| T-22-01-03 | (architecture erosion / SC-001) | a workflow-name branch sneaks into the injects-merge wiring | mitigate | The merge keys on generic `step.injects`; `test_banned_patterns.py` INV-1 grep (`if pipeline_type ==`/`spec.id ==`) → 0, CI hard-fail. |
</threat_model>

<verification>
- Wave-merge gate: `cd backend && python3.11 -m pytest tests/agents/test_characterization_{prototype,od_prototype,prototype_revision,od_ppt,app_builder}.py tests/agents/test_migration_ledger.py tests/agents/test_banned_patterns.py -q` (expect 44 passed / 7 skipped).
- `lint-imports` (`/opt/homebrew/bin/lint-imports`) → 4 kept / 0 broken.
- SC-001 grep: `cd backend && grep -nE "if pipeline_type ==|spec\.id ==" agents/workflows/compiler.py agents/execution_engine/engine.py agents/factory.py` → 0 on routed paths.
- The full offline pytest hangs (Chromium/Bedrock/Postgres-gated) — use the targeted suite only (project memory).
</verification>

<success_criteria>
- [ ] A per-step + top-level `model:` changes the resolved model (test asserts resolved id); a per-step `retry:{max_attempts:N}` activates the RESUME-02 wrapper; per-step `injects:` merges order-stably.
- [ ] `tests/agents/test_allowed_step_keys.py` proves NO `_ALLOWED_STEP_KEYS` entry is accepted-but-dropped (each consumed or raises `CompilerError`).
- [ ] 5 characterization goldens byte-identical + event-identical (INV-3, no re-baseline).
- [ ] `_ALLOWED_STEP_KEYS` unchanged (INV-5); SC-001 grep 0; lint-imports 4/0.
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-01-SUMMARY.md` when done.
</output>
