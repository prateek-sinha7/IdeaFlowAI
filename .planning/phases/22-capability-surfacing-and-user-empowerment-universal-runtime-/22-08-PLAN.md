---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DECIDE-01, DECIDE-02]
must_haves:
  truths:
    - "ART-04 records keep-by-default retention (run artifacts retained indefinitely); run_ttl/days:N are opt-in overrides"
    - "The 3 stale 'confirm' labels REPO-02 (N6/N10), REPO-04 (N5/N7), FANOUT-05 (N2) are reconciled (no dangling 'confirm')"
    - "WAVE-03 (N8) is NOT touched — it is out of DECIDE-01 scope"
    - "MODEL-05 records premium-open-to-all-tiers (no per-tier premium gating) + the Haiku default + ordered fallback chain"
    - "The 17 new Phase-22 requirement families are registered in REQUIREMENTS.md with statements + phase-22 traceability rows"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ART-04 keep-by-default; MODEL-05 premium-open; 3 confirm labels reconciled; 17 new families + traceability rows"
      contains: "keep-by-default"
  key_links:
    - from: ".planning/REQUIREMENTS.md ART-04 / MODEL-05"
      to: "22-CONTEXT.md D-22 / D-23 (locked decisions)"
      via: "decision record edits"
---

<objective>
Record the two locked product decisions in REQUIREMENTS.md and register the 17 new Phase-22 requirement families so traceability holds and the coverage gates pass.

DECIDE-01 (D-22, N9): ART-04 → keep-by-default retention; reconcile the 3 stale "confirm" labels (REPO-02 N6/N10, REPO-04 N5/N7, FANOUT-05 N2) — and explicitly NOT WAVE-03 N8 (out of scope per RESEARCH Pitfall 4). DECIDE-02 (D-23, N11): MODEL-05 → premium-open-to-all-tiers + Haiku default + ordered fallback chain. (The FE picker tier-filter removal that DECIDE-02 also requires lands in 22-06 — FE file ownership.)

Purpose: These are execution deliverables that UPDATE the EXISTING ART-04/MODEL-05 entries and ADD the 17 new families (SURF/EMP/WIRE/UXFIX/DECIDE/LIVE) with traceability rows, so every Phase-22 requirement ID resolves in REQUIREMENTS.md.
Output: Edited ART-04 + MODEL-05; reconciled REPO-02/REPO-04/FANOUT-05; a new Phase-22 requirements section + 17 traceability rows.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan edits REQUIREMENTS.md only:
- ART-04 (REQUIREMENTS.md:49) rewritten to keep-by-default: "Default retention = keep (run artifacts retained indefinitely); `run_ttl`/`days:N` opt-in overrides (N9 — DECIDED keep-by-default, Phase 22 / DECIDE-01)". The trailing "(N9 — confirm)" is removed.
- MODEL-05 (REQUIREMENTS.md:64) rewritten to premium-open: "Global default stays Haiku with an ordered fallback chain on throttle/error; premium (`cost_class=premium`) models selectable by ALL tiers (no per-tier premium gating) (N11 — DECIDED premium-open-to-all-tiers, Phase 22 / DECIDE-02)". The "(N11 — confirm …)" is removed.
- REPO-02 (REQUIREMENTS.md:107) "confirm N6/N10" → reconciled (threshold decided/recorded, no dangling "confirm").
- REPO-04 (REQUIREMENTS.md:109) "confirm N5/N7" → reconciled.
- FANOUT-05 (REQUIREMENTS.md:123) "confirm N2" → reconciled.
- WAVE-03 (REQUIREMENTS.md:135) "confirm N8" → LEFT UNTOUCHED (out of DECIDE-01 scope — RESEARCH Pitfall 4).
- A NEW "### Capability Surfacing & User Empowerment (Phase 22)" requirements section with one `- [ ]` line per family (SURF-01/02/03, EMP-01/02/03/04, WIRE-01/02/03, UXFIX-01/02/03/04, DECIDE-01/02, LIVE-01) — a one-line statement each.
- 17 NEW traceability rows (`| <ID> | Phase 22 | Planned (22-NN) |`) in the Traceability table.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record DECIDE-01 + DECIDE-02 in ART-04 / MODEL-05 and reconcile the 3 stale confirm labels</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:49 (ART-04 — the DECIDE-01 target), :64 (MODEL-05 — the DECIDE-02 target)
    - .planning/REQUIREMENTS.md:107 (REPO-02), :109 (REPO-04), :123 (FANOUT-05) — the 3 in-scope confirm labels; :135 (WAVE-03) — OUT of scope, do NOT touch
    - 22-CONTEXT.md D-22 (keep-by-default) + D-23 (premium-open-to-all-tiers, Haiku default + fallback chain)
    - 22-RESEARCH.md § Pitfall 4 (DECIDE-01 scopes ONLY REPO-02/REPO-04/FANOUT-05; WAVE-03 N8 is wave-resume, unrelated to retention — leave it)
    - 22-SPEC.md DECIDE-01/DECIDE-02 acceptance criteria
  </read_first>
  <action>
    Rewrite ART-04 (REQUIREMENTS.md:49) to keep-by-default: default retention is KEEP (run artifacts retained indefinitely); `run_ttl`/`days:N` are opt-in overrides; tag it "(N9 — DECIDED keep-by-default, Phase 22 / DECIDE-01)" and remove the "(N9 — confirm)" dangler. Rewrite MODEL-05 (REQUIREMENTS.md:64): global default stays Haiku with an ordered fallback chain on throttle/error; premium (`cost_class=premium`) models selectable by ALL tiers (no per-tier premium gating); tag "(N11 — DECIDED premium-open-to-all-tiers, Phase 22 / DECIDE-02)" and remove the "(N11 — confirm …)" dangler. Reconcile REPO-02 (:107 "confirm N6/N10"), REPO-04 (:109 "confirm N5/N7"), FANOUT-05 (:123 "confirm N2") — replace each "— confirm Nx" with a settled disposition note so no "confirm" remains on those three lines. Do NOT edit WAVE-03 (:135) — its "(§21 / N8 — confirm)" is out of DECIDE-01 scope (RESEARCH Pitfall 4). Confirm no code/config retention default contradicts keep (search for a hardcoded 48h `run_ttl` default and note it if found; the spec says any retention default in code/config reflects keep — if a code default exists it is flagged for a follow-up, the doc decision is the deliverable here).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin && C=$(grep -nE "REPO-02|REPO-04|FANOUT-05" .planning/REQUIREMENTS.md | grep -c "confirm"); W=$(grep -n "WAVE-03" .planning/REQUIREMENTS.md | grep -c "confirm"); A=$(grep -c "keep-by-default" .planning/REQUIREMENTS.md); M=$(grep -c "premium-open-to-all-tiers" .planning/REQUIREMENTS.md); echo "in-scope-confirm-remaining=$C (want 0)  wave03-confirm-preserved=$W (want 1)  ART04-keep=$A (>=1)  MODEL05-premium=$M (>=1)"; test "$C" = "0" && test "$W" = "1" && test "$A" -ge 1 && test "$M" -ge 1 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - ART-04 records keep-by-default (TTL opt-in); MODEL-05 records premium-open-to-all-tiers + Haiku default + fallback chain.
    - The 3 in-scope "confirm" labels (REPO-02/REPO-04/FANOUT-05) carry no dangling "confirm"; WAVE-03's "confirm" is PRESERVED untouched.
    - The verify gate prints PASS (in-scope-confirm-remaining=0, wave03-confirm-preserved=1, ART04/MODEL05 recorded).
  </acceptance_criteria>
  <done>ART-04 = keep-by-default; MODEL-05 = premium-open; the 3 stale confirm labels reconciled; WAVE-03 N8 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Register the 17 new Phase-22 requirement families + traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:10-180 (the v1 requirements sections — the `### <Group> (Phase N)` + `- [x] **ID**: <statement>` format to mirror)
    - .planning/REQUIREMENTS.md:213-244 (the Traceability table — the `| Requirement | Phase | Status |` row format to mirror)
    - 22-SPEC.md § Requirements (the 17 one-line statements to copy faithfully) + § Acceptance Criteria
    - .planning/ROADMAP.md:676-699 (the Phase-22 requirement statements, authoritative)
  </read_first>
  <action>
    Add a new section "### Capability Surfacing & User Empowerment (Phase 22)" in the v1 Requirements area (after the Dynamic API / Frontend Contract section, before Budgets & Observability — the logical home, since SURF/EMP extend API-02/API-06), with one `- [ ] **<ID>**: <one-line statement>` line per family for all 17 IDs (SURF-01/02/03, EMP-01/02/03/04, WIRE-01/02/03, UXFIX-01/02/03/04, DECIDE-01/02, LIVE-01) — copy the statements faithfully from 22-SPEC.md § Requirements (do NOT paraphrase into a reduced scope; per the no-simplification rule each statement is the full requirement). Add 17 corresponding rows to the Traceability table: `| <ID> | Phase 22 | Planned (22-NN) |` where 22-NN is the owning plan (SURF-01/03→22-05; SURF-02→22-02; EMP-01→22-04/22-06; EMP-02→22-04/22-05; EMP-03→22-04; EMP-04→22-06; WIRE-01/02/03→22-01; UXFIX-01/03/04→22-07; UXFIX-02→22-03; DECIDE-01→22-08; DECIDE-02→22-06/22-08; LIVE-01→22-09). Keep the existing one-row-per-requirement convention.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin && for id in SURF-01 SURF-02 SURF-03 EMP-01 EMP-02 EMP-03 EMP-04 WIRE-01 WIRE-02 WIRE-03 UXFIX-01 UXFIX-02 UXFIX-03 UXFIX-04 DECIDE-01 DECIDE-02 LIVE-01; do n=$(grep -c "$id" .planning/REQUIREMENTS.md); test "$n" -ge 2 && echo "$id OK ($n)" || echo "$id MISSING ($n)"; done | tee /dev/stderr | grep -qc MISSING && echo "FAIL" || echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 17 IDs appear in REQUIREMENTS.md BOTH as a requirement-statement line AND a traceability row (each ID count ≥ 2).
    - Statements copy 22-SPEC.md faithfully (no scope reduction, no "v1/simplified/placeholder" language).
    - The Traceability table keeps one row per requirement; the verify gate prints PASS.
  </acceptance_criteria>
  <done>The 17 new families are registered with full statements + Phase-22 traceability rows; every Phase-22 requirement ID resolves in REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation-only plan) | No runtime trust boundary crossed; edits are to `.planning/REQUIREMENTS.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08-01 | Tampering | accidental edit to the out-of-scope WAVE-03 (N8) confirm label | mitigate | The verify gate asserts `wave03-confirm-preserved=1`; the action explicitly forbids touching REQUIREMENTS.md:135 (RESEARCH Pitfall 4). |
| T-22-08-02 | (scope reduction) | a registered family statement silently reduced below the SPEC | mitigate | Statements are copied faithfully from 22-SPEC.md § Requirements; the no-simplification rule is enforced (no "v1/simplified/placeholder"). |
</threat_model>

<verification>
- `grep -nE "REPO-02|REPO-04|FANOUT-05" .planning/REQUIREMENTS.md | grep -c confirm` → 0; `grep -n WAVE-03 .planning/REQUIREMENTS.md | grep -c confirm` → 1.
- Each of the 17 IDs resolves (statement + traceability row) — the Task-2 gate.
- No code/test runs (documentation-only); commit with `docs(22):`.
</verification>

<success_criteria>
- [ ] ART-04 = keep-by-default (TTL opt-in); MODEL-05 = premium-open-to-all-tiers + Haiku default/fallback chain.
- [ ] REPO-02/REPO-04/FANOUT-05 "confirm" labels reconciled; WAVE-03 N8 untouched.
- [ ] All 17 Phase-22 families registered with faithful statements + traceability rows.
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-08-SUMMARY.md` when done.
</output>
