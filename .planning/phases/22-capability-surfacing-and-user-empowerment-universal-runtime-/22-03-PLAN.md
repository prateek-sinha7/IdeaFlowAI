---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/workflow.py
  - backend/alembic/versions/0022_workflow_run_deliverable_mimetype.py
  - backend/tests/unit/test_deliverable_mimetype.py
  - frontend/src/types/index.ts
autonomous: true
requirements: [UXFIX-02]
must_haves:
  truths:
    - "deliverable_mimetype and deliverable_filename persist on the WorkflowRun row (additive nullable columns)"
    - "History-reopen drives the deliverable mimetype from the persisted value, not the deriveDeliverableMimetype text heuristic"
    - "A custom binary deliverable (e.g. application/zip) re-renders correctly on history-reopen using the persisted mimetype"
    - "The migration is additive only (no drop/alter-narrow); existing rows stay NULL and fall back to the heuristic (parity)"
    - "The 5 characterization goldens stay byte-identical (both keys remain in _VOLATILE_STRIP_KEYS) (INV-3)"
  artifacts:
    - path: "backend/app/models/workflow.py"
      provides: "deliverable_mimetype + deliverable_filename nullable columns on WorkflowRun"
      contains: "deliverable_mimetype"
    - path: "backend/alembic/versions/0022_workflow_run_deliverable_mimetype.py"
      provides: "additive migration 0022 (down_revision 0021) adding the two nullable columns"
      contains: "down_revision"
    - path: "frontend/src/types/index.ts"
      provides: "history-reopen reads the persisted mimetype, falling back to deriveDeliverableMimetype only for legacy NULL rows"
  key_links:
    - from: "backend/alembic/versions/0022_*.py"
      to: "backend/app/models/workflow.py WorkflowRun"
      via: "add_column(deliverable_mimetype/deliverable_filename)"
      pattern: "add_column"
    - from: "frontend/src/types/index.ts reopen path"
      to: "WorkflowRun.deliverable_mimetype (persisted)"
      via: "persisted-value-first, heuristic fallback"
---

<objective>
Persist `deliverable_mimetype` + `deliverable_filename` as additive nullable columns on `WorkflowRun` (D-19) via the single additive migration `0022`, and drive history-reopen from the persisted values so a custom BINARY deliverable (e.g. `application/zip`) re-renders faithfully instead of being re-sniffed from text.

Purpose: The engine already EMITS both keys (`engine.py:2117-2128`) and both are already in `_VOLATILE_STRIP_KEYS` — so persistence is INV-3-safe by construction. Today reopen re-derives the mimetype from output text (`types/index.ts:342`), which cannot recover a binary deliverable's true type. This closes the P18 reopen gap and feeds the generic-primary renderer (UXFIX-04, 22-07).
Output: Two nullable columns on `WorkflowRun`; migration `0022`; reopen reads the persisted value; extended `test_deliverable_mimetype.py` + a migration-additive assertion.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies these NEW symbols (UXFIX-04 in 22-07 consumes the persisted mimetype on reopen):
- `workflow.py` — `deliverable_mimetype = Column(String, nullable=True)` and `deliverable_filename = Column(String, nullable=True)` on `WorkflowRun` (beside the Phase-5 forward columns at workflow.py:54-62; run already carries `owner_id`/`workspace_id` scope at :58-59).
- `backend/alembic/versions/0022_workflow_run_deliverable_mimetype.py` (NEW) — `revision = "0022"`, `down_revision = "0021"`, `upgrade()` adds the two nullable columns via `op.batch_alter_table("workflow_runs")` (analog: `0021_saved_user_workflows.py`); `downgrade()` drops them. ADDITIVE ONLY, single-head chain.
- The write site: wherever the run row is finalized on `pipeline_complete`, persist the emitted `deliverable_mimetype`/`deliverable_filename` (the engine already emits them at engine.py:2117-2128) into the new columns.
- `frontend/src/types/index.ts` — the reopen path reads the persisted mimetype first; `deriveDeliverableMimetype` (types/index.ts:342) stays ONLY as the legacy-NULL fallback.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the two nullable WorkflowRun columns + additive migration 0022 + persist on finalize</name>
  <files>backend/app/models/workflow.py, backend/alembic/versions/0022_workflow_run_deliverable_mimetype.py, backend/tests/unit/test_deliverable_mimetype.py</files>
  <read_first>
    - backend/app/models/workflow.py:50-69 (the Phase-5 additive forward columns block — add the two new nullable columns BESIDE them; confirm owner_id/workspace_id at :58-59)
    - backend/alembic/versions/0021_saved_user_workflows.py (the full additive batch_alter migration to MIRROR — revision/down_revision/upgrade/downgrade shape)
    - backend/agents/execution_engine/engine.py:2117-2128 (the live `deliverable_mimetype`/`deliverable_filename` emit on pipeline_complete — the values to persist)
    - backend/tests/unit/test_deliverable_mimetype.py (the existing test that asserts the keys are in `_VOLATILE_STRIP_KEYS` — extend it)
    - backend/tests/agents/characterization/_normalize.py:95-141 (`_VOLATILE_STRIP_KEYS` — confirm both keys are present so goldens stay byte-identical)
    - 22-PATTERNS.md § "workflow.py + 0022_*.py — UXFIX-02 (D-19)"
  </read_first>
  <action>
    Add `deliverable_mimetype = Column(String, nullable=True)` and `deliverable_filename = Column(String, nullable=True)` to `WorkflowRun` (workflow.py, beside the Phase-5 forward columns at :54-62). Create `alembic/versions/0022_workflow_run_deliverable_mimetype.py` mirroring `0021_saved_user_workflows.py`: `revision = "0022"`, `down_revision = "0021"`, `upgrade()` uses `op.batch_alter_table("workflow_runs")` to `add_column` both nullable String columns, `downgrade()` drops them in reverse. ADDITIVE ONLY — no alter/drop/narrow of existing columns; single-head chain (0021 to 0022). Persist the emitted values at the run-finalize site: the engine already emits them at engine.py:2117-2128 — thread them into the row write (find the `pipeline_complete`/run-row-update site; if persistence is in a store/repo layer, write the columns there), keying generically on the emitted deliverable values, NOT a workflow name (SC-001). Run `alembic upgrade head` to confirm the migration applies. Extend `test_deliverable_mimetype.py` with a migration-additive assertion (the columns exist on the model; both keys remain in `_VOLATILE_STRIP_KEYS`).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_deliverable_mimetype.py -q && python3.11 -c "from app.models.workflow import WorkflowRun; cols={c.name for c in WorkflowRun.__table__.columns}; assert {'deliverable_mimetype','deliverable_filename'} <= cols, cols; print('cols OK')" && alembic upgrade head 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `WorkflowRun.__table__.columns` includes `deliverable_mimetype` + `deliverable_filename` (both nullable String).
    - `0022_workflow_run_deliverable_mimetype.py` exists with `down_revision = "0021"`; `alembic upgrade head` applies clean (single head); the migration is additive (no `drop_column`/`alter_column` of existing columns in `upgrade()`).
    - Both keys remain in `_VOLATILE_STRIP_KEYS`; `test_deliverable_mimetype.py` green.
    - The run-finalize write persists the emitted values keyed generically (SC-001 grep on the write site → 0 workflow-name branches).
  </acceptance_criteria>
  <done>The two nullable columns exist and persist the engine-emitted deliverable mimetype/filename; migration 0022 is additive and applies; goldens stay byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Drive history-reopen from the persisted mimetype (heuristic fallback only for legacy NULL)</name>
  <files>frontend/src/types/index.ts, backend/tests/unit/test_deliverable_mimetype.py</files>
  <read_first>
    - frontend/src/types/index.ts:342-356 (`deriveDeliverableMimetype(output)` — the text-sniff heuristic to demote to fallback)
    - 22-RESEARCH.md § "State of the Art" (the `deriveDeliverableMimetype` may REMAIN as a last-resort fallback for legacy NULL rows)
    - 18-03-SUMMARY.md (the shared `deriveDeliverableMimetype` helper imported by BOTH the page reopen block AND WorkflowHistory — both reopen surfaces must stay consistent)
    - 22-SPEC.md UXFIX-02 acceptance (a binary deliverable e.g. application/zip re-renders from the persisted mimetype)
  </read_first>
  <action>
    Update the history-reopen path so the deliverable mimetype is read FROM the persisted `deliverable_mimetype` field on the run record when present, and only falls back to `deriveDeliverableMimetype(output)` (types/index.ts:342) for legacy rows where the persisted value is NULL/absent. Keep the shared-helper consistency invariant from 18-03 (both the page reopen block and `WorkflowHistory` resolve the mimetype the same way — thread the persisted value through the same shared resolution so the two reopen surfaces cannot diverge). Surface the persisted `deliverable_mimetype`/`deliverable_filename` on the run-history record type so the FE can read them. Add a unit assertion (FE or BE-projection level) that when a persisted mimetype is `application/zip` the reopen resolution returns `application/zip` (NOT the text heuristic's guess).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run src/types 2>/dev/null | tail -15 || echo "no types test file — run the reopen/PreviewPanel suite"</automated>
  </verify>
  <acceptance_criteria>
    - The reopen mimetype resolution prefers the persisted `deliverable_mimetype` and uses `deriveDeliverableMimetype` only when it is NULL/absent (legacy parity).
    - A persisted `application/zip` mimetype yields `application/zip` on reopen (a binary deliverable recovers its true type — the UXFIX-02 acceptance).
    - Both reopen surfaces (page reopen + WorkflowHistory) resolve identically (the 18-03 shared-helper invariant holds); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>History-reopen renders a binary deliverable from the persisted mimetype; the text heuristic survives only as the legacy-NULL fallback; both reopen surfaces stay consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine emit → WorkflowRun row | the emitted deliverable mimetype/filename cross into owner-scoped persistence |
| persisted run row → FE reopen render | the persisted mimetype drives the deliverable renderer dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | a destructive/narrowing migration breaking existing run rows | mitigate | Migration `0022` is ADDITIVE only (two nullable columns via batch_alter), single-head (down_revision 0021), no alter/drop of existing columns; existing rows stay NULL → reopen falls back to the heuristic (parity). Verified by `alembic upgrade head` + the additive assertion. |
| T-22-03-02 | Information Disclosure | the persisted mimetype/filename leaking cross-owner | accept | The two columns live on `WorkflowRun`, which already carries `owner_id`/`workspace_id` scope (workflow.py:58-59) and is read through the existing default-deny scoped-query path (AUTHZ-02); no new read path is introduced. |
| T-22-03-03 | (INV-3 erosion via persistence side channel) | persisting the emitted keys diffing a golden | mitigate | Both `deliverable_mimetype`/`deliverable_filename` are already in `_VOLATILE_STRIP_KEYS` (normalized out of the event snapshot); the columns are nullable + populated post-emit. The 5 characterization goldens are run to confirm byte/event identity. |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_deliverable_mimetype.py -q` + `python3.11 -m pytest tests/unit/ -k migration -q`.
- INV-3: `cd backend && python3.11 -m pytest tests/agents/test_characterization_{prototype,od_prototype,prototype_revision,od_ppt,app_builder}.py -q` (byte/event-identical, no `SNAPSHOT_UPDATE`).
- `alembic upgrade head` applies clean (single head); manual review confirms `0022` is additive.
- `cd frontend && npx tsc --noEmit` clean; wave-merge gate full targeted suite + lint-imports 4/0 + SC-001 grep 0.
</verification>

<success_criteria>
- [ ] `deliverable_mimetype`/`deliverable_filename` persist on `WorkflowRun`; migration `0022` is additive and applies.
- [ ] History-reopen renders a binary deliverable (e.g. `application/zip`) from the persisted mimetype, not the text heuristic.
- [ ] 5 characterization goldens byte-identical (both keys stay in `_VOLATILE_STRIP_KEYS`); INV-3 holds.
- [ ] Additive-migration-only invariant holds (owner_id+workspace_id scope already on the run row); SC-001 grep 0.
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-03-SUMMARY.md` when done.
</output>
