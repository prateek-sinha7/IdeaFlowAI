---
phase: 22-capability-surfacing-and-user-empowerment-universal-runtime
plan: 06
type: execute
wave: 3
depends_on: ["22-04", "22-05"]
files_modified:
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/workflow/AgentModelPicker.tsx
  - frontend/src/components/workflow/IdeaInputPage.tsx
  - frontend/src/components/workflow/AdvancedExpander.test.tsx
  - frontend/src/components/workflow/AgentModelPicker.test.tsx
autonomous: true
requirements: [EMP-01, EMP-04, DECIDE-02]
must_haves:
  truths:
    - "Each agent row in AgentsPopup has an Advanced expander exposing validator + human/validation gate + non-default model + retry levers (agent ≈ step, D-05)"
    - "Selecting a lever feeds the per-step selections map that IdeaInputPage threads into the save payload (reaching the 22-04 persistence + run path)"
    - "Selecting a capability that requires a coupled gate auto-attaches the required gate inline (EMP-04) with the auto-attach notice"
    - "The model picker offers premium (cost_class=premium) models to ALL tiers — the user_allowed tier filter at AgentModelPicker.tsx:76 is removed (DECIDE-02)"
    - "An unselected lever emits no override (parity with the Default model semantics)"
  artifacts:
    - path: "frontend/src/components/workflow/AgentsPopup.tsx"
      provides: "per-agent Advanced expander with validator/gate/model/retry lever rows + EMP-04 auto-attach; selections reported upward"
      contains: "aria-expanded"
    - path: "frontend/src/components/workflow/AgentModelPicker.tsx"
      provides: "the user_allowed tier filter at :76 removed — premium models offered to all tiers"
    - path: "frontend/src/components/workflow/IdeaInputPage.tsx"
      provides: "threads the per-step selections map into the createUserWorkflow save payload (extends the P21 triple)"
  key_links:
    - from: "frontend/src/components/workflow/AgentsPopup.tsx Advanced expander"
      to: "the per-step selections map → IdeaInputPage save payload → 22-04 manifest_json"
      via: "onSelectionsChange reported upward (pure data, no new run endpoint)"
    - from: "frontend/src/components/workflow/AgentModelPicker.tsx"
      to: "palette.model_catalog (all tiers)"
      via: "drop the .filter((m)=>m.user_allowed)"
      pattern: "model_catalog"
---

<objective>
Add the per-agent "Advanced" expander to `AgentsPopup` (D-05, agent ≈ step) exposing the representative lever-set — a validator + a human/validation gate + a non-default model + retry (EMP-01) — with inline coupled-gate auto-attach (EMP-04), thread the selections into the save payload (reaching 22-04's persistence + the existing run path), and remove the `user_allowed` tier filter from the model picker so premium models are offered to all tiers (DECIDE-02).

Purpose: This is the FE half of EMP-01 — the user opts steps into user-allowed capabilities; the selections persist (22-04) and reach execution (22-01 materializes them; 22-04 launches them). EMP-04 auto-attaches a required coupled gate in the COMPOSER (the compiler stays the server backstop — it RAISES, never auto-injects). DECIDE-02 drops the one-line tier filter per D-23.
Output: a per-agent Advanced expander in `AgentsPopup`; the selections threaded through `IdeaInputPage`; the tier filter removed in `AgentModelPicker`; `AdvancedExpander.test.tsx` (new) + extended `AgentModelPicker.test.tsx`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-CONTEXT.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-RESEARCH.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-PATTERNS.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-UI-SPEC.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-04-SUMMARY.md
@.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates / modifies these NEW surfaces:
- `AgentsPopup.tsx` — a per-agent "Advanced" expander (collapsed by default; `ChevronRight`→`ChevronDown`; `aria-expanded`/`aria-controls`) reusing the relocated per-agent model picker location (P18/ISS-014, D-06). Lever rows in fixed order: Validator → Gate → Model → Retry (each reuses the gray lever-row chrome `bg-gray-50 border-gray-100 rounded-lg px-2.5 py-1.5`). Lever options come from the 22-05 palette data (the `user_allowed=True` caps of the relevant kinds). Selecting a lever updates a per-step selections map `{agent_id: {validators?, gates?, model?, retry?}}` reported upward via a new `onSelectionsChange` prop (pure data — no new run endpoint, SC-001). An unselected lever emits no override.
- EMP-04 auto-attach: when a selected cap requires a coupled gate (validation/approval), the composer adds/marks the required gate row inline and renders the auto-attach notice ("Added required {gate-name} gate — this capability needs it.", `role="status"`). The compiler coupling check stays the server backstop (it RAISES on a missing gate — never auto-injects; T-10-02-03 / RESEARCH anti-pattern).
- `IdeaInputPage.tsx` — threads the per-step selections map into the `createUserWorkflow` save payload as `selections` (extends the P21 `{base_pipeline_type, agent_ids, model_overrides}` triple — the EXACT shape 22-04 persists; pure data, `onRun` unchanged).
- `AgentModelPicker.tsx` — DROP the `.filter((m) => m.user_allowed)` at :76 so premium models are offered to ALL tiers (D-23); keep the "Default" no-override option + the "Defaults to Haiku." helper (UI-SPEC).
- `AdvancedExpander.test.tsx` (NEW) — expander toggle + lever selection feeds the selections map + EMP-04 auto-attach. Extended `AgentModelPicker.test.tsx` — premium model offered to a non-premium tier (filter removed).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 — Advanced-expander lever test (selection feeds the map) + EMP-04 auto-attach + DECIDE-02 picker test</name>
  <files>frontend/src/components/workflow/AdvancedExpander.test.tsx, frontend/src/components/workflow/AgentModelPicker.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentModelPicker.test.tsx (the analog; extend for DECIDE-02 premium-offered-to-non-premium-tier)
    - frontend/src/components/workflow/AgentsPopup.tsx:800-1000 (the Advanced section at :816 + the agent rows + the AgentModelPicker mount at :985 — the expander host)
    - 22-UI-SPEC.md § Interaction & State Contract (Advanced expander: collapsed default, fixed lever order Validator→Gate→Model→Retry, auto-attach inline notice) + § Copywriting (auto-attach copy, "Defaults to Haiku.") + § Accessibility (aria-expanded/aria-controls, role=status)
    - 22-VALIDATION.md Wave-0 (FE Advanced-expander test + EMP-04 auto-attach + DECIDE-02 picker-offers-premium)
  </read_first>
  <behavior>
    - Toggling "Advanced" on an agent row reveals lever rows in order Validator → Gate → Model → Retry; `aria-expanded` reflects state.
    - Selecting a validator + a gate + a non-default model + a retry value updates the per-step selections map for that agent; the map is reported via `onSelectionsChange`.
    - Selecting a cap that requires a coupled gate auto-attaches the required gate row + renders the auto-attach notice (`role="status"`).
    - An unselected lever emits no override (the map omits the key — parity with "Default").
    - The model picker offers a `cost_class=premium` model to a non-premium-tier user (the `user_allowed` filter is gone).
  </behavior>
  <action>
    Author `AdvancedExpander.test.tsx`: render the AgentsPopup composer (or the expander unit) with a fixture palette (reuse the 22-05 fixture shape), toggle Advanced, select each lever, assert the reported selections map and the EMP-04 auto-attach notice + the auto-attached gate row. Extend `AgentModelPicker.test.tsx`: with the tier filter removed, a premium model appears in the options for a non-premium tier user. RED until Task 2.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/workflow/AdvancedExpander.test.tsx src/components/workflow/AgentModelPicker.test.tsx 2>&1 | tail -20 || echo "RED expected pre-Task-2"</automated>
  </verify>
  <acceptance_criteria>
    - The expander test binds: toggle, lever selection → selections map, EMP-04 auto-attach notice + gate row, unselected-lever-no-override.
    - The picker test asserts a premium model is offered to a non-premium tier.
    - Both are RED before Task 2.
  </acceptance_criteria>
  <done>The expander + auto-attach + DECIDE-02 picker tests exist, bind the behavior, and are RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Advanced expander + auto-attach + thread selections + drop the tier filter (GREEN)</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx, frontend/src/components/workflow/AgentModelPicker.tsx, frontend/src/components/workflow/IdeaInputPage.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentsPopup.tsx:800-1000 (the Advanced section + agent rows + the AgentModelPicker mount + the existing `onModelOverridesChange` upward-report pattern at :40/:985 to mirror for `onSelectionsChange`)
    - frontend/src/components/workflow/AgentModelPicker.tsx:72-78 (the `.filter((m) => m.user_allowed)` at :76 to DROP — DECIDE-02/D-23)
    - frontend/src/components/workflow/IdeaInputPage.tsx (the `createUserWorkflow` save payload that sends `{base_pipeline_type, agent_ids, model_overrides}` — extend with `selections`; the P21 preload props + `onRun`)
    - 21-02-SUMMARY.md + 21-03-SUMMARY.md (the P21 save payload + launch preload — EXTEND, never fork; `onRun` stays pure data)
    - 22-RESEARCH.md § Anti-Pattern "Auto-injecting coupled gates in the compiler" (auto-attach happens in the COMPOSER; the compiler RAISES — EMP-04)
    - 22-UI-SPEC.md § Spacing/Typography/Color (the lever-row chrome + #1B2A4A accent + auto-attach pill tokens)
  </read_first>
  <action>
    In `AgentsPopup`, add a per-agent "Advanced" expander (collapsed by default, `ChevronRight`→`ChevronDown`, `aria-expanded`/`aria-controls`) at the relocated per-agent model-picker location (D-06). Render lever rows in fixed order Validator → Gate → Model → Retry, options sourced from the 22-05 palette data filtered to `user_allowed=True` caps of the relevant kinds (no hardcoded option list — SC-001). Maintain a per-step selections map `{agent_id: {validators?, gates?, model?, retry?}}`; report it upward via a new `onSelectionsChange` prop (mirror the existing `onModelOverridesChange` pattern). Implement EMP-04 (D-07): when a selected cap requires a coupled gate, add/mark the required gate row inline and render the auto-attach notice (`role="status"`, the UI-SPEC copy) — the composer auto-attaches; the compiler stays the server backstop. An unselected lever omits its key (parity with "Default"). In `AgentModelPicker.tsx`, DROP the `.filter((m) => m.user_allowed)` at :76 (offer all `model_catalog` entries to every tier — DECIDE-02). In `IdeaInputPage.tsx`, thread the selections map into the `createUserWorkflow` save payload as `selections` (the exact shape 22-04 persists), extending the P21 triple — `onRun` and the launch preload stay pure data (SC-001).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/workflow/AdvancedExpander.test.tsx src/components/workflow/AgentModelPicker.test.tsx src/components/workflow/IdeaInputPage.modelOverrides.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The expander tests pass (GREEN): toggle + lever selection → selections map; EMP-04 auto-attach renders the notice + the required gate; unselected lever omits its key.
    - The picker offers premium models to a non-premium tier (filter removed at :76); `AgentModelPicker.test.tsx` + the existing `IdeaInputPage.modelOverrides` regression stay green.
    - `IdeaInputPage` sends `selections` in the save payload (the 22-04 shape); `onRun`/launch preload unchanged; `npx tsc --noEmit` clean.
    - SC-001: `grep -rnE "if \(.*== ['\"]prototype|== ['\"]custom['\"]" frontend/src/components/workflow/AgentsPopup.tsx` shows no new workflow-name branch driving lever options.
  </acceptance_criteria>
  <done>Per-agent Advanced expander exposes validator/gate/model/retry; selections thread into the save payload; EMP-04 auto-attaches the coupled gate; premium models offered to all tiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer selections → save payload | user-composed lever selections cross into the persistence/run path (re-validated server-side in 22-04) |
| FE auto-attach (EMP-04) | composer-side convenience; the compiler coupling check is the authoritative backstop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Elevation | a user composing a privileged cap via the expander | mitigate (authoritative gate is 22-04) | The expander only offers `user_allowed=True` caps as options; privileged caps render locked in the palette (22-05). The FE is advisory — the authoritative rejection is `compile(trust="user")` at SAVE+LAUNCH (22-04). |
| T-22-06-02 | Tampering | a manifest missing a required coupled gate reaching the compiler | mitigate | EMP-04 auto-attaches the gate in the COMPOSER; the compiler's coupling check (compiler.py:458-465) RAISES on a missing gate as the server backstop (never auto-injects — RESEARCH anti-pattern). 22-04's compile(trust=user) enforces it. |
| T-22-06-03 | (SC-001 erosion) | a workflow-name branch driving the lever options | mitigate | Lever options derive from the palette payload (kind-keyed), not a workflow name; SC-001 grep on `AgentsPopup.tsx` → 0 new name branches. |
| T-22-06-04 | (DECIDE-02 regression) | dropping the tier filter accidentally exposing a non-`user_allowed` model | accept/mitigate | D-23 decision: premium IS open to all tiers; the dropped filter was `user_allowed` (premium models are `user_allowed=True`, just `cost_class=premium`). Non-`user_allowed` models are not in `model_catalog`'s offered set per MODEL-04. The picker still maps `model_overrides` through the server `_validate_model_overrides` allow-list (22-04). |
</threat_model>

<verification>
- `cd frontend && npm test -- --run src/components/workflow/AdvancedExpander.test.tsx src/components/workflow/AgentModelPicker.test.tsx src/components/workflow/IdeaInputPage.modelOverrides.test.tsx` + `npx tsc --noEmit`.
- SC-001: `grep -rnE "if \(.*== ['\"]prototype|spec\.id ==" frontend/src/components/workflow/AgentsPopup.tsx` → 0 new branches.
- FE regression sweep: the existing workflow component tests stay green.
</verification>

<success_criteria>
- [ ] Each agent row has an Advanced expander with validator + gate + non-default model + retry levers (agent ≈ step); selections thread into the save payload (the 22-04 shape).
- [ ] EMP-04 auto-attaches a required coupled gate inline (compiler stays the server backstop).
- [ ] The model picker offers premium models to ALL tiers (tier filter removed at :76 — DECIDE-02).
- [ ] SC-001: no new workflow-name branch; `npx tsc --noEmit` clean; FE regression green.
</success_criteria>

<output>
Create `.planning/phases/22-capability-surfacing-and-user-empowerment-universal-runtime-/22-06-SUMMARY.md` when done.
</output>
