---
phase: 15-live-pass-prompt-contract-closure
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - .planning/live-verification/PHASE-15-RECHECK.md
autonomous: true
requirements: [LV-02, F4-residual, F5-residual]
user_setup:
  - service: aws-bedrock
    why: "Live od_ppt re-check on real Haiku 4.5 (~$0.10, SC1 evidence) — only if the existing SSO session has expired"
    env_vars: []
    dashboard_config:
      - task: "If `aws sts get-caller-identity --profile default` (and `--profile hexaware-srini`) fails, run `aws sso login --profile <profile>` and complete the browser flow; region eu-central-1"
        location: "terminal + AWS SSO browser flow"
must_haves:
  truths:
    - "One live od_ppt run + one FE-exact od_ppt_output revision were executed with deck evidence (resolved final_output contains <section class=\"slide\">; revision returns a revised deck), OR an explicit SSO/quota deferral is recorded with the failing command output (D-05 locked fallback)"
    - "Criteria 2-3 (sdlc zero tool-XML preambles; infra /api/v1 adherence) have a recorded live disposition — ride-along evidence or named next-live-pass items (D-05)"
    - "The disposition record links the three findings (LV-02, F4-residual, F5-residual) back to REPORT-2026-06-12.md and cites the 15-02 offline gate as the offline evidence"
  artifacts:
    - path: ".planning/live-verification/PHASE-15-RECHECK.md"
      provides: "Phase-15 live re-check evidence / deferral disposition record (ROADMAP SC5)"
      contains: "Disposition:"
  key_links:
    - from: ".planning/live-verification/PHASE-15-RECHECK.md"
      to: ".planning/live-verification/REPORT-2026-06-12.md"
      via: "finding-id closure linkage"
      pattern: "LV-02"
---

<objective>
Execute the cheap live re-check per D-05 (locked): one live od_ppt run + one FE-exact `od_ppt_output` revision on real Haiku 4.5 (~$0.10) proving the resolved final_output is a deck and the revision returns a revised deck (ROADMAP SC1 live half / SC5) — or, if SSO/quota-blocked, record an explicit deferral. Record criteria 2-3 live dispositions (ride the session or next-live-pass items — D-05 permits either). Self-drive everything; never ask per-test confirmation; only surface real failures.

Decision IDs map to `15-CONTEXT.md ## Implementation Decisions` in order; this plan implements D-05 (and the live halves of D-01/D-02/D-03 evidence).

Purpose: ROADMAP SC5 — "record disposition either way" — closes the milestone-end live loop without blocking phase completion on live-env availability (defer-live-verification convention).
Output: `.planning/live-verification/PHASE-15-RECHECK.md` + saved live deck artifacts (when the live path runs).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-live-pass-prompt-contract-closure/15-CONTEXT.md
@.planning/phases/15-live-pass-prompt-contract-closure/15-02-SUMMARY.md
@.planning/live-verification/REPORT-2026-06-12.md
</context>

## Artifacts this phase produces (plan 03 share)

| Symbol | Where |
|--------|-------|
| `.planning/live-verification/PHASE-15-RECHECK.md` | new disposition record (SC5) |
| `.planning/live-verification/artifacts/phase15-od-ppt-final.html` | saved live resolved deck (live path only) |
| `.planning/live-verification/artifacts/phase15-od-ppt-revision.html` | saved live revised deck (live path only) |

<tasks>

<task type="auto">
  <name>Task 1: Attempt the live od_ppt run + FE-exact revision (SC1 evidence, D-05)</name>
  <files>.planning/live-verification/artifacts/phase15-od-ppt-final.html, .planning/live-verification/artifacts/phase15-od-ppt-revision.html (live path only — skipped on deferral)</files>
  <read_first>
    - .planning/live-verification/REPORT-2026-06-12.md — sections "LV-02" (the failure shape to disprove: 1,350-char QA narration resolved instead of the 21,394-char deck) and the run-procedure/evidence conventions of the 2026-06-12 pass
    - /tmp/flowin-live-pass2.py and /tmp/flowin-live-launcher.py IF still present (the 2026-06-12 driver patterns — preferred procedure); if absent, reconstruct from live_harness
    - backend/tests/agents/live_harness.py — `drive_engine_pipeline` line 484: `model=None` is the LIVE Bedrock path (no patch; `create_runner` → `build_model()` on the default profile); `CaptureResult.final_output` is the resolved deliverable string
    - .planning/phases/15-live-pass-prompt-contract-closure/15-CONTEXT.md — "Live re-check (LOCKED)" + Specific Ideas (what the live evidence looked like on 2026-06-12)
  </read_first>
  <action>
    Self-drive; do not ask for per-step confirmation (only surface real failures). Budget ~$0.10 on Haiku 4.5, region eu-central-1.

    1. Credential check: `aws sts get-caller-identity --profile default` (on failure try `--profile hexaware-srini`). If BOTH fail, or the first live call throws an SSO/expired-token or quota/Throttling error that a single retry does not clear: take the DEFERRAL branch — capture the exact failing command + error output verbatim for Task 2, do NOT fabricate evidence, do NOT loop retries (D-05 locks deferral as acceptable; record disposition either way).

    2. LIVE branch — od_ppt run: prefer the 2026-06-12 driver procedure (/tmp/flowin-live-pass2.py via /tmp/flowin-live-launcher.py) if those files still exist; otherwise drive from backend/ with python3.11 using tests/agents/live_harness.drive_engine_pipeline("od_ppt", model=None) — supply the same od_context seed dict the harness uses for od_ppt (template_body/template_id/ds_id/ds_body/craft_block/is_design_system_required — see live_harness.py ~line 570) and a short deck brief. Save `result.final_output` to .planning/live-verification/artifacts/phase15-od-ppt-final.html and record result.run_id + token totals.

    3. LIVE branch — FE-exact od_ppt_output revision: against the run from step 2 (parent_run_id chain), issue one revision exactly as the FE does — follow the 2026-06-12 pass's revision procedure recorded in REPORT-2026-06-12.md (the Phase-14 SC4 FE-exact `od_ppt_output` revision message over the live stack) with a one-line revision instruction (e.g. change the deck title). Save the resolved revision output to .planning/live-verification/artifacts/phase15-od-ppt-revision.html.

    4. Evidence checks (LIVE branch): on BOTH saved files — `grep -c '<section class="slide"'` >= 1 and the file starts with `<!DOCTYPE` (after leading whitespace); the revision file is a deck, not narration, and differs from the parent deck (`cmp -s` returns non-zero). Note in passing whether the validator stream contained QA commentary before the single artifact (expected and allowed by the contract).

    5. Criteria 2-3 (sdlc-governance zero `<function_calls>`/`<invoke>` preambles; infra `/api/v1` adherence) require live app_builder/migration pipeline runs — DEFAULT: record them as next-live-pass items per D-05 (locked deferral path). Do not start an app_builder live run for this plan.
  </action>
  <verify>
    <automated>test -f .planning/live-verification/artifacts/phase15-od-ppt-final.html && test "$(grep -c '<section class="slide"' .planning/live-verification/artifacts/phase15-od-ppt-final.html)" -ge 1 && test -f .planning/live-verification/artifacts/phase15-od-ppt-revision.html && ! cmp -s .planning/live-verification/artifacts/phase15-od-ppt-final.html .planning/live-verification/artifacts/phase15-od-ppt-revision.html</automated>
  </verify>
  <acceptance_criteria>
    - LIVE branch: both artifact files exist; each contains `<section class="slide"` (>= 1) and begins with `<!DOCTYPE` after leading whitespace; `cmp -s final revision` exits non-zero (revision actually revised); run ids + token totals captured for Task 2
    - DEFERRAL branch (only if creds/quota block per step 1): no artifact files fabricated; the exact failing command and verbatim error output are captured for Task 2; Task 2's record carries `Disposition: DEFERRED`
    - in BOTH branches: no engine/prompt/test file modified by this task (`git status --porcelain backend/` prints nothing)
    - criteria 2-3 were NOT chased with an app_builder live run (cost fence)
  </acceptance_criteria>
  <done>SC1 live evidence captured (deck + revised deck on real Haiku), or a precise deferral basis exists for the disposition record.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase-15 live re-check disposition record (SC5, D-05)</name>
  <files>.planning/live-verification/PHASE-15-RECHECK.md</files>
  <read_first>
    - .planning/live-verification/REPORT-2026-06-12.md — heading/disposition conventions to mirror (finding ids, evidence tables)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-02-SUMMARY.md — the offline gate evidence to cite (contract pins + LV-02 composition test + byte-identical goldens)
  </read_first>
  <action>
    Create `.planning/live-verification/PHASE-15-RECHECK.md` with these sections (mirror REPORT-2026-06-12.md conventions; no AWS account ids, no credentials, no SSO URLs — run ids and excerpts only):

    1. Header: date, model (Haiku 4.5), profile name + region, phase pointer (Phase 15), cost estimate.
    2. `## LV-02 (SC1)` — one line per check with `Disposition: CLOSED (live)` on the live branch: od_ppt run id, resolved final_output byte length, `<section class="slide">` count, first-line excerpt; revision run id, confirmation the revision output is a revised deck (what changed). On the deferral branch instead: `Disposition: DEFERRED — <verbatim reason: sts/SSO/quota error>` plus the named follow-up ("re-run SC1 live check on next live pass"). EITHER WAY also cite the offline evidence: 15-02's LV-02 composition pin + contract pins green, goldens byte-identical.
    3. `## F4-residual (SC2)` and `## F5-residual (SC3)` — `Disposition: NEXT-LIVE-PASS` (unless ride-along evidence was captured): name the exact checks for the next pass — F4: zero `<function_calls>`/`<invoke>` preamble lines in the three sdlc-governance live streams; F5: `/api/v1` occurrence count in app-infra-generator live output consistent with api-design/devops (was 0 vs 58x/50x). Cite the offline contract pins as the shipped mitigation.
    4. `## Linkage` — table mapping LV-02 / F4-residual / F5-residual → REPORT-2026-06-12.md sections → Phase-15 plan that closed the prompt half (15-01) and the pin half (15-02) → live status.

    Keep it short (a page). Commit with the .planning docs flow.
  </action>
  <verify>
    <automated>test -f .planning/live-verification/PHASE-15-RECHECK.md && test "$(grep -cE 'Disposition: (CLOSED|DEFERRED|NEXT-LIVE-PASS)' .planning/live-verification/PHASE-15-RECHECK.md)" -ge 3 && grep -q 'LV-02' .planning/live-verification/PHASE-15-RECHECK.md && grep -q 'F4-residual' .planning/live-verification/PHASE-15-RECHECK.md && grep -q 'F5-residual' .planning/live-verification/PHASE-15-RECHECK.md && grep -q 'REPORT-2026-06-12' .planning/live-verification/PHASE-15-RECHECK.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/live-verification/PHASE-15-RECHECK.md` exists with >= 3 `Disposition:` lines (one per finding), each one of CLOSED / DEFERRED / NEXT-LIVE-PASS
    - LV-02 section carries either live run ids + deck evidence (CLOSED) or the verbatim blocking error (DEFERRED) — never an empty claim
    - F4/F5 sections name the exact next-pass checks (tool-XML preamble grep; /api/v1 occurrence comparison)
    - file references REPORT-2026-06-12.md and the 15-02 offline gate evidence
    - file contains no AWS account ids or credentials (`! grep -qE '[0-9]{12}' .planning/live-verification/PHASE-15-RECHECK.md` as a cheap account-id screen)
  </acceptance_criteria>
  <done>ROADMAP SC5 satisfied: the live re-check disposition is recorded either way, with finding-level linkage back to the 2026-06-12 report and forward to the next live pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local driver → live AWS Bedrock | real cloud calls under existing SSO profiles; spend + account metadata exposure |
| live outputs → committed planning docs | evidence files are committed to the repo — must not leak credentials/account identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Information disclosure | PHASE-15-RECHECK.md + saved artifacts | mitigate | record run ids/excerpts only; no AWS account ids, credentials, or SSO URLs; 12-digit account-id grep screen in acceptance criteria |
| T-15-08 | Elevation of privilege | live Bedrock invocation | accept | existing read-only-checked SSO profiles; `sts get-caller-identity` precheck; bounded ~$0.10 spend; no new IAM surface |
| T-15-09 | Denial of service | quota/throttle burn from retry loops | mitigate | single retry max, then locked DEFERRED branch (D-05); no app_builder live run (cost fence) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | zero package installs this phase |
</threat_model>

<verification>
- Task 1 evidence checks (live branch) or captured deferral basis (deferral branch)
- Task 2 disposition-record gates pass (3+ Disposition lines, finding linkage, no account ids)
- `git status --porcelain backend/` prints nothing (this plan touches `.planning/` only)
</verification>

<success_criteria>
- ROADMAP SC1 live half: resolved live final_output is a deck and the FE-exact revision returns a revised deck — or explicitly DEFERRED with verbatim basis (D-05)
- ROADMAP SC5: disposition recorded either way; criteria 2-3 carry named next-live-pass checks or ride-along evidence
- Phase completion is NOT blocked on live availability (defer-live-verification convention; offline evidence from 15-02 stands)
</success_criteria>

<output>
Create `.planning/phases/15-live-pass-prompt-contract-closure/15-03-SUMMARY.md` when done (state which branch ran — CLOSED live or DEFERRED — and the cost actually incurred).
</output>
