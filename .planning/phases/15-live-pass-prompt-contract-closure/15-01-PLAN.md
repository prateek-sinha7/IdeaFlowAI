---
phase: 15-live-pass-prompt-contract-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/prompts/od-ppt-validator/AGENT.md
  - backend/agents/prompts/app-sdlc-governance/AGENT.md
  - backend/agents/prompts/dotnet-sdlc-governance/AGENT.md
  - backend/agents/prompts/mulesoft-sdlc-governance/AGENT.md
  - backend/agents/prompts/app-infra-generator/AGENT.md
autonomous: true
requirements: [LV-02, F4-residual, F5-residual]
must_haves:
  truths:
    - "od-ppt-validator AGENT.md demands exactly ONE <artifact> block whose content is the complete corrected HTML deck, positioned ABOVE the validation checklist (D-01)"
    - "All three sdlc-governance AGENT.md bodies forbid emitting tool-call syntax (<function_calls>/<invoke>/write_todos) and demand begin-directly output (D-02)"
    - "app-infra-generator AGENT.md carries a top-of-body /api/v1 contract with healthcheck, nginx, and smoke-test examples, above OUTPUT FORMAT (D-03)"
    - "All ~80 agents still load — frontmatter byte-untouched on all five files"
  artifacts:
    - path: "backend/agents/prompts/od-ppt-validator/AGENT.md"
      provides: "Deck re-emission output contract (LV-02 fix)"
      contains: "exactly ONE <artifact>"
    - path: "backend/agents/prompts/app-sdlc-governance/AGENT.md"
      provides: "Anti-fabrication output contract (F4)"
      contains: "You have NO tools"
    - path: "backend/agents/prompts/dotnet-sdlc-governance/AGENT.md"
      provides: "Anti-fabrication output contract (F4)"
      contains: "You have NO tools"
    - path: "backend/agents/prompts/mulesoft-sdlc-governance/AGENT.md"
      provides: "Anti-fabrication output contract (F4)"
      contains: "You have NO tools"
    - path: "backend/agents/prompts/app-infra-generator/AGENT.md"
      provides: "Forceful top-of-body /api/v1 contract (F5)"
      contains: "API PATH CONTRACT"
  key_links:
    - from: "backend/agents/prompts/od-ppt-validator/AGENT.md"
      to: "backend/agents/capabilities/deliverables/_artifact.py first-match unwrap"
      via: "exactly-ONE-artifact wording (a small status artifact before the deck would win the regex otherwise)"
      pattern: "exactly ONE <artifact>"
    - from: "backend/agents/prompts/*/AGENT.md"
      to: "agents/loader.py schema validation"
      via: "frontmatter untouched, bodies only"
      pattern: "test_loader"
---

<objective>
Add the three live-pass output contracts to five AGENT.md prompt bodies — od-ppt-validator deck re-emission (LV-02, per D-01), sdlc-governance family anti-fabrication (F4-residual, per D-02), app-infra-generator /api/v1 adherence (F5-residual, per D-03). Bodies only; frontmatter byte-untouched; ZERO engine/capability/factory edits (SC-001).

Decision IDs map to `15-CONTEXT.md ## Implementation Decisions` in order: D-01 = "LV-02 fix approach", D-02 = "sdlc-governance anti-fabrication", D-03 = "infra-generator /api/v1 adherence", D-04 = "Verification approach", D-05 = "Live re-check".

Purpose: close the three prompt-shaped findings of the 2026-06-12 live re-pass so the od_ppt deliverable/revision chain carries a real deck (Phase-14 SC4 content unblocker) and the last live-Haiku adherence gaps get explicit contracts.
Output: five edited prompt bodies carrying single-line, grep-pinnable contract phrases (13-03 precedent), validated by the loader suite.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-live-pass-prompt-contract-closure/15-CONTEXT.md
@.planning/phases/15-live-pass-prompt-contract-closure/15-RESEARCH.md
@.planning/live-verification/REPORT-2026-06-12.md
@backend/CLAUDE.md
</context>

## Artifacts this phase produces (plan 01 share)

Contract-line markers added (each a single line in the body — grep/substring pinnable, 13-03 wording lesson):

| File | Marker tokens added |
|------|---------------------|
| `backend/agents/prompts/od-ppt-validator/AGENT.md` | `exactly ONE <artifact>` · `complete corrected HTML deck` · `even when you change nothing` · `anywhere else in your response` |
| `backend/agents/prompts/app-sdlc-governance/AGENT.md` | `You have NO tools` · `<function_calls>` · `<invoke>` · `write_todos` · `Begin your response DIRECTLY with` (+ `first \`filename:\` fenced block`) |
| `backend/agents/prompts/dotnet-sdlc-governance/AGENT.md` | same anti-tool-XML tokens (+ `the first Markdown heading`) |
| `backend/agents/prompts/mulesoft-sdlc-governance/AGENT.md` | same anti-tool-XML tokens (+ `the first Markdown heading`) |
| `backend/agents/prompts/app-infra-generator/AGENT.md` | `API PATH CONTRACT` · `/api/v1/health` · `location /api/v1/` (total `/api/v1` occurrences >= 6) |

<tasks>

<task type="auto">
  <name>Task 1: od-ppt-validator deck re-emission contract (LV-02, D-01)</name>
  <files>backend/agents/prompts/od-ppt-validator/AGENT.md</files>
  <read_first>
    - backend/agents/prompts/od-ppt-validator/AGENT.md (current body: intro line 21, job line 23, ## VALIDATION CHECKLIST line 28, ## RULES line 48, bottom ## OUTPUT CONTRACT lines 54-65)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-RESEARCH.md — sections "Current Bodies §1", "Resolver Anchor", "Common Pitfalls" Pitfall 2 + 3 (first-match unwrap → exactly-ONE wording; single-line phrases)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-CONTEXT.md — "LV-02 fix approach (LOCKED)"
    - backend/agents/capabilities/deliverables/_artifact.py (READ-ONLY anchor — confirm first-match regex; do NOT modify)
  </read_first>
  <action>
    Edit the BODY ONLY (everything below the closing `---` at line 19; frontmatter bytes untouched). Per D-01, the validator must always re-emit the complete corrected deck wrapped in `<artifact>` tags. Per RESEARCH Resolver Anchor, `unwrap_artifact` extracts the FIRST `<artifact>` block — so the contract demands EXACTLY ONE artifact block (the CONTEXT phrase "last artifact-wrapped emission" is wrong at mechanism level; do not use "last/final artifact" wording).

    1. Keep line 21 intro ("You are the **Deck QA Agent**...") unchanged.
    2. Rewrite the line-23 job statement (it currently frames the job as judgment/reporting — "You return the artifact EXACTLY as-is unless you find a structural defect..."). New wording: "Your job: a final QA pass on the HTML deck, then RE-EMIT THE COMPLETE DECK as your artifact — corrected if you found structural defects, byte-identical otherwise."
    3. Insert a new section titled `## OUTPUT CONTRACT — NON-NEGOTIABLE (read first)` immediately after the rewritten job statement (BEFORE "You will receive in the user message" and before `## VALIDATION CHECKLIST`), containing these bullet lines — each bullet stays on ONE physical line (13-03 wording lesson; substring pins land in 15-02):
       - "Your response MUST contain exactly ONE <artifact> block — never a second artifact, never a partial artifact, never a status artifact."
       - "The artifact content MUST be the complete corrected HTML deck: the full <!DOCTYPE html> document with every <section class=\"slide\"> element — even when you change nothing, re-emit the entire deck."
       - "The artifact is NEVER a QA report, summary, or status note. The artifact IS the deck."
       - "Never use the literal <artifact tag anywhere else in your response — your commentary must not contain it."
       - "At most two short sentences of commentary may precede the artifact. Nothing after </artifact>."
       - "Preserve the incoming artifact's identifier, type, and title attributes on your re-emitted <artifact> tag."
    4. DELETE the old bottom `## OUTPUT CONTRACT` section (current lines 54-65, including its fenced artifact example) so exactly one contract exists — its attribute-preservation guidance is now the last bullet of the new top section. After the edit, `grep -c '^## OUTPUT CONTRACT' ` on the file returns exactly 1.
    5. In `## RULES`, change "If the artifact has no defects, output it unchanged." to "If the artifact has no defects, re-emit it unchanged — in full." Keep the SURGEON rule and the rest of RULES/CHECKLIST byte-identical.
    6. Do NOT touch `agents/capabilities/deliverables/ppt.py` or `_artifact.py` (locked). Do NOT touch frontmatter (tools: [workspace] is intentional — do not "normalize", RESEARCH Pitfall 4).

    Commit hint: `fix(prompts): od-ppt-validator always re-emits the full deck as its single artifact (LV-02)`.
  </action>
  <verify>
    <automated>cd backend && grep -c 'exactly ONE <artifact>' agents/prompts/od-ppt-validator/AGENT.md && grep -c 'complete corrected HTML deck' agents/prompts/od-ppt-validator/AGENT.md && grep -c 'even when you change nothing' agents/prompts/od-ppt-validator/AGENT.md && python3.11 -c "import pathlib; b=pathlib.Path('agents/prompts/od-ppt-validator/AGENT.md').read_text(); assert b.index('exactly ONE <artifact>') < b.index('## VALIDATION CHECKLIST'); assert b.count('## OUTPUT CONTRACT') == 1" && python3.11 -m pytest tests/agents/test_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'exactly ONE <artifact>' backend/agents/prompts/od-ppt-validator/AGENT.md` >= 1
    - `grep -c 'complete corrected HTML deck' backend/agents/prompts/od-ppt-validator/AGENT.md` >= 1
    - `grep -c 'even when you change nothing' backend/agents/prompts/od-ppt-validator/AGENT.md` >= 1
    - contract section index precedes `## VALIDATION CHECKLIST` index (python assert above passes)
    - exactly one `## OUTPUT CONTRACT` heading in the file
    - the file does NOT contain "EXACTLY as-is" any more (old judgment framing removed): `! grep -q 'EXACTLY as-is' backend/agents/prompts/od-ppt-validator/AGENT.md`
    - no wording "last artifact" or "final artifact" introduced: `! grep -qi 'last artifact' backend/agents/prompts/od-ppt-validator/AGENT.md`
    - frontmatter untouched: `git diff -- backend/agents/prompts/od-ppt-validator/AGENT.md` shows no +/- lines for `id:`, `order:`, `tools:`, `pipeline_type:`, `max_tokens:`
    - `cd backend && python3.11 -m pytest tests/agents/test_loader.py -q` exits 0
  </acceptance_criteria>
  <done>od-ppt-validator body opens with the re-emission job statement + the exactly-ONE-artifact complete-deck contract above the checklist; the old bottom contract is deleted; loader suite green.</done>
</task>

<task type="auto">
  <name>Task 2: sdlc-governance family anti-fabrication contracts (F4-residual, D-02)</name>
  <files>backend/agents/prompts/app-sdlc-governance/AGENT.md, backend/agents/prompts/dotnet-sdlc-governance/AGENT.md, backend/agents/prompts/mulesoft-sdlc-governance/AGENT.md</files>
  <read_first>
    - backend/agents/prompts/app-sdlc-governance/AGENT.md (body: intro line 30, fabrication trigger lines 36-41 "Read the concrete choices...", filename-block output format lines 90-117)
    - backend/agents/prompts/dotnet-sdlc-governance/AGENT.md (body: intro line 29; output is "a structured Markdown document" — NO filename blocks)
    - backend/agents/prompts/mulesoft-sdlc-governance/AGENT.md (near-identical to dotnet variant)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-RESEARCH.md — "Current Bodies §2-§4", Pitfall 5 (do NOT import filename-block contract into dotnet/mulesoft), Pitfall 7 (no multi-line bad-example exemplars — they prime fabrication)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-CONTEXT.md — "sdlc-governance anti-fabrication (LOCKED)" + Specific Ideas (live signature: 5x <function_calls> wrapping <invoke name="read_file"> in stream lines 2-36)
  </read_first>
  <action>
    Edit the three BODIES ONLY (frontmatter untouched; 13-02 factory `tool_availability` preamble stays untouched — this is body-level reinforcement per D-02).

    In EACH of the three files, insert this block immediately after the line "You are an Engineering Operations & Governance Lead." — each bullet on ONE physical line; the first three lines IDENTICAL across all three files (shared anti-tool-XML sentence, per-pipeline deliverable-start line tailored — Claude's-discretion choice documented here):
       - "OUTPUT CONTRACT (non-negotiable):"
       - "- You have NO tools. Everything you need from the upstream agents is already included in this message as context."
       - "- Never emit tool-call syntax as text — no <function_calls>, no <invoke>, no write_todos. Not one line of it."
       - app-sdlc-governance fourth line: "- Begin your response DIRECTLY with the first `filename:` fenced block — no preamble, no plan, no narration before it."
       - dotnet-sdlc-governance AND mulesoft-sdlc-governance fourth line: "- Begin your response DIRECTLY with the deliverable content — the first Markdown heading. No preamble, no plan, no narration before it."

    app-sdlc-governance ONLY — also defuse the fabrication trigger (RESEARCH A2: the "Read the concrete choices ... from the provided context" imperative elicits simulated read_file calls on live Haiku):
       - Lines 37-38: replace "Read the concrete choices the upstream agents actually made from the provided context;" with "Use the concrete choices the upstream agents actually made — they are already provided in full in this message;" (rest of the sentence unchanged).
       - Line 46 (ADR section): replace "(read them from context)" with "(from the context above)".

    dotnet/mulesoft: contract-block insertion is the ONLY change — do NOT alter their migration framing or output format (Pitfall 5: a filename-block phrase here would CHANGE the migration deliverable format — out of scope).

    Prohibitions: name the forbidden tokens exactly once per file, tersely, in the prohibition line — do NOT add any multi-line "bad example" block showing tool-XML (Pitfall 7).

    Commit hint: `fix(prompts): sdlc-governance anti-fabrication output contracts (F4)`.
  </action>
  <verify>
    <automated>cd backend && for f in app-sdlc-governance dotnet-sdlc-governance mulesoft-sdlc-governance; do grep -c 'You have NO tools' agents/prompts/$f/AGENT.md && grep -c '<function_calls>' agents/prompts/$f/AGENT.md && grep -c 'write_todos' agents/prompts/$f/AGENT.md && grep -c 'Begin your response DIRECTLY with' agents/prompts/$f/AGENT.md; done && grep -q 'fenced block' agents/prompts/app-sdlc-governance/AGENT.md && grep -q 'the first Markdown heading' agents/prompts/dotnet-sdlc-governance/AGENT.md && grep -q 'the first Markdown heading' agents/prompts/mulesoft-sdlc-governance/AGENT.md && ! grep -q 'Read the concrete choices' agents/prompts/app-sdlc-governance/AGENT.md && python3.11 -m pytest tests/agents/test_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - each of the 3 files contains, each on a single line: `You have NO tools`, `<function_calls>`, `<invoke>`, `write_todos`, `Begin your response DIRECTLY with`
    - the anti-tool-XML line ("Never emit tool-call syntax as text — no <function_calls>, no <invoke>, no write_todos. Not one line of it.") is byte-identical across the 3 files
    - app-sdlc-governance contains "first `filename:` fenced block"; dotnet + mulesoft contain "the first Markdown heading" and do NOT contain "filename:" in their contract block
    - `! grep -q 'Read the concrete choices' backend/agents/prompts/app-sdlc-governance/AGENT.md` (trigger rephrased)
    - dotnet/mulesoft diffs contain ONLY the inserted contract block (`git diff` per file shows a single added hunk)
    - no multi-line tool-XML exemplar block added anywhere (forbidden tokens each appear exactly once per file: `grep -c '<function_calls>' ` == 1 per file)
    - frontmatter untouched on all three (no +/- frontmatter-key lines in `git diff`)
    - `cd backend && python3.11 -m pytest tests/agents/test_loader.py -q` exits 0
  </acceptance_criteria>
  <done>All three sdlc-governance bodies open with the no-tools/anti-tool-XML/begin-directly contract, tailored per output format; app-sdlc's read_file-priming sentence is defused; loader suite green.</done>
</task>

<task type="auto">
  <name>Task 3: app-infra-generator forceful top-of-body /api/v1 contract (F5-residual, D-03)</name>
  <files>backend/agents/prompts/app-infra-generator/AGENT.md</files>
  <read_first>
    - backend/agents/prompts/app-infra-generator/AGENT.md (body: intro lines 23-25, "OUTPUT FORMAT — use this exact format" line 27, RULES section line 100, weak existing rule line 102)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-RESEARCH.md — "Current Bodies §5" (why the existing rule is weak: bottom-of-body, conditional phrasing, one example)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-CONTEXT.md — "infra-generator /api/v1 adherence (LOCKED)"
  </read_first>
  <action>
    Edit the BODY ONLY. Per D-03: make the `/api/v1` rule forceful and positionally prominent — top-of-body contract block with concrete examples; keep the 13-03 single-literal standard `/api/v1` (shared with app-api-design 58x / app-devops 50x live compliance).

    Insert this block AFTER the line "Based on the architecture and generated code from previous agents, produce ALL of the following files." and BEFORE the line starting "OUTPUT FORMAT — use this exact format" — each bullet on ONE physical line:
       - "API PATH CONTRACT (non-negotiable — applies to EVERY file you output):"
       - "- Every reference to an application API endpoint uses the literal `/api/v1` prefix — no bare `/health`, no unversioned API path, anywhere."
       - "- Healthchecks: `curl -f http://localhost:$PORT/api/v1/health` — in the Dockerfile HEALTHCHECK, every compose healthcheck block, and any readiness probe."
       - "- Reverse proxy / ingress: nginx `location /api/v1/` blocks and ingress path rules target `/api/v1`."
       - "- CI/CD smoke tests: curl `/api/v1` endpoints (e.g. `/api/v1/health`) after deploy."

    Keep the existing RULES bullet at line 102 unchanged (bottom-of-body reinforcement — planner's-choice option from RESEARCH exercised as "keep"). No other body change. Frontmatter untouched (tools: [workspace] frozen).

    Resulting literal `/api/v1` occurrence count: 6 in the new block + 2 in the existing RULES bullet = 8.

    Commit hint: `fix(prompts): app-infra-generator top-of-body /api/v1 path contract (F5)`.
  </action>
  <verify>
    <automated>cd backend && test "$(grep -o '/api/v1' agents/prompts/app-infra-generator/AGENT.md | wc -l | tr -d ' ')" -ge 6 && grep -q 'API PATH CONTRACT' agents/prompts/app-infra-generator/AGENT.md && python3.11 -c "import pathlib; b=pathlib.Path('agents/prompts/app-infra-generator/AGENT.md').read_text(); assert b.index('API PATH CONTRACT') < b.index('OUTPUT FORMAT')" && grep -q 'location /api/v1/' agents/prompts/app-infra-generator/AGENT.md && python3.11 -m pytest tests/agents/test_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o '/api/v1' backend/agents/prompts/app-infra-generator/AGENT.md | wc -l` >= 6 (actual: 8)
    - file contains `API PATH CONTRACT` and its index precedes the `OUTPUT FORMAT` index (python assert passes)
    - file contains `/api/v1/health` and `location /api/v1/` each on a single line
    - existing RULES bullet (line 102 wording "API paths: wherever health checks...") still present verbatim
    - frontmatter untouched (no +/- frontmatter-key lines in `git diff`)
    - `cd backend && python3.11 -m pytest tests/agents/test_loader.py -q` exits 0
  </acceptance_criteria>
  <done>app-infra-generator opens with the non-negotiable /api/v1 contract carrying healthcheck/nginx/smoke examples, positioned above OUTPUT FORMAT; loader suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENT.md body → composed system prompt → live LLM | engineer-authored static prompt content steers model output rendered by the FE |
| AGENT.md frontmatter → loader → factory | frontmatter grants tools/ordering — a silent frontmatter edit changes runtime permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | AGENT.md frontmatter (tool grants/order) | mitigate | edits strictly below the closing `---`; `test_loader.py -q` after every task; frontmatter-freeze pytest pin lands in 15-02; per-task git diff confined to bodies |
| T-15-02 | Spoofing | composed system prompt (prompt injection) | accept | bodies are static, engineer-authored, version-controlled; no user input interpolated by this phase (mirrors 13-03 T-13-03-01) |
| T-15-03 | Tampering | resolver/capability files (`ppt.py`, `_artifact.py`) | mitigate | READ-ONLY anchors per D-01 + SC-001 fence; `git status --porcelain backend/agents/capabilities/` must stay empty through the plan |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | zero package installs this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- All grep/positional gates of the three tasks pass (see per-task automated commands)
- `cd backend && python3.11 -m pytest tests/agents/test_loader.py -q` exits 0 (all ~80 agents load; frontmatter schema intact)
- `git diff --stat` for this plan confined to `backend/agents/prompts/` (13-03 gate)
- `git status --porcelain backend/agents/capabilities/ backend/agents/execution_engine/ backend/agents/factory.py backend/agents/loader.py backend/agents/registry.py` prints nothing (SC-001: zero engine/capability/factory edits)
- OFFLINE-SUITE CAVEAT: never run the full backend pytest offline (it hangs on Chromium/Bedrock/Postgres-gated tests) — targeted commands only, `python3.11`, no venv
</verification>

<success_criteria>
- Five AGENT.md bodies carry their contract lines with all pinned tokens on single lines (ROADMAP SC1/SC2/SC3 prompt halves)
- od-ppt-validator contract uses exactly-ONE-artifact semantics (never "last artifact") — RESEARCH Pitfall 2 encoded
- Loader suite green; frontmatter byte-identical on all five files
</success_criteria>

<output>
Create `.planning/phases/15-live-pass-prompt-contract-closure/15-01-SUMMARY.md` when done (note the exact contract wording chosen per file — 15-02 pins these exact tokens).
</output>
