---
phase: 15-live-pass-prompt-contract-closure
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - backend/tests/agents/test_prompt_contracts.py
autonomous: true
requirements: [LV-02, F4-residual, F5-residual]
must_haves:
  truths:
    - "Durable pytest pins exist for all five contract bodies and fail if any contract line is removed (D-04 — upgrades 13-03's grep-only acceptance to a persistent test layer)"
    - "An offline engine drive with a contract-shaped scripted validator resolves final_output to the deck (contains <section, starts with <!DOCTYPE), not the QA narration (LV-02 composition proof, D-04)"
    - "All 5 characterization pipeline goldens are byte-identical after the prompt edits (INV-3); zero edits to _scripted_model.py, live_harness.py, or golden/"
    - "Frontmatter of the five edited agents is frozen by an explicit pytest pin"
  artifacts:
    - path: "backend/tests/agents/test_prompt_contracts.py"
      provides: "Prompt-contract pins (LV-02/F4/F5) + frontmatter freeze + LV-02 harness composition test"
      exports: ["test_od_ppt_validator_deck_reemission_contract", "test_sdlc_governance_anti_fabrication_contract", "test_infra_generator_api_v1_contract", "test_contract_agents_frontmatter_frozen", "test_od_ppt_deck_resolution_with_contract_shaped_validator"]
      min_lines: 80
  key_links:
    - from: "backend/tests/agents/test_prompt_contracts.py"
      to: "agents/loader.py"
      via: "load_agent_spec(id).prompt_body (the parser the factory composes from — test_guardrails.py precedent)"
      pattern: "load_agent_spec"
    - from: "backend/tests/agents/test_prompt_contracts.py"
      to: "backend/tests/agents/live_harness.py"
      via: "drive_engine_pipeline per-agent model factory (REAL engine + REAL resolver + REAL prompts, scripted model only)"
      pattern: "drive_engine_pipeline"
    - from: "backend/tests/agents/test_prompt_contracts.py"
      to: "backend/tests/agents/_scripted_model.py"
      via: "import-only reuse of ScriptedFakeChatModel/_ScriptedTurn/_scripts_for — ZERO edits (od_ppt goldens pin that module's bytes)"
      pattern: "ScriptedFakeChatModel"
---

<objective>
Create the durable offline pin layer per D-04: `backend/tests/agents/test_prompt_contracts.py` with (a) prompt_body substring pins for all five contracts shipped by 15-01, (b) a frontmatter-freeze pin for the five agents, and (c) the LV-02 harness composition test proving contract-shaped validator output resolves to the deck through the REAL engine + REAL `PptResolver`. Then run the full targeted offline gate proving the 5 characterization goldens stay byte-identical (INV-3).

Decision IDs map to `15-CONTEXT.md ## Implementation Decisions` in order (D-01 LV-02 fix, D-02 sdlc anti-fabrication, D-03 /api/v1, D-04 verification approach, D-05 live re-check). This plan implements D-04.

Purpose: 13-03 left grep-only acceptance with NO persistent test (RESEARCH-verified); this plan makes the contract lines survive future prompt rewrites and proves LV-02's resolver+contract composition end-to-end offline.
Output: one new test file (~9-11 test nodes green) + a green phase gate with byte-identical goldens.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-live-pass-prompt-contract-closure/15-CONTEXT.md
@.planning/phases/15-live-pass-prompt-contract-closure/15-RESEARCH.md
@.planning/phases/15-live-pass-prompt-contract-closure/15-01-SUMMARY.md
@backend/CLAUDE.md
</context>

## Artifacts this phase produces (plan 02 share)

| Symbol | Where |
|--------|-------|
| `backend/tests/agents/test_prompt_contracts.py` | new file |
| `test_od_ppt_validator_deck_reemission_contract` | pin: LV-02 contract tokens + position |
| `test_sdlc_governance_anti_fabrication_contract` | pin: F4 tokens, parametrized over the 3 sdlc ids |
| `test_infra_generator_api_v1_contract` | pin: F5 tokens + count + position |
| `test_contract_agents_frontmatter_frozen` | pin: (order, pipeline_type, tools) for the 5 agents |
| `test_od_ppt_deck_resolution_with_contract_shaped_validator` | LV-02 composition: scripted contract-shaped validator → resolved deck |

<tasks>

<task type="auto">
  <name>Task 1: Write tests/agents/test_prompt_contracts.py (prompt pins + frontmatter freeze + LV-02 harness composition)</name>
  <files>backend/tests/agents/test_prompt_contracts.py</files>
  <read_first>
    - .planning/phases/15-live-pass-prompt-contract-closure/15-01-SUMMARY.md (the EXACT contract wording 15-01 shipped — pin those tokens, not paraphrases)
    - backend/tests/agents/test_guardrails.py (the `load_agent_spec(id).prompt_body` assertion shape — lines ~104, ~211; replicate import style and module conventions)
    - backend/tests/agents/live_harness.py — `drive_engine_pipeline` signature (line 484: keyword-only `model` accepts a `Callable[[str], BaseChatModel]` per-agent factory; `fake_planner`, `od_context`, `gate_agent_ids`) and `CaptureResult` (line 181: fields `completed`, `error`, `events`, `deliverable`, `final_output` — engine world mirrors final_output == deliverable, the string off pipeline_complete.data)
    - backend/tests/agents/test_live_harness.py — `test_prototype_via_per_agent_model_factory` (line ~147): the exact @pytest.mark.asyncio + per-agent-factory invocation precedent to copy
    - backend/tests/agents/_scripted_model.py — `ScriptedFakeChatModel` + `_ScriptedTurn` (lines 76-216: constructor takes a list of turns; a turn has `texts=[...]` streamed sequentially and `usage=(in, out)`), `_scripts_for` (line 244), the od_context seed dict in `_drive` (lines 570-579). IMPORT ONLY — ZERO EDITS (RESEARCH Pitfall 1: the od-ppt-validator branch bytes ARE the od_ppt goldens)
    - .planning/phases/15-live-pass-prompt-contract-closure/15-RESEARCH.md — "Harness — Scripted-Model Pin Design" + "Code Examples" (fenced reference implementations live there)
  </read_first>
  <action>
    Create `backend/tests/agents/test_prompt_contracts.py` — module docstring: "Phase 15 prompt output-contract pins (LV-02 / F4-residual / F5-residual). Durable upgrade of 13-03's grep-only acceptance. Per D-04." Imports: `pytest`, `from agents.loader import load_agent_spec`, plus the harness imports used by the composition test (copy the sys-path/import conventions from test_live_harness.py / test_guardrails.py exactly).

    Five tests:

    1. `test_od_ppt_validator_deck_reemission_contract` — body = load_agent_spec("od-ppt-validator").prompt_body; assert the substrings "exactly ONE <artifact>", "complete corrected HTML deck", "even when you change nothing" are present; assert body.index("exactly ONE <artifact>") < body.index("## VALIDATION CHECKLIST") (positional prominence); assert body.count("## OUTPUT CONTRACT") == 1 (no dual contract drift).

    2. `test_sdlc_governance_anti_fabrication_contract` — @pytest.mark.parametrize over ("app-sdlc-governance", "dotnet-sdlc-governance", "mulesoft-sdlc-governance"); per body assert "You have NO tools", "<function_calls>", "<invoke>", "write_todos", "Begin your response DIRECTLY with" present; for app-sdlc-governance additionally assert "fenced block" in body and "Read the concrete choices" not in body; for dotnet/mulesoft additionally assert "the first Markdown heading" in body.

    3. `test_infra_generator_api_v1_contract` — body = load_agent_spec("app-infra-generator").prompt_body; assert body.count("/api/v1") >= 6; assert "API PATH CONTRACT" in body; assert body.index("API PATH CONTRACT") < body.index("OUTPUT FORMAT"); assert "/api/v1/health" in body and "location /api/v1/" in body.

    4. `test_contract_agents_frontmatter_frozen` — @pytest.mark.parametrize over five (agent_id, order, pipeline_type, tools) tuples: ("od-ppt-validator", 3, "od_ppt", ["workspace"]), ("app-sdlc-governance", 15, "app_builder", []), ("dotnet-sdlc-governance", 13, "dotnet_to_azure", []), ("mulesoft-sdlc-governance", 13, "mulesoft_to_springboot", []), ("app-infra-generator", 10, "app_builder", ["workspace"]); assert spec.order/spec.pipeline_type/list(spec.tools) match (T-15-01 mitigation made durable; check the AgentSpec attribute names against loader.py while writing).

    5. `test_od_ppt_deck_resolution_with_contract_shaped_validator` — @pytest.mark.asyncio (copy the invocation shape from test_live_harness.py::test_prototype_via_per_agent_model_factory). Define module constants: _DECK = a small COMPLETE deck on the LV-02 evidence shape — single-line or compact string containing '<!DOCTYPE html><html><head><title>Phase 15 LV-02 pin</title></head><body>' + two slide sections where the first is '<section class="slide active">...' and the second '<section class="slide">...' + a minimal nav '<script>' + '</body></html>'. _NARRATION = "Running the final QA pass. P0 checks complete - one fix applied.\n" (multi-sentence QA commentary FIRST — the live LV-02 shape). Model factory function _model_for(agent_id): if agent_id == "od-ppt-validator" return ScriptedFakeChatModel([_ScriptedTurn(texts=[_NARRATION, '<artifact identifier="deck" type="text/html" title="Deck">' + _DECK + '</artifact>'], usage=(22, 14))]); else return ScriptedFakeChatModel(_scripts_for(agent_id)). Drive: result = await drive_engine_pipeline("od_ppt", model=_model_for, fake_planner=True, gate_agent_ids=(), od_context={...copy the exact seed dict keys/values from _scripted_model.py lines 572-579: template_body, template_id, ds_id, ds_body, craft_block, is_design_system_required...}). Assert: result.completed is True; result.error is None; fo = result.final_output; isinstance(fo, str); fo.lstrip().startswith("<!DOCTYPE html>"); '<section class="slide"' in fo; "Running the final QA pass" not in fo (narration did NOT win — the LV-02 failure mode); result.deliverable == fo.

    HARD CONSTRAINTS: zero edits to tests/agents/_scripted_model.py, tests/agents/live_harness.py, tests/agents/characterization/** (goldens), agents/capabilities/** (resolver). The composition test runs the REAL engine + REAL PptResolver + REAL prompts — only the model is scripted; that is the point (RESEARCH Don't Hand-Roll: never re-implement unwrap/sanitize in the test).

    Commit hint: `test(agents): pin live-pass prompt contracts + LV-02 deck resolution (Phase 15)`.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_prompt_contracts.py -q && git status --porcelain tests/agents/_scripted_model.py tests/agents/live_harness.py tests/agents/characterization/ agents/capabilities/ | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_prompt_contracts.py -q` exits 0 with >= 9 test nodes passed (1 + 3 parametrized + 1 + 5 parametrized + 1 = 11 expected)
    - `python3.11 -m pytest tests/agents/test_prompt_contracts.py -q -k deck_resolution` exits 0 (the LV-02 composition pin specifically)
    - `git status --porcelain backend/tests/agents/_scripted_model.py backend/tests/agents/live_harness.py backend/tests/agents/characterization/ backend/agents/capabilities/` prints nothing (zero forbidden edits)
    - the new file imports `load_agent_spec` and `drive_engine_pipeline` (grep both names in the file)
    - no `SNAPSHOT_UPDATE` appears in any command run for this task
  </acceptance_criteria>
  <done>test_prompt_contracts.py green: five contract-pin/freeze tests + the LV-02 composition test prove contract-shaped validator output resolves to the deck through the real resolver, with zero harness/golden/resolver edits.</done>
</task>

<task type="auto">
  <name>Task 2: Full targeted offline phase gate — characterization byte-identical (INV-3)</name>
  <files>none (verification-only gate task — no edits)</files>
  <read_first>
    - .planning/phases/15-live-pass-prompt-contract-closure/15-RESEARCH.md — "Validation Architecture" (the exact full-suite command; full backend pytest HANGS offline — targeted suite only) + "Sibling facts" (why prompt edits cannot perturb goldens: scripted model never reads the system prompt — Phase-3 caveat; the gate run is the mandatory PROOF per D-04)
  </read_first>
  <action>
    Run the full targeted phase gate (single command, from backend/, python3.11 no venv — NEVER the unscoped pytest suite offline):

    python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_loader.py tests/agents/test_prompt_contracts.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -q

    followed by /opt/homebrew/bin/lint-imports (from the repo's backend/ — same invocation 13/14 phase gates used).

    Then prove byte-identity and scope containment: `git status --porcelain backend/tests/agents/characterization/golden/ backend/tests/agents/_scripted_model.py backend/agents/capabilities/deliverables/` must print nothing, and `git status --porcelain` overall must show no unexpected tracked-file modifications outside `.planning/` (plan-01 prompt edits and the new test file are already committed by their tasks). Run with NO `SNAPSHOT_UPDATE` in the environment (INV-3: zero golden re-baselines — `env | grep -c SNAPSHOT_UPDATE` is 0).

    If any characterization test fails: STOP — do not regenerate goldens; a failure means a forbidden file was touched (diagnose via `git diff`); fix the violation, never the golden.

    If a run hangs (orphaned pytest contention): `pkill -9 -f pytest` and re-run the targeted command (RESEARCH Pitfall 6).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py tests/agents/test_loader.py tests/agents/test_prompt_contracts.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -q && /opt/homebrew/bin/lint-imports && test -z "$(git status --porcelain tests/agents/characterization/golden/ tests/agents/_scripted_model.py agents/capabilities/deliverables/ 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - the targeted pytest command exits 0 — all 9 listed test files pass, including all 5 characterization snapshot pipelines (prototype / od_prototype / prototype_revision / od_ppt / app_builder)
    - `/opt/homebrew/bin/lint-imports` exits 0 (hexagonal import boundaries intact)
    - `git status --porcelain backend/tests/agents/characterization/golden/` prints nothing (goldens byte-identical — ROADMAP SC4)
    - `git status --porcelain backend/tests/agents/_scripted_model.py backend/agents/capabilities/deliverables/` prints nothing
    - no command in this task used `SNAPSHOT_UPDATE`
  </acceptance_criteria>
  <done>Phase gate green: characterization byte-identical (INV-3 proven), loader + banned-patterns + ledger + new contract pins all pass, import boundaries clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → engine drive | the composition test executes the REAL engine offline with a scripted model — a test that patched gates or goldens could mask regressions |
| golden snapshots → INV-3 backward-compat guarantee | goldens are the byte-level contract; any re-baseline silently changes the guaranteed behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | test harness weakening security/regression gates | mitigate | additive test file ONLY; `_scripted_model.py`/`live_harness.py`/`agents/capabilities/` porcelain-empty gates in both tasks; characterization suite runs unmodified |
| T-15-05 | Tampering | golden re-baseline (INV-3 violation) | mitigate | zero `SNAPSHOT_UPDATE`; `git status --porcelain golden/` empty gate; on characterization failure: fix the violation, never the golden |
| T-15-06 | Repudiation | gate evidence not reproducible | mitigate | gate is a single deterministic command recorded in SUMMARY; per-commit cadence pins loader + contract tests (13-03 cadence) |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | zero package installs this phase (pytest + existing harness only) |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_loader.py tests/agents/test_prompt_contracts.py -q` (quick run) exits 0
- Full targeted gate (Task 2 command) exits 0 with goldens porcelain-empty
- `git diff --stat` for this plan confined to `backend/tests/agents/test_prompt_contracts.py`
- OFFLINE-SUITE CAVEAT: never run unscoped backend pytest offline; `python3.11`, no venv
</verification>

<success_criteria>
- ROADMAP SC4 fully landed: contract-grep pins for all five prompts as durable pytest, the scripted-model pin proves ppt deliverable resolution yields the validator's re-emitted artifact deck, characterization goldens byte-identical
- The LV-02 composition test reproduces the live evidence shape (QA commentary first, single artifact-wrapped complete deck second) and asserts narration does NOT win
</success_criteria>

<output>
Create `.planning/phases/15-live-pass-prompt-contract-closure/15-02-SUMMARY.md` when done (record the gate command output counts and the porcelain-empty proof — 15-03 cites this as the offline-evidence pointer).
</output>
