---
phase: 32-run-screen-redesign-a4
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/hooks/useWorkflow.ts
  - frontend/src/hooks/useRunChat.ts
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/hooks/__tests__/useWorkflow.pipelineCancelled.test.ts
autonomous: true
requirements: [SC-4, SC-2]
must_haves:
  truths:
    - "pipeline_cancelled sets a cancelled terminal marker on pipelineState so runLaneState can derive terminal (ISS-035, SC-4, LIVE-STATE-CONTRACT §1)"
    - "The FIX-039 unconditional agent_start accumulator reset ordering is unchanged (edits stay inside the pipeline_cancelled case)"
    - "useRunChat.runId is fed pipelineState.pipelineRunId ?? activePipelineRunId so the command path targets the live building run (ISS-036) — SSE stays dormant (LOCK-B)"
    - "The SC-001 useWorkflow prototype-specify literal (isSpecifyRerun) is replaced by a generic spec-revision signal; review_gate_ready parses the new update_specs_eligible flag defensively (SC-2, SC-001)"
  artifacts:
    - path: "frontend/src/hooks/useWorkflow.ts"
      provides: "pipeline_cancelled cancelled marker + generic spec-revision signal (no prototype-specify literal)"
    - path: "frontend/src/hooks/useRunChat.ts"
      provides: "runId prop threading for the live building run (SSE dormant)"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "runId wiring + review_gate_ready parse of update_specs_eligible/artifact_kind into reviewGateData"
    - path: "frontend/src/hooks/__tests__/useWorkflow.pipelineCancelled.test.ts"
      provides: "reducer regression guard: cancelled marker set + FIX-039 ordering intact"
  key_links:
    - from: "frontend/src/hooks/useWorkflow.ts pipeline_cancelled case"
      to: "pipelineState cancelled terminal marker"
      via: "reducer sets a terminal-reason field so RunChatLane derives terminal"
      pattern: "cancelled"
    - from: "frontend/src/app/dashboard/page.tsx useRunChat call"
      to: "useRunChat runId"
      via: "runId: pipelineState.pipelineRunId ?? activePipelineRunId"
      pattern: "pipelineRunId"
---

<objective>
Land the chat-lane STATE plumbing the reskin (plan 06) and Steps (plan 08) consume: the ISS-035 cancel-ack marker, the ISS-036 runId thread, the SC-001 useWorkflow literal fix, and the defensive parse of the new backend `update_specs_eligible` flag. This is pure logic (no styling), FIX-039-sensitive, so it lands isolated and tested before the reskin renders it.

Purpose: Make the cancelled/terminal state derivable (so SC-4's "Cancelled by you" ack has a signal), correct the latent SSE runId bug (ISS-036) while leaving SSE dormant (LOCK-B), and remove the `prototype-specify` literal (SC-001 L2 leak) — driving the spec-revision badge off a generic signal + the plan-04 payload flag.
Output: A cancelled marker on `pipelineState`, runId threading into `useRunChat`, the generic spec-revision signal, the `update_specs_eligible`/`artifact_kind` parse into `reviewGateData`, and a reducer regression test.

HARD LIMITS: FIX-039 — the unconditional per-run accumulator reset in the `agent_start` case (useWorkflow.ts L271-306) must NOT be reordered/duplicated; keep ISS-035 edits INSIDE the `pipeline_cancelled` case (L534). LOCK-B — thread the runId correctly but do NOT mount `RunConnectionProvider` or set `NEXT_PUBLIC_SSE_TRANSPORT`; legacy WS stays the ACTIVE transport.

INDEPENDENCE NOTE: Scheduled Wave 2 (not Wave 1) to honor the D-15 token-layer-alone constraint; it consumes no tokens. It reads the plan-04 `update_specs_eligible` field DEFENSIVELY (optional; undefined when absent), so it has no hard ordering dependency on plan 04 — the field NAME is the pinned contract.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ISS-035 cancel-ack marker + FIX-039-safe reducer test (RED first)</name>
  <files>frontend/src/hooks/useWorkflow.ts, frontend/src/hooks/__tests__/useWorkflow.pipelineCancelled.test.ts</files>
  <behavior>
    - After dispatching a pipeline_cancelled event, the reducer sets a terminal cancelled marker on pipelineState (e.g. a terminalReason cancelled field) alongside the existing isRunning false / totalDuration / completedCount, so a downstream selector derives the LIVE-STATE-CONTRACT §1 cancelled state (not fall through to idle).
    - The unconditional agent_start accumulator reset (FIX-039) still runs BEFORE status running and is neither reordered nor duplicated — asserted by firing agent_start twice (regenerate) and confirming the accumulators are REPLACED not appended.
  </behavior>
  <read_first>
    - frontend/src/hooks/useWorkflow.ts L271-306 (case agent_start — the FIX-039 unconditional reset + comment L280-291 documenting the ordering that MUST be preserved)
    - frontend/src/hooks/useWorkflow.ts L488 (case pipeline_failed — the symmetric teardown to mirror) and L534 (case pipeline_cancelled — the edit site; today sets NO cancelled marker)
    - frontend/src/types/index.ts (WorkflowStatus includes cancelled; the PipelineRunState shape to extend with the terminal marker)
    - .planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md §1 (cancelled row: Cancelled by you + relaunch)
  </read_first>
  <action>
    Write the reducer test FIRST (RED): assert (a) pipeline_cancelled sets the cancelled terminal marker; (b) the FIX-039 double-agent_start replace-not-append invariant holds. Then, INSIDE the pipeline_cancelled case ONLY, add the cancelled terminal marker to the returned pipelineState (a terminalReason / cancelledByUser style field on PipelineRunState — extend the type in types/index.ts if needed, additively). Keep it symmetric with the pipeline_failed teardown (L488). Do NOT touch the agent_start case. Do NOT push a chat message from the reducer (the Cancelled by you transcript line is rendered by RunChatLane in plan 06 off this marker + the generic RunLaneState).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/__tests__/useWorkflow.pipelineCancelled.test.ts src/hooks/useWorkflow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New test green: pipeline_cancelled sets the cancelled marker; the FIX-039 replace-not-append invariant holds under double agent_start.
    - The pre-existing useWorkflow.test.ts suite still passes (no reducer regression).
    - git diff useWorkflow.ts shows edits confined to the pipeline_cancelled case (+ additive type), NOT the agent_start reset block.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SC-001 L2 literal fix + defensive parse of update_specs_eligible</name>
  <files>frontend/src/hooks/useWorkflow.ts, frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/hooks/useWorkflow.ts L288-322 (isSpecifyRerun = wasAlreadyDone && agentId === prototype-specify at L293 → specRevisionCount bump — the SC-001 L2 leak; wasAlreadyDone at L292 is ALREADY the generic signal)
    - frontend/src/app/dashboard/page.tsx L804-838 (case review_gate_ready sets reviewGateData; redoable: data.redoable ?? false at L815) and L160-167 (the reviewGateData state type to extend)
  </read_first>
  <action>
    (a) SC-001 L2: replace agentId === prototype-specify at useWorkflow.ts L293 with the GENERIC signal already present — a re-fired agent_start on an already-done agent (wasAlreadyDone) is itself the spec-revision signal (ND-11); drive isSpecifyRerun / specRevisionCount off wasAlreadyDone (+ the declared spec-revision context if available), NOT the name literal. Delete the prototype-specify string.
    (b) Parse the plan-04 flag: in page.tsx case review_gate_ready (L804), read data.update_specs_eligible and data.artifact_kind DEFENSIVELY (default false / undefined when absent) into the reviewGateData object next to redoable; extend the reviewGateData state type (L160) additively with updateSpecsEligible optional boolean and artifactKind optional string. This is the single parse point; plan 06 maps it into laneGate and plan 08 into the Steps inline gate. Do NOT add any prototype-* literal.
  </action>
  <verify>
    <automated>cd frontend && grep -c "prototype-specify" src/hooks/useWorkflow.ts | grep -qx 0 && grep -q "update_specs_eligible" src/app/dashboard/page.tsx && npx vitest run src/hooks/useWorkflow.test.ts && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - grep for prototype-specify in useWorkflow.ts returns 0; the spec-revision badge is driven off the generic wasAlreadyDone signal.
    - page.tsx review_gate_ready parses update_specs_eligible + artifact_kind into reviewGateData (defensive, optional); the type is extended additively.
    - useWorkflow.test.ts green; tsc --noEmit identity (0 new errors).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ISS-036 runId threading into useRunChat (SSE dormant, LOCK-B)</name>
  <files>frontend/src/hooks/useRunChat.ts, frontend/src/app/dashboard/page.tsx</files>
  <read_first>
    - frontend/src/hooks/useRunChat.ts L54-62 (runId: string | null prop) + L148-174 (runId derivation from data.run_id)
    - frontend/src/app/dashboard/page.tsx L917-918 (useRunChat({ runId: activePipelineRunId, ... }) — bound to the clarify-only id, null while building) + L158/L813/L165 (pipelineState.pipelineRunId is the live building run id; activePipelineRunId set from a clarify event)
    - .planning/phases/31-chat-lane-mvp-a3/31-VERIFICATION.md (Key Link row: SSE branch present but DORMANT — provider not mounted; legacy WS is the ACTIVE path)
  </read_first>
  <action>
    At page.tsx L917, thread runId: pipelineState.pipelineRunId ?? activePipelineRunId into the useRunChat call so the REST command path targets the live building run instead of null-then-fresh-POST. This is a PURE FE prop change. LOCK-B: do NOT mount RunConnectionProvider, do NOT set NEXT_PUBLIC_SSE_TRANSPORT, do NOT delete the legacy WS path — SSE stays dormant; the fix merely corrects the latent bug so an SSE-ON future targets the right run. Verify assumption A2 at wiring time: if pipelineState.pipelineRunId is null while building too, keep the ?? activePipelineRunId fallback (it is already the correct precedence) and note it in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && grep -q "pipelineRunId ?? activePipelineRunId\|pipelineRunId ?? activePipelineRunId" src/app/dashboard/page.tsx && npx vitest run src/hooks/useRunChat.test.ts && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx useRunChat call passes runId: pipelineState.pipelineRunId ?? activePipelineRunId.
    - useRunChat.test.ts green; tsc --noEmit identity.
    - LOCK-B held: RunConnectionProvider is NOT mounted (grep of app/layout.tsx unchanged) and NEXT_PUBLIC_SSE_TRANSPORT is untouched — SSE dormant, legacy WS still the active transport.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WS event → reducer state | Untrusted WS payload (pipeline_cancelled, review_gate_ready) is parsed into client state; the reducer must handle missing/extra fields defensively. No new network boundary (SSE stays dormant). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05-01 | Tampering (FIX-039 regression) | useWorkflow agent_start reset | mitigate | ISS-035 edits confined to the pipeline_cancelled case; a reducer test asserts the double-agent_start replace-not-append invariant. |
| T-32-05-02 | Elevation (LOCK-B SSE activation) | useRunChat runId + provider | mitigate | Pure prop change only; RunConnectionProvider not mounted, NEXT_PUBLIC_SSE_TRANSPORT untouched — asserted by the SSE-dormant acceptance check. |
| T-32-05-03 | Info-disclosure (SC-001 name leak) | spec-revision signal | mitigate | Drive off the generic wasAlreadyDone signal + declared flag; grep confirms 0 prototype-specify literals. |
| T-32-05-04 | Tampering (bad WS payload) | review_gate_ready parse | mitigate | Parse update_specs_eligible/artifact_kind defensively with defaults; no crash on absent/extra fields. |
No new trust boundary introduced (no live server, no SSE activation).
</threat_model>

<verification>
- cd frontend && npx vitest run src/hooks/__tests__/useWorkflow.pipelineCancelled.test.ts src/hooks/useWorkflow.test.ts src/hooks/useRunChat.test.ts — green.
- cd frontend && npx tsc --noEmit | grep -v mockApi.ts | grep -c "error TS" — 0 (identity).
- grep -c "prototype-specify" src/hooks/useWorkflow.ts — 0 (SC-001 L2 closed).
- LOCK-B: app/layout.tsx unchanged (RunConnectionProvider still not mounted); NEXT_PUBLIC_SSE_TRANSPORT untouched.
- git diff useWorkflow.ts confined to the pipeline_cancelled case + additive types (NOT the agent_start reset).
</verification>

<success_criteria>
SC-4 (state half) + SC-2 (SC-001 FE-state half): pipeline_cancelled produces a derivable cancelled terminal marker (FIX-039 ordering intact); useRunChat targets the live run (SSE dormant, LOCK-B); the prototype-specify literal is gone and the update_specs_eligible flag is parsed defensively — the single feed for plans 06/08.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-05-SUMMARY.md` when done.
</output>
