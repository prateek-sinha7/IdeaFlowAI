---
phase: 32-run-screen-redesign-a4
plan: 10
type: execute
wave: 4
depends_on: [32-06, 32-07, 32-08, 32-09]
files_modified:
  - frontend/e2e/tests/ts-m.questionnaire.spec.ts
  - frontend/e2e/tests/ts-c.input-trigger.spec.ts
  - frontend/e2e/tests/ts-i.agent-panels.spec.ts
  - frontend/e2e/tests/ts-f.skills-hooks.spec.ts
  - frontend/e2e/tests/ts-n.review-gate.spec.ts
autonomous: true
requirements: [SC-4]
must_haves:
  truths:
    - "The brittle color-class assertions (#1B2A4A, bg-gray-900, font-mono toHaveClass/locators) in ts-m/ts-c/ts-i/ts-f/ts-n are replaced by token/data-testid assertions that survive the reskin (SC-4)"
    - "The targeted specs are verified BY DELTA against the pre-existing 128-red mocked baseline (DEF-29-06-1) — they move toward green with NO new reds introduced elsewhere (SC-4)"
  artifacts:
    - path: "frontend/e2e/tests/ts-i.agent-panels.spec.ts"
      provides: "progress-fill asserted by data-testid, not the div.bg-[#1B2A4A] locator"
    - path: "frontend/e2e/tests/ts-n.review-gate.spec.ts"
      provides: "gate textarea asserted by data-testid, not textarea.font-mono"
  key_links:
    - from: "frontend/e2e/tests/*.spec.ts"
      to: "the reskinned run-screen DOM (data-testids from plans 06/08)"
      via: "role/text + data-testid selectors replacing color-class locators"
      pattern: "data-testid"
---

<objective>
Harden the run-screen e2e specs for the reskin (SC-4): replace the brittle color-class assertions (`#1B2A4A`, `bg-gray-900`, `font-mono` `toHaveClass`/locators) — which break the moment the token layer lands — with token/`data-testid` assertions against the reskinned DOM (the data-testids were added in plans 06/08). Verify BY DELTA against the pre-existing 128-red mocked baseline: the target is that these specs MOVE toward green with NO new reds elsewhere — absolute-green is NOT the target.

Purpose: The reskin retires the navy/gray classes the old specs assert on, so those assertions must be re-anchored on stable selectors. This is the LAST wave — the reskinned DOM + testids must exist first.
Output: The 5 targeted specs re-anchored on role/text + data-testid selectors, verified BY DELTA.

HARD LIMITS: e2e specs ONLY — the components + their data-testids are owned by earlier waves (06 chat lane, 08 gate/clarify/Steps). If a required testid is missing from a reskinned component, note it in the SUMMARY as a follow-up rather than editing the component here (avoid cross-wave file conflict). NEVER chase absolute-green — verify BY DELTA. NEVER run the full backend pytest.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/phases/32-run-screen-redesign-a4/32-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor the color-class assertions to token/data-testid selectors</name>
  <files>frontend/e2e/tests/ts-m.questionnaire.spec.ts, frontend/e2e/tests/ts-c.input-trigger.spec.ts, frontend/e2e/tests/ts-i.agent-panels.spec.ts, frontend/e2e/tests/ts-f.skills-hooks.spec.ts, frontend/e2e/tests/ts-n.review-gate.spec.ts</files>
  <read_first>
    - .planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md §6 (the exact brittle sites: ts-m:57/79/83, ts-c:149/150, ts-i:100/107, ts-f:95, ts-n:139/183)
    - frontend/e2e/tests/ts-i.agent-panels.spec.ts L100-107 (the `div.bg-[#1B2A4A]` progress-fill locator idiom) + ts-n.review-gate.spec.ts L139/183 (`textarea.font-mono` locator)
    - the reskinned components + their data-testids from plans 06 (chat lane / stop / suggestions) and 08 (gate/clarify inline cards, Steps drill-down levels); grep the components for the added data-testid names to assert against
  </read_first>
  <action>
    Replace the enumerated brittle assertions with stable selectors:
    - ts-m:57/79/83 + ts-c:149/150 (`toHaveClass(/bg-\[#1B2A4A\]/)` selected-state) → a `data-testid` + `data-selected`/`aria-selected` assertion.
    - ts-i:100/107 (`div.bg-[#1B2A4A]` progress-fill locator) → the `data-testid` on the fill.
    - ts-f:95 (`toHaveClass(/bg-gray-900/)` count pill) → a token-class or `data-testid` assertion.
    - ts-n:139/183 (`locator("textarea.font-mono")`) → the `data-testid` on the gate textarea.
    Prefer role/text selectors (they survive the reskin, POR §7); add `data-testid` assertions only where color/class was load-bearing, referencing the testids the components already expose (plans 06/08). Do NOT edit any component file in this plan.
  </action>
  <verify>
    <automated>cd frontend && grep -rciE "1B2A4A|bg-gray-900|textarea\.font-mono" e2e/tests/ts-m.questionnaire.spec.ts e2e/tests/ts-c.input-trigger.spec.ts e2e/tests/ts-i.agent-panels.spec.ts e2e/tests/ts-f.skills-hooks.spec.ts e2e/tests/ts-n.review-gate.spec.ts | awk -F: '{s+=$2} END{print s}' | grep -qx 0 && echo BRITTLE_CLEAR</automated>
  </verify>
  <acceptance_criteria>
    - The enumerated `#1B2A4A` / `bg-gray-900` / `textarea.font-mono` brittle assertions are gone from all 5 specs (grep sum = 0).
    - The replacements use role/text + data-testid selectors that reference testids the reskinned components expose.
    - Only the 5 e2e spec files are in git diff (no component edits).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify BY DELTA against the 128-red mocked baseline</name>
  <files>frontend/e2e/tests/ts-i.agent-panels.spec.ts, frontend/e2e/tests/ts-n.review-gate.spec.ts, frontend/e2e/tests/ts-m.questionnaire.spec.ts, frontend/e2e/tests/ts-c.input-trigger.spec.ts, frontend/e2e/tests/ts-f.skills-hooks.spec.ts</files>
  <read_first>
    - .planning/phases/32-run-screen-redesign-a4/32-VALIDATION.md (Sampling Rate + "verify BY DELTA vs 128-red baseline"; DEF-29-06-1 — the pre-existing home-redesign reds are NOT this phase's target)
    - frontend/playwright.config.ts (the mocked project config)
  </read_first>
  <action>
    Run the 5 targeted specs under the mocked project and compare to the pre-existing 128-red baseline (DEF-29-06-1). The target: the re-anchored specs MOVE toward green (their re-anchored assertions pass against the reskinned DOM) and introduce NO new reds elsewhere. If a re-anchored assertion still fails because a testid the component should expose is missing, record it as a follow-up in the SUMMARY (do NOT edit the component here — that is a plan 06/08 residual). Do NOT chase absolute-green; the ~120 home-redesign reds owned by feat/ui-2 spec-realignment are out of scope.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --project=mocked e2e/tests/ts-i.agent-panels.spec.ts e2e/tests/ts-n.review-gate.spec.ts e2e/tests/ts-m.questionnaire.spec.ts e2e/tests/ts-c.input-trigger.spec.ts e2e/tests/ts-f.skills-hooks.spec.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The mocked run of the 5 specs completes; the re-anchored assertions pass against the reskinned DOM (or any still-red is documented as a component-testid follow-up, not a spec bug).
    - DELTA is non-regressing: no previously-green run-screen spec is turned red by the re-anchoring.
    - The result is reported as a DELTA vs the 128-red baseline (moved count), NOT as an absolute-green claim.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Test-only changes to e2e spec files; no production code, no runtime boundary, no data flow. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10-01 | Tampering (masking real regressions) | e2e re-anchoring | mitigate | Re-anchor to stable role/text/data-testid selectors that still assert the real behavior; verify BY DELTA so a green→red regression is caught, never chase absolute-green by weakening assertions. |
| T-32-10-SC | Tampering | (no package install) | accept | Test-file edits only; no npm install, no new dependency. |
No new trust boundary; test-only plan.
</threat_model>

<verification>
- cd frontend && npx playwright test --project=mocked <the 5 specs> — the re-anchored assertions pass against the reskinned DOM; DELTA non-regressing vs the 128-red baseline.
- grep for `1B2A4A`/`bg-gray-900`/`textarea.font-mono` across the 5 specs — 0.
- git diff lists ONLY the 5 e2e spec files (no component edits).
- NEVER run the full backend pytest.
</verification>

<success_criteria>
SC-4 (e2e half): the brittle color-class assertions in ts-m/ts-c/ts-i/ts-f/ts-n are replaced by token/data-testid selectors that survive the reskin, and the targeted specs are verified BY DELTA against the 128-red baseline (moved toward green, no new reds) — completing SC-4 alongside the faithful terminal states from plans 05/06.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-10-SUMMARY.md` when done.
</output>
