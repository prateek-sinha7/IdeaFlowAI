---
phase: 32-run-screen-redesign-a4
plan: 08
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-04]
files_modified:
  - frontend/src/components/results/AgentThinkingTab.tsx
  - frontend/src/components/workflow/WaveTreePanel.tsx
  - frontend/src/components/preview/ReviewGatePanel.tsx
  - frontend/src/components/results/PrototypePipelineView.tsx
  - frontend/src/components/results/__tests__/StepsDrilldown.test.tsx
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "Steps renders a 3-level drill-down from real events: L1 overview spine → L2 agent detail + Context-received rail → L3 task detail, dual-source (task_progress AND subagent_*/wave_*) (SC-2, STEPS-ARTIFACT-DERIVATION-CONTRACT)"
    - "KAN-99: the construction checklist caps at N-1 until agent_complete (the final task_progress precedes the fix-loop); N-1 is not treated as a stall (SC-2)"
    - "Gate/clarify render INLINE in Steps via the reused generic InlineGateActions/InlineClarifyActions, driven off updateSpecsEligible (SC-2, KAN-101/95/100/98)"
    - "The SC-001 prototype-specify/prototype-analyze render-path literals in AgentThinkingTab, ReviewGatePanel, and PrototypePipelineView are removed (delete-if-superseded per INV-3, else generalize off the declared flag/artifact-kind) (SC-2, SC-001)"
    - "WaveTreePanel (L3 fanout) is mounted into the drill-down, resolving the ISS-019 below-the-fold problem"
  artifacts:
    - path: "frontend/src/components/results/AgentThinkingTab.tsx"
      provides: "L1/L2 drill-down + Context-received rail + inline gate/clarify + KAN-99 cap + token reskin + SC-001 fix"
    - path: "frontend/src/components/workflow/WaveTreePanel.tsx"
      provides: "L3 dual-source fanout mounted into the drill-down (ISS-019 fold)"
    - path: "frontend/src/components/preview/ReviewGatePanel.tsx"
      provides: "SC-001 de-literalized (drive labels/affordance off updateSpecsEligible/artifactKind, not prototype-* names)"
    - path: "frontend/src/components/results/__tests__/StepsDrilldown.test.tsx"
      provides: "scripted task_progress/wave_*/subagent_* fixtures + KAN-99 N-1 assertion + inline-gate render"
  key_links:
    - from: "frontend/src/components/results/AgentThinkingTab.tsx L3"
      to: "frontend/src/components/workflow/WaveTreePanel.tsx"
      via: "dual-source construction (task_progress + subagent_*/wave_*) mounted into the drill-down"
      pattern: "WaveTreePanel"
    - from: "AgentThinkingTab inline gate"
      to: "InlineGateActions"
      via: "updateSpecsEligible-driven Update-the-Specs affordance (no prototype-* literal)"
      pattern: "InlineGateActions"
---

<objective>
Restructure the "Thinking" tab into the Steps 3-level DRILL-DOWN (SC-2): L1 overview spine → L2 agent detail + sticky "Context received" rail → L3 task detail, dual-source (`task_progress` for the task-loop AND `subagent_*`/`wave_*` for fanout), per STEPS-ARTIFACT-DERIVATION-CONTRACT. Relocate gate/clarify from full-panel overlays to inline Steps cards (reusing the Phase-31 generic InlineGateActions/InlineClarifyActions), carry KAN-99's N-1 cap, close the SC-001 render-path literal leak (AgentThinkingTab:608 + ReviewGatePanel:251-253), and mount WaveTreePanel into the drill-down (ISS-019).

Purpose: The strongest analog for each surface is the file itself (D-15 reuse) — AgentThinkingTab + WaveTreePanel already hold the L1/L2/L3 data; this restructures them into the drill-down and reskins to tokens/primitives.
Output: A 3-level Steps drill-down with inline gate/clarify, KAN-99 cap, dual-source L3, tokenized surfaces, SC-001-clean render paths, and a scripted-fixture test suite.

HARD LIMITS: SC-001 — Steps/gate keyed on GENERIC event/artifact types + the declared updateSpecsEligible/artifactKind flag, never a workflow name; remove the prototype-* render-path literals here. Do NOT touch the composer/config literal sites (AgentsPopup.tsx, AgentLibraryData.ts) — those are Phase 37. KAN-99 — the final task_progress precedes the fix-loop; cap at N-1 until agent_complete; do NOT treat completed_count == total-1 as a stall. Reuse KAN-100's cancel-aware gate wait — do NOT re-implement Stop-while-paused. Consumes the optional gate/clarify props threaded by plans 06/07 (same wave, pinned to the existing GateContext/questionnaire/reviewGateData shapes); verified standalone via scripted fixtures.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: L1 overview spine + L2 agent detail + Context-received rail + token reskin + SC-001 fix</name>
  <files>frontend/src/components/results/AgentThinkingTab.tsx</files>
  <read_first>
    - frontend/src/components/results/AgentThinkingTab.tsx (whole file — props L16-25 incl. runInput/clarifications/revisionParentVersion/originalBriefRootRunId (the C2 Context-received sources), AGENT_ACCENT L31-38 (hardcoded navy — reroute to tokens), the L608 SC-001 literal `[...].includes(a.id)`)
    - .planning/phases/28-chat-contracts-guards-a0/contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md §1 (the 4 artifact kinds: pages/tasks/checks/construction) + §4 (L3 shape)
    - frontend/src/components/ui/{Card,Badge,Tabs,Pill}.tsx + frontend/src/styles/globals.css (tokens/primitives)
  </read_first>
  <action>
    Restructure the Thinking view into the Steps drill-down levels: L1 = overview spine (status line + per-agent progress + clarifications block + trace rows with inline gate strips); L2 = agent detail (reasoning, the 4 artifact-kind blocks, tool calls, full input prompt, output, summary, handoff) + a sticky "Context received" rail sourced from the C2 props (runInput/context_sources/clarifications/revisionParentVersion). Reroute AGENT_ACCENT (L31-38) and any inline navy/#2563eb through the plan-01 tokens + plan-02 primitives (Card/Badge/Tabs). SC-001: replace the L608 `["prototype-specify","prototype-plan","prototype-analyze",...].includes(a.id)` gating with a declared artifact-kind / generic-signal check (drive off the artifactKind/updateSpecsEligible flag from plan 04/05, or a structural event-kind check) — delete the literal list. ALSO close the SC-001 L4 leak in PrototypePipelineView.tsx L389-391 (agents.find(a => a.id === "prototype-specify"/"prototype-plan"/"prototype-analyze")): if the Steps L1/L2 spine supersedes the prototype-only PrototypePipelineView, DELETE it and drop the import from AgentThinkingTab (INV-3 no-dual-impl favors deletion); else generalize its 3 literals off the declared flag. Keep the checks block GREEN (the status-palette governance exception). Preserve existing L1/L2 data wiring (reuse, don't rebuild). Do NOT touch AgentsPopup.tsx / AgentLibraryData.ts (Phase 37 composer/config sites).
  </action>
  <verify>
    <automated>cd frontend && grep -rciE "prototype-specify|prototype-analyze|prototype-plan" src/components/results/AgentThinkingTab.tsx src/components/results/PrototypePipelineView.tsx 2>/dev/null | awk -F: '{s+=$2} END{print s}' | grep -qx 0 && grep -riE "#1B2A4A|#2563eb" src/components/results/AgentThinkingTab.tsx | grep -c . | grep -qx 0 && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - AgentThinkingTab renders L1 spine + L2 detail + a sticky Context-received rail from the C2 props.
    - Zero prototype-specify/prototype-analyze/prototype-plan literals remain in AgentThinkingTab OR PrototypePipelineView (deleted or generalized); zero raw #1B2A4A/#2563eb; tsc identity.
    - The checks block keeps the green governance palette (status exception).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: L3 dual-source (mount WaveTreePanel) + KAN-99 N-1 cap + scripted-fixture tests</name>
  <files>frontend/src/components/workflow/WaveTreePanel.tsx, frontend/src/components/results/AgentThinkingTab.tsx, frontend/src/components/results/__tests__/StepsDrilldown.test.tsx</files>
  <behavior>
    - Given scripted task_progress events (task-loop) AND subagent_spawned/subagent_result + wave_started/wave_completed events (fanout), the L3 construction drill-down renders BOTH sources merged (waves with per-task rows: pending→running→done from spawned→result).
    - KAN-99: when the final task_progress reports completed_count == total-1 (fix-loop pending), the checklist caps at N-1 and does NOT render a stall/error; it reaches N only on agent_complete.
    - WaveTreePanel is mounted INSIDE the drill-down (not below the fold — ISS-019).
  </behavior>
  <read_first>
    - .planning/phases/28-chat-contracts-guards-a0/contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md §2 (dual-source: task_progress fields + wave_*/subagent_* fields) + §3 (KAN-99 N-1 cap contract) + §4 (L3 per-task shape)
    - frontend/src/components/workflow/WaveTreePanel.tsx (whole — WaveTreePanelProps L26-32, statusKind() L34-45; today mounts unconditionally below the fold at DashboardLayout.tsx:1639)
    - frontend/src/components/results/AgentThinkingTab.tsx (the construction block where L3 mounts)
  </read_first>
  <action>
    Write the StepsDrilldown fixture tests FIRST (dual-source merge + KAN-99 N-1 cap + WaveTreePanel-mounted-in-drilldown). Then mount WaveTreePanel into the AgentThinkingTab L3 construction drill-down (resolving ISS-019 — remove/relocate the below-fold standalone mount). Implement the dual-source construction: merge task_progress (completed_tasks/completed_count) with the wave_*/subagent_* families (wave brackets keyed by wave_index; per-task rows from subagent_spawned→subagent_result). Enforce the KAN-99 N-1 cap: cap the checklist at total-1 until agent_complete; never treat completed_count==total-1 as a stall. Reskin WaveTreePanel to tokens/primitives (statusKind → Badge statuses; cancelled→failed-terminal per IN-05).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/__tests__/StepsDrilldown.test.tsx src/components/workflow/ 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - StepsDrilldown test green: dual-source merge renders both task_progress and subagent_*/wave_* data; KAN-99 caps at N-1 until agent_complete (no stall); WaveTreePanel is mounted in the drill-down.
    - WaveTreePanel reskinned to tokens (no raw #1B2A4A/#2563eb); tsc identity.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Inline gate/clarify Steps cards + ReviewGatePanel SC-001 de-literalization</name>
  <files>frontend/src/components/results/AgentThinkingTab.tsx, frontend/src/components/preview/ReviewGatePanel.tsx, frontend/src/components/results/__tests__/StepsDrilldown.test.tsx</files>
  <behavior>
    - A paused gate renders an INLINE gate card in the Steps trace row (reusing InlineGateActions) with Approve/Reject/Redo/Update-the-Specs, the Update-the-Specs affordance shown IFF updateSpecsEligible (KAN-101), the reject-confirm + navigate-home (KAN-95), the !isPipelineRunning terminal fence (KAN-100), and retained edit (KAN-98) — all inherited from the reused generic component.
    - A clarify-waiting state renders an inline clarify card (reusing InlineClarifyActions) emitting the canonical [{question_id, answer}].
    - ReviewGatePanel's icon/label/description + Update-Specs affordance are driven off the declared updateSpecsEligible/artifactKind flag, NOT the prototype-specify/prototype-plan/prototype-analyze literals.
  </behavior>
  <read_first>
    - frontend/src/components/preview/ReviewGatePanel.tsx L251-305 (isSpec/isTasks/isAnalysis === prototype-* literals → icon/label/description + Update-Specs; L265-273 reset-edit-on-fresh-gate KAN-98) — the SC-001 leak to remove
    - frontend/src/components/chat/InlineGateActions.tsx L26-51 (the 4 KAN behaviors + updateSpecsEligible flag) and InlineClarifyActions.tsx (the sibling) — the generic components to REUSE inline in Steps
    - frontend/src/components/layout/DashboardLayout.tsx (the gate/clarify callbacks + reviewGateData now threaded to PreviewPanel→AgentThinkingTab, plans 06/07)
  </read_first>
  <action>
    Add the inline gate/clarify cards to the Steps trace rows by REUSING InlineGateActions/InlineClarifyActions (do NOT re-implement — they are already generic + SC-001-clean, carry all 4 KAN behaviors), fed the optional gate/clarify props threaded by plans 06/07. De-literalize ReviewGatePanel: replace the L251-253 `=== "prototype-specify"/"prototype-plan"/"prototype-analyze"` checks that drive isSpec/isTasks/isAnalysis (icon/label/description + Update-Specs) with the declared updateSpecsEligible/artifactKind flag (from plan 04/05). Keep KAN-98 reset-edit-on-fresh-gate. Extend StepsDrilldown.test with the inline-gate/clarify render + updateSpecsEligible-gated Update-Specs assertion. Verify BY the same generic-flag path the chat lane uses (one approve_review channel regardless of surface).
  </action>
  <verify>
    <automated>cd frontend && grep -ciE "prototype-specify|prototype-analyze|prototype-plan" src/components/preview/ReviewGatePanel.tsx | grep -qx 0 && npx vitest run src/components/results/__tests__/StepsDrilldown.test.tsx src/components/preview/ 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Inline gate/clarify render in Steps via the reused generic components; Update-the-Specs shows only when updateSpecsEligible.
    - Zero prototype-* literals in ReviewGatePanel (grep 0); the labels/affordance drive off the declared flag.
    - StepsDrilldown + ReviewGatePanel suites green; tsc identity.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WS event stream → Steps render | Untrusted live event payloads (task_progress/wave_*/subagent_*/gate) render into the drill-down; must handle missing/partial fields without crashing. No new network boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08-01 | Info-disclosure (SC-001) | Steps/gate render paths | mitigate | Keyed on generic event/artifact kinds + the declared updateSpecsEligible/artifactKind flag; grep confirms zero prototype-* literals in AgentThinkingTab + ReviewGatePanel. |
| T-32-08-02 | Tampering (bad/partial event) | dual-source construction merge | mitigate | Defensive rendering of scripted-fixture-covered event shapes; unknown status → neutral (statusKind fallback); no crash on missing wave/task fields. |
| T-32-08-03 | Elevation (gate action post-terminal) | inline gate | mitigate | Reuse InlineGateActions KAN-100 !isPipelineRunning fence (renders nothing post-terminal); do NOT re-implement Stop-while-paused. |
No new trust boundary; the reused inline components inherit the Phase-31 owner-scoped gate contract.
</threat_model>

<verification>
- cd frontend && npx vitest run src/components/results/ src/components/workflow/ src/components/preview/ReviewGatePanel.test.tsx — green.
- cd frontend && npx tsc --noEmit | grep -v mockApi.ts | grep -c "error TS" — 0.
- grep -ciE "prototype-specify|prototype-analyze|prototype-plan" src/components/results/AgentThinkingTab.tsx src/components/preview/ReviewGatePanel.tsx — 0 (SC-001 L1/L3 closed).
- Do NOT touch AgentsPopup.tsx / AgentLibraryData.ts (Phase 37) — confirm they are absent from git diff.
</verification>

<success_criteria>
SC-2 (Steps half): Steps renders the 3-level drill-down from real events (dual-source L3, WaveTreePanel mounted / ISS-019 fixed), KAN-99 caps at N-1 until agent_complete, gate/clarify render inline via the reused generic components driven off updateSpecsEligible (KAN-101/95/100/98), and the prototype-* render-path literals are gone (SC-001). Reskinned to tokens/primitives.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-08-SUMMARY.md` when done.
</output>
