---
phase: 32-run-screen-redesign-a4
plan: 06
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-05]
files_modified:
  - frontend/src/components/chat/RunChatLane.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/workflow/AgentProgressPanel.tsx
  - frontend/src/components/chat/__tests__/RunChatLane.terminal.test.tsx
autonomous: true
requirements: [SC-4, SC-1]
must_haves:
  truths:
    - "RunChatLane is reskinned to the token layer + primitives and absorbs the AgentProgressPanel controls (stop, revise-as-chat, suggestions-as-chips); the duplicate AgentProgressPanel mount is removed (INV-3 no dual impl) (SC-1, SC-4)"
    - "Cancelled → Cancelled by you ack + Run again; Failed → What went wrong card (agents_failed[] + sanitized error); Degraded → completed with issues card — all off the generic RunLaneState terminal + pipelineState marker (SC-4, LIVE-STATE-CONTRACT §1)"
    - "DashboardLayout derives runLaneState (incl. terminal/cancelled/degraded) from generic signals + maps reviewGateData.updateSpecsEligible→laneGate; no workflow-name branch (SC-001)"
    - "The run output is labelled Deliverable (LOCK-F); data-testids added on the chat lane surfaces"
  artifacts:
    - path: "frontend/src/components/chat/RunChatLane.tsx"
      provides: "token/primitive reskin + absorbed controls + terminal/cancelled/failed/degraded renders + data-testids"
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "runLaneState terminal derivation + laneGate.updateSpecsEligible mapping + duplicate AgentProgressPanel mount removed"
    - path: "frontend/src/components/chat/__tests__/RunChatLane.terminal.test.tsx"
      provides: "cancelled/failed/degraded render assertions"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx runLaneState"
      to: "frontend/src/components/chat/RunChatLane.tsx terminal render"
      via: "generic RunLaneState (terminal) derived from pipelineState cancelled marker + agents_failed"
      pattern: "terminal"
    - from: "DashboardLayout laneGate"
      to: "RunChatLane InlineGateActions updateSpecsEligible"
      via: "reviewGateData.updateSpecsEligible → GateContext.updateSpecsEligible"
      pattern: "updateSpecsEligible"
---

<objective>
Finish the left chat lane: reskin `RunChatLane` to the plan-01 tokens + plan-02 primitives, complete the absorption of `AgentProgressPanel` (stop / revise-as-chat / suggestions-as-chips) and DELETE the duplicate panel mount (INV-3), and render the faithful terminal states — cancelled, failed, degraded — off the plan-05 markers per LIVE-STATE-CONTRACT §1 (SC-4).

Purpose: The lane is already mounted (Phase 31, DashboardLayout.tsx:1592) with the generic props (onStop/onRevise/onRelaunch/suggestions) and the terminal RunLaneState already exists — this plan completes the reskin + absorption + terminal renders and removes the dead second panel.
Output: A token/primitive RunChatLane with "Cancelled by you"/"What went wrong"/"completed with issues" cards + relaunch, DashboardLayout deriving terminal runLaneState + mapping the updateSpecsEligible flag into laneGate, AgentProgressPanel's duplicate mount gone, and terminal-render tests.

HARD LIMITS: SC-001 — composer mode + terminal renders key off the generic RunLaneState / pipelineState markers, never a workflow name. LOCK-F — the deliverable card says "Deliverable". D-15 reuse — reskin the existing RunChatLane; do NOT rebuild from the mock. The KAN cancel-aware gate wait already exists (KAN-100) — do NOT re-implement Stop-while-paused.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
@.planning/phases/31-chat-lane-mvp-a3/31-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin RunChatLane to tokens/primitives + absorb AgentProgressPanel controls + data-testids</name>
  <files>frontend/src/components/chat/RunChatLane.tsx</files>
  <read_first>
    - frontend/src/components/chat/RunChatLane.tsx (whole file — RunLaneState union L50-56, GateContext L59-67 incl. updateSpecsEligible/approveLabel, props L77-101 incl. onStop L95 / suggestions L101, suggestion chips L255-264)
    - frontend/src/components/workflow/AgentProgressPanel.tsx L248-253 (the Stop control) + L299-358 (the revise textarea/modal) — the control SOURCE being absorbed
    - frontend/src/components/ui/Button.tsx, Badge.tsx, Card.tsx, Pill.tsx (the plan-02 primitives to consume) + frontend/src/styles/globals.css (the plan-01 tokens)
  </read_first>
  <action>
    Route RunChatLane's inline navy/gray/#2563eb classes through the plan-01 tokens and plan-02 primitives (Button for stop/revise/relaunch, Badge for status, Card for cards, Pill for suggestion chips). Complete the absorption: the Stop / revise-as-chat / suggestions-as-chips controls (already wired as generic props onStop/onRevise/onRelaunch/suggestions) render IN the lane composer using the primitives — the source behavior is AgentProgressPanel L248/L305/L347. Keep composer mode keyed on the generic RunLaneState (idle/building/clarify/gate/complete/terminal), never a workflow name. Add data-testids on the lane surfaces per RESEARCH §6 (chat lane region, composer, stop button, suggestion chips) — role/text selectors survive the reskin, add testids only where color/class was load-bearing. Deliverable card keeps the "Deliverable" label (LOCK-F).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/RunChatLane.test.tsx && grep -riE "#1B2A4A|#2563eb" src/components/chat/RunChatLane.tsx | grep -c . | grep -qx 0 && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - RunChatLane renders via the ui primitives; no raw #1B2A4A/#2563eb literal remains in the file (grep 0).
    - The stop / revise / suggestion-chip controls render in the lane (absorbed) and fire onStop/onRevise/onSuggestion.
    - Existing RunChatLane.test.tsx still green; tsc identity; data-testids present on the lane surfaces.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminal-state renders — cancelled / failed / degraded cards + relaunch</name>
  <files>frontend/src/components/chat/RunChatLane.tsx, frontend/src/components/chat/__tests__/RunChatLane.terminal.test.tsx</files>
  <behavior>
    - Given RunLaneState terminal + pipelineState cancelled marker: renders a "Cancelled by you" ack + a "Run again" (relaunch) action.
    - Given a failed run (agents_failed present + sanitized error): renders a "What went wrong" card naming the failed agents + resume option "Edit brief & run again" (relaunch).
    - Given a degraded run (status degraded): renders a "completed with issues" card naming failed agents (agents_failed[]) — degraded is a REAL product state (D-12) even though the mocks omit it.
    - Each terminal render keys off the GENERIC RunLaneState/pipelineState marker, never a workflow name.
  </behavior>
  <read_first>
    - .planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md §1 (cancelled / failed / degraded rows — exact copy + composer mode)
    - frontend/src/lib/parseFailedAgents.ts (the failed-agents parser to reuse for the What-went-wrong / degraded cards)
    - frontend/src/components/chat/RunChatLane.tsx (the terminal RunLaneState target L56 + the existing card render idiom)
  </read_first>
  <action>
    Write the terminal-render tests FIRST. Then add the three terminal renders to RunChatLane, driven by the generic RunLaneState terminal + the plan-05 pipelineState cancelled marker + parseFailedAgents. Cancelled → "Cancelled by you" + "Run again". Failed → "What went wrong" (P16) from agents_failed[] + sanitized error + "Edit brief & run again". Degraded → "completed with issues" naming failed agents. Reuse the existing relaunch callback (onRelaunch). Do NOT surface raw error internals — use the sanitized error field only.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/chat/__tests__/RunChatLane.terminal.test.tsx src/components/chat/RunChatLane.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Terminal test green: cancelled renders "Cancelled by you" + relaunch; failed renders "What went wrong" + failed-agent names + relaunch; degraded renders "completed with issues".
    - No workflow-name literal in the terminal render path (grep clean).
    - Sanitized error only (no raw stack/internal field surfaced).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: DashboardLayout — terminal runLaneState + updateSpecsEligible mapping + remove duplicate AgentProgressPanel mount (INV-3)</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/workflow/AgentProgressPanel.tsx</files>
  <read_first>
    - frontend/src/components/layout/DashboardLayout.tsx L1291 (runLaneState derivation — extend to terminal/cancelled/degraded) + L1299-1305 (laneGate GateContext mapping incl. redoable at L1305 — add updateSpecsEligible/approveLabel) + L1623/L1630 (the retained AgentProgressPanel mount + the "absorbed" comment) + L1652-1663 (the full-panel gate branch)
    - frontend/src/app/dashboard/page.tsx reviewGateData.updateSpecsEligible/artifactKind (added in plan 05 — the source field to map)
    - .planning/phases/31-chat-lane-mvp-a3/31-VERIFICATION.md truth 11 (DashboardLayout already derives runLaneState from generic signals — extend it, don't rebuild)
  </read_first>
  <action>
    (a) Extend the DashboardLayout runLaneState derivation (L1291) to yield the generic terminal state when the plan-05 pipelineState cancelled marker is set OR the run is failed/degraded (agents_failed / status), so RunChatLane renders the Task-2 cards. Keep it keyed on generic signals (reviewGateData/questionnaireQuestions/pipelineState), no workflow-name branch (SC-001).
    (b) Map reviewGateData.updateSpecsEligible → laneGate.updateSpecsEligible and reviewGateData.artifactKind → the approveLabel/relabel (KAN-101), mirroring the redoable mapping at L1305 — so the RunChatLane InlineGateActions Update-the-Specs affordance is driven off the declared flag, not a literal.
    (c) INV-3: remove the now-redundant duplicate AgentProgressPanel mount (L1623) since its controls are fully absorbed into RunChatLane (Task 1). If AgentProgressPanel.tsx has no remaining callers after removal, delete the file; if it retains a distinct non-lane caller, leave the file but remove only the duplicate run-lane mount and note the residual caller in the SUMMARY. Also thread the gate/clarify props DashboardLayout already holds down to PreviewPanel (an optional prop passthrough) so plan 07/08 can surface the inline gate in Steps — additive optional props, pinned to the existing GateContext/questionnaire shapes.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/ && grep -rn "AgentProgressPanel" src/components/layout/DashboardLayout.tsx | grep -c . && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - runLaneState yields terminal for cancelled/failed/degraded; laneGate carries updateSpecsEligible + the KAN-101 relabel, mapped from reviewGateData (no prototype-* literal in DashboardLayout).
    - The duplicate AgentProgressPanel run-lane mount is removed (INV-3); if orphaned, AgentProgressPanel.tsx is deleted; git diff proves no second stop/revise implementation remains.
    - DashboardLayout tests green (or updated); tsc identity.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (no new boundary) | FE reskin + render of already-received WS state; no new data fetch, no new input parsing beyond the plan-05 markers. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06-01 | Info-disclosure | "What went wrong" / degraded card | mitigate | Render only the sanitized error + agents_failed[] names (LIVE-STATE-CONTRACT §1); never raw stack/internal fields. |
| T-32-06-02 | Tampering (INV-3 dual impl) | AgentProgressPanel duplicate mount | mitigate | Delete the duplicate mount/file after absorption; git diff confirms one stop/revise implementation only. |
| T-32-06-03 | Info-disclosure (SC-001) | runLaneState / terminal renders | mitigate | Keyed on generic RunLaneState/pipelineState markers; grep confirms no workflow-name literal. |
No new trust boundary (no live server, no SSE activation — LOCK-B intact).
</threat_model>

<verification>
- cd frontend && npx vitest run src/components/chat/ src/components/layout/ — green.
- cd frontend && npx tsc --noEmit | grep -v mockApi.ts | grep -c "error TS" — 0.
- grep -riE "#1B2A4A|#2563eb" src/components/chat/RunChatLane.tsx — 0 (reskinned).
- git diff proves the duplicate AgentProgressPanel mount is gone (INV-3, no dual stop/revise impl).
</verification>

<success_criteria>
SC-4 (faithful terminal states) + SC-1 (lane consumes tokens/primitives): RunChatLane is reskinned + fully absorbs the controls (duplicate panel removed, INV-3), and renders Cancelled/Failed/Degraded per LIVE-STATE-CONTRACT §1 off generic markers; DashboardLayout derives terminal runLaneState + maps the updateSpecsEligible flag (KAN-101) with no workflow-name branch (SC-001); "Deliverable" label held (LOCK-F).
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-06-SUMMARY.md` when done.
</output>
