---
phase: 32-run-screen-redesign-a4
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/app/api/runs.py
  - backend/agents/authz.py
  - backend/tests/agents/test_audit_endpoints.py
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "GET /api/runs/{id}/gate-events, /validation-results, /exec-runs return owner-scoped rows for the authed owner (SC-3)"
    - "A cross-owner or missing run id returns 404 (IDOR→404, never 403, never a 200 with another owner's rows) (SC-3, P13/P25)"
    - "exec-runs surfaces the truncated output_digest only — never raw child output/secrets (SC-3, ASVS V7)"
    - "The additive reads keep import-linter at 4 contracts kept / 0 broken; no migration (tables already exist) (INV-3)"
  artifacts:
    - path: "backend/app/api/runs.py"
      provides: "3 additive read-only owner-scoped audit endpoints on the /api/runs router"
      contains: "gate-events"
    - path: "backend/tests/agents/test_audit_endpoints.py"
      provides: "Owner-scope + IDOR→404 + projection tests for all 3 endpoints (Wave-0 scaffold)"
  key_links:
    - from: "backend/app/api/runs.py audit endpoints"
      to: "WorkflowRun (owner gate) + GateEvent/ValidationResult/ExecRun (child rows)"
      via: "two-layer filter: WorkflowRun.user_id==current_user.id →404, then child .run_id==id AND .owner_id==current_user.id"
      pattern: "user_id == current_user.id"
---

<objective>
Add the 3 additive, READ-ONLY, owner-scoped Audit endpoints that back the Audit tab (SC-3): `GET /api/runs/{id}/gate-events`, `/validation-results`, `/exec-runs`. The three tables (`gate_events`/`validation_results`/`exec_runs`) already exist (alembic 0016/0018), are engine-populated, and carry `owner_id`+`workspace_id`+`run_id` — so this is pure additive reads with NO migration.

Purpose: Repoint the Audit tab (plan 09) off the wrong `hook_runs` source onto real governance/validation/exec data, owner-gated exactly like the existing `get_hook_runs` read.
Output: 3 endpoints cloned from the `get_hook_runs` template + the missing `read_validation_results` scoped-reader seam (or a direct owner-scoped ORM query) + an owner-scope/IDOR test suite.

INDEPENDENCE NOTE: This is a backend-only plan with zero FE/token overlap. It is scheduled in Wave 2 (not Wave 1) only to honor the D-15 hard constraint that Wave 1 is the token layer + primitives ALONE; it has no functional dependency on Wave 1.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/phases/32-run-screen-redesign-a4/32-VALIDATION.md
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Owner-scope + IDOR→404 test suite for the 3 audit endpoints (Wave-0 scaffold)</name>
  <files>backend/tests/agents/test_audit_endpoints.py</files>
  <behavior>
    - Seeds one WorkflowRun owned by user A with gate_events/validation_results/exec_runs child rows; a second run owned by user B.
    - As A: GET each of the 3 endpoints returns 200 with A's rows, ordered by created_at ascending, with the pinned column projection.
    - As A requesting B's run id (or a random uuid): each endpoint returns 404 (never 403, never 200 with B's rows).
    - Unauthenticated: 401/403 via the existing get_current_user dep.
    - exec-runs response contains `output_digest` as-is (truncated) and never a raw-output field.
  </behavior>
  <read_first>
    - backend/app/api/runs.py L1021-1065 (`get_hook_runs` — the exact owner-scoped read + its test-friendly shape) and its existing test (clone `get_hook_runs`'s test pattern)
    - backend/app/models/gate_events.py, validation_results.py, exec_runs.py (the column sets to project + assert)
    - backend/CLAUDE.md "Testing" (python3.11, tests/agents layout, TestClient idiom)
  </read_first>
  <action>
    Write the failing suite FIRST (RED), cloning the existing `get_hook_runs` owner-scope test. Use the offline TestClient + in-memory/SQLite ScopedStore idiom already used by the run endpoint suites (do NOT require live Postgres/Bedrock). Assert the two-layer owner gate: run-not-owned → 404; child rows filtered to the owner. Assert the pinned projections: gate-events {id,run_id,step,gate,outcome,detail,created_at}; validation-results {id,run_id,step,validator,severity,attempt,issues,created_at}; exec-runs {id,run_id,step,argv_json,outcome,exit_code,duration_ms,policy_snapshot_json,output_digest,created_at}. This is the Wave-0 scaffold — it must exist and fail before Task 2 implements.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_audit_endpoints.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The suite RUNS and initially FAILS (endpoints not yet implemented) — captured before Task 2.
    - It covers: 200+owner-rows, cross-owner→404, missing→404, unauth→401/403, and the exec-runs output_digest-only projection for all 3 endpoints.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement the 3 read-only owner-scoped audit endpoints</name>
  <files>backend/app/api/runs.py, backend/agents/authz.py</files>
  <read_first>
    - backend/app/api/runs.py L1021-1065 (`get_hook_runs` — COPY this shape verbatim, swapping model + projection) + L35/41/34 (GateEvent/ValidationResult/ExecRun already imported) + L446-450 (delete_run cascade confirms tables exist)
    - backend/agents/authz.py L831 (`read_gate_events`), L972 (`read_exec_runs`), L119 (`_scope_owner_ws`) — the two existing scoped readers; validation_results has NO reader (the seam gap)
    - backend/app/api/run_files.py header + L1-40 (the two-layer owner-check docstring: IDOR→404 never 403)
    - backend/pyproject.toml L134-185 (the 4 import-linter contracts — app.api is the legal caller; agents.*→app is the forbidden direction)
  </read_first>
  <action>
    Add three `@router.get` handlers on the existing `/api/runs` router (mounted at main.py — no new registration): `/{workflow_id}/gate-events`, `/{workflow_id}/validation-results`, `/{workflow_id}/exec-runs`. Each mirrors `get_hook_runs` EXACTLY:
    - Layer 1 (owner gate): `db.query(WorkflowRun).filter(WorkflowRun.id == workflow_id, WorkflowRun.user_id == current_user.id).first()` → `HTTPException(404, "Workflow run not found")` on miss. Use `user_id` (the principal), NEVER the nullable `owner_id` as the gate.
    - Layer 2 (child rows, defense-in-depth): filter the child model by `.run_id == workflow_id AND .owner_id == current_user.id`, `order_by(created_at.asc())`, project to the pinned inline-dict shape (mirroring get_hook_runs L1052-1064 — prefer inline-dict over a Pydantic schema per table).
    - exec-runs: surface `output_digest` as stored (truncated by design) — do NOT expand or re-read child output.
    For validation-results, EITHER add `ScopedStore.read_validation_results` in authz.py mirroring `read_gate_events`/`read_exec_runs` (owner+ws `_scope_owner_ws`) for seam symmetry, OR query `ValidationResult` directly in the app layer scoped by run_id+owner_id (both additive, both import-linter-safe — Claude's discretion; prefer the reader for symmetry). Keep the handlers app-side reading ORM models directly (like get_hook_runs) so `agents.*→app` is never introduced. NO migration.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_audit_endpoints.py -q 2>&1 | tail -5 && /opt/homebrew/bin/lint-imports 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test_audit_endpoints.py` now PASSES (all owner-scope/IDOR/projection cases green).
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken.
    - `git diff` shows NO alembic/migration file added and NO new table.
    - The 3 endpoints appear on the `/api/runs` router; each returns 404 for a cross-owner/missing id (never 403, never 200 with foreign rows).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → /api/runs/{id}/{gate-events,validation-results,exec-runs} | Untrusted `{id}` path param + authed principal cross here; the endpoints must not leak another owner's audit data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03-01 | Elevation / Info-disclosure (IDOR) | the 3 GET handlers `{run_id}` | mitigate | Two-layer `WorkflowRun.user_id == current_user.id` → 404 (never 403); child rows re-filtered by owner_id (P13/P25). Tested by `test_audit_endpoints.py` cross-owner cases. |
| T-32-03-02 | Info-disclosure | exec-runs response | mitigate | Surface only the truncated `output_digest` column; never re-read or expand raw child output (ASVS V7). Asserted in the test. |
| T-32-03-03 | Info-disclosure | validation-results / gate-events rows | mitigate | `_scope_owner_ws` / owner+workspace filter on the audit tables (AUTHZ-01, nullable=False). |
| T-32-03-04 | Tampering (SQLi) | ORM query | mitigate | SQLAlchemy parameterized ORM only; no raw SQL, no string interpolation of `{id}`. |
| T-32-03-05 | Spoofing | auth | mitigate | `Depends(get_current_user)` on all 3 endpoints (existing dep). |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_audit_endpoints.py -q` — green.
- `/opt/homebrew/bin/lint-imports` — 4 kept / 0 broken.
- `git diff --name-only` — exactly runs.py, authz.py (optional), and the test file; NO alembic migration, NO new table.
- NEVER run the full backend pytest (hangs offline) — the targeted suite + lint-imports is the gate.
</verification>

<success_criteria>
SC-3 (endpoint half): the 3 owner-scoped read endpoints return the pinned projections for the authed owner, resolve cross-owner/missing to 404, surface only the truncated exec digest, keep import-linter 4/0, and add no migration. INV-3 preserved (additive read-only over engine-populated tables; no engine/golden touch).
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-03-SUMMARY.md` when done.
</output>
