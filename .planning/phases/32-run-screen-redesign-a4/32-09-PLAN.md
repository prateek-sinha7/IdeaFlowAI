---
phase: 32-run-screen-redesign-a4
plan: 09
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-03]
files_modified:
  - frontend/src/components/results/AuditTab.tsx
  - frontend/src/lib/api.ts
  - frontend/src/lib/exporters/auditExporter.ts
  - frontend/src/components/results/__tests__/AuditTab.test.tsx
autonomous: true
requirements: [SC-3]
must_haves:
  truths:
    - "The Audit tab reads the 3 new endpoints (gate-events/validation-results/exec-runs) instead of the wrong hook_runs source, per run (SC-3)"
    - "Client-side stat counters / coverage chips / severity filters render over the fetched rows (SC-3)"
    - "CSV and JSON export are client-side only (ND-6 — no PDF, no backend export route) (SC-3)"
    - "The governance status palette (green/amber/red) is the SOLE one-chroma exception; the rest of the tab uses the token layer (SC-3, SC-1)"
  artifacts:
    - path: "frontend/src/components/results/AuditTab.tsx"
      provides: "3-endpoint reader + counters/coverage chips/severity filters + export buttons, tokenized (governance palette exception)"
    - path: "frontend/src/lib/api.ts"
      provides: "3 client fetchers for the audit endpoints (owner-scoped, 404-aware)"
    - path: "frontend/src/lib/exporters/auditExporter.ts"
      provides: "exportAuditCSV / exportAuditJSON client-side blob download (ND-6)"
    - path: "frontend/src/components/results/__tests__/AuditTab.test.tsx"
      provides: "endpoint-read + filter + export assertions"
  key_links:
    - from: "frontend/src/components/results/AuditTab.tsx"
      to: "GET /api/runs/{id}/gate-events,/validation-results,/exec-runs"
      via: "the plan-03 endpoints via lib/api.ts fetchers (replacing getRunHookRuns)"
      pattern: "gate-events|validation-results|exec-runs"
---

<objective>
Repoint the Audit tab off the WRONG data source (`hook_runs` via `getRunHookRuns`) onto the 3 new plan-03 endpoints (SC-3), add client-side stat counters / coverage chips / severity filters, and ship client-side CSV/JSON export (ND-6). The governance status palette (green/amber/red) is the SOLE one-chroma exception; everything else consumes the token layer.

Purpose: The Audit tab exists but reads the wrong table — this repoints it at real governance/validation/exec data and adds the analyst affordances + export.
Output: A tokenized AuditTab reading the 3 endpoints with counters/chips/filters + export, 3 typed client fetchers, an auditExporter util, and tests.

HARD LIMITS: ND-6 — CSV/JSON ONLY, client-side; NO PDF, NO backend export route. Governance palette exception applies to the verdict/severity colors ONLY. D-15 reuse — the CSV/JSON blob-download idiom mirrors the existing exporters; the endpoint-read idiom mirrors getRunHookRuns.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/CHAT-AND-UI-CONVERGENCE-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3 client fetchers for the audit endpoints</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts L928 area (`getRunHookRuns` → `GET /api/runs/{id}/hook-runs` — the exact fetcher idiom + token/getToken pattern to clone)
    - .planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md §3 (the 3 endpoints + their pinned column projections)
  </read_first>
  <action>
    Add three typed client fetchers cloning `getRunHookRuns`: `getRunGateEvents(token, runId)`, `getRunValidationResults(token, runId)`, `getRunExecRuns(token, runId)` → `GET /api/runs/{id}/gate-events`, `/validation-results`, `/exec-runs`. Type each response to the plan-03 projection (gate-events {step,gate,outcome,detail,created_at}; validation-results {step,validator,severity,attempt,issues,created_at}; exec-runs {step,argv_json,outcome,exit_code,duration_ms,policy_snapshot_json,output_digest,created_at}). Handle the 404 (cross-owner/missing) as an empty/typed error the tab renders gracefully — do NOT surface another owner's data.
  </action>
  <verify>
    <automated>cd frontend && grep -qE "gate-events" src/lib/api.ts && grep -qE "validation-results" src/lib/api.ts && grep -qE "exec-runs" src/lib/api.ts && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Three fetchers exist targeting the 3 endpoints; each returns a typed projection; 404 handled gracefully.
    - tsc --noEmit identity.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CSV/JSON export util (client-side, ND-6)</name>
  <files>frontend/src/lib/exporters/auditExporter.ts, frontend/src/components/results/__tests__/AuditTab.test.tsx</files>
  <behavior>
    - exportAuditJSON(rows) produces a pretty-printed JSON blob download of the audit rows.
    - exportAuditCSV(rows) produces a CSV blob download with a header row + one row per audit record, escaping commas/quotes/newlines in cell values.
    - Both trigger a client-side blob download; NEITHER calls a backend export route (ND-6); no PDF path exists.
  </behavior>
  <read_first>
    - frontend/src/lib/exporters/prototypeExporter.ts (the pretty-print + blob-download idiom to mirror — exportPrototype(content, filename?))
    - frontend/src/lib/exporters/storyExporter.ts / pptExporter.ts (sibling exporter shapes)
  </read_first>
  <action>
    Write the exporter tests FIRST. Then create auditExporter.ts with exportAuditCSV(rows, filename?) and exportAuditJSON(rows, filename?) modelled on the existing blob-download exporters. CSV: derive the header from the row keys, escape cell values (wrap in quotes + double internal quotes; handle commas/newlines). JSON: pretty-print. Client-side only — no fetch, no backend route (ND-6). No PDF.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/results/__tests__/AuditTab.test.tsx 2>&1 | tail -6 && grep -ciE "\.pdf|export-pdf|api/.*export" src/lib/exporters/auditExporter.ts | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Exporter tests green: JSON pretty-prints; CSV has a header + escaped cells; both are client-side blob downloads.
    - No PDF / backend-export reference in auditExporter.ts (grep 0) — ND-6 honored.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Repoint AuditTab to the 3 endpoints + counters/coverage chips/severity filters + export</name>
  <files>frontend/src/components/results/AuditTab.tsx, frontend/src/components/results/__tests__/AuditTab.test.tsx</files>
  <behavior>
    - AuditTab fetches gate-events + validation-results + exec-runs for the current run (via the Task-1 fetchers), replacing the getRunHookRuns/hook_runs source.
    - Renders stat counters (e.g. #gates pass/block/wait, #validations by severity, #exec allowed/denied/killed), coverage chips, and severity filters that filter the rendered rows.
    - Export CSV / Export JSON buttons call the Task-2 util over the current (filtered) rows.
    - The governance verdict/severity colors use the green/amber/red status palette (the SOLE one-chroma exception); all other chrome uses the token layer + primitives.
  </behavior>
  <read_first>
    - frontend/src/components/results/AuditTab.tsx (whole — L16-20 getRunHookRuns/hookRuns source to REPLACE, L368 component, L373-392 the historicRuns fetch to repoint)
    - frontend/src/components/ui/{Badge,Card,Pill,Button,Tabs}.tsx + globals.css (tokens/primitives + the status ramp for the governance exception)
  </read_first>
  <action>
    Write/extend the AuditTab tests FIRST (fetches the 3 endpoints; filters apply; export invoked). Then repoint AuditTab: replace the getRunHookRuns/hook_runs read with the Task-1 fetchers keyed on workflowRunId (the prop already exists — do NOT change the prop signature PreviewPanel passes). Add the client-side stat counters, coverage chips, and severity filters over the merged rows. Wire Export CSV / Export JSON to the Task-2 util. Reskin to tokens/primitives; the governance verdict/severity colors are the ONLY green/amber/red usage (status-palette exception). Keep the empty/404 state graceful.
  </action>
  <verify>
    <automated>cd frontend && ( grep -c "getRunHookRuns" src/components/results/AuditTab.tsx | grep -qx 0 ) && npx vitest run src/components/results/__tests__/AuditTab.test.tsx 2>&1 | tail -6 && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - AuditTab reads the 3 endpoints (getRunHookRuns removed); counters/coverage chips/severity filters render and filter; export buttons invoke the util.
    - Governance colors are the only green/amber/red; the rest is tokenized (no raw #1B2A4A/#2563eb).
    - AuditTab.test.tsx green; tsc identity; the workflowRunId prop signature is unchanged (PreviewPanel mount untouched).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → 3 audit endpoints | The tab fetches owner-scoped audit rows; a 404 (cross-owner/missing) must render gracefully, never surface another owner's data. |
| audit rows → client export file | Rows are serialized to a client-side CSV/JSON download; no data leaves the client to a third party. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-09-01 | Info-disclosure (IDOR downstream) | audit fetchers | mitigate | Rely on the plan-03 owner-scoped 404; the tab renders the 404 as empty, never another owner's rows. |
| T-32-09-02 | Info-disclosure (export data handling) | auditExporter CSV/JSON | mitigate | Client-side only (ND-6), no upload/third-party; only the already-authorized owner-scoped rows are serialized; exec output_digest is already truncated upstream. |
| T-32-09-03 | Tampering (CSV injection) | exportAuditCSV | mitigate | Escape cell values (quote-wrap + double quotes; handle commas/newlines) so a cell cannot break the CSV structure. |
No new trust boundary beyond the plan-03 owner-scoped reads.
</threat_model>

<verification>
- cd frontend && npx vitest run src/components/results/__tests__/AuditTab.test.tsx — green.
- cd frontend && npx tsc --noEmit | grep -v mockApi.ts | grep -c "error TS" — 0.
- grep -c "getRunHookRuns" src/components/results/AuditTab.tsx — 0 (repointed).
- grep -ciE "\.pdf|export-pdf|api/.*export" src/lib/exporters/auditExporter.ts — 0 (ND-6, client-only).
</verification>

<success_criteria>
SC-3 (tab + export half): the Audit tab reads the 3 owner-scoped endpoints (hook_runs source removed), renders counters/coverage chips/severity filters, exports CSV/JSON client-side (ND-6, no PDF), keeps the governance status palette as the sole one-chroma exception, and consumes the token layer everywhere else.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-09-SUMMARY.md` when done.
</output>
