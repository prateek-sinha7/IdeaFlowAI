---
phase: 32-run-screen-redesign-a4
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/tests/agents/characterization/_normalize.py
  - backend/tests/agents/test_sc001_gate_flag.py
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "review_gate_ready.data carries a GENERIC, name-free eligibility discriminator (update_specs_eligible + artifact_kind) stamped only at the inline analyze/spec call site — never a workflow/agent literal (SC-2, SC-001)"
    - "The FE can drive the Update-the-Specs affordance off the payload flag with zero prototype-analyze/prototype-specify literal (SC-001 leak closed at the source)"
    - "The 5 characterization goldens stay BYTE-identical after the change, proven by adding the new key(s) to _VOLATILE_STRIP_KEYS (INV-3)"
    - "No agent-runtime / agent-loop change — deepagents runtime untouched; this is metadata on an existing event only (INV-13)"
  artifacts:
    - path: "backend/agents/execution_engine/engine.py"
      provides: "update_specs_eligible + artifact_kind stamped on review_gate_ready.data, mirroring the redoable precedent"
      contains: "update_specs_eligible"
    - path: "backend/tests/agents/characterization/_normalize.py"
      provides: "the new key(s) added to _VOLATILE_STRIP_KEYS so goldens stay byte-identical"
      contains: "update_specs_eligible"
    - path: "backend/tests/agents/test_sc001_gate_flag.py"
      provides: "asserts the flag is stamped at the analyze/spec gate + name-free + goldens-neutral"
  key_links:
    - from: "engine.py review_gate_ready emit helper"
      to: "review_gate_ready.data.update_specs_eligible"
      via: "inline call-site parameter (True only for analyze/spec gates, via _artifact_kind_for), mirroring redoable"
      pattern: "update_specs_eligible"
---

<objective>
Close the SC-001 literal leak AT ITS SOURCE by stamping a generic, name-free eligibility discriminator onto `review_gate_ready.data` — the ONE sanctioned engine-payload touch this phase makes. Today the payload carries only `redoable`; the FE has no analyze/update-specs discriminator, so it decides "is this an analyze gate?" by matching the `prototype-analyze`/`prototype-specify` agent-id string literal — that IS the leak. This plan supplies the declared flag so the FE (plans 05/06/08) can drop the literals.

Purpose: Mirror the proven `redoable`/`_VOLATILE_STRIP_KEYS` precedent EXACTLY so the 5 characterization goldens stay byte-identical (INV-3) while the FE gains a generic `updateSpecsEligible` signal (SC-001 / KAN-101).
Output: `update_specs_eligible: bool` (and `artifact_kind: str`) on `review_gate_ready.data`, stamped only at the inline analyze/spec call site via the existing `_artifact_kind_for`; the new key(s) added to `_VOLATILE_STRIP_KEYS`; a proof the goldens are byte-identical.

HARD LIMIT: This is metadata on an EXISTING event. Do NOT touch `deep_agent_runner`/`create_deep_agent`/the agent loop (INV-13). Do NOT add a new event type. Do NOT reference any workflow/agent name literal in the engine — derive eligibility structurally from `_artifact_kind_for(spec)` exactly as `redoable` is derived (name-free path).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
@backend/CLAUDE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp update_specs_eligible + artifact_kind on review_gate_ready.data (mirror redoable)</name>
  <files>backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py L4463-4513 (the review_gate_ready emit helper: the `redoable` param at L4463, the docstring L4475-4480 documenting the generic-name-free discriminator pattern, and the `.data` dict where `redoable` is stamped at L4513)
    - backend/agents/execution_engine/engine.py L2716, L3540 (the INLINE call sites that pass `redoable=True` — where the generic analyze/spec eligibility must also be stamped True) and L2731/L3560/L3585/L3730/L4310 (the `_ek = self._artifact_kind_for(spec)` derivations already computed at the call sites)
    - backend/agents/execution_engine/engine.py L5217-5234 (`_AGENT_KIND_MAP` + `_artifact_kind_for(spec)` fallback "summary" — the name-free kind resolver to key eligibility on)
  </read_first>
  <action>
    Add an `update_specs_eligible: bool = False` parameter (and optionally pass through the already-computed `artifact_kind` string) to the review_gate_ready emit helper, mirroring `redoable` byte-for-byte: stamp both new keys into the `.data` dict right next to `redoable`. Compute eligibility STRUCTURALLY at the inline call site from `self._artifact_kind_for(spec)` — the same `_ek` value the call site already computes — using the artifact-kind (e.g. the analyze/spec/plan kinds) to decide `update_specs_eligible=True`; NEVER compare `spec.id` to a workflow/agent name literal. Declared/user-composed `gate:human` gates that reach the helper WITHOUT passing the flag default to `update_specs_eligible=False` (exactly as `redoable` defaults False), so custom prototype-like workflows get the affordance IFF the structural kind matches — which is the SC-001 generalization. Keep the change minimal: helper signature + `.data` dict + the inline call-site argument. No new event type, no agent-loop change.
  </action>
  <verify>
    <automated>cd backend && grep -c "update_specs_eligible" agents/execution_engine/engine.py && python3.11 -m pytest tests/agents/test_sc001_gate_flag.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `review_gate_ready.data` contains `update_specs_eligible` (and `artifact_kind`) stamped next to `redoable`.
    - `grep -nE "prototype-analyze|prototype-specify|prototype-plan" agents/execution_engine/engine.py` shows NO new literal introduced by this change (the eligibility is derived from `_artifact_kind_for`, not a name).
    - The emit helper defaults `update_specs_eligible=False` when the flag is not passed (declared/user gates unaffected).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the new keys to _VOLATILE_STRIP_KEYS and PROVE the 5 goldens byte-identical</name>
  <files>backend/tests/agents/characterization/_normalize.py, backend/tests/agents/test_sc001_gate_flag.py</files>
  <behavior>
    - test_sc001_gate_flag: an analyze/spec gate emits review_gate_ready with update_specs_eligible=True + a resolved artifact_kind; a non-eligible gate emits update_specs_eligible=False; NO prototype-* literal appears in the engine emit path.
    - After adding update_specs_eligible + artifact_kind to _VOLATILE_STRIP_KEYS, ALL 5 characterization goldens pass byte-identical with NO SNAPSHOT_UPDATE / no golden fixture regeneration.
  </behavior>
  <read_first>
    - backend/tests/agents/characterization/_normalize.py L101 (`_VOLATILE_STRIP_KEYS` frozenset), L142-157 (the docstring documenting `redoable` as the precedent-to-mirror), L148 (`redoable` entry), L207 (the strip application)
    - backend/tests/agents/test_characterization_prototype.py (the golden harness invocation shape)
  </read_first>
  <action>
    Add `"update_specs_eligible"` and `"artifact_kind"` to `_VOLATILE_STRIP_KEYS` (next to `redoable`, updating the mirror-comment to name them). Then run all 5 characterization goldens WITHOUT snapshot-update and confirm byte-identity — this empirically closes Assumption A1 (the precedent is strong but MUST be proven, not presumed). Add `test_sc001_gate_flag.py` asserting the flag semantics + name-freedom (RED first, then green after Task 1). If a golden is NOT byte-identical after the strip, STOP and report — do not regenerate the fixture (that would violate INV-3); investigate whether the key leaked into a non-stripped position.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_app_builder.py tests/agents/test_sc001_gate_flag.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - All 5 characterization goldens pass BYTE-identical with NO SNAPSHOT_UPDATE env set and NO `*.wsframes.json`/golden fixture in `git diff` (INV-3 proven).
    - `_VOLATILE_STRIP_KEYS` contains `update_specs_eligible` and `artifact_kind`.
    - `test_sc001_gate_flag.py` green: eligibility True at analyze/spec, False elsewhere, no prototype-* literal in the emit path.
    - `/opt/homebrew/bin/lint-imports` unchanged at 4 kept / 0 broken (run as a spot-check).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| engine → WS event consumers (FE) | The new `review_gate_ready.data` keys cross to the FE; they must be metadata only and must not alter the deterministic golden byte-stream. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04-01 | Tampering (golden drift / INV-3 break) | review_gate_ready payload + goldens | mitigate | Mirror the `redoable` precedent + add both keys to `_VOLATILE_STRIP_KEYS`; prove all 5 goldens byte-identical with NO regeneration. If not byte-identical, STOP (do not regen). |
| T-32-04-02 | Elevation (INV-13 runtime change) | engine emit helper | mitigate | Metadata on an existing event only; `deep_agent_runner`/`create_deep_agent`/agent loop untouched (git diff scoped to engine.py + test files). |
| T-32-04-03 | Info-disclosure (SC-001 name leak) | eligibility derivation | mitigate | Eligibility derived from `_artifact_kind_for(spec)` (structural), never a workflow/agent name literal; banned-pattern/grep gate confirms no new literal. |
| T-32-04-SC | Tampering | (no package install) | accept | No npm/pip/cargo install in this plan; no supply-chain surface. |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest <5 goldens> tests/agents/test_sc001_gate_flag.py -q` — all green, goldens byte-identical (no SNAPSHOT_UPDATE).
- `git diff --name-only` — exactly engine.py, _normalize.py, test_sc001_gate_flag.py; NO `*.wsframes.json`/golden fixture, NO runner/factory/loader file.
- `grep -nE "prototype-analyze|prototype-specify" agents/execution_engine/engine.py` — no NEW literal from this change.
- `/opt/homebrew/bin/lint-imports` — 4 kept / 0 broken.
- NEVER run the full backend pytest.
</verification>

<success_criteria>
SC-2 (SC-001 backend half): `review_gate_ready` carries a generic name-free `update_specs_eligible`/`artifact_kind` discriminator stamped structurally at the analyze/spec gate; the 5 characterization goldens stay byte-identical (INV-3, A1 proven); no agent-runtime change (INV-13); the FE can now drive KAN-101 off the flag with zero prototype-* literal.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-04-SUMMARY.md` when done.
</output>
