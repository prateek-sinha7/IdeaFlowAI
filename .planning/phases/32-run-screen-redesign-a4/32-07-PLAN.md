---
phase: 32-run-screen-redesign-a4
plan: 07
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/src/components/preview/__tests__/PreviewPanel.switcher.test.tsx
autonomous: true
requirements: [SC-2, SC-1]
must_haves:
  truths:
    - "The right-panel tab host (Preview / Steps / Files / Audit) is reskinned to the token layer + primitives; the Thinking tab is relabelled Steps (SC-1, SC-2)"
    - "Preview keeps FIRST_PARTY_RENDERERS + generic mimetype dispatch as PRIMARY and gains a typed-renderer switcher as a MANUAL override on top (SC-2, SC-001)"
    - "PreviewPanel forwards the gate/clarify/pipelineState props to AgentThinkingTab (Steps) as optional passthroughs so plan 08 can render inline gate/clarify (SC-2)"
    - "Dispatch keys on renderType/mimetype, never a workflow name (SC-001)"
  artifacts:
    - path: "frontend/src/components/preview/PreviewPanel.tsx"
      provides: "reskinned tab host + Thinking→Steps relabel + Preview manual switcher + Steps prop passthrough"
      contains: "Steps"
    - path: "frontend/src/components/preview/__tests__/PreviewPanel.switcher.test.tsx"
      provides: "switcher override + generic-dispatch-primary + relabel assertions"
  key_links:
    - from: "frontend/src/components/preview/PreviewPanel.tsx"
      to: "FIRST_PARTY_RENDERERS generic dispatch"
      via: "manual typed-renderer override layered over the generic renderType dispatch (dispatch stays primary)"
      pattern: "FIRST_PARTY_RENDERERS"
    - from: "PreviewPanel.tsx Steps tab body"
      to: "AgentThinkingTab (plan 08)"
      via: "optional gate/clarify/pipelineState prop passthrough"
      pattern: "AgentThinkingTab"
---

<objective>
Reskin the right-panel tab HOST — Preview / Steps / Files / Audit — to the plan-01 tokens + plan-02 primitives (underline Tabs), relabel the existing "Thinking" tab to "Steps", add the Preview typed-renderer switcher as a MANUAL override over the generic dispatch, and forward the gate/clarify/pipelineState props to the Steps body so plan 08's inline gate/clarify can render.

Purpose: The tab host already has four tabs (PreviewPanel.tsx:310-313: preview/files/thinking/audit) — this is a rename+reskin, not a new tab. FIRST_PARTY_RENDERERS generic dispatch is the SC-001-clean primary and must stay; the switcher is additive.
Output: A reskinned PreviewPanel with an underline Tabs bar, "Steps" label, a manual renderer switcher over FIRST_PARTY_RENDERERS, optional Steps-gate prop passthrough, and switcher/dispatch tests.

HARD LIMITS: SC-001 — the Preview dispatch keys on renderType/mimetype, never a workflow name; the switcher is a manual OVERRIDE, not a replacement of the generic route. D-15 reuse — keep the existing FIRST_PARTY_RENDERERS + Files fallback + AuditTab mount; do NOT rebuild. AuditTab internals belong to plan 09 — this plan does NOT change AuditTab's signature (it already receives workflowRunId).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/phases/28-chat-contracts-guards-a0/contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin the tab host + relabel Thinking→Steps + Steps prop passthrough</name>
  <files>frontend/src/components/preview/PreviewPanel.tsx</files>
  <read_first>
    - frontend/src/components/preview/PreviewPanel.tsx L236 (PanelTab type), L310-313 (tab defs incl. Thinking L312 / Audit L313), L318 (PANEL_TAB_IDS), L406 (activeTab), L700 (tab render/isActive), L780-789 (Steps/Thinking body → AgentThinkingTab), L792-801 (Audit body → AuditTab)
    - frontend/src/components/ui/Tabs.tsx, Button.tsx (plan-02 primitives) + frontend/src/styles/globals.css (plan-01 tokens)
    - frontend/src/components/layout/DashboardLayout.tsx (the gate/clarify/pipelineState props plan 06 now passes to PreviewPanel — the optional passthrough source)
  </read_first>
  <action>
    Reskin the tab bar (L700 region) to the plan-02 underline Tabs primitive + tokens (active #15161A + 2px #3C2CDA underline). Relabel the "thinking" tab's LABEL from "Thinking" to "Steps" (keep the internal id stable to avoid churn unless a clean rename is trivial — the deep-link/testids reference the id). Thread the gate/clarify/pipelineState props DashboardLayout now passes (plan 06) through to the AgentThinkingTab render at L789 as OPTIONAL props (pinned to the existing GateContext/questionnaire/reviewGateData shapes) so plan 08 can render the inline gate/clarify in Steps — these props are optional and dormant until plan 08 consumes them. Do NOT change the AuditTab mount signature (plan 09 owns AuditTab internals). Route any inline navy/#2563eb in the tab host through tokens.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/preview/ 2>&1 | tail -6 && grep -q "Steps" src/components/preview/PreviewPanel.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The tab bar renders via the underline Tabs primitive; the third tab reads "Steps".
    - AgentThinkingTab receives the optional gate/clarify/pipelineState passthrough props (present in the JSX, pinned to existing shapes).
    - Existing PreviewPanel vitest suites (genericDeliverable, degraded, etc.) still green; tsc identity.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preview typed-renderer switcher as a manual override (generic dispatch stays primary)</name>
  <files>frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/preview/__tests__/PreviewPanel.switcher.test.tsx</files>
  <behavior>
    - By default, Preview dispatches through FIRST_PARTY_RENDERERS / the generic mimetype renderer (unchanged behavior — first-party types render as before, generic is the primary route).
    - A manual switcher lets the user override the auto-selected renderer for the current deliverable; selecting an override renders that renderer; clearing it returns to the generic auto-dispatch.
    - The switcher keys on renderType/mimetype options, never a workflow name (SC-001).
  </behavior>
  <read_first>
    - frontend/src/components/preview/PreviewPanel.tsx L586-628 (FIRST_PARTY_RENDERERS Record + renderDeliverable/renderType dispatch — the generic table to preserve; L601/L625 the dispatch call) + L525-531 (mimetype dispatch, SC-001 note)
    - frontend/src/components/ui/Tabs.tsx / Button.tsx (for the switcher control idiom)
  </read_first>
  <action>
    Write the switcher tests FIRST (generic-primary-unchanged + override-selects + clear-returns-to-generic). Then add a manual typed-renderer switcher layered ON TOP of the existing FIRST_PARTY_RENDERERS dispatch: a local override state that, when set, forces a specific renderType; when unset, the existing generic dispatch runs unchanged. Do NOT replace or reorder the generic dispatch (it stays PRIMARY). Keep the sandboxed-iframe contract intact. The switcher options are derived from the available renderType/mimetype set, never a workflow-name check.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/preview/__tests__/PreviewPanel.switcher.test.tsx src/components/preview/PreviewPanel.genericDeliverable.test.tsx 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Switcher test green: default = generic dispatch (first-party + generic unchanged); override forces the chosen renderer; clear returns to generic.
    - The generic FIRST_PARTY_RENDERERS dispatch remains the primary route (existing genericDeliverable tests still pass).
    - No workflow-name literal in the switcher/dispatch path (grep clean).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deliverable content → sandboxed iframe preview | Untrusted generated deliverable renders inside the existing sandboxed iframe (sandbox=allow-scripts, no allow-same-origin) — unchanged by this plan. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07-01 | Tampering (XSS via deliverable) | Preview iframe render | mitigate | Preserve the existing sandboxed-iframe contract (allow-scripts, NOT allow-same-origin); the switcher only selects among existing renderers, adds no new injection path. |
| T-32-07-02 | Info-disclosure (SC-001) | renderer dispatch/switcher | mitigate | Dispatch keyed on renderType/mimetype; grep confirms no workflow-name literal. |
| T-32-07-03 | Info-disclosure | Steps prop passthrough | accept | Optional props are the same gate/clarify state Steps already consumes (owner-scoped upstream); no new data source. |
No new trust boundary introduced.
</threat_model>

<verification>
- cd frontend && npx vitest run src/components/preview/ — green (switcher + genericDeliverable + degraded).
- cd frontend && npx tsc --noEmit | grep -v mockApi.ts | grep -c "error TS" — 0.
- grep -riE "#1B2A4A|#2563eb" src/components/preview/PreviewPanel.tsx — 0 in the tab-host regions touched (legacy sites outside the tab host may remain for later; note any residual in SUMMARY).
</verification>

<success_criteria>
SC-2 (tab-shell half) + SC-1 (tab host consumes tokens/primitives): the Preview/Steps/Files/Audit host is reskinned with underline Tabs, "Thinking" reads "Steps", the Preview keeps the generic FIRST_PARTY_RENDERERS dispatch as primary + gains a manual switcher override, and the Steps gate/clarify props are forwarded — all keyed on generic types (SC-001), AuditTab signature untouched.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-07-SUMMARY.md` when done.
</output>
