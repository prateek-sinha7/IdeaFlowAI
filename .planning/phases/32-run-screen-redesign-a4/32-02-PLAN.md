---
phase: 32-run-screen-redesign-a4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Card.tsx
  - frontend/src/components/ui/Tabs.tsx
  - frontend/src/components/ui/Badge.tsx
  - frontend/src/components/ui/Pill.tsx
  - frontend/src/components/ui/__tests__/primitives.test.tsx
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "A token-consuming primitive set (Button/Card/Tabs/Badge/Pill) exists in components/ui and renders with token classes, not hardcoded hex (SC-1, D-15)"
    - "Every primitive value resolves to a token (radius ladder, brand, ink, line) — no raw #1B2A4A/#2563eb literal in any primitive (SC-1)"
    - "The primitives expose a generic variant API keyed on presentational props, never a workflow name (SC-001)"
  artifacts:
    - path: "frontend/src/components/ui/Button.tsx"
      provides: "Primary/secondary button — radius10, font 600 12.5px, brand fill / #FCFBF7 border variants"
      min_lines: 20
    - path: "frontend/src/components/ui/Card.tsx"
      provides: "Card surface — #FCFBF7 / border #E6E3DB / radius14"
    - path: "frontend/src/components/ui/Tabs.tsx"
      provides: "Underline tabs — active #15161A + 2px #3C2CDA underline, inactive #8A8B82"
    - path: "frontend/src/components/ui/Badge.tsx"
      provides: "Status badge — radius5, Manrope 600 8-8.5px, status-ramp keyed"
    - path: "frontend/src/components/ui/Pill.tsx"
      provides: "Chip/pill — radius999, border #E0DDD3"
    - path: "frontend/src/components/ui/__tests__/primitives.test.tsx"
      provides: "Render + variant + token-class assertions for all five primitives"
  key_links:
    - from: "frontend/src/components/ui/Badge.tsx"
      to: "frontend/src/styles/globals.css status ramp"
      via: "status token classes (running/done/failed/cancelled/queued)"
      pattern: "(running|done|failed|cancelled|queued|status)"
---

<objective>
Create the run screen's first shared PRIMITIVE library — Button, Card, Tabs, Badge, Pill — in `frontend/src/components/ui/`. No primitive lib exists today (the tree inlines Tailwind utility strings), so this is a net-new but token-derived set the Wave-2/3 consumers reskin onto.

Purpose: Give every downstream surface (chat lane, tab shell, Steps, Audit) a token-consuming component vocabulary so the reskin routes through tokens, not new hardcoded palette (SC-1). Component idiom values come from `evidence 11 §B4`; the radius ladder + status ramp come from the plan-01 token layer.
Output: Five primitives + a vitest suite asserting each renders with its token classes and generic variant API.

INTERFACE NOTE: This plan runs in Wave 1 alongside plan 01 (token layer). It has zero file overlap with 01 and sources its token NAMES/VALUES from `evidence 11 §B` (the shared source of truth), so it is parallel-safe. Primitive tests assert className strings (which are stable regardless of when the @theme lands), not computed colors.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/v2.0-evidence/11-cross-mock-reconciliation.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Button, Card, Pill primitives</name>
  <files>frontend/src/components/ui/Button.tsx, frontend/src/components/ui/Card.tsx, frontend/src/components/ui/Pill.tsx, frontend/src/components/ui/__tests__/primitives.test.tsx</files>
  <behavior>
    - Button renders `variant="primary"` (brand `#3C2CDA` fill / white text) and `variant="secondary"` (`#FCFBF7` fill / `#E0DDD3` border / ink text), both radius10, font-weight 600, 12.5px, passing through `onClick`/`disabled`/`children` and `data-testid`.
    - Card renders a `#FCFBF7` surface, `#E6E3DB` border, radius14, forwarding `className` + children.
    - Pill renders radius999 with `#E0DDD3` border, `#FFFFFF`/token fill, forwarding children.
    - Every primitive routes values through token classes (or the token CSS vars) — the test asserts NO raw `#1B2A4A`/`#2563eb` literal appears in the rendered class output.
  </behavior>
  <read_first>
    - frontend/src/components/workflow/AgentProgressPanel.tsx L59-92 (the inline-Tailwind idiom to model — template-literal className conditionals)
    - frontend/src/components/results/AgentThinkingTab.tsx L31-38 (the AGENT_ACCENT const idiom — but note it hardcodes navy; route through tokens instead)
    - .planning/v2.0-evidence/11-cross-mock-reconciliation.md §B4 (primary/secondary button, card, chip idioms)
    - frontend/src/components/ui/CompletionToast.tsx (existing components/ui idiom + export shape)
  </read_first>
  <action>
    Create Button.tsx, Card.tsx, Pill.tsx as pure presentational client components composing the token utility classes generated by the plan-01 `@theme` (or `var(--...)` where a class is not generated). Model the className idiom on AgentProgressPanel (template-literal conditionals); place a per-file variant-map object like AGENT_ACCENT but with TOKEN class strings, never raw hex. Button API: `{ variant?: "primary"|"secondary", size?, disabled?, onClick?, children, className?, "data-testid"? }`. Card/Pill: `{ children, className?, ...rest }`. The variant selector is a GENERIC presentational prop — never a workflow name (SC-001). Start the shared `primitives.test.tsx` suite here (extended in Task 2).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/__tests__/primitives.test.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ui/__tests__/primitives.test.tsx` green for Button/Card/Pill cases.
    - Button `variant="primary"` and `variant="secondary"` render different token classes; `onClick`/`disabled`/`data-testid` pass through.
    - No raw `#1B2A4A`/`#2563eb` in any of the three source files (`grep` returns 0).
    - `tsc --noEmit` identity (0 new errors).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tabs + Badge primitives (status-ramp keyed)</name>
  <files>frontend/src/components/ui/Tabs.tsx, frontend/src/components/ui/Badge.tsx, frontend/src/components/ui/__tests__/primitives.test.tsx</files>
  <behavior>
    - Tabs renders a list of `{id,label}` tabs; the active tab gets ink `#15161A` text + a 2px `#3C2CDA` underline, inactive tabs `#8A8B82`; clicking a tab calls `onChange(id)`; each tab carries a `data-testid`/`role="tab"` and `aria-selected`.
    - Badge renders a status via a GENERIC `status` prop keyed on the canonical status model (running→blue, done→green, failed→red, cancelled→amber, queued→neutral-grey) with radius5, Manrope 600, 8-8.5px; an unknown status falls back to neutral-grey (never throws).
  </behavior>
  <read_first>
    - .planning/v2.0-evidence/11-cross-mock-reconciliation.md §B3 (status model: one key/label/color set) + §B4 (underline tabs + badge idioms)
    - frontend/src/components/workflow/WaveTreePanel.tsx L34-45 (statusKind() — the free-string→bucket normalizer to mirror for Badge's status keying)
  </read_first>
  <action>
    Create Tabs.tsx and Badge.tsx as token-consuming presentational components. Tabs API: `{ tabs: {id,label,icon?}[], active, onChange, className? }` — underline-active idiom from §B4/§6.9 (already near-consistent app-wide). Badge API: `{ status: "running"|"done"|"failed"|"cancelled"|"queued"|string, label?, className? }` — map each status key to its fill/border/text token class from the §B3 ramp; NORMALIZE `completed`→`done` (per §B3); unknown→neutral-grey. Governance green/amber/red usage stays the SOLE one-chroma exception, consumed by Audit (plan 09) — Badge exposes those status classes but does not enforce chroma policy. Extend `primitives.test.tsx` with Tabs + Badge cases.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui/__tests__/primitives.test.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - Tabs: clicking a non-active tab fires `onChange` with its id; active tab has `aria-selected="true"` + the underline token class.
    - Badge: each of running/done/failed/cancelled/queued renders its distinct status token class; `completed` normalizes to the `done` class; an unknown status renders neutral-grey (no throw).
    - Full `primitives.test.tsx` suite green; `tsc --noEmit` identity.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational client components; props in, JSX out; no data fetch, no user-input parsing, no new trust boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02-T | Tampering | primitive className composition | accept | Static token class strings; no `dangerouslySetInnerHTML`, no dynamic HTML, no eval. |
| T-32-02-I | Information disclosure | Badge/status render | accept | Renders only caller-supplied status label; no data source, no PII. |
No new trust boundary; no external service; no package install.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/ui/__tests__/primitives.test.tsx` — green.
- `cd frontend && npx tsc --noEmit | grep -v mockApi.ts | grep -c "error TS"` — 0.
- `grep -rniE "#1B2A4A|#2563eb" src/components/ui/{Button,Card,Tabs,Badge,Pill}.tsx` — 0 hits (no hardcoded retired palette in the primitives).
</verification>

<success_criteria>
SC-1 (primitive half): a token-consuming Button/Card/Tabs/Badge/Pill set exists in `components/ui`, every value resolves to a token (radius ladder + status ramp + brand/ink/line), the variant API is generic (no workflow-name), and vitest proves render + variant + token-class for all five.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-02-SUMMARY.md` when done.
</output>
