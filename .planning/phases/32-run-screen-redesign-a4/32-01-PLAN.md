---
phase: 32-run-screen-redesign-a4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/globals.css
  - frontend/src/app/layout.tsx
  - frontend/src/styles/__tests__/token-layer.test.ts
autonomous: true
requirements: [SC-1]
must_haves:
  truths:
    - "The run-screen token layer resolves the canonical Hexaware palette (brand #3C2CDA, warm ink, beige surfaces, status ramp) + Manrope/Heebo/SF-Mono fonts from a SINGLE @theme block (SC-1, D-15)"
    - "The retired values (#2563eb, Inter, Fraunces, JetBrains Mono) are absent from the token layer; no per-page palette fork exists (SC-1)"
    - "The radius ladder (button=10, card=14, 5/8/11/12/18/999) + elevation/motion/scrim tokens are declared for consumers (SC-1)"
  artifacts:
    - path: "frontend/src/styles/globals.css"
      provides: "Tailwind v4 @theme + :root canonical token layer (brand/ink/surface/line/status/radius/elevation/motion/scrim)"
      contains: "#3C2CDA"
    - path: "frontend/src/app/layout.tsx"
      provides: "Manrope + Heebo next/font/google loaders wired to --font-sans/--font-serif; SF Mono code token"
      contains: "Manrope"
    - path: "frontend/src/styles/__tests__/token-layer.test.ts"
      provides: "Offline guard: canonical tokens present, retired tokens absent"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/styles/globals.css"
      via: "next/font CSS variables (--font-manrope/--font-heebo) consumed by @theme --font-sans/--font-serif"
      pattern: "font-(manrope|heebo|sans|serif)"
---

<objective>
Rewrite the run-screen TOKEN LAYER to the canonical Hexaware palette + fonts, the D-15 blocking dependency the rest of the shell (Phases 34/35/36/37) gates on. This is Wave 1, isolated and first: every downstream surface consumes these tokens, so there must be NO per-page palette fork.

Purpose: Land black/beige/one-blue `#3C2CDA` + Manrope/Heebo + the status ramp (which the DS mock omits) + a resolved radius ladder, sourced verbatim from `evidence 11 §B2/§B3/§B4` — NOT any single mock (mocks drift; running=blue is correct, Handoff's amber is a bug).
Output: A rewritten `styles/globals.css` `@theme inline`+`:root`, Manrope/Heebo font loaders in `layout.tsx`, and an offline token-layer guard test.

SCOPE DISCIPLINE: Define tokens + retire the old palette IN THE TOKEN LAYER ONLY. Do NOT sweep the ~370 `#1B2A4A` sites / 54 files — only the run subtree consumes these tokens, and it converts in later waves. Leaving admin/login/settings/wizard pages on the old navy is expected and correct (Phase 35+).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-run-screen-redesign-a4/32-CONTEXT.md
@.planning/phases/32-run-screen-redesign-a4/32-RESEARCH.md
@.planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md
@.planning/v2.0-evidence/11-cross-mock-reconciliation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the @theme + :root token layer in styles/globals.css</name>
  <files>frontend/src/styles/globals.css</files>
  <read_first>
    - frontend/src/styles/globals.css (the live token file — Tailwind v4 @theme inline + :root; the CURRENT Inter/#2563eb/#f5f5f0 values to replace)
    - .planning/v2.0-evidence/11-cross-mock-reconciliation.md §B2 (tokens), §B3 (status model), §B4 (component idioms) — THE source of truth for every hex
    - .planning/phases/32-run-screen-redesign-a4/32-PATTERNS.md "Token consumption" section (the @theme structure + gotcha: tailwind.config.ts is inert, app/globals.css is a re-export shim)
  </read_first>
  <action>
    Replace the `@theme inline` fonts + the entire `:root` palette with the canonical evidence-11-§B families as CSS custom properties (both as `@theme inline` tokens so Tailwind v4 generates utilities, and as `:root` vars for direct var() consumers):
    - Fonts: `--font-sans` = Manrope stack, `--font-serif` (reading) = Heebo stack, `--font-mono` = `'SF Mono', ui-monospace, Menlo, monospace`. Bind sans/serif to the `next/font` CSS variables Task 2 exposes (--font-manrope / --font-heebo).
    - Brand (one chroma): brand `#3C2CDA`, pressed `#3324C4`, fill `#ECEAFC`, border `#DED9F7`, violet-tint `#F4F2FB`, on-dark `#8E88E8`.
    - Ink (warm ramp): `#15161A #20222B #3A3B42 #5B5C63 #6E6F76 #8A8B82 #9A9B92 #A0A199`.
    - Surface: paper `#F0EEE7`, warm `#F6F4EE`, card `#FCFBF7`, white `#FFFFFF`, near-black `#111114`, ink-black `#0C0D12`.
    - Line: border `#E6E3DB`, divider `#E2DFD6`, control `#E0DDD3`, faint-row `#EDEBE3`, faint `#C6C3B9`.
    - Status ramp (NEW — DS documents zero status colors): running-blue `#3C2CDA`/`#ECEAFC`/`#DED9F7`, done-green `#1F7A4D`/`#E7F0EA`/`#CFE3D6`, failed-red `#A33A32`/`#F5E5E2`/`#E8CDC8`, cancelled/gate-amber `#9A6B1E`/`#F5EEDD`/`#E8DBC0`, queued/neutral-grey `#9A9B92`/`#EFEDE6`/`#E2DFD6`. Severity ladder: low `#6E6F76`, medium `#9A6B1E`, high `#B4531E`, critical `#A33A32`.
    - Radius ladder (RESOLVED per mandate): tag/badge=5, node=8, button=10, list-row=11, menu=12, card/panel=14, hero=18, pill=999.
    - Elevation: raised `0 1px 2px rgba(0,0,0,.08)`, menu `0 12px 32px rgba(0,0,0,.12)`, notif `0 16px 40px rgba(0,0,0,.18)`, modal `0 30px 80px rgba(0,0,0,.30)`.
    - Motion: collapse `max-height .4s cubic-bezier(.4,0,.2,1)` + opacity `.3s`; chevron `rotate .3s`; hover `.15s`; focus ring `0 0 0 4px rgba(60,44,218,.15)`.
    - Scrim: `rgba(17,17,20,.5)` + `blur(3px)`.
    RETIRE from the token layer: `--accent: #2563eb`, `--background: #f5f5f0`, `var(--font-inter)`, `var(--font-fraunces)`, `"JetBrains Mono"`, and the cool-grey / near-white-dup values. Keep the legacy `--theme-*` alias keys ONLY if run-subtree components still reference them — but repoint their VALUES to the new palette (do not leave `#2563eb`/`#111827` live). Governance status colors (green/amber/red) are declared here but are the SOLE one-chroma exception, consumed only by the Audit surface (plan 09).
    Do NOT touch `tailwind.config.ts` (inert v3 stub) or any component file — token layer only.
  </action>
  <verify>
    <automated>cd frontend && grep -c "#3C2CDA" src/styles/globals.css && grep -v '^ *\/\*' src/styles/globals.css | grep -Eic "2563eb|f5f5f0|font-inter|font-fraunces|jetbrains" | grep -qx 0 && echo RETIRED_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#3C2CDA" src/styles/globals.css` ≥ 1 (brand token present).
    - After stripping comment lines, `#2563eb`, `#f5f5f0`, `font-inter`, `font-fraunces`, and `jetbrains` (case-insensitive) return ZERO matches in globals.css (retired from the token layer).
    - The status ramp hexes (`#1F7A4D`, `#A33A32`, `#9A6B1E`, `#9A9B92`) and radius `14`/`10` are all present in the file.
    - `tailwind.config.ts` is unchanged (`git diff --name-only` does not list it).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Swap the next/font loaders to Manrope + Heebo in layout.tsx</name>
  <files>frontend/src/app/layout.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx (lines 2, 6, 15 — the current `Inter, Fraunces` from `next/font/google` loaders and their CSS-variable wiring onto the <html>/<body> className)
    - .planning/v2.0-evidence/11-cross-mock-reconciliation.md §B2 (Fonts: Manrope structure · Heebo reading · SF Mono code)
  </read_first>
  <action>
    Replace the `Inter` and `Fraunces` imports/loaders with `Manrope` and `Heebo` from `next/font/google`, exposing them as CSS variables `--font-manrope` and `--font-heebo` (via the `variable` option), applied on the root element className exactly where Inter/Fraunces were applied. Manrope is the structural/sans font (bound to `--font-sans` in globals.css), Heebo is the reading/serif-slot font (bound to `--font-serif`). Keep the same subsets/display/weight config idiom the existing loaders use. SF Mono is a system stack (no google-font loader needed — it lives only in the `--font-mono` token). Assumption A3: Manrope+Heebo are Google Fonts (confirmed in evidence 11); if `next/font/google` rejects a weight, fall back to the nearest available weight set — do not self-host in this plan.
  </action>
  <verify>
    <automated>cd frontend && grep -Eq "Manrope|Heebo" src/app/layout.tsx && ( grep -Eic "\\bInter\\b|Fraunces" src/app/layout.tsx | grep -qx 0 ) && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c "error TS" | grep -qx 0 && echo FONTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` imports `Manrope` and `Heebo` from `next/font/google`; `Inter`/`Fraunces` no longer appear.
    - The font CSS variables (`--font-manrope`/`--font-heebo`) are applied to the root element and referenced by globals.css `--font-sans`/`--font-serif`.
    - `npx tsc --noEmit` shows ZERO new errors beyond the known `mockApi.ts` baseline (identity).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the offline token-layer guard test</name>
  <files>frontend/src/styles/__tests__/token-layer.test.ts</files>
  <behavior>
    - Reads styles/globals.css as text and asserts the canonical tokens are PRESENT: `#3C2CDA` (brand), `#1F7A4D`/`#A33A32`/`#9A6B1E`/`#9A9B92` (status ramp), radius `14` and `10`.
    - Asserts the retired values are ABSENT from the token layer (comment lines stripped): `#2563eb`, `font-inter`, `font-fraunces`, `jetbrains`.
    - Reads layout.tsx and asserts `Manrope` + `Heebo` are wired and `Inter`/`Fraunces` are gone.
  </behavior>
  <read_first>
    - frontend/vitest.config.ts (test discovery + node fs access pattern in existing tests)
    - frontend/src/styles/globals.css (the file under assertion, after Task 1)
  </read_first>
  <action>
    Create a vitest suite that reads the two token-layer files with `fs.readFileSync` and asserts the PRESENT/ABSENT sets above. Strip comment lines before the ABSENT assertions so header prose cannot self-invalidate the guard (do NOT bare-`toBe(0)` an unfiltered file). This is the SC-1 "no per-page palette fork / retired-palette-gone" regression guard for the token layer; the per-consumer "no new raw hex in the run subtree" checks live in the downstream consumer plans (06/07/08/09).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/styles/__tests__/token-layer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/styles/__tests__/token-layer.test.ts` passes (all present/absent assertions green).
    - The test fails if `#2563eb` or `Inter`/`Fraunces` are reintroduced into the token layer (verified by construction of the ABSENT assertions).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Token layer + font config are static build-time assets; no runtime input, no data boundary crossed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-I | Information disclosure | globals.css / layout.tsx | accept | No secrets/PII in a CSS token layer or font config; static assets only. |
| T-32-01-T | Tampering | next/font/google fetch | accept | Google Fonts are self-optimized/self-hosted at build by `next/font`; no third-party runtime CDN call, no new package install (no supply-chain surface). |
No new trust boundary introduced by this plan.
</threat_model>

<verification>
- `cd frontend && npx vitest run src/styles/__tests__/token-layer.test.ts` — green.
- `cd frontend && npx tsc --noEmit | grep -v mockApi.ts | grep -c "error TS"` — 0 (identity).
- Token-layer-only scope: `git diff --name-only` lists exactly globals.css, layout.tsx, and the new test file (NOT tailwind.config.ts, NOT any component).
</verification>

<success_criteria>
SC-1 (token half): the run-screen token layer resolves the canonical brand/ink/surface/line/status/radius/elevation/motion/scrim families + Manrope/Heebo/SF-Mono from a single `@theme`+`:root`, the retired Inter/Fraunces/`#2563eb`/`#1B2A4A`/JetBrains values are gone from the token layer, and the offline guard proves both. No per-page palette fork. No component/`tailwind.config.ts` edits.
</success_criteria>

<output>
Create `.planning/phases/32-run-screen-redesign-a4/32-01-SUMMARY.md` when done.
</output>
