---
phase: 36-home-history-my-workflows-b2
plan: 05
type: execute
wave: 3
depends_on: ["36-04"]
files_modified:
  - frontend/src/components/history/WorkflowHistory.tsx
  - frontend/src/components/history/RevisionFamilyView.tsx
  - frontend/src/components/history/WorkflowHistory.grouping.test.tsx
autonomous: true
requirements: [SHELL-02, SHELL-03]
must_haves:
  truths:
    - "History groups runs into Today / Earlier / Older buckets and can sort by tokens or duration — derived client-side from the created_at / token_usage / duration fields already on each list row (no backend change) (SHELL-02)"
    - "Real delete removes a run (DELETE /api/runs/{id}, already wired); revision families stay intact via groupRunsByFamily (SHELL-02)"
    - "KAN-96 preserved: a running run click → the live execution view (onViewRunningPipeline); a terminal run click → the new RunDetailPage (from 36-04)"
    - "KAN-92 preserved: rows/detail render the real async-generated run.title — never a `Revision:` placeholder"
    - "The summary-fed detail surfaces (agents/KPIs/failure banner/version timeline) render via RunDetailPage — the duplicated hand-rolled JSX is retired (no dual implementation; SHELL-03 / D-15)"
    - "WorkflowHistory + RevisionFamilyView consume Phase-32 @theme tokens + ui/ primitives; retired-palette grep = 0 (token authority); list rows + kebab + detail keyboard + aria (a11y)"
  artifacts:
    - path: "frontend/src/components/history/WorkflowHistory.tsx"
      provides: "Today/Earlier/Older grouping + token/duration sort + RunDetailPage-promoted detail; reskinned"
      contains: "RunDetailPage"
    - path: "frontend/src/components/history/RevisionFamilyView.tsx"
      provides: "Family grouping helpers extended for the date buckets; reskinned"
      contains: "groupRunsByFamily"
    - path: "frontend/src/components/history/WorkflowHistory.grouping.test.tsx"
      provides: "Today/Earlier/Older grouping + token/duration sort assertions"
      exports: []
  key_links:
    - from: "frontend/src/components/history/WorkflowHistory.tsx handleSelectRun"
      to: "onViewRunningPipeline (running) / RunDetailPage (terminal)"
      via: "the preserved KAN-96 click branch"
      pattern: "onViewRunningPipeline"
    - from: "frontend/src/components/history/WorkflowHistory.tsx"
      to: "RunDetailPage"
      via: "promoted detail render for summary-fed surfaces"
      pattern: "RunDetailPage"
---

<objective>
Restructure **History**: Today/Earlier/Older grouping + token/duration sort (client-side over existing fields), real delete (verify), revision families intact, KAN-96 + KAN-92 preserved, and promote the in-panel detail onto the **RunDetailPage** (from 36-04) so the summary-fed surfaces stop being hand-rolled — all reskinned onto the Phase-32 tokens (the largest reskin surface).

Purpose: SHELL-02 SC-2 (History) + SHELL-03 wiring (terminal run → the new detail page). Preserve shipped behaviors (KAN-96 running→live / terminal→detail, KAN-92 real titles, families, delete) — a face-value rebuild would REGRESS them (D-15).
Output: grouped/sortable History; RunDetailPage-promoted detail (no dual impl); retired-palette grep 0 on WorkflowHistory.tsx + RevisionFamilyView.tsx; existing history suites + a new grouping test green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-home-history-my-workflows-b2/36-CONTEXT.md
@.planning/phases/36-home-history-my-workflows-b2/36-RESEARCH.md
@.planning/phases/36-home-history-my-workflows-b2/36-PATTERNS.md
@.planning/phases/36-home-history-my-workflows-b2/36-VALIDATION.md
@frontend/src/components/history/WorkflowHistory.tsx
@frontend/src/components/history/RevisionFamilyView.tsx
@.planning/phases/36-home-history-my-workflows-b2/36-04-PLAN.md
@frontend/src/styles/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Today/Earlier/Older grouping + token/duration sort (client-side)</name>
  <files>frontend/src/components/history/WorkflowHistory.tsx, frontend/src/components/history/RevisionFamilyView.tsx, frontend/src/components/history/WorkflowHistory.grouping.test.tsx</files>
  <behavior>
    - Runs created today bucket under "Today"; this-week/older buckets under "Earlier"/"Older"
    - Sort control by tokens (tokenUsage.total_tokens) and by duration reorders the list deterministically
    - Grouping/sort layers OVER the existing family grouping — a family card matches if ANY member matches; families stay intact
  </behavior>
  <read_first>
    frontend/src/components/history/WorkflowHistory.tsx (list fetch :165-173, matchesFilter :282-287, formatDate :111-123, formatDuration :125-129, fmt/token render :485-505, groupRunsByFamily import :38, visibleFamilies grouping :885);
    frontend/src/components/history/RevisionFamilyView.tsx (groupRunsByFamily, baseWorkflowType — extend the grouping helpers HERE, not inline);
    36-PATTERNS.md §3 (grouping+sort over existing fields) + 36-RESEARCH.md Pattern 4
  </read_first>
  <action>
    Add a Today/Earlier/Older date-bucketing layer + a tokens/duration sort over the family-grouped list, derived entirely from fields ALREADY on each row (`createdAt` → bucket key via the existing date helpers; `duration` and `tokenUsage.total_tokens` → sort keys) — NO backend change, NO new fetch (36-RESEARCH A2). Bucket keyed on `group.root.created_at`; apply the sort within/across buckets. Extend the grouping helpers in `RevisionFamilyView.tsx` (where `groupRunsByFamily`/`baseWorkflowType` live), not inline in WorkflowHistory. **Preserve revision families** (a family card matches if any member matches — the existing `matchesFilter` semantics) and **KAN-92** (render `run.title`, never a `Revision:` placeholder). Write `WorkflowHistory.grouping.test.tsx` first: assert the three buckets partition by date and the tokens/duration sort reorders deterministically while families stay grouped.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/history/WorkflowHistory.grouping.test.tsx src/components/history/WorkflowHistory.family.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Grouping partitions rows into Today/Earlier/Older by `createdAt`; sort by tokens + by duration reorders deterministically
    - Grouping/sort derive from existing row fields only (grep: no new `getWorkflows`/fetch added; no backend change)
    - Families stay intact (the family test suite stays green); titles render `run.title` (KAN-92)
    - `WorkflowHistory.grouping.test.tsx` + the existing family suite green
  </acceptance_criteria>
  <done>History groups by Today/Earlier/Older and sorts by tokens/duration off existing fields, families + KAN-92 intact, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the detail onto RunDetailPage + preserve KAN-96 / delete / families</name>
  <files>frontend/src/components/history/WorkflowHistory.tsx</files>
  <read_first>
    frontend/src/components/history/WorkflowHistory.tsx (KAN-96 handleSelectRun :175-198, delete :263-276, family fetch/switch :203-220/:242-255, the in-panel detail half :382-880 — KPI strip :485-505, per-agent :508-566, VersionTimeline :676-681, DegradedRunAffordance gate :436-452, tabbed preview/files/audit :683-870, clarify fetch :226-237);
    frontend/src/components/history/RunDetailPage.tsx (the 36-04 summary-fed detail — agents/KPIs/failure/timeline);
    frontend/src/components/history/WorkflowHistory.genericReopen.test.tsx + .revise.test.tsx + .runInput.test.tsx (the locked behaviors to keep green)
  </read_first>
  <action>
    Replace the hand-rolled summary-fed detail JSX in WorkflowHistory (the KPI strip, per-agent breakdown, `VersionTimeline`, and `DegradedRunAffordance` failure block) with a `<RunDetailPage runId={selectedRun.id} onBack={...} activeRunId={activeRunId} onViewRunningPipeline={onViewRunningPipeline} />` mount fed by `getRunSummary` — retiring the duplicated code (no dual implementation; the summary endpoint IS the single source for agents/KPIs/failure/timeline, D-15/INV-3). **Preserve KAN-96 verbatim** — keep the `handleSelectRun` guard `if (activeRunId && run.id === activeRunId && onViewRunningPipeline) { onViewRunningPipeline(); return; }` (running run → live execution view); a terminal run click sets `selectedRun` and now renders `RunDetailPage` (terminal → detail page). **Preserve** the real delete wiring (`deleteWorkflow` + optimistic filter), the revision-family fetch/version-switch, KAN-92 titles, and the tabbed preview/files/audit + clarify sub-surfaces the summary does not cover — keep those in the History detail wrapper (reskinned in Task 3) or thread the existing `getWorkflow`/`getRunArtifacts` fetches into RunDetailPage; executor's D-15 discretion, but NO regression and NO dual impl. **SC-001:** the detail is keyed on generic run data — never a workflow-name branch. The existing `WorkflowHistory.genericReopen/revise/runInput/family` suites MUST stay green.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/history/ && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - Terminal-run detail renders `<RunDetailPage>` (grep confirms mount); the hand-rolled KPI/agent/VersionTimeline/DegradedRunAffordance JSX is removed (no dual impl)
    - KAN-96 guard preserved (grep: `onViewRunningPipeline` early-return in `handleSelectRun` intact)
    - Delete (`deleteWorkflow`) + family fetch + KAN-92 `run.title` render preserved
    - All existing `src/components/history/` suites (family/revise/runInput/genericReopen) green; `tsc` identity clean
  </acceptance_criteria>
  <done>Detail promoted to RunDetailPage (single source for the summary surfaces), KAN-96/KAN-92/delete/families preserved, existing suites green, no dual impl.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin WorkflowHistory + RevisionFamilyView onto Phase-32 tokens (largest surface)</name>
  <files>frontend/src/components/history/WorkflowHistory.tsx, frontend/src/components/history/RevisionFamilyView.tsx</files>
  <read_first>
    frontend/src/components/history/WorkflowHistory.tsx (post-Task-2; retired hexes concentrated at the former :455/:514/:590-663/:714/:896/:936/:958 and the `var(--font-fraunces)` heroes);
    frontend/src/components/history/RevisionFamilyView.tsx (family cards + VersionTimeline chrome);
    frontend/src/styles/globals.css (@theme tokens + font-serif=Heebo);
    36-RESEARCH.md Pitfall 1/2 (grep anchors; fraunces dead var) + 36-VALIDATION.md token gate
  </read_first>
  <action>
    Migrate the remaining retired-palette literals (`#1B2A4A` ×17-region, `#f5f5f0` ×2, `var(--font-fraunces)`→`font-serif`) and stray stock palette (~102 stock-Tailwind classes at research time, reduced after the Task-2 detail retirement) to the Phase-32 `@theme` tokens (`bg-surface-*`, `text-ink-*`, `border-line-*`, `text-brand`, the `status-*` ramp for run status badges) + `components/ui/` primitives + the Phase-35 shell chrome — NO palette fork (D-15). Reskin the list rows, group headers (Today/Earlier/Older), filter/sort controls, kebab, delete modal, and the RevisionFamilyView family cards. Keep a11y: list rows keyboard-focusable + aria, kebab (aria-haspopup/expanded, role=menu, Escape), delete modal keyboard + aria. This is the largest reskin surface — budget accordingly; verify the grep hits 0 on BOTH files.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' src/components/history/WorkflowHistory.tsx src/components/history/RevisionFamilyView.tsx && npx vitest run src/components/history/</automated>
  </verify>
  <acceptance_criteria>
    - Retired-palette grep == 0 on WorkflowHistory.tsx AND RevisionFamilyView.tsx
    - No stray stock palette (`gray-`, `blue-`, raw `#hex`); positive proof of `@theme`/`ui/` usage
    - a11y on list rows + kebab + delete modal
    - All `src/components/history/` vitest suites green
  </acceptance_criteria>
  <done>History + RevisionFamilyView render on Phase-32 tokens with retired-palette grep 0 on both files, a11y intact, all history suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render → route | KAN-96 click branch + detail keyed on generic run data (SC-001) |
| History list → RunDetailPage | terminal run reopen fetches owner-scoped summary (server-gated, 36-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05-KAN96 | Correctness | handleSelectRun click branch | mitigate | Preserve the `activeRunId`/`onViewRunningPipeline` early-return verbatim (running→live); terminal→RunDetailPage; genericReopen suite stays green |
| T-36-05-KAN92 | Correctness | title rendering | mitigate | Render `run.title` (real async title), never a `Revision:` placeholder; family suite stays green |
| T-36-05-DUP | Correctness | summary-fed detail surfaces | mitigate | Retire the hand-rolled KPI/agent/timeline/failure JSX in favor of RunDetailPage (single source; no dual impl → live+reopen cannot drift) |
| T-36-05-SC001 | Tampering | grouping/sort/detail branch | mitigate | Key grouping/sort/detail on generic run fields (created_at/tokens/duration/status), never a workflow-name branch (SC-001/INV-1) |
| T-36-05-A11Y | Denial (accessibility) | list rows + kebab + delete modal | mitigate | keyboard + aria on rows, kebab menu, delete modal |
| T-36-SC | Tampering | npm/pip installs | accept | No packages installed (restructure + reskin only) |
</threat_model>

<verification>
- Retired-palette grep == 0 on WorkflowHistory.tsx AND RevisionFamilyView.tsx
- `npx vitest run src/components/history/` green (grouping + family + revise + runInput + genericReopen)
- `npx tsc --noEmit` delta-clean
- LOCK-B: no transport touch (no useWebSocket/useRunStream/layout edits)
- Mocked Playwright e2e + live KAN-96 running→live SSE: LIVE-DEFERRED (offline) — capture baseline, do not hang
</verification>

<success_criteria>
- History grouped (Today/Earlier/Older) + sortable (tokens/duration) off existing fields (SHELL-02)
- Real delete + revision families intact; KAN-96 + KAN-92 preserved
- Detail promoted to RunDetailPage (summary-fed, no dual impl; SHELL-03)
- Retired-palette grep 0 on both files (token authority); a11y; existing history suites green
</success_criteria>

<artifacts_this_phase_produces>
- Today/Earlier/Older grouping + tokens/duration sort in `WorkflowHistory.tsx` (helpers extended in `RevisionFamilyView.tsx`)
- RunDetailPage-promoted detail in `WorkflowHistory.tsx` (retired hand-rolled summary JSX)
- `frontend/src/components/history/WorkflowHistory.grouping.test.tsx` (new)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/36-home-history-my-workflows-b2/36-05-SUMMARY.md` when done.
</output>
