---
phase: 36-home-history-my-workflows-b2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/runs.py
  - backend/tests/unit/test_runs_api_summary.py
autonomous: true
requirements: [SHELL-03]
must_haves:
  truths:
    - "GET /api/runs/{id}/summary returns 200 for the owner, 404 for a cross-owner run, 404 for a missing run (owner-scoped IDOR→404, keyed on WorkflowRun.user_id, never the nullable owner_id)"
    - "The summary aggregates ONLY existing WorkflowRun columns + the existing family walk — per-agent breakdown (agent_outputs), KPI stats (token_usage + duration + agent_count), failure banner (status + error), version/revision timeline (parent_run_id family) — inventing ZERO fields (SHELL-03)"
    - "The endpoint is additive read-only — no new tables, no migrations; the 5 backend goldens are untouched by construction (INV-3)"
    - "import-linter stays 4 kept / 0 broken; no engine/transport touch (LOCK-B)"
  artifacts:
    - path: "backend/app/api/runs.py"
      provides: "GET /{workflow_id}/summary route + RunSummaryResponse model"
      contains: "def get_run_summary"
    - path: "backend/tests/unit/test_runs_api_summary.py"
      provides: "owner-gate (200/404/404) + aggregation-shape tests over existing columns"
      exports: []
  key_links:
    - from: "backend/app/api/runs.py get_run_summary"
      to: "_owner_gate_or_404"
      via: "owner-scoped resolve (user_id → 404)"
      pattern: "_owner_gate_or_404"
    - from: "backend/app/api/runs.py get_run_summary"
      to: "_compute_root_ids + family BFS"
      via: "version/revision timeline reuse"
      pattern: "_compute_root_ids"
---

<objective>
Add the additive, read-only, owner-scoped **`GET /api/runs/{id}/summary`** endpoint that aggregates existing `WorkflowRun` data — per-agent breakdown, KPI stats, failure banner, and version/revision timeline — as the data spine for the Wave-2 Run detail page (SHELL-03).

Purpose: SHELL-03 backend half. Invents ZERO fields, adds ZERO tables/migrations, touches ZERO goldens (INV-3 by construction — it only READS). Owner-scoped `WorkflowRun.user_id == principal` → 404 (P13/P25 IDOR posture), NOT the nullable `owner_id`.
Output: `get_run_summary` route + `RunSummaryResponse` model in `runs.py`; `test_runs_api_summary.py` proving owner-gate 200/404/404 + aggregation shape.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-home-history-my-workflows-b2/36-CONTEXT.md
@.planning/phases/36-home-history-my-workflows-b2/36-RESEARCH.md
@.planning/phases/36-home-history-my-workflows-b2/36-PATTERNS.md
@.planning/phases/36-home-history-my-workflows-b2/36-VALIDATION.md
@backend/app/api/runs.py
@backend/app/models/workflow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GET /{id}/summary — owner-scoped aggregation over existing columns</name>
  <files>backend/app/api/runs.py</files>
  <behavior>
    - Owner run → 200 with {id, title, type, status, duration, agent_count, token_usage, error, agents, root_id, members}
    - Cross-owner run id → 404 "Workflow run not found" (never 403, never a 200 with foreign data)
    - Missing run id → 404
    - Malformed stored agent_outputs / token_usage JSON → [] / {} fallback (never a 500)
    - members timeline = the owned family walk (revision_index, parent_run_id nulled at root)
  </behavior>
  <read_first>
    backend/app/api/runs.py (_owner_gate_or_404 :1068-1085, get_run_family + family BFS :933-1018, _compute_root_ids :134, WorkflowRunResponse/RunFamilyResponse/FamilyMemberResponse :88-931, get_run :386-408, the JSON-parse try/except idiom :500-506);
    backend/app/models/workflow.py (WorkflowRun columns :19-91 — agent_outputs :32, agent_count :33, duration :34, error :35, token_usage :36, status :22-29, title :21, parent_run_id :46; owner_id nullable :58);
    36-PATTERNS.md §6 (the datum→existing-column map)
  </read_first>
  <action>
    Register `@router.get("/{workflow_id}/summary", response_model=RunSummaryResponse)` after `get_run_family`. Define a `RunSummaryResponse(BaseModel)` near `WorkflowRunResponse` with fields: `id`, `title`, `type`, `status`, `duration: Optional[float]`, `agent_count: int`, `token_usage: dict`, `error: Optional[str]`, `agents: list` (per-agent breakdown), `root_id: str`, `members: list[FamilyMemberResponse]` (REUSE the existing `FamilyMemberResponse`). Handler: (1) `run = _owner_gate_or_404(db, workflow_id, current_user.id)` — the shared gate keyed on `WorkflowRun.user_id`, NEVER `owner_id` (nullable). (2) Parse `run.agent_outputs` (JSON array) and `run.token_usage` (JSON) each inside a try/except → `[]`/`{}` fallback (mirror the :500-506 idiom; never trust stored JSON to be well-formed → DoS guard). (3) Build the version/revision timeline by REUSING `_compute_root_ids(db, current_user.id, [run])` for the root and the SAME owned BFS-down-over-children the `get_run_family` body uses (`FamilyMemberResponse` with `revision_index` + root parent nulled — do NOT duplicate the walk logic gratuitously; call/extract from the existing pattern). (4) Project ONLY summary-safe fields — do NOT echo raw child output/`run.output` that could carry a secret (V7). Invent NO columns; every datum maps to an existing `WorkflowRun` column or the family walk (36-PATTERNS §6). Additive `BaseModel`, no engine/golden/transport touch (INV-3/LOCK-B).
  </action>
  <verify>
    <automated>cd backend && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `get_run_summary` calls `_owner_gate_or_404(db, workflow_id, current_user.id)` (grep confirms `user_id` gate, NOT `owner_id`)
    - `RunSummaryResponse` fields map 1:1 to existing columns/family walk (no new model column; no schema/migration file added)
    - JSON parses wrapped in try/except with `[]`/`{}` fallback
    - `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
  </acceptance_criteria>
  <done>The additive owner-scoped summary route exists, aggregates existing data only, import-linter 4/0, no migrations/tables added.</done>
</task>

<task type="auto">
  <name>Task 2: test_runs_api_summary.py — owner-gate 200/404/404 + aggregation shape</name>
  <files>backend/tests/unit/test_runs_api_summary.py</files>
  <read_first>
    backend/tests/unit/test_runs_api.py (existing runs-API test harness: client, auth, seeded WorkflowRun rows);
    backend/tests/unit/test_runs_api_family.py (the owner-gate 404 + family assertion patterns to mirror);
    36-RESEARCH.md Validation Architecture (targeted BE suite — never full pytest, it HANGS offline)
  </read_first>
  <action>
    Add `test_runs_api_summary.py` mirroring the `test_runs_api_family.py` fixtures. Cases: (1) owner GET /{id}/summary → 200 and the body aggregates the seeded row's `agent_outputs`/`token_usage`/`duration`/`agent_count`/`status`/`error` (assert the KPI + per-agent + failure fields reflect the seeded columns, no invented keys). (2) a SECOND user's run id → 404 (cross-owner IDOR→404, never 403, never foreign data). (3) a random/missing id → 404. (4) a run with malformed `agent_outputs`/`token_usage` strings → 200 with `[]`/`{}` fallback (no 500). (5) a revision family (parent + child) → `members` carries both with 1-based `revision_index` and the root's `parent_run_id` nulled. Run ONLY this targeted file (full pytest hangs offline).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_runs_api_summary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 200-for-owner, 404-cross-owner, 404-missing all asserted and green
    - malformed-JSON case returns 200 with fallback (no 500)
    - family-timeline case asserts revision_index + nulled root parent
    - `python3.11 -m pytest tests/unit/test_runs_api_summary.py -q` green
  </acceptance_criteria>
  <done>The summary endpoint's owner-scope + aggregation contract is proven by a targeted offline suite; goldens untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`GET /{id}/summary`) | untrusted `workflow_id` path param crosses here; must owner-gate |
| stored JSON (agent_outputs/token_usage) → response | stored text may be malformed or secret-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-IDOR | Information Disclosure | `get_run_summary` run resolve + family walk | mitigate | `_owner_gate_or_404` on `WorkflowRun.user_id` → 404; the owned family BFS terminates at any foreign ancestor so no foreign run metadata leaks into the timeline (test: 404 cross-owner) |
| T-36-02-DoS | Denial of Service | JSON parse of `agent_outputs`/`token_usage` | mitigate | `json.loads` in try/except → `[]`/`{}` fallback (existing runs.py:500-506 idiom); test: malformed-JSON → 200 |
| T-36-02-Leak | Information Disclosure | summary field projection | mitigate | Project only summary-safe fields; never echo raw child output/`run.output` (V7) |
| T-36-02-INV3 | Tampering | 5 backend goldens | accept | Endpoint is read-only additive; goldens untouched by construction; import-linter 4/0 |
| T-36-SC | Tampering | npm/pip installs | accept | No packages installed (endpoint over existing deps) |
</threat_model>

<verification>
- `python3.11 -m pytest tests/unit/test_runs_api_summary.py -q` green
- `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken
- `git diff --stat` shows only runs.py + the new test (no migration file, no model column, no engine/golden touch)
- Full `pytest` (bare) NOT run — HANGS offline; targeted suite only
</verification>

<success_criteria>
- Additive owner-scoped `GET /{id}/summary` returns 200 owner / 404 cross-owner / 404 missing
- Aggregates only existing columns + family walk; no new tables/migrations (Q3 additive-only)
- INV-3 goldens untouched by construction; import-linter 4/0; LOCK-B (no transport touch)
</success_criteria>

<artifacts_this_phase_produces>
- Route: `GET /api/runs/{workflow_id}/summary` (`get_run_summary` in `backend/app/api/runs.py`)
- Response model: `RunSummaryResponse` (`backend/app/api/runs.py`)
- Test suite: `backend/tests/unit/test_runs_api_summary.py`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/36-home-history-my-workflows-b2/36-02-SUMMARY.md` when done.
</output>
