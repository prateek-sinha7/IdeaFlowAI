---
phase: 36-home-history-my-workflows-b2
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx
  - frontend/src/components/savedworkflows/SavedWorkflowsPage.test.tsx
autonomous: true
requirements: [SHELL-02]
must_haves:
  truths:
    - "The My Workflows page heading reads 'My Workflows' (not 'Workflow Catalogue'); 'Catalogue' appears nowhere as a user-facing label (D-11 — reserved for the future marketplace)"
    - "The kebab actions Rename / Duplicate / Delete are real (wired to /api/user-workflows CRUD) and verified — never regressed to static text (D-15 reuse-don't-rebuild)"
    - "The page consumes Phase-32 @theme tokens + ui/ primitives; retired-palette grep = 0 (token authority)"
    - "Kebab menu + delete modal are keyboard + aria accessible (a11y invariant)"
  artifacts:
    - path: "frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx"
      provides: "Reskinned My Workflows page; heading relabeled; kebab CRUD preserved"
      contains: "My Workflows"
    - path: "frontend/src/components/savedworkflows/SavedWorkflowsPage.test.tsx"
      provides: "label='My Workflows' + kebab CRUD parity test (Wave 0 gap closure)"
      exports: []
  key_links:
    - from: "frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx kebab"
      to: "renameUserWorkflow / createUserWorkflow / deleteUserWorkflow"
      via: "handleRenameSave / handleDuplicate / handleDeleteConfirm"
      pattern: "(rename|create|delete)UserWorkflow"
---

<objective>
Relabel the "My Workflows" page (drop the reserved "Catalogue" wording), reskin it onto the Phase-32 token layer, and add the missing test proving the label + the already-wired kebab CRUD (D-11 / SHELL-02).

Purpose: SHELL-02 SC-3. The AppHeader nav pill already reads "My Workflows" (Phase 35); only the in-page `<h1>Workflow Catalogue</h1>` and delete copy remain. The kebab actions are ALREADY real — this is verify + reskin, not a rebuild (D-15).
Output: heading = "My Workflows"; retired-palette grep 0; a new `SavedWorkflowsPage.test.tsx` asserting the label + kebab CRUD parity.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-home-history-my-workflows-b2/36-CONTEXT.md
@.planning/phases/36-home-history-my-workflows-b2/36-RESEARCH.md
@.planning/phases/36-home-history-my-workflows-b2/36-PATTERNS.md
@.planning/phases/36-home-history-my-workflows-b2/36-VALIDATION.md
@frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx
@frontend/src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel "My Workflows" + reskin the page onto tokens (D-11 / D-15)</name>
  <files>frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx</files>
  <read_first>
    frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx (heading `<h1>Workflow Catalogue</h1>` :200-206, `var(--font-fraunces)` :201, delete copy "removed from your catalogue" :372, kebab handlers :118-186, KebabMenu :151-186, delete modal :353-387, retired hexes :27/:189/:271/:300/:332);
    frontend/src/styles/globals.css (@theme token layer + font-serif=Heebo);
    36-PATTERNS.md §4 (label rename + kebab-already-wired) + 36-RESEARCH.md Pitfall 2 (fraunces dead var)
  </read_first>
  <action>
    (1) Rename the `<h1>` at :202 from "Workflow Catalogue" → "My Workflows" and drop the `style={{ fontFamily: "var(--font-fraunces)" }}` (dead var) in favor of the `font-serif` class; update the delete copy at :372 ("removed from your catalogue" → "removed from your saved workflows") and the comment at :192. "Catalogue" must not survive as a user-facing label (D-11 — reserved for the future marketplace). (2) Reskin the retired-palette literals (`#f5f5f0` :189, `#1B2A4A` :27/:271/:300/:332) and stray stock palette to the Phase-32 tokens (`bg-surface-*`, `text-ink-*`, `border-line-*`, `text-brand`, `status-*`), consuming `components/ui/` primitives where present, mirroring the Phase-35 reskin idiom. **Preserve ALL behavior** (D-15 reuse-don't-rebuild): the cancellable mount-fetch, the KebabMenu Rename/Duplicate/Delete → `renameUserWorkflow`/`createUserWorkflow`/`deleteUserWorkflow` handlers with optimistic `setUserWorkflows`, the delete-confirm modal, and the launch/preload wiring stay byte-behavior-identical. Add/keep a11y on the kebab (aria-haspopup/expanded, role=menu/menuitem, Escape-close) and the delete modal (keyboard + aria) per the a11y invariant.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' src/components/savedworkflows/SavedWorkflowsPage.tsx && ! grep -q "Workflow Catalogue" src/components/savedworkflows/SavedWorkflowsPage.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - Heading renders "My Workflows"; `grep -q "Workflow Catalogue"` returns nothing; no "catalogue" user-facing copy remains
    - Retired-palette grep on SavedWorkflowsPage.tsx == 0; no stray stock palette; positive proof of `@theme`/`ui/` usage
    - Kebab handlers still call `renameUserWorkflow`/`createUserWorkflow`/`deleteUserWorkflow` (grep confirms — not regressed to static text)
    - `tsc --noEmit` no NEW errors (identity)
  </acceptance_criteria>
  <done>Heading is "My Workflows", retired-palette grep 0, kebab CRUD wiring preserved, a11y intact, tsc identity clean.</done>
</task>

<task type="auto">
  <name>Task 2: SavedWorkflowsPage.test.tsx — label + kebab CRUD parity (Wave 0 gap)</name>
  <files>frontend/src/components/savedworkflows/SavedWorkflowsPage.test.tsx</files>
  <read_first>
    frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx (post-relabel handlers + KebabMenu);
    frontend/src/components/catalog/HomeLaunchGrid.test.tsx (the co-located vitest + api-mock idiom to mirror);
    36-VALIDATION.md Wave 0 Requirements (this test is the named gap)
  </read_first>
  <action>
    Create the missing `SavedWorkflowsPage.test.tsx` (no test exists today — 36-VALIDATION Wave 0 gap). Mock `@/lib/api` (`getUserWorkflows` + `renameUserWorkflow`/`createUserWorkflow`/`deleteUserWorkflow`) with the co-located vitest idiom. Assert: (1) the page renders the heading "My Workflows" (never "Workflow Catalogue"). (2) Opening the kebab and clicking Rename invokes `renameUserWorkflow` and optimistically updates the row. (3) Duplicate invokes `createUserWorkflow`. (4) Delete → confirm invokes `deleteUserWorkflow` and removes the row. (5) The kebab trigger exposes aria-haspopup/expanded and the menu is Escape-closable (a11y). These lock the D-15 "kebab is real, not static text" contract.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/savedworkflows/SavedWorkflowsPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - heading-label test asserts "My Workflows" and the absence of "Workflow Catalogue"
    - each kebab action test asserts the real CRUD api call fires + the optimistic list mutation
    - a11y assertion on the kebab (aria + Escape)
    - `npx vitest run src/components/savedworkflows/SavedWorkflowsPage.test.tsx` green
  </acceptance_criteria>
  <done>The label + real-kebab-CRUD + a11y contract is locked by a green offline suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`/api/user-workflows` CRUD) | kebab mutations cross here; already owner-scoped server-side (existing, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-REG | Tampering/Correctness | kebab CRUD handlers | mitigate | Reskin only; preserve `renameUserWorkflow`/`createUserWorkflow`/`deleteUserWorkflow` wiring; Task 2 test asserts the real calls fire (no regression to static text) |
| T-36-03-A11Y | Denial (accessibility) | kebab menu + delete modal | mitigate | aria-haspopup/expanded + role=menu/menuitem + Escape-close; keyboard + aria on the delete modal |
| T-36-SC | Tampering | npm/pip installs | accept | No packages installed (reskin + test only) |
</threat_model>

<verification>
- `grep -q "Workflow Catalogue" SavedWorkflowsPage.tsx` returns nothing
- Retired-palette grep on SavedWorkflowsPage.tsx == 0
- `npx vitest run src/components/savedworkflows/SavedWorkflowsPage.test.tsx` green
- `npx tsc --noEmit` delta-clean
- Mocked Playwright e2e: LIVE-DEFERRED (offline)
</verification>

<success_criteria>
- Heading = "My Workflows"; "Catalogue" gone as a user label (D-11)
- Kebab Rename/Duplicate/Delete verified real (D-15); retired-palette grep 0 (token authority)
- a11y on kebab + delete modal; new test green
</success_criteria>

<artifacts_this_phase_produces>
- Reskinned + relabeled `SavedWorkflowsPage.tsx` (heading "My Workflows")
- `frontend/src/components/savedworkflows/SavedWorkflowsPage.test.tsx` (new — Wave 0 gap closure)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/36-home-history-my-workflows-b2/36-03-SUMMARY.md` when done.
</output>
