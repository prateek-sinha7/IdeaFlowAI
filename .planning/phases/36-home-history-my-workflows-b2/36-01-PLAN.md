---
phase: 36-home-history-my-workflows-b2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/catalog/HomeLaunchGrid.tsx
  - frontend/src/components/catalog/HomeLaunchGrid.test.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/layout/DashboardLayout.catalogHome.test.tsx
  - frontend/src/components/layout/DashboardLayout.waveMount.test.tsx
  - frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements: [SHELL-02]
must_haves:
  truths:
    - "The component is named HomeLaunchGrid; `grep -rn WorkflowCatalog frontend/src` returns 0 (D-11 rename complete)"
    - "The Home landing shows a prompt launcher + the deliverable launch grid + a recents strip in one fused view (merges input/home)"
    - "Selecting prototype/ppt/requiresWizard deliverables still routes to their template wizard; the input view stays reachable for the saved-workflow preload path (INV-3 no regression)"
    - "MainView page-keys stay generic — no route/page-key is renamed to a workflow name (SC-001/INV-1)"
    - "HomeLaunchGrid consumes Phase-32 @theme tokens + ui/ primitives; retired-palette grep = 0 (D-15 reskin-look-keep-behavior; token authority)"
  artifacts:
    - path: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      provides: "Renamed + reskinned home launch grid (was WorkflowCatalog)"
      exports: ["HomeLaunchGrid"]
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "Fused Home view (launcher + grid + recents); repointed HomeLaunchGrid import"
      contains: "HomeLaunchGrid"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "frontend/src/components/catalog/HomeLaunchGrid.tsx"
      via: "import + JSX mount on the home view"
      pattern: "HomeLaunchGrid"
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "recentRuns prop"
      via: "recents strip on the fused home view"
      pattern: "recentRuns"
---

<objective>
Land the deferred **`WorkflowCatalog`→`HomeLaunchGrid` rename (D-11)** and **fuse the `input` + `home` views into one landing** (prompt launcher + deliverable grid + recents strip), reskinned onto the Phase-32 token layer.

Purpose: SHELL-02 SC-1 (fused Home) + the D-11 rename that was correctly deferred from Phase 35. The component IS the home launch grid; "Catalogue" stays reserved for the future marketplace.
Output: `HomeLaunchGrid` component (renamed from `WorkflowCatalog`), a fused Home view in `DashboardLayout`, `grep -c WorkflowCatalog frontend/src == 0`, retired-palette grep = 0 on the reskinned file.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-home-history-my-workflows-b2/36-CONTEXT.md
@.planning/phases/36-home-history-my-workflows-b2/36-RESEARCH.md
@.planning/phases/36-home-history-my-workflows-b2/36-PATTERNS.md
@.planning/phases/36-home-history-my-workflows-b2/36-VALIDATION.md
@frontend/src/components/catalog/WorkflowCatalog.tsx
@frontend/src/components/layout/DashboardLayout.tsx
@frontend/src/styles/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-all-callers rename WorkflowCatalog → HomeLaunchGrid (D-11)</name>
  <files>frontend/src/components/catalog/HomeLaunchGrid.tsx, frontend/src/components/catalog/HomeLaunchGrid.test.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/layout/DashboardLayout.catalogHome.test.tsx, frontend/src/components/layout/DashboardLayout.waveMount.test.tsx, frontend/src/components/savedworkflows/SavedWorkflowsPage.tsx, frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/components/catalog/WorkflowCatalog.tsx (symbol+interface+JSDoc);
    36-PATTERNS.md §1 (the verified 7-file caller map);
    36-RESEARCH.md Pattern 2 + Runtime State Inventory (the vi.mock module-path gotcha)
  </read_first>
  <action>
    Run `grep -rn "WorkflowCatalog" frontend/src` FIRST to snapshot the 29 refs across 7 files, then repoint EVERY reference. (1) `git mv frontend/src/components/catalog/WorkflowCatalog.tsx → HomeLaunchGrid.tsx` and `WorkflowCatalog.test.tsx → HomeLaunchGrid.test.tsx` (keep the `catalog/` dir — it is a developer-facing path, not a user label, so D-11 "Catalogue reserved" is not violated; keeping the dir minimizes mock-path churn). (2) In HomeLaunchGrid.tsx rename the named export `function WorkflowCatalog`→`HomeLaunchGrid`, the `interface WorkflowCatalogProps`→`HomeLaunchGridProps`, and the JSDoc header. (3) In HomeLaunchGrid.test.tsx repoint `import { WorkflowCatalog } from "./WorkflowCatalog"`→`from "./HomeLaunchGrid"`, rename all 7 `<WorkflowCatalog .../>` render calls + the 2 describe strings. (4) In DashboardLayout.tsx repoint the import at :9 (`@/components/catalog/WorkflowCatalog`→`.../HomeLaunchGrid`), rename the JSX tag at :1429, and update the comments at :1413/:1567. (5) In DashboardLayout.catalogHome.test.tsx AND DashboardLayout.waveMount.test.tsx update the `vi.mock("@/components/catalog/WorkflowCatalog", …)` module path to `.../HomeLaunchGrid` and the stub export key `WorkflowCatalog:`→`HomeLaunchGrid:` (a stale mock path silently mounts the real component → jsdom getToken failure); the `stub-workflow-catalog` testid may stay. (6) Update the comment-only refs in SavedWorkflowsPage.tsx:192 and api.ts:613/629. Per D-11. **SC-001 guard:** do NOT touch the `MainView` union at DashboardLayout.tsx:173 or the `"home"`/`"catalog"` page-key STRINGS — the rename is component-symbol only, never a route/page-key rename.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rq "WorkflowCatalog" src && npx vitest run src/components/catalog/HomeLaunchGrid.test.tsx src/components/layout/DashboardLayout.catalogHome.test.tsx src/components/layout/DashboardLayout.waveMount.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WorkflowCatalog" frontend/src == 0` (the D-11 rename hard gate)
    - `grep -n '"home"' + '"catalog"' + '"saved-workflows"'` page-keys in DashboardLayout.tsx:173 UNCHANGED (SC-001)
    - HomeLaunchGrid.test.tsx + both DashboardLayout mock-path tests pass (mock paths resolve; real component not accidentally mounted)
  </acceptance_criteria>
  <done>The component + file are named HomeLaunchGrid, all 7 caller files repointed, grep-count 0, all three touched vitest suites green, page-keys generic.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin HomeLaunchGrid onto Phase-32 tokens (D-15 / token authority)</name>
  <files>frontend/src/components/catalog/HomeLaunchGrid.tsx</files>
  <read_first>
    frontend/src/components/catalog/HomeLaunchGrid.tsx (post-rename; retired palette at the former :115 `#f5f5f0`, :131 `var(--font-fraunces)`, :191/:200 `#1B2A4A`);
    frontend/src/styles/globals.css (the @theme token layer + font-serif=Heebo);
    36-RESEARCH.md Pitfall 1 + Pitfall 2 (grep anchors; fraunces is a DEAD var)
  </read_first>
  <action>
    Migrate every retired-palette literal to the Phase-32 token layer, preserving ALL behavior (the two-gate filter `user_launchable` + `canRunPipeline`, the wizard fork, the friendly-label rule, the cancellable mount-fetch) — reskin-look, keep-behavior (D-15). Replace `style={{ background: "#f5f5f0" }}` with the `bg-surface-paper` token class; replace `style={{ fontFamily: "var(--font-fraunces)" }}` on the hero h1 with the `font-serif` class (the fraunces var is DEAD — live fonts are Manrope/Heebo); replace the `#1B2A4A` hover/upgrade colors with `--brand`/`text-brand` tokens; migrate stray stock palette (`text-gray-*`, `bg-white/60`, `divide-gray-200`, `text-red-600`/`bg-red-50` error chip) to `text-ink-*`/`bg-surface-*`/`border-line-*`/`status-failed` tokens, mirroring the Phase-35 reskin idiom. Do NOT fork the palette; consume the shared @theme tokens + `components/ui/` primitives where a primitive exists.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' src/components/catalog/HomeLaunchGrid.tsx && npx vitest run src/components/catalog/HomeLaunchGrid.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Retired-palette grep on HomeLaunchGrid.tsx == 0 (`#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b`)
    - No stray stock palette (`gray-`, `blue-`, raw `#hex`) remaining; positive proof of `@theme` token / `ui/` primitive usage
    - HomeLaunchGrid.test.tsx green (two-gate filter + friendly label + wizard fork unchanged)
  </acceptance_criteria>
  <done>HomeLaunchGrid renders on Phase-32 tokens with retired-palette grep 0, behavior tests green, no palette fork.</done>
</task>

<task type="auto">
  <name>Task 3: Fuse the Home view — prompt launcher + recents strip (SHELL-02 SC-1)</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx</files>
  <read_first>
    frontend/src/components/layout/DashboardLayout.tsx (home block :1420-1431, input block :1545-1574, handleSelectFeature :807-811, handleLaunchSaved preload :866-879, recentRuns prop :68/:283, MainView union :173);
    36-RESEARCH.md Pattern 3 + Open Question 1 (fused-home shape recommendation)
  </read_first>
  <action>
    On the `mainView === "home"` view block, compose the fused landing: the prompt launcher affordance (folded from the `input` view idiom) + the existing `<HomeLaunchGrid onSelectFeature={handleSelectFeature} onLaunchSaved={handleLaunchSaved} userTier={userTier} />` deliverable grid + a recents strip driven by the already-threaded `recentRuns?: WorkflowRun[]` prop (:68) — NO new fetch. Keep the `AnimatePresence mode="wait"` keyed `motion.div` idiom. **Preserve the wizard fork** — prototype/ppt/`requiresWizard` selections still `router.push` to their template wizard via `handleClick` inside HomeLaunchGrid; the generic-path prompt launcher routes through the existing `handleSelectFeature`/launch seam (keep page-keys generic, SC-001). **INV-3 no-regression:** do NOT delete the `input` view — `handleLaunchSaved` sets `mainView="input"` with `savedComposition` for the saved-workflow preload path (:866-879); the fused launcher adds to `home`, it does not remove `input`. Exact visual composition is Claude's discretion within the D-15 "adopt mock visual language, keep behavior" envelope.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/layout/DashboardLayout.catalogHome.test.tsx src/components/layout/DashboardLayout.waveMount.test.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardLayout.catalogHome.test.tsx` still asserts the default home landing mounts the (renamed) grid — green
    - The `input` view block remains present (saved-workflow preload path intact); `grep -n 'mainView === "input"' DashboardLayout.tsx` still matches
    - `tsc --noEmit` yields no NEW errors vs baseline (identity, filtering the known mockApi.ts baseline)
    - Recents strip reads `recentRuns` prop (no new fetch added)
  </acceptance_criteria>
  <done>Home renders launcher + grid + recents in one view; wizard fork + input-preload path preserved; page-keys generic; tsc identity clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/module graph → vitest | a stale `vi.mock` module path silently mounts the real component (jsdom getToken failure) |
| client render → route | fused-home selection must not branch a route on a workflow name (SC-001) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-R | Repudiation/Correctness | vi.mock module paths in the 2 DashboardLayout tests | mitigate | Update both mock paths + stub keys in lockstep with the file rename; Task 1 acceptance asserts both suites green |
| T-36-03 | Tampering | fused-home selection routing | mitigate | Keep MainView page-keys generic; wizard fork by `CHAIN_OPTIONS`/type, never a workflow-name branch (SC-001/INV-1) |
| T-36-SC | Tampering | npm/pip installs | accept | No packages installed this plan (rename + reskin + view-state only) |
</threat_model>

<verification>
- `grep -c "WorkflowCatalog" frontend/src == 0`
- Retired-palette grep on HomeLaunchGrid.tsx == 0
- `npx vitest run` green on: HomeLaunchGrid.test.tsx, DashboardLayout.catalogHome.test.tsx, DashboardLayout.waveMount.test.tsx
- `npx tsc --noEmit` delta-clean (no NEW errors)
- Mocked Playwright e2e: LIVE-DEFERRED (offline webServer timeout, Phase-35 precedent) — capture baseline, do not hang
</verification>

<success_criteria>
- HomeLaunchGrid rename complete (grep 0), all callers repointed, page-keys generic (SC-001)
- Fused Home shows launcher + grid + recents; wizard fork + input-preload path preserved (INV-3)
- HomeLaunchGrid retired-palette grep 0 (D-15 / token authority)
- All touched vitest suites green; tsc identity clean
</success_criteria>

<artifacts_this_phase_produces>
- `HomeLaunchGrid` component — `frontend/src/components/catalog/HomeLaunchGrid.tsx` (renamed from `WorkflowCatalog.tsx`)
- `HomeLaunchGridProps` interface (renamed from `WorkflowCatalogProps`)
- `HomeLaunchGrid.test.tsx` (renamed test)
- Fused Home view block in `DashboardLayout.tsx` (launcher + grid + recents)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/36-home-history-my-workflows-b2/36-01-SUMMARY.md` when done.
</output>
