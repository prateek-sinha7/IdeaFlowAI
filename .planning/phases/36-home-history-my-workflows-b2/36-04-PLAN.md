---
phase: 36-home-history-my-workflows-b2
plan: 04
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/components/history/RunDetailPage.tsx
  - frontend/src/components/history/RunDetailPage.test.tsx
autonomous: true
requirements: [SHELL-03]
must_haves:
  truths:
    - "A RunDetailPage renders per-agent breakdown, KPI stats, a failure banner (when the run failed/degraded/cancelled), and a version/revision timeline — all sourced from GET /api/runs/{id}/summary (SHELL-03)"
    - "The page REUSES the existing shared surfaces (VersionTimeline, DegradedRunAffordance, parseFailedAgentIds/buildAgentNameById, the token/duration formatters) — no duplicate Phase-32 surfaces (D-15 reuse-don't-rebuild; no dual implementation)"
    - "The page is keyed on generic run data (status/agents/tokens) — never a workflow-name branch (SC-001/INV-1)"
    - "The page consumes Phase-32 @theme tokens + ui/ primitives; retired-palette grep = 0 (token authority); keyboard + aria accessible (a11y)"
  artifacts:
    - path: "frontend/src/components/history/RunDetailPage.tsx"
      provides: "Run detail/reopen page fed by the run-summary endpoint"
      exports: ["RunDetailPage"]
    - path: "frontend/src/lib/api.ts"
      provides: "getRunSummary client + RunSummary response type"
      contains: "getRunSummary"
  key_links:
    - from: "frontend/src/components/history/RunDetailPage.tsx"
      to: "GET /api/runs/{id}/summary"
      via: "getRunSummary in a cancellable mount-fetch"
      pattern: "getRunSummary"
    - from: "frontend/src/components/history/RunDetailPage.tsx"
      to: "DegradedRunAffordance / VersionTimeline"
      via: "reused shared failure + timeline surfaces"
      pattern: "(DegradedRunAffordance|VersionTimeline)"
---

<objective>
Build the **Run detail/reopen page** (`RunDetailPage`) fed by the Wave-1 **`GET /api/runs/{id}/summary`** endpoint — per-agent breakdown, KPI stats, failure banner, and version/revision timeline — reusing the existing shared surfaces rather than duplicating Phase-32 UI (SHELL-03 / D-15).

Purpose: SHELL-03 FE half. The current terminal-run detail is the in-panel `selectedRun` view inside `WorkflowHistory`; this promotes it into a self-contained page fed by the summary read, so History (Wave 3) can render it and retire its inline duplicate (no dual implementation).
Output: `getRunSummary` + `RunSummary` type in `api.ts`; `RunDetailPage.tsx` (+ test) composing the reused KPI strip / per-agent cards / `VersionTimeline` / `DegradedRunAffordance` on Phase-32 tokens.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-home-history-my-workflows-b2/36-CONTEXT.md
@.planning/phases/36-home-history-my-workflows-b2/36-RESEARCH.md
@.planning/phases/36-home-history-my-workflows-b2/36-PATTERNS.md
@.planning/phases/36-home-history-my-workflows-b2/36-VALIDATION.md
@frontend/src/components/history/WorkflowHistory.tsx
@frontend/src/lib/api.ts
@.planning/phases/36-home-history-my-workflows-b2/36-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: getRunSummary client + RunSummary type (interface-first)</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/lib/api.ts (getWorkflow + getRunFamily client idiom, `request(...)` + `authHeaders`, WorkflowSummary/RunFamily response-type idiom :607-640);
    36-02-PLAN.md (the RunSummaryResponse field contract the endpoint returns);
    backend/app/api/runs.py get_run_summary (the authoritative response shape, once 36-02 lands)
  </read_first>
  <action>
    Add a `RunSummary` TypeScript interface mirroring the backend `RunSummaryResponse` field-for-field (`id`, `title`, `type`, `status`, `duration`, `agent_count`, `token_usage`, `error`, `agents`, `root_id`, `members` reusing the existing family-member type). Add `export async function getRunSummary(token: string, runId: string): Promise<RunSummary>` calling `request<RunSummary>("/api/runs/${runId}/summary", { method: "GET", headers: authHeaders(token) })` — mirror the `getRunFamily`/`getWorkflow` idiom exactly. Interface-first: this contract is consumed by Task 2 and by History (Wave 3). Also update any remaining comment-only `WorkflowCatalog` reference if 36-01 left one (grep confirms 0).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `getRunSummary` + `RunSummary` exported; fields match the backend `RunSummaryResponse` (no extra/missing key)
    - `request<RunSummary>("/api/runs/${runId}/summary", ...)` with `authHeaders` (owner JWT)
    - `tsc --noEmit` no NEW errors (identity)
  </acceptance_criteria>
  <done>The typed run-summary client exists and type-checks; contract stable for the page + History.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunDetailPage.tsx — reuse shared surfaces, feed off getRunSummary</name>
  <files>frontend/src/components/history/RunDetailPage.tsx, frontend/src/components/history/RunDetailPage.test.tsx</files>
  <behavior>
    - Given a completed run summary → renders the KPI strip (total/input/output tokens + duration), the per-agent breakdown cards, and the VersionTimeline
    - Given a failed/degraded/cancelled run summary → renders the DegradedRunAffordance failure banner keyed on generic status (no workflow-name branch)
    - Loading → a skeleton/spinner; fetch error → a graceful empty/error state (no crash)
    - The summary fetch is cancellable (no stale set after unmount)
  </behavior>
  <read_first>
    frontend/src/components/history/WorkflowHistory.tsx (the in-panel detail anatomy to promote — KPI strip :485-505, per-agent breakdown + detailAgentOutputs dedup memo :303-336/:508-566, VersionTimeline :676-681, DegradedRunAffordance gate :436-452, tabs :683-870);
    frontend/src/components/preview/PreviewPanel (DegradedRunAffordance export), frontend/src/lib/parseFailedAgents.ts (parseFailedAgentIds/buildAgentNameById), frontend/src/components/history/RevisionFamilyView.tsx (VersionTimeline);
    36-PATTERNS.md §5 (the RunDetailPage composition) + 36-RESEARCH.md Open Question 2 (avoid duplicating Phase-32 surfaces)
  </read_first>
  <action>
    Create `RunDetailPage` (`{ runId, onBack, activeRunId?, onViewRunningPipeline? }` props) that fetches `getRunSummary(token, runId)` in a cancellable mount-fetch (the `WorkflowCatalog.tsx:64-89` guard idiom) and composes the detail from the summary payload — REUSING the existing shared components, never re-implementing them (D-15 / no dual impl): the KPI token strip via the existing `fmt` M/K formatter + `formatDuration`, the per-agent breakdown from `summary.agents` (the same dedup shape as `detailAgentOutputs`), `VersionTimeline` from `summary.members`/`root_id`, and `DegradedRunAffordance` gated on the generic `summary.status ∈ {failed,cancelled,degraded}` with failed-agent ids via `parseFailedAgentIds(summary.error)` + `buildAgentNameById`. **SC-001/INV-1:** key every branch on generic run fields (status/agents/tokens), NEVER on `summary.type` name. Frame the page with Phase-32 `@theme` tokens + `components/ui/` primitives + the Phase-35 shell chrome — NO palette fork. a11y: the page frame, tab controls, and back button keyboard + aria. Write `RunDetailPage.test.tsx` first (mock `getRunSummary`): completed → KPI+agents+timeline render; failed → DegradedRunAffordance renders; error → graceful; assert no retired palette in class strings.
  </action>
  <verify>
    <automated>cd frontend && ! grep -rEn '#1B2A4A|#2563eb|#f5f5f0|\bInter\b|\bFraunces\b|\bJetBrains\b' src/components/history/RunDetailPage.tsx && npx vitest run src/components/history/RunDetailPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RunDetailPage renders KPI stats + per-agent breakdown + VersionTimeline from `getRunSummary` data
    - Failure banner via the REUSED `DegradedRunAffordance`, keyed on generic `summary.status` (grep: no `summary.type ===` branch)
    - Imports (not re-implements) `VersionTimeline`, `DegradedRunAffordance`, `parseFailedAgentIds` (no dual implementation)
    - Retired-palette grep on RunDetailPage.tsx == 0; a11y on frame/tabs/back
    - `npx vitest run src/components/history/RunDetailPage.test.tsx` green
  </acceptance_criteria>
  <done>RunDetailPage composes the reused surfaces off the summary endpoint on Phase-32 tokens, generic-keyed, retired-palette 0, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (`GET /{id}/summary`) | owner JWT sent via authHeaders; server owner-gates (36-02) |
| summary payload → render | display of owned run data; must not workflow-name-branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04-SC001 | Tampering | RunDetailPage branch logic | mitigate | Key failure/timeline/agent rendering on generic run fields (status/agents/tokens), never `summary.type` name (SC-001/INV-1); test asserts no name branch |
| T-36-04-DUP | Correctness | shared failure/timeline surfaces | mitigate | Import + reuse `DegradedRunAffordance`/`VersionTimeline`/`parseFailedAgentIds` (no dual impl → live + reopen cannot drift, ISS-017) |
| T-36-04-A11Y | Denial (accessibility) | page frame + tabs + back | mitigate | keyboard + aria on the detail page controls |
| T-36-SC | Tampering | npm/pip installs | accept | No packages installed (page + client + test only) |
</threat_model>

<verification>
- Retired-palette grep on RunDetailPage.tsx == 0
- `npx vitest run src/components/history/RunDetailPage.test.tsx` green
- `npx tsc --noEmit` delta-clean (getRunSummary + RunSummary type)
- Mocked Playwright e2e: LIVE-DEFERRED (offline); live summary round-trip → Phase 34
</verification>

<success_criteria>
- RunDetailPage renders agents + KPIs + failure banner + version timeline off `GET /{id}/summary` (SHELL-03)
- Reuses shared surfaces, no dual implementation (D-15); generic-keyed (SC-001)
- Retired-palette grep 0 (token authority); a11y; tsc identity clean
</success_criteria>

<artifacts_this_phase_produces>
- `getRunSummary(token, runId)` client + `RunSummary` type — `frontend/src/lib/api.ts`
- `RunDetailPage` component — `frontend/src/components/history/RunDetailPage.tsx`
- `frontend/src/components/history/RunDetailPage.test.tsx` (new)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/36-home-history-my-workflows-b2/36-04-SUMMARY.md` when done.
</output>
