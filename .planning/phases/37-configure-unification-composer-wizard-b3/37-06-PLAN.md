---
phase: 37-configure-unification-composer-wizard-b3
plan: 06
type: execute
wave: 3
depends_on: ["37-01", "37-03"]
files_modified:
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/workflow/WorkflowDialog.tsx
  - frontend/src/components/workflow/WorkflowDialog.test.tsx
  - frontend/src/components/workflow/AgentDrawer.test.tsx
autonomous: true
requirements: [SHELL-04]
must_haves:
  truths:
    - "ND-7 (LOCK-E): the Agent-drawer Config tab SURFACES per-agent prompt-override but its PERSISTENCE is DEFERRED — NO durable override storage (no table, no endpoint, no save wiring); surface only."
    - "The Agent drawer is a 4-tab inspector (Overview/Skills/Hooks/Config) reading real capability/manifest data — reusing AgentPromptSection + SkillsHooksTab, not rebuilt."
    - "SC-001/INV-5: the Workflow dialog's 'Engineer-only' gating is the capability user_allowed reflection (declared data), never a code branch; it surfaces declared capabilities/context/compaction from the compiled WorkflowDetail."
    - "Token gate: the drawer Config tab + Workflow dialog use Phase-32 @theme tokens + ui/ primitives — retired palette 0 on touched files."
  artifacts:
    - path: "frontend/src/components/workflow/WorkflowDialog.tsx"
      provides: "Declared capabilities/context/compaction dialog with user_allowed gating"
      contains: "user_allowed"
  key_links:
    - from: "frontend/src/components/workflow/WorkflowDialog.tsx"
      to: "getWorkflowDetail (/api/workflows/{id}) + /api/capabilities"
      via: "surfaces declared capabilities/context/compaction + user_allowed gating"
      pattern: "context_providers|user_allowed"
---

<objective>
Complete the shell inspectors: the 4-tab Agent drawer (Overview/Skills/Hooks/Config) and the Workflow dialog. The Agent drawer reads real capability/manifest data and REUSES AgentPromptSection (Config tab) + SkillsHooksTab — the Config tab SURFACES per-agent prompt-override but its PERSISTENCE is DEFERRED (ND-7/LOCK-E): no durable store, no endpoint, no save wiring. The Workflow dialog surfaces the compiled workflow's declared capabilities/context/compaction, with "Engineer-only" gating driven by the capability `user_allowed` flag (declared data, INV-5 — never a code branch).

Purpose: give capabilities a real home in the shell (declared, live data) while honoring the ND-7 persistence deferral.
Output: the drawer's 4-tab structure (incl. the surface-only Config tab) + WorkflowDialog.tsx with user_allowed gating.

Depends on 37-03 (shares AgentsPopup.tsx — must land after the composer reskin).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-CONTEXT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-RESEARCH.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md

# The drawer surfaces (REUSE) + the declared-data sources
@frontend/src/components/workflow/AgentsPopup.tsx    # AgentPromptSection:197 (Config tab, mounted :514), SkillsHooksTab:715, CapabilityPaletteSection:1023, user_allowed:1160
@frontend/src/lib/api.ts                             # WorkflowDetail (context_providers:746, steps) + getWorkflowDetail:758; capability shape {security_gated,user_allowed}
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agent drawer 4-tab structure incl. the surface-only Config tab (ND-7 persistence deferred)</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx, frontend/src/components/workflow/AgentDrawer.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentsPopup.tsx:197-260 (AgentPromptSection — per-agent prompt-override surface), mount :514
    - frontend/src/components/workflow/AgentsPopup.tsx:715 (SkillsHooksTab — Skills/Hooks tabs)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md (Wave 5 — Agent drawer Config tab ND-7 deferred)
    - .planning/CHAT-AND-UI-CONVERGENCE-PLAN.md (LOCK-E: ND-7 persistence deferred)
  </read_first>
  <behavior>
    - The Agent drawer presents four tabs: Overview, Skills, Hooks, Config.
    - The Config tab renders AgentPromptSection (the per-agent prompt-override surface) but has NO Save/Reset that persists to a server — surface only.
    - Skills/Hooks tabs reuse SkillsHooksTab (per-agent suggestions).
    - No durable override write occurs (no createUserWorkflow-style override endpoint, no localStorage override store).
  </behavior>
  <action>
    Assemble/confirm the 4-tab Agent drawer (Overview/Skills/Hooks/Config) reusing the existing sections: Config tab -> AgentPromptSection; Skills/Hooks -> SkillsHooksTab; Overview -> the existing agent overview surface. Reskin any drawer chrome onto Phase-32 tokens. CRITICAL (ND-7/LOCK-E): the Config tab SURFACES the prompt-override field ONLY — do NOT build durable persistence (no new endpoint, no table, no localStorage/sessionStorage override store, no save-to-server wiring). If the mock shows a persisting Save/Reset, render it inert or omit it (mock-fiction). Add AgentDrawer.test.tsx: four tabs present; Config tab renders the override surface; asserting NO persistence call fires on edit (the ND-7 gate — grep/behavioral assertion that no override-save request is made).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/AgentDrawer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflow/AgentDrawer.test.tsx` green (4 tabs + Config surface + no-persistence assertion).
    - ND-7 persistence NOT built: no override-persistence endpoint/store — `grep -cEi 'saveOverride|updateAgentPrompt|prompt.override.*(fetch|request<|/api/)' frontend/src/components/workflow/AgentsPopup.tsx` == 0.
    - Retired-palette gate 0 on the touched drawer chrome; positive @theme/ui usage.
  </acceptance_criteria>
  <done>The 4-tab drawer surfaces the Config prompt-override with NO durable persistence (ND-7); Skills/Hooks/Overview reuse existing sections; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Workflow dialog — declared capabilities/context/compaction + user_allowed 'Engineer-only' gating</name>
  <files>frontend/src/components/workflow/WorkflowDialog.tsx, frontend/src/components/workflow/WorkflowDialog.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentsPopup.tsx:1023-1170 (CapabilityPaletteSection — user_allowed gate at :1160; the declared strip :1101-1119)
    - frontend/src/lib/api.ts:739-766 (WorkflowDetail.context_providers + steps + getWorkflowDetail)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md (Wave 3 — Workflow dialog: Engineer-only = user_allowed reflection)
  </read_first>
  <behavior>
    - The Workflow dialog fetches getWorkflowDetail and surfaces the compiled workflow's declared capabilities, context_providers, and compaction.
    - A capability with user_allowed=false renders as 'Engineer-only' (locked) — the gating is a reflection of the declared flag, not a hardcoded branch.
    - The dialog shows real declared data (no mock '8 agents/Single-shot' fiction).
  </behavior>
  <action>
    Create frontend/src/components/workflow/WorkflowDialog.tsx surfacing the compiled workflow's declared capabilities/context_providers/compaction from getWorkflowDetail (+ the /api/capabilities registry for the capability metadata). The 'Engineer-only' gating MUST be `!cap.user_allowed` (declared-data reflection, INV-5/SC-001 — never `pipelineType === ...`). Reuse the CapabilityPaletteSection declared-strip idiom rather than rebuilding it. Use Phase-32 tokens + ui/ primitives. Write WorkflowDialog.test.tsx: renders declared context_providers/capabilities from a mocked WorkflowDetail; a user_allowed=false capability renders Engineer-only/locked; a user_allowed=true one does not; no hardcoded agent-count fiction.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/WorkflowDialog.test.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflow/WorkflowDialog.test.tsx` green (declared surfacing + user_allowed gating).
    - Gating is declared-data: `grep -c 'user_allowed' frontend/src/components/workflow/WorkflowDialog.tsx` >= 1; `grep -cE 'od_prototype|od_ppt|pipelineType ===' frontend/src/components/workflow/WorkflowDialog.tsx` == 0 (SC-001).
    - Real declared data (no fiction): surfaces context_providers from WorkflowDetail — `grep -c 'context_providers' frontend/src/components/workflow/WorkflowDialog.tsx` >= 1.
    - `npx tsc --noEmit` IDENTITY (filter mockApi.ts); retired-palette 0 on WorkflowDialog.tsx.
    - Mocked Playwright e2e: LIVE-DEFERRED.
  </acceptance_criteria>
  <done>WorkflowDialog surfaces declared capabilities/context/compaction with user_allowed 'Engineer-only' gating (declared data); tsc identity; token gate 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog -> /api/workflows/{id} + /api/capabilities | read-only compiled declared data |
| drawer Config tab | per-agent prompt-override input (NOT persisted — ND-7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06-01 | Elevation | capability escalation surfaced in the dialog | mitigate | user_allowed=false renders Engineer-only/locked read-only; the dialog cannot flip a security_gated/user_allowed flag (display only) |
| T-37-06-02 | Tampering | per-agent prompt-override injection | accept | Config tab is surface-only (ND-7) — no override reaches the server this phase, so no injection path is opened |
| T-37-06-03 | Information disclosure | WorkflowDetail leakage | accept | getWorkflowDetail returns compiled declared metadata (no owner data); owner-scoping unchanged |
| T-37-SC | Tampering | package installs | accept | No npm installs this phase — N/A |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflow/AgentDrawer.test.tsx src/components/workflow/WorkflowDialog.test.tsx` green.
- Retired-palette grep 0 on WorkflowDialog.tsx + touched drawer chrome; positive @theme/ui usage.
- ND-7: no override-persistence endpoint/store (grep == 0).
- SC-001: user_allowed gating is declared-data, no prototype-name branch.
- `npx tsc --noEmit` identity (filter mockApi.ts).
- Mocked Playwright e2e: LIVE-DEFERRED (Phase 34).
</verification>

<success_criteria>
- The 4-tab Agent drawer surfaces the Config prompt-override with NO durable persistence (ND-7/LOCK-E).
- The Workflow dialog surfaces declared capabilities/context/compaction with user_allowed 'Engineer-only' gating (declared data, INV-5/SC-001).
- Touched files at 0 retired palette; tsc identity.
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/components/workflow/AgentsPopup.tsx` — MODIFIED: 4-tab drawer incl. the surface-only Config tab (ND-7); token reskin.
- `frontend/src/components/workflow/WorkflowDialog.tsx` — NEW: declared capabilities/context/compaction + user_allowed gating.
- `frontend/src/components/workflow/WorkflowDialog.test.tsx`, `frontend/src/components/workflow/AgentDrawer.test.tsx` — NEW tests.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-configure-unification-composer-wizard-b3/37-06-SUMMARY.md` when done.
</output>
