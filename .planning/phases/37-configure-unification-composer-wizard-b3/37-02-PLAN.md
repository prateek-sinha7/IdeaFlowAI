---
phase: 37-configure-unification-composer-wizard-b3
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - frontend/src/app/workflow/configure/page.tsx
  - frontend/src/components/workflow/ConfigureScreen.tsx
  - frontend/src/components/workflow/ConfigureScreen.test.tsx
  - frontend/src/lib/draft.ts
  - frontend/src/lib/draft.test.ts
autonomous: true
requirements: [SHELL-04]
must_haves:
  truths:
    - "SC-001/INV-1: the Configure screen shows the Templates + Design System accordions ONLY when the selected deliverable's WorkflowDetail.context_providers includes 'opendesign' — keyed on the SAME declared signal as the D-15/C backend seam, never a prototype-name branch."
    - "D-15/C consumer: Configure is a generic per-run setup surface (Describe + Templates + Design System + Review Gates + Workflow Settings) for ANY deliverable type; launch composes the generic LaunchCommand fields (template_id/design_system_id/agent_ids/selections)."
    - "ND-1 (LOCK-E): the Save-draft button persists the run draft CLIENT-SIDE only via sessionStorage (a single keyed JSON blob), read + cleared once at launch — NO draft rows, NO DB, NO migration."
    - "Token gate: the new Configure surface uses Phase-32 @theme tokens + ui/ primitives + Phase-35 shell — retired palette count 0 on every new file."
  artifacts:
    - path: "frontend/src/components/workflow/ConfigureScreen.tsx"
      provides: "Generic per-run setup accordions keyed on the declared signal"
      contains: "context_providers"
    - path: "frontend/src/lib/draft.ts"
      provides: "Client-side sessionStorage draft helper (ND-1)"
      contains: "sessionStorage"
  key_links:
    - from: "frontend/src/components/workflow/ConfigureScreen.tsx"
      to: "getWorkflowDetail (/api/workflows/{id})"
      via: "declared-signal read to gate Templates/DS accordions"
      pattern: "context_providers"
    - from: "frontend/src/components/workflow/ConfigureScreen.tsx"
      to: "frontend/src/lib/draft.ts"
      via: "Save-draft writes + launch reads/clears the sessionStorage blob"
      pattern: "saveDraft|readDraft|clearDraft"
---

<objective>
The Configure screen: ONE generic per-run setup surface for EVERY deliverable type — Describe + Templates + Design System + Review Gates + Workflow Settings, laid out as accordions. It replaces the prototype-only wizardry with a surface keyed on DECLARED run inputs (D-15/C): the Templates + Design System accordions appear only when the selected deliverable declares `context_providers:[opendesign]` (the SAME signal the Wave-1 backend seam keys on — SC-001/INV-1, never a prototype-name branch). It also lands the ND-1 consumer #2: a CLIENT-SIDE Save-draft (sessionStorage) kept until launch.

Purpose: unify per-run setup so any deliverable that opts into template/DS gains the accordions with zero per-deliverable code.
Output: `/workflow/configure` route + `ConfigureScreen` composing REUSED surfaces (DiscoveryForm, TemplateGallery, DesignSystemPicker, AdvancedExpander/ReviewGates) + a `draft.ts` sessionStorage helper.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-CONTEXT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-RESEARCH.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md

# REUSE surfaces (compose, do NOT rebuild)
@frontend/src/lib/api.ts                                     # WorkflowDetail (context_providers:746) + getWorkflowDetail:758; LaunchCommand fields
@frontend/src/components/workflow/prototype/DiscoveryForm.tsx    # Describe body (DiscoveryAnswers:20-29)
@frontend/src/components/workflow/prototype/TemplateGallery.tsx  # Templates (token-clean; live GET /api/prototype/templates)
@frontend/src/components/workflow/prototype/DesignSystemPicker.tsx  # Design System (onSelect/onSelectCustom)
@frontend/src/components/workflow/AgentsPopup.tsx            # AdvancedExpander export:1337 (Review Gates + Workflow Settings levers)
@frontend/src/app/workflow/prototype/templates/page.tsx     # prototype.draft sessionStorage idiom to generalize (STORAGE_KEY:25, read:98, clear:135)

# Phase-35 shell + Phase-36 surfaces + Phase-32 tokens
@.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
@.planning/phases/36-home-history-my-workflows-b2/36-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client-side draft helper (ND-1) + the Configure route/screen scaffold keyed on the declared signal</name>
  <files>frontend/src/lib/draft.ts, frontend/src/lib/draft.test.ts, frontend/src/app/workflow/configure/page.tsx, frontend/src/components/workflow/ConfigureScreen.tsx, frontend/src/components/workflow/ConfigureScreen.test.tsx</files>
  <read_first>
    - frontend/src/app/workflow/prototype/templates/page.tsx:25,74-135 (the prototype.draft read/clear-once idiom + chain.* siblings to NOT confuse)
    - frontend/src/lib/api.ts:739-766 (WorkflowDetail.context_providers + getWorkflowDetail)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-RESEARCH.md (Client-Side Draft Persistence section)
  </read_first>
  <behavior>
    - draft.ts: saveDraft(payload) writes ONE keyed JSON blob to sessionStorage; readDraft() returns it or null; clearDraft() removes it; the payload shape is a superset of LaunchCommand (templateId, designSystemId, brief, agentIds, selections?, modelOverrides?, customDsBody?, customTemplateBody?, gateAgentIds?).
    - draft.ts: reading after clear returns null; malformed JSON returns null (never throws).
    - ConfigureScreen: given a WorkflowDetail whose context_providers includes 'opendesign', the Templates + Design System accordions render; given one that does NOT, they are hidden and only Describe + Review Gates + Workflow Settings render (SC-001 generic keying).
  </behavior>
  <action>
    Create frontend/src/lib/draft.ts — a standalone client-side draft helper copying the prototype.draft idiom EXACTLY (single keyed JSON blob in sessionStorage, written on save, read + removed once). Use a distinct key (e.g. "configure.draft") so it is a peer of prototype.draft, NOT of the chain.* run-chaining keys. NO DB, NO fetch, NO new endpoint (ND-1/LOCK-E). Export saveDraft/readDraft/clearDraft with a typed ConfigureDraft interface (superset of LaunchCommand fields).
    Create the route frontend/src/app/workflow/configure/page.tsx that renders ConfigureScreen inside the Phase-35 shell/DashboardLayout, and the ConfigureScreen.tsx component scaffold: it fetches getWorkflowDetail(token, workflowId) for the selected deliverable and derives `acceptsTemplateDs = detail.context_providers.includes("opendesign")`. Render accordion sections; the Templates + Design System accordions are gated on acceptsTemplateDs (SC-001 — NO `pipelineType === "od_prototype"` branch anywhere). Use Phase-32 @theme tokens + ui/ primitives only. Do NOT wire the accordion bodies yet (Task 2). Write draft.test.ts (RED-first) + a ConfigureScreen.test.tsx stub asserting the declared-signal gating.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/draft.test.ts src/components/workflow/ConfigureScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/lib/draft.test.ts src/components/workflow/ConfigureScreen.test.tsx` green.
    - Declared-signal gating proven: `grep -c 'context_providers' frontend/src/components/workflow/ConfigureScreen.tsx` >= 1 and `grep -cE 'od_prototype|=== .prototype.' frontend/src/components/workflow/ConfigureScreen.tsx` == 0.
    - draft.ts has NO fetch/request/api import: `grep -cE 'fetch\(|request<|/api/' frontend/src/lib/draft.ts` == 0.
    - Retired-palette gate on both new files: `grep -cnE '#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains' <file>` == 0.
  </acceptance_criteria>
  <done>draft.ts (client-side only) + the Configure route/screen scaffold exist; Templates/DS accordions are gated on the declared signal; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the five accordions (reuse existing surfaces) + Save-draft + generic launch</name>
  <files>frontend/src/components/workflow/ConfigureScreen.tsx, frontend/src/components/workflow/ConfigureScreen.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/prototype/DiscoveryForm.tsx (Describe body)
    - frontend/src/components/workflow/prototype/TemplateGallery.tsx + DesignSystemPicker.tsx (live pickers)
    - frontend/src/components/workflow/AgentsPopup.tsx:1337 (AdvancedExpander — Review Gates + Workflow Settings levers; preserve COUPLED_GATE + retry-int contract)
    - frontend/src/lib/api.ts (createRun / launch client + LaunchCommand fields template_id/design_system_id/agent_ids/selections)
  </read_first>
  <behavior>
    - Describe accordion renders DiscoveryForm; Templates renders TemplateGallery (when acceptsTemplateDs); Design System renders DesignSystemPicker (when acceptsTemplateDs); Review Gates + Workflow Settings render AdvancedExpander levers.
    - Save-draft writes the composed ConfigureDraft via saveDraft(); on mount, ConfigureScreen hydrates from readDraft() if present.
    - Launch composes the generic LaunchCommand (template_id/design_system_id/custom bodies/agent_ids/selections/gate_agent_ids) and clears the draft once (clearDraft()).
  </behavior>
  <action>
    Wire each accordion body to its REUSED surface (import, do not rebuild): Describe -> DiscoveryForm; Templates -> TemplateGallery (onSelect sets templateId); Design System -> DesignSystemPicker (onSelect/onSelectCustom set designSystemId/customDsBody); Review Gates + Workflow Settings -> AdvancedExpander (preserve the validator->gate coupling COUPLED_GATE and retry-as-int contract — do NOT reimplement them). Hydrate state from readDraft() on mount; the Save-draft button calls saveDraft(composedPayload); the Launch action composes the generic LaunchCommand from the accordion state (the SAME fields LaunchCommand accepts) and calls clearDraft() once after handoff. NO visibility/sharing card, NO cost/duration estimate, NO discovery page-selection (ND-12/Category-D — declared OUT). Extend ConfigureScreen.test.tsx: accordion render gating, Save-draft round-trip (saveDraft called with composed payload), launch composes generic fields + clears draft.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/ConfigureScreen.test.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflow/ConfigureScreen.test.tsx` green (accordion gating + Save-draft round-trip + generic launch).
    - `npx tsc --noEmit` IDENTITY (no NEW errors vs the pre-phase baseline; filter mockApi.ts).
    - No deferred-backend surfaces: `grep -cEi 'visibility|just me|team|estimat|pages\?' frontend/src/components/workflow/ConfigureScreen.tsx` == 0.
    - Retired-palette gate 0 on ConfigureScreen.tsx; positive @theme/ui usage present.
    - Mocked Playwright e2e: LIVE-DEFERRED (offline webServer timeout).
  </acceptance_criteria>
  <done>All five accordions render reused surfaces; Save-draft persists client-side; launch composes the generic LaunchCommand and clears the draft; tsc identity; token gate 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser sessionStorage (draft) | per-tab client store; never leaves the browser |
| Configure -> launch API | template_id/design_system_id/agent_ids cross to the owner-scoped launch endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Information disclosure | draft.ts sessionStorage | accept | Client-side per-tab only, no server persistence (ND-1) — no cross-owner exposure |
| T-37-02-02 | Elevation | launch composition | accept | The generic LaunchCommand is validated + owner-scoped server-side (37-01 seam + launch_run owner checks); FE composes declared fields only |
| T-37-02-03 | Tampering | template/DS ids in draft | mitigate | Server re-validates template_id against the registry (template_not_found) at launch — the FE draft cannot smuggle an invalid template past the boundary |
| T-37-SC | Tampering | package installs | accept | No npm installs this phase — N/A |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/draft.test.ts src/components/workflow/ConfigureScreen.test.tsx` green.
- `npx tsc --noEmit` identity (filter mockApi.ts).
- Retired-palette grep 0 on ConfigureScreen.tsx + draft.ts; positive @theme/ui usage.
- SC-001: no prototype-name branch in ConfigureScreen (gating keyed on context_providers).
- Mocked Playwright e2e + live launch: LIVE-DEFERRED (Phase 34).
</verification>

<success_criteria>
- Configure is a generic per-run setup surface; Templates/DS accordions gated on the declared signal (SC-001).
- Save-draft is client-side only (ND-1); read + cleared once at launch.
- Launch composes the generic LaunchCommand — no prototype-specific path.
- Token gate 0 on all new files; tsc identity.
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/app/workflow/configure/page.tsx` — NEW: the Configure route.
- `frontend/src/components/workflow/ConfigureScreen.tsx` — NEW: generic per-run setup accordions keyed on the declared signal.
- `frontend/src/lib/draft.ts` — NEW: client-side sessionStorage draft helper (ND-1 consumer #2).
- `frontend/src/components/workflow/ConfigureScreen.test.tsx`, `frontend/src/lib/draft.test.ts` — NEW tests.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-configure-unification-composer-wizard-b3/37-02-SUMMARY.md` when done.
</output>
