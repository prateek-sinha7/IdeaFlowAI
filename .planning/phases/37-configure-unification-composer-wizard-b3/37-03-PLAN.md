---
phase: 37-configure-unification-composer-wizard-b3
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - frontend/src/components/workflow/AgentsPopup.tsx
  - frontend/src/components/workflow/AgentLibrary.tsx
  - frontend/src/components/workflow/AgentsPopup.reskin.test.tsx
autonomous: true
requirements: [SHELL-04]
must_haves:
  truths:
    - "REUSE over rebuild (D-15/ND-12): the composer reskins already-shipped P22 code — the LIVE /api/capabilities palette (NOT the mock's hardcoded 8), the AdvancedExpander levers, /api/user-workflows CRUD, SkillsHooksTab, AgentLibrary — a face-value rebuild from the mock REGRESSES."
    - "The validator->gate coupling is PRESERVED: COUPLED_GATE='validation' auto-attach stays (the mock drops it; the server selections.py enforces it — EMP-04)."
    - "Retry stays an INT from RETRY_OPTIONS=[1,2,3] (the mock models it as a bool — WIRE-02 compiles the wrong type if changed)."
    - "SC-001/INV-1: capability rendering + user_allowed 'Engineer-only' gating read the live registry (declared data), never a hardcoded per-workflow list."
    - "ND-12: workflow visibility/team-sharing is DECLARED OUT — the Save button wires to createUserWorkflow (owner-only), no sharing/visibility field is built."
    - "AgentModelPicker (per 37-CONTEXT amendment 2026-07-09): inline per-agent model selection is SATISFIED by the live-catalog Model lever (EMP-01/DECIDE-02, AgentsPopup.tsx:1563-1604); the standalone AgentModelPicker.tsx is dead code and is NOT re-mounted (INV-3 dual-impl). Explicit invariant-grounded resolution of the locked decision, not an optional downgrade."
  artifacts:
    - path: "frontend/src/components/workflow/AgentsPopup.tsx"
      provides: "Reskinned composer preserving COUPLED_GATE + retry-int + live registry"
      contains: "COUPLED_GATE"
  key_links:
    - from: "frontend/src/components/workflow/AgentsPopup.tsx (CapabilityPaletteSection)"
      to: "GET /api/capabilities"
      via: "live registry render + user_allowed gating"
      pattern: "user_allowed"
    - from: "frontend/src/components/workflow/AgentsPopup.tsx (Save)"
      to: "createUserWorkflow (/api/user-workflows)"
      via: "wire Save button to owner-scoped CRUD"
      pattern: "createUserWorkflow"
---

<objective>
Reskin the composer (AgentsPopup) onto Phase-32 tokens WITHOUT regressing any shipped P22 behavior. The dominant risk here is regression-by-face-value-rebuild: the mock hardcodes 8 capabilities (vs the live /api/capabilities registry), models retry as a bool (vs RETRY_OPTIONS=[1,2,3]), and drops the validator->gate coupling (COUPLED_GATE='validation', server-enforced). This plan REUSES the live surfaces verbatim and only reskins their chrome + wires the Save button to /api/user-workflows.

Purpose: land the composer look from the mock while keeping the product's richer, live-registry-driven, server-coupled behavior (D-15 reuse-over-rebuild).
Output: reskinned AgentsPopup (capability palette + AdvancedExpander + SkillsHooksTab) + AgentLibrary + a wired Save-to-createUserWorkflow, with a reskin test pinning the reuse contracts.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-CONTEXT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-RESEARCH.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md

# The composer surfaces (REUSE — do NOT rebuild from the mock)
@frontend/src/components/workflow/AgentsPopup.tsx    # CapabilityPaletteSection:1023, AdvancedExpander:1337, COUPLED_GATE:1318, RETRY_OPTIONS:1322, SkillsHooksTab:715, user_allowed:1160
@frontend/src/components/workflow/AgentLibrary.tsx   # add-agent palette (already richer than the mock)
@frontend/src/lib/api.ts                             # createUserWorkflow:819, UserWorkflowSummary:780, WorkflowDetail:739
@frontend/src/components/catalog/NameWorkflowModal.tsx   # the Save name/description modal to reuse

# Phase-32 tokens + Phase-35 shell
@.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reskin capability palette + AdvancedExpander (preserve COUPLED_GATE + retry-int + live registry)</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx, frontend/src/components/workflow/AgentsPopup.reskin.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentsPopup.tsx:986-1170 (CapabilityPaletteSection — live registry render, user_allowed gate at :1160)
    - frontend/src/components/workflow/AgentsPopup.tsx:1293-1670 (AdvancedExpander — COUPLED_GATE:1318, RETRY_OPTIONS:1322, auto-attach:1432, notice:1655)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-RESEARCH.md (FE Reuse Gotchas 1-4)
  </read_first>
  <behavior>
    - Selecting a validator still auto-adds the 'validation' gate (COUPLED_GATE); removing all validators drops the auto gate (EMP-04 unchanged).
    - The retry lever still offers [1,2,3] as ints (not a bool toggle).
    - The capability palette still renders the WHOLE live /api/capabilities payload; user_allowed=false caps render as Engineer-only/locked.
    - Retired palette absent in rendered class strings after reskin.
  </behavior>
  <action>
    Reskin CapabilityPaletteSection + AdvancedExpander chrome onto Phase-32 @theme tokens + ui/ primitives, migrating the retired palette (currently ~16 #1B2A4A hits in this file) to 0. PRESERVE verbatim: the COUPLED_GATE='validation' constant + its auto-attach logic (:1432) + the "Added required validation gate" notice (:1655); RETRY_OPTIONS=[1,2,3] and sel.retry as an int; the live-registry render (do NOT introduce any hardcoded capability list — SC-001); the user_allowed=false Engineer-only lock. This is a LOOK change only — no behavioral/data edits. Add AgentsPopup.reskin.test.tsx (RED-first on the retired-palette + a token assertion; GREEN on the preserved-contract assertions): validator selection auto-adds COUPLED_GATE, retry options are ints [1,2,3], user_allowed=false renders locked, no retired palette in class strings.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/AgentsPopup.reskin.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflow/AgentsPopup.reskin.test.tsx` green (COUPLED_GATE auto-attach + retry-int + user_allowed lock + no-retired-palette).
    - Coupling preserved: `grep -c 'COUPLED_GATE' frontend/src/components/workflow/AgentsPopup.tsx` unchanged from baseline (>= the pre-reskin count).
    - Retry-int preserved: `grep -c 'RETRY_OPTIONS = \[1, 2, 3\]' frontend/src/components/workflow/AgentsPopup.tsx` == 1.
    - No hardcoded capability list introduced (SC-001): no new `CAPDEF`/8-item literal array — `grep -ci 'CAPDEF' frontend/src/components/workflow/AgentsPopup.tsx` == 0.
    - Retired-palette gate: `grep -cnE '#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains' frontend/src/components/workflow/AgentsPopup.tsx` == 0.
  </acceptance_criteria>
  <done>Capability palette + AdvancedExpander reskinned to 0 retired palette; COUPLED_GATE, retry-int, live registry, and user_allowed gating all preserved and test-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Save -> createUserWorkflow + reskin SkillsHooksTab + AgentLibrary (no visibility/sharing)</name>
  <files>frontend/src/components/workflow/AgentsPopup.tsx, frontend/src/components/workflow/AgentLibrary.tsx, frontend/src/components/workflow/AgentsPopup.reskin.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/AgentsPopup.tsx:715 (SkillsHooksTab — keep suggested-per-agent compatible_agents)
    - frontend/src/components/workflow/AgentLibrary.tsx (add-agent palette)
    - frontend/src/lib/api.ts:819 (createUserWorkflow) + frontend/src/components/catalog/NameWorkflowModal.tsx
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md (Wave 3 composer section — user_workflows CRUD; DECLARED OUT visibility)
  </read_first>
  <behavior>
    - The Save button opens NameWorkflowModal and, on confirm, calls createUserWorkflow with {name, description?, base_pipeline_type, agent_ids, model_overrides?, selections?}.
    - SkillsHooksTab still surfaces suggested-per-agent skills/hooks (compatible_agents), not global-only toggles.
    - AgentLibrary renders search + categories (its existing richer data), reskinned to tokens.
    - No visibility/'Just me / Team' control is present.
  </behavior>
  <action>
    Wire the composer Save button to createUserWorkflow via the existing NameWorkflowModal (reuse both — do NOT build new persistence). Do NOT add any visibility/team-sharing field or card (ND-12/Category-D — owner-only CRUD is the whole surface). Reskin SkillsHooksTab + AgentLibrary chrome onto Phase-32 tokens, preserving the suggested-per-agent (compatible_agents) model and the search/category richness. Extend AgentsPopup.reskin.test.tsx: Save-confirm calls createUserWorkflow with the composed triple; SkillsHooksTab keeps per-agent suggestions; no visibility control rendered. AgentModelPicker resolution (PER 37-CONTEXT amendment 2026-07-09 — a first-class decision, NOT optional): inline per-agent model selection is SATISFIED by the already-relocated live-catalog Model lever (EMP-01/DECIDE-02, AgentsPopup.tsx:1563-1604; whole catalog, threads model_overrides via updateLever->onModelOverridesChange). The standalone AgentModelPicker.tsx is confirmed dead code (imported only by its own test); re-mounting it is REJECTED as an INV-3 dual-implementation. This plan REUSES the inline Model lever and does NOT mount the standalone. Record this drift finding in the 37-03-SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/AgentsPopup.reskin.test.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflow/AgentsPopup.reskin.test.tsx` green (Save->createUserWorkflow + per-agent suggestions + no visibility control).
    - Save wired: `grep -c 'createUserWorkflow' frontend/src/components/workflow/AgentsPopup.tsx` >= 1.
    - No visibility/sharing built: `grep -cEi 'visibility|just me|team-shar|shared with' frontend/src/components/workflow/AgentsPopup.tsx` == 0.
    - AgentModelPicker NOT re-mounted: `grep -c "AgentModelPicker" frontend/src/components/workflow/AgentsPopup.tsx` == 0.
    - `npx tsc --noEmit` IDENTITY (filter mockApi.ts); retired-palette 0 on AgentLibrary.tsx.
    - Mocked Playwright e2e: LIVE-DEFERRED.
  </acceptance_criteria>
  <done>Save wires to owner-scoped createUserWorkflow; SkillsHooksTab + AgentLibrary reskinned with per-agent suggestions intact; no visibility/sharing; tsc identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer -> /api/user-workflows | save payload {base_pipeline_type, agent_ids, model_overrides, selections} crosses to owner-scoped CRUD |
| composer -> /api/capabilities | read-only registry with security_gated/user_allowed flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Elevation | Save to another owner's workflow | accept | createUserWorkflow is owner-scoped (user_workflows.py:317-319); server re-validates model_overrides + selections at save — no client bypass |
| T-37-03-02 | Elevation | capability escalation (exec/network/secrets) | mitigate | security_gated/user_allowed reflected read-only; user_allowed=false renders locked; defaults OFF unchanged |
| T-37-03-03 | Tampering | dropping the validator->gate coupling | mitigate | COUPLED_GATE auto-attach preserved + test-pinned; server selections.py is the backstop so a UI desync cannot skip the gate |
| T-37-SC | Tampering | package installs | accept | No npm installs this phase — N/A |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflow/AgentsPopup.reskin.test.tsx` green.
- `npx tsc --noEmit` identity (filter mockApi.ts).
- Retired-palette grep 0 on AgentsPopup.tsx + AgentLibrary.tsx; positive @theme/ui usage.
- COUPLED_GATE + retry-int + live registry + user_allowed gating preserved (grep + test).
- Mocked Playwright e2e: LIVE-DEFERRED (Phase 34).
</verification>

<success_criteria>
- Composer reskinned to 0 retired palette with every P22 reuse contract intact (COUPLED_GATE, retry-int, live /api/capabilities, user_allowed gating).
- Save wires to owner-scoped createUserWorkflow; no visibility/sharing built (ND-12).
- No hardcoded capability list; no AgentModelPicker re-mount (INV-3).
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/components/workflow/AgentsPopup.tsx` — MODIFIED: reskinned capability palette + AdvancedExpander + SkillsHooksTab; Save wired to createUserWorkflow; contracts preserved.
- `frontend/src/components/workflow/AgentLibrary.tsx` — MODIFIED: token reskin, per-agent suggestions preserved.
- `frontend/src/components/workflow/AgentsPopup.reskin.test.tsx` — NEW: pins COUPLED_GATE, retry-int, user_allowed lock, Save->createUserWorkflow, no-visibility, no-retired-palette.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-configure-unification-composer-wizard-b3/37-03-SUMMARY.md` when done.
</output>
