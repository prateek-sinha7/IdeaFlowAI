---
phase: 37-configure-unification-composer-wizard-b3
plan: 05
type: execute
wave: 3
depends_on: ["37-01", "37-04"]
files_modified:
  - frontend/src/components/workflow/prototype/DesignSystemPicker.tsx
  - frontend/src/app/workflow/prototype/discovery/page.tsx
  - frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx
autonomous: true
requirements: [SHELL-04]
must_haves:
  truths:
    - "LOCK-F/ND-8: the Design System picker shows the REAL ~14 systems from live GET /api/prototype/design-systems — no catalog expansion, no hardcoded 5 (the mock's count)."
    - "RESTRUCTURE (real design change): the DS chip-list becomes a swatch band-card grid; the live registry + detail-modal preview + onSelect/onSelectCustom wiring are preserved (D-15 reuse-not-rebuild)."
    - "The orphaned DiscoveryForm route (discovery/page.tsx already built, reads prototype.draft) is WIRED as step 3 of the WizardStepper — the missing piece was navigation, not the form (INV-3: do not duplicate the discovery UI)."
    - "ND-12: discovery page-selection ('Which pages?') is DECLARED OUT — DiscoveryAnswers carries no pages field; do NOT add one."
  artifacts:
    - path: "frontend/src/components/workflow/prototype/DesignSystemPicker.tsx"
      provides: "Band-card swatch grid over the real ~14 DS registry"
      contains: "/api/prototype/design-systems"
  key_links:
    - from: "frontend/src/app/workflow/prototype/discovery/page.tsx"
      to: "WizardStepper discoverySlot"
      via: "the stepper supplies the missing navigation into the built discovery route"
      pattern: "DiscoveryForm|WizardStepper"
---

<objective>
Two coupled deliverables that complete the wizard: (1) RESTRUCTURE the Design System picker from its rounded-pill chip list into a swatch band-card grid (a genuine design change, LOCK-F/ND-8) over the REAL ~14 systems from the live registry — preserving the live fetch + detail-modal preview + selection wiring; (2) WIRE the orphaned DiscoveryForm as step 3 of the WizardStepper. The discovery route already exists and renders DiscoveryForm (reading prototype.draft) — the only missing piece is the navigation INTO it, which the stepper (37-04) now supplies. This is nav-wiring + restructure, NOT building an orphan from scratch.

Purpose: finish the unified wizard with a live-registry DS band-card grid and a wired discovery step, without duplicating the built discovery UI (INV-3).
Output: restructured DesignSystemPicker (band-cards) + the discovery route reskinned + wired into the stepper's Discovery slot.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-CONTEXT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-RESEARCH.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md

# The DS picker to restructure + the built (orphaned) discovery route + the stepper slot
@frontend/src/components/workflow/prototype/DesignSystemPicker.tsx        # chip list; onSelect:96/onSelectCustom; live GET /api/prototype/design-systems
@frontend/src/components/workflow/prototype/DesignSystemDetailModal.tsx   # swatch preview (reuse)
@frontend/src/app/workflow/prototype/discovery/page.tsx                   # BUILT: DiscoveryForm + reads prototype.draft (DRAFT_KEY:27); retired palette:4
@frontend/src/components/workflow/prototype/DiscoveryForm.tsx             # DiscoveryAnswers:20-29 (surface/audience/tone/scale — NO pages)
@frontend/src/components/workflow/WizardStepper.tsx                       # the dsSlot + discoverySlot contract (from 37-04)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure DesignSystemPicker chip-list -> swatch band-card grid (real ~14, live registry)</name>
  <files>frontend/src/components/workflow/prototype/DesignSystemPicker.tsx, frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/prototype/DesignSystemPicker.tsx (chip list ~:256; onSelect:96/:109/:115; onSelectCustom; live GET /api/prototype/design-systems)
    - frontend/src/components/workflow/prototype/DesignSystemDetailModal.tsx (the swatch preview to reuse)
    - frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx (existing reskin test to extend)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md (Wave 4 DS flow — RESTRUCTURE to band-cards)
  </read_first>
  <behavior>
    - The picker renders one band-card per live design system (real ~14 from GET /api/prototype/design-systems), each showing its colour swatches — not a rounded-pill chip.
    - Selecting a card calls onSelect(id) / clears to onSelect(null) exactly as the chip list did; custom DS still routes onSelectCustom.
    - The DS count is the live registry length (~14), never a hardcoded 5.
    - Opening a card's detail still mounts DesignSystemDetailModal with the live preview.
  </behavior>
  <action>
    Restructure the DesignSystemPicker body from the rounded-pill chip list (rounded-[var(--radius-pill)] at ~:297/:410) into a swatch band-card grid on Phase-32 tokens + ui/ primitives. This is a real design change (RESTRUCTURE) — surface each system's swatches on the card (source the swatch data the same way DesignSystemDetailModal does; reuse it for the card preview). PRESERVE the live GET /api/prototype/design-systems fetch, the onSelect/onSelectCustom contract (unchanged signatures), the selected-state toggle (onSelect(null) on re-click), and the detail-modal mount. Do NOT hardcode any DS count (LOCK-F/ND-8 — the real ~14). Extend DesignSystemPicker.reskin.test.tsx: renders one card per registry entry, card select fires onSelect(id), re-click clears, no retired palette, no hardcoded count.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx` green (band-card render + select/clear + live count).
    - Live registry preserved: `grep -c '/api/prototype/design-systems' frontend/src/components/workflow/prototype/DesignSystemPicker.tsx` >= 1; no hardcoded 5-item DS array.
    - onSelect contract preserved: `grep -c 'onSelect' frontend/src/components/workflow/prototype/DesignSystemPicker.tsx` >= baseline.
    - Retired-palette gate 0: `grep -cnE '#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains' frontend/src/components/workflow/prototype/DesignSystemPicker.tsx` == 0.
  </acceptance_criteria>
  <done>DS picker is a live-registry swatch band-card grid (real ~14); onSelect/onSelectCustom/detail-modal preserved; token gate 0; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the orphaned DiscoveryForm as stepper step 3 + reskin the discovery route</name>
  <files>frontend/src/app/workflow/prototype/discovery/page.tsx, frontend/src/components/workflow/prototype/DiscoveryForm.tsx</files>
  <read_first>
    - frontend/src/app/workflow/prototype/discovery/page.tsx (BUILT: DiscoveryForm + reads prototype.draft DRAFT_KEY:27; Skip support; retired palette:4)
    - frontend/src/components/workflow/prototype/DiscoveryForm.tsx (DiscoveryAnswers:20-29 — surface/audience/tone/scale; NO pages)
    - frontend/src/components/workflow/WizardStepper.tsx (discoverySlot contract from 37-04)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md (Wave 4 — Wire orphaned DiscoveryForm; audience/tone enum reconcile)
  </read_first>
  <action>
    Wire the WizardStepper's discoverySlot (37-04) to the EXISTING discovery route/DiscoveryForm — the stepper supplies the navigation that was missing; do NOT rebuild the discovery UI (INV-3 dual-impl). Reconcile the audience/tone enums per POR where the mock and current sets differ (keep surface + scale; discretion on presentation). Do NOT add a `pages` / page-selection field (ND-12/Category-D — DiscoveryAnswers has none, keep it that way). Reskin discovery/page.tsx onto Phase-32 tokens (migrate its 4 retired-palette hits to 0), preserving the prototype.draft read and Skip support. Extend/adjust the discovery test if present; otherwise assert the stepper mounts DiscoveryForm in step 3 via WizardStepper.test coverage.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/WizardStepper.test.tsx && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - Discovery wired (not duplicated): `grep -c 'DiscoveryForm' frontend/src/app/workflow/prototype/discovery/page.tsx` >= 1 and no second DiscoveryForm implementation created.
    - No page-selection added (ND-12): `grep -ci 'pages' frontend/src/components/workflow/prototype/DiscoveryForm.tsx` == 0.
    - Retired-palette gate 0 on discovery/page.tsx: `grep -cnE '#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains' frontend/src/app/workflow/prototype/discovery/page.tsx` == 0; positive @theme/ui usage.
    - `npx tsc --noEmit` IDENTITY (filter mockApi.ts); WizardStepper.test green (Discovery slot mounts the form).
    - Mocked Playwright e2e: LIVE-DEFERRED.
  </acceptance_criteria>
  <done>The stepper's step 3 mounts the existing DiscoveryForm (nav wired, no duplicate); discovery route reskinned to 0 retired palette; no pages field; tsc identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard -> /api/prototype/design-systems | read-only DS registry |
| discovery form -> prototype.draft | client sessionStorage read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Tampering | design_system_id from the card | mitigate | Server validates design_system_id against the registry at launch; od_loader containment unchanged |
| T-37-05-02 | Information disclosure | discovery answers in prototype.draft | accept | Client sessionStorage only (per-tab), no server persistence (ND-1) |
| T-37-SC | Tampering | package installs | accept | No npm installs this phase — N/A |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx src/components/workflow/WizardStepper.test.tsx` green.
- Retired-palette grep 0 on DesignSystemPicker.tsx + discovery/page.tsx; positive @theme/ui usage.
- Live DS registry preserved (~14, no hardcoded 5); no pages field added.
- `npx tsc --noEmit` identity (filter mockApi.ts).
- Mocked Playwright e2e: LIVE-DEFERRED (Phase 34).
</verification>

<success_criteria>
- DS picker is a live-registry swatch band-card grid (real ~14, LOCK-F/ND-8) with the selection + detail-modal wiring preserved.
- The stepper's step 3 mounts the existing DiscoveryForm (nav wired, INV-3 no duplicate); no page-selection (ND-12).
- Both touched files at 0 retired palette; tsc identity.
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/components/workflow/prototype/DesignSystemPicker.tsx` — MODIFIED: chip-list -> swatch band-card grid over the real ~14 registry; wiring preserved.
- `frontend/src/app/workflow/prototype/discovery/page.tsx` — MODIFIED: reskin + wired as the stepper's Discovery step (nav supplied).
- `frontend/src/components/workflow/prototype/DesignSystemPicker.reskin.test.tsx` — MODIFIED: band-card + live-count coverage.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-configure-unification-composer-wizard-b3/37-05-SUMMARY.md` when done.
</output>
