---
phase: 37-configure-unification-composer-wizard-b3
plan: 04
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - frontend/src/components/workflow/WizardStepper.tsx
  - frontend/src/components/workflow/WizardStepper.test.tsx
  - frontend/src/app/workflow/prototype/templates/page.tsx
  - frontend/src/app/workflow/ppt/templates/page.tsx
autonomous: true
requirements: [SHELL-04]
must_haves:
  truths:
    - "NEW-BUILD: the unified Template->DS->Discovery stepper with a Web/Deck toggle is the one genuine new component (today the flow is route-split prototype/templates vs ppt/templates with no stepper)."
    - "REUSE step bodies unchanged: the Web toggle swaps in TemplateGallery + live GET /api/prototype/templates (~43), the Deck toggle swaps in PPTTemplateGallery — the stepper builds only the chrome + toggle, never a hardcoded 12-template grid."
    - "Token gate: templates/page.tsx (6 retired hits) + ppt/templates/page.tsx (6 retired hits) reskinned to 0 retired palette with positive @theme/ui usage; live registry + custom-upload + blank-canvas wiring preserved (D-15 reuse-not-rebuild)."
    - "SC-001/INV-1: the stepper is keyed on a generic deliverable/toggle prop, never a prototype-name branch; it exposes a step-3 Discovery slot that 37-05 wires."
  artifacts:
    - path: "frontend/src/components/workflow/WizardStepper.tsx"
      provides: "Unified Template->DS->Discovery stepper chrome + Web/Deck toggle"
      contains: "Web"
  key_links:
    - from: "frontend/src/components/workflow/WizardStepper.tsx"
      to: "TemplateGallery / PPTTemplateGallery"
      via: "Web/Deck toggle swaps the template-step body"
      pattern: "TemplateGallery|PPTTemplateGallery"
---

<objective>
Build the ONE genuine new component of the phase: a unified Template -> Design System -> Discovery stepper with a Web/Deck toggle. Today the flow is route-split (prototype/templates vs ppt/templates) with no stepper. The stepper is CHROME ONLY — its step bodies REUSE the shipped surfaces (TemplateGallery for Web, PPTTemplateGallery for Deck, DesignSystemPicker, DiscoveryForm) against live registries. This plan builds the stepper + Web/Deck toggle + the template step, and reskins the two template pages onto tokens. The DS band-card restructure + the Discovery step wiring land in 37-05 (which depends on this stepper).

Purpose: consolidate the route-split wizard into a single stepper without regressing the live template registries (~43 web templates) or the custom-upload/blank-canvas wiring.
Output: WizardStepper.tsx (+ Web/Deck toggle + step-slot contract) + reskinned prototype/ppt template pages.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-CONTEXT.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-RESEARCH.md
@.planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md

# Step bodies (REUSE unchanged) + the pages to reskin
@frontend/src/components/workflow/prototype/TemplateGallery.tsx   # Web step (token-clean; live GET /api/prototype/templates)
@frontend/src/components/workflow/ppt/PPTTemplateGallery.tsx      # Deck step
@frontend/src/app/workflow/prototype/templates/page.tsx          # RESKIN (retired palette:6; STORAGE_KEY:25; blank-canvas KAN-87)
@frontend/src/app/workflow/ppt/templates/page.tsx                # RESKIN (retired palette:6)

# Phase-32/35 segmented-control + layout primitives
@.planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build WizardStepper chrome + Web/Deck toggle + the Template step (reuse galleries)</name>
  <files>frontend/src/components/workflow/WizardStepper.tsx, frontend/src/components/workflow/WizardStepper.test.tsx</files>
  <read_first>
    - frontend/src/components/workflow/prototype/TemplateGallery.tsx (onSelect contract; live GET /api/prototype/templates)
    - frontend/src/components/workflow/ppt/PPTTemplateGallery.tsx (Deck template body)
    - .planning/phases/35-shell-chrome-reskin-pages-b1/35-PATTERNS.md (segmented-control / Tabs primitive)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md (Wave 4 stepper section)
  </read_first>
  <behavior>
    - The stepper renders three ordered steps: Template (1), Design System (2 — slot), Discovery (3 — slot); back/next navigation moves between them.
    - A Web/Deck toggle at the Template step swaps the body: Web renders TemplateGallery, Deck renders PPTTemplateGallery; toggling does not lose the rest of the wizard state.
    - The stepper exposes typed step-slot props (dsSlot, discoverySlot) so 37-05 can inject the DS band-cards + Discovery step without editing the stepper.
    - Retired palette absent in rendered class strings.
  </behavior>
  <action>
    Create frontend/src/components/workflow/WizardStepper.tsx: the stepper CHROME (step header/progress + back/next) built on Phase-32/35 primitives (segmented-control / Tabs), plus a Web/Deck toggle that swaps the Template-step body between TemplateGallery (Web) and PPTTemplateGallery (Deck). The stepper is keyed on a generic deliverable/mode prop + a toggle value — NO `pipelineType === "od_prototype"` branch (SC-001). Reuse the gallery bodies unchanged (import, do not rebuild — do NOT hardcode 12 templates / lose live GET /api/prototype/templates). Define typed step-slot props for step 2 (Design System) and step 3 (Discovery) as render slots so 37-05 wires them. Write WizardStepper.test.tsx (RED-first on the retired-palette + toggle): Web/Deck toggle swaps the template body, back/next navigates, slots render their injected content, no retired palette.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/workflow/WizardStepper.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/workflow/WizardStepper.test.tsx` green (toggle swap + nav + slots).
    - Live registry preserved (no hardcoded grid): `grep -cEi 'TemplateGallery|PPTTemplateGallery' frontend/src/components/workflow/WizardStepper.tsx` >= 2 and no 12-item template literal.
    - SC-001: `grep -cE 'od_prototype|od_ppt' frontend/src/components/workflow/WizardStepper.tsx` == 0.
    - Retired-palette gate: `grep -cnE '#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains' frontend/src/components/workflow/WizardStepper.tsx` == 0.
  </acceptance_criteria>
  <done>WizardStepper renders the 3-step flow with a working Web/Deck toggle over the reused galleries + typed DS/Discovery slots; token gate 0; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin the two template pages onto tokens (preserve live registry + custom-upload + blank-canvas)</name>
  <files>frontend/src/app/workflow/prototype/templates/page.tsx, frontend/src/app/workflow/ppt/templates/page.tsx</files>
  <read_first>
    - frontend/src/app/workflow/prototype/templates/page.tsx (retired palette:6; STORAGE_KEY:25; blank-canvas KAN-87; chain.* keys)
    - frontend/src/app/workflow/ppt/templates/page.tsx (retired palette:6)
    - .planning/phases/37-configure-unification-composer-wizard-b3/37-PATTERNS.md (Shared Patterns — token gate; live registries never hardcode)
  </read_first>
  <action>
    Reskin frontend/src/app/workflow/prototype/templates/page.tsx and frontend/src/app/workflow/ppt/templates/page.tsx onto Phase-32 @theme tokens + ui/ primitives (migrate the 6 retired-palette hits each to 0). This is a LOOK change: PRESERVE all wiring D-15 reuse-not-rebuild — the live GET /api/prototype/templates fetch, custom-template upload/localStorage, the blank-canvas/KAN-87 optional-template path, the prototype.draft sessionStorage handoff (STORAGE_KEY:25) and the chain.* run-chaining keys. Do NOT hardcode any template count. If the stepper (Task 1) is adopted as these pages' render surface, mount it here; otherwise reskin the existing page layout in place. Do NOT alter the launch payload composition.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -v mockApi.ts | grep -c error</automated>
  </verify>
  <acceptance_criteria>
    - Retired-palette gate 0 on BOTH files: `grep -cnE '#1B2A4A|#2563eb|#f5f5f0|Inter|Fraunces|JetBrains' <file>` == 0 each; positive @theme/ui usage present.
    - Live registry preserved: `grep -c '/api/prototype/templates' frontend/src/app/workflow/prototype/templates/page.tsx` >= 1.
    - Draft/chain wiring preserved: `grep -c 'prototype.draft' frontend/src/app/workflow/prototype/templates/page.tsx` >= 1.
    - `npx tsc --noEmit` IDENTITY (filter mockApi.ts); existing prototype template suite green (`npx vitest run src/components/workflow/prototype/TemplateGallery.reskin.test.tsx`).
    - Mocked Playwright e2e: LIVE-DEFERRED.
  </acceptance_criteria>
  <done>Both template pages at 0 retired palette; live registry + custom-upload + blank-canvas + draft/chain wiring all preserved; tsc identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard -> /api/prototype/templates | read-only template registry (path-traversal contained in od_loader) |
| wizard -> launch payload | template_id/design_system_id composed into the owner-scoped launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Tampering | template_id from the gallery | mitigate | Server validates template_id against the registry at launch (template_not_found); od_loader path-traversal containment unchanged |
| T-37-04-02 | Tampering | custom-template upload body | accept | Existing 2MB guard + FileReader path preserved (reskin does not touch the upload path) |
| T-37-SC | Tampering | package installs | accept | No npm installs this phase — N/A |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/workflow/WizardStepper.test.tsx` green.
- Retired-palette grep 0 on WizardStepper.tsx + both template pages; positive @theme/ui usage.
- `npx tsc --noEmit` identity (filter mockApi.ts); TemplateGallery.reskin suite green.
- SC-001: no prototype-name branch in WizardStepper.
- Mocked Playwright e2e: LIVE-DEFERRED (Phase 34).
</verification>

<success_criteria>
- A working unified Template->DS->Discovery stepper with a Web/Deck toggle over the reused galleries + typed DS/Discovery slots for 37-05.
- Both template pages reskinned to 0 retired palette with every live-registry/upload/blank-canvas/draft wiring preserved.
- No hardcoded template grid; no prototype-name branch (SC-001).
</success_criteria>

<artifacts_this_phase_produces>
- `frontend/src/components/workflow/WizardStepper.tsx` — NEW: unified stepper chrome + Web/Deck toggle + typed DS/Discovery step slots.
- `frontend/src/components/workflow/WizardStepper.test.tsx` — NEW test.
- `frontend/src/app/workflow/prototype/templates/page.tsx`, `frontend/src/app/workflow/ppt/templates/page.tsx` — MODIFIED: token reskin, all wiring preserved.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/37-configure-unification-composer-wizard-b3/37-04-SUMMARY.md` when done (record the stepper step-slot contract for 37-05).
</output>
