---
phase: 17-test-infra-and-verification-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/agents/test_phase3_token_delta_live.py
autonomous: true
requirements: [ISS-003]
must_haves:
  truths:
    - "_drive_live compiles clarify.mode=off (via the compile_for_run wrap) instead of the dead ALWAYS_CLARIFY flag"
    - "A fault-injected _drive_live drive RETURNS (does not hang at the clarify event.wait()) within a bounded timeout"
    - "Offline collection of the live test still skips cleanly (no live Bedrock dependency for phase exit)"
    - "No production file (backend/app/**, non-test backend/agents/**) appears in the diff"
  artifacts:
    - path: "backend/tests/agents/test_phase3_token_delta_live.py"
      provides: "compile_for_run clarify-off wrap in _drive_live (+ finally restore) and a bounded fault-injection regression test"
      contains: "compile_for_run"
  key_links:
    - from: "backend/tests/agents/test_phase3_token_delta_live.py::_drive_live"
      to: "agents.execution_engine.engine.compile_for_run"
      via: "snapshot original + install wrapper that sets compiled.clarify.mode='off' + restore in finally"
      pattern: "compile_for_run"
---

<objective>
Fix ISS-003: `backend/tests/agents/test_phase3_token_delta_live.py::_drive_live` hangs forever at the clarify gate because line 173 sets `engine_mod.ALWAYS_CLARIFY = False` — a DEAD module attribute (the flag was deleted in 07-05; `grep -rn ALWAYS_CLARIFY agents/ app/` = 0 hits). Because `_drive_live` never sets `compiled.clarify.mode="off"`, `prototype`'s `clarify.mode="auto"` forces the gate and the run blocks forever at `clarify_engine.py:155 await event.wait()` (no answerer, no WS client).

Replace the dead line with the same `compile_for_run`-wrap every other harness already uses (snapshot `engine_mod.compile_for_run`, install a wrapper that calls the original then sets `compiled.clarify.mode = "off"`, and restore it in the existing `finally`). Add a bounded offline fault-injection regression test that proves the drive returns instead of hanging.

Purpose: closes ROADMAP Phase 17 Success Criterion 1 (ISS-003) — the live token-delta driver compiles clarify off instead of poking a dead flag; the hang is gone; offline collection still skips. The defect is in the TEST; the fix lives there (no production code).
Output: the corrected `_drive_live` wrap + a new bounded fault-injection regression test, both test-only.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the dead ALWAYS_CLARIFY flag in _drive_live with the compile_for_run clarify-off wrap + a bounded fault-injection regression test</name>
  <files>backend/tests/agents/test_phase3_token_delta_live.py</files>
  <read_first>
    - .planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md — the LOCKED ISS-003 decision (the dead flag, the wrap mechanism, the rejected hacks, "doesn't weaken the test").
    - .planning/ISSUES-REGISTER.md → "Deep Root-Cause Investigation" row ISS-003 (root cause / proper fix / rejected hack columns).
    - backend/tests/agents/test_phase3_token_delta_live.py — the file being edited; `_drive_live` spans ~lines 153-285; the dead `engine_mod.ALWAYS_CLARIFY = False` is at :173; the existing `finally` (~:283-285) already restores `factory_create_runner_orig`.
    - backend/tests/agents/_scripted_model.py:495-513 — the NAMED PRECEDENT to mirror: snapshot `_orig_compile_for_run = engine_mod.compile_for_run`, define `_patched_compile_for_run(pipeline_type, _orig=...)` that calls `_orig(...)`, sets `compiled.clarify.mode = "off"`, returns it, then `engine_mod.compile_for_run = _patched_compile_for_run` (restored in finally).
    - backend/tests/agents/live_harness.py:556-563 — the second NAMED PRECEDENT (`_harness_compile_for_run`), identical shape.
  </read_first>
  <action>
    In `_drive_live`, delete the dead `engine_mod.ALWAYS_CLARIFY = False` at :173 and replace it with the `compile_for_run` clarify-off wrap mirroring `_scripted_model.py:506-513` exactly: before the `try:`, snapshot the original (`_orig_compile_for_run = engine_mod.compile_for_run`); inside the `try` (alongside the existing `engine_mod.create_runner` / `factory_mod.create_runner` patches) install a wrapper that calls `_orig_compile_for_run(pipeline_type)`, sets `compiled.clarify.mode = "off"` in place (ClarifySpec is a mutable dataclass — `agents/workflows/plan.py:289-298` — so the in-place write is valid), and returns the compiled workflow; assign it to `engine_mod.compile_for_run`. In the existing `finally`, restore `engine_mod.compile_for_run = _orig_compile_for_run` alongside the `factory_create_runner_orig` restore so the patch is reverted across both `_drive_live` invocations (OFF then ON). Keep an inline comment that this disables the auto-clarify forcing exactly as the deleted ALWAYS_CLARIFY=False knob used to (per the LOCKED ISS-003 decision) — the planner still runs, only the PROCEED-to-CLARIFY_REQUIRED forcing is suppressed; this removes a HANG, not the A/B token-delta measurement (clarify emits no `input_tokens`).

    Add a new bounded OFFLINE fault-injection regression test `test_drive_live_does_not_hang_at_clarify_gate` (or similar) that proves the wrap defeats the gate without any live Bedrock: monkeypatch `engine_mod.create_runner` / `agents.factory.create_runner` to a scripted no-op runner (reuse the per-agent scripted-model pattern from `_scripted_model.py` so each agent's stream terminates immediately) so `_drive_live` runs fully offline, then wrap the call in `asyncio.wait_for(_drive_live(compaction_on=True), timeout=10)` and assert it RETURNS (no `asyncio.TimeoutError`). Mark it `@pytest.mark.asyncio`. Keep `test_token_delta_live` itself gated/skipped offline as before (it requires live Bedrock) — do NOT remove its skip guard. Everything stays inside the test module; do not touch production code.

    REJECTED hacks (do NOT do): re-introduce the deleted `ALWAYS_CLARIFY` flag in the engine (dual-impl, INV-12); add a clarify timeout in `clarify_engine.py` (perturbs production HITL — the gate is deliberately no-timeout); flip the `prototype` manifest `clarify.mode` to off (breaks INV-3 + `test_manifest_parity.py`).
  </action>
  <verify>
    <automated>cd backend && test $(grep -c "ALWAYS_CLARIFY" tests/agents/test_phase3_token_delta_live.py) -eq 0 && grep -q "compile_for_run" tests/agents/test_phase3_token_delta_live.py && python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ALWAYS_CLARIFY tests/agents/test_phase3_token_delta_live.py` == 0 (the dead flag is gone — no reintroduction).
    - `_drive_live` snapshots `engine_mod.compile_for_run`, installs a wrapper that sets `compiled.clarify.mode = "off"`, and the existing `finally` restores `engine_mod.compile_for_run` to the snapshot.
    - The new fault-injection test `test_drive_live_does_not_hang_at_clarify_gate` runs OFFLINE (scripted runners, no Bedrock) and `asyncio.wait_for(_drive_live(...), 10)` RETURNS without raising `asyncio.TimeoutError`. (Documented evidence: pre-fix this drive would raise TimeoutError at the clarify `event.wait()`; post-fix it returns.)
    - `python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py` passes offline: the new test passes and `test_token_delta_live` SKIPS (live Bedrock opt-in) — never hangs, never errors.
    - `git diff --name-only` shows ONLY `backend/tests/agents/test_phase3_token_delta_live.py` — no `backend/app/**`, no non-test `backend/agents/**`.
  </acceptance_criteria>
  <done>The dead ALWAYS_CLARIFY flag is replaced by the compile_for_run clarify-off wrap (restored in finally); a bounded offline fault-injection test proves _drive_live returns rather than hanging; offline collection skips the live test cleanly; diff is test-only.</done>
</task>

<task type="auto">
  <name>Task 2: Parity guard — assert test-only diff + 5 characterization goldens + lint-imports stay green</name>
  <files>backend/tests/agents/test_phase3_token_delta_live.py</files>
  <read_first>
    - .planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md → INVARIANTS (test/harness-only; the 5 goldens untouched by construction; still run them + lint-imports as the parity guard; `python3.11` no venv; FULL pytest hangs offline — run only the targeted suite).
    - backend/tests/agents/test_characterization_prototype.py / test_characterization_od_prototype.py / test_characterization_prototype_revision.py / test_characterization_od_ppt.py / test_characterization_app_builder.py — the 5 characterization golden suites (READ-ONLY; must stay byte-identical).
  </read_first>
  <action>
    No code changes — this task is the SC-001 / INV-3 parity guard for the ISS-003 edit. Run the 5 characterization golden suites and `lint-imports` and confirm both are unaffected by the test-only change, and confirm the working-tree diff touches test files only. This is trivially satisfied (the edit is confined to one test module and never imports into production), but the LOCKED INVARIANTS require it be asserted, not assumed. Record the evidence (goldens green, lint 4 kept / 0 broken, test-only diff) in the SUMMARY. SNAPSHOT_UPDATE must remain UNSET (do not regenerate goldens).
  </action>
  <verify>
    <automated>cd backend && git diff --name-only | grep -vE '^backend/tests/' | grep -q . && { echo "NON-TEST FILE IN DIFF"; exit 1; } ; python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -p no:cacheprovider -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` lists ONLY paths under `backend/tests/` (no `backend/app/**`, no non-test `backend/agents/**`) — the grep-for-non-test-files guard exits 0 (none found).
    - All 5 characterization golden suites pass byte-identical with SNAPSHOT_UPDATE unset (no golden regeneration).
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken.
    - SC-001 / INV-3 statement recorded in the SUMMARY: this phase is test-harness-only, so parity is satisfied by construction AND verified.
  </acceptance_criteria>
  <done>The 5 characterization goldens are byte-identical, lint-imports is 4/0, and the diff is test-only — the ISS-003 fix introduced zero production/behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches ONLY `backend/tests/agents/test_phase3_token_delta_live.py` (a test/harness file). It introduces no new runtime, no network surface, no new dependency, and no production code path. There is NO trust boundary crossed by this change — the test code runs only under the developer's offline pytest invocation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-S | Spoofing | test harness | N/A (test-only) | No identity/auth surface; test code runs locally under the developer. |
| T-17-01-T | Tampering | test harness | N/A (test-only) | No persisted state or production input is written; the compile_for_run wrap is reverted in `finally`. The one real care item: do NOT alter `clarify_engine.py` or the prototype manifest (REJECTED hacks) — those WOULD touch production HITL/INV-3. |
| T-17-01-R | Repudiation | test harness | N/A (test-only) | No audit/logging surface. |
| T-17-01-I | Info Disclosure | test harness | N/A (test-only) | No secrets read/written; the offline fault-injection test uses scripted models, no Bedrock creds. |
| T-17-01-D | Denial of Service | test harness | accept | The change REMOVES a hang (the dead-flag clarify block); the new bounded `asyncio.wait_for` guards against regression. |
| T-17-01-E | Elevation of Privilege | test harness | N/A (test-only) | No permission/tool grant surface. |
| T-17-01-SC | Tampering | npm/pip installs | N/A | No package installs in this plan; no new dependency. |

Honest surface: negligible — test-harness file only, no runtime/attack surface, no new deps. No block-worthy threats.
</threat_model>

<verification>
- `grep -c ALWAYS_CLARIFY tests/agents/test_phase3_token_delta_live.py` == 0.
- `tests/agents/test_phase3_token_delta_live.py` passes offline (new fault-injection test passes; `test_token_delta_live` skips on live opt-in; nothing hangs).
- 5 characterization goldens byte-identical; `/opt/homebrew/bin/lint-imports` 4 kept / 0 broken.
- `git diff --name-only` is test-only.
</verification>

<success_criteria>
- ISS-003 closed offline-deterministically: the clarify-off compile wrap replaces the dead flag and a bounded fault-injection test proves no hang (ROADMAP Phase 17 SC1).
- INV-3 / SC-001 parity guard green (goldens + lint-imports), diff test-only.
- DEFERRED follow-up recorded (not blocking): the live `RUN_LIVE_BEDROCK=1` token-delta re-run needs fresh AWS creds — record as a next-live-pass item in the SUMMARY.
</success_criteria>

<deferred>
- The live re-run of `test_token_delta_live` (`RUN_LIVE_BEDROCK=1`, real Bedrock token-delta A/B) requires fresh AWS creds (default profile / acct 473293451041). Record as a DEFERRED next-Bedrock-pass item — NOT a blocking task for phase exit (phase exit is offline-deterministic).
</deferred>

<artifacts_this_phase_produces>
- `_drive_live` compile_for_run clarify-off wrap (snapshot + wrapper + finally restore) replacing the dead `ALWAYS_CLARIFY` line.
- New offline fault-injection regression test `test_drive_live_does_not_hang_at_clarify_gate` (bounded `asyncio.wait_for`, scripted runners).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/17-test-infra-and-verification-gap-closure/17-01-SUMMARY.md` when done
</output>
