---
phase: 17-test-infra-and-verification-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/agents/test_hooks.py
autonomous: true
requirements: [ISS-010]
must_haves:
  truths:
    - "A deterministic offline test asserts OtelTracingHook.handle emits exactly 1 span with attributes flowin.hook/flowin.event/flowin.agent_id under scope flowin.agents.hooks.otel_tracing, captured via an injected InMemorySpanExporter"
    - "The injection uses the hook's OWN factory: monkeypatch otel._build_span_processor to a SimpleSpanProcessor(InMemorySpanExporter()) AND reset otel._TRACER=None (a global set_tracer_provider test would MISS the hook's module-private _TRACER → false green)"
    - "A graceful-degrade case: OTEL_EXPORTER_OTLP_ENDPOINT set + OTLP exporter pkg absent → _build_span_processor() returns the console processor"
    - "The existing 8 otel tests stay green; ISS-010 flips DEFERRED → FIXED-by-test (no live collector)"
    - "No production file appears in the diff"
  artifacts:
    - path: "backend/tests/agents/test_hooks.py"
      provides: "in-memory-exporter span-capture test + OTLP-degrade test extending the otel cluster"
      contains: "InMemorySpanExporter"
  key_links:
    - from: "backend/tests/agents/test_hooks.py (new otel test)"
      to: "agents.capabilities.hooks.otel_tracing._build_span_processor / _TRACER"
      via: "monkeypatch _build_span_processor → SimpleSpanProcessor(InMemorySpanExporter()) + reset _TRACER=None, then drive OtelTracingHook.handle"
      pattern: "InMemorySpanExporter"
---

<objective>
Close ISS-010 (verification gap, NOT a code gap) with a deterministic OFFLINE in-memory-span-exporter test. "Declaration-driven hooks → legacy pipelines emit no spans" is true and proven (all 18 manifests declare no `hooks:`). Given a collector + a hook-declaring step, spans DO export — prove it by running the real `OtelTracingHook.handle` with an injected `InMemorySpanExporter` (1 span, correct attrs `flowin.hook`/`flowin.event`/`flowin.agent_id`, scope `flowin.agents.hooks.otel_tracing`). Add a parallel graceful-degrade case: with `OTEL_EXPORTER_OTLP_ENDPOINT` set but the OTLP exporter pkg absent, `_build_span_processor()` returns the console processor.

THE #1 LANDMINE (LOCKED): the hook caches a module-private `_TRACER` and does NOT call `trace.set_tracer_provider()`, so a global-provider test won't capture its spans. Inject via the module's OWN factory: `monkeypatch.setattr(otel, "_build_span_processor", lambda: SimpleSpanProcessor(InMemorySpanExporter()))` (hold the exporter ref) and `monkeypatch.setattr(otel, "_TRACER", None)`. Use `SimpleSpanProcessor` (synchronous — no `force_flush`).

Purpose: closes ROADMAP Phase 17 Success Criterion 2 (ISS-010) — OTLP span export is verified deterministically by an in-memory exporter, not by standing up a live collector. ISS-010 flips DEFERRED → FIXED-by-test.
Output: two new tests in the `test_hooks.py` otel cluster (span-capture + OTLP-degrade), test-only.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an in-memory-exporter span-capture test (via the hook's own _build_span_processor factory) + an OTLP graceful-degrade test to the test_hooks.py otel cluster</name>
  <files>backend/tests/agents/test_hooks.py</files>
  <read_first>
    - .planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md — the LOCKED ISS-010 decision (the disposition, the injection seam, the #1 landmine, the rejected hacks, the DEFERRED→FIXED-by-test closure).
    - .planning/ISSUES-REGISTER.md → "Deep Root-Cause Investigation" row ISS-010.
    - backend/agents/capabilities/hooks/otel_tracing.py — READ-ONLY (the fix is in the test): `handle` (:138-172) sets `flowin.hook`/`flowin.event`/`flowin.agent_id` and opens span `hook.{event_name}`; `_get_tracer` (:104-120) caches the module-private `_TRACER` (no `set_tracer_provider`) and builds the provider from `_build_span_processor()`; `_build_span_processor` (:77-101) returns `SimpleSpanProcessor(ConsoleSpanExporter())` by default, or a BatchSpanProcessor(OTLP) when `OTEL_EXPORTER_OTLP_ENDPOINT` is set (degrading to console if the OTLP pkg import fails); `_TRACER_NAME` = `flowin.agents.hooks.otel_tracing`.
    - backend/tests/agents/test_hooks.py:503-635 — the existing otel cluster (8 tests) + the `_RecordingRunner` / `_Ctx` fakes (:64-86): `_Ctx(runner)` exposes `.runner`, `_RecordingRunner.record_hook_run(...)` appends to `.hook_runs`. The existing `test_otel_tracing_continues_and_writes_row_on_fire` (:534-554) asserts `detail["span"] is True` but does NOT capture the actual span — that is exactly the gap ISS-010 closes. The existing `test_otel_tracing_degrades_to_noop_span_when_otel_unavailable` (:557-585) covers the api/sdk-absent path (`_OTEL_AVAILABLE=False`) — DISTINCT from ISS-010's OTLP-exporter-absent path.
  </read_first>
  <action>
    Extend the `test_hooks.py` otel cluster with two new tests (do NOT modify the 8 existing otel tests):

    (1) `test_otel_tracing_exports_one_span_with_attrs_via_in_memory_exporter` — import `agents.capabilities.hooks.otel_tracing as otel`; import `InMemorySpanExporter` from `opentelemetry.sdk.trace.export.in_memory_span_exporter` and `SimpleSpanProcessor` from `opentelemetry.sdk.trace.export`. Create an `exporter = InMemorySpanExporter()` and HOLD the ref. `monkeypatch.setattr(otel, "_build_span_processor", lambda: SimpleSpanProcessor(exporter))` and `monkeypatch.setattr(otel, "_TRACER", None)` so `_get_tracer()` rebuilds the provider/tracer through the patched factory (this is mandatory — the hook does NOT use the global provider, so a `set_tracer_provider` approach would silently miss its `_TRACER` → false green). Drive the real hook: `hook = otel.OtelTracingHook()`, `await hook.handle({"event": "before_step", "agent_id": "build"}, _Ctx(_RecordingRunner()))` under `@pytest.mark.asyncio`. SimpleSpanProcessor is synchronous so no `force_flush` is needed; read `spans = exporter.get_finished_spans()` and assert: exactly 1 span; `spans[0].name == "hook.before_step"`; `spans[0].attributes["flowin.hook"] == "otel_tracing"`; `spans[0].attributes["flowin.event"] == "before_step"`; `spans[0].attributes["flowin.agent_id"] == "build"`; and the span's instrumentation scope name == `otel._TRACER_NAME` (`flowin.agents.hooks.otel_tracing`). Also assert the hook still returned `HOOK_CONTINUE` and wrote one `hook_runs` row with `detail["span"] is True` (the audit half stays intact). After the test, leave `_TRACER` reset (the monkeypatch undoes the factory; explicitly null `_TRACER` again if needed) so no patched tracer leaks into the other otel tests.

    (2) `test_otel_build_span_processor_degrades_to_console_when_otlp_pkg_absent` — pin the env-degradation contract: `monkeypatch.setenv("OTEL_EXPORTER_OTLP_ENDPOINT", "http://localhost:4317")` and force the lazy OTLP-exporter import to fail (e.g. `monkeypatch.setitem(sys.modules, "opentelemetry.exporter.otlp.proto.grpc.trace_exporter", None)` so the `from ... import OTLPSpanExporter` raises and the `except Exception` branch degrades). Call `proc = otel._build_span_processor()` and assert it is a `SimpleSpanProcessor` over a `ConsoleSpanExporter` (the console-degrade path at otel_tracing.py:101) — i.e. NOT a BatchSpanProcessor — proving graceful degradation when the optional OTLP package is absent but an endpoint is configured. Restore env/modules via the monkeypatch fixture (automatic).

    REJECTED hacks (do NOT do): stand up a live OTLP collector or `pip install opentelemetry-exporter-otlp` just to go green (env-dependent, non-deterministic, adds a deliberately-optional dep); a global `set_tracer_provider(InMemory…)` test (silently MISSES the hook's own `_TRACER` → false green). After this lands, flip ISS-010 DEFERRED → FIXED-by-test in the SUMMARY note (live OTLP export remains an optional end-of-milestone smoke, not a blocker).
  </action>
  <verify>
    <automated>cd backend && grep -q "InMemorySpanExporter" tests/agents/test_hooks.py && grep -q "_build_span_processor" tests/agents/test_hooks.py && python3.11 -m pytest tests/agents/test_hooks.py -k otel -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - The span-capture test injects via `otel._build_span_processor` (monkeypatched to `SimpleSpanProcessor(InMemorySpanExporter())`) AND resets `otel._TRACER=None` — NOT via a global `set_tracer_provider` (the false-green landmine).
    - After driving `OtelTracingHook.handle({"event":"before_step","agent_id":"build"}, _Ctx(_RecordingRunner()))`, `exporter.get_finished_spans()` returns exactly 1 span named `hook.before_step` with attributes `flowin.hook == "otel_tracing"`, `flowin.event == "before_step"`, `flowin.agent_id == "build"`, instrumentation scope `flowin.agents.hooks.otel_tracing`; outcome is `HOOK_CONTINUE`; one `hook_runs` row with `detail["span"] is True`.
    - The degrade test asserts `_build_span_processor()` with `OTEL_EXPORTER_OTLP_ENDPOINT` set and the OTLP exporter import forced to fail returns a `SimpleSpanProcessor` over a `ConsoleSpanExporter` (console-degrade), not a Batch/OTLP processor.
    - `python3.11 -m pytest tests/agents/test_hooks.py -k otel` passes: the 8 existing otel tests stay green AND the 2 new tests pass (10 total). No span/tracer leak across tests.
    - `git diff --name-only` shows ONLY `backend/tests/agents/test_hooks.py`.
  </acceptance_criteria>
  <done>The otel cluster proves the hook exports exactly 1 span with the right attrs through an injected in-memory exporter (via its own factory, _TRACER reset) and that _build_span_processor degrades to console when the OTLP pkg is absent; existing otel tests stay green; ISS-010 closed by test; diff test-only.</done>
</task>

<task type="auto">
  <name>Task 2: Parity guard — assert test-only diff + 5 characterization goldens + lint-imports stay green</name>
  <files>backend/tests/agents/test_hooks.py</files>
  <read_first>
    - .planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md → INVARIANTS (test/harness-only; 5 goldens untouched by construction; still run goldens + lint-imports as the parity guard; `python3.11` no venv; FULL pytest hangs offline — run only the targeted suite).
    - backend/tests/agents/test_characterization_prototype.py / test_characterization_od_prototype.py / test_characterization_prototype_revision.py / test_characterization_od_ppt.py / test_characterization_app_builder.py — READ-ONLY; must stay byte-identical.
  </read_first>
  <action>
    No code changes — the SC-001 / INV-3 parity guard for the ISS-010 edit. Run the 5 characterization golden suites and `lint-imports`; confirm both are unaffected by the test-only change and that the working-tree diff touches test files only. The otel hook reads no characterization input (all 18 manifests declare no `hooks:`), so the goldens are unaffected by construction — but the LOCKED INVARIANTS require it be asserted. Record evidence (goldens green, lint 4/0, test-only diff) in the SUMMARY; SNAPSHOT_UPDATE stays UNSET.
  </action>
  <verify>
    <automated>cd backend && git diff --name-only | grep -vE '^backend/tests/' | grep -q . && { echo "NON-TEST FILE IN DIFF"; exit 1; } ; python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -p no:cacheprovider -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` lists ONLY paths under `backend/tests/` — the non-test-file guard exits 0 (none found).
    - All 5 characterization golden suites pass byte-identical with SNAPSHOT_UPDATE unset (no regeneration).
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken.
    - SC-001 / INV-3 statement recorded in the SUMMARY (test-harness-only; parity satisfied by construction AND verified).
  </acceptance_criteria>
  <done>The 5 characterization goldens are byte-identical, lint-imports is 4/0, and the diff is test-only — the ISS-010 in-memory-exporter test introduced zero production/behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches ONLY `backend/tests/agents/test_hooks.py` (a test file). It introduces no new runtime, no network surface, no new dependency (it uses the in-memory exporter from the already-installed `opentelemetry-sdk`), and no production code path. There is NO trust boundary crossed — the test runs only under the developer's offline pytest invocation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-S | Spoofing | test harness | N/A (test-only) | No identity/auth surface. |
| T-17-02-T | Tampering | test harness | N/A (test-only) | The otel module is monkeypatched (`_build_span_processor`, `_TRACER`) only within the test scope and reverted by the `monkeypatch` fixture; the real `otel_tracing.py` is READ-ONLY (not edited). |
| T-17-02-R | Repudiation | test harness | N/A (test-only) | No audit surface beyond the in-memory exporter the test owns. |
| T-17-02-I | Info Disclosure | test harness | N/A (test-only) | Spans carry only test-supplied attrs (hook/event/agent_id); no secrets; the OTLP-degrade test forces an import failure rather than connecting to any endpoint. |
| T-17-02-D | Denial of Service | test harness | N/A (test-only) | SimpleSpanProcessor is synchronous and in-memory; no external I/O. |
| T-17-02-E | Elevation of Privilege | test harness | N/A (test-only) | No permission/tool grant surface. |
| T-17-02-SC | Tampering | npm/pip installs | N/A | No package installs in this plan — the in-memory exporter ships with the already-present opentelemetry-sdk; do NOT add `opentelemetry-exporter-otlp` (a REJECTED hack). |

Honest surface: negligible — test file only, no runtime/attack surface, no new deps. No block-worthy threats.
</threat_model>

<verification>
- `tests/agents/test_hooks.py -k otel` passes: 8 existing + 2 new (span-capture + OTLP-degrade) = 10 green.
- The span-capture test injects via the hook's own `_build_span_processor` + `_TRACER` reset (not a global provider) and asserts exactly 1 span with the right attrs/scope.
- 5 characterization goldens byte-identical; `/opt/homebrew/bin/lint-imports` 4 kept / 0 broken.
- `git diff --name-only` is test-only.
</verification>

<success_criteria>
- ISS-010 closed offline-deterministically: the in-memory exporter proves 1 span + correct attrs; the OTLP-degrade case pins the console fallback (ROADMAP Phase 17 SC2). ISS-010 flips DEFERRED → FIXED-by-test.
- INV-3 / SC-001 parity guard green, diff test-only.
- DEFERRED follow-up recorded (not blocking): a live OTLP collector export remains an optional end-of-milestone smoke.
</success_criteria>

<deferred>
- Live OTLP collector export (real `OTEL_EXPORTER_OTLP_ENDPOINT` + a hook-declaring workflow) remains an OPTIONAL end-of-milestone smoke — NOT a blocker. Record in the SUMMARY; ISS-010 is closed by the in-memory-exporter test.
</deferred>

<artifacts_this_phase_produces>
- `test_otel_tracing_exports_one_span_with_attrs_via_in_memory_exporter` — drives the real `OtelTracingHook.handle` through an injected `InMemorySpanExporter` (via `_build_span_processor` monkeypatch + `_TRACER` reset) and asserts 1 span + attrs + scope.
- `test_otel_build_span_processor_degrades_to_console_when_otlp_pkg_absent` — pins the OTLP-exporter-absent → console-processor degrade contract.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/17-test-infra-and-verification-gap-closure/17-02-SUMMARY.md` when done
</output>
