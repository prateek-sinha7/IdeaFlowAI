---
phase: 17-test-infra-and-verification-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/agents/test_phase8_live.py
autonomous: true
requirements: [ISS-011]
must_haves:
  truths:
    - "The 3 TestLiveHITL cases SKIP (not FAIL) on mid-sweep SSO/credential expiry via a per-test live_skip_reason() re-check (autouse fixture or per-test guard)"
    - "test_live_gate_is_consistent_and_self_skipping no longer asserts strict _LIVE_SKIP == live_skip_reason() equality — it tolerates the legitimate collection-valid → runtime-expired drift"
    - "An expired-creds simulation (monkeypatched live_skip_reason returning a reason) makes the HITL re-check SKIP, proven offline"
    - "TestOfflineHITL stays green offline (the gate seams are sound with no creds)"
    - "The transient-error classification is NOT widened — expired-SSO stays non-transient (changing it would perturb production error semantics, INV-3-relevant)"
    - "No production file appears in the diff"
  artifacts:
    - path: "backend/tests/agents/test_phase8_live.py"
      provides: "per-test live_skip_reason() re-check that SKIPs on mid-sweep expiry + relaxed self-check + an expired-creds skip simulation test"
      contains: "live_skip_reason"
  key_links:
    - from: "backend/tests/agents/test_phase8_live.py::TestLiveHITL (per-test re-check)"
      to: "tests.agents.live_harness.live_skip_reason"
      via: "autouse/per-test fixture calls live_skip_reason() at test start and pytest.skip(...) when creds lapsed"
      pattern: "live_skip_reason"
---

<objective>
Fix ISS-011 (env/credential, NOT product). The 3 Phase-8 HITL live-tail tests (`TestLiveHITL::{test_live_gates_off_completes_without_gating, test_live_gate_on_pause_then_auto_resume, test_live_runner_tool_gate_resume}`) plus the self-check (`TestCollectionSelfCheck::test_live_gate_is_consistent_and_self_skipping`) all trace to ONE cause: the default-profile SSO token expired ~2.5h into the live sweep. Expired-SSO is non-transient (`model_policy.py:185-211` lists only throttle/5xx/429), so it is swallowed to `error` → 0 tokens → `_check_tokens`'s `require_tokens=True` fails. The self-check fails because `_LIVE_SKIP` (captured at collection, :116) != runtime `live_skip_reason()` once STS stops resolving.

Make the live tests RESILIENT (the recurring-mode fix — the real deliverable): add a per-`@requires_live`-test re-check (autouse fixture or per-test start) that calls `live_skip_reason()` and `pytest.skip("AWS SSO session expired mid-sweep — re-run after aws sso login")` when creds have lapsed, instead of failing an assertion; and relax `test_live_gate_is_consistent_and_self_skipping` so it tolerates the legitimate collection-valid → runtime-expired drift (do NOT assert strict equality across a long run). `live_skip_reason()` is already runtime-callable — that is what makes the fix clean. Prove the skip-on-expiry behavior offline with an expired-creds simulation; `TestOfflineHITL` already proves the seams are sound with no creds.

Purpose: closes ROADMAP Phase 17 Success Criterion 3 (ISS-011) — mid-sweep credential expiry SKIPS (not fails) the HITL cases; the self-check tolerates the drift; offline HITL stays green. No production code; the transient-error classification is deliberately left unchanged.
Output: the per-test re-check guard, the relaxed self-check, and an offline expired-creds skip simulation test, all test-only.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a per-test live_skip_reason() re-check that SKIPs the HITL cases on mid-sweep expiry, relax the self-check's strict equality, and add an offline expired-creds skip simulation</name>
  <files>backend/tests/agents/test_phase8_live.py</files>
  <read_first>
    - .planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md — the LOCKED ISS-011 decision (disposition; the two parts; Part 1 deferred = re-run after `aws sso login`, NOT blocking; Part 2 = the recurring-mode harness fix; the rejected hacks; "offline proof: TestOfflineHITL already passes").
    - .planning/ISSUES-REGISTER.md → "Deep Root-Cause Investigation" row ISS-011.
    - backend/tests/agents/test_phase8_live.py:110-117 — `_LIVE_SKIP = live_skip_reason()` (collection-time) + `requires_live = pytest.mark.skipif(_LIVE_SKIP is not None, ...)`.
    - backend/tests/agents/test_phase8_live.py:528-560 — `TestLiveHITL` and its 3 cases (the ones that must SKIP on expiry).
    - backend/tests/agents/test_phase8_live.py:991-1021 — `TestCollectionSelfCheck::test_live_gate_is_consistent_and_self_skipping`; the strict `assert _LIVE_SKIP == live_skip_reason()` at :1012 is the actual bug (it assumes the SSO session is immortal across a multi-hour run); also the :1021 `assert requires_live.kwargs.get("reason") == _LIVE_SKIP`.
    - backend/tests/agents/live_harness.py:330-377 — `_aws_creds_resolve()` (STS probe) and `live_skip_reason()` (the runtime re-check; returns a reason string or None) — the runtime source of truth Part 2 uses; do NOT modify it.
    - backend/agents/execution_engine/model_policy.py:185-211 — the transient-error classification: expired-SSO is non-transient here. READ-ONLY — do NOT widen it (the dangerous rejected hack).
  </read_first>
  <action>
    Make `test_phase8_live.py` resilient to mid-sweep credential expiry (Part 2 — the recurring-mode fix). Add a per-`@requires_live`-test runtime re-check: prefer an `autouse=True` fixture scoped to the live HITL class (or, if cleaner, a small helper called at the top of each of the 3 `TestLiveHITL` cases) that calls `from tests.agents.live_harness import live_skip_reason` then `reason = live_skip_reason()` and, when `reason is not None`, `pytest.skip("AWS SSO session expired mid-sweep — re-run after aws sso login: " + reason)`. This converts a mid-sweep STS lapse from an assertion FAILURE (0 tokens → `require_tokens` fails) into a clean SKIP-with-reason. Keep the existing collection-time `@requires_live` skipif (it still gates the offline case); the new re-check only adds the RUNTIME re-evaluation that the collection-time capture cannot see. Scope the autouse fixture so it does NOT skip the offline classes (`TestOfflineHITL`/`TestOfflineProof`) — bind it to the `TestLiveHITL` class (and optionally `TestLivePipelines`) only, or guard it on the same `@requires_live` membership.

    Relax `test_live_gate_is_consistent_and_self_skipping`: replace the strict `assert _LIVE_SKIP == live_skip_reason()` (:1012) with a tolerance for the legitimate collection-valid → runtime-expired DRIFT — assert the GATE'S correctness, not byte-equality across a multi-hour run. Concretely: keep asserting that when the runtime `live_skip_reason()` is None the gate is runnable and when it is a string it carries a non-empty human-readable reason, and that the `@requires_live` mark's reason equals the COLLECTION-time `_LIVE_SKIP` (the mark IS captured at collection — that pairing is still valid), but DROP the assertion that `_LIVE_SKIP` must equal the current runtime `live_skip_reason()`. Add a comment citing ISS-011: the strict cross-time equality was the bug (it assumed the SSO session is immortal across the sweep).

    Add an OFFLINE expired-creds skip simulation test (e.g. `test_live_hitl_skips_on_mid_sweep_credential_expiry`) that proves the re-check SKIPs rather than fails: monkeypatch the `live_skip_reason` symbol the re-check imports so it returns a non-None expiry reason (simulating a lapsed SSO token mid-run), then assert the guard raises `pytest.skip.Exception` (e.g. `with pytest.raises(pytest.skip.Exception): <call the guard helper>`, or invoke the fixture/helper and assert the Skipped outcome). This runs fully offline (no Bedrock) and pins the recurring-mode contract deterministically. Do NOT add Part 1 (the live re-run) as a task — it needs fresh creds; record it as DEFERRED in the SUMMARY.

    REJECTED hacks (do NOT do): "just re-run" with no harness change (leaves the recurring fault — the next 3-hr sweep reddens whatever runs past the token boundary, indistinguishable from a real regression); add "expired"/"security token" to `_TRANSIENT_SUBSTRINGS` / `model_policy.py` so the runner "retries" (DANGEROUS — an expired SSO token can't be refreshed by a botocore retry; it would mask real `AccessDenied`/`ValidationException` faults and perturb production error semantics — INV-3-relevant). The classification stays untouched.
  </action>
  <verify>
    <automated>cd backend && grep -q "live_skip_reason" tests/agents/test_phase8_live.py && python3.11 -m pytest "tests/agents/test_phase8_live.py" -k "OfflineHITL or self_skipping or mid_sweep" -p no:cacheprovider -q</automated>
  </verify>
  <acceptance_criteria>
    - The 3 `TestLiveHITL` cases gain a per-test runtime re-check (autouse fixture bound to the live class or a per-test helper) that calls `live_skip_reason()` and `pytest.skip(...)` with a re-run-after-`aws sso login` reason when it returns non-None — converting a mid-sweep STS lapse from FAIL to SKIP. The re-check does NOT skip `TestOfflineHITL`/`TestOfflineProof`.
    - `test_live_gate_is_consistent_and_self_skipping` no longer asserts strict `_LIVE_SKIP == live_skip_reason()`; it asserts gate correctness (runnable when reason None; non-empty reason + matching mark.reason when skipped) and tolerates the collection-valid → runtime-expired drift, with an ISS-011 comment.
    - The offline simulation test `test_live_hitl_skips_on_mid_sweep_credential_expiry` monkeypatches `live_skip_reason` to return an expiry reason and proves the guard raises `pytest.skip.Exception` (SKIP, not FAIL) — fully offline.
    - `python3.11 -m pytest tests/agents/test_phase8_live.py -k "OfflineHITL or self_skipping or mid_sweep"` passes offline: `TestOfflineHITL` green, the relaxed self-check green, the new simulation green.
    - `git diff --name-only` shows ONLY `backend/tests/agents/test_phase8_live.py` — `backend/agents/execution_engine/model_policy.py` is NOT in the diff (classification untouched).
  </acceptance_criteria>
  <done>The HITL live cases SKIP (not fail) on mid-sweep credential expiry via a runtime live_skip_reason() re-check, the self-check tolerates the collection→runtime drift, an offline simulation proves the skip, TestOfflineHITL stays green, the transient classification is untouched, and the diff is test-only.</done>
</task>

<task type="auto">
  <name>Task 2: Parity guard — assert test-only diff + 5 characterization goldens + lint-imports stay green</name>
  <files>backend/tests/agents/test_phase8_live.py</files>
  <read_first>
    - .planning/phases/17-test-infra-and-verification-gap-closure/17-CONTEXT.md → INVARIANTS (test/harness-only; 5 goldens untouched by construction; still run goldens + lint-imports as the parity guard; `python3.11` no venv; FULL pytest hangs offline — run only the targeted suite).
    - backend/tests/agents/test_characterization_prototype.py / test_characterization_od_prototype.py / test_characterization_prototype_revision.py / test_characterization_od_ppt.py / test_characterization_app_builder.py — READ-ONLY; must stay byte-identical.
  </read_first>
  <action>
    No code changes — the SC-001 / INV-3 parity guard for the ISS-011 edit. Run the 5 characterization golden suites and `lint-imports`; confirm both are unaffected by the test-only change and that the working-tree diff touches test files only (and explicitly that `model_policy.py` is NOT touched — the dangerous rejected hack). Record evidence (goldens green, lint 4/0, test-only diff, classification untouched) in the SUMMARY; SNAPSHOT_UPDATE stays UNSET.
  </action>
  <verify>
    <automated>cd backend && git diff --name-only | grep -vE '^backend/tests/' | grep -q . && { echo "NON-TEST FILE IN DIFF"; exit 1; } ; python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -p no:cacheprovider -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` lists ONLY paths under `backend/tests/` — the non-test-file guard exits 0 (none found); `model_policy.py` is absent from the diff.
    - All 5 characterization golden suites pass byte-identical with SNAPSHOT_UPDATE unset (no regeneration).
    - `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken.
    - SC-001 / INV-3 statement recorded in the SUMMARY (test-harness-only; parity satisfied by construction AND verified; the transient classification deliberately untouched).
  </acceptance_criteria>
  <done>The 5 characterization goldens are byte-identical, lint-imports is 4/0, the diff is test-only, and model_policy.py is untouched — the ISS-011 fix introduced zero production/behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches ONLY `backend/tests/agents/test_phase8_live.py` (a test file). It introduces no new runtime, no network surface, no new dependency, and no production code path. There is NO trust boundary crossed — the test runs only under the developer's offline pytest invocation.

The ONE real care item (LOCKED): do NOT widen the transient-error classification in `model_policy.py` to auto-retry credential errors. That WOULD touch production error handling — masking real `AccessDenied`/`ValidationException` faults (INV-3-relevant). This plan keeps the fix entirely in the test harness (skip-on-expiry), leaving the production classification untouched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-S | Spoofing | test harness | N/A (test-only) | No identity/auth surface beyond the read-only STS preflight `live_skip_reason()` already performs. |
| T-17-03-T | Tampering | test harness / error classification | mitigate | The fix is confined to the test (skip-on-expiry). The dangerous path — widening `_TRANSIENT_SUBSTRINGS`/`model_policy.py` to retry credential errors — is EXPLICITLY rejected; the diff-guard asserts `model_policy.py` is not in the diff. |
| T-17-03-R | Repudiation | test harness | N/A (test-only) | No audit surface. |
| T-17-03-I | Info Disclosure | test harness | N/A (test-only) | The skip reason carries only the botocore error type/message that `live_skip_reason()` already surfaces; no new secret exposure; the offline simulation monkeypatches the reason (no real creds). |
| T-17-03-D | Denial of Service | test harness | accept | The change turns a hard FAIL on expiry into a clean SKIP — it reduces, not increases, spurious red. |
| T-17-03-E | Elevation of Privilege | test harness | N/A (test-only) | No permission/tool grant surface. |
| T-17-03-SC | Tampering | npm/pip installs | N/A | No package installs in this plan; no new dependency. |

Honest surface: negligible — test file only, no runtime/attack surface, no new deps. The single real care item (don't widen the transient classification) is dispositioned `mitigate` and guarded by the test-only-diff assertion. No block-worthy threats.
</threat_model>

<verification>
- `tests/agents/test_phase8_live.py -k "OfflineHITL or self_skipping or mid_sweep"` passes offline: TestOfflineHITL green, relaxed self-check green, the expired-creds skip simulation green.
- The 3 TestLiveHITL cases SKIP (not fail) on mid-sweep expiry via the runtime `live_skip_reason()` re-check.
- 5 characterization goldens byte-identical; `/opt/homebrew/bin/lint-imports` 4 kept / 0 broken; `model_policy.py` untouched.
- `git diff --name-only` is test-only.
</verification>

<success_criteria>
- ISS-011 closed offline-deterministically: mid-sweep credential expiry SKIPS the HITL cases, the self-check tolerates the drift, TestOfflineHITL stays green (ROADMAP Phase 17 SC3).
- INV-3 / SC-001 parity guard green, diff test-only, transient classification untouched.
- DEFERRED follow-up recorded (not blocking): Part 1 — re-run the 3 HITL cases after `aws sso login` (sub-$0.05, ~minutes) to flip ISS-011 MONITORING→FIXED — needs fresh creds; record as a next-live-pass item.
</success_criteria>

<deferred>
- ISS-011 Part 1 (closure): re-run the 3 `TestLiveHITL` cases after `aws sso login` on fresh default-profile creds (acct 473293451041) — sub-$0.05, ~minutes — and flip ISS-011 MONITORING → FIXED. NOT blocking: it needs fresh creds and the phase exit is offline-deterministic. Record in the SUMMARY as a next-Bedrock-pass item.
</deferred>

<artifacts_this_phase_produces>
- A per-test runtime `live_skip_reason()` re-check (autouse fixture bound to the live HITL class or a per-test guard helper) that SKIPs the 3 HITL cases on mid-sweep credential expiry.
- The relaxed `test_live_gate_is_consistent_and_self_skipping` (tolerates collection→runtime drift; ISS-011 comment).
- New offline test `test_live_hitl_skips_on_mid_sweep_credential_expiry` proving the skip-on-expiry contract via a monkeypatched `live_skip_reason`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/17-test-infra-and-verification-gap-closure/17-03-SUMMARY.md` when done
</output>
