---
phase: 06-model-policy-1c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/model_catalog.py
  - backend/agents/capabilities/registry.py
  - backend/app/api/settings.py
  - backend/tests/agents/test_model_catalog.py
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "ModelCatalog enumerates 5 models, each with id/label/description/tier/cost_class/provider/context_window/user_allowed"
    - "All models including Opus are user_allowed=true (no gate, N11)"
    - "The capability ('model_catalog','default') is discoverable via CapabilityRegistry.is_registered"
    - "app/api/settings.py::AVAILABLE_MODELS is a derived projection over the catalog (INV-12 single source)"
    - "/api/settings response shape is unchanged: each model is exactly {id, name, description, tier}"
    - "Exactly one hand-maintained model-id list remains in the codebase (the catalog) — grep proves it"
  artifacts:
    - path: "backend/agents/capabilities/model_catalog.py"
      provides: "ModelCatalog pure-data capability (list/get/is_allowed/ids); kernel-pure, no app.* import"
      contains: "class ModelCatalog"
    - path: "backend/agents/capabilities/registry.py"
      provides: "('model_catalog','default') name-only membership in _KNOWN"
      contains: "model_catalog"
    - path: "backend/app/api/settings.py"
      provides: "AVAILABLE_MODELS + _VALID_MODEL_IDS derived from ModelCatalog"
      contains: "ModelCatalog"
    - path: "backend/tests/agents/test_model_catalog.py"
      provides: "Catalog field-set + registry membership + projection + single-source grep tests"
      exports: []
  key_links:
    - from: "backend/app/api/settings.py"
      to: "agents.capabilities.model_catalog.ModelCatalog"
      via: "import + projection comprehension"
      pattern: "from agents.capabilities.model_catalog import ModelCatalog"
    - from: "backend/agents/capabilities/registry.py"
      to: "_KNOWN membership"
      via: "set literal entry"
      pattern: "model_catalog.*default"
---

<objective>
Create the `ModelCatalog` kernel-pure data capability as the single source of model metadata (MODEL-04), register its name in the capability registry (name-only, Phase-4 D-07 pattern), and refactor `app/api/settings.py::AVAILABLE_MODELS` + `_VALID_MODEL_IDS` into derived projections over the catalog (INV-12).

Purpose: The resolver (06-03), override-validator (06-04), and loader (06-02) all need a single authoritative, kernel-importable source of selectable model ids + metadata. This plan establishes that source and eliminates the hand-maintained `AVAILABLE_MODELS` list so exactly ONE model-id list remains (INV-12).
Output: `agents/capabilities/model_catalog.py` (new), `registry._KNOWN += ("model_catalog","default")`, `settings.py` projection, `tests/agents/test_model_catalog.py` (new). `/api/settings` response shape byte-identical.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-model-policy-1c/06-SPEC.md
@.planning/phases/06-model-policy-1c/06-CONTEXT.md
@.planning/phases/06-model-policy-1c/06-RESEARCH.md
@.planning/phases/06-model-policy-1c/06-PATTERNS.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (source-grounding pass should exclude these from drift verification — they do not exist before this plan):
- `backend/agents/capabilities/model_catalog.py` — module
- `ModelCatalog` — class with methods `list()`, `get(id)`, `is_allowed(id)`, `ids()`
- the 5 catalog entry objects (dataclass/record per model: `id`, `label`, `description`, `tier`, `cost_class`, `provider`, `context_window`, `user_allowed`)
- the registry name tuple `("model_catalog", "default")` in `agents/capabilities/registry.py::_KNOWN`
- `backend/tests/agents/test_model_catalog.py` — new test file
NOTE: `AVAILABLE_MODELS` and `_VALID_MODEL_IDS` are EXISTING symbols (settings.py:40,74) being refactored to projections — not new.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ModelCatalog data capability + Wave-0 test scaffold</name>
  <files>backend/agents/capabilities/model_catalog.py, backend/tests/agents/test_model_catalog.py</files>
  <read_first>
    - backend/app/api/settings.py:40-74 — the `AVAILABLE_MODELS` seed list (5 models with id/name/description/tier) and `_VALID_MODEL_IDS`; this is the data to lift into the catalog
    - backend/agents/capabilities/registry.py:27-64 — the kernel-pure data-module shape (`_KNOWN` set + `is_registered` pure membership); the catalog mirrors this layer's purity
    - .planning/phases/06-model-policy-1c/06-PATTERNS.md § "agents/capabilities/model_catalog.py" — the exact field map table (5 rows) + import-purity rule + "no new Protocol" guidance
    - .planning/phases/06-model-policy-1c/06-RESEARCH.md § "MUST RESOLVE #4" — the 5 catalog entries table (exact ids, labels, descriptions, tier↔cost_class mapping) + the [ASSUMED] context_window note + the kernel-purity contract (pyproject.toml:165-169, forbidden_modules=["agents.execution_engine","app"])
    - backend/pyproject.toml — the import-linter contract "agents.capabilities must not import the execution kernel or the web layer" (do NOT import app.* from the catalog)
  </read_first>
  <behavior>
    - Test 1 (field set): ModelCatalog().list() returns 5 entries; each carries id, label, description, tier, cost_class, provider, context_window, user_allowed
    - Test 2 (ids): ModelCatalog().ids() == the 5 model ids; is_allowed(known_id) True; is_allowed("garbage") False; get(known_id) returns the entry; get("garbage") returns None/raises consistently
    - Test 3 (cost_class assignment, N11): Haiku=cheap, Sonnet 4.5/4.6=standard, Opus 4.5/4.6=premium
    - Test 4 (tier↔cost_class consistency): every entry satisfies the mapping fast↔cheap, balanced↔standard, powerful↔premium (assert no entry violates it)
    - Test 5 (user_allowed): all 5 entries user_allowed=true, including both Opus entries (no gate)
    - Test 6 (kernel purity): importing agents.capabilities.model_catalog pulls no app.* module (assert "app" not in the module's resolved imports / lint-imports covers this at wave level)
  </behavior>
  <action>
    Create `backend/agents/capabilities/model_catalog.py` as a kernel-pure data module (per D-03). Define an immutable per-model record (a frozen dataclass `ModelEntry` or equivalent) with fields `id: str`, `label: str`, `description: str`, `tier: str`, `cost_class: str`, `provider: str`, `context_window: int`, `user_allowed: bool`. Define `class ModelCatalog` exposing `list() -> list[ModelEntry]`, `get(model_id) -> ModelEntry | None`, `is_allowed(model_id) -> bool`, `ids() -> list[str]`. Seed EXACTLY these 5 entries (ids/labels/descriptions verbatim from settings.py:40-71, per the RESEARCH #4 table):
      - `eu.anthropic.claude-haiku-4-5-20251001-v1:0` → label "Claude Haiku 4.5", tier `fast`, cost_class `cheap`, provider `bedrock`, user_allowed true
      - `eu.anthropic.claude-sonnet-4-5-20250929-v1:0` → "Claude Sonnet 4.5", tier `balanced`, cost_class `standard`, provider `bedrock`, user_allowed true
      - `eu.anthropic.claude-sonnet-4-6` → "Claude Sonnet 4.6", tier `balanced`, cost_class `standard`, provider `bedrock`, user_allowed true
      - `eu.anthropic.claude-opus-4-5-20251101-v1:0` → "Claude Opus 4.5", tier `powerful`, cost_class `premium`, provider `bedrock`, user_allowed true
      - `eu.anthropic.claude-opus-4-6-v1` → "Claude Opus 4.6", tier `powerful`, cost_class `premium`, provider `bedrock`, user_allowed true
    For `context_window` use 200000 for all except `eu.anthropic.claude-sonnet-4-6` = 1000000 (per the "1M token context" description; [ASSUMED] numerics per RESEARCH A1 — metadata only, does not affect resolution). Keep BOTH `tier` (legacy/display, D-04 store-both) and `cost_class` (canonical). Do NOT import `app.*` — the module is self-contained data (Pitfall 3). Do NOT add a Protocol to base.py (catalog is data, not behavior — RESEARCH #3). Then create `backend/tests/agents/test_model_catalog.py` with the 6 behavior tests above (field set, ids/get/is_allowed, cost_class assignment, tier↔cost_class consistency, user_allowed-all-true incl. Opus, kernel-purity import check). Use pytest (no asyncio needed for catalog tests).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_model_catalog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/agents/capabilities/model_catalog.py contains class ModelCatalog` (source assertion)
    - `cd backend && python3.11 -m pytest tests/agents/test_model_catalog.py -x` exits 0 (test command)
    - `cd backend && python3.11 -c "import agents.capabilities.model_catalog as m; assert len(m.ModelCatalog().list())==5; assert all(e.user_allowed for e in m.ModelCatalog().list())"` exits 0 (behavior: 5 entries, all user_allowed)
    - `grep -n "import app" backend/agents/capabilities/model_catalog.py` returns nothing (kernel-purity source assertion — no app.* import)
    - Behavior: `ModelCatalog().is_allowed("eu.anthropic.claude-opus-4-6-v1")` is True and `is_allowed("not-a-model")` is False
  </acceptance_criteria>
  <done>ModelCatalog exists as kernel-pure data with 5 fully-fielded entries (Opus user_allowed); test_model_catalog.py passes with the 6 behavior tests; no app.* import in the module.</done>
</task>

<task type="auto">
  <name>Task 2: Register catalog name + refactor AVAILABLE_MODELS to a projection (INV-12)</name>
  <files>backend/agents/capabilities/registry.py, backend/app/api/settings.py, backend/tests/agents/test_model_catalog.py</files>
  <read_first>
    - backend/agents/capabilities/registry.py:27,36-64 — `_KNOWN` set + the "authoritative 14" docstring comment (becomes 15) + `is_registered` pure membership at line 64
    - backend/app/api/settings.py:40-74 — `AVAILABLE_MODELS` literal list + `_VALID_MODEL_IDS` (line 74); also note consumers at lines 318, 334-337, 347 that must keep working unchanged
    - .planning/phases/06-model-policy-1c/06-RESEARCH.md § "MUST RESOLVE #4" — the projection shape `{id, name, description, tier}` (frontend ModelOption requires EXACTLY these 4, VERIFIED api.ts:394-399) + the INV-12 grep assertion (model-id list in exactly one place after refactor)
    - .planning/phases/06-model-policy-1c/06-PATTERNS.md § "app/api/settings.py" and § "Shared Patterns → Name-only capability membership" — the projection comprehension + the registry-membership add pattern
  </read_first>
  <action>
    In `agents/capabilities/registry.py`: add `("model_catalog", "default")` to the `_KNOWN` set literal (line 36-51 area) as pure membership — NO held instance, NO `@register`, NO trust enforcement (those are Phase 8, D-03). Update the docstring comment at line 27 from "authoritative 14" to "authoritative 15". `is_registered` stays the unchanged pure set-membership check.
    In `app/api/settings.py`: replace the literal `AVAILABLE_MODELS` list (lines 40-71) with a projection — `from agents.capabilities.model_catalog import ModelCatalog` (app→kernel import is allowed) then `AVAILABLE_MODELS = [{"id": m.id, "name": m.label, "description": m.description, "tier": m.tier} for m in ModelCatalog().list()]`. Replace `_VALID_MODEL_IDS` (line 74) with `set(ModelCatalog().ids())`. The projection MUST emit EXACTLY `{id, name, description, tier}` (4 keys, mapping `label`→`name`) so the frontend ModelOption shape and `/api/settings` response stay byte-identical; the catalog's extra fields (cost_class/provider/context_window/user_allowed) are dropped by the projection. Do NOT change the `/api/settings` endpoint logic or response model — only the source of the two module-level constants.
    Extend `tests/agents/test_model_catalog.py` with: (a) `test_available_models_is_projection` — `AVAILABLE_MODELS` equals the projection over `ModelCatalog().list()` and every entry has exactly the 4 keys `{id,name,description,tier}`; (b) `test_registry_membership` — `is_registered("model_catalog","default")` is True; (c) `test_single_source_grep` — assert the model-id literals (`claude-haiku-4`/`claude-sonnet-4`/`claude-opus-4`) appear as a maintained list in exactly ONE file under `backend/app/api` + `backend/agents/capabilities`, namely `model_catalog.py` (settings.py must contain no literal model dicts — only the projection). Reference RESEARCH #4's grep assertion for the exact command.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_model_catalog.py -x -q && grep -rn 'claude-haiku-4\|claude-sonnet-4\|claude-opus-4' app/api/settings.py | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python3.11 -m pytest tests/agents/test_model_catalog.py -x` exits 0 (includes projection + membership + single-source grep tests)
    - Behavior: `is_registered("model_catalog","default")` returns True (registry membership)
    - INV-12 source assertion: `grep -rnE 'claude-(haiku|sonnet|opus)-4' backend/app/api backend/agents/capabilities | grep -v '^[[:space:]]*#'` shows model-id literals ONLY in `backend/agents/capabilities/model_catalog.py` (exactly one maintained list)
    - `/api/settings` shape unchanged: `AVAILABLE_MODELS[0]` has exactly keys `{id, name, description, tier}` (4 keys, no extras)
    - `cd backend && python3.11 -m pytest tests/agents/test_loader.py tests/agents/test_banned_patterns.py tests/agents/test_migration_ledger.py -q` stays green (no gate regression)
  </acceptance_criteria>
  <done>Registry knows `("model_catalog","default")` (15 names); `AVAILABLE_MODELS`/`_VALID_MODEL_IDS` derive from the catalog; grep proves exactly one model-id list (INV-12); `/api/settings` response shape unchanged; gate subset green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import-linter (agents.capabilities → app) | The catalog must NOT reach into the web/app layer; a leaked `app.*` import breaks kernel-purity and the resolver's ability to import it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `model_catalog.py` kernel-purity | mitigate | No `app.*` import in the module (Task 1 acceptance grep `grep -n "import app"` returns nothing); `lint-imports` stays 3-kept/0-broken at wave merge |
| T-06-02 | Information Disclosure / Integrity | INV-12 dual model lists | mitigate | `AVAILABLE_MODELS` derives from the catalog; Task 2 single-source grep test proves exactly one maintained model-id list (a second drifting list could expose a stale/wrong allow-list) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO new packages (RESEARCH § Package Legitimacy Audit = N/A); no install task exists in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_model_catalog.py -x -q` (catalog field set, ids, cost_class, projection, membership, single-source)
- `cd backend && lint-imports` → 3 kept / 0 broken (catalog kernel-pure)
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` (no regression)
- INV-12: `grep -rnE 'claude-(haiku|sonnet|opus)-4' backend/app/api backend/agents/capabilities | grep -v '^[[:space:]]*#'` → only `model_catalog.py`
</verification>

<success_criteria>
- ModelCatalog enumerates 5 models with the full field set; Opus entries `user_allowed=true`; discoverable via the registry by `("model_catalog","default")`
- `AVAILABLE_MODELS` derives from the catalog; grep shows no second hand-maintained model list (INV-12)
- `/api/settings` response shape unchanged (`{id,name,description,tier}`)
- import-linter stays 3-kept / 0-broken (catalog kernel-pure)
- Gate subset (loader/banned-pattern/migration-ledger) stays green
</success_criteria>

<output>
Create `.planning/phases/06-model-policy-1c/06-01-SUMMARY.md` when done.
</output>
