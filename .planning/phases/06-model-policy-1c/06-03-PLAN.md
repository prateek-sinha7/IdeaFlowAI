---
phase: 06-model-policy-1c
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - backend/agents/model_policy.py
  - backend/agents/execution_engine/context.py
  - backend/agents/execution_engine/engine.py
  - backend/tests/unit/test_model_resolver.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-05]
must_haves:
  truths:
    - "ModelResolver resolves the effective model id by precedence: override > step.model > AgentSpec.model > workflow.model > (session model_id or Haiku)"
    - "With overrides={} and all manifest/agent tiers None, every agent resolves to `session model_id or Haiku` == today's exact behavior (INV-3 parity)"
    - "None tiers are skipped (an empty tier does not block lower tiers)"
    - "An empty ModelPolicy.fallback derives the tier-descent chain: Opus→[Sonnet,Haiku], Sonnet→[Haiku], Haiku→[]"
    - "A ModelPolicy.max_tokens does NOT raise the runtime cap (still settings.MAX_OUTPUT_TOKENS)"
    - "The resolver is constructed at execute() entry, carried on ExecutionContext.model_resolver, and consulted at each _run_agent model site"
    - "ExecutionContext stays import-pure (model_resolver typed object | None, like scoped_store)"
    - "Existing characterization snapshots stay byte/semantic-identical (INV-3)"
  artifacts:
    - path: "backend/agents/model_policy.py"
      provides: "ModelResolver (precedence resolve + chain derivation) + _is_transient_throttle predicate"
      contains: "class ModelResolver"
    - path: "backend/agents/execution_engine/context.py"
      provides: "model_resolver: object | None + model_overrides: dict fields"
      contains: "model_resolver"
    - path: "backend/agents/execution_engine/engine.py"
      provides: "resolver construction at execute() entry + 3 _run_agent model sites rewired to resolver"
      contains: "ModelResolver"
    - path: "backend/tests/unit/test_model_resolver.py"
      provides: "5 precedence tests + parity-default + max_tokens-doc-only + default tier-descent chains"
      exports: []
  key_links:
    - from: "backend/agents/execution_engine/engine.py::_run_agent"
      to: "ctx.model_resolver.resolve(spec, step)"
      via: "resolved id into AgentContext(model=...)"
      pattern: "model_resolver.resolve"
    - from: "backend/agents/model_policy.py::ModelResolver"
      to: "agents.capabilities.model_catalog.ModelCatalog"
      via: "chain validity check + AGENT.md model resolve-time validation"
      pattern: "ModelCatalog"
---

<objective>
Create the `ModelResolver` (kernel module `agents/model_policy.py`) that resolves the effective model id per agent invocation by the locked precedence (D-02), derive default tier-descent fallback chains, co-locate the `_is_transient_throttle` predicate (consumed by the 06-05 fallback loop), carry the resolver on `ExecutionContext` (import-pure, like `scoped_store`), construct it at `execute()` entry, and rewire the three `_run_agent` model sites to consult it (MODEL-01/02/05).

Purpose: This is the heart of the phase — one resolution path for every agent. The parity-critical default (tier 5 = `session model_id or Haiku`) keeps existing single-model runs byte/semantic-identical (INV-3). The resolver also owns chain composition for the 06-05 fallback layer.
Output: `agents/model_policy.py` (new: `ModelResolver` + `_is_transient_throttle`), `context.py` (+2 fields), `engine.py` (resolver construct + 3 model sites rewired), `tests/unit/test_model_resolver.py` (new). Characterization snapshots unchanged.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-model-policy-1c/06-SPEC.md
@.planning/phases/06-model-policy-1c/06-CONTEXT.md
@.planning/phases/06-model-policy-1c/06-RESEARCH.md
@.planning/phases/06-model-policy-1c/06-PATTERNS.md
@backend/CLAUDE.md
@.planning/phases/06-model-policy-1c/06-01-SUMMARY.md
@.planning/phases/06-model-policy-1c/06-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (exclude from drift verification — none exist before this plan):
- `backend/agents/model_policy.py` — module
- `ModelResolver` — class with `resolve(spec, step=None) -> str` (effective id), chain derivation (e.g. `chain_for(model_id) -> list[str]`), and `advance()` semantics for 06-05 (the resolver holds the active chain + cursor)
- `_is_transient_throttle(exc) -> bool` — module-level predicate
- `ExecutionContext.model_resolver: object | None` and `ExecutionContext.model_overrides: dict` — new fields (context.py)
- `backend/tests/unit/test_model_resolver.py` — new test file
CONSUMES (existing, do not flag): `ModelPolicy`/`Step.model`/`CompiledWorkflow.model` (plan.py:64-71/209/248), `AgentSpec.model` (created in 06-02), `ModelCatalog` (created in 06-01), `settings.BEDROCK_INFERENCE_PROFILE_ID` (config.py).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ModelResolver + _is_transient_throttle + context fields + Wave-0 resolver tests</name>
  <files>backend/agents/model_policy.py, backend/agents/execution_engine/context.py, backend/tests/unit/test_model_resolver.py</files>
  <read_first>
    - backend/agents/execution_engine/context.py:76-82 — the `scoped_store: object | None = None` precedent (import-purity typing) + the module's stdlib-only imports (lines 25-37) to preserve
    - backend/agents/workflows/plan.py:64-71,209,248 — `ModelPolicy` (model/max_tokens/cost_class/fallback), `Step.model: ModelPolicy | None`, `CompiledWorkflow.model: ModelPolicy` (read-only; the resolver reads these)
    - .planning/phases/06-model-policy-1c/06-RESEARCH.md § "Code Examples → Resolver precedence (D-02)" — the exact precedence skeleton (parity-critical) + § "MUST RESOLVE #7" — the `_is_transient_throttle` exception-type/code/status set (Bedrock ClientError codes, 429/503/500; ChatAnthropic 429/529/overloaded; non-matching propagate)
    - .planning/phases/06-model-policy-1c/06-PATTERNS.md § "agents/model_policy.py" — Analog A (scoped_store carry), Analog B (precedence shape), chain-derivation (tier-descent), imports the resolver MAY make (workflows.plan, model_catalog, app.core.config settings — all import-clean for this module)
    - backend/app/core/config.py:63,70,75,81 — `ANTHROPIC_MODEL_ID`/`BEDROCK_INFERENCE_PROFILE_ID` (the Haiku global default the resolver reads at tier 5b)
    - backend/agents/capabilities/model_catalog.py (from 06-01) — `ModelCatalog.ids()`/`is_allowed()` for chain validity + resolve-time AGENT.md model validation
  </read_first>
  <behavior>
    - Test 1 (override beats step): overrides[agent]=X, step.model=Y → resolves X
    - Test 2 (step beats agent): step.model=Y, AgentSpec.model=Z → resolves Y
    - Test 3 (agent beats workflow): AgentSpec.model=Z, workflow.model=W → resolves Z
    - Test 4 (workflow beats global): workflow.model=W, all higher None → resolves W
    - Test 5 (global = parity default): all tiers None → resolves `session model_id or Haiku` (session set → session id; session None → settings.BEDROCK_INFERENCE_PROFILE_ID)
    - Test 6 (None-skipping): step.model None but AgentSpec.model set → falls through to AgentSpec.model (empty tier does not block)
    - Test 7 (max_tokens doc-only): a ModelPolicy with max_tokens does not change the resolved id and does not raise the runtime cap (assert resolver does not pass max_tokens into a cap)
    - Test 8 (default chains): chain_for(Opus id) == [Sonnet, Haiku]; chain_for(Sonnet) == [Haiku]; chain_for(Haiku) == []; an explicit ModelPolicy.fallback overrides the derived default; every chain entry is catalog-valid
    - Test 9 (resolve-time AGENT.md validation): AgentSpec.model not in catalog → rejected/raised (allow-list Q2)
    - Test 10 (throttle predicate): _is_transient_throttle returns True for a synthetic Bedrock ThrottlingException / ClientError(429) / Anthropic RateLimitError; False for a ValidationException / AccessDenied / generic ValueError
  </behavior>
  <action>
    Create `backend/agents/model_policy.py`. Define `class ModelResolver` constructed with run-level inputs: `model_overrides: dict[str,str]`, `workflow_model: ModelPolicy | None` (from `CompiledWorkflow.model`), `session_model_id: str | None`, `haiku_default: str` (read by the engine from `settings.BEDROCK_INFERENCE_PROFILE_ID`), and a `ModelCatalog` reference. Method `resolve(spec, step=None) -> str` returns the effective id by precedence (None tiers skipped): (1) `self._overrides.get(spec.id)` → (2) `step.model.model if step and step.model else None` → (3) `getattr(spec, "model", None)` → (4) `self._workflow_model.model if self._workflow_model else None` → (5a) `self._session_model_id` → (5b) `self._haiku_default`. Validate any AGENT.md `model` (tier 3) against `ModelCatalog.ids()` at resolve time (reject if unknown — Q2 allow-list, T-06-03). Method `chain_for(model_id) -> list[str]` derives the tier-descent default when `ModelPolicy.fallback` is empty (Opus→[Sonnet,Haiku], Sonnet→[Haiku], Haiku→[]) using `cost_class` from the catalog; an explicit `ModelPolicy.fallback` overrides the derived default; assert every chain entry is catalog-valid. Add resolver state the 06-05 fallback loop needs: hold the active chain + a cursor with an `advance()` that moves to the next chain id (used by the engine retry loop in 06-05). Co-locate module-level `_is_transient_throttle(exc) -> bool` as a defensive layered predicate (type-name OR HTTP-status OR error-code/message substring) matching the RESEARCH #7 set; non-matching errors return False (propagate). The `ModelPolicy.max_tokens` stays doc-only — the resolver must NOT thread it into any runtime cap (cap stays `settings.MAX_OUTPUT_TOKENS` in build_model). This module may import `agents.workflows.plan`, `agents.capabilities.model_catalog`, and `app.core.config.settings` (all import-clean — no contract restricts `agents/model_policy.py`).
    In `agents/execution_engine/context.py` (after `scoped_store` at line 82) add `model_resolver: object | None = None` and `model_overrides: dict = field(default_factory=dict)` — typed `object` to keep the module import-pure (no new import; preserve the stdlib-only import set).
    Create `backend/tests/unit/test_model_resolver.py` with behavior tests 1–10 above (precedence, parity default, None-skip, max_tokens-doc-only, default chains + explicit override + catalog-valid, resolve-time AGENT.md validation, throttle predicate). Use lightweight fakes for spec/step/ModelPolicy; no engine, no live Bedrock.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_model_resolver.py -x -q && lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - `backend/agents/model_policy.py contains class ModelResolver` and `_is_transient_throttle` (source assertion)
    - `cd backend && python3.11 -m pytest tests/unit/test_model_resolver.py -x` exits 0 (5 precedence + parity + None-skip + max_tokens + chains + throttle-predicate)
    - `cd backend && lint-imports` exits 0 (model_policy.py + context.py import-clean; context.py gains no app.* import)
    - Behavior (parity): with overrides={}, step.model None, spec.model None, workflow.model.model None and session_model_id set, `resolve()` returns the session id; with session None it returns `settings.BEDROCK_INFERENCE_PROFILE_ID`
    - Behavior (chains): `chain_for(<opus-id>) == [<sonnet-id>, <haiku-id>]`; `chain_for(<haiku-id>) == []`
    - `grep -n "import app" backend/agents/execution_engine/context.py` returns nothing (context import-purity preserved)
  </acceptance_criteria>
  <done>ModelResolver resolves by the 5-tier precedence with None-skipping and the parity default; chain_for derives tier-descent chains; _is_transient_throttle classifies the throttle set; ExecutionContext carries the two new import-pure fields; resolver tests + lint-imports green.</done>
</task>

<task type="auto">
  <name>Task 2: Construct resolver at execute() entry + rewire the 3 _run_agent model sites (parity)</name>
  <files>backend/agents/execution_engine/engine.py</files>
  <read_first>
    - backend/agents/execution_engine/engine.py:537-552,608-623 — `execute()` and `_execute_impl()` signatures (this plan does NOT add the model_overrides param — 06-04 does; here construct the resolver from the already-threaded `model_id`/`CompiledWorkflow.model`, seeding overrides from `ectx.model_overrides` which defaults to `{}`)
    - backend/agents/execution_engine/engine.py:711 area — where `ExecutionContext(...)` is constructed (set `ectx.model_resolver` near here, after the workflow is compiled so `CompiledWorkflow.model` is available)
    - backend/agents/execution_engine/engine.py:1286,1560,2033 — the three `model=model_id` AgentContext sites (primary `_run_agent`, revision path, validation fix-loop) to rewire to the resolved id
    - backend/agents/execution_engine/engine.py:1432 — `SmartPlanner(model_id=model_id)` — LEAVE UNCHANGED (no agent_id, builds its own client; routing it through the resolver risks an INV-3 parity break — RESEARCH #2 note)
    - backend/agents/factory.py:44,155 — `AgentContext.model` + `create_runner` `model=ctx.model` — UNCHANGED; the resolved id flows through unchanged (INV-13: reaches the graph only via build_model)
    - .planning/phases/06-model-policy-1c/06-PATTERNS.md § "agents/execution_engine/engine.py" items 2,3,4 — the verbatim record_capabilities/model-site/SmartPlanner anchors
    - backend/tests/agents/characterization/ — the INV-3 parity snapshots that MUST stay byte/semantic-identical
  </read_first>
  <action>
    In `engine.py`, after the workflow is compiled and `ExecutionContext` (`ectx`) is built (~line 711+), construct `ModelResolver` and assign it to `ectx.model_resolver`: seed it with `ectx.model_overrides` (defaults `{}` this plan; populated by 06-04), `compiled_workflow.model` (the `CompiledWorkflow.model` / workflow default), the run-wide `session model_id` (the existing `model_id` param), the global Haiku default read from `settings.BEDROCK_INFERENCE_PROFILE_ID`, and a `ModelCatalog()`. Import `ModelResolver` from `agents.model_policy` and `ModelCatalog` from `agents.capabilities.model_catalog` (engine→kernel is import-clean).
    Rewire the THREE `model=model_id` AgentContext sites (engine.py:1286 revision, 1560 primary, 2033 validation fix-loop): replace `model=model_id` with the resolved id from `ectx.model_resolver.resolve(spec, step)` — pass the per-agent `spec` (the AgentSpec for that invocation) and the compiled `step` (the Step for that agent, or None if not a manifest step). For the revision/fix-loop sites, pass the same agent's spec/step (the fix-loop reuses the build task's agent — RESEARCH). Do NOT change `factory.py` (`ctx.model` now carries the resolved id). Do NOT touch SmartPlanner (engine.py:1432) — it stays on the session `model_id`.
    PARITY GUARD: with `ectx.model_overrides == {}` and all manifest/agent tiers None (today's state), `resolve()` MUST return `session model_id or Haiku` = exactly today's `model_id` input to `build_model`. Run the characterization snapshots after this change — they MUST be byte/semantic-identical.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/characterization/ -q && python3.11 -m pytest tests/agents/ tests/unit/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `engine.py` source assertion: `grep -n "model_resolver.resolve" backend/agents/execution_engine/engine.py` shows the rewire at the 3 model sites; `grep -n "model=model_id" backend/agents/execution_engine/engine.py` shows ONLY the SmartPlanner site (1432) remaining
    - `cd backend && python3.11 -m pytest tests/agents/characterization/ -q` passes UNCHANGED (INV-3 parity — byte/semantic-identical snapshots)
    - `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` green (no regression)
    - Behavior: a no-override run with no manifest model fields resolves every agent to `session model_id or Haiku` (the resolver returns today's exact id)
    - `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -q` green (no new create_deep_agent; INV-13 preserved — resolved id reaches graph only via build_model)
  </acceptance_criteria>
  <done>The resolver is constructed at execute() entry on `ectx.model_resolver`; all three `_run_agent` model sites consult it; SmartPlanner unchanged; characterization snapshots byte/semantic-identical (INV-3); banned-pattern + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENT.md `model` (tier 3) → resolved model id | An AGENT.md model value flows into model selection; it must be catalog-validated before reaching build_model |
| Resolver → build_model (INV-13) | The resolved id must reach the graph only through build_model (the single source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | AGENT.md `model` tier (resolve-time) | mitigate | `ModelResolver.resolve` validates an AgentSpec.model against `ModelCatalog.ids()` (allow-list, Q2); unknown id rejected — Task 1 behavior Test 9 proves it |
| T-06-04 | Tampering / Elevation | INV-3 parity on no-override runs | mitigate | Tier 5 = `session model_id or Haiku`; characterization snapshots asserted byte/semantic-identical (Task 2 acceptance) — a parity break (e.g. dropping session id) would silently change every existing run's model |
| T-06-05 | Elevation | INV-13 deepagents-only | mitigate | Resolved id flows through `AgentContext.model` → `build_model` only (factory.py unchanged); banned-pattern gate green (Task 2 acceptance) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO new packages; no install task in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/unit/test_model_resolver.py -x -q` (precedence, parity, chains, throttle predicate)
- `cd backend && python3.11 -m pytest tests/agents/characterization/ -q` (INV-3 parity, unchanged)
- `cd backend && lint-imports` → 3 kept / 0 broken
- `cd backend && python3.11 -m pytest tests/agents/test_banned_patterns.py -q` (INV-13)
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` (no regression)
</verification>

<success_criteria>
- ModelResolver resolves by the 5-tier precedence (override>step>agent>workflow>global), None tiers skipped, 5 precedence unit tests pass
- No-override + no-manifest-model run resolves to `session model_id or Haiku` = today's exact behavior; characterization snapshots unchanged (INV-3)
- Default tier-descent chains derived (Opus→Sonnet→Haiku, Sonnet→Haiku, Haiku→none)
- `ModelPolicy.max_tokens` does not raise the runtime cap
- ExecutionContext stays import-pure; resolved id reaches the graph only via build_model (INV-13)
</success_criteria>

<output>
Create `.planning/phases/06-model-policy-1c/06-03-SUMMARY.md` when done.
</output>
