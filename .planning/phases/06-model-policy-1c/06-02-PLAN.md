---
phase: 06-model-policy-1c
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/loader.py
  - backend/tests/agents/test_loader.py
autonomous: true
requirements: [MODEL-01]
must_haves:
  truths:
    - "An AGENT.md with `model: \"<id>\"` loads with AgentSpec.model == \"<id>\""
    - "An AGENT.md without a `model` field loads with AgentSpec.model is None (fully additive)"
    - "A non-string `model` value raises AgentSpecError (per-field validation, like description)"
    - "The loader schema test stays green for all existing ~80 AGENT.md (none declare model)"
  artifacts:
    - path: "backend/agents/loader.py"
      provides: "AgentSpec.model: str | None field + _build_spec optional-string parse"
      contains: "model: str | None"
    - path: "backend/tests/agents/test_loader.py"
      provides: "model-field parse + absent=None + invalid-type cases"
      exports: []
  key_links:
    - from: "backend/agents/loader.py::_build_spec"
      to: "AgentSpec(model=...)"
      via: "metadata.get('model') parse + constructor pass-through"
      pattern: "model=model"
---

<objective>
Add the optional `model` frontmatter field to AGENT.md, parsed onto a new `AgentSpec.model: str | None` attribute (D-09). This backs tier 3 (agent default) of the resolver's precedence (MODEL-01). Fully additive — no existing AGENT.md declares it, so absent → None for all agents.

Purpose: The resolver (06-03) reads `AgentSpec.model` as the agent-default tier. Without this field the agent-default tier has no backing data. This plan supplies the field via the loader's established per-field parse pattern.
Output: `AgentSpec.model` field + loader parse in `agents/loader.py`; new test cases in `tests/agents/test_loader.py`. Loader schema test stays green for all existing agents.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-model-policy-1c/06-SPEC.md
@.planning/phases/06-model-policy-1c/06-CONTEXT.md
@.planning/phases/06-model-policy-1c/06-RESEARCH.md
@.planning/phases/06-model-policy-1c/06-PATTERNS.md
@backend/CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbol (exclude from drift verification — does not exist before this plan):
- `AgentSpec.model: str | None` — new dataclass field in `backend/agents/loader.py::AgentSpec`
- the `model` parse block in `backend/agents/loader.py::_build_spec`
- new test cases in `backend/tests/agents/test_loader.py` (test_model_field_parses, test_model_absent_is_none, test_model_invalid_type_raises)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AgentSpec.model field + loader parse + tests</name>
  <files>backend/agents/loader.py, backend/tests/agents/test_loader.py</files>
  <read_first>
    - backend/agents/loader.py:65-103 — the `AgentSpec` dataclass (add `model` after `injects` at line 102)
    - backend/agents/loader.py:229,325-335,356-374 — `_build_spec`, the `description` optional-string guard (the exact template), and the `AgentSpec(...)` constructor call where `model=model` is passed
    - .planning/phases/06-model-policy-1c/06-PATTERNS.md § "agents/loader.py" + § "Shared Patterns → Optional frontmatter field" — the verbatim `description` guard and the 3-step add (field, parse, constructor)
    - .planning/phases/06-model-policy-1c/06-RESEARCH.md § "MUST RESOLVE #8 (D-09)" — the parse pattern + load-time-vs-resolve-time validation note (this plan defers catalog validation to RESOLVE time in 06-03 to keep the loader decoupled; the loader only enforces the type guard here)
    - backend/tests/agents/test_loader.py — the existing schema test that loads every AGENT.md (must stay green)
  </read_first>
  <behavior>
    - Test 1 (parse): a synthetic/temp AGENT.md (or metadata dict) with `model: "eu.anthropic.claude-sonnet-4-6"` loads with AgentSpec.model == that id
    - Test 2 (absent): an AGENT.md without a `model` key loads with AgentSpec.model is None
    - Test 3 (invalid type): `model: 123` (non-string, non-null) raises AgentSpecError with a message naming the field
    - Test 4 (schema green): the existing all-agents loader schema test still passes (absent=None for all ~80 agents)
  </behavior>
  <action>
    In `agents/loader.py::AgentSpec` (after `injects`, ~line 102) add `model: str | None = None` with a comment "optional AGENT.md model id (D-09); absent → None". In `_build_spec` (alongside the `description` guard at lines 325-335) add the parse: `raw_model = metadata.get("model")`; if `raw_model is not None and not isinstance(raw_model, str)` raise `AgentSpecError(f"Invalid field 'model' in {file_path_str}: expected a string or null, got {type(raw_model).__name__!r}")`; else `model: str | None = raw_model`. Pass `model=model` to the `AgentSpec(...)` constructor call (lines 356-374). Do NOT validate `model` against the catalog here — defer catalog membership validation to RESOLVE time in the resolver (06-03) to keep the loader decoupled from the catalog (D-09 allows either; this phase chooses resolve-time so the loader gains no catalog import and the 80-agent schema test is unaffected by catalog contents). Add the 3 new test cases to `tests/agents/test_loader.py` (parse, absent=None, invalid-type-raises), mirroring how existing optional-field tests construct their inputs.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/agents/loader.py` source assertion: `grep -n "model: str | None" backend/agents/loader.py` shows the new AgentSpec field
    - `cd backend && python3.11 -m pytest tests/agents/test_loader.py -x` exits 0 (new cases + existing schema test for all agents)
    - Behavior: a metadata dict with `model: "eu.anthropic.claude-sonnet-4-6"` produces `AgentSpec.model == "eu.anthropic.claude-sonnet-4-6"`; a dict without `model` produces `AgentSpec.model is None`
    - Behavior: `model: 123` raises `AgentSpecError`
    - Loader schema test stays green for all existing AGENT.md (none declare `model` → all None)
  </acceptance_criteria>
  <done>AgentSpec.model parses from optional `model:` frontmatter (absent → None, non-string → AgentSpecError); all loader tests green including the all-agents schema test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AGENT.md frontmatter → AgentSpec | AGENT.md `model` is untrusted-ish config; a bad value must not silently flow into model selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | AGENT.md `model` field | mitigate | Loader enforces a type guard (string-or-null → else AgentSpecError); catalog-membership validation of an AGENT.md `model` is enforced at RESOLVE time in 06-03 against `ModelCatalog.ids()` (allow-list, Q2) — an arbitrary AGENT.md model string cannot reach the graph unvalidated |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO new packages; no install task in this plan |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_loader.py -x -q`
- `cd backend && python3.11 -m pytest tests/agents/ tests/unit/ -q` (no regression; schema test green for all agents)
- `cd backend && lint-imports` → 3 kept / 0 broken (loader gains no forbidden import)
</verification>

<success_criteria>
- `AgentSpec.model` parses from optional AGENT.md `model:` (absent → None)
- Non-string `model` raises AgentSpecError
- Loader schema test green for all existing ~80 agents
- import-linter stays 3-kept / 0-broken
</success_criteria>

<output>
Create `.planning/phases/06-model-policy-1c/06-02-SUMMARY.md` when done.
</output>
