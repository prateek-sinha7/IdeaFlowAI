---
phase: 28-chat-contracts-guards-a0
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md
  - .planning/phases/28-chat-contracts-guards-a0/contracts/RESOLVED-DECISIONS.md
autonomous: true
requirements: [CHAT-06]
must_haves:
  truths:
    - "The mockWs chat-driver contract defines the new e2e driver's API against the existing mockWs.ts frame shape { type, data: { …, event_id, seq } }"
    - "The driver contract enumerates the chat frames it must emit (chat_message echo, chat_reply cards, stream_attached handshake) and preserves the 123 existing mocked specs"
    - "The resolved-decisions record transcribes LOCK-A..G and ND-1..ND-13 with each ND marked RESOLVED or DEFERRED per the AUTONOMOUS-RUN DECISION LOCK"
    - "The record states transport is ADDITIVE-ONLY (LOCK-B), Concierge always-on (ND-2), and the LOCK-E deferrals (team-sharing, resume-from-failed, prompt-override, image-persistence)"
  artifacts:
    - path: ".planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md"
      provides: "e2e chat-driver API + frame shape + chat frame catalogue"
      contains: "event_id"
    - path: ".planning/phases/28-chat-contracts-guards-a0/contracts/RESOLVED-DECISIONS.md"
      provides: "LOCK-A..G + ND-1..ND-13 resolved-decisions record"
      contains: "LOCK-B"
  key_links:
    - from: "MOCKWS-CHAT-DRIVER-CONTRACT.md"
      to: "frontend/e2e/fixtures/mockWs.ts"
      via: "extends the same { type, data:{…, event_id, seq } } frame contract"
      pattern: "event_id"
    - from: "RESOLVED-DECISIONS.md"
      to: "POR §3 AUTONOMOUS-RUN DECISION LOCK (LOCK-A..G)"
      via: "one row per ND-1..ND-13 with disposition"
      pattern: "LOCK-[A-G]"
---

<objective>
Pin the last two Phase 28 [A0] contract deliverables: the e2e mockWs chat-driver contract (the test-harness surface phases 31/32 build their chat specs against) and the resolved-decisions record (LOCK-A..G + ND-1..ND-13 transcribed as DECIDED, so the unattended auto-chain never re-opens them).

Documents only — no code. Both are transcribed from the locked POR + the existing `mockWs.ts` frame contract (do NOT re-derive, do NOT re-open any ND — the AUTONOMOUS-RUN DECISION LOCK resolved every one).

Purpose: Phase 31/32's e2e work (RUNUI-05: "mockWs chat driver in use") needs the driver's frame contract pinned before FE specs are written; the decision record is the single artifact later phases cite instead of re-litigating ND-1..ND-13.
Output: `contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md`, `contracts/RESOLVED-DECISIONS.md` under the phase directory.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-chat-contracts-guards-a0/28-CONTEXT.md

# Authoritative sources to TRANSCRIBE (do NOT re-derive or re-open):
# - POR §3 (the ⭐ AUTONOMOUS-RUN DECISION LOCK: LOCK-A..G) + ND-1..ND-13 bodies + §6 (contract list):
#   .planning/CHAT-AND-UI-CONVERGENCE-PLAN.md
# - Evidence 11 §B (canonical shared-surface spec) for the ND-13 cosmetic conflict defaults:
#   .planning/v2.0-evidence/11-cross-mock-reconciliation.md

# The existing e2e driver whose frame contract the new chat driver EXTENDS (read the header
# INBOUND FRAME CONTRACT block fully — the new driver must match { type, data:{…, event_id, seq } }):
@frontend/e2e/fixtures/mockWs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the mockWs e2e chat-driver contract</name>
  <files>.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md</files>
  <action>
Read `frontend/e2e/fixtures/mockWs.ts` fully (esp. the header INBOUND FRAME CONTRACT and the existing emit wrapper `{ type, data: { ...payload, event_id, seq } }`). Write `MOCKWS-CHAT-DRIVER-CONTRACT.md` specifying the NEW chat driver as an extension of the existing `MockWs` controller (not a rewrite), matching its exact frame shape. Define:
  - The frame envelope every chat frame uses: `{ type, data: { …payload, event_id, seq } }` (dedup + resume cursor read `data.event_id` string + `data.seq` number — same as today).
  - The chat frame catalogue the driver must be able to emit, keyed to the three new event types (POR D-01): `chat_message` (the user turn echo — payload incl. client `message_id`, text, attachments[]), `chat_reply` (narrator result cards for clarify/gate/pipeline/deliverable milestones with the deep-link nonce seam), and the `stream_attached` handshake (`{ live, replayed_through_seq }`) that replaces `pipeline_reconnected`.
  - Driver methods (mirroring the existing `MockWs` shape): a way to script an inbound `chat_reply` card, assert an outbound `POST`-shaped chat command was sent (Phase 29 REST-up is additive per LOCK-B — but under mocked e2e the app still drives frames through the same controller), and a `stream_attached` replay helper. Note the ADDITIVE-ONLY transport constraint (LOCK-B): the driver keeps the existing `/ws/chat` frame path working AND models the new chat frames — do NOT remove the WS frame contract.
  - Preservation constraint: the 123 existing mocked specs must stay green — the chat driver is additive to `MockWs`, reusing its seq/event_id counters and connection tracking. Cite RUNUI-05 as the consumer.
Prose + tables/method-signature descriptions only — no fenced TypeScript implementation (this is a contract, Phase 31 implements it).
  </action>
  <verify>
    <automated>F=.planning/phases/28-chat-contracts-guards-a0/contracts/MOCKWS-CHAT-DRIVER-CONTRACT.md; test -f "$F" && for tok in chat_message chat_reply stream_attached event_id seq LOCK-B RUNUI-05; do grep -q "$tok" "$F" || { echo "MISSING $tok"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>`MOCKWS-CHAT-DRIVER-CONTRACT.md` exists; defines the chat driver as an additive extension of `MockWs` using the `{ type, data:{…, event_id, seq } }` envelope; enumerates the three chat frame types with payload shapes; records the LOCK-B additive-only constraint and the 123-spec preservation requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Write the resolved-decisions record (LOCK-A..G + ND-1..ND-13)</name>
  <files>.planning/phases/28-chat-contracts-guards-a0/contracts/RESOLVED-DECISIONS.md</files>
  <action>
Write `RESOLVED-DECISIONS.md` transcribing the POR §3 AUTONOMOUS-RUN DECISION LOCK. Two sections:
  (1) LOCK-A..G verbatim-in-substance: LOCK-A (v2.0 execution proceeds; v1.0 stays verifying), LOCK-B (transport ADDITIVE-ONLY this milestone — build SSE+REST ALONGSIDE `/ws/chat` behind a flag; NO deletion, NO ratchets, do NOT touch `useWebSocket.ts`; deletion is a deferred supervised follow-up), LOCK-C (no human checkpoints; mode yolo + skip_discuss; routine gates auto-proceed), LOCK-D (full power Opus/max-effort on substantive agents; checkers on Sonnet), LOCK-E (DEFER: team-sharing, resume-from-failed-step, per-agent prompt-override persistence, image-persistence-on-reopen; pre-launch chat client-side only; Concierge always-on), LOCK-F (output name "Deliverable"; DS picker real ~14 count; P22 display_name deliverable-type names; the 5 cosmetic cross-mock defaults), LOCK-G (live-Bedrock phases finish offline + mark "live check pending"; phases sequential; genuine blocker parks).
  (2) A ND-1..ND-13 table: one row per ND with columns ND | Decision (RESOLVED/DEFERRED) | Disposition source. Fill from the POR: ND-1 pre-launch chat = client-side only (RESOLVED); ND-2 Concierge always-on, Haiku default (RESOLVED); ND-3 legacy free-chat retirement = record now, execute post-milestone (RESOLVED-deferred); ND-4 resume-from-failed = DEFERRED (LOCK-E); ND-5 Share = DEFERRED; ND-6 audit export CSV/JSON in scope, PDF deferred; ND-7 prompt-override persistence = DEFERRED (LOCK-E); ND-8 DS catalog scale = real ~14 count, product decision deferred; ND-9 steering-state across resume = addressed in Phase 29 design; ND-10 image persistence = DEFERRED, render "image not retained" placeholder (LOCK-E); ND-11 unify consume-once seams + thread-id policy = Phase 29 first design task; ND-12 sibling-screen scope = per the second-teardown lock (Login/Admin→35, Composer/Wizard→37, Handoff deferred); ND-13 the seven cross-mock conflicts = reconciliation defaults (running=blue, Not-run=grey, nav=purple-underline, avatar=rounded-square, radius buttons 10/cards 14, output="Deliverable", per-type roster=real manifests). Header: these are DECIDED — Phase 28 only FORMALIZES them; do NOT re-open (POR §3). No fenced code blocks.
  </action>
  <verify>
    <automated>F=.planning/phases/28-chat-contracts-guards-a0/contracts/RESOLVED-DECISIONS.md; test -f "$F" && for tok in LOCK-A LOCK-B LOCK-C LOCK-D LOCK-E LOCK-F LOCK-G ND-1 ND-13 Deliverable additive; do grep -q "$tok" "$F" || { echo "MISSING $tok"; exit 1; }; done && n=$(grep -oE "ND-1?[0-9]" "$F" | sort -u | wc -l | tr -d ' '); test "$n" -ge 13 || { echo "expected >=13 distinct ND rows, got $n"; exit 1; } && echo OK</automated>
  </verify>
  <done>`RESOLVED-DECISIONS.md` exists with LOCK-A..G transcribed and a ND-1..ND-13 disposition table (≥13 distinct ND ids, each RESOLVED or DEFERRED); states transport ADDITIVE-ONLY, Concierge always-on, the LOCK-E deferrals, and output name "Deliverable".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only plan producing two contract `.md` files. No runtime surface, endpoint, or input parser; one read-only pass over `mockWs.ts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering | Driver contract diverges from the live `mockWs.ts` frame shape (would break the 123 mocked specs later) | mitigate | Contract is written by reading `mockWs.ts` first and pinning the exact `{ type, data:{…, event_id, seq } }` envelope + seq/event_id reuse; the additive-extension constraint is stated so Phase 31 cannot rewrite the controller. |
| T-28-06 | Repudiation | A later phase re-opens a locked ND (scope creep / drift) | mitigate | RESOLVED-DECISIONS.md records every ND-1..ND-13 disposition with its POR §3 lock source as the single citable authority; header forbids re-opening. |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — nothing to audit (skip-research; evidence pack is the research). |
</threat_model>

<verification>
- Both contract docs exist under `contracts/`; grep gates confirm the required tokens (chat_message/chat_reply/stream_attached/event_id/seq/LOCK-B/RUNUI-05 in the driver contract; LOCK-A..G + ≥13 distinct ND ids + "Deliverable"/"additive" in the decisions record).
- The mockWs driver contract preserves the existing frame envelope and the additive-only (LOCK-B) transport constraint.
</verification>

<success_criteria>
POR SC-3 (mockWs driver) + SC-4 (ND decision records) satisfied: the e2e chat-driver contract pins the additive driver surface against the live `mockWs.ts` frame shape, and the resolved-decisions record transcribes LOCK-A..G + ND-1..ND-13 as DECIDED. Documents only; no code change; no decision re-opened.
</success_criteria>

<output>
Create `.planning/phases/28-chat-contracts-guards-a0/28-03-SUMMARY.md` when done.
</output>
