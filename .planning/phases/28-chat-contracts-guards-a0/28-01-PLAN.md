---
phase: 28-chat-contracts-guards-a0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/agents/test_phase3_cutover_verify.py
  - backend/tests/agents/characterization/_normalize.py
  - backend/tests/agents/test_chat_event_neutrality.py
autonomous: true
requirements: [CHAT-06]
must_haves:
  truths:
    - "chat_message, chat_reply and stream_attached are members of _DOCUMENTED_EVENT_TYPES (the forward vocabulary guard no longer rejects them)"
    - "The run-specific/client-generated subkeys carried by the three new event types are members of _VOLATILE_STRIP_KEYS"
    - "The 5 characterization goldens (prototype, od_prototype, od_ppt, prototype_revision, app_builder) remain byte-identical and event-identical after the vocabulary/normalizer additions"
    - "A characterization proof test asserts that none of the three chat event types appears in any of the 5 golden event streams"
    - "lint-imports reports 4 contracts / 0 broken"
  artifacts:
    - path: "backend/tests/agents/test_phase3_cutover_verify.py"
      provides: "chat_message/chat_reply/stream_attached added to _DOCUMENTED_EVENT_TYPES with documenting comment"
      contains: "stream_attached"
    - path: "backend/tests/agents/characterization/_normalize.py"
      provides: "new-event volatile subkeys added to _VOLATILE_STRIP_KEYS with documenting comment"
      contains: "_VOLATILE_STRIP_KEYS"
    - path: "backend/tests/agents/test_chat_event_neutrality.py"
      provides: "proof that chat events never fire on the 5 golden pipelines"
      min_lines: 30
  key_links:
    - from: "backend/tests/agents/test_chat_event_neutrality.py"
      to: "backend/tests/agents/characterization/golden/*.events.json"
      via: "reads each golden event stream and asserts the chat event types are absent"
      pattern: "chat_message|chat_reply|stream_attached"
    - from: "backend/tests/agents/test_phase3_cutover_verify.py::TestNewEngineEventVocabulary"
      to: "_DOCUMENTED_EVENT_TYPES"
      via: "subset check of emitted types against the documented vocabulary"
      pattern: "_DOCUMENTED_EVENT_TYPES"
---

<objective>
Pin the chat event vocabulary and prove golden neutrality — the "guards" half of Phase 28 [A0] and the phase's only formal requirement (CHAT-06). Add the three new event types (`chat_message`, `chat_reply`, `stream_attached`) to the documented outbound vocabulary and add their run-specific/client-generated subkeys to the normalizer's volatile-strip set, then add a characterization proof that these events NEVER fire on the 5 golden pipelines so all 5 goldens stay byte/event-identical (INV-3).

This is a contracts/guards phase: no production runtime change beyond additive test-vocabulary and normalizer entries. Per POR D-01 and evidence 03 §7, the scripted golden harness emits none of these chat events, so the vocabulary/normalizer additions are additive and parity-neutral (the same precedent as `image_count`, `redoable`, and the prompt-cache keys already in `_VOLATILE_STRIP_KEYS`).

Purpose: Every later v2.0 phase (29 persists `chat_message`/`chat_reply`; 31/32 render them) needs these types to be legal in the documented contract BEFORE any code emits them, and needs a standing guard that they can never perturb the 5 characterization goldens.
Output: Vocabulary additions in `test_phase3_cutover_verify.py`, volatile-key additions in `_normalize.py`, and a new proof test `test_chat_event_neutrality.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-chat-contracts-guards-a0/28-CONTEXT.md

# The two files being edited (read fully before editing — the existing documenting-comment
# convention for each additive entry MUST be matched):
@backend/tests/agents/test_phase3_cutover_verify.py
@backend/tests/agents/characterization/_normalize.py

# Authoritative facts (do NOT re-derive):
# - POR D-01 + §6 + §7: .planning/CHAT-AND-UI-CONVERGENCE-PLAN.md
# - Evidence 03 §7 (the _VOLATILE_STRIP_KEYS / _DOCUMENTED_EVENT_TYPES contract + the
#   "chat events must never fire on golden paths" landmine): .planning/v2.0-evidence/03-backend-chat-surface-map.md
# The 5 golden event streams the proof reads:
#   backend/tests/agents/characterization/golden/{prototype,od_prototype,od_ppt,prototype_revision,app_builder}.events.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chat event vocabulary + volatile subkeys (additive, comment-documented)</name>
  <files>backend/tests/agents/test_phase3_cutover_verify.py, backend/tests/agents/characterization/_normalize.py</files>
  <action>
Add the three new event types to `_DOCUMENTED_EVENT_TYPES` (the frozenset at `test_phase3_cutover_verify.py:182-227`) per POR D-01/§6: `chat_message`, `chat_reply`, `stream_attached`. Match the existing in-frozenset documenting-comment style (see the "Declared-gate dispatch events" block at lines 206-225): add a `# ── Chat-lane event types (POR D-01, Phase 29+) ─────` comment block explaining that these are ADDITIVE — the chat lane (POST /api/runs/{id}/messages persistence + narrator projections + the SSE stream_attached handshake) lands in Phase 29+, and the scripted golden harness emits NONE of them, so adding them to the documented vocabulary is parity-neutral and only widens the allowed superset checked by TestNewEngineEventVocabulary. Do NOT add any entry to `_REQUIRED_DATA_KEYS` (these types have no load-bearing required keys under the golden harness).

Add the run-specific/client-generated subkeys these three types will carry to `_VOLATILE_STRIP_KEYS` (`_normalize.py:101-170`), following the exact precedent of the `image_count` entry (lines 163-168): a documenting comment noting the strip is belt-and-suspenders because no golden emits these events. Per D-01 the volatile subkeys are: `message_id` (FE-generated idempotency key on `chat_message`) and `replayed_through_seq` (run-specific cursor on the `stream_attached` handshake). Do NOT strip `live` (the `stream_attached` boolean is deterministic, not run-specific) and do NOT strip stable card discriminators on `chat_reply`. Keep both edits additive-only — no existing entry is removed or reordered.
  </action>
  <verify>
    <automated>cd backend && python3.11 -c "from tests.agents.test_phase3_cutover_verify import _DOCUMENTED_EVENT_TYPES as D; from tests.agents.characterization._normalize import _VOLATILE_STRIP_KEYS as V; assert {'chat_message','chat_reply','stream_attached'} <= D, 'missing event types'; assert {'message_id','replayed_through_seq'} <= V, 'missing volatile keys'; print('OK')"</automated>
  </verify>
  <done>`chat_message`, `chat_reply`, `stream_attached` are in `_DOCUMENTED_EVENT_TYPES`; `message_id`, `replayed_through_seq` are in `_VOLATILE_STRIP_KEYS`; `_REQUIRED_DATA_KEYS` unchanged; both additions carry a documenting comment matching the file's existing convention.</done>
</task>

<task type="auto">
  <name>Task 2: Write the golden-neutrality proof test and prove byte/event identity</name>
  <files>backend/tests/agents/test_chat_event_neutrality.py</files>
  <action>
Create `backend/tests/agents/test_chat_event_neutrality.py` proving CHAT-06's golden-neutrality claim (POR §7: "chat events must never fire on golden paths"). The test loads each of the 5 committed golden event streams — `backend/tests/agents/characterization/golden/{prototype,od_prototype,od_ppt,prototype_revision,app_builder}.events.json` — parametrized by pipeline name, and asserts that NONE of `{chat_message, chat_reply, stream_attached}` appears in any golden's event `type` sequence. Resolve the golden dir with `pathlib.Path(__file__).parent / "characterization" / "golden"`. Add a second test asserting `{chat_message, chat_reply, stream_attached}` is a subset of `_DOCUMENTED_EVENT_TYPES` (import from `tests.agents.test_phase3_cutover_verify`) so the two guards are pinned together and a future accidental removal fails here. Give each assertion a message naming the offending pipeline/type. Keep it self-contained (no engine drive, no Bedrock) — it reads committed fixtures only, so it runs in the offline targeted suite.

Then prove INV-3 holds by running the 5 characterization goldens plus the vocabulary forward-guard: the goldens must stay byte-identical and event-identical (the additive vocabulary/normalizer entries change nothing because no golden emits chat events). Do NOT regenerate or overwrite any `golden/*.json` or `golden/*.html` fixture — a passing run against the UNCHANGED fixtures IS the proof.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_chat_event_neutrality.py tests/agents/test_phase3_cutover_verify.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py -q</automated>
  </verify>
  <done>The new proof test passes (all 5 goldens are chat-event-free; the three types are documented); the 5 characterization golden suites + `test_phase3_cutover_verify.py` pass against unchanged fixtures (INV-3 held); no `golden/*` fixture is modified in git status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-vocabulary + normalizer edits and a fixture-reading proof test. No new runtime surface, endpoint, capability, input parser, or trust boundary crosses in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `_DOCUMENTED_EVENT_TYPES` / `_VOLATILE_STRIP_KEYS` widening | mitigate | Additions are strictly additive and scoped to the three D-01 chat types + their two run-specific subkeys; `_REQUIRED_DATA_KEYS` untouched so no load-bearing shape is weakened; the golden suites re-run against unmodified fixtures prove no parity regression. |
| T-28-02 | Repudiation | Golden fixtures silently regenerated to "pass" | mitigate | `done` requires `git status` to show zero `golden/*` fixture changes; a passing run is only valid against the committed fixtures. |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — nothing to audit; RESEARCH Package Legitimacy Gate not applicable (skip-research; evidence pack is the research). |
</threat_model>

<verification>
- `chat_message`, `chat_reply`, `stream_attached` ∈ `_DOCUMENTED_EVENT_TYPES`; `message_id`, `replayed_through_seq` ∈ `_VOLATILE_STRIP_KEYS`; `_REQUIRED_DATA_KEYS` unchanged.
- Targeted parity/gate suite green: `test_chat_event_neutrality.py`, `test_phase3_cutover_verify.py`, and the 5 `test_characterization_*.py` goldens (byte + event identical — INV-3).
- `git status` shows no modification to any `backend/tests/agents/characterization/golden/*` fixture.
- `/opt/homebrew/bin/lint-imports` reports 4 contracts kept / 0 broken (no import graph change expected; run as the standing offline gate).
</verification>

<success_criteria>
CHAT-06 satisfied: the three chat event types are registered in the documented vocabulary, their volatile subkeys are in the strip set, and a characterization proof demonstrates chat events never fire on the 5 golden pipelines with all goldens byte/event-identical. lint-imports 4/0. Additive-only; no kernel edit; no golden fixture regenerated.
</success_criteria>

<output>
Create `.planning/phases/28-chat-contracts-guards-a0/28-01-SUMMARY.md` when done.
</output>
