---
phase: 28-chat-contracts-guards-a0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-chat-contracts-guards-a0/contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md
  - .planning/phases/28-chat-contracts-guards-a0/contracts/FIGURE-TO-FIELD-PINNING.md
  - .planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md
autonomous: true
requirements: [CHAT-06]
must_haves:
  truths:
    - "The Steps-L2 artifact-derivation contract states the exact data source for each of the four artifact kinds (pages, tasks, checks, construction)"
    - "The construction rule records the KAN-99 N-1 checklist cap (final task_progress fires before the fix-loop)"
    - "Every run-screen figure is pinned to an exact producing field, with agent_input.context_sources verified as carrying name+size in code"
    - "The D-12 live-state contract table maps every real run state to chat-lane render + composer mode + Steps behavior, including the post-merge deltas (update_specs gate action, reject-confirm, terminal fence, spec-revision loop-back state, planner window)"
  artifacts:
    - path: ".planning/phases/28-chat-contracts-guards-a0/contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md"
      provides: "pages←route_table, tasks←task_list, checks←validation_results, construction←wave_*/subagent_*/task_progress with the KAN-99 N-1 cap"
      contains: "route_table"
    - path: ".planning/phases/28-chat-contracts-guards-a0/contracts/FIGURE-TO-FIELD-PINNING.md"
      provides: "every displayed number mapped to its producing field, context_sources verified"
      contains: "context_sources"
    - path: ".planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md"
      provides: "D-12 real-state → render/composer/Steps table with post-merge deltas"
      contains: "spec-revision"
  key_links:
    - from: "STEPS-ARTIFACT-DERIVATION-CONTRACT.md (construction kind)"
      to: "backend engine wave_*/subagent_*/task_progress events"
      via: "named event types + KAN-99 N-1 cap note"
      pattern: "task_progress"
    - from: "FIGURE-TO-FIELD-PINNING.md (Context received rail)"
      to: "agent_input.context_sources"
      via: "verified field-name mapping (name+size)"
      pattern: "context_sources"
---

<objective>
Pin the two UI-facing contracts the run-screen phases (31/32) build against — the Steps-L2 artifact-derivation rules (with figure-to-field pinning) and the D-12 live-state contract table — as durable UI-SPEC input documents. These formalize POR D-12 and evidence 07 §1/§3 + evidence 01 §2B/§7 so the frontend phases build against pinned field names, not ad-hoc mock interpretation.

This plan produces contract DOCUMENTS only — no code. Its content is transcribed from the locked POR + evidence pack (do NOT re-derive or re-open decisions). The one verification-in-code step: confirm `agent_input.context_sources` actually carries name+size in the emitted payload (POR SC-2 requires every displayed figure pinned to a real field).

Purpose: Phase 32's Steps 3-level drill-down (RUNUI-03) and the run-screen figures render from these exact sources; pinning them now prevents the mock-fiction regressions §9 warns about (synthesized agent statuses/tokens/versions).
Output: `contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md`, `contracts/FIGURE-TO-FIELD-PINNING.md`, `contracts/LIVE-STATE-CONTRACT.md` under the phase directory.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/28-chat-contracts-guards-a0/28-CONTEXT.md

# Authoritative sources to TRANSCRIBE (do NOT re-derive):
# - POR D-12 (full live-state contract incl. post-merge deltas) + §6: .planning/CHAT-AND-UI-CONVERGENCE-PLAN.md
# - Evidence 07 §1 (D-12 table), §2 (coverage cross-check), §3 (figure-to-field pinning seed list),
#   + the CORRECTIONS block: .planning/v2.0-evidence/07-synthesized-contracts.md
# - Evidence 01 §2B (the four L2 artifact kinds pages/tasks/checks/construction + their data),
#   §2C (L3 task detail), §7 (consolidated data contract): .planning/v2.0-evidence/01-run-ui-teardown.md
# - Evidence 03 §7 (context_sources in _VOLATILE_STRIP_KEYS; agent_input payload):
#   .planning/v2.0-evidence/03-backend-chat-surface-map.md

# Code to READ for the figure-to-field verification step (confirm field names, do not edit):
@backend/app/agents/route_table.py
# grep the engine for the emit sites of: agent_input (context_sources), agent_complete
# (duration/input_tokens/output_tokens/total_tokens/cache split), pipeline_complete (totals),
# task_progress/wave_*/subagent_* — pin the exact key names into FIGURE-TO-FIELD-PINNING.md.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Steps-L2 artifact-derivation contract + figure-to-field pinning</name>
  <files>.planning/phases/28-chat-contracts-guards-a0/contracts/STEPS-ARTIFACT-DERIVATION-CONTRACT.md, .planning/phases/28-chat-contracts-guards-a0/contracts/FIGURE-TO-FIELD-PINNING.md</files>
  <action>
Write `STEPS-ARTIFACT-DERIVATION-CONTRACT.md`: for each of the four Steps-L2 artifact kinds (evidence 01 §2B.2), state the exact backend data source (per POR D-12 / CONTEXT decisions):
  - `pages` ← the prototype route table from `backend/app/agents/route_table.py` (name each field the table exposes that maps to the mock's `pages[]` of page names);
  - `tasks` ← the `task_list` artifact (the typed artifact-graph kind; name it);
  - `checks` ← `validation_results` (the governance verdict + rows; the green-only status-palette exception);
  - `construction` ← DUAL SOURCE: `task_progress` (task-loop) AND `subagent_*`/`wave_*` (fanout) events, per evidence 07 §2 (Steps L3 dual source). Record the KAN-99 contract explicitly: the final `task_progress` fires BEFORE the build agent's fix-loop, so the checklist caps at N-1 until the build agent truly finishes — cite KAN-99. Include the L3 per-task shape (n, title, status, dur, thinking, tools[]) from evidence 01 §2C/§7.
State it as a table (kind → source event/artifact/module → field(s) → notes) so Phase 32 can wire directly. Add a header noting this is a Phase 28 UI-SPEC input for RUNUI-03 (Steps drill-down), authority = POR D-12 + evidence 01/07 (mock is visual reference only).

Write `FIGURE-TO-FIELD-PINNING.md`: transcribe evidence 07 §3's seed list and COMPLETE it by grepping the engine emit sites — pin every number the run screens show to an exact producing field: per-agent duration/tokens → `agent_complete` payload keys; run totals + cache split → `pipeline_complete` totals / `workflow_runs.token_usage` (P26); est. cost → `estimated_cost_usd`; context-source sizes → `agent_input.context_sources`; cache −% → cache telemetry + compaction ratios; version numbers → `/family` `revision_index`. VERIFY IN CODE that `agent_input.context_sources` carries name+size (read the engine emit site; note it is stripped from goldens per `_VOLATILE_STRIP_KEYS` but is present on the live wire) and record the exact sub-field names for name and size. Mark any figure whose field you could not confirm in code as UNVERIFIED so Phase 32 chases it. No fenced code blocks required — prose + tables.
  </action>
  <verify>
    <automated>D=.planning/phases/28-chat-contracts-guards-a0/contracts; test -f "$D/STEPS-ARTIFACT-DERIVATION-CONTRACT.md" && test -f "$D/FIGURE-TO-FIELD-PINNING.md" && for tok in route_table task_list validation_results task_progress wave_ subagent_ KAN-99; do grep -q "$tok" "$D/STEPS-ARTIFACT-DERIVATION-CONTRACT.md" || { echo "MISSING $tok in derivation contract"; exit 1; }; done && for tok in context_sources agent_complete pipeline_complete estimated_cost_usd revision_index; do grep -q "$tok" "$D/FIGURE-TO-FIELD-PINNING.md" || { echo "MISSING $tok in pinning"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Both docs exist; the derivation contract names all four kinds with their exact sources and the KAN-99 N-1 cap; the pinning doc maps every seed figure to a field and records the verified `context_sources` name+size sub-fields (or marks them UNVERIFIED).</done>
</task>

<task type="auto">
  <name>Task 2: Write the D-12 live-state contract table (UI-SPEC input)</name>
  <files>.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md</files>
  <action>
Write `LIVE-STATE-CONTRACT.md` transcribing POR D-12 + evidence 07 §1 (the full table) INCLUDING the CORRECTIONS/post-merge deltas — do not summarize away any row. Columns: Real state (driving event/signal) | Chat lane renders | Composer mode | Steps behavior. Cover every state from evidence 07 §1: planner-running (pre-clarify), clarify-waiting (`questionnaire_ready`), gate-paused (`review_gate_ready`), building (`agent_start`/`agent_chunk`/`agent_thinking`), validation/fix-loop, complete (`pipeline_complete`), degraded, failed (`pipeline_failed`), cancelled (`pipeline_cancelled`), revision-running (child run). Then add the post-merge deltas as explicit rows/notes:
  (a) gate-paused gains the FOURTH action **Update the Specs** (analyze gates, KAN-101) + the reject-confirmation dialog + navigate-home-on-reject (KAN-95) + the terminal fence (gate card auto-dismissed on cancel/fail; `approve_review` → `pipeline_not_running`, KAN-100);
  (b) the **spec-revision loop-back state** (`update_specs` re-runs specify→plan→analyze in-run, Building-like via re-fired `agent_start` on already-done agents, returns to the SAME gate — narrator "Revising spec — cycle N"); record the terminology rule: "spec-revision loop" (intra-run KAN-101) ≠ "revision run" (family child, D-02) — never conflate;
  (c) the **planner window** between questionnaire-submit and `pipeline_start` as a named transient (KAN-97);
  (d) the KAN-99 N-1 checklist cap on the Building row;
  (e) gates are event-driven (KAN-94 — a declared gate may not fire when `gate_agent_ids` excludes the agent).
Note the excluded mock devices: the Clarify/Gate/Building segmented scrubber is a demo device (replaced by a real phase indicator); the transcript ACCUMULATES across states and revisions (the mock swaps content per phase — the product must not). Header: Phase 28 UI-SPEC input for phases 31/32; authority = POR D-12 + evidence 07 (D-15 rule iii: product behavior is truth).
  </action>
  <verify>
    <automated>F=.planning/phases/28-chat-contracts-guards-a0/contracts/LIVE-STATE-CONTRACT.md; test -f "$F" && for tok in questionnaire_ready review_gate_ready pipeline_complete pipeline_failed "Update the Specs" spec-revision KAN-100 KAN-99 KAN-94 KAN-97; do grep -q "$tok" "$F" || { echo "MISSING $tok"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>`LIVE-STATE-CONTRACT.md` exists with a four-column table covering all D-12 states plus the five post-merge deltas (update_specs, spec-revision loop-back, planner window, N-1 cap, event-driven gates) and the terminology rule distinguishing the intra-run loop from the family revision run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only plan producing three contract `.md` files. No runtime surface, endpoint, or input parser. The one code interaction is READ-ONLY field verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Information Disclosure | UI figures pinned to wrong/synthesized source (mock-fiction regression, §9) | mitigate | Figure-to-field pinning requires an in-code field verification for `context_sources`; unconfirmed figures are marked UNVERIFIED rather than asserted, so Phase 32 never wires a fabricated number. |
| T-28-04 | Tampering | Contract drifts from the locked POR (re-derivation) | mitigate | Docs are transcribed verbatim from POR D-12 + evidence 07/01 with post-merge CORRECTIONS folded in; the terminology rule (loop ≠ revision run) prevents the highest-risk conflation. |
</threat_model>

<verification>
- Three contract docs exist under `contracts/`; grep gates confirm the required source tokens (route_table, task_list, validation_results, task_progress, wave_, subagent_, KAN-99) and figure fields (context_sources, agent_complete, pipeline_complete, estimated_cost_usd, revision_index) are present.
- `LIVE-STATE-CONTRACT.md` covers every D-12 state + the five post-merge deltas.
- `agent_input.context_sources` name+size sub-fields are verified against the engine emit site (or explicitly marked UNVERIFIED).
</verification>

<success_criteria>
POR SC-2 + SC-3 (partial) satisfied: the Steps-L2 artifact-derivation rules (four kinds → exact sources, KAN-99 cap), the figure-to-field pinning (every number → producing field, context_sources verified), and the D-12 live-state contract table (all states + post-merge deltas) exist as UI-SPEC inputs for phases 31/32. Documents only; no code change; no decision re-opened.
</success_criteria>

<output>
Create `.planning/phases/28-chat-contracts-guards-a0/28-02-SUMMARY.md` when done.
</output>
