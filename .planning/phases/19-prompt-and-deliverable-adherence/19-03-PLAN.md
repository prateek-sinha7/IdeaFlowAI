---
phase: 19-prompt-and-deliverable-adherence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/execution_engine/engine.py
  - backend/tests/agents/test_chunk_sanitizer.py
autonomous: true
requirements: [ISS-004]
must_haves:
  truths:
    - "The engine routes each streamed chunk through the runner's existing `_strip_fabricated_tool_xml` (via the runner's sanitize_output capability) before yielding the `agent_chunk` event — but ONLY for tool-less agents."
    - "A chunk-straddle streaming buffer holds a chunk's tail when an unterminated <function_calls>/<invoke opener is seen, and flushes on close or at stream end — so tool-XML split across two chunk deltas is still stripped."
    - "A tool-USING agent's identical stream is untouched (same chunks pass through verbatim)."
    - "The 5 characterization goldens stay byte/event-identical (goldens normalize chunk text to a sentinel + pin only post-sanitized output)."
    - "SC-001: the sanitizer keys on the generic tool-less condition, never a workflow/agent name; lint-imports 4/0; zero migrations."
  artifacts:
    - path: "backend/agents/execution_engine/engine.py"
      provides: "Chunk-stream sanitizer branch at the agent_chunk yield (~lines 2423-2425) gated on the tool-less runner capability, with a span-straddle buffer; flush-tail logic on close + at stream end."
      contains: "agent_chunk"
    - path: "backend/tests/agents/test_chunk_sanitizer.py"
      provides: "Fault-injection: a scripted model streams <function_calls> SPLIT across two chunk deltas for a tool-less agent → no <function_calls> in emitted agent_chunk; a tool-using agent's identical stream untouched."
      contains: "function_calls"
  key_links:
    - from: "backend/agents/execution_engine/engine.py (agent_chunk yield)"
      to: "DeepAgentRunner.sanitize_output / _strip_fabricated_tool_xml"
      via: "duck-typed call on the runner's sanitize capability, gated on the tool-less condition (no app import, no agent-name branch)"
      pattern: "sanitize"
    - from: "the chunk-straddle buffer"
      to: "the agent_chunk event payload"
      via: "hold the tail on an unterminated opener, flush on close/stream-end"
      pattern: "function_calls|invoke"
---

<objective>
Close ISS-004 (sdlc-governance family fabricates tool-XML in the live UI stream) with the deterministic backstop. The 13-02 sanitizer (`_strip_fabricated_tool_xml`) is applied to the AUTHORITATIVE output (the chunk-joined output via `sanitize_output()` at engine.py:~2645) but the engine YIELDS each raw `chunk` as an `agent_chunk` event at engine.py:2423-2425 BEFORE accumulation/sanitization — so fabricated `<function_calls>`/`<invoke name="read_file">` reaches the live UI stream (downstream context + the persisted deliverable + `final_output` are already clean). Route each chunk through the SAME sanitizer before yielding the `agent_chunk`, gated on the agent being tool-less, WITH a chunk-straddle streaming buffer (tool-XML can split across two chunk deltas → a naive per-chunk regex misses it).

Purpose: the fabricated tool-XML must never reach the live UI stream, not just the accumulated output — a single sanitization locus, on the backend, for the durable-replay collector + the reconnect tail too. The prompt fix (P15) defused the trigger; this is the deterministic belt.
Output: a chunk-sanitizer branch + a span-straddle buffer in engine.py's `_run_agent` stream loop + a fault-injection test.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-prompt-and-deliverable-adherence/19-CONTEXT.md
@backend/app/agents/deep_agent_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the tool-less chunk sanitizer + span-straddle buffer at the agent_chunk yield</name>
  <files>backend/agents/execution_engine/engine.py</files>
  <read_first>
    - .planning/phases/19-prompt-and-deliverable-adherence/19-CONTEXT.md (ISS-004 LOCKED decision — the fix, the span-straddle subtlety, REJECTED hacks, and the SC-001 / INV-3 rationale)
    - backend/app/agents/deep_agent_runner.py (lines 129-151 `_strip_fabricated_tool_xml` — returns the SAME object when the pattern is absent → no-op on clean chunks; lines 534-547 `sanitize_output()` — the public duck-typed method the engine already calls; line 278 `self._sanitize_fabricated_xml = exclude_builtin_tools and not self.tools` — the tool-less signal; the docstring explicitly notes the chunk stream is intentionally unfiltered — that is the gap this plan closes)
    - backend/agents/execution_engine/engine.py (lines 2410-2430 — the stream loop; line 2424 `output_chunks.append(event["chunk"])` + line 2425 the `yield {"type":"agent_chunk", ...}` — the EDIT SITE; lines 2638-2650 — the existing `sanitize_output` duck-typed call on the chunk-joined output, the precedent for the duck-typed pattern + the SC-001 wording)
  </read_first>
  <behavior>
    - For a tool-less agent (the runner reports the tool-less/sanitize capability — duck-typed, e.g. `sanitize_output` is a no-op identity for tool-using agents), a `<function_calls>...</function_calls>` span contained in ONE chunk is stripped before the `agent_chunk` event is yielded.
    - A `<function_calls>` span SPLIT across two consecutive chunk deltas is stripped: when a chunk ends with an UNTERMINATED `<function_calls>`/`<invoke` opener, the buffer holds the tail (does NOT yield it yet); the next chunk is appended; on seeing the close (or at stream end) the joined text is sanitized and the cleaned remainder is yielded. No fabricated XML survives across the boundary.
    - At stream end, any held tail is flushed through the sanitizer (an unterminated opener at EOF is stripped, mirroring `_UNTERMINATED_TOOL_XML_RE`) so the buffer never silently swallows legitimate trailing content.
    - A tool-USING agent's stream is byte-for-byte untouched (the gate is false → chunks pass through verbatim, buffer inert).
    - Clean chunks (no opener) pass through unbuffered and unchanged (same-object identity — no latency, no content change) so the goldens stay byte-identical.
    - The accumulated `output_chunks` used for the authoritative output is unaffected in a way that changes the post-loop `sanitize_output` result (the final output is already sanitized at ~2645; the streamed-chunk sanitizer must not double-strip into corruption — accumulate the RAW chunks for the authoritative path OR the sanitized stream, whichever keeps `output_length`/`final_output` byte-identical to the golden; verify against the golden).
  </behavior>
  <action>
    Edit the stream loop in `_run_agent` (engine.py around lines 2421-2425). Before the loop, derive the tool-less gate ONCE per agent via the runner's duck-typed capability — reuse the SAME duck-typing the post-loop block at ~2645 uses (`getattr(agent, "sanitize_output", None)` callable) PLUS the tool-less signal (the runner's `sanitize_output` is already an identity for tool-using agents — `_sanitize_fabricated_xml` is False — so calling it unconditionally on each chunk is SAFE and self-gating). Keep a small per-agent streaming buffer string (initialized empty) ONLY needed for the span-straddle case.

    On each `chunk` event: append to `output_chunks` for the authoritative path EXACTLY as today (so the post-loop `sanitize_output` at ~2645 still produces the byte-identical golden `final_output`/`output_length` — do NOT change what feeds the authoritative output). For the YIELDED `agent_chunk` value: concatenate any held buffer + the new chunk; if the text contains an UNTERMINATED `<function_calls>`/`<invoke` opener (a `<function_calls>` / `<invoke` with no matching close yet — reuse the runner's existing pattern semantics; do NOT hand-roll a divergent regex, route through `sanitize_output` and detect the unterminated-opener condition with the same `<function_calls>`/`<invoke` tokens the runner uses), HOLD the unterminated tail in the buffer and yield only the safely-sanitized prefix (possibly empty — if empty, yield nothing for this delta). Otherwise run the held+new text through the runner's `sanitize_output` (no-op identity for tool-using agents / clean text → same object) and yield the cleaned chunk, clearing the buffer. At stream end (after the loop), flush any residual buffer through `sanitize_output` and yield a final `agent_chunk` if non-empty (this strips an unterminated opener at EOF).

    SC-001: gate ONLY on the generic tool-less / runner-capability condition — NO workflow name, NO agent-id literal (`sdlc-governance`, `app_builder` must NOT appear in the branch). Mirror the SC-001 comment style already at ~2645. Do NOT strip in the FE, do NOT add a second sanitizer — reuse the runner's single sanitization function (the REJECTED hacks per CONTEXT).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/characterization -q</automated>
  </verify>
  <acceptance_criteria>
    - The `agent_chunk` yield site routes the yielded chunk through the runner's `sanitize_output` (duck-typed) with a span-straddle buffer; the authoritative `output_chunks` path is unchanged.
    - `grep -v '^#' backend/agents/execution_engine/engine.py | grep -cE 'sdlc-governance|app_builder|prototype-' ` in the new branch region shows no workflow/agent-name literal in the sanitizer gate (SC-001) — the gate is the generic tool-less/runner-capability condition only.
    - All 5 characterization goldens are byte/event-identical (NO SNAPSHOT_UPDATE) — goldens normalize chunk text to a sentinel + pin only post-sanitized `final_output`/`output_length`.
    - `lint-imports` 4/0; no new migration; no FE file touched.
  </acceptance_criteria>
  <done>The engine sanitizes the streamed `agent_chunk` for tool-less agents with a working span-straddle buffer; tool-using streams are untouched; the 5 goldens stay byte/event-identical; SC-001 holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fault-injection test — split tool-XML across chunk deltas</name>
  <files>backend/tests/agents/test_chunk_sanitizer.py</files>
  <read_first>
    - .planning/phases/19-prompt-and-deliverable-adherence/19-CONTEXT.md (ISS-004 — the fault-injection: a scripted model streams <function_calls> SPLIT across two chunk deltas for a tool-less agent → no <function_calls> in the emitted agent_chunk sequence; a tool-USING agent's identical stream untouched)
    - backend/tests/agents/_scripted_model.py (the ScriptedFakeChatModel / _ScriptedTurn / _scripts_for harness — drive a scripted stream whose AIMessageChunks split `<function_calls>` across two deltas; the scripted-model recipe is also in backend/CLAUDE.md "Test recipe notes")
    - backend/app/agents/deep_agent_runner.py (the tool-less vs tool-using construction — exclude_builtin_tools=True + zero tools → sanitize active; tools present → identity)
    - backend/agents/execution_engine/engine.py (the edited _run_agent stream loop — the unit under test; drive it via the same harness the characterization/live_harness tests use)
  </read_first>
  <behavior>
    - Tool-less agent, scripted stream emits `<function`+`_calls><invoke name="read_file">...</invoke></function_calls>` SPLIT so the opener and close land in DIFFERENT chunk deltas → the collected sequence of emitted `agent_chunk.chunk` values contains NO `<function_calls>` and NO `<invoke` substring (assert over the joined emitted stream AND that no single emitted chunk leaks a partial opener that is never closed).
    - Tool-USING agent (workspace tools), identical scripted stream → the emitted `agent_chunk` sequence is byte-identical to the scripted input (the sanitizer gate is false; nothing stripped).
    - Single-chunk case (whole `<function_calls>...</function_calls>` in one delta, tool-less) → stripped (covers the non-straddle path).
    - Clean stream (no XML) → emitted chunks identical to input (no-op).
  </behavior>
  <action>
    Write `backend/tests/agents/test_chunk_sanitizer.py` driving the engine's `_run_agent` stream through a `ScriptedFakeChatModel` (model the harness on `tests/agents/characterization` / `live_harness.drive_engine_pipeline` / `_scripted_model`). Author a scripted turn whose deltas SPLIT `<function_calls>` across two chunks (e.g. delta 1 ends with `...here is the result <function_calls><invoke name="read_`, delta 2 starts with `file">...</invoke></function_calls> done`). Collect the emitted `agent_chunk` events and assert the joined chunk text contains no `<function_calls>`/`<invoke` for the tool-less agent. Add the mirror case for a tool-using agent (assert the emitted chunks equal the scripted input). Add the single-chunk and clean-stream cases. Keep it offline/deterministic (scripted model, tmp `RUNS_ROOT`, no Bedrock).

    Then re-confirm INV-3: run the 5 goldens once more without snapshot-update; `lint-imports` 4/0; zero migrations. Record in the SUMMARY.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_chunk_sanitizer.py -q</automated>
  </verify>
  <acceptance_criteria>
    - SPLIT-across-deltas case (tool-less): joined emitted `agent_chunk` text contains no `<function_calls>` and no `<invoke` — the span-straddle buffer works.
    - Tool-using agent, identical stream: emitted chunks byte-identical to the scripted input (untouched).
    - Single-chunk + clean-stream cases pass (non-straddle + no-op coverage).
    - INV-3 re-confirmed: 5 goldens byte/event-identical (NO SNAPSHOT_UPDATE); lint-imports 4/0; zero migrations.
  </acceptance_criteria>
  <done>The split-chunk fault-injection proves no fabricated tool-XML reaches the emitted stream for tool-less agents while tool-using streams are untouched; INV-3 re-confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model token stream → engine agent_chunk yield → live UI stream | The model's raw token chunks cross into the UI stream here. Fabricated tool-XML is the untrusted content the sanitizer removes; the sanitizer must not also remove legitimate content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | chunk sanitizer over-stripping legitimate output | mitigate | Reuse the runner's EXISTING `_strip_fabricated_tool_xml` (same-object identity on clean text, no divergent regex); the span-straddle buffer flushes the held tail at stream end so legitimate trailing content is never silently swallowed; the clean-stream + tool-using test cases assert zero over-stripping. |
| T-19-03-02 | Information Disclosure | fabricated tool-XML leaking to the UI / durable-replay collector | mitigate | Sanitize at the single backend locus (the agent_chunk yield) so the live stream, the durable-replay collector, and the reconnect tail all see cleaned chunks — closing the gap the WR-01 note left open. |
| T-19-03-03 | Denial of Service | the span-straddle buffer growing unbounded | mitigate | The buffer only holds the tail FROM an unterminated opener to stream end; it reuses the runner's linear single-pass regex (no nested quantifiers / catastrophic backtracking); on a never-closed opener it flushes (strips) at stream end — bounded by one agent's output. |
| T-19-03-04 | Repudiation | INV-3 golden parity | mitigate | The streamed-chunk sanitizer must not perturb the byte-goldens (they normalize chunk text + pin only the already-sanitized final_output); proven by running all 5 goldens with NO snapshot-update. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs (engine edit + one test file). |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_chunk_sanitizer.py -q` — split-chunk + tool-using + single-chunk + clean cases green.
- `cd backend && python3.11 -m pytest tests/agents/characterization -q` — all 5 goldens byte/event-identical (NO SNAPSHOT_UPDATE).
- `lint-imports` (at /opt/homebrew/bin/lint-imports) — 4 kept / 0 broken.
- `git status` — no new migration; no FE file changed; only the 2 declared files touched.
</verification>

<success_criteria>
- ROADMAP SC3 met: the engine sanitizes the streamed `agent_chunk` (reusing `_strip_fabricated_tool_xml` with a chunk-straddle buffer) for tool-less agents so fabricated XML is removed from the live UI stream, proven by a split-chunk fault-injection test; a tool-using agent's identical stream is untouched; SC-001 (keys on the generic tool-less condition, never a workflow name).
- INV-3 parity proven (5 goldens byte/event-identical — goldens normalize chunk text + pin only post-sanitized output; lint-imports 4/0; zero migrations).
</success_criteria>

<deferred>
- LIVE re-confirm on `default` Bedrock: a real Haiku sdlc-governance run (app_builder + dotnet + mulesoft) shows 0 fabricated tool-XML in the sdlc-governance chunks in the live UI stream. Runs in the consolidated live + Playwright pass — NOT a blocker (the split-chunk fault-injection + golden parity are the phase-exit gate).
</deferred>

<output>
Create `.planning/phases/19-prompt-and-deliverable-adherence/19-03-SUMMARY.md` when done.
</output>
