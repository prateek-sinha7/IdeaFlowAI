---
phase: 19-prompt-and-deliverable-adherence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/prompts/app-code-generator/AGENT.md
  - backend/agents/prompts/app-test-implementation/AGENT.md
  - backend/tests/agents/test_prompt_contracts.py
autonomous: true
requirements: [ISS-006]
must_haves:
  truths:
    - "The app_builder producer (app-code-generator) and consumer (app-test-implementation) AGENT.md bodies share the single canonical DB-accessor literal `getDatabase`."
    - "The consumer's Contract-fidelity rule explicitly covers the Jest/Vitest global-setup file (tests/setup.ts / configured globalSetup), demanding it import the EXACT canonical named export."
    - "An offline acceptance-grep test pins both sides; reverting either side fails the pin."
    - "The 5 characterization goldens stay byte/event-identical (scripted model ignores prompt bodies) and lint-imports stays 4/0 with zero migrations."
  artifacts:
    - path: "backend/agents/prompts/app-code-generator/AGENT.md"
      provides: "Producer export contract: the DB module exposes the accessor as the single canonical NAMED export getDatabase at a canonical path."
      contains: "getDatabase"
    - path: "backend/agents/prompts/app-test-implementation/AGENT.md"
      provides: "Consumer Contract-fidelity rule extended to the global-setup file, demanding the exact getDatabase named import."
      contains: "getDatabase"
    - path: "backend/tests/agents/test_prompt_contracts.py"
      provides: "New acceptance-grep pin asserting the shared literal + global-setup contract on both bodies."
      contains: "getDatabase"
  key_links:
    - from: "backend/agents/prompts/app-code-generator/AGENT.md"
      to: "backend/agents/prompts/app-test-implementation/AGENT.md"
      via: "shared canonical literal getDatabase (13-03 shared-literal pattern)"
      pattern: "getDatabase"
    - from: "backend/tests/agents/test_prompt_contracts.py"
      to: "load_agent_spec(id).prompt_body"
      via: "grep pin over the parsed prompt body (test_guardrails precedent)"
      pattern: "load_agent_spec\\(.+\\)\\.prompt_body"
---

<objective>
Close ISS-006 (app_builder cross-agent contract drift) durably with the 13-03 shared-literal pattern. The producer `app-code-generator` emits the DB module and the consumer `app-test-implementation` emits the Jest global-setup (`tests/setup.ts`); today the accessor name is model-invented on each side (`getDatabase` vs `getDb`) → TS2305 at test build. Thread a single canonical named-export literal `getDatabase` through BOTH prompt bodies (including the global-setup contract on the consumer), and pin it with an offline acceptance-grep test that fails if either side reverts.

Purpose: a non-deterministic model can't re-drift the accessor name — the contract is shared text + a regression pin. The proportionate fix now (the N3-era `tsc`/`code_typecheck` validator is deferred per CONTEXT — needs exec + a TS toolchain).
Output: body-only edits to two AGENT.md files + one new test function in `test_prompt_contracts.py`.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-prompt-and-deliverable-adherence/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the canonical `getDatabase` literal through producer + consumer prompt bodies</name>
  <files>backend/agents/prompts/app-code-generator/AGENT.md, backend/agents/prompts/app-test-implementation/AGENT.md</files>
  <read_first>
    - .planning/phases/19-prompt-and-deliverable-adherence/19-CONTEXT.md (ISS-006 LOCKED decision — the exact shared-literal contract + REJECTED hacks)
    - backend/agents/prompts/app-code-generator/AGENT.md (producer body — where the DB module export contract is added)
    - backend/agents/prompts/app-test-implementation/AGENT.md (consumer body, esp. the "Contract fidelity" rule around lines 34-52 — the per-story-symbol scope that today excludes the global-setup file)
    - .planning/IMPLEMENTATION-REGISTER.md (13-03 row — the shared single `/api/v1` literal precedent; D-13-03-a: default app_builder stack is Node.js/TypeScript)
  </read_first>
  <action>
    Body-only edits (DO NOT touch frontmatter — frontmatter is frozen by `test_contract_agents_frontmatter_frozen` / the 15-01 freeze; an order/tools/guardrails change breaks it).

    (1) In `app-code-generator/AGENT.md`: add a one-line DB export contract — the database connection module MUST expose its accessor as a single canonical NAMED export `getDatabase` (NEVER a default export), at a canonical path (e.g. `src/db/index.ts`). State the literal `getDatabase` verbatim so it is greppable. Phrase it as a hard contract ("MUST export"), mirroring the forceful top-of-body style of the existing `/api/v1` API PATH CONTRACT in the infra prompt.

    (2) In `app-test-implementation/AGENT.md`: extend the "Contract fidelity" rule so it EXPLICITLY covers test-infrastructure files — the Jest/Vitest global setup (`tests/setup.ts` / the configured `globalSetup`) MUST import the DB accessor by the EXACT canonical named export the impl emits (`getDatabase`), quoted from the provided context; never invent `getDb`/`getConnection`/a default import. State the literals `getDatabase` and `tests/setup.ts` verbatim. The current rule (per CONTEXT) is scoped to per-story domain symbols and EXCLUDES the global-setup file — that scope gap is the root cause, so the new sentence must name the global-setup file.

    Use the literal `getDatabase` on BOTH sides (single source of truth — 13-03 pattern). REJECTED (from CONTEXT, do NOT do): rename only one side (the other re-drifts); add a `tsc --noEmit`/`code_typecheck` gate now (N3-gated — exec OFF + would add a gate to the golden pipeline).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -c "from agents.loader import load_agent_spec as L; p=L('app-code-generator').prompt_body; c=L('app-test-implementation').prompt_body; assert 'getDatabase' in p, 'producer missing getDatabase'; assert 'getDatabase' in c, 'consumer missing getDatabase'; assert 'setup.ts' in c, 'consumer missing global-setup contract'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `load_agent_spec('app-code-generator').prompt_body` contains the literal `getDatabase` AND the "named export" / "not a default export" contract phrasing.
    - `load_agent_spec('app-test-implementation').prompt_body` contains the literal `getDatabase` AND names the global-setup file (`tests/setup.ts` / `globalSetup`) inside the Contract-fidelity rule.
    - Only the two AGENT.md bodies changed; `git diff` shows no frontmatter edits (no `---`-block lines changed).
  </acceptance_criteria>
  <done>Both prompt bodies carry the canonical `getDatabase` literal; the consumer's Contract-fidelity rule names the global-setup file; no frontmatter changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the offline acceptance-grep pin + prove INV-3 parity</name>
  <files>backend/tests/agents/test_prompt_contracts.py</files>
  <read_first>
    - .planning/phases/19-prompt-and-deliverable-adherence/19-CONTEXT.md (ISS-006 — step 4: the acceptance-grep pin; INV-3 rationale: scripted model NEVER reads prompt bodies)
    - backend/tests/agents/test_prompt_contracts.py (the Phase-15 pin layer — model the new test on `test_infra_generator_api_v1_contract` at line 124, which asserts on `load_agent_spec(id).prompt_body`)
    - backend/tests/agents/_scripted_model.py (proves goldens are prompt-independent — `_scripts_for` drives app-code-generator to write `src/app.py`+`README.md`, app-test-implementation in TEXT_ONLY; a body edit can't perturb a golden)
  </read_first>
  <behavior>
    - Test `test_getdatabase_accessor_contract_shared`: `load_agent_spec('app-code-generator').prompt_body` contains `getDatabase`; `load_agent_spec('app-test-implementation').prompt_body` contains `getDatabase`.
    - Same test asserts the consumer body names the global-setup file (`tests/setup.ts` or `globalSetup`) so the global-setup contract specifically is pinned, not just the bare literal.
    - Reverting either prompt side (removing the literal) makes the test FAIL (the regression-pin property — verify by reasoning about the assertions; a missing literal trips an assert).
  </behavior>
  <action>
    Add ONE test function (and a parametrization if it reads cleaner) to `backend/tests/agents/test_prompt_contracts.py`, modeled on `test_infra_generator_api_v1_contract`. Assert on `load_agent_spec(id).prompt_body` (NOT the raw file — the parser is what the factory composes from; this is the established pin pattern). Assert: producer body contains `getDatabase`; consumer body contains `getDatabase` AND a global-setup file token (`tests/setup.ts` or `globalSetup`). Keep the assert messages explicit so a future revert points at the dropped contract.

    Then PROVE INV-3 parity (do NOT assume): run the characterization golden suite WITHOUT snapshot-update and confirm all 5 goldens (prototype/od_prototype/prototype_revision/od_ppt/app_builder) are byte/event-identical — the scripted model never reads prompt bodies, so a body edit cannot perturb them. Run `lint-imports` (4 kept / 0 broken) and confirm zero new migrations (no `backend/alembic`/migration file added). Record these three results in the SUMMARY.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_prompt_contracts.py -k "getdatabase or getDatabase" -q</automated>
  </verify>
  <acceptance_criteria>
    - New pin test passes; it asserts on `load_agent_spec(...).prompt_body` for BOTH agents.
    - Fault-injection (manual reasoning recorded in SUMMARY): removing the literal from either side trips an explicit assert → the pin catches a revert.
    - INV-3 PROVEN: `python3.11 -m pytest tests/agents/characterization -q` green with NO `SNAPSHOT_UPDATE` (5 goldens byte/event-identical).
    - `lint-imports` reports 4 contracts kept / 0 broken; `git status` shows no new migration file.
  </acceptance_criteria>
  <done>The acceptance-grep pin is committed and green; the 5 goldens are proven byte/event-identical; lint-imports 4/0; zero migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt body → generated app code | The producer/consumer prompt text steers what the live model writes; it is NOT executed by Flowin. No runtime trust boundary is crossed by these edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | app-code-generator / app-test-implementation AGENT.md bodies | accept | Body-only prompt text; no code path, no data flow, no secret. The contract is advisory prose to the live model. A bad edit is caught by the grep pin + the loader schema test (frontmatter unchanged). |
| T-19-01-02 | Repudiation | INV-3 golden parity | mitigate | Prove the 5 characterization goldens stay byte/event-identical (scripted model ignores prompt bodies) — a regression here would silently change pipeline behavior; the no-snapshot-update run is the evidence. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (prompt + test-file edits only). |
</threat_model>

<verification>
- `cd backend && python3.11 -m pytest tests/agents/test_prompt_contracts.py -q` — the new pin + existing Phase-15 pins green.
- `cd backend && python3.11 -m pytest tests/agents/characterization -q` — 5 goldens byte/event-identical (NO SNAPSHOT_UPDATE).
- `cd backend && python3.11 -m pytest tests/agents/test_loader.py -k schema_validation -q` — frontmatter still valid (bodies-only edit).
- `lint-imports` (at /opt/homebrew/bin/lint-imports) — 4 kept / 0 broken.
- `git status` — no new migration file; only the 3 declared files changed.
</verification>

<success_criteria>
- ROADMAP SC1 met: producer + consumer share the canonical `getDatabase` literal (incl. the global-setup file), pinned by an offline grep test; reverting either side fails the pin; INV-3-safe.
- INV-3 parity proven (5 goldens byte/event-identical; lint-imports 4/0; zero migrations).
</success_criteria>

<deferred>
- LIVE re-confirm on `default` Bedrock: a real Haiku app_builder run produces a Jest global-setup that imports `getDatabase` with no TS2305 (no `getDb` mismatch). Runs in the consolidated live + Playwright pass — NOT a blocker for this phase (offline grep pin is the phase-exit gate).
- A `tsc --noEmit` / `code_typecheck` / `symbol_contract` validator — N3/v2 era (needs exec + a TS toolchain).
</deferred>

<output>
Create `.planning/phases/19-prompt-and-deliverable-adherence/19-01-SUMMARY.md` when done.
</output>
