# SSE QA Bug Log — feat/ui-2 (live Bedrock)

> Append-only log of every bug found during the live-SSE QA campaign ([test sheet](./SSE-QA-TEST-SHEET.md)).
> **Process (per user):** when a test fails → spawn a deep-investigation agent that **FIRST reads `.planning/IMPLEMENTATION-REGISTER.md` fully to EOF** (understand the architecture with no assumptions), root-causes the bug to file:line, and returns a structured finding. THEN it is logged here. Never log a symptom without a root cause.
> **Severity:** 🔴 blocker · 🟠 major · 🟡 minor · 🔵 cosmetic/polish
> **Status:** OPEN → INVESTIGATING → ROOT-CAUSED → FIXED → VERIFIED

Last updated: 2026-07-16 (BUG-005 root-caused — an `od_prototype` no-template launch paused at the clarify gate renders "Running 0/0 BUILDING" with no questions because a **concurrently-attached** run's `pipeline_start` wipes the *global, non-run-scoped* clarify state: the SSE provider fans every frame from every attached run to one subscriber (`RunConnectionProvider.tsx:276`), whose `pipeline_start` branch unconditionally `setQuestionnaireData(null)`/`setActivePipelineRunId(null)` + clears the shared seen-set (`page.tsx:462-482`) with no run-identity guard → `runLaneState` falls through `clarify`→`building` (`DashboardLayout.tsx:1308-1317`). Backend/SSE/`missing_template_context` guard all correct (KAN-87 no-template is supported); "0 agents" is a red herring (agents arrive only in post-clarify `pipeline_start`). PRE-EXISTING; same viewed-run-binding class as BUG-001/002/003. Also: BUG-003 root-caused — revise-via-chat "Run refinement" silently no-ops on a reopened od_ppt run: `handleRevisePpt`'s pre-existing empty-content guard `if (!pptxCode && !pptContent) return;` (DashboardLayout.tsx:493) short-circuits BEFORE the self-sufficient `contentSourceRunId` REST path (:504) whenever the reopened deck content didn't load — `pptxCode` is structurally undefined for od_ppt and `pptContent` was empty because `fullRun.output` was empty (LV-02-class). The confirm chip clears unconditionally (RunChatLane.tsx:714), so it vanishes with zero requests fired).

---

## Schema

Each entry:

```
### BUG-NNN — <one-line symptom>  [severity] [status]
- **Found:** <date> · test <TEST-ID> · surface <screen/pipeline>
- **Symptom:** what was observed (with screenshot ref).
- **Root cause (investigation agent, read impl-register to EOF):** file:line + mechanism.
- **Fix:** commit / plan / deferred-item ref, or "proposed".
- **Verify:** how it was re-proven.
```

---

## Known/tracked from earlier this session (context — already handled)

- **DEF-44-12-1** (FIXED `f4df5be1`) — run-chat send misrouted to launch on a history-opened run.
- **DEF-44-12-3** (FIXED `0563afad`) — history-opened non-terminal run never attached its SSE stream (status set too narrow).
- **DEF-44-12-4 / DEF-44-12-2** (FIXED, quick 260716-dhr, commits 396ae3db..8f7d9e9b) — run-screen live reducer + transcript bound to launched run not viewed run; Concierge reply durable-only + id-collision. **Live proof pending in this campaign (tests B3/B5).**

---

## Bugs found in this campaign

### BUG-001 — Run-screen left-lane title shows the most-recent run's title, not the viewed run's  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-j1u, cc2f98f7) — live-proven

- **Found:** 2026-07-16 · test DEF-44-12-4 B3/B5 live-verification (history-open path) · surface run screen → left conversation lane header (`LaneRunHeader`, `data-testid="lane-run-title"`).
- **Symptom:** Opening three different runs from the Home "Jump back in" recents, the left-lane title heading showed the WRONG run's title while all other lane/Steps content was correct for the opened run:
  - user_stories `ab90f5f4` (brief "A shift-scheduling app for cafes…") → heading "An interactive analytics dashboard for a fitness app: active" (WRONG). Starting-point card + pipeline correct.
  - app_builder `bb6dc438` (brief "A URL shortener web app…") → same fitness heading (WRONG). Content correct (URL-shortener 15-agent build).
  - od_prototype `4cd2f478` (brief "An interactive analytics dashboard for a fitness app…") → heading correct — but only *coincidentally*, because od_prototype was the LAST run launched (= `recents[0]`).
  - Shots: `~/.claude/jobs/e660aea4/tmp/shots-qa/{user_stories,app_builder,od_prototype}-steps.png`.
- **Root cause (investigation agent — read `.planning/IMPLEMENTATION-REGISTER.md` global + all FE run-screen phase sections 25/31/32/36–39/41/42; grep-confirmed no run-title architecture in the backend engine phases):**
  - **Symptom site → cause site.** Heading is rendered by `frontend/src/components/chat/LaneRunHeader.tsx:241-248` (`{runTitle}`). Feed chain: `frontend/src/components/layout/DashboardLayout.tsx:1323-1325` computes `runHeaderTitle` → passed as `runTitle={runHeaderTitle}` at `DashboardLayout.tsx:1713` → `frontend/src/components/chat/RunChatLane.tsx:1173` `runTitle={runTitle ?? firstUserTurn}` → `LaneRunHeader`.
  - **The mechanism (`DashboardLayout.tsx:1323-1325`):**
    ```
    const latestRunTitle = recentRuns?.[0]?.title;
    const runHeaderTitle =
      latestRunTitle && latestRunTitle !== "Untitled" ? latestRunTitle : submittedBrief;
    ```
    `recentRuns?.[0]` is **unconditionally the most-recently-created run** (the Home recents list, `page.tsx:150` + prepend/update at `page.tsx:294/557/585/753`), never the run being VIEWED. The ternary prefers `latestRunTitle` whenever it is a real (non-"Untitled") title — which it almost always is — so `submittedBrief` (the fallback) is never reached, and the title never tracks the viewed run. The viewed-run identity `contentSourceRunId` (`page.tsx:156`; set to `data.pipeline_run_id` on `pipeline_complete` at `page.tsx:496`, to `fullRun.id` on history-open at `page.tsx:1187`, cleared to `null` on fresh launch at `page.tsx:1427`) — already destructured into this component at `DashboardLayout.tsx:210` and used everywhere else for run identity — is simply not consulted by the title expression.
  - **Root-cause assumption that broke:** the Phase-39 title code assumed "the active/on-screen run is always `recents[0]`." That holds for the *primary launch flow* (a fresh build clears `contentSourceRunId` to null and the just-launched run is `recents[0]`), but is false the moment you view a **non-latest** run from history/recents, where the on-screen run is `contentSourceRunId`, not `recents[0]`.
  - **PRE-EXISTING, not introduced by DEF-44-12-4 — 12-4 EXPOSED it.** `git blame -L1319,1326` → the whole block is commit **`5003aca47` "feat(39-05): wire the lane Back-to-history + run metadata into the run shell" (2026-07-11, Phase 39 / RUNUI-06)**. The DEF-44-12-4 quick-task range **`396ae3db..8f7d9e9b` (2026-07-16) never touched `DashboardLayout.tsx`** (`git log … -- DashboardLayout.tsx` in that range = empty). What 12-4 *did* do (Piece 1, commit `349e5038`, `page.tsx:1189-1202`) is seed the viewed run's Steps trace + set `submittedBrief = fullRun.input` on history-open — i.e. it made a history-opened run actually RENDER its own content instead of the old empty "Start a pipeline…" placeholder (comment at `page.tsx:1191-1193`). That is exactly what made the always-`recents[0]` title *visibly* wrong: before 12-4 the run screen showed almost nothing for a non-latest run so nobody saw the title; after 12-4 the correct content renders beside the stale title. This is a sibling of DEF-44-12-4 (same "bound to launched run, not viewed run" class — 12-4 rebound the reducer/transcript/runInput; the lane-header *title* is a separate binding 12-4 did not cover).
- **Fix (PROPOSED — investigation only, not applied):** bind the title to the VIEWED run at `DashboardLayout.tsx:1323`. Replace the single line
  ```
  const latestRunTitle = recentRuns?.[0]?.title;
  ```
  with a viewed-run lookup (both `contentSourceRunId` and `recentRuns` are already props in scope — `DashboardLayout.tsx:209-210`):
  ```
  const viewedRun =
    contentSourceRunId != null
      ? recentRuns?.find((r) => r.id === contentSourceRunId)
      : recentRuns?.[0];
  const latestRunTitle = viewedRun?.title;
  ```
  Keep the existing ternary unchanged (`… ? latestRunTitle : submittedBrief`). Behaviour by flow:
  - **Fresh live build** (`contentSourceRunId` null) → `recents[0]` = the just-launched run → title correct (byte-identical to today's primary flow; no regression).
  - **Live complete** (`contentSourceRunId` = completed run) & **history/recents reopen** (`contentSourceRunId` = viewed run) → `find(...)` resolves the viewed run's clean async title → **correct (bug fixed)**.
  - **Viewed run older than the recents window** (`find` → undefined) → falls back to `submittedBrief`, which on reopen is the VIEWED run's own `fullRun.input` (12-4 Piece 1) → still the viewed run, never a foreign run's title (strictly better than today).
  - Coherent with revisions: an inline-revise keeps `contentSourceRunId` = the source/parent run and keeps the parent's deliverable on screen until new output arrives (`page.tsx:1429`), so the title correctly matches the on-screen content, then re-points to the revision run on its `pipeline_complete` (`page.tsx:496`).
  - Minimal + SC-001-safe: one contiguous edit in a guarded component, no page.tsx change, no new prop/state, keys only on generic run identity (`contentSourceRunId` / `run.id`) — zero workflow-name literal. Rejected alternative: threading a dedicated `viewedRunTitle` state from `page.tsx` (larger diff, redundant — `recents.find` already yields the same clean async title the launch flow uses).
- **Verify (fail-before / pass-after):**
  1. **Fail-before:** with ≥2 runs of different types, open a non-latest run from Home recents; assert `data-testid="lane-run-title"` == `recents[0].title` (reproduced live: user_stories `ab90f5f4` and app_builder `bb6dc438` both showed the fitness title). 
  2. **Pass-after:** after the fix, `lane-run-title` == the OPENED run's title for `ab90f5f4` (shift-scheduling) and `bb6dc438` (URL-shortener); `od_prototype 4cd2f478` stays correct.
  3. **No launch regression:** a fresh live run shows its own title throughout the build (`contentSourceRunId` null → `recents[0]`).
  4. **Unit:** add a `DashboardLayout` (or `LaneRunHeader`) vitest asserting `lane-run-title` tracks `contentSourceRunId` across a 3-element `recentRuns`; `tsc --noEmit` clean.

---

### BUG-002 — Run History row-tap opens a degraded static detail (no chat lane / no revise / stale QUEUED / No agent data / No preview) instead of the shared run screen  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-j1u, e8a22842) — live-proven

- **Found:** 2026-07-16 · test history-open path (top-nav "Run History" / a run's "Back to history" → tap a run row) · surface Run History detail vs the shared run screen.
- **Symptom:** Two paths open a past run and render COMPLETELY DIFFERENT UIs.
  - **Home "Jump back in" recents → tap run (GOOD):** the FULL run screen — left conversation lane (`data-testid="execution-chat-lane"`) with the Steer/revise composer, working concierge, live Steps trace + deliverable. Tab set Preview / **Steps** / Files / Audit. Shots: `~/.claude/jobs/e660aea4/tmp/shots-qa/{recents-open-run,concierge-reply2}.png`.
  - **Top-nav "Run History" → tap a run row (BAD):** a DIFFERENT, degraded detail — **NO left chat lane, NO composer, NO revise affordance**; WRONG status ("QUEUED" for an actively-generating run); empty **"No agent data"** left panel + **"No preview available"** main area; a DIFFERENT tab set Preview / Files / **Thinking** / Audit. Run tapped = the live app_builder run `bb6dc438` ("A URL shortener web app"). Shot: `~/.claude/jobs/e660aea4/tmp/shots-qa/history-row-tap.png`.
  - User expectation: "when you go to workflow history, and tap, the left chat panel should be there to help revise." It is not.
- **Root cause (investigation agent — read `.planning/IMPLEMENTATION-REGISTER.md` global + FE run-screen/history phase sections 21/31/32/**36**/39/42/44; grep-confirmed no run-open architecture in the backend engine phases):**
  - **The two open paths physically diverge into two different components.**
    - **Home recents (GOOD):** `frontend/src/components/layout/DashboardLayout.tsx:1475` — `onOpenRun={(run) => { onSelectWorkflowRun?.(run); setMainView("execution"); }}`. `onSelectWorkflowRun` (prop `DashboardLayout.tsx:76`, destructured `:211`) is wired to `page.tsx`'s `handleSelectWorkflowRun` (`frontend/src/app/dashboard/page.tsx:1444`). `handleSelectWorkflowRun` (`page.tsx:1165`) fetches the run, sets the VIEWED-run identity `setContentSourceRunId(fullRun.id)` (`page.tsx:1187`), and — when the opened run differs from the live launched run (`page.tsx:1198`) — **seeds the Steps pipeline reducer from the durable `run_events` and sets `submittedBrief = fullRun.input`** (`page.tsx:1202-1219+`, the DEF-44-12-4 Piece 1 seed) and attaches the live SSE tail for a non-terminal run (DEF-44-12-3). Then `setMainView("execution")` mounts the shared run screen whose left column is `RunChatLane` (`frontend/src/components/chat/RunChatLane.tsx`, `data-testid="execution-chat-lane"`) — live-bound, with the concierge/steer/revise composer.
    - **Run History (BAD):** `DashboardLayout.tsx:1506-1508` mounts `<WorkflowHistory … activeRunId={pipelineState?.pipelineRunId ?? null} onViewRunningPipeline={() => setMainView("execution")} />` — and passes **NO `onSelectWorkflowRun`/`onOpenRun`**. A run row's tap (`WorkflowHistory.tsx:953` `onSelectRun={handleSelectRun}`) runs `handleSelectRun` (`WorkflowHistory.tsx:155`): a KAN-96 guard (`:156-161`) routes to the live execution view **only when `run.id === activeRunId`** (i.e. equals the single `pipelineState.pipelineRunId`); for every other run it falls through to `setSelectedRun(run)` (`:162`), which triggers the `if (selectedRun)` branch (`:362`) that renders WorkflowHistory's OWN internal two-column detail: left `<RunDetailPage runId={selectedRun.id}>` (`:424`) + a main deliverable panel with tabs `["preview","files","thinking","audit"]` (`:547`). `handleSelectWorkflowRun` is **never called**, so `contentSourceRunId`, the durable-event Steps seed, the SSE attach, and `RunChatLane` are all skipped.
  - **Symptom-by-symptom:**
    - **(a) No chat lane / composer / revise.** `RunDetailPage.tsx` is a static summary page (header + KPI strip + Agents/Timeline tabs) — it contains no `RunChatLane`, no textarea, no steer. The only revise on this surface is a small left-footer textarea gated on `WorkflowHistory.tsx:438` `selectedRun.status === "completed"` — so an actively-generating (non-completed) run shows **no revise at all**. The `RunChatLane` conversation lane (concierge/steer) is never mounted on this path.
    - **(b) "QUEUED" for a generating run.** `RunDetailPage` is fed by a **one-shot** `getRunSummary(jwt, activeVersionId)` (`RunDetailPage.tsx:102`, `useEffect` deps `[activeVersionId]` `:119`) rendered via `<Badge status={summary.status}>` (`:224`). No SSE attach, no reducer, no poll → the badge is a snapshot taken at open-time and never advances. The row-tap didn't reach the live view because `bb6dc438 !== pipelineState.pipelineRunId` (the last-launched run — od_prototype `4cd2f478` per BUG-001 — held `activeRunId`). (The exact persisted status value returned by `/summary` = *unverified* — did not read the endpoint; the not-live-bound mechanism = *verified*.)
    - **(c) "No agent data" + "No preview available".** `RunDetailPage.tsx:343-346` renders "No agent data" when `summary.agents.length === 0` (the one-shot summary of a just-started run has no agents yet). "No preview available" is WorkflowHistory's own deliverable-panel empty state (`WorkflowHistory.tsx:626` / `:646`) shown when `selectedOutput`/`selectedRun.output` is empty (a generating run has no output yet). Both are static — no durable-event seed (DEF-44-12-4) and no SSE tail (DEF-44-12-3) that the execution path now has.
    - **Tab-set difference confirms two implementations.** The run screen relabels the thinking tab to "Steps" (`PreviewPanel.tsx:356` `{ id: "thinking", label: "Steps" }`); WorkflowHistory's internal detail keeps raw order/labels Preview/Files/**Thinking**/Audit (`WorkflowHistory.tsx:547`).
  - **Pre-existing verdict — PRE-EXISTING, not introduced by the SSE/DEF-44 work; the SSE work made the contrast starker.** `git blame`: `handleSelectRun → setSelectedRun` (the internal-detail branch) = `09c7609bf` Prateek Sinha **2026-05-07** — the original History behavior, long pre-dating the run-screen/`RunChatLane` work. The narrow KAN-96 live-route guard = `71eec1b25` **2026-07-06**. `RunDetailPage.tsx` created `fb0b5e9c` **2026-07-09** (Phase 36-04, promoting the in-panel detail into a page). The Home-recents path was *separately* fixed to route into the run screen (`ba159678` "fix(dashboard): Home recents chip opens the run in the execution view"), but the **symmetric History path was never rewired**. The SSE cutover + DEF-44-12-4 seed + DEF-44-12-3 SSE-attach all landed **only** on `page.tsx handleSelectWorkflowRun` (the execution path); the History path never received live binding, so as the execution path got much richer the History detail stayed static and the divergence became glaring. Same class as BUG-001 / DEF-44-12-4 ("a run-open surface not brought up to the execution path's viewed-run/live binding").
- **Fix (PROPOSED — investigation only, not applied):** route the History row tap through the SAME wiring as Home recents so the shared run screen (`RunChatLane` + `contentSourceRunId` + DEF-44-12-4 durable seed + DEF-44-12-3 SSE attach) is used everywhere. Two edits:
  1. **`WorkflowHistory.tsx`** — add an optional prop `onOpenRun?: (run: WorkflowRun) => void` to `WorkflowHistoryProps` (near `:45`) and destructure it (`:108`). In `handleSelectRun` (`:155`), when `onOpenRun` is provided call `onOpenRun(run); return;` in place of the `setSelectedRun(run)` internal-detail branch (`:162`). (The KAN-96 `activeRunId` guard can fold away — the shared run screen already handles a live run via the same seed.)
  2. **`DashboardLayout.tsx:1506`** — pass `onOpenRun={(run) => { onSelectWorkflowRun?.(run); setMainView("execution"); }}` to `<WorkflowHistory>` — **byte-identical to the Home-recents wiring at `DashboardLayout.tsx:1475`** (`onSelectWorkflowRun` and `setMainView` are already in scope; `onSelectWorkflowRun` destructured `:211`). SC-001-safe: keys only on generic run identity, zero workflow-name literal.
  - **Must-verify / carry-over before RETIRING the internal detail** (`WorkflowHistory.tsx:362-730` + `RunDetailPage.tsx`, per INV-3/INV-12 no-dual-implementation — grep shows `RunDetailPage` is mounted only by `WorkflowHistory`):
    - **Cross-workflow chaining** ("Suggested next steps", `onChainPipeline`/`availableChainTargets`, `WorkflowHistory.tsx:450`/`:518`) — grep-confirmed **absent** from the run screen (0 hits for `availableChainTargets|onChainPipeline` in `RunChatLane.tsx`/`PreviewPanel.tsx`). The one genuinely History-only capability: add it to the run screen's complete-state affordances or keep it reachable before deleting the internal detail.
    - **Version-family switch column** (`VersionTimeline`, `RunDetailPage.tsx:262`) — also **absent** from the run screen (0 hits), BUT each family member is already its own row in the History LIST (`RevisionFamilyView` family expansion), so every version stays reachable by tapping its list row → run screen. Confirm that list-level reachability suffices.
    - **Per-agent KPI breakdown / terminal-failure banner** (RunDetailPage Agents tab + `DegradedRunAffordance`) — covered by the run screen's Steps + Audit (P26 token telemetry) + its own terminal cards (Phase 32/42). Low risk.
    - **Delete** = list-level kebab (not in the detail) — preserved. **Back nav** — run screen already has "Back to history" (`LaneRunHeader.tsx:219`, `RunChatLane.tsx:155 onBackToHistory`) — no regression.
  - Retiring the internal detail is a *follow-up*; the minimal fix is only the 2-point rewiring above.
- **Verify (fail-before / pass-after):**
  1. **Fail-before:** with a run that is NOT `pipelineState.pipelineRunId` (any completed historical run, or a second generating run), open it from top-nav Run History → assert NO `data-testid="execution-chat-lane"`, RunDetailPage summary visible, tabs = Preview/Files/Thinking/Audit. (Reproduced live: `bb6dc438` → no lane, QUEUED, "No agent data"/"No preview available".)
  2. **Pass-after:** same tap → `data-testid="execution-chat-lane"` present with the composer + concierge/steer; live status/agents/preview render (parity with `recents-open-run.png`); tabs = Preview/Steps/Files/Audit.
  3. **No-regression:** Home recents tap still opens the run screen; the active-run KAN-96 case still lands on the live view; "Back to history" returns to the list; chaining + version switch still reachable (list-level).
  4. **Unit:** a `WorkflowHistory` vitest asserting a row tap calls `onOpenRun(run)` (not `setSelectedRun`); `tsc --noEmit` clean.

---

### BUG-003 — Revise-via-chat "Run refinement" fires nothing / launches no revision on an opened run  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-j1u, 8b4e2eb7) — offline-proven TS-U-09

- **Found:** 2026-07-16 · live Bedrock QA · surface run screen → left chat lane (`RunChatLane`) confirm-first refinement chip, on a **recents/history-opened COMPLETED od_ppt run** (`6c6f38c7`, "A 6-slide investor pitch for a B2B carbon-accounting SaaS").
- **Symptom:** On the opened run, typing a change and chat-send correctly HELDS it behind the confirm chip ("RUN A REFINEMENT WITH THIS CHANGE? … [Run refinement] [Dismiss]", 44-02 behavior — no POST on send, verified). Clicking **"Run refinement"** → **ZERO non-GET `/api/` requests fire**, the chip disappears, and **NO `*_revision` run is created**. The opened run's deliverable **Preview was also BLANK** (presentation.pptx did not render inline). Shots: `~/.claude/jobs/e660aea4/tmp/shots-qa/{revise-chip,revise-launched,revise-clean-after}.png`.
- **Root cause (investigation agent — read `.planning/IMPLEMENTATION-REGISTER.md` global + FE run-screen/revise phase sections 31/39/42/44 + Revision-Families B1 + LV-02/Phase-15; grep-confirmed od_ppt agent membership in `backend/agents/registry.py` and the od_ppt deliverable manifest):**
  - **The swallow (primary, deterministic) — `frontend/src/components/layout/DashboardLayout.tsx:493`.** `handleRevisePpt` opens with a pre-existing empty-content guard `if (!pptxCode && !pptContent) return;` that short-circuits BEFORE the self-sufficient REST path at `:504` (`if (contentSourceRunId) { postRevision(token, contentSourceRunId, { target_artifact_type, instruction }) … }`). On a reopened od_ppt run BOTH content fields are empty, so the handler returns immediately → no `postRevision`, no `onStartPipeline` → **zero network requests**. The confirm handler `confirmRefinement` (`frontend/src/components/chat/RunChatLane.tsx:712-715`) calls `setHeldRefinement(null)` **unconditionally** after invoking `onRevise`, so the chip vanishes even when `onRevise` no-ops — exactly the observed "chip disappears, nothing happens." Wire: chip button `data-testid="chat-refinement-confirm"` `onClick={confirmRefinement}` (`RunChatLane.tsx:803`) → `onRevise` = `activeReviseHandler` (`DashboardLayout.tsx:1717` ← selector `:1233-1240`) → `handleRevisePpt`.
  - **The empty/null fields (why both are empty on a reopened od_ppt run — VERIFIED):**
    - `pptxCode` (`DashboardLayout.tsx:481`) = `pipelineState?.agents.find(a => a.id === "ppt-code-generator" && a.status === "done")?.output`. **od_ppt has NO `ppt-code-generator` agent** — its agents are `od-ppt-brief-analyst` / `od-ppt-composer` / `od-ppt-validator` (`backend/agents/registry.py:53-57`); that agent belongs only to the legacy `ppt` pipeline. So `pptxCode` is **structurally always `undefined` for od_ppt** (live and reopened). The guard therefore reduces to `if (!pptContent) return;` for od_ppt — no second field can rescue it.
    - `pptContent` (a page.tsx prop, `page.tsx:83`) on reopen = `fullRun.output` (set at `page.tsx:1254`, `else if (fullRun.type === … "od_ppt" …) setPptContent(fullRun.output)`), but ONLY `if (fullRun.output && isContentTerminal)` (`:1250`). For this run `fullRun.output` was **empty**, so `pptContent` stayed `""` (cleared at reopen start `page.tsx:1169`, never repopulated). Empty `fullRun.output` is ALSO exactly why the Preview was blank — **the blank preview and the no-op revise are SIBLING symptoms of the same empty `output`.**
    - **Why od_ppt's `output` can be empty (LV-02, register lines 79/1487/1512/1553/1605):** od_ppt's deliverable is `strategy: ppt`, `name: presentation.pptx` (`backend/agents/workflows/od_ppt/workflow.yaml:11-13`); the `ppt` strategy unwraps the `od-ppt-validator`'s `<artifact>`-wrapped deck into `final_output`, persisted to `wr.output` only `if final_output:` (`backend/app/api/run_commands.py:1445-1446`). LV-02 is the KNOWN failure mode where the validator streams QA narration (or nothing parseable) instead of a deck → `final_output` empty/non-deck. Phase 15 "closed" LV-02 with an `od-ppt-validator` AGENT.md **output contract** (a prompt instruction, NOT a hard guarantee), so on a live Haiku run it CAN recur → empty `output` → blank preview + no-op revise.
  - **Aggravator (L2) — the selected handler may be the WRONG one on a completed reopen (BUG-001/002 class).** `workflowType` is DashboardLayout-internal state (`:250-256`, default `"user_stories"`); it is only rebound by the sync effect at `:330-345`, which is **gated on `if (pipelineState?.isRunning)`** and thus **never runs for a completed reopen** (`isRunning` is false). So on a reopened od_ppt run `workflowType` stays stale (likely `"user_stories"`), and the `onRevise` selector (`:1233-1240`) resolves to `handleReviseUserStory` (guard `if (!userStoryContent) return;`, `:541`) — NOT `handleRevisePpt`. Every run-lane revise handler has the same empty-content guard (`handleReviseUserStory:541`, `handleRevisePrototype:557`, `handleReviseAppBuilder:573`), and on a reopened run with empty `output` ALL content fields are empty — so whichever handler is picked, it no-ops identically. Crucially, `handleRevisePpt` is the ONLY one with the content-free REST bypass (`postRevision`), so a stale `workflowType` means even the path that COULD succeed is not reached. This is the same "reopen doesn't fully bind to the viewed run" defect as BUG-001 (viewed-run TITLE) and BUG-002 (viewed-run open path) — here the viewed-run TYPE.
  - **Confound verdicts (which is the true cause):** (1) **Blank preview / deliverable not loaded — TRUE, the trigger.** Empty `fullRun.output` → empty `pptContent` → guard fires. (2) **BUG-001 header mis-binding — NOT the cause.** `contentSourceRunId` IS correctly set to the od_ppt run id on reopen (`page.tsx:1187`); BUG-001 only mis-binds the lane TITLE. The revise no-op happens upstream of any `contentSourceRunId` read — the guard returns before `:504`. (The parallel `workflowType` mis-binding, L2, IS a genuine BUG-001-class sibling.) (3) **Run out of recents — NOT the cause.** `handleSelectWorkflowRun` binds by fetched run id regardless of scroll position.
  - **DEF-44-12-4 seed is NOT the gap.** The Steps/transcript seed (`page.tsx:1198-1241`) is separate from the deliverable-content seed (`:1250-1256`), which is an **unconditional** block that DOES set `pptContent` from `fullRun.output`. The gap is that `fullRun.output` itself was empty, not a missing seed.
  - **Pre-existing verdict — the guard is PRE-EXISTING; the DEFECT (guard blocking the REST path) is RECENT.** `git blame`: the guard line 493 + `handleRevisePpt` decl (line 492) = `dc9de28fa` **Prateek Sinha 2026-05-11** (the legacy text-injection revise era, where the guard was correct — that path embeds `pptContent`/`pptxCode` into the message and genuinely needs them). The `contentSourceRunId` REST branch at line 504 = `a931c066a` **imran-hex 2026-07-15** (Revision Families / `postRevision`). The REST path is self-sufficient (backend seeds the parent artifact server-side from `contentSourceRunId` — Strategy A byte-twin of `engine._handle_revision`), but was added BELOW the stale content guard without relaxing it → the guard now wrongly gates a path designed to work WITHOUT local content. So the swallow defect dates to **2026-07-15**.
  - **od_ppt-specific vs general.** In practice **specific to od_ppt** (two od_ppt-only aggravators: no `pptxCode` fallback + LV-02-fragile empty `output`). Text-deliverable types (user_stories / app_builder / prototype) reliably persist non-empty text `output`, so their content guards pass on reopen and their revises fire (via `onStartPipeline` + `source_workflow_run_id`). But the empty-content guard pattern is in all four handlers, so the **latent general mechanism** is: any reopened run whose `fullRun.output` fails to load silently no-ops its revise. Severity **🟠 major** (not 🔴): conditioned on empty `output`, not universal across opened runs.
- **Fix (PROPOSED — investigation only, not applied):** two edits (both needed for od_ppt reopen-revise end-to-end); minimal + SC-001-safe (keys only on generic run identity, zero workflow-name literal added):
  1. **`DashboardLayout.tsx:493`** — stop the empty-content guard from blocking the self-sufficient REST path:
     ```
     -    if (!pptxCode && !pptContent) return;
     +    if (!contentSourceRunId && !pptxCode && !pptContent) return;
     ```
     When `contentSourceRunId` is present (always, on a reopened/completed run), the handler proceeds to `postRevision` (`:504`), which reseeds the parent deck server-side — working even when the FE never loaded the deck. The legacy text-injection fallback (`:520-536`) is unchanged and only reached when `contentSourceRunId` is falsy (and already reads `pptContent`/`pptxCode` with `|| ""`).
  2. **Bind the revise-handler selection to the VIEWED run's type on reopen** so `onRevise` actually resolves to `handleRevisePpt` for a reopened od_ppt run. The sync effect at `DashboardLayout.tsx:330` is `isRunning`-gated and never fires for a completed reopen; thread the viewed run's type down from `page.tsx handleSelectWorkflowRun` (mirroring how it already threads `contentSourceRunId`/`submittedBrief`), or have the selector (`:1233-1240`) fall back to the reopened run's type when not running. (BUG-001/002-class viewed-run binding — for run TYPE.)
  - **Secondary hardening (recommended, not required):** make the confirm path never SILENTLY no-op — when a change can't be launched, surface an error/toast instead of clearing the chip with nothing fired (`RunChatLane.tsx:712-715`), so an empty-`output` condition is visible.
  - **Do NOT "fix" the empty `output` in the od_ppt resolver** — register line 1594 locks that `ppt.py`/`_artifact.py` are READ-ONLY; the upstream LV-02 remedy lives in the `od-ppt-validator` output contract. The FE guard relaxation is the correct FE-side resilience so a content-less-but-valid parent still revises over REST.
- **Verify (fail-before / pass-after):**
  1. **Fail-before:** open a COMPLETED od_ppt run from Home recents whose Preview is blank (empty `output`); type a change → confirm chip appears; click "Run refinement" → capture non-GET `/api/` traffic = EMPTY, chip clears, no new `*_revision` in `GET /api/runs`. (Reproduced live: `6c6f38c7`.)
  2. **Pass-after:** same click → exactly one `POST /api/runs/{contentSourceRunId}/revisions` fires (`target_artifact_type: "od_ppt_output"`), the returned run attaches over SSE (`runConnection.attachRun`), and a new `od_ppt_revision` appears in `GET /api/runs` linked to the parent — even with the deck content unloaded.
  3. **No-regression:** a fresh live od_ppt build (with `pptContent`) still revises; a reopened run WITH a loaded deck still revises; text-deliverable reopened runs (user_stories/app_builder/prototype) unaffected.
  4. **Unit:** a `DashboardLayout` vitest — `handleRevisePpt` with `contentSourceRunId` set but `pptContent`/`pptxCode` empty calls `postRevision` (not early-return); and the reopen path selects `handleRevisePpt` for an od_ppt viewed run. `tsc --noEmit` clean.

---

### BUG-004 — DB connection-pool exhaustion (QueuePool 5+10) under concurrent runs → 500 on submit_answers/_review_gate_owned_by  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-heb, d418b5a1/56deb5c4) — live-proven (25 streams)

- **Found:** 2026-07-16 · live Bedrock QA under concurrent load (~10+ simultaneous runs — several `generating`/streaming, some at clarify/gates) · surface: `POST /api/runs/{id}/answers` (clarify-skip) and any transactional REST command. Worked (200) earlier with fewer active runs; became a consistent 500 once ~10+ runs were live. Traceback: `~/.claude/jobs/e660aea4/tmp/uvicorn44.log`.
- **Symptom:** `submit_answers` (`run_commands.py:261`) → `_review_gate_owned_by` (`run_engine.py:344`, the `.first()`) raises `sqlalchemy.exc.TimeoutError: QueuePool limit of size 5 overflow 10 reached, connection timed out, timeout 30.00` → HTTP 500. i.e. all 15 SQLAlchemy connections were checked out and NONE freed within the 30s pool_timeout.
- **Root cause (investigation agent — read `.planning/IMPLEMENTATION-REGISTER.md` global + Phase-5 D-08 sync-DB idiom, Phase-29 SSE endpoint, Phase-44 WS→SSE cutover; verified against installed fastapi 0.115.6 / starlette 0.41.3 / sqlalchemy 2.0.36; empirically reproduced the leak):**
  - **This is a genuine CONNECTION LEAK on the SSE down-channel, not merely a too-small pool.** The failing endpoint is the *victim*: `_review_gate_owned_by` (`run_engine.py:339-348`) opens `_get_db()` and correctly `finally: db.close()`; it blocks on `.first()` only because the pool is already fully drained by leaked stream connections.
  - **The leaker — `app/api/run_stream.py` (the SSE endpoint `stream_run_events`, the SOLE run-event transport since 44-07).** It declares `db: Session = Depends(get_db)` (`run_stream.py:208`), builds `store = ScopedStore(owner_id=…, workspace_id=…, session=db)` (`:233-237`), and passes that store into the streaming generator `_iter_sse_frames`, which calls `store.read_events(...)` INSIDE the stream body at `:145` (durable replay) and `:178` (gate re-arm full-log read).
  - **Why the injected session leaks a pool connection (mechanism, verified in installed source):** FastAPI ≥0.106 tears down `yield` dependencies BEFORE the streaming response body runs. In `fastapi/routing.py::get_request_handler`, `solve_dependencies` + `run_endpoint_function` run *inside* `async with AsyncExitStack() as async_exit_stack:`; the handler then `return response` and Starlette does `await response(scope, receive, send)` *outside* that block — so `get_db`'s `finally: db.close()` (`database.py:43-49`) fires before `_iter_sse_frames` yields its first frame. When the generator then calls `store.read_events` on that already-closed session, `ScopedStore._acquire` sees an injected session (`authz.py:87-99`) → `owned=False` → the query RE-BEGINS a transaction and checks out a FRESH QueuePool connection, and `read_events`'s `finally: if owned: session.close()` (`authz.py:328-330`) does NOT close it (owned=False) — while `get_db`'s finally has ALREADY run. Nothing deterministically returns that connection; it is reclaimed only when the orphaned `Session` is garbage-collected.
  - **Empirical proof (`/tmp/sse_pool_repro2.py`, same interpreter/versions, QueuePool pool_size=1):** ordered log — `EXIT get_db (finally close) checkedout=0` at 1.5ms (FastAPI closes the request session BEFORE streaming) → `after read_events in gen, checkedout=1` at 1.8ms (generator re-acquires) → `gen done, checkedout=1` (held the whole stream) → `client stream closed, checkedout=1` → `after 0.3s settle, checkedout=1` (STILL held) → `after gc.collect(), checkedout=0` (released ONLY by GC). This reproduces the leak exactly; a plain StreamingResponse that does NOT query inside the generator keeps `checkedout=0` (repro v1), confirming the query-on-injected-session-inside-the-generator is the trigger.
  - **Connection math / does it structurally exceed 15:** pool = `create_engine(..., pool_pre_ping=True)` with NO explicit sizing (`database.py:12-16`) → SQLAlchemy defaults `pool_size=5 + max_overflow=10 = 15` max, `pool_timeout=30`. Each live SSE stream leaks exactly 1 connection (the generator's re-acquired session; the `get_current_user` session + the two pre-stream owner-check queries at `:220`/`:239` return normally). N concurrent live streams → up to N leaked connections held at once, PLUS connections from recently-ended/reconnected streams that are orphaned-but-not-yet-GC'd (the browser's native EventSource auto-reconnect, duplicate tabs, and React remounts each open a new stream that leaks again on teardown). So under the reported ~10+ concurrent runs the held count reaches/exceeds 15 and the pool saturates; the next transactional command (`submit_answers` → `_review_gate_owned_by` → `_get_db().query().first()`) waits 30s and 500s. GC is the only reclaim path, so under sustained streaming load the leak outruns reclamation.
  - **Checkpointer is NOT the cause (ruled out).** The LangGraph Postgres checkpointer uses a SEPARATE psycopg `AsyncConnectionPool(max_size=10)` (`app/agents/checkpointer.py:91-96`), independent of the SQLAlchemy engine/QueuePool — it adds its own ~10-conn load to Postgres but does not draw from (or exhaust) the 15-conn QueuePool named in the traceback.
  - **Sibling sweep (only run_stream.py has the leak pattern):** the 2nd SSE endpoint — chat, `run_commands.py:1819` — is SAFE: its generator opens its OWN `_get_db()` and closes it in `finally` (`:1795`/`:1815`). `runs.py:838`/`:888` also use `ScopedStore(session=db)` but are non-streaming JSON handlers (query runs within request scope → returned normally). `prototype_templates.py:259` StreamingResponse touches no DB in its generator. The correct idiom is exactly what `post_message` uses (`run_commands.py:758` — `ScopedStore` with NO injected session, so every store op self-manages its SessionLocal).
  - **Pre-existing vs recent:** the default-pool config (`database.py:12`, no `pool_size`/`max_overflow`) is PRE-EXISTING — commit `34ac261e` (2026-05-10). The leaky SSE endpoint shipped `7020bf82` (2026-07-08, Phase 29-02) behind a flag, but became the ALWAYS-ON SOLE run-event transport on `7b79b972` (2026-07-15, 44-07, feat/ui-2) — one day before this was observed live. So a latent leak went hot exactly when SSE replaced WS as the sole down-channel; the small pool (pre-existing) is what makes the leak fatal quickly.
- **Fix (PROPOSED — investigation only, NOT applied; a leak fix is more correct than just enlarging the pool):**
  1. **PRIMARY — stop the SSE generator from using the request session (removes the leak).** In `run_stream.py`, do NOT hand the `Depends(get_db)` session to the store consumed inside `_iter_sse_frames`. Construct the generator's store WITHOUT a session — `store = ScopedStore(owner_id=current_user.id, workspace_id=workflow_run.workspace_id)` (session=None) — so each `read_events` opens AND closes its own `SessionLocal` (`authz.py:95-99` owned=True → `:328-330` closes). Keep the request `db` only for the pre-stream Layer-1/Layer-2 owner checks (`:220`, `:239`). Net effect: the replay reads become 2 brief open/close checkouts and NO connection is held during the idle live-drain loop. This mirrors the safe pattern already in `post_message`/the chat SSE. (Alternative but weaker: wrap `_iter_sse_frames` in `try/finally: db.close()` — still holds one connection idle for the entire stream; prefer the session-less store.)
  2. **SECONDARY — right-size the pool (defense-in-depth, pre-existing config).** `database.py:12` — set explicit `pool_size`/`max_overflow` to the concurrent-run target plus resiliency, e.g. `pool_size=20, max_overflow=40, pool_timeout=30, pool_recycle=1800` (tune to expected concurrency; `pool_pre_ping=True` already present). NOTE: sizing ALONE does not fix the leak — with the leak, a bigger pool only delays exhaustion under sustained streaming; #1 is the actual fix, #2 is warranted regardless for multi-run scale.
  3. **Optional hardening:** consider a NullPool/short `pool_recycle` only if #1+#2 prove insufficient; not needed once the leak is closed.
- **Verify (fail-before / pass-after — proposed):**
  1. **Fail-before:** open N≥15 concurrent SSE streams (or fewer with reconnect churn) against a Postgres-backed instance; poll `engine.pool.checkedout()` → climbs to 15 and stays (does not fall between streams); a concurrent `POST /api/runs/{id}/answers` 500s with the `QueuePool limit … timed out` TimeoutError. (Matches the live traceback + `/tmp/sse_pool_repro2.py` `checkedout=1` held-until-GC.)
  2. **Pass-after (#1 applied):** same load → `checkedout()` stays near 0 during idle live-drain (only brief spikes during replay reads); the concurrent `submit_answers` returns 200; no TimeoutError. Re-run repro with the session-less store → `after read_events in gen, checkedout=0` (no post-query hold).
  3. **No-regression:** SSE frames + durable replay + D-14g gate re-arm still correct (owner-scoped reads unchanged — same `ScopedStore`, only its session-acquisition mode changes); the two owner checks still 404 cross-owner.

---

### BUG-005 — Prototype launched with no template: clarify never renders (UI stuck at "Running 0/0 BUILDING")  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-j1u, 8eec694c) — live-proven

- **Found:** 2026-07-16 · live Bedrock QA (heavy concurrent load — same session as BUG-004's ~10+ simultaneous runs) · surface run screen → clarify-gate rendering, on an **`od_prototype` no-template launch** (KAN-87), brief "a website that mimics apple.com". Runs `afb5de91-7017-422e-a1c9-6872109b49f9` + `bea8b22d-4e0d-44f1-a32e-c5b63b6bb41f`.
- **Symptom:** The run screen shows **"Pipeline running · 0/0 · BUILDING"** with ONLY the "Starting point" card and a **"Steer the run"** (building-mode) composer — **no clarifying questions render, nothing progresses, it looks stuck** — even though the run is server-side paused at the clarify gate (`status=waiting_for_user`) with a well-formed 6-question questionnaire ready.
  - **Task-brief premises corrected against the live API (the user/agent capture was stale):** the run `type` is **`od_prototype`** (not `prototype`); `agent_count` is **5** (not 0) with a valid 5-agent DAG (`prototype-specify/plan/analyze/build/validate`); and it carries a **spurious `parent_run_id`/`root_run_id` = `c530695f`** (a completed *user_stories* "Task Manager" run) — a revision-family mis-link from a stale `sourceRunId` in the wizard draft (`page.tsx:984` → `DashboardLayout.tsx:643` `source_workflow_run_id`), the BUG-001/002/003 stale-viewed-run class, but NOT the cause of the stuck clarify.
  - **"0/0 agents" is a red herring.** Agents reach the FE only in the **`pipeline_start`** event, which fires *after* `questionnaire_complete` (verified: completed `ab90f5f4`/`4cd2f478` both emit `pipeline_start{agent_count,agents}` only post-clarify). So *every* workflow shows 0/0 agents while paused at the clarify gate — user_stories included. The bug is the **missing questions**, not the agent count.
- **Root cause (investigation agent — read `.planning/IMPLEMENTATION-REGISTER.md` global + Phase 07 prototype-as-manifest, Phase 25 run-inputs/ClarificationsCard, Phase 28/29 chat+SSE backbone, Phase 31/32/39/42 run-screen/clarify FE; curled the live run detail + `/events` + `/events/stream`; grep-confirmed the launch guard in `run_commands.py`):**
  - **Backend + SSE wire are CORRECT — do not "fix" them.** `GET /api/runs/afb5de91/events/stream` delivers a well-formed live frame `id:5` = `{"data":{questions:[6 …],"round":1,…},"type":"questionnaire_ready"}`; `planner_complete` carries `"no_template_mode": true`. The **`missing_template_context` guard (`run_commands.py:1176-1188`) correctly did NOT fire** — it only rejects when a resolved agent declares `template` in `injects` AND `_needs_template` AND `not od_context.no_template`; the no-template prototype is a **supported KAN-87 feature** (`page.tsx:975-978`, `templateId ?? ""`), so `no_template` exempts it. **So the launch should NOT be rejected pre-mint** (task option (a) = NO) and the compile is **not empty** (5 agents; task option (c) = N/A).
  - **The FE bug (primary, deterministic under concurrency) — a foreign run's `pipeline_start` WIPES the viewed run's clarify.** The run-screen clarify state is a **single, global, page-level React state** (`page.tsx:157` `questionnaireData` → effect `DashboardLayout.tsx:585-589` → `questionnaireQuestions`), and `runLaneState` derives clarify **purely** from it: `laneClarifyOpen = questionnaireQuestions.length>0 && (…activePipelineRunId)` → `runLaneState = … : laneClarifyOpen ? "clarify" : isPipelineRunning ? "building" : …` (`DashboardLayout.tsx:1308-1317`). It is **not bound to the viewed run**. It is fed by the SSE provider's **NON-run-scoped fan-out** — `RunConnectionProvider.tsx:275-283` fans *every frame from every attached run* to the *single* subscriber `handleWebSocketMessage`, and `refreshLiveRuns` (`:226-244`) auto-attaches **all** non-terminal runs on boot (`attachRun` `:305-308` appends more). The `pipeline_start` branch of that one subscriber (`page.tsx:462-482`) then runs **unconditionally, with no run-identity guard**: `resetReplayState(…)` clears the global seen-set (`:463`), `setQuestionnaireData(null)` (`:481`), `setActivePipelineRunId(null)` (`:482`). So the moment **ANY** concurrently-attached run enters its build phase and emits `pipeline_start`, that foreign frame **clears the clarify questionnaire of the run the user is viewing** → `questionnaireQuestions` empties → `runLaneState` falls through `clarify`→`building` → "Pipeline running · 0/0 · BUILDING", no questions, "Steer the run" composer. (The comment at `:477-480` intends this clear only for a *previous* run in the same tab — but the non-run-scoped fan-out makes it fire for *concurrent* runs too.)
  - **Why it hit here and not the "working" user_stories clarify.** Not type-specific: user_stories at clarify is equally vulnerable. Two factors made od_prototype the victim: (1) the od_prototype **planner is slow** (~23s to `questionnaire_ready`: `workflow_validated` 10:30:45.9 → `planner_complete` 10:30:56.5 → `questionnaire_ready` 10:31:08.7), so its clarify gate stays open long enough to be wiped; (2) it was hit under **heavy concurrent load** — the two od_prototype runs were even minted **120ms apart** (`created_at` 10:30:45.246 & .368 — a double-mint), and BUG-004 documents ~10+ simultaneous runs, several *building*. Any of those building runs' `pipeline_start` wipes the clarify. user_stories clarify "worked" because it was exercised in isolation (no concurrent build to wipe it). The two clarify runs alone do NOT self-wipe (neither emits `pipeline_start`); the wipe requires ≥1 concurrent building run — guaranteed under the QA's load.
  - **Sibling cross-run poisoning (same seam, `:463`).** `resetReplayState` on a *foreign* `pipeline_start` also **clears the global `seenEventIdsRef` dedup set** shared by all attached runs, so the viewed run's already-applied events can double-apply / its next reconnect after_seq is wrong — a second facet of the same "one global reducer serving N runs" defect.
  - **PRE-EXISTING — not introduced by the recent DEF-44 SSE work.** `git blame`: the unconditional `setQuestionnaireData(null)`/`setActivePipelineRunId(null)` on `pipeline_start` = `7d95409cf` **Prateek Sinha 2026-06-30** (WR-03 per-run reset, written for the single-run-per-tab assumption); the non-run-scoped fan-out = `1cc8e92ab` **imran-hex 2026-07-08** (Phase 29 SSE backbone); `laneClarifyOpen`/`runLaneState` = `368bc5229` **imran-hex 2026-07-08** (Phase 32 run-screen). All predate the feat/ui-2 DEF-44 campaign. Same **"run-screen live state not bound to the viewed run"** class as BUG-001 (title), BUG-002 (open path), BUG-003 (revise type) — here the **clarify questionnaire**.
- **Fix (PROPOSED — investigation only, not applied):** task option **(b)** — bind the clarify state to the viewed/active run so a foreign run's `pipeline_start` can't wipe it. Minimal + SC-001-safe (keys only on the generic `pipeline_run_id`, zero workflow-name literal):
  1. **`page.tsx:462`** — run-scope the `pipeline_start` reset block: only run `resetReplayState` + `setReviewGateData(null)` + `setQuestionnaireData(null)` + `setActivePipelineRunId(null)` when the incoming frame's `pipeline_run_id` is the run this consumer is tracking — i.e. gate on `data.pipeline_run_id === (activePipelineRunId ?? contentSourceRunId ?? pipelineState.pipelineRunId)` (or the just-launched run id), so a **concurrent foreign** `pipeline_start` is ignored. Preserves the intended "new run in this tab clears the old questionnaire" behavior; drops the cross-run wipe.
  2. **Systemic (recommended) — run-scope the page subscriber itself.** At `page.tsx:909-912` (the `runSubscribe` → `handleWebSocketMessage` bridge) drop frames whose `data.pipeline_run_id` ≠ the viewed/active run before they reach the single global reducer/clarify state, so the non-run-scoped fan-out (`RunConnectionProvider.tsx:276`) can't bleed one run's frames (agents, gate, clarify, seen-set) into another's view. This is the durable fix the BUG-001/002/003 "viewed-run binding" class points to; #1 is the minimal subset that stops *this* symptom.
  - **Secondary (separate, lower-priority): the double-mint.** Two `od_prototype` runs minted 120ms apart = the staged wizard launch (`page.tsx:959` effect, deps `[effectiveConnectionStatus]`) or the launch button firing twice (non-idempotent). Guard the launch so one user action mints one run. Not required to fix the stuck-clarify symptom, but it doubles the concurrent-run count that triggers it.
  - **Do NOT:** reject the no-template launch (KAN-87 supported; guard already exempts), touch the backend clarify/planner, or "fix" the compile (5 agents present).
- **Verify (fail-before / pass-after — proposed):**
  1. **Fail-before:** pause run A at the clarify gate (any workflow); while A is viewed, launch/attach run B and let B reach its build phase (`pipeline_start`). Observe A's clarify questionnaire vanish and A's screen fall to "Pipeline running · N/M · BUILDING" with the "Steer the run" composer and no questions. (Reproduced live: two `od_prototype` clarify runs `afb5de91`/`bea8b22d` under concurrent build load.)
  2. **Pass-after:** same sequence with the reset run-scoped (#1) and/or the subscriber run-scoped (#2) → B's `pipeline_start` no longer clears A's clarify; A's 6 questions stay and are answerable; `submit_questionnaire` advances A. user_stories + od_prototype clarify both survive a concurrent build.
  3. **No-regression:** a fresh single-run od_prototype no-template launch still shows clarify (already correct at the wire); reopening a clarify-paused run from recents/history still replays `questionnaire_ready` → clarify (`handleSelectWorkflowRun` durable seed, `page.tsx:1222`); answering clarify → build still clears the questionnaire for the SAME run.
  4. **Unit:** a `page.tsx`/reducer vitest — a `pipeline_start` whose `pipeline_run_id` ≠ the active run does NOT null `questionnaireData`/`activePipelineRunId` nor clear the seen-set; a matching `pipeline_start` still does. `tsc --noEmit` clean.

---

### BUG-006 — Run-screen pipeline-type label shows the wrong (stale) type for a history-opened run  [🟡 minor] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-lb6, 616d481a) — live-proven

- **Found:** 2026-07-16 · live Bedrock QA · surface run screen → left conversation lane header (`LaneRunHeader`, `data-testid="lane-run-type"`), on a **history/recents-opened COMPLETED app_builder run** ("A URL shortener web app", 15 app_builder agents, "app" deliverable — verified app_builder).
- **Symptom:** The small pipeline-type chip above the lane title read **"USER_STORIES"**, not the run's real type (app_builder → would render "APP_BUILDER"). Everything else on the screen is the OPENED run's correct data — the 15 app_builder agents, audit, and deliverable all render correctly; only the type LABEL is stale. Sibling of BUG-001 (the lane TITLE had the same "bound to the wrong run" defect, fixed today in quick 260716-j1u) and of BUG-003's L2 aggravator (the revise-handler selection, also fixed today) — here the third consumer of the same stale state: the displayed TYPE.
- **Root cause (investigation agent — read `.planning/SSE-QA-BUG-LOG.md` BUG-001/003 + the impl-register run-screen phases 32/39/44; grep-confirmed all 19 `setWorkflowType` writers + git-blamed the symptom line and both sibling fixes):**
  - **Symptom site → cause site.** The chip renders `{type}` at `frontend/src/components/chat/LaneRunHeader.tsx:231` (inside `data-testid="lane-run-type"`, `:228`; CSS `uppercase` at `:229` is why it shows "USER_STORIES" not "user_stories"). `type` = `runType || humanizeRunType(pipelineState?.pipeline_type)` (`LaneRunHeader.tsx:184`) — when `runType` is a truthy string it is used **verbatim** (the `humanizeRunType` fallback is only reached when `runType` is falsy). Feed chain: `frontend/src/components/layout/DashboardLayout.tsx:1745` `runType={workflowType || pipelineState?.pipeline_type}` → `frontend/src/components/chat/RunChatLane.tsx:641` (prop) → `:1172` `runType={runType}` (straight pass-through, no transform) → `LaneRunHeader`.
  - **The mechanism (`DashboardLayout.tsx:1745`).** The displayed type is bound to `workflowType` — DashboardLayout-internal state (`:250-256`, default `"user_stories"`) — which is **only** auto-synced from the live pipeline's declared type by the effect at `DashboardLayout.tsx:330`, gated on `if (pipelineState?.isRunning)` (`:331`, `setWorkflowType(normalised)` at `:345`). On a **completed** history/recents reopen `isRunning` is false → the sync effect never fires → `workflowType` stays stale (its `"user_stories"` default, or whatever the last launched/selected run set it to). The reopen path (`page.tsx handleSelectWorkflowRun`, reached via `onSelectWorkflowRun`/`onOpenRun` at `DashboardLayout.tsx:1505`/`:1539`) sets the viewed-run identity `contentSourceRunId` but **never** `workflowType` — verified: none of the 19 `setWorkflowType` writers (`:250`/`:345`/`:522`/`:531`/`:539`/`:551`/`:569`/`:583`/`:639`/`:688`/`:757`/`:839`/`:856`/`:937`/`:1042`/`:1542`/`:1553`/`:1562`/`:1571`) is on the history-reopen path; they are all launch/feature-select/revise/chain paths. Because `workflowType` is truthy, `workflowType || pipelineState?.pipeline_type` short-circuits to the stale value and never consults the viewed run's real type. The viewed run's type IS available and is exactly what the two sibling fixes already read — `recentRuns.find((r) => r.id === contentSourceRunId)?.type` (`:1250`, `:1351`).
  - **Same broken assumption as BUG-001/003 — "the on-screen run's type is `workflowType`."** That holds for the primary launch flow (the gated effect syncs `workflowType` while `isRunning`), but is false the moment a completed non-latest run is opened from history/recents. The assumption has **three** consumers in this component: (1) the lane TITLE, (2) the REVISE-handler selection, (3) the displayed TYPE chip. The quick-260716-j1u sweep fixed (1) and (2) but missed (3).
  - **PRE-EXISTING, and an unlogged miss in the BUG-001/003 sweep.** `git blame`: the buggy `runType={workflowType || pipelineState?.pipeline_type}` (`:1745`) = **`5003aca47` "feat(39-05): wire the lane Back-to-history + run metadata into the run shell" (imran-hex, 2026-07-11, Phase 39 / RUNUI-06)** — the SAME commit that introduced BUG-001's original always-`recents[0]` title mis-binding. The `isRunning` gate on the sync effect (`:331`) = `0590c2448` (2026-05-19), pre-existing. Today's quick-260716-j1u cluster rebound the two sibling consumers of the same stale `workflowType`: the lane TITLE (`cc2f98f74`, `viewedRun` at `:1349-1353`) and the REVISE-handler selection (`8b4e2eb7b`, `viewedRunType`/`effectiveReviseType` at `:1248-1252`), both keyed on `recentRuns.find(r => r.id === contentSourceRunId)`. It did **not** touch the DISPLAY-type prop at `:1745`, and j1u's `deferred-items.md` (DEF-BUG-002-generic-reopen + WARNING-2) does **not** log it — so BUG-006 is a genuine unlogged sibling that slipped the sweep (fable step 8). Notably the fix's input is already computed and in scope but unused by the display: `viewedRunType` (`:1248`) / `effectiveReviseType = viewedRunType ?? workflowType` (`:1252`). Same **"run-screen state not bound to the viewed run"** class as BUG-001 (title), BUG-002 (open path), BUG-003 (revise type), BUG-005 (clarify) — here the type LABEL.
- **Fix (PROPOSED — investigation only, NOT applied):** bind the displayed type to the VIEWED run, mirroring the two sibling fixes already in this component. Reuse the in-scope `effectiveReviseType` (= `viewedRunType ?? workflowType`, `:1252`), one contiguous edit at `DashboardLayout.tsx:1745`:
  ```
  - runType={workflowType || pipelineState?.pipeline_type}
  + runType={effectiveReviseType || pipelineState?.pipeline_type}
  ```
  (Equivalent, if decoupling from the revise-named const is preferred: `runType={(viewedRunType ?? workflowType) || pipelineState?.pipeline_type}` — identical value.) Both consts are in scope at `:1745` (proven: `activeReviseHandler`, built from `effectiveReviseType` at `:1253`, is consumed at `:1748`; `runHeaderTitle` at `:1354` is consumed at `:1744`). Behaviour by flow:
  - **Fresh launch / live** (`isRunning` true or `contentSourceRunId` null) → `viewedRunType` undefined → `workflowType` (kept correct by the gated sync effect) → **byte-identical to today, no regression**.
  - **Live-complete / history-reopen** (`isRunning` false, `contentSourceRunId` set) → `viewedRunType` = the viewed run's `type` → **correct label (bug fixed)**.
  - **Viewed run older than the recents window** (`find` → undefined) → falls back to `workflowType` → no worse than today (strictly no regression; label stale only for out-of-window runs, same caveat as BUG-001).
  - Minimal + SC-001-safe: one-line edit in a guarded component, keys only on generic run identity (`contentSourceRunId`/`run.id`/`run.type`), **zero** workflow-name literal added, no `page.tsx` change, no new state/prop. Rejected alternative: reorder to prefer `pipelineState?.pipeline_type` — depends on the DEF-44-12-4 durable seed populating `pipeline_type` on reopen (unverified) and diverges from the sibling fixes' proven `recentRuns.find` source.
  - **Optional polish (separate, pre-existing, out of scope):** the chip shows the type RAW (the `runType` branch bypasses `humanizeRunType`, `LaneRunHeader.tsx:184`) then CSS-uppercases it (`:229`), so it renders "APP_BUILDER", not "App Builder". Wrapping in `humanizeRunType(...)` would prettify but also changes the launch-flow formatting — a cosmetic decision unrelated to the wrong-TYPE defect; do not fold it into the minimal fix.
- **Verify (fail-before / pass-after):**
  1. **Fail-before:** with ≥2 runs whose last-launched/selected `workflowType` ≠ the app_builder run's type, open the completed app_builder run "A URL shortener web app" from Run History/recents → assert `data-testid="lane-run-type"` shows the stale type ("USER_STORIES") while the agents/audit/deliverable are the app_builder run's. (Reproduced live 2026-07-16.)
  2. **Pass-after:** same reopen → `lane-run-type` == the OPENED run's type ("APP_BUILDER"); user_stories / od_ppt / prototype reopens each show their own type.
  3. **No launch regression:** a fresh live run shows its own type throughout the build (`contentSourceRunId` null / `isRunning` true → `workflowType` path unchanged).
  4. **Unit:** a `DashboardLayout`/`LaneRunHeader` vitest asserting `lane-run-type` tracks `contentSourceRunId` across a mixed-type 3-element `recentRuns` (mirror the BUG-001 title test); `tsc --noEmit` clean.

---

### BUG-007 — One UI launch mints TWO runs (double POST /api/runs)  [🟡 minor] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-lb6, c959fe44) — dev-only, offline-proven

- **Found:** 2026-07-16 · test LJ-04 (independently seen in the BUG-005 investigation) · surface prototype LaunchWizard → select design system → "Continue" → the Network panel shows `POST /api/runs` fire **twice**, minting two `od_prototype` runs. BUG-005 measured the pair **120 ms apart** (`created_at` 10:30:45.246 & .368). The extra run wastes tokens and doubles the concurrent-run load that triggers BUG-004/BUG-005.
- **Symptom:** A single "Continue" click → 2× `POST /api/runs` → 2 sibling runs for one launch intent.
- **VERDICT UP FRONT — dev-only React-StrictMode artifact, NOT a production double-mint (verified).** In a production build the launch mints **once**; the second POST appears **only** under `next dev` (React StrictMode's dev-only mount-effect double-invoke). See the dev-vs-real analysis below. Severity **🟡 minor** per the task rubric (a prod build does not double-mint).
- **Root cause (investigation agent — grounded on the live launch/staging source as ground truth + `IMPLEMENTATION-REGISTER.md` prior-art on this exact consumer, lines 2900 / 3168-M4 / 3404; the 797 KB register was not read line-by-line, the code was traced directly):**
  - **The launch has exactly ONE mint path (ruled out a second launch site).** `LaunchWizard.handleLaunch` (`frontend/src/components/workflow/LaunchWizard.tsx:436-483`) only writes `sessionStorage` (draft `:469`, `od_prototype.pending="true"` `:477`) then `router.push("/dashboard")` (`:478`) — **it does not mint.** The sole mint is the staged path: staging effect → `pendingOdProtoParams` state → DashboardLayout consumer effect → `onStartPipeline` → `startPipeline` → `POST /api/runs`.
  - **The actual mint has NO in-flight/idempotency guard.** `useWorkflow.startPipeline` (`frontend/src/hooks/useWorkflow.ts:54-127`) unconditionally calls `runConnection.sendCommand(null, payload)` (`:126`) — `runId=null` ⇒ launch ⇒ `POST /api/runs`. Every call mints; there is no "already launching" latch. So one extra `onStartPipeline` call = one extra run.
  - **The double-fire is in the CONSUMER effect (`frontend/src/components/layout/DashboardLayout.tsx:636-679`, deps `[pendingOdProtoParams]`).** Its ONLY guard is `if (!pendingOdProtoParams) return;` (`:637`); it then "clears" via `onClearPendingOdProto()` (`:642`) = `page.tsx:1554` `() => setPendingOdProtoParams(null)` — an **async parent setState** — and mints at `onStartPipeline("od_prototype", …)` (`:668`). Because the clear is async, it does **not** null the param **between** StrictMode's two *synchronous* mount invocations, so both invocations pass the `!pendingOdProtoParams` guard and both mint.
  - **Why the consumer effect runs on MOUNT with a non-null param (the enabling ordering).** DashboardLayout is gated out of the tree by `page.tsx:1412` `if (!mounted || !isAuthenticated) return null;` — `mounted` flips post-hydration (`:64`/`:72`) and `isAuthenticated` flips only after the **async** auth/profile fetch (`:66`/`:219`). Meanwhile the staging effect (`page.tsx:1004-1072`, deps `[effectiveConnectionStatus]`) is a hook registered *before* that early return, so it runs anyway, and `effectiveConnectionStatus` is **"connected" from the first render** because `phaseToConnectionStatus` maps the provider's initial `idle` phase → `"connected"` (`page.tsx:36-52`; provider initial `phase:"idle"` at `RunConnectionProvider.tsx:108`, mounted in the ROOT `frontend/src/app/layout.tsx:43` so it persists across the wizard→dashboard client nav). Net timeline: connection is connected → staging effect consumes the flag and `setPendingOdProtoParams(objA)` **while the render gate still returns null** → async auth resolves → **DashboardLayout mounts for the first time with `pendingOdProtoParams` already non-null** → its consumer effect fires on MOUNT (not on a dep-change) → StrictMode double-invokes the mount → **2 mints.** (Because idle→connected almost always beats the async auth round-trip, this is the *common* dev case, not a rare race — consistent with it being reproduced live.)
  - **Why the STAGING effect is NOT vulnerable but the consumer IS (the asymmetry).** The staging effect consumes its trigger **synchronously before** the side effect: `pendingOdProtoRef.current = null` + `sessionStorage.removeItem("od_prototype.pending")` + `removeItem("prototype.draft")` (`page.tsx:1039-1041`) run *before* `setPendingOdProtoParams` (`:1053`). Refs and `sessionStorage` persist across StrictMode's mount double-invoke, so the 2nd invocation reads the already-cleared flag and returns at `:1010` — **idempotent.** The consumer effect has no such synchronous consume; it relies on an async parent setState, which is exactly what StrictMode's synchronous re-invoke defeats. That missing synchronous latch is the root defect.
- **Dev-StrictMode-vs-real (the critical distinction, verified):**
  - **StrictMode IS enabled here, by default.** `frontend/next.config.ts` omits `reactStrictMode`; installed Next 16.2.4 resolves that to ON for the App Router: `node_modules/next/dist/build/define-env.js:144` — `'process.env.__NEXT_STRICT_MODE_APP': config.reactStrictMode === null ? true : config.reactStrictMode`, consumed at `node_modules/next/dist/client/app-index.js:217` — `StrictModeIfEnabled = process.env.__NEXT_STRICT_MODE_APP ? React.StrictMode : React.Fragment`. So the tree is wrapped in `<React.StrictMode>`.
  - **StrictMode's extra setup→cleanup→setup of effects is DEV-ONLY.** In a React **production** build `<React.StrictMode>` is an inert passthrough (no mount double-invoke). The Docker/standalone deploy runs a prod build — `frontend/Dockerfile` builds `.next/standalone` and runs `CMD ["node","server.js"]` with `ENV NODE_ENV=production` (compose `frontend` service uses this image) — so it **cannot** double-invoke.
  - **In production the launch mints exactly once (verified by mechanism, no StrictMode path):** (1) the staging effect is `sessionStorage`-idempotent (consume-before-set), so `pendingOdProtoParams` is set at most once per staged launch even if `effectiveConnectionStatus` re-fires the effect; (2) the consumer effect fires once per committed param value — mint once, then `onClearPendingOdProto` nulls it and the re-fire returns at `:637`. No non-StrictMode double-mint path exists (handleLaunch doesn't mint; the `connectionStatus==="connected"` branch at `:667` is if/else with the `pendingStartOnConnectRef` queue, never both).
  - **Therefore:** the two POSTs are the **StrictMode dev double-invoke of the consumer mount effect** — a `next dev` artifact. The very fact it was *observed* is itself strong evidence the QA frontend ran `next dev` (a prod build would not exhibit it via this mechanism). *Caveat (unverified):* I could not confirm from the repo whether this specific live session was served by `next dev` vs the standalone build — but the intermittent/dev-shaped signature, the local-Bedrock dev workflow, and the present `.next/dev/*` artifacts all indicate `next dev`. If (contrary to the evidence) the session were a prod build, this mechanism would NOT explain it and a different cause would need hunting — but the mechanism above is prod-safe.
- **Pre-existing / class — PRE-EXISTING, not introduced by the feat/ui-2 DEF-44 SSE campaign.** `git blame`: the consumer effect (the whole `useEffect(() => { if (!pendingOdProtoParams) return; … })`) = **`52296f44` Prateek Sinha 2026-05-26** ("prototype tweaks … questionnaire flow"); the render gate `!mounted || !isAuthenticated` = `2d2d35b69` (2026-05-25); the `onStartPipeline` mint line = `4889e3a85` (2026-06-02); the staging effect's synchronous clear = `0590c2448`/`d2970ff7f` (May). All predate the SSE cutover. The register already documents this exact `setPendingOdProtoParams`/`setPendingOdPptParams` consumer as a pre-existing, separately-patched FE hand-off (register 2900/3404, the images-drop bug), and M4 (register 3168) notes a sibling **re-mint** class on the revision path (a replayed `message_id` re-minting a revision child) — different trigger, same "one intent should mint once" invariant. **Structural twin:** the od_ppt consumer effect (`DashboardLayout.tsx:685-727`) has the identical missing-guard pattern → same latent dev double-mint for od_ppt; any fix must cover both.
- **Fix (PROPOSED — investigation only, NOT applied):** add a **synchronous** already-fired latch to the consumer effect so one param object mints at most once, robust to StrictMode's double-invoke — mirroring the consume-before-side-effect idempotency the staging effect already has. Minimal, SC-001-safe (keys only on generic param identity, zero workflow-name literal):
  ```
  // module/component scope:
  const launchedProtoParamsRef = useRef<object | null>(null);
  // inside the effect, DashboardLayout.tsx:636-637:
      if (!pendingOdProtoParams) return;
  +   if (launchedProtoParamsRef.current === pendingOdProtoParams) return; // StrictMode 2nd mount-invoke: same object identity → skip
  +   launchedProtoParamsRef.current = pendingOdProtoParams;
  ```
  Each staged launch produces a fresh `{…}` object (`page.tsx:1053`), so identity comparison correctly fires once per real launch while suppressing the synchronous StrictMode re-invoke (the ref persists across the mount double-invoke, unlike the async `setPendingOdProtoParams(null)`). Apply the identical latch to the od_ppt twin (`DashboardLayout.tsx:685-687`, `launchedPptParamsRef`). No `page.tsx` change, no new state/prop, no backend touch.
  - **Rejected alternatives:** (a) setting `reactStrictMode: false` in `next.config.ts` — WRONG: it hides a whole class of dev-surfaced non-idempotency bugs and does not fix the genuine missing latch (prod already single-mints, so this changes nothing shippable while blinding dev). (b) an in-flight guard inside `startPipeline` (`useWorkflow.ts`) — larger blast radius (affects every launch/revise caller) and would also swallow legitimate rapid re-launches; the local ref-latch at the intent boundary is smaller and exactly scoped. (c) making `onClearPendingOdProto` synchronous — not possible without lifting `pendingOdProtoParams` out of `page.tsx` state; the ref-latch is simpler.
- **Verify (fail-before / pass-after — proposed):**
  1. **Fail-before (dev):** `next dev` (StrictMode on); prototype LaunchWizard → pick DS → "Continue"; assert **2×** `POST /api/runs` in the Network panel and 2 `od_prototype` rows in `GET /api/runs` for one click (repro of LJ-04 / BUG-005's 120 ms pair). Confirm **1×** in a `next build && next start` (or standalone) run to prove the dev-only nature.
  2. **Pass-after:** with the ref-latch, `next dev` fires **exactly 1×** `POST /api/runs` per "Continue"; a second, genuinely new launch (fresh param object) still mints; od_ppt "Continue" also single-mints.
  3. **No-regression:** a real back-to-back two-launch sequence mints twice (distinct param objects); the staging-effect reload path (survives refresh via `sessionStorage`) still mints once; `tsc --noEmit` clean.
  4. **Unit:** a `DashboardLayout` vitest that mounts with a non-null `pendingOdProtoParams` and simulates a StrictMode-style double mount-invoke → asserts `onStartPipeline` is called **once**; a subsequent new param object → called again.

---

### BUG-008 — Reopened generic/custom deliverable renders empty on the shared run screen  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-n2d, `3cce39df`) — decoupled `hasGenericDeliverable` (`PreviewPanel.tsx:577`) from the stale-`workflowType` gate via a `knownContentPresent` guard: a present generic deliverable now renders whenever the typed renderer for the (possibly-stale) `renderType` has nothing to show, so a terminal reopen no longer short-circuits to "Output will appear here". RED→GREEN unit (2 cases: `text/html`→P18 sandboxed iframe with no `allow-same-origin`; `text/markdown`→MarkdownPreview) + TS-T-04b un-`fixme`'d (real chromium History-tap of a `custom` completed HTML run → sandboxed Deliverable Preview iframe renders). tsc clean; `WorkflowHistory.genericReopen` + at-risk suites green; SC-001 clean (no workflow-name literal). **Offline-proven via the real-browser e2e** — a completed run's FE render gate is backend-agnostic (page.tsx seeds `genericDeliverable` from `fullRun.output` regardless of producer), so no live Bedrock custom run was required.

- **Tracks:** `DEF-BUG-002-generic-reopen` (deferred-items.md, quick 260716-j1u) · tracking test `e2e/tests/ts-t.history.spec.ts` TS-T-04b (`test.fixme`, :166).
- **Found:** 2026-07-16 · run-screen reopen of a `custom`/unknown-type deliverable · surface = shared run screen (execution view) PreviewPanel. Reached via BOTH the BUG-002 History-tap route (`onOpenRun`) and the pre-existing Home-recents route — both funnel through `page.tsx handleSelectWorkflowRun` → the single execution-view `<PreviewPanel>`.

- **Symptom:** Reopening a completed `custom`/unknown-type run whose `output` is HTML renders the run screen's Preview tab as the neutral empty state **"Output will appear here"** (`PreviewPanel.tsx:1018`) instead of the sandboxed `Deliverable Preview` iframe. The Files tab also loses its generic deliverable row. TYPED deliverables (user_stories/ppt/prototype/app_builder) render correctly. The OLD `RunDetailPage`/`WorkflowHistory` in-panel detail rendered the same generic deliverable fine (locked by `WorkflowHistory.genericReopen.test.tsx`).

- **Root cause — a DISPATCH/GATE gap keyed on a stale `workflowType`, NOT a data-loading gap. Pin: `PreviewPanel.tsx:577` (+ its upstream `:536`), fed a stale `workflowType` from `DashboardLayout.tsx:1825` because the binder at `DashboardLayout.tsx:330-368` is `isRunning`-gated (`:331`).**

  The surface hypothesis in `deferred-items.md` (page.tsx "does not repopulate `genericDeliverable` on this path") is **incorrect** — the data IS loaded and passed. Full chain:

  1. **Data IS seeded + passed (so this is NOT a data gap).** `handleSelectWorkflowRun` clears content (`page.tsx:1216`), then in the generic `else` branch — gated on `fullRun.output && isContentTerminal` (`:1293-1295`, completed/degraded) and a non-known type — DOES call `setGenericDeliverable({ mimetype: resolveReopenMimetype(...), filename, content: fullRun.output })` (`page.tsx:1311-1318`). For the TS-T-04b fixture (`type:"custom", status:"completed", output:"<!DOCTYPE html>…"`) this branch **fires**: `genericDeliverable.content` = the HTML, mimetype `text/html`. It is passed to `<DashboardLayout genericDeliverable={genericDeliverable}>` (`page.tsx:1428`) and on to `<PreviewPanel genericDeliverable={genericDeliverable}>` (`DashboardLayout.tsx:1823`). Verified present.

  2. **The gate suppresses it.** `PreviewPanel` decides "is there content to show" via `hasContent = !!(effUserStoryContent || effPptContent || effPrototypeContent || pptxCode || hasGenericDeliverable)` (`:579`), where `hasGenericDeliverable = !isKnownRenderType && !!genericDeliverable?.content` (`:577`) and `isKnownRenderType = KNOWN_RENDER_TYPES.includes(renderType)` (`:575-576`). `renderType` normalizes `detectedType = workflowType || (userStoryContent?…:…:"user_stories")` (`:536`). On a completed reopen the typed content is all empty, so `detectedType` collapses to the `workflowType` prop — and **`detectedType` never inspects `genericDeliverable`**, so it can only reach a non-known value if `workflowType` itself is non-known.

  3. **`workflowType` is stale on reopen.** The prop is `DashboardLayout`'s own `workflowType` state (`:1825`), default `"user_stories"` (`:250-256`). The effect that would rebind it to the viewed run's declared type (`:330-346`) is gated on `if (pipelineState?.isRunning)` (`:331`) and **never runs for a terminal reopen** (`isRunning` false). So `workflowType` stays `"user_stories"` → `renderType="user_stories"` → `isKnownRenderType=true` → `hasGenericDeliverable=false` → `hasContent=false`.

  4. **Empty state short-circuits before the renderer that WOULD have worked.** With `hasContent=false`, the preview tab takes the `!hasContent` branch (`:1016-1019` → "Output will appear here") and **never calls `renderDeliverable()`** — whose step-1 first-party lookup returns null (no typed content) and whose step-2 `if (genericDeliverable?.content) return <GenericDeliverablePreview …>` (`:890-891`) would have rendered the HTML in the P18 sandboxed iframe. The renderer is correct; the `hasContent`/`hasGenericDeliverable` gate (workflowType-dependent) starves it. `hasGenericDeliverable` also gates the Files-tab generic row (`:1081`), which is why the Files tab loses it too.

  **Why the OLD path (RunDetailPage/WorkflowHistory) renders it:** `WorkflowHistory` derives its type from the **actual reopened run** — `const workflowType = selectedRun.type` (`WorkflowHistory.tsx:377`) — so `isGeneric = !isUserStory && !isAppBuilder && !isPpt && !isPrototype` (`:401`) is correctly true for a `custom` run; it then resolves the mimetype straight from the output via the SAME `resolveReopenMimetype(selectedRun.deliverableMimetype, selectedOutput)` (`:403`) and renders HTML→sandboxed iframe (`:666-676`, `srcDoc`, `sandbox="allow-scripts"`, no `allow-same-origin`) / zip→bundle (`:682`) / else→MarkdownPreview (`:687`). It depends on the run's own type + output shape, **never** on a DashboardLayout-global `workflowType`. That is the exact difference: the OLD path self-detects; the execution-view PreviewPanel relies on a `workflowType` binding that the reopen path never establishes.

- **Pre-existing verdict — PRE-EXISTING; BUG-002 exposed it, did not introduce it.** All constituent lines predate the BUG-002 fix (`e8a22842`, 2026-07-16): the `isRunning`-gated binder `DashboardLayout.tsx:331` = `0590c2448` (imran-hex, 2026-05-19) — the SAME stale-`workflowType` root as BUG-003 L2 and BUG-006; the `workflowType={workflowType}` prop `:1825` = `4f7ec0898` (2026-05-07); the workflowType-gated `hasGenericDeliverable` `PreviewPanel.tsx:577` = `3ae121209` (imran-hex, 2026-06-13, Phase 18-03 — the generic-deliverable feature only ever rendered on the LIVE path, where `workflowType` is bound during `isRunning`); its folding into `hasContent` `:579` = `c5d8e4449` (2026-07-02); `detectedType` `:536` = `365b3a58f` (2026-05-06). BUG-002 (routing History taps into the execution view) merely made the custom-History-tap case reach this gate; the Home-recents route already shared it. Same "run-screen state not bound to the viewed run" class as BUG-001 (title)/BUG-003 (revise type)/BUG-005 (clarify)/BUG-006 (type label) — here the deliverable-render TYPE. NOT covered: `WorkflowHistory.genericReopen.test.tsx` renders `WorkflowHistory` directly (`:103`), never the execution-view PreviewPanel, so the run-screen generic reopen is genuinely untested (TS-T-04b `test.fixme` confirms).

- **Fix proposed (MINIMAL, not applied) — decouple the generic-deliverable "has content" signal from the stale-`workflowType` gate. Preferred: `PreviewPanel.tsx:577`.**
  Make a present generic deliverable count whenever the typed renderer for the (possibly-stale) `renderType` has nothing to show:
  ```
  // PreviewPanel.tsx:577 — before:
  const hasGenericDeliverable = !isKnownRenderType && !!genericDeliverable?.content;
  // after:
  const knownContentPresent = !!(effUserStoryContent || effPptContent || effPrototypeContent || pptxCode);
  const hasGenericDeliverable = !!genericDeliverable?.content && (!isKnownRenderType || !knownContentPresent);
  ```
  Effect: reopen custom (stale `workflowType="user_stories"`, no typed content) → `false || true` = true → `hasContent` true → `renderDeliverable()` runs → step-1 first-party returns null → step-2 renders the sandboxed iframe (and the Files-tab row via `:1081`). No regression: a live/first-party run with typed content has `knownContentPresent=true` + `isKnownRenderType=true` → `false || false` = false (and `genericDeliverable` is undefined there anyway); a live custom run is unchanged (`isKnownRenderType=false`); a terminal-FAILED run (status `failed`, not `completed`/`degraded`) never gets a `genericDeliverable` so `showFailureAffordance` (`:602`) is unaffected; a `degraded` run WITH a partial deliverable now renders it, which is the WR-01 intent. `renderDeliverable()` (`:868-895`) and `GenericDeliverablePreview` (the P18 `sandbox="allow-scripts"` / no `allow-same-origin` contract, `:178-215`) are untouched. Robust to runs outside `recentRuns` and to an empty/undefined `workflowType`. Preserves the launch/live flow byte-identically.

  **Alternative (pattern-consistent with BUG-003/006, but narrower):** at `DashboardLayout.tsx:1825` pass `workflowType={viewedRunType ?? workflowType}` (and `:1826` similarly), reusing the already-computed `viewedRunType` (`:1265-1268`). This binds `detectedType` to the viewed run's real type (`"custom"`) → `isKnownRenderType` false → renders. Caveat: `viewedRunType = recentRuns?.find(r => r.id === contentSourceRunId)?.type` is `undefined` when the reopened run is not in `recentRuns` (History surfaces runs beyond the recents window) → the bug persists for those. Prefer the PreviewPanel fix (no recentRuns dependency); the two are compatible if both are desired.

- **Verify (fail-before / pass-after):**
  1. **Unit (new, PreviewPanel):** mount `<PreviewPanel workflowType="user_stories" userStoryContent="" pptContent="" prototypeContent="" genericDeliverable={{content:"<!DOCTYPE html><h1>Hi</h1>", mimetype:"text/html"}} pipelineState={{isRunning:false}} />` → assert an `iframe[sandbox="allow-scripts"]` whose `srcdoc` contains the HTML AND `sandbox` lacks `allow-same-origin`; assert NO "Output will appear here". Fail-before (empty state), pass-after (iframe). Add a markdown-mimetype case → MarkdownPreview.
  2. **e2e:** un-`fixme` `TS-T-04b` (`e2e/tests/ts-t.history.spec.ts:166`) — History-tap a `custom` completed HTML run → assert `data-testid="execution-chat-lane"` present AND the sandboxed iframe renders in the shared run screen. Mirror for the Home-recents tap.
  3. **No-regression:** `WorkflowHistory.genericReopen.test.tsx` (6) still green; reopened user_stories/ppt/prototype/app_builder still render bespoke (no iframe); the 8 pre-existing PreviewPanel vitest baseline unchanged; `tsc --noEmit` clean; SC-001 grep still 0 (fix keys on `genericDeliverable.content` + generic content flags, no workflow-name literal).

---

### BUG-009 — od_ppt can complete with an empty deck output (LV-02 recurrence)  [🟠 major] [ROOT-CAUSED]

- **Found:** 2026-07-16 · backend root-cause investigation (upstream of BUG-003's blank Preview + no-op revise) · surface: `od_ppt` pipeline deliverable persist (`WorkflowRun.output`). Live shape: a `completed` od_ppt run whose `wr.output` is empty/`NULL` (deck never persisted), so the FE Preview is blank and the reopen-revise silently no-ops (BUG-003).

- **Symptom:** An `od_ppt` run finishes with `status="completed"` but `wr.output` is empty (or non-deck narration text). No `presentation.pptx` deliverable is persisted; no `kind="deliverable"` artifact_ref is written, so the FR-014 revision chain also has no parent deck to revise. Intermittent — the same manifest/agents produce a correct 18k-char deck on most runs (Phase-15 live proof, register 1605) and an empty/narration deliverable on others.

- **Root cause (investigation agent — read `.planning/IMPLEMENTATION-REGISTER.md` global to EOF incl. LV-02 refs 79/1487/1495/1512/1553/1559/1594/1605 + `.planning/SSE-QA-BUG-LOG.md` BUG-003; traced the deck pipeline end-to-end in source + git-blame):**

  - **The deliverable is defined as the LAST agent's stream, and the last agent is a QA validator that must re-emit the entire deck.** od_ppt's manifest declares `deliverable: strategy: ppt, name: presentation.pptx` (`backend/agents/workflows/od_ppt/workflow.yaml:11-13`) and three steps `od-ppt-brief-analyst → od-ppt-composer → od-ppt-validator`, the **validator LAST** (order 3) (`workflow.yaml:16-25`). The engine binds `ectx.last_streamed = results[-1]["output"]` (`backend/agents/execution_engine/engine.py:2395`) — `results[-1]` is the last step = **od-ppt-validator**. Then `final_output = _resolver.resolve(ectx)` (`engine.py:2406`) routes to the `ppt` resolver.

  - **The `ppt` resolver reads ONLY the validator's stream** (`backend/agents/capabilities/deliverables/ppt.py:63-65`): `return unwrap_artifact(sanitize_carousel_deck_html(ctx.last_streamed))`. `unwrap_artifact` is **first-match** over `<artifact[^>]*>…</artifact>` and **passes the text through unchanged when there is no `<artifact>`** (`backend/agents/capabilities/deliverables/_artifact.py:34-38`). The resolver never reads the sandbox and never looks at any earlier agent's stream.

  - **Why `final_output` is empty/non-deck (the actual LV-02 mechanism — a SOFT prompt contract on the last agent, three failure shapes):** the validator (`od-ppt-validator/AGENT.md`) carries a Phase-15 `## OUTPUT CONTRACT — NON-NEGOTIABLE` (`AGENT.md:23-38`) demanding it begin with `<artifact` and re-emit the complete deck. That is a *prompt instruction on a cheap live model (Haiku), not a hard guarantee.* On a non-compliant completion the resolver yields non-deck/empty:
    - **(a) narration, no artifact** → `unwrap_artifact` finds no `<artifact>` and returns the QA narration verbatim → `final_output` is non-empty but NOT a deck (the 2026-06-12 LV-02 shape: a 1,350-char QA report won over the 21k deck; register 1495).
    - **(b) empty assistant stream → strictly EMPTY output** → the validator declares `tools: [workspace]` (`AGENT.md:17-18`), so it can emit an all-tool-call turn (`write_file` the deck to the sandbox) and stream *no* assistant text → `results[-1]["output"] == ""` → `ectx.last_streamed == ""` → `final_output == ""`. The resolver reads the stream, **not** the sandbox, so the disk deck is discarded. This is BUG-003's observed empty `fullRun.output`.
    - **(c) leading status artifact** → a first `<artifact>…status…</artifact>` before the deck artifact → first-match `unwrap_artifact` extracts the status blob, not the deck (register 1595 warns of exactly this).

  - **The engine's only fallback does NOT catch it.** `engine.py:2407-2413` falls back to `streamed_text` **only when `final_output is None`**. `PptResolver.resolve` returns a **string** (possibly `""`), never `None` — so the empty case bypasses the fallback entirely.

  - **The empty deliverable then fails to persist and poisons the revision chain.** `final_output` rides `pipeline_complete` → read at `backend/app/api/run_commands.py:1416` → persisted only under `if final_output: wr.output = final_output` (`run_commands.py:1445-1446`), so an empty `final_output` leaves `wr.output` unset. In the engine, `if final_output and results:` (`engine.py:2428`) likewise **skips the `kind="deliverable"` artifact_ref write**, so a later `run_revision` (FR-014) has no parent deck (the server-side reason BUG-003's revise no-ops even on the REST path).

  - **A valid deck exists one stream earlier and is thrown away.** The od-ppt-composer (Deck Engineer, step 2) streams the complete deck wrapped in a single `<artifact>` as its contract (`backend/agents/prompts/od-ppt-composer/AGENT.md:50-52, 104-117`) and declares `produces: [od-ppt-composer]` (`AGENT.md:18-19`). The engine therefore captures the composer's deck in TWO durable places at resolution time: `results[-2]["output"]` (a local, `engine.py:3664`) **and** a typed `ArtifactRef` `kind="od-ppt-composer"` in `ectx.artifacts` (`engine.py:3651-3660`, reachable via `ectx.artifacts.tree(ectx.run_id)` — the engine already scans that tree at `engine.py:2441`). The `ppt` resolver consults **neither**. So the validator is a **single point of failure between a good deck and the deliverable.**

  - **Classification — PROMPT-RELIABILITY, not a parser bug.** The parser (`unwrap_artifact`/`sanitize_carousel_deck_html`, ppt.py/_artifact.py) behaves exactly per contract: given a compliant validator stream it yields the deck (Phase-15 live-resolved an 18,661-char deck, register 1605). The defect is that the *deliverable-producing agent is an LLM QA pass that unreliably re-streams a ~20k-char deck.* Phase 15 mitigated it with a prompt contract; a prompt contract is inherently non-deterministic and **recurs** (BUG-003, 2026-07-16, states this outright). The `tools: [workspace]` grant is an aggravator specific to the strict-EMPTY shape (b).

  - **Golden blind spot (why offline never catches it).** The characterization scripted model drives od-ppt-validator as **text-only** and always emits the deck as one `<artifact>` (`tests/agents/_scripted_model.py:293-296, 323-340` — note it literally comments the validator as "text-only (no filesystem tools)", contradicting the live `tools: [workspace]`). So the od_ppt golden can never reproduce shapes (a)/(b)/(c); LV-02 is a live-only escape.

  - **Pre-existing verdict — PRE-EXISTING and a KNOWN, deliberately prompt-only-mitigated failure mode (LV-02); NOT introduced by the feat/ui-2 / SSE work.** `git blame`: the ppt resolver's stream-only `resolve` = `d677944dd` (imran-hex, **2026-06-08**, Phase 7); the persist guard `if final_output:` (`run_commands.py:1445`) = `3d951b2da` (imran-hex, **2026-07-08**, Phase 29 era); the engine `last_streamed = results[-1]` binding = Phase 7. The validator output contract was added by Phase 15 (**2026-06-13**) and then **re-touched again** by `262496c6` "fix: PPT pipeline preview generation and display" (Prateek Sinha, **2026-06-18**, which also changed +24 lines of `engine.py` and the brief-analyst AGENT.md). Despite two prompt iterations the failure recurs live — confirming the mitigation is soft. Same LV-02 the register tracks as "closed by Phase 15 but recurs" (register 1512).

- **Fix (PROPOSED — investigation only, NOT applied; respects the READ-ONLY locks on `ppt.py`/`_artifact.py`, register 1594):**

  There is a genuine tension: register 1559/1594 **LOCK** the LV-02 remedy to "the validator's output contract" and explicitly REJECT a `deliverable: ppt` resolver fallback. But a prompt contract **cannot be a hard guarantee** (it is what already recurs). So the two options trade off compliance vs. certainty:

  1. **PRIMARY (lock-compliant, ships now, but SOFT — does not fully guarantee the deck).** Harden the validator toward a strict pass-through in `backend/agents/prompts/od-ppt-validator/AGENT.md`:
     - Remove `workspace` from `tools:` (`AGENT.md:17-18`) → make it **text-only**, eliminating failure shape (b) (it can no longer sink the deck to disk and stream nothing; all output is forced into the stream). This also realigns the live agent with the scripted golden's own "text-only" assumption (`_scripted_model.py:293`). ⚠ Note register 1593 cautions against "normalizing" this intentional `tools: [workspace]` (RESEARCH Pitfall 4) — reversing it is a conscious decision to make here, justified because the validator makes no legitimate use of the workspace (it only re-emits text).
     - Keep/strengthen the begin-with-`<artifact` + exactly-ONE-artifact clauses (already present, `AGENT.md:25-38`) to bound shapes (a)/(c).
     - **Limit:** shapes (a)/(c) remain possible on any live model — this reduces recurrence, it does not eliminate it. Not a hard guarantee.

  2. **DURABLE (the only option that makes the deck a HARD guarantee) — a NEW opt-in deliverable resolver, ZERO kernel/locked-file edits.** Add `backend/agents/capabilities/deliverables/ppt_guarded.py` registered as `@register("deliverable", "ppt_guarded")`, and flip **one manifest line** `backend/agents/workflows/od_ppt/workflow.yaml:12` `strategy: ppt` → `strategy: ppt_guarded`. The engine already routes any declared strategy via `registry.resolve("deliverable", compiled.deliverable.strategy)` (`engine.py:2405`) — no kernel edit. The new resolver:
     - `deck = unwrap_artifact(sanitize_carousel_deck_html(ctx.last_streamed))` by **importing** the locked helpers from `_artifact.py` read-only (import is allowed; the files are not edited);
     - returns `deck` when it is a real deck (`<!DOCTYPE html` / `<section` present) → **byte-identical to `ppt` on the happy path**;
     - else recovers the deck from `ctx.artifacts.tree(ctx.run_id)` — the most-recent earlier artifact that unwraps to a deck (the composer's `od-ppt-composer` ref, `engine.py:3651-3660`) — a **structural** deck-shape match, no workflow-name/agent-id branch in the kernel (SC-001/INV-1 safe).
     - **INV-3 stays green:** the od_ppt characterization golden's scripted validator always emits a proper `<artifact>` deck, so `ppt_guarded` returns byte-identical output to `ppt`; the fallback branch only fires on the live bug shape the golden never produces (re-run the targeted characterization battery to prove `git status --porcelain golden/` is empty).
     - **Honest caveat:** this partially revisits the register-1559/1594 LOCK (which pinned the fix to the prompt and rejected a resolver fallback). It differs from the rejected form in that it is a NEW registered capability (not an edit to `ppt.py`/`_artifact.py` and not a kernel change) and preserves INV-3 — but the lock owner should sign off on reversing that decision before applying.

  - **Do NOT:** edit `ppt.py`/`_artifact.py` (READ-ONLY, register 1594); add a `if final_output is None`→also-empty branch or a deck scan inside the kernel resolution block (`engine.py:2405-2413`) — that is the register-rejected engine-side change and is golden-sensitive; or "fix" it purely by enlarging the validator prompt again (already done twice, still recurs).
  - **Optional (architectural, larger — out of minimal scope):** stop making a QA agent the deliverable producer at all — e.g. give the validator `revises_existing`/sandbox-edit semantics so the deliverable is read from the composer-written + validator-edited sandbox file rather than re-streamed. Removes the re-emit fragility entirely but is a manifest+strategy change, not minimal.

- **Verify (fail-before / pass-after — proposed):**
  1. **Fail-before:** drive the real od_ppt engine with a scripted validator that emits (a) QA narration with no `<artifact>`, (b) an empty assistant stream, and (c) a leading status artifact; assert `final_output`/`wr.output` is empty or non-deck and **no** `kind="deliverable"` ref is written (`engine.py:2428`). (Extend `tests/agents/test_prompt_contracts.py`'s LV-02 composition test, which currently only pins the *compliant* path.)
  2. **Pass-after (option 2):** same three scripted validators through `strategy: ppt_guarded` → `final_output` is the composer's deck (`<section …>` present) every time; `wr.output` persisted; deliverable ref written; a subsequent `od_ppt_output` revision resolves a real parent deck.
  3. **No-regression / INV-3:** re-run the targeted characterization battery — the 5 goldens stay byte-identical (`git status --porcelain golden/` empty); a compliant validator still yields the identical deck bytes as `strategy: ppt`.
  4. **Live:** one real-Haiku od_ppt run under the failure conditions (or repeated runs) → Preview renders the deck, `wr.output` non-empty, revise fires (closes the BUG-003 upstream).

---

### BUG-010 — Run-scope ref mistracks when a background build is active while viewing a different run (j1u WARNING-2)  [🟢 NON-ISSUE — no fix needed]

**Class:** run-screen live state not bound to the viewed run (same family as BUG-001/002/003/005/006).
**Severity:** 🟢 minor / effectively NON-ISSUE as framed (see Reachability verdict).
**Status:** INVESTIGATED — do NOT fix (a priority flip regresses BUG-005's primary case). Evidence below.
**Branch:** feat/ui-2. All cited fix sites are from commit `8eec694c` (quick-260716-j1u BUG-005), 2026-07-16.

---

#### Found
Plan-checker WARNING-2, logged in `.planning/quick/260716-j1u-.../deferred-items.md:15-17` and `260716-j1u-SUMMARY.md:100`: BUG-005's `trackedRunIdRef` syncs from priority `activePipelineRunId ?? contentSourceRunId`; the checker feared that a background build A (claimed `activePipelineRunId = A`) plus opening a different completed run C from history (`contentSourceRunId = C`) resolves `tracked = A`, so a later revision of C is guard-skipped and C's clarify/seen-set corrupted.

The actual j1u fix (all commit `8eec694c`, verified via `git blame`):
- **Declaration** — `frontend/src/app/dashboard/page.tsx:156` `const trackedRunIdRef = useRef<string | null>(null);`
- **Sync effect** — `page.tsx:941-943`
  `trackedRunIdRef.current = activePipelineRunId ?? contentSourceRunId ?? trackedRunIdRef.current;` deps `[activePipelineRunId, contentSourceRunId]`. Priority + the exclusion of `pipelineState.pipelineRunId` **confirmed**. (Exclusion is justified: `useWorkflow.ts:243` `pipelineRunId: pipelineRunIdFromStart ?? prev.pipelineRunId` adopts *any* pipeline_start id, including a foreign concurrent run's — tracking it would hijack the id.)
- **Launch set** — `page.tsx:1489` `trackedRunIdRef.current = launchedRunId;` inside the `onStartPipeline` handler (`page.tsx:1451`), in `startPipeline(...).then(launchedRunId => { attachRun; trackedRunIdRef.current = launchedRunId })`.
- **Guard** — `page.tsx:485-495`: `isForeignRun = !!incomingRunId && !!trackedRunIdRef.current && incomingRunId !== trackedRunIdRef.current`; if foreign → skip the reset block (`resetReplayState` + `setReviewGateData(null)` + `setQuestionnaireData(null)` + `setActivePipelineRunId(null)`, lines 496-515); else run it.

#### Symptom
None reproduced. The feared symptom (a revised/rebuilt C's legitimate `pipeline_start` reset skipped → C's clarify/seen-set corrupted) does not occur for the reasons below.

#### Reachability verdict — LARGELY A NON-ISSUE (headline case resolves CORRECTLY)
The headline premise "A **actively building**, `activePipelineRunId = A`" is **not a real state**. `activePipelineRunId` is:
- SET **only** at `questionnaire_ready` (`page.tsx:830`), i.e. when a run is **paused at the clarify gate**; and
- CLEARED **only** inside the non-foreign reset (`page.tsx:515 setActivePipelineRunId(null)`), which fires on the run's **own** `pipeline_start` as it leaves clarify.

So while A is **building**, `activePipelineRunId` is **null**. Opening C then resolves `tracked = null ?? C = C` — **correct**, no divergence. (`grep` confirms only two writers of `setActivePipelineRunId`: `:515` null and `:830` set.)

The divergence `tracked = A while viewing C` is only reachable in the **narrower, transient** window "A **paused at the clarify gate** while the user opens a different run C." `handleSelectWorkflowRun` (`page.tsx:1210-1232`) sets `contentSourceRunId = C` and does **not** clear `activePipelineRunId`, so A's clarify id survives and the `??` picks A. Verdict: reachable but narrow; and even there it does not produce the claimed failure (next section).

#### Root cause file:line + mechanism (why the narrow window still doesn't fail)
A revision **always creates a new run R with a new id** (R ≠ C, R ≠ A) — it never re-emits C's id. Behaviour splits by revise path:

1. **user_stories / prototype / app_builder revise** — `DashboardLayout.tsx:548 / 564 / 580` call `onStartPipeline(...)` → `page.tsx:1451` handler → `page.tsx:1489` sets `trackedRunIdRef.current = launchedRunId (= R)`. R's `pipeline_start` (id = R) then matches `tracked = R` → **reset runs correctly**. The divergence is **self-healed**. No bug.

2. **od_ppt revise** — `handleRevisePpt` REST path `DashboardLayout.tsx:512-524`: `postRevision(contentSourceRunId)` → new run R, `attachRun(R)`, but it does **not** call `onStartPipeline`, so `trackedRunIdRef` is **not** updated. R's `pipeline_start` (id = R) is foreign to `tracked` — **but this is true whether `tracked = C` or `tracked = A`** (R ≠ both), so the divergence is **not** the operative cause. The skip is largely benign: `onResetPipeline()` (`:523`) already reset the reducer; R's fresh `event_id`s don't collide with the seen-set; the only residue is a stale `lastSeqRef` (`page.tsx:498`) that matters solely on a **mid-revision SSE reconnect** (after_seq too high → R's early frames skipped on replay). This is a pre-existing od_ppt-revise/reconnect edge, independent of WARNING-2.

The one genuinely real (but **racy + minor**) divergence-window issue is the **inverse** of WARNING-2: if A's **own** `pipeline_start` arrives while the user is viewing C (user submitted A's clarify, then quickly opened C), the reset — correctly self-recognized since `incoming(A) == tracked(A)` — has **global** side effects (`resetReplayState` at `:496-502`) that wipe **C's** seeded replay state (seen-set / `lastSeqRef` / `waveGroups`, seeded at `page.tsx:1252-1272`), a transient Steps-panel glitch on C. Not the WARNING-2 mechanism, and the proposed priority flip would make it worse.

#### Fix — NON-ISSUE (do NOT apply the proposed priority flip)
Recommendation: **close as won't-fix / non-issue as framed.** The proposed "prioritize `contentSourceRunId` over `activePipelineRunId` when they diverge" is a **regression**: with `tracked = C`, A's own clarify→build `pipeline_start` (id = A) becomes `foreign` → its reset (`setActivePipelineRunId(null)` etc.) is **skipped** → A's `activePipelineRunId`/questionnaire stay stuck, and if the user returns to A it shows stale clarify — i.e. **re-introducing BUG-005 for the launched run A**. The launch-set at `:1489` and the `??` order exist precisely to keep the primary launch / clarify-advance reset working; flipping them breaks it.

Root truth: a **single global `trackedRunIdRef` cannot serve both "the run I'm driving (A)" and "the run I'm viewing (C)"** once they diverge — any single-ref priority is a heuristic with a losing case. The only correct durable fix is the **systemic per-run subscriber scoping already logged in the BUG-005 investigation** (`.planning/SSE-QA-BUG-LOG.md:192`, option #2): at the `runSubscribe → handleWebSocketMessage` bridge (`page.tsx:909-912`) drop frames whose `pipeline_run_id` ≠ the viewed/active run, making the whole reducer + reset per-run and deleting the ref heuristic entirely. That is a larger change, out of this quick's scope, and would also subsume the adjacent observation below.

**Adjacent (separate bug, not WARNING-2, worth a follow-up):** `pipeline_complete → setContentSourceRunId(pipeline_run_id)` at `page.tsx:530` is **also not run-scoped** — a background run A completing while the user views C flips `contentSourceRunId` C→A and hijacks the viewed content-source/preview. Same "global state not bound to the viewed run" class; distinct from WARNING-2.

#### Verify
- **Disprove headline:** launch A **without** a clarify gate (or let A pass clarify) so A is *building*; open C from history → `trackedRunIdRef === C` (because `activePipelineRunId` is null). Confirms the "background build" case resolves correctly.
- **Narrow window, 3 run_pipeline types:** launch A **with** clarify, let it reach `questionnaire_ready` (`activePipelineRunId = A`); open a different completed **user_stories/prototype/app_builder** run C; revise C → observe `trackedRunIdRef` flip to the new revision run at `:1489` and its `pipeline_start` reset **run** (GREEN, no corruption).
- **Narrow window, od_ppt:** same but C is od_ppt → R's `pipeline_start` reset is skipped, yet the revision still streams (reducer reset by `onResetPipeline`); the only observable degradation requires forcing a **mid-revision SSE reconnect** to surface the stale-`after_seq` frame skip.
- **Regression check on the proposed flip (to justify rejecting it):** temporarily set the sync to `contentSourceRunId ?? activePipelineRunId`; repro A-at-clarify + view C, then submit A → A's `pipeline_start` is now `foreign` → A's clarify never clears (BUG-005 re-appears for A). Revert.
- **Unit-level (optional):** a `page.tsx` vitest asserting the sync resolves `tracked = C` when `{activePipelineRunId: null, contentSourceRunId: C}` (build case) and `tracked = A` when `{activePipelineRunId: A, contentSourceRunId: C}` (clarify case) — documents the semantics without a code change.

---

### BUG-011 — pipeline_complete → setContentSourceRunId is not run-scoped: a background run completing hijacks the VIEWED content-source  [🟡 minor] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-n2d, `e604b361`) — run-scoped the `setContentSourceRunId` at `page.tsx:530` with an `isForeignCompletion` guard mirroring the BUG-005 `isForeignRun` shape, reusing the existing `trackedRunIdRef`: a completion for the tracked/launched run (or when no run is tracked yet) is NOT foreign → the set runs byte-identically; only a genuine foreign concurrent completion is skipped. RED→GREEN source-lock (`contentSourceRunScope.source.test.ts` 3/3: old unconditional set gone, guard present, launch pins intact) + `revisionFamilyLinkage.source` updated to the refactored token (semantic preserved). Launch→watch flow byte-identical. Offline-proven; a live 2-concurrent-run timing repro was not run (flaky) — the guard is structurally symmetric with BUG-005, which WAS live-proven this campaign.
- **Found:** 2026-07-16 · surfaced by the BUG-010 investigation (adjacent, NOT WARNING-2) · surface: run-screen viewed content-source.
- **Symptom:** while viewing run C, if a DIFFERENT attached run (e.g. a background/launched run A) emits `pipeline_complete`, the page subscriber's `pipeline_complete` branch unconditionally runs `setContentSourceRunId(data.pipeline_run_id)` (`frontend/src/app/dashboard/page.tsx:530`), re-pointing the VIEWED content-source to A → C's deliverable/preview is hijacked.
- **Root cause:** same class as BUG-001/005/006/008 — a run-screen state mutation with NO run-identity guard. The BUG-005 fix (quick j1u) run-scoped the `pipeline_start` reset via `trackedRunIdRef`; the `pipeline_complete` branch was not covered.
- **Fix (APPLIED — 260716-n2d, `e604b361`):** guarded `setContentSourceRunId` at `page.tsx:530` by the tracked run id (`isForeignCompletion` — only re-point when the completing run == `trackedRunIdRef.current`, or when no run is tracked), mirroring the BUG-005 run-scope guard. The durable fix remains the systemic per-run subscriber scoping (see the "Bugs found" option #2 note) — this targeted guard covers the `pipeline_complete` branch specifically. Launch flow preserved (launched run's own completion still sets the content-source).
- **Verify (fail-before/pass-after):** two runs; view C; a background run A completes → assert `contentSourceRunId` stays C (fail-before: it flips to A).

### BUG-012 — Reopened od_ppt (typed) deck renders BLANK on the shared run screen despite valid deck HTML  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-o6z, commits `9320445f`/`ef4ccd26`) via the **durable** viewed-run-type thread (user's choice over the targeted one-liner): a new `contentSourceRunType` state in `page.tsx` (set = `fullRun.type` on reopen :1244, cleared on the fresh-launch reset :1484, passed to `DashboardLayout` :1507) is folded into `viewedRunType` (`DashboardLayout.tsx:1265-1268`), and the PreviewPanel dispatch is fed the viewed type by changing `:1825` `workflowType={workflowType}` → `workflowType={effectiveReviseType}` — so `detectedType`/`renderType` bind to the real viewed type for ANY reopened run (incl. outside the recents window). `PreviewPanel.tsx` untouched (its dispatch was already correct once fed the right type). RED→GREEN: source-lock `contentSourceRunType.source.test.ts` 7→7; e2e TS-T-04c — `iframe[title="Slide Deck Preview"]` not-found (blank) → renders (od_ppt "Slide Deck Preview" + od_prototype "Prototype Preview", blank count 0). tsc clean, at-risk 42/42, launch→watch byte-identical, SC-001 clean. **LIVE-PROVEN:** the exact run that was blank (od_ppt `cbd46671`, 20 KB deck) now renders the full DogWalk deck with the chrome correctly reading "RENDERS AS Slides" (was "User Stories"); user_stories reopen still renders (no regression). Screenshots `o6z-reopen-odppt.png` (after) vs `n2d-reopen-odppt.png` (before). This closes the stale-`workflowType` family (BUG-006 label / BUG-008 generic gate / BUG-012 typed dispatch).
- **Found:** 2026-07-16 · during the BUG-008 orchestrator live-proof visual pass (reopen an existing completed od_ppt run in the running app) · surface: shared run-screen PreviewPanel.
- **Symptom (VERIFIED, screenshot `n2d-reopen-odppt.png`):** reopening a completed `od_ppt` run whose `output` is **20,183 chars of valid deck HTML** (`GET /api/runs/cbd46671-…` → `type:od_ppt status:completed mime:text/html`, output head `<!DOCTYPE html>…DogWalk — Pitch…`) renders the Preview tab **BLANK** (no slides, `iframe` count 0) — NOT the "Output will appear here" empty state. The render-mode chrome shows **"RENDERS AS Auto / User Stories"** on an od_ppt run, the tell that `renderType` collapsed to the stale default `workflowType="user_stories"`. The left lane is correct (OD_PPT · Done, right title, 3-agent trace, `presentation.pptx` card), so the run bound correctly — only the deliverable render is wrong.
- **CONFIRMED root cause (investigation agent 2026-07-16 — read `.planning/IMPLEMENTATION-REGISTER.md` overview+locked-decisions + FE render phases 16/18/22/32/39/42 + `.planning/SSE-QA-BUG-LOG.md` BUG-003/006/008; traced the full chain in source `page.tsx`/`PreviewPanel.tsx`/`DashboardLayout.tsx`):** classification **(b) renderer-dispatch keyed on the stale `renderType`** — a pure DISPATCH bug, NOT a content-seeding gap (classification (a) is FALSE: the deck IS seeded). Confirmed pin: **`PreviewPanel.tsx:893`** (`FIRST_PARTY_RENDERERS[renderType]?.()`) fed a stale `renderType="user_stories"` from **`PreviewPanel.tsx:536`** (`detectedType = workflowType || …`), whose `workflowType` prop is the stale `DashboardLayout` state passed at **`DashboardLayout.tsx:1825`** (`workflowType={workflowType}`), left stale because the sync binder at **`DashboardLayout.tsx:330-368`** is gated `if (pipelineState?.isRunning)` (`:331`) and never fires on a terminal reopen. Full chain, each link verified in source:
  1. **The deck IS seeded (so (a) is FALSE).** `page.tsx handleSelectWorkflowRun` clears all content (`page.tsx:1225-1228`) then, in the `fullRun.output && isContentTerminal` block (`:1307`), the `else if (fullRun.type === … "od_ppt" …)` branch (`:1310`) runs **`setPptContent(fullRun.output)`** (`:1311`) — the 20 KB deck lands in `pptContent`, passed to `<PreviewPanel pptContent={pptContent || undefined}>` (`DashboardLayout.tsx:1821`). od_ppt does NOT reach the generic-else at `:1314` (that is a non-known-type fallback; od_ppt is a known typed branch), so `genericDeliverable` stays `undefined`.
  2. **`workflowType` is stale on reopen.** `page.tsx` cannot set it (it is `DashboardLayout` state, default `"user_stories"` `DashboardLayout.tsx:250-256`); the only rebinder is the `isRunning`-gated effect at `:330-368` which never runs for a terminal reopen. So the `workflowType` prop at `:1825` is `"user_stories"` (the same stale-`workflowType` root as BUG-003-L2/BUG-006/BUG-008; binder blamed `0590c2448`, 2026-05-19).
  3. **`renderType` collapses to `"user_stories"`.** `PreviewPanel.tsx:536` `detectedType = workflowType || (…)` short-circuits to the truthy stale `"user_stories"`; `:538-545` maps it through unchanged (its `od_ppt → ppt` case at `:540` is DEAD because `detectedType` is never `"od_ppt"`) → `renderType = "user_stories"`. This is exactly the screenshot's **"RENDERS AS Auto / User Stories"** tell (`rendererOptions` pushes `{value: renderType, label: FIRST_PARTY_LABELS[renderType]}` = "User Stories", `:846-847`/`:828`).
  4. **`hasContent` is TRUE → NOT the empty state.** `effPptContent = pptContent` (`:555`, override inactive), so `hasContent = !!(… || effPptContent || …)` = true (`:588`) → the render reaches `renderDeliverable()` (`:1017`/`:1075`), not the "Output will appear here" branch (`:1025-1027`). Matches the symptom: BLANK, not empty-state.
  5. **The dispatch misroutes to the empty user-stories renderer → BLANK.** `renderDeliverable()` (`:877-904`): step-1 `const firstParty = FIRST_PARTY_RENDERERS[renderType]?.()` (`:893`) = `FIRST_PARTY_RENDERERS["user_stories"]()` = `effUserStoryContent ? <UserStoryPreview/> : null` (`:797-800`) → `effUserStoryContent` is empty (reopen seeded only `pptContent`) → **`null`**; step-2 `if (genericDeliverable?.content)` (`:899`) → `undefined` → skip; **`return null` (`:903`) → blank**. The `ppt` renderer that WOULD show the deck (`(effPptContent || pptxCode) ? <PPTPreview content={effPptContent}/>`, `:805-808`) is registered under key `"ppt"` and is NEVER invoked because the dispatch key is the stale `"user_stories"`. The 20 KB deck in `effPptContent` is orphaned. (The `PPTTabActions` block `:972` and the deck-only preview chrome are likewise gated `renderType === "ppt"` → also silent.)
- **Why BUG-008 (n2d) does NOT fix this (confirmed):** BUG-008 decoupled ONLY `hasGenericDeliverable` (`PreviewPanel.tsx:586`) from the stale gate — but od_ppt seeds `pptContent` (a TYPED slot), not `genericDeliverable`, so `hasGenericDeliverable` stays `false` and the BUG-008 change is a provable no-op here. BUG-008 fixed the GENERIC-deliverable render path; BUG-012 is the TYPED-deliverable render dispatch — different seeding channel, different failure.
- **Affected workflow types (reasoned from the reopen seeding branches + the dispatch):** **od_ppt AFFECTED** (blank — content → `pptContent`). **od_prototype AFFECTED** (blank — `page.tsx:1312-1313` seeds `setPrototypeContent(fullRun.output)`; same misroute: `renderType="user_stories"` → the `"prototype"` renderer at `:809-812` never fires). Plain `ppt`/`prototype` and the `*_revision` variants (all route to typed slots at `page.tsx:1308-1313`) are AFFECTED whenever the stale `workflowType`'s `renderType` ≠ the seeded slot. **user_stories is AFFECTED only when the stale `workflowType` ≠ `user_stories`** — in this repro it happened to BE `"user_stories"`, so the reopened user_stories run rendered *coincidentally* (the `n2d-reopen-userstories.png` "renders perfectly" is luck, exactly the BUG-006 coincidence). **app_builder is NOT affected (does not blank):** app_builder is absent from the known-typed reopen branches (`page.tsx:1308/1310/1312` omit it), so it falls to the generic-else at `:1314` → `setGenericDeliverable(…)` → rescued by BUG-008's `:586` fix → renders via `GenericDeliverablePreview` (sandboxed iframe). (Fidelity nuance, separate/lower-sev: reopened app_builder renders as a generic HTML iframe, not the bespoke `AppBuilderIDEPreview` "Code" view — not blank, out of BUG-012 scope.)
- **PRE-EXISTING; the fourth-and-last unlogged consumer of the stale `workflowType` that the BUG-006 sweep (quick 260716-lb6) missed.** BUG-006 enumerated three consumers of the stale `workflowType` and fixed them keyed on `recentRuns.find(r => r.id === contentSourceRunId)?.type`: the lane TITLE (BUG-001), the revise-handler selection (BUG-003-L2), the type LABEL (BUG-006, applied at `DashboardLayout.tsx:1762` `runType={effectiveReviseType || pipelineState?.pipeline_type}`). The **PreviewPanel render-dispatch type** at `DashboardLayout.tsx:1825` is the fourth consumer and still passes raw `workflowType`. Same "run-screen state not bound to the viewed run" class as BUG-001/003/005/006/008. No register lock forbids rebinding `workflowType` on reopen (the binder's own comment `:325-329` states its intent is to keep `workflowType` in sync so PreviewPanel renders the right component; the `isRunning` gate is an incidental limitation, not a locked decision); the fix stays inside the UXFIX-04/D-21 `renderType`-dispatch table (register 2291/3057/3500 — renderers REUSED, ND-G) and adds no workflow-name literal (SC-001).
- **Proposed MINIMAL fix (NOT applied) — bind PreviewPanel's `workflowType` prop to the VIEWED run, mirroring the applied BUG-006 fix.** One line at `DashboardLayout.tsx:1825`: `workflowType={workflowType}` → `workflowType={effectiveReviseType || pipelineState?.pipeline_type || workflowType}` (reuse the in-scope `effectiveReviseType` = `viewedRunType ?? workflowType`, computed at `:1265-1269`; already consumed 63 lines above at `:1762`). Then reopened od_ppt → `detectedType="od_ppt"` → `renderType="ppt"` (`:540`) → `FIRST_PARTY_RENDERERS["ppt"]` renders `effPptContent`; od_prototype → `"prototype"` renderer. Behaviour: live/launch (`isRunning` true / `contentSourceRunId` null → `viewedRunType` undefined) → `workflowType` (byte-identical, no regression); terminal reopen → the viewed run's real type (fixed). Same known caveat as BUG-006/008: a run OUTSIDE the `recentRuns` window has `viewedRunType===undefined` → falls back to stale `workflowType` → still blank; the durable variant threads `fullRun.type` from `page.tsx handleSelectWorkflowRun` as a dedicated `contentSourceRunType` state (works for any reopened run). Optional hardening: relax the `:331` `isRunning` gate to also rebind `workflowType` from the viewed run on a terminal reopen — a single change that closes this whole stale-`workflowType` family at the source (larger blast radius: `workflowType` also drives revise handlers/chaining/notifications).
- **Classification — TARGETED guard (like BUG-006/008), NOT the systemic per-run-subscriber-scoping fix.** The two mechanisms are orthogonal: BUG-012 is a reopen-time stale-STATE binding feeding the render dispatch (no SSE frame involved), so the systemic per-run subscriber scoping (drop foreign-`pipeline_run_id` frames at `page.tsx:909-912`, the durable fix for the LIVE cross-run poisoning family BUG-005/010/011) would NOT touch it. BUG-012 needs its own targeted viewed-run binding — the direct sibling of the BUG-006 label fix.
- **Distinct from:** BUG-008 (generic/custom empty-state gate — FIXED), BUG-009 (empty deck OUTPUT — this run's output is present/valid, 20KB), BUG-006 (pipeline-type LABEL — FIXED).
- **NOT caused by n2d:** the BUG-008 change is a provable no-op for known types (`isKnownRenderType=true` + `knownContentPresent=true` → `hasGenericDeliverable` stays `false`); this blank-typed-deck reproduces independently of it.
- **Fix:** PENDING (awaiting root-cause + user scope decision).

### BUG-009 recurrence confirmed LIVE  (see BUG-009 entry above)
- **2026-07-16:** the *other* completed od_ppt run `6c6f38c7-…` has `output` length **3** (`"..."`) with `mime:text/html` — a live recurrence of BUG-009 (od_ppt completes with an effectively-empty deck). Distinct from BUG-012 (which has a full 20KB deck that renders blank). Backend fix still gated on the READ-ONLY register sign-off.

---

### BUG-013 — Reopening a clarify-paused run under ≥6 concurrent non-terminal runs hangs the reopen `GET /api/runs/{id}` (browser HTTP/1.1 connection-limit exhausted by per-run SSE streams) → empty IDLE lane, no clarify, stale "USER_STORIES" label  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-r7d, commits `aebff868`/`c35f73e6`) via the minimal+hardening fix. **Part A** (`RunConnectionProvider.tsx`): replaced `NON_TERMINAL_STATUSES` with `AUTO_STREAM_STATUSES`={running,planning,analyzing,generating,revising} — BACKGROUND parked runs (`waiting_for_user`/`clarifying`) no longer auto-open a stream (they emit nothing while parked); a single sticky `focusedRunId` (set by `attachRun`, new replaces prior) is unioned into `liveRunIds` behind the identity guard so the VIEWED/launched run still streams; the page `attachRun(fullRun.id)`s on reopen, gated to NON-terminal runs (`REOPEN_TERMINAL_STATUSES`) so a completed reopen never pins a dead-stream reconnect loop (plan-checker warning). **Part B** (`api.ts`): AbortController ~30s timeout on `request()` → a starved call rejects with a typed `ApiError` instead of hanging silently. RED→GREEN: 7 provider/timeout/source-lock tests (5 fail→7 pass); tsc clean; full vitest 742 pass / 8 known baseline reds; mocked SSE/reconnect/history/revision/clarify Playwright 32 pass/0 fail; at-risk 29/29; SC-001 clean. **LIVE-PROVEN** (screenshot `bug013-clarify-fixed.png`): with 6 parked runs present, dashboard auto-streams dropped **12 → 0**; the in-page `localhost:8000` fetch that hung 8s now returns **200 @113ms**; reopening the clarify-paused run renders lane state **"clarify"** with all **6 questions + options** on screen (was empty "idle"), and the reopen attaches exactly **1** stream (the focused run). Regression from Phase 44 SSE cutover + DEF-44-12-3; fixed here. Residual (separate, cosmetic) — **NOW FIXED** (quick 260716-sml, `d917d8c1`): the type LABEL read "USER_STORIES" for a non-terminal reopen because the BUG-012 `viewedRunType` binding (`DashboardLayout.tsx:1274`) was `!isPipelineRunning`-gated + the header pill (`:1475`) used raw `workflowType`. Fixed by un-gating `viewedRunType` (prefer the durable `contentSourceRunType`, which is set only on reopen, regardless of running state) + binding the header pill to `effectiveReviseType`. Launch→watch byte-identical (contentSourceRunType null on launch). RED→GREEN (DashboardLayout.laneTitle Test A/B); tsc clean; at-risk 23/23. **LIVE-PROVEN**: the same reopened clarify-paused prototype run now shows lane "OD_PROTOTYPE" + header "od_prototype" (was "USER_STORIES"/"User Stories"), clarify intact. Closes the entire stale-`workflowType` family (BUG-006 label / BUG-008 generic gate / BUG-012 typed dispatch / this = non-terminal reopens).
- **Found:** 2026-07-16 · live Bedrock QA (user had 6 `waiting_for_user` runs) · surface run screen reopen via Home recents, on an **`od_prototype` no-template clarify-paused** run `586f4ad4-bdc1-4b21-b295-ea262b0b609f` ("Build a prototype on apple products…", 6 questions ready). Screenshot `~/.claude/jobs/e660aea4/tmp/clarify-reopen.png` + `probe-clarify.png`.
- **Symptom:** Reopening the run from Home recents (fresh browser) renders the run screen's left lane **empty in `data-run-state="idle"`** — no clarify card, no questionnaire (the 6 real questions never appear), no Steps/pipeline trace, no chat — with the deliverable area showing "Output will appear here" and the neutral "Message the run…" composer. The lane type chip wrongly reads **"USER_STORIES"** for this od_prototype run. **NOT** BUG-005's "0/0 BUILDING + Steer" symptom (that was `building` + `isRunning` true; this is `idle` + `isRunning` false). Backend is CORRECT (durable events `workflow_validated, planner_start, planner_complete, gate_status, questionnaire_ready(6q, carries pipeline_run_id), chat_reply`; **no** `pipeline_start` — clarify is PRE-build; verified via `/events`).
- **Root cause (investigation agent — read `.planning/IMPLEMENTATION-REGISTER.md` run-screen/SSE sections + `SSE-QA-BUG-LOG.md` BUG-004/005/010/011/012 + traced the reopen + SSE transport in source; RUNTIME-PROVEN with a Playwright discriminator, not reasoned):** **the browser's HTTP/1.1 ~6-connections-per-origin limit is fully consumed by the 6 concurrent long-lived EventSource SSE streams that `refreshLiveRuns` auto-attaches (one per non-terminal run), so the reopen's same-origin `GET /api/runs/{id}` has no free connection slot and QUEUES INDEFINITELY** — the reopen `await` never resolves, so `contentSourceRunId`/`contentSourceRunType`/the durable-event replay never run, and the lane renders the initial idle state with the stale default `workflowType`.
  - **Chain (each link verified, clean-tree line numbers):**
    1. **Trigger:** `frontend/src/providers/RunConnectionProvider.tsx:222-244` `refreshLiveRuns` does `getWorkflows(t,{limit:50})` then `.filter((r) => NON_TERMINAL_STATUSES.has(r.status))` (`:232`) and mounts **one `RunStreamConnection` per id** — `liveRunIds.map((runId) => <RunStreamConnection>)` (`:358`), i.e. **one native EventSource SSE stream per run**. `NON_TERMINAL_STATUSES` (`:60-68`) includes `waiting_for_user` + `clarifying`. The 6 clarify-paused runs (`586f4ad4` od_prototype, `6c230eab` app_builder, `6af0a82d` user_stories, `8941d0f3`/`7cf6ddb8` od_prototype, `50e59943` user_stories) therefore open **6 concurrent long-lived streams to `localhost:8000`** — exactly Chromium's 6-per-origin HTTP/1.1 cap. (Backend is HTTP/1.1: `curl -I` → `HTTP/1.1 405`; h2 would multiplex and dodge the cap.)
    2. **The starved call:** reopen click → `page.tsx handleSelectWorkflowRun` (`:1228`) → `getToken()` OK (`:1242`) → `await getWorkflow(currentToken, run.id)` (`:1246`) → `frontend/src/lib/api.ts` `getWorkflow` (`:405`) → `request('/api/runs/586f4ad4', {GET})` → **bare `await fetch(url)` with NO timeout/retry/mutex** (`api.ts:85`), `url = BASE_URL + path`, `BASE_URL = ENV.API_URL` (`api.ts:18`) = **`http://localhost:8000`** in dev (`env.ts resolveApiUrl` → localhost branch). This is the SAME origin as the 6 streams → **no free connection → the request is issued but never gets a socket → hangs forever.**
    3. **Reopen aborts silently:** because the `await` at `page.tsx:1246` never settles, `setContentSourceRunId(fullRun.id)` (`:1250`), `setContentSourceRunType` (`:1253`), the `if (fullRun.id !== pipelineState.pipelineRunId)` durable-event replay (`:1264`) + `seedRunChatTranscript`, and the deliverable seed **all never execute**. No throw ⇒ the outer catch (`page.tsx:1375` "Failed to load workflow output") never fires either — consistent with the "no console errors captured" ground truth.
    4. **Render = initial idle + stale label.** With `contentSourceRunId` still `null`, `questionnaireData` `null`, `activePipelineRunId` `null`, `pipelineState` reset (`isRunning=false`, `agents=[]`): `laneClarifyOpen = questionnaireQuestions.length>0 && (…)` (`DashboardLayout.tsx:1355`) = **false**, `isPipelineRunning=false` ⇒ `runLaneState` (`:1358-1364`) falls all the way to **`"idle"`**. And `viewedRunType` (`:1274`) is gated `!isPipelineRunning && contentSourceRunId != null` → `true && false` → **undefined** → `effectiveReviseType = viewedRunType ?? workflowType` = the default `workflowType="user_stories"` → the type chip `runType={effectiveReviseType || pipelineState?.pipeline_type}` (`:1771`) renders **"USER_STORIES"**. So the stale label is a **downstream symptom of the same hang** (contentSourceRunId never set) — NOT the BUG-006/012 `isRunning`-gate mechanism (here `isRunning` is false) and NOT a separate seed gap.
  - **RUNTIME PROOF (discriminator, `~/.claude/jobs/e660aea4/tmp/probe-connlimit.mjs`, temporary probes in `page.tsx` since reverted — tree clean):** at reopen, probes fired `hsr-called` → `token=present` → `before-getWorkflow`, then **nothing** (no `after-getWorkflow`, no `outercatch`) for 35 s — the app's `GET http://localhost:8000/api/runs/586f4ad4` request was issued (`p.on('request')`) but received **no response/finished/failed** in 12 s. The clincher: two timed fetches to the **same endpoint** fired concurrently from the page — `sameOrigin http://localhost:8000` → **AbortError at 7002 ms (hung)**; `crossOrigin http://127.0.0.1:8000` → **200 in 8 ms**. Same backend, same URL path, same instant — only the ORIGIN differs (127.0.0.1 has its own un-saturated connection pool). `curl localhost:8000/api/runs/586f4ad4` also returns 200 in 3 ms. Backend/pool are healthy — this is purely a browser per-origin connection-queue starvation. (Ruled out: DB pool exhaustion — BUG-004 fix `d418b5a1`/`56deb5c4` IS present; getToken null — `token=present`; getWorkflow reject — no catch fired; replay throw — replay never runs.)
  - **Hypotheses (task) verdict:** **H1 (replay throws before `questionnaire_ready`) — REFUTED:** the replay loop never runs (hang precedes it); and the 4 pre-questionnaire reducer handlers (`useWorkflow.ts:710/731/754/795`) are pure scalar setters that don't touch `agents[]`, so they wouldn't throw on a missing `pipeline_start` anyway. **H2 (`questionnaireQuestions` effect timing, `DashboardLayout.tsx:597-602`) — REFUTED:** `questionnaireData` is never set, so the effect has nothing to copy; not a timing issue. **H3 (`activePipelineRunId` never set / cleared) — TRUE as a downstream symptom, not the cause:** the durable `questionnaire_ready` DOES carry `pipeline_run_id` (verified), so the replay WOULD set it — it's just never reached. **H4 (multi-run fan-out) — CORRECT as the TRIGGER, but via a DIFFERENT mechanism than hypothesized:** it is not a foreign `pipeline_start` poisoning global clarify state (that is BUG-005, already fixed `8eec694c`, and needs ≥1 *building* run — all 6 here are gate-paused and emit no `pipeline_start`); it is the 6 concurrent *streams* exhausting the browser connection pool.
  - **Launch vs reopen — SAME root, different starved call (reopen RUNTIME-PROVEN; launch REASONED):** the live "no-template prototype parks at clarify but never shows questions" path shares the identical connection-exhaustion root — with 6 streams already open, a fresh launch's same-origin `POST /api/runs` (`RunConnectionProvider.tsx:318-320` `sendCommand`) queues behind the streams (mint hangs/delays), and even once minted the launched run's own stream is a 7th connection that can't open → its clarify frames can't arrive → no questions. A **clean single-run launch (≤5 other non-terminal runs)** stays under the cap and shows clarify normally — i.e. the failure is conditional on ≥6 concurrent non-terminal runs, not on workflow type or no-template.
  - **REGRESSION — introduced by the SSE work, in two steps; NOT pre-existing and NOT from the BUG-005/DEF-44-12-4 state-binding quicks:** (1) **Phase 44 / 44-06 (`a931c066`/`f6ce1142`, 2026-07-15)** deleted the single multiplexed WebSocket transport and replaced it with **one SSE EventSource per run** — so N runs now = N browser connections (the file's own comment at `RunConnectionProvider.tsx:58` admits "the WS transport masked this because it reconnected by run-id, status-agnostic"). (2) **DEF-44-12-3 (`0563afad`, 2026-07-16 — THIS session)** authored the current `NON_TERMINAL_STATUSES` set INCLUDING `waiting_for_user`/`clarifying` (git-blame `:60-68` = `0563afad7`), so gate-paused runs — which stream nothing — now each HOLD a connection. The BUG-005 (`8eec694c`) and DEF-44-12-4 (`dhr`) work is orthogonal (reducer/clarify state binding, not transport) and neither caused nor fixes this.
- **Fix (PROPOSED — investigation only, NOT applied; this is a TRANSPORT-layer bug, so the deferred "systemic per-run subscriber scoping" from BUG-005 #2 / BUG-010 does NOT address it — that scopes which frames reach the reducer, not how many connections are open):**
  - **PRIMARY (minimal, directly removes the starvation): don't hold an SSE stream open for gate-paused runs.** A `waiting_for_user`/`clarifying` run emits NO further frames until the user acts, so its stream is pure connection-waste. Drop `"waiting_for_user"` + `"clarifying"` from `NON_TERMINAL_STATUSES` (`RunConnectionProvider.tsx:60-68`) so `refreshLiveRuns` attaches only genuinely-streaming states (`running`/`planning`/`analyzing`/`generating`/`revising`). The viewed clarify run is already seeded via the durable `getRunEvents` replay in `handleSelectWorkflowRun` (`page.tsx:1287`), not a live stream; **re-attach on demand** when the user submits the questionnaire/opens the run (`attachRun(id)` already exists, `:305`). This keeps concurrent streams well under 6 in the reported scenario. Caveat to verify: a clarify run advanced by ANOTHER tab won't auto-refresh its lane until re-attach — acceptable, and DEF-44-12-3's history-open attach can be preserved by attaching the *single* opened run.
  - **DURABLE (correct architecture, larger): a single multiplexed SSE connection for all runs** (one `GET /api/runs/events/stream?ids=…` fan-in) instead of one EventSource per run — restores the pre-44 single-connection model and removes the N-connection cap entirely. Backend adds a multiplexed endpoint; FE collapses `liveRunIds.map(<RunStreamConnection>)` into one stream. **OR** cap `refreshLiveRuns` to attach at most ~3 streams (prioritising the viewed/active/most-recent run) so REST always has free slots. Serving prod over HTTP/2 raises the cap to ~100 streams/connection but is not a real fix (still bounded, and dev stays broken).
  - **HARDENING (defense-in-depth, orthogonal): add an `AbortController` timeout to `request()` (`api.ts:80-95`)** so a starved fetch fails fast (→ the outer catch fires + a visible error) instead of hanging silently forever — turns this class of bug from an invisible idle screen into a surfaced error. Does not fix the starvation; makes it diagnosable.
- **Verify (fail-before / pass-after — proposed):**
  1. **Fail-before:** with ≥6 `waiting_for_user`/non-terminal runs (⇒ 6 auto-attached SSE streams to the app origin), reopen a clarify-paused run from Home recents → lane `data-run-state="idle"`, no clarify/questions, type chip "USER_STORIES"; the app's `GET /api/runs/{id}` stays pending indefinitely. (Reproduced live 2026-07-16 for `586f4ad4`; discriminator: same-origin fetch AbortError@7s vs cross-origin 200@8 ms — `probe-connlimit.mjs`.)
  2. **Pass-after (PRIMARY):** with clarify/`waiting_for_user` excluded from auto-attach (≤2 streams open), the same reopen resolves `getWorkflow` in <50 ms → `contentSourceRunId` set → durable replay runs → `questionnaire_ready` seeds the 6 questions → `runLaneState="clarify"`, questions render + answerable, chip = "OD_PROTOTYPE". Repeat with 8–10 concurrent non-terminal runs to confirm REST never starves.
  3. **No-regression:** a single live no-template od_prototype launch still shows clarify (≤ cap); answering clarify → build still streams (on-demand re-attach); ≤5-concurrent-run reopens unaffected; `tsc --noEmit` clean.
  4. **Unit:** a `RunConnectionProvider` vitest asserting `NON_TERMINAL_STATUSES` excludes `waiting_for_user`/`clarifying` and that `refreshLiveRuns` yields ≤ the cap; optionally an `api.request` test asserting an `AbortController` timeout rejects a hung fetch.
