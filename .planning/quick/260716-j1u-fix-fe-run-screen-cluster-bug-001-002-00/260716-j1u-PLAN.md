---
phase: quick-260716-j1u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/history/WorkflowHistory.tsx
  - frontend/e2e/tests/ts-y.run-scope-clarify.spec.ts
  - frontend/src/components/history/WorkflowHistory.openRun.test.tsx
  - frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx
  - frontend/src/app/dashboard/reviseGuard.source.test.ts
  - frontend/e2e/tests/ts-u.revisions.spec.ts
autonomous: true
requirements: [BUG-001, BUG-002, BUG-003, BUG-005]
must_haves:
  truths:
    - "A concurrent FOREIGN run's pipeline_start no longer clears the VIEWED run's clarify questionnaire / active-run-id / seen-set (BUG-005)"
    - "A top-nav Run History row tap opens the shared execution run screen (execution-chat-lane + composer), not the static RunDetailPage (BUG-002)"
    - "The run-lane header title tracks the VIEWED run (contentSourceRunId), not recents[0] (BUG-001)"
    - "Run refinement on a reopened run with empty local deck content fires exactly one POST /api/runs/{parent}/revisions over REST (BUG-003)"
    - "The primary launch->watch flow (launched==viewed) is unregressed: fresh run resets its own clarify, shows its own title, and revises"
  artifacts:
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Run-scoped pipeline_start reset via a tracked-run ref (stale-closure-safe)"
      contains: "trackedRunIdRef"
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "onOpenRun to WorkflowHistory; viewed-run lane title; relaxed revise guard + viewed-run-type selector"
      contains: "onOpenRun"
    - path: "frontend/src/components/history/WorkflowHistory.tsx"
      provides: "onOpenRun prop; handleSelectRun routes taps through it"
      contains: "onOpenRun"
  key_links:
    - from: "page.tsx trackedRunIdRef"
      to: "pipeline_start reset block (page.tsx:462)"
      via: "skip reset when incoming pipeline_run_id differs from tracked run"
      pattern: "trackedRunIdRef\\.current"
    - from: "DashboardLayout <WorkflowHistory onOpenRun=...>"
      to: "onSelectWorkflowRun + setMainView(execution)"
      via: "byte-identical to Home-recents wiring (DashboardLayout.tsx:1475)"
      pattern: "onOpenRun=\\{\\(run\\)"
    - from: "WorkflowHistory.handleSelectRun"
      to: "onOpenRun(run)"
      via: "replaces the setSelectedRun internal-detail fall-through"
      pattern: "onOpenRun\\(run\\)"
    - from: "DashboardLayout latestRunTitle"
      to: "recentRuns.find(r => r.id === contentSourceRunId)"
      via: "viewed-run lookup, falls back to recents[0] on launch"
      pattern: "r\\.id === contentSourceRunId"
    - from: "handleRevisePpt guard (DashboardLayout.tsx:493)"
      to: "postRevision REST path (:504)"
      via: "guard relaxed to allow the content-free REST path when contentSourceRunId exists"
      pattern: "!contentSourceRunId && !pptxCode && !pptContent"
---

<objective>
Fix FE run-screen cluster BUG-001/002/003/005 — four live-QA bugs of ONE class: the
run-screen live state is not bound to the run being VIEWED (it binds to the launched/latest
run, or accepts frames from any concurrently-attached run). Frontend-only.

Purpose: the WS->SSE cutover + DEF-44-12-4 (which made opened runs render) EXPOSED these
pre-existing bugs. Each is root-caused to file:line in `.planning/SSE-QA-BUG-LOG.md` and
specified in `.planning/FE-CLUSTER-GROUNDED-CONTEXT.md`.
Output: one atomic commit per bug (highest-risk/systemic first), each with a
fail-before/pass-after test, then a regression-green gate. Backend + SSE wire are correct —
DO NOT touch them.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/FE-CLUSTER-GROUNDED-CONTEXT.md
@.planning/SSE-QA-BUG-LOG.md
@frontend/src/app/dashboard/page.tsx
@frontend/src/components/layout/DashboardLayout.tsx
@frontend/src/components/history/WorkflowHistory.tsx
@frontend/src/app/dashboard/revisionFamilyLinkage.source.test.ts
@frontend/e2e/tests/ts-live-state.spec.ts
@frontend/e2e/fixtures/mockSse.ts
</context>

<constraints>
- Branch **feat/ui-2** (NEVER main/staging). Worktrees OFF -> sequential. **NO commit trailer. NEVER push.**
- **FRONTEND-ONLY.** Only `page.tsx`, `DashboardLayout.tsx`, `WorkflowHistory.tsx` (+ tests) change. No backend/engine/queue. Do NOT rip out `RunConnectionProvider`'s fan-out — only run-scope the reducer's reset.
- **cwd-sensitive: run ALL tsc/vitest/playwright from inside `frontend/`** (not `--root frontend`). Kill :3000 before Playwright.
- **SC-001:** `page.tsx` exempt. Add NO workflow-name literal to a guarded component (RunChatLane/LaneRunHeader/PreviewPanel). BUG-001/003 fixes key only on generic run identity (`run.id`/`run.type`/`contentSourceRunId`).
- **Do NOT regress launch->watch** (launched==viewed). Keep GREEN: `ts-u.revisions:86` (preserve `page.tsx:496`), `revisionFamilyLinkage.source.test.ts` (the guard relaxation must NOT alter its pinned tokens `postRevision(getToken() ?? "", contentSourceRunId,` / `source_workflow_run_id: contentSourceRunId` / `source_workflow_run_id: sourceRunId`), `useRunChat.test.ts:140-143`, `ts-live-state.spec.ts`.
- "132/0" baseline is STALE — establish the REAL green count by running (Task 5).
- **Executor MUST NOT run a live Bedrock run** — the ORCHESTRATOR does the live proof.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-005 — run-scope the pipeline_start reset so a concurrent foreign run cannot wipe the viewed run's clarify</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/e2e/tests/ts-y.run-scope-clarify.spec.ts</files>
  <behavior>
    - Run A paused at clarify (questionnaire_ready; activePipelineRunId=A; 6 questions shown). A FOREIGN run B emits pipeline_start(pipeline_run_id=B). Fail-before: A's questionnaire vanishes, lane -> "0/0 BUILDING". Pass-after: A's questions remain, answerable.
    - Same-run reset preserved: A's OWN pipeline_start (id=A) after questionnaire_complete still clears A's questionnaire + seen-set + reviewGate.
    - Fresh-launch reset preserved: a launched run's own pipeline_start still resets/seeds (launch flow unregressed).
  </behavior>
  <action>
    Root cause (verified): the page subscriber forwards EVERY frame from EVERY attached run to the single global reducer; the `pipeline_start` branch (`page.tsx:462-483`) UNCONDITIONALLY runs `resetReplayState(...)` (:463, clears shared `seenEventIdsRef`), `setReviewGateData(null)` (:476), `setQuestionnaireData(null)` (:481), `setActivePipelineRunId(null)` (:482) with no run guard. `laneClarifyOpen` (`DashboardLayout.tsx:1308`) depends on `activePipelineRunId`, so nulling it collapses the viewed run's clarify.

    Implement the grounded TARGETED run-scope guard (safe default; do NOT run-scope the subscriber, do NOT touch the fan-out). Edits in `page.tsx`:
    1. Add `const trackedRunIdRef = useRef<string | null>(null);` beside the other refs (~`:146`) — holds "the run this tab is driving/viewing", the ONLY run whose pipeline_start may reset this tab's clarify/seen-set.
    2. Sync it via an effect keyed on `[activePipelineRunId, contentSourceRunId]`: `trackedRunIdRef.current = activePipelineRunId ?? contentSourceRunId ?? trackedRunIdRef.current` (keep last id when both null so the building phase retains identity). **CRITICAL: do NOT sync from `pipelineState.pipelineRunId`** — the foreign frame is still forwarded to `useWorkflow` at `:485` and `useWorkflow.ts:243` adopts the foreign `pipeline_run_id` into `pipelineState.pipelineRunId`; syncing from it would hijack the tracked id to the foreign run.
    3. In the launch `.then` at `page.tsx:1437-1438`, set `trackedRunIdRef.current = launchedRunId;` (alongside `attachRun`). REQUIRED so a same-tab NEW run (launched after a prior run completed, where activePipelineRunId/contentSourceRunId are stale/null) is recognized as self and its own pipeline_start still fires the WR-03 reset.
    4. Run-scope the reset at `:462`: read `const incomingRunId = (msg.data as Record<string, unknown> | undefined)?.pipeline_run_id as string | undefined;` and SKIP the whole reset body when foreign, i.e. when `incomingRunId && trackedRunIdRef.current && incomingRunId !== trackedRunIdRef.current`. Otherwise (no tracked id yet OR ids match) run the reset as today. **CRITICAL: the guard MUST read `trackedRunIdRef.current` (a ref), NOT the `activePipelineRunId`/`contentSourceRunId` STATE** — `handleWebSocketMessage` is `useCallback(..., [])` (closes at `:871`), so a direct state read captures the initial `null` and treats every pipeline_start as foreign, breaking the launch reset (DEF-44-12-4 stale-closure class).

    Do NOT change the `:485` forward or the backend clarify/planner. Update the WR-03 comment (`:455-461`) to note the run-scope guard. Double-mint guard is out of scope.

    Test `ts-y.run-scope-clarify.spec.ts` (mirror `ts-m.questionnaire.spec.ts` + `ts-live-state.spec.ts`): drive A to `mockSse.questionnaireReady([...6])`; assert clarify present (e.g. `lane-clarify-status` / the questions). Emit `mockSse.emit("pipeline_start", { pipeline_run_id: "run-e2e-foreign", agent_count: 3, agents: [3 idle] })`; assert A's clarify SURVIVES. Positive control: a matching pipeline_start (id===A, after questionnaire_complete) still clears A's questionnaire.
  </action>
  <verify>
    <automated>cd frontend && (lsof -ti :3000 | xargs kill -9 2>/dev/null; true) && npx playwright test --project=mocked ts-y.run-scope-clarify</automated>
  </verify>
  <done>New spec passes: foreign pipeline_start does not clear the viewed run's clarify; same-run pipeline_start still does. `ts-m.questionnaire` green. tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BUG-002 — route History-list tap through onOpenRun into the shared run screen</name>
  <files>frontend/src/components/history/WorkflowHistory.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/history/WorkflowHistory.openRun.test.tsx</files>
  <behavior>
    - WorkflowHistory WITH onOpenRun: tapping a row that is NOT activeRunId calls onOpenRun(run) and does NOT render the internal RunDetailPage/detail.
    - KAN-96 case (run.id === activeRunId) still routes via onViewRunningPipeline.
    - Without onOpenRun (legacy), the tap still falls back to setSelectedRun (no change).
  </behavior>
  <action>
    Root cause (verified): the History mount `DashboardLayout.tsx:1506` passes NO onOpenRun; `WorkflowHistory.handleSelectRun` (`:155-162`) routes live only when `run.id === activeRunId`, else `setSelectedRun(run)` -> the divergent internal `RunDetailPage` (one-shot getRunSummary, no SSE, no chat lane -> stale QUEUED / "No agent data" / "No preview"). The Home-recents path already routes via onOpenRun (`:1475`).

    Two edits:
    1. `WorkflowHistory.tsx`: add `onOpenRun?: (run: WorkflowRun) => void;` to `WorkflowHistoryProps` (after `onReviseAppBuilder`, ~`:41`) and destructure it. In `handleSelectRun` (`:155`), AFTER the KAN-96 `activeRunId` guard (`:158-161`), when `onOpenRun` is provided call `onOpenRun(run); return;` INSTEAD of the `setSelectedRun(run)` fall-through (`:162`). Keep `setSelectedRun(run)` only as the fallback when `onOpenRun` is undefined.
    2. `DashboardLayout.tsx:1506` `<WorkflowHistory ...>`: add `onOpenRun={(run) => { onSelectWorkflowRun?.(run); setMainView("execution"); }}` — byte-identical to `:1475` (`onSelectWorkflowRun` destructured `:211`).

    Do NOT delete RunDetailPage/the internal detail here — chaining ("Suggested next steps"/onChainPipeline) + VersionTimeline stay reachable via the History LIST rows; retiring the internal detail (INV-3) is a follow-up. Note the carry-over in a comment.

    Test `WorkflowHistory.openRun.test.tsx` (mirror existing `WorkflowHistory.*.test.tsx`): render with `onOpenRun={spy}` + a row whose id !== activeRunId; tap -> assert spy called with the run AND internal RunDetailPage NOT rendered. Control: no onOpenRun -> tap opens the internal detail (fallback preserved).
  </action>
  <verify>
    <automated>cd frontend && npx vitest --run WorkflowHistory.openRun</automated>
  </verify>
  <done>Tap calls onOpenRun (not setSelectedRun); DashboardLayout passes the execution wiring; existing WorkflowHistory tests green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BUG-001 — bind the run-lane title to the viewed run (contentSourceRunId), not recents[0]</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx</files>
  <behavior>
    - recentRuns=[A,B,C], contentSourceRunId=B.id -> `lane-run-title` == B.title (not recents[0]=A).
    - contentSourceRunId=null (fresh launch) -> title == recents[0].title (byte-identical to today).
    - Viewed run outside the recents window (find -> undefined) -> falls back to submittedBrief (viewed run's own input), never a foreign run's title.
  </behavior>
  <action>
    Root cause (verified): `DashboardLayout.tsx:1323` `const latestRunTitle = recentRuns?.[0]?.title;` is unconditionally the most-recent run; the ternary (`:1324-1325`) prefers it over `submittedBrief`, so the title never tracks the viewed run.

    Replace `:1323` with a viewed-run lookup, keeping the ternary (`:1324-1325`) unchanged: `const viewedRun = contentSourceRunId != null ? recentRuns?.find((r) => r.id === contentSourceRunId) : recentRuns?.[0];` then `const latestRunTitle = viewedRun?.title;`. Both `contentSourceRunId` (`:210`) and `recentRuns` (`:209`) are already props. SC-001-safe (keys on `run.id`). Update the `:1319-1322` comment.

    Test `DashboardLayout.laneTitle.test.tsx` (mirror `DashboardLayout.catalogHome.test.tsx` render harness): render with recentRuns=[A,B,C] (distinct titles) + contentSourceRunId=B.id, driven to a lane-visible (`execution-chat-lane`) state; assert `lane-run-title` === B.title. Add the launch control (contentSourceRunId=null -> A.title). If a full complete-state render is impractical, fall back to a source-lock pinning `recentRuns?.find((r) => r.id === contentSourceRunId)` and that `recentRuns?.[0]?.title` is no longer the sole title source — but prefer the render assertion.
  </action>
  <verify>
    <automated>cd frontend && npx vitest --run DashboardLayout.laneTitle</automated>
  </verify>
  <done>lane-run-title tracks contentSourceRunId across a 3-run recents; launch case unchanged; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: BUG-003 — relax handleRevisePpt guard so the REST path fires + bind the revise-handler to the viewed run's type</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/app/dashboard/reviseGuard.source.test.ts, frontend/e2e/tests/ts-u.revisions.spec.ts</files>
  <behavior>
    - handleRevisePpt with contentSourceRunId set but pptxCode/pptContent empty no longer early-returns — reaches postRevision.
    - On a reopened (completed, not running) od_ppt run, activeReviseHandler resolves to handleRevisePpt.
    - e2e: reopening a completed od_ppt run with EMPTY output, typing a change, clicking Run refinement records exactly one POST /api/runs/{parent}/revisions (fail-before: zero non-GET /api/ commands).
    - No-regression: text-deliverable reopened runs unaffected; a fresh od_ppt build with content still revises.
  </behavior>
  <action>
    Root cause (verified): (1) `handleRevisePpt` opens `if (!pptxCode && !pptContent) return;` (`:493`), short-circuiting BEFORE the self-sufficient `contentSourceRunId` REST path (`:504` `postRevision`, which reseeds the parent server-side). On a reopened od_ppt run both fields are empty (no `ppt-code-generator` agent -> `pptxCode` undefined; `pptContent` empty when `fullRun.output` empty, LV-02), so it returns firing nothing; `confirmRefinement` (`RunChatLane.tsx:712-715`) clears the chip unconditionally. (2) the `workflowType` sync effect (`:330`) is `isRunning`-gated -> never fires on a completed reopen, so `activeReviseHandler` (`:1232-1237`) may pick the wrong handler.

    Two edits in `DashboardLayout.tsx`:
    1. Relax the guard `:493` to `if (!contentSourceRunId && !pptxCode && !pptContent) return;` — when a parent run id exists the handler proceeds to `postRevision` (`:504`). `contentSourceRunId` is already a dep (`:537`); the legacy text-injection fallback (`:520-536`) is unchanged (reached only when contentSourceRunId falsy). **Apply ONLY to `handleRevisePpt`** — the sole handler with a content-free REST bypass. Do NOT relax `handleReviseUserStory` (`:541`)/`handleRevisePrototype`/`handleReviseAppBuilder` (no REST path; they need content for text-injection; text deliverables persist non-empty output so their guards already pass on reopen).
    2. Bind the selector to the VIEWED run's type on a completed reopen (mirrors BUG-001): derive `const viewedRunType = !isPipelineRunning && contentSourceRunId != null ? recentRuns?.find((r) => r.id === contentSourceRunId)?.type : undefined;` and `const effectiveReviseType = viewedRunType ?? workflowType;`. Use `effectiveReviseType` in place of `workflowType` in the `activeReviseHandler` ternary (`:1232-1237`) so od_ppt/od_ppt_revision map to `handleRevisePpt` and add an explicit `od_prototype` case to the prototype branch. When running/launching, `viewedRunType` is undefined -> falls back to `workflowType` (byte-identical; no regression). SC-001-safe (keys on `run.type`; no literal added to a guarded component).

    Do NOT touch the od_ppt backend resolver (READ-ONLY). Confirm-path toast hardening is out of scope.

    Tests:
    - `reviseGuard.source.test.ts` (mirror `revisionFamilyLinkage.source.test.ts`): assert `DashboardLayout.tsx` CONTAINS `if (!contentSourceRunId && !pptxCode && !pptContent) return;` and `recentRuns?.find((r) => r.id === contentSourceRunId)?.type`, and STILL CONTAINS the linkage tokens `revisionFamilyLinkage.source.test.ts` pins (`postRevision(getToken() ?? "", contentSourceRunId,` and `source_workflow_run_id: sourceRunId`).
    - Extend `ts-u.revisions.spec.ts`: stub a completed od_ppt run with empty `output`, open it into the run screen, type a change, click the confirm chip, assert exactly one `POST /api/runs/{parent}/revisions` recorded (`mockSse.waitForCommand`). Preserve the existing `:86` case.
  </action>
  <verify>
    <automated>cd frontend && npx vitest --run reviseGuard.source revisionFamilyLinkage.source</automated>
  </verify>
  <done>Relaxed guard + viewed-run-type selector pinned; pre-existing revision-linkage locks green; ts-u.revisions reopen-revise fires one POST /revisions; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 5: Regression gate — establish the REAL green baseline across the at-risk suites + tsc</name>
  <files>frontend/ (test run + reconcile selector-drift only)</files>
  <action>
    Establish the real green count (the "132/0" baseline is stale). From inside `frontend/` (kill :3000 first): `npx tsc --noEmit` (clean). Run mocked Playwright: `ts-j`, `ts-sse`, `ts-s.reconnect`, `ts-chat`, `ts-chat-cards`, `ts-t.history`, `ts-u.revisions`, `ts-live-state`, new `ts-y.run-scope-clarify`. Run vitest: `useRunChat`, `useWorkflow*`, `RunChatLane`, `revisionFamilyLinkage.source`, `reviseGuard.source`, `AgentThinkingTab`, `DashboardLayout*`, `WorkflowHistory*`, new `WorkflowHistory.openRun` + `DashboardLayout.laneTitle`.
    For each RED: classify as selector-drift from these fixes (fix the test to the new correct behavior), changed-behavior (reconcile / test.fixme with a note — NEVER delete a failing test to go green), or pre-existing/flake (re-baseline against the pre-plan commit before calling it pre-existing). Report the real pass/fail counts in the SUMMARY. Do NOT run a live Bedrock run.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && (lsof -ti :3000 | xargs kill -9 2>/dev/null; true) && npx vitest --run useRunChat useWorkflow RunChatLane revisionFamilyLinkage.source reviseGuard.source DashboardLayout WorkflowHistory && npx playwright test --project=mocked ts-u.revisions ts-live-state ts-t.history ts-y.run-scope-clarify</automated>
  </verify>
  <done>tsc clean; at-risk vitest + Playwright pass (or every RED classified selector-drift-fixed / reconciled, never deleted); real green count reported in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> API | Unchanged — no new endpoint/payload/field; `postRevision` (POST /{id}/revisions) already exists. |
| SSE down-channel -> reducer | The run-scope guard only DROPS a foreign-run reset side-effect; it never widens what a frame can do. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j1u-01 | Tampering | page.tsx pipeline_start guard | accept | Guard keys only on `pipeline_run_id` equality vs a ref; a spoofed id at worst fails open to today's reset behavior — no new trust granted. |
| T-j1u-02 | Information Disclosure | WorkflowHistory onOpenRun | accept | Routes to the same owner-scoped execution screen the Home-recents path already uses; backend owner checks unchanged. |
| T-j1u-SC | Tampering | npm/pip/cargo installs | N/A | No package installs (FE source + tests only) — no supply-chain checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean.
- New fail-before/pass-after tests pass: `ts-y.run-scope-clarify` (005), `WorkflowHistory.openRun` (002), `DashboardLayout.laneTitle` (001), `reviseGuard.source` + `ts-u.revisions` reopen-revise (003).
- At-risk suites GREEN: `ts-u.revisions:86`, `revisionFamilyLinkage.source`, `useRunChat:140-143`, `ts-live-state`.
- SC-001: no workflow-name literal added to any guarded component; `page.tsx` exempt.
- Orchestrator (NOT executor) runs the live Bedrock proof: relaunch prototype in isolation -> clarify renders; open a live + terminal run from history -> correct title + chat lane + revise fires; two concurrent runs -> the viewed run's clarify survives.
</verification>

<success_criteria>
- BUG-005: a concurrent foreign run's pipeline_start no longer wipes the viewed run's clarify/seen-set; launch flow + same-tab new-run reset preserved.
- BUG-002: a top-nav History row tap opens the shared run screen (execution-chat-lane + composer), not RunDetailPage.
- BUG-001: the lane title tracks the viewed run; launch case byte-identical.
- BUG-003: Run refinement on a reopened empty-content run fires exactly one POST /revisions; sibling text-handler guards unchanged.
- One atomic commit per bug (order 005 -> 002 -> 001 -> 003), each with its fail-before/pass-after test; a final regression-green gate with real pass counts. No commit trailer; never pushed.
</success_criteria>

<output>
Create `.planning/quick/260716-j1u-fix-fe-run-screen-cluster-bug-001-002-00/260716-j1u-SUMMARY.md` when done.
</output>
