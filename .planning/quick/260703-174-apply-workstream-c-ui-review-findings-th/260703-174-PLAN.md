---
phase: 260703-174-apply-workstream-c-ui-review-findings-th
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/src/components/history/WorkflowHistory.tsx
  - frontend/src/components/results/ClarificationsCard.tsx
  - frontend/src/components/history/WorkflowHistory.runInput.test.tsx
  - frontend/src/components/preview/PreviewPanel.revisionChip.test.tsx
autonomous: true
requirements: [C-FLAG-1, C-FLAG-2, C-FLAG-3]

must_haves:
  truths:
    - "On a LIVE revision run, the Thinking tab's Starting-point card renders the 'revision of v{n}' chip, where n is the 1-based family index of the active run's PARENT (C-FLAG-1)."
    - "On a LIVE revision run, the Thinking tab's Starting-point card renders the 'Original brief (v1)' expander button — live/reopen symmetry (C-FLAG-2)."
    - "On a REOPEN of a revision run, the Thinking tab's Starting-point card renders the 'revision of v{n}' chip from the fetched family (C-FLAG-1)."
    - "A root run (parent_run_id/parentRunId null) renders NO revision chip on either mount (correct graceful absence)."
    - "The Clarifications timeline dot is a solid navy (bg-[#1B2A4A]) filled dot with a white glyph, matching the Starting-point first dot and the Planner done dot (C-FLAG-3)."
    - "REGRESSION GUARD: the full audited sibling suite stays green — WorkflowHistory.{family,test,revise,genericReopen,runInput}, PreviewPanel.{versionChip,degraded,genericDeliverable,revisionChip}, FilesTab.{test,baseVersion,runInput}, DashboardLayout.{catalogHome,waveMount}, StartingPointCard, ClarificationsCard, AgentThinkingTab.narrativeOrder — and the tsc identity gate prints GATE_OK (COUNT==3, FILT==0)."
  artifacts:
    - path: "frontend/src/components/preview/PreviewPanel.tsx"
      provides: "LIVE mount computes revisionParentVersion + originalBriefRootRunId from runFamily and threads both to the AgentThinkingTab mount (~:752)"
      contains: "revisionParentVersion="
    - path: "frontend/src/components/history/WorkflowHistory.tsx"
      provides: "REOPEN mount computes revisionParentVersion from the fetched family + selectedRun.parentRunId and threads it to the AgentThinkingTab mount (~:814); originalBriefRootRunId already threaded"
      contains: "revisionParentVersion="
    - path: "frontend/src/components/results/ClarificationsCard.tsx"
      provides: "timeline dot aligned to the solid-navy sibling-dot convention"
      contains: "bg-[#1B2A4A]"
    - path: "frontend/src/components/history/WorkflowHistory.runInput.test.tsx"
      provides: "extended reopen spec: revision run → chip + Original-brief expander render"
    - path: "frontend/src/components/preview/PreviewPanel.revisionChip.test.tsx"
      provides: "new LIVE-mount spec: revision family → chip + expander; root run → no chip"
  key_links:
    - from: "PreviewPanel.tsx activeParentRunId"
      to: "StartingPointCard 'revision of v{n}' chip"
      via: "sortedMembers.findIndex(parent) + 1 → revisionParentVersion → AgentThinkingTab prop"
      pattern: "revisionParentVersion=\\{"
    - from: "PreviewPanel.tsx runFamily.root_id"
      to: "StartingPointCard 'Original brief (v1)' expander"
      via: "originalBriefRootRunId (revision members only) → AgentThinkingTab prop"
      pattern: "originalBriefRootRunId=\\{"
    - from: "WorkflowHistory.tsx selectedRun.parentRunId"
      to: "StartingPointCard 'revision of v{n}' chip"
      via: "family.members findIndex(parent) + 1 → revisionParentVersion → AgentThinkingTab prop"
      pattern: "revisionParentVersion=\\{"
---

<objective>
Close the Workstream-C UI-review (`.planning/WORKSTREAM-C-UI-REVIEW.md`, 23/24 SHIP, 0 BLOCK, 3 FLAG) by fixing the 3 FLAGs, FRONTEND-ONLY. The substantive one is a dead spec-mandated feature: `StartingPointCard` already renders the "revision of v{n-1}" chip and the "Original brief (v1)" expander, but NEITHER `AgentThinkingTab` mount computes/threads `revisionParentVersion`, so the chip never appears in production (UI-SPEC Surface 1 acceptance unmet at runtime). This is a WIRING + cosmetic fix — the card, chip, and expander are ALREADY authored; do NOT re-style them.

Purpose: Make the contract-mandated revision-lineage affordances actually render, symmetrize the live/reopen surfaces, and align the Clarifications timeline dot to its sibling dots — with zero backend and zero new visual language.

Output:
- FIX 1 (C-FLAG-1): thread `revisionParentVersion` at BOTH `AgentThinkingTab` mounts (live `PreviewPanel.tsx:752`, reopen `WorkflowHistory.tsx:814`).
- FIX 2 (C-FLAG-2): thread `originalBriefRootRunId` at the LIVE mount (reopen already threads it) → live/reopen symmetry.
- FIX 3 (C-FLAG-3): swap the `ClarificationsCard` timeline dot to the solid-navy filled sibling-dot convention.
- Tests: extend the reopen spec + add a focused live-mount spec; full audited-set regression + tsc identity gates green.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/WORKSTREAM-C-UI-REVIEW.md
@.planning/WORKSTREAM-C-UI-SPEC.md
@frontend/src/components/results/StartingPointCard.tsx
@frontend/src/components/results/AgentThinkingTab.tsx
@frontend/src/components/results/ClarificationsCard.tsx
@frontend/src/components/preview/PreviewPanel.tsx
@frontend/src/components/history/WorkflowHistory.tsx
@frontend/src/components/history/RevisionFamilyView.tsx
@frontend/src/types/index.ts
@frontend/src/components/preview/PreviewPanel.versionChip.test.tsx
@frontend/src/components/history/WorkflowHistory.runInput.test.tsx
@frontend/src/components/results/StartingPointCard.test.tsx

# Contract facts confirmed from source (do NOT re-style — feed the card):
# - StartingPointCard renders the chip ONLY when `isRevision && revisionParentVersion != null`
#   (`StartingPointCard.tsx:106-110`) and the Original-brief expander ONLY when
#   `originalBriefRootRunId` is passed (`:131`). AgentThinkingTab already forwards BOTH
#   props to StartingPointCard (`AgentThinkingTab.tsx:634-638`). The gap is purely at the mounts.
# - `revisionParentVersion` = 1-based family index of the ACTIVE run's PARENT.
#   LIVE: PreviewPanel already derives `sortedMembers` (by revision_index, `:410-412`),
#   `activeRunId` (`:414`), `activeIdx` (`:415`), `activeParentRunId` (`:421`).
#   REOPEN: WorkflowHistory already fetches `family` (state, `:135`,`:197`) and has
#   `selectedRun.parentRunId` / `selectedRun.rootRunId`. The reopen mount (`:819`) ALREADY
#   threads `originalBriefRootRunId={selectedRun.parentRunId ? selectedRun.rootRunId : undefined}`
#   — so reopen only needs `revisionParentVersion` added (FLAG 2 is live-only).
# - FamilyMember carries snake_case `revision_index` + `parent_run_id`; RunFamily.root_id
#   (`types/index.ts:316-330`). WorkflowRun.parentRunId/rootRunId (`:304-305`).
# - tsc baseline is COUNT==3 total `error TS`, all in mockApi/IdeaInputPage → FILT==0.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread revisionParentVersion + originalBriefRootRunId at both mounts; align the Clarifications dot</name>
  <files>frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/history/WorkflowHistory.tsx, frontend/src/components/results/ClarificationsCard.tsx</files>
  <action>
Three edits, no new visual language, zero backend.

FIX 1+2 — PreviewPanel.tsx (LIVE mount): PreviewPanel already computes `sortedMembers` (members sorted by `revision_index`, ~:410-412) and `activeParentRunId` (the active member's `parent_run_id`, ~:421). Immediately AFTER the `activeParentRunId` const, add two derived consts: `revisionParentVersion` = the 1-based family index of the parent — compute `const activeParentIdx = activeParentRunId ? sortedMembers.findIndex((m) => m.id === activeParentRunId) : -1;` then `const revisionParentVersion = activeParentIdx >= 0 ? activeParentIdx + 1 : undefined;`; and `const originalBriefRootRunId = activeParentRunId ? runFamily?.root_id : undefined;` (root id only when the active member IS a revision — matches the reopen mount's guard). Then at the live `<AgentThinkingTab .../>` mount (~:752) add the two new props `revisionParentVersion={revisionParentVersion}` and `originalBriefRootRunId={originalBriefRootRunId}` alongside the existing `runInput` + `clarifications` props. Do NOT change any other prop. Rationale to encode in a brief comment: this closes C-FLAG-1 (dead chip) + C-FLAG-2 (live Original-brief asymmetry) — the derivation via parent_run_id lookup coincides with the existing `activeIdx` note (~:419) for a linear chain and is robust for branched families.

FIX 1 — WorkflowHistory.tsx (REOPEN mount): the reopen `<AgentThinkingTab .../>` mount (~:814-820) already threads `originalBriefRootRunId={selectedRun.parentRunId ? selectedRun.rootRunId : undefined}` — VERIFIED present, leave it. Only ADD `revisionParentVersion`. Inside the `if (selectedRun) {` render block (~:371), alongside the existing per-render consts (near `meta`/`workflowType`, before the JSX return), compute from the component-level `family` state (fetched via `getRunFamily`, ~:135/:197) and `selectedRun.parentRunId`: `const reopenSortedMembers = family ? [...family.members].sort((a, b) => a.revision_index - b.revision_index) : [];` then `const reopenParentIdx = selectedRun.parentRunId ? reopenSortedMembers.findIndex((m) => m.id === selectedRun.parentRunId) : -1;` then `const revisionParentVersion = reopenParentIdx >= 0 ? reopenParentIdx + 1 : undefined;`. Add `revisionParentVersion={revisionParentVersion}` to the reopen `<AgentThinkingTab>` props. Brief comment: C-FLAG-1 reopen fix; `family.members` uses the same `revision_index`-ASC ordering the VersionTimeline derives from (`RevisionFamilyView.tsx:402-406`), so the chip's v-number matches the version timeline.

FIX 3 — ClarificationsCard.tsx (cosmetic, C-FLAG-3): the TIMELINE DOT (~:63-64) currently fills with the tint `bg-[#E8EDF5]` + a navy glyph `text-[#1B2A4A]`. Align it to the solid-navy filled sibling-dot convention used by the Starting-point first dot (`StartingPointCard.tsx:87-88`: `bg-[#1B2A4A]` + `text-white` glyph) and the Planner done dot (`AgentThinkingTab.tsx:133,136`): change the dot wrapper class `bg-[#E8EDF5]` → `bg-[#1B2A4A]` (keep `border-2 z-10 border-[#1B2A4A]`), and the `MessageCircleQuestion` glyph class `text-[#1B2A4A]` → `text-white`. This is a pure className swap on the DOT only. Do NOT touch the header ICON CHIP (~:76-77, `bg-[#E8EDF5]` + `text-[#1B2A4A]`) — that chip is the correct inherited card-icon token and must stay. No new hex is introduced (both `#1B2A4A` and white are inherited tokens).

SCOPE FENCE: do NOT restyle StartingPointCard / the chip / the expander (already authored to spec). Do NOT touch B revision-family surfaces beyond reading the family. NO backend, NO new props on StartingPointCard/AgentThinkingTab (both already accept `revisionParentVersion` + `originalBriefRootRunId`), NO new visual language.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend && grep -q 'revisionParentVersion={revisionParentVersion}' src/components/preview/PreviewPanel.tsx && grep -q 'originalBriefRootRunId={originalBriefRootRunId}' src/components/preview/PreviewPanel.tsx && grep -q 'revisionParentVersion={revisionParentVersion}' src/components/history/WorkflowHistory.tsx && [ "$(grep -c 'bg-\[#1B2A4A\]' src/components/results/ClarificationsCard.tsx)" -ge 1 ] && [ "$(grep -c 'bg-\[#E8EDF5\]' src/components/results/ClarificationsCard.tsx)" -eq 1 ] && echo WIRING_OK</automated>
  </verify>
  <done>PreviewPanel computes revisionParentVersion (1-based parent index) + originalBriefRootRunId (revision-only) from runFamily and threads both at the live AgentThinkingTab mount; WorkflowHistory computes revisionParentVersion from the fetched family + selectedRun.parentRunId and threads it at the reopen mount (originalBriefRootRunId already present); the ClarificationsCard timeline dot is solid navy with a white glyph while the header icon chip is unchanged. `WIRING_OK` prints (the single remaining `bg-[#E8EDF5]` is the header icon chip).</done>
</task>

<task type="auto">
  <name>Task 2: Mount-level chip/expander specs + full regression + tsc identity gate</name>
  <files>frontend/src/components/history/WorkflowHistory.runInput.test.tsx, frontend/src/components/preview/PreviewPanel.revisionChip.test.tsx</files>
  <action>
Two specs proving the MOUNTS compute+thread the props (StartingPointCard unit-level chip rendering is already covered by `StartingPointCard.test.tsx` — do NOT duplicate that; assert at the mount level, which is the genuinely new coverage).

REOPEN (extend `WorkflowHistory.runInput.test.tsx` — do NOT create a parallel file): the harness already mocks `getRunFamily`/`getRunArtifacts`/`getWorkflow`/`getWorkflows`, the motion proxy, and stubs FilesTab, and has `makeRun(overrides)`. Add one `it(...)` in a new/existing describe: reopen a REVISION run — `mockGetWorkflows`/`mockGetWorkflow` resolve `makeRun({ id: "r1", title: "Revised run", type: "prototype_revision", input: "=== REVISION REQUEST ===\nmake the header blue\n=== END REQUEST ===", parentRunId: "root", rootRunId: "root" })`; `mockGetRunFamily.mockResolvedValue({ root_id: "root", members: [ { id: "root", type: "prototype", title: "v1", status: "completed", revision_index: 0, parent_run_id: null, created_at: t0, completed_at: t0 }, { id: "r1", type: "prototype_revision", title: "v2", status: "completed", revision_index: 1, parent_run_id: "root", created_at: t1, completed_at: t1 } ] })` (FamilyMember snake_case shape per `types/index.ts:316`); `mockGetRunArtifacts` → empty `{ workflow_id: "r1", artifacts: [] }` (PROCEED — isolates the chip). Render `<WorkflowHistory onBack={vi.fn()} />`, click the run title, `await` the family fetch, click the "Thinking" tab, then assert the Starting-point chip `screen.getByText(/revision of v1/i)` (parent "root" is family index 0 → 1-based v1) AND the Original-brief expander `screen.getByRole("button", { name: /original brief version 1/i })` is present. Keep the existing tests untouched. (The existing root-run reopen test already exercises the no-chip path; a light `expect(screen.queryByText(/revision of v/i)).toBeNull()` may be added there but is optional.)

LIVE (new focused spec `PreviewPanel.revisionChip.test.tsx` — JUSTIFIED, not redundant: the only PreviewPanel spec that renders the thinking tab, `PreviewPanel.versionChip.test.tsx`, STUBS AgentThinkingTab, so no existing spec can observe the live-mount computation): clone the versionChip harness — the `@/lib/api` mock exporting `getToken`/`getWorkflow`/`getRunFamily`, the STRIPPED_MOTION_PROPS motion proxy + `AnimatePresence` passthrough, and the heavy-child stubs (UserStoryPreview/PPTPreview/PrototypePreview/MarkdownPreview/AppBuilderPreview + `@/components/results/FilesTab`) — but DO NOT stub `@/components/results/AgentThinkingTab` (render the REAL card). Reuse the `M`/`family3` fixtures idiom (3 members: root idx0 parent null, r1 idx1 parent root, r2 idx2 parent r1). Because `initialTab` only switches to preview/files (`PreviewPanel.tsx:403`), reach the thinking tab by clicking the "Thinking" tab button (`screen.getByText("Thinking")`; the tab is registered at `PreviewPanel.tsx:303`). Test A (revision): render `<PreviewPanel workflowType="prototype" prototypeContent="<html>latest</html>" runFamily={family3} liveRunId="r2" runInput={"=== REVISION REQUEST ===\nmake the header blue\n=== END REQUEST ==="} />`, click "Thinking", assert `screen.getByText(/revision of v2/i)` (parent r1 = family index 1 → 1-based v2) AND `screen.getByRole("button", { name: /original brief version 1/i })`. Test B (root → no chip): same props but `liveRunId="root"` (root member, parent_run_id null) → after clicking "Thinking", `expect(screen.queryByText(/revision of v/i)).toBeNull()`. Do NOT click the Original-brief expander (its lazy `import("@/lib/api")` fetch is not under test; presence is enough). Note: with no `pipelineState`, AgentThinkingTab renders the timeline from `runInput` alone (`hasAnyData` true via `!!runInput`) and skips PlannerCard/TokenUsageSummary — the StartingPointCard revision variant renders because the input carries the REVISION markers.

Then run the FULL audited regression set + the tsc identity gate (both are hard gates — see <verify>).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend && npx vitest --run src/components/history/WorkflowHistory.family.test.tsx src/components/history/WorkflowHistory.test.tsx src/components/history/WorkflowHistory.revise.test.tsx src/components/history/WorkflowHistory.genericReopen.test.tsx src/components/history/WorkflowHistory.runInput.test.tsx src/components/preview/PreviewPanel.versionChip.test.tsx src/components/preview/PreviewPanel.degraded.test.tsx src/components/preview/PreviewPanel.genericDeliverable.test.tsx src/components/preview/PreviewPanel.revisionChip.test.tsx src/components/results/FilesTab.test.tsx src/components/results/FilesTab.baseVersion.test.tsx src/components/results/FilesTab.runInput.test.tsx src/components/layout/DashboardLayout.catalogHome.test.tsx src/components/layout/DashboardLayout.waveMount.test.tsx src/components/results/StartingPointCard.test.tsx src/components/results/ClarificationsCard.test.tsx src/components/results/AgentThinkingTab.narrativeOrder.test.tsx</automated>
  </verify>
  <done>The reopen spec asserts a REVISION reopen renders "revision of v1" + the Original-brief expander; the new live spec asserts a revision family (liveRunId=r2) renders "revision of v2" + the expander, and a root run (liveRunId=root) renders NO chip. The full 17-file audited regression set is green (extended, not parallelized). The tsc identity gate (Task-2 second verify) prints GATE_OK.</done>
</task>

</tasks>

<verification>
Phase-level hard gates (must both pass):

1. REGRESSION GUARD — the full audited sibling suite is green (Task 2 <verify> command). A wiring/cosmetic edit to PreviewPanel/WorkflowHistory/ClarificationsCard must not regress any sibling that renders AgentThinkingTab/FilesTab/PreviewPanel/WorkflowHistory (the B2/B3/C2 regression-guard lesson).

2. TSC IDENTITY GATE — the edits introduce zero new type errors:
   `cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend && OUT=$(npx tsc --noEmit 2>&1 || true); COUNT=$(printf '%s\n' "$OUT" | grep -c 'error TS'); FILT=$(printf '%s\n' "$OUT" | grep 'error TS' | grep -vE 'mockApi|IdeaInputPage' | grep -c 'error TS'); [ "$COUNT" -eq 3 ] && [ "$FILT" -eq 0 ] && echo GATE_OK || echo "GATE_FAIL count=$COUNT filt=$FILT"`
   Must print `GATE_OK` (COUNT==3 total, FILT==0 after filtering the known mockApi/IdeaInputPage baseline).

INV-3 holds by construction: zero backend edits, zero new dependencies.
Known-red baseline (WorkflowCatalog ×5 + others) is NOT chased — only the audited set above must be green.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | FE-only wiring + one className swap. No new trust boundary is crossed; no untrusted input is newly rendered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-174-01 | Information Disclosure | `revisionParentVersion` / `originalBriefRootRunId` threaded to StartingPointCard | accept | Values are the run's own family lineage (already owner/workspace-scoped by the B `/family` fetch); no cross-tenant data introduced. The Original-brief lazy fetch (`getWorkflow(rootRunId)`) is pre-existing and unchanged by this task. |
| T-174-02 | Tampering (XSS) | Chip/expander render path | accept | UI-review Pillar 6 confirmed zero `dangerouslySetInnerHTML` / `<iframe>` in the C surface; run-family content renders only as React text nodes / inside `<pre>`. This task adds numeric/id props + a className swap only — no new injection surface. |
| T-174-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — zero new dependencies. Nothing to audit. |
</threat_model>

<success_criteria>
- FIX 1 (C-FLAG-1): the "revision of v{n}" chip renders on BOTH the live and reopen Thinking-tab mounts for a revision run; a root run shows no chip.
- FIX 2 (C-FLAG-2): the live mount threads `originalBriefRootRunId`, so the "Original brief (v1)" expander appears on a live revision run — symmetric with reopen.
- FIX 3 (C-FLAG-3): the Clarifications timeline dot is a solid-navy filled dot matching the Starting-point and Planner done dots; the header icon chip is unchanged.
- StartingPointCard / the chip / the expander are NOT re-styled (fed, not authored). Zero backend edits (INV-3). Zero new visual language.
- Both hard gates green: full audited-set regression + tsc identity gate (GATE_OK, COUNT==3, FILT==0).
</success_criteria>

<output>
Create `.planning/quick/260703-174-apply-workstream-c-ui-review-findings-th/260703-174-SUMMARY.md` when done.
</output>
