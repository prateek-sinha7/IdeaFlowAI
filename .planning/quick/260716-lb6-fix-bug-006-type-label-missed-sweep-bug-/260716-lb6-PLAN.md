---
phase: quick-260716-lb6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx
  - frontend/src/components/layout/DashboardLayout.launchLatch.test.tsx
autonomous: true
requirements: [BUG-006, BUG-007]

must_haves:
  truths:
    - "Opening a history/recents run whose type differs from the last-launched workflowType shows the VIEWED run's type in the lane-run-type chip (not the stale workflowType)."
    - "A fresh launch (contentSourceRunId null / isRunning true) still shows the launched run's type in the chip — byte-identical to today."
    - "One prototype launch (one param object) mints exactly ONE run even under a React StrictMode double mount-effect invoke."
    - "A genuinely new launch (a new param object) still mints — the latch does not permanently block launches."
    - "The od_ppt launch consumer has the same single-mint latch as the od_prototype consumer."
  artifacts:
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "runType feed bound to effectiveReviseType (BUG-006) + two useRef object-identity latches on the launch-consumer effects (BUG-007)"
      contains: "launchedProtoParamsRef"
    - path: "frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx"
      provides: "extended stub echoing runType + BUG-006 lane-run-type assertions"
      contains: "lane-run-type"
    - path: "frontend/src/components/layout/DashboardLayout.launchLatch.test.tsx"
      provides: "StrictMode single-mint regression test for the launch-consumer latch"
      contains: "StrictMode"
  key_links:
    - from: "DashboardLayout.tsx:1745 (runType feed)"
      to: "effectiveReviseType (viewedRunType ?? workflowType, :1252)"
      via: "prop binding"
      pattern: "runType=\\{effectiveReviseType"
    - from: "DashboardLayout.tsx:637 (od_prototype consumer effect)"
      to: "launchedProtoParamsRef identity latch"
      via: "synchronous ref compare before side effects"
      pattern: "launchedProtoParamsRef\\.current === pendingOdProtoParams"
    - from: "DashboardLayout.tsx:686 (od_ppt consumer effect)"
      to: "launchedPptParamsRef identity latch"
      via: "synchronous ref compare before side effects"
      pattern: "launchedPptParamsRef\\.current === pendingOdPptParams"
---

<objective>
Close out the FE run-screen viewed-run-binding cluster with the two remaining minor, root-caused follow-ups from the live SSE QA campaign — both isolated to `frontend/src/components/layout/DashboardLayout.tsx`:

- **BUG-006** — the lane pipeline-type chip (`data-testid="lane-run-type"`) is fed the stale `workflowType` at `:1745`, so a completed history/recents-opened run shows the wrong type (e.g. an app_builder run labelled "USER_STORIES"/"PROTOTYPE"). This is the third consumer of the same stale state that quick 260716-j1u swept for the lane TITLE and the revise-handler selection but MISSED. The correct viewed-run value (`effectiveReviseType = viewedRunType ?? workflowType`, `:1252`) is already in scope and unused by the display.
- **BUG-007** — the od_prototype launch-consumer effect (`:636-679`) and its od_ppt twin (`:685-727`) are non-idempotent: their only guard (`if (!pendingOdProtoParams) return;`) clears the param asynchronously, so React StrictMode's dev-only double mount-effect invoke passes the guard twice and mints two runs for one launch. A synchronous `useRef` object-identity latch makes one param object mint once while a new launch (new object) still mints.

Purpose: complete the "run-screen state bound to the VIEWED run, not the last-launched run" sweep (siblings BUG-001/002/003/005) and make the launch intent idempotent, without touching any j1u fix, page.tsx, or the backend.

Output: one-line viewed-run binding at `:1745`, two ref latches on the launch consumers, and two fail-before/pass-after vitest specs. One atomic commit per bug.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/BUG-006-007-GROUNDED-CONTEXT.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/layout/DashboardLayout.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/frontend/src/components/chat/LaneRunHeader.tsx

# BUG-006 + BUG-007 full root causes (read the two entries before editing):
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.planning/SSE-QA-BUG-LOG.md

Pre-flight (executor): confirm the working tree is on branch `feat/ui-2` (the SSE-campaign
branch that carries the j1u sweep) before editing — the line anchors below assume that state.
`git branch --show-current` must print `feat/ui-2`. Grounding was verified against this tree:
runType feed at :1745, `effectiveReviseType`/`viewedRunType` at :1248-1252, `isPipelineRunning`
at :478, the od_prototype consumer effect at :636-679, the od_ppt twin at :685-727. If line
numbers have drifted, locate by the identifiers named in each action, not the raw line number.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-006 — bind the lane-run-type chip to the viewed run (+ fail-before/pass-after vitest)</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx</files>
  <behavior>
    Extend the RunChatLane stub in DashboardLayout.laneTitle.test.tsx to also echo its `runType`
    prop into `data-testid="lane-run-type"` (keep the existing `lane-run-title` echo untouched so
    the BUG-001 title tests still pass). Extend the `makeRun` helper to accept a `type` argument
    (default `"user_stories"` so existing runs are unchanged). Add a `describe` block for BUG-006:
    - Test 1 (viewed app_builder run): recentRuns where recents[0] is NOT the viewed run and the
      viewed run's `type` is `"app_builder"`; contentSourceRunId = that run's id. Assert
      `lane-run-type` == `"app_builder"`. (RED before the :1745 change — it shows the stale
      workflowType, `"prototype"` under the beforeEach od_prototype.pending latch; GREEN after.)
    - Test 2 (different viewed type, deeper in window): a viewed `"user_stories"` run distinct from
      the launch-latch workflowType; assert `lane-run-type` == `"user_stories"`. (Together Tests 1+2
      prove the chip tracks the VIEWED run's type, not one shared stale value.)
    - Test 3 (no launch regression): contentSourceRunId = null → assert `lane-run-type` ==
      workflowType (the `"prototype"` latch value) — passes both before and after (regression guard).
  </behavior>
  <action>
    BUG-006 fix (one contiguous edit): in `frontend/src/components/layout/DashboardLayout.tsx` at the
    `LaneRunHeader`/`RunChatLane` mount (~:1745), change the `runType` prop from
    `runType={workflowType || pipelineState?.pipeline_type}` to
    `runType={effectiveReviseType || pipelineState?.pipeline_type}`. `effectiveReviseType`
    (= `viewedRunType ?? workflowType`) is already declared at ~:1252 and in scope here (its sibling
    `activeReviseHandler`, built from it at :1253, is consumed two lines below at :1748). This mirrors
    the two j1u sibling fixes exactly: on a fresh launch/live run (`isRunning` true or
    `contentSourceRunId` null) `viewedRunType` is undefined so it resolves to `workflowType`
    (byte-identical to today, no launch regression); on a completed history/recents reopen it resolves
    to the viewed run's `type` (bug fixed). Do NOT reorder to prefer `pipelineState?.pipeline_type`,
    do NOT add `humanizeRunType`, do NOT add any workflow-name string literal (SC-001 — this is a
    guarded component; key only on generic run identity, which `effectiveReviseType` already does).
    Touch ONLY this one line in the .tsx.

    Test edit: extend `DashboardLayout.laneTitle.test.tsx` per the behavior block above — do not create
    a new file for BUG-006 (reuse the existing 80-line mock boilerplate). The RunChatLane stub must
    destructure `runType` in addition to `runTitle` and render a second `<div data-testid="lane-run-type">{runType}</div>`
    alongside the existing title div. Run the new BUG-006 tests and CONFIRM Test 1/Test 2 are RED
    against the pre-fix :1745 line first (see them fail — do not trust "should fail"), then apply the
    one-line fix and confirm all three GREEN and the existing BUG-001 title tests still GREEN.

    Commit atomically (BUG-006 only — the .tsx line + the extended laneTitle test), on branch
    `feat/ui-2`, NO commit trailer (no Co-Authored-By / Claude-Session lines), NEVER push.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest --run src/components/layout/DashboardLayout.laneTitle.test.tsx</automated>
  </verify>
  <done>
    `runType` at :1745 reads `effectiveReviseType || pipelineState?.pipeline_type`; the laneTitle test
    file has a `lane-run-type` echo + a BUG-006 describe block whose viewed-app_builder and
    viewed-user_stories assertions were seen RED pre-fix and are GREEN post-fix; the launch-null
    regression test and all BUG-001 title tests are GREEN; `tsc --noEmit` clean; one atomic
    trailer-free commit on feat/ui-2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BUG-007 — StrictMode-safe single-mint latch on both launch consumers (+ fail-before/pass-after vitest)</name>
  <files>frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/layout/DashboardLayout.launchLatch.test.tsx</files>
  <behavior>
    New file `DashboardLayout.launchLatch.test.tsx` (own mock boilerplate — stub RunChatLane,
    AppHeader, PreviewPanel, AgentProgressPanel, HomeLaunchGrid, CreationHub, LibraryPage,
    WorkflowHistory, AccountSettings, AnalyticsPage, IdeaInputPage, CompletionToast to inert markers,
    and mock next/navigation + motion/react + @/lib/api getToken/getRunFamily, mirroring the
    laneTitle test). `useNotifications` is provider-free (local useState) — no extra provider needed
    beyond the existing `SkillsHooksProvider` wrapper.
    - Test 1 (single-mint under StrictMode): render `<React.StrictMode><SkillsHooksProvider><DashboardLayout ...
      pendingOdProtoParams={paramsA} onStartPipeline={spy} onClearPendingOdProto={vi.fn()}
      connectionStatus="connected" /></SkillsHooksProvider></React.StrictMode>` where `paramsA`
      = `{ brief: "test brief", templateId: "t1", designSystemId: "ds1", discovery: undefined }`.
      Assert `spy` was called exactly ONCE with first arg `"od_prototype"`. MUST be seen RED
      (called twice) before the latch is added — this proves React 19 StrictMode's dev mount-effect
      double-invoke is active in the harness; if it is NOT called twice pre-fix, the test cannot
      validate the fix and must be adjusted before proceeding.
    - Test 2 (new launch still mints): after Test 1's mount, rerender the SAME StrictMode tree with a
      NEW param object `paramsB` (fresh identity) and assert `spy` fires again for the new object
      (the latch keys on object identity, not a permanent block).
    - Test 3 (od_ppt twin single-mints): same StrictMode mount with `pendingOdPptParams` + a
      `launchedPptParamsRef`-guarded consumer → `onStartPipeline` called exactly once with `"od_ppt"`.
  </behavior>
  <action>
    BUG-007 fix in `frontend/src/components/layout/DashboardLayout.tsx` — add a synchronous
    object-identity latch to BOTH launch-consumer effects so one param object mints at most once,
    robust to StrictMode's synchronous double mount-invoke (the async `onClearPendingOdProto`
    setState cannot null the param between the two invokes; a ref persists across the same-instance
    double mount).

    1. Declare two refs alongside the existing launch refs (near `pendingStartOnConnectRef` at ~:610,
       or immediately above each effect): `const launchedProtoParamsRef = useRef<object | null>(null);`
       and `const launchedPptParamsRef = useRef<object | null>(null);` (`useRef` is already imported).
    2. In the od_prototype consumer effect (~:636-679), immediately AFTER `if (!pendingOdProtoParams) return;`
       and BEFORE the first side effect (`setMainView("execution")`), add:
       `if (launchedProtoParamsRef.current === pendingOdProtoParams) return;` then
       `launchedProtoParamsRef.current = pendingOdProtoParams;`. Placement before the side effects is
       load-bearing — the second StrictMode invoke must early-return before any setState / mint.
    3. Apply the identical latch to the od_ppt twin effect (~:685-727) with `launchedPptParamsRef` and
       `pendingOdPptParams`.
    Preserve everything else: the existing async `onClearPendingOdProto()`/`onClearPendingOdPpt()`
    clears, the `connectionStatus === "connected"` vs `pendingStartOnConnectRef` branch, the extraParams
    assembly, and the `eslint-disable react-hooks/exhaustive-deps` + `[pendingOdProtoParams]` /
    `[pendingOdPptParams]` deps stay unchanged. Do NOT touch `useWorkflow.startPipeline`, do NOT set
    `reactStrictMode: false` in next.config.ts (that hides the real non-idempotency and changes nothing
    shippable), do NOT add an in-flight guard inside startPipeline. No page.tsx / backend change. No
    new workflow-name literal.

    Write the new `DashboardLayout.launchLatch.test.tsx` per the behavior block. Run it and CONFIRM
    Test 1 is RED (onStartPipeline called twice) against the pre-latch effects first, then add the two
    ref latches and confirm Tests 1/2/3 GREEN.

    Commit atomically (BUG-007 only — the two refs + two guards + the new launchLatch test), on branch
    `feat/ui-2`, NO commit trailer, NEVER push. This is a separate commit from Task 1 (atomic per bug).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest --run src/components/layout/DashboardLayout.launchLatch.test.tsx</automated>
  </verify>
  <done>
    Both launch-consumer effects have a `useRef` object-identity latch placed before their side
    effects; `DashboardLayout.launchLatch.test.tsx` exists with a StrictMode single-mint test seen
    RED (2 calls) pre-latch and GREEN (1 call) post-latch, a new-param-object re-mint test, and an
    od_ppt-twin single-mint test — all GREEN; `next.config.ts` unchanged; `tsc --noEmit` clean; one
    atomic trailer-free commit on feat/ui-2, distinct from Task 1's.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression gate — establish the real green baseline (vitest + mocked Playwright)</name>
  <files>(no source edits — verification only)</files>
  <action>
    Establish the REAL green baseline (the "132/0" figure is stale) after both fixes land. From the
    `frontend/` directory (FE tooling is cwd-sensitive):
    1. `npx tsc --noEmit` — clean.
    2. `npm test` (= `vitest --run`, the full FE vitest suite) — green, including the two touched/added
       specs and the at-risk vitest around revisions/live-state. Record the real pass/fail counts.
    3. Kill any process on port 3000 first, then `npm run e2e` (the mocked Playwright project). Keep the
       at-risk mocked specs green: `ts-u.revisions`, `revisionFamilyLinkage.source`, `ts-live-state`,
       and the j1u cluster specs (lane title / revise-type). Report the actual green/red counts as the
       new baseline — do NOT cite the stale 132/0.
    If any at-risk test goes red, triage per the e2e-staleness method: selector-drift (fix the shared
    page-object) vs changed-behavior (reconcile) vs harness-flake — do NOT delete or loosen a failing
    test, and do NOT let a red mask a real regression from these two edits. Do NOT run a live Bedrock
    run — the orchestrator does the live check (reopen an app_builder run → correct type label; one
    wizard launch in a prod-like build → one mint).
    This task makes NO source edits and NO new commit (it is a verification gate); if a fix is needed it
    belongs to Task 1 or Task 2's scope and its own atomic commit.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>
    `tsc --noEmit` clean; full FE vitest suite green with real recorded counts; mocked Playwright run
    completed with the at-risk specs (ts-u.revisions, revisionFamilyLinkage.source, ts-live-state, j1u
    lane-title/revise-type) green and the true baseline reported; no live Bedrock run performed; no
    source edits in this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Both fixes are client-side render/idempotency changes. No new untrusted input crosses any boundary; no new network path, endpoint, param, or persisted field is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-lb6-01 | Tampering | BUG-007 launch latch could suppress a legitimate second launch | mitigate | Latch keys on object identity (`=== pendingOdProtoParams`); each real launch produces a fresh object (page.tsx:1053), so a genuine new launch still mints — asserted by Task 2 Test 2. |
| T-lb6-02 | Denial of Service | non-idempotent launch mints extra runs (BUG-007) wasting tokens + doubling concurrent-run load feeding BUG-004/005 | mitigate | Synchronous ref latch collapses the StrictMode double mint to one; verified single-mint under StrictMode double-invoke (Task 2). |
| T-lb6-03 | Information Disclosure | BUG-006 mislabels a run's type in the UI (cosmetic mislabel, not data leak) | mitigate | Bind the chip to the viewed run's own `type` via `effectiveReviseType`; verified across mixed-type reopens (Task 1). |
| T-lb6-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed — edits reuse existing React `useRef` + in-repo test tooling only; no package-manager install task in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (from `frontend/`).
- BUG-006: `DashboardLayout.laneTitle.test.tsx` — `lane-run-type` tracks the viewed run's type across a mixed-type recents (RED before :1745 change, GREEN after); launch-null regression + BUG-001 title tests stay GREEN.
- BUG-007: `DashboardLayout.launchLatch.test.tsx` — one param object mints once under StrictMode double mount-invoke (RED = 2 calls before the latch, GREEN = 1 after); a new param object still mints; od_ppt twin single-mints.
- Regression: full FE vitest suite + mocked Playwright green; at-risk specs (ts-u.revisions, revisionFamilyLinkage.source, ts-live-state, j1u lane-title/revise-type) green; real baseline recorded (132/0 is stale).
- No `page.tsx`, `next.config.ts`, backend, or j1u-fix edits. One atomic trailer-free commit per bug on feat/ui-2. No push. No live Bedrock run (orchestrator owns the live check).
</verification>

<success_criteria>
- BUG-006: the lane-run-type chip shows the VIEWED run's type on a history/recents reopen (fixed) and the launched run's type on a fresh launch (byte-identical) — proven RED→GREEN in vitest.
- BUG-007: one prototype (and one od_ppt) launch mints exactly one run under a StrictMode double mount-invoke, while a new launch still mints — proven RED→GREEN in vitest.
- Only `DashboardLayout.tsx` (one line for 006 + two refs/two guards for 007) and its two test files changed; no scope-reduction language, no j1u regression, no new workflow-name literals.
- Two atomic commits (one per bug), no trailer, on feat/ui-2, never pushed; real green baseline established offline.
</success_criteria>

<output>
Create `.planning/quick/260716-lb6-fix-bug-006-type-label-missed-sweep-bug-/260716-lb6-SUMMARY.md` when done.
</output>
