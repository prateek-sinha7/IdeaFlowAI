---
phase: quick-260704-t2x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/capabilities/model_pricing.py
  - backend/agents/execution_engine/engine.py
  - backend/app/api/websocket.py
  - backend/tests/agents/test_model_pricing.py
  - .planning/FIX-REGISTER.md
  - .planning/ISSUES-REGISTER.md
  - .planning/STATE.md
autonomous: true
requirements: [FIX-035, ISS-032]

must_haves:
  truths:
    - "A Sonnet/Opus run reports its own per-token cost (not Haiku's) — 4x–20x higher than before the fix."
    - "Both cost sites (engine pipeline_complete AND persisted workflow_runs.token_usage) compute cost via the ONE shared estimate_cost_usd function — no inline formula, no per-model branch at either call site."
    - "A run with NULL model_id prices against settings.BEDROCK_INFERENCE_PROFILE_ID (the Haiku-4.5 default profile)."
    - "eu./us./apac regional model ids carry a +10% premium; global./anthropic-direct/bare do not."
    - "The 5 characterization goldens stay byte/event-identical with SNAPSHOT_UPDATE unset (INV-3 golden-neutral — cost + model_id are stripped keys, so a rate change cannot perturb a golden)."
    - "estimate_cost_usd stays cache-ready (read/write-5m/write-1h tiers) even though both call sites pass cache args = 0 today."
  artifacts:
    - path: "backend/agents/capabilities/model_pricing.py"
      provides: "Kernel-pure per-family + regional pricing: Price, MODEL_PRICING, _FALLBACK, _canonical, _regional_premium, estimate_cost_usd"
      contains: "def estimate_cost_usd"
      min_lines: 60
    - path: "backend/tests/agents/test_model_pricing.py"
      provides: "Offline unit + reconciliation pin + both-call-sites-import coverage"
      contains: "def test_"
  key_links:
    - from: "backend/agents/execution_engine/engine.py"
      to: "agents.capabilities.model_pricing.estimate_cost_usd"
      via: "import + call at the pipeline_complete cost site (~L2277)"
      pattern: "estimate_cost_usd\\("
    - from: "backend/app/api/websocket.py"
      to: "agents.capabilities.model_pricing.estimate_cost_usd"
      via: "import + call at the workflow_runs.token_usage persist site (~L1993)"
      pattern: "estimate_cost_usd\\("
---

<objective>
Flowin under-reports run cost 4x–20x: BOTH cost sites hardcode Claude-3-Haiku $0.25/$1.25 per M for EVERY model. Introduce one kernel-pure pricing module (`backend/agents/capabilities/model_pricing.py`) with per-family + regional + cache-tier rates, and route both cost sites through its shared `estimate_cost_usd`. INV-3 golden-neutral: `estimated_cost_usd` + `model_id` are already stripped keys, so this is a pure rate swap with NO golden regeneration.

Purpose: Correct billing/telemetry accuracy without changing the event contract, the runner, the catalog, or any golden.
Output: New kernel-pure module + both sites re-routed + offline test suite + FIX-035/ISS-032 docs.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@backend/CLAUDE.md
@.planning/STATE.md
@backend/agents/capabilities/model_catalog.py
@backend/tests/agents/characterization/_normalize.py
@backend/pyproject.toml

# Cost sites (read the exact in-scope variables before editing):
# - backend/agents/execution_engine/engine.py  ~L2243-2279  (model_id, _tok_in, _tok_out, _settings)
# - backend/app/api/websocket.py                ~L1936-1996  (wr, wr.model_id, settings@L18, total_input/total_output)
</context>

<constraints>
- Kernel-purity (import-linter contract "agents.capabilities must not import the execution kernel or the web layer"): `model_pricing.py` imports ONLY stdlib (`dataclasses`, `re`, `logging`, `__future__`). NO `app.*`, NO `agents.execution_engine`, NO `agents.workflows`. Mirror the style/purity of the sibling `model_catalog.py`.
- INV-12 (no dual impl): both cost sites MUST call the SAME `estimate_cost_usd`. Delete both inline `(_tok_in * 0.00000025) + (_tok_out * 0.00000125)` formulas entirely.
- INV-3 (golden-neutral): NO new key in `pipeline_complete` / `token_usage`. `estimated_cost_usd` + `model_id` are in `_VOLATILE_STRIP_KEYS` and tokens are sentinel-replaced, so a rate change is invisible to the goldens by construction. Run goldens with SNAPSHOT_UPDATE UNSET; NEVER regenerate.
- SC-001: NO per-model / per-workflow branching at the call sites — the module owns all model-keyed logic.
- Scope ONLY these files. NO runner change, NO model_catalog edit, NO migration, NO FE change.
- Runtime: `python3.11`, no venv. Do NOT run the full pytest suite (offline-hangs). Atomic commits, NO `Co-Authored-By` trailer.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the kernel-pure model_pricing module</name>
  <files>backend/agents/capabilities/model_pricing.py</files>
  <behavior>
    - estimate_cost_usd("eu.anthropic.claude-haiku-4-5-20251001-v1:0", 21_460_378, 225_860) == 24.85 (±0.01)  [reconciliation pin]
    - _canonical("eu.anthropic.claude-haiku-4-5-20251001-v1:0") == "claude-haiku-4-5"
    - _canonical("eu.anthropic.claude-sonnet-4-6") == "claude-sonnet-4-6"
    - _canonical("eu.anthropic.claude-opus-4-6-v1") == "claude-opus-4-6"
    - _canonical("global.anthropic.claude-sonnet-4-5-20250929-v1:0") == "claude-sonnet-4-5"
    - _canonical("nonsense-model") == "claude-haiku-4-5"  [unknown -> _FALLBACK, warn once]
    - _regional_premium("eu.anthropic.claude-...") == 1.10 ; _regional_premium("us....") == 1.10 ; _regional_premium("apac....") == 1.10
    - _regional_premium("global.anthropic.claude-...") == 1.0 ; _regional_premium("anthropic.claude-...") == 1.0 ; _regional_premium("") == 1.0
    - estimate_cost_usd(None, 1_000_000, 0) prices as claude-haiku-4-5 at 1.0x premium == round(1.0, 6)
    - cache tiers (haiku base input=1e-6): cache_read priced at 0.1e-6/tok; cache_write at 1.25e-6/tok when cache_ttl="5m", 2e-6/tok when cache_ttl="1h"
  </behavior>
  <action>
    Create `backend/agents/capabilities/model_pricing.py` as a kernel-pure, stdlib-only DATA+PURE-FN module, mirroring the docstring + `from __future__ import annotations` + frozen-dataclass style of the sibling `model_catalog.py`. Module docstring must cite the import-linter contract (no `app.*` / no `agents.execution_engine`) and note this is the single source of run-cost rates (INV-12).

    Define, in order:
    - `@dataclass(frozen=True) class Price` with fields `input, output, cache_read, cache_write_5m, cache_write_1h` — each a float USD price per SINGLE token.
    - `MODEL_PRICING: dict[str, Price]` keyed by FAMILY id (verified base Anthropic-direct / Bedrock-global rates), exactly:
        claude-haiku-4-5  = Price(1e-6,  5e-6,  0.1e-6, 1.25e-6, 2e-6)
        claude-sonnet-4-5 = Price(3e-6,  15e-6, 0.3e-6, 3.75e-6, 6e-6)
        claude-sonnet-4-6 = Price(3e-6,  15e-6, 0.3e-6, 3.75e-6, 6e-6)
        claude-opus-4-5   = Price(5e-6,  25e-6, 0.5e-6, 6.25e-6, 10e-6)
        claude-opus-4-6   = Price(5e-6,  25e-6, 0.5e-6, 6.25e-6, 10e-6)
      Add a code comment: "Opus 4.6 / Sonnet 4.6 Bedrock $ are DERIVED — operator confirm on the live AWS pricing page."
    - `_FALLBACK = "claude-haiku-4-5"`.
    - `_canonical(model_id: str) -> str`: iteratively strip any leading geographic/provider prefix token from the set {`eu.`, `us.`, `apac.`, `global.`, `anthropic.`} (so `eu.anthropic.` fully strips), THEN strip a trailing version/date suffix via `re.sub(r"(-\d{8})?(-v\d+(?::\d+)?)?$", "", key)` (covers `-vN`, `-vN:0`, `-YYYYMMDD`, `-YYYYMMDD-vN:0`). Return the resulting family key if it is in `MODEL_PRICING`, else return `_FALLBACK` after `logger.warning` ONCE per unknown family (dedupe via a module-level `_WARNED_UNKNOWN: set[str]`).
    - `_regional_premium(model_id: str) -> float`: return 1.10 iff `model_id` starts with one of `eu.`, `us.`, `apac.`; else 1.0 (covers `global.`, `anthropic.`-direct, bare `claude-`, and empty string).
    - `estimate_cost_usd(model_id: str | None, input_tokens: int, output_tokens: int, cache_read_tokens: int = 0, cache_write_tokens: int = 0, *, cache_ttl: str = "5m") -> float`:
        p = MODEL_PRICING[_canonical(model_id or _FALLBACK)]
        mult = _regional_premium(model_id or "")
        write_rate = p.cache_write_1h if cache_ttl == "1h" else p.cache_write_5m
        usd = (input_tokens * p.input + output_tokens * p.output + cache_read_tokens * p.cache_read + cache_write_tokens * write_rate) * mult
        return round(usd, 6)
    - Module-level `logger = logging.getLogger(__name__)`.
    No `app.*` import, no `agents.execution_engine` import, no `agents.workflows` import.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -c "from agents.capabilities.model_pricing import estimate_cost_usd, _canonical, _regional_premium; assert abs(estimate_cost_usd('eu.anthropic.claude-haiku-4-5-20251001-v1:0', 21_460_378, 225_860) - 24.85) < 0.01; assert _canonical('eu.anthropic.claude-opus-4-6-v1')=='claude-opus-4-6'; assert _canonical('nonsense-model')=='claude-haiku-4-5'; assert _regional_premium('eu.anthropic.x')==1.10 and _regional_premium('global.anthropic.x')==1.0; print('OK')" && (grep -Ev '^\s*#' agents/capabilities/model_pricing.py | grep -Eq 'import (app|agents\.execution_engine|agents\.workflows)' && echo 'PURITY-FAIL' || echo 'PURITY-OK') && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Module exists, is stdlib-only (PURITY-OK), the reconciliation pin returns 24.85 (±0.01), _canonical/_regional_premium behave per the behavior block, and lint-imports reports 4 contracts kept / 0 broken.</done>
</task>

<task type="auto">
  <name>Task 2: Route both cost sites through estimate_cost_usd</name>
  <files>backend/agents/execution_engine/engine.py, backend/app/api/websocket.py</files>
  <action>
    Replace BOTH inline hardcoded-Haiku cost formulas with a call to the shared function (INV-12; SC-001 — no per-model branch, both sites share ONE function).

    engine.py (pipeline_complete site, ~L2277): add a module-level import `from agents.capabilities.model_pricing import estimate_cost_usd` (place it with the other top-level `agents.capabilities.*` imports, not lazily inside the method — engine.py already imports from `agents.capabilities.deliverables._mimetype` lazily, but a top-level import is preferred here and is import-linter-safe since the kernel importing a capability PORT is the sanctioned direction). Change the `"estimated_cost_usd"` value from `round((_tok_in * 0.00000025) + (_tok_out * 0.00000125), 6)` to `estimate_cost_usd(model_id or _settings.BEDROCK_INFERENCE_PROFILE_ID, _tok_in, _tok_out)`. Leave the adjacent `"model_id": model_id or _settings.BEDROCK_INFERENCE_PROFILE_ID` line unchanged. Cache args omitted (default 0) — the runner does not surface cache tokens yet (flagged follow-up ISS-032).

    websocket.py (workflow_runs.token_usage persist site, ~L1993): add `from agents.capabilities.model_pricing import estimate_cost_usd` (top-level, near the existing `app.core.config` import at L18 — this is the app layer importing a capability, which is allowed). Change the `"estimated_cost_usd"` value inside the `wr.token_usage = json.dumps({...})` block from `round((total_input * 0.00000025) + (total_output * 0.00000125), 6)` to `estimate_cost_usd(wr.model_id or settings.BEDROCK_INFERENCE_PROFILE_ID, total_input, total_output)`. `wr` (the WorkflowRun row, fetched ~L1936) carries a nullable `model_id` column; `settings` is imported at L18. Remove the now-stale `# Haiku pricing: $0.25/M input, $1.25/M output` comment.

    Do NOT add any new key to either payload. Do NOT touch the tokens keys or model_id keys.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && grep -c 'estimate_cost_usd' agents/execution_engine/engine.py app/api/websocket.py && (grep -Eq '0\.00000025|0\.00000125' agents/execution_engine/engine.py app/api/websocket.py && echo 'STALE-FORMULA-REMAINS' || echo 'FORMULAS-GONE') && python3.11 -c "import ast; [ast.parse(open(p).read()) for p in ['agents/execution_engine/engine.py','app/api/websocket.py']]; print('PARSE-OK')" && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>Both files import + call estimate_cost_usd (grep count >= 2 each: import + call), both inline 0.00000025/0.00000125 formulas are gone (FORMULAS-GONE), both files parse, and lint-imports = 4 kept / 0 broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline test suite + INV-3 golden gate</name>
  <files>backend/tests/agents/test_model_pricing.py</files>
  <behavior>
    - Per-family base rates: assert MODEL_PRICING[family].input/output for all 5 families match the pinned table.
    - _regional_premium: eu.=1.10x, us.=1.10x, apac.=1.10x, global.=1.0x, anthropic-direct=1.0x, bare/empty=1.0x.
    - _canonical mappings: the 5 full model ids -> their families; unknown id -> claude-haiku-4-5.
    - Reconciliation pin: estimate_cost_usd("eu.anthropic.claude-haiku-4-5-20251001-v1:0", 21_460_378, 225_860) == pytest.approx(24.85, abs=0.01).
    - Cache tiers on haiku: cache_read priced 0.1x input; cache_write 1.25e-6/tok at ttl "5m" and 2e-6/tok at ttl "1h" (assert the delta between the two ttls).
    - Regional premium compounds: a us./eu. Opus quote is exactly 1.10x its global. quote for identical tokens.
    - Both call sites use the module: assert `estimate_cost_usd` appears in the SOURCE of both engine.py and websocket.py (read the files, assert the import/symbol present) — pins INV-12 shared-function contract.
  </behavior>
  <action>
    Create `backend/tests/agents/test_model_pricing.py` (offline, no network, no DB, no Bedrock) covering the behavior block. Import from `agents.capabilities.model_pricing`. For the "both call sites use the module" test, read the two source files off disk (`pathlib.Path(...).read_text()`) and assert `"estimate_cost_usd"` is present in each — this locks INV-12 without importing the heavy engine/websocket modules. Use `pytest.approx` for the reconciliation and cache-tier assertions. Keep it fast and fully offline.

    Then run the INV-3 golden gate WITHOUT regenerating (SNAPSHOT_UPDATE must be unset) plus the context-providers regression, and confirm the pricing change is invisible to the goldens (it is, by construction — `estimated_cost_usd` + `model_id` are in `_VOLATILE_STRIP_KEYS`).
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/agents/test_model_pricing.py tests/agents/test_context_providers.py -q && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_od_ppt.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>test_model_pricing.py green (all behavior-block cases pass), test_context_providers.py green, the 5 characterization goldens byte/event-identical with SNAPSHOT_UPDATE unset (NO regen), and lint-imports 4 kept / 0 broken. If any golden shows a diff, confirm it is NOT in the stripped estimated_cost_usd/model_id keys — a pricing-caused golden change is impossible and would indicate an unrelated pre-existing drift to log, not regenerate.</done>
</task>

<task type="auto">
  <name>Task 4: Register the fix (FIX-035, ISS-032) + STATE</name>
  <files>.planning/FIX-REGISTER.md, .planning/ISSUES-REGISTER.md, .planning/STATE.md</files>
  <action>
    FIX-REGISTER.md: append one new 8-column table row FIX-035 immediately after the FIX-034 row (~L44), matching the existing pipe-delimited column shape (`| ID | Date | short-desc | root-cause+fix | Files | Phase | Invariants | Status |`). Date 2026-07-04. Short-desc: run cost under-reported ~4x (Haiku 4.5) to ~20x (Opus) — both engine.py:2277 + websocket.py:1993 hardcoded Claude-3-Haiku $0.25/$1.25/M for every model. Root-cause+fix: new kernel-pure `model_pricing.py` (per-family `MODEL_PRICING` + `_canonical` + `_regional_premium` +10% eu./us./apac + cache tiers + `estimate_cost_usd`); both sites route through it; `model_id` -> `BEDROCK_INFERENCE_PROFILE_ID` fallback; cache-token surfacing a flagged follow-up (ISS-032); INV-3 golden-neutral (cost+model_id stripped keys). Files: `backend/agents/capabilities/model_pricing.py`, `backend/agents/execution_engine/engine.py`, `backend/app/api/websocket.py`, `backend/tests/agents/test_model_pricing.py`. Phase: quick-260704-t2x. Invariants: INV-1/3/12/13 · SC-001 ✅. Status: Done.

    ISSUES-REGISTER.md: append ISS-032 (respect the register's append-only-by-ID convention) — follow-up: the runner does not yet surface `cache_read_tokens` / `cache_write_tokens` from LangChain `usage_metadata.input_token_details`; `estimate_cost_usd` is already cache-ready (cache_read / cache_write_5m / cache_write_1h tiers) but both call sites pass 0 today, so cache-discounted billing is not yet reflected. Action: thread cache token counts from `usage_metadata.input_token_details` into `estimate_cost_usd` at both sites for true cache-discounted pricing. Status: OPEN (deferred).

    STATE.md: update the `## Current Position` "Last activity" line to record the completed quick task 260704-t2x (per-model/region-aware run-cost pricing; FIX-035 + ISS-032; INV-3 5 goldens byte/event-identical, no regen; lint-imports 4/0). Do NOT churn the front-matter progress block.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin && grep -q 'FIX-035' .planning/FIX-REGISTER.md && grep -q 'ISS-032' .planning/ISSUES-REGISTER.md && grep -q '260704-t2x' .planning/STATE.md && echo 'DOCS-OK'</automated>
  </verify>
  <done>FIX-035 row present in FIX-REGISTER.md, ISS-032 appended to ISSUES-REGISTER.md, and STATE.md records the 260704-t2x completion (DOCS-OK).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run telemetry -> billing/cost display | `estimated_cost_usd` values feed operator-facing cost telemetry; an incorrect rate misinforms billing decisions (the very bug being fixed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t2x-01 | Tampering | new `model_pricing.py` reaching across the hexagonal boundary | mitigate | Purity gate: stdlib-only imports; enforced by `lint-imports` (contract "agents.capabilities must not import the execution kernel or the web layer") + a grep purity check in Task 1 verify. |
| T-t2x-02 | Information disclosure | wrong cost reported to operators (under-report) | mitigate | Reconciliation pin (24.85 ±0.01) + per-family/regional unit tests pin the rates; both sites share ONE function (INV-12) so they cannot silently diverge again. |
| T-t2x-03 | Denial of service | unknown/malformed model_id crashes the cost calc | mitigate | `_canonical` degrades unknown ids to `_FALLBACK` (warn-once, no raise); `model_id or _FALLBACK` and `model_id or ""` guard None; cost calc never raises. |
| T-t2x-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this task — stdlib-only new module, no new dependency; nothing to legitimacy-gate. |
</threat_model>

<verification>
Whole-plan gates (run from `backend/`, `python3.11`, no venv, offline):
1. `python3.11 -m pytest tests/agents/test_model_pricing.py tests/agents/test_context_providers.py -q` → green.
2. `python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_od_ppt.py -q` (SNAPSHOT_UPDATE UNSET) → 5 goldens byte/event-identical, NO regen.
3. `/opt/homebrew/bin/lint-imports` (from `backend/`) → 4 contracts kept, 0 broken.
4. `grep -E '0\.00000025|0\.00000125' agents/execution_engine/engine.py app/api/websocket.py` → no matches (both inline formulas removed).
5. Docs: FIX-035 in FIX-REGISTER.md, ISS-032 in ISSUES-REGISTER.md, 260704-t2x in STATE.md.
</verification>

<success_criteria>
- A run's `estimated_cost_usd` reflects the run's actual model family + region premium (Haiku 1x → Opus ~20x correction), at BOTH the engine `pipeline_complete` event and the persisted `workflow_runs.token_usage`.
- Both cost sites compute cost via the single shared `estimate_cost_usd` (INV-12); no inline formula, no per-model branch (SC-001) remains.
- `model_pricing.py` is kernel-pure (stdlib-only); `lint-imports` 4 kept / 0 broken.
- INV-3 preserved: 5 characterization goldens byte/event-identical, SNAPSHOT_UPDATE unset, NO regen.
- `estimate_cost_usd` is cache-ready (read / write-5m / write-1h tiers); cache-token surfacing logged as ISS-032 follow-up.
- FIX-035 + ISS-032 + STATE recorded.
</success_criteria>

<output>
Create `.planning/quick/260704-t2x-per-model-region-aware-run-cost-pricing/260704-t2x-SUMMARY.md` when done.
</output>
