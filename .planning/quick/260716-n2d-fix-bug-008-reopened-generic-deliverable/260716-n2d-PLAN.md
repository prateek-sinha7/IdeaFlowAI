---
phase: quick-260716-n2d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/preview/PreviewPanel.tsx
  - frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx
  - frontend/e2e/tests/ts-t.history.spec.ts
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/app/dashboard/contentSourceRunScope.source.test.ts
autonomous: true
requirements: [BUG-008, BUG-011]
must_haves:
  truths:
    - "Reopening a completed custom/generic HTML run on the shared run screen renders the P18 sandboxed iframe (sandbox=allow-scripts, no allow-same-origin), NOT the neutral 'Output will appear here' empty state (BUG-008)"
    - "A present generic deliverable is no longer suppressed by a stale workflowType default ('user_stories') when the typed renderer for that renderType has nothing to show (BUG-008)"
    - "A FOREIGN concurrent run's pipeline_complete no longer re-points the VIEWED contentSourceRunId (BUG-011)"
    - "The launched/tracked run's own pipeline_complete STILL sets contentSourceRunId — the primary launch->watch flow (launched==viewed) is byte-identical (BUG-011)"
    - "Typed deliverables (user_stories/ppt/prototype/app_builder) still render their bespoke renderer with no iframe; the P18 sandbox security contract and GenericDeliverablePreview are untouched (no regression)"
  artifacts:
    - path: "frontend/src/components/preview/PreviewPanel.tsx"
      provides: "hasGenericDeliverable decoupled from the stale-workflowType gate via knownContentPresent (BUG-008)"
      contains: "knownContentPresent"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Run-scoped setContentSourceRunId guard on pipeline_complete (BUG-011)"
      contains: "isForeignCompletion"
    - path: "frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx"
      provides: "BUG-008 stale-workflowType reopen cases (html -> iframe, markdown -> MarkdownPreview)"
      contains: "BUG-008"
    - path: "frontend/src/app/dashboard/contentSourceRunScope.source.test.ts"
      provides: "BUG-011 source-lock: run-scoped guard present, old unconditional set gone, launch pins intact"
      contains: "isForeignCompletion"
    - path: "frontend/e2e/tests/ts-t.history.spec.ts"
      provides: "TS-T-04b un-fixme'd — generic reopen renders the sandboxed iframe in the run screen"
      contains: "TS-T-04b generic/custom reopen"
  key_links:
    - from: "PreviewPanel hasGenericDeliverable (:577)"
      to: "renderDeliverable() step-2 GenericDeliverablePreview (:890)"
      via: "hasContent (:579) no longer starved -> preview tab calls renderDeliverable instead of the empty-state short-circuit (:1016)"
      pattern: "hasGenericDeliverable"
    - from: "page.tsx pipeline_complete branch (:530)"
      to: "setContentSourceRunId"
      via: "gate on trackedRunIdRef.current so a foreign completion is skipped"
      pattern: "trackedRunIdRef\\.current"
---

<objective>
Fix two frontend "run-screen state not bound to the viewed run" bugs (same family as BUG-001/005/006), both root-caused to exact lines in `.planning/BUG-008-011-GROUNDED-CONTEXT.md`:

- **BUG-008 (major):** a reopened generic/custom HTML deliverable renders the empty "Output will appear here" state instead of the sandboxed iframe, because `hasGenericDeliverable` (`PreviewPanel.tsx:577`) is gated by `isKnownRenderType`, which derives from a stale `workflowType="user_stories"` default on a terminal reopen. Decouple the "has a generic deliverable to show" signal from that stale gate.
- **BUG-011 (minor):** `pipeline_complete` runs `setContentSourceRunId(pipeline_run_id)` (`page.tsx:530`) with no run-identity guard, so a background/foreign run completing hijacks the VIEWED content-source. Run-scope it by the existing `trackedRunIdRef` (from the BUG-005 fix), preserving the launched run's own completion.

Purpose: reopened custom deliverables render; concurrent completions stop hijacking the viewed run; the primary launch->watch flow stays byte-identical.
Output: two one-site source fixes + a new PreviewPanel unit test + a new page-source lock test + TS-T-04b un-fixme'd, all green offline.

FRONTEND ONLY. No backend. Do NOT touch GenericDeliverablePreview, the P18 sandbox contract, `renderDeliverable()`, the SSE fan-out, or the j1u/lb6 fixes.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/BUG-008-011-GROUNDED-CONTEXT.md
@.planning/SSE-QA-BUG-LOG.md

# BUG-008 fix site + surrounding gate
@frontend/src/components/preview/PreviewPanel.tsx
# BUG-008 test scaffold (motion + child mocks, iframe assertions) — extend this file
@frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx
# BUG-008 e2e to un-fixme (TS-T-04b, :166)
@frontend/e2e/tests/ts-t.history.spec.ts
# BUG-011 fix site + the trackedRunIdRef it reuses (:156/:488-491/:942/:1489)
@frontend/src/app/dashboard/page.tsx
# Sanctioned source-lock test PATTERN for a page/DashboardLayout guard (mirror it)
@frontend/src/app/dashboard/reviseGuard.source.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BUG-008 — reopened generic deliverable renders (PreviewPanel gate decouple) + unit test + un-fixme TS-T-04b</name>
  <files>frontend/src/components/preview/PreviewPanel.tsx, frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx, frontend/e2e/tests/ts-t.history.spec.ts</files>
  <behavior>
    New PreviewPanel unit cases (write FIRST, observe RED against the current :577, then GREEN after the edit):
    - Mount with workflowType="user_stories", userStoryContent="" pptContent="" prototypeContent="" (all typed content empty), genericDeliverable={{content:"&lt;!DOCTYPE html&gt;&lt;h1&gt;Hi&lt;/h1&gt;", mimetype:"text/html"}}, pipelineState={{isRunning:false}}, isStreaming={false} -> the Preview tab renders an `iframe` whose `sandbox` attribute is EXACTLY "allow-scripts" (and does NOT contain "allow-same-origin"), whose `srcdoc` contains the HTML; and NO "Output will appear here" text. RED before (empty state), GREEN after.
    - Same mount but genericDeliverable mimetype "text/markdown", content "# Hi" -> the (stubbed) MarkdownPreview renders and there is NO iframe. RED before, GREEN after.
    - Regression re-affirm (already covered at the existing test :167 — keep it green): workflowType="user_stories" WITH userStoryContent="# Real stories" AND a generic deliverable also set -> the bespoke user-story renderer wins, NO iframe.
  </behavior>
  <action>
    Add the two BUG-008 cases above to `frontend/src/components/preview/PreviewPanel.genericDeliverable.test.tsx` as a new `describe("BUG-008 — reopened generic deliverable survives a STALE workflowType", ...)` block (reuse the file's existing motion mock and child stubs — MarkdownPreview is stubbed to `data-testid="markdown-preview"`, so a markdown route is asserted by that marker, an iframe by `container.querySelector("iframe")`). Run the block and OBSERVE IT FAIL against the unmodified `:577`.
    Then apply the single-site fix in `frontend/src/components/preview/PreviewPanel.tsx` at line 577 (per BUG-008-011-GROUNDED-CONTEXT.md). Replace the one line `const hasGenericDeliverable = !isKnownRenderType && !!genericDeliverable?.content;` with TWO lines: first introduce `const knownContentPresent = !!(effUserStoryContent || effPptContent || effPrototypeContent || pptxCode);` (these are the generic typed-content slots already in scope at :554-556/:481 — NOT workflow-name literals), then `const hasGenericDeliverable = !!genericDeliverable?.content && (!isKnownRenderType || !knownContentPresent);`. A present generic deliverable now counts whenever the typed renderer for the (possibly-stale) renderType has nothing to show. Do NOT touch `hasContent` (:579), `renderDeliverable()` (:868-895), the empty-state short-circuit (:1016), the Files-tab generic row (:1081), `GenericDeliverablePreview`, or the P18 sandbox attributes — they already consume `hasGenericDeliverable` unchanged. Do NOT use the `workflowType={viewedRunType}` alternative (it has a recentRuns-membership gap per the spec). Confirm no NEW workflow-name string literal is introduced (SC-001).
    Finally, un-`fixme` the tracking e2e: in `frontend/e2e/tests/ts-t.history.spec.ts` at line 166 change `test.fixme("TS-T-04b generic/custom reopen ...` to `test("TS-T-04b generic/custom reopen ...` (leave the test body and its `iframe[title="Deliverable Preview"]` / sandbox assertions unchanged — the body was authored to pass once this fix lands).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run src/components/preview/PreviewPanel.genericDeliverable.test.tsx</automated>
    <human-check>From frontend/, kill any listener on :3000, then run `npm run e2e -- ts-t.history` and confirm TS-T-04b passes (the reopened custom run renders the sandboxed Deliverable Preview iframe in the run screen).</human-check>
  </verify>
  <done>The two new BUG-008 cases were observed RED against the unmodified :577 and are GREEN after the two-line edit; the existing genericDeliverable + degraded + switcher PreviewPanel tests stay green; `tsc --noEmit` clean; TS-T-04b (un-fixme'd) passes in the mocked e2e; no new workflow-name literal in PreviewPanel.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BUG-011 — run-scope pipeline_complete's setContentSourceRunId + source-lock test</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/app/dashboard/contentSourceRunScope.source.test.ts</files>
  <behavior>
    A new source-lock test (the sanctioned pattern for a page.tsx guard impractical to exercise in a full render — mirrors reviseGuard.source.test.ts). Write it FIRST and observe it RED against the current page.tsx:
    - The old UNCONDITIONAL set `if (data.pipeline_run_id) setContentSourceRunId(data.pipeline_run_id as string);` is NO LONGER present (currently present -> RED now).
    - The pipeline_complete branch now computes a foreign-completion guard: the source contains `isForeignCompletion` and the set is gated (setContentSourceRunId is called inside a `!isForeignCompletion` guard). Absent now -> RED now.
    - Launch-flow / BUG-005 regression pins stay intact: `trackedRunIdRef.current = launchedRunId;` (launch set, :1489) and the existing pipeline_start `isForeignRun` guard token remain present.
  </behavior>
  <action>
    Create `frontend/src/app/dashboard/contentSourceRunScope.source.test.ts` mirroring `reviseGuard.source.test.ts` (readFileSync of `../../app/dashboard/page.tsx`, `toContain` / `not.toContain` assertions). Encode the three behavior bullets above. Run it and OBSERVE IT FAIL.
    Then apply the single-site fix in `frontend/src/app/dashboard/page.tsx` at line 530 (per BUG-008-011-GROUNDED-CONTEXT.md, inside the `if (msg.type === "pipeline_complete" && msg.data)` branch). Replace `if (data.pipeline_run_id) setContentSourceRunId(data.pipeline_run_id as string);` with a run-scoped guard that MIRRORS the existing `isForeignRun` shape at :488-491: read `const completingRunId = data.pipeline_run_id as string | undefined;`, compute `const isForeignCompletion = !!completingRunId && !!trackedRunIdRef.current && completingRunId !== trackedRunIdRef.current;`, then `if (completingRunId && !isForeignCompletion) setContentSourceRunId(completingRunId);`. Rationale to preserve exactly: when the completing run IS the tracked/launched run (launch->watch) OR there is no tracked id yet, it is NOT foreign, so the set runs byte-identically to today; ONLY a genuine foreign concurrent completion is skipped. Do NOT modify the `trackedRunIdRef` declaration (:156), its sync effect (:942), the launch set (:1489), or the BUG-005 pipeline_start `isForeignRun` guard (:485-517). Add a short comment tying it to BUG-011 + the launch-preservation reasoning.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run src/app/dashboard/contentSourceRunScope.source.test.ts src/app/dashboard/reviseGuard.source.test.ts src/app/dashboard/revisionFamilyLinkage.source.test.ts</automated>
  </verify>
  <done>The source-lock test was observed RED against the unmodified page.tsx and is GREEN after the guard edit; the old unconditional set is gone and `isForeignCompletion` gates the set; reviseGuard.source + revisionFamilyLinkage.source stay green; `tsc --noEmit` clean; trackedRunIdRef sync/launch-set and the BUG-005 pipeline_start guard are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Regression gate — targeted vitest + mocked e2e + SC-001 grep, real-green baseline</name>
  <files>frontend/src/components/preview/PreviewPanel.tsx, frontend/src/app/dashboard/page.tsx</files>
  <action>
    Prove no regression from Tasks 1-2. Run from `frontend/` (cwd-sensitive). NO code edits in this task unless a genuine regression is found; if one is, fix it minimally within the two touched source files only.
    1. Type-check: `npx tsc --noEmit` clean across the frontend.
    2. Targeted vitest (must be GREEN): the PreviewPanel suite (`PreviewPanel.test.tsx`, `PreviewPanel.genericDeliverable.test.tsx`, `PreviewPanel.degraded.test.tsx`, `PreviewPanel.versionChip.test.tsx`, `PreviewPanel.revisionChip.test.tsx`, `__tests__/PreviewPanel.switcher.test.tsx`, `RunHeader.test.tsx`) plus the at-risk locks named in the constraints: `WorkflowHistory.genericReopen.test.tsx`, `DashboardLayout.laneTitle.test.tsx`, `DashboardLayout.launchLatch.test.tsx`, `reviseGuard.source.test.ts`, `revisionFamilyLinkage.source.test.ts`, and the new `contentSourceRunScope.source.test.ts`.
    3. Real-green baseline (the "132/0" figure is stale): run the FULL vitest (`npm test`). Any RED must be BOTH (a) in a file this plan did NOT modify AND (b) one of the ~8 known pre-Phase-42 reds that are red on the pre-fix baseline too. Confirm by comparing against the pre-fix HEAD (do not count them as regressions; a red in any file this plan touched IS a regression and must be fixed).
    4. Mocked e2e (kill :3000 first): `npm run e2e -- ts-t.history ts-live-state ts-u.revisions ts-y.run-scope-clarify` — all GREEN, including the now-un-fixme'd TS-T-04b.
    5. SC-001 grep: confirm the PreviewPanel.tsx diff introduced NO workflow-name string literal — the fix keys only on `genericDeliverable.content`, the generic typed-content slots, and the structural `isKnownRenderType`/`KNOWN_RENDER_TYPES` check. page.tsx is SC-001-exempt but the guard likewise adds no workflow-name literal.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run src/components/preview src/components/history/WorkflowHistory.genericReopen.test.tsx src/components/layout/DashboardLayout.laneTitle.test.tsx src/components/layout/DashboardLayout.launchLatch.test.tsx src/app/dashboard/contentSourceRunScope.source.test.ts src/app/dashboard/reviseGuard.source.test.ts src/app/dashboard/revisionFamilyLinkage.source.test.ts</automated>
    <human-check>From frontend/: kill :3000, run `npm run e2e -- ts-t.history ts-live-state ts-u.revisions ts-y.run-scope-clarify` (all green, TS-T-04b passing) and run the full `npm test`, confirming every remaining red is a known pre-Phase-42 red in a file this plan did NOT touch.</human-check>
  </verify>
  <done>tsc clean; the targeted PreviewPanel + at-risk vitest suites and all three source-lock tests are GREEN; the four mocked e2e specs pass with TS-T-04b green; any full-suite red is a known pre-existing red in an untouched file (not a regression); SC-001 grep confirms no new workflow-name literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run deliverable HTML -> rendered preview | Untrusted, model/agent-authored HTML is rendered in the browser. The ONLY safe render path is the P18 sandboxed iframe (`sandbox="allow-scripts"`, NO `allow-same-origin`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n2d-01 | Elevation of Privilege | PreviewPanel generic deliverable render | mitigate | The BUG-008 fix only flips the `hasGenericDeliverable` GATE; it routes through the unchanged `renderDeliverable()` -> `GenericDeliverablePreview`, which keeps `sandbox="allow-scripts"` and NO `allow-same-origin`. Task 1 asserts the sandbox attributes exactly; the fix must not loosen them. |
| T-n2d-02 | Tampering | run-screen viewed content-source | mitigate | BUG-011 adds a run-identity guard so a foreign run's completion cannot re-point the viewed contentSourceRunId; keyed on the generic `pipeline_run_id` vs `trackedRunIdRef`, no privilege change. |
| T-n2d-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; frontend-only edits to existing files. No package-legitimacy gate required. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (from frontend/).
- Task 1 + Task 2 tests observed RED before their respective source edits, GREEN after.
- Targeted PreviewPanel suite + the at-risk locks (WorkflowHistory.genericReopen, DashboardLayout.laneTitle/launchLatch, reviseGuard.source, revisionFamilyLinkage.source) + the new contentSourceRunScope.source test all GREEN.
- Mocked e2e ts-t.history / ts-live-state / ts-u.revisions / ts-y.run-scope-clarify GREEN, TS-T-04b passing.
- SC-001: no new workflow-name literal in PreviewPanel.tsx.
- Live proof (orchestrator, NOT the executor): reopen a real custom HTML run -> iframe renders; two runs, a background completion does not hijack the viewed content-source.
</verification>

<success_criteria>
- BUG-008: reopening a completed custom/generic HTML run on the shared run screen renders the sandboxed iframe (not "Output will appear here"); typed deliverables still render bespoke.
- BUG-011: a foreign run's pipeline_complete leaves the viewed contentSourceRunId unchanged; the launched/tracked run's own completion still sets it (launch->watch byte-identical).
- All offline gates green; live verification deferred to the orchestrator.
</success_criteria>

<output>
Create `.planning/quick/260716-n2d-fix-bug-008-reopened-generic-deliverable/260716-n2d-SUMMARY.md` when done.
</output>
