---
phase: quick-260716-o6z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/tests/ts-t.history.spec.ts
  - frontend/src/app/dashboard/contentSourceRunType.source.test.ts
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
autonomous: true
requirements: [BUG-012]
must_haves:
  truths:
    - "Reopening a completed od_ppt run whose output is valid deck HTML renders the deck (the ppt 'Slide Deck Preview' iframe) on the shared run screen, NOT the neutral 'Output will appear here' blank state (BUG-012)"
    - "Reopening a completed od_prototype run renders the prototype (not blank) — same reopen misroute, fixed by the same thread"
    - "The Preview render dispatch keys on the VIEWED run's REAL type (fullRun.type threaded page.tsx -> contentSourceRunType -> DashboardLayout viewedRunType -> effectiveReviseType -> PreviewPanel), independent of the stale workflowType default AND independent of recents-window membership"
    - "During live launch->watch (isPipelineRunning true -> viewedRunType undefined -> effectiveReviseType === workflowType) the PreviewPanel dispatch is byte-identical to today (no regression)"
    - "PreviewPanel.tsx, FIRST_PARTY_RENDERERS, GenericDeliverablePreview and the P18 sandbox are untouched; reopened user_stories still renders (now via the real type, not the coincidental stale default) and reopened app_builder is unchanged (generic path, BUG-008)"
  artifacts:
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Durable viewed-run-type: new contentSourceRunType state set on reopen, cleared on fresh launch, passed to DashboardLayout"
      contains: "setContentSourceRunType"
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "contentSourceRunType prop folded into viewedRunType; effectiveReviseType fed to the PreviewPanel render dispatch"
      contains: "workflowType={effectiveReviseType}"
    - path: "frontend/e2e/tests/ts-t.history.spec.ts"
      provides: "TS-T-04c — od_ppt (and od_prototype) reopen renders its typed deliverable on the shared run screen, fail-before-blank / pass-after"
      contains: "TS-T-04c"
    - path: "frontend/src/app/dashboard/contentSourceRunType.source.test.ts"
      provides: "BUG-012 source-lock: setContentSourceRunType on reopen + cleared on fresh-launch reset + workflowType={effectiveReviseType} at the PreviewPanel site"
      contains: "setContentSourceRunType"
  key_links:
    - from: "page.tsx contentSourceRunType (set at reopen :1244, cleared at :1484)"
      to: "DashboardLayout viewedRunType (:1265-1268)"
      via: "the contentSourceRunType prop passed at :1507"
      pattern: "contentSourceRunType"
    - from: "DashboardLayout effectiveReviseType (:1269)"
      to: "PreviewPanel workflowType prop (:1825)"
      via: "the render-dispatch feed change workflowType={workflowType} -> workflowType={effectiveReviseType}"
      pattern: "workflowType=\\{effectiveReviseType\\}"
    - from: "PreviewPanel workflowType prop"
      to: "detectedType/renderType (:536-545) -> FIRST_PARTY_RENDERERS['ppt'] (:805)"
      via: "the correct viewed type now flows so the ppt/prototype renderer fires instead of returning null (READ-ONLY reference — PreviewPanel is NOT edited)"
      pattern: "FIRST_PARTY_RENDERERS"
---

<objective>
Fix BUG-012 (major): reopening a completed **od_ppt** (and **od_prototype**) run renders the shared run screen's Preview **BLANK** despite the deck/prototype HTML being correctly seeded. Root-caused to file:line in `.planning/BUG-012-GROUNDED-CONTEXT.md` — the 4th and last stale-`workflowType` consumer (siblings BUG-006 label + BUG-008 generic gate already fixed). On a terminal reopen the PreviewPanel render dispatch keys on a stale `workflowType` default ("user_stories"), so `detectedType`/`renderType` never consult the seeded typed slot and the typed renderer holding the content never fires -> `return null` -> blank.

The user chose the **DURABLE** fix (NOT the targeted BUG-006-style recents-window one-liner): thread the viewed run's REAL type (`fullRun.type`) end-to-end so `viewedRunType` is defined even for runs outside the recents window. Implement the spec faithfully:
- **page.tsx** — new `contentSourceRunType` state, SET = the viewed run's type on reopen (:1244), CLEARED = null on the fresh (non-revision) launch reset (:1484), PASSED to DashboardLayout (:1507).
- **DashboardLayout.tsx** — receive the prop, prefer it in `viewedRunType` (`contentSourceRunType ?? recents.find(...)`, :1265-1268), and change the PreviewPanel prop at :1825 from `workflowType={workflowType}` to `workflowType={effectiveReviseType}`.

Purpose: reopened typed deliverables render on the shared run screen; the primary launch->watch flow stays byte-identical.
Output: the two-file durable thread + a mocked-Playwright reopen proof (TS-T-04c) + a source-lock test, all green offline.

FRONTEND ONLY. Do NOT edit PreviewPanel.tsx (its dispatch is correct once fed the right type), FIRST_PARTY_RENDERERS, GenericDeliverablePreview, the P18 sandbox, or the SSE/reducer path; do NOT touch the j1u/lb6/n2d fixes.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/BUG-012-GROUNDED-CONTEXT.md
@.planning/SSE-QA-BUG-LOG.md

# The durable-thread fix sites (declare :165 / set-on-reopen :1244 / clear-on-fresh-launch :1484 / pass-to-layout :1507)
@frontend/src/app/dashboard/page.tsx
# The fold site (:75/:210 prop, :1265-1269 viewedRunType/effectiveReviseType, :1745 BUG-006 label precedent, :1825 PreviewPanel prop)
@frontend/src/components/layout/DashboardLayout.tsx
# READ-ONLY reference — the render dispatch that is already correct once fed the right type (:536 detectedType, :805 ppt renderer, :893/:903 return null). Do NOT edit this file.
@frontend/src/components/preview/PreviewPanel.tsx
# The reopen e2e to extend (TS-T-04b reopen pattern + openHistory + makeRun usage)
@frontend/e2e/tests/ts-t.history.spec.ts
# The sanctioned source-lock PATTERN to mirror (readFileSync + toContain grep assertions)
@frontend/src/app/dashboard/contentSourceRunScope.source.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — author the reopen proof (TS-T-04c) + the source-lock test, observed failing against the unmodified tree</name>
  <files>frontend/e2e/tests/ts-t.history.spec.ts, frontend/src/app/dashboard/contentSourceRunType.source.test.ts</files>
  <behavior>
    Two failing tests, written FIRST and OBSERVED RED against the current (unmodified) tree:
    - Mocked-Playwright reopen (TS-T-04c, extend ts-t.history.spec.ts, mirror the TS-T-04b/TS-T-03 shape): `mockApi.setRuns([makeRun({ id, title, type:"od_ppt", status:"completed", output:"<!DOCTYPE html><html><body><h1>DECK-SLIDE-1</h1></body></html>" })])` (add `deliverable_mimetype:"text/html"` if the reopen mimetype dispatch needs it for the HTML iframe route), `dashboard.goto()`, `openHistory(page)`, tap the row, assert `execution-chat-lane` is visible, click `dashboard.previewTab()`, then assert the DECK renders — `iframe[title="Slide Deck Preview"]` is visible AND the neutral `"Output will appear here"` blank text has count 0. RED now = the iframe is absent / the blank text is present (the BUG-012 blank). If cheap, add a sibling assertion for a `type:"od_prototype"` reopen (its typed prototype renderer also fires an iframe once fed the right type) — mirror, do not block on it.
    - Source-lock (contentSourceRunType.source.test.ts, mirror contentSourceRunScope.source.test.ts): readFileSync BOTH page.tsx and DashboardLayout.tsx and assert — page.tsx contains `setContentSourceRunType(` on the reopen path AND `setContentSourceRunType(null)` on the fresh-launch reset; DashboardLayout.tsx contains `workflowType={effectiveReviseType}` (the fixed PreviewPanel dispatch site) and NO longer contains the exact old dispatch literal `workflowType={workflowType}` at that PreviewPanel. All RED now (tokens absent).
  </behavior>
  <action>
    Author both tests only — do NOT edit page.tsx / DashboardLayout.tsx in this task. Reuse the existing e2e helpers (`openHistory`, `makeRun`, `dashboard.previewTab()`) exactly as TS-T-04b/TS-T-03 do; keep the new test named so it greps as `TS-T-04c`. For the source-lock, mirror the readFileSync + `toContain`/`not.toContain` structure of `contentSourceRunScope.source.test.ts`, resolving `../../app/dashboard/page.tsx` and `../../components/layout/DashboardLayout.tsx`. Un-fixme NOTHING new (no matching fixme exists for these). Then RUN both and OBSERVE THEM FAIL: the source-lock reds because the tokens are absent; the Playwright TS-T-04c reds because the reopened od_ppt deck renders blank (this is the fail-before evidence the fix must flip). From `frontend/`, kill any listener on :3000 before the mocked Playwright run.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/app/dashboard/contentSourceRunType.source.test.ts 2>&1 | tail -20</automated>
    <human-check>From frontend/: kill :3000, run `npm run e2e -- ts-t.history` and CONFIRM the new TS-T-04c FAILS on the unmodified tree — the reopened od_ppt run shows the blank "Output will appear here" state and the "Slide Deck Preview" iframe is absent (this RED is the required fail-before evidence).</human-check>
  </verify>
  <done>TS-T-04c and contentSourceRunType.source.test.ts both exist and were OBSERVED RED against the unmodified tree (source-lock tokens absent; TS-T-04c reopen renders blank). No production source edited in this task; no unrelated fixme un-fixme'd.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — thread the viewed run's real type end-to-end (page.tsx + DashboardLayout.tsx)</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/components/layout/DashboardLayout.tsx</files>
  <action>
    Apply the durable-thread fix EXACTLY as `.planning/BUG-012-GROUNDED-CONTEXT.md` specifies. NOTE (correction to the spec text): `WorkflowType` is NOT currently imported in page.tsx — add `WorkflowType` to the existing `import type { ... WorkflowRun, ... } from "@/types/index"` at page.tsx:20. `WorkflowRun.type` is already `WorkflowType`, so no cast is needed on the value.

    In `frontend/src/app/dashboard/page.tsx`:
    1. DECLARE — immediately after the `contentSourceRunId` state at :165: `const [contentSourceRunType, setContentSourceRunType] = useState<WorkflowType | null>(null);` (add a one-line comment tying it to BUG-012 durable viewed-run-type).
    2. SET ON REOPEN — immediately after `setContentSourceRunId(fullRun.id);` at :1244: `setContentSourceRunType(fullRun.type ?? null);`.
    3. CLEAR ON FRESH LAUNCH — immediately after `setContentSourceRunId(null);` at :1484 (inside the fresh-run reset block that clears pptContent/prototypeContent/genericDeliverable): `setContentSourceRunType(null);`.
    4. PASS TO LAYOUT — beside `contentSourceRunId={contentSourceRunId}` at :1507: `contentSourceRunType={contentSourceRunType}`.

    In `frontend/src/components/layout/DashboardLayout.tsx`:
    5. ADD THE PROP — declare `contentSourceRunType?: WorkflowType | null;` in the props type near `contentSourceRunId?: string | null;` (:75) and destructure `contentSourceRunType,` near :210 (beside `contentSourceRunId,`). `WorkflowType` is already imported in this file (used at :75/recentRuns typing).
    6. FOLD INTO viewedRunType (:1265-1268) — change the recents-only source to prefer the durable threaded type: `!isPipelineRunning && contentSourceRunId != null ? (contentSourceRunType ?? recentRuns?.find((r) => r.id === contentSourceRunId)?.type) : undefined`. Leave `effectiveReviseType = viewedRunType ?? workflowType` (:1269) unchanged in FORM (now durable). For in-recents runs contentSourceRunType equals the recents type (identical value); for out-of-recents runs it is now defined where recents returned undefined.
    7. FEED THE PREVIEW DISPATCH — at :1825 change `workflowType={workflowType}` to `workflowType={effectiveReviseType}`. Do NOT change the `rawPipelineType={pipelineState?.pipeline_type || workflowType}` line at :1826 (orthogonal — download hint). During live/launch `viewedRunType` is undefined (gated on `!isPipelineRunning`) -> `effectiveReviseType === workflowType` -> byte-identical.

    STRICT fences: do NOT edit PreviewPanel.tsx, FIRST_PARTY_RENDERERS, GenericDeliverablePreview, the P18 sandbox, or the SSE/reducer path; do NOT touch the trackedRunIdRef / isForeignCompletion (BUG-011) or the j1u/lb6/n2d logic. SC-001: DashboardLayout keys on `run.type` / the existing `effectiveReviseType` var — add NO new workflow-name string literal to DashboardLayout or PreviewPanel (page.tsx is SC-001-exempt). Then run Task 1's tests and observe them GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run src/app/dashboard/contentSourceRunType.source.test.ts</automated>
    <human-check>From frontend/: kill :3000, run `npm run e2e -- ts-t.history` and CONFIRM TS-T-04c now PASSES — the reopened od_ppt run renders the "Slide Deck Preview" iframe (deck visible), not the blank state; the od_prototype sibling (if added) also renders.</human-check>
  </verify>
  <done>`tsc --noEmit` clean; the source-lock test that was RED in Task 1 is GREEN (setContentSourceRunType present on reopen + fresh-launch clear, workflowType={effectiveReviseType} at the PreviewPanel site); TS-T-04c that was RED (blank) is GREEN (deck renders); PreviewPanel.tsx untouched; no new workflow-name literal in DashboardLayout/PreviewPanel.</done>
</task>

<task type="auto">
  <name>Task 3: Regression gate — targeted vitest + mocked e2e + SC-001 + launch->watch byte-identity, real-green baseline</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/components/layout/DashboardLayout.tsx</files>
  <action>
    Prove no regression from Tasks 1-2. Run from `frontend/` (cwd-sensitive). NO code edits unless a genuine regression is found; if one is, fix it minimally within the two touched source files only.
    1. Type-check: `npx tsc --noEmit` clean across the frontend.
    2. At-risk vitest (must be GREEN — named in the constraints): `ts-u.revisions`-adjacent unit locks, `revisionFamilyLinkage.source.test.ts`, `WorkflowHistory.genericReopen.test.tsx`, `DashboardLayout.laneTitle.test.tsx`, `DashboardLayout.launchLatch.test.tsx`, `PreviewPanel.genericDeliverable.test.tsx`, `contentSourceRunScope.source.test.ts`, and the new `contentSourceRunType.source.test.ts`. The BUG-006 label precedent must stay correct (effectiveReviseType now durable — confirm laneTitle/label locks green).
    3. Real-green baseline (the "132/0" figure is stale): run the FULL vitest (`npm test`). Any RED must be BOTH (a) in a file this plan did NOT modify AND (b) one of the 8 known pre-Phase-42 reds (HomeLaunchGrid.inspect, PreviewPanel.switcher, PreviewPanel.degraded, FilesTab.runInput). A red in any file this plan touched IS a regression and must be fixed; compare against the pre-fix HEAD if unsure.
    4. Mocked e2e (kill :3000 first): `npm run e2e -- ts-t.history ts-u.revisions` — all GREEN, including TS-T-04c and the at-risk revision specs. Confirm the launch->watch path is unaffected (a live/launched run still renders its own deliverable — viewedRunType undefined during live -> effectiveReviseType === workflowType, byte-identical).
    5. SC-001 grep: confirm the DashboardLayout diff introduced NO workflow-name string literal (the change is `workflowType={effectiveReviseType}` + the `contentSourceRunType ?? recents.find(...)` fold — both key on existing type vars). PreviewPanel.tsx is unchanged. page.tsx is SC-001-exempt.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run src/app/dashboard/contentSourceRunType.source.test.ts src/app/dashboard/contentSourceRunScope.source.test.ts src/app/dashboard/revisionFamilyLinkage.source.test.ts src/components/history/WorkflowHistory.genericReopen.test.tsx src/components/layout/DashboardLayout.laneTitle.test.tsx src/components/layout/DashboardLayout.launchLatch.test.tsx src/components/preview/PreviewPanel.genericDeliverable.test.tsx</automated>
    <human-check>From frontend/: kill :3000, run `npm run e2e -- ts-t.history ts-u.revisions` (all green, TS-T-04c passing) and the full `npm test`, confirming every remaining red is one of the 8 known pre-Phase-42 reds in a file this plan did NOT touch (not a regression).</human-check>
  </verify>
  <done>tsc clean; the at-risk vitest suites + both source-lock tests GREEN; the mocked ts-t.history and ts-u.revisions e2e pass with TS-T-04c green; launch->watch confirmed byte-identical; any full-suite red is a known pre-existing red in an untouched file; SC-001 grep confirms no new workflow-name literal in DashboardLayout/PreviewPanel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run deliverable HTML -> rendered preview | Untrusted, model/agent-authored deck/prototype HTML is rendered in the browser via the existing PPTPreview / prototype iframe. This fix changes only WHICH renderer is dispatched, not the iframe/sandbox contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o6z-01 | Elevation of Privilege | PreviewPanel typed-deliverable render dispatch | mitigate | The fix only feeds the correct viewed TYPE into the unchanged PreviewPanel dispatch (`workflowType={effectiveReviseType}`); PreviewPanel.tsx, FIRST_PARTY_RENDERERS and the iframe sandbox attributes are NOT edited. Task 1/2 assert the deck iframe renders via the existing renderer — no sandbox attribute is loosened. |
| T-o6z-02 | Tampering | viewed-run-type binding | mitigate | The threaded `contentSourceRunType` is sourced from `fullRun.type` (server-persisted run type) captured at reopen and cleared on fresh launch; it feeds a render selector only, no privilege/data mutation, and is gated on `!isPipelineRunning` so it never perturbs the live launch->watch flow. |
| T-o6z-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; frontend-only edits to existing files + one new test file. No package-legitimacy gate required. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (from frontend/).
- Task 1's TS-T-04c (mocked Playwright reopen) + contentSourceRunType.source.test.ts observed RED against the unmodified tree; both GREEN after the Task 2 thread.
- At-risk vitest locks (revisionFamilyLinkage.source, WorkflowHistory.genericReopen, DashboardLayout.laneTitle/launchLatch, PreviewPanel.genericDeliverable, contentSourceRunScope.source) + the new source-lock GREEN.
- Mocked e2e ts-t.history (TS-T-04c passing) + ts-u.revisions GREEN; launch->watch byte-identical.
- SC-001: no new workflow-name literal in DashboardLayout/PreviewPanel.
- Live proof (orchestrator, NOT the executor): reopen a real od_ppt run -> deck renders; reopen a real od_prototype run -> prototype renders.
</verification>

<success_criteria>
- BUG-012: reopening a completed od_ppt / od_prototype run on the shared run screen renders its typed deliverable (deck / prototype), NOT the blank "Output will appear here" state, for runs both inside AND outside the recents window.
- The render dispatch sources the viewed run's real type durably (fullRun.type -> contentSourceRunType -> viewedRunType -> effectiveReviseType -> PreviewPanel), decoupled from the stale workflowType default.
- The primary launch->watch flow is byte-identical (viewedRunType undefined during live -> effectiveReviseType === workflowType).
- PreviewPanel.tsx / FIRST_PARTY_RENDERERS / P18 sandbox / SSE path untouched; all offline gates green; live verification deferred to the orchestrator.
</success_criteria>

<output>
Create `.planning/quick/260716-o6z-fix-bug-012-durable-reopened-od-ppt-od-p/260716-o6z-SUMMARY.md` when done.
</output>
