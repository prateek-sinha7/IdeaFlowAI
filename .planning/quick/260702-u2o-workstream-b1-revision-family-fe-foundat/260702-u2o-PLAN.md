---
phase: 260702-u2o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/types/index.ts
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/history/WorkflowHistory.tsx
  - frontend/src/components/history/WorkflowHistory.test.tsx
  - frontend/src/components/history/WorkflowHistory.genericReopen.test.tsx
  - frontend/src/lib/api.getRunFamily.test.ts
  - frontend/src/components/history/WorkflowHistory.revise.test.tsx
  - frontend/src/app/dashboard/revisionFamilyLinkage.source.test.ts
autonomous: true
requirements: [B1-1, B1-2, B1-7, D1, D2, D7]

must_haves:
  truths:
    - "getRunFamily(token, id) issues GET /api/runs/{id}/family with a Bearer token and returns the parsed {root_id, members} shape"
    - "A normalized WorkflowRun carries parentRunId and rootRunId mapped from the API's parent_run_id/root_run_id (parentRunId defaults null, rootRunId defaults to the run's own id)"
    - "Every revision launch path (4 inline revise handlers + 4 history onRevise* callbacks) sends the parent run id sourced from contentSourceRunId / selectedRun.id — never the old heuristic; today's history revisions send nothing, which this fixes"
    - "The fragile currentWorkflowRunId heuristic (with its prototype_revision_revision never-match) no longer exists in DashboardLayout"
    - "contentSourceRunId is set from pipeline_complete.data.pipeline_run_id (live) and fullRun.id (reopen), and cleared when a fresh (non-revision) run starts"
    - "npx tsc --noEmit is clean and the new/touched vitest specs are green; zero backend files are touched (INV-3)"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "getRunFamily fetcher + RawWorkflowRun parent_run_id/root_run_id + normalizeWorkflowRun mapping"
      contains: "getRunFamily"
    - path: "frontend/src/types/index.ts"
      provides: "RunFamily + FamilyMember types + WorkflowRun.parentRunId/rootRunId"
      contains: "RunFamily"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "contentSourceRunId state + setters (pipeline_complete, reopen, fresh-run reset) + prop pass-down"
      contains: "contentSourceRunId"
    - path: "frontend/src/components/layout/DashboardLayout.tsx"
      provides: "contentSourceRunId prop; heuristic deleted; 4 inline handlers + 4 history wirings send parent linkage"
      contains: "contentSourceRunId"
    - path: "frontend/src/components/history/WorkflowHistory.tsx"
      provides: "onRevise* callback signatures gain sourceRunId; detail-view call sites pass selectedRun.id"
      contains: "sourceRunId"
  key_links:
    - from: "frontend/src/components/layout/DashboardLayout.tsx"
      to: "onStartPipeline 6th context arg"
      via: "source_workflow_run_id = contentSourceRunId prop"
      pattern: "source_workflow_run_id: contentSourceRunId"
    - from: "frontend/src/components/history/WorkflowHistory.tsx"
      to: "DashboardLayout history wiring -> onStartPipeline"
      via: "onRevise*(instruction, content, selectedRun.id) -> source_workflow_run_id: sourceRunId"
      pattern: "source_workflow_run_id: sourceRunId"
    - from: "frontend/src/app/dashboard/page.tsx"
      to: "DashboardLayout contentSourceRunId prop"
      via: "setContentSourceRunId(data.pipeline_run_id) / setContentSourceRunId(fullRun.id)"
      pattern: "setContentSourceRunId"
    - from: "frontend/src/lib/api.ts"
      to: "GET /api/runs/{id}/family"
      via: "request<RunFamily>"
      pattern: "runs/\\$\\{.*\\}/family"
---

<objective>
Deliver Workstream-B1 of the Revision Families plan-of-record (`.planning/REVISION-FAMILY-AND-RUN-INPUTS-PLAN.md` §5 items 1/2/7, decisions D1/D2/D7): the FRONTEND plumbing layer that consumes the already-shipped Workstream-A backend and makes every revision launch path populate parent linkage.

Three tightly-coupled deliverables, FRONTEND ONLY:
1. API + types — `getRunFamily` fetcher, `RunFamily`/`FamilyMember` types, `WorkflowRun` gains `parentRunId`/`rootRunId` mapped in `normalizeWorkflowRun`.
2. `contentSourceRunId` — the reliable "run id that produced the on-screen content", replacing the fragile `currentWorkflowRunId` heuristic at every consumer.
3. Universal linkage — every revision launch path (4 inline handlers + 4 history callbacks) sends the parent run id so the backend links `parent_run_id`. History revisions send nothing today; this is the core fix.

Purpose: B2/B3 (grouping, timeline, chips) and Workstream C build on this. Without reliable `contentSourceRunId` + universal linkage, revision runs stay orphaned and families cannot be assembled.

Output: 5 source files edited, 2 existing test fixtures extended, 3 new vitest specs. Zero backend edits.

SCOPE FENCE: NO visual rendering, NO new components, NO grouping/timeline/chips (B2/B3). Fetcher + types + state + payloads only. NO backend edits (Workstream A shipped the API — verified: `WorkflowRunResponse.parent_run_id`/`root_run_id` at `backend/app/api/runs.py:115-116`, `GET /{id}/family` at `:933`).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REVISION-FAMILY-AND-RUN-INPUTS-PLAN.md
@CLAUDE.md

# Backend contract already shipped (Workstream A) — FE consumes, does NOT edit:
#   backend/app/api/runs.py:115-116  WorkflowRunResponse.parent_run_id / root_run_id
#   backend/app/api/runs.py:911-930  FamilyMemberResponse / RunFamilyResponse
#   backend/app/api/runs.py:933      GET /api/runs/{id}/family
#   backend/agents/execution_engine/engine.py:2265-2279  pipeline_complete data carries pipeline_run_id

# FE files touched (analogs called out inline in tasks):
@frontend/src/lib/api.ts
@frontend/src/types/index.ts
@frontend/src/app/dashboard/page.tsx
@frontend/src/components/layout/DashboardLayout.tsx
@frontend/src/components/history/WorkflowHistory.tsx
@frontend/src/hooks/useWorkflow.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: API fetcher + types + WorkflowRun extension</name>
  <files>frontend/src/lib/api.ts, frontend/src/types/index.ts, frontend/src/components/history/WorkflowHistory.test.tsx, frontend/src/components/history/WorkflowHistory.genericReopen.test.tsx</files>
  <action>
In `types/index.ts` (near the WorkflowRun interface at :273-305): (a) add two exported interfaces mirroring the backend shape verbatim — `FamilyMember` = { id: string; type: string; title: string; status: string; revision_index: number; parent_run_id: string | null; created_at: string; completed_at: string | null } and `RunFamily` = { root_id: string; members: FamilyMember[] } (these carry the API's snake_case field names on purpose — they are the raw wire shape, like the existing ChainContext interface, NOT camelCased). (b) Extend the WorkflowRun interface with two REQUIRED fields: `parentRunId: string | null` and `rootRunId: string`. Per D1/D2/D7 — child-run family model unified at the read layer, root_run_id computed server-side.

In `lib/api.ts`: (a) import `RunFamily` from `@/types/index` alongside the existing WorkflowRun import. (b) Add `parent_run_id?: string | null` and `root_run_id?: string` to the `RawWorkflowRun` interface (:251-269) — optional so legacy raw rows still parse. (c) In `normalizeWorkflowRun` (:272-305) map them: `parentRunId: raw.parent_run_id ?? null` and `rootRunId: raw.root_run_id ?? raw.id` (a standalone/legacy run with no root is its own root — matches the backend's "standalone run → own id" semantics at runs.py). (d) Add an exported `getRunFamily(token: string, runId: string): Promise<RunFamily>` immediately after `getWorkflow` (:325-334), copying the exact idiom of getWorkflow/getChainContext — `return request<RunFamily>(\`/api/runs/\${runId}/family\`, { method: "GET", headers: authHeaders(token) });`. No normalizer — return the raw shape like getChainContext does.

Keep tsc green: adding the two REQUIRED WorkflowRun fields breaks the two existing `makeRun` fixtures that construct WorkflowRun literals. Add `parentRunId: null` and `rootRunId: "run-1"` to the `makeRun` return object in BOTH `WorkflowHistory.test.tsx` (:73-87) and `WorkflowHistory.genericReopen.test.tsx` (:65-79). These are the ONLY WorkflowRun literal construction sites besides normalizeWorkflowRun (confirmed by grep of `agentCount:`).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>tsc --noEmit is clean; getRunFamily is exported returning Promise&lt;RunFamily&gt;; RunFamily/FamilyMember exist in types/index.ts; WorkflowRun has parentRunId/rootRunId; normalizeWorkflowRun maps them with the null/own-id fallbacks; both makeRun fixtures updated.</done>
</task>

<task type="auto">
  <name>Task 2: contentSourceRunId lift + universal revision linkage</name>
  <files>frontend/src/app/dashboard/page.tsx, frontend/src/components/layout/DashboardLayout.tsx, frontend/src/components/history/WorkflowHistory.tsx</files>
  <action>
Implements POR §5 items 1 and 2 (D1/D7). `contentSourceRunId` lives in page.tsx (where pipeline_complete + reopen land) and is threaded down as a prop.

page.tsx: (a) add state near the recentRuns state (:116): `const [contentSourceRunId, setContentSourceRunId] = useState<string | null>(null);`. (b) In the pipeline_complete handler (inside `if (msg.type === "pipeline_complete" && msg.data)` at :424, where `const data = msg.data as Record<string, unknown>` already exists) add: `if (data.pipeline_run_id) setContentSourceRunId(data.pipeline_run_id as string);` — the live completion source (engine emits pipeline_run_id in the pipeline_complete data, verified engine.py:2267). (c) In `handleSelectWorkflowRun` (:1110-1200), right after `const fullRun = await getWorkflow(currentToken, run.id);` (:1128) add `setContentSourceRunId(fullRun.id);` — the history-reopen source. (d) In the onStartPipeline wrapper (:1284-1303), inside the existing `if (!isRevision)` fresh-run content-clear block, add `setContentSourceRunId(null);` — a fresh run has no source until it completes (prevents a stale-source revise). (e) Pass the prop to DashboardLayout in the JSX (:1259+): `contentSourceRunId={contentSourceRunId}`.

DashboardLayout.tsx: (a) add `contentSourceRunId?: string | null;` to DashboardLayoutProps (near :60 recentRuns) and destructure it in the component signature (near :240). (b) DELETE the fragile heuristic entirely (:447-449, the `recentRuns?.find(r => (r.type === workflowType || r.type === workflowType + "_revision") && r.status === "completed")?.id || ""` — the one with the prototype_revision_revision never-match bug). Every reader of `currentWorkflowRunId` now reads the `contentSourceRunId` prop instead. (c) handleRevisePpt (:454-491): swap `currentWorkflowRunId` → `contentSourceRunId` at the run_revision gate (`if (contentSourceRunId && websocketSend)`) and the payload field (`parent_run_id: contentSourceRunId`); update the useCallback deps array (:491) accordingly. (d) handleReviseUserStory (:494-502): add `contentSourceRunId ? { source_workflow_run_id: contentSourceRunId } : undefined` as the 6th arg to onStartPipeline; add contentSourceRunId to deps. (e) handleRevisePrototype (:509-522): replace `currentWorkflowRunId` → `contentSourceRunId` in the existing 6th-arg expression and deps. (f) handleReviseAppBuilder (:525-533): add the same `contentSourceRunId ? { source_workflow_run_id: contentSourceRunId } : undefined` 6th arg + deps. (g) History-revise wiring (:1251-1286): each of the four `onRevise*` inline callbacks gains a 3rd param `sourceRunId` and threads `sourceRunId ? { source_workflow_run_id: sourceRunId } : undefined` as the 6th arg to its onStartPipeline call — this is the core fix (history revisions send NOTHING today → orphan runs). The backend owner-checks the parent (Workstream A §3), so a foreign id is simply not linked.

WorkflowHistory.tsx: (a) the four onRevise* prop signatures (:39-42) each gain a required 3rd param: `onReviseUserStory?: (instruction: string, content: string, sourceRunId: string) => void;` and likewise for Ppt/Prototype/AppBuilder (keep the `?` optional prop, add the param to the function type). (b) The detail-view reviseCallback builders (:474-477) pass `selectedRun.id` as the 3rd arg to each onRevise* call (e.g. `onRevisePrototype(instruction, selectedOutput, selectedRun.id)`). The unrelated `onRevise={undefined}` props on the preview components (:651,656,696,702) and DashboardLayout's `onRevisePpt={undefined}` (:1477-1480) are a DIFFERENT prop and stay untouched.

Style: match surrounding brownfield idiom (the existing prototype handler at :520 is the exact template for the `X ? { source_workflow_run_id: X } : undefined` 6th-arg pattern).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>tsc clean; the currentWorkflowRunId heuristic is deleted; all 4 inline handlers source linkage from the contentSourceRunId prop; all 4 history onRevise* callbacks thread selectedRun.id; page.tsx sets contentSourceRunId on pipeline_complete + reopen, clears on fresh run, and passes the prop down.</done>
</task>

<task type="auto">
  <name>Task 3: vitest coverage — fetcher, mapping, linkage, contentSourceRunId</name>
  <files>frontend/src/lib/api.getRunFamily.test.ts, frontend/src/components/history/WorkflowHistory.revise.test.tsx, frontend/src/app/dashboard/revisionFamilyLinkage.source.test.ts</files>
  <action>
Follow existing vitest idioms (WorkflowHistory.test.tsx for component render/mock; workflowChaining.test.ts for pure/lib tests).

`api.getRunFamily.test.ts` (real behavior via a global.fetch mock — api.ts uses the global fetch): (1) getRunFamily — stub `global.fetch` to resolve `{ ok: true, json: async () => ({ root_id: "root-9", members: [{ id:"root-9", type:"prototype", title:"t", status:"completed", revision_index:1, parent_run_id:null, created_at:"2026-07-01T00:00:00Z", completed_at:null }] }) }`; call `getRunFamily("tok", "run-1")`; assert fetch was called once with a url arg containing `/api/runs/run-1/family`, method "GET", and headers.Authorization === "Bearer tok"; assert the returned object deep-equals the parsed body (root_id + members[0].revision_index). (2) normalizeWorkflowRun mapping — call `getWorkflow("tok","run-1")` with fetch resolving a raw run that includes `parent_run_id: "p-1"` and `root_run_id: "r-1"`; assert result.parentRunId === "p-1" and result.rootRunId === "r-1". (3) fallback — same but with parent_run_id/root_run_id ABSENT; assert result.parentRunId === null and result.rootRunId === result.id. Reset the fetch mock per test.

`WorkflowHistory.revise.test.tsx` (real behavior — the emit side of the history linkage fix): reuse the WorkflowHistory.test.tsx mock scaffold (mock `@/lib/api` getToken/getWorkflows/getWorkflow/deleteWorkflow, the preview components, and motion/react). Drive getWorkflows to return one completed prototype run (id "hist-7", output non-empty) and getWorkflow to return the same full run. Render `<WorkflowHistory onBack={vi.fn()} onRevisePrototype={spy} />`, click the run row to open the detail, open the revise affordance ("Revise Prototype"), type an instruction into the textarea, submit, and assert the `onRevisePrototype` spy was called with `(instruction, output, "hist-7")` — proving selectedRun.id is forwarded as the 3rd arg. (If the submit control is keyed to Enter/a button, follow the detail-view markup at WorkflowHistory.tsx:493-520.)

`revisionFamilyLinkage.source.test.ts` (source-lock assertions — the sanctioned "grep-style source assertion" for the untestable-in-unit internals of the huge DashboardLayout/page.tsx; read the files with fs.readFileSync + path.resolve relative to the test dir): For DashboardLayout.tsx — assert the source does NOT contain the string `workflowType + "_revision"` and does NOT contain `const currentWorkflowRunId` (heuristic gone, proving the never-match path is dead); assert it DOES contain `parent_run_id: contentSourceRunId` and `source_workflow_run_id: contentSourceRunId` and `source_workflow_run_id: sourceRunId`. For page.tsx — assert it contains `setContentSourceRunId(data.pipeline_run_id`, `setContentSourceRunId(fullRun.id)`, `setContentSourceRunId(null)`, and `contentSourceRunId={contentSourceRunId}`. These lock the exact wiring the two implementation tasks produced.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/lib/api.getRunFamily.test.ts src/components/history/WorkflowHistory.revise.test.tsx src/app/dashboard/revisionFamilyLinkage.source.test.ts src/components/history/WorkflowHistory.test.tsx src/components/history/WorkflowHistory.genericReopen.test.tsx</automated>
  </verify>
  <done>tsc clean; the three new specs pass; the two touched fixtures (WorkflowHistory.test.tsx, WorkflowHistory.genericReopen.test.tsx) still pass (no regression). No known-red baseline spec is touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → REST API | JWT-authed; the FE sends `source_workflow_run_id`/`parent_run_id` as revision-launch parent linkage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u2o-01 | Tampering / Elevation (IDOR) | parent-link ingress (source_workflow_run_id) | accept (already mitigated upstream) | Backend Workstream A §3 hardens both ingress sites: `parent_run_id` is linked ONLY when the parent exists AND `WorkflowRun.user_id == current user`; a foreign id is silently not linked (404-not-403 posture). FE change adds no new authority — it only supplies the id the owner-checked backend validates. |
| T-u2o-02 | Information Disclosure | getRunFamily fetch | accept | `GET /api/runs/{id}/family` is owner-scoped server-side (runs.py:933 — every walk step filters user_id); cross-owner/missing → 404. FE fetcher rides the existing JWT authHeaders idiom. |
| T-u2o-SC | Tampering (supply chain) | package installs | N/A | No npm/pip installs in this plan — zero new dependencies; no legitimacy gate required. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean across the whole frontend project (all 5 source edits + 2 fixture updates + 3 new specs typecheck).
- `cd frontend && npx vitest run <the 3 new specs + 2 touched fixtures>` green.
- INV-3: `git status` shows ZERO files changed under `backend/` — this is an FE-only change, so the 5 backend characterization goldens and the semantic-event multiset are untouched by construction (no engine/websocket/manifest edit). Do NOT run the backend golden battery; state the no-backend-file-touched argument instead.
- Known-red baseline is NOT chased: do not run the full FE vitest suite expecting all-green (≈7 pre-existing unrelated reds: workflowChaining ×6, AgentProgressPanel ×1, IdeaInputPage.declaredCapabilities ×1, WorkflowCatalog ×5). Run only the touched/new specs listed in the Task 3 verify command.
</verification>

<success_criteria>
- getRunFamily hits `GET /api/runs/{id}/family` with a Bearer token and returns `{root_id, members}` (parsed, unnormalized).
- normalizeWorkflowRun maps parentRunId (default null) and rootRunId (default own id) from the raw snake_case fields.
- The currentWorkflowRunId heuristic is gone; every revision launch path sources parent linkage from contentSourceRunId (inline) or selectedRun.id (history), and history revisions now send the parent id (previously nothing).
- contentSourceRunId updates on pipeline_complete + reopen and clears on a fresh run.
- tsc clean; the 3 new specs + 2 touched fixtures green; zero backend files touched.
</success_criteria>

<output>
Create `.planning/quick/260702-u2o-workstream-b1-revision-family-fe-foundat/260702-u2o-SUMMARY.md` when done.
</output>
