---
id: 260812-35u
title: fix ISS-068 — restore the golden oracle and the import contract
status: planned
date: 2026-08-12
branch: bugfix/spec-revision-context-loss
base_sha: f5f2f7c64147f3900d519751d53261e480568c7f
ids:
  fix: [FIX-223, FIX-224]
  test: [TEST-008]
  iss_closed: [ISS-068]
  iss_filed: [ISS-078]
---

# ISS-068 — restore the golden oracle and the import contract

## Measured baseline (re-verified at `f5f2f7c6`, this session)

| Oracle | Baseline |
|---|---|
| Characterization goldens | **5 failed / 5 passed** (the 5 event snapshots; the 5 deliverable byte-snapshots are green) |
| `lint-imports` (from `backend/`) | **3 kept / 1 broken** |
| `tests/agents/test_restart_resume.py` | **7 failed / 48 passed** (same 7 ids) |

## The three causes (from the read-only investigation, independently re-verified here)

| # | Cause | Verdict | Action |
|---|---|---|---|
| **C1** | `pipeline_start.resume_offset` — additive key from `c71f3d9c`, hits **all 5** goldens | Neither golden nor engine wrong; the **normalizer** is stale | Add `resume_offset` to `_VOLATILE_STRIP_KEYS`. **No golden bytes touched.** |
| **C2** | `prototype-build` lost `icon: "🏗️"` in `f608e4bc` (the `- accessibility` line landed **on** the icon line); hits `prototype`, `od_prototype` | **REAL REGRESSION — the golden is RIGHT** | Restore the icon. **No golden bytes touched.** Add a pin. |
| **C3** | `planner: run → skip` in `fd2ad8f1` drops `planner_start`/`planner_complete`/`gate_status`; hits `prototype_revision` | **STALE GOLDEN** — deliberate, documented, tested change | Regenerate **only** `prototype_revision.events.json`, **strictly after C1**. |

Re-verified independently this session:
- Golden expects `icon='🏗️'` for `prototype-build` in `pipeline_start.agents[]` **and** both `agent_start`s (extracted from the golden JSON, not from the report).
- `git show f608e4bc -- .../prototype-build/AGENT.md` is `-icon: "🏗️"` / `+- accessibility`; the other 3 files in that commit are pure `1 +` insertions. A clobber, not a decision.
- Sweep: **86** `AGENT.md` files, exactly **1** without `icon:` — `prototype-build`.
- Original bytes: `icon: "\xf0\x9f\x8f\x97\xef\xb8\x8f"` (U+1F3D7 U+FE0F, double-quoted).
- `prototype_revision.events.json` has **17** events, exactly 3 of which are `planner_start` / `planner_complete` / `gate_status`; it references only `prototype-revision-agent` / `prototype-revision-validate`, so **C2 cannot touch it**.

## Import contract — option (a), break the import

`agents/capabilities/strategies/task_loop.py:562` does `from agents.execution_engine.od_context import get_example_html`.
The port already exists and is already used by a sibling capability:
- `KernelServices.template_example()` (`kernel_services.py:1163`) → `engine._load_template_example()` (`engine.py:9040`) → `get_example_html()` — same function, same `EXAMPLE_MAX_CHARS` cap, same swallow-and-return-`None`.
- Consumer pattern to mirror: `agents/capabilities/context_providers/opendesign.py:128-135` (`hasattr(runner, "template_example")` guard, because unit-test fakes set only `ctx.runner`).

Rejected: amending the contract (pins the architecture break).

## Tasks

### T1 — C1: strip `resume_offset` (FIX-223)
- `backend/tests/agents/characterization/_normalize.py` — add `"resume_offset"` to `_VOLATILE_STRIP_KEYS` with the same commented justification style as the 6 precedents.
- Expected: goldens go **5F/5P → 1F/9P** (only `prototype_revision` red). C2 still red on `prototype`/`od_prototype`? **No** — C2 is a separate cause; expect **3 failed / 7 passed** after T1 alone (`prototype`, `od_prototype` from C2, `prototype_revision` from C3).

### T2 — C2: restore the icon + pin it (FIX-223 / TEST-008)
- `backend/agents/prompts/prototype-build/AGENT.md` — restore `icon: "🏗️"` (keep the `- accessibility` guardrail).
- `backend/tests/agents/test_loader.py` — new pin in `TestSchemaValidationAllAgents`: **every** real `AGENT.md` must declare an explicit `icon:` in its frontmatter. Must read the RAW frontmatter, not `spec.icon` (the loader defaults to 🤖, so a `spec.icon` assertion is green-from-birth and proves nothing).
- Must be seen **RED before** (at the current AGENT.md) and **GREEN after**.
- Expected: goldens **3F/7P → 1F/9P**.

### T3 — Import contract (FIX-224 / TEST-008)
- `backend/agents/capabilities/strategies/task_loop.py` — replace the direct import with `runner.template_example(_tid)` behind the `hasattr` guard, mirroring `opendesign.py`.
- Add a pin that `agents.capabilities` imports neither `agents.execution_engine` nor `app` (a test-level mirror of the import-linter contract, so a violation fails pytest too — the contract is only checked by a separate binary today).
- Expected: `lint-imports` **3 kept / 1 broken → 4 kept / 0 broken**.

### T4 — C3: regenerate `prototype_revision.events.json` (FIX-223), STRICTLY AFTER T1
- `SNAPSHOT_UPDATE=1` on that one test only.
- **Proof obligation:** `git diff` the golden. The ONLY changes may be the removal of the 3 events (`planner_start`, `planner_complete`, `gate_status`), 17 → 14. If a single unexpected byte moves: STOP, do not commit, report.
- Its own commit, so it can be reverted independently.

### T5 — File ISS-078 for the 7 `test_restart_resume.py` reds
Major, out of scope here. Must carry:
- `c71f3d9c` inverted `_first_incomplete_step`'s skip predicate `or` → `and`; #2/#3/#4 all fail with the `idx=0` signature.
- **#1 and #6 are end-to-end** — they drive the real engine, let it write its own durable rows, and still observe completed wave workers re-invoked. A hand-seeding artefact cannot explain those, so this may be a **live production resume regression**, not test staleness.
- The discriminating experiment: revert only the `c71f3d9c` predicate hunk in a scratch worktree and re-run the 7; if ≥3 go green, it is a production regression.
- **#5 is proven trivial:** `2d5b9fcc` updated the 4-tuple unpack at `:2663` but not the 3-tuple stub at `:2639`.
- Do NOT delete/xfail/loosen any of the 7.

### T6 — Bookkeeping
Run the `velocity-ai-bookkeeping` skill and complete its Step 5 closing gate in full.

## Definition of done

1. Goldens **10/10 green**; `lint-imports` **4 kept / 0 broken**. If not honestly reachable, report the real number.
2. `test_restart_resume.py` stays at **7F/48P** (unchanged).
3. These stay green (29 tests at HEAD): `tests/agents/test_update_specs_enforcement.py`, `tests/unit/test_update_specs_ingress_fence.py`, `tests/agents/test_spec_revision_cycles.py`, `tests/agents/test_gate_revision_discriminator.py`.
4. New tests seen RED before / GREEN after, both pasted.
5. Commits: code separate from docs; the golden regeneration in its **own** commit. Do NOT push.

## Anti-goals

- Do NOT regenerate any golden other than `prototype_revision.events.json`.
- Do NOT amend the import-linter contract.
- Do NOT `git stash`.
- Do NOT touch register rows other than ISS-068 and the new rows filed here.
