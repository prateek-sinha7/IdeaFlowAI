---
quick_id: 260705-ed8
date: 2026-07-05
type: quick
mode: backend-regression-fix
branch: new-workflow-engine
autonomous: true
files_modified:
  - backend/agents/capabilities/model_catalog.py
  - backend/agents/capabilities/model_pricing.py
  - backend/tests/agents/test_model_pricing.py
  - .planning/FIX-REGISTER.md
requirements:
  - INV-12 single model-id source RESTORED
  - test_model_catalog::test_single_source_grep GREEN (no test-loosening)
invariants:
  - "INV-1 / SC-001: no workflow-name branching — cost dispatch is by model_id + region only"
  - "INV-3: goldens byte-identical + semantic-event-parity (cost is a stripped volatile key)"
  - "INV-12: model-id literals live in exactly ONE list (model_catalog.py) — the whole point"
  - "INV-13: no runtime / agent-loop change"
---

<objective>
Fix a proven regression introduced by this session's earlier quick-task **t2x**
(`260704-t2x`). t2x created `backend/agents/capabilities/model_pricing.py` with a
hardcoded `MODEL_PRICING` dict keyed by model-family literals
(`claude-haiku-4-5`, `claude-sonnet-4-5`, `claude-sonnet-4-6`, `claude-opus-4-5`,
`claude-opus-4-6`). That is a SECOND hand-maintained model-id list, which:
1. Violates **INV-12** (model-id literals must live in exactly ONE list —
   `model_catalog.py`), and
2. Breaks `backend/tests/agents/test_model_catalog.py::test_single_source_grep`,
   which greps `backend/app/api` + `backend/agents/capabilities` for
   `claude-(haiku|sonnet|opus)-4` on non-comment lines and asserts the hit set is
   exactly `{"model_catalog.py"}`. Post-t2x it gets
   `{"model_catalog.py", "model_pricing.py"}` -> FAIL.

Proven regression: the test PASSED at baseline `eb3ccced` (pre-t2x) and FAILS at
current HEAD `a6670e32`.

Purpose: Restore the single model-id source (INV-12) the PROPER way — co-locate
pricing data IN `model_catalog.py` (the one authoritative catalog) and make
`model_pricing.py` DERIVE each model's rates from that catalog, holding ZERO
model-id literals. No test-loosening. The public `estimate_cost_usd` signature +
math are unchanged, so both cost sites (engine `pipeline_complete`, persisted
`workflow_runs.token_usage`) keep working untouched, and the reconciliation pin
($24.85) is preserved.

Output: A frozen `Pricing` dataclass + `pricing` field on `ModelEntry` in
`model_catalog.py`; a catalog-derived `model_pricing.py` (exact get +
region/version-normalized fallback + cheap default) with only `_regional_premium`
+ `estimate_cost_usd` left; a rewired `test_model_pricing.py`; and FIX-038 logged
in FIX-REGISTER.md.
</objective>

<scope_discipline>
IN SCOPE — code changes ONLY in these three files (+ the FIX-REGISTER docs file):
- `backend/agents/capabilities/model_catalog.py`
- `backend/agents/capabilities/model_pricing.py`
- `backend/tests/agents/test_model_pricing.py`
- `.planning/FIX-REGISTER.md` (docs)

DO NOT (hard prohibitions):
- Do NOT loosen `test_single_source_grep`. Do NOT add `model_pricing.py` to its
  allowed set. `model_catalog.py` stays the ONE model-id source.
- Do NOT touch the two cost sites — `backend/agents/execution_engine/engine.py`
  (~L2287/2291) or `backend/app/api/websocket.py` (~L2004/2006). The
  `estimate_cost_usd` signature is unchanged, so they keep working as-is.
- Do NOT change ISS-032 cache-token plumbing (deep_agent_runner / engine
  accumulation / websocket surfacing).
- Do NOT touch the OUT-OF-SCOPE pre-existing baseline reds (`test_factory_injects`
  x5, `test_manifest_parity` clarify x7, `test_characterization_od_ppt`). These are
  identical at baseline `eb3ccced` and are NOT part of this fix.

VERIFIED SCOPE FACT (do not re-derive): across the whole backend, only
`estimate_cost_usd` is imported by non-test code (`engine.py:46`,
`websocket.py:18`). `MODEL_PRICING`, `Price`, `_canonical`, and `_regional_premium`
are imported ONLY by `tests/agents/test_model_pricing.py`. `test_iss032_cache_tokens.py`
imports ONLY `estimate_cost_usd`. So removing the literal machinery is safe — only
Task 3 (test_model_pricing.py) must be rewired.
</scope_discipline>

<context>
@backend/CLAUDE.md
@backend/agents/capabilities/model_catalog.py
@backend/agents/capabilities/model_pricing.py
@backend/tests/agents/test_model_pricing.py
@backend/tests/agents/test_model_catalog.py
@.planning/FIX-REGISTER.md

Executor runs all commands from `backend/` using `python3.11` (NO venv — see the
dev-runtime memory note). `lint-imports` is at `/opt/homebrew/bin/lint-imports`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Co-locate a frozen Pricing dataclass + pricing field in model_catalog.py (the single source)</name>
  <files>backend/agents/capabilities/model_catalog.py</files>
  <action>
FIRST READ the current `model_catalog.py` in full to learn the exact `ModelEntry`
field list and how the 5 `_ENTRIES` are constructed. Confirmed baseline: `ModelEntry`
is a frozen dataclass with fields id, label, description, tier, cost_class, provider,
context_window, user_allowed; `dataclasses.dataclass` is already imported (no new
import needed); all 5 entries are built with KEYWORD arguments.

Additive changes (per INV-12 — this file is the ONE authoritative model-id list):

1. Add a new frozen dataclass ABOVE `ModelEntry` named `Pricing` (decorated
   `@dataclass(frozen=True)`) with five float fields in this exact order/name:
   input, output, cache_read, cache_write_5m, cache_write_1h. Docstring: each field is
   a USD price per ONE token — the base / Anthropic-direct / Bedrock-global rate,
   BEFORE any regional inference-profile premium.

2. Add a `pricing: Pricing` field to `ModelEntry`, appended AFTER user_allowed (no
   default — every entry supplies it, so field ordering with the existing no-default
   fields is safe).

3. Populate ALL 5 entries by passing pricing=Pricing(...) (keyword, matching the
   existing keyword-construction style) with these VERIFIED per-single-token rates:
   - Haiku 4.5  (id eu.anthropic.claude-haiku-4-5-20251001-v1:0, cost_class cheap):
     Pricing(1e-6, 5e-6, 0.1e-6, 1.25e-6, 2e-6)
   - Sonnet 4.5 (id eu.anthropic.claude-sonnet-4-5-20250929-v1:0, standard):
     Pricing(3e-6, 15e-6, 0.3e-6, 3.75e-6, 6e-6)
   - Sonnet 4.6 (id eu.anthropic.claude-sonnet-4-6, standard):
     Pricing(3e-6, 15e-6, 0.3e-6, 3.75e-6, 6e-6)
   - Opus 4.5   (id eu.anthropic.claude-opus-4-5-20251101-v1:0, premium):
     Pricing(5e-6, 25e-6, 0.5e-6, 6.25e-6, 10e-6)
   - Opus 4.6   (id eu.anthropic.claude-opus-4-6-v1, premium):
     Pricing(5e-6, 25e-6, 0.5e-6, 6.25e-6, 10e-6)

4. Move the caveat comment from `model_pricing.py` — "Opus 4.6 / Sonnet 4.6 Bedrock $
   are DERIVED — operator confirm on the live AWS pricing page (they mirror the 4.5
   tier pending published Bedrock rates)" — into `model_catalog.py`, placed next to the
   Sonnet 4.6 / Opus 4.6 entries.

Do NOT change any existing `ModelEntry` field, the `ModelCatalog` accessor methods, or
any id/label/description/tier/cost_class value. This is purely additive so existing
`test_model_catalog.py` (field-set subset check, projection, cost_class, tier
consistency) and the `/api/settings` projection stay green.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_model_catalog.py -q</automated>
  </verify>
  <done>
`Pricing` is a frozen dataclass exported from `model_catalog.py`; `ModelEntry` has a
`pricing: Pricing` field; all 5 entries carry the verified rates; the DERIVED-rate
caveat comment lives next to Sonnet 4.6 / Opus 4.6. `test_model_catalog.py` passes in
full (the field-set test does a subset hasattr check, so the added field does not break
it; `test_single_source_grep` still sees only `model_catalog.py`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Make model_pricing.py DERIVE rates from the catalog — remove all model-id literals</name>
  <files>backend/agents/capabilities/model_pricing.py</files>
  <action>
FIRST READ the current `model_pricing.py` in full to preserve the EXACT
`estimate_cost_usd` behavior. Confirmed baseline: it holds a literal `MODEL_PRICING`
dict (the 5 claude-*-4 family keys), a local `Price` dataclass, `_FALLBACK =
"claude-haiku-4-5"` (a model-id literal), a `_canonical()` family-key resolver, and
`_regional_premium()`.

Rewrite as a catalog-derived module holding ZERO model-id literals:

1. REMOVE: the `MODEL_PRICING` dict, the local `Price` dataclass, `_FALLBACK`, and
   `_canonical` (all of these contain or reference claude-*-4 literals).

2. ADD import: from agents.capabilities.model_catalog import ModelCatalog, Pricing
   (both are in `agents.capabilities`, so import-linter stays 4 kept / 0 broken — the
   contract only forbids app.* / agents.execution_engine / agents.workflows imports
   from agents.capabilities).

3. ADD `_resolve_pricing(model_id: str) -> Pricing` that resolves a model's rates FROM
   the catalog, in three tiers:
   (a) EXACT: entry = ModelCatalog().get(model_id); if found, return entry.pricing.
   (b) NORMALIZED FALLBACK: normalize BOTH the input id AND each catalog id by (i)
       iteratively stripping any leading region/provider prefix
       (eu. / us. / apac. / global. / anthropic.) and (ii) stripping any trailing
       version/date suffix (-vN, -vN:0, -YYYYMMDD, -YYYYMMDD-vN:0). Reuse the existing
       prefix tuple + suffix regex machinery for this (they carry NO model-id literals,
       so they stay). Match normalized-input to normalized-catalog-id; on a match
       return that entry's .pricing.
   (c) FINAL DEFAULT: return the catalog's CHEAP entry's .pricing — locate it by
       cost_class == "cheap" (NOT by a literal id/family). Keep a warn-once log for an
       unknown id (the existing `_WARNED_UNKNOWN` set + logger.warning pattern is fine —
       just change the message so it holds no claude-* literal).

4. KEEP `_regional_premium(model_id) -> float` UNCHANGED (prefix eu./us./apac. -> 1.10,
   else 1.0). Region strings are not model-id literals — allowed.

5. KEEP the public `estimate_cost_usd` signature EXACTLY: parameters
   (model_id: str | None, input_tokens, output_tokens, cache_read_tokens=0,
   cache_write_tokens=0, *, cache_ttl="5m") and the math: resolve
   p = _resolve_pricing(model_id or ""); write_rate = p.cache_write_1h if
   cache_ttl == "1h" else p.cache_write_5m;
   usd = (input_tokens*p.input + output_tokens*p.output + cache_read_tokens*p.cache_read
   + cache_write_tokens*write_rate) * _regional_premium(model_id or "");
   return round(usd, 6). A None / unknown id must price as the cheap (Haiku) tier at
   1.0x (_resolve_pricing("") -> no exact, no normalized match -> cheap default;
   _regional_premium("") -> 1.0), preserving `test_none_model_prices_as_haiku_1x`.
   `Pricing` field names (input/output/cache_read/cache_write_5m/cache_write_1h) are
   identical to the old `Price`, so p.field access is unchanged.

6. Update the module docstring to say rates are DERIVED from `model_catalog.py` (the
   single source) rather than a local per-family dict — and ensure NO claude-* literal
   appears anywhere in the file, including docstring/comments.

HARD RESULT: grep for claude-(haiku|sonnet|opus)-4 in `model_pricing.py` returns ZERO
lines (aim for zero; at worst only comment lines).
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_iss032_cache_tokens.py -q && grep -nE 'claude-(haiku|sonnet|opus)-4' agents/capabilities/model_pricing.py | grep -vE '^[0-9]+:[[:space:]]*#' | grep -c . | grep -qx 0 && echo 'GREP CLEAN: zero non-comment model-id literals'</automated>
  </verify>
  <done>
`model_pricing.py` derives every model's `Pricing` from the catalog (exact get +
region/version-normalized fallback + cheap default), keeps only `_regional_premium` +
`estimate_cost_usd`, imports `ModelCatalog`/`Pricing` from `model_catalog`, and holds
ZERO non-comment model-id literals. `test_iss032_cache_tokens.py` stays green (it uses
only the unchanged `estimate_cost_usd`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewire test_model_pricing.py to the catalog-derived structure (pins preserved)</name>
  <files>backend/tests/agents/test_model_pricing.py</files>
  <action>
FIRST READ the current `test_model_pricing.py` to see what to rewire. Confirmed
baseline imports: MODEL_PRICING, Price, _canonical, _regional_premium, estimate_cost_usd
from model_pricing. Two of those (MODEL_PRICING, Price, _canonical) no longer exist
after Task 2, so this test MUST be updated (it is the ONLY importer of them — verified).

Rewire (test files MAY reference model-id literals — `test_single_source_grep` scans only
app/api + agents/capabilities, NOT tests):

1. Imports: drop MODEL_PRICING / Price / _canonical. Keep _regional_premium,
   estimate_cost_usd. Add `from agents.capabilities.model_catalog import ModelCatalog,
   Pricing` for the catalog-derived rate assertions.

2. Per-tier base rates: rewire `test_per_family_base_rates` /
   `test_pricing_covers_exactly_five_families` to assert the catalog carries the rates —
   e.g. for each of the 5 catalog ids assert ModelCatalog().get(<id>).pricing ==
   Pricing(...) with the verified numbers (Opus via eu.anthropic.claude-opus-4-5-...,
   Sonnet via eu.anthropic.claude-sonnet-4-5-..., Haiku via
   eu.anthropic.claude-haiku-4-5-..., etc.), and that exactly 5 entries each expose a
   `pricing` field.

3. Replace the `_canonical` tests with `_resolve_pricing` (or estimate_cost_usd)
   behavior tests covering the three tiers: (a) exact catalog id -> that entry's pricing;
   (b) a region/version-normalized variant id (e.g. a bare or differently-prefixed form
   of a catalog id) resolves to the correct entry's pricing; (c) an unknown id -> the
   cheap/Haiku pricing (fallback). Import `_resolve_pricing` if you assert it directly.

4. KEEP these behavioral pins UNCHANGED (they must still pass):
   - test_reconciliation_pin: estimate_cost_usd("eu.anthropic.claude-haiku-4-5-20251001-v1:0",
     21_460_378, 225_860) == pytest.approx(24.85, abs=0.01).
   - test_none_model_prices_as_haiku_1x: estimate_cost_usd(None, 1_000_000, 0) == round(1.0, 6).
   - Regional-premium tests (+10% for eu./us./apac.; 1.0 for global./anthropic-direct/bare/"").
   - Cache-tier tests: cache_read priced 0.1x input on Haiku; cache_write 5m (1.25) vs 1h
     (2.0) delta; regional premium compounds exactly 1.10x on Opus.
   - test_engine_cost_site_uses_shared_function / test_websocket_cost_site_uses_shared_function
     (they read engine.py + websocket.py off disk — both untouched, so they stay green).

5. Do NOT re-add any `test_single_source_grep`-style allow-list change here — that test
   lives in `test_model_catalog.py` and must NOT be touched.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/agents/test_model_pricing.py tests/agents/test_iss032_cache_tokens.py tests/agents/test_model_catalog.py -q</automated>
  </verify>
  <done>
`test_model_pricing.py` imports only symbols that still exist, asserts the catalog-derived
pricing structure + the three-tier resolve behavior, and preserves the reconciliation pin
($24.85), the None->Haiku@1x pin, regional-premium, cache-tier, and both-cost-site checks.
All three suites pass together.
  </done>
</task>

<task type="auto">
  <name>Task 4: Log FIX-038 in FIX-REGISTER.md (docs)</name>
  <files>.planning/FIX-REGISTER.md</files>
  <action>
READ `.planning/FIX-REGISTER.md` and copy the EXACT column layout of the FIX-037 row
(8 columns / 9 pipes): Fix ID | Date | Description | Root Cause | Files Changed |
Phase Involved | Invariants | Status. Insert a new row for FIX-038 immediately AFTER the
FIX-037 row.

Column values for FIX-038:
- Fix ID: FIX-038
- Date: 2026-07-05
- Description: "t2x regression: model_pricing.py hardcoded model-family literals violated
  INV-12 (single model-id source) and broke test_model_catalog::test_single_source_grep
  (passed at baseline eb3ccced, failed after t2x)."
- Root Cause: "Fix (proper, no test-loosen): co-located a frozen Pricing dataclass +
  pricing field on ModelEntry in model_catalog.py (the single source); model_pricing.py
  now derives each model's Pricing FROM the catalog (exact get + region/version-normalized
  fallback + cheap default) and keeps only _regional_premium + estimate_cost_usd — ZERO
  model-id literals. Reconciliation pin ($24.85) preserved; goldens byte-identical; lint
  4/0."
- Files Changed: backend/agents/capabilities/model_catalog.py,
  backend/agents/capabilities/model_pricing.py,
  backend/tests/agents/test_model_pricing.py
- Phase Involved: quick-260705-ed8
- Invariants: INV-1/3/12/13 · SC-001 (checkmark) — match the checkmark glyph used in the
  FIX-037 row.
- Status: Done

Escape any literal pipe characters inside cell text so the table stays well-formed. Do
NOT alter any other row.

NOTE (orchestrator responsibility — do NOT do this yourself): the STATE.md "Quick Tasks
Completed" row is added by the orchestrator, not the executor.
  </action>
  <verify>
    <automated>grep -nE '^\| FIX-038 \| 2026-07-05 \|' .planning/FIX-REGISTER.md && grep -c '^| FIX-038 ' .planning/FIX-REGISTER.md | grep -qx 1 && echo 'FIX-038 row present (single, dated 2026-07-05)'</automated>
  </verify>
  <done>
A single well-formed FIX-038 row (dated 2026-07-05, 9 pipes matching FIX-037's layout)
sits directly after FIX-037, with Phase Involved quick-260705-ed8 and invariants
INV-1/3/12/13 · SC-001.
  </done>
</task>

</tasks>

<verification>
## HARD GATES / EXIT CRITERIA
All commands run from `backend/`, `python3.11`, NO venv. Every gate must pass before the
task is considered complete.

1. `python3.11 -m pytest tests/agents/test_model_catalog.py -q` -> ALL pass, INCLUDING
   `test_single_source_grep` now GREEN.
2. `python3.11 -m pytest tests/agents/test_model_pricing.py tests/agents/test_iss032_cache_tokens.py -q`
   -> all green; reconciliation pin ($24.85 +-0.01) holds.
3. `python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_od_ppt.py -q`
   -> SAME pass/fail shape as before (SNAPSHOT_UPDATE UNSET; cost is a stripped volatile
   key, so this change is golden-neutral — confirm NO new golden divergence). Expected
   shape: the 4 non-ppt characterizations pass; `test_characterization_od_ppt` is a KNOWN
   pre-existing offline red and must be UNCHANGED (not newly broken by this fix).
4. `/opt/homebrew/bin/lint-imports` (run from `backend/`) -> 4 kept / 0 broken.
5. Grep proof: `grep -nE 'claude-(haiku|sonnet|opus)-4' backend/agents/capabilities/model_pricing.py`
   returns ONLY comment lines (ideally ZERO lines).
6. Scope discipline: `git diff --name-only` shows code changes ONLY in
   `model_catalog.py`, `model_pricing.py`, `test_model_pricing.py` (+ the `.planning/FIX-REGISTER.md`
   docs change). No other files touched — in particular NOT engine.py, websocket.py, or
   the deep_agent_runner.

## INVARIANTS (assert in the fix)
- INV-1 / SC-001: cost dispatch keys off model_id + region only — NO workflow-name/agent
  branching anywhere in the pricing path.
- INV-3: prototype/od_prototype/revision/app_builder goldens byte-identical +
  event-parity (cost + model_id are stripped volatile keys; od_ppt red unchanged).
- INV-12: model-id literals live in exactly ONE list — `model_catalog.py` — RESTORED
  (the whole point of this fix).
- INV-13: no agent-runtime / loop / deepagents change.
</verification>

<success_criteria>
- `test_model_catalog.py::test_single_source_grep` is GREEN (hit set == {model_catalog.py}).
- `model_pricing.py` holds ZERO non-comment model-id literals and derives all rates from
  the catalog via `_resolve_pricing`.
- `estimate_cost_usd` signature + math unchanged; both cost sites untouched and working;
  reconciliation pin $24.85 preserved.
- `test_model_pricing.py` rewired and green alongside `test_iss032_cache_tokens.py` and
  `test_model_catalog.py`.
- Characterization goldens byte/event-identical (od_ppt red unchanged); `lint-imports`
  4/0.
- FIX-038 logged in FIX-REGISTER.md (dated 2026-07-05, after FIX-037).
</success_criteria>

<commits>
Plan for ~3 atomic code commits + 1 docs commit. Plain messages — NO Co-Authored-By,
NO Claude-Session trailer. Suggested scopes (per backend commit conventions):

1. `fix(agents): co-locate frozen Pricing + pricing field in model_catalog (INV-12 single source)`
   -> Task 1 (model_catalog.py)
2. `fix(agents): derive model_pricing rates from catalog — remove model-id literals (INV-12)`
   -> Task 2 (model_pricing.py)
3. `test(agents): rewire test_model_pricing to catalog-derived pricing (pins preserved)`
   -> Task 3 (test_model_pricing.py)
4. `docs(planning): add FIX-038 (model_pricing INV-12 single-source restore)`
   -> Task 4 (.planning/FIX-REGISTER.md)

STATE.md "Quick Tasks Completed" row is the ORCHESTRATOR's job — the executor does NOT
add it.
</commits>

<output>
On completion, create
`.planning/quick/260705-ed8-model-pricing-inv12-single-source/260705-ed8-SUMMARY.md`
recording: the regression fixed, the three-tier `_resolve_pricing` design, gate results
(all 6 hard gates), the reconciliation pin value observed, and the commit hashes.
</output>
