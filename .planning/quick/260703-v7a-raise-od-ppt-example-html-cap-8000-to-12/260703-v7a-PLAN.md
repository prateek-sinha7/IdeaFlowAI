---
phase: quick-260703-v7a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agents/execution_engine/od_context.py
  - backend/agents/capabilities/context_providers/opendesign.py
  - backend/tests/agents/test_context_providers.py
  - backend/agents/prompts/od-ppt-brief-analyst/AGENT.md
autonomous: true
requirements: [FIX-1-example-cap, FIX-2-design-md-claim]

must_haves:
  truths:
    - "The od_ppt composer (od-ppt-composer) receives the FULL example.html — deck templates up to ~94k chars are injected untruncated (no 8000-char clip)."
    - "example.html is truncated at exactly ONE point (od_context.get_example_html); the redundant second [:8000] clip in opendesign.py is gone (INV-12 single-truncation)."
    - "All 5 characterization goldens stay byte/event-identical with SNAPSHOT_UPDATE UNSET — neither fix regenerates any golden (hard gate; if any golden changes, STOP)."
    - "od-ppt-brief-analyst's prompt body no longer claims to receive the full DESIGN.md body it never gets (injects stays [template]); the design system is described as applied downstream by the composer and referenced symbolically by the planner."
  artifacts:
    - path: "backend/agents/execution_engine/od_context.py"
      provides: "Module-level EXAMPLE_MAX_CHARS = 120_000 constant; get_example_html default = EXAMPLE_MAX_CHARS (was 8000). Seed [:6000] / reference [:4000] caps UNCHANGED."
      contains: "EXAMPLE_MAX_CHARS = 120_000"
    - path: "backend/agents/capabilities/context_providers/opendesign.py"
      provides: "Single-truncation provider — the redundant [:8000] re-truncation (old lines 136-139) removed; uses runner.template_example(...) result directly. Docstring line ~11 no longer says example[:8000]. The example.html GATE (line ~130) UNCHANGED."
    - path: "backend/tests/agents/test_context_providers.py"
      provides: "New regression pin: composer-shaped ctx with a >8000-char example asserts the injected TEMPLATE EXAMPLE block is the FULL example (no ...[truncated])."
      contains: "def test_opendesign_composer_example_not_truncated"
    - path: "backend/agents/prompts/od-ppt-brief-analyst/AGENT.md"
      provides: "Body-only reword (below the --- frontmatter): the ~L29 'full DESIGN.md body' claim dropped/reframed and the ~L33 design-system line reframed as downstream/symbolic. Frontmatter (injects:[template], tools, order) UNCHANGED."
  key_links:
    - from: "backend/agents/capabilities/context_providers/opendesign.py"
      to: "runner.template_example(template_id)"
      via: "provider consumes the already-capped string directly (no second truncate)"
      pattern: "runner\\.template_example"
    - from: "backend/agents/execution_engine/od_context.py get_example_html default"
      to: "EXAMPLE_MAX_CHARS constant"
      via: "single authoritative cap"
      pattern: "max_chars: int = EXAMPLE_MAX_CHARS"
---

<objective>
Two register-adherent, BACKEND-ONLY od_ppt context-delivery fixes, both proven INV-3-neutral (NO golden regeneration for either — hard gate).

FIX 1 (HIGH impact): The od_ppt deck builder's injected example.html is truncated to 8000 chars at TWO redundant points, so real deck templates (25k-94k chars) reach the composer with partial CSS, ZERO <section> slides, and no nav script — despite its SKILL.md ordering it to "clone example.html / copy the nav script verbatim." Raise the cap to a single bounded 120_000-char constant and consolidate the double-truncation to ONE point (INV-12).

FIX 2 (LOW impact, prompt honesty): od-ppt-brief-analyst's prompt body claims it receives "the full DESIGN.md body," but it declares injects:[template] (NOT design_system), so DESIGN.md is never delivered. Reword the body so the prompt is honest — the design system is applied downstream by the composer; the planner references it symbolically.

Purpose: The composer can finally clone the full deck template; the planner prompt stops promising context it never receives.
Output: 4 edited files (od_context.py, opendesign.py, test_context_providers.py, od-ppt-brief-analyst/AGENT.md), 5 goldens untouched, one new regression pin.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@backend/CLAUDE.md

# The cap (authoritative truncation) + the seed/reference caps that MUST NOT change
@backend/agents/execution_engine/od_context.py
# The redundant second truncation + the example.html gate (DO NOT touch the gate)
@backend/agents/capabilities/context_providers/opendesign.py
# FIX 2 target — body only, below the frontmatter
@backend/agents/prompts/od-ppt-brief-analyst/AGENT.md
# The regression-pin home (reuse _FakeRunner / _Ctx / _OD)
@backend/tests/agents/test_context_providers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FIX 1 — raise example.html cap to 120k, consolidate the double-truncation, add regression pin</name>
  <files>backend/agents/execution_engine/od_context.py, backend/agents/capabilities/context_providers/opendesign.py, backend/tests/agents/test_context_providers.py</files>
  <behavior>
    New regression pin in test_context_providers.py (reuse _FakeRunner / _Ctx / _OD, async, mirror test_opendesign_block_name_keys_and_content):
    - Build a composer-shaped ctx via _Ctx(runner, od_context=_OD, current_spec_tools={"workspace"}, current_spec_injects={"template","design_system","template_example"}) with a _FakeRunner whose example is a >8000-char string (a padded HTML doc ~12000 chars). build_task_number defaults to "" (single-shot composer, so no task-2+ suppression).
    - blocks = await OpenDesignProvider().load(ctx); block = blocks["TEMPLATE EXAMPLE (example.html): web-prototype"].
    - RED (before the fix, provider clips at 8000): block contains "...[truncated]" and block != full example, so the assertion fails.
    - GREEN (after removing the redundant clip): "...[truncated]" not in block AND block == full example AND len(block) > 8000.
  </behavior>
  <action>
    Implements FIX 1. INV-3 is PROVEN neutral: the characterization fixture uses template_id="web-prototype" whose example.html is 4217 chars, which is under both 8000 and 120000, so it is returned whole before AND after (byte-identical); the oracle's synthetic small example is likewise unaffected.

    In backend/agents/execution_engine/od_context.py:
    - Add ONE module-level constant near the top-level definitions: EXAMPLE_MAX_CHARS = 120_000 (covers the current catalog max approx 93,663 with headroom, bounded well under Haiku's 200k window; the composer is single_shot). Keep od_context.py import-neutral — this is a plain module constant, add NO new import (import-linter must stay 4 kept / 0 broken).
    - Change get_example_html (line 170) default from "max_chars: int = 8000" to "max_chars: int = EXAMPLE_MAX_CHARS". KEEP the existing logic verbatim: return the whole string if len < max_chars, else content[:max_chars] + "\n...[truncated]" (fixture stays byte-identical).
    - DO NOT touch the TEMPLATE SEED [:6000] cap (line 154) or the TEMPLATE REFERENCE [:4000] cap (line 163) — those appear as ...[truncated] in the prototype / od_prototype goldens. DO NOT reuse BRIEF_MAX_CHARS. DO NOT lower the cap.

    In backend/agents/capabilities/context_providers/opendesign.py (INV-12 single-truncation — consolidate to the one authoritative point in od_context.get_example_html):
    - REMOVE the redundant second truncation (old lines 136-139: the "truncated = example_html[:8000]" / "if len(example_html) > 8000" / append "...[truncated]" block). Assign the block directly from the already-capped string returned by the runner: blocks[f"TEMPLATE EXAMPLE (example.html): {template_id}"] = example_html (byte-identical for the web-prototype fixture: 4217 < cap, so whole either way).
    - Update the stale docstring mention (line ~11) that reads "example[:8000]" so it reflects the single 120k cap owned by od_context (e.g. describe it as the example.html capped in od_context.get_example_html).
    - DO NOT touch the example.html GATE at line ~130: (is_builder or "template_example" in injects) and runner is not None and hasattr(runner, "template_example") — locked by Phase-7 07-REVIEW-0706 + ISS-029. DO NOT change the block-name key or its ordering.

    In backend/tests/agents/test_context_providers.py: add the regression pin described in <behavior>, named test_opendesign_composer_example_not_truncated. Reuse the existing _FakeRunner / _Ctx / _OD helpers and the @pytest.mark.asyncio pattern; add NO new fixtures.
  </action>
  <verify>
    <automated>cd backend && grep -q "EXAMPLE_MAX_CHARS = 120_000" agents/execution_engine/od_context.py && grep -q "max_chars: int = EXAMPLE_MAX_CHARS" agents/execution_engine/od_context.py && ! grep -q "= 8000" agents/execution_engine/od_context.py && ! grep -q "\[:8000\]" agents/capabilities/context_providers/opendesign.py && grep -q "\[:6000\]" agents/execution_engine/od_context.py && grep -q "\[:4000\]" agents/execution_engine/od_context.py && python3.11 -m pytest tests/agents/test_context_providers.py tests/agents/test_context_message_oracle.py -q && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_od_ppt.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>
    EXAMPLE_MAX_CHARS = 120_000 exists as a module constant in od_context.py; get_example_html default is EXAMPLE_MAX_CHARS (no "8000" remains in od_context.py); the seed [:6000] and reference [:4000] caps are untouched. opendesign.py has NO [:8000] anywhere (code or docstring); the example.html gate (line ~130) is unchanged. The new pin test_opendesign_composer_example_not_truncated passes (block == full >8000-char example, no ...[truncated]). All 5 characterization goldens pass with SNAPSHOT_UPDATE unset, the oracle passes, test_context_providers passes, and lint-imports reports 4 kept / 0 broken.
  </done>
</task>

<task type="auto">
  <name>Task 2: FIX 2 — trim od-ppt-brief-analyst's dangling DESIGN.md claim (prompt honesty, body-only)</name>
  <files>backend/agents/prompts/od-ppt-brief-analyst/AGENT.md</files>
  <action>
    Implements FIX 2 (prompt honesty). The planner declares injects:[template] (NOT design_system), so DESIGN.md is NEVER delivered — the provider + factory both gate on "design_system" in injects, and the disk never carries DESIGN.md. The planner does not NEED it: it plans CONTENT symbolically, theme_choice comes from the ACTIVE TEMPLATE SKILL.md, and the composer applies the real DESIGN.md tokens downstream.

    Edit BELOW the frontmatter "---" (line 19) ONLY. Do NOT change injects (stays [template]), tools, order, or ANY other frontmatter key. od-ppt-brief-analyst is NOT in _FROZEN_FRONTMATTER, so a body edit is safe.

    - Line ~29: reword or drop the input-list bullet that currently reads "The ACTIVE DESIGN SYSTEM — full DESIGN.md body (only present for templates that require it)" so it no longer claims to receive the full DESIGN.md body (the planner does not receive it).
    - Line ~33: reframe the "The plan you produce MUST respect: ... The design system's tokens (if provided) — referenced symbolically" bullet so it states the design system is applied DOWNSTREAM by the Deck Engineer (the composer), and the planner references it SYMBOLICALLY by intent (e.g. "use the accent color for the title"), not by token value.

    DO NOT add design_system to injects (rejected — would change the od_ppt golden for no material benefit). DO NOT touch any other agent — especially NOT the frozen-5 (od-ppt-validator, app/dotnet/mulesoft-sdlc-governance, app-infra-generator; Phase-15 Pitfall 4).

    INV-3: body-only edit; the scripted model IGNORES prompt bodies (Phase-3/15 caveat), so all 5 goldens stay byte-identical. NO regen.
  </action>
  <verify>
    <automated>cd backend && ! grep -q "full DESIGN.md body" agents/prompts/od-ppt-brief-analyst/AGENT.md && grep -Pzoq "injects:\n- template\n" agents/prompts/od-ppt-brief-analyst/AGENT.md && python3.11 -m pytest tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>
    The phrase "full DESIGN.md body" no longer appears in AGENT.md; the ~L29 and ~L33 bullets are reworded to be honest (design system applied downstream by the composer, referenced symbolically by the planner). Frontmatter is byte-unchanged (injects still exactly "- template"; tools/order untouched). All 5 characterization goldens pass with SNAPSHOT_UPDATE unset. No other agent's AGENT.md is modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| od_loader disk read (example.html) -> LLM prompt (composer) | Template HTML (engineer-authored, in-repo catalog) is injected into the model context. Bound the injected size. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v7a-01 | Denial of Service | get_example_html injection into the composer prompt | mitigate | Cap is a bounded module constant EXAMPLE_MAX_CHARS = 120_000 (covers catalog max ~93,663 with headroom, well under Haiku's 200k window). The cap is NOT removed — only raised. Single-truncation (INV-12) keeps one enforcement point. |
| T-v7a-02 | Tampering | example.html source | accept | Templates are engineer-authored, version-controlled in-repo (not user-supplied); no new input surface introduced by this change. |
| T-v7a-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new imports; od_context stays import-neutral). Nothing to audit. |
</threat_model>

<verification>
Run from backend/ (python3.11, NO venv; do NOT set SNAPSHOT_UPDATE; do NOT run the full backend pytest — it hangs offline):

1. Goldens unchanged (hard gate — if any golden changes, STOP):
   python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_characterization_od_ppt.py -q
2. Oracle + providers (incl. the new pin):
   python3.11 -m pytest tests/agents/test_context_message_oracle.py tests/agents/test_context_providers.py -q
3. Import contracts: /opt/homebrew/bin/lint-imports  (expect 4 kept / 0 broken)
4. Scope guard: git status --porcelain shows ONLY the 4 target files:
   backend/agents/execution_engine/od_context.py
   backend/agents/capabilities/context_providers/opendesign.py
   backend/tests/agents/test_context_providers.py
   backend/agents/prompts/od-ppt-brief-analyst/AGENT.md
</verification>

<success_criteria>
- FIX 1: EXAMPLE_MAX_CHARS = 120_000 is the single authoritative cap; get_example_html defaults to it; opendesign.py has no [:8000] (code or docstring) and reuses the runner's capped string directly; seed [:6000] / reference [:4000] and the example.html gate are untouched; the new regression pin proves a >8000-char example is injected whole.
- FIX 2: od-ppt-brief-analyst body no longer claims to receive the full DESIGN.md body; injects stays [template]; no other agent touched.
- Hard gate: all 5 characterization goldens byte/event-identical with SNAPSHOT_UPDATE unset (NO regen for either fix).
- Oracle + test_context_providers green; lint-imports 4 kept / 0 broken.
- git status --porcelain lists ONLY the 4 target files.
- Branch new-workflow-engine (NEVER main). Commits (no Co-Authored-By trailer):
  FIX 1: fix(agents): raise od_ppt example.html cap 8000->120k + consolidate double-truncate
  FIX 2: fix(prompts): trim od-ppt-brief-analyst dangling DESIGN.md claim (symbolic planning)
</success_criteria>

<output>
Create `.planning/quick/260703-v7a-raise-od-ppt-example-html-cap-8000-to-12/260703-v7a-SUMMARY.md` when done.
</output>
