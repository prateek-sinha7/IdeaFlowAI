---
phase: quick-260702-s3p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/websocket.py
  - backend/app/api/runs.py
  - backend/tests/unit/test_ws_parent_link_ownership.py
  - backend/tests/unit/test_runs_api_family.py
  - backend/tests/unit/test_runs_api_artifacts.py
autonomous: true
requirements: [POR-S3-OWNERSHIP, POR-S4-RUN-FIELDS, POR-S4-FAMILY, POR-S4-KIND-FILTER]

must_haves:
  truths:
    - "A foreign (other user's) source_workflow_run_id sent over WS never produces a row-level parent_run_id link — at BOTH the run_pipeline ingress and the run_revision row creation; an owned source id still links (POR §3)"
    - "run_revision dispatch behavior is unchanged by the hardening — the engine is still invoked identically; only the ROW linkage is conditioned on ownership"
    - "GET /api/runs (list) and GET /api/runs/{id} (get) both return parent_run_id (verbatim column) and root_run_id (server-computed); a standalone run's root_run_id == its own id; a chained run's root_run_id == the last OWNED ancestor (POR §4.2, D7)"
    - "GET /api/runs/{id}/family returns {root_id, members[]} with members ordered created_at ASC carrying 1-based revision_index + parent_run_id; cross-owner/missing run → 404 (never 403); a foreign parent in the chain terminates the walk and NO foreign run metadata appears in any response (POR §4.3, D2, D6)"
    - "GET /api/runs/{id}/artifacts?kind=X returns only kind-X refs (filtered refs surface as roots when their parents are filtered out); NO kind param → byte-identical current behavior; unknown kind → empty artifacts list, not 422; composes with include=content (POR §4.4, D8)"
    - "The 5 characterization goldens stay byte/event-identical with SNAPSHOT_UPDATE unset (the one KNOWN pre-existing od_ppt environmental failure excepted) and lint-imports reports 4 contracts kept / 0 broken (INV-3, Q3)"
  artifacts:
    - path: "backend/app/api/websocket.py"
      provides: "Shared ownership-checked parent resolution used at both ingress sites"
      contains: "_resolve_owned_parent_run_id"
    - path: "backend/app/api/runs.py"
      provides: "parent_run_id + root_run_id response fields, /family endpoint, kind filter"
      contains: "def get_run_family"
    - path: "backend/tests/unit/test_ws_parent_link_ownership.py"
      provides: "Ingress hardening proof at both sites"
      min_lines: 60
    - path: "backend/tests/unit/test_runs_api_family.py"
      provides: "Family endpoint + root_run_id/parent_run_id field tests"
      min_lines: 100
    - path: "backend/tests/unit/test_runs_api_artifacts.py"
      provides: "kind-filter tests extending the existing artifacts suite"
      contains: "kind"
  key_links:
    - from: "backend/app/api/websocket.py:_handle_workflow_execution"
      to: "_resolve_owned_parent_run_id"
      via: "parent-link resolution replacing the exists-only check at :1688-1695"
      pattern: "_resolve_owned_parent_run_id"
    - from: "backend/app/api/websocket.py:_handle_revision_execution"
      to: "_resolve_owned_parent_run_id"
      via: "row-creation parent_run_id kwarg replacing the exists-only ternary at :2247-2253"
      pattern: "_resolve_owned_parent_run_id"
    - from: "backend/app/api/runs.py:list_runs + get_run"
      to: "WorkflowRunResponse.root_run_id"
      via: "_compute_root_ids batched owned walk + explicit response construction"
      pattern: "root_run_id"
    - from: "backend/app/api/runs.py:get_run_artifacts"
      to: "_build_lineage_tree"
      via: "kind filter applied to refs BEFORE tree building"
      pattern: "r.kind == kind"
---

<objective>
Workstream A of the Revision-Families & Run-Inputs plan of record (`.planning/REVISION-FAMILY-AND-RUN-INPUTS-PLAN.md` §3 + §4): the backend read surface for revision families, app-layer only.

Four deliverables: (1) IDOR fix — ownership-harden both parent-link ingress sites in websocket.py; (2) additive `parent_run_id` + server-computed `root_run_id` on `WorkflowRunResponse` (list + get); (3) new owner-scoped `GET /api/runs/{id}/family`; (4) additive `kind` query param on `GET /api/runs/{id}/artifacts`.

Purpose: revisions are currently disconnected orphans in every read surface — the data model already holds the lineage (`parent_run_id`); this exposes it safely. Workstream B (FE) consumes these fields next.
Output: hardened websocket.py ingress, extended runs.py read surface, 3 test files (2 new, 1 extended), all gates green.

Scope fence (POR §4): ZERO edits under `backend/agents/`, ZERO migrations, ZERO new WS event types, ZERO frontend, do NOT touch the dead `source_run_id` column — `parent_run_id` is the ONLY lineage column.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REVISION-FAMILY-AND-RUN-INPUTS-PLAN.md   (authoritative spec — §1 root causes, §3 security corrections, §4 Workstream A, §10 invariants)
@backend/app/api/runs.py                              (response model :88-110, list :116, get :263, delete child-nulling :334, _build_lineage_tree :579-666, artifacts endpoint :669-716)
@backend/app/api/websocket.py                         (READ ONLY the regions: ingress ~:730, parent FK-check :1685-1695, run-row creation :1697-1719, _handle_revision_execution signature :2160, run_revision row creation :2224-2260 — the file is huge, never read it whole)
@backend/tests/unit/test_runs_api_artifacts.py       (TestClient + dependency-override + seed-helper idiom to follow and extend)
@backend/tests/unit/test_run_revision_ws_dispatch.py (offline harness that drives _handle_revision_execution directly: in-memory SQLite monkeypatched onto ws_module._get_db + stub engine via get_execution_engine — the pattern for the site-2 handler test)
@backend/tests/unit/test_runs_api.py                 (list/get/delete test idiom)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ownership-harden both parent-link ingress sites (IDOR fix, POR §3)</name>
  <files>backend/app/api/websocket.py, backend/tests/unit/test_ws_parent_link_ownership.py</files>
  <behavior>
    - Helper direct: owned candidate id → returns the id; foreign-owned candidate → None; missing candidate → None; None/empty candidate → None (no query)
    - Site 2 handler-level (offline harness): a revision launched against a FOREIGN parent creates its WorkflowRun row with parent_run_id IS None, AND the stub engine is still invoked (dispatch behavior unchanged — engine-level assert_owns stays the content gate); an OWNED parent → row linked (the existing owned-link test in test_run_revision_ws_dispatch.py keeps passing)
    - Site 1 source-level: inspect.getsource(_handle_workflow_execution) contains the FULL call signature `_resolve_owned_parent_run_id(db, source_workflow_run_id, user.id)` (pin the exact args — NOT just the helper name — so a mis-wired user-id argument at the primary IDOR site cannot pass; plan-checker WARNING) and no longer contains the exists-only pattern (a WorkflowRun.id filter on source_workflow_run_id without a user_id condition)
  </behavior>
  <action>
    In `backend/app/api/websocket.py`, add a module-level helper `_resolve_owned_parent_run_id(db, candidate_id, user_id)` (place it near the other module helpers around :374-415, matching their docstring/comment density). Semantics: return `candidate_id` iff `candidate_id` is truthy AND a `WorkflowRun` row exists with `WorkflowRun.id == candidate_id` AND `WorkflowRun.user_id == user_id` (D-06: key on `user_id` — the Phase-13 CR-01 precedent — NEVER the nullable backfilled `owner_id`); else return `None`. Docstring must state the POR §3 rationale: `parent_run_id` is an enforced FK (migration 0013) so a stale id would abort run creation, AND a foreign ROW linkage would let the family walk leak foreign run metadata — engine `assert_owns` already gates content, this gates linkage.

    Site 1 (`_handle_workflow_execution`, :1685-1695): replace the exists-only `_src` query block with `parent_run_id = _resolve_owned_parent_run_id(db, source_workflow_run_id, user.id)` (`user` is in scope — used at :1702). Keep/merge the existing degrade-gracefully comment.

    Site 2 (`_handle_revision_execution`, run_revision row creation :2247-2253): replace the exists-only ternary in the `WorkflowRun(...)` kwargs with `parent_run_id=_resolve_owned_parent_run_id(db, parent_run_id, user.id)`. Do NOT touch anything else in the row creation or the dispatch path — the engine still receives the caller's `parent_run_id` argument exactly as before; only the persisted ROW column is conditioned.

    New test file `backend/tests/unit/test_ws_parent_link_ownership.py`: copy the offline harness idiom from `test_run_revision_ws_dispatch.py` (in-memory SQLite StaticPool, model modules imported before `Base.metadata.create_all`, `monkeypatch.setattr(ws_module, "_get_db", ...)`, stub engine via `agents.execution_engine.engine.get_execution_engine`). Cover the three behavior groups above. For the site-1 source assertion use `inspect.getsource` — the same source-assertion idiom this codebase already uses (migration-0022 additive assertion in test_deliverable_mimetype.py); combined with the direct helper tests it transitively proves site 1 without driving the full pipeline handler. IMPORTANT (plan-checker tightening): pin the FULL call signature `_resolve_owned_parent_run_id(db, source_workflow_run_id, user.id)` in the assertion, not merely the helper name — site 1 is the primary run_pipeline IDOR path, so the exact `user.id` argument must be verified or a mis-wired user variable would pass.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_ws_parent_link_ownership.py tests/unit/test_run_revision_ws_dispatch.py -q</automated>
  </verify>
  <done>Both ingress sites route through the single ownership-checked helper; foreign source id → row NOT linked, owned → linked; run_revision dispatch untouched; all new + existing ws-dispatch tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parent_run_id + root_run_id response fields and GET /api/runs/{id}/family</name>
  <files>backend/app/api/runs.py, backend/tests/unit/test_runs_api_family.py</files>
  <behavior>
    - Family chain order: seed owned chain A→B→C with strictly increasing created_at; GET family on ANY member returns root_id=A and members [A,B,C] with revision_index 1,2,3 and each member's parent_run_id populated (revision-of-revision depth covered by the 3-deep chain)
    - Branching: A with two children B,C → 3 members ordered created_at ASC, both B and C carry parent_run_id=A (v-number is chronological, D2)
    - Orphan after parent delete: seed A→B, call the REAL DELETE /api/runs/A (the :334 child-nulling runs), then GET /api/runs/B/family → root_id=B, single member, revision_index 1
    - IDOR: attacker principal requests an owned run's family → 404 (never 403)
    - Foreign parent in chain: owned run C whose parent_run_id points at a foreign-owned row F → C/family → root_id=C; no F id/title/any F metadata anywhere in the response; GET /api/runs/C returns root_run_id=C
    - list+get fields: standalone run → parent_run_id null + root_run_id == own id on BOTH list and get; chained child → root_run_id == chain root on BOTH
  </behavior>
  <action>
    All in `backend/app/api/runs.py` (POR §4.2 + §4.3).

    1. `WorkflowRunResponse`: add `parent_run_id: Optional[str] = None` (verbatim column, resolved by from_attributes) and `root_run_id: str` (required — server-computed, NOT on the ORM row). Comment-tag them as the Workstream-A additive fields, mirroring the UXFIX-02 comment style at :102-104.

    2. Root computation helper `_compute_root_ids(db, user_id, runs) -> dict[str, str]` — the memoized in-Python walk with batched parent fetches (chosen over a recursive CTE for SQLite+Postgres portability, per the POR's implementation freedom). Build `parent_of` from the page rows; iteratively batch-fetch unknown ancestor ids with `.filter(WorkflowRun.id.in_(frontier), WorkflowRun.user_id == user_id)` selecting only (id, parent_run_id); ids NOT returned by the owned fetch are marked with a FOREIGN/missing sentinel (the walk terminator — root = last OWNED ancestor, POR §3.2). Then resolve each run's root by walking the in-memory map with a per-walk visited set (cycle guard: a revisit terminates at the current node) and memoize across the page. Latency invariant to preserve and state in the docstring: a page whose rows all have NULL parent_run_id issues ZERO additional queries — one extra query per chain-depth level otherwise.

    3. Response construction: because `model_config from_attributes` cannot supply the computed field, add `_run_response(run, root_id) -> WorkflowRunResponse` that builds the kwargs dict by iterating `WorkflowRunResponse.model_fields` with `getattr(run, name)` for every field except `root_run_id`, then injects `root_run_id=root_id`. This keeps `root_run_id: str` required (contract-strong) and auto-tracks future field additions without a hand-maintained field list. `list_runs` computes the roots dict once for the page and returns `[_run_response(r, roots[r.id]) for r in runs]`; `get_run` reuses `_compute_root_ids(db, current_user.id, [workflow_run])` for the single row.

    4. NEW endpoint `get_run_family` at `GET /{workflow_id}/family`, sync def, `response_model=RunFamilyResponse`. Add pydantic models `FamilyMemberResponse` (id, type, title, status, revision_index: int, parent_run_id: Optional[str], created_at: datetime, completed_at: Optional[datetime]) and `RunFamilyResponse` (root_id: str, members: list[FamilyMemberResponse]). Handler: (a) resolve the run with the standard `.filter(WorkflowRun.id == workflow_id, WorkflowRun.user_id == current_user.id)` owner filter — missing/cross-owner → 404 "Workflow run not found" (never 403, the runs.py:14-17 IDOR posture); (b) resolve the root via the owned walk (reuse `_compute_root_ids`); (c) collect members by BFS from the root over OWNED children only — iteratively `.filter(WorkflowRun.parent_run_id.in_(frontier), WorkflowRun.user_id == current_user.id)` with a visited-id set as the cycle guard (BFS-over-owned-children is exactly "all owned runs whose chain-root == root" because the owned walk terminates at any foreign link); (d) sort members by `(created_at ASC, id ASC)` — the id tiebreak makes same-timestamp ordering deterministic on SQLite; (e) `revision_index` = 1-based enumerate position.

    ROUTE PLACEMENT: declare `get_run_family` in the Phase-5 read-surface section beside `/artifacts` and `/events` (:669+). Shadowing note for the reviewer: the bare `GET /{workflow_id}` at :263 compiles to a single-segment Starlette path regex (path params never span "/"), so the two-segment `/{workflow_id}/family` cannot be shadowed regardless of declaration order — the already-working `/artifacts` + `/events` routes declared after :263 are the living proof, and the family tests below pin resolution (any 200 from /family proves no shadowing).

    NEW test file `backend/tests/unit/test_runs_api_family.py`: copy the fixture idiom from `test_runs_api_artifacts.py` verbatim (in-memory SQLite StaticPool, `_FakeUser`, `state["user"]` principal switch, dependency overrides for `get_current_user` + `get_db`). `from app.api.runs import router` already imports every child-table model, so `Base.metadata.create_all` covers the DELETE endpoint's 9 child-table cleanup queries — the orphan test can call the real DELETE. Extend the `_seed_run` helper with `parent_run_id=None` and `created_at` kwargs; seed strictly increasing created_at values so chronological ordering is deterministic. Cover every behavior group above.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_runs_api_family.py tests/unit/test_runs_api.py -q</automated>
  </verify>
  <done>list + get carry parent_run_id/root_run_id (standalone → own id); /family returns the ordered owned family with 1-based revision_index and per-member parent_run_id; cross-owner/missing → 404; foreign-parent chains terminate with zero foreign metadata leaked; existing runs-api tests untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: kind filter on GET /api/runs/{id}/artifacts + full gate battery</name>
  <files>backend/app/api/runs.py, backend/tests/unit/test_runs_api_artifacts.py</files>
  <behavior>
    - Seed A(kind=spec) + B(kind=design, derived_from=A) + C(kind=clarifications): ?kind=design → exactly one root node B with children==[] (A filtered out → B surfaces as a root — the :612-622 in-set parent restriction already guarantees this; this test PINS it)
    - ?kind=clarifications&include=content → the filtered node carries its content (composability)
    - ?kind=nonexistent → 200 with artifacts == [] (not 422)
    - No kind param → the pre-existing TestLineageTree/TestCrossOwnerDenied assertions all still pass UNCHANGED (byte-identical behavior proof — do not modify any existing test)
  </behavior>
  <action>
    In `get_run_artifacts` (runs.py:669): add `kind: Optional[str] = None` as a query param beside `include`. After `refs = await store.lineage(workflow_id)` and BEFORE `_build_lineage_tree`, apply `if kind is not None: refs = [r for r in refs if r.kind == kind]`. Exact-match equality only — no LIKE, no SQL (the value never reaches a query; ASVS V5 posture free). Unknown kind naturally yields an empty refs list → empty tree → NOT a 422. Update the endpoint docstring to document the param (D-8: precise fetches like `?kind=clarifications&include=content` instead of pulling the whole tree). With no param the code path is identical to today — byte-identical behavior by construction.

    Extend `backend/tests/unit/test_runs_api_artifacts.py` with a new `TestKindFilter` class covering the behavior group above, reusing the existing `_seed_run`/`_seed_ref` helpers. Do NOT edit any existing test — their unmodified pass is the no-param parity gate.

    Then run the FULL hard acceptance battery (non-negotiable, POR §4 Gates):
    1. Characterization goldens, SNAPSHOT_UPDATE unset: `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q`. KNOWN BASELINE: test_characterization_od_ppt has ONE pre-existing environmental failure on this machine (event-snapshot drift from a local skills example.html) — treat exactly that one failure as baseline; every other characterization test MUST pass. NEVER run the full backend pytest (hangs offline).
    2. `/opt/homebrew/bin/lint-imports` from `backend/` → 4 contracts kept / 0 broken.
    3. All touched unit suites: `python3.11 -m pytest tests/unit/test_runs_api_artifacts.py tests/unit/test_runs_api_family.py tests/unit/test_runs_api.py tests/unit/test_ws_parent_link_ownership.py tests/unit/test_run_revision_ws_dispatch.py tests/unit/test_runs_api_events.py -q`.
    4. Scope-fence self-check: `git status --porcelain` shows changes ONLY under backend/app/api/ + backend/tests/unit/ — zero `backend/agents/` edits, zero migration files, and `git diff` contains no new WS event type and no `source_run_id` reference.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && python3.11 -m pytest tests/unit/test_runs_api_artifacts.py tests/unit/test_runs_api_family.py tests/unit/test_runs_api.py tests/unit/test_ws_parent_link_ownership.py tests/unit/test_run_revision_ws_dispatch.py tests/unit/test_runs_api_events.py -q && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q; /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>kind filter live and composable; unknown kind → empty list; no-param behavior byte-identical (existing tests unmodified + green); characterization goldens byte/event-identical (od_ppt single pre-existing env failure excepted); lint-imports 4 kept / 0 broken; scope fence clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS client → server | `source_workflow_run_id` (run_pipeline) and `parent_run_id` (run_revision) are attacker-controlled run ids crossing here |
| HTTP client → /api/runs | `workflow_id` path param + `kind` query param on the new/extended read endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-A-01 | Info disclosure / Elevation | websocket.py both parent-link ingress sites | mitigate | `_resolve_owned_parent_run_id` links only when `WorkflowRun.user_id == user.id` (D-06); foreign id → row unlinked (Task 1) |
| T-A-02 | Info disclosure (IDOR) | runs.py `get_run_family` | mitigate | owner filter on the entry query AND on every BFS/walk step; 404-not-403 posture (Task 2) |
| T-A-03 | Info disclosure | `_compute_root_ids` ancestor walk | mitigate | every batched fetch filters `user_id == current_user.id`; unowned/missing ancestor = walk terminator, its metadata never enters any response (Task 2) |
| T-A-04 | DoS | cyclic `parent_run_id` chains in walk/BFS | mitigate | visited-set cycle guards in both the root walk and the family BFS (Task 2) |
| T-A-05 | Tampering | `kind` query param | mitigate | exact-match in-Python equality against already-scoped refs; value never reaches SQL (Task 3) |
| T-A-SC | Tampering | supply chain | accept | zero new packages installed in this plan — no npm/pip/cargo surface |
</threat_model>

<verification>
1. All targeted unit suites green: test_ws_parent_link_ownership, test_runs_api_family, test_runs_api_artifacts (incl. unmodified pre-existing tests), test_runs_api, test_run_revision_ws_dispatch, test_runs_api_events — `python3.11`, no venv.
2. INV-3 gate: the 5 characterization goldens byte/event-identical with SNAPSHOT_UPDATE unset (targeted suite only — full pytest hangs offline); the single od_ppt environmental failure is the accepted pre-existing baseline.
3. `/opt/homebrew/bin/lint-imports` → 4 contracts kept / 0 broken (app-layer-only change keeps this by construction, POR §10).
4. Scope fence: `git status --porcelain` shows NO paths under backend/agents/, NO files under backend/alembic|migrations, NO frontend paths; `git diff` introduces no new WS event type and no `source_run_id` usage.
</verification>

<success_criteria>
- Foreign `source_workflow_run_id`/`parent_run_id` can no longer create a cross-owner row linkage at either ingress site; owned linkage and run_revision dispatch behavior are unchanged (POR §3).
- `parent_run_id` + `root_run_id` returned on list AND get; standalone → root == own id; owned-walk semantics with foreign/missing-parent termination; no list-latency regression for NULL-parent pages (zero extra queries).
- `GET /api/runs/{id}/family` returns the chronologically ordered owned family with 1-based `revision_index` + per-member `parent_run_id` (branching-safe); 404 posture throughout.
- `GET /api/runs/{id}/artifacts?kind=X` filters before tree-building (filtered refs become roots), composes with `include=content`, unknown kind → empty list, no param → byte-identical.
- All hard gates green: goldens (od_ppt baseline excepted), lint-imports 4/0, targeted unit suites.
</success_criteria>

<output>
Create `.planning/quick/260702-s3p-workstream-a-revision-family-backend-rea/260702-s3p-SUMMARY.md` when done.
</output>
