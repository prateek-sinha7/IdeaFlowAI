---
phase: quick-260701-n4l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/agents/_scripted_model.py
  - backend/tests/agents/characterization/golden/prototype.events.json
  - backend/tests/agents/characterization/golden/od_prototype.events.json
autonomous: true
requirements: [KAN-86, SAFE-02, INV-3, INV-12]

must_haves:
  truths:
    - "The offline harness can drive the new prototype-analyze agent with a faithful <analysis>…</analysis> turn (no fallback stub)."
    - "test_prototype_event_snapshot passes against a regenerated 5-step golden."
    - "test_od_prototype_event_snapshot passes against a regenerated 5-step golden."
    - "The prototype + od_prototype BYTE goldens (.html) stay byte-identical (analyze is read-only/terminal)."
    - "The other 3 pipeline goldens (prototype_revision, od_ppt, app_builder) stay byte/event-identical (not regenerated)."
    - "The event-golden diff is ONLY the analyze-step addition (agent_count 4→5, new analyze agent block, index/total shifts); no review_gate/gate events, no byte-golden change."
  artifacts:
    - path: "backend/tests/agents/_scripted_model.py"
      provides: "A dedicated prototype-analyze branch in _scripts_for returning a fixed <analysis> report + fixed usage"
      contains: "prototype-analyze"
    - path: "backend/tests/agents/characterization/golden/prototype.events.json"
      provides: "Regenerated 5-step normalized event multiset for the prototype pipeline"
      contains: "prototype-analyze"
    - path: "backend/tests/agents/characterization/golden/od_prototype.events.json"
      provides: "Regenerated 5-step normalized event multiset for the od_prototype alias pipeline"
      contains: "prototype-analyze"
  key_links:
    - from: "backend/tests/agents/_scripted_model.py::_scripts_for"
      to: "prototype-analyze agent (order 3, tools:[], produces prototype-analyze)"
      via: "dedicated text-only script branch keyed on agent_id"
      pattern: "prototype-analyze"
    - from: "test_prototype_event_snapshot / test_od_prototype_event_snapshot"
      to: "golden/{prototype,od_prototype}.events.json"
      via: "_drive → _normalize → _canonical_order compared to load_events_golden"
      pattern: "load_events_golden"
---

<objective>
Complete KAN-86's loose end. The remote merged a 5th agent `prototype-analyze` (specify → plan → **analyze** → build → validate) into the `prototype` workflow, registry, and AGENT.md, but updated NO `tests/agents/` files. Result: the offline characterization harness (`_scripted_model.py`) has zero references to `prototype-analyze` (it currently drives it only via the generic fallback stub), and the `prototype` + `od_prototype` EVENT goldens still encode the stale 4-step stream — so `test_prototype_event_snapshot` and `test_od_prototype_event_snapshot` fail (actual stream now emits `pipeline_start.agent_count: 5` + a `prototype-analyze` agent block vs the 4-step golden).

This is an INTENTIONAL upstream change. The sanctioned fix (SAFE-02 golden-regen convention): teach the harness the new agent with a FAITHFUL `<analysis>` turn, THEN regenerate ONLY those 2 goldens under `SNAPSHOT_UPDATE=1` and review the diff.

Purpose: restore the offline characterization safety net to green after an intentional workflow addition, without weakening INV-3 (byte/event parity for everything the change did not touch).
Output: a dedicated `prototype-analyze` branch in `_scripts_for`; regenerated `golden/{prototype,od_prototype}.events.json`; a captured, reviewed event diff proving the change is ONLY the analyze-step addition.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./backend/CLAUDE.md
@.planning/IMPLEMENTATION-REGISTER.md

# The new agent (text-only, tools:[], gate:Human_Gate, order 3, produces <analysis>…</analysis>, consumes specify+plan; NOTHING consumes it → read-only/terminal)
@backend/agents/prompts/prototype-analyze/AGENT.md

# The harness to extend (add a branch to _scripts_for in place; do NOT fork _drive)
@backend/tests/agents/_scripted_model.py

# How SNAPSHOT_UPDATE + the event goldens work
@backend/tests/agents/test_characterization_prototype.py
@backend/tests/agents/test_characterization_od_prototype.py
@backend/tests/agents/characterization/__init__.py
@backend/tests/agents/characterization/_normalize.py
</context>

<facts_verified_during_planning>
These were confirmed against the current tree — the executor should not re-derive them, but MUST reconcile the actual regen diff against the "expected diff" below.

- Registry `PIPELINE_AGENTS["prototype"]` is already `[specify, plan, analyze, build, validate]` (analyze at index 2 / order 3). `agents/workflows/prototype/workflow.yaml` already has the `prototype-analyze` step (`strategy: single_shot`, `gates: [human]`). No production/registry/manifest edits are needed — this is harness + goldens ONLY.
- `_scripted_model.py` currently has 0 references to `prototype-analyze`; it is NOT in the `TEXT_ONLY` set and has no branch, so it hits the generic fallback (`"{agent_id} default output."`). The run already produces 5 steps today — it is the GOLDEN that is stale, not the drive path.
- `prototype-build` `consumes: [prototype-plan]` (+ reads spec via task_source `spec_step: prototype-specify`). NOTHING consumes `prototype-analyze`. The prototype deliverable is read from the RunSandbox disk (`prototype.html`), never from analyze's streamed text — so the `.html` byte goldens are unaffected by analyze content.
- Gates are SUPPRESSED in the harness (`_drive` passes `gate_agent_ids=[]` and monkeypatches `_run_review_gate` to a no-op). The existing prototype golden has **0** `review_gate` events even though specify + plan both declare `gate: Human_Gate`. `gate_status` is emitted ONCE at the planner level (verdict), NOT per-agent. → adding analyze (also Human_Gate) adds NO gate/review_gate/gate_status events. The `<the_fix>` prediction of "analyze's Human_Gate events (review_gate_*)" is therefore WRONG; do not expect them.
- Normalization strips `seq` and `event_id`, and the golden is an order-canonical MULTISET (`_canonical_order` sorts by canonical JSON). So "seq renumbering" will NOT appear in the diff. `index` and `total` ARE kept in `agent_start`/`agent_complete` and WILL shift.
- The existing 4-step golden already contains 5 `agent_start` / 5 `agent_complete` / 5 `agent_input` events (the build agent runs once per task → build appears twice). With analyze added the counts become 6 / 6 / 6.
- Chromium is present locally (render_check runs for real).
</facts_verified_during_planning>

<tasks>

<task type="auto">
  <name>Task 1: Add a faithful prototype-analyze branch to _scripts_for and confirm it drives offline</name>
  <files>backend/tests/agents/_scripted_model.py</files>
  <action>
In `_scripts_for(agent_id)` in `backend/tests/agents/_scripted_model.py`, add a DEDICATED branch for `agent_id == "prototype-analyze"` (INV-12: extend the existing function in place; do NOT add it to the generic `TEXT_ONLY` set — mirror the `prototype-plan` special-case pattern so the golden captures a realistic report rather than "output line one."). Place the branch near the other prototype agent branches (after `prototype-build` / `prototype-validate` is a natural home) so it reads as a family group.

The branch returns a single `_ScriptedTurn` whose text is a small, FIXED `<analysis>…</analysis>` report that mirrors the AGENT.md ABSOLUTE OUTPUT CONTRACT: the very first characters are `<analysis>` and the text ends with `</analysis>`; include a `## Spec Kit Analysis Report` heading, a `### Summary` sentence, a couple of finding lines, and a `### Readiness verdict` line reading `READY TO BUILD`. Give it a fixed `usage=(…, …)` tuple (e.g. `(28, 16)`) so token normalization stays deterministic. Keep the text short and deterministic — its exact bytes become the pinned `output_length` and it is consumed by nothing downstream (analyze is terminal), so keep it stable.

Do NOT change `_drive`, the `_OD_ALIAS_FOR_LOOKUP` map, the `od_context` seeding, or any other branch. This turn's content is READ-ONLY/terminal: nothing consumes `prototype-analyze`, and the prototype deliverable is the on-disk `prototype.html` — so the analyze text MUST NOT (and does not) alter the built artifact.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -c "from tests.agents._scripted_model import _scripts_for; t=_scripts_for('prototype-analyze'); assert len(t)==1, t; txt=''.join(t[0].texts); assert txt.startswith('&lt;analysis&gt;') and txt.rstrip().endswith('&lt;/analysis&gt;'), repr(txt[:40]); assert 'READY TO BUILD' in txt; assert t[0].usage is not None; print('prototype-analyze branch OK')"</automated>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_characterization_prototype.py::test_prototype_deliverable_byte_snapshot tests/agents/test_characterization_od_prototype.py::test_od_prototype_deliverable_byte_snapshot -q</automated>
  </verify>
  <done>
`_scripts_for("prototype-analyze")` returns exactly one turn whose text starts with `<analysis>`, ends with `</analysis>`, contains `READY TO BUILD`, and carries a fixed usage tuple. Both prototype/od_prototype BYTE-snapshot tests still PASS (deliverable unchanged — analyze is terminal), proving the harness drives the 5-agent pipeline end-to-end offline without altering the artifact. No production/registry/manifest file changed; only `_scripted_model.py` is modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate ONLY the prototype + od_prototype event goldens, capture the diff, and verify scope</name>
  <files>backend/tests/agents/characterization/golden/prototype.events.json, backend/tests/agents/characterization/golden/od_prototype.events.json</files>
  <action>
Regenerate ONLY the two stale event goldens with the deliberate-regeneration flag (SAFE-02): run the two event-snapshot tests with `SNAPSHOT_UPDATE=1`, which makes `write_events_golden` overwrite `golden/prototype.events.json` and `golden/od_prototype.events.json`. Run ONLY these two tests under the flag — do NOT run the whole characterization suite under `SNAPSHOT_UPDATE=1` (that would regenerate the 3 out-of-scope goldens). Command: `SNAPSHOT_UPDATE=1 python3.11 -m pytest tests/agents/test_characterization_prototype.py::test_prototype_event_snapshot tests/agents/test_characterization_od_prototype.py::test_od_prototype_event_snapshot -q` from `backend/`.

Then CAPTURE and reconcile the diff (`git diff -- tests/agents/characterization/golden/prototype.events.json tests/agents/characterization/golden/od_prototype.events.json`) against the EXPECTED DIFF below. Summarize which event blocks/keys were added/changed into the SUMMARY so the orchestrator can confirm it is ONLY the analyze-step addition.

EXPECTED DIFF (normalized, order-canonical multiset; `seq`/`event_id` are stripped so they never appear):
  - `pipeline_start`: `agent_count` 4→5, and a `prototype-analyze` object (icon/id/name/order 3/role) inserted into the `agents` list.
  - NEW `agent_start` for `prototype-analyze` (index 2, total 5).
  - NEW `agent_complete` for `prototype-analyze` (index 2, total 5, a fixed `output_length` = length of the `<analysis>` text; tokens are `<normalized>`).
  - NEW `agent_input` for `prototype-analyze` (its `context_message`, built from the consumed specify+plan outputs).
  - NEW `agent_chunk` event(s) for `prototype-analyze` (chunk value normalized to `<normalized>`; count = number of text pieces in the turn).
  - Existing `agent_start`/`agent_complete` `total` 4→5 everywhere; `prototype-build` index 2→3 (two blocks each), `prototype-validate` index 3→4; specify(0)/plan(1) indices unchanged.
  - NO changes to `tool_call`, `tool_result`, `task_progress`, `task_loop_progress`, `gate_status`, `planner_start`, `planner_complete`, `pipeline_complete`, `workflow_validated`.

STOP-AND-INVESTIGATE tripwires (do NOT blind-accept):
  - Any `review_gate_*` / new `gate_status` event appears → UNEXPECTED (gates are suppressed in the harness; specify+plan Human_Gate produce none).
  - Either `.html` BYTE golden changes (`git diff` non-empty on `golden/prototype.html` or `golden/od_prototype.html`) → UNEXPECTED (analyze is read-only/terminal); do not regenerate them.
  - Any of the 3 out-of-scope goldens (`prototype_revision.events.json`, `od_ppt.events.json`, `app_builder.events.json`, or their `.html`) shows a diff → UNEXPECTED; revert it (they were not regenerated).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_nav_coverage.py tests/agents/test_route_table.py -q</automated>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin &amp;&amp; git diff --stat -- backend/tests/agents/characterization/golden/prototype.html backend/tests/agents/characterization/golden/od_prototype.html backend/tests/agents/characterization/golden/prototype_revision.events.json backend/tests/agents/characterization/golden/od_ppt.events.json backend/tests/agents/characterization/golden/app_builder.events.json | (! grep .) &amp;&amp; echo "OUT-OF-SCOPE GOLDENS UNCHANGED"</automated>
    <automated>/opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>
`golden/prototype.events.json` and `golden/od_prototype.events.json` are regenerated to the 5-step stream. The offline verify suite (2 regenerated + prototype_revision + app_builder + nav_coverage + route_table) all PASS with NO `SNAPSHOT_UPDATE` set (real assertions). `git diff` is EMPTY on both `.html` byte goldens and on all 3 out-of-scope pipeline goldens. `lint-imports` reports 4 contracts kept / 0 broken. The captured event diff — recorded in the SUMMARY — is ONLY the analyze-step addition (agent_count 4→5, new analyze blocks, index/total shifts); no gate/review_gate events, no byte-golden change. `od_ppt` remains its known pre-existing environmental failure (out of scope, untouched — not in the verify suite).
  </done>
</task>

</tasks>

<verification>
Offline only (full pytest hangs on Chromium/Bedrock/Postgres gates — see the offline-test-suite memory note). Run from `backend/`:

- `python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_app_builder.py tests/agents/test_nav_coverage.py tests/agents/test_route_table.py -q` → ALL PASS, with NO `SNAPSHOT_UPDATE` in the verify run.
- `/opt/homebrew/bin/lint-imports` → 4 kept / 0 broken.
- `git diff` empty on both `.html` byte goldens and on the 3 out-of-scope pipeline goldens.
- The captured event diff is ONLY the analyze-step addition (no gate/review_gate events; no byte change).
</verification>

<success_criteria>
- `test_prototype_event_snapshot` and `test_od_prototype_event_snapshot` PASS against the regenerated 5-step goldens.
- `prototype.html` + `od_prototype.html` byte goldens byte-identical (unchanged).
- `prototype_revision` + `app_builder` byte/event goldens unchanged (git diff empty).
- `od_ppt` untouched (remains its known environmental failure; excluded from the verify suite).
- The event-golden diff is ONLY the analyze-step addition, captured/summarized in the SUMMARY for orchestrator review.
- The nav_coverage + route_table validator tests still green.
- INV-12 honored: `_scripts_for` extended in place; no production/registry/manifest change; no migration.
</success_criteria>

<output>
Create `.planning/quick/260701-n4l-complete-kan-86-loose-end-add-prototype-/260701-n4l-SUMMARY.md` when done. MUST include the captured, reconciled event-golden diff summary (which event blocks/keys were added/changed) and explicit confirmation that: (1) the byte goldens are unchanged, (2) the 3 out-of-scope goldens are unchanged, and (3) no gate/review_gate events appeared.
</output>
