---
phase: quick-260813-3wo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/run_stream.py
  - backend/tests/unit/test_sse_stream.py
  - frontend/e2e/fixtures/mockSse.ts
  - frontend/e2e/tests/ts-sse-resilience.spec.ts
autonomous: true
requirements: [SSE-REPLAY-IDENTITY]
must_haves:
  truths:
    - "A durable-replay SSE frame for an app-layer event type that persisted NO identity inside its payload (chat_reply / chat_message / chat_usage / run_resuming) is served with the row's real `event_id` and `seq` COLUMNS merged into `data` — the same projection `getRunEvents` already applies at `frontend/src/lib/api.ts:583-590`."
    - "The columns WIN over any same-named payload key, so for engine-authored events (whose payload already carries matching values, stamped at `engine.py:1085-1086`) the merge is a provable no-op."
    - "Exactly ONE line of production code changes: `backend/app/api/run_stream.py:202`. No other line of that file, no line of `run_commands.py`, and NO frontend production file (`useRunChat.ts`, `useRunStream.ts`, `wsReplayState.ts`, `api.ts`) is touched."
    - "The new backend test is observed RED against the UNFIXED code and GREEN after — both pytest outputs pasted verbatim into SUMMARY.md, never inferred."
    - "The pre-existing exact-body assertion at `test_sse_stream.py:180` is RECONCILED (asserts the full merged dict against the row's real persisted event_id, read back from the DB), never deleted, never loosened to a subset check without a stated reason."
    - "The whole of `tests/unit/test_sse_stream.py` is run before AND after the fix, in isolation, with exact pass/fail counts reported both times (baseline: 43 passed, 0 failed)."
    - "The new mocked-e2e test is observed RED (two `Delivered` cards) against a mock that faithfully models TODAY's pre-fix wire, and GREEN (exactly one card) once the mock's replay path mirrors the shipped server fix — both Playwright outputs pasted verbatim."
    - "The final committed `mockSse.ts` models the POST-fix wire and contains NO dead toggle, NO unused simulation flag, and no second implementation of the replay-serving path."
    - "Characterization goldens are re-run with a reported pass count and `git status` proves ZERO golden fixture files were modified or regenerated."
    - "`lint-imports` is run FROM `backend/` (running it elsewhere silently false-passes) and REAL contract output is shown — expected 4 kept / 0 broken."
    - "The live proof is run twice (`DROP=1` and `DROP=0`, fresh unique markers) and BOTH printed `LIVE LANE COUNTS` lines are pasted into SUMMARY.md showing `delivered:1`, `openInPreview:1`, `runStarted:1`, `clarAnswered:1`."
    - "SUMMARY.md records the out-of-scope `useRunStream.ts:301` / ADR-0001 finding verbatim so a later bookkeeping pass can file it as its own issue — no code or test for it lands here."
    - "No FIX/TEST/ISS number appears in any commit message, code comment, or SUMMARY.md line."
  artifacts:
    - path: "backend/app/api/run_stream.py"
      provides: "Durable-replay projection that merges the row's authoritative event_id/seq columns over payload_json"
      contains: "\"event_id\": r.event_id"
    - path: "backend/tests/unit/test_sse_stream.py"
      provides: "RED-before/GREEN-after proof that a replayed identity-less chat_reply row is served WITH its column identity, plus the reconciled line-180 exact-body assertion"
      contains: "chat_reply"
    - path: "frontend/e2e/fixtures/mockSse.ts"
      provides: "Wire-faithful SSE mock: replay-catch-up serving + the durable REST twin (GET /api/runs/{id}/events) the completion backfill reads"
      contains: "events"
    - path: "frontend/e2e/tests/ts-sse-resilience.spec.ts"
      provides: "Mounted-app browser proof that one narrator milestone renders exactly ONE chat result card across a terminal-close reconnect + completion backfill"
      contains: "chat-result-card"
  key_links:
    - from: "backend/app/api/run_stream.py:202 (durable replay step 1)"
      to: "frontend/src/hooks/useRunChat.ts:359-399 upsertNarratorMessage"
      via: "the replayed frame's data.event_id becomes the assistant message id, matching the copy delivered by getRunEvents instead of falling back to `chat-reply:${message_id}`"
      pattern: "\\{\\*\\*\\(r\\.payload_json or \\{\\}\\), \"event_id\": r\\.event_id, \"seq\": r\\.seq\\}"
    - from: "backend/app/api/run_stream.py:202"
      to: "frontend/src/lib/api.ts:583-590 getRunEvents"
      via: "both projections now merge the authoritative event_id/seq COLUMNS over payload_json, so the SSE twin and the REST twin key identically"
      pattern: "row\\.event_id"
---

<objective>
One narrator milestone card ("Delivered — open in Preview") renders TWICE in the run chat
lane after a mid-run SSE reconnect. Fix the single server-side line that causes it, prove it
RED-then-GREEN at the unit layer and at the mounted-browser layer, and prove it live.

Purpose: the durable-replay step of the per-run SSE stream throws away the row's identity
columns, so the four app-layer event types that do NOT embed identity inside their payload
arrive at the frontend anonymous. The frontend's keying logic is already correct — it simply
never received the key.

Output: a one-line projection fix in `run_stream.py`, a new backend replay test, a
wire-faithful correction + new browser test in the mocked e2e harness, and a live
double-run proof.
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@backend/CLAUDE.md

Source files (read only the ranges you need — do NOT re-read a range already in context):
@backend/app/api/run_stream.py
@backend/tests/unit/test_sse_stream.py
@frontend/e2e/fixtures/mockSse.ts
@frontend/e2e/tests/ts-sse-resilience.spec.ts
</context>

<verified_facts>

Everything below was read directly from source during planning. Treat it as given — do NOT
re-derive it, and do NOT re-read these ranges unless you are editing them.

## The defect, end to end

1. `backend/app/api/run_stream.py:202` (durable replay, step 1 of `_iter_sse_frames`):
   `yield _sse_frame(r.seq, r.type, r.payload_json)` — emits `payload_json` RAW, discarding
   the row's `event_id` / `seq` columns. (Note line 204 immediately after it already reads
   `r.event_id` for the C-06a dedup set, so the column is right there.)
2. `backend/app/agents/chat_narrator.py:308-322` (`persist_milestone_card`) persists the
   narrator card with `payload_json = card`, and `project_milestone_card` builds `card` as
   ONLY `{pipeline_run_id, message_id, card_kind, text, deep_link}` — no `event_id`, no `seq`.
   So for this row the columns are the ONLY identity that exists.
3. `backend/app/agents/chat_narrator.py:260-284` (`_reply_event_id`) mints the durable
   `event_id` as `f"chat_reply:{source_event_id}"` (UNDERSCORE, keyed on the SOURCE milestone
   event's uuid).
4. `frontend/src/hooks/useRunChat.ts:359-399` (`upsertNarratorMessage`) keys the assistant
   bubble on `data.event_id` when truthy, ELSE falls back to `` `chat-reply:${data.message_id}` ``
   (HYPHEN), then `findIndex(m => m.id === id)` → update in place, or APPEND.
   The narrator's real key can never be reproduced by that fallback → two different ids →
   two bubbles.
5. NARRATOR-ONLY: `backend/app/api/run_commands.py:1687-1696` writes the Concierge's own
   durable chat_reply row with `event_id=f"chat-reply:{body.message_id}"` — byte-identical to
   the FE fallback — so a Concierge reply replayed anonymously still collapses correctly.

## The exact production duplicate mechanism (verified, complete)

- `run_stream.py:79` → `_STREAM_TERMINAL_TYPES = REVIEW_RESOLUTIONS - {review_gate_approved}`,
  and `agents/capabilities/gate_pendency.py:53-62` shows `REVIEW_RESOLUTIONS` INCLUDES
  `pipeline_complete`. So `run_stream.py:281-282` **returns the moment `pipeline_complete` is
  drained** — the live drain ends there.
- `agents/execution_engine/engine.py:1103-1132` yields the narrator card AFTER the milestone
  event it was projected from. For `pipeline_complete` that means the card is queued when the
  live drain has ALREADY returned → **the "Delivered" card is never delivered live on that
  connection.**
- `frontend/src/hooks/useRunStream.ts:301` marks only `pipeline_cancelled` / `pipeline_failed`
  as non-live, so a `pipeline_complete` close leaves `sawNonLiveAttachRef === false` →
  line 415-420 `scheduleReconnect()` → reattach carrying
  `Last-Event-ID: <pipeline_complete's seq>` (line 351-352).
- The server replays `seq > completeSeq` — i.e. the card row — through line 202, so PRE-FIX it
  arrives with no `event_id` AND no `seq`. Copy #1 renders keyed `chat-reply:{message_id}`,
  and because it carries no `seq` the chat hook's last-seq cursor does not advance past it.
- `frontend/src/app/dashboard/page.tsx:1009` (the FIX-172/176 completion backfill) then runs
  `getRunEvents(tok, runId, getRunChatLastSeq())`, and `frontend/src/lib/api.ts:583-590`
  merges the row's real `event_id`/`seq` COLUMNS. Copy #2 renders keyed
  `chat_reply:{source_event_id}` → **two cards.**

## Blast radius (counted, not assumed)

`_sse_frame` (defined `run_stream.py:106-113`) has 6 call sites: `run_stream.py:202, 220, 258,
279` and `run_commands.py:1660, 1796, 1821`.
- `202` — the ONLY site to change.
- `220` — synthesized `stream_attached` handshake, not a DB row. Leave alone.
- `258` — the D-14g gate re-arm's deliberate re-emission of an already-seen row. Explicitly
  OUT OF SCOPE. Leave alone.
- `279` — live-queue drain; the `event` dict already carries `event_id` from whoever queued
  it. Leave alone.
- The `run_commands.py` sites belong to the Concierge's one-shot streamed-POST reply body
  (`POST .../messages`, a different response object entirely) and their dict literals already
  embed `event_id` explicitly (e.g. `run_commands.py:1813`). Leave alone.

## Harness facts

- `backend/tests/unit/test_sse_stream.py` is 1252 lines. Measured baseline on this branch:
  **43 passed, 0 failed.**
- `_seed_events(db, rows, *, run_id, owner_id, workspace_id)` at lines 122-137 takes
  `[(seq, type, payload), ...]` and assigns each row a RANDOM `uuid.uuid4()` as `event_id`.
  It has many call sites in this same file — do not break its signature.
- The exact-body assertion to reconcile is `test_sse_stream.py:180`:
  `assert replay[0]["data"] == {"seq": 2, "text": "hi"}` inside
  `TestReplay::test_replay_from_cursor_projects_seq_frames`.
- `frontend/e2e/fixtures/mockSse.ts`: `emit()` (169-176) stamps a FRESH `event_id`/`seq` into
  `frame.data` at creation and stores that object in `this.frames` forever; `framesAfter()`
  (408-411) filters by `seq > cursor`; `serialize()` (433-449) writes
  `id: {seq}\r\ndata: {"type","data"}\r\n\r\n`; `handleStream()` (534-593) long-polls an empty
  tail and never ships a `TERMINAL_TYPES` frame (110: pipeline_complete / _failed / _cancelled)
  in the same response as preceding frames. The mock therefore ALWAYS produces the POST-fix
  wire and cannot currently reproduce the bug.
- The mock does NOT serve `GET /api/runs/{id}/events` (the REST durable twin). `mockSse.route()`
  falls it through to `mockApi`, which does not implement it either (`mockApi.ts:641` only has
  `/gate-events`). So today's mocked suite cannot exercise the completion-backfill path at all.
- `frontend/src/components/chat/ResultCard.tsx:148-149,185-186` renders
  `data-testid="chat-result-card"` + `data-card-kind={kind}`. That is the ONE selector
  convention — do not invent another.
- `ts-sse-resilience.spec.ts` has four existing tests (99-264); ALL set `mockSse.autoAttach =
  false` and drive a spec-local `fetchSse()` helper (40-75). None mount the app. Leave all four
  byte-identical.
- **A confirmed exhaustive search of `frontend/e2e/` found ZERO tests combining a mounted app +
  a `chat_reply` + a reconnect + a chat-lane DOM assertion.** This is a real coverage gap.

## Planning finding you MUST heed (it overrides the naive test sketch)

The obvious construction — mount, `chatReply()`, `mockSse.drop()`, assert one card — **cannot
go RED, ever.** `useRunStream.ts:258-273` advances the cursor from `data.seq` (falling back to
the `id:` line) on EVERY dispatched block, so once the card has been processed the reconnect
carries `Last-Event-ID >= cardSeq` and `framesAfter(cursor)` excludes it. There is no replay,
so there is no second copy. Do not spend time on that shape. Build the verified
terminal-close + completion-backfill mechanism above instead (Task 4/5 spell it out).

## Standing constraints

- Branch `bugfix/spec-revision-context-loss`, clean at `f87d1880`. Do NOT create a branch.
- **NEVER `git stash`** — gsd executors sweep the whole dirty tree and a swept file has been
  lost here before.
- **NEVER regenerate a golden.**
- Do NOT hardcode any FIX / TEST / ISS number anywhere. A separate bookkeeping pass allocates
  real ids from four sources afterwards and must not collide with a guess.
- Commit idiom on this branch (from `git log --oneline -15`): `test(quick-260813-1b1): … (RED)`
  → `fix(quick-260813-1b1): …` → `docs(quick-260813-1b1): …`. Use scope `quick-260813-3wo`.
- **HARD MONEY RULE: never launch, resume, or approve any gate on any `od_prototype` run.**
  One build is 5-21M Bedrock tokens of the owner's money. Live work in this plan is
  `user_stories` ONLY, launched via the "Generate product requirements" button. Never click any
  header/sidebar run pill — it opens the paused `od_prototype` run
  `41f77342-b3c4-42ee-bf9c-215209dfc2aa`. Exact accessible names / test ids only.
- Do NOT gate anything on overall vitest / mocked-e2e suite colour: there is a large
  pre-existing unrelated red baseline on this branch (vitest 147 failed / 894 passed; mocked
  e2e 34 failed). Baselines are recorded at
  `/private/tmp/claude-502/-Users-1000060523-Documents-Work-UKI-Flowin-flowin/e1aacc4e-8507-40a3-9195-e65d4c37a595/scratchpad/baseline-fails.txt`
  (34 `file:line` entries) and `…/baseline-e2e.txt`. Compare failing test NAMES against that
  list, not colour.
- **Warning:** `ts-chat-cards.spec.ts` (31, 57, 75) and `ts-chat.spec.ts` (34, 61, 77, 113) are
  ALREADY in that red baseline. Copy their mount idiom only after confirming it works; if the
  idiom itself is broken on this branch, take the mount pattern from a spec that is green.

</verified_facts>

<tasks>

<task type="auto">
  <name>Task 1: RED backend test — a replayed identity-less chat_reply row loses its event_id</name>
  <files>backend/tests/unit/test_sse_stream.py</files>
  <action>
    First measure the baseline so the RED is attributable. From `backend/`, run the whole file
    in isolation and record the exact counts:
    `AWS_PROFILE=hex-uki AWS_REGION=eu-central-1 ENV=development ANTHROPIC_API_KEY= RUNS_ROOT=/tmp/flowin-runs DATABASE_URL="sqlite:///./dev.db" python3.11 -m pytest tests/unit/test_sse_stream.py -q`
    (expect 43 passed, 0 failed — if it differs, STOP and report before writing anything).

    Add ONE new test, in `TestReplay` or a clearly-named new class, that pins the durable-replay
    projection for an app-layer row that persisted NO identity inside its payload:
      - `_seed_run(db_session)` then `_seed_events` ONE row: seq 2, type `chat_reply`, payload
        `{"pipeline_run_id": "run-1", "message_id": "m1", "card_kind": "deliverable", "text": "Delivered"}`
        — exactly what `chat_narrator.persist_milestone_card` actually persists (no event_id,
        no seq inside).
      - Learn that row's REAL `event_id` column. `_seed_events` mints a random uuid4, so either
        query it back (`db_session.query(RunEvent).filter_by(run_id="run-1", seq=2).one().event_id`)
        or extend `_seed_events` minimally to accept an optional explicit event_id — your call,
        but keep the diff small and do NOT break any of its other call sites in this 1252-line
        file. Never hardcode a literal uuid: it changes every run.
      - Replay through the injected generator:
        `_iter_sse_frames(run_id="run-1", store=_store(db_session), after_seq=1, live_queue=None)`
        collected with the file's existing `_collect` / `_parse` helpers.
      - Assert the parsed non-`stream_attached` frame's `data` CONTAINS `event_id` equal to the
        row's real column value AND `seq` equal to 2 — and that the original payload keys
        (`message_id`, `card_kind`, `text`) survive the merge unchanged.

    Do NOT touch `run_stream.py` in this task. Do NOT touch line 180 yet.
  </action>
  <verify>
    <automated>cd backend && AWS_PROFILE=hex-uki AWS_REGION=eu-central-1 ENV=development ANTHROPIC_API_KEY= RUNS_ROOT=/tmp/flowin-runs DATABASE_URL="sqlite:///./dev.db" python3.11 -m pytest tests/unit/test_sse_stream.py -q</automated>
  </verify>
  <done>
    The new test FAILS with `event_id` simply absent from `data` (not an error, not a collection
    failure), the other 43 still pass, and the verbatim pytest output is captured for SUMMARY.md.
    Committed alone as `test(quick-260813-3wo): durable SSE replay must carry the row's event_id (RED)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: The one-line fix + reconcile the exact-body assertion at line 180</name>
  <files>backend/app/api/run_stream.py, backend/tests/unit/test_sse_stream.py</files>
  <action>
    In `backend/app/api/run_stream.py`, inside `_iter_sse_frames` step 1, replace line 202
    `yield _sse_frame(r.seq, r.type, r.payload_json)` with:
    `yield _sse_frame(r.seq, r.type, {**(r.payload_json or {}), "event_id": r.event_id, "seq": r.seq})`

    Add a short comment above it stating WHY: the columns are authoritative and win over any
    same-named payload key, mirroring the REST twin `frontend/src/lib/api.ts:583-590`; for
    engine-authored rows (identity stamped into the payload at `engine.py:1085-1086`) the merge
    is a no-op, so only the four app-layer types that persist no embedded identity
    (`chat_reply`, `chat_message`, `chat_usage`, `run_resuming`) are actually repaired.
    Do not reference any FIX/ISS number.

    Change NOTHING else in this file — not line 220, not 258, not 279 — and nothing in
    `run_commands.py`, and no frontend production file.

    Then reconcile `test_sse_stream.py:180`. That row's replayed frame now also carries its real
    (random, per-run) `event_id`. Read the row's actual persisted `event_id` back from the DB and
    assert the FULL merged dict, e.g.
    `assert replay[0]["data"] == {"seq": 2, "text": "hi", "event_id": <real value>}`.
    Do NOT delete the assertion, do NOT hardcode a uuid literal, and do NOT downgrade it to a
    subset/`in` check unless you state the reason in SUMMARY.md.

    Re-run the WHOLE file in isolation and read the output — do not assume the far-later
    fan-out/pump tests (well past line 800) are unaffected. If ANY other test moves from pass to
    fail, stop and report it rather than adjusting it silently.
  </action>
  <verify>
    <automated>cd backend && AWS_PROFILE=hex-uki AWS_REGION=eu-central-1 ENV=development ANTHROPIC_API_KEY= RUNS_ROOT=/tmp/flowin-runs DATABASE_URL="sqlite:///./dev.db" python3.11 -m pytest tests/unit/test_sse_stream.py -q</automated>
  </verify>
  <done>
    44 passed, 0 failed (43 baseline + the new test), verbatim output captured. `git diff` on
    `run_stream.py` shows exactly one changed yield line plus its comment. Committed as
    `fix(quick-260813-3wo): merge the durable row's event_id/seq onto replayed SSE frames`
    together with the line-180 reconciliation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-measure the standing backend gates with numbers</name>
  <files>(no edits — measurement only)</files>
  <action>
    Characterization goldens: locate the right invocation (likely
    `python3.11 -m pytest tests/agents/characterization/ -q` from `backend/`, with the same env
    prefix as Task 1) and run it. Report the pass count. Then run `git status --porcelain` and
    `git diff --stat` and confirm ZERO golden fixture files are modified. If any golden differs,
    STOP — do not regenerate it, report the byte-level diff instead.

    `lint-imports`: run `/opt/homebrew/bin/lint-imports` FROM the `backend/` directory
    specifically. Running it from anywhere else silently false-passes by printing "Could not
    read any configuration" — so confirm the output contains real contract lines. Expected
    4 kept / 0 broken; treat that as the last known-good baseline on this branch and report the
    actual numbers, don't assume them.
  </action>
  <verify>
    <automated>cd backend && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>
    Golden pass count reported, `git status` proves no golden file changed, and lint-imports
    printed real contract output at 4 kept / 0 broken. Nothing to commit.
  </done>
</task>

<task type="auto">
  <name>Task 4: Make mockSse model the real wire — replay-catch-up serving + the durable REST twin</name>
  <files>frontend/e2e/fixtures/mockSse.ts</files>
  <action>
    Two targeted corrections so the harness can express the mechanism in `<verified_facts>`.
    Ground both in the mock's OWN existing bookkeeping; do not invent a parallel structure.

    (a) **Replay-catch-up awareness.** `handleStream()` already computes `this.framesAfter(cursor)`
    at line 558 to decide whether to long-poll. Capture that first result (the frames that were
    ALREADY pending before this invocation started waiting) and treat exactly those as REPLAY
    frames for this response; anything that arrived later (i.e. woke a parked long-poll) is a
    LIVE frame. Then, in `serialize()`, for a REPLAY frame whose type is one of the four
    app-layer types (`chat_reply`, `chat_message`, `chat_usage`, `run_resuming`), OMIT
    `event_id` and `seq` from the served inner `data` — modelling TODAY's pre-fix server. Keep
    the `id: {seq}` line exactly as it is: the real backend always emits it from `r.seq`; only
    the inner payload lacked identity.

    (b) **The durable REST twin.** Add a `GET /api/runs/{id}/events?after=N` handler to
    `mockSse.route()` returning `{ events: [...] }`, where each event is the durable-row shape
    `getRunEvents` consumes: `{ type, event_id, seq, payload_json }` with `payload_json` being
    the frame's data MINUS `event_id`/`seq` (that is exactly how the server stores an app-layer
    row) and the identity carried in the columns — so `frontend/src/lib/api.ts:583-590` merges
    them back. Filter by `seq > after`. The mock already owns the durable tail (`this.frames`),
    so this is a small addition, and it is the reason this whole bug class was invisible to the
    mocked suite: the completion-backfill path (`page.tsx:1009`) has never been exercisable.

    **Blast-radius rule (b is the risky half):** this route is currently unhandled, so today
    `getRunEvents` fails and the completion backfill is a silent no-op in ~100 specs. Before
    going further, run the SSE/chat spec files and compare the failing test list against
    `…/scratchpad/baseline-fails.txt`. If enabling it globally moves ANY test from pass to fail,
    make it opt-in via a public flag that the new spec in Task 5 sets (an opt-in that a test
    actually uses is fine; an unused flag is not). Record which option you chose and the
    evidence in SUMMARY.md.

    Correction (a) is a TEMPORARY state that exists only to prove the Task 5 test is not
    vacuous — Task 6 removes it. Say so in a comment so a reader of the RED commit is not misled.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <done>
    `mockSse.ts` typechecks; the SSE/chat spec files run with a failing-test list identical to
    the recorded 34-entry baseline (or the REST twin is opt-in and the list is identical with it
    off). Both the before and after failing lists are captured. Not committed yet — Task 5
    commits this together with the RED spec.
  </done>
</task>

<task type="auto">
  <name>Task 5: New mounted-app e2e proving the duplicate — observed RED (two cards)</name>
  <files>frontend/e2e/tests/ts-sse-resilience.spec.ts</files>
  <action>
    Add ONE new `test(...)` block to `ts-sse-resilience.spec.ts`. Do not modify the four existing
    tests (99-264) — they set `autoAttach = false` deliberately and test a different contract.

    Build the VERIFIED mechanism, not the naive drop-after-card shape (see the planning finding
    in `<verified_facts>` — that shape cannot go red):
      1. Mount the real app on a live run. Leave `autoAttach` at its default `true` so
         `ensureAttached()` fires `online`, the real `RunConnectionProvider` / `useRunStream` /
         `useRunChat` attach, and every later frame is processed. Take the mount idiom from
         `ts-chat-cards.spec.ts:24-28` or `ts-chat.spec.ts:27-31` — but note BOTH files are in
         the 34-failure red baseline, so first confirm the idiom actually mounts here; if it
         does not, use a green spec's pattern instead. Wait on
         `page.getByTestId("run-chat-lane")` being visible before proceeding.
      2. Drive the run to `pipeline_complete`. The mock ships a `TERMINAL_TYPES` frame alone
         (mockSse.ts:572-579), so the app commits the terminal state and — per
         `useRunStream.ts:301` omitting `pipeline_complete` — the stream close schedules a
         reconnect carrying `Last-Event-ID = completeSeq`.
      3. THEN emit the narrator deliverable card, e.g.
         `mockSse.chatReply({ cardKind: "deliverable", text: "<unique marker>" })`, so its seq is
         `completeSeq + 1`. It is pending before the next attach, so Task 4(a) serves it as a
         REPLAY — identity stripped, exactly like today's server.
      4. The app's completion backfill (`page.tsx:1009` → `getRunEvents`) fetches the same row
         from the Task 4(b) REST twin WITH its identity columns → a second, differently-keyed
         copy.
      5. Assert the reconnect actually happened (`mockSse.connectionCount` increased, or the
         equivalent convention already used in this file).
      6. Final assertion: EXACTLY ONE `page.getByTestId("chat-result-card")` with
         `data-card-kind="deliverable"`.

    Use `expect.poll` / web-first assertions with a bounded timeout rather than blind
    `waitForTimeout` where possible; if you do need a fixed wait, mirror how
    `TS-SSE-RESILIENCE-03` sequences its own `drop()` at line 198.

    Run it and OBSERVE the RED: it must fail showing TWO cards. If after a bounded effort you
    cannot produce two cards, **STOP and report** with what you observed at each step (which
    copy is missing and why) — do NOT make the mock serve frames at or below the client cursor
    to force it, and do NOT weaken the assertion. A test that cannot be seen failing is not
    trusted here.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/tests/ts-sse-resilience.spec.ts --reporter=line</automated>
  </verify>
  <done>
    The new test fails on the final assertion with a received count of 2 (the four existing
    TS-SSE-RESILIENCE tests still pass), and the verbatim Playwright output is captured.
    Committed with the Task 4 mock work as
    `test(quick-260813-3wo): a replayed identity-less narrator card renders twice (RED)`.
  </done>
</task>

<task type="auto">
  <name>Task 6: Mirror the server fix in the mock's replay path — observed GREEN (one card)</name>
  <files>frontend/e2e/fixtures/mockSse.ts</files>
  <action>
    Remove the Task 4(a) identity-stripping so the mock's replay-serving path mirrors the shipped
    `run_stream.py:202` projection: a replayed frame carries the row's `event_id`/`seq` merged
    into `data`, exactly as the fixed server now does. Replace the temporary comment with a
    permanent one that names the invariant and points at `backend/app/api/run_stream.py` as the
    server-side guarantee this mock models.

    **The final committed `mockSse.ts` must contain no dead toggle and no second implementation
    of the replay path.** If the net remaining diff for part (a) is only a comment, that is the
    correct outcome — do NOT fabricate changes to make the diff look larger. Part (b) (the REST
    twin) stays: it is the permanent fidelity gain.

    Re-run the new test — it must now pass with exactly ONE card. Then re-run the SSE/chat spec
    files and confirm the failing-test list still matches the recorded baseline (no test moved
    from pass to fail).
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/tests/ts-sse-resilience.spec.ts --reporter=line</automated>
  </verify>
  <done>
    The new test passes (one `chat-result-card[data-card-kind="deliverable"]`), all four existing
    TS-SSE-RESILIENCE tests still pass, the SSE/chat failing list is unchanged versus
    `baseline-fails.txt`, and both the RED and GREEN outputs are captured verbatim. Committed as
    `fix(quick-260813-3wo): mock the fixed replay projection in the SSE e2e harness`.
  </done>
</task>

<task type="auto">
  <name>Task 7: Live proof on the running stack (twice) + SUMMARY.md</name>
  <files>.planning/quick/260813-3wo-fix-sse-durable-replay-dropping-event-id/260813-3wo-SUMMARY.md</files>
  <action>
    FIRST verify whether the already-running backend on `http://127.0.0.1:8010` picked up the
    `run_stream.py` edit — this repo's local uvicorn runs WITHOUT `--reload`, so assume it did
    NOT until proven. Check the running process's start time / command line against the edit
    time. If a restart is needed, stop it and bring it back with EXACTLY this command from
    `backend/`, backgrounded (e.g. `nohup … &`):
    `AWS_PROFILE=hex-uki AWS_REGION=eu-central-1 ENV=development ANTHROPIC_API_KEY= RUNS_ROOT=/tmp/flowin-runs DATABASE_URL="sqlite:///./dev.db" python3.11 -m uvicorn app.main:app --host 127.0.0.1 --port 8010 --timeout-graceful-shutdown 5`
    Confirm it answers before proceeding, and NEVER leave it down. Do not rebuild or restart the
    frontend on `http://localhost:3000`.

    READ both
    `/private/tmp/claude-502/-Users-1000060523-Documents-Work-UKI-Flowin-flowin/e1aacc4e-8507-40a3-9195-e65d4c37a595/scratchpad/20-live-control.mjs`
    and its sibling `lib.mjs` BEFORE running anything — they already encode this project's
    safety rules (unique-marker matching, terminal-status waiting, `user_stories` ONLY via the
    "Generate product requirements" button). Then run it twice with FRESH markers never used
    before:
      `MARK=FIXPROOF260813A DROP=1 node <script path>`
      `MARK=FIXPROOF260813B DROP=0 node <script path>`

    HARD MONEY RULE applies for the whole task: `user_stories` only; never launch, resume or
    approve a gate on any `od_prototype` run; never click a header/sidebar run pill.

    Both runs must print a `LIVE LANE COUNTS` line with `delivered:1` and `openInPreview:1`
    (NOT 2), with `runStarted:1` and `clarAnswered:1`. Paste BOTH lines verbatim. If either
    shows 2, the fix is not proven — report it as a failure, do not rationalise it.

    Then write SUMMARY.md containing: the one-line change and why it works; every pytest and
    Playwright RED/GREEN output captured in Tasks 1-6; the golden + lint-imports numbers from
    Task 3; both live `LIVE LANE COUNTS` lines; the Task 4(b) opt-in-vs-global decision and its
    evidence; and — verbatim, as an explicit OUT-OF-SCOPE finding for a later bookkeeping pass
    to file as its own new issue:

      `frontend/src/hooks/useRunStream.ts:301` — `if (type === "pipeline_cancelled" || type ===
      "pipeline_failed")` omits `pipeline_complete` from the terminal-frame list that ADR-0001
      (`.knowledge/cards/ADR-0001.md`) governs. ADR-0001's own "Negative consequence" predicts
      exactly this: "the terminal-frame list is maintained by hand. A new terminal frame type
      that is not added to that branch reintroduces the banner, and the symptom will look like a
      UI bug rather than a dispatcher omission." No code or test for it lands in this change.

    No FIX/TEST/ISS numbers anywhere in SUMMARY.md or the commit message.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8010/docs</automated>
  </verify>
  <done>
    Backend confirmed serving the fixed code; both live invocations printed `delivered:1` and
    `openInPreview:1` with `runStarted:1` / `clarAnswered:1`, and both lines are pasted into
    SUMMARY.md; SUMMARY.md also carries every captured test output, the gate numbers, and the
    verbatim out-of-scope ADR-0001 note. Committed as
    `docs(quick-260813-3wo): record the SSE replay identity fix and its live proof`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → SSE client | `GET /api/runs/{id}/events/stream` response body crosses to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3wo-01 | Information disclosure | `run_stream.py:202` now surfaces the `event_id` + `seq` COLUMNS in the replayed frame body | accept | No new exposure: the rows are already owner+workspace scoped by `ScopedStore` (a cross-owner run is a 404 before the body starts), and the REST twin `GET /api/runs/{id}/events` has surfaced these same two columns to the same principal since BUG-018 (`api.ts:583-590`). The values are an opaque uuid/monotonic int carrying no user data. |
| T-3wo-02 | Tampering | payload key shadowing — a payload containing its own `event_id`/`seq` | mitigate | The merge order puts the COLUMNS last, so the authoritative DB columns always win over payload-supplied values; a poisoned payload cannot forge a client-visible identity. |
| T-3wo-03 | Denial of service | replay projection cost | accept | The change is a per-row dict merge inside a loop that already reads `r.event_id` on the next line (line 204's dedup set); no extra query, no extra retained object — the `rows = ()` / `r = None` release at 212-213 is untouched. |
| T-3wo-SC | Tampering | package installs | mitigate | No new npm/pip/cargo package is installed by this plan. If any task finds it needs one, STOP and report — installs are out of scope here. |
</threat_model>

<verification>
- `tests/unit/test_sse_stream.py` in isolation: 43 passed / 0 failed before, 44 passed / 0 failed
  after, both outputs pasted.
- The new backend test seen RED (event_id absent) before the fix and GREEN after.
- `git diff` on `backend/app/api/run_stream.py` touches exactly one yield line plus its comment.
- `git diff --stat` shows zero changes under any golden fixture directory.
- `lint-imports` run from `backend/`, real contract output, 4 kept / 0 broken.
- The new e2e test seen RED (2 cards) then GREEN (1 card), both outputs pasted; the mocked
  SSE/chat failing-test list unchanged versus the recorded 34-entry baseline.
- Two live `user_stories` runs (DROP=1 and DROP=0) both printing `delivered:1`,
  `openInPreview:1`, `runStarted:1`, `clarAnswered:1`.
- No frontend production file modified; `run_commands.py` untouched; `run_stream.py` lines 220,
  258 and 279 untouched.
</verification>

<success_criteria>
The narrator's milestone card renders exactly once after a mid-run SSE reconnect, on the real
running stack, proven twice with fresh markers — and the same invariant is pinned by a backend
unit test and a mounted-browser e2e test that were each observed failing first.
</success_criteria>

<output>
Create `.planning/quick/260813-3wo-fix-sse-durable-replay-dropping-event-id/260813-3wo-SUMMARY.md` when done.
</output>
