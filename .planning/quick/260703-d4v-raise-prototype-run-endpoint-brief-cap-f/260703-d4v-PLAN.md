---
phase: quick-260703-d4v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/prototype_templates.py
  - backend/tests/unit/test_prototype_run_request.py
autonomous: true
requirements: [QUICK-260703-d4v]

must_haves:
  truths:
    - "POST /api/prototype/run accepts a brief longer than 8000 chars (up to 450,000) without a 422 / pydantic ValidationError"
    - "An empty brief is still rejected (min_length=1 unchanged)"
    - "The RunRequest.brief cap tracks settings.BRIEF_MAX_CHARS (no new magic number)"
    - "Briefs <= 8000 chars behave byte-identically (INV-3): the 5 characterization goldens are unaffected"
  artifacts:
    - path: "backend/app/api/prototype_templates.py"
      provides: "RunRequest.brief cap sourced from settings.BRIEF_MAX_CHARS; imports settings"
      contains: "max_length=settings.BRIEF_MAX_CHARS"
    - path: "backend/tests/unit/test_prototype_run_request.py"
      provides: "Validation test: brief >8000 accepted, empty rejected, cap == settings.BRIEF_MAX_CHARS"
      contains: "settings.BRIEF_MAX_CHARS"
  key_links:
    - from: "backend/app/api/prototype_templates.py"
      to: "app.core.config.settings"
      via: "from app.core.config import settings"
      pattern: "from app\\.core\\.config import settings"
    - from: "backend/app/api/prototype_templates.py::RunRequest.brief"
      to: "settings.BRIEF_MAX_CHARS"
      via: "Field max_length reference"
      pattern: "max_length=settings\\.BRIEF_MAX_CHARS"
---

<objective>
Raise the `POST /api/prototype/run` request-body brief cap from a hardcoded `8000` to the app-wide `settings.BRIEF_MAX_CHARS` constant (450,000 chars ~= 150k tokens).

Purpose: The `8000` at `backend/app/api/prototype_templates.py:217` is a legacy Phase-4 OpenDesign straggler. When the app-wide brief ceiling was raised to 450k (`settings.BRIEF_MAX_CHARS`, commit f351d446) and the planner / clarify / upload-ingest paths were wired to it, this endpoint was missed — so briefs >8000 chars get a 422 here while working everywhere else.

Output: One import + one Field cap change in `prototype_templates.py`, plus a focused Pydantic-model validation test. Backend-only, additive/permissive (INV-3 preserved).
</objective>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@backend/CLAUDE.md

# The file to change (RunRequest at line 214-221; brief cap at line 217)
@backend/app/api/prototype_templates.py

# The exact import form to reuse (line 25)
@backend/app/api/file_extract.py

# The constant to reference (line 119)
@backend/app/core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source the RunRequest brief cap from settings.BRIEF_MAX_CHARS</name>
  <files>backend/app/api/prototype_templates.py</files>
  <action>
Two edits in `backend/app/api/prototype_templates.py`, nothing else in the file:

1. Add the import `from app.core.config import settings`. The module does NOT currently import it. Reuse the EXACT form used at `backend/app/api/file_extract.py:25`. Place it in the `from app.*` import group respecting existing alphabetical ordering — `config` sorts before `dependencies`, so it goes immediately before the existing `from app.core.dependencies import get_current_user` (currently line 23).

2. In the `RunRequest` model (line 214), change the `brief` field cap on line 217 from `max_length=8000` to `max_length=settings.BRIEF_MAX_CHARS`. Keep `min_length=1` unchanged. This yields 450,000 chars — matches the established app-wide pattern (no new magic number, self-maintaining).

DO NOT TOUCH:
- `backend/agents/execution_engine/od_context.py:170` `get_example_html(..., max_chars=8000)` — UNRELATED (caps the template's example.html asset injected into the prompt, NOT the user brief; coincidental 8000). Leave it entirely.
- `backend/app/core/config.py` — `BRIEF_MAX_CHARS` is already 450k; correct as-is.
- No frontend change: no `maxLength` exists on the prototype discovery / DiscoveryForm brief input (already confirmed by grep). SECONDARY / low priority: quickly audit the prototype brief-entry FE for any `maxLength` that would still cap input client-side; if one is found, align it to a comparably large value. Do NOT invent FE changes if no `maxLength` exists — the backend Pydantic cap is the hard 422 gate and is THE essential fix.
  </action>
  <verify>
    <automated>cd backend && grep -q 'from app.core.config import settings' app/api/prototype_templates.py && grep -q 'max_length=settings.BRIEF_MAX_CHARS' app/api/prototype_templates.py && ! grep -q 'max_length=8000' app/api/prototype_templates.py && python3.11 -c "from app.api.prototype_templates import RunRequest; print(RunRequest.model_fields['brief'].metadata)"</automated>
  </verify>
  <done>`prototype_templates.py` imports `settings` (alphabetically ordered in the app.* group), the `RunRequest.brief` cap reads `max_length=settings.BRIEF_MAX_CHARS`, no `max_length=8000` remains in the file, and the module imports cleanly. `od_context.py:170` and `config.py` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add RunRequest brief-cap validation test + run offline gates</name>
  <files>backend/tests/unit/test_prototype_run_request.py</files>
  <action>
Create `backend/tests/unit/test_prototype_run_request.py` — a direct Pydantic-model validation test (no live pipeline needed). No existing test pins the old 8000 cap (the only backend `8000` refs are agent `max_tokens=8000` and `od_context.py` `max_chars=8000` — both unrelated), so no existing test needs editing.

Test cases against `from app.api.prototype_templates import RunRequest`:
- A brief of length 9000 (e.g. `"x" * 9000`) constructs OK: `RunRequest(template_id="t", design_system_id="d", brief="x" * 9000)` does NOT raise `pydantic.ValidationError`, and `.brief` round-trips length 9000.
- An empty brief still rejects: `RunRequest(template_id="t", design_system_id="d", brief="")` raises `pydantic.ValidationError` (min_length=1).
- The cap tracks the constant, not a literal: assert the effective `max_length` on the `brief` field equals `settings.BRIEF_MAX_CHARS` (import `from app.core.config import settings`). Read the constraint off `RunRequest.model_fields["brief"].metadata` (the `MaxLen` constraint) so the test fails if anyone reverts to a hardcoded literal. Include a boundary assertion that a brief at exactly `settings.BRIEF_MAX_CHARS` length constructs OK.

After the test is written, run the offline verification gates (full backend pytest hangs offline — Chromium/Bedrock/Postgres-gated — so run the TARGETED suite only):
- The new validation test.
- The 5 characterization goldens (byte/event-identical, INV-3): `test_characterization_prototype.py`, `test_characterization_od_prototype.py`, `test_characterization_prototype_revision.py`, `test_characterization_od_ppt.py`, `test_characterization_app_builder.py` — run WITHOUT `SNAPSHOT_UPDATE`.
- `lint-imports` at `/opt/homebrew/bin/lint-imports` stays green (adding `from app.core.config import settings` into an `app/api` module is the established pattern per `file_extract.py:25` — no §31 hexagonal boundary violation).
- Confirm `git status --porcelain` scope is limited to `backend/app/api/prototype_templates.py` + this new test file.

Runtime: `python3.11`, no venv.
  </action>
  <verify>
    <automated>cd backend && python3.11 -m pytest tests/unit/test_prototype_run_request.py -q && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py tests/agents/test_characterization_prototype_revision.py tests/agents/test_characterization_od_ppt.py tests/agents/test_characterization_app_builder.py -q && /opt/homebrew/bin/lint-imports</automated>
  </verify>
  <done>New test passes (brief >8000 accepted, empty rejected, cap == `settings.BRIEF_MAX_CHARS`, boundary length OK). The 5 characterization goldens pass byte/event-identical with NO snapshot update (INV-3). `lint-imports` green. `git status --porcelain` shows only `backend/app/api/prototype_templates.py` + `backend/tests/unit/test_prototype_run_request.py`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /api/prototype/run | Authenticated user submits a `brief` string; `RunRequest` (Pydantic) is the validation gate before the pipeline runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d4v-01 | Denial of Service | `RunRequest.brief` cap raised 8000 -> 450,000 | accept | Cap stays BOUNDED at the app-wide `settings.BRIEF_MAX_CHARS` (450k) — the same ceiling every other brief-ingest endpoint (planner / clarify / upload) already enforces; no new/unbounded surface. Downstream planner head+tail samples / clarify head-caps beyond the ceiling (config.py:117). Auth (`Depends(get_current_user)`) still gates the route. |
| T-d4v-SC | Tampering | package installs | accept | No package installs in this plan (edit + test only); no dependency surface changed. |
</threat_model>

<verification>
- `grep` confirms `from app.core.config import settings` present and `max_length=settings.BRIEF_MAX_CHARS` present in `prototype_templates.py`; no `max_length=8000` remains.
- `backend/agents/execution_engine/od_context.py:170` (`max_chars=8000`) and `backend/app/core/config.py` are UNCHANGED (`git diff --stat` shows only the two intended files).
- New `tests/unit/test_prototype_run_request.py` passes.
- INV-3: 5 characterization goldens byte/event-identical (no snapshot update).
- `lint-imports` green (§31 hexagonal boundaries intact).
</verification>

<success_criteria>
- `POST /api/prototype/run` accepts briefs up to 450,000 chars (no 422 at 9000 chars); empty brief still 422.
- The brief cap is sourced from `settings.BRIEF_MAX_CHARS` — no hardcoded literal.
- Change is additive/permissive: briefs <= 8000 behave byte-identically (INV-3).
- Diff scope limited to `backend/app/api/prototype_templates.py` + `backend/tests/unit/test_prototype_run_request.py`.
- Commit on branch `new-workflow-engine` (never `main`), scope `fix(api)` or `fix(config)` (per the f351d446 brief-cap precedent).
</success_criteria>

<output>
Create `.planning/quick/260703-d4v-raise-prototype-run-endpoint-brief-cap-f/260703-d4v-SUMMARY.md` when done.
</output>
