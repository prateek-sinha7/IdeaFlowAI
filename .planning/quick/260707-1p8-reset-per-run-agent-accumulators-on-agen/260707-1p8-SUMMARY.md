---
phase: quick-260707-1p8
plan: 01
subsystem: frontend/workflow-dashboard
tags: [frontend, useWorkflow, regenerate, agent_start, FIX-039]
requires: []
provides: ["agent_start run-scoped accumulator reset (regenerate replaces, not appends)"]
affects: [frontend/src/hooks/useWorkflow.ts]
tech-stack:
  added: []
  patterns: ["run-scoped field reset on (re)start; replay-safety via upstream shouldApplyEvent dedup"]
key-files:
  created:
    - frontend/src/hooks/useWorkflow.regenerateReset.test.ts
  modified:
    - frontend/src/hooks/useWorkflow.ts
    - .planning/FIX-REGISTER.md
decisions:
  - "Reset run-scoped AgentRunState fields in the agent_start case (replace-not-append); rejected the parseTasks last-<tasks>-block band-aid."
metrics:
  duration: ~10m
  completed: 2026-07-07
  tasks: 2
  files: 3
---

# Phase quick-260707-1p8 Plan 01: Reset per-run agent accumulators on agent_start Summary

FIX-039: `case "agent_start"` in `frontend/src/hooks/useWorkflow.ts` now resets the (re)starting agent's run-scoped accumulators (`output`, `thinking`, `thinkingText`, `toolCalls`, `validationIssues`, `error`, `validationPassed`, plus overwrite-only duration/token/inputPrompt/contextSources) BEFORE flipping to `status:"running"`, so a regenerate REPLACES the streamed output instead of appending onto the stale prior run — a regenerated 9-task plan now reads "9 tasks", not the stale "1 Build Task". Identity fields (`id/name/role/icon/index`), pipeline-level state, and other agents are untouched.

## What was done

- **Task 1 (TDD):** Wrote `useWorkflow.regenerateReset.test.ts` (5 cases) first — RED confirmed (2 failing: replace + thinkingText reset). Then implemented the run-scoped reset in the `agent_start` case — GREEN.
- **Task 2:** Ran exit gates, appended FIX-039 to `.planning/FIX-REGISTER.md` (matching the FIX-038 row format), committed code + doc together in one trailer-free commit.

## Test cases (all green)

1. Genuine re-start (new event_id) REPLACES output — exact equality on the 2nd chunk, no "Task 1: X" residue, `## Task \d+` count === 2.
2. `thinkingText` resets to "" on re-start.
3. Replay-safety: a replayed `agent_start` (same event_id) is dropped by `shouldApplyEvent` → handler never called → live output preserved.
4. First-run reset is a no-op; identity fields unchanged.
5. Cross-agent + pipeline-level isolation (restarting A leaves B's output and `completedCount` untouched).

## Deviations from Plan

None - plan executed exactly as written. Optional/recommended overwrite-only field resets (duration/token/input) were applied for full re-run parity as the plan recommended.

## Gate outputs

- `vitest run useWorkflow`: 3 files, 12 tests passed (incl. the new file).
- `tsc -p tsconfig.json --noEmit`: 0 errors (no new errors; no refs to changed files).
- `git status --porcelain` (post-commit): only the untracked plan dir (orchestrator-owned); commit changed exactly `useWorkflow.ts`, `useWorkflow.regenerateReset.test.ts`, `FIX-REGISTER.md`.

## Commit

- `650b0064` — fix(workflow): reset per-run agent accumulators on agent_start so regenerate replaces (not appends) stale output (trailer-free)

## Self-Check: PASSED

- FOUND: frontend/src/hooks/useWorkflow.regenerateReset.test.ts
- FOUND: frontend/src/hooks/useWorkflow.ts (modified)
- FOUND: .planning/FIX-REGISTER.md (FIX-039 row)
- FOUND: commit 650b0064
