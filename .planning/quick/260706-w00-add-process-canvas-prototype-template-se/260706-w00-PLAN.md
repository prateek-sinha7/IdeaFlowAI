---
phase: quick-260706-w00
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/opendesign/design-templates/process-canvas/example.html
  - skills/opendesign/design-templates/process-canvas/SKILL.md
autonomous: true
requirements: ["260706-w00"]
must_haves:
  truths:
    - "od_loader.list_prototype_templates() includes id 'process-canvas' (dir-scan, zero code change)"
    - "example.html renders fully self-contained — no external network of any kind (no http(s), googleapis, gstatic, support.js, image-slot.js, .dc.html)"
    - "example.html contains no DC-framework syntax (no <x-dc>, <helmet>, <sc-if>, <sc-for>, {{ }}, data-dc-script, data-props, data-screen-label, hint-placeholder-*)"
    - "All four screens (Overview / Connections / Recommendation / Sign-off) switch via the sidebar in vanilla JS"
    - "Recommendation sub-tabs (Process / Gate / Simulate) switch, and the version segmented control (baseline / v1 / v2) swaps the process map"
    - "Swimlane node -> detail drill works: clicking a step opens/closes its detail panel (master->detail) in vanilla JS"
    - "Gate step-through works: next / prev / auto-play (setInterval) / reset advance the 3 criteria"
    - "Simulate scan animation runs (setInterval over 40 cells) and the sign/unsign toggle flips state"
    - "Naming is fully generic — zero claims-domain or product-specific vocabulary remains"
  artifacts:
    - path: "skills/opendesign/design-templates/process-canvas/example.html"
      provides: "Self-contained flattened prototype: sidebar + 4 screens + swimlane map + drill + gate + sim + sign-off, one inline vanilla-JS controller"
      contains: "data-od-id"
      max_chars: 110000
    - path: "skills/opendesign/design-templates/process-canvas/SKILL.md"
      provides: "Template frontmatter (od.mode: prototype) + build-agent body, mirroring web-prototype/SKILL.md schema"
      contains: "mode: prototype"
  key_links:
    - from: "skills/opendesign/design-templates/process-canvas/SKILL.md"
      to: "od_loader.list_prototype_templates"
      via: "od.mode == 'prototype' frontmatter picked up by dir-scan"
      pattern: "mode:\\s*prototype"
    - from: "skills/opendesign/design-templates/process-canvas/example.html"
      to: "in-file state controller"
      via: "single inline <script> wiring nav/tabs/version/drill/gate/sim/sign via addEventListener + setInterval"
      pattern: "addEventListener|setInterval"
---

<objective>
Add ONE new prototype template — `process-canvas` — to Flowin's EXISTING OpenDesign template
system, mirroring the ~111 templates already under `skills/opendesign/design-templates/`.
The loader (`backend/app/services/od_loader.py::list_prototype_templates`) dir-scans that
directory, so a new folder containing a self-contained `example.html` + a `SKILL.md` with
`od.mode: prototype` is picked up automatically with ZERO engine/loader/config edits.

The source `~/Downloads/Claims Recovery POC.dc.html` is a DC-framework file (`<x-dc>`,
`<sc-if>`, `<sc-for>`, `{{ }}`, `class Component extends DCLogic`, `support.js`) that renders
ONLY under the DC runtime — so it is FLATTENED into one self-contained plain-HTML file with an
inline vanilla-JS controller, and its claims-recovery domain naming is genericized into a
reusable process-workflow template. The look (colors, layout, SVGs, animations, interactions)
is preserved byte-for-look; only words/labels and the runtime mechanism change.

Purpose: Ship a reusable "process canvas" prototype template through the platform's existing,
zero-code template mechanism — HARD RULE "not a single thing different": introduce no new
code, mechanism, config, or design-system/craft file.
Output: Exactly TWO new files in ONE new folder — nothing else.
</objective>

<constraints>
DO NOT edit any code or config: `od_loader.py`, `prototype_templates.py`, `opendesign.py`,
`od_context.py`, `registry.py`, any `workflow.yaml`, any frontend, any test, any guardrail. NO
new mechanism. NO other template modified. NO design-system/craft file added. The ONLY writes
allowed are the two files under `skills/opendesign/design-templates/process-canvas/`. If
anything beyond those two files would be needed to satisfy a requirement -> STOP and report.

FONT CONVENTION DECISION (locked): The two named convention models —
`web-prototype/example.html` and `github-dashboard/example.html` — both use a SYSTEM font stack
with NO external `<link>`. Per the source-of-truth rule ("follow whatever they do; if system
fonts, do NOT add an external font link"), process-canvas uses a system font stack and adds NO
font link. Consequence: gate #2 expects ZERO matches — there is NO legitimate font link.
</constraints>

<execution_context>
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
# Source to flatten (DC-framework, 980 lines / ~99.5 KB — READ FULLY FIRST)
@/Users/1000060523/Downloads/Claims Recovery POC.dc.html

# Convention models — mirror these exactly (frontmatter schema + self-contained structure + system-font convention)
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/skills/opendesign/design-templates/web-prototype/SKILL.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/skills/opendesign/design-templates/web-prototype/example.html
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/skills/opendesign/design-templates/github-dashboard/SKILL.md
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/skills/opendesign/design-templates/github-dashboard/example.html

# Loader that dir-scans and consumes these files (READ-ONLY — do not edit)
@/Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend/app/services/od_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten the DC source into a self-contained, generic process-canvas/example.html</name>
  <files>skills/opendesign/design-templates/process-canvas/example.html</files>
  <action>
Read `~/Downloads/Claims Recovery POC.dc.html` end-to-end (all 980 lines, including the
`class Component extends DCLogic` controller at lines 745-977) before writing. Produce ONE
self-contained HTML file. Two mechanical transforms + one genericize pass; NEVER touch colors,
layout, spacing, radii, shadows, SVGs, or interaction semantics.

1) REMOVE ALL DC RUNTIME + EXTERNAL NETWORK. Delete `<script src="./support.js">`, the `<x-dc>`
   wrapper, the `<helmet data-dc-atomics>` block, the three Google-Fonts `<link>`s (preconnect
   googleapis, preconnect gstatic, IBM Plex css2), every `<sc-if>` / `<sc-for>`, every `{{ }}`
   binding, `data-dc-script`, the `data-props` JSON, `data-screen-label`, and every
   `hint-placeholder-*` attribute. Keep the inline `<style>` block (the four `@keyframes`
   acPulse / acFadeUp / acFade / acDash and all their `animation:` usages MUST survive verbatim).
   Replace the two font-families — `'IBM Plex Sans', system-ui, sans-serif` ->
   `system-ui, -apple-system, "Segoe UI", sans-serif`, and `'IBM Plex Mono', monospace` ->
   `ui-monospace, "SF Mono", Menlo, monospace` — everywhere they appear. NO external `<link>`
   of any kind (locked font decision above). Final file must contain zero `http(s)://`.

2) RE-AUTHOR THE CONTROLLER AS INLINE VANILLA JS. Convert `renderVals()` + the DCLogic
   state/handlers/timers into ONE inline `<script>` at the end of `<body>`: a plain `state`
   object `{ view:'home', recoTab:'process', version:'v2', selectedStepId:null, gateStep:0,
   simState:'idle', scanIndex:0, signed:false }`, plus render function(s) that repaint from
   state and `addEventListener` (or inline `onclick`) wiring. It MUST reproduce, behavior-for-
   behavior: (a) sidebar screen-switch across the 4 views; (b) Recommendation sub-tabs
   process/gate/simulate (`goTab`); (c) version segmented control baseline/v1/v2 (`setVersion`)
   swapping which nodes show + routing color exactly as `decorate()` / the `d*` flags do;
   (d) DRILLED NAVIGATION — clicking a swimlane node toggles `selectedStepId` and opens the
   master->detail step panel (title, kind badge, system tags, description, and the `change`
   note) with `closeStep`; the empty-selection state when none is selected (this drill MUST be
   present and functional); (e) gate walkthrough `gateNext`/`gatePrev`/`gateReset` and
   auto-play via `setInterval(..., 1150)` capped at step 3, with the Auto-play/Pause/Replay
   label logic and the reveal of 0..gateStep criteria; (f) simulate `simRun` via
   `setInterval(..., 34)` advancing `scanIndex` 0->40 over the 40-cell grid with caught set
   {4,11,18,25,31,37} and the idle/running/done cell colors + status/labels, plus `simReset`;
   (g) sign/unsign toggle `doSign`/`doUnsign`. Keep the exact inline styles the DC `renderVals`
   emitted (nav style, segmented style, tab style, step card/diamond style, sim cell style) —
   port them as JS style strings, do not restyle. Do NOT use `eval`, `fetch`, timers hitting
   the network, or any external asset.

3) GENERICIZE NAMING (design unchanged — change ONLY words/labels). Strip every claims/product
   term and re-label into a reusable process-workflow. Apply this mapping (extend the same way
   for any missed occurrence): "Scout" -> a generic app name (e.g. "Flowline"); "Automated
   Consultant" -> "Process Studio"; "ContextFabric" (the mono eyebrow) -> "PROCESS CANVAS";
   sidebar nav "Workflow twins" -> "Connections", "Recommendation"/"Sign-off"/"Home" stay
   (already generic, rename Home -> "Overview" if desired); the 5 systems Guidewire/Outlook/
   InspectPro/ArbFile/Xactimate + their roles (claim summary/adjuster correspondence/inspection/
   subrogation & arbitration/loss valuation) -> generic connectors with generic 2-letter codes
   (e.g. System of record / Messaging / Field data / Case tracker / Valuation); the 10 process
   steps + 3 lanes -> a generic Intake -> Assess -> Capture/flag -> Route -> Resolve -> Close
   process (keep IDENTICAL topology, x/y/w, shapes, kinds new/updated/unchanged, and system
   tags — only rename titles/descriptions/change-notes); the baseline/v1/v2 caption copy ->
   generic "current state / interim iteration / approved future state (a decision step moved
   upstream)"; the 3 gate criteria (third-party FNOL / paint-transfer / $9,480 valuation) ->
   3 generic decision criteria all resolving HOLDS; the sample case "MM-2214788 · Rear-end
   collision" -> a generic work item (e.g. "Case #A-4821 · Sample work item"); "40 open claims /
   6 matched" -> "40 open work items / 6 matched". FORBIDDEN substrings (case-insensitive, must
   not survive anywhere): scout, contextfabric, claims recovery, subrogation, adjuster,
   adjusting, guidewire, outlook, inspectpro, arbfile, xactimate, fnol, insurer.

4) SELF-CONTAINED + TAGGING. Put a stable `data-od-id` on each top-level `<section>` / screen
   region (mirror how web-prototype/github-dashboard tag regions — e.g. sidebar, overview,
   connections, recommendation, sign-off). No external network; no DC syntax left; do NOT
   inline base64 fonts. Keep total file size well under 110,000 chars — prefer JS-generated
   repeated markup (render loops over the steps/systems/criteria/sim-cells arrays) over static
   duplication of the sc-for-expanded blocks; if approaching the limit, compress markup, never
   drop a feature.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin && test -f skills/opendesign/design-templates/process-canvas/example.html && ! grep -qEi "https?://|googleapis|gstatic|unpkg|support\.js|image-slot\.js|\.dc\.html" skills/opendesign/design-templates/process-canvas/example.html && ! grep -qE "<sc-if|<sc-for|<x-dc|<helmet|\{\{|data-dc-script|data-props|data-screen-label|hint-placeholder" skills/opendesign/design-templates/process-canvas/example.html && ! grep -qiE "scout|contextfabric|claims recovery|subrogation|adjuster|adjusting|guidewire|outlook|inspectpro|arbfile|xactimate|fnol" skills/opendesign/design-templates/process-canvas/example.html && grep -q "data-od-id" skills/opendesign/design-templates/process-canvas/example.html && grep -qE "setInterval|addEventListener" skills/opendesign/design-templates/process-canvas/example.html && [ "$(wc -c < skills/opendesign/design-templates/process-canvas/example.html)" -lt 110000 ] && echo PASS</automated>
  </verify>
  <done>example.html exists, has zero external-network/DC/domain matches, carries data-od-id + an inline vanilla-JS controller (setInterval/addEventListener), and is < 110,000 chars. Opening it in a browser renders the sidebar + 4 screens with working nav, sub-tabs, version swap, node drill, gate step-through, sim scan, and sign toggle — visually identical to the source.</done>
</task>

<task type="auto">
  <name>Task 2: Author process-canvas/SKILL.md mirroring web-prototype's frontmatter schema</name>
  <files>skills/opendesign/design-templates/process-canvas/SKILL.md</files>
  <action>
Create SKILL.md using the EXACT frontmatter schema of `web-prototype/SKILL.md` (and the richer
`od:` keys from `github-dashboard/SKILL.md`), so `od_loader._load_one_template` parses it and
`list_prototype_templates` surfaces it. Frontmatter fields: `name` (generic, e.g. "Process
Canvas"); `description` (block scalar — a reusable process/workflow canvas: sidebar + swimlane
process map, version comparison, decision-gate step-through, outcome simulation, sign-off);
`triggers` list (e.g. workflow, process, swimlane, process map, dashboard, decision gate); and
an `od:` block with `mode: prototype` (MANDATORY — this is the filter key), `platform: desktop`,
`scenario: operations` (the bucket dashboard/operations templates use — dashboard, kanban-board,
weekly-update all use `operations`), `preview: { type: html, entry: index.html }`,
`design_system: { requires: true, sections: [color, typography, layout, components] }`,
`craft: { requires: [...] }` mirroring web-prototype's craft requirement, `example_prompt`
(a generic process-canvas ask), `inputs` (list), and `outputs` (e.g. `{ primary: index.html }`).
Body = build-agent workflow instructions in web-prototype's style, adapted to this
process-canvas design (resource map, when-to-use, step-by-step build workflow, visual rules,
self-check, single self-contained `index.html` output contract). Keep ALL naming generic — the
FORBIDDEN substrings from Task 1 must not appear here either. Do NOT reference any file outside
this folder and do NOT imply any new design-system/craft file is added.
  </action>
  <verify>
    <automated>cd /Users/1000060523/Documents/Work/UKI/Flowin/flowin/backend && grep -qE "mode:\s*prototype" ../skills/opendesign/design-templates/process-canvas/SKILL.md && ! grep -qiE "scout|contextfabric|claims recovery|subrogation|adjuster|adjusting|guidewire|xactimate|fnol" ../skills/opendesign/design-templates/process-canvas/SKILL.md && python3.11 -c "from app.services.od_loader import list_prototype_templates as L; ids=[t['id'] for t in L()]; assert 'process-canvas' in ids, ids; print('OK', len(ids))"</automated>
  </verify>
  <done>SKILL.md exists with valid frontmatter (`od.mode: prototype`, `scenario: operations`, design_system + craft keys) mirroring web-prototype; the loader lists 'process-canvas' among prototype templates; zero forbidden domain substrings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template file -> preview iframe | `example.html` is served by `get_template_preview_path` and rendered in the gallery preview; its inline CSS/JS executes in that context. |
| SKILL.md frontmatter -> od_loader | YAML frontmatter is parsed by `_parse_frontmatter` (already `yaml.safe_load`) into the gallery payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w00-01 | Tampering | example.html rendered in preview | mitigate | Zero external network: gate greps forbid `http(s)`, googleapis, gstatic, unpkg, support.js, image-slot.js, .dc.html — preview cannot phone home or pull remote code. |
| T-w00-02 | Elevation | inline vanilla-JS controller | mitigate | Controller is self-contained state + render + setInterval only; no `eval`, no `fetch`, no dynamic import (Task 1 forbids them); nothing new is executed by the engine. |
| T-w00-03 | Info disclosure | genericized copy | mitigate | Claims-domain/product vocabulary stripped and gated (forbidden-substring grep on both files) — no real-account or domain data ships in the template. |
| T-w00-04 | Tampering | scope creep beyond 2 files | accept | `git status --porcelain` gate asserts ONLY the 2 new files under process-canvas/ changed; no code/config/test/guardrail touched. |
| T-w00-SC | Tampering | package installs | accept | No npm/pip/cargo install occurs — pure static content files added; no supply-chain surface. |
</threat_model>

<verification>
Executor runs from repo root with `python3.11` (no venv). All seven exit gates must pass:

1. `git status --porcelain` after commit shows ONLY the two new files under
   `skills/opendesign/design-templates/process-canvas/`.
2. `grep -nEi "https?://|googleapis|gstatic|unpkg|support\.js|image-slot\.js|\.dc\.html" skills/opendesign/design-templates/process-canvas/example.html`
   -> no output (font decision is locked to system fonts, so NO legitimate font link either).
3. `grep -nE "<sc-if|<sc-for|<x-dc|\{\{|data-dc-script|data-screen-label" skills/opendesign/design-templates/process-canvas/example.html`
   -> no output.
4. `cd backend && python3.11 -c "from app.services.od_loader import list_prototype_templates as L; ids=[t['id'] for t in L()]; assert 'process-canvas' in ids, ids; print('OK', len(ids))"`
   -> passes.
5. `grep -niE "scout|contextfabric|claims recovery|subrogation|adjuster" skills/opendesign/design-templates/process-canvas/example.html skills/opendesign/design-templates/process-canvas/SKILL.md`
   -> no output.
6. `wc -c skills/opendesign/design-templates/process-canvas/example.html` -> < 115000 (target < 110000).
7. `cd backend && python3.11 -m pytest tests/agents/test_characterization_prototype.py tests/agents/test_characterization_od_prototype.py -q`
   -> green (these suites do not reference the template gallery, so a new dir leaves goldens
   byte-identical).
</verification>

<success_criteria>
- Exactly two new files exist under `skills/opendesign/design-templates/process-canvas/`
  (`example.html` + `SKILL.md`); nothing else in the repo changed.
- `list_prototype_templates()` includes `process-canvas` with `mode == 'prototype'`.
- `example.html` is fully self-contained (no external network, no DC syntax), < 110,000 chars,
  carries `data-od-id` regions, and its inline vanilla-JS controller reproduces every source
  interaction: 4-screen nav, reco sub-tabs, version swap, swimlane node drill (master->detail),
  gate next/prev/auto-play/reset, sim scan, sign/unsign.
- All claims-recovery / product-specific vocabulary is genericized; forbidden-substring greps
  return nothing on both files.
- Characterization goldens (`test_characterization_prototype`, `test_characterization_od_prototype`)
  stay green.
- Single commit `feat(templates): add process-canvas prototype template (self-contained, generic naming, drilled nav)`
  with NO Co-Authored-By / Claude-Session trailer. (STATE.md row update is the orchestrator's job.)
</success_criteria>

<output>
Create `.planning/quick/260706-w00-add-process-canvas-prototype-template-se/260706-w00-SUMMARY.md` when done.
</output>
