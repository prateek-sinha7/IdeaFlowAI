# Resume QA Bug Log — milestone v3.0 live-Bedrock pass (feat/ui-2)

> Companion to [RESUME-QA-TEST-SHEET](./RESUME-QA-TEST-SHEET.md). Numbering **BUG-R01+** (separate series from the SSE campaign's BUG-001..016 in [SSE-QA-BUG-LOG](./SSE-QA-BUG-LOG.md)).
>
> **Discipline (established this project):** NO bug is logged from symptoms alone. Every ❌ first gets a dedicated deep-investigation agent (max effort) that reads `.planning/IMPLEMENTATION-REGISTER.md` **fully to EOF**, root-causes with file:line evidence, checks the finding against locked decisions (POR §8, INV-1/3/12/13), and classifies pre-existing vs v3.0-introduced (re-baseline against the pre-phase commit when "pre-existing" is claimed — see the pre-phase-baseline rule). Only then is the entry written here.
>
> **Entry states:** [OPEN] → [ROOT-CAUSED] → [FIXED ✅] (fix = its own gsd-quick task with RED→GREEN + live re-proof) · [WONTFIX/BY-DESIGN] with the register citation.

## Known-open pre-existing (carried in — do NOT re-log)

- **BUG-009 (SSE log)** — od_ppt can complete with an empty deck output (LV-02 recurrence; prompt-contract, not a resume defect).
- Offline-held red suites, each proven pre-existing at phase baselines: `redo_gate_safety` 4 passed/3 failed (harness drift) · `declared_gate_streaming` 3 FK env-reds · `test_migrations` 2 stale-head asserts · `attach_replay` 1 SSE red. Each wants its own future quick task.
- **AUD-1** — audit item, not a bug (yet): the auto-resume path's terminal-status write posture. Checked live by R-A1's final-DB-status assertion; if it fails, it becomes BUG-R01 with the `resume_run`-writes-no-status analysis from Phase 50 research as the head start.

## Template

```
### BUG-Rxx — <one-line defect statement>  [severity 🔴/🟠/🟡] [OPEN]
- **Found by:** R-<row> on <date> · run id <id>
- **Symptom:** <observed, with shot/transcript refs>
- **Expected:** <sheet row's expected proof>
- **Investigation:** <agent's root cause, file:line, register cross-refs, pre-existing-vs-v3.0 classification with baseline evidence>
- **Fix:** <quick-task id + commits + live re-proof> | deferred rationale
```

---

## Shared root cause (BUG-R01 + BUG-R02) — the "empty agent list" re-arm no-op for id-aliased pipelines

**One sentence:** On restart, both gate re-arm drivers rebuild the run's agent roster with `get_pipeline_agents(wr.type)` using the RAW stored type WITHOUT resolving the `od_prototype -> prototype` id-alias, so for every `od_prototype` run the roster is empty, the driver early-returns ("empty agent list"), and no server-side coroutine is ever re-created to await the gate response — the answer/approval is persisted but nothing consumes it.

### The two live warnings that name the defect
- clarify: `WARNING _replay_clarify_run(75979e2c-...): empty agent list — nothing to replay` (engine.py:7609-7611)
- review:  `WARNING resume_run(0139f9de-...): empty agent list — nothing to resume` (engine.py:7305)

### Happy path — what a live (never-restarted) run holds
A live gate is a coroutine blocked on an in-memory `asyncio.Event`, driven by the run's `_execute_impl` task:
- **Clarify:** `ClarifyEngine.run` (clarify_engine.py:191) emits `questionnaire_ready` then blocks at `await event.wait()` (clarify_engine.py:299), where `event = store.get_resume_event(run_id)` (clarify_engine.py:283; store.py:53-60). `POST /{id}/answers` -> `submit_answers` (run_commands.py:253-277) -> `store.set_questionnaire_responses` (store.py:62-86) stores the responses AND calls `event.set()` (store.py:80-81) on that same event, unblocking clarify_engine.py:299 -> merge/persist -> the run proceeds into planner/agents dispatch.
- **Review:** `_run_review_gate` (engine.py:5000) blocks at `await event.wait()` (engine.py:5105), where `event = store.get_review_event(gate_key)` (store.py:111-116). `POST /{id}/gate` -> `resolve_gate` (run_commands.py:165-...) -> `store.set_review_response` (store.py:118-144) stores the action AND calls `event.set()` (store.py:143-144), unblocking engine.py:5105 -> the 5-action consumer (approve/reject/edit/redo/update_specs) drives the run forward.

A `kill -9` + restart destroys BOTH the awaiting coroutine (the `_execute_impl` task) AND the per-process `store._resume_events` registry (store.py:42). The durable event log survives (questions/gate + spec preserved); the in-memory waiter does not.

### The re-arm path and where it breaks
`restore_non_terminal_runs` branch (a) (engine.py:5226-5343) handles a `waiting_for_user` run: `compile_for_run(wr.type)` **succeeds** (engine.py:5244) because `compile_for_run` resolves the alias internally (`manifest_id = resolve_alias(pipeline_type)`, engine.py:563 inside 547-565), so the run is classified compilable, `derive_open_gate` finds the open gate, `_is_resumable_in_flight` is True -> the store event is re-created and cleared (engine.py:5286-5294) and `_rearm_gate_run` is spawned (engine.py:5312-5314). **Status correctly stays `waiting_for_user`** (this is the KAN-88 re-arm; the live restore logs "restored N resumable run(s)"). `_rearm_gate_run` (engine.py:5423-5476) dispatches on the gate kind: `review -> resume_run(run_id)` (engine.py:5469), `questionnaire -> _replay_clarify_run(run_id)` (engine.py:5476).

Both drivers then rebuild the roster from the RAW stored type:
- `_replay_clarify_run` (engine.py:7556): `pipeline_type = wr.type` (7584) -> `agents = get_pipeline_agents(pipeline_type)` (7601) -> **`if not agents:`** log "empty agent list — nothing to replay" + `_fire_resume_cleanup` + **`return`** (7608-7613). It NEVER reaches `_drive_resumed_stream` (7682), so `_execute_impl` is never re-driven with `_clarify_replay`, so `ClarifyEngine.run`'s `await event.wait()` is never re-entered.
- `resume_run` (engine.py:7233): `pipeline_type = wr.type` (7265) -> `agents = get_pipeline_agents(pipeline_type)` (7298) -> **`if not agents:`** log "empty agent list — nothing to resume" + `_fire_resume_cleanup` + **`return`** (7304-7308). It NEVER reaches `_compute_resume_offset` (7314) or the 49-02 offset-override/`gate_reentry` re-entry, so `_run_review_gate`'s `await event.wait()` is never re-entered.

The store event exists (armed at 5286-5294) but no coroutine awaits it. `set_questionnaire_responses`/`set_review_response` then `.set()` an event with no listener -> **black hole**; status stays `waiting_for_user` forever.

### Why the roster is empty — the id-alias, verified
`get_pipeline_agents` (registry.py:224-244) discovers agents whose `AGENT.md` declares that exact `pipeline_type`. `od_prototype` is a routing **id-alias with no AGENT.md of its own** (the prototype agents declare `pipeline_type: prototype`); it must be resolved to `prototype` before the lookup. Empirically confirmed offline (python3.11, no server):
```
resolve_alias("od_prototype")            == "prototype"
get_pipeline_agents("od_prototype")      -> len 0      # the bail
get_pipeline_agents("prototype")         -> len 5      # what a fresh/aliased run gets
```
The register documents this exact gotcha: line 254 ("od_ alias gotcha: `od_prototype` ... have NO AGENT.md of their own ... `get_pipeline_agents` returns none for them ... resolve the alias for agent LOOKUP"); lines 464-465 ("`od_prototype` is the SOLE id-alias -> `prototype`, no own dir"). A **fresh** run applies exactly this: `_execute_impl` resolves via `compile_for_run` (563) and takes membership from `get_pipeline_agents(compiled.id)` (engine.py:1678, where `compiled.id` is the RESOLVED id) with a `PIPELINE_AGENTS.get(compiled.id, [])` fallback (1679-1683). `resume_run` and `_replay_clarify_run` skip that resolution -> pass `wr.type="od_prototype"` -> empty.

### Classification — v3.0 / Phase 49 REGRESSION (headline-breaking)
- **Symptom is a Phase 49 regression.** Before Phase 49, branch (a) marked every restart-`waiting_for_user` run **failed** with an honest WR-05 message ("start a new run") — the KAN-88 behavior. Phase 49 flipped fail->re-arm (engine.py:5226-5343) but the re-arm driver bails for `od_prototype`, converting the honest failure into a **silent black hole**. Phase 49 both introduced `_replay_clarify_run` with the alias omission fresh AND newly routed review-gate-parked runs into `resume_run`, which already had it.
- **The `resume_run` half of the defect pre-exists since Phase 12 (12-03)** (resume_run born there) but was DORMANT: pre-49, gate-parked runs were failed not re-armed. NOTE (code-verified, not live-observed in this pass): branch (b) mid-build auto-resume of an `od_prototype` run (status generating/etc.) ALSO routes through `resume_run` (engine.py:5376-5377) and would ALSO bail at 7305 -- i.e. NO restart-resume of any `od_prototype` run works today. Only the two gate-parked cases were live-reproduced here.
- **Scope of blast radius:** `od_prototype` is the SOLE non-self entry in `_OD_ALIAS_BASE` (registry.py:263-265) and the primary user-facing prototype pipeline. `od_ppt`/`od_ppt_revision` are REAL registry keys (self-aliasing), so `get_pipeline_agents(wr.type)` is non-empty for them -- they are NOT affected. The defect is specific to `od_prototype` (and any future non-self id-alias).

### The coverage hole that let this ship
Every `test_restart_resume.py` restart test drives through `_ResumeHarness`, which monkeypatches `get_pipeline_agents` so the fixture type returns non-empty specs: `_patched_gpa` = "return `list(specs)` if `pt == _FIXTURE_ID`" (test lines 244-245), with `_FIXTURE_ID = "sample_wave"` (line 58) and `_seed_workflow_run(..., type_="sample_wave")` by default (line 375). So the driver's `get_pipeline_agents(wr.type)` is ALWAYS non-empty in tests. The only non-fixture type any test seeds is `type_="__nonexistent_pipeline__"` (line 1077) -- the *uncompilable* negative case (correctly WR-05-fails). **No test ever seeds a COMPILABLE-yet-aliased type (`od_prototype`) against the REAL `get_pipeline_agents`** -- the exact case where `compile_for_run` succeeds (re-arm) but `get_pipeline_agents(raw)` is empty (bail).

The two Phase 49 twin tests stop one assertion short of the bug:
- KAN-88 `test_waiting_for_user_run_is_rearmed_not_driven` (lines 887-909): asserts status stays `waiting_for_user` AND `resume_run` is NOT called. The re-arm satisfies both. It does NOT submit an answer/gate and assert progression.
- `test_clarify_parked_run_replays_durable_questions_and_proceeds` (line 2827) and `test_gate_reentry_all_five_actions_post_restart` (line 1251): these DO drive the answer/gate forward, but only because the fixture roster is non-empty. They never exercise the aliased-empty-roster path, so the missing `resolve_alias` is invisible to them.

### Fix (design only -- not implemented here)
Resolve the id-alias before the roster lookup in BOTH drivers, exactly as the fresh path does:
- `resume_run` (engine.py:7298): `agents = get_pipeline_agents(resolve_alias(pipeline_type))`
- `_replay_clarify_run` (engine.py:7601): `agents = get_pipeline_agents(resolve_alias(pipeline_type))`

`resolve_alias` is already module-level in engine.py (defined 532, used by `compile_for_run` at 563). For full parity with the fresh path, mirror `_execute_impl`'s membership fallback (engine.py:1678-1683): `get_pipeline_agents(resolve_alias(pt)) or PIPELINE_AGENTS.get(resolve_alias(pt), [])` (also covers the ppt/od_ppt `get_pipeline_agents` quirk registered as WR-01). Everything downstream of the bail is already correct: for clarify, `_drive_resumed_stream` (7421) -> `_execute_impl` with `_clarify_replay` (1716, forces the gate at 1723-1727, replays at 1852-1889 -> re-enters `await event.wait()`); for review, `resume_run`'s 49-02 offset-override + `gate_reentry` sentinel re-enter `_run_review_gate`'s wait. The alias resolution is the ONLY missing piece.

**Guardrails (register):** INV-12 -- reuse `resolve_alias` + the two existing drivers; no third driver, no new machinery. INV-1/SC-001 -- `resolve_alias` is a generic data map (`_OD_ALIAS_BASE`), no workflow-name literal in the kernel branch (banned-pattern gate stays green). INV-3 -- `resolve_alias` is identity for real registry keys, so scripted golden runs are byte/event-identical (goldens 10/10). No new WS types, no migration, no FE.

**Coverage fix (mandatory, closes the hole):** add a regression test that seeds `wr.type="od_prototype"` against the REAL `get_pipeline_agents` (NOT `_patched_gpa`) and asserts RED->GREEN: pre-fix the driver logs "empty agent list" and no `_drive_resumed_stream`/offset-override runs (POST answers/gate does not advance); post-fix the alias resolves to `prototype`'s 5 agents and the run drives forward. This must fail on the current code before the fix is trusted.

---

### BUG-R01 — clarify answer black-holes after a backend restart (re-arm never re-creates the awaiting coroutine)  [severity 🔴 major] [ROOT-CAUSED]
- **Found by:** R-clarify-restart on 2026-07-19 · run id `75979e2c-c02a-480a-8610-051f6b9ff785` (od_prototype, parked at the 5-question clarify questionnaire)
- **Symptom:** `kill -9` + bare-uvicorn restart. Restore logged `restored 2 resumable run(s), auto-resumed 0, marked 0 abandoned failed` (status correctly stayed `waiting_for_user`) AND `WARNING _replay_clarify_run(75979e2c-...): empty agent list — nothing to replay`. Questions replayed byte-identical (durable `questionnaire_ready`, event count 6->6, zero model re-gen). Then `POST /api/runs/75979e2c-.../answers` with 5 valid responses returned HTTP 200 `{"ok":true,"count":5}` (answers STORED) but the run stayed `waiting_for_user` indefinitely (0 new events after 40s+, no submission log activity). Discriminating control: a FRESH never-crashed od_prototype clarify run (`0139f9de-...`) submitted the identical payload shape and immediately proceeded (`waiting_for_user`->`generating`, events 6->187 in ~5s). The ONLY difference is the restart.
- **Expected:** after restart, a clarify answer over `POST /{id}/answers` proceeds into the normal planner->agents dispatch exactly as a never-restarted run (Phase 49 SC-4/SC-5; RESUME-17).
- **Investigation:** See "Shared root cause" above. Clarify-specific bail: `_replay_clarify_run` (engine.py:7556) reads `pipeline_type = wr.type = "od_prototype"` (7584), calls `get_pipeline_agents("od_prototype")` (7601) which returns `[]` (id-alias, no own AGENT.md -- verified `len 0` offline; register line 254), hits `if not agents:` -> logs the observed warning + `_fire_resume_cleanup` + returns (7608-7613) BEFORE `_drive_resumed_stream` (7682). So `_execute_impl` is never re-driven with `_clarify_replay` and `ClarifyEngine.run`'s `await event.wait()` (clarify_engine.py:299) is never re-established; the answer's `event.set()` (store.py:80-81) has no listener. Classification: v3.0 / Phase 49 regression (the fail->re-arm flip turned KAN-88's honest WR-05 failure into a silent black hole for od_prototype). Coverage hole: `test_clarify_parked_run_replays_durable_questions_and_proceeds` (test line 2827) passes only because `_ResumeHarness._patched_gpa` (244-245) returns fixture specs for `_FIXTURE_ID="sample_wave"`; no test seeds the aliased `od_prototype` against the real `get_pipeline_agents`.
- **Fix:** design in "Shared root cause" -> Fix. Change `_replay_clarify_run` (engine.py:7601) to `get_pipeline_agents(resolve_alias(pipeline_type))`. Its own gsd-quick with a RED->GREEN aliased-clarify restart test + live re-proof on od_prototype. Deferred to that task (analysis only here).

### BUG-R02 — review-gate approve/all-actions black-hole after a backend restart (same empty-roster re-arm no-op)  [severity 🔴 major] [ROOT-CAUSED]
- **Found by:** R-review-restart on 2026-07-19 · run id `0139f9de-5d89-4b1d-a4c4-2da69d73674c` (od_prototype, parked at the spec review gate; gate_key `0139f9de...:prototype-specify`; also served as BUG-R01's fresh-clarify control before it reached this gate)
- **Symptom:** `kill -9` + restart. Restore logged `restored 3 resumable run(s), auto-resumed 0 in-flight, marked 0 abandoned failed` THEN `WARNING resume_run(0139f9de-...): empty agent list — nothing to resume`. Post-restart status correctly stayed `waiting_for_user`, `review_gate_ready` sha unchanged (spec preserved), gate UI re-rendered pixel-identical (D-14g on attach). BUT `POST /api/runs/0139f9de.../gate {"action":"approve","gate_key":"...:prototype-specify"}` returned HTTP 200 `{"ok":true,"action":"approve"}` yet the run stayed `waiting_for_user` with event count frozen at 2648 and ZERO progression after 48s+. Approve black-holes exactly like the clarify answer. The gate parked AFTER the spec-writer agent completed (its `agent_complete` is durable), confirming the bail is NOT about missing durable step state -- it is purely the empty roster.
- **Expected:** after restart, all five gate actions (approve/reject/edit/redo/update_specs) over `POST /{id}/gate` work identically to a never-restarted run and drive the run downstream (Phase 49 SC-3; RESUME-17).
- **Investigation:** See "Shared root cause" above. Review-specific bail: `_rearm_gate_run` routes an open review gate to `resume_run(run_id)` (engine.py:5469). `resume_run` (7233) reads `pipeline_type = wr.type = "od_prototype"` (7265), calls `get_pipeline_agents("od_prototype")` (7298) -> `[]`, hits `if not agents:` -> logs the observed warning + `_fire_resume_cleanup` + returns (7304-7308) BEFORE `_compute_resume_offset` (7314) and the 49-02 offset-override/`gate_reentry` re-entry. So `_run_review_gate`'s `await event.wait()` (engine.py:5105) is never re-established; the approve's `event.set()` (store.py:143-144) has no listener. The re-arm reads/keys on the run's registry agent MEMBERSHIP (`get_pipeline_agents(wr.type)`), NOT the durable compiled plan -- and that lookup returns empty for the aliased type even though the spec agent already completed. Classification and coverage hole identical to BUG-R01: `test_gate_reentry_all_five_actions_post_restart` (test line 1251) and the branch-(a) re-arm tests (e.g. line 975) all use `_FIXTURE_ID="sample_wave"` with `_patched_gpa` non-empty; KAN-88 (887-909) asserts status-stays + not-driven but never asserts a post-restart `POST /gate` advances the run.
- **Fix:** design in "Shared root cause" -> Fix. Change `resume_run` (engine.py:7298) to `get_pipeline_agents(resolve_alias(pipeline_type))` (same one-line pattern; fixes the review twin AND the pre-existing branch-(b) mid-build auto-resume of od_prototype in one place). Its own gsd-quick with a RED->GREEN aliased-review restart test (all five actions) + live re-proof on od_prototype. Deferred to that task (analysis only here).

---

### BUG-R03 — restart-auto-resumed run completes with an UNDER-POPULATED workflow_runs row: `output`/`agent_outputs`/`token_usage`/`duration` empty (status written by the engine state machine, output columns written only by the app-layer launch driver the resume tier never replicates)  [severity 🟠 moderate] [ROOT-CAUSED]
- **Found by:** R-A4 on 2026-07-19 · run id `15c4372f-7682-4288-aa80-299788337e84` (user_stories, `kill -9` mid-build then restart-auto-resumed at step offset 2/6)
- **Symptom:** A user_stories run was crashed mid-build (`kill -9`) and AUTO-RESUMED by the restart restore scan (`restore_non_terminal_runs` -> "auto-resumed 1 in-flight run"; `resume_run(...): resuming pipeline=user_stories at step offset 2/6`). It ran to completion correctly — all 6 agents, deliverable produced. BUT the completed run's DB row is under-populated: `workflow_runs.output` is EMPTY (NULL/0-length) and `workflow_runs.agent_outputs` is EMPTY, while `status` IS `completed`. By contrast three normal (never-resumed) completed user_stories runs have both populated: `97a97b7b` (output 52386, agent_outputs 320771), `c7b8e26b` (34866/291040), `ab90f5f4` (44292/264355). The deliverable itself IS durably present: `artifact_refs` row `producer_agent='backlog-compiler', kind='deliverable', version=1, length=47109`, and the FE renders it fine (FE reads artifacts, never the `output` column). So this is NOT a lost-deliverable bug; it is a run-row persistence gap on the resume-completion path. `/summary` returned `{}` for the deliverable/KPI fields live. DISTINCT from BUG-R01/R02: those are the od_prototype id-alias empty-roster BAIL (run never progresses); this is a real registry key (user_stories -> `get_pipeline_agents` non-empty -> `resume_run` does NOT bail -> the run COMPLETES and the output-persistence gap bites). Different root cause, different code, different pipeline.
- **Expected:** a restart-auto-resumed run that completes persists the SAME terminal `workflow_runs` columns as a never-restarted completion — `output`, `agent_outputs`, `token_usage`, `duration`, `deliverable_mimetype/filename` — so `/summary`, `/chain-context`, export, and analytics read the same row a normal completion writes (RESUME-10; the design-draft §5.6 mandate "the terminal status ladder must stay behaviorally identical to the just-fixed fail-safe launch driver").
- **Investigation (root cause, file:line):**
  - **The asymmetry (verified by grep):** the terminal `workflow_runs` output columns are written in exactly ONE place in the whole backend — the APP-LAYER launch driver `_drive_launch_to_queue`'s post-stream terminal block (`app/api/run_commands.py:1677-1745`): `wr.output = final_output` (1704-1705), `wr.agent_outputs = json.dumps(agent_outputs_collector)` (1710-1711), `wr.deliverable_mimetype/filename` (1706-1709), `wr.token_usage` (1723-1738), `wr.duration` (1741-1742), `wr.completed_at` (1739-1740), `db.commit()` (1743). Both payloads are built by the driver CONSUMING the engine's live event stream: `final_output` from the `pipeline_complete` event (`run_commands.py:1655-1659`) and `agent_outputs_collector` from the `agent_start`/`agent_complete`/`tool_*` branches (`run_commands.py:1599-1654`). The ENGINE itself writes NONE of these columns: a backend-wide grep finds zero `.output =`/`.agent_outputs =`/`.token_usage =` writes in `agents/execution_engine/engine.py`, and the only `wr.status =` writes there are three `"failed"` in the restore scan (`engine.py:5251, 5333, 5401`).
  - **Why status IS `completed` but output is empty (the anomaly resolved):** status is NOT written by the launch driver alone — it is written by the engine STATE MACHINE inside `_execute_impl`'s "Step 5: Pipeline complete": `if current_state not in ("cancelled", "failed"): self._state_machine.transition(pipeline_run_id, "completed")` (`engine.py:2512-2513`), and `StateMachine._persist` writes ONLY `wr.status = new_state` (`agents/execution_engine/state_machine.py:100-118`, write at :117 — no other column). BOTH the normal launch AND the resume drive `_execute_impl`, so BOTH reach 2513 and get `status=completed` from the state machine. The launch path ADDITIONALLY runs the driver's 1677-1745 block (output/agent_outputs/etc.); the resume path does NOT. Hence: status yes (engine state machine, both paths), output no (app-layer driver, launch path only).
  - **The resume path writes no output columns (traced):** restart branch (b) spawns bare `asyncio.create_task(self.resume_run(pipeline_run_id))` (`engine.py:5376-5378`) — NOT wrapped in any app-layer driver. `resume_run` (`engine.py:7233`) ends by awaiting `_drive_resumed_stream` (`engine.py:7406-7419`) with NO post-drive terminal persistence. `_drive_resumed_stream` (`engine.py:7421`) drives `_execute_impl` (7456-7474), persists each event to the durable run_events sink (`sink.persist`, 7484) and pushes to the live_queue (7489-7495), and its `finally` (7526-7554) does ONLY cleanup + live-ectx unregister — it writes NOTHING to the `workflow_runs` row. So the resume tier is structurally silent on every output-bearing column; only the engine state-machine `status` write lands.
  - **The Phase 50 user-resume path shares the identical gap (BY DESIGN, cited):** `_drive_user_resume` (`run_commands.py:413`) is documented (docstring 419-425) to add "ONLY the two status writes the resume tier structurally lacks" and carries a "HARD FENCE (INV-12 / Pitfall 7): this body MUST NOT contain an `async for ... engine.execute(` event ladder and MUST NOT clone `_drive_launch_to_queue`'s per-event accumulation. It is NOT a third driver." Its `_reconcile_terminal_status` (`run_commands.py:440-493`) reads the durable run_events tail and computes status (482-488) -> `_persist_resume_status` (`run_commands.py:503-517`) writes ONLY `swr.status` (511), `wr.error` (513), `completed_at` (514). So NEITHER resume entry point (Phase 46 restart auto-resume NOR Phase 50 user-resume) persists output/agent_outputs/token_usage/duration. The observed run hit the restart auto-resume path (branch b), where even the app-layer status reconcile does not run — status came purely from the engine state machine.
  - **Downstream impact (each verified):**
    - **Deliverable render (FE): SAFE.** FE reads the durable `artifact_refs` (kind=deliverable, present, length 47109), never the `output` column.
    - **Revision / family revise: SAFE.** `_handle_revision` (`engine.py:5575`) reads the PARENT run's artifacts via the owner-scoped `ScopedStore` against `artifact_refs` (F2/13-05 fallback chain — link 2 = the `kind="deliverable"` completion ref), NOT `wr.output`. Backend-wide grep confirms ZERO `.output`/`.agent_outputs` reads in the revision driver (`_drive_revision_to_queue`, `run_commands.py:1884`) or the engine revision path. The resumed run's deliverable artifact IS present, so link 2 resolves -> revision reseeds correctly.
    - **`GET /{id}/chain-context`: BREAKS.** `_extract_chain_context` (`runs.py:488`, endpoint 664-665) reads `workflow_run.agent_outputs` (506-508) as the primary source for every pipeline branch via `get_agent_output()` (517-521); empty -> "" for every agent. user_stories branch: `compiler_output = get_agent_output("backlog-compiler")` (562) -> "" then fallback `workflow_run.output or ""` (565) -> "" ; the generic (632-633) and final (636-637) fallbacks also read the empty `output`. Every branch depends on `agent_outputs` OR `output`, both empty -> returns empty `structured_summary`/`context_block`. A resumed run chained as context into a NEW pipeline silently contributes NOTHING.
    - **`GET /{id}/summary`: DEGRADED.** `get_run_summary` (`runs.py:1084`) reads `run.agent_outputs` (1112-1118 -> empty per-agent breakdown) + `run.token_usage` (1121-1127 -> empty KPIs). Matches the live `/summary` `{}` observation.
    - **`POST /export-pptx`: DEGRADED (PPT pipelines only; N/A for this user_stories run).** `export_pptx` (`runs.py:274`) Strategy 1 reads `wr.agent_outputs` (312-320) to find the `ppt-code-generator` source; empty -> falls back to the request-body `html`/`js_code`. Breaks only if a caller relies on the DB copy.
    - **Analytics under-count.** `_aggregate` (`app/api/analytics.py:145`) sums `_parse_token_usage(r.token_usage)` (174) into total_tokens/cost rollups (201/206/216); a resumed run's empty `token_usage` contributes 0 tokens / $0 -> analytics silently under-reports.
  - **AUD-1 linkage (materialized + refined):** this IS the AUD-1 audit item ("the auto-resume path's terminal-status write posture"; design-draft §5.6 "the resume tier writes no status, so the app layer reconciles it here") materializing live — but REFINED by the code: status IS written on the auto-resume path, by the engine state machine (`engine.py:2513` -> `state_machine.py:117`), which BOTH paths execute; the "resume_run writes no status" framing from Phase 50 research was incomplete because it looked at `resume_run`'s own body and missed that `_execute_impl`'s state-machine transition covers status for free. The ACTUAL gap AUD-1 should name is the resume tier writes no OUTPUT-bearing columns — the fields ONLY `_drive_launch_to_queue` persists and neither resume path replicates.
  - **Classification — pre-existing latent gap surfaced by v3.0 resume QA (NOT a v3.0 regression).** Origin is two-fold and both predate v3.0: (1) branch-(b) `_is_resumable_in_flight` mid-build auto-resume has existed since Phase 12; (2) the Phase 29 SSE cutover made `_drive_launch_to_queue` the SOLE writer of the output columns, and `resume_run` never replicated that block. The auto-resume path's output-persistence posture did not CHANGE in v3.0 — it never had it — so this is not a v3.0-introduced regression; v3.0's "top-tier resume" live QA is simply the first exercise of a real-key (user_stories) restart-resume-to-completion, which is where the long-standing gap becomes observable. (Contrast BUG-R01/R02, which ARE Phase 49 regressions on the od_prototype alias.)
  - **Coverage hole (why it shipped):** NO resume/restart test asserts the output-bearing columns. `grep -l` for `\.output`/`agent_outputs`/`token_usage`/`duration` across all six resume test files (`tests/agents/test_restart_resume.py` [131 status-related asserts], `tests/unit/test_rest_resume.py` [25], `tests/agents/test_phase8_resume.py`, `test_resume_marker_workspace.py`, `test_resume_ws_bridge.py`, `_resume_child.py`) returns EMPTY — not one touches those columns. Structural reason: the resume suites drive `resume_run`/`_execute_impl` (or the state machine) and assert durable events + `status`, but the output columns are written by the app-layer driver `_drive_launch_to_queue`, which the resume tests never invoke — so the missing output persistence is invisible to them.
- **Fix (design only — not implemented here; INV-12: reuse ONE persistence mapping, no third driver):** Extract the event->`WorkflowRun`-column mapping currently inline in `_drive_launch_to_queue` (`run_commands.py:1655-1742`: `pipeline_complete.final_output`->`output`, `agent_*` accumulation->`agent_outputs`, token sums->`token_usage`, `duration`, `deliverable_mimetype/filename`) into a SHARED app-layer helper keyed on a durable run_events list, then invoke it from BOTH resume entry points so the resume-completion row matches a launch completion: (a) **USER-resume** — extend `_reconcile_terminal_status` (`run_commands.py:440`), which ALREADY reads the durable tail for status, to feed the SAME tail to the helper and persist the output columns; (b) **RESTART auto-resume** — inject the helper as a NEW engine resume hook (mirroring the injected `_resume_milestone_sink` at `engine.py:839` / the ports-and-adapters callback pattern, since the engine cannot import app.*) and fire it in `_drive_resumed_stream`'s `finally` (`engine.py:7526-7554`) from the durable tail, so the branch-(b) path persists too. Leave the state-machine `status` write (`engine.py:2513`) as-is (already correct on both paths); add ONLY the missing output-column persistence, byte-for-byte the `_drive_launch_to_queue` mapping. **Coverage fix (mandatory):** add a RED->GREEN restart-resume test on a REAL registry key (user_stories, non-aliased, real `get_pipeline_agents`) asserting that after auto-resumed completion `wr.output`/`agent_outputs`/`token_usage`/`duration` are populated — currently RED (no resume test asserts these). Its own gsd-quick with live re-proof on a crashed-then-resumed user_stories run. Deferred to that task (analysis only here).

---

### BUG-R04 — clarify-skip (`POST /answers {responses:[], skip_clarification:true}`) returns 200 but the run stays `waiting_for_user` — the `skip_clarification` flag is CORRECTLY wired + consumed (NOT a dead field); the stays-waiting symptom is a pre-existing lost-wakeup race at `clarify_engine.py:284` reachable ONLY by a client that submits BEFORE `questionnaire_ready` (the QA harness, never the FE)  [severity 🟡 minor] [BY-DESIGN (flag) + ROOT-CAUSED (pre-arm race)]
- **Found by:** R-skip-clarify repro on 2026-07-19 · runs `feb0e17c-752b-4c15-9a09-b8f963d49c20` + `1386315d-a2e0-47b0-b7ff-62f7f6c1e346` (both `user_stories`, FRESH / never-crashed, parked at the clarify questionnaire)
- **Symptom:** `POST /api/runs/{id}/answers` with `{"responses":[], "skip_clarification":true}` returned HTTP 200 `{"ok":true,"count":0}` (accepted) but the run then sat at `waiting_for_user` for 30–100s+ with no new events / no log activity — it did NOT proceed to planner/agents. Reproduced twice. DISCRIMINATING CONTROL: on the SAME two runs, a subsequent NORMAL answer (`{"responses":[{question_id,answer}…]}`, count>0) advanced the run to `generating` immediately (`waiting_for_user`→`generating`). So a normal answer wakes the run's waiter; the empty-`skip_clarification` submit did not. NOT resume-related (fresh runs, no restart) and NOT the od_prototype id-alias class (BUG-R01/R02): `user_stories` is a real registry key.
- **Expected:** "Skip all & run directly" (`skip_clarification:true`) force-proceeds the clarify gate into planner→agents dispatch, per ISS-027.
- **Investigation (root cause, file:line + verdict):**
  - **`skip_clarification` is FULLY WIRED end-to-end — every site enumerated (NOT a dead/ignored field):**
    - **FE (does send it):** `DashboardLayout.tsx:1178-1182` `handleQuestionnaireSkip` → `onSubmitQuestionnaire(activePipelineRunId, [], true)` (wired to `onSkipClarify` at `:1806`/`:1874`); the KAN-90 "Cancel Workflow" path `:1223-1225` sends the identical `([], true)`. → `useWorkflow.ts:150-159` `submitQuestionnaire(…, skipClarification=true)` → `postAnswers({responses, skip_clarification})`. `api.ts:724-731` passthrough.
    - **HTTP handler:** `run_commands.py:116-127` `AnswersCommand.skip_clarification: bool = False`; `:252-277` `submit_answers` → `:274-276` `store.set_questionnaire_responses(run_id, responses, skip_clarification=bool(body.skip_clarification))`; returns `count=len(responses)` (=0 for the skip). The handler does NOT branch on skip beyond forwarding it to the store.
    - **Chat path (2nd caller of the same seam):** `run_commands.py:1025`/`1036` via `route_chat_turn`; `chat_router.py:135`/`160`/`215` threads `skip_clarification`.
    - **Store:** `store.py:44-48` `_questionnaire_force_proceed` dict; `:62-86` `set_questionnaire_responses` stores `responses` + `force_proceed` **AND** calls `event.set()` (`:80-81`) UNCONDITIONALLY (identical for skip and normal); `:95-105` `get_questionnaire_force_proceed`.
    - **Consumer:** `clarify_engine.py:307-309` reads `force_proceed` after `await event.wait()`; `:332-334` `if force_proceed: execution_gate="PROCEED"; return`.
    - **Tests (green offline):** `test_rest_answers_cancel.py:145-157` (HTTP stores `force_proceed=True`); `test_execution_engine.py:192-229` (`ClarifyEngine` force-proceeds: exactly 1 `questionnaire_ready`, no re-ask, PROCEED).
    - **Register lock:** `IMPLEMENTATION-REGISTER.md:3757` lists the `force_proceed` "Skip all" exit as one of "**three legitimate PROCEED exits**." ⇒ INTENDED and wired; the WS→HTTP move did NOT drop it (the HTTP handler calls the SAME `store.set_questionnaire_responses` seam the retired WS `submit_questionnaire` used — docstrings `run_commands.py:15`/`119`/`258`).
  - **Why "the flag is silently ignored" CANNOT be the cause:** `user_stories` compiles `effective_rounds=1` (`compiler.py:240` `rounds=int(clarify_raw.get("rounds",1) or 1)`, default 1; the `user_stories` manifest sets no `rounds`; clamp at `clarify_engine.py:258`). With ONE round the loop ALWAYS returns PROCEED after a SINGLE `event.wait()` return — via `:332` (force_proceed) OR, even if the flag were False, via `:337` (`missing_information` non-empty → fall through) → loop guard `round_num(1) < effective_rounds(1)` false → exit → `:342` `clarification_limit_reached` → `:352` PROCEED (this is exactly `test_clarify_engine_empty_submit_proceeds_after_one_round`, `test_execution_engine.py:233`). There is NO re-ask path for a 1-round pipeline. ∴ a fully-ignored flag would STILL proceed; "stays `waiting_for_user`" ⟺ the waiter never woke (`await event.wait()` at `clarify_engine.py:299` never returned).
  - **Why the waiter didn't wake — the LOST-WAKEUP race (ACTUAL root cause):** `get_artifact_store()` is a module singleton (`store.py:173-184`), so the HTTP handler and the engine share the SAME `_resume_events[run_id]`. The ONLY `clear()` of that clarify event is `clarify_engine.py:284`, and it runs AFTER **(a)** `engine.py:1870` transitions status→`waiting_for_user` and **(b)** the seconds-long LLM question generation at `clarify_engine.py:269` (`_generate_questions`). Reproducing sequence: status→`waiting_for_user` (`engine.py:1870`) fires BEFORE the questions exist → the harness (polling status) POSTs skip → `set_questionnaire_responses` stores `[]`+`force_proceed=True` AND `event.set()` (`store.py:81`) → `clarify.run` finishes generating, reaches `:284` `event.clear()` which **WIPES the set** → `:287` emits `questionnaire_ready` → `:299` `await event.wait()` blocks with nothing set → run sits at `waiting_for_user`. The later NORMAL answer (necessarily sent AFTER `questionnaire_ready`, since it carries real `question_id`s) sets the now-armed event → `:332`/`:337` → PROCEED → `generating`. This matches "skip stays, later normal advances, same run" exactly, and reproduces reliably because the wipe window is LLM-question-generation-wide (seconds). The clear-before-emit comment (`clarify_engine.py:279-282`) only guards a responder acting AFTER `questionnaire_ready`; it does NOT guard a submit that lands between the status flip (`engine.py:1870`) and the `clear()` (`:284`).
  - **FE-usage / severity check:** the FE Skip button is gated behind the RENDERED questionnaire — `laneClarifyOpen = questionnaireQuestions.length > 0` (`DashboardLayout.tsx:1361`), `onSkipClarify=handleQuestionnaireSkip` (`:1806`/`:1874`), and `questionnaireQuestions` is populated only from `questionnaire_ready`. So a real FE user CANNOT POST skip before `questionnaire_ready`; every FE-issued skip lands AFTER `:284`'s clear on the armed waiter → proceeds correctly. Only a client that submits PRE-`questionnaire_ready` (a QA harness firing skip on the first `waiting_for_user`) hits the race. ∴ **🟡 minor** — does not block the documented FE flow.
  - **Distinct from prior entries:** SSE **BUG-004** = `submit_answers` 500 under concurrent load (QueuePool connection leak); here skip returns 200, so different. SSE **BUG-005/010/012** = FE clarify state not bound to the viewed run (a rendering/observation defect). This is a BACKEND pre-arm lost-wakeup — a different mechanism. NOT BUG-R01/R02 (those are the `od_prototype` id-alias empty-roster bail; `user_stories` has a non-empty roster and does not bail).
  - **Classification — PRE-EXISTING, NOT v3.0.** `skip_clarification` is ISS-027; the clear-before-emit ordering (`clarify_engine.py:279-284`) and the status-flip-before-question-gen (`engine.py:1870` vs `clarify_engine.py:269`) are the ISS-027 / clarify-rounds (KAN-74 / T-v1f) design, all predating v3.0. The fresh-run clarify launch path is unchanged by v3.0/resume. (Contrast BUG-R01/R02 = Phase 49 regressions; BUG-R03 = resume output-column gap.)
- **Verdict:** `skip_clarification` handling on `POST /answers` is **BY-DESIGN correct and fully wired/consumed — NOT a silently-ignored flag** (FE→HTTP→store→clarify all present; register-locked as a legitimate PROCEED exit; unit-tested both layers). The observed "200 but stays waiting" is a **real but narrow, pre-existing lost-wakeup race at `clarify_engine.py:284`**, reachable ONLY by a client submitting before `questionnaire_ready` (the QA harness), never the FE. Honest residual: the secondhand repro does not pin the skip's wall-clock order vs `questionnaire_ready`; but the code proves (i) a skip landing AFTER `questionnaire_ready` proceeds, and (ii) a 1-round pipeline proceeds regardless of the flag — so the pre-arm race is the ONLY code path that yields "stays waiting," and the flag itself is not defective.
- **Fix (design only — NOT implemented here; 🟡, optional robustness hardening):** close the pre-arm race in `ClarifyEngine.run`: BEFORE `await event.wait()` (`clarify_engine.py:299`), re-check `await self._store.get_questionnaire_responses(pipeline_run_id) is not None` (an early submit already stored a list — `store.py:78` stores `[]`, NOT `None`, so it is detectable; `force_proceed=True` is already stored too); if present, skip the wait and fall straight into the existing read/merge/force_proceed at `:301-332`. Symmetric for an early NORMAL submit. Alternative: arm+clear the resume event ONCE before `engine.py:1870` flips status→`waiting_for_user` (and drop the `:284` clear), so no `set()` can be wiped after the run is claimable. Either is a small generic change — no workflow-name literal (INV-1/SC-001 safe), reuses the existing seam (INV-12). **Coverage:** RED→GREEN test that seeds `responses`+`force_proceed` in the store BEFORE calling `ClarifyEngine.run` (simulating a pre-`questionnaire_ready` submit) and asserts exactly one `questionnaire_ready` + `execution_gate=="PROCEED"` — currently RED (the waiter blocks on the wiped event). Deferred to its own gsd-quick if pursued (analysis only here).
