---
milestone: v1.0
milestone_name: Workflow Engine Decoupling & Universal Workflow Runtime
audited: 2026-06-14
status: tech_debt
scores:
  requirements: 117/117   # 116 satisfied + 1 intentional VOID (CTX-04); 0 unsatisfied, 0 orphaned
  phases: 21/21           # every NN-VERIFICATION.md status: passed
  integration: 9/9        # all E2E flows wired (7 VERIFIED, 2 PARTIAL/WARNING); 0 blockers
  flows: 7/9              # fully VERIFIED; 2 PARTIAL (non-blocking warnings)
invariants:               # cross-phase integrity spot-checks (integration checker)
  sc_001_inv_1: PASS      # kernel knows no workflow by name
  inv_13: PASS            # create_deep_agent only inside langchain_deepagents adapter
  inv_3_goldens: PASS     # 5 characterization goldens present + wired
  import_linter: PASS     # 4 kept / 0 broken
  migration_chain: PASS   # 0014→0021 additive, reversible, owner_id+workspace_id on every new table
gaps: []                  # no critical blockers, no unsatisfied/orphaned requirements
tech_debt:
  - phase: 06-model-policy-1c
    items:
      - "Deferred (live): CR-02 live-checkpointer fallback restart — offline thread_id derivation proven; live Postgres-checkpointer + Bedrock-throttle re-check outstanding (06-UAT.md)."
  - phase: 08-capabilities-hardened
    items:
      - "Deferred (live): real OTLP span export to a live collector — closed by-test offline (ISS-010 / 17-02 InMemorySpanExporter); logging-only is functional; live collector export is the optional upgrade."
  - phase: 13-live-verification-gap-closure
    items:
      - "Deferred (live): F1 review_gate_ready on a real WS stream; F4 zero fabricated tool-XML on live Haiku; F5 all 15 app_builder agents role-conformant live. Structural fixes verified offline."
  - phase: 14-run-revision-real-revision-loop
    items:
      - "Deferred (live): SC4 FE-exact run_revision → genuinely revised deck in preview on real Bedrock; live cancel/reconnect during a minutes-long revision run."
  - phase: 16-terminal-state-integrity
    items:
      - "Deferred (live): SC1 real ValidationException → pipeline_failed and SC2 real Stop → pipeline_cancelled on the wire (offline fault-injection complete)."
  - phase: 18-custom-workflow-ux
    items:
      - "Deferred (visual): ISS-019 wave-panel pixel-fold proof + ISS-021 live custom-HTML iframe render — claimed covered on the default profile in the 2026-06-13 campaign; re-confirm at archive."
      - "WARNING (integration): generic mimetype deliverable renderer is fallback-only (PreviewPanel.tsx:151) — 4 first-party per-type branches precede it; custom types render, but the SC-001 dividend is not the primary dispatch path."
  - phase: 19-prompt-and-deliverable-adherence
    items:
      - "Deferred (live): ISS-004 chunks clean / ISS-005 /api/v1 present / ISS-006 no getDb error on real Haiku — durable structural fixes landed offline; live re-confirm batched to the consolidated pass."
  - phase: 20-workflow-catalog
    items:
      - "WARNING (integration): the default `home` landing still mounts the hardcoded CreationHub WORKFLOWS array (CreationHub.tsx:15 via DashboardLayout.tsx:1079); the data-driven WorkflowCatalog lives on the separate `catalog` nav tab. P20 SC-1 ('no hardcoded name list') holds only on the catalog tab. Read-only UI duplication (no INV-12 dual-write, no dead-end) — product-intent decision on the intended landing surface."
  - phase: cross-cutting
    items:
      - "Nyquist validation coverage weak: 1 COMPLIANT (P14) / 10 PARTIAL (draft, nyquist_compliant:false: P4-12,15) / 10 MISSING (no VALIDATION.md: P1-3,13,16-21). Discovery-only; run /gsd-validate-phase per phase to fill."
      - "ISS-028 (LOW, FIXED): app_builder task-loop partial failure degraded-decision now keyed on (agent_id, task_number); recorded as the deferred ISS-016 'second signal' (empty-but-clean→degraded) follow-up."
      - "ISS-018 (env, external): hexaware-srini Bedrock account lost ConverseStream entitlement mid-campaign (org SCP/permission-set, not quota). Live re-verification runs on the `default` profile (acct 473293451041). IT escalation drafted; no repo code fix."
      - "CTX-04 (VOIDED/DEFERRED): dead-_handle_revision deletion deferred pending a product decision on retiring run_revision (the handler is LIVE). Documented across SUMMARY/SPEC/REQUIREMENTS/ledger — intentional, not a gap."
---

# Milestone v1.0 — Audit: Workflow Engine Decoupling & Universal Workflow Runtime

**Audited:** 2026-06-14 · **Status:** `tech_debt` (all requirements met, no blockers, accumulated deferred items to review before archive)

## Summary

The milestone achieved its definition of done. The prototype-coupled `ExecutionEngine` is now a workflow-agnostic kernel driven by declarative manifests, and the **core value (SC-001)** is proven: a brand-new non-prototype `task_loop` workflow runs from manifest + AGENT.md only with zero engine edits (Phase 7, plan 07-11). All 117 v1 requirements trace to a passed phase; the kernel carries no workflow-name branches; the LangChain `deepagents` mandate (INV-13) holds; ports-and-adapters boundaries are clean (import-linter 4/0); the migration chain is additive and reversible (0014→0021).

It is **not** a clean `passed` because of accumulated, non-blocking debt: a set of deferred live-Bedrock re-confirmations (partly closed by the 2026-06-13 / 2026-06-14 campaigns, the rest blocked on the external ISS-018 entitlement), weak Nyquist VALIDATION.md coverage, and two integration warnings from the cross-phase wiring check (home-vs-catalog dual surface; fallback-only generic renderer). None block completion.

## Requirements coverage (3-source cross-reference)

Cross-referenced REQUIREMENTS.md traceability ↔ each phase VERIFICATION.md ↔ each SUMMARY.md frontmatter.

| Metric | Result |
|--------|--------|
| v1 requirements defined | 117 (one row per REQ-ID; reconciled 2026-06-14 via ISS-012) |
| Mapped to a phase | 117 / 117 ✓ (Phases 1–12) |
| Found in ≥1 VERIFICATION.md | 117 / 117 ✓ (orphan check clean) |
| Found in ≥1 SUMMARY.md | 117 / 117 ✓ |
| **Satisfied** | **116** (VERIFICATION passed ∧ SUMMARY-listed ∧ traceability `[x]`) |
| **Intentional VOID** | **1** — CTX-04 (`_handle_revision` is the live `run_revision` handler; deletion deferred pending a product decision; documented everywhere — not a gap) |
| Unsatisfied | 0 |
| Orphaned (in traceability, absent from all VERIFICATIONs) | 0 |

> Note on SAFE-01/02/03 and CTX-03: their SUMMARYs record coverage under `provides`/`tags`/`requirements:` rather than the `requirements_completed:` frontmatter field (older plan convention) — verified present, not coverage gaps.

Phases 13–21 are post-milestone gap-closure phases (findings F1–F7 + ISS-002…028); they add no new v1 REQ-IDs. Notably **WF-DB-01** (listed v2/deferred) was pulled forward and **shipped** in Phases 20–21 (data-driven catalog + saved user workflows).

## Phase verification roll-up

All 21 phases: `status: passed`. Scores (must-haves verified): P1 3/3 · P2 4/4 · P3 8/8 · P4 11/11 · P5 14/14 · P6 13/13 · P7 9/9 · P8 29/29 · P9 4/4 · P10 14/14 · P11 13/13 · P12 15/15 · P13 6/6 · P14 4/4 · P15 5/5 · P16 5/5 · P17 4/4 · P18 5/5 · P19 4/4 · P20 5/5 · P21 5/5. Zero overrides applied anywhere.

## Cross-phase integration (9 E2E flows)

`integration-checker` verdict: **integration clean — no blockers.** 7 VERIFIED, 2 PARTIAL (warnings).

| # | Flow | Status | Evidence |
|---|------|--------|----------|
| 1 | Manifest → compile → execute | VERIFIED | `compiler.py` raises `CompilerError` on unknown capability (:206/:268/:321); `registry.discover()` at startup; engine runs from `compiled.*`, no name branch |
| 2 | Prototype parity chain | VERIFIED | 5 goldens present; `od_prototype`→`prototype` manifest; banned-pattern greps clean (`accumulated_outputs`=0) |
| 3 | Typed artifacts + persistence + ownership | VERIFIED | mirror deleted (L15, 0 hits); `ScopedStore` default-deny; `run_events`/`subagent_runs`/`wave_runs`; reconnect `after=<seq>` (websocket.py:840-871) |
| 4 | Model resolution end-to-end | VERIFIED | FE `model_overrides` → `_validate_model_overrides` → `execute(model_overrides=)` → `ModelResolver` precedence #1 (model_policy.py:18/68) |
| 5 | Fan-out → wave → durable resume | VERIFIED | single spawn path: `fanout_batch.py:92` + `wave_scheduler.py:251` both call `runner.run_fanout`; `spawn_subagents` funnels to it (fanout.py:241) |
| 6 | WS reconnect frame contract | VERIFIED | live-attach ack `live:True` (websocket.py:871); replay ack `live:False`+status (:849); terminal states faithful (:652/:911) |
| 7 | run_revision real loop | VERIFIED | WS branch → `_handle_revision` → `execute()` (not echo); frames carry real `section`; `derived_from` persisted (engine.py:2048) |
| 8 | FE catalog + saved workflows | **PARTIAL ⚠** | catalog filters `user_launchable`; `/api/user-workflows` CRUD owner-scoped + re-validate-at-launch; dead writer removed (INV-12). **But** default `home` view still renders hardcoded `CreationHub` |
| 9 | Generic deliverable renderer | **PARTIAL ⚠** | BE emits `deliverable_mimetype`/`_filename`; FE dispatches html→iframe/md/zip with no per-workflow branch — **but fallback-only** behind 4 first-party branches (PreviewPanel.tsx:151) |

### Invariant spot-checks
- **SC-001 / INV-1** — PASS. No `pipeline_type ==` / `spec.id ==` / workflow-name equality in kernel decision paths.
- **INV-13** — PASS. `create_deep_agent` sole call site `app/agents/deep_agent_runner.py:291`, reached only via the `langchain_deepagents` adapter.
- **INV-3 goldens** — PASS. All 5 fixtures + `test_characterization_*` present and wired (suite not run — network/Bedrock-gated).
- **Import-linter** — PASS. 4 kept / 0 broken.
- **Migration chain** — PASS. 0014→0021 single head, every revision reversible, `owner_id`+`workspace_id` on each new table; 0021 reuses the dormant `workflows` table.

## Tech debt by category

### A. Deferred live-Bedrock re-verification (per the project defer-live-verification convention)
Substantially exercised by the 2026-06-13 (phases 16–19) and 2026-06-14 (saved workflows) live + Playwright campaigns; the remainder is gated on **ISS-018** (the `hexaware-srini` account lost Bedrock entitlement; the `default` profile, acct 473293451041, works). Outstanding live items: P6 CR-02 (checkpointer fallback), P8 real OTLP collector export, P13 F1/F4/F5, P14 SC4 + live cancel/reconnect, P16 SC1/SC2, P18 ISS-019/021 visual, P19 ISS-004/005/006 re-confirm.

### B. Nyquist validation coverage (weak — discovery only, non-blocking)
| Classification | Count | Phases |
|----------------|-------|--------|
| COMPLIANT (nyquist_compliant: true) | 1 | 14 |
| PARTIAL (VALIDATION.md exists, draft / false) | 10 | 4, 5, 6, 7, 8, 9, 10, 11, 12, 15 |
| MISSING (no VALIDATION.md) | 10 | 1, 2, 3, 13, 16, 17, 18, 19, 20, 21 |

Run `/gsd-validate-phase <N>` per flagged phase to fill coverage retroactively.

### C. Integration warnings (product-intent, non-blocking)
1. **Home-vs-catalog dual surface** — default `home` landing mounts the pre-P20 hardcoded `CreationHub.WORKFLOWS` array; the data-driven `WorkflowCatalog` is a separate `catalog` tab. P20 SC-1 ("no hardcoded name list") holds only on the catalog tab. Decide: redirect `home` to the data-driven catalog, or keep `CreationHub` as the intended landing.
2. **Generic renderer is fallback-only** — `PreviewPanel.tsx:151` reaches the mimetype dispatch only after 4 first-party per-type branches. Custom/unknown types render correctly; the SC-001 dividend just isn't the primary path.

### D. Recorded non-code / deferred follow-ups
- **ISS-028** (LOW, FIXED) — degraded-decision granularity; recorded as the deferred ISS-016 "second signal" follow-up.
- **ISS-018** (env, external) — Bedrock entitlement; IT escalation drafted; no repo fix.
- **ISS-020** (harness NOTE) — throwaway ws_rewrite driver; fixture workflows have no product launch surface.
- **CTX-04** (VOIDED/DEFERRED) — `run_revision` retirement is a product decision.

## Issues register

28 issues logged (ISS-001…028). **0 OPEN code issues.** Every code/test/docs issue FIXED (the 2026-06-14 sweep closed the last 7: ISS-012/022/023/024/025/026/028); ISS-015 (was WONTFIX) **shipped** as WF-DB-01 in Phase 20. Only two remain as non-code NOTEs: ISS-018 (external env) and ISS-020 (throwaway harness).

## Conclusion

Milestone v1.0 **met its definition of done with no blockers**. The deferred live re-confirmations, the Nyquist coverage gap, and the two integration warnings are reviewable debt — not gates. Recommended before archive: (1) decide the two product-intent UI warnings, (2) run one consolidated live-Bedrock pass on the `default` profile to clear the remaining deferred live items, (3) optionally backfill Nyquist VALIDATION.md for the 20 partial/missing phases.
