# Milestone-End Live Re-Pass — 2026-06-12 — COMPLETE

Real AWS Bedrock Haiku 4.5 (`eu.anthropic.claude-haiku-4-5-20251001-v1:0`, eu-central-1,
AWS_PROFILE=hexaware-srini) · real Postgres 16 (app DB + AsyncPostgresSaver checkpointer,
alembic 0020) · stock backend via the sample-admission launcher (zero repo edits) ·
Next.js dev FE up. Driver scripts + evidence: `/tmp/flowin-live-pass2*.py`,
`/tmp/flowin-live-evidence/` (ws2-/ws2b-prefixed), `/tmp/cr02_driver.py`.

Scope: the deferred-item checklist from /gsd-audit-uat 2026-06-12 + the register's
end-of-milestone live obligations (06-UAT CR-02; 13 F1/F4/F5 re-confirm + F8 re-captures;
14 SC4; Phase-3 COMPACT-03 figure; Phase-8 sweep; Phase-11 isolated writes; 13 WR-02/WR-04
human confirms; test_pipeline_cancel).

## Verdicts so far

| # | Item | Verdict | Evidence |
|---|------|---------|----------|
| 1 | **Phase 6 CR-02** live-checkpointer fallback restart | **PASS** | `01-cr02-checkpointer-fallback.md` — base thread froze with partial durable state (`branch:to:model`), retry ran fresh on `{base}:retry1`, `agent_model_fallback{fallback_model=haiku, reset_output:true}`, coherent Haiku-only deliverable |
| 2 | **F1** declared human gates stream live (13-01) | **PASS** (both paths) | Default/inline: `ws2b-od_prototype-gates` — review_gate_ready arrived t=34.0s/t=50.6s, approved on receipt, run completed (no speculative workaround). Declared-streaming (`gate_agent_ids:[]`): `ws2b-od_prototype-declared-gate` — both gates arrived t=4.9s/t=25.5s via evaluate_stream |
| 3 | **F4** no fabricated tool-XML on live Haiku (13-02) | **PASS** | 0 XML hits across chunks+finals of user_stories (6 text-only agents), od_ppt, all revisions (regex `<function_calls|<invoke|write_todos|<antml`) |
| 4 | **F3 ingress guard** (13-06) | **PASS** (incidental) | bare `prototype` rejected pre-spin-up with `missing_template_context` naming the inject-declaring agents — fired exactly as designed |
| 5 | **Phase 14 SC4** run_revision end-to-end mechanics | **PASS** | FE-exact `od_ppt_output` frames: real `execute()` dispatch (118-frame revision, model ran, tools:[] bounding logged), FR-014 chain resolution logged (`resolved_kind='deliverable'` for parent; exact-kind for rev-of-rev), `derived_from` lineage rows, terminal fidelity (completed/cancelled; cancelled runs write NO exact-kind ref — no poisoned parents), seq 1..118/1..42 contiguous (single stamping chokepoint), A1: zero questionnaire_ready on every revision (planner:skip live) |
| 6 | **14 SC4 content** ("revised deck in the preview") | **BLOCKED by LV-02** | revision loop is healthy; the content entering the chain is the parent's narration deliverable (below) |
| 7 | **od_ppt template-seeding retest** (8ba8f626 / F8a) | **PASS (seeding) / new finding LV-02 (deliverable)** | composer made real template reads (glob+read_file on seeded files) and streamed a 21,394-char `<artifact>` deck (`<section class="slide">` ×5, validator-QA-confirmed); deck also written to sandbox `ai-platform-overview.html` |
| 8 | **WR-03** revision section on live-attach | **PASS** | `wr03-live-attach-frames.jsonl` — second-socket live attach: ack "Reconnected — resuming pipeline stream", 29 live frames all `section='od_ppt_output'`, terminal included |
| 9 | **Phase 11 isolated writes** (CR-02 wiring, real models) | **PASS** | driver-bound base workspace: 3 real-Haiku workers spawned `isolation:sub_sandbox` (events + subagent_runs rows), per-worker writes, `copy_disjoint` merged part_1..3 with 0 conflicts; fragment artifact_refs persisted (typed `file_bundle`) |
| 10 | **F6** sample_fanout deliverable (13-04) | **PASS** | product-path run: serialized bundle with all 3 parts as final_output; 0 "falling back to streamed" hits in the backend log |
| 11 | **API-06** capability palette | **PASS (API) / FE half known-unmounted** | `/api/capabilities` auth-gated, 60 registry-reflective capabilities across 20 kinds + 5-model catalog; FE CapabilityPalette still only in the unrouted WorkflowComposer (pre-existing Phase-12 note, not a regression) |

## Known-Info debt LIVE-CONFIRMED (no action, matches the register)

- **IN-02 (13/14)** — `pipeline_cancelled` ack not delivered on the live wire for cancel_pipeline
  (driver idled out twice); the run row still lands `cancelled` AND the durable log persists
  `pipeline_cancelled` (a reconnect learns the outcome — softens the gap).
- **IN-04 (14)** — durable-REPLAY revision frames carry `section: None` (live-attach carries the
  real section; replay path uncovered, as recorded).
- **IN-01 (12)** — `pipeline_reconnected` live-attach ack omits `live:true` (only `live:false` explicit).
- **Cancel-on-disconnect** applies to revision runs as to pipelines (client drop mid-revision →
  run cancelled; durable tail replayable). Pre-existing policy, now observed on the revision path.

## NEW findings this pass

### LV-01 (test-infra, minor) — test_pipeline_cancel failure is NOT credential-gated
With VALID creds it still fails deterministically (3/3): the handler task finishes normally
(`result=None`) because the 0.15s `task.cancel()` collides with the 0.05s-cadence stub events
inside `asyncio.wait_for(event_queue.get(), timeout=10.0)` (websocket.py:1669) — the CPython 3.11
`wait_for` result-vs-cancel race absorbs the cancellation. Same pattern at websocket.py:802 and
:1999. Supersedes the 14 deferred-items "expired token" hypothesis. Candidate fix: `asyncio.timeout()`
context (correct cancel semantics) at the three drainer sites, or re-time the test off the event boundary.

### LV-02 (major, product) — od_ppt deliverable takes the validator's narration, not the deck
`deliverable: ppt` resolves `unwrap_artifact(sanitize(ctx.last_streamed))` (capabilities/deliverables/ppt.py)
— the LAST agent's stream. Live Haiku's od-ppt-validator streams a QA REPORT (1,350 chars,
"I'm running the final QA pass…") instead of re-emitting the corrected deck, so final_output/preview
gets narration while the composer's real 21K template-driven deck sits one stream earlier (and on
disk). Downstream: the 13-05 `deliverable` artifact ref persists the narration, so the FR-014
revision chain feeds narration to revision agents (rev2's model explicitly asked for the missing
deck). Phase-14 surfaces are NOT implicated — the loop faithfully revised what it was given.
F5-family root cause (missing output contract): candidate fix is a prompt-contract line on
`od-ppt-validator` AGENT.md ("re-emit the complete corrected deck wrapped in `<artifact>`"),
mirroring the 13-03 app-devops fix; alternative: resolver fallback to the most recent
`<section`-bearing artifact stream (engine-side, INV-3-sensitive — prompt fix preferred).

### LV-03 (test-infra, minor) — test_phase3_token_delta_live hangs live at the clarify gate
The 13-04 repair fixed the monkeypatch seams (collection passes offline) but the live drive blocks
forever (45+ min, 0% CPU, no sockets) at the clarify `event.wait()` — `_drive_live` no-ops review
gates and passes `gate_agent_ids=[]` but never disables `clarify.mode=auto`, and nothing answers
the questionnaire. The test has never completed live (Phase 3 had no creds). Fix: flip compiled
clarify.mode off inside `_drive_live` (harness precedent). COMPACT-03 figure captured via a
driver-side patched run (pending below).

## Pending — NONE (all sections below record the completions)

## WR-02 / WR-04 human-confirm material (live-collected)

Two gate-review paths now exist, with different review semantics, observed live:

| Path | When it pauses | What the reviewer sees | Live evidence |
|------|----------------|------------------------|---------------|
| Inline (AGENT.md `gate: Human_Gate`, the FE default) | AFTER the gated agent runs | that agent's own output (specify: 19,910 ch; plan: 6,842 ch) | `ws2b-od_prototype-gates` |
| Declared (manifest `gates: [human]`, fires when inline set is empty) | BEFORE the gated step runs | the PREVIOUS step's output — first gated step shows EMPTY (len 0) | `ws2b-od_prototype-declared-gate` |

WR-02 question: when an agent has BOTH inline + declared gates, the declared one is skipped
(single review, inline wins — observed: exactly 1 prompt/agent). Confirm this dedupe is the
intended product UX.

WR-04 question: a declared-gate EDIT applies to the PREVIOUS step's artifact (the content shown
at the pause). Confirm this pre-step-edit semantic; note the first-step empty-review corollary
above (a declared gate on step 1 has nothing to show/edit — possibly worth a product rule:
"declared human gates only meaningful from step 2 onward" or surface the upcoming step's INPUT
instead).

## F5 re-audit (app_builder, 15 agents live, 3 parallel reviewers) — PASS w/ 3 minor residuals

2026-06-11 state: 4 agents dead (2 empty, 1 stack-stall refusal, 1 narration-only), tests
uncompilable against impl, /api vs /api/v1 drift. 2026-06-12 state (capture2-app_builder,
completed, 6.8M tok, 2599s):

- **5/5 previously-broken agents PASS**: code-compliance (980-line on-domain config, zero
  migration language), test-compliance (721-line strategy), sdlc-governance (6 real file blocks,
  "greenfield with no legacy constraints"), devops (**32 `filename:` file blocks** — was
  narration-only), system-design (complete 7-section design, assumptions table instead of stall).
- **All 15 agents substantive** (min 8.8K chars), stack 100% Node/TS (zero Java/.NET hits).
- **Auth contract drift GONE**: impl exports (authService/ValidationError/AuthError/bcryptjs/
  EMAIL_EXISTS…) exactly match what tests import/assert.
- Residual 1 (minor): `tests/setup.ts` imports `getDb`; impl exports `getDatabase` — one TS2305
  in the Jest global setup (vs a wholly uncompilable suite before).
- Residual 2 (minor): infra-generator emitted unversioned `/api/*` (0×`/api/v1` in 3,083 lines)
  while api-design (58×) + devops (50×) follow the 13-03 `/api/v1` standard — live-model
  adherence variance against a prompt rule that is present.
- Residual 3 (F4 residual): 10 fabricated tool-XML elements in app-sdlc-governance's stream
  PREAMBLE (5 `<function_calls>`+5 `<invoke name="read_file">`, lines 2–36) — 1 of 15 agents
  (was widespread). Note: packets are built from raw chunks; the WR-01 sanitizer cleans the
  engine-path persisted output, the UI chunk stream is deliberately unfiltered.

## COMPACT-03 live figure (Phase 3) — recorded with confound caveat

Patched driver (clarify off — LV-03 workaround) ran the full A/B live:
compaction OFF: input 1,423,757 / output 33,993 · compaction ON: input 2,879,492 / output 57,709.
Run-level totals are CONFOUNDED by build nondeterminism (the ON run built a larger prototype —
more tasks/fix-loops; the skeleton path cannot increase per-message size). The controlled,
deterministic measurement remains the offline per-message gate: 96.9% reduction
(test_phase3_compaction). COMPACT-03's "real-run evidence" = this attempt + caveat; a clean
same-plan A/B would need a pinned task plan (future option), not re-runs.

## F8 mulesoft-with-source — inventory agent VERIFIED (partial capture)

capture2-mulesoft_to_springboot: 10/13 agents completed before the daily quota died (last 3
empty: test-compliance, validation, sdlc-governance). The deferral's question is answered:
given embedded source (5-flow order-intake XML + RAML + config), `mulesoft-inventory` produced
a faithful estate inventory — all 5 flows with correct triggers/connectors/purposes, Mule 4.x
inferred from DataWeave 2.0 syntax, the >10000 review flag + VM queue mapped. 57,216 chars.

## Quota event — 2026-06-12 ~19:15

Bedrock daily token quota exhausted on AWS_PROFILE=hexaware-srini after ~13M tokens
(22 ThrottlingException hits, "Too many tokens per day"). Blocked: dotnet_to_azure + chat
re-captures, Phase-8 live sweep (~18M tokens — needs a fresh quota window of its own).

## WR-02 / WR-04 — HUMAN-CONFIRMED 2026-06-12

Both Phase-13 fix decisions confirmed by the user on this pass's live evidence:
- **WR-02 CONFIRMED — keep the dedupe.** One review per agent; when an agent carries both an
  inline (AGENT.md) and a manifest-declared human gate, the inline output-bearing gate is the
  single review.
- **WR-04 CONFIRMED — keep pre-step edit semantics.** A declared-gate edit applies to the
  PREVIOUS step's artifact (the content shown at the pause). The step-1 empty-review corollary
  is accepted as a documented quirk.

## Quota continuation

Remaining Bedrock items moved to AWS profile `default` (account 473293451041, same Haiku 4.5,
fresh quota): dotnet_to_azure + chat captures re-running; Phase-8 sweep to follow.

## Phase-8 live sweep — PASSED (budget VERIFIED), 4 SSO-expiry artifacts

`RUN_LIVE_BEDROCK=1` full sweep on AWS profile `default` (fresh account): **27 passed /
4 failed / 2 skipped in 2:54:30**. Every LIVE pipeline case green: user_stories, prototype ×2,
prototype_revision, app_builder, od_ppt, chat, **handoff coding + test (the 2026-06-11
1-of-2 JSON failure did not recur — 13-02 retry hardening holds)**, mulesoft, dotnet.

**`test_zz_live_cost_summary_under_budget` PASSED — GRAND TOTAL $4.8362 (14.1M in / 1.0M out
= 15.16M tokens) under the 13-04 recalibrated $8.00 ceiling** (yesterday's measured $5.94 and
this run's $4.84 both fit with margin; the old $5.00 ceiling was the calibration drift).

The 4 failures are ONE root cause — the default profile's SSO token EXPIRED ~2.5h into the
sweep, at the HITL tail: `test_live_gates_off_completes_without_gating`,
`test_live_gate_on_pause_then_auto_resume`, `test_live_runner_tool_gate_resume` (zero-token
captures / 'error'), and `test_live_gate_is_consistent_and_self_skipping` which captured the
proof ("security token expired"). NOT quota, NOT product failures. The HITL gate behavior is
independently live-proven this pass on the REAL WS product stack (ws2b gate scenarios:
review_gate_ready streamed, approved, run resumed — both gate paths). Optional cleanup: re-run
`pytest tests/agents/test_phase8_live.py::TestLiveHITL -v` after a fresh `aws sso login`.

**OTLP export closure:** declaration-driven hooks mean legacy pipelines emit no spans by
design; real OTLP export needs an OTLP collector endpoint + a hook-declaring workflow —
neither exists in this environment. Environment-dependent, NOT a code gap (console/logging
exporter verified in Phase 8). Repro when an endpoint exists: declare `hooks: [otel_tracing]`
on a step + set OTEL_EXPORTER_OTLP_ENDPOINT.

## Pass complete — 2026-06-12/13

All checklist items executed or dispositioned. Bedrock spend across the pass: ~13M tokens
(hexaware-srini, to quota) + ~20M tokens (default profile incl. the sweep).
