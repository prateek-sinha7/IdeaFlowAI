## Phase 13 — Live Verification Gap Closure [(post-milestone)]

**Folder:** `.planning/phases/13-live-verification-gap-closure/`  ·  **Status:** Complete (2026-06-12) — verified PASSED (6/6 at the offline boundary); **security-audited `threats_open: 0` (the CR-01 Critical is now FIXED)**; ⚠ 13 functional review findings (7 Warning / 6 Info) remain open  ·  **Plans:** 6/6  ·  **Plan-id → source plan §25:** (post-milestone, no plan-id)
**Requirements delivered:** None new — closes live-verification findings **F1–F8** (no REQUIREMENTS.md ids; post-milestone UAT). → ROADMAP "Phase 13: Live Verification Gap Closure" (Requirements) / REPORT.md "Findings register". **NOTE:** these live-verification **F1–F8** are a DIFFERENT numbering space from the glossary factory-leak **F1–F5** — do not conflate. When this section says "F#" it always means the LIVE finding unless it says "F#(factory)".
**One-line outcome:** The real-Haiku-4.5 product gaps found AFTER v1.0 was marked complete on offline evidence are now closed in code — declared human-gate events stream to the WS client before the await (F1), `run_revision` resolves via a new generic `deliverable` artifact kind + fallback chain (F2), all-agents-failed runs emit `pipeline_failed` instead of a silent `pipeline_complete` (F3), tool-less agents get an anti-fabrication preamble + handoff retry (F4), the four broken app_builder prompts are re-templated for greenfield scope (F5), and `sample_fanout` + two live-test seams are repaired (F6/F7) — all engine-additive, SC-001-preserving (no workflow-name literal added to the kernel), characterization byte-parity held.

> FOR A FUTURE AGENT: Phase 13 is now EXECUTED, REVIEWED, SECURITY-AUDITED, and VERIFIED PASSED. All six SUMMARY files, 13-REVIEW.md, 13-SECURITY.md, 13-VERIFICATION.md and the now-final 13-UAT.md (status `resolved`) exist on disk. Do NOT re-fix F1–F7 — they are closed. **UPDATE 2026-06-12: the CR-01 Critical (`approve_review` ownership/IDOR) has since been FIXED and re-audited** (commit `b7203212`; `13-SECURITY.md` `threats_open: 0`; regression `test_approve_review_ownership.py` 7/7) — so the gate/approval write path is now owner-gated. The remaining open carry-forwards are the **7 Warning + 6 Info functional findings in `13-REVIEW.md`** (NOT yet fixed; the review file still reads `issues_found`). The two that most affect "does the user-facing fix actually work end-to-end": **WR-01** (F4's XML sanitizer is inert on the real engine pipeline path) and **WR-06** (F2's revision still routes to no FE preview branch + remains a Phase-3 stub deliverable). **WR-07** is security-relevant (gate eval can fail-OPEN `security`/`human` gates to PASS on a mid-stream exception). See §6/§7.

### 1. Goal & Success Criteria (what was PLANNED)

Goal: close the product gaps surfaced by the 2026-06-11 post-milestone **live-Bedrock** verification pass — every workflow + custom workflows with all options on real AWS Bedrock Claude **Haiku 4.5** (`eu.anthropic.claude-haiku-4-5-20251001-v1:0`), with semantic review of every agent's output by 8 parallel **Opus 4.8** reviewer agents. v1.0 had been marked complete on offline (scripted-model) evidence; this live pass found 8 gaps the offline suite could not catch. → REPORT.md "Live-Bedrock Verification Pass — 2026-06-11"; ROADMAP "### Phase 13" (Goal). **Depends on: Phase 12.** Wave structure: Wave 1 = 13-01/02/03/04 (independent, `depends_on: []`); Wave 2 = 13-05 (`depends_on: [13-01]` — shares the engine.py diff base); Wave 3 = 13-06 (`depends_on: [13-05]` — its total-failure branch composes with 13-05's `final_output and results` deliverable-write guard).

The 6 ROADMAP success criteria (one per finding-cluster) — each verified TRUE at the offline boundary; live re-verification deferred per project convention:

1. Declared `gates:[human]` step surfaces `review_gate_ready` BEFORE awaiting the response; client approves; run proceeds (F1). → ROADMAP SC1; closed by **13-01**.
2. `run_revision` with the FE-exact payload (`ppt_output`/`od_ppt_output`) against a completed parent passes FR-014 and drives a revision (F2). → ROADMAP SC2; closed by **13-05**.
3. All-agents-failed runs terminate as a visible failure / fail fast at ingress — never `pipeline_complete` with empty deliverable (F3). → ROADMAP SC3; closed by **13-06**.
4. Text-only agents emit no fabricated tool-call XML; handoff coder reliably returns parseable JSON (F4). → ROADMAP SC4; closed by **13-02**.
5. All 15 app_builder agents role-conformant; the 3 migration-templated prompts re-templated; devops emits files not narration (F5). → ROADMAP SC5; closed by **13-03**.
6. `sample_fanout` resolves its declared deliverable (no `single_file` fallback); `test_phase3_token_delta_live` runs against the current engine; phase-8 budget recalibrated (F6/F7). → ROADMAP SC6; closed by **13-04**.

Findings register (root cause + file:line artifact) — F1–F7 are the ROADMAP-named set; F8 is environment-only (no code plan). Full definitions → REPORT.md "Findings register" + 13-UAT.md "Gaps":

- **F1 (major, product)** — Declared-gate events undeliverable. `HumanGate.evaluate` consumed the whole `run_human_gate` delegate into a list; `_evaluate_gates` yielded events only post-resolution → `review_gate_ready` reached the client only after approval; a UI run hung at the first declared gate. → REPORT.md F1; 13-UAT.md Test 1 / Gaps[0]. **Closed by 13-01.**
- **F2 (major, product)** — `run_revision` dead end-to-end. FE sends `target_artifact_type: "ppt_output"`/`"od_ppt_output"`; FR-014 lookup `list_refs(kind=target)` matched no persisted kind (`grep ppt_output backend/` = 0 writers); revision characterization tests masked it by seeding the exact target kind. → REPORT.md F2; 13-UAT.md Test 2 / Gaps[1]. **Closed by 13-05.**
- **F3 (major, product)** — Inject-missing agents halt silently but the run still "completes". Bare `prototype`/`ppt` (no od_context) → injects-declaring agents `agent_error` and halt, yet pipeline ends `pipeline_complete` with empty/improvised output (ws-ppt deck authored solo by the validator — role inversion). → REPORT.md F3; 13-UAT.md Test 3 / Gaps[2]. **Closed by 13-06.**
- **F4 (major, model robustness)** — Live Haiku fabricates tool-call XML (`<function_calls>`/`<invoke>`/`write_todos`) as text when prompts imply tool actions without bound tools (handoff coder returned XML not JSON in 1/2 runs; XML leaked into user_stories/custom/app_builder/dotnet prose). The od_ppt instance was ALREADY FIXED pre-phase (commit `8ba8f626`); remaining = text-only prompt hygiene + handoff hardening. → REPORT.md F4; 13-UAT.md Test 4 / Gaps[3]. **Closed by 13-02.**
- **F5 (major, agent config)** — app_builder prompt defects (Opus 15-agent audit): `app-code-compliance`/`app-test-compliance`/`app-sdlc-governance` carry Java/.NET *migration* template language → stalls/empties; `app-devops` narration-only; `app-system-design` stalls on clarifying questions; auth impl↔test contract mismatch; `/api` vs `/api/v1` drift. → REPORT.md F5; 13-UAT.md Test 5 / Gaps[4]. **Closed by 13-03.**
- **F6 (minor, samples)** — `sample_fanout` declares `single_file: merged.txt` which no step produces → fallback degrades output to one worker's text. Same quirk 12-10 fixed for sample_wave (commit `8ffae37c`); fanout was out of scope then. → REPORT.md F6; 13-UAT.md Test 6 / Gaps[5]. **Closed by 13-04.**
- **F7 (test-infra)** — `live_harness` bit-rot already FIXED pre-phase (commit `6e68d9f4`); `test_phase3_token_delta_live` monkeypatched the deleted `_build_context_message` seam (→ AttributeError at setup); phase-8 `LIVE_BUDGET_USD=5.0` < measured $5.94. → REPORT.md F7; 13-UAT.md Tests 7 & 8 / Gaps[6][7]. **Closed by 13-04** (seam repair + budget recalibration).
- **F8 (environment, NO code plan)** — migration pipelines need source-repo inputs the bare-brief harness lacks; Bedrock daily token quota exhausted late in the pass (dotnet/chat captures degraded, od_ppt retest blocked). Re-run / environment-fidelity follow-up, EXCLUDED from the gap-closure plans (deferral note in 13-UAT.md frontmatter). → REPORT.md F8. **DEFERRED — no plan; re-run after quota reset with repo inputs.**

### 2. What Was Implemented — per plan (what was BUILT)

All six **Complete**; rows reflect SUMMARY actuals (deviations called out in §6).

| Plan | Title (short) | What it built (names only) | Key modules/files touched | Status | Detail |
| --- | --- | --- | --- | --- | --- |
| 13-01 | Declared-gate streaming (F1) | `HumanGate.evaluate_stream` + `ApprovalGate.evaluate_stream` (async-gen, immediate re-yield, terminal `GateOutcome(events=[])`); `evaluate()` rewritten as a thin collector over the stream (INV-12 single impl); duck-typed streaming branch in `_evaluate_gates`; regression test `test_declared_gate_streaming.py` | `gates/human.py`, `gates/approval.py`, `engine.py`, `tests/agents/test_declared_gate_streaming.py` | Complete | → 13-01-SUMMARY.md (Accomplishments) |
| 13-02 | Text-only prompt hygiene + handoff hardening (F4) | `tool_availability` prompt block (`_NO_TOOLS_PREAMBLE`) emitted when `no_tools` (zero callable tools); `_strip_fabricated_tool_xml` runner sanitizer on the tool-less `done` output; `HandoffCoder` bounded 2-attempt parse-retry + XML-tolerant `_extract_json` + JSON-only `_CODING_SYSTEM_PROMPT` constraint | `factory.py`, `prompt/policy.py`, `deep_agent_runner.py`, `handoff/coder.py`, 2 new tests + `test_guardrails.py` | Complete | → 13-02-SUMMARY.md (What was built) |
| 13-03 | app_builder prompt re-template + cross-agent contracts (F5) | Greenfield re-templated bodies for `app-code-compliance`/`app-test-compliance`/`app-sdlc-governance`; `## Output contract` (filename:-block mandatory) on `app-devops`; anti-stall rules; `## Contract fidelity` (exact export surface) on `app-test-implementation`; stable-export rule on `app-feature-implementation`; single literal `/api/v1` across api-design/infra-generator/devops | 9 × `agents/prompts/app-*/AGENT.md` (bodies only, frontmatter untouched) | Complete | → 13-03-SUMMARY.md (Accomplishments) |
| 13-04 | sample_fanout deliverable + live-test seams + budget (F6/F7) | Manifest swap `single_file: merged.txt` → `strategy: serialized_sandbox` (zero engine edits, SC-001); `test_sc001_fanout` bundle assertions; `test_phase3_token_delta_live` re-pointed at async `_compose_context_message` + `html_skeleton` capability (+ module-level seam-existence assert); `LIVE_BUDGET_USD = 8.0` with measurement citation | `workflows/sample_fanout/workflow.yaml`, 3 × live/sc001 tests | Complete | → 13-04-SUMMARY.md (What was done) |
| 13-05 | run_revision FR-014 closure (F2) | `"deliverable"` added to `ARTIFACT_KINDS`; completion-time `kind="deliverable"` dual-write in `execute()` terminal block (guarded `final_output and results`, `visibility="workspace"`); 3-link FR-014 fallback chain (exact → `deliverable` → `summary`) in `_handle_revision`; de-masked seed tests + organic `test_run_revision_fe_contract.py` | `artifacts/graph.py`, `engine.py`, 2 × `tests/unit/` | Complete | → 13-05-SUMMARY.md (What was done) |
| 13-06 | Pipeline failure semantics + ingress guard (F3) | `pipeline_failed` terminal WS event + state `"failed"` on total collapse; strictly-conditional additive `status:"degraded"` + `agents_failed` on `pipeline_complete`; `_failed_agent_ids` observation in the dispatch loop; `missing_template_context` WS ingress guard keyed on declared `spec.injects`; FE `pipeline_failed` handling (both unions + `useWorkflow` + dashboard) | `engine.py`, `app/api/websocket.py`, `tests/unit/test_pipeline_failure_semantics.py`, 3 × FE files | Complete | → 13-06-SUMMARY.md (What was done) |

### 3. Capabilities, Modules, Schema & API Added

- **Gate streaming protocol** — `evaluate_stream(step, ctx)` async-generator on `HumanGate` + `ApprovalGate` (kind:gate); engine duck-types it via `getattr(gate, "evaluate_stream")`. Non-streaming gates (validation/security) keep the awaited path byte-identically. → 13-01-SUMMARY.md (Accomplishments); 13-VERIFICATION.md (Observable Truths #1).
- **New artifact kind** — `"deliverable"` member of the closed `ARTIFACT_KINDS` vocabulary in `artifacts/graph.py`; a completion-time `kind="deliverable"` artifact_ref now persists the resolved `final_output` (visibility `workspace`, additive — no migration, additive to the existing typed-ref store from Phase 1B; carries owner_id+workspace_id via the existing `_dual_write_artifact`/ScopedStore path). → 13-05-SUMMARY.md (Task 1); 13-VERIFICATION.md (Observable Truths #2).
- **FR-014 fallback chain** — `_handle_revision` now resolves the original via exact `target_artifact_type` → `deliverable` → `summary`, `assert_owns` FIRST; one INFO log names the matched link; FR-014 ValueError message byte-unchanged. No new WS event. → 13-05-SUMMARY.md (Task 2).
- **New terminal WS event `pipeline_failed`** — data {pipeline_type, pipeline_run_id, total_duration, agents_completed:0, agents_total, agents_failed, error, timestamp}; plus additive conditional `status:"degraded"`+`agents_failed` keys on `pipeline_complete` (absent on clean runs → characterization byte-parity). → 13-06-SUMMARY.md (Task 1).
- **New WS ingress rejection code `missing_template_context`** — in `_handle_workflow_execution`; rejects template-inject-declaring pipelines with no loadable `template_body` BEFORE engine/checkpointer/sandbox spin-up; condition mirrors `factory._compose_injection`'s `TemplateMissingError` exactly, keyed on declared `spec.injects` (no pipeline-name branch). → 13-06-SUMMARY.md (Task 2).
- **Prompt-composition slot** — `"tool_availability"` placed FIRST in `PromptAssemblyPolicy.DEFAULT_ORDER`; conditional, so tool-having compositions are byte-identical. Runner gains `_strip_fabricated_tool_xml`; handoff coder gains `_MAX_PARSE_ATTEMPTS`/`_CORRECTIVE_SUFFIX`/`_FABRICATED_TOOL_XML_RE`. → 13-02-SUMMARY.md (Task 1/Task 2).
- **Manifest-only swap** — `sample_fanout/workflow.yaml` deliverable strategy `single_file` → `serialized_sandbox` (data-only, no engine edit). → 13-04-SUMMARY.md (Task 1).
- **FE changes** — `pipeline_failed` added to both WS unions in `types/index.ts`, a teardown case in `useWorkflow.ts`, and terminal handling in `dashboard/page.tsx`; `npx tsc --noEmit` clean. → 13-06-SUMMARY.md (Task 3).
- **_KNOWN movement:** none — Phase 13 registered no new named capabilities (the gate-streaming surface is a method added to existing registered gates; the deliverable kind is a vocabulary member, not a capability). The capability-registry name counter is unchanged by this phase.

### 4. What Was Deleted / Superseded (INV-12 move-don't-copy)

Phase 13 is **almost entirely additive** (gap-closure on a completed milestone), so there are no single-impl collapses or table drops. The move-don't-copy discipline still shows up in three places:

- **`evaluate()` collapsed onto `evaluate_stream`** — the old list-buffering gate body was *replaced* (not duplicated): `evaluate()` is now a thin collector over the single streaming implementation (INV-12, no dual gate logic). → 13-01-SUMMARY.md (Accomplishments).
- **Dead manifest deliverable name removed** — `sample_fanout`'s `name: merged.txt` (a file no step ever wrote) is gone, eliminating the `single_file` fallback path. → 13-04-SUMMARY.md (Task 1).
- **Bit-rotted test seams retired** — `test_phase3_token_delta_live` no longer references the deleted `_build_context_message`/`_extract_html_skeleton` (grep 0/0); it targets the current `_compose_context_message` + the `html_skeleton` capability, with a module-level seam-existence assert so a future engine rename fails at OFFLINE collection, not a paid live run. → 13-04-SUMMARY.md (Task 2).
- **Migration-template prose stripped** — the 3 app_builder compliance/governance prompts had all Java/.NET/Maven/NuGet/legacy/parallel-run language removed (grep 0); `app-test-implementation`'s Java/.NET framework pins were de-templated in a Rule-2 deviation. → 13-03-SUMMARY.md (Deviations). No frontmatter, registry, or agent-id renames. INV-3 gates stayed armed: zero golden/snapshot files modified across all six plans; banned-pattern (`if pipeline_type ==` → 0) and import-linter (4 contracts kept) green throughout.

### 5. Key Decisions & Locked Constraints (do NOT contradict)

- **SC-001 zero-engine-edit / kernel-name-free clauses (INV-1), per plan:**
  - 13-01: streaming branch keys purely on the gate object's capability surface (`getattr evaluate_stream`) — no workflow/agent-id literal added to `agents/execution_engine/`.
  - 13-05: the only new literal in the kernel is the generic kind `"deliverable"` (and `"summary"` in the chain) — `grep ppt_output\|od_ppt_output agents/execution_engine/` = 0; no `*_output` workflow literal.
  - 13-06: failure tracking keys on counters only; the `missing_template_context` ingress guard keys on declared `spec.injects` membership of `"template"` — NOT on any pipeline name (generic, INV-1). 13-04 is a manifest-data-only fix (zero `agents/execution_engine/` edits in its commits).
- **D-13-06-a (product decision, recorded as a code comment at the guard):** bare `prototype`/`ppt` remain ADMISSIBLE pipeline types but REQUIRE template context — they fail fast with `missing_template_context` instead of silently collapsing; the FE always sends `od_*` aliases, so no user-visible flow changes except the broken ones becoming visible. → 13-06-SUMMARY.md (decisions).
- **D-13-05-a:** the FR-014 deliverable ref uses `visibility="workspace"` (Phase 05-06 producer-write policy) so the same-owner cross-run `_handle_revision` read passes the owner+visibility scope filter; `assert_owns(parent_run_id)` stays FIRST (T-5-SEED ordering, L16 ownership). → 13-05-SUMMARY.md (Task 1/2).
- **D-13-03-a:** API prefix standardized on the literal `/api/v1`; default app_builder stack is Node.js/TypeScript when upstream context does not pin one. → 13-03-SUMMARY.md (Decisions Made).
- **INV-13 (deepagents-only):** no new agent-loop code; the handoff coder constructs a FRESH `DeepAgentRunner` per retry attempt (independent one-shots, no re-implemented loop). → 13-02-SUMMARY.md (Task 2); 13-VERIFICATION.md (Invariant Spot-Checks).
- **Generic (no-workflow-name) approach pattern:** the `missing_template_context` guard demonstrates the INV-1 idiom — gate ingress on a *declared inject* (`template` in `spec.injects`), never on a pipeline name; the `pipeline_failed`/degraded branches gate on observed `agent_error` counts, never on `spec.id`.

### 6. Status, Verification & Evidence (what HAPPENED)

- **13-VERIFICATION.md verdict: PASSED — 6/6 must-haves (success criteria) verified** at the offline-verifiable boundary; 0 overrides. Evidence: F1/F2/F3 regressions 8 passed; F4/F5/F6 + loader/guardrails/revision suites 90 passed; INV-3 regression set (5 × characterization + banned_patterns + migration_ledger) 44 passed / 7 skipped; live-gated files collect offline (34 collected, 0 errors); `/opt/homebrew/bin/lint-imports` 4 contracts kept / 0 broken; frontend `npx tsc --noEmit` exit 0 — **~142 passing targeted tests total**. SC-001 grep over `agents/execution_engine/` clean; INV-3 zero golden re-baselines; INV-13 no new loop code. → 13-VERIFICATION.md (Goal Achievement / Behavioral Spot-Checks).
- **Security audit — `13-SECURITY.md`: `status: verified`, `threats_open: 0` (17/17 threats closed)** across a full audit + a scoped re-audit. The plan-time accept for CR-01 (AR-13-01) was WITHDRAWN (disposition flipped accept→mitigate). → 13-SECURITY.md (Threat Register / Audit Trail).
- **13-REVIEW.md (standard depth, 33 files): 14 findings — 1 Critical, 7 Warning, 6 Info.** The **Critical is now FIXED**; the **7 Warning + 6 Info remain OPEN** advisory carry-forwards (the review frontmatter still reads `issues_found`, but its CR-01 entry now carries a RESOLVED banner; no WR/IN fix commits exist). The core mechanisms were judged sound; the open findings flag newly-reachable surfaces:
  - **CR-01 (Critical) — ✅ RESOLVED 2026-06-12** (commit `b7203212`, re-audited `ed10bc05`): `approve_review` (`websocket.py`) now owner-gates the gate-approval write via `_review_gate_owned_by` — it resolves the run from `gate_key` and requires `WorkflowRun.user_id == authenticated principal` before `set_review_response` is reachable; a deny is indistinguishable from an unknown key (no run-existence oracle); keyed on `user_id`, NOT the nullable Phase-5 `owner_id` backfill (which the main creation path leaves NULL). Regression `tests/unit/test_approve_review_ownership.py` 7/7. → 13-SECURITY.md (T-13-01-01); 13-REVIEW.md (CR-01 RESOLVED banner).
  - **WR-01:** the F4 `done`-output sanitizer is INERT on the engine pipeline path (the engine assembles output from `chunk` events, never reads `done`) — only the no-tools preamble (which IS on that path) defends pipeline prose; the sanitizer is defense-in-depth only and does not handle truncated/unterminated XML spans.
  - **WR-02/03/04:** the newly-reachable declared-gate surface has empty review payloads, double-prompts on default prototype runs (declared + inline gate both fire), does NOT cancel the run on reject (state stranded in `waiting_for_user`), and silently discards user edits.
  - **WR-05:** recoverable-timeout `agent_error`s can list an agent in BOTH `agents_completed` and `agents_failed` (contradictory degraded payload).
  - **WR-06:** F2 passes FR-014 and drives, but the emitted `pipeline_type` (`ppt_output_revision`/`od_ppt_output_revision`) matches no FE preview routing branch, and the revision "deliverable" is still the Phase-3 context/instruction blob — the user-facing PPT-revision flow F2 was diagnosed from still produces no visible revised artifact.
  - **WR-07:** `_evaluate_gates` continues past a blocking gate, and the broad exception swallow can fail-OPEN HITL/security gates to PASS.
  - **Info:** IN-01 `ARTIFACT_KINDS` unenforced; IN-02 WS drainers omit `pipeline_failed`/`budget_aborted` from terminal break-lists; IN-03 degraded run shows success in FE but DB marks it `failed` (three-surface disagreement); IN-04 inaccurate "all agents failed" message on gate-halt; IN-05 deliverable `producer_agent` is the last manifest agent not the real producer; IN-06 summary-fallback can resolve an `[Error: …]` placeholder on legacy degraded parents. → 13-REVIEW.md (full register).
- **13-UAT.md final status: `resolved`** — all 8 tests were `issue` at diagnosis; all 8 gaps now `status: resolved` (F1→13-01, F2→13-05, F3→13-06, F4→13-02, F5→13-03, F6→13-04, F7→13-04, F8 deferral-noted). Tests re-verified OFFLINE only (regression suites + source assertions); **live re-verification of F1/F4/F5 actual Haiku behavior + the od_ppt/dotnet/mulesoft/chat re-captures (F8) are DEFERRED to the end-of-milestone live-Bedrock pass** per project convention (defer-live-verification-to-milestone-end), pre-declared in the 13-VERIFICATION.md `deferred` frontmatter (4 items).
- **Key commit SHAs (recorded in SUMMARYs):** 13-01 `eaee864b`+`03d55a75`; 13-02 `78441dad`+`1ce434e4`; 13-03 `d905059b`+`02c68cb0`+`070ace5a`; 13-04 `25c0d270`+`b8bad06a`; 13-05 `6276c92f`+`a76dad36`+`f0474384`; 13-06 `08114a64`+`f54944fe`+`e136c16b`. Pre-phase fixes that closed parts of F4/F7: `8ba8f626` (od_ppt template seeding), `6e68d9f4` (live_harness repair), `8ffae37c` (sample_wave precedent), `b610e9ef` (CAP-03 user_allowed flags).

### 7. Gotchas, Survivors & Carry-Forward

- **Milestone NOT yet fully closed.** STATE.md is `status: verifying`, 13/13 phases / 77/77 plans / 100%; ROADMAP marks Phase 13 `[x]`. Security is now CLEARED (`13-SECURITY.md` threats_open: 0). The remaining gates are: (1) the **end-of-milestone live-Bedrock pass** — re-confirm F1/F4/F5 on real Haiku + re-run the F8 quota-blocked captures (od_ppt template-driven deck retest; dotnet/chat re-captures; mulesoft with a source-repo input); and (2) the **7 Warning + 6 Info functional findings in 13-REVIEW.md**, none yet actioned.
- **CR-01 (the security top-risk) is RESOLVED** — `approve_review` is owner-gated (`_review_gate_owned_by`) and re-audited; a future agent touching the gate/approval surface should preserve that check (`gate_key` → `run_id`, `WorkflowRun.user_id == user.id`, deny == unknown-key error). The remaining open risks are functional, not authz: **WR-07** (gate eval fail-OPENs `security`/`human` to PASS on a mid-stream exception — security-relevant), **WR-01** (F4 sanitizer never runs on the engine pipeline path), and **WR-06** (F2 revision unrouted in the FE + stub deliverable).
- **WR-06 will likely re-flag F2** on the live pass: even though FR-014 now resolves, the revision `pipeline_type` is unrouted in the FE and the revision body is still the Phase-3 stub (no DeepAgent revision loop). Do not assume "PPT revision works end-to-end for the user" — only the FR-014 contract layer is closed.
- **Declared-gate UX (WR-02/03/04) is half-built:** pause/approve/resume (the SC1 contract) works, but empty payloads, double-prompting, no-cancel-on-reject, and dropped edits remain. The F1 regression test deliberately passes `gate_agent_ids=[]`, so the combined default-run (declared + inline) behavior is UNTESTED.
- **Live survivors kept on purpose:** the legacy inline `_should_gate`→`_run_review_gate` gate path is retained (it always worked and is the parity reference for the new streaming path); the Phase-3 revision-context stub in `_handle_revision` is retained (out of F2's scope — F2 only fixed FR-014 resolution); bare `prototype`/`ppt` remain admissible pipeline types (now template-gated, not removed).
- **Latent items for a future agent:** `ARTIFACT_KINDS` is still an unenforced vocabulary (IN-01) — a typo'd kind silently misses every FR-014 chain link; the WS drainers don't early-break on `pipeline_failed` (IN-02); the FE renders a `degraded` run as full success while the DB marks it `failed` (IN-03). These are tracked in 13-REVIEW.md, not yet fixed.

### 8. File Index (every file in this folder)

- `13-01-PLAN.md` — F1 declared-gate streaming plan (objective, 2 tasks, threat model). Open for the streaming-gate protocol contract + parity argument.
- `13-01-SUMMARY.md` — F1 ACTUALS: `evaluate_stream` on both gates, `evaluate()` collector, regression test that fails-by-timeout pre-fix. Commits `eaee864b`/`03d55a75`.
- `13-02-PLAN.md` — F4 prompt-hygiene + handoff-hardening plan (3 defenses, 2 tasks).
- `13-02-SUMMARY.md` — F4 ACTUALS: `tool_availability` preamble, runner sanitizer, coder retry; note WR-01 (sanitizer inert on engine path). Commits `78441dad`/`1ce434e4`.
- `13-03-PLAN.md` — F5 app_builder prompt re-template + cross-agent contracts plan (3 tasks, 9 prompt files).
- `13-03-SUMMARY.md` — F5 ACTUALS: greenfield re-template, `/api/v1`, contract-fidelity rule; 1 Rule-2 deviation (de-templated app-test-implementation). Commits `d905059b`/`02c68cb0`/`070ace5a`.
- `13-04-PLAN.md` — F6/F7 manifest swap + live-test seam repair + budget plan (2 tasks, zero engine edits).
- `13-04-SUMMARY.md` — F6/F7 ACTUALS: `serialized_sandbox` swap, token-delta re-point, `LIVE_BUDGET_USD = 8.0`. Commits `25c0d270`/`b8bad06a`.
- `13-05-PLAN.md` — F2 run_revision FR-014 plan (3 tasks; deliverable ref + fallback chain + de-masked tests).
- `13-05-SUMMARY.md` — F2 ACTUALS: `"deliverable"` kind, completion-time persist, 3-link chain, organic FE-contract regression. Commits `6276c92f`/`a76dad36`/`f0474384`.
- `13-06-PLAN.md` — F3 pipeline-failure semantics + ingress guard + FE plan (3 tasks).
- `13-06-SUMMARY.md` — F3 ACTUALS: `pipeline_failed`/degraded terminals, `missing_template_context` guard, FE handling. Commits `08114a64`/`f54944fe`/`e136c16b`.
- `13-REVIEW.md` — code review (33 files, 1 CR / 7 WR / 6 IN). CR-01 now carries a RESOLVED banner; the 7 WR + 6 IN remain open. **Open this before touching the gate/approval/revision/F2/F4 surfaces** — WR-01/WR-06/WR-07 are load-bearing carry-forwards.
- `13-SECURITY.md` — threat register (17 threats, all closed; `threats_open: 0`, `status: verified`) incl. the CR-01 fix (T-13-01-01, accept→mitigate) + accepted-risks log. **Open this when touching `approve_review`, the gate/approval flow, `run_revision` ownership, or the run_pipeline ingress guard.**
- `13-VERIFICATION.md` — verdict PASSED 6/6 at the offline boundary; full evidence table + 4 deferred live items. Open to confirm a criterion is met or to see what live re-check remains.
- `13-UAT.md` — the 8 diagnosed gaps (status `resolved`), each with root cause, file:line artifacts, and `missing` items. The canonical finding→plan map.
- `.gitkeep` — placeholder; ignore.
- **Adjacent (`.planning/live-verification/`):** `REPORT.md` — the source live-Bedrock pass report defining findings F1–F8, the workflow × verification matrix, and the changes shipped during the pass; `evidence/` — wire frames / suite logs / packet samples cited by the findings. Open REPORT.md for the original live evidence behind any finding.
