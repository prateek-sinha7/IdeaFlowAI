## Phase 14 — run_revision Real Revision Loop (F2 end-to-end) (post-milestone)

**Folder:** `.planning/phases/14-run-revision-real-revision-loop-f2-end-to-end/`  ·  **Status:** Complete (2026-06-12) — VERIFIED 4/4 must-haves (offline), SC4 live-confirm deferred  ·  **Plans:** 4/4  ·  **Plan-id → source plan §25:** (post-milestone, no plan-id)
**Requirements delivered:** none new — completes the **live-verification finding F2** (`run_revision`) end-to-end, beyond its 13-05/WR-06 scoped fixes (validation + FE routing). NOTE on numbering spaces: this is the **live-finding F2** (run_revision deliverable was the Phase-3 stub), NOT the glossary factory-leak F2 — the two F-number spaces are distinct. No REQUIREMENTS.md row maps to Phase 14 (the lone `(F2)` in REQUIREMENTS.md belongs to the unrelated AGENTRT-04). → ROADMAP.md "### Phase 14" / 14-VERIFICATION.md (Requirements Coverage)
**One-line outcome:** `run_revision` now drives the registry's real revision pipeline agents through the normal `execute()` chokepoint (a model actually runs) and persists the agents' **revised** artifact under the exact target kind with `derived_from` lineage — replacing the Phase-3 echo stub that wrote the composed context back as the "revision" with a fake `total_duration: 0.0` completion.

### 1. Goal & Success Criteria (what was PLANNED)

**Closes the Phase-13 deferred feature.** Phase 13 (live finding F2) only closed the *routing + contract* half: `_handle_revision` resolved the parent via the FR-014 chain, composed a three-section revision context, then wrote that CONTEXT back as the "revision" and emitted a hand-rolled `pipeline_complete` with `total_duration: 0.0` — **no model ever ran** (the "Phase-3 stub"). The register's Phase-13 "deferred feature" note (run_revision deliverable still the Phase-3 stub) is **SUPERSEDED here**. → 14-04-SUMMARY.md (Implementation Register Note); ROADMAP.md "### Phase 14" (Goal, engine.py:3656 cited)

**Goal:** dispatch the registry's real revision pipelines (`ppt_revision` / `od_ppt_revision`, resolved via the WR-06 generic alias) through `execute()` with the composed context as `user_message`, and persist the run's actual deliverable as the `derived_from` artifact. → ROADMAP.md "### Phase 14" (Goal)

**Four success criteria** (each → ROADMAP.md "### Phase 14" Success Criteria):
1. SC1 — `run_revision` against a completed parent drives the real revision agents (model runs observed) and the final deliverable is a revised artifact, not the instruction/context blob.
2. SC2 — the revision artifact persists with `derived_from` lineage to the parent original, owner/workspace-scoped, and revision-of-revision still resolves (FR-014 chain link 1).
3. SC3 — stub-pinning tests (`test_revision_intelligence`, `test_run_revision_fe_contract`) updated to the real-dispatch contract; non-revision characterization goldens stay byte/event-identical (INV-3); no workflow-name literal added to the kernel (SC-001).
4. SC4 — live-confirmed on real Bedrock as part of the **milestone-end live pass** (FE-exact `run_revision` frame → revised deck in the preview). DEFERRED by project convention; verification scores it ✓ by confirming the deferral is **recorded**, not by running it.

**Known design wrinkle and how it was settled:** `od_*_revision` agents *could* declare `injects: [template, design_system]`, which would collide with the 13-06 `missing_template_context` ingress guard when revising from a completed parent. Resolution (locked): dispatch passes `od_context=None`; **no revision agent declares any inject** today; the 13-06 guard lives ONLY at the `run_pipeline` ingress, which `_handle_revision → execute()` never traverses; the composed context already embeds the complete parent deck (the physical realization of the template). A future inject-declaring revision agent must FIRST persist `template_id` on `WorkflowRun` (additive migration, Q3) — made loud by an executable pin (14-01). → 14-RESEARCH.md (Settled Design Wrinkle); 14-01-PLAN.md (Task 1 step 4)

**Depends on:** Phase 13. **Wave structure:** Wave 1 = 14-01 (manifest flips + parity traps + scripted harness) ∥ 14-02 (WS queue dispatch, stub-compatible); Wave 2 = 14-03 (engine real dispatch, blocked on 14-01); Wave 3 = 14-04 (intelligence-suite rewrite + phase gate, blocked on 14-03). → ROADMAP.md "### Phase 14" (Plans)

### 2. What Was Implemented — per plan (what was BUILT)

| Plan | Title (short) | What it built (names only) | Key modules/files touched | Status | Detail |
|------|---------------|-----------------------------|----------------------------|--------|--------|
| 14-01 | Manifest planner flips + parity traps + scripted harness | `planner: skip` data flip on both run_revision manifests (INV-5, no engine edit); `_RUN_REVISION_DISPATCHED` frozenset carve-out in BOTH planner parity traps; `test_run_revision_manifests_declare_planner_skip`; executable design-wrinkle pin `test_run_revision_revision_agents_declare_no_template_injects`; three deterministic `_scripts_for` branches | `ppt_revision/workflow.yaml`, `od_ppt_revision/workflow.yaml`, `test_manifest_parity.py`, `test_id_alias_resolver.py`, `_scripted_model.py` | Complete — none-deviated | → 14-01-PLAN.md / 14-01-SUMMARY.md (Accomplishments) |
| 14-02 | WS run_revision queue dispatch + terminal fidelity | `_handle_revision_execution` module coroutine (row create + `_get_or_create_queue` + inner `_run_revision_to_queue` bg task + sentinel drainer); event-derived terminal status (completed/failed/cancelled, later degraded via WR-02); registry-derived `agent_count`; 6-scenario handler suite. DELETED the inline `await _handle_revision(...)` + unconditional `status="completed"` flip + inline `_send_revision_event` | `app/api/websocket.py`, `test_run_revision_ws_dispatch.py` (new) | Complete — none-deviated | → 14-02-PLAN.md / 14-02-SUMMARY.md (Accomplishments) |
| 14-03 | Engine real dispatch + lineage write | `_handle_revision` rewritten to `get_pipeline_agents(revision_pipeline_type)` + forward-and-capture `async for self.execute(...)` through the RAW sender; pre-dispatch empty-registry ValueError; guarded post-dispatch exact-kind `derived_from` write; FE-exact suite rewritten incl. revision-of-revision. DELETED stub/`_stamped_send`/fake event pair (INV-12) | `agents/execution_engine/engine.py` (_handle_revision only), `test_run_revision_fe_contract.py` | Complete — none-deviated; one documented intra-phase RED intermediate (test_revision_intelligence, closed by 14-04) | → 14-03-PLAN.md / 14-03-SUMMARY.md (Accomplishments) |
| 14-04 | Intelligence-suite rewrite + phase gate | `test_revision_intelligence.py` rewritten to real-dispatch contract (`_dispatch_wiring` ctxmgr; 8 guards kept byte-meaning-identical; proceed-paths on scripted dispatch; three-section/planning-prefix asserted on the agent INPUT; run_events single-stamping seq trap); phase-gate battery + SC-001/INV-3/INV-12 grep sweep; SC4 deferral recorded | `test_revision_intelligence.py` | Complete — auto-fixed 2 items (added missing `test_falsy_owner_raises`; made cross-owner "no events" + FR-014 byte-exact assertions executable) | → 14-04-PLAN.md / 14-04-SUMMARY.md (Deviations from Plan) |

### 3. Capabilities, Modules, Schema & API Added

- **Real-dispatch path** — `_handle_revision` now does `revision_pipeline_type = f"{target.removesuffix('_output')}_revision"` (WR-06 alias, data-only) → `get_pipeline_agents(revision_pipeline_type)` → `async for event in self.execute(agents=…, user_message=revision_context, pipeline_type=revision_pipeline_type, od_context=None, gate_agent_ids=[], parent_run_id=…)`, forwarding every pre-stamped event verbatim through the RAW `websocket_send_fn`, capturing `final_output` from `pipeline_complete` and a `terminal_failed` flag from `pipeline_failed`. → engine.py:3882 / 14-03-SUMMARY.md (Accomplishments); 14-VERIFICATION.md (truth 1)
- **Pre-dispatch guards** — empty `get_pipeline_agents` → ValueError naming the unsupported target (Pitfall 7); **CR-02 review fix** added a second gate: `compile_for_run(revision_pipeline_type).planner != "skip"` → ValueError ("not revision-dispatchable"), so a `planner: run` target fails fast (mapped to `revision_validation_error`) instead of hanging at the clarify gate. Both predicates are compiled-manifest DATA — SC-001 held. → engine.py:3838/3856 / 14-REVIEW-FIX.md (CR-02); 14-VERIFICATION.md (Required Artifacts → engine.py)
- **`derived_from` lineage write** — post-dispatch, guarded `if final_output and not terminal_failed:`, `ArtifactGraph.write_ref` + `ScopedStore.write_ref` with `kind=target_artifact_type`, `content=final_output`, `derived_from=original.id`, `workspace_id=original.workspace_id`, `visibility="workspace"`, `producer_agent=agents[-1].id` (derived from resolved specs — IN-05 spirit, no literal). On persist failure: keep `state_restoration_failed` vocabulary, do NOT fail the run. → engine.py:3908-3937 / 14-03-SUMMARY.md (Exact-kind lineage); 14-PATTERNS.md (Post-dispatch lineage write)
- **`_handle_revision_execution`** (WS) — module coroutine mirroring `_handle_workflow_execution`: revision row creation, per-run event queue, inner `_run_revision_to_queue` bg engine task, sentinel-terminated drainer with 10s heartbeat. Revision runs now register in `_PIPELINE_TASKS`/`_PIPELINE_QUEUES` (cancellable via `cancel_pipeline`, reconnect-discoverable via the existing owner-gated path) and clean via `_cleanup_pipeline`. → websocket.py:1787 / 14-02-SUMMARY.md (Accomplishments)
- **Manifest planner flips** — `ppt_revision` (2 steps) and `od_ppt_revision` (1 step) declare `planner: skip` so a dispatched revision bypasses the planner LLM call AND the indefinite clarify `event.wait()` (`prototype_revision`/`user_stories_revision`/`app_builder_revision` ride `run_pipeline` and are explicitly OUT OF SCOPE — untouched). → 14-01-SUMMARY.md (Files Created/Modified)
- **Terminal-status fidelity** — `pipeline_complete`→completed, `pipeline_complete` with `data.status=="degraded"`→degraded (WR-02), `pipeline_failed`/no terminal→failed, `CancelledError`→cancelled; no path leaves the row "revising". This stops a failed revision being offered as a future revision parent by the FE `status === "completed"` lookup. → websocket.py:1946-1979 / 14-02-SUMMARY.md (Accomplishments)
- **Registry-derived `agent_count`** — `len(get_pipeline_agents(<derived alias>)) or 1` (ppt_revision=2, od_ppt_revision=1, unknown→cosmetic 1), never hardcoded. → websocket.py:1875 / 14-02-SUMMARY.md
- **WR-03 reconnect section contract** — live-attach drainer derives `_reattach_section` from the owner-filtered AUTHZ-03 gate row via the inverse suffix transform (`od_ppt_revision` → `od_ppt_output`); non-revision runs keep `section: None` byte-identically. → websocket.py:656-822 / 14-REVIEW-FIX.md (WR-03)
- **Scripted revision-agent harness** — `_scripts_for` branches `od-ppt-revision-agent`, `ppt-revision-agent` (narration-only), `ppt-revision-assembler`; the last agent of each pipeline emits an `<artifact>`-wrapped REVISED deck (sanitizer pass-through, byte-distinct from the od-ppt-validator parent deck). → 14-01-SUMMARY.md (Accomplishments)
- **Events / FE contract** — NO new event types and NO FE-contract change: drained frames keep `{type, chunk: None, section: <target_artifact_type>, data}`; ingress error vocabulary (`empty_revision_instruction`, `missing_revision_params`, `revision_validation_error`, `revision_error`, CR-01 `pipeline_already_running`) byte-preserved. The real terminal `pipeline_start`/`pipeline_complete` pair is now `execute()`'s own. → 14-02-PLAN.md (must_haves); 14-VERIFICATION.md (Data-Flow Trace)
- **Migrations / `_KNOWN`:** none — no new table, no new capability, no `_KNOWN` movement, no new dependency (Package Legitimacy Audit: zero). → 14-03-PLAN.md (Artifacts); 14-RESEARCH.md (Standard Stack)

### 4. What Was Deleted / Superseded (INV-12 move-don't-copy)

All deleted inside `_handle_revision` / the WS run_revision branch in the SAME change that added the real path — no branch-around, no dual path. → engine.py:3656 (method) / 14-03-SUMMARY.md (INV-3/INV-12 deletion); 14-02-SUMMARY.md (Accomplishments)

- The **Phase-3 echo stub** — the `content=revision_context` write that returned the composed context as the "revision". → 14-03-PLAN.md (DELETED symbols/blocks)
- The duplicate stamping path: **`_rev_sink` / `_rev_counter` / `_raw_send_fn` / `_stamped_send`** and the `websocket_send_fn = _stamped_send` reassignment — `execute()` is the single seq/event_id stamping + run_events persistence chokepoint. → 14-03-SUMMARY.md; 14-PATTERNS.md (Single stamping chokepoint)
- The **fake event pair** — the hand-rolled `pipeline_start` and the fake `pipeline_complete` with `total_duration: 0.0` and the stub-only `planning_context_unavailable` payload key, plus the `"revision-agent"` / `"Revision Agent"` literals. (The local var `planning_context_unavailable` survives only as input to an INFO log; the deleted item was the terminal *payload key*.) → 14-VERIFICATION.md (Anti-Patterns Found note)
- The **CR-01 scope-writeback / sink-arm block** — `store._workspace_id` + the in-method `set_run_scope` writeback; `execute()` mints the revision run's OWN workspace and calls `set_run_scope` itself. → 14-03-SUMMARY.md
- WS layer: the inline `await _rev_engine._handle_revision(...)`, the **unconditional `status = "completed"` flip**, the inline `_send_revision_event` helper, and the inline ValueError/Exception handlers. → 14-02-SUMMARY.md
- **Stub-pinning test expectations** replaced by the real-dispatch contract: `test_revision_intelligence.py` (was 792 lines pinning context-blob `final_output`, the `_stamped_send` ledger path, `planning_context_unavailable` on `pipeline_complete`) and `test_run_revision_fe_contract.py` rewritten; the old `test_planning_context_unavailable_*` renamed to `test_planning_context_prefix_absent_*` (the survivor is the input-side prefix ABSENT/PRESENT contract). → 14-04-SUMMARY.md (Decisions Made)

### 5. Key Decisions & Locked Constraints (do NOT contradict)

- **D-01 — `od_context=None` + `gate_agent_ids=[]` on dispatch** (the settled design wrinkle): no revision agent declares injects; the 13-06 guard lives only at the `run_pipeline` ingress; the parent deck embedded in the composed context realizes the template. `gate_agent_ids=[]` (not `None`) suppresses the static gate-set fallback — no inter-agent HITL pause on a panel revision. → 14-03-SUMMARY.md (key-decisions)
- **D-02 — failed/empty dispatch writes NOTHING under the exact kind** — a failed revision must never become a future revision parent via FR-014 chain link 1; composes with 14-02's terminal-status fidelity. → 14-03-SUMMARY.md (key-decisions); T-14-03-05 (14-SECURITY.md)
- **D-03 — lineage-persist failure keeps `state_restoration_failed` but does NOT fail the completed run** (RESEARCH Open Q2): the 13-05 terminal-block `kind="deliverable"` ref keeps FR-014 chain link 2 functional. → 14-03-SUMMARY.md (key-decisions)
- **D-04 — `producer_agent=agents[-1].id`** derived from the resolved spec list (no literal — IN-05 spirit); revision pipelines are 1-2 single_shot steps and the last agent's stream is the declared `ppt`-strategy deliverable. → 14-03-SUMMARY.md; 14-PATTERNS.md
- **Two planner-parity traps must move in lockstep** — `test_manifest_parity.py` (`test_planner_run_everywhere`) AND `test_id_alias_resolver.py` (`test_compiled_planner_is_run_everywhere`) BOTH carve out exactly `{ppt_revision, od_ppt_revision}` → `skip`, every other dispatchable manifest stays `run`. (14-RESEARCH missed trap #2; planning caught it by grep.) `test_id_alias_resolver.py` keeps its OWN `_RUN_REVISION_DISPATCHED` copy with a cross-citing lockstep comment (no cross-module import). → 14-01-SUMMARY.md (key-decisions / Decisions Made)
- **Injects/ingress-guard resolution is an executable pin** — `test_run_revision_revision_agents_declare_no_template_injects` fails CI if any revision agent of either pipeline ever declares `template`/`design_system` injects; that future change requires `template_id` persistence on `WorkflowRun` first (additive, Q3). → 14-01-PLAN.md (Task 1 step 4); T-14-01-02 (14-SECURITY.md)
- **SC-001 (kernel name-free)** — no workflow-name literal in `agents/execution_engine/`: agents/manifest resolved only via the derived WR-06 alias; behavior change shipped as manifest data (`planner: skip`) routed through the existing engine skip branch (engine.py:1198-1205) — ZERO engine edits for the flip. Banned-pattern gate green; `grep` over the kernel returns 0 code matches for `ppt_revision|od_ppt_revision|ppt_output` and `if pipeline_type ==`. → 14-04-SUMMARY.md (Invariant grep sweep); 14-PATTERNS.md (SC-001 compliance)
- **INV-3 (byte/event parity)** — the 5 characterization goldens cover NO run_revision path (`prototype_revision` rides `run_pipeline` and is NOT flipped); zero golden re-baselines across the phase even though engine.py was touched (only `_handle_revision`'s body changed). → 14-VERIFICATION.md (truth 3); 14-03-PLAN.md (Pitfall 8 scope)
- **WR-06 generic alias reuse** — the `f"{target.removesuffix('_output')}_revision"` forward transform (and its inverse for the reconnect section) is the only string mechanism; no new alias machinery. → 14-PATTERNS.md (WR-06 alias transform)

### 6. Status, Verification & Evidence (what HAPPENED)

**Verification verdict (14-VERIFICATION.md):** `status: passed`, **4/4 must-haves verified**, 0 overrides. Each truth carries source-line evidence + a pinning test (SC1 `test_fe_exact_run_revision_dispatches_real_pipeline`; SC2 `test_revision_of_revision_resolves_via_exact_kind_chain_link_1` + `test_revision_stores_new_version_with_lineage`; SC3 grep sweep + both suites; SC4 deferral recorded). No gaps; no human verification required (live items are SC4-deferred and recorded). → 14-VERIFICATION.md (Goal Achievement, Gaps Summary)

**Evidence:** full targeted battery **226 passed / 7 skipped (pre-existing)** in one ~40s session (14-04 gate); independent verifier re-run **135 passed in 6.5s** + orchestrator **231 passed**; `lint-imports` **4 contracts kept / 0 broken**; characterization parity = `git status --porcelain tests/agents/characterization/golden/` **empty**; SC-001/INV-12 greps all **0**. Full backend pytest skipped (hangs offline — project convention: targeted battery only). → 14-VERIFICATION.md (Behavioral Spot-Checks); 14-04-SUMMARY.md (Phase-Gate Battery Results)

**Review → fix → re-review arc:**
- **Iteration 1 (14-REVIEW iter-1, commit `d1dff9f4`):** 2 Critical + 4 Warning + 3 Info. → 14-REVIEW-FIX.md (header)
- **Fixes (14-REVIEW-FIX.md, all 6 in-scope fixed, 0 skipped, commits `9ab3f7f2..4e700d15`):** **CR-01** run_revision overlap guard (`pipeline_already_running`, cancel handle never overwritten); **CR-02** reject `planner: run` targets pre-dispatch (no clarify-gate hang); **WR-01** residual drain delivers post-terminal `state_restoration_failed`; **WR-02** degraded completion persists `degraded` not `completed`; **WR-03** reconnect live-attach preserves the revision `section` contract; **WR-04** rewrite the stale AUTHZ comment. Every fix carries an executable pin; two endpoint tests drive the REAL `websocket_chat` receive loop via a new `_ScriptedLoopWebSocket`.
- **Iteration 2 (14-REVIEW.md, `iteration: 2`, `e423fbaf`):** all 6 fixes VERIFIED against live source; **0 Critical / 0 Warning / 5 Info** (IN-01..IN-05 — mirrored run_pipeline debt + two consciously-scoped residuals of the WR-01/WR-03 fixes; none blocking). 129 + 21 tests green in-review, lint 4/0. → 14-REVIEW.md (Summary)

**Security (14-SECURITY.md):** `status: verified`, **threats_open: 0** (19 threats, all closed), ASVS L1. Ownership stays first (`assert_owns` before any cross-run read AND before dispatch — T-14-03-01); prompt-injection bounded (revision agents are `tools: []`, `gate_agent_ids=[]` removes only an HITL pause — AR-14-03); no packages installed (AR-14-04). Review fixes mapped onto existing entries as hardening, no new attack surface. → 14-SECURITY.md (Threat Register, Sign-Off)

**Validation (14-VALIDATION.md):** `nyquist_compliant: true`, quick run ~<30s, targeted battery ~35s; per-task verification map covers SC1-SC4 to plan/task; SC4 is the single manual-only item. → 14-VALIDATION.md (Validation Sign-Off)

**Key commit SHAs:** plan work `22e10df0`, `25f02ae1` (14-01) · `ec590467`, `e9a6a2ca` (14-02) · `8173a8e0`, `10d712cc` (14-03) · `2f45ee54` (14-04). Fixes `9ab3f7f2` (CR-01), `b53d4aca` (CR-02), `ca65d7d4` (WR-01), `2a7d6aa2` (WR-02), `d71c55e6` (WR-03), `4e700d15` (WR-04). → 14-VERIFICATION.md (Post-SUMMARY Fix Commits)

**SC4 (live confirmation) deferred** to the milestone-end live pass per project convention (defer-live-verification-to-milestone-end) — recorded, not blocking; scored ✓ as "deferral recorded".

### 7. Gotchas, Survivors & Carry-Forward

- **F2 is now end-to-end** — vs Phase 13 where only routing/contract was closed and the deliverable was still the Phase-3 stub. A model now runs and the revised artifact (not the context blob) is the deliverable. Future agents: do NOT rebuild any of `_handle_revision` real dispatch, `_handle_revision_execution`, the planner flips, the lineage write, or the scripted revision harness — they exist.
- **SC4 live re-pass deferral** carries forward to the milestone-end live pass: (1) FE-exact `run_revision` frame → revised deck in the preview on real Bedrock; (2) live `cancel_pipeline`/`reconnect_pipeline` during a minutes-long revision (offline cancellation is pinned); (3) A1 — the FE revision panel wants no clarifying questionnaire (`planner: skip` is the intended product behavior, incl. the legacy `run_pipeline` fallback losing its planner overlay). → 14-04-SUMMARY.md (Deferred); 14-VERIFICATION.md (Deferred Items)
- **deferred-items.md entry** — `test_pipeline_cancel.py::test_cancel_marks_workflow_cancelled_and_sends_ack` fails OFFLINE only (reaches a live `_generate_workflow_title` Bedrock call → `ExpiredTokenException`); proven **pre-existing** (fails byte-identically at HEAD~2), out of scope, NOT a phase gap. Candidate for a test-hardening quick task (no-op the title generator). → deferred-items.md
- **Live (iteration-2 Info) survivors — informational, none blocking:** IN-01 final bg-task wait swallows `CancelledError` (mirrored run_pipeline debt); IN-02 `pipeline_cancelled` ack typically not delivered on cancel; IN-03 two `section` shapes for the revision-error family (ingress `None` vs drainer `<target>`); IN-04 durable-replay revision frames still carry `section: None` (WR-03 fix covered live-attach only); IN-05 residual drain skipped if the bg task outlives the 5s grace. → 14-REVIEW.md (Info IN-01..IN-05)
- **Pitfalls baked into the contract (do not regress):** planner must stay `skip` on the two manifests or a dispatched revision hangs at the clarify `event.wait()` (Pitfall 1, now also fenced by CR-02); never re-introduce a second stamping counter (Pitfall 2 — the seq-contiguity trap fails it); proceed-path tests MUST target only `ppt_output`/`od_ppt_output` (any other `*_output` derives a `planner: run` manifest and hangs). → 14-RESEARCH.md (Common Pitfalls); 14-04-PLAN.md (Targeting rule)
- **Workspace delta gotcha** — `execute()` mints the revision run's OWN workspace (it does NOT inherit `original.workspace_id`); only the exact-kind lineage ref carries the parent's workspace. Resolve run_events scope owner-scoped + recover the minted workspace from the persisted rows, never from the parent. → 14-04-SUMMARY.md (key-decisions); WR-04 (14-REVIEW-FIX.md)

### 8. File Index (every file in this folder)

- `14-01-PLAN.md` — Wave-1 plan: `planner: skip` flips + both parity-trap carve-outs + design-wrinkle pin + scripted-harness branches. Open for the manifest-flip rationale and the injects-pin contract.
- `14-01-SUMMARY.md` — 14-01 actuals (2 commits, none-deviated): the two manifests flipped, both traps in lockstep, three scripted branches. Open for what the scripted decks contain.
- `14-02-PLAN.md` — Wave-1 plan: extract `_handle_revision_execution` (queue + bg task + drainer), terminal-status fidelity, registry `agent_count`, 6-scenario suite. Open for the WS dispatch contract + STRIDE register.
- `14-02-SUMMARY.md` — 14-02 actuals (2 commits): coroutine extracted, inline await + unconditional-completed flip deleted. Open for the CancelledError/error-frame/post-drain decisions.
- `14-03-PLAN.md` — Wave-2 plan: real `execute()` dispatch in `_handle_revision`, stub/`_stamped_send`/fake-pair deletion, guarded `derived_from` write, FE-exact rewrite. Open for the exact KEEP/DELETE/ADD list + the documented RED intermediate.
- `14-03-SUMMARY.md` — 14-03 actuals (2 commits): stub gone, real dispatch live, lineage write, 2-test FE suite. Open for the deletion inventory and SC1/SC2 evidence.
- `14-04-PLAN.md` — Wave-3 plan: rewrite the 792-line intelligence suite to real dispatch (guards kept), phase-gate battery, grep sweep, SC4 recording. Open for the guard KEEP-list and proceed-path targeting rule.
- `14-04-SUMMARY.md` — 14-04 actuals (1 test commit + gate): 17→19 tests, battery 226 passed, the Implementation Register supersede note, the Deferred section. Open for the auto-fixed items and battery table.
- `14-PATTERNS.md` — pattern map (8/8 analogs in-repo): forward-and-capture loop, post-dispatch lineage write, manifest flip, scripted wiring, the four shared patterns (SC-001, single-stamping, owner-scope, degrade-don't-fail). Open before writing any `_handle_revision`-shaped code.
- `14-RESEARCH.md` — full research (zero new deps): 8 Pitfalls (each file:line-anchored), the Settled Design Wrinkle, resolved Open Questions, A1-A3 assumptions, ASVS categories. Open for the WHY behind every decision.
- `14-REVIEW.md` — code review **iteration 2** (fix verification): all 6 iter-1 Critical/Warning VERIFIED fixed, 0 Critical / 0 Warning / 5 Info (IN-01..IN-05). Open for the residual Info debt and fix-by-fix verification.
- `14-REVIEW-FIX.md` — iteration-1 fix report: CR-01/CR-02/WR-01..WR-04 applied with commits and pinning tests, 6 fixed / 0 skipped. Open to see exactly what each fix changed.
- `14-SECURITY.md` — security contract: 19 threats all closed (`threats_open: 0`), trust boundaries, Accepted Risks AR-14-01..04, audit trail. Open for the ownership/prompt-injection/DoS dispositions.
- `14-VALIDATION.md` — validation strategy: test infra, sampling rate, per-task verification map (SC→task), Wave-0 requirements, the single manual-only SC4 item. Open for the battery definition and sign-off.
- `14-VERIFICATION.md` — phase verification: `passed` 4/4, truth-by-truth evidence with file:lines, key-link + data-flow traces, deferred items, all 13 commit SHAs. Open for the authoritative phase verdict.
- `deferred-items.md` — one out-of-scope discovery: the pre-existing offline-gated `test_pipeline_cancel` flake (live Bedrock title call). Open before assuming that failure is a Phase-14 regression.
