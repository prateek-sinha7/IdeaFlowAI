## Phase 03 — Token-Trim (measured change) [0C]

**Folder:** `.planning/phases/03-token-trim-measured-change-0c/`  ·  **Status:** Complete (2026-06-07)  ·  **Plans:** 2/2  ·  **Plan-id → source plan §25:** 0C
**Requirements delivered:** COMPACT-01, COMPACT-02, COMPACT-03 (all three map exclusively to this phase → REQUIREMENTS.md "Context Compaction / Token-Trim (Phase 0C)")
**One-line outcome:** Wired the previously-dead `_extract_html_skeleton` helper into the `prototype-build` task-2+ prompt path so the build sub-agent now receives a ~1–3k-char skeleton state-map instead of the full current HTML (proven ≥50% smaller, semantic parity held) — the one sanctioned non-byte-identical change in the migration.

### 1. Goal & Success Criteria (what was PLANNED)
Wire the dead `_extract_html_skeleton` (ledger item **L13**) as build-task-2+ context compaction: in `_build_context_message`, for `is_build_task_2_plus`, replace the full current-HTML block (up to 120k chars) with the compact skeleton; prove a **≥50% reduction** in the task-2+ prompt input (deterministic offline CI gate) plus a real-run token delta (SUMMARY evidence), while holding **semantic parity** (same pages/routes, equal-or-better validation). This is the **one sanctioned INV-3 exception** — gated on the semantic event snapshot + measured token delta, NOT deliverable byte-identity (→ 03-SPEC.md "Goal" / "Constraints"; §25 lines 806-809; §3 line 252 INV-3). Covers both `prototype` and `od_prototype` (od alias → same `prototype-build` path).
Success criteria (→ 03-SPEC.md "Acceptance Criteria", 8 items): skeleton invoked on task-2+ & full-HTML block absent there; offline ≤50% gate on a ≥2-page HTML; SUMMARY records a real token/cost delta; `prototype`/`od_prototype` semantic event snapshots green with no golden edit & `seq` contiguous; the two deliverable byte-goldens re-recorded while all other pipelines stay byte-identical; pages/routes + equal-or-better-validation assertion; `read_file` access preserved with a read-before-edit instruction; **L13 stays `☐`** (wired, not deleted).
**Depends on:** Phase 02 (ectx threading through `_build_context_message`) and Phase 00 (offline `_scripted_model._drive` harness + characterization snapshot split) (→ 03-01-SUMMARY.md "Dependency graph").

### 2. What Was Implemented — per plan (what was BUILT)
| Plan | Title (short) | What it built (names only) | Key modules/files touched | Status | Detail |
|------|---------------|----------------------------|---------------------------|--------|--------|
| 03-01 | Wire skeleton + ≥50% CI gate | `is_build_task_2_plus` branch in `_build_context_message` calling `self._extract_html_skeleton`; new offline test module `test_phase3_compaction.py` (≥50% gate, marker presence/absence, task-1 control, read_file-access, revert-ratchet, + faithfulness unit test added in review) | `agents/execution_engine/engine.py`; `tests/agents/test_phase3_compaction.py` | Complete | → 03-01-PLAN.md (planned) / 03-01-SUMMARY.md (built) |
| 03-02 | Parity proof + golden re-baseline + live evidence | `test_phase3_parity.py` (pages/routes + validation parity for `prototype`/`od_prototype`); `test_phase3_token_delta_live.py` (opt-in dual-gated live token-delta evidence); `SNAPSHOT_UPDATE=1` re-baseline of the two deliverable goldens (confirmed NO-OP diff) | `tests/agents/test_phase3_parity.py`; `tests/agents/test_phase3_token_delta_live.py`; `characterization/golden/prototype.html` + `od_prototype.html` | Complete | → 03-02-PLAN.md (planned) / 03-02-SUMMARY.md (built) |

### 3. Capabilities, Modules, Schema & API Added
- **No new registered capability, no port/Protocol, no `CompactionStrategy`, no manifest, no Alembic migration, no API endpoint, no new event type.** This phase is a single in-place edit to an existing private method plus test scaffolding (→ 03-CONTEXT.md "Phase Boundary"; 03-VERIFICATION.md "Anti-Patterns Found" confirms no `CompactionStrategy` match in engine.py).
- The only production symbol touched: `ExecutionEngine._build_context_message` — its `is_build_task_2_plus` HTML branch now calls the existing `self._extract_html_skeleton(current_html)` and emits a distinctly-marked skeleton block; the helper's own extraction logic is byte-unchanged (→ 03-01-SUMMARY.md "Files Created/Modified"; 03-VERIFICATION.md "Required Artifacts").
- Skeleton block marker (chosen per D-01): `=== CURRENT PROTOTYPE (skeleton — call read_file('prototype.html') for full content before editing) ===` … `=== END CURRENT PROTOTYPE ===` — visually distinct from the old `--- CURRENT HTML (modify this …) ---` block and carrying the `read_file` pointer (→ 03-01-SUMMARY.md "Decisions Made").
- Registry counter (_KNOWN N→M): **not applicable** — no capability registered this phase.

### 4. What Was Deleted / Superseded (INV-12 move-don't-copy)
- **This phase is pure-additive at the production level — it DELETES nothing.** It only *wires* (calls) the already-existing `_extract_html_skeleton`; the helper body, the `=== TEMPLATE COMPLIANCE ===` block, and the empty/`[Error:` guard are all left byte-unchanged. No AGENT.md edit (Req 7 already satisfied by `prototype-build/AGENT.md` lines 40/78 which already mandate `read_file` + "NEVER rebuild") (→ 03-CONTEXT.md "Implementation Decisions" D-01; 03-01-SUMMARY.md "Accomplishments").
- **Ledger row L13 deliberately stays `☐`** at end of this phase — the deletion gate (grep `_extract_html_skeleton` → 0) is owned by **Phase 7** (`0C→2`), which re-expresses this inline wiring as `CompactionStrategy(html_skeleton)` and removes the helper (PARITY-04). Confirmed `☐` and the Phase-1 ledger ratchet (`test_migration_ledger.py`) green at completion (→ 03-VERIFICATION.md truth #8; 03-SPEC.md "Boundaries — Out of scope"). NOTE for future agents: the live migration-ledger.md now shows L13 as **`☑` (commit 1d9234b)** — Phase 7 has since flipped it; do not infer Phase 3 deleted the helper.
- No banned-pattern/grep gate armed, no import-linter contract added, no migration-ledger row flipped *by this phase*.

### 5. Key Decisions & Locked Constraints (do NOT contradict)
- **D-01 (locked):** distinct skeleton-map marker + literal `read_file('prototype.html')` pointer at the injection site; honest "state-map not editable source" framing; keep the empty/`[Error:` guard; NO AGENT.md edit (→ 03-CONTEXT.md "Skeleton injection framing"; 03-DISCUSSION-LOG.md).
- **D-02 / D-02a (locked):** the deterministic CI gate is a message-level `len(compacted) ≤ 0.5 × len(fullhtml)` assertion exercising the real `_build_context_message`, with the full-HTML baseline reconstructed inline (capped at 120k); fixture is an inline multi-page HTML literal, NOT the tiny 84-byte committed golden (→ 03-CONTEXT.md "Token-reduction gate test design"; 03-PATTERNS.md "≥50% deterministic gate").
- **D-03 (locked):** re-baseline ONLY the `prototype` + `od_prototype` deliverable goldens via `SNAPSHOT_UPDATE=1`; hold the semantic `*.events.json` snapshots green with no edit; add a focused pages/routes + equal-or-better-validation assertion (→ 03-CONTEXT.md "Parity assertion approach").
- **D-04 (locked):** the real-run token delta is captured by an opt-in, dual-gated (`RUN_LIVE_BEDROCK=1` + AWS STS probe) live test, evidence-only, NEVER a CI gate; documented manual-run procedure is the accepted fallback if no live model is available (→ 03-CONTEXT.md "Real-run token-delta evidence"; matches MEMORY "Defer live verification").
- **INV-3 sanctioned exception (the load-bearing constraint):** Phase 0C is the ONLY phase permitted to move a deterministic deliverable's bytes; it is gated on semantics + token delta, never byte-identity. Every other migration phase stays byte-identical-where-deterministic. A future agent must NOT treat 0C as license to break byte-parity elsewhere (→ 03-SPEC.md "Constraints"; §3 line 252).
- **Reduction floor = 50%** of the task-2+ context-message size (chars, input-token proxy), measured on a ≥2-page HTML (→ 03-SPEC.md "Constraints").
- **Discretion items resolved:** keep the `=== TEMPLATE COMPLIANCE ===` reminder for task-2+; leave `_extract_html_skeleton` extraction logic as-is unless a concrete parity gap surfaces; on empty/`[Error:` HTML emit NO block (→ 03-CONTEXT.md "Claude's Discretion").

### 6. Status, Verification & Evidence (what HAPPENED)
- **Verdict: PASSED — 8/8 must-haves verified, 0 overrides** (→ 03-VERIFICATION.md frontmatter + "Goal Achievement").
- **Test evidence:** combined offline suite = **18 passed, 1 skipped** (the L16 ledger skip is out-of-band/expected); the live token-delta test SKIPS cleanly with no env (CI-safe). The ≥50% gate measured a **96.9% reduction (ratio 0.031)** on a 29,749-char fixture (419-char skeleton; 941-char compacted message vs 30,227-char full-HTML message). Revert-ratchet confirmed non-vacuous (reverting the engine edit fails 4/5 of the original tests) (→ 03-01-SUMMARY.md "Measured token reduction"; 03-VERIFICATION.md "Behavioral Spot-Checks").
- **Parity evidence:** `prototype`/`od_prototype` semantic event snapshots green with NO `*.events.json` edit, `assert_seq_contiguous` holds; deliverable golden re-baseline = **empty/NO-OP diff** (the scripted model writes a fixed 84-byte single-section HTML regardless of prompt content — a documented, expected outcome, NOT a deviation); `prototype_revision.html`/`od_ppt.html`/`app_builder.txt` byte-identical (→ 03-02-SUMMARY.md "Deliverable golden re-baseline"; 03-PATTERNS.md "deliverable re-baseline" Caveat).
- **Code review:** 0 critical, **5 warnings, 3 info** (8 total), status `issues_found` (→ 03-REVIEW.md frontmatter). Resolution (commit `f89be6e`, scope tests): **WR-01 FIXED** (live-test monkeypatch had keyword-only params vs the engine's positional call → would `TypeError`; now positional) and **WR-04 ADDRESSED** (added `test_extract_html_skeleton_is_faithful_to_source` so the offline suite actually covers skeleton fidelity rather than relying on the prompt-independent scripted model). **WR-02/WR-03/WR-05 accepted/deferred to Phase 7** (pre-existing `_extract_html_skeleton` robustness gaps — final-section drop with no `</body>`, nested-brace route maps, no engine-side read-before-edit enforcement). **IN-01/IN-02/IN-03 deferred** (dead `_fullhtml_baseline_len`, duplicated full-HTML marker strings across 4 sites, magic `_build_task_number`/`_build_task_total` control keys mutated on the shared `accumulated_outputs` dict) (→ 03-REVIEW.md "Resolution").
- **Security:** STRIDE register only — all threats `accept`/`mitigate`, NO new threats (information surface strictly shrinks: skeleton < full HTML; live-test reuses the established double-gated SSO/Bedrock path; golden re-baseline scoped by an explicit `git diff --name-only` check) (→ 03-01-PLAN.md / 03-02-PLAN.md "threat_model"). No standalone SECURITY.md.
- **Commit SHAs:** 03-01 → `3907274` (engine, Task 1), `cddb293` (tests, Task 2); 03-02 → `3ab8ccb` (parity test), Task 2 golden re-baseline = no commit (no-op diff), `6371744` (live test); review fixes → `f89be6e` (→ 03-01-SUMMARY.md / 03-02-SUMMARY.md "Task Commits").

### 7. Gotchas, Survivors & Carry-Forward
- **`_extract_html_skeleton` is a deliberate live survivor at end-of-phase** — it is now CALLED, not dead; vulture (`min_confidence=80`) does not flag it and NO allow-list entry was added (and none must be). Do not delete it within Phase 3's frame; its deletion is Phase 7's job (→ 03-01-SUMMARY.md "Verification Results"; 03-SPEC.md "Constraints").
- **Scripted-model caveat (critical for any future parity/golden work here):** the offline `_scripted_model` `prototype-build` step writes a FIXED 84-byte single-section HTML *regardless of prompt content* (`_scripted_model.py:269-281`). Therefore (a) the deliverable golden re-baseline is a no-op diff, (b) the offline parity test (`test_phase3_parity.py`) only proves output-preservation, not skeleton fidelity — it cannot detect a corrupted skeleton (WR-04). Real byte/token change is only observable under a live model run (→ 03-PATTERNS.md "deliverable re-baseline" Caveat; 03-REVIEW.md WR-04).
- **Real-run live token delta is UNMEASURED / deferred:** no live AWS/Bedrock run was available at execution time, so the COMPACT-03 Req-3 real-run figure is the documented D-04 manual-run procedure, not actual numbers. **Outstanding non-blocking action:** a developer with AWS SSO runs `RUN_LIVE_BEDROCK=1 AWS_PROFILE=personal-sso python3.11 -m pytest tests/agents/test_phase3_token_delta_live.py -v -s` once and pastes the figure into the 03-02-SUMMARY.md COMPACT-03 evidence block (→ 03-02-SUMMARY.md "COMPACT-03 real-run token-delta evidence"; "Next Phase Readiness").
- **Phase 7 carry-forward (out of scope here, do not duplicate):** re-express this inline wiring as a registered `CompactionStrategy(html_skeleton)` + delete `_extract_html_skeleton` (flips L13 → `☑`, PARITY-04); also the `_current_task_block` → `TaskLoopStrategy` move (carried from Phase 02 D-02); compaction for non-build agents / additional Tier#2+ tiers is later capability work (→ 03-CONTEXT.md "Deferred Ideas").
- **Deferred robustness debt on the now-load-bearing helper** (track when touching it in Phase 7): WR-02 final-section drop on missing `</body>`, WR-03 nested-brace `:root`/`const routes` truncation, WR-05 no engine-side read-before-edit guard (correctness hinges on agent compliance with the prompt pointer) (→ 03-REVIEW.md WR-02/03/05).
- No separate deferred-items.md or UAT.md file exists for this phase (verification was self-driven per MEMORY "UAT self-drive").

### 8. File Index (every file in this folder)
- `03-SPEC.md` — the 7 locked requirements (COMPACT-01/02/03 + derived), boundaries, INV-3 exception constraint, acceptance criteria, ambiguity report (0.12). Open FIRST to know what 0C was contracted to deliver and the byte-parity exception.
- `03-CONTEXT.md` — phase boundary, the four locked implementation decisions D-01..D-04, Claude's-discretion edge cases, canonical refs (engine line anchors, AGENT.md, registry alias), reusable assets. Open to see HOW and WHY the wiring was framed.
- `03-DISCUSSION-LOG.md` — audit trail of the two discussed gray areas (skeleton framing, token-gate test design) and the two locked-to-recommendation areas; alternatives considered. Open for decision provenance only.
- `03-PATTERNS.md` — file classification + analog map; the exact "block to REPLACE" and "Target shape" for the engine edit, the skeleton output markers to assert, test construction patterns, the scripted-fixed-HTML caveat. Open before re-implementing or extending the engine branch or its tests.
- `03-01-PLAN.md` — plan for wiring the skeleton + the ≥50% offline CI gate (2 tasks, requirements COMPACT-01/COMPACT-03, threat model, success criteria). Open to compare intent vs build for the engine edit.
- `03-01-SUMMARY.md` — what 03-01 built: the engine branch, the 96.9% measured reduction table, marker decision, commits `3907274`/`cddb293`, no deviations. Open for the as-built engine change + the deterministic evidence.
- `03-02-PLAN.md` — plan for parity proof + golden re-baseline + opt-in live token-delta test (3 tasks, COMPACT-02/COMPACT-03). Open to compare intent vs build for the test/parity scaffolding.
- `03-02-SUMMARY.md` — what 03-02 built: parity tests, the confirmed NO-OP golden diff, the D-04 manual-run fallback (no live AWS), commits `3ab8ccb`/`6371744`, Task 2 = no commit. Open for the parity/evidence as-built + the outstanding live-run action.
- `03-REVIEW.md` — standard code review: 0 critical / 5 warning / 3 info; WR-01 + WR-04 fixed (`f89be6e`), WR-02/03/05 + IN-01/02/03 deferred to Phase 7 / cleanup. Open before touching `_extract_html_skeleton` or the phase-3 tests to inherit the known-debt list.
- `03-VERIFICATION.md` — PASSED, 8/8 must-haves, full evidence table, key-link wiring, requirements coverage (COMPACT-01/02/03 SATISFIED), anti-pattern scan clean. Open for the authoritative completion verdict.
