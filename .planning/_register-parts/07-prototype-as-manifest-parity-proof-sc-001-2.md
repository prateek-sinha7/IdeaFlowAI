## Phase 07 — Prototype as Manifest — Parity Proof (SC-001) [2]

**Folder:** `.planning/phases/07-prototype-as-manifest-parity-proof-sc-001-2/`  ·  **Status:** Complete (2026-06-09)  ·  **Plans:** 11/11  ·  **Plan-id → source plan §25:** 2
**Requirements delivered:** PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05, PARITY-06, PARITY-07, PARITY-08, PARITY-09 (the 9 PARITY-* reqs; the SPEC's "11 locked" = these 9 + the 2 belt-and-suspenders re-verifications of L14/L15/L16 closure). → 07-SPEC.md (Requirements) · 07-VERIFICATION.md (Requirements Coverage)
**One-line outcome:** Made the 9 Phase-4-registered-by-name-only capability families LIVE behind their ports, routed the engine through them off the compiled manifest, then DELETED kernel leaks L1–L13 + the `agents/prototype/` package — so the kernel knows no workflow by name (SC-001/INV-1, ratchet-locked).

### 1. Goal & Success Criteria (what was PLANNED)
Re-express `prototype`/`od_prototype`/`prototype_revision` (and keep `ppt`/code-gen at parity) with ALL capability behavior sourced from the compiled manifest — `single_shot`/`task_loop` strategies, `single_file`/`serialized_sandbox`/`streamed_text`/`ppt` resolvers, `opendesign`/`previous_run` providers, `heading_tasks` parser, `html_skeleton` compaction — then delete the hardcoded kernel leaks L1–L13 so `grep` for `if pipeline_type`/`spec.id ==` and the L1–L13 patterns returns 0 in `agents/execution_engine/`, while every pipeline holds deliverable byte + semantic-event parity vs the post-0C baseline. This is the SC-001 core-value proof. → 07-SPEC.md (Goal) · 07-CONTEXT.md (Phase Boundary).
Success = the 9-truth verification table (kernel name-free dispatch, both strategies behind the port, four resolvers, opendesign byte-identical to the acd1636 oracle, all 5 pipelines at parity, L1–L13 deleted, ledger flipped + banned-pattern hard-fail, and a NON-prototype `task_loop` workflow runs from manifest+AGENT.md with zero engine edits). → 07-VERIFICATION.md (Observable Truths).
**Depends on:** Phase 04 (CapabilityRegistry + `_KNOWN` + the 6 Protocol ports in `base.py`; authored manifests), Phase 05 (`ExecutionContext` object-typed per-run fields + `ScopedStore.assert_owns`), Phase 06 (`model_resolver` object-typed precedent), Phase 03 (0C `_extract_html_skeleton` + the ≥50% reduction gate). → 07-01-SUMMARY.md (Dependency graph).

### 2. What Was Implemented — per plan (what was BUILT)
> Plans 07-01…07-05 = the original 5-plan ROADMAP split (D-05). Plans 07-06…07-11 = gap-closure plans opened after the deep multi-agent review (07-REVIEW-DEEP.md) found 15 findings in 4 clusters; each closes a cluster. All 11 executed and Complete.

| Plan | Title (short) | What it built (names only) | Key modules/files touched | Status | Detail |
|------|---------------|----------------------------|---------------------------|--------|--------|
| 07-01 | Execution strategies + resolution seams | `SingleShotStrategy`, `TaskLoopStrategy`, `HeadingTasksParser`; `CapabilityRegistry.resolve()`+`install()`/`_register_builtins()` + `_IMPLS` map (D-02 seam); `ExecutionContext.runner` object-typed handle (D-03); `KernelServices` SHAPE | `capabilities/strategies/{single_shot,task_loop}.py`, `task_parsers/heading_tasks.py`, `registry.py`, `execution_engine/context.py` | Complete | → 07-01-PLAN.md / 07-01-SUMMARY.md |
| 07-02 | Deliverable resolvers + context providers | `SingleFileResolver`, `SerializedSandboxResolver`, `StreamedTextResolver`, `PptResolver`; `OpenDesignProvider`, `PreviousRunProvider`; `_artifact.py` (shared unwrap/sanitize) | `capabilities/deliverables/*`, `capabilities/context_providers/*`, `od_context.py` (relocation home), `prototype/context.py` (emptied), `engine.py` (3 importer rewires) | Complete | → 07-02-PLAN.md / 07-02-SUMMARY.md |
| 07-03 | `html_skeleton` CompactionStrategy | `HtmlSkeletonCompaction` (verbatim lift of `_extract_html_skeleton`); bound in `install()`; re-pointed 0C gate | `capabilities/compaction/html_skeleton.py`, `registry.py`, `test_phase3_compaction.py` | Complete | → 07-03-PLAN.md / 07-03-SUMMARY.md |
| 07-04 | Engine wiring + parity proof (leaks still DEAD-present) | `KernelServices` concrete class (the D-03 handle attached as `ctx.runner`); per-step `resolve("strategy",…).run`; deliverable `resolve("deliverable",…)`; `_compose_context_message` generic injector; `_seed_workflow_context`; `_legacy_*_DEAD` strangler stubs | `execution_engine/kernel_services.py` (NEW), `engine.py`, `strategies/task_loop.py`, `test_routing_parity.py` (NEW) | Complete | → 07-04-PLAN.md / 07-04-SUMMARY.md |
| 07-05 | Leak deletion + ratchet flip (SC-001 landing) | DELETE L1–L13 from `engine.py`; remove `agents/prototype/` package; migrate L3/L10 survivors to key off `ctx.deliverable.strategy`; L6→`compiled.clarify.mode`; ledger L1–L13 → ☑ (kernel-scoped); banned-pattern → kernel-scoped HARD-FAIL | `engine.py`, `context.py`, `kernel_services.py`, `migration-ledger.md`, `test_banned_patterns.py`, `test_migration_ledger.py`, many test files | Complete | → 07-05-PLAN.md / 07-05-SUMMARY.md |
| 07-06 | opendesign L12 byte-parity gap closure (cluster A/CR-01..03 of 0706 review) | Restored DS preamble (CR-01), builder-gated example.html (CR-02), build-task-2+ suppression (CR-03); threaded `ectx.current_spec_tools`; de-blinded `context_message` (removed from `_VOLATILE_STRIP_KEYS`) + regenerated 5 goldens | `context_providers/opendesign.py`, `engine.py`, `characterization/_normalize.py`, 5× `golden/*.events.json` | Complete | → 07-06-PLAN.md / 07-06-SUMMARY.md |
| 07-07 | Delete dead dual validation fix-loop (cluster B / WR-07, WR-08) | Removed the dead `else`-branch `_run_validation_fix_loop`/`_SkippedRender`/duplicated fix-sig helpers from `task_loop.py` (−263 lines); strategy now delegates unconditionally to `runner.run_validation_fix_loop` | `strategies/task_loop.py`, `test_strategies.py` | Complete | → 07-07-PLAN.md / 07-07-SUMMARY.md |
| 07-08 | Pre-Phase-7 `context_message` oracle (cluster C step 1) | `oracle/legacy_context_message.py` `build_oracle_message()` (PINNED-BYTES from acd1636); `test_context_message_oracle.py` with a tracked xfail proving the routed build prompt drifted | `characterization/oracle/*` (NEW), `test_context_message_oracle.py` (NEW) | Complete | → 07-08-PLAN.md / 07-08-SUMMARY.md |
| 07-09 | Cluster C part 2 — parity restorations vs the oracle | Restored CR-01/02/04, WR-01/02/03/05; `RAW_BLOCK_PREFIX` sentinel for raw injection parts; bare END markers; unconditional TEMPLATE COMPLIANCE; skeleton wrapper via `ectx.current_prototype_skeleton`; typed re-persist after fix (WR-05); flipped xfail→hard PASS; regenerated 3 drifted goldens | `engine.py`, `opendesign.py`, `kernel_services.py`, `task_loop.py`, `ppt.py`, oracle, 3× goldens | Complete | → 07-09-PLAN.md / 07-09-SUMMARY.md |
| 07-10 | Declared revision-intent + kernel-evicted revision block (cluster D + E/CR-06) | `DeliverableSpec.revises_existing` declared flag; `PostStep` Protocol port + `post_steps/revision_validation.py` capability + `Step.post_step`; evicted the whole prototype-revision block (`REVISION_FILE_NAME`, agent-id literal, DELIBERATE EXCEPTION) from the kernel | `plan.py`, `compiler.py`, `prototype_revision/workflow.yaml`, `base.py`, `registry.py`, `previous_run.py`, `engine.py`, `kernel_services.py`, `post_steps/*` (NEW) | Complete | → 07-10-PLAN.md / 07-10-SUMMARY.md |
| 07-11 | De-hardcode `prototype.html` + prove SC-001 (cluster E / CR-05, CR-07) | Threaded `ctx.deliverable.name` through `task_loop`+seam (13× literal removed); `TaskSource.source_step`/`spec_step` declared; declared `seed_files.from_run` honored; the SC-001 PROOF — a NON-prototype `task_loop` fixture (`sc001_task_loop`) producing `app.py` end-to-end, zero engine edits | `plan.py`, `compiler.py`, `prototype/workflow.yaml`, `task_loop.py`, `kernel_services.py`, `engine.py`, `context.py`, `previous_run.py`, `fixtures/sc001_task_loop/*` (NEW), `test_sc001_nonprototype_task_loop.py` (NEW) | Complete | → 07-11-PLAN.md / 07-11-SUMMARY.md |

### 3. Capabilities, Modules, Schema & API Added
**New registered capability impls (`kind:name`)** — all bound via `registry.install()`/`_register_builtins()` into the `_IMPLS` map (D-02 seam). → 07-01/02/03/10-SUMMARY.md (Accomplishments):
- `strategy:single_shot` (`SingleShotStrategy`), `strategy:task_loop` (`TaskLoopStrategy`)
- `deliverable:single_file`, `deliverable:serialized_sandbox`, `deliverable:streamed_text`, `deliverable:ppt`
- `context_provider:opendesign` (`OpenDesignProvider`), `context_provider:previous_run` (`PreviousRunProvider`)
- `task_parser:heading_tasks` (`HeadingTasksParser`)
- `compaction:html_skeleton` (`HtmlSkeletonCompaction`) — duck-typed `compact(html)->str`, NO Protocol port (intentional; a port would be a `base.py` edit, Phase 8). → 07-03-SUMMARY.md (Decisions Made)
- `post_step:revision_validation` — NEW capability KIND added this phase (07-10); the only NEW `_KNOWN` membership entry. → 07-10-SUMMARY.md
**New ports/Protocols:** `PostStep` Protocol added to `capabilities/base.py` (07-10). The other 6 ports pre-existed (Phase 4).
**Resolution seam (D-02):** `CapabilityRegistry.resolve(kind,name)` (static dict lookup over `_IMPLS`; checks `_KNOWN` first, raises on unknown/unbound — NOT `getattr`/`eval`/`importlib`), `install()`/`_register_builtins()` (explicit local-import binding, lazy on first call; NO `@register`/`discover()` — that is Phase 8). `is_registered`/`_KNOWN`/`resolve_alias` untouched. → 07-01-SUMMARY.md (Accomplishments).
**Runner handle (D-03):** `ExecutionContext.runner` object-typed field; concrete `KernelServices` (`execution_engine/kernel_services.py`) — the ONLY kernel module capabilities reach `_run_agent`/sandbox/`static_check`/`render_check`/typed reads/OD reads/parent-run read/`run_validation_fix_loop`/`persist_task_html` through. KernelServices DELEGATES to the engine's existing `_run_agent` (which wraps `create_deep_agent` via `langchain_deepagents` — INV-13, no hand-rolled loop). → 07-04-SUMMARY.md (Accomplishments).
**Manifest/compiled-model fields made live or added:** `strategy`, `task_source.parser`, `deliverable.strategy`/`deliverable.name`, `compaction`, `context_providers`, `clarify.mode`, `seed_files` (Phase-4 inert fields, now consumed); NEW declared: `DeliverableSpec.revises_existing`, `Step.post_step`, `TaskSource.source_step`/`spec_step` (07-10/07-11). → 07-10/07-11-SUMMARY.md.
**Migrations:** NONE — Phase 7 is additive engine-internal + capability code only, no DB table / no Alembic revision (per SPEC Out-of-scope). → 07-SPEC.md (Boundaries).
**API endpoints / event types:** NONE new — the WS event vocabulary is byte/multiset-preserved (strategies re-yield the engine's existing event dicts). → 07-PATTERNS.md (Yield-the-engine's-event-dicts).
**Registry counter (_KNOWN):** entered at 15 (kind,name) pairs (Phase 4 + Phase 6 model_catalog); the strategies/resolvers/providers/parser/compaction names were ALREADY in `_KNOWN` (only their impls were added to `_IMPLS`). `post_step:revision_validation` (07-10) is the one NEW `_KNOWN` entry → 15→16; the drift guard `test_registry_capabilities` was updated 15→16. → 07-10-SUMMARY.md (Deviations, auto-fix 1).

### 4. What Was Deleted / Superseded (INV-12 move-don't-copy)
This phase is the phase's whole point — a large deletion. → 07-05-SUMMARY.md (Accomplishments) · 07-VERIFICATION.md (Observable Truth 7).
- **DELETED from `agents/execution_engine/engine.py` (L1–L13 leaks)** — do NOT resurrect: `_PPT_PIPELINE_TYPES`/`_PROTOTYPE_PIPELINE_TYPES` (L1 name-sets), `_resolve_final_output` (L2/L9 deliverable chooser), `_sanitize_carousel_deck_html`+`_unwrap_artifact` (L3 — both lifted to `_artifact.py` / owned by `ppt` resolver), `ALWAYS_CLARIFY` (L6 — re-homed to `compiled.clarify.mode == "auto"`), the `spec.id == "prototype-build"` dispatch predicate (L7), `_run_build_task_loop`/`_write_build_reference_files`/`_count_plan_tasks`/`_extract_task_block` (L11), `_build_context_message` (L12), `_extract_html_skeleton` (L13), and the `_legacy_*_DEAD` strangler stubs from 07-04.
- **DELETED whole package:** `agents/prototype/` — `pipeline.py` (dead; carried `SKIP_PLANNER_FOR_PROTOTYPE`=L5, `PROTOTYPE_PIPELINE_TYPES`, `is_prototype_pipeline`) + `context.py` (emptied — its 3 OD loaders MOVED to `execution_engine/od_context.py`, NOT into the capability, to satisfy import-linter) + `__init__.py` + `README.md`. INV-12 exit gate proven. → 07-02-SUMMARY.md (Decisions Made) · 07-05-SUMMARY.md.
- **EVICTED in 07-10 (cluster E/CR-06):** the kernel-resident prototype-revision block, `REVISION_FILE_NAME`, the `prototype-revision-agent` literal, the `_AGENT_KIND_MAP` revision entry, and the "DELIBERATE EXCEPTION" comment — relocated to the `previous_run` provider + the `revision_validation` post-step capability.
- **DELETED test files (07-05, fully superseded):** `test_phase4_build_loop.py`, `test_pipeline_type_routing.py`, `tests/unit/test_final_output_resolution.py`.
- **Ledger rows flipped ☑ (kernel-scoped patterns), SHA `1d9234b`:** L1–L13 in `migration-ledger.md`; L4/L8 + L11 patterns REFINED to the leak-construct (bare token survives as LIVE behavior); L1 pattern EXCLUDES `REVISION_FILE_NAME` (still live). L14/L15/L16 re-confirmed ☑; D1 stays VOIDED (`_handle_revision`/`run_revision` retained). → 07-05-SUMMARY.md (key-decisions).
- **Gates armed:** `test_banned_patterns.py` flipped warn-only → kernel-scoped (`agents/execution_engine/`) HARD-FAIL with a non-vacuity guard on `class ExecutionEngine`; `test_migration_ledger.py` per-row kernel-scoped. import-linter contract "agents.capabilities must not import the execution kernel or the web layer" stays KEPT (3 contracts, 0 broken). → 07-05-SUMMARY.md (Files).

### 5. Key Decisions & Locked Constraints (do NOT contradict)
- **D-01:** §32 capability subpackage tree under `agents/capabilities/{strategies,deliverables,context_providers,task_parsers,compaction,post_steps}/`; one impl per module; `name` attr == registry key == manifest reference (snake_case). → 07-CONTEXT.md (Area A).
- **D-02:** Resolution seam EXTENDS `CapabilityRegistry` (separate `_IMPLS` map + `resolve()`); explicit `install()` registration, module-level singleton (capabilities stateless, per-run state on `ExecutionContext`). Evolution to `@register`/`discover()`/trust is Phase 8 — do NOT add decorators here. → 07-CONTEXT.md (Area B).
- **D-03:** `ExecutionStrategy.run(step,ctx)` OWNS the step loop; the kernel run-primitive/sandbox/validators are reached ONLY via the object-typed `ctx.runner` handle. Capabilities MUST NOT import `agents.execution_engine.*` or `app.*` (import-linter). `ctx`/`step` typed `Any`/`object`; capabilities MAY import `agents.workflows.plan`. → 07-CONTEXT.md (Area C) · 07-PATTERNS.md (Shared Patterns).
- **D-04:** opendesign re-home is move-don't-copy; the 3 loaders live in `od_context.py` (a kernel-package boundary module that legitimately imports `app.services.od_loader`) — NOT inside the capability (that would break import-linter). The provider composes blocks FROM the boundary `od_context` dict + handle (Assumption A6). → 07-02-SUMMARY.md (key-decisions).
- **Parity traps (locked — do NOT "fix"):** `planner: run` NOT skip (`SKIP_PLANNER_FOR_PROTOTYPE`=False); revision clarify.defaults fall to `custom`; ALL authored manifests have `seed_files: {}` so the `_SEED_FILES` fallback always fires (byte-identical). → 07-PATTERNS.md (Key Patterns) · 07-RESEARCH.md (Pitfall 2).
- **INV-1 is a KERNEL property** — legitimate non-kernel `pipeline_type`/name refs (registry `REVISION_BASE_MAP`/`PIPELINE_AGENTS`, `app/core/entitlements.py`, `app/api/runs.py`/`websocket.py` display, tests, the move-don't-copy capability homes) are RETAINED; the grep gates are kernel-scoped. → 07-SPEC.md (Req 9, Out of scope).
- **Revision behavior keys off the DECLARED `deliverable.revises_existing` flag** (NOT the `"previous_run" in context_providers` proxy, NOT the workflow name); the CompiledWorkflow is compiled at run ENTRY so the flag is available to per-run setup. → 07-10-SUMMARY.md (key-decisions).
- **WR-04 fix (locked):** `previous_run.assert_owns` now FAILS CLOSED on any non-`PermissionError` store error (returns `{}`, no seed); `PermissionError` still propagates (L16, never swallowed). → 07-VERIFICATION.md (WR-04 Fix Verification).
- **D1 VOIDED** (`_handle_revision`/`run_revision` is a LIVE PPT-revision handler, not dead code — not touched). → 07-SPEC.md (Background, Out of scope).

### 6. Status, Verification & Evidence (what HAPPENED)
- **Verdict: PASSED, 9/9 must-have truths VERIFIED** (4th/final verification pass). → 07-VERIFICATION.md.
- **Evidence:** full agents suite 582 passed / 19 skipped / 0 failed; SC-001 proof 3 hard PASS; oracle 10 hard PASS (no xfail masking); routing-parity 5; ownership/L16 13; revision-gating 7; context-providers 17; all 5 characterization pipelines green (10) with goldens regenerated to oracle ground truth; INV-1 kernel grep = 0; import-linter 3 contracts kept / 0 broken; migration-ledger 26/1 (L1–L13 ☑ kernel-scoped); 0C ≥50% reduction gate held (STATE records 96.9%). → 07-VERIFICATION.md (Full Regression Results).
- **Code-review findings:** TWO review passes. (1) `07-REVIEW-DEEP.md` — deep 9-angle pass found **15 findings (0 blocker, 7 CR, 8 WR)** in 4 clusters [B golden-safe, C parity-needs-oracle, D revision-gating, E sc001-architecture] + 2 systemic (07-06 de-blind pinned DRIFTED bytes; SC-001 prose contradicted by hardcoded prototype names). ALL 15 closed by plans 07-07…07-11. (2) `07-REVIEW.md` — standard pass on the gap-closure, **10 findings (0 CR, 5 WR, 5 IN)**: WR-01 (RESOLVED, commit `16cfe43`) + WR-04 (RESOLVED, commit `96b6484`); WR-02/WR-03/WR-05 + IN-01..IN-05 INTENTIONALLY DEFERRED as latent/cosmetic. (3) `07-REVIEW-0706.md` — focused 07-06 review; its CR-01 (drop the example gate) was REJECTED as a false-positive (the `is_builder` gate is the deliberate Phase-4 contract; dropping it would re-leak example.html to planning agents). → 07-REVIEW-DEEP.md / 07-REVIEW.md / 07-REVIEW-0706.md.
- **Security/threats closed:** L16 cross-owner parent-run denial (`assert_owns` before any seed, `PermissionError` propagates, now fail-closed); INV-13 (no new agent loop — KernelServices wraps existing `_run_agent`); INV-1 kernel re-coupling now HARD-FAILED in CI. threats_open: none. → 07-04/07-05-SUMMARY.md (Threat Flags) · 07-VERIFICATION.md.
- **Key commit SHAs:** 07-01 `0a19dd4`,`8f68a6c`; 07-02 `d677944`,`fda337d`; 07-03 `a963ed9`; 07-04 `09cffc6`,`0129cea`; 07-05 `1d9234b`(L1–L13 delete + ledger SHA),`0bb8bca`,`5d141fc`; 07-06 `2a4335a`,`e8e4a89`,`fdd8aef`; 07-07 `5540b5d`,`cc716cd`; 07-08 `2bc175d`,`ac382af`; 07-09 `54e0bef`,`f8ccdb2`,`0b7ada2`; 07-10 `37fd045`,`6cf201f`,`db6e923`; 07-11 `c50cae9`,`00262f0`,`7152b27`; review fixes `16cfe43`(WR-01),`96b6484`(WR-04). Baseline ref `acd1636` (last Phase-6 commit; oracle pinned to it).

### 7. Gotchas, Survivors & Carry-Forward
- **LIVE SURVIVORS that LOOK like leaks (do NOT delete):** `_handle_revision`/`run_revision` (D1 voided, PPT-revision handler); `REVISION_FILE_NAME` (still live in the agnostic revision setup `_slim_revision_message` — excluded from the L1 ledger pattern); `_run_validation_fix_loop` + `_load_template_example` (LIVE via the KernelServices handle — dropped from the L11 ledger pattern); the engine `_run_validation_fix_loop` `agent_id="prototype-build"` default (IN-02, unreachable — callers always pass explicit id); `_AGENT_KIND_MAP` prototype ids (lineage labels only, not a routing branch). → 07-05-SUMMARY.md (key-decisions) · 07-VERIFICATION.md (Anti-Patterns Remaining).
- **DEFERRED findings (latent, unreachable today — close before exercising):** WR-02 silent `prototype.html` fallback when `deliverable.name` is empty (add manifest validation in Phase 8); WR-03 `previous_run.load` is re-invokable per-agent and would re-seed/re-`assert_owns` IF a `previous_run` workflow ever declared `injects` (none do today); WR-05 `KernelServices.run_agent` mutates shared `ectx` build-scratch with no re-entrancy guard — a latent FAN-OUT hazard (task_loop is strictly sequential today; MUST address before Phases 11/12 fan-out). → 07-VERIFICATION.md (Deferred Findings) · 07-REVIEW.md.
- **DEFERRED items file (`deferred-items.md`):** (a) 8 pre-existing unrelated failures (7× `test_logout.py` JWT/JTI, 1× `test_pipeline_cancel.py`) — out of scope, NOT introduced by Phase 7. (b) `live_harness.py` snapshots the DELETED thin-store `engine._store.store` (removed in Phase 1B/05-07) — a 05-07 follow-up; repoint to the typed `ScopedStore`/`ArtifactGraph`. (c) test-isolation pollution: `test_strategies.py::test_task_loop_requests_html_skeleton_compaction_for_task_2` mutates the process-global registry `_IMPLS` without full restore → poisons characterization snapshots when run in the same session (pre-existing test-harness hygiene bug; needs an autouse registry-reset fixture). → deferred-items.md.
- **The oracle is the source of truth for context_message bytes** — `characterization/oracle/legacy_context_message.py` PINS the pre-Phase-7 (`acd1636`) bytes; the de-blinded goldens were regenerated to MATCH the oracle (not the 07-06 drift). Any future context-injection change must keep `test_context_message_oracle.py` (10 hard PASS) green. → 07-08/07-09-SUMMARY.md.
- **No phase reopened after final close;** Phase 8 (factory F1–F5 decoupling) is the next leak family; formal Validator/GateHandler registries + `@register`/`discover()`/trust are Phase 8. → 07-05/07-11-SUMMARY.md (Next Phase Readiness).

### 8. File Index (every file in this folder)
- `07-SPEC.md` — the 11 locked requirements (PARITY-01..09 + the 4 interview seams), boundaries, per-leak acceptance greps; open when you need the exact requirement/leak definition.
- `07-CONTEXT.md` — the 5 locked HOW decisions D-01..D-05 + canonical refs + reusable assets; open before touching capability layout / the resolution seam / the runner handle.
- `07-DISCUSSION-LOG.md` — audit trail of the "lock all to recommendations" discuss-mode meta-decision; rarely needed.
- `07-PATTERNS.md` — analog map (every new file → its live engine source region file:line) + the 6 pitfalls; open to find WHERE a behavior was lifted from.
- `07-RESEARCH.md` — deep research; key conclusion "Phase 7 is relocation, not reimplementation"; the `_run_agent` call/thread-id shape, the 4 pitfalls, Assumptions A1/A6, Open Q1/Q2. Open for the runner-handle / resolution-seam design rationale.
- `07-VALIDATION.md` — per-phase validation strategy: the parity/ratchet test commands, Wave-0 new-test list, the 2 manual-only live (Chromium/Bedrock) deferrals.
- `07-01-PLAN.md` / `07-01-SUMMARY.md` — strategies + resolution seam + runner handle + heading_tasks parser.
- `07-02-PLAN.md` / `07-02-SUMMARY.md` — 4 deliverable resolvers + 2 context providers + the opendesign relocation.
- `07-03-PLAN.md` / `07-03-SUMMARY.md` — html_skeleton compaction (verbatim lift) + 0C gate re-point.
- `07-04-PLAN.md` / `07-04-SUMMARY.md` — engine wiring (KernelServices, per-step dispatch, generic injector) + 5-pipeline parity proof; has the vulture orphan list for 07-05.
- `07-05-PLAN.md` / `07-05-SUMMARY.md` — L1–L13 deletion + prototype-package removal + ledger flip + banned-pattern hard-fail; THE SC-001 landing.
- `07-06-PLAN.md` / `07-06-SUMMARY.md` — opendesign L12 byte-parity gap closure (CR-01/02/03) + de-blind goldens.
- `07-07-PLAN.md` / `07-07-SUMMARY.md` — delete the dead dual validation fix-loop (WR-07/WR-08).
- `07-08-PLAN.md` / `07-08-SUMMARY.md` — the acd1636 context_message ORACLE + tracked xfail divergence target.
- `07-09-PLAN.md` / `07-09-SUMMARY.md` — cluster C restorations vs the oracle (CR-01/02/04, WR-01/02/03/05).
- `07-10-PLAN.md` / `07-10-SUMMARY.md` — declared `revises_existing` + `post_step`/`revision_validation` capability; kernel revision block evicted (WR-04/06, CR-06).
- `07-11-PLAN.md` / `07-11-SUMMARY.md` — de-hardcode `prototype.html` + declared source/seed steps + the NON-prototype SC-001 proof (CR-05/CR-07).
- `07-REVIEW-DEEP.md` — the deep 9-angle multi-agent review: 15 findings, 4 clusters, 2 systemic; the source of plans 07-07…07-11.
- `07-REVIEW-0706.md` — focused 07-06 review; its CR-01 (drop example gate) was REJECTED as false-positive (adjudication with git evidence).
- `07-REVIEW.md` — standard gap-closure review (10 findings): WR-01/WR-04 resolved, rest deferred; the authoritative WR/IN deferral list.
- `07-VERIFICATION.md` — final PASSED verdict, 9/9 truths, full regression table, WR-01/WR-04 fix verification, requirements coverage, deferred-findings rationale.
- `deferred-items.md` — out-of-scope/pre-existing failures + the 3 test-harness hygiene items carried forward.
