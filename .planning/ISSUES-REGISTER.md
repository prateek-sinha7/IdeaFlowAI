# Issues Register

> Running log of every issue found — product, test-infra, harness, or cosmetic — no matter how small.
> Seeded 2026-06-13 from the milestone-end live pass + Phase 15 + the in-progress UI campaign.
> Status: OPEN · FIXED · DEFERRED (recorded, intentional) · MONITORING (watch on next pass) · WONTFIX.
> Tag: `product` (user-facing/runtime), `test-infra`, `harness` (my driver, not the app), `docs`, `cosmetic`.

| ID | Sev | Tag | Status | Title | Source / Evidence |
|----|-----|-----|--------|-------|-------------------|
| ISS-001 | major | product | **FIXED** (P15, live-confirmed) | od_ppt deliverable returned the validator's QA narration, not the deck (LV-02) — broke od_ppt preview + the whole revision chain | REPORT-2026-06-12 §LV-02; closed by Phase 15 prompt contract; live run 8c060c35 resolved an 18,661-char deck |
| ISS-002 | minor | test-infra | **FIXED** (16-02) | `test_pipeline_cancel.py::test_cancel_marks_workflow_cancelled_and_sends_ack` fails deterministically — CPython 3.11 `wait_for` result-vs-cancel race at the 3 WS drainer sites (websocket.py:1669/:802/:1999); NOT credential-gated (LV-01) | REPORT-2026-06-12 §LV-01; fix: `asyncio.timeout()` at the 3 sites |
| ISS-003 | minor | test-infra | FIXED (17-01, 2026-06-13) | `test_phase3_token_delta_live` hangs live forever at the clarify `event.wait()` — `_drive_live` never sets `clarify.mode=off` (LV-03) | REPORT-2026-06-12 §LV-03; FIXED 17-01: `_drive_live` wraps `compile_for_run` to set `compiled.clarify.mode="off"` (restored in finally) replacing the dead `ALWAYS_CLARIFY` flag; bounded offline fault-injection test `test_drive_live_does_not_hang_at_clarify_gate` proves no hang. Live `RUN_LIVE_BEDROCK=1` re-run deferred (needs creds). |
| ISS-004 | minor | product | **FIXED** (19-03, structural; live re-confirm deferred) | sdlc-governance agent family emits fabricated tool-call XML preamble (`<function_calls>`/`<invoke name="read_file">`) on live Haiku before content (F4 residual) | REPORT-2026-06-12 §F5-residual; P15 defused the prompt trigger; 19-03 added the deterministic engine `agent_chunk` sanitizer (chunk-straddle buffer, tool-less) so fabricated XML can't reach the live UI stream. Live re-confirm (0 tool-XML in chunks) deferred to the live pass. |
| ISS-005 | minor | product | **FIXED** (19-02, structural; live re-confirm deferred) | `app-infra-generator` ignored its own `/api/v1` rule live (0× in 3,083 lines vs api-design 58× / devops 50×) (F5 residual) | REPORT-2026-06-12 §F5; P15 repositioned the prompt; 19-02 added the deterministic pure-stdlib `api_prefix` validator wired as an event-free `post_step` on the app_builder infra step (catches missing `/api/v1` in a `validation_results` row, golden byte-identical). Live re-confirm (count `/api/v1`) deferred to the live pass. |
| ISS-006 | minor | product | **FIXED** (19-01; live re-confirm deferred) | app_builder contract drift: `tests/setup.ts` imports `getDb` but impl exports `getDatabase` — one TS2305 in the Jest global setup | REPORT-2026-06-12 §F5 re-audit, residual 1; FIXED 19-01: a shared canonical `getDatabase` named-export contract threaded through the producer (app-code-generator) + consumer (app-test-implementation, incl. the Jest global-setup) prompts + a 2-way grep pin. Live re-confirm (tsc/grep no getDb mismatch) deferred to the live pass. |
| ISS-007 | minor | product | **FIXED** (16-02) | IN-02: `pipeline_cancelled` ack not delivered on the live wire for `cancel_pipeline`. **UI manifestation (S12)**: after the real Stop click the header correctly flips to "Pipeline stopped" and the DB row IS `cancelled` (server-side correct), BUT an agent card stays stuck on "In progress... RUNNING" with no completion toast — inconsistent panel state, because the terminal event never arrives to clear the card. Upgraded trivial→minor: it's user-visible (a stopped run still shows a running agent). | REPORT-2026-06-12 §Known-Info + UI campaign S12 `11-after-cancel` 2026-06-13 (DB=cancelled, header="Pipeline stopped", card stuck RUNNING) |
| ISS-008 | trivial | product | **FIXED** (16-03) | IN-04: durable-REPLAY revision frames carry `section: None` (live-attach carries the real section; replay path uncovered) | REPORT-2026-06-12 §Known-Info |
| ISS-009 | trivial | product | **FIXED** (16-03) | IN-01 (Phase 12): `pipeline_reconnected` live-attach ack omits `live:true` (only `live:false` is explicit) | REPORT-2026-06-12 §Known-Info |
| ISS-010 | trivial | product | **FIXED** (17-02, by-test) | OTLP span export verified deterministically OFFLINE: a new `test_hooks.py` otel test injects an `InMemorySpanExporter` via the hook's OWN `_build_span_processor` factory (+ reset of the module-private `_TRACER`) and proves the real `OtelTracingHook.handle` exports exactly 1 span with `flowin.hook/flowin.event/flowin.agent_id` attrs under scope `flowin.agents.hooks.otel_tracing`; a parallel test pins the OTLP-pkg-absent → console-processor degrade. Live OTLP-collector export remains an OPTIONAL end-of-milestone smoke, not a blocker. | REPORT-2026-06-12 §Phase-8 sweep; closed by 17-02 in-memory-exporter test |
| ISS-011 | minor | product | **FIXED** (17-03, by-test; Part-1 live re-run deferred) | env/credential (NOT product): the 3 Phase-8 HITL live-tail tests + the self-check traced to ONE cause — the default-profile SSO token expired mid-sweep. RECURRING-MODE FIX (Part 2, the real deliverable): `test_phase8_live.py` now re-checks `live_skip_reason()` per live test (autouse on `TestLiveHITL`) → `pytest.skip()`s with the re-run-after-`aws sso login` reason on mid-sweep expiry instead of failing the 0-token assertion; the self-check no longer asserts strict `_LIVE_SKIP == live_skip_reason()` equality (tolerates the collection-valid → runtime-expired drift, asserts gate correctness per point-in-time). Proven OFFLINE: an expired-creds simulation (monkeypatched `live_harness.live_skip_reason`) shows the guard SKIPS (not FAILS) + a no-op complement; `TestOfflineHITL` green. The transient-error classification (`model_policy.py:185-211`) was deliberately left UNCHANGED (widening it to retry expired SSO would mask real `AccessDenied`/`ValidationException` — INV-3). DEFERRED (not blocking, needs fresh creds): Part 1 — re-run the 3 HITL cases after `aws sso login` (sub-$0.05, ~minutes) on the next Bedrock pass to confirm the live-green path. | REPORT-2026-06-12 §Phase-8 sweep (4 failed = 1 root cause); closed by 17-03 skip-on-expiry harness fix |
| ISS-012 | cosmetic | docs | **FIXED** (2026-06-14) | 113 REQ-IDs present in REQUIREMENTS.md body but missing from its Traceability table (project-wide doc drift) | surfaced by `/gsd-progress`, noted in PROJECT.md Phase-14 evolution. **Root cause:** the table used grouped/comma rows (`SAFE-01, SAFE-02, …`), so the coverage check (first table cell must be EXACTLY one REQ-ID) recognized only 9 of 122 → 113 flagged. **Fix:** rewrote `.planning/REQUIREMENTS.md` Traceability table to **one row per REQ-ID** (117 v1 + 6 v2 = 123 rows), each mapped to its owning phase + that phase's `NN-VERIFICATION.md` `status: passed` verdict; stale Pending/In-progress/Partial statuses refreshed to verified-Complete; CTX-04 kept as VOIDED/DEFERRED; API-06 keeps the ISS-014 deletion note; v2 IDs listed explicitly as deferred. **Verify:** body↔table diff = 0 missing / 0 stray under both the strict single-cell parse and the simple `[A-Z]+-[0-9]+` parse (was 113). Requirement *definitions* untouched (body byte-identical to HEAD). |
| ISS-013 | minor | harness | FIXED | UI driver generic selectors didn't match real Flowin controls (submit is "Run with defaults"/"Skip all & run directly"; options are radio-card buttons) → fell back to WS on S01 | UI campaign S01 `02-questionnaire.png`; fixed in driver v2 with the real selector reference |
| ISS-014 | minor | product | **FIXED** (18-04/05) | The **agent-level** custom composer IS live and well-built — the "Compose a custom workflow" tile → IdeaInputPage → Advanced → "Workflow configuration" modal (Agents / Skills & Hooks tabs, drag-to-reorder, "+ Add agent (8 left)") → a searchable "Add agent" library (category sidebar + agent cards with descriptions + "+ Add"). What's dead is only the **capability-level** `CapabilityPalette.tsx`/`WorkflowComposer.tsx` (the `/api/capabilities` allow-listed-palette composer) — mounted by no live route. So users CAN compose custom agent workflows; they CANNOT compose from the capability palette. | UI campaign S07 `A-agents-popup.png` + `B-agent-library.png` 2026-06-13 (corrects the earlier "composer is dead" reading) |
| ISS-015 | trivial | product | **SHIPPED — Phase 20 / WF-DB-01** (was WONTFIX 18-05) | No generic pipeline-type picker in the UI — **was BY DESIGN; now SHIPPED in Phase 20.** The product launch surface is the hardcoded 6-tile `CreationHub.tsx:15-22` (a closed launchable set); `od_ppt`/`od_prototype` go through the PPT/Prototype wizards (which MUST collect template + design-system → a bare picker structurally can't); bare `prototype`/`ppt` are template-gated-unreachable (D-13-06-a, `websocket.py:1344-1361`); `sample_*` aren't in `SUPPORTED_PIPELINE_TYPES` (test fixtures). The custom-workflow need is already met by the live agent-composer (ISS-014). A user-facing named-manifest picker = WF-DB-01 (v2). **Guardrail (REJECTED hack):** if ever built, a manifest-driven picker MUST read `GET /api/workflows` (manifest discovery — exists, no FE consumer today) + a `user_launchable` manifest flag — NOT a hardcoded dropdown of names (that hack ships broken template-gated buttons + exposes test fixtures). **UPDATE 2026-06-14 — SHIPPED in Phase 20 exactly per this guardrail:** a data-driven catalog reads `GET /api/workflows` + the new additive `user_launchable` manifest flag (zero engine/kernel edits, no new tables), filters by `user_launchable` ∧ tier, reuses CreationHub/AgentModelPicker, and routes prototype/ppt to their wizards — NOT a hardcoded dropdown. INV-3 goldens byte-identical, SC-001 (`WORKFLOWS` const deleted), lint-imports 4/0, verify 5/5. | FE selector map 2026-06-13 (§2); dispositioned 18-05 per 18-CONTEXT ISS-015 LOCKED; **realized in Phase 20 (2026-06-14)** |
| ISS-016 | **major** | product | FIXED (engine, 16-01) | A hard model error (`ValidationException` from ConverseStream) on EVERY agent is swallowed into an empty-but-successful agent_complete (0 tokens, dur ~0.2s, no chunks) — the run ends `pipeline_complete` with DONE badges + "User Stories complete" toast + empty Preview, instead of `pipeline_failed`. F3 family (13-06), different trigger: F3's pipeline_failed branch keys on `agent_error` counts, but a runner-level model exception becomes a silent empty completion, not an agent_error, so it rides through as success. Offline tests can't catch it (scripted model never raises ConverseStream errors). | UI campaign S01 2026-06-13: frames show 6× agent_complete/0-tokens/empty final_output; backend log `deep_agent_runner.astream_events failed: ValidationException ... Operation not allowed`; UI `06-terminal.png` shows DONE + empty preview |
| ISS-017 | minor | product | **FIXED** (16-04 live path; history-reopen gap closed 2026-06-14) | Preview pane shows the empty-state "Output will appear here" placeholder on a run that reported `pipeline_complete` — no error/degraded affordance for a completed-but-empty run (compounds ISS-016: the user sees success chrome + empty canvas with no signal anything went wrong). **Gap-audit 2026-06-14:** the 16-04 fix landed the affordance on the LIVE execution path but its history-reopen half threaded through a DEAD `onSelectWorkflowRun`/`Sidebar` surface (unreachable — no live route mounts it), so the real `WorkflowHistory.tsx` detail view STILL showed "No preview available" for a terminal-empty failed/cancelled/degraded reopen — SC3 "BOTH paths" was half-met. **CLOSED 2026-06-14:** `DegradedRunAffordance` now renders in `WorkflowHistory.tsx` detail (server-status-gated on `selectedRun.status ∈ {failed,cancelled,degraded}`), sharing the lifted `parseFailedAgentIds` (`lib/parseFailedAgents.ts`) + the exported affordance (no dual-impl); +5 WorkflowHistory tests; independently verified (live PreviewPanel path unbroken, 19/19 tests). | UI campaign S01 `06-terminal.png`; gap-audit + close 2026-06-14 |
| ISS-019 | cosmetic | product | **FIXED** (18-02; live fold proof in Playwright pass) | WaveTreePanel renders correctly (mounts, "WAVE / SUBAGENT TREE" heading + "No waves running." empty state + populated Wave 0/Wave 1 groups during active waves — backend wave data perfect) but sits below the left execution-column fold at 1440×950 (heading at y=963, ~13px below the fold; reachable by scrolling the column). A headline Phase-12 capability is invisible at default viewport without scrolling. Matches 12-UAT test-5 cosmetic note; persists. Worth a follow-up: mount it higher or collapse the agent panel. | UI campaign S09 `wave-tree-scrolled.png` + DOM probe 2026-06-13 |
| ISS-020 | trivial | harness | NOTE | ws_rewrite scenarios (sample_wave/fanout/ui_custom_proto — no product launch surface) show "User Stories" FE labels + the user_stories questionnaire, because the rewrite only changes the backend `pipeline_type`, not the FE's displayed workflow type. Harness artifact, not a product bug; means FE pipeline-specific labels aren't faithfully exercised for the fixture workflows. | UI campaign 2026-06-13 |
| ISS-021 | minor | product | **FIXED** (18-01/03; live render proof in Playwright pass) | A custom workflow producing a non-markdown deliverable (HTML) has no faithful FE path: Preview shows "Output will appear here" (no renderer for the type) and the Files tab lists only the 4 per-agent `.md` outputs, NOT the resolved `prototype.html` deliverable (which IS in `pipeline_complete.final_output`, 21KB). The actual built artifact is unreachable from the UI. Inherent to the dynamic-composer design (FE can't know a custom type's deliverable shape) — but means SC-001 custom workflows are backend-complete yet UI-incomplete. Relates to ISS-014 (composer is dead UI anyway). | UI campaign S11 `07-preview.png` (empty) + `07b-files.png` (agent outputs only); backend final_output has the full HTML |
| ISS-018 | env | infra | NOTE | `hexaware-srini` Bedrock began returning `ValidationException: Operation not allowed` for Haiku 4.5 ConverseStream mid-session (worked earlier today for the P15 live check; direct CLI converse also fails) — not quota/expiry. Switched campaign to the `default` profile (account 473293451041, working). Likely an SSO-role/Bedrock-entitlement change on srini. | direct `aws bedrock-runtime converse --profile hexaware-srini` 2026-06-13 |
| ISS-022 | minor | test-infra | **FIXED** (2026-06-14) | `frontend/src/components/workflow/AgentProgressPanel.test.tsx:176` fails — **pre-existing, NOT a Phase-16 regression** (the file is untouched since base commit `a219fade`; the assertion text is unchanged; it only imports an additive-optional type). Surfaced while running the FE suite during Phase 16. | Phase 16 16-04 execution 2026-06-13; logged to `16-.../deferred-items.md` |
| ISS-023 | trivial | product | **FIXED** (2026-06-14) | IN-04 (Phase-16 review): the per-chunk cooperative-cancel path can emit `pipeline_cancelled` twice (engine yield + the bg-task handler) — **client-invisible** (the FE reset is idempotent) and pre-existing to the 16-02 cancel rework; left as-is, worth a tidy later. | 16-REVIEW IN-04 2026-06-13 |
| ISS-024 | cosmetic | product | **FIXED** (2026-06-14) | IN-02 (Phase-16 review): the new terminal-failure Preview affordance lists raw agent **IDs** instead of human agent **names** (no id→name map threaded into the degraded panel). | 16-REVIEW IN-02 2026-06-13 |
| ISS-025 | minor | test-infra | **FIXED** (2026-06-14) | `frontend/src/.../workflowChaining.test.ts` — 6 **pre-existing** FE test failures (NOT a Phase-18 regression: present with the deleted composer files restored; the failing sources were last touched in pre-phase commits, never by P18). Surfaced while running the FE suite during 18-04. Companion to ISS-022 (AgentProgressPanel.test.tsx:176). | Phase 18 18-04 execution 2026-06-13; logged to `18-.../deferred-items.md` |
| ISS-026 | minor | test-infra | **FIXED** (2026-06-14) | `test_phase5_revision_validation.py::test_event_types_subset_of_documented_vocabulary` fails offline — `prototype_revision` emits a `gate_blocked` event not in the test's documented-vocabulary set. **Pre-existing** (proven NOT a Phase-19 regression: re-ran green/red with the 19-02 changes stashed). Surfaced while running the suite during 19-02. | Phase 19 19-02 execution 2026-06-13; logged to `19-.../deferred-items.md` |
| ISS-028 | low | product | **FIXED** (2026-06-14) | **app_builder task-loop partial failure can report a clean `pipeline_complete` (no `degraded`).** Surfaced by the 2026-06-14 gap-audit of ISS-016. `engine.py:2141-2143` computes `_unrecovered_failed = _failed_agent_ids - {r.agent_id for r in results}`. In the prototype/build task-loop, ONE agent_id (e.g. `prototype-build`) runs across all tasks; if task K succeeds (appends a `prototype-build` result) and task K+1 hard-errors (adds `prototype-build` to `_failed_agent_ids`, returns without appending), the subtraction zeroes it → the run emits `pipeline_complete` with NO `status:degraded` over a half-built deliverable. **Pre-existing** (the agent_id-keyed subtraction is the Phase-13 WR-05 fix, NOT introduced by ISS-016/16-01), LOW-probability (a hard `ValidationException` is account/entitlement-level → all-or-nothing per ISS-018; a realistic mid-build transient is a throttle, correctly retried), and OUTSIDE ISS-016's stated all-fail/agent-granular-partial scope (both of which work + are tested). Belongs to the already-DEFERRED ISS-016 "second signal" (empty-but-clean→degraded) follow-up. **Proper fix (future):** key the degraded decision on `(agent_id, task_number)` rather than collapsing on agent_id. Do NOT reopen ISS-016. | ISS-016 gap-audit 2026-06-14 (`engine.py:2141-2143`, `task_loop.py:211-266`, `kernel_services.py:1181-1197`) |
| ISS-027 | minor | product | **FIXED** (Option A force-proceed; INV-3 goldens byte-identical; live re-confirm deferred) | **"Skip all & run directly" does not skip clarification.** `handleQuestionnaireSkip` (DashboardLayout.tsx:932) sends `submit_questionnaire` with **empty** responses — identical to answering nothing — so the clarify engine (clarify_engine.py) re-evaluates, finds info still missing, and re-asks. It issues **up to 3 rounds** before `clarification_limit_reached` → `pipeline_start`. A user clicking the button once sees a NEW "Quick Setup" panel appear (looks like the skip failed); they must click it ~3×. Behavior contradicts the button's label. **FIX (Option A, proper force-proceed):** the skip button now sends a distinct `skip_clarification: true` on `submit_questionnaire`; the WS handler (`websocket.py:1046`) threads it to `ArtifactStore.set_questionnaire_responses(..., skip_clarification=True)` which records a per-run `_questionnaire_force_proceed` flag; `ClarifyEngine.run` reads it right after `event.wait()` and returns `execution_gate=PROCEED` immediately (no re-ask). **Generic (SC-001 — no pipeline_type branch); INV-3-safe because the goldens compile `clarify.mode="off"` so `ClarifyEngine.run` is never reached** — proven: the 5 characterization goldens stay byte-identical + banned-patterns gate green. FE: `useWorkflow.submitQuestionnaire` + `DashboardLayout.handleQuestionnaireSkip` pass `skipClarification=true`; ordinary answer submits leave it False (byte-identical default). Tests added: `test_execution_engine.py::test_clarify_engine_skip_clarification_force_proceeds_round_1` (drives the real engine: asks once → PROCEED, no limit-reached) + `::test_clarify_engine_empty_submit_without_flag_still_loops` (regression guard: no flag ⇒ still 3 rounds) + `test_artifact_store.py` flag tests. **Not a functional break** — was bounded at 3 rounds. **Evidence:** live WS probe 2026-06-14 — `run_pipeline(user_stories)` → `questionnaire_ready` ×3 (each needing a submit) → `clarification_limit_reached` → `pipeline_start` → `agent_start`. Surfaced during the live E2E pass (TS-V-01 deadlocked on round 2; the live spec's single-shot dismiss was fixed to loop — `dismissClarifyRounds`). Live re-confirm (1× questionnaire_ready then pipeline_start on skip) deferred to the next live pass. | Live E2E TS-V-01 + `/tmp/ws_clarify_probe.py`, 2026-06-14; closed 2026-06-14 |
| ISS-029 | minor | product | **FIXED** (2026-07-03, B-explicit; INV-3 restored) | **od_ppt event-golden diverged for ~3 weeks — a planning agent was leaked `example.html`.** `test_characterization_od_ppt::test_od_ppt_event_snapshot` failed: idx 3 (`od-ppt-brief-analyst`) + idx 7 (`od-ppt-composer`) gained a `TEMPLATE EXAMPLE (example.html)` block absent from the golden. Root cause: **KAN-63 commit `b5f5885e` (2026-06-15)** widened the `opendesign` example gate from `is_builder` to `(is_builder or "workspace" in spec_tools)` to feed the deck COMPOSER (intended), but the broad `workspace`-tool proxy ALSO fed the full `example.html` to the order-1 PLANNER `od-ppt-brief-analyst` (`injects:[template]`, `tools:[workspace]`) — violating the **locked Phase-7 decision** (`07-REVIEW-0706.md`: a proposal to loosen the gate and "re-leak example.html to planning agents" was explicitly REJECTED) and degrading planning (a finished example nudges copy-continue over decompose). The golden (last regen `0b7ada2b`, pre-KAN-63) was never regenerated → INV-3 broke; Phase 24 hand-waved it as "environmental" and deferred WITHOUT a tracked ID. **FIX (B-explicit — proper, no-shortcut; validated by a 5-agent deep investigation):** re-keyed the gate to `(is_builder or "template_example" in injects)` (`opendesign.py`) + declared a new `template_example` inject on `od-ppt-composer/AGENT.md` (composer only) — a declarative per-agent opt-in (SC-001 name-free), keeping `is_builder` as the prototype-builder path the oracle pins. REJECTED alternatives (evidence-backed): `design_system`-coupled gate (would re-leak to `prototype-specify`/`prototype-plan`, both `injects:[template,design_system]`+`tools:[]`, breaking the prototype oracle); make-the-composer-a-builder (semantically false — it streams the deck as text, and `prototype_emit_only` drags in the injection-parts branch); regen-only Option A (pins the violation). Regenerated ONLY `od_ppt.events.json` (composer keeps the block, planner drops it). 2 regression pins added (`test_context_providers.py`: builder/`template_example`→example ✔ vs `workspace`-planner→✗) — closes the coverage gap that let it ship silently. Verified independently: 5 goldens byte/event-identical (od_ppt now green, other 4 untouched — INV-3 held), oracle 10/10, lint-imports 4/0, scope=4 files. Out-of-scope latent gap noted (composer's `TEMPLATE SEED/REFERENCE` injection-parts branch is prototype-tool-gated → a `workspace` composer receives none; separate follow-up, NOT this fix). | `.planning/debug/od-ppt-golden-divergence.md` (root cause + 5-agent verification); commits `46863db7` (fix) + `7714bd4a` (test+golden); 2026-07-03 |
| ISS-030 | minor | product | **FIXED** (quick-260704-er8) | Files tab renders no run-input prompt.md for REVISION runs — derived only from parsed.brief while a revision parses to revisionInstruction with empty brief | 2-agent investigation + live-DB proof (workflow_runs.input persisted 155k+ chars); fixed FIX-033 — parsed-shape dispatch parsed.revisionInstruction OR parsed.brief |
| ISS-031 | minor | product | **FIXED** (quick-260704-p10) | Bedrock prompt caching silently OFF in prod + no operator knob for extended thinking. deepagents' built-in `AnthropicPromptCachingMiddleware` caches ONLY `ChatAnthropic`, but production runs `ChatBedrockConverse` → every build re-sent its ~45-68k fixed prefix uncached across ~300 turns. Fix (FIX-034): a provider-agnostic `_BedrockCachePointsMiddleware` injects `model_settings['cache_control']` on ChatBedrockConverse requests (config-gated `BEDROCK_PROMPT_CACHE_ENABLED` default ON, `BEDROCK_PROMPT_CACHE_TTL` '5m') so `langchain_aws._apply_cache_points` appends cachePoints ≈ 55-80% cheaper billed input; plus an enable-only `THINKING_BUDGET_TOKENS` knob (default 0 = never fires) threading a clamped thinking budget into both `build_model` provider branches. INV-3 goldens byte/event-identical (scripted model is not ChatBedrockConverse; build_model not called under goldens; default budget 0). | Closed by FIX-034 (quick-260704-p10); offline structural proof lands now (10 new unit tests, 5 goldens byte/event-identical, lint 4/0). Live `cache_read_input_tokens>0` confirmation DEFERRED to the end-of-milestone Bedrock pass. |
| ISS-032 | minor | product | **RESOLVED** (quick-260704-ttk) | Run cost not yet cache-discounted — the runner did not surface `cache_read_tokens` / `cache_write_tokens` from LangChain `usage_metadata.input_token_details`, so both cost sites passed 0 for the cache args. `estimate_cost_usd` was already cache-ready (`cache_read` / `cache_write_5m` / `cache_write_1h` tiers, FIX-035), but with FIX-034's Bedrock prompt caching ON in prod a large share of billed input is cache-read (≈0.1x) and was priced at full input rate → cost OVER-reported for cached runs. Resolution (FIX-036): the shared runner surfaces `input_token_details` (cache_read/cache_creation) into the usage event + text-only TokenUsage → the engine accumulates per-agent → `results` + `agent_complete` + run totals on `pipeline_complete` → BOTH cost sites price the UNCACHED split (`input_tokens=max(0, total − cache_read − cache_write)` + cache_read/write tiers + cache_ttl) via the ONE shared `estimate_cost_usd`; golden-neutral via additive `_VOLATILE_STRIP_KEYS` (no regen). | Resolved by FIX-036 (quick-260704-ttk); companion to FIX-034/ISS-031 (Bedrock cachePoints ON). Offline proof lands now (test_iss032_cache_tokens green; 5 goldens byte/event-identical; lint 4/0). Live `cache_read>0` confirmation DEFERRED to the end-of-milestone Bedrock pass. |
| ISS-033 | minor | product | **RESOLVED** — part A closed by FIX-230 (quick-260812-9tq, 2026-08-12); part B split out as ISS-092 | **The row below was STALE for a month and is corrected here.** Original finding: SmartPlanner + ClarifyEngine + the handoff Test/Compliance agents call the model DIRECTLY (not via the shared cached deep-agent runner), so those calls get NO Bedrock prompt caching (the FIX-034 middleware runs only in the runner path) AND their tokens are UNCOUNTED in run cost. **Correction 1 — most of it was already fixed on 2026-07-15 and the row never said so.** Phase 43 plan 04 built exactly the proposed shared cached-invoke helper: `backend/app/agents/cached_invoke.py` (commit `0aa2072c`) and routed four sites (commit `8518f29b`). The aux fold is **live-proven**, not merely unit-tested — on the `user_stories` runs `a4bec7e1` / `a7dba362` / `caa5d175` the `pipeline_complete` totals exceed the sum of the `agent_complete` events by exactly the planner + clarify spend (+2,104 / +2,002 / +2,091 input). Nothing in the card store caught the drift either. **Correction 2 — the true call-site count is 9, not the 3 filed**, and three were unknown to anyone: `handoff/test_agent.py:156` (raw `.ainvoke` — uncached AND uncounted; 43-04 routed `classifier.py` by mistake), `engine.py` `_run_validation_fix_loop` (the internal drain `continue`d past every `usage`), and `chat/concierge.py:365` (discards `usage`; input unbounded). **Correction 3 — severity is understated at `minor`.** Quantified: the fix-loop alone hides ~929,000 input tokens on the live run `fa66227a` = **6.9% of that run's input, +$0.176 of a reported $2.550**; the two cost sites additionally disagree by exactly the `results.pop()`ed specify+plan+analyze trio (70,346 tok on `5ecb990f`, 123,137 on `6e38b9a7` ≈ 3.3%); handoff runs are 100% invisible (no cost surface exists). Every uncounted path is an UNDER-report — there is no over-report anywhere. **Part A (FIX-230)** closes the two measurable, safely-foldable sites: `TestAgent` → `cached_invoke`, and the fix-loop `usage` → the existing `aux_token_usage` fold. | Filed as a companion to FIX-036/ISS-032; **~70% silently closed by Phase 43-04 (2026-07-15)**, the rest by FIX-230 (quick-260812-9tq). Evidence for the corrections: read-only queries against the live `backend/dev.db` + the engine's own INFO log. Remaining scope is ISS-092 (Concierge/handoff — unbounded input and no cost surface), which is a product decision, not plumbing. ISS-034 ("saved $Y") depends on this row and is still OPEN. |
| ISS-034 | minor | product | **RESOLVED** by FIX-239 (quick-260812-mq5, 2026-08-12) — backend half + SIGNED Analytics line; no per-run badge | Optional dollar-savings — surface cache $ saved. Backend would emit `estimated_cost_full_usd` (cost as-if-uncached) → FE shows "saved $Y (Z%)". Needs a backend add-on (per-model rates live backend-side per INV-12); explicitly NOT built in FIX-037 (which shows only the cache token count + %). | Split out of quick-260704-uvs (FIX-037); companion to FIX-036/ISS-032. **BLOCKED-BY / DESIGN CORRECTION (2026-08-12, recorded during quick-260812-9tq):** (a) **Depends on ISS-033.** A "saved $Y" computed from a token base that is missing the fix-loop, the Concierge and the handoff agents publishes a precise-looking figure wrong in a direction the user cannot see — worse than showing nothing, because it invites trust. The *percentage* is fairly robust (numerator and denominator scale together); the *dollar* figure is not. Part A landed as FIX-230; the residual is ISS-092. (b) **The naive spec would render a WRONG-SIGNED number.** Computed across every run in the live `dev.db`: on **7 of 9 runs the saving is NEGATIVE** (−5.0% to −8.9%). Every `user_stories` run has `cache_read = 0` with a non-zero `cache_write` — its agents are single-turn text-only, so each writes a cache entry and nothing ever reads it back, and Haiku's `cache_write_5m` is **1.25×** the input rate. Hand-verified on `a7dba362`: `(3183·1e-6 + 40170·5e-6 + 86976·1.25e-6)·1.1 = $0.344028` (matches the persisted value) vs `$0.320110` uncached. The `+80.6%` aggregate is carried entirely by two long prototype runs and is NOT representative. So the FE contract must render a **signed delta** with an explicit negative case — never "saved $Y (Z%)". (c) Backend shape is unchanged and still small: one extra `estimate_cost_usd(model_id, input_tokens=_tok_in, output_tokens=_tok_out)` call at each of the two cost sites, plus `"estimated_cost_full_usd"` added to `_VOLATILE_STRIP_KEYS` exactly as FIX-036 did for the four cache keys (golden-neutral, no regeneration). (d) Falls out for free: prompt caching is a **measured net cost increase** on single-turn workflows, so `BEDROCK_PROMPT_CACHE_ENABLED` arguably belongs per-step rather than global. | **RESOLVED 2026-08-12 (FIX-239, commit `c5b4a818`), scoped by owner decision D2.** Shipped: `estimated_cost_full_usd` (the as-if-UNCACHED counterfactual on the SAME token base) at BOTH cost sites — `engine.py:2881-2891` and `run_commands.py:2179-2189` (line numbers RE-VERIFIED; every cited source had drifted, and a third `estimate_cost_usd` at `run_commands.py:1574` is the Concierge `chat_usage` writer, correctly left alone) — plus the key in `_VOLATILE_STRIP_KEYS`, `spend_full` + `metered_runs` on `AnalyticsSummary`, and a SIGNED line on the Analytics page. **Concern (a) is answered, not ignored:** the delta is computed from the SAME token base on both sides, so any token missing from the base is missing from BOTH — the reported delta is a true statement about the tokens that were measured, incomplete in magnitude but never fabricated. The percentage is the primary figure for exactly the reason this row gives. **The row's 'saved $Y' framing was the defect, and is refuted by its own data:** re-derived read-only through the real `estimate_cost_usd`, **6 of 11 persisted runs are strictly NEGATIVE** (-4.98%..-8.86%), 1 exactly zero, 4 positive (aggregate +80.34%, carried by two long prototype runs) — so an unconditional 'saved' would be wrong on the majority of runs in the only dataset that exists. The renderer branches on sign; zero renders nothing. **Legacy rows contribute a ZERO delta, never a fabricated $0 baseline** (`r_cost_full = _num(...) or r_cost`) — verified: 0 of 11 rows carried the key, and the naive sum was OBSERVED producing `spend_full = $0.00` against `spend = $15.00` (the -100% catastrophe) before the guard went in. Golden-neutral PROVEN BOTH WAYS: emitted+stripped -> 10 passed with all 15 golden files byte-identical; emitted+NOT stripped -> 5 event snapshots red. No migration, no per-run badge (that would reverse KAN-83 and revive 3 quarantined e2e cases). The global-cache-flag half of this row is split out as **ISS-120**.
| ISS-035 | minor (review: HIGH) | product | **RESOLVED** (Phase 32 / 32-05; reconciled 42-11) | Run chat lane shows no "Cancelled by you" acknowledgement after Stop — `pipeline_cancelled` sets no cancelled flag and pushes no chat message, so `runLaneState` falls through to `idle` (violates LIVE-STATE-CONTRACT §1). New-lane manifestation of the ISS-007 class; the revived RunChatLane never got the equivalent cancel ack. **CLOSED in Phase 32 plan 32-05** (code landed but the register row stayed stale — the tracking gap noted in `IMPLEMENTATION-REGISTER.md` §Phase-32): the `pipeline_cancelled` reducer now stamps a derivable `cancelled: true` terminal marker (symmetric with `pipeline_failed`'s `failed: true`), and `RunChatLane.tsx` (32-06) renders the "Cancelled by you" + relaunch card off that marker (`data-testid="chat-terminal-cancelled"`); the FIX-039 unconditional accumulator reset stayed byte-unchanged. VERIFIED in `32-VERIFICATION.md` Truth #8 (VERIFIED against the codebase, not the SUMMARY). | 31-REVIEW.md HI-02 (2026-07-08); relates ISS-007; closed 32-05 (`8b478161` GREEN marker) + 32-06 render, `32-VERIFICATION.md` Truth #8; reconciled 42-11 |
| ISS-036 | minor | product | **RESOLVED** (Phase 32 / 32-05; reconciled 42-11) | Latent (SSE-ON only, inert today: flag OFF + `RunConnectionProvider` unmounted): `useRunChat.runId` bound to clarify-only `activePipelineRunId` (null while "building") → with `NEXT_PUBLIC_SSE_TRANSPORT` ON, `sendCommand` would `POST /api/runs` and spawn a NEW run per in-run message instead of targeting the live run. Fix: thread `pipelineState.pipelineRunId`. **CLOSED in Phase 32 plan 32-05** (register row stayed stale — same tracking gap as ISS-035): `useRunChat({ runId: pipelineState.pipelineRunId ?? activePipelineRunId, … })` at the `dashboard/page.tsx` call site so the REST command path targets the live building run; pure additive FE prop change — LOCK-B held (SSE stays dormant, `RunConnectionProvider` NOT mounted, no env/transport file touched), so the latent bug is closed in code with zero runtime effect until SSE is activated. VERIFIED in `32-VERIFICATION.md` (LOCK-B, grep-confirmed). | 31-REVIEW.md HI-01 (2026-07-08); closed 32-05 (`03997db3`), `32-VERIFICATION.md`; reconciled 42-11 |
| ISS-037 | low | product | **OPEN** (advisory cluster — TodoCard clause CLOSED 42-04; reconciled 42-11) | Phase-31 review MED/LOW cluster: (M) unhandled `sendMessage` rejection strands the optimistic bubble; (M) sent attachments not rendered in transcript; (M) transcript not cleared on historical-run switch; (L) ~~`TodoCard` dead (no `todo` kind emitted)~~ **CLOSED — `TodoCard.tsx` DELETED in Phase 42 plan 42-04** (`23e05167`; no `todo` block kind is emitted, its only importer was its own block test, retired with it — INV-12); (L) `useTabDeepLink().consume()` never called; (L) unused `agentId` prop. The remaining M/L advisory items stay OPEN (non-blocking); only the TodoCard-dead-code clause is resolved. | 31-REVIEW.md (2026-07-08); TodoCard clause closed 42-04 (`23e05167`); reconciled 42-11 |
| ISS-038 | minor (review: MD-01) | product | **FIXED** (commit bab42693, 2026-07-08) | "Update the Specs" gate affordance BROADENED from analyze-gate-only (KAN-101 documented scope) to all spec-artifact gates (`{spec, task_list, summary}` → also specify/plan gates) as a side effect of the SC-001 de-literalization (32-04/32-08). A pure de-literalize refactor should be behavior-preserving; this changed WHEN the affordance appears, and it's unconfirmed the `update_specs` action even works from specify/plan gates. Options: narrow the generic `update_specs_eligible` flag back to analyze-only (preserve KAN-101 behavior, still SC-001-safe) OR confirm broadening is intended (+ verify/wire the action there). | 32-REVIEW.md MD-01 (2026-07-08); relates KAN-101 |
| ISS-039 | minor (review: MD-02) | product | **FIXED** (commit 621a406d, 2026-07-08) | `specRevisionCount` in `useWorkflow.ts` (L325-327) is now WRITE-ONLY dead state — its sole reader `PrototypePipelineView.tsx` was deleted in Phase 32. Harmless today (no reader), but would ~3× over-count if re-consumed. Fix MUST preserve the FIX-039 unconditional-reset block. | 32-REVIEW.md MD-02 (2026-07-08); relates FIX-039 |
| ISS-040 | trivial (review: LW-01) | product | **FIXED** (commit 9df1960a, 2026-07-08) | New `Button` primitive has no default `type` → a Button inside a form defaults to `type=submit` (form-submit footgun) as usage spreads. Add `type="button"` default. | 32-REVIEW.md LW-01 (2026-07-08) |
| ISS-041 | trivial (review: LW-02) | product | **FIXED** (commit 9df1960a, 2026-07-08) | New `Badge` primitive renders the raw `status` string when `label` is omitted (should map to a human label or safe default). | 32-REVIEW.md LW-02 (2026-07-08) |
| ISS-042 | trivial (review: LW-03) | product | **FIXED** (commit 650af67c, 2026-07-08) | Cancelled-state color inconsistent: `WaveTreePanel` renders red vs `Badge` amber for cancelled — reconcile against the locked status palette (evidence 11 §B). | 32-REVIEW.md LW-03 (2026-07-08) |
| ISS-043 | cosmetic | docs | **FIXED** (commit 7b2aa701, 2026-07-08) | `REQUIREMENTS.md` marks RUNUI-01/02/04/05 `Pending` though delivered by Phase 32; the SC→RUNUI mapping is ambiguous (verifier flagged). Reconcile the Traceability status to Phase 32 + its VERIFICATION. | 32-VERIFICATION.md note (2026-07-08) |
| ISS-044 | low | product | **FIXED** (feat/ui-2, 2026-07-10 — v2.0 polish sweep, 3 atomic commits: FLAG-1 relabel "Workflow History"→"Run History" `42a83031`; FLAG-2/3 amber cancelled icon + valid role=dialog ARIA (a11y test reconciled) `e4fcb011`; FLAG-4/5 motion modal roots + --scrim backdrops `c5ed2dab`. (f) tier-Badge decouple left as optional/design-sanctioned. tsc 0, retired-palette 0, NotificationPanel-a11y + LibraryPage-reskin suites green) | Phase-35 shell/reskin advisory items (verifier PASSED 21/21, UI-audit PASS 19/24, 0 blockers, WCAG-AA passes): (a) `AppHeader.tsx:247` profile item "Workflow History" → should be "Run History" per §B5 (terminology deviation); (b) `NotificationPanel.tsx:34` cancelled leading-icon grey vs its amber `Badge` (§B3 status-color inconsistency); (c) `NotificationPanel.tsx:88,103` `role="menu"`/`aria-haspopup="menu"` without `menuitem` children (invalid ARIA); (d) `LibraryPage.tsx:606-611` Skill/Hook modals inert inside `AnimatePresence` (exit-anim no-op); (e) `LibraryPage.tsx:88,235` 2 backdrops `bg-black/40` vs the `--scrim` token; (f) status `Badge` reused for tier color (optional decouple). ME-01 "completed→DONE" copy is design-sanctioned (§B3), NOT a defect. | 35-REVIEW.md + 35-UI-REVIEW.md (2026-07-09) |
| ISS-045 | low | product | **FIXED** (feat/ui-2, 2026-07-10 — L-01 dead `Calendar` import + `formatFullDate` removed `c367c265`; 5 stale "Your workflows" tests + orphaned `userEvent` import removed from `HomeLaunchGrid.test.tsx` `d0054959` (feature lives in `SavedWorkflowsPage`, its suite green) → HomeLaunchGrid now 4/4) | Phase-36 verifier PASSED 4/4, 0 genuine gaps, backend owner-gate/secret-non-echo verified. Two non-blocking follow-ups: (a) **L-01** dead `Calendar` import + `formatFullDate` in `SavedWorkflowsPage.tsx` (cosmetic dead code, unfixed Low); (b) `HomeLaunchGrid.test.tsx` 5 stale "Your workflows" tests — RED at base `92af09ec` (pre-existing, inherited by the `WorkflowCatalog`→`HomeLaunchGrid` `git mv`; the functionality moved to `SavedWorkflowsPage`, whose test passes) → retarget/remove. | 36-VERIFICATION.md + 36-REVIEW.md (2026-07-09) |
| ISS-046 | minor | product | **FIXED** (2026-07-10 — WR-01 rebuilt in 37-07 contract-byte-identical, WR-02 mounted, WR-03/04 + IN-01/02 fixed; IN-03 seam-robustness a minor residual) | Phase-37 review (verifier PASSED 3/3, 0 blockers, D-15/C goldens byte-identical, high-risk 37-01/03/05/06 clean): (**WR-01**) NEW-BUILD `WizardStepper` (unified Template→DS→Discovery + Web/Deck) built but MOUNTED NOWHERE — users still hit the old route-split flow; wire (retire old flow, INV-3) or explicitly defer; (**WR-02**) `WorkflowDialog` (300 lines) also mounted nowhere; (**WR-03**) stale `model_overrides` on Save-to-catalogue (`AgentsPopup.tsx:1793`) — save emits the static `initialModelOverrides` seed while `onModelOverridesChange` is a DEAD prop → divergent/lost model on reload (REAL data bug, reachable); (**WR-04**) `ConfigureScreen` gates template/DS on raw `opendesign` but the backend seam excludes bare `prototype` → latent gate divergence; (IN-01) discovery answers dropped from launch command; (IN-02) step-counter "1 of 2" vs "3 of 3"; (IN-03) seam compiles every manifest catching only FileNotFoundError. Plus verifier WARNING: `launch_context.py:96` `FIXME(D-15/C v1)` needs a tracked DEF-id. | 37-REVIEW.md + 37-VERIFICATION.md (2026-07-09) |
| ISS-047 | minor | product | **FIXED** (feat/ui-2, 2026-07-10) | 37-07 unified-launch rebuild re-review (verifier PASSED 3/3, 0 blockers, contract byte-identical, 0 dropped behaviors): (**WR-05**, VISIBLE bug) `LaunchWizard.tsx:125` `cfg = MODE_CONFIG[initialMode]` freezes header/brief/placeholder/save-modal chrome to the INITIAL family — after the Web/Deck toggle the page reads "prototype" while building/saving a ppt deck (Phase-36 stale-chrome-on-toggle class) → `MODE_CONFIG[mode]` + a chrome-after-toggle test; (**WR-06**) ppt custom-template branch (`customTemplateBody` + `designSystemId:null`) UNCOVERED by golden + component test (byte-correct by construction, unproven) → add golden + `pick-deck-custom` test; (**WR-07**) chaining launch (`finalBrief=contextBlock`+`sourceRunId`) untested; (**IN-04**) dead `onModelOverridesChange` prop + vestigial `modelOverrides` draft field → divergent-model-on-restore risk; (**IN-05**) orphaned `prototype/discovery` route + dropped template-input discovery (INV-12 smell); (IN-06) WR-04 FE gate keys on `workflowId` not the resolved base pipeline_type (latent); (IN-07) redundant double auth gate. **FIXED** FE-only on feat/ui-2 in 7 atomic commits (all 7 items): WR-05 `b7f957e9` (cfg→`MODE_CONFIG[mode]` + chrome-after-toggle test), WR-06 `98190930` (ppt custom-template golden + `pick-deck-custom` component test, additive), WR-07 `0e6d86b9` (chaining Continue → `finalBrief=contextBlock`+`sourceRunId` test), IN-04 `b4633ab0` (removed the `modelOverridesRef` mirror from launch-draft + save payload + restore + dead prop → `selections[id].model` single source; pin test; AgentsPopup interface prop kept for IdeaInputPage), IN-05 `4004fc5c` (deleted the orphan `prototype/discovery` route + its dead sole-consumer `getPrototypeTemplate`/`PrototypeTemplateDetail` + the unreachable `template inputs only` golden → one discovery impl), IN-06 `060dcafd` (Configure gate keys on resolved base `detail.id` not `workflowId`; discriminating test), IN-07 `e4dd37c7` (dropped the wrapper's redundant auth gate; LaunchWizard owns it). Verified offline: touched suites 39/39 (parity+LaunchWizard+ConfigureScreen+IdeaInputPage.modelOverrides), tsc --noEmit 0 (identity), retired-palette grep 0 on all touched files, 5 backend goldens byte-identical (git-clean), lint-imports 4/0, SC-001 mode-keyed (no per-workflow-name branch), AgentModelPicker not mounted. Mocked Playwright live-deferred. | 37-REVIEW.md re-review (2026-07-10) |
| ISS-048 | minor | product | **FIXED** (feat/ui-2, 2026-07-10 — MD-1 `markGateResumed` reverts gate→running off the generic reviewGateData+isRunning markers + hook pin (both directions) `ce0ac16a`; MD-2 `avgTokens = totalTokens/totalCount` + "tokens per run" label + pin (fail-before proven) `352d6614`; MD-3 left AS-SPECIFIED (tolerant fallback, SC-2 holds); ReviewGatesSection 3 reds reconciled — stale `PIPELINE_CATEGORIES` prototype=4/all=54 → 5/55 (invariant-preserving) + uncheck-all-3-gates test `4d683ffa`, now 19/19. HomeLaunchGrid 5 reds = dup of ISS-045, resolved there. tsc 0, lint-imports 4/0, 5 backend goldens byte-identical, retired-palette 0) | Phase-38 review (verifier PASS 11/11, 0 crit/high): (**MD-1**) `useNotifications.ts:99`/`DashboardLayout.tsx:489` gate notification never reverts — `markGatePaused` sets `"gate"` and nothing returns it to `"running"` on gate-resolve → a resumed run shows "paused" until a terminal mark (real feed UX bug); (**MD-2**) `AnalyticsPage.tsx:183` KPI `avgTokens = totalTokens/completedCount` divides an all-run numerator by a completed-run denominator + inconsistent labels (display-correctness); (MD-3) estimate key divergence (`analytics.py` keys `type_avg_duration_sec` on `run.type` vs card `[row.id]`) — WORKING AS SPECIFIED (mandated tolerant fallback; SC-2 holds) → optional enhancement. Pre-existing test reds (NOT 38-caused, byte-identical at base): `ReviewGatesSection.test.tsx` 3 (LIBRARY_AGENTS data-drift) + `HomeLaunchGrid.test.tsx` 5 (Phase-21 "Your workflows" moved to SavedWorkflowsPage — dup of ISS-045). | 38-REVIEW.md + 38-VERIFICATION.md (2026-07-10) |
| ISS-049 | trivial | test-infra | **FIXED** (be620716, test-only — both were STALE tests, ZERO product bugs; #2 SURF-03 confirmed WORKING [strip is on the AgentsPopup "Workflow" tab; the test only opened "Agents"]; the ~8 "flaky" were collateral parallel-timing damage from the 2 stale tests — now gone; full vitest suite 585/585 green ×5 runs, tsc 0. Two independent analyses concurred.) | Surfaced by the 37-07 rebuild agent's careful re-analysis (corrects the earlier "~10 pre-existing" hand-wave): the FE suite has **2 DETERMINISTIC failures** (both pre-existing, unrelated to 37-07) + ~8 FLAKY. (1, TEST bug) `AgentProgressPanel.test.tsx` "_revision same as base filter" — PRODUCT is correct (a `ppt_revision` correctly hides the Presentation chain button) but the loose `queryByText("Presentation")` catches the run's display-name header label; fix to `queryByRole("button", {name:/^presentation/i})` per its sibling. (2, PROBABLE REAL REGRESSION) `IdeaInputPage.declaredCapabilities` (SURF-03) — the "Declared by this workflow" strip does NOT render after opening Advanced; the fetch fires but the strip is gone (`IdeaInputPage` last changed by 30-04 image-resize, AFTER the SURF-03 test) — investigate real-regression vs mock-mismatch, fix the correct side. (3, test-infra) ~8 tests (`ReviewGatesSection`/`HomeLaunchGrid`) pass in ISOLATION but leak global state (`IntersectionObserver`/`sessionStorage`/singletons) → fail under parallel load; harness weakness, not product. | 37-07 rebuild agent re-analysis (2026-07-10) |

## Deep Root-Cause Investigation (2026-06-13 — one Opus 4.8 agent per issue, max effort)

> Each agent read IMPLEMENTATION-REGISTER.md in full, traced the real code from the cited evidence, and was required to find the TRUE root cause + a PROPER fix that respects the invariants (INV-3 parity, SC-001, Ports & Adapters) — explicitly naming and rejecting the tempting hack. All claims are code-proven (file:line) unless marked *inferred*. Agents were read-only; fixes are not yet applied.

| ID | Conf. | Root cause (proven file:line + mechanism) | Proper fix (no-hack) | Rejected hack | Eff/Risk |
|----|-------|-------------------------------------------|----------------------|---------------|----------|
| ISS-001 | ✓ FIXED | validator emitted QA narration; `ppt` resolver unwraps `ctx.last_streamed` | P15 output contract: validator re-emits full deck in `<artifact>` (shipped) | engine-side resolver fallback (INV-3) | — |
| ISS-002 | ◑ partly | **Two stacked defects.** (1) CPython-3.11 `wait_for` result-vs-cancel swallow at `websocket.py:1669` (proven 37/210). (2) ack never delivered: WS uses destructive `task.cancel()` (`websocket.py:605`) not the engine's cooperative `cancel_event` (`engine.py:523`); drainer's `except CancelledError` (`:1718-1722`) re-raises **before** the bg task emits `pipeline_cancelled` | use `cancel_event` for `cancel_pipeline`; close engine pre-agent cancel gap (`engine.py:1509`→`pipeline_cancelled`); adopt `asyncio.timeout()` at the 3 sites as defense; update the test | **"just swap `wait_for`→`asyncio.timeout()`"** (the register's own suggestion) — proven insufficient: cancel propagates but ack delivered 0/10 | M / med |
| ISS-003 | ✓ | `test_phase3_token_delta_live.py:173` sets **dead** `engine_mod.ALWAYS_CLARIFY=False` (flag deleted 07-05; grep=0 hits); `_drive_live` never sets `compiled.clarify.mode="off"`, so `prototype`'s `clarify.mode=auto` blocks at `clarify_engine.py:155 await event.wait()` | mirror `_scripted_model.py:506-513` / `live_harness.py:556-563`: wrap `compile_for_run`, set `clarify.mode="off"`, restore in `finally` | re-add the module flag (dual-impl) / add a clarify timeout (perturbs HITL) / flip the manifest default (breaks INV-3) | S / low |
| ISS-004 | ✓ (live re-confirm pending) | **Two layers.** (A) prompt: tool-less aggregator's "Read … from context" framing elicits simulated `<invoke read_file>` on live Haiku — P15 defused the trigger (shipped+pinned). (B) structural: the 13-02 sanitizer by design does **not** touch the UI chunk stream — `engine.py:2330-2332` yields raw chunks **before** `sanitize_output()` at `:2480-2492`, so it's **UI-stream-cosmetic only** (downstream context + deliverable are clean) | Part 1: the named live re-confirm (`PHASE-15-RECHECK.md:40-43`). Part 2 (optional belt): apply the sanitizer to the streamed `agent_chunk` path with chunk-straddle buffering (engine-touching → separate phase) | tick closed on the offline pin (scripted model ignores prompts) / strip XML in the FE | S / low (re-confirm); M / med (belt) |
| ISS-005 | ✓ | prose-only `/api/v1` standard: lives only in 3 AGENT.md bodies, **zero code enforcement** (`grep /api/v1 *.py` = 0); infra-generator's rule is incidental to its 12-file templating task → lost on live Haiku. P15 repositioned the contract (shipped+pinned) | Part A: named live re-confirm (count `/api/v1`). Part B (durable): a declared `api_prefix` **deliverable-validator** (clone of `spec_plan_coverage.py`), wired as an **event-free `post_step`** | declare `gates:[validation]` — emits `validation_warning` events → **breaks the app_builder golden (INV-3)** | S / low (re-confirm); M / low (validator) |
| ISS-006 | ✓ | prompt-contract gap: producer `app-code-generator` (order 8) exports `getDatabase`; consumer `app-test-implementation` (order 12) imports `getDb` in `tests/setup.ts`; no shared source of truth + contract-fidelity rule excludes the Jest global-setup file (symbols are model-invented: grep=0 in prompts/golden) | thread a shared canonical accessor literal (`getDatabase`) through both prompts (13-03 shared-literal pattern) + offline acceptance-grep pin | rename one side only (other side re-drifts) / build a `tsc` gate now (TS + exec OFF until N3 + golden-pipeline) | S / low |
| ISS-007 | ✓ (repro `/tmp/iss007_repro.py`) | **SAME root as ISS-002 Defect-2.** `websocket.py:1718-1722` drainer's `except CancelledError` runs `bg.cancel(); raise` and dies **before** the bg task yields `pipeline_cancelled` (`engine.py:1670-1678`); the frame lands on a queue no one drains. DB is correctly `cancelled`; live wire gets no terminal → card stuck RUNNING. Mirrored in revision drainer `:1997-2050` | residual-drain on the cancel path: `await` (shield+timeout) the bg task, drain `event_queue`→WS, then `raise` (reuse the WR-01 residual-drain at `websocket.py:2069-2084`). **FE needs zero change** (`useWorkflow.ts:403` already resets cards on `pipeline_cancelled`) | FE clears cards on a timeout / sync ack inside the cancel handler (races) / engine special-casing | S / low |
| ISS-008 | ✓ | `websocket.py:766-767` replay loop hardcodes `"section": None`; `section` is a WS-frame-wrapper field, **never persisted** (`RunEvent` has no column). WR-03 (`d71c55e6`) fixed only live-attach (`:807/:822`) via `_reattach_section` computed inside `if _has_live_task`; the replay branch was never given it | derive `_replay_section` in the replay branch from the run row `type` via the same WR-06 inverse: `f"{run_type.removesuffix('_revision')}_output"` | synthesize a fake/constant section or special-case the workflow (SC-001) / add a `section` DB column (over-eng + migration) | S / low |
| ISS-009 | ✓ | `websocket.py:792-796` live-attach ack omits the `live` key; only `:783` sets `live:False`. FE tolerates via `useWorkflow.ts:455 if (live!==false)`, so it's latent/cosmetic, but the FE comment already (wrongly) assumes the BE sends `live:true` | add `"live": True` to the live-attach ack dict (`:794`) — symmetric, self-describing | leave the FE inferring from absence / derive liveness from the `message` text | S / low |
| ISS-010 | ✓ (verification gap, **not** a code gap) | "declaration-driven hooks → legacy pipelines emit no spans" is **true & proven** (all 18 manifests declare no `hooks:`). Given a collector + a hook-declaring step, spans **do** export — proven by running the real `OtelTracingHook.handle` with an injected `InMemorySpanExporter` (1 span, correct attrs) | add a deterministic offline `InMemorySpanExporter` test (monkeypatch `otel._build_span_processor` + reset the module-private `_TRACER`) → close DEFERRED | stand up a live OTLP collector / a global `set_tracer_provider` test (misses the hook's own `_TRACER` → false green) | S / low |
| ISS-011 | ✓ (env/credential, **not** product) | all 3 HITL tests + the self-check trace to ONE cause: default-profile SSO token expired ~2.5h into the sweep. Expired-SSO is non-transient (`model_policy.py:185-211`) → swallowed to `error` → 0 tokens → `require_tokens` fails. Self-check fails because `_LIVE_SKIP` (collection-time) ≠ runtime `live_skip_reason()` | Part 1: re-run after `aws sso login` (close MONITORING→FIXED). Part 2 (recurring-mode): harness re-checks `live_skip_reason()` per live test → **skip** (not fail) on mid-sweep expiry; relax the self-check's strict-equality | "just re-run" with no harness change (leaves the recurring fault) / add "expired" to transient substrings (masks real `AccessDenied`/`ValidationException`) | S / low |
| ISS-012 | ✓ (docs, direct-fill) | REQUIREMENTS.md Traceability table not regenerated as 113 REQ-IDs were appended to the body | regenerate the table from the REQ-ID definitions; ideally a generator + a CI drift-check | hand-patch a few rows (re-drifts on the next REQ) | S / low |
| ISS-013 | ✓ FIXED | driver v1 generic selectors didn't match real Flowin controls | driver v2 with the real FE selector reference (shipped) | — | — |
| ISS-014 | ✓ (orphaned dead code) | `WorkflowComposer.tsx` mounted by **no route** (pre-existing legacy, base commit `4889e3a8`); `CapabilityPalette`/`AgentModelPicker` consumed only by it; `/api/capabilities` live but **zero FE consumer**. Phase-8 API-06 hung new panels into the unrouted legacy composer; Phase-12 logged it a "separate composer-routing concern" → never resolved = INV-3/INV-12 dual-impl smell | **DELETE** `WorkflowComposer`+`CapabilityPalette`; relocate `AgentModelPicker` into the live `AgentsPopup` + wire `model_overrides` into the run payload (delivers MODEL-03) **or** delete + v2-carry; **KEEP** `/api/capabilities` (API-02 registry-reflection contract) | route the legacy composer (resurrects a superseded dual impl w/ a hardcoded 15-agent stub) / delete `/api/capabilities` (regresses API-02) | S / low (delete); M / low-med (relocate picker) |
| ISS-015 | ✓ → **SHIPPED — Phase 20 (WF-DB-01)** | launch surface WAS hardcoded 6-tile `CreationHub.tsx:15-22`; Phase 20 added a data-driven catalog alongside it. Bare `prototype`/`ppt` are template-gated-unreachable (D-13-06-a); `sample_*` aren't in `SUPPORTED_PIPELINE_TYPES` (test fixtures). `GET /api/workflows` (manifest discovery) now HAS its FE consumer — the Phase 20 data-driven catalog (+ additive `user_launchable` flag). Custom need already met by the agent-composer (ISS-014); named-manifest picker = WF-DB-01 → **DONE (Phase 20)** | **SHIPPED Phase 20** (was: none/WONTFIX). *If ever in-scope:* a manifest-driven picker off `/api/workflows` + a `user_launchable` flag — **not** a hardcoded dropdown | hardcode a longer type dropdown — SC-001 violation; ships broken template-gated buttons + exposes test fixtures | S / low (doc-only) |
| ISS-016 | ✓ **(MAJOR)** | `engine.py:2326-2377` agent-consume loop has **no `error` branch**, so the runner's `{"type":"error"}` (`deep_agent_runner.py:507-527` swallowing a non-throttle `ValidationException`) is dropped → empty `agent_complete` (`:2619`). F3 terminal gating keys on `agent_error` counts (`:1704-1891`), which this never produces → clean `pipeline_complete`. (Encoded as "expected" by `test_model_fallback.py:257-271`.) | add `elif etype=="error":` → route to the existing `agent_error`→`_failed_agent_ids`→`pipeline_failed`/degraded machinery (generic, no FE change, SC-001-safe). Optional 2nd signal: empty-but-clean run → degraded marker (after deliverable readback) | re-raise ALL exceptions (breaks 06-05 D-05 LOCKED) / re-key F3 on empty completions / match `ValidationException` text (SC-001) | S / low |
| ISS-017 | ✓ | FE `PreviewPanel.tsx:205,291-294` `hasContent` gate + `page.tsx:359` content gate (empty `final_output`→no content set) + `:1006` reopen gate → neutral empty-state; the only non-success affordance keys on a server `degraded` flag the empty run never carries. True upstream = ISS-016 | **BE owns the signal** (ISS-016 reclassify + additive empty/degraded flag); **FE owns the affordance** (degraded/empty panel on live + reopen, keyed on the server signal) | client-side `if(terminal && !finalOutput) show failed` — re-creates the IN-03 FE-vs-DB disagreement | S / low (FE) — M / med with ISS-016 |
| ISS-018 | ✓ (env, diagnosed; direct-fill) | account-wide Bedrock **invoke** block on acct `731451715500`: every model/API/region returns `ValidationException: Operation not allowed` while control-plane (`ListFoundationModels`) + STS work → org SCP / permission-set change (not quota/expiry) | IT escalation (message drafted). Workaround: use the `default` profile (acct `473293451041`). No code fix | — | — (external) |
| ISS-019 | ✓ | `DashboardLayout.tsx:1158-1190`: `AgentProgressPanel` (`flex h-full`, designed to fill 100% + scroll internally) is the **first** of two block siblings in an `overflow-y-auto` column, so it eats the full height and `WaveTreePanel` starts at/after the fold; the column (not the panel) owns the only scroll. Test stubs the agent panel → never caught | flex-budget split (CSS-only, `DashboardLayout.tsx`): column `flex flex-col overflow-hidden`; agent wrapper `flex-1 min-h-0 overflow-hidden`; wave wrapper `flex-shrink-0 max-h-[40%] overflow-y-auto` | clamp the agent panel / shrink the wave panel to a fixed height — clips content, viewport-fragile (the 122px-vs-13px drift) / reorder wave above agent (pushes agent below fold) | S / low |
| ISS-020 | ✓ (harness, direct-fill) | `ws_rewrite` mutates only the backend `pipeline_type`, not the FE's displayed workflow type | none — harness artifact; the fixture workflows have no product launch surface anyway (see ISS-015) | — | — |
| ISS-021 | ✓ | FE deliverable surface is a **closed dispatch** keyed on hardcoded pipeline-type strings (`page.tsx:360`, `PreviewPanel.tsx:191-204`, `WorkflowHistory.tsx:264-269`) with no generic fallback; BE `pipeline_complete` (`engine.py:1854-1866`) carries `final_output` but **no mimetype/filename hint** (though `DeliverableSpec` is in scope at `:923`). 2nd facet: even the `custom` branch → `MarkdownPreview` (no `rehype-raw`) → HTML renders as escaped text | BE additive: `DeliverableSpec.mimetype` + emit `deliverable_mimetype`/`_filename` on `pipeline_complete` (+ `_VOLATILE_STRIP_KEYS` for golden parity). FE generic fallback renderer keyed on mimetype (html→sandboxed iframe / md→markdown / zip→bundle) + a generic Files row | hardcode `ui_custom_proto`→iframe (SC-001 violation) / route unknown→`custom` (markdown-not-HTML) / BE content-sniff `final_output` | M / low-med |
| ISS-064 | major | agents | **CLOSED 2026-08-12 — FULLY FIXED by FIX-218.** The "UI half is NOT reachable" clause below was based on ISS-062, which is now closed as a misdiagnosis (see that row): the re-opened analyze gate does advertise `eligible=True`, so the button is NOT hidden there and the engine fix is reachable from the UI. The engine half was re-proven independently rather than taken on FIX-218's word — the pre-fix tree at `30159b36` was exported to a scratch worktree and observed RED (`test_reopened_gate_second_update_specs_runs_a_second_cycle` and `test_reopened_gate_cycle_keeps_a_flat_stack` both FAILED, "the second 'Update the Specs' ran no cycle"), and GREEN at HEAD (5 passed). All three `_gate_update_specs` branches now drive the single shared `_consume_update_specs` consumer, which holds the file's only sub-pipeline call site (INV-12 gate: 1 definition / 3 callers / 0 duplicates). Live corroboration: run `d5dbc9f2` produced spec v3 at 40,052 chars. The one genuinely missing guard — asserting the eligibility vector against the REAL gate rather than a stub — was added as part of FIX-219 (ISS-053), where the three-gate verdict measured honored / REFUSED / honored. Original text follows — (filed 2026-08-11 as ISS-050 and RENUMBERED to 064 — ISS-050 was already taken by a different, unrelated defect: "CR-02 guard blocks prototype_revision and user_stories_revision", closed by FIX-185 / commit `fd2ad8f1`. That id lived only in a commit message, never in either register, so an issues-register-only lookup missed it.) — FIX-218 / quick-260811-si4 fixed the ENGINE half (a real second cycle now runs; live-proven, spec v3 on run d5dbc9f2). The UI half is NOT reachable — see ISS-062: the eligibility fence over-fires so the button is hidden at exactly the re-opened gate. Original resolution note follows — (2026-08-11) | A second "Update the Specs" at the RE-OPENED analyze gate is a SILENT NO-OP. `engine.py:3545` re-seeds `ectx.spec_revision_pending_output` and `break`s without calling `_run_spec_revision_sub_pipeline`; the other two `_gate_update_specs` branches (`:3428`, `:4444`) do call it (`:3444`, `:4480`). The click is accepted, the gate closes, no revision runs, and the build proceeds from the spec the user was trying to change — invisible on screen, one wasted build. | Found during quick-260811-mxg plan-check/verify; brief `.planning/BUGFIX-NESTED-REVISION.md`. NOT observed live — on run 5ecb990f I reached that exact gate and approved instead of re-revising. si4 planner independently reproduced the flat-sibling shape offline. **RESOLVED (FIX-218 / quick-260811-si4):** the re-opened gate now drives the shared `_consume_update_specs` helper as a FLAT SIBLING cycle — the sub-pipeline's `finally` precedes its terminal yield and the driving `async for` exhausts the generator before breaking, so the previous pass is fully unwound and the second cycle sits at the same stack depth (the REDO-GATE F2 shape), threading `:rev2`. Guarded by `test_reopened_gate_second_update_specs_runs_a_second_cycle` + `test_reopened_gate_cycle_keeps_a_flat_stack`, both observed RED first. |
| ISS-051 | minor | agents | **Closed** — FIX-218 / quick-260811-si4 (2026-08-11) | A nested revision reaches depth 2 with BOTH levels computing `revision_index=1`, so two passes collide on the same `:rev1` checkpoint thread (measured: 7 dispatches, only 4 unique thread ids). The inner `finally` (`engine.py:5325`) also resets `ectx.revision_attempt` to 0 AND wipes `ectx.spec_revision_context` while the outer pass is still on the stack. Reachable TODAY, independent of ISS-064: the analyze re-run inside the sub-pipeline opens its own gate with `update_specs_eligible=True`. | Observed by the 260811-mxg verifier via instrumentation; reproduced offline by the si4 planner. Duplicate ids + counter reset confirmed; a resulting bad spec NOT observed. **RESOLVED (FIX-218 / quick-260811-si4):** the sub-pipeline is now properly re-entrant. `ExecutionContext.revision_high_water` is a monotone per-run mark that is never cleared or restored, and the effective index derives from it, so no two passes at any depth can mint the same `:rev{N}` (7 of 7 unique, was 4 of 7). The `finally` RESTORES the three scratch fields instead of clearing them, so an inner pass cannot strip the outer's index or analysis report mid-flight. A companion affordance fence (`_update_specs_eligible`, now `and not ectx.revision_attempt`) stops the UI OFFERING the nesting route at all. Guarded by `test_nested_revision_keeps_distinct_threads_and_restores_the_outer_pass` + `test_update_specs_not_offered_while_a_revision_is_in_flight`. |
| ISS-052 | minor | agents,product | **PARTIALLY FIXED — FIX-220 / quick-260812-1nz (2026-08-12). The ambiguity clause is CLOSED; the extra-approval clause is NOT — it is now ISS-072.** This row had two clauses and they have different answers. **(1) "the two are visually identical, so the user cannot tell which one they are on" — FIXED.** `review_gate_ready` now carries `revision_cycle` (which spec-revision cycle this FIRING belongs to, 0 = none) + `revision_in_flight` (is the pass still on the stack), `(0,False)/(1,True)/(1,False)` across `[0]/[1]/[2]` — the PAIR is unique per firing, which cycle alone is not. Both come from existing run scratch (`revision_high_water` / `revision_attempt`) through ONE resolver `_revision_stamp` with three callers: no new state, no migration, no workflow/agent literal. The FE names the cycle on the gate card and no longer claims "Accept & continue to build" at a firing where approving merely finishes the revision. It also closed an UNTESTED second harm the investigation found: `InlineGateActions` reset its one-action latch on `[output, gateKey]`, and NEITHER changes between `[1]` and `[2]`, so the re-opened gate rendered with every action disabled; the stamp is now in that dependency array, with a test seen failing first. **(2) "costs FOUR approvals" — NOT fixed, deliberately.** Suppressing `[2]` (what this row's text proposes) is disqualified on evidence: it is the ONLY firing with `update_specs_eligible=True`, so removing it deletes the sole entry to a second cycle and regresses ISS-064, closed the same day. The genuinely redundant firing is `[1]` — same bytes, strictly smaller action set, approval with no externally visible effect — and suppressing THAT is a different, larger change. Filed as **ISS-072**. **Also rejected outright, on measured evidence:** FE-side dedupe of "same gate_key + same output". It converts a redundant gate into a HANG (the engine is still blocked on `event.wait()`; swallowing the frame removes the only UI that can resolve it) and it would eat legitimate restart-resume re-opens, whose signature is identical — live-proven at `5ecb990f` seq 8402/8406 and `d5dbc9f2` seq 24788/24792, each preceded by `workflow_validated` + `pipeline_start`. **Residual (known, unfixed):** the chat-lane milestone card still reads "Paused — needs approval" for both firings (`chat_narrator.py:171-173`) — the data to disambiguate it now rides the event (SSE re-emits `payload_json` verbatim), so that is a pure render follow-up. NOTE `chat_narrator._revision_index` reads `spec_revision_attempt`/`revision_index`, NOT `revision_cycle` — verified, no collision, and the names were kept distinct deliberately so an in-revision gate card does not silently become a "Revising spec" card. Original text follows — | One "Update the Specs" click costs FOUR approvals: the re-run specify gate, the re-run plan gate, then the analyze gate RE-OPENS after the sub-pipeline returns. The doubled analyze gate is redundant and the two are visually identical, so the user cannot tell which one they are on. This is the real UX fix behind ISS-064. | Observed live driving run 5ecb990f end to end on Bedrock (2026-08-11). **Deliberately left OPEN by quick-260811-si4** (which closed ISS-064/051): collapsing the doubled analyze gate means SUPPRESSING a gate — a behaviour change no source artifact analysed, and outside the `.planning/BUGFIX-NESTED-REVISION.md` brief. It must NOT be done via `ectx.revision_attempt` alone, because the specify and plan gates also fire inside the pass and those ARE wanted. si4 re-confirmed the shape by probe: 3 gate firings per cycle, `[0]` outer / `[1]` in-pass analyze / `[2]` re-opened. |
| ISS-053 | major | agents,auth | **FIXED** — FIX-219 / quick-260812-12t (2026-08-12) | `update_specs_eligible` is an FE affordance only — `_run_review_gate` (`engine.py:5470`) acts on the gate action WITHOUT ever consulting it. A replayed or hand-crafted POST can therefore trigger a revision from a gate where the UI does not offer one, bypassing whatever eligibility rule is in force. | Found by the si4 planner while evaluating whether suppressing the affordance would be a sufficient fix for ISS-064. **si4's disposition was reversed on evidence.** T-si4-01 accepted the gap on the premise that a crafted or replayed POST "causes a REDUNDANT revision, never a corrupted one"; that premise is false in three independent ways. (1) **Corruption, not redundancy** — the sub-pipeline picks targets POSITIONALLY, so at the two DEFAULT-gated agents (`prototype-specify` idx 0, `prototype-plan` idx 1, both `gate: Human_Gate`) the `index < 2` early-return revises NOTHING yet still sets `spec_revision_pending_output=""`, blanking the agent's `results` output and re-opening the gate empty — every stock prototype run exposed with zero configuration. (2) **Cost** — at an inline gate on `prototype-build`/`prototype-validate` (both selectable in the shipped "Review gates" wizard control) one POST re-runs three agents INCLUDING the build; measured on live run `5ecb990f` that is ≈$3.68 vs ≈$0.03 for the revision cycle si4 reasoned about (~127×), with no rate limiting anywhere in the API layer. (3) **It already happened** — live run `d5dbc9f2` seq 20360 advertised `eligible=0` and a durable `gate_events` row records `prototype-analyze / human / update_specs` **6.05s later**. Also: `gate_key = f"{run_id}:{agent_id}"` names a gate SLOT, not a FIRING, so no crafting is needed — replaying a legitimate earlier click suffices. **Fixed as two layers over ONE rule** (the rule stays solely in `_update_specs_eligible`): the engine refuses an ineligible action and KEEPS WAITING (never approve — fail-open, the Phase-23 F1a hole, against WR-07; never reject — that cancels the run), and all THREE `set_review_response` ingresses answer 409 `update_specs_not_offered` by reading the engine's own published verdict back off `review_gate_ready`. The declared-gate `_gate_update_specs` silent-PASS + wire leak (`gates/human.py`, `gates/approval.py`) closed in the same pass. si4's nesting safety is retained and still tested as defence in depth. |
| ISS-070 | major | api,validation | **FIXED** (FIX-241 / TEST-025, quick-260812-sgu, commit c500fafa, 2026-08-12) | **The gate-action discriminator is unvalidated, and an unknown action silently APPROVES.** `run_commands.py` `resolve_gate` dispatches on `action` with `else: # approve` as the fallback, so any unrecognised string — a typo, a stale client, a probe — resolves the HITL gate as approved. `GateCommand.action` is a bare `str` with no enum (live OpenAPI confirms `{"type": "string", "default": "approve"}`) while the frontend declares the closed union `"approve" \| "reject" \| "redo" \| "update_specs"` (`frontend/src/lib/api.ts:690`). The endpoint also contradicts its own sibling: the Concierge disposal takes the opposite, safer default and says so in-code — "M1: a MISSING action must NEVER silently approve a gate". Sibling gap on the same model: `GateCommand.analysis_report` is `str \| None` with **no length cap**, and it is injected into every re-dispatched prompt. | Found 2026-08-12 by the ISS-053 investigation + fix (quick-260812-12t). **Deliberately NOT fixed there** — it is a distinct defect from the eligibility gap and folding it in would have been scope creep; ISS-053's fence is orthogonal (it constrains one action, this constrains which strings are actions at all). Fix shape: promote `action` to a `Literal[...]` so FastAPI rejects garbage at the boundary, and make the fallback deny rather than approve. Note the fix is NOT purely additive — a client today relying on the lenient default would start getting 422s, so check the FE and any scripted callers first. **CLOSED 2026-08-12 by FIX-241 — and this row was corrected on three points before it was.** **(1) Scope was over-generalised.** Only ONE of the three `set_review_response` ingresses was defective: `resolve_gate`'s `else` at `run_commands.py:272`. `/messages` `CHANNEL_GATE` (`:1322`) is fenced by `chat_router.py:234` — verified by reading BOTH `Dispatch(channel=CHANNEL_GATE)` construction sites (`chat_router.py:244`/`:255`), each of which requires `turn.action in GATE_ACTIONS`; the Concierge confirm (`:1101`) is fenced by `concierge.py:350` normalisation plus H1 (params are server-written, never client body). Both were hardened to the same fail-closed shape, but as hardening, not as the fix. **(2) Retagged `api,auth` → `api,validation`.** This is NOT an authorization defect: `resolve_gate:222` → `run_engine.py:765` filters `WorkflowRun.user_id == user_id`, so a cross-owner caller gets a non-revealing 404 and nobody else can approve your gate. The affected principal is the run's own owner. Severity stays **major** — a HITL safety gate fails open, invisibly, on the remainder of a 5–21M-token budget. **(3) The row understated it: the audit record was LAUNDERED.** The `else` never passed `action=`, so `store.set_review_response`'s own default (`store.py:123`) persisted `action="approve"` while `resolve_gate:278` echoed the garbage string back to the caller — measured directly: `action='rejct' → HTTP200 approved=True stored_action='approve'`. That is very likely why this was never noticed in a live log. On the declared-gate path an unknown action also resolves a formal `GATE_PASS` (`human.py:104-141`). **Fix:** `action` → `Literal["approve","reject","redo","update_specs"]` **with the default retained** (making it required would break `test_attach_replay_matrix.py:551`'s bare POST and the published OpenAPI contract), plus a fail-closed dispatch `else` raising 400 `unknown_gate_action`/`recoverable:true`. It REFUSES and never degrades to a reject — FIX-232 makes rejection terminal, so a typo must not destroy the run. Both layers mutation-proved independently. The `analysis_report` length-cap half of this row is split out as **ISS-127**, not folded in. |
| ISS-071 | minor | agents | **OPEN** | **The "Update the Specs" affordance is advertised at EVERY gate on every non-prototype workflow.** `_update_specs_eligible` keys on `artifact_kind in {"summary"}`, and `_artifact_kind_for` falls back to `"summary"` for any agent absent from `_AGENT_KIND_MAP` — which maps only the four prototype agents. Measured over every pipeline roster: `prototype`/`od_prototype` resolve correctly (analyze alone, True; the other four False), but `od_ppt` (3/3), `user_stories` (6/6), `app_builder` (15/15) and every composed workflow advertise it at **all** gates. The in-code comment ("the ANALYZE gate only") is accurate for prototype and describes none of the others. Post-ISS-053 this is no longer a security gap — the engine now enforces exactly what was advertised — but it means those workflows offer a spec-revision affordance whose sub-pipeline re-runs three positionally-chosen agents that are not a specify/plan/analyze triple. | Found 2026-08-12 by the ISS-053 investigation (PROBE A over every roster) and re-confirmed while fixing it (quick-260812-12t). Explicitly REJECTED as the fix for ISS-053 (report Option B): narrowing the advertisement changes nothing about enforcement, so all three harms would have survived. Filed as the separate correctness item it is. Fix shape: extend `_AGENT_KIND_MAP`, or derive the kind structurally from the manifest, so a workflow with no specify/plan/analyze triple advertises nothing — WITHOUT introducing a workflow-name literal (SC-001/INV-1). |
| ISS-072 | minor | agents,product | **OPEN — the deferred half of ISS-052** | **Collapse the doubled analyze gate by suppressing the IN-PASS firing `[1]`, not the re-opened one.** One "Update the Specs" click still costs four approvals. FIX-220 made the firings identifiable but suppressed nothing. The evidence says the redundant firing is `[1]`, the analyze re-run INSIDE the sub-pipeline: it shows content the user is about to be shown again (live `5ecb990f` seq 24652/24655 — same 11,974 bytes, sha1 `0136795fc392`), it offers a strictly SMALLER action set than `[2]` (no "Update the Specs", by FIX-218's design), and approving it has no externally visible effect (approve seq 24654 -> gate 24655; only the SECOND approval reached `agent_start prototype-build`). Suppressing `[2]` instead — what ISS-052's own text proposed — is disqualified: it is the only `update_specs_eligible=True` firing, so it deletes the sole entry to a second cycle and regresses ISS-064. **Fix shape (from the ISS-052 investigation, use it):** in `_run_spec_revision_sub_pipeline`, set a generic `ectx` suppression marker around the THIRD loop iteration using OBJECT IDENTITY (`sub_spec is analyze_spec`) — the same name-free idiom already used for `spec_revision_prior_artifact` at `engine.py:5281-5283` — and have `_should_gate` honour it. Do NOT key it on `ectx.revision_attempt` alone: that also kills the specify and plan in-pass gates, which ISS-052 says are WANTED and which the live data shows the user genuinely uses (seq 13640/23221 both approved). **Why it needs its own brief, not a rider:** it rewrites all six assertions in `test_spec_revision_cycles.py` (`:84-88`, `:219-224`, `:253-265`, `:293-297`); by INV-12 it forces DELETION of condition 2 of `_update_specs_eligible` (`engine.py:6649`, the one-day-old FIX-218 fence — with `[1]` gone that condition is unreachable, since the specify/plan in-pass gates are already ineligible by KIND); it must decide what happens on the empty-analysis backstop path (`engine.py:5214-5222` returns silently when `index < 2`, after which `[2]` is the ONLY human checkpoint before the build); and the underlying question — is the in-pass analyze review wanted at all? — is a PRODUCT call nobody has stated a position on. Saves 3 `run_events` + one approval per cycle. | Split out of ISS-052 by quick-260812-1nz (2026-08-12) after the deep investigation adjudicated both options. Full analysis, including the firing map with stack depths and the live A/B, is summarised in ISS-052's resolution note and in `.planning/quick/260812-1nz-.../260812-1nz-PLAN.md`. Do NOT re-derive it. |
| ISS-073 | trivial | test-infra | **OPEN** | **A stale frontend test asserts a layout the component deliberately abandoned.** `frontend/src/components/chat/InlineGateActions.test.tsx:54` ("renders exactly two primary buttons") asserts `queryByTestId("chat-gate-update-specs")` is null before "Request changes" is clicked, but the component elevates that button OUTSIDE the collapse (`InlineGateActions.tsx:255-269`, with an in-code comment saying so: "elevated outside Request changes so it is visible without a second click"). The test has been red ever since and nobody noticed, because the file is only ever run inside a 147-red full-suite baseline. Fix is one line — assert the button IS present, or drop that clause and let the dedicated update-specs test own it. | Measured 2026-08-12 during quick-260812-1nz, PROVEN pre-existing rather than labelled so: the same file measured **1 failed / 13 passed in a detached worktree at `28c0111c`** and 1 failed / 15 passed after (the +2 are that task's new cases). Not fixed there because it is unrelated to ISS-052 and the task was scoped to one issue. |
| ISS-074 | **major** (raised from minor) | test-infra | **FIXED — FIX-231 / TEST-015 (quick-260812-fbk), commits `3def9d1a` (RED guard) + `f36c3a07` (fix), 2026-08-12. Baseline 5 failed / 3 passed → **8 passed / 0 failed**. Offline-proven; NOT live-proven (deliberate — one build is 5-21M tokens).** **THIS ROW'S CENTRAL CLAIM WAS WRONG, and correcting it is the point.** It said the defect is *"UNVERIFIABLE offline, which is why it was filed rather than patched."* **Refuted at HEAD `fab9b646` in 2.19 s with no credentials and zero Bedrock spend** — five tests were already red: `test_live_harness.py::TestEngineGate` ×2 + `test_phase8_live.py::TestOfflineHITL` ×3, all with `TypeError: drive_engine_pipeline.<locals>._auto_resume_review_gate() got an unexpected keyword argument 'redoable'`. **The stated reason was also wrong:** neither file is SSO/Bedrock-gated — `test_live_harness.py` has no module skip at all, and `test_phase8_live.py` gates only `TestLiveHITL`/`TestLivePipelines`, leaving `TestOfflineHITL` ungated. **The real root cause is coverage selection, not credentials:** `.planning/TEST-REGISTER.md:85` excluded offline suites by **FILENAME** — *"Avoid offline: anything `*_live*`"* — and both files are offline-safe despite their names. That heuristic is why a red dating to `f18de868` (2026-06-30, when `redoable` landed) went **43 days** unseen. **Three further corrections.** (a) *"three inline call sites"* — there is a **fourth**: `kernel_services.py:1116`, the declared `gates:[human]`/`approval` delegate (it resolves `self._engine._run_review_gate` at call time and passes `cancel_event`), which is why the **declared**-gate path broke too, not just the legacy inline one. (b) *"11 sites"* undercounts: a mechanical AST census finds **29 assignment sites across 18 files** plus one **class-based** stub no assignment census can see (`test_gates.py:920` `_FakeReviewEngine`, 5 of 10 params, no `**kwargs` — widened here, one forwarded parameter from the identical failure). (c) **Severity raised to major.** The `TypeError` does not abort the run: the engine's per-agent error handler swallows it, the gated agent is marked failed, and the pipeline reaches `pipeline_complete` reporting `gated=False, completed=True`. A HITL harness that silently does not gate **and reports success** is a false-green oracle, which is worse than a crash. **THE TRAP, measured — `**kwargs` on the `def` ALONE is a shadow fix.** It greens all five previously-red tests (measured: 1 failed / 7 passed, the single red being only the new forwarding assertion) while discarding all six extras — including `cancel_event`, i.e. a gate that cannot honour Stop, silently undoing quick-260720-ec4's BUG-2 Cond B threading, and `redoable`/`update_specs_eligible`, the name-free SC-001 discriminators. **Both tokens are load-bearing: `**kwargs` on the def AND on the forwarded call.** **The guard the row asked for now exists** (`tests/agents/test_gate_stub_signature_drift.py`, 4 tests, <1 s): it AST-**derives** the engine's real parameter list (never hand-copied — restating it would reproduce the drift), resolves monkeypatched names, factory-returned inner functions **and** class-based doubles, and asserts `inspect.Signature.bind`; unresolvable stubs FAIL rather than skip. Proven against the **next** drift, not just this one: a stub pinning today's ten parameters + a temporary 11th engine parameter → RED naming the stub and the exact missing parameter; control (engine reverted) → green. **Scope limit, stated honestly:** the guard covers ARITY only. It cannot see a wrapper that swallows kwargs without forwarding (covered behaviourally by the Part-2 assertion), nor attribute drift on hand-rolled doubles (**ISS-094**). Goldens **10 passed** with **0 golden files modified**, `lint-imports` **4 kept / 0 broken** (run from `backend/`), **0 production modules changed** — all identical to `fab9b646`. ORIGINAL REPORT: | **The LIVE harness's review-gate wrapper cannot accept the gate's own arguments — every live gated drive raises TypeError.** `backend/tests/agents/live_harness.py:672` defines `_auto_resume_review_gate(pipeline_run_id, agent_id, agent_name, output)` and installs it over `engine._run_review_gate` unconditionally at `:683`, but the engine's three inline call sites pass `redoable=`, `update_specs_eligible=`, `artifact_kind=`, `cancel_event=` (and, since FIX-220, `revision_cycle=` / `revision_in_flight=`). The wrapper also forwards only the same four positionally to `_orig_review_gate`, so even a widened signature would drop them. Pre-existing since REDO-GATE added `redoable`; it has rotted unnoticed because the live harness is SSO/Bedrock-gated and never runs in the offline sweeps. Fix is two tokens — `**kwargs` on the def and on the forwarded call — but it is UNVERIFIABLE offline, which is why it was filed rather than patched. Exactly the class of rot that FIX-218 already had to repair in four `_fake_gate` stubs and FIX-220 in three `_gate` stubs; a signature-drift guard over the stubs would end the family. | Found 2026-08-12 by quick-260812-1nz while sweeping every test that monkeypatches `_run_review_gate` (11 sites). Every OTHER stub absorbs `**kwargs`/`**k`; this one and the three in `test_restart_resume.py` did not — the latter three were fixed in that task because they fail offline and were provably that task's regression (+7 reds), this one cannot be exercised offline at all. NOT re-verified live. |
| ISS-062 | ~~major~~ n/a | agents,frontend | **CLOSED 2026-08-12 — NOT A DEFECT. Misdiagnosis; the A/B is off by one gate firing.** The original text is preserved below for the record, but its central measurement is wrong. **Seq 20360 is the IN-PASS analyze gate, not the re-opened one** — `run_events` shows `agent_complete` for `prototype-analyze` at seq 20359 immediately before it, so an analyze agent actually ran; a re-opened gate emits no `agent_start` at all and its signature is a `review_gate_ready` directly preceded by another gate event with no agent run between (visible on this same run at seq 24788 → 24792, and pre-si4 on `5ecb990f` at 24654 → 24655). `eligible=False` at the in-pass gate is exactly FIX-218's design intent. Run `d5dbc9f2` has only TWO analyze gates and **never reached a re-opened one** — the gate was consumed by a second `update_specs` POST 6s later (the ISS-053 path, since the button was hidden). A HEAD probe returns `eligible=True` at the genuine re-opened gate. **Do NOT relax `_update_specs_eligible` to "fix" this** — that re-opens the nesting recursion ISS-051 closed (measured +3 stack frames per level). The real, valid substance of this row — that the in-pass and re-opened analyze gates are byte-identical to the user yet carry opposite affordances, which is what made the tester misread the run — is transferred to **ISS-052**. Verdict reached independently by four investigations and confirmed by direct DB query. Original text follows — | A/B measured across two live Bedrock runs: PRE-si4 run `5ecb990f` reported `eligible=True` at every analyze gate including both re-opens (seq 9572/24652/24655); POST-si4 run `d5dbc9f2` reports `True` at the original gate (seq 9283) but **False** at the re-opened one (seq 20360). So FIX-218 traded one broken behaviour for another: before, the button was VISIBLE and did nothing (ISS-064); now the action WORKS but the button is HIDDEN, so a user still cannot run a second cycle from the UI. The `update_specs_eligible` fence added by FIX-218 OVER-FIRES at the re-opened analyze gate. Measured live on run `d5dbc9f2` (2026-08-11): original analyze gate seq 9283 `eligible=True`, RE-OPENED analyze gate seq 20360 `eligible=False` — the plan's design intent was True/False/True across outer / in-pass / re-opened, so `ectx.revision_attempt` is still truthy when the re-opened gate is armed. Consequence: `InlineGateActions.tsx:130` computes `canUpdateSpecs = !!onUpdateSpecs && !!updateSpecsEligible` and `:258` renders the button only when true, so the user CANNOT see "Update the Specs" at the one gate where a second cycle is offered. ISS-064's engine half works (a POST does run a real cycle — spec v3, 40,052 chars) but is unreachable from the UI. | Live end-to-end test of quick-260811-si4 on Bedrock, run `d5dbc9f2-dbe8-480f-8b13-788794a6788e`; gate inventory from `run_events` type=`review_gate_ready`. NOTE: the si4 planner's offline probe measured True/False/True — the discrepancy between that probe and live behaviour is itself worth understanding before fixing. |
| ISS-063 | ~~minor~~ **major** | frontend,product | **FIXED** — FIX-221 (2026-08-12, quick-260812-2ci) | The violet "Spec Revision Cycle N" banner (FIX-163) is LIVE-SESSION-ONLY state and never back-fills from durable events, so reopening a run that was revised shows no sign a revision ever happened. `dashboard/page.tsx:357` holds `specRevisionCount` in `useState(0)` and `:907-912` increments it only when `revisionCycleArmedRef` is armed by the FE's own click AND a matching live event arrives. Same invisibility family as FIX-217: a revision occurred and the screen does not say so. **CORRECTIONS ON INVESTIGATION (2026-08-12) — this row was wrong twice.** (1) *"armed by the FE's own click"* is **wrong**: arming was a derived-state condition in a `useEffect` (`page.tsx:1646`), never a click. (2) Far more importantly, the defect was **not** "live works, reload does not" — it was **dead on every path** for `od_prototype`, live included. FIX-164's arming predicate required EVERY agent to be `"done"` while the run was still `isRunning`, but the update_specs gate sits on `prototype-analyze`, agent 2 of 5; the only sites that mark the rest done also set `isRunning:false`. The arm was structurally unsatisfiable, so the counter never left 0 in ANY session, on the flagship workflow, since `219702a4` (2026-08-03). Severity accordingly raised minor → **major**. Two further sufficient mechanisms sat behind it (a post-commit ref frozen during the synchronous replay loop; the trailing resume `pipeline_start` zeroing the counter last on every replay) — all three are one root cause: FIX-163/164 modelled "a revision is happening" as assembled live-session state instead of a property derivable from accumulated events. **Closed by deriving it in the reducer** (`agentStartCounts` → `deriveSpecRevisionCount`), with the arm/consume machinery DELETED, not shadowed. | Filed from the live Playwright pass on run `d5dbc9f2`. Root-caused 2026-08-12 by a dedicated read-only investigation that read the durable `run_events` log and simulated the FE logic against it (predicted 0, correct value 2). Fixed the same day, shipped as ONE change with ISS-065/FIX-222 — see that row for why. 15 regression tests, all seen RED first; there had been **zero** coverage before, which is exactly how FIX-164 silently killed the feature FIX-163 had just shipped. |
| ISS-065 | major | frontend,product,artifacts | **FIXED** — FIX-222 (2026-08-12, quick-260812-2ci) | No UI surfaces PER-ARTIFACT versions inside a run. `artifact_refs` durably stores every version (run `d5dbc9f2` holds spec v1/v2/v3, task_list v1/v2, summary v1/v2) and `GET /api/runs/{id}/artifacts` already returns `version` per node, but the only FE caller is `WorkflowHistory.tsx:303`, hard-coded to `kind="clarifications"`. The "Version v1 ▾" chip users DO see is a different axis — the whole-RUN family via `getRunFamily()` (`parent_run_id`), so three in-run revision cycles change it not at all (verified live: `/family` returned 0 members, no `v2`/`v3` anywhere on the run screen). **This invisibility is why FIX-217 shipped undetected** — a spec was silently rewritten and nothing on screen said so. Data, API and the Concierge's `list_refs`/`get_ref` tools all already exist; this is wiring on an axis that is already there. Do NOT reuse the existing "Version" chip — two meanings on one control is what made this confusing. **CORRECTION (2026-08-12):** `/family` returns **1** member for `d5dbc9f2`, not 0 — itself, `revision_index: 1`, `parent_run_id: null`. That makes the point *worse*, not weaker: `RunHeader.tsx:62` hides the menu only below 1 member, so the "Version v1" chip DOES render, with a single option, while the spec is rewritten three times underneath — a control that actively teaches the wrong lesson. **Closed by a new `ArtifactVersionPicker` in the Steps L2 detail**, frontend-only: `?kind=X&include=content` already returns every version WITH its body, so no endpoint and no migration were needed. The "do NOT reuse the Version chip" instruction was honoured and is now backed by evidence — locked decision D2 (`IMPLEMENTATION-REGISTER.md:2531`) defines that chip's version as the family chronological index and says in terms it is NOT `artifact_refs.version`, and `handleSelectVersion` calls `getWorkflow(memberId)`, so feeding it an artifact-ref id would 404 into a dead control and corrupt the state behind FilesTab and StartingPointCard. | Verified live 2026-08-11 on runs `6e38b9a7`, `5ecb990f`, `d5dbc9f2`; re-verified against the running backend on `:8010` during the fix. Shipped as ONE change with ISS-063/FIX-221 — deliberately, because both are "a revision happened and the screen does not say so", they share the design decision (*revision visibility derives from durable state, never from live-session state*), and sequencing them was the identified path to a THIRD dead counter in this family. They also validate each other: `max(spec.version) - 1 == specRevisionCount` is now a test assertion, available only because both landed together. |
| ISS-066 | minor | test-infra,product | **FIXED** (2026-08-12) — all 8 rows backfilled above; see the correction below | Register drift: `ISS-054`..`ISS-061` have NO rows in `ISSUES-REGISTER.md`, yet those ids are live in FIX-190/192/193/194/195 rows, in cards, and in commit messages. ~~`ISS-055` is a free gap inside a used range.~~ **CORRECTION (2026-08-12): that clause was WRONG. `ISS-055` is NOT free** — it is the tier-entitlement-bypass defect closed by FIX-189 (KAN-161, commit `d2ddc73a`), cited in `backend/app/core/entitlements.py:14`, `backend/app/api/run_commands.py:1722,2326` and `.knowledge/cards/FIX-189.md:17`. Acting on the original clause would have re-issued a live id and caused a second ISS-050-class collision. The four-source sweep found **all 8** ids (054-061) in use, none free. Consequence: a register-only "next id" lookup returns a number that is already taken — which is exactly how ISS-050 was re-issued for an unrelated defect on 2026-08-11 and had to be renumbered to ISS-064. Backfill the missing rows (or record why they are absent) so the register is once again a usable allocator. | Found by the quick-260811-si4 executor, then reproduced during the ISS-050 collision post-mortem. Mitigated for now by the four-source rule in `.claude/skills/velocity-ai-bookkeeping/SKILL.md`. |
| ISS-067 | minor | test-infra | **FIXED 2026-08-12 (commit `cb55a498`) — except the `dev.db` clause, deliberately left; see below.** Seven wrong facts were corrected, not the five filed. Beyond the five listed here: (6) the doc's **"Next FIX-ID = highest in INDEX.md + 1"** rule (line ~485) is the single-source rule that re-issued a live id and forced the ISS-050→ISS-064 renumber — replaced with the four-source rule; and (7) **`lint-imports` must be run from `backend/`** (its config is in `backend/pyproject.toml`); from the repo root it prints "Could not read any configuration" and exits, which reads like a pass. Claim (1) was also wrong in a more dangerous way than filed: `uv run --no-sync` does **not** fail loudly here — it silently CREATES an empty CPython 3.12 venv, skips the dependency install, then dies on the first import, leaving a stray `backend/.venv` that shadows the working interpreter (reproduced and cleaned up). The stale migration head appeared in **three** files, not one (`velocity-ai-fix.md`, `velocity-ai-analyze.md`, `backend-models-migrations.md`) — all corrected. **Fix approach:** correcting the numbers alone would only reset the clock, so every brittle point-in-time assertion was replaced with the command that yields the current value plus a compare-to-baseline rule; a new "Step 0 — Local environment facts" block states that the whole document was written for CI (`ci.yml:95-121` provisions a synced env) and that local differs. `.claude/skills/velocity-ai-bookkeeping/SKILL.md` Step 0 was re-verified and updated too. **NOT done — the `dev.db` clause:** the 0-byte repo-root `dev.db` still exists. It is untracked and gitignored (`.gitignore:25 *.db`), and the cause is `backend/app/core/config.py:322` defaulting to the RELATIVE `sqlite:///./dev.db`, so it resolves against the process cwd. Deleting it would make a wrong-directory query fail loudly instead of silently returning zero rows, which is better — but deletion is destructive and was left for the owner to decide. The trap itself is now documented in the steering file, which addresses the actual harm of misleading an agent. | `.kiro/steering/velocity-ai-fix.md` carries five facts that are wrong on this machine and will mislead any agent that follows it: (1) it prescribes `uv run --no-sync pytest` but `backend/.venv` does NOT exist, so every command in it fails — use `python3.11`; (2) it asserts lint-imports is `4 kept / 0 broken`, actual is `3 kept / 1 broken`; (3) migration head `0026`, actual `0030`; (4) it implies the characterization goldens are green, actual is 5 failed / 5 passed on `dev`; (5) Playwright live on `:8002`, actual `:8010`. Separately, a **0-byte stale `dev.db` sits at the repo root** while the live 160MB database is `backend/dev.db` — a read-only query from the repo root silently returns an empty database with no error. | All five verified 2026-08-11 while extracting `.claude/skills/velocity-ai-bookkeeping/SKILL.md`, whose Step 0 documents the corrections. The steering source itself is unfixed. |
| ISS-068 | major | test-infra | **FIXED** (2026-08-12, quick-260812-35u; FIX-223 + FIX-224 + TEST-008; INV-3 + Ports & Adapters restored) — **goldens 5 failed/5 passed → 10 passed/0 failed; lint-imports 3 kept/1 broken → 4 kept/0 broken.** The 5 red goldens reduced to exactly **3 causes**, proven exhaustive by counterfactual, each adjudicated separately per the **ISS-029** method (which explicitly REJECTED regen-only). **C1 (all 5) — STALE NORMALIZER, neither golden nor engine wrong:** `c71f3d9c` added an additive `pipeline_start.resume_offset`; 0 on every non-resumed run, but a 0-valued key is still a NEW key and the snapshot compares whole canonical-JSON dicts. Fixed by adding it to `_VOLATILE_STRIP_KEYS` (the 7th instance of a 6-precedent convention) — **zero golden bytes touched**; `_REQUIRED_DATA_KEYS` has no `pipeline_start` entry so nothing is weakened, and resume-offset behaviour is pinned directly by `test_restart_resume.py`. **C2 (`prototype`, `od_prototype`) — a REAL REGRESSION; the golden was RIGHT:** `f608e4bc` (FIX-190) appended an `accessibility` guardrail to 4 prototype-family agents and in `prototype-build/AGENT.md` the `+` line landed ON TOP OF `icon: "🏗️"` (the other 3 files are pure `1 +`; this one is `2 +-`), so the agent fell through to the loader's `🤖` default — a user-visible UI regression in `pipeline_start.agents[]` + both `agent_start`s. Sweep: **exactly 1 of 86** AGENT.md files was missing an icon. Restored byte-identical to `f608e4bc^` — **zero golden bytes touched**. Regenerating here would have permanently pinned the regression. **C3 (`prototype_revision`) — a genuinely STALE GOLDEN:** `fd2ad8f1` (FIX-185) deliberately set `planner: skip`. Regenerated **ONLY** this file, in its own revertable commit `a6b2097e`, **strictly after C1** so `resume_offset` could not be baked in; diff fully accounted — **33 deletions / 0 insertions**, 17→14 events, removed exactly {`planner_start`, `planner_complete`, `gate_status`}, the 14 survivors byte-identical and same-order. **Import contract:** the single violation `task_loop.py:562` was a one-off bypass of the already-existing `KernelServices.template_example()` port (already consumed correctly by `context_providers/opendesign.py:128-135`); fixed by breaking the import, NOT by amending the contract. **2 coverage gaps closed (ISS-029 discipline):** every AGENT.md must now declare an explicit `icon:`, and the capability import boundary now fails **pytest**, not only the separately-invoked `lint-imports` binary — the reason a one-line violation survived ~3 weeks. Carve-outs filed, not dropped: **ISS-078** (the 7 `test_restart_resume.py` reds, unchanged at 7F/48P) and **ISS-079** (2 `test_phase6_frontend_consistency.py` reds). ORIGINAL REPORT: Shared red baseline on `dev` blocks the goldens from serving as a CI gate: **5 of 5 characterization event-snapshot tests fail** (`prototype`, `od_prototype`, `od_ppt`, `app_builder`, `prototype_revision`) and **1 import-linter contract is broken** (`agents.capabilities` → `execution_engine.od_context` → `app.services.od_loader`). Also 7 reds in `test_restart_resume.py`. Re-measured independently in a detached worktree at `edc44daa` and again at `84f4bbe3` — identical failing ids both times, so it is genuinely pre-existing and shared with `dev`, not introduced by FIX-217/218. While it stands, INV-3 dormancy can only be checked by diffing counts and ids against a hand-captured baseline rather than by a clean green run. | Measured repeatedly 2026-08-11 across quick-260811-mxg and -si4. NOTE: an older memory recorded "10 failed / 6 passed" — that was stale `feat/ui-2` data; the current figure is 5/5. |
| ISS-054 | major | frontend,product | **FIXED** — FIX-188, later extended by FIX-210 (2026-08-06 / 2026-08-11) | Concierge chain / gate_action / revision proposals never surface — the proposals binding was dead, so consequential Concierge proposals (chain, gate_action, revision) were held but never rendered as chips. FIX-210 later routed ALL free-text through the Concierge, replacing `classifyFreeText`. | **BACKFILLED 2026-08-12 by ISS-066** — this row never existed; the id was live only in code comments and cards. Reconstructed from: `.planning/FIX-REGISTER.md` FIX-188 row (KAN-160); `.knowledge/cards/FIX-188.md:17` (`Phase / trigger: … ISS-054`); 11 in-code citations incl. `frontend/src/hooks/useRunChat.ts:233,447,551,766`, `frontend/src/components/layout/DashboardLayout.tsx:219,261,1758`, `frontend/src/components/chat/RunChatLane.tsx:334,1220,1238,1256`, `backend/app/api/run_commands.py:806,920,1356,1442`, `backend/app/agents/chat/concierge.py:551`. Severity is a 2026-08-12 assessment (an entirely inert user-facing affordance), not an original filing. |
| ISS-055 | major | auth,product | **FIXED** — FIX-189 (2026-08-06) | Tier entitlement never enforced at launch — any user could run any pipeline regardless of their tier (KAN-161). Introduced a scoped tier for deployments needing prototype + a subset. | **BACKFILLED 2026-08-12 by ISS-066. This id was NOT free** — the ISS-066 row below originally claimed "`ISS-055` is a free gap inside a used range", which was wrong and would have caused a second re-issue collision of the ISS-050 kind. Reconstructed from: FIX-REGISTER FIX-189 row (KAN-161); `.knowledge/cards/FIX-189.md:17` (`Phase / trigger: … ISS-055`); commit `d2ddc73a`; in-code citations `backend/app/core/entitlements.py:14`, `backend/app/api/run_commands.py:1722,2326`, `backend/tests/unit/test_rest_run_launch.py:52`. Severity is a 2026-08-12 assessment (authorization bypass), not an original filing. |
| ISS-056 | minor | agents,product | **FIXED** — FIX-190 (2026-08-06) | Clarify question quality gaps (KAN-162): (H1) 7 manifests declare 8 defaults but only 1 round fires, silently dropping 3+ topics; (H2) prototype build/validate agents lack a WCAG AA guardrail; (H3) the style question fires after the wizard has already collected a design system. | **BACKFILLED 2026-08-12 by ISS-066.** Title quoted verbatim from the FIX-REGISTER FIX-190 row, which carries the `ISS-056:` prefix. Commit `f608e4bc`. |
| ISS-057 | major | api,product | **FIXED** — FIX-191 (2026-08-06) | `_extract_docx` silently drops table / header / footer content from uploaded Word documents; an all-table docx fires a misleading 422; `python-docx` / `python-pptx` / `pypdf` were absent from `requirements.txt` (KAN-163). | **BACKFILLED 2026-08-12 by ISS-066.** Title quoted verbatim from the FIX-REGISTER FIX-191 row (`ISS-057:` prefix). Commit `0516c5e2`. Severity raised to major at backfill: silent content loss on user-supplied documents, plus unpinned extraction dependencies — the failure mode recorded separately as the "silent doc-extraction failure" trap. |
| ISS-058 | minor | frontend | **FIXED** — FIX-192 (2026-08-06) | RunChatLane voice input mic button was a dead control — no `onClick`, no hook; it rendered "Voice · transcribe" and did nothing (KAN-164). | **BACKFILLED 2026-08-12 by ISS-066.** Title quoted verbatim from the FIX-REGISTER FIX-192 row (`ISS-058:` prefix). Commit `34c57058`. |
| ISS-059 | minor | frontend,product | **FIXED** — FIX-193 (2026-08-06) | Concierge free-text ask during an active ("building") run silently no-ops — every message typed while agents are running returned HTTP 200 `{"channel":"steering"}` with no reply (KAN-165). | **BACKFILLED 2026-08-12 by ISS-066.** Title quoted verbatim from the FIX-REGISTER FIX-193 row (`ISS-059:` prefix). Commit `47ee1f6e`. |
| ISS-060 | minor | frontend | **FIXED** — FIX-194 (2026-08-06) | Completion notification / toast is not run-scoped — a finishing run marks a DIFFERENT live run as "complete"; the toast shows the wrong label (KAN-166). | **BACKFILLED 2026-08-12 by ISS-066.** Title quoted verbatim from the FIX-REGISTER FIX-194 row (`ISS-060:` prefix). Commit `552e10e7`. Same family as the SSE run-binding class recorded against Phase 44. |
| ISS-061 | minor | frontend | **FIXED** — FIX-195 (2026-08-06) | Header badge / notification clicks navigate to the wrong run; the Steps panel shows a two-run hybrid; the notification row tap does nothing (KAN-166). | **BACKFILLED 2026-08-12 by ISS-066.** Title quoted verbatim from the FIX-REGISTER FIX-195 row (`ISS-061:` prefix). Commit `3daef64a`. |
| ISS-050 | — | — | **BURNED — do not reuse. Not an open issue.** | **TOMBSTONE.** This id was allocated to "CR-02 guard blocks `prototype_revision` and `user_stories_revision`", closed by FIX-185 (commit `fd2ad8f1`), and the id lived ONLY in that commit message — never in a register or card. On 2026-08-11 a register-only "next id" lookup returned 049, so 050 was re-issued for an unrelated defect and had to be renumbered to **ISS-064**. This row exists so the id reads as taken from the register alone. | **BACKFILLED 2026-08-12 by ISS-066.** The originating incident is described in the ISS-064 row above and in `.claude/skills/velocity-ai-bookkeeping/SKILL.md` Step 1. Detected mechanically by `python3 scripts/knowledge/check_ids.py ISS`. |
| ISS-069 | minor | test-infra | **FIXED 2026-08-12** — all 11 rows backfilled from named evidence; `check_ids.py` now exits **0** on FIX, ISS and TEST alike (first clean run). Two tool bugs were found and fixed while closing this: (1) `check_ids.py` swept **itself**, counting the real ids cited as worked examples in its own docstring, so it reported drift it had invented — it now skips its own file; (2) a genuine blind spot is now documented rather than hidden — ids written as a range or slash-list (`FIX-158..FIX-182`, `FIX-182/183/184`) contain no literal `FIX-183` token, so the tool cannot see them and a busy range still needs a manual log grep. **Honesty note on the backfill:** 4 of the 11 (FIX-020, 158, 162, and the FIX-183/184 pair's individual split) have scope that is genuinely **unrecoverable** from the surviving evidence; those rows say so explicitly rather than inventing a plausible description. The other 7 were reconstructed from a card summary, an in-code citation, or a commit subject, and each row names which. | **FIX-REGISTER drift — the ISS-066 defect, in the other register.** 11 `FIX-NNN` ids are in active use with NO row in `.planning/FIX-REGISTER.md`: **FIX-020** (commit messages only), **FIX-128**, **FIX-129** (both: register prose mentions + cards + code), **FIX-131** (code only), **FIX-158** (all four sources), **FIX-159**, **FIX-162**, **FIX-183**, **FIX-184** (commit messages ± code), **FIX-214**, **FIX-215** (commit messages + code). Same consequence as ISS-066: a register-only "next id" lookup under-reports, and the register cannot serve as the allocator or as the index of what shipped. Backfill each row from its commit, or record why it is absent. | Found 2026-08-12 by the new `scripts/knowledge/check_ids.py` while closing ISS-066 — the check was written as ISS-066's prevention half and immediately paid for itself. Reproduce with `python3 scripts/knowledge/check_ids.py FIX` (exit 1 on drift). Spot-verified by hand that FIX-020/128/158/214 genuinely have no `\| FIX-NNN \|` row. NOTE: the same tool initially reported 32 phantom `TEST-*` drift rows; that was a real false positive caused by two unrelated TEST namespaces (padded `TEST-001` in `FIX-TEST-REGISTER.md` vs unpadded QA-case ids `TEST-45` / `TEST-2-2` in `.knowledge/cards/`) and is now fixed in the tool — do not re-file it. |

**Verification / fault-injection levers (offline-deterministic where live can't trigger):**
- **ISS-016**: the lever already exists — `ScriptedFakeChatModel([], raise_exc=ScriptedNonTransientError())` (`_scripted_model.py:106-176`) raises a `ValidationException`-coded error pre-token; assert `pipeline_failed` (all-fail) / `degraded` (partial); parity guard = the 5 characterization goldens stay byte-identical (scripted model never raises). **`test_model_fallback.py:257-271` currently asserts the bug as expected — must be updated.**
- **ISS-007/002**: `/tmp/iss007_repro.py` reproduces the three-layer cancel and proves `pipeline_cancelled` never reaches the WS; post-fix flips to delivered. Tighten `test_run_revision_ws_dispatch.py::test_cancellation_lands_row_cancelled` (currently accepts the frame stuck in-queue).
- **ISS-019**: `/tmp/wave_panel_probe.py` already measures heading-`y`; pass = `y+h ≤ 950` without scroll during an active wave run.
- **ISS-010**: `InMemorySpanExporter` injected via `otel._build_span_processor` (proven to capture 1 span); **ISS-011**: offline `TestOfflineHITL` already green proves the gate seams; **ISS-003**: `asyncio.wait_for(_drive_live(...), 10)`→TimeoutError pre-fix, returns post-fix.

**Fix-clustering (convergent root causes → coherent phases):**
- **A · Terminal-state integrity (BE+FE)** — ISS-016 + ISS-017 + **ISS-007 + ISS-002**: all "the server must emit a faithful terminal event (error/cancel) and the FE must reflect it." ISS-002≡ISS-007 (one cancel-delivery root cause); ISS-016≡ISS-017 (one error-swallow root cause). Closes the IN-03 family too.
- **B · Reconnect frame-contract** — ISS-008 + ISS-009: same handler (`websocket.py:763-822`), fix together (replayed `section` + ack `live`).
- **C · Test-infra / verification gaps** — ISS-003 (clarify-off) + ISS-011 (skip-on-expiry) + ISS-010 (in-memory span test): harness/test only, zero prod code.
- **D · Prompt & deliverable adherence** — ISS-004 + ISS-005 + ISS-006: one live re-confirm pass closes the observation; ISS-005/006 share one event-free `post_step` **Validator** mechanism (two sibling validators); ISS-004 needs a separate chunk-stream **hook** (or stays prompt-only).
- **E · Custom-workflow UX completeness** — ISS-014 (delete dead composer) + ISS-015 (WONTFIX/doc) + ISS-021 (generic deliverable renderer) + ISS-019 (wave-fold CSS): FE-weighted, zero INV-3 exposure.

## Notes
- **Gap-audit of every FIXED issue (2026-06-14 — one Opus 4.8 agent per issue, max effort, register-first):** all 16 repo-backed FIXED issues were re-audited for residual gaps. **10 confirmed clean** (root-fixed, test-proven, live-confirmed): ISS-002, 003, 004, 007, 008, 009, 010, 014, 019, 021. **6 had residuals — all closed + independently verified except the accepted/locked ones:** ISS-017 (history-reopen affordance was wired to a dead surface → CLOSED, rendered in `WorkflowHistory`); ISS-006 (`app-feature-implementation` unpinned 3rd `getDatabase` node → pinned + test extended); ISS-005 (`api_prefix` regex missed k8s-probe/ingress paths + a `proxy_pass` false-positive → linear line-scan rewrite, +6 tests, golden byte-identical); ISS-011 (`TestLivePipelines` lacked the mid-sweep-expiry re-check → shared `_LiveCredsRecheckMixin`); ISS-001 (deck-contract test wouldn't catch a contract deletion → +2 negative composition cases). Accepted/locked residuals left as-is (documented): ISS-001 "no deterministic net" (resolver fallback rejected as INV-3-sensitive), ISS-005 audit-only/never-read (INV-3 tradeoff), ISS-006 prompt-only (deferred to N3). ISS-016's one narrow same-agent task-loop partial-failure edge → logged as **ISS-028** (DEFERRED, pre-existing, low-prob). **All fixes proven INV-3-safe** (5 goldens byte-identical, golden dir clean) + SC-001 (banned-patterns green) + Ports&Adapters (lint-imports 4 kept/0 broken). Plus **ISS-027** root-caused + fixed (Option-A force-proceed) + independently verified. Consolidated re-run: backend 162 passed/7 skipped; FE vitest 107 passed (7 known-fail baseline ISS-022/025 only).
- **Remaining-issue sweep (2026-06-14 — one Opus agent per issue, register-first → root-cause → fix → independent verify):** the user directed "fix ALL issues." The 7 code/test/docs-fixable were closed: **ISS-012** (Traceability table rewritten one-row-per-REQ-ID, 0 missing); **ISS-022 + ISS-025** (one root cause — a phantom 4th `app_builder` `CHAIN_OPTIONS` entry added 2026-05-14 with tests never updated; removed, data-only → whole FE suite 117/0 green); **ISS-026** (added 3 genuinely-emitted gate events `gate_blocked`/`validation_warning`/`gate_wait_human` to the documented vocab); **ISS-024** (id→name on all 3 degraded-affordance paths, id-fallback; **verified**); **ISS-023** (cooperative-cancel yields `pipeline_cancelled` once + returns, killing the discarded duplicate) + **ISS-028** (degraded keyed on `(agent_id, task_number)` so task-loop partials end `degraded`) — both **engine, INV-3-safe (5 goldens byte-identical, golden dir clean, lint-imports 4/0, ISS-016/007 intact), non-vacuity empirically proven, verified**. The 3 non-code issues stayed as-is at the time: **ISS-015** WONTFIX → **SHIPPED in Phase 20 (2026-06-14) as WF-DB-01** (the deliberate v2 *feature* — a generic pipeline picker — was later built additively, zero engine edits), **ISS-018** env (the `hexaware-srini` AWS-account Bedrock entitlement — not repo code; `default` profile works), **ISS-020** harness (the throwaway `/tmp` ws_rewrite driver — no repo artifact; the fixture workflows have no product launch surface). **Net: every issue is now FIXED (incl. ISS-015 SHIPPED in Phase 20, 2026-06-14) except 2 non-code env/harness items (ISS-018 Bedrock entitlement, ISS-020 throwaway driver) — 0 OPEN code issues remain.**
- **Live + Playwright re-confirmation (2026-06-13):** `.planning/live-verification/CAMPAIGN-2026-06-13-phases16-19.md` — on `default`: ISS-019/021/007(+002)/014/005/006 **live+visually confirmed**; ISS-008/009 reconnect-contract live (revision-section/`live:true` offline). **ISS-016(major)/017 live-confirmed on srini** (the original `ValidationException: Operation not allowed` → run ends `pipeline_failed` with 6 agent_error/0 agent_complete + the FE "did not complete successfully" degraded panel + sanitized error text — exactly the S01 scenario, now fixed). Only ISS-004 stays offline-proven (sdlc-governance didn't reach in the app_builder window; chunk-sanitizer fault-injection is deterministic). No product defect surfaced in the entire pass.
- This register is append-only by ID. When an issue is fixed, flip Status to FIXED and cite the closing commit/phase; don't delete the row.
- UI-campaign findings land here as scenarios run (a row per real product issue; harness/driver gaps tagged `harness`).
- The IN-01..IN-06 advisories from phases 12–14 are tracked in the per-phase REVIEW.md files; only the ones independently re-observed this pass are surfaced here (ISS-007/008/009) to avoid duplicating the phase registers.
| ISS-075 | major | frontend | **FIXED** (FIX-225, quick-260812-4ss, 2026-08-12 — live-proven) | **Reopening a run resets the whole Steps trace to idle.** The trailing resume `pipeline_start` carries `resume_offset = 0` (run `d5dbc9f2` seq 24791 — verified in `run_events`: both of that run's `pipeline_start` frames read `"resume_offset": 0`), and `useWorkflow.ts:234-240` seeds `status: idx < resumeOffset ? "done" : "idle"`. With the offset at 0 every agent is rebuilt as `"idle"`, so a completed 5-agent run that is reopened — or any live session whose SSE connection reattaches and replays from seq 0 — should repaint its Steps spine as if nothing had ever run. Note the frame is emitted ~24 minutes after the last agent finished (21:25:42 vs 21:01:19), i.e. it is not part of the original execution. Same invisibility family as ISS-063/ISS-065: durable state is correct, the screen contradicts it. Suspected cause is the resume path computing the offset before the completed-agent count is known, or defaulting it; not traced. | Found 2026-08-12 during the ISS-063 investigation and re-verified from `backend/dev.db` while fixing it (quick-260812-2ci). **Confirmed in code and data; NOT observed on screen** — that observation is the first thing a fixer should do, because if it reproduces it is arguably more serious than the banner defect it was found beside. FIX-221 is independent of it: the revision count is now carried across a same-run `pipeline_start` precisely so this frame cannot erase it. **CLOSED 2026-08-12 by FIX-225** (commit `c46c8160`). The fix is the one this row asked for: `useWorkflow.ts` now computes `isSameRunReannounce` ONCE inside the `pipeline_start` updater and MERGES the roster on a re-announcement — identity fields from the frame, live `status`/`output`/`duration`/tokens preserved — with `completedCount = max(agents done, resumeOffset)`; the proto task carry-overs ride the same predicate. The engine was NOT touched, so the goldens stayed 10/10. LIVE PROOF: run `d5dbc9f2` reopened from Run History now renders **"3 / 5 agents"** with specify/plan/analyze `disabled=false` and their real durations ("Spec Writer Agent 1m 42s · 42.4K tok"); build/validate stay disabled because they genuinely never ran (`scratchpad/verify-4ss-after-02-steps.png` vs the pre-fix `live-04-steps.png`). That also makes FIX-222's version picker REACHABLE on a paused run for the first time. Pinned by 4 new cases in `useWorkflow.specRevisionCount.test.ts` (TEST-009), all seen RED first. |
| ISS-076 | major | test-infra | **OPEN** | **The mocked Playwright suite has a 33-red baseline, not the "132 green" the records claim.** Measured on `bugfix/spec-revision-context-loss` at `b13d5c33`: **33 failed / 43 skipped / 108 passed**. The "123–132 green" figure in `TEST-REGISTER.md` and in session memory is `feat/ui-2` data and is stale on this branch. Failing clusters include ts-chat (4), ts-e.model-picker (5), ts-u.revisions (4), ts-t.history (4), ts-l.token-usage (3), ts-k, ts-m, ts-h, ts-chat-cards. Same consequence as ISS-068 for the goldens: while it stands, the mocked suite cannot serve as a pass/fail CI gate, and a change can only be checked by diffing failing IDs against a hand-captured baseline. **Includes a methodology trap worth as much as the count:** `playwright.config.ts` sets `reuseExistingServer: true` on `:3000`, so running the suite from a baseline worktree silently tests the MAIN tree's code through the already-running dev server and returns a *confirmatory* answer no matter what — it nearly produced a false "pre-existing" claim here. A real baseline needs its own server on another port, and a symlinked `node_modules` will not do (Turbopack: *"Symlink [project]/node_modules is invalid, it points out of the filesystem root"*) — clone it with `cp -Rc` instead (~6 s on APFS). | Measured 2026-08-12 during quick-260812-2ci, both at `b13d5c33` (own server on `:3100`) and after the change, comparing failing IDs. That comparison is what proved FIX-221/222 introduced **0** new failures. |
| ISS-077 | minor | frontend | **OPEN** | **`ArtifactNode`'s TypeScript declaration disagrees with the wire shape on three fields.** `frontend/src/lib/api.ts:602-604` declares `derived_from: string[]` and `children: string[]`, but the backend returns `derived_from: string \| null` (`runs.py:920`) and `children: ArtifactNode[]` (`runs.py:921`); `task_id` is declared `string` but is `null` on every row observed. Nothing reads those three fields today — `WorkflowHistory.tsx:303` and the new `ArtifactVersionPicker` both use only `kind` / `version` / `content_hash` / `producer_agent` / `id` / `content` — so nothing is broken, but the first consumer of the lineage tree will be type-checked against a lie and will write `.map()` over a nullable string. The same file's docstring also cites `runs.py:590-608` for the node shape, which has drifted; the real `_node` is `runs.py:907-925`. | Found 2026-08-12 during the ISS-065 investigation and deliberately NOT fixed while implementing FIX-222 — it is unrelated to the version picker and folding it in would have widened a scoped diff for no test coverage. One-line change plus a docstring correction. |
| ISS-078 | major | engine | **FIXED — FIX-229 / TEST-013 (quick-260812-97f), commits `1c7569db` (RED tests) + `d3184e10` (fix), 2026-08-12. `test_restart_resume.py` 7 failed / 48 passed -> **60 passed / 0 failed**. Offline-proven; deliberately NOT live-proven (one build is 5-21M tokens).** **BOTH register hypotheses were WRONG, and the answer was a third thing.** *H1 (test stale)* and *H2 (code regressed)* both assumed ONE cause; there were **three**, from three commits. **Six of the seven reds were TEST defects; exactly one was a product defect — and it was not the one the row was watching.** **(1) Five reds = fixture drift, NOT a resume regression (#1 #2 #3 #4 #6).** Every one of those fixtures persists **zero** `run_events`, so the `agent_complete` evidence `_first_incomplete_step` requires cannot exist in them by construction. #1/#6 — the two the row called "end-to-end" and treated as H2's strongest support — drove `engine._execute_impl` **directly** (`:431`, `:3049`); the durable `_RunEventSink` is built in the PUBLIC entry (`engine.py:1020`, persisted `:1065`) and `_execute_impl` persists nothing, so instance A wrote artifacts and not one event row. They called themselves end-to-end while skipping the durability layer their own premise depends on. Driving `execute()` makes them pass **with the engine untouched** — which is the disproof of H2. #2/#3/#4 hand-seed `ArtifactRef` only; #3/#4 were failing at index 0 on the PLAN step and never reaching the `task_loop` branch under test. **(2) THE PREDICATE WAS NOT LOOSENED — and that is the load-bearing decision here.** The investigation recommended replacing `c71f3d9c`'s conjunction with an interruption test (`in agent_start_events and not in agent_complete_events`). **Rejected on measured evidence** (read-only against live `dev.db`): its differential vs HEAD is exactly *artifact producer with NO `agent_start` AND NO `agent_complete`* = **27 rows, all `deep-planner`/`clarify-agent`** — and **neither appears in any `workflow.yaml` step list nor in `registry.PIPELINE_AGENTS`** (grep: 0 hits), so neither is ever in `ordered_agents`. Meanwhile the case it was argued from (a lost `agent_complete` write) has **count 0** live **and the proposal is a no-op for it anyway** — `agent_start` present + `agent_complete` absent is `_interrupted=True`, i.e. still re-run, identical to HEAD. **Net: zero production effect, sole observable consequence = five broken fixtures go green.** That is loosening a predicate to satisfy a test. **`engine.py` is byte-identical to `99fcf4a2`.** **(3) #5 confirmed exactly as this row stated** — 3-tuple stub (`:2642`) vs the 4-tuple contract (`chat_narrator.persist_milestone_card:287-289`). It was breaking the resumed **drive** (`engine.py:8509`, swallowed), not just the test. One line. **(4) #7 = the one REAL product defect, and it is a status LIE (FIX-229).** A user who resumes a run parked at a gate and REJECTS had it recorded `completed`. Probe-captured tail: `...5:review_gate_ready / 6:pipeline_cancelled / 7:pipeline_complete`. `resume_supersedes` (`run_commands.py:602`, `bac1e556`/KAN-120) asked *"higher seq?"* and read it as *"later attempt succeeded"* — two different questions; on a rejection both events are ONE attempt. Now requires a `run_resuming`/`pipeline_start` boundary between them. **Coverage gaps closed (ISS-029 discipline):** FIX-121 and `resume_supersedes` **both shipped with no test at all** — `grep resume_supersedes tests/` returned zero. Both are now pinned, each seen RED under the mutation it guards. **Goldens 10/10 and lint-imports 4 kept / 0 broken, both identical to `99fcf4a2`; no golden regenerated.** Protected suites 52 passed and the resume/cancel/shutdown adjacents 44 passed, both identical to baseline. **Filed, not folded in: ISS-091** — gate rejection does not STOP the pipeline (measured at HEAD, i.e. *with* FIX-227: the full fan-out still runs after `pipeline_cancelled`). FIX-229 stops the record lying about it; the wasted work is ISS-091's. **Also observed, unregistered: 3 pre-existing reds in `tests/agents/test_gates.py`** (`test_raising_hitl_gate_blocks_and_stops_gate_evaluation`, `test_engine_sentinel_carries_human_gate_edit_detail`, `test_apply_declared_gate_edit_rewrites_upstream_artifact_and_result`) — present at `99fcf4a2`, unrelated to ISS-078, in no register row I could find. ORIGINAL REPORT: | **7 reds in `backend/tests/agents/test_restart_resume.py` (7 failed / 48 passed), and at least two of them may be describing a LIVE PRODUCTION RESUME BUG rather than test rot.** Carved out of ISS-068 (which owned the golden + import halves and is now FIXED); measured identically at `f5f2f7c6` and after FIX-223/224, same 7 ids, twice in one session — **not flaky, and not environment-gated** (55 collected, **0 skipped**, whole file runs offline in ~5 s; no Postgres/Bedrock/Chromium gate fires). All 7 exist on `dev` too. They were green once: commit `3e80fcc4` (2026-07-19) records "restart_resume 39 -> 41/0" with all 7 already present, so these are **regressions since 2026-07-19**, not red-by-design pins. **Failing ids:** `test_midwave_resume_does_not_reinvoke_completed_workers` (:472), `test_open_gate_override_is_noop_without_review_gate` (:1204), `test_partial_task_loop_build_reenters_step_not_skipped` (:1575), `test_completed_task_loop_build_stays_complete_no_rerun` (:1603), `test_resumed_run_is_wired_live_ectx_and_milestone_cards` (:2663), `test_failed_run_resumes_skips_completed_tasks` (:3081), `test_failed_run_with_open_gate_resumes_into_gate` (:3173). **LEAD (high confidence, UNPROVEN):** `c71f3d9c` — a commit titled `fix(frontend)` — is the only commit that has ever touched `_first_incomplete_step`'s skip predicate, and it inverted it from `or` to `and` (`engine.py:7882-7885`: `complete_by_artifact = agent_id in produced_agents and agent_id in agent_complete_events`), strictly tightening what counts as "already complete". Three of the reds (#2/#3/#4) fail with the identical `idx=0` signature — exactly what a stricter predicate produces when a test seeds durable artifact rows but no `agent_complete` rows. **The same commit also added `resume_offset`, which was ISS-068 cause C1 — it is implicated in both halves.** **TWO LIVE HYPOTHESES — do not guess:** *H1 (test stale)* the tightening is correct and the hand-seed idiom now needs `agent_complete` rows; *H2 (code regressed)* the tightening over-corrects and really does re-invoke completed work on resume in production. **H2 has real support:** #1 and #6 are **end-to-end** — they drive the real engine, let it write its own durable rows, and still observe `part_a` re-invoked (`{'a': 4, 'b': 4, 'c': 2, 'd': 2}`). A hand-seeding artefact cannot explain those two. **RUN THIS FIRST:** in a scratch worktree revert *only* the `c71f3d9c` predicate hunk and re-run the 7; if ≥3 go green, H2 is live and this is a production resume regression, not test rot. **One red is PROVEN trivial and independent (#5):** `2d5b9fcc` (FIX-175) updated the unpack at `test_restart_resume.py:2665` to a 4-tuple (`_created, _card_seq, _, _reply_eid`) but left its own stub at `:2642` appending a 3-tuple (`emitted_cards.append((created, card_seq, card))`) → `ValueError: not enough values to unpack (expected 4, got 3)`. One line, zero production risk. **DO NOT delete, xfail or loosen any of the 7** — at least two are end-to-end assertions that completed work is not re-invoked on resume; if they are right they are describing a live bug. Estimated multi-day; plausibly ≥3 distinct causes. | Carved out of ISS-068 during quick-260812-35u, 2026-08-12. Baseline re-measured before and after FIX-223/224: unchanged at 7 failed / 48 passed with the same 7 ids. Root-cause detail in the ISS-068 investigation report §6. |
| ISS-079 | minor | test-infra | **OPEN** | **2 further reds in the shared baseline that ISS-068 never recorded: `backend/tests/agents/test_phase6_frontend_consistency.py`.** `TestPrototypeAndPptSpecifics::test_prototype_real_ids_and_human_gate` (extra item in the left set: `'prototype-analyze'`) and `TestPipelineCategoryCounts::test_ppt_and_prototype_counts_reconciled` (`assert 5 == 4`, "prototype category count must be 4 (spec-kit)"). **Re-baselined as pre-existing with evidence, not labelled:** measured identically (2 failed / 15 passed, same 2 ids) in a **detached worktree at `f5f2f7c6`** — so they are not a FIX-223/224 regression. Cause is a **stale test, not a defect**: commit `5c947270` added the `prototype-analyze` Spec-Kit step to the prototype workflow (present on `dev`, so shared, not branch-local), making the prototype category 5 while the test still hardcodes the pre-analyze 4-agent id set and count. Fix is to update the expected id set + count to include `prototype-analyze` — but **verify first** that 5 is the intended membership (check `registry.PIPELINE_AGENTS["prototype"]` and the prototype manifest) rather than editing the number to match reality, which is the mistake this whole ISS-068 family exists to prevent. Same consequence as ISS-068 while it stands: this suite cannot serve as a clean pass/fail gate. | Found incidentally during the FIX-223 blast-radius sweep in quick-260812-35u, 2026-08-12, and filed rather than left in a transcript. |
| ISS-080 | major | frontend | **FIXED** (FIX-225, quick-260812-4ss, 2026-08-12 — live-proven) | **The revision banner back-fills but over-counts: it renders "Spec Revision Cycle 5" on a run that had 2 revision cycles.** FIX-221 fixed the right half of ISS-063 — the banner now appears on a fresh load, where before it never appeared at all — but the derived number is wrong, which is the same over-counting class FIX-164 originally existed to fix ("Cycle 6 after one click"). Ground truth for run `d5dbc9f2-dbe8-480f-8b13-788794a6788e`: `agent_start` counts are `prototype-specify` **3**, `prototype-plan` 2, `prototype-analyze` 2, and `artifact_refs` holds spec v1/v2/v3 — so the correct derived value is `3 − 1 = **2**`. The UI shows **5**, which is exactly `(3 × 2) − 1`. **Hypothesis (to be confirmed, not yet proven):** every durable frame reaches the reducer TWICE on a reopen — the REST replay in `dashboard/page.tsx` and the SSE replay from `seq = 0` both deliver it — so `useRunStateStore.handleFrame`'s accumulate-restarts derivation is not idempotent under double delivery, despite FIX-221's claim that it is a pure function of accumulated frames. If that holds, the fix is to dedupe by event `seq` (or derive from a set of unique seqs / the `artifact_refs` version count) rather than to accumulate. **Do NOT revert FIX-221** — the back-fill half is correct and was the substance of ISS-063; only the derivation is wrong. Note the lesson: FIX-221 shipped with 15 green tests and still over-counts live, because no test replays the same frame twice. | Observed by me in a live headless-browser pass on 2026-08-12 against the running stack (backend `:8010` on current code, frontend `:3000`), reopening the run from Run History on a fresh load. Screenshots `live-03-run.png` / `live-05-agent-detail.png`; driver scripts `live-verify.mjs` / `live-picker.mjs`. Counts read directly from `backend/dev.db` `run_events`. **ROOT CAUSE PROVEN 2026-08-12 — the hypothesis above is confirmed.** `useWorkflow.ts:369-376` does `agentStartCounts[agentId] += 1` with **no identity key**, so it counts *events*, not *distinct* events; `deriveSpecRevisionCount` (`useWorkflow.ts:213-219`) is then `max − 1`. On reopen the same durable event reaches the router twice (REST replay + SSE replay). A dedupe seen-set exists and should have stopped the second copy, but the trailing same-run `pipeline_start` fires `resetReplayState` (`page.tsx:774`) which **wipes the seen-set mid-replay** — measured at 24,783 discarded ids, exactly `24791 − 9 + 1`. Replaying the real log through the real reducer gives **2** for one pass and **5** for two, reproducing the screen exactly. **Fix: key the counter on `event_id` as a set, never `+= 1`** — that is immune to any multiplicity. The `artifact_refs`-derived alternative was considered and rejected (extra fetch, workflow-specific kind, SC-001). Shares its trigger with ISS-075 and must ship with it, plus the shared "skip `resetReplayState` for a same-run frame" predicate. See also **ISS-081**, a second and independent defect in the same expression. **CLOSED 2026-08-12 by FIX-225** (commit `c46c8160`), with BOTH halves the investigation recommended. (1) Identity: `agentStartCounts` (a bare `+= 1`) is REPLACED by `agentStartEventIds: Record<agentId, string[]>` — a SET of durable event identities (`event_id`, else `seq`; unstamped legacy frames stay un-deduped exactly as `shouldApplyEvent` treats them), so the value is a function of the SET of events and immune to ANY multiplicity from ANY transport. (2) Class: `page.tsx` no longer calls `resetReplayState` for a `pipeline_start` naming the run this tab is already tracking, so the seen-set is not wiped mid-replay and the SSE pass is fully deduped against the REST pass. The predicate is captured BEFORE the launch-window block re-points `trackedRunIdRef`, or a fresh launch would misread as "same run"; the four sibling clears (reviewGateData / questionnaireData / activePipelineRunId / lastCancelledRunId) are deliberately left running. LIVE PROOF: the banner now reads **"Spec Revision Cycle 2"** (was 5) on run `d5dbc9f2`. Pinned by 3 new cases (double + triple delivery + the per-agent tally), all seen RED at 5 / 8 / `{6,4,4}`. **The `page.tsx:837-841` contradiction flagged as unresolved in the investigation is now EXPLAINED:** the REST-replayed `agent_*` frames really cannot reach the store directly (the inner `frameRunId` const shadows the function parameter and is `undefined` for them), but they DO reach `useWorkflow.pipelineState` via `handlePipelineMsgRef` at `:897` (`isForActiveRun` is true precisely because that id is undefined) — and the FIX-201 bridge effect at `page.tsx:1640-1649` then pushes that whole state object into the store wholesale (`runStore.updatePipelineState(runId, () => pipelineState)`). The SSE pass increments on top of the copied base. That is the transfer step; no third delivery path exists. |
| ISS-081 | major | frontend | **FIXED** (FIX-225, quick-260812-4ss, 2026-08-12) | **`deriveSpecRevisionCount` takes `max` over ALL agents, but `prototype-build` restarts once per task.** Even with ISS-080's double-counting fully fixed, the derivation is wrong on any real prototype build: replaying run `6e38b9a7`'s real log through the current code yields **10** for a run with **one** revision, because the build agent restarted 11 times across its task loop and the `max` picks it up. Run `d5dbc9f2` hid this because it never reached the build phase — it is paused at a spec gate, so `prototype-specify` (3 starts) happened to be the max. **This will surface on the next completed prototype build**, which is the common case. The count must be scoped to the agent that actually produces the revised artifact rather than taken across the whole roster. **Design constraint the fixer must respect:** scoping it by hardcoding `prototype-specify` would be an SC-001 violation (kernel/FE must not branch on a workflow-specific agent name) — derive the identity generically, e.g. from the agent that produces the gated spec artifact, and say in the fix why it is name-free. Same defective expression as ISS-080 (`useWorkflow.ts:213-219`), different mechanism: ISS-080 is *multiplicity*, ISS-081 is *scope*. | Found 2026-08-12 by the joint ISS-080/ISS-075 root-cause investigation, which replayed a second run's real event log through the real reducer rather than stopping at the first reproduction. Filed separately because fixing either one alone still leaves a wrong number on screen. **CLOSED 2026-08-12 by FIX-225** (commit `c46c8160`). `deriveSpecRevisionCount` now counts restarts of the pipeline HEAD only — `state.agents[0].id` — instead of `max` over every agent. **SC-001/INV-1 safety:** the head is POSITIONAL, the first step of whatever roster the compiled manifest announced on `pipeline_start`; there is no agent id, no workflow name and no artifact `kind` anywhere in the expression, so a custom workflow gets the same rule for free. Verified against both real logs: `d5dbc9f2` head=3 starts → 2 (durable truth: `spec` v1/v2/v3), `6e38b9a7` head=2 starts → 1 (durable truth: 1 revision) with `prototype-build`'s 11 task-loop starts correctly ignored. Known and accepted limitation, recorded here rather than hidden: a workflow whose revision window excludes step 0 UNDER-reports (banner hidden) instead of over-reporting — a strictly better failure mode than the 10x over-report it replaces. Pinned by 5 new cases (incl. the predicted live symptom "0 revisions on a run whose only repeated agent is the task loop", seen RED at 10). |
| ISS-082 | minor | frontend | **FIXED** (FIX-235 / quick-260812-iqh, 2026-08-12) | **The sibling reducer accumulators carry no identity key either — `agent_chunk` (`output: prev.output + chunk`) and `hook_run` (`hookRuns: [...prev.hookRuns, entry]`) in `useWorkflow.ts`.** FIX-225 gave the revision counter its own identity key because that number is the one users read; these two are now protected ONLY by the class-level fix (the seen-set is no longer wiped mid-replay), i.e. by an external guarantee — exactly the arrangement that failed for ISS-080. A partial double-delivery would duplicate streamed agent text or an audit row. Generalised rule the investigation drew out and worth adopting: **any reducer field that accumulates (`+= 1`, `push`, `concat`) must have a test that replays its input twice.** | Sibling sweep during quick-260812-4ss (`grep -nE '\+= 1|\.push\(|\.concat\(' src/hooks/useWorkflow.ts`); not exercised, not fixed — deliberately out of FIX-225's scope. |  **RESOLVED — and the row was wrong about three facts.** (1) It names **2** accumulating fields; there are **7** (6 unprotected): `output`, `thinkingText`, `toolCalls`, `validationIssues`, `hookRuns`, `agentStartEventIds` (the FIX-225 exemplar) and `clarifications`. (2) **The cited grep finds NEITHER field it names** — re-run verbatim at HEAD `07197b0e` it returns a **comment** (`useWorkflow.ts:431`) and a **local array build** (`:954`), and zero real accumulators, because they all use `[...prev.x, y]` or string `+`. That is why the sweep under-reported by 3.5x. (3) **`hook_run` was never protected by anything**, not "protected only by the class-level fix": `emit_hook_event` (`kernel_services.py:397-414`) pushes the frame straight onto the live queue, bypassing `execute()`'s stamping chokepoint (`engine.py:1058-1065`), so it arrives with **no `event_id`** and `wsReplayState.ts:36` returns `true` unconditionally for unstamped frames **by design**. **Reachability proven, not theoretical**: 12 tests seen RED first — `output "Hello worldHello world"`, `thinkingText "step one\nstep one\n"`, `toolCalls 2`, `validationIssues 2`, `hookRuns 2`, each also at n=3; and `hookRuns 2` on a **FULL** replay too, the only field nothing resets (`agent_start`'s FIX-039 reset is what makes the others converge). Fixed by giving the reducer its own identity gate (an O(1) per-run `seq` cursor + a small unsequenced-id set), stamping `hook_run` in the backend, un-shadowing `page.tsx`'s run-id parameter and DELETING FIX-201's deliberately-undeduped second replay pass. **The generalised rule is now mechanical**: `frontend/src/hooks/useWorkflow.accumulators.test.ts` — a declared registry of all seven fields x three multiplicities (partial redelivery / full replay / triple), plus **GUARD-1**, a source assertion keyed on the structural invariant (a property whose value both READS its own previous value and GROWS it), written against the seven known sites and validated to find exactly 7 with zero false positives before being trusted. *A reducer field that accumulates is not done until a test has fed it the same input twice and it did not change.*
| ISS-083 | trivial | frontend | **RESOLVED** by FIX-236 (quick-260812-jn9, `0312a0af`, 2026-08-12) — **the row's FACTS were right and its FIX was WRONG; the proposed wiring was REJECTED and nothing was wired.** The prop was not a gap but a **duplicate**: the header rendered `` `Revising spec — cycle ${cycle ?? 1}` `` while the body rendered the backend narrator's own text (`chat_narrator.py:157`), so had the card ever mounted it would have printed "cycle 1" directly above "cycle 2" — INV-12, and the superseded copy was **deleted, not extended**. Four grounds for the rejection, each re-verified: **(1)** the entire `spec_revision` card kind is unreachable end-to-end — `chat_narrator._revision_index` keys on `spec_revision_attempt`/`revision_index` and those strings appear as quoted dict keys **only at `:112`/`:114`, the reader itself**, so `attempt` is always 0 (**0 `spec_revision` cards across 232,023 durable events over 14 runs, 52 of which prove spec revisions demonstrably happened**); **(2)** the dormancy is DELIBERATE and LOCKED — `IMPLEMENTATION-REGISTER.md:4214` records FIX-220 misnaming its keys *specifically* to avoid waking this reader; **(3)** the same line forbids the proposed action outright (*"must CONSUME `revision_cycle`, not invent a third counter"*) and the per-card value already rides the wire twice (`text` + `deep_link.target`); **(4)** `specRevisionCount` is the run's CURRENT count while a chat card is a HISTORICAL entry, so every card in a transcript would repaint with the live number. Also: **the row's cited `97-100,119` was stale at both ends** — the doc block opens at `:96` and it misses the destructure at `:103`, which any fix must touch. The card KIND is deliberately KEPT (cross-stack contract, parked on purpose) and its unreachability filed as **ISS-111**. Family correction: this is the **fifth** dead counter, not the third the row claims. | **A third, dormant counter in the spec-revision family: `ResultCard.tsx:97-100,119` renders `Revising spec — cycle {cycle ?? 1}` for the `spec_revision` card kind and NOTHING passes `cycle`** (`grep -rn "cycle={" src` → no non-test hit), so that chat card would print "cycle 1" forever. Not a regression and not user-visible today, but this family has already produced two dead counters (ISS-039's and FIX-164's). Wire it from the same single source the banner now uses (`runStore.viewed.specRevisionCount`) rather than reintroducing a local count. | REOPEN-REPLAY-ANALYSIS §5.1 (blast-radius sweep); re-confirmed during quick-260812-4ss. |
| ISS-084 | **CRITICAL** (raised from major) | product,api,engine | **FIXED — FIX-227 / TEST-011 (quick-260812-7sk), commit `ebf83005`, 2026-08-12. Offline-proven end-to-end (real resume drive, real durable store, scripted model); deliberately NOT live-proven — this defect was found because a resumed build burned 16.5M tokens, so no run was launched, resumed or gate-approved to verify it.** Original report follows. **ROOT CAUSE (proven, offline-reproduced):** `ExecutionEngine._drive_resumed_stream` (`engine.py:8274-8332`) takes **no `cancel_event` parameter and passes none** to `_execute_impl`, whose default is `None` (`engine.py:1096`). Every kernel guard reads `if cancel_event and cancel_event.is_set()`, so with `None` **all 12 short-circuit**. Separately, `run_engine._register_resume_task` (`:520-521`) and `run_commands.py:436` mint an **orphan `asyncio.Event` into `_CANCEL_EVENTS`** for exactly these runs — so `cancel_run` (`run_commands.py:335-338`) finds one, sets it, and returns `cancelled: true`. **Nobody reads it.** The false ack is a second, separate defect: the API asserts success for something it never verified. **CORRECTED FORENSICS — my first reading was wrong and the truth is worse.** I reported this run as "orphaned at `generating`". It is not: it reads **`completed`**, having run to `pipeline_complete` at 03:08:21 and billed **16,530,718 tokens total, of which 7,510,082 (45%) were billed AFTER the API returned `cancelled: true`**. **Zero `pipeline_cancelled` events exist on the run**; the entire database contains exactly ONE such event ever, and its payload shows it came from a destructive `task.cancel()`, not a cooperative Stop. **The escalation ladder is broken at every rung:** (1) UI Stop — no effect; (2) `POST /cancel` — returns 200 `cancelled:true`, no effect; (3) `SIGTERM` — ignored, because local uvicorn runs without `--timeout-graceful-shutdown` (only `docker-entrypoint.sh:35` passes it) so it waits forever on the live SSE stream and `lifespan.shutdown()` never runs (documented verbatim at `run_shutdown.py:8-13`), and `SHUTDOWN_STOP_RUNS` defaults **False** so it would not stop drivers anyway; (4) `kill -9` — stops that process, **but the next startup AUTO-RESUMES the run** (`restore_non_terminal_runs`, `main.py:203`, `run_engine.py:482+`) and drives it to completion. Measured exactly that: the replacement backend started 03:03:36 and emitted `pipeline_start` at 03:03:38, then ran the build to completion. **So restarting the backend to stop a run causes the run to finish.** **REGRESSION, not always-broken:** the gap dates to `83cfff4c` (2026-06-11), but `849e536d` / KAN-88 (2026-06-30) **removed the working destructive fallback** (`elif … current_pipeline_task.cancel()` in the old WS handler) on a false premise stated in its own comment — that `resume_run` checks the event. Phase 44's REST port then dropped the fallback entirely. **FIX (one change covers every loop):** all four resume drivers funnel through `_drive_resumed_stream`; thread the signal there via an injected hook wired in `main.py` (keeping the kernel free of `app.api`, per Ports & Adapters), make `_CANCEL_EVENTS[run_id]` the **same object** the engine reads, make the ack honest, and reuse `stop_pipeline_drivers`' bounded escalation as fallback. **Rejected hack:** unconditional `task.cancel()` — reintroduces the ISS-007 race and leaves the row non-terminal. **ISS-078's hypothesis is REFUTED as the cause** — `engine.py:2492-2499` does lack a break (real, but bounded by `_run_agent`'s terminal guard at `:3299`); ISS-084 originates upstream of every loop. **Why tests missed it:** `tests/unit/test_rest_answers_cancel.py:210-224` seeds `_CANCEL_EVENTS` itself and asserts only that the endpoint set it — it asserts the one thing that still works. No test asserts that a drive observes the event, that `pipeline_cancelled` is emitted, or that the run status flips. Fix is additive, all-default-`None`, offline-testable, needs no live run. | **Stop / cancel does not stop a building run. `POST /api/runs/{id}/cancel` returns `HTTP 200 {"ok":true,"cancelled":true}` and the run keeps generating.** Confirmed twice, independently: the owner clicked **Stop** in the UI on run `d5dbc9f2` and it did not stop; I then called the REST endpoint directly and got a success response while the event stream kept flowing — **109 new `agent_chunk` events in the following 20 seconds**, and 311 more after that. **Zero `pipeline_cancelled` events were ever emitted** for the run, so nothing downstream (UI, DB status, cost accounting) is told either. The run row stays `generating` forever. Escalation path measured: `SIGTERM` to the uvicorn process was ALSO ignored — the process stayed alive and kept driving the run; only `kill -9` stopped it. So there is currently **no working way for a user to stop a run that has entered the build task-loop**, and each un-stoppable prototype build is a 5-21M-token spend. Related but NOT the same as the known clarify-gate Stop no-op fixed in quick-260720-ec4 (that one was at a clarify/declared gate; this is mid-build). Suspected same family as the finding in the ISS-078 investigation that the dispatch loop (`engine.py:2492-2499`) does not `break` on `pipeline_cancelled` — that would explain a cancel flag being set while the loop keeps dispatching. **Do not fold into ISS-078**; that row is about resume, this is about cancel. | Found live 2026-08-12: owner clicked Stop in the UI (reported: "i tried to stop visually and it didnt stop that run"); I reproduced via `POST /api/runs/{run}/cancel` with a valid owner token — `HTTP 200 cancelled:true`, then measured `MAX(seq)` twice 20s apart (33638 → 33747, still climbing). Backend restart with SIGTERM did not stop it; `kill -9` on pid 63746 did. Final run state: status stuck at `generating`, orphaned, 0 `pipeline_cancelled` rows. **RESOLVED 2026-08-12 (FIX-227 / TEST-011, commit `ebf83005`, branch `bugfix/spec-revision-context-loss`, NOT pushed):** the signal now reaches the kernel. `_drive_resumed_stream` — the ONE funnel all four resume drivers pass through — resolves the run's cooperative Event via a new INJECTED hook (`engine._resume_cancel_event`, wired once in `main.py` beside the five existing resume hooks, so the kernel still imports no `app.api`; lint-imports 4 kept / 0 broken) and hands it to `_execute_impl`, lighting all twelve armed boundaries at once. `run_engine._resume_cancel_event` is now the ONLY resume-path mint site (get-or-create), so `_CANCEL_EVENTS[run_id]` **is** the object the engine reads — the orphan is superseded, not left alongside (INV-12), and asserted by an object-**identity** test. `cancel_run` no longer reports registry mutation as cancellation: liveness (`_is_run_live`, the driver task) is the truth condition and the answer is `accepted`/`status: "stopping"`, never an unearned `cancelled: true`. `stop_pipeline_drivers`' drain-then-`task.cancel()` body was extracted to `_drain_then_cancel` and reused by a new per-run `stop_run_driver` — one escalation policy, not two — which afterwards calls the existing `_reconcile_terminal_status` so a force-cancelled run cannot be left non-terminal. **The 4-rung ladder is closed except rung 3** (see ISS-088 for the SIGTERM half, which is operational, not engine). **Terminal-state proof, the part that makes the difference:** the offline end-to-end test asserts the cancelled resume dispatches ZERO agents, persists exactly one `pipeline_cancelled` to `run_events`, and leaves `WorkflowRun.status = "cancelled"`; a separate test then runs a fresh `restore_non_terminal_runs()` and asserts it adopts nothing. Both were seen RED by mutating the one root-cause line back, reproducing the production symptom verbatim — *"a cancelled resume kept dispatching agents: {'a': 2, 'b': 2} -> {'a': 4, 'b': 4, 'c': 2, 'd': 2}"* and *"the next boot re-adopted a run the owner paid to stop"*. Goldens **10/10** and lint-imports **4 kept / 0 broken**, both identical to `1ed94666`, no golden regenerated; `test_restart_resume.py` held at its 7 failed / 48 passed ISS-078 baseline. **Not fixed here, deliberately:** the four loops that read no cancel flag at all (`engine.py:2492` forwarding, `:3318` redo, `task_loop.py:492` validator, the fan-out family) — one variable at a time, they belong with ISS-078; and a cancel is still purely in-process (ISS-089 territory — a Stop that arrives with no live driver returns `accepted:false` honestly, but the next boot still auto-resumes the run). |
| ISS-085 | minor | frontend | **FIXED — FIX-226 / TEST-010 (quick-260812-77g), commit `e92a9ad0`, live-proven read-only on run `5ecb990f` 2026-08-12** | **Switching artifact versions does not change the spec cards — only the raw output below them.** The owner tested the new ISS-065 picker and reported: "while changing the version there, i couldnt see the older version's content". Confirmed by reading the code. `AgentDetailPanel.tsx:738-740` computes `shownOutput = viewed?.content ?? agent.output` and correctly passes it to `OutputPreviewSection` (`:869`), so the raw output DOES follow the selection. But `:840` renders `<SettledArtifactCards agent={agent} model={cardModel} />` — passing the **raw agent**, not `shownOutput` — and that component parses `agent.output` directly at `:611` (`parseSpecSections`) and `:612` (`parseSpecOverview`). The prominent "Specification · N pages" card therefore always renders the LATEST spec regardless of which version is selected, while the less prominent raw-output section changes underneath it. `AnalysisPreview content={agent.output}` (`:687`) has the same flaw. So a user selecting v1 sees the v3 page-grid and reasonably concludes the picker did nothing. **Fix shape:** thread `shownOutput` into `SettledArtifactCards` (and the analysis preview) instead of `agent`, so one selection drives every consumer — there must be exactly one source for "what am I looking at". Check `discriminateArtifact(agent.output)` at `:130` for the same problem while in there. | Reported by the owner during manual testing of FIX-222, 2026-08-12; mechanism confirmed by reading `AgentDetailPanel.tsx` (`:130`, `:611-612`, `:687`, `:738-740`, `:840`, `:869`), not inferred. A live browser repro was attempted but blocked — the run under test had been resumed and was mid-build, so the panel was unavailable; the code path is unambiguous and the reporter's observation matches it exactly. **RESOLVED 2026-08-12 (FIX-226, commit `e92a9ad0`):** `displayed` — the agent AS OF the selected version — now feeds the card model, the cards and the output section, so one selection moves the whole panel; `SettledArtifactCards` lost its `agent` prop for `output: string` and can no longer reach the latest output. `discriminateArtifact` DOES follow the selection (deliberate — the artifact's type belongs to the artifact; pinning it to the latest suppressed the card entirely whenever FIX-039 had cleared `agent.output` mid-re-run). The `(live)` reasoning card deliberately still reads `agent.output`, documented in-line. 4 new cases (TEST-010) seen RED before / GREEN after; frontend vitest failing-id set byte-identical (147 both sides, +4 passed); goldens 10/10 and lint-imports 4/0 identical to `a48965a5`. Live read-only proof on completed run `5ecb990f`: Spec Kit Analyzer `summary · v2 of 2` → `v1 of 2` repaints the Governance-checks card from "Issues Resolution & Revised Findings / Risk Mitigation Plan" to "**READY WITH CAUTION** / Findings / Risk register". Residual filed as ISS-087. |
| ISS-087 | minor | frontend | **FIXED** (quick-260812-kpb / FIX-237, 2026-08-12) — **two of this row's own claims were FALSE and are corrected here; one third was NOT a defect** | **The un-versioned halves of the agent-detail cards do not follow the artifact version picker.** The mechanism was real: `deriveArtifactCardModel` took `protoCompletedTasks` (the "Task plan · N planned" rows), `validationPassed`/`validationIssues` (the checks badge) and `dagEdges` (the handoff) from run state, so selecting an older `<tasks>` version showed that version's card TYPE with the run's CURRENT task rows. **CORRECTION 1 — the impact claim was backwards.** "The task-planner agent produces one version on most runs" is false: measured on `backend/dev.db`, `prototype-plan` has 6 run-groups and **5 are multi-version** (`prototype-specify` 5/6, `prototype-build` 5/5). The picker is visible on most prototype runs. **CORRECTION 2 — F1/F2 were mis-cited as the remedy.** Neither mentions `protoCompletedTasks` or `dagEdges`; F1 is a run-scoped aggregate on a table with no artifact/version column and F2 serves the PAGES card. Neither could ever have fixed this. **CORRECTION 3 — the row understated the defect.** `protoCompletedTasks` is the BUILD agent's *completed*-task stream, so a card labelled "planned" rendered the number *completed* — wrong **at the latest version, with no picker involved**: run `d5dbc9f2`'s plan holds 7 tasks and the card rendered "6 planned". **CLOSED AS NOT-A-DEFECT — `dagEdges`.** A DAG handoff edge is a property of the run's workflow graph and is constant within the run (one `workflow_validated` frame); selecting an older artifact does not change who consumes it. No work is owed. **FIXED (owner decision D1):** the card now parses the `<tasks>` body on screen via `parseTasks`, EXTRACTED from `TasksPreview` (`artifactPreview.tsx:80-104`) rather than written a third time (INV-12). This repairs the latest-version defect and the picker defect together. The validation badge was NOT version-threaded — it is DEAD, see ISS-116. | Identified while fixing ISS-085 (quick-260812-77g); re-investigated and MEASURED against `backend/dev.db` in quick-260812-kpb. Fix commit `54d013f9`. Verified through the shipped code on three real terminal runs: `d5dbc9f2` (DOM now reads "7 planned", was 6), `6e38b9a7` (v1 → 10, v2 → 11), `5ecb990f` (8/8, titles repaint). |
| ISS-086 | **major** | agents,engine | **FIXED — FIX-228 / TEST-012 (quick-260812-8j4), commits `70eb3636` (RED tests) + `38d0166f` (fix), branch `bugfix/spec-revision-context-loss`, NOT pushed.** Root cause was a *decision*, not a mechanism: P23 modelled a re-run as a REGENERATION and minted a fresh `:redo{N}` thread specifically to stop the model seeing its rejected output; FIX-217 hit the identical fork on the `update_specs` path and resolved it by pairing that fresh thread **with** a prior-artifact injection — **P23 shipped only the fresh-thread half of the pair.** The fix completes the pairing on the path FIX-217 did not cover, reusing FIX-217's `spec_revision_prior_artifact` seam and its renderer (one field, one block, two publishers — INV-12) rather than building a parallel one. Three near-identical `_gate_redo` consumers collapsed into one `_consume_redo`, which also closes the re-opened-gate branch's thin-copy defects (it set only the directive, so it ALSO lost `derived_from` lineage, left a duplicate `results` entry, and wrote no audit row — under-seeding `_seed_gate_reentry_attempts` and risking a colliding `:redo{N}` after a restart). The `PRIOR ARTIFACT` block moved **above** `ADDITIONAL INSTRUCTIONS` (subject first, instructions second) and its prose generalised — it read "the document the analysis report below refers to", and a redo has no report. Withheld for a **blank** redo (the FE's own "leave blank to just regenerate" — the retained P23 semantics) and for a per-task build dispatch (already carries a compacted view). Proven agent-general by parametrizing over registry-read agent ids across two pipelines (SC-001). Verified vs `742f0c6e`: goldens **10/10**, lint-imports **4 kept / 0 broken**, the first (non-redo) dispatch **byte-identical by sha256 `9f84b82e…`**, `test_restart_resume` unchanged at 7 failed / 48 passed with the SAME 7 ids. **Live Bedrock acceptance deferred** to the end-of-milestone pass (recipe in the quick task's BASELINE.md). Original report follows. | **"Request changes" (`redo`) at an agent's own gate re-runs that agent WITHOUT injecting its prior output, so the agent returns only the delta and the previous work is destroyed.** Owner asked the Task Planner to "add 2 more pages"; it came back with **one** task. Measured on run `a0d17afa-7292-4bc9-88d9-681437d315bb`: `task_list` **v1 → v2 = 12,542 → 3,495 chars (−72.1%)**, headings **7 → 1**, **0 of 7 kept**. All six real tasks were destroyed (`Task 1: HTML Shell & Navigation Chrome`, `Task 2: Dashboard Overview`, `Task 3: Feature Analytics`, `Task 4: User Cohorts`, `Task 5: System Health`, `Task 6: Settings Page`) and replaced by the single heading `Task 1: Extend Dashboard with Two Additional Pages` — i.e. the model wrote ONLY the requested change, because it could not see what it was changing. **Prompt-level proof:** the redo dispatch (`agent_input` seq 5552, 02:57:09, matching the `gate_events` `redo` row exactly) is 80,306 chars and contains **neither** `Task 2: Dashboard` **nor** `HTML Shell` — versus 80,200 chars on the first dispatch (seq 4099). The redo added **106 characters** — the instruction alone — and no prior artifact. **The contrast that localises the bug:** on the SAME run the `update_specs` path at the analyze gate behaved CORRECTLY — `spec` v1 → v2 went 53,288 → 25,988 chars but kept **12 of 12 headings (100%)** and added 2, i.e. compaction, not loss. So FIX-217's `spec_revision_prior_artifact` injection works for `update_specs` → specify, and the **`redo` path at every agent gate has the same hole FIX-217 closed for one path only**. Judging this by length alone would have mis-read it in both directions — the healthy path shrank 51% and the broken path is the one to fix. **Scope note (owner's framing, and it is correct):** re-running from a specific agent (`redo`) and re-running the specify→plan→analyze sub-pipeline (`update_specs`) are different code paths with different context assembly, and only the second was ever fixed or tested. Every per-agent iteration path needs coverage. | Found by the owner during manual testing of the "apple design website" run and reported in their own words ("i tried to change spec in the first iteration to task planner agent to add 2 more pages and it came back with only 1 task"). I confirmed it from `artifact_refs` (version-by-version char/heading diff), `gate_events` (the `redo` row at `prototype-plan`, 02:57:09) and the durable `agent_input` prompts — mechanism proven at the prompt, not inferred. |
| ISS-088 | major | infra,api | **RESOLVED 2026-08-12** (FIX-234 / TEST-018, quick-260812-hsx; commits `f87b92d9`, `d9692ec8`, `3a6b972d`, `1de7a24e`) — **PARTIAL BY DESIGN: this does NOT close the money hole.** A *production* run still resumes and finishes after a restart, because `SHUTDOWN_STOP_RUNS` stays False there deliberately, leaving in-flight runs non-terminal for the next boot's `restore_non_terminal_runs` to adopt. That is **ISS-089** (durable cancel), which the investigation judged worth more money than this row. ISS-088 makes the shutdown path REACHABLE; only ISS-089 makes a Stop SURVIVE the process. | **The local backend cannot be stopped by SIGTERM while any run is streaming, so `shutdown_run_infrastructure()` never executes locally at all — and even where it does execute it does not stop the runs.** Two independent facts, both verified 2026-08-12. (1) `docker-entrypoint.sh:35` passes `--timeout-graceful-shutdown 5`, but the local invocation does not: `ps -o command= -p 91032` → `python -m uvicorn app.main:app --host 127.0.0.1 --port 8010`. uvicorn's `timeout_graceful_shutdown` therefore defaults to `None`, and `H11Protocol.shutdown()` does NOT close a connection whose response is still streaming — so a live SSE stream never leaves `server_state.connections`, `_wait_tasks_to_complete()` spins forever, and `lifespan.shutdown()` is NEVER reached. `run_shutdown.py:8-13` documents exactly this and it is why `kill -9` was the only thing that worked on run `d5dbc9f2`. Consequence beyond the stuck stop: **no local shutdown behaviour is exercised the way production exercises it** — the Concierge drain, the pump teardown and `close_checkpointer()` have never run on a developer machine. (2) `SHUTDOWN_STOP_RUNS` defaults `False` (`config.py:215`, KAN-151 D8 §I11), so even a reachable shutdown deliberately leaves in-flight runs non-terminal for the next boot's auto-resume — meaning a deploy cannot stop a 21M-token build either. That default is a real trade (it is what keeps the Phase 45-50 resume tier working) and is a product decision, not a bug; the missing local flag is not. **Fix shape:** add `--timeout-graceful-shutdown 5` to the documented local run command so dev and prod exercise the same path; then re-open the `SHUTDOWN_STOP_RUNS` default as a product question with cost, not as an engineering default. | Confirmed while fixing ISS-084 (quick-260812-7sk): `ps` on the live pid 91032, `docker-entrypoint.sh:35`, `config.py:203-215` and the `run_shutdown.py` module docstring — read, not inferred. Filed rather than fixed because it changes how the process is launched and how a deploy behaves, which is neither the engine defect ISS-084 named nor safe to bundle with it. **RESOLUTION.** Both facts in this row were verified exact at HEAD and the fix was demonstrated end-to-end on a throwaway server (port 8099; the owner's 8010 backend was never signalled). (1) Reachability: `--timeout-graceful-shutdown 5` added to the ten developer-facing documented commands and to both out-of-repo memory notes (`dev-runtime.md`, `local-run-bedrock.md`) — the ones agents actually read, so the fix takes effect in practice. Measured: no flag → alive at **31.25s** with ONLY `startup_complete` written, SIGKILL required; with the flag → exit in **5.82s** with the shutdown body completing. `--reload` was DROPPED rather than carried over (untreated it leaves TWO stuck processes — parent 96488 and child both alive after 20s — it masks crashes, and `watchfiles` is absent so it degrades to `StatReload` polling). Port kept at **8000**: the repo's committed default in `frontend/src/lib/env.ts:37`, `env-templates/.env.frontend:7` and `frontend/e2e/fixtures/live.ts:13`; 8010 is a per-machine override. Deliberately NOT edited: `docs/LOCALSTACK_SMOKE_REPORT.md` (a dated report), `frontend/src/data/skills.ts` + `skills/ecc/flox-environments` (seeded user-facing product content), `frontend/src/app/test-preview/page.tsx` (a preview fixture), and the `.planning/phases/**` / `.knowledge` point-in-time artifacts. (2) The `SHUTDOWN_STOP_RUNS` product question was decided as **environment-differentiated** (owner decision D3): ON for `ENV=development`, OFF for every other ENV, with an explicit env var winning in both directions. Two prerequisites landed FIRST, in the mandated order — the double `close_checkpointer()` (`main.py:302-307`, deleted; step 4 in `run_shutdown.py` is now the single site its docstring always claimed, INV-12), and the under-counting budget comment (it said "under 25s" while omitting both cancel-and-wait escalations AND the pump drain; the real figures are 24s off / 30s on against a 30s `stop_grace_period`, now asserted by a test that PARSES both numbers). (3) **The blocking open question is settled and the prior hypothesis is REVERSED:** `asyncio.run`'s loop teardown does NOT already mark in-flight runs `cancelled`. `capture_signals()` restores the original signal disposition and re-raises the captured signal INSIDE `serve()` (`uvicorn/server.py:326-330`), so under SIGTERM the process dies before `Runner.close()` → `_cancel_all_tasks` can run. Proven three ways: the driver markers are absent under SIGTERM and present under SIGINT; a no-uvicorn control shows plain asyncio DOES run the handler; and a marker stamped after `server.run()` returns never appears. Under SIGINT it does fire — see **ISS-103**. Goldens 10 passed / 0 moved, lint 4 kept / 0 broken, the 11 pre-existing reds unmoved, all vs `9df9c1c8`. |
| ISS-089 | major | api,engine | **FIXED — FIX-243 / TEST-027 (quick-260812-tni), commit `8d84b1a6`, 2026-08-12.** Option A shipped; the row's own Option B REJECTED — see the resolution note | **A cancel is purely in-process, so a Stop that arrives when no driver is live is lost — and the next boot auto-resumes the run the owner just tried to stop.** After FIX-227, `POST /cancel` on a run with no in-process driver answers honestly (`accepted: false`, `status: "not_running"`) instead of lying — but the run is still `running`/`generating` in `workflow_runs`, still inside `restore_non_terminal_runs`' `NON_TERMINAL` set (`engine.py:5710-5713`), and the next startup adopts it and drives it to completion at the owner's expense. This is exactly the second half of what happened to `d5dbc9f2`: the process was killed at 03:03:36 and the replacement backend emitted `pipeline_start` two seconds later and billed 7.5M further tokens. The window is real in normal operation, not only after a `kill -9`: `SHUTDOWN_STOP_RUNS=False` means every deploy leaves its in-flight runs non-terminal by design, so a Stop pressed during a rolling restart lands on nothing. **Fix shape (product decision, not technical):** make the cancel DURABLE — a `cancel_requested` marker (an additive `run_events` row or a `workflow_runs` column) written by the endpoint regardless of liveness, and checked by `restore_non_terminal_runs` before it adopts a run, so the boot classifier fails the run terminal instead of resuming it. Note the ordering constraint: the check must run before `_stamp_resume_marker`, and the resulting status must be `cancelled` (terminal, outside `NON_TERMINAL`) rather than `failed`. | Identified while fixing ISS-084 (quick-260812-7sk) and recorded rather than silently left: FIX-227 makes every in-process cancel real and terminal, and makes the no-driver case honest, but it does not and cannot make an in-memory `asyncio.Event` survive the process that holds it. The analysis carried it as Open Question 1 (`ISS-084-ANALYSIS.md` §11). <br><br>**RESOLVED — and the row's own proposed fix was the worse of the two.** Two corrections to the row as filed. (1) **The `NON_TERMINAL` citation `engine.py:5710-5713` was wrong**, as were the three other recorded values (`:5263`, `:5838-5841`, `:5899-5902`); at `2df5324b` the tuple was an inline literal at **`engine.py:5921`** — a fifth value in one week. It is now ONE module constant, `NON_TERMINAL_RUN_STATUSES`, imported by the endpoint and by `scripts/cutover_legacy_runs.py` (which held a second copy), pinned by an AST source guard that catches a copy under any name. (2) **Option B — the `cancel_requested` marker checked inside `restore_non_terminal_runs` — was rejected.** It needs a migration or a new event type; it edits the resume tier's most delicate three-branch classifier; it **misses branch (a)** (`waiting_for_user` spawns `_rearm_gate_run` without ever calling `_stamp_resume_marker`, so a marker checked before the stamp misses every gate-parked run — the row's own ordering constraint is true but insufficient); and it **leaves the row lying** `running` to history, analytics, `/resume` eligibility and the UI until the next boot, possibly forever. **Shipped instead: write the terminal status at cancel time.** One function. `cancel_run`'s not-live branch reads the row; TERMINAL ⇒ the pre-existing `not_running` ack, byte-identical, no write; NON-TERMINAL ⇒ a durable `pipeline_cancelled` row via the collision-safe `ScopedStore.append_event_at_or_after` (best-effort audit/replay) + `status="cancelled"` + `completed_at` via `_persist_resume_status` (**authoritative** — a failure answers 500 `cancel_not_persisted`, it does NOT inherit `_reconcile_terminal_status`'s degrade) + `accepted:true, cancelled:true, status:"cancelled"`. **No migration, no new table/column/event type, and no change to `restore_non_terminal_runs` at all** — `cancelled` is already outside the non-terminal set and `POST /resume` already accepts it, so the boot scan consults the decision through the filter it already has and the run moves from AUTOMATIC to EXPLICIT, owner-authenticated resume. **Proof is the boot scan's task count, not prose:** at `2df5324b` the same test observed `ExecutionEngine.restore_non_terminal_runs.<locals>._admitted_resume` spawned for a run whose Stop had just answered `not_running`. **Cost framing corrected:** the register's "5-21M tokens" understates the tokens and overstates the drama — measured ceiling on this corpus is **37.3M tokens / $7.07**, the `d5dbc9f2` incident cost **$3.58** with **~$1.61 (45%) billed after the cancel**, and all 14 metered runs together total **$17.75**. It is a **correctness-and-trust defect first, a cost defect second** on a Haiku workload — about an order of magnitude worse on an Opus-tier model, which is what the fix insures against. **SCOPE — single-process only (→ ISS-134).** `_is_run_live` is process-local; the deployment is single-process today (`exec uvicorn`, no `--workers`, no replicas) so "not live here" == "not live anywhere". Under the locked ECS Fargate to-be a Stop landing on instance B would mark a row cancelled while instance A kept billing. Do not later assume this fix is multi-instance-safe. |
| ISS-090 | minor | agents,engine | OPEN (recorded during FIX-228 / ISS-086; deliberately out of that fix's scope) | **`redoable` has no eligibility fence — unlike `update_specs_eligible` it is an inline `True` literal at all three gate call sites, so ANY agent named in a per-run `gate_agent_ids` gets a Redo button with no server-side rule.** `update_specs_eligible` is computed by the structural predicate `_update_specs_eligible(artifact_kind, ectx)` (engine.py), published on `review_gate_ready`, and **binding** — ISS-053 tracks the remaining ingress enforcement. `redoable=True` has no equivalent: it is hard-coded at `engine.py` gate call sites (the RESUME-17 re-entry, the post-revision re-open, and the live post-stream gate). Consequences are bounded but real: a run that gates a **task-loop** or **tool-bearing** agent advertises "Request changes" on a dispatch whose semantics differ (FIX-228 handles the task-loop case defensively in `_consume_redo` by withholding the injected subject when `build_task_number` is set — but that is the *engine* being careful, not the *affordance* being correct). The symmetric fix is a `_redoable(artifact_kind, spec, ectx)` predicate mirroring `_update_specs_eligible`, published and enforced at the same three sites. Not required for ISS-086 and not attempted: it changes which buttons users see, so it needs its own decision. | Found while wiring FIX-228's three redo consumers into one shared `_consume_redo`; the asymmetry with `_update_specs_eligible` (one structural predicate vs three inline literals) is visible at the call sites. Also flagged in the ISS-086 root-cause analysis §6/§14 as an open question. |
| ISS-091 | major | engine | **FIXED — FIX-232 / TEST-016 (quick-260812-g1c), commits `4f058f9a` (RED tests) + `3f37bea4` (primary) + `6df1f1a0` (secondary), 2026-08-12. Offline-proven; deliberately NOT live-proven (one build is 5–21M tokens).** Reproduced byte-identically at `c0bbb6e2`; after the fix the stream ends on `pipeline_cancelled` with **0** trailing events, **0** `wave_runs` and **0** `subagent_runs`. **The row's own SEVERITY ANALYSIS was wrong in an important way, and the correction is the whole point of this fix.** The row's COST line ("worker + merge cost of every wave after a rejected gate") is **materially overstated — the real figure is ZERO model tokens**: every post-rejection worker short-circuits on `_run_agent`'s terminal guard (`:3329`), `call_log {}`, `subagent_runs.tokens` all `None`. What the row MISSED is far worse than what it claimed: the rejection **poisons the durable resume record**. Each no-op worker is still written `subagent_runs.status='complete'` and each wave `wave_runs.status='completed'`, and `wave_scheduler.py:251-256` skips completed waves on resume — so reject → restart → resume skipped **both** waves, produced **zero** files and recorded the run `completed`. **Silent, permanent loss of a fan-out run's entire output**, on the live user-facing Phase-51 fan-out feature. The row's "WHY IT IS NOT WORSE" reasoning is exactly backwards: the terminal guard is the mechanism that turns a wasted run into an unrecoverable one. **Also REFUTED: the row's "SECOND, RELATED GAP."** `wave_scheduler` needing a `cancel_event` check is wrong — `run_fanout` already owns that boundary (`fanout.py:231-233 _check_cancel`, called pre-wave / between sequential workers / pre-merge, plus `_await_workers_with_cancel`), and a probe proves Stop mid-wave IS honoured at wave granularity today. `fanout_batch.py` has no `is_set()` either, which the row did not name. **THE FIX** is the row's suggested shape with one correction: `return`, **not** `break` — `break` falls through to the Step-5 terminal block which still emits `pipeline_complete`. It is the exact sibling of the declared-gate handler **WR-03** (`engine.py:2454-2479`), whose own comment describes this defect verbatim: the declared path stopped the run, the inline path did not. Secondary (defence-in-depth): `fanout.py::_check_cancel` + a new `KernelServices.is_run_terminal()` — deliberately **not** in `wave_scheduler.py`, which is a capability that may not import the kernel (import-linter) and would have been a second cancel implementation (INV-12) still missing `fanout_batch` and `spawn_subagents`. **Rejected shortcuts, recorded so they are not re-proposed:** making the gate call `cancel_event.set()` (inverts app/kernel ownership of `_CANCEL_EVENTS`, flips the `except asyncio.CancelledError` re-raise decision FIX-227 depends on, double-emits); guarding Step 5 on terminal state only (fixes the event, not the work — the data loss survives). **Blast radius re-verified independently:** all four `pipeline_cancelled` emitters reachable by the dispatch loop (`:3413`, `:3520`, `:4458`, `:5502`) already `return`; there is no producer outside `engine.py`; fan-out workers cannot leak one (`fanout.py::_run_one` reads only `agent_complete` and drops every other child event); revision runs nest via the API layer (`run_commands.py:2560`), never inside a parent dispatch loop. **No emitter that reaches this loop means "continue."** Goldens 10 passed with **0 golden files modified**, lint-imports 4 kept / 0 broken, `test_restart_resume.py` 60 → **64 passed**, the 11 pre-existing reds unmoved with identical ids — all vs `c0bbb6e2`. **Named limitation:** the goldens compile `gate_agent_ids=[]` (`_scripted_model.py:649`) so **not one golden contains a gate event** — they cannot detect this change and prove only that nothing else moved; the 7 new tests are the only real oracle. | **Rejecting at a review gate does not STOP the pipeline: every remaining step still runs and the run ends on `pipeline_complete`.** Measured at `99fcf4a2` — i.e. **WITH FIX-227 and FIX-228 already landed, so FIX-227 does NOT cover this.** Offline probe, a normal (non-resume) launch with an inline `Human_Gate` that rejects: `BEFORE cancel: [... agent_complete, review_gate_ready]` then `AFTER cancel: [wave_started, subagent_spawned x2, subagent_result x2, merge_started, merge_completed, wave_completed, wave_started, subagent_spawned x2, subagent_result x2, merge_started, merge_completed, wave_completed, pipeline_complete]` — the user rejects, the engine says `pipeline_cancelled`, and both remaining waves run to completion. **ROOT CAUSE (proven, two parts).** (a) `_run_agent` yields `pipeline_cancelled` and `return`s (`engine.py:4448-4452` live gate, `:3403` / `:3510` re-entry + post-revision), but `return` only ends **that step's generator**. The consuming loop (`engine.py:2513-2520`) inspects only `agent_error` — it has no notion of a terminal event arriving through the stream — so the outer `for i, spec in enumerate(ordered_agents)` simply advances to the next step and falls through to the sole `pipeline_complete` emitter. (b) The step-boundary cancel check that WOULD stop it (`engine.py:2371`) reads `cancel_event`, and **a gate rejection sets no `cancel_event` at all** — which is precisely why FIX-227, whose whole mechanism is threading a real `cancel_event` into the resume path, is orthogonal to this. **WHY IT IS NOT WORSE:** `_run_agent` opens with a terminal-state guard (`engine.py:3319-3320`, `self._state_machine.get_state(...)`), and the rejection transitions the machine to `cancelled` — so every step routed through `_run_agent` (`single_shot`, and `task_loop`, which calls `_run_agent` per task) short-circuits. **`wave_scheduler` does not route through `_run_agent` and has ZERO cancel/terminal checks** (`grep 'is_set()' wave_scheduler.py` -> 0 hits), which is exactly the family the probe shows running. So the exposure is **fan-out workflows**, not the `od_prototype` build. **SECOND, RELATED GAP in the same family:** `wave_scheduler.py` has no `cancel_event` check either, so a **Stop** during an in-flight wave is only observed at the next step boundary — the residual FIX-227 explicitly deferred ("the four loops that read no cancel flag at all ... they belong with ISS-078"). Of those four, `engine.py`'s outer per-step loop (`:2371`) and `task_loop.py` (`:275`, `:376`) **do** check; **`wave_scheduler.py` is the one that genuinely has none.** **NOT an ISS-078 regression and older than all of it** — the prior investigation reproduced a byte-identical tail at `3e80fcc4` (2026-07-19, the last-known-green commit). *That cross-commit reproduction is cited from the investigation, not re-run by me; everything else in this row I measured at `99fcf4a2`.* **COST — stated honestly, not inflated.** Worker + merge cost of every wave after a rejected gate, on fan-out workflows only. **NOT** the 5-21M `od_prototype` figure: `task_loop` is protected by the `_run_agent` terminal guard. I did **not** measure this in tokens (the probe uses a scripted model, and the probe's own workers recorded no model calls and wrote no files — the fan-out machinery ran, but I cannot claim from it what a live wave would spend). **RELATION TO FIX-229:** FIX-229 stops the RECORD lying (the run is now `cancelled`, not `completed`). It does not and cannot stop the work — that is this issue. **SUGGESTED FIX (not attempted):** give the dispatch loop a terminal-event observation symmetric to its existing `agent_error` observation — break on `pipeline_cancelled` — and/or give `wave_scheduler` the terminal-state + `cancel_event` checks every other strategy already has. Blast radius is every workflow's termination path; needs its own task with golden re-verification. | Found + measured 2026-08-12 during quick-260812-97f (ISS-078 / FIX-229). Probe: offline `_ResumeHarness` launch, inline `Human_Gate`, rejecting gate spy; event stream captured either side of `pipeline_cancelled`. Predicted by the ISS-078 investigation report §4 and by FIX-227's own deferred-items note. |
| ISS-092 | major | product | **CLOSED 2026-08-12** (FIX-233 / TEST-017, quick-260812-gsf; commits `47c7672f` + `7b79e426`) — **two claims in this row were REFUTED on measurement and the fix design turned on them: (a) the payload is NOT `agent_chunk` (max row 169 chars) but 18 `agent_input` rows totalling 7,290,638 chars (largest 479,605), so a chunk filter would have fixed almost nothing; (b) there were THREE unbounded tools, not one — `list_refs()` projected artifact bodies inline (5,804,067 chars ≈ 1.45M tok in a single call) and `get_ref()` returned bodies up to 389,651. The '~60% of a $0.34 run's input' figure could not be reproduced under any pairing; the measured reality is worse — 144% and 151% of the runs' entire recorded input. `run_commands.py:1442` is the ctx construction; the `converse` call is at `:1503`. The handoff half was deliberately SPLIT OUT (now ISS-100) rather than bundled.** Resolution: `read_events`/`list_refs`/`get_ref` DELETED (INV-12) and replaced by seven bounded tools closed over `(scoped_store, run_id)`; one new bounded `ScopedStore.read_events_of_types` puts the cap in SQL while leaving the 18-caller replay primitive untouched; `exclude_builtin_tools=True` removes the filesystem WRITE surface the model was holding; `get_artifact` closes a cross-run scope escape; multi-turn preserved by reusing `context_provider:conversation` + `compaction:chat_history`; and one idempotent durable `chat_usage` row per turn makes the spend exist, reported as a SEPARATE line, never estimated when unobserved. **Measured through the shipped tools on real `dev.db` rows: 2,494,799 → 4,710 est. tokens (530×); no fully tool-saturated turn in the corpus exceeds 4,785.** | **A single Concierge chat question can feed the model up to 2.3M tokens, and not one of them is counted or capped.** Two compounding facts, both verified 2026-08-12. (1) **Uncounted:** `app/agents/chat/concierge.py:365` drains its runner stream with `if event["type"] != "chunk": continue`, so every `usage` event is discarded. Nothing anywhere records a Concierge token — its spend is not merely under-reported, it is *unmeasurable from the data*. (2) **Unbounded input:** `concierge.py:434` calls `scoped_store.read_events(run_id, 0)` and `agents/authz.py:315-327` applies **no LIMIT and no type filter**, projecting every row including 200k+ `agent_chunk` payloads. Measured payload available at each recorded question (6 conversations, 15 `chat_message` events in `dev.db`): `fa66227a`@1322 ≈53.6k tok · `caa5d175`@10519 ≈385.7k · `a7dba362`@7721 ≈267.3k · **`0a27b397`@12123 ≈2,306,800 tok**. On a $0.34 `user_stories` run a single 205k-token Concierge read is ~60% of the run's entire input. The replies are coherent multi-turn answers, so the calls succeeded — what the model actually received is simply unknowable. Same class, same row: the handoff pipeline is **100% invisible** — `handoff_pipeline.py:403`/`:494`/`:514` pass no `usage_sink` (the seams exist, unwired) and `handoff_sessions` has no token column at all; `handoff/coder.py:235` also drops its usage. **Why this was NOT bundled into FIX-230:** `converse` runs from a REST handler (`run_commands.py:1442`) outside `execute()`, often after the run is terminal, so `aux_token_usage` is out of scope AND out of lifetime. Wiring it needs an answer to a product question — *should user-initiated chat spend appear in the run's headline cost, or as a separate line?* — and the handoff half needs a cost surface that does not exist. Both are features, not bug fixes. | Split out of ISS-033 during quick-260812-9tq, whose part A (FIX-230) closed the two measurable, safely-foldable sites. Evidence: read-only queries against the live `backend/dev.db` + the cited file:line. **The rejected shortcut, recorded so it is not re-proposed:** estimating Concierge spend from `len(chat_reply)` (or the fix-loop from a fudge factor) puts a fabricated number where a real one is one event-branch away — and would make ISS-034's "saved $Y" a fiction built on a fiction. If a token was not observed it must be reported as unmeasured, never invented. Independent of accounting, the uncapped `read_events` is also a latent context-blowout and latency risk. |
| ISS-093 | minor | test-infra | **OPEN** (pre-existing; found while verifying FIX-230, deliberately not fixed there) | **3 stale clarify-round tests in `backend/tests/unit/test_execution_engine.py` assert an event vocabulary the product no longer emits.** `test_clarify_engine_asks_once_then_limit_reached` (:152), `test_clarify_engine_rounds_knob_allows_multi_round` (:186) and `test_clarify_engine_empty_submit_proceeds_after_one_round` (:309) expect `clarification_limit_reached`; the engine now emits `['questionnaire_ready', 'questionnaire_complete']`. Reads as fallout from **ISS-027**'s "Option A force-proceed" change, which replaced the limit-reached path — the tests were not reconciled with it. Classify before fixing: this is *changed behaviour* (reconcile the assertions to the current vocabulary), **not** selector drift, and the tests must not simply be deleted. | **Verified pre-existing, with a SHA:** re-measured at `d24c576a` in a detached worktree containing none of FIX-230's changes — the identical 3 fail there (`3 failed / 11 passed`), and the same 3 fail after (`3 failed / 78 passed` across the wider selection). Not previously registered as its own row. Left unfixed because reconciling them means deciding what the clarify round-limit contract now IS, which is ISS-027's territory, not a token-accounting fix. |
| ISS-094 | minor | test-infra | **OPEN** (pre-existing; found while baselining ISS-074, deliberately not folded in) | **3 reds in `tests/agents/test_gates.py` — hand-rolled context doubles drifted from the real engine contract on an ATTRIBUTE, so no signature guard can catch them.** Measured offline at `fab9b646` and again after FIX-231: **3 failed / 39 passed**, unchanged, in 0.7 s — not flaky, not environment-gated (no Postgres/Bedrock/Chromium gate fires). Failing ids: `test_raising_hitl_gate_blocks_and_stops_gate_evaluation`, `test_engine_sentinel_carries_human_gate_edit_detail`, `test_apply_declared_gate_edit_rewrites_upstream_artifact_and_result`. Two distinct symptoms: `engine.py:6955` in `_latest_typed_ref_id` does `for ref in ectx.artifacts.tree(ectx.run_id)` → **`AttributeError: '_Ectx' object has no attribute 'artifacts'`** (the file's local `_Ectx` stub at `test_gates.py:939` sets only `scoped_store`, while `_apply_declared_gate_edit` → `_latest_typed_ref_id` now reads `ectx.artifacts`), and two × **`AttributeError: '_Ctx' object has no attribute 'gate_agent_ids'`**. **Same defect family as ISS-074** — a hand-rolled test double that silently fell behind the real contract — **but the drifting surface is an attribute, not a parameter, so FIX-231's `test_gate_stub_signature_drift.py` guard does NOT and cannot catch it.** An analogous attribute-conformance guard over `_Ectx`/`_Ctx` doubles is the family-ending move, and is the reason this is worth a row rather than a one-line stub patch. **Do NOT just add the missing attributes without checking what the engine now expects them to contain** — a double that returns an empty `artifacts` tree would make these tests green while asserting nothing. Prior art: acknowledged in prose inside the **ISS-078** row (*"Also observed, unregistered: 3 pre-existing reds in `tests/agents/test_gates.py` … present at `99fcf4a2`, unrelated to ISS-078, in no register row I could find"*) but never given a row of its own — this is that row. | Observed 2026-08-12 by quick-260812-fbk while establishing the ISS-074 baseline; independently present at `99fcf4a2` per ISS-078. FIX-231 widened `_FakeReviewEngine._run_review_gate` in this same file with `**kwargs` (a different, signature-level hazard) and the 3 reds were re-measured unchanged at 3 failed / 39 passed, confirming they are untouched by and unrelated to that change. |
| ISS-095 | major | test-infra,engine | **OPEN** (pre-existing; symptom mismatch found while baselining ISS-074) | **3 reds in `tests/agents/test_declared_gate_streaming.py`, and the register's "environmental" label for them looks WRONG — a real defect may be hiding behind it.** `.planning/IMPLEMENTATION-REGISTER.md:4075` records these as *"`declared_gate_streaming` 3 env-reds (sqlite FK, Postgres-gated)"*. **The observed failures are not sqlite FK errors.** Measured offline at `fab9b646` and again after FIX-231: **3 failed**, unchanged, in ~17 s. `:203` `AssertionError: no review_gate_ready was ever received`; `:264` `AssertionError: precondition: the static inline gate set changed: {'prototype-analyze', 'prototype-specify', 'prototype-plan'}`; `:474` `AssertionError: rejection never produced pipeline_cancelled; got [... 'pipeline_complete']`. These are assertions that a **declared human gate never fired** and that a **gate rejection did not cancel the run** — product-shaped claims, not environment noise. (sqlite FK warnings DO appear in this suite's stderr, but they are the engine's documented best-effort `PERSIST-02`/`PERSIST-03` degrade path and are logged as warnings on passing tests too, so they are not what fails these three.) The third symptom is plausibly the same defect as **ISS-091** (gate rejection does not stop the pipeline); the first two may be a declared-gate arming regression. **NOT root-caused here — deliberately.** This row exists because a mislabelled "environmental" red is how a real defect stays invisible, which is exactly the failure mode that let ISS-074 sit for 43 days. **First action for whoever takes this: correct or confirm the IMPLEMENTATION-REGISTER:4075 label, then re-baseline at a pre-regression commit to date it.** Note this file deliberately does NOT monkeypatch `_run_review_gate` (see its docstring at line 51), so it is not ISS-074 and was not fixed by FIX-231. | Observed 2026-08-12 by quick-260812-fbk while establishing the ISS-074 baseline; re-measured unchanged after FIX-231. |
| ISS-096 | trivial | test-infra | **OPEN** (pre-existing; found while sweeping `live_harness` consumers) | **Stale prompt-contract pin: `tests/agents/test_prompt_contracts.py::test_od_ppt_validator_deck_reemission_contract` asserts a sentence that is no longer in the agent's prompt.** `test_prompt_contracts.py:63` asserts `"exactly ONE <artifact>" in body`, where `body` is `load_agent_spec("od-ppt-validator").prompt_body`. That substring is absent from the current `backend/agents/prompts/od-ppt-validator/AGENT.md`. **Proven pre-existing:** reproduced identically at `fab9b646` in a clean detached worktree, so it is not a FIX-231 regression. The rest of that file is green (44 passed / 1 skipped alongside `test_live_contract.py` + `test_phase3_token_delta_live.py`). **Decide which side is stale before editing either** — this file is a deliberate durable pin (Phase 15-02) whose whole job is to fail when a prompt contract silently changes, so the correct fix may be to restore the prompt line rather than relax the pin. The `od-ppt-validator` output contract has been hardened three times (FIX-001, FIX-014, FIX-109) precisely because losing it makes the live deck render as raw QA checklist text, so treat a missing contract sentence as a possible product regression until the AGENT.md history says otherwise. | Observed 2026-08-12 by quick-260812-fbk while running every consumer of the changed `live_harness.py` to bound FIX-231's blast radius; confirmed pre-existing at `fab9b646` via `git worktree add --detach`. |
| ISS-097 | **critical** (RAISED from major 2026-08-12 — see below) | engine | **FIXED** (FIX-242 / TEST-026, quick-260812-syf, commit 91a1a1fa, 2026-08-12) | **Gating a fan-out step's agent parks the run at a review gate NOBODY CAN SEE, DISCOVER OR RESOLVE.** `_should_gate` (`engine.py:4908-4925`, predicate at `:4922-4925`) is a per-INVOCATION predicate on `spec.id`, but `gate_agent_ids` is a per-STEP selection — the UI's own words are "Checked agents pause the pipeline for your review after they finish" (`ReviewGatesSection.tsx:128-129`). Those readings were identical for every workflow that existed when the selector shipped, because every step invoked its agent exactly once. Fan-out (Phase 11) broke that equivalence — `fanout.py:95-96` resolves `agent: self` to `step.agent_id`, so every worker runs the SAME `spec.id` as its parent step and every one independently reaches `_should_gate → True`. **The row as originally filed predicted "N simultaneous review gates, each awaiting a human". That is WRONG, in the direction that matters — it is worse.** Every worker's `review_gate_ready` is consumed and DISCARDED by `fanout.py:414-429`, whose loop body reads only `agent_complete` for token accounting and forwards nothing. So **zero** gate frames reach the SSE stream, **zero** rows reach durable `run_events`, `derive_open_gate` (`gate_pendency.py:68-108`) returns `(None, None)`, and the run stops dead at a HITL pause with `workflow_runs.status` reading `waiting_for_user` indefinitely. All N workers share ONE gate key `f"{run_id}:{agent_id}"` (`engine.py:5705`), so they are N arms of one slot, not N gates. **MEASURED offline at `eb12bc7e`** (`sample_wave`, single-wave plan, 4 workers, `gate_agent_ids=["sample-wave-worker"]`): `_should_gate` True 5/5, `_run_review_gate` entered **4×**, **1** distinct gate key, **0** `review_gate_ready` on the wire, **0** in `run_events`, outcome **HUNG** (20 s timeout). Control with `[]` completes cleanly. The same hang reproduces on the builder-composed fan-out fixture. **The gate is resolvable but undiscoverable** — `store.review_event_pending(gate_key)` returns True and `_gate_is_pending` would accept a resolution, so a user who GUESSES the predictable key can free the run by API. That is an expert escape hatch, not a product behaviour. It does not compound across restarts (`restore_non_terminal_runs` needs `derive_open_gate` to return a gate before re-arming, so the run parks harmlessly) but it leaves a permanently orphaned run row plus every worker's workspace allocated and every `subagent_runs` row `running` forever, because `run_fanout`'s `finally` teardown never runs. **SEVERITY RAISED major → critical**: an invisible, unresolvable hang on a shipped user-facing feature. Reachability is real, not theoretical — the gate picker (`ReviewGatesSection.tsx:131`) lists EVERY pipeline agent with no filter, the composer's fan-out toggle (`CanvasConfigRail.tsx:284-295`) is one click away in the SAME launch UI, and a saved workflow replays `gateAgentIds` on every subsequent launch (`DashboardLayout.tsx:1227-1228`), so it is sticky rather than one-off. **FIXED by FIX-242** with one keyword-only invocation-scope flag `invocation_gated`, threaded from the two kernel sites that create non-step invocations (`run_worker`, `run_merge_agent`) through `run_agent` into `_run_agent`, folded into the EXISTING predicate in place (INV-12). Default `True` ⇒ every step-shaped invocation byte-unchanged. **Three designs were evaluated and REJECTED** — do not retry them: (a) making `_should_gate` read `step.gates` CANNOT work, because `kernel_services.py:992-993` reuses the PARENT step object, gates included, whenever the worker is `agent: self` with no isolated workspace; (b) parking the flag on `ExecutionContext` races, because N workers share ONE `ectx` under `asyncio.gather`; (c) renaming worker agent ids breaks `_spec_for`, `allowed_workers`, fragment provenance and the wave resume cursor. **Residual, deliberately NOT folded:** `engine.py:2470` still passes `inline_gated=self._should_gate(spec, ectx)` at the step boundary, so a fan-out step that BOTH declares `gates:[human]` AND has its agent ticked now dedupes its declared gate against an inline gate that no longer fires — filed as **ISS-131**. Pre-fix that same combination hung; post-fix it silently has no gate. No manifest declares gates on a fan-out step today (verified across `agents/workflows/`); it is reachable only through composed selections. | Found 2026-08-12 during quick-260812-g1c while enumerating the ISS-091 blast radius; nominated "Q2" by the ISS-091 investigation and deliberately filed rather than folded. **All four file:line citations in the original row had DRIFTED and are corrected above** (worker step view `kernel_services.py:971-984`→`:995-1008` with `gates=[]` at `:1001`; `_should_gate` `engine.py:4861-4864`→`:4908-4925`). The original row's suggested first step — "count `review_gate_ready` events; 1 is correct, N is the bug" — would have MEASURED ZERO and read as a pass, because the events never reach the stream; the working assertion counts `_run_review_gate` ENTRIES and asserts the run terminates. **Method trap that nearly buried this twice:** `tests/agents/test_restart_resume.py:365` (`make_engine`) assigns an empty async generator to `engine._run_review_gate` as an INSTANCE attribute, silently shadowing the real bound method; the first investigation run showed zero gates and nearly filed "not reachable". `del engine._run_review_gate` is what makes the defect visible — measured: without the `del` the very same test passes GREEN in 1.07 s against a live 4-arm hang. Any future gate assertion on that harness must un-shadow first. |
| ISS-098 | trivial | engine | **OPEN** (found during quick-260812-g1c / FIX-232; cosmetic, self-healing on resume) | **A mid-wave `CancelledError` leaves its `wave_runs` row stuck at `running` forever.** `wave_scheduler.py:340` catches `except Exception:` to flip the in-flight wave's row terminal, but `asyncio.CancelledError` has inherited from `BaseException` since Python 3.8, so it is not caught and the row is never updated. Reachable on every Stop-during-a-wave (probe-confirmed that Stop does raise `CancelledError` through `run_fanout`). **Why it is only trivial:** WR-01's stale-row sweep (`wave_scheduler.py:304-313`) flips such rows `superseded` on the next resume, so the durable record self-corrects and no resume decision is made on the stale value. The visible cost is a `wave_runs` row reading `running` for a run that is plainly terminal — misleading to anyone reading the table directly. Fix is one token: `except BaseException:` (re-raising) or an explicit `except asyncio.CancelledError:` arm beside the existing one. | Found 2026-08-12 during quick-260812-g1c; carried over as "N5/Q4" from the ISS-091 investigation, which reached it via probe 3 plus a code read. |
| ISS-099 | minor | engine | **OPEN** (found during quick-260812-g1c / FIX-232; DORMANT — no shipped manifest can reach it) | **A retry-enabled step records a CANCELLED step as reusable.** When `step.retry.max_attempts > 0`, `_dispatch_step_with_retry` (`engine.py:7236-7274`) runs the strategy inside an attempt loop and, on normal completion of `strategy.run`, unconditionally yields `step_completed` carrying the step's `input_hash`. A gate rejection ends `strategy.run` normally (the producer yields `pipeline_cancelled` then returns), so the success branch fires anyway and the stream carries `pipeline_cancelled` followed by `step_completed`. That `step_completed` is exactly what `_find_reused_completion` matches on a later restart, so a step the user REJECTED would be skipped as already-done — the same "durable record of work that did not happen" family as ISS-091, one layer down. **Dormant, not theoretical:** `grep` finds no `retry:` key in any manifest under `agents/workflows/`, so `max_attempts` is 0 everywhere and the gate-TRUE branch never runs today. It is reachable in principle through `agents/workflows/selections.py`, which accepts a user-supplied `retry: {max_attempts: N}` on a composed workflow. FIX-232 deliberately did not fold this in: the dispatch-loop fix already stops the run at the step boundary, and suppressing events mid-stream is a different and riskier change than observing them. Suggested fix: do not yield `step_completed` when the attempt ended on a terminal event. | Found 2026-08-12 during quick-260812-g1c while reasoning about how the new terminal observation interacts with the retry wrapper; NOT in the ISS-091 investigation report — surfaced by reading `_dispatch_step_with_retry` before writing the fix. |
| ISS-100 | major | cost | **OPEN** (split out of ISS-092 during quick-260812-gsf — deliberately NOT bundled) | **The handoff pipeline's model spend is 100% invisible: there is nowhere to even put the number.** Three call sites construct/call agents that ACCEPT a `usage_sink` and are given none — `app/services/handoff_pipeline.py:403` (`classify_task`, sink at `classifier.py:33`), `:494` (`TestAgent()`, sink at `test_agent.py:109`), `:514` (`ComplianceAgent()`, sink at `compliance_agent.py:121`) — and `app/agents/handoff/coder.py:235` drains `astream_events` with no `usage` branch, exactly the defect FIX-233 fixed in the Concierge. `handoff_sessions` has **no token column at all** (21 columns, none cost-related), so even a correctly captured number has no home. **Why it was split out rather than fixed with ISS-092:** different code (a generator over `websocket_handoff.py`, not `run_commands.py`), different lifecycle, no shared ctx / ScopedStore / run_events stream — nothing in the Concierge fix is reusable. The wiring is ~10 lines; the missing cost SURFACE is a feature, not a bug fix. And it is unverifiable offline: it needs a real repo, a real clone and real GitHub credentials, so bundling it would have put an unprovable half inside a fully-proven one. | Found 2026-08-12 during quick-260812-gsf (FIX-233). All four file:line claims verified at HEAD; the absent column confirmed by reading the live `handoff_sessions` schema out of `backend/dev.db`. |
| ISS-101 | minor | perf | **OPEN** (found during quick-260812-gsf / FIX-233) | **`context_provider:conversation` still materialises a whole run's event log server-side to keep six chat turns.** `agents/capabilities/context_providers/conversation.py:99` calls `scoped_store.read_events(run_id, 0)` and then filters to `chat_message`/`chat_reply` in Python — on the worst run in the local corpus that is 12,123 rows / ~9.2 MB to render a ≤6,000-char transcript. **This is not a token cost and not a regression:** the model never sees it, and before FIX-233 the deleted `read_events` tool triggered the identical read (the prompt ordered the model to call it). But FIX-233 wired the provider into every Concierge turn, so the read is now UNCONDITIONAL rather than dependent on the model choosing to call a tool. The bounded counterpart already exists — `ScopedStore.read_events_of_types(run_id, types, limit=…)`, added by FIX-233 — so the fix is a one-line swap. **Deliberately not taken in FIX-233:** `conversation.py` is shared kernel capability code with its own contract and unit tests, and narrowing its read from 'all chat rows' to 'the last N chat rows' is a behaviour change to a capability outside that issue's scope; its degrade-to-`{}` error path would also hide the change from any duck-typed store fake that lacks the new method. | Found 2026-08-12 while wiring the multi-turn replacement in quick-260812-gsf. Row counts measured read-only against `backend/dev.db`. |
| ISS-102 | major | test | **CLOSED 2026-08-12 by FIX-238 / quick-260812-lfv** (found during quick-260812-gsf / FIX-233) | **[TWO CORRECTIONS TO THIS ROW, both verified by re-running it at `d31a5a7b` — read these before trusting anything below.] (1) It is TWO tests, not three.** `test_terminal_routes_to_revision` never reaches a model: it dies earlier at `app/api/run_commands.py:2497` with `AttributeError: '_FakeUser' object has no attribute 'tier'` (the KAN-161/ISS-055 entitlement gate; the `_FakeUser` double at `:33` is stale). Only `test_clarify_waiting_routes_to_answers_seam` and `test_gate_paused_routes_to_gate_seam_approve` reach the wire — proven by per-test log attribution, where the `ExpiredToken` lines appear under those two ids and not under the third. **(2) It is TWO calls, not "four attempts per run".** The original "4" was a grep miscount: 2 ERROR log lines plus the same 2 exceptions repeated inside their own tracebacks. **[SCOPE CORRECTION]** the defect was also far wider than this row's framing: a full four-tree sweep found **10** offline tests constructing a live client, 6 of them via paths `build_model()` never touches — so the row's suggested "guard that fails any offline test which reaches `build_model()`" would have been theatre. See FIX-238 for the chokepoint analysis and ISS-118 for the remaining 8. **[FIXED AS]** `TestRouting` got a class-placed autouse `_resolve_concierge` patch, and `backend/tests/conftest.py` got an autouse guard over the three provider classes' `__init__`. `ExpiredTokenException`/`ConverseStream` grep hits in that file: **4 → 0**. The two tests remain red, deliberately — ISS-119. **ORIGINAL ROW TEXT FOLLOWS, uncorrected, for provenance:** **Three offline unit tests attempt a REAL Bedrock model call; they only 'pass as failures' because the SSO token was expired.** `tests/unit/test_chat_messages_endpoint.py::TestRouting::{test_clarify_waiting_routes_to_answers_seam, test_gate_paused_routes_to_gate_seam_approve, test_terminal_routes_to_revision}` do NOT monkeypatch `run_commands._resolve_concierge` (unlike `TestConciergeStreaming`, which does), so the endpoint resolves the real `ConciergeCapability`, whose ctx carries `model=None` → `build_model()` → `ChatBedrockConverse`. Observed verbatim: `DeepAgentRunner.astream_events failed: An error occurred (ExpiredTokenException) when calling the ConverseStream operation` — four attempts per run. **The cost today is zero only because the credentials are stale; on a machine with a fresh `aws sso login` these tests spend real money every time the suite runs**, and the three are already red for an unrelated reason, so nobody looks at their output. Suggested fix: monkeypatch `_resolve_concierge` in `TestRouting` exactly as `TestConciergeStreaming` already does, and add a suite-level guard that fails any offline test which reaches `build_model()`. | Found 2026-08-12 during quick-260812-gsf. Attributed by running the classes separately: `-k TestRouting` → 4 `ExpiredToken` log lines, `-k ChatUsage` (the new tests) → 0. Pre-existing — this task's own tests never reach a model. |
| ISS-103 | **minor** (lowered from major, quick-260812-tni — see the reassessment) | infra | **OPEN — direction now FIXED, code change DEFERRED with a mechanical reason** (reassessed quick-260812-tni, 2026-08-12) | **Ctrl-C already cancels in-flight runs — by an uncontrolled path that runs AFTER the checkpointer pool is closed — while `kill`/`docker stop` does not. The two signals take different teardown paths, and only one of them is the one the code reasons about.** `uvicorn/server.py:312-330`'s `capture_signals()` restores each signal's ORIGINAL disposition and then calls `signal.raise_signal(captured_signal)` — still inside `serve()`, i.e. still inside `asyncio.run(...)` (`server.py:64-66`). For **SIGTERM** the restored disposition is the default, so the process dies right there: `asyncio.run` never returns, `Runner.close()` → `_cancel_all_tasks(loop)` (`asyncio/runners.py:193-201`) never runs, and no pipeline driver is ever cancelled. For **SIGINT** the restored handler raises `KeyboardInterrupt` in Python, which unwinds through `asyncio.run` normally — so `_cancel_all_tasks` DOES run, every registered driver task is cancelled, and each one's `except asyncio.CancelledError` handler (`run_commands.py:2424-2435` launch, `:2654-2659` revision) writes `wr.status = "cancelled"`. **Consequences.** (a) A developer pressing Ctrl-C has always been cancelling their in-flight runs, regardless of `SHUTDOWN_STOP_RUNS` — undocumented anywhere. (b) That cancel happens AFTER the lifespan has completed, i.e. after `close_checkpointer()` has closed the pool, so any driver whose unwind path touches `get_checkpointer()` hits `RuntimeError` (`checkpointer.py:41-49`); the two handlers above use the sync SQLAlchemy session so this is a latent hazard rather than an observed crash, but nothing bounds what else runs there. (c) `_cancel_all_tasks` gathers with **no timeout** (`loop.run_until_complete(gather(...))`), so a driver that does not unwind promptly hangs the process past every documented budget — outside `stop_grace_period`'s accounting entirely. **Fix shape:** decide whether SIGINT should behave as SIGTERM does (bring the cancel inside the lifespan where its ordering is controlled) and document whichever is chosen; the `SHUTDOWN_STOP_RUNS=True` path already does this deterministically and in the right order. | Settled empirically during quick-260812-hsx on a throwaway uvicorn (port 8099), not inferred: a probe modelling the driver task + handler recorded NO `driver_wrote_status_cancelled` / `driver_finally` under SIGTERM and BOTH under SIGINT; a no-uvicorn control (`isolate_cancel_all_tasks.py`) confirmed plain `asyncio.run` DOES run the handler (`cancelled: False`); and a variant stamping a marker after `server.run()` returns never wrote it, proving `server.run()` does not return. Mechanism then read in the installed `uvicorn==0.34.0` source. <br><br>**REASSESSED 2026-08-12 (quick-260812-tni). Three changes to this row.** **(1) The fix DIRECTION is now fixed and the row's wording no longer leaves the wrong door open: SIGINT must be made to match SIGTERM, never the reverse.** Making SIGTERM behave like SIGINT means every `docker stop` / ECS deploy cancels every in-flight run and writes it terminal — precisely the `SHUTDOWN_STOP_RUNS=True` semantics production rejects for two independent recorded reasons (`config.py:206-211`, `run_shutdown.py:26-36`): it destroys deploy-invisibility, which is the entire point of Phases 45-50, **and** it pushes the teardown budget from 24 s to 30 s, i.e. onto the SIGKILL line. **(2) Severity lowered to `minor`.** Production never sends SIGINT: `docker-entrypoint.sh` uses `exec uvicorn` so uvicorn is PID 1, `docker stop` / ECS send **SIGTERM**, and a repo-wide grep for `StopSignal` / `stopSignal` / `stop_signal` returns **zero** hits in any compose file, Dockerfile, entrypoint or deployment doc. And FIX-234 already defused it locally: with `ENV=development` resolving `SHUTDOWN_STOP_RUNS=True` (verified at HEAD: `settings.SHUTDOWN_STOP_RUNS is True`), step 3 of the lifespan cancels the drivers **cooperatively, inside the lifespan, in the right order**, so by the time the accidental `_cancel_all_tasks` runs the drivers are already done. The remaining window is SIGINT with the flag explicitly forced off, or a non-development `ENV` that somehow receives SIGINT. Latent-hazard + observability row, not a money row. **(3) Sub-claim (b) probed and answered: NO checkpoint is written on a driver's cancellation unwind.** A no-network, no-model probe (`scratchpad/batch2/probe/checkpoint_on_cancel_probe.py`) built a LangGraph `StateGraph` on a spy checkpointer, streamed it inside a task, cancelled mid-node and gathered with no timeout (the `asyncio.runners._cancel_all_tasks` shape). Result for BOTH `astream_events` and `ainvoke`: 5 checkpoint writes total, **0 after the cancel** — the timeline is `… aput, node_slow_start, cancel_requested, node_slow_cancelled, driver_cancelled_error_handler, driver_finally`. LangGraph's pregel loop checkpoints around node completion; a node cancelled mid-flight writes nothing on the way out. Combined with the fact that `get_checkpointer()` has exactly **one** non-test caller in the whole backend (`engine.py:1512`, the run ENTRY path, not the unwind path), sub-claim (b) stays **latent, and is now latent by measurement rather than by assumption**. Caveat: the probe used `InMemorySaver` and a plain async node, so it pins LangGraph's write SITES, not any hypothetical `finally` inside deepagents middleware. **Why the code change is DEFERRED and not simply unfinished.** The clean fix is "own the SIGINT disposition", and it is **not reachable from inside the application**. `uvicorn 0.34.0`'s `Server.capture_signals()` snapshots `original_handlers = {sig: signal.signal(sig, self.handle_exit) …}` and in its `finally` reinstalls exactly that snapshot before `signal.raise_signal(...)`. Any handler the app installs later is therefore **discarded** at that moment. And the snapshot is taken BEFORE our code exists: `serve()` is `with self.capture_signals(): await self._serve(sockets)`, and `_serve` is what calls `config.load()` — the call that imports `app.main`. So there is no in-process seam. The only clean alternative is to stop launching uvicorn from its CLI and run a `uvicorn.Server` we own, which changes the production start command, the PID-1 process and the plumbing of `--proxy-headers` / `--forwarded-allow-ips` / `--timeout-graceful-shutdown` — an outward-facing deployment change, not a clean one, and out of scope for a quick task. **Explicitly NOT done: no second terminal-write path was added beside `_drain_then_cancel` (`run_shutdown.py:222-259`), which remains deliberately the only escalation policy.** Sub-claim (c) — `_cancel_all_tasks` gathering with no timeout (`asyncio/runners.py:201`) — is unchanged and is the same defect as ISS-106's untimeouted `pool.close()`: the documented teardown budget is not a budget. Fix those two together. |
| ISS-104 | trivial | infra | **OPEN** (found during quick-260812-hsx / FIX-234) | **Stale line-number citations in the shutdown-path comments — each points at code that has since moved, and a fixer following them lands in the wrong place.** `backend/docker-entrypoint.sh:27` cites `docker-compose.yml:138` for `stop_grace_period`, which is at `docker-compose.yml:75`. `backend/app/api/run_shutdown.py:30` cites `engine.py:5263` for the `NON_TERMINAL` set, which is at `engine.py:5838-5841` (the same wrong cite appears on the ISS-089 row as `engine.py:5710-5713`). `run_shutdown.py:97-98` cites `run_commands.py:1401` for the Concierge hold and `:1356` for the durable `chat_reply` persist. `run_shutdown.py:275` cites `run_commands.py:2155` / `:2473` for the driver terminal writes; the `CancelledError` handlers are at `:2424-2435` and `:2654-2659`. Filed rather than folded into FIX-234: they are in files that change touched, but they are a separate class of defect (documentation drift) and fixing them inside a behaviour change would blur the diff. The equivalent cite inside the block FIX-234 was mandated to rewrite (`config.py:197` → `docker-compose.yml:75`) WAS corrected there. | Verified during quick-260812-hsx by reading each cited location at `9df9c1c8`. The `run_shutdown.py:275` and `:97-98` targets were re-derived by grep (`grep -n "except asyncio.CancelledError" app/api/run_commands.py` → 2424, 2654), not carried over from the investigation, whose own cites had drifted with HEAD. |
| ISS-105 | minor | infra | **OPEN** (found during quick-260812-hsx / FIX-234) | **`lifespan.shutdown()` is entered BEFORE the SSE request generators finish unwinding, so `shutdown_run_infrastructure()` snapshots `_PIPELINE_QUEUES` / `_PUMP_TASKS` while stream teardown is still in flight.** `uvicorn/server.py:287-292` cancels `server_state.tasks` when the graceful window expires and then calls `await self.lifespan.shutdown()` **without awaiting the cancelled tasks**. Reproduced twice on the throwaway probe: `lifespan_shutdown_entered` is stamped ~0.4 ms BEFORE `sse_generator_finally` (t=1786531398.0193348 vs 1786531398.019713, and again at 1786531538.125244 vs 1786531538.126372). In the real app the SSE generator's `finally` (`run_stream.py:413-419`) only logs, so no harm is expected — but the full subscriber-detach path has NOT been traced under concurrent teardown, and step 2/2b of `shutdown_run_infrastructure` reason about exactly those registries. **Fix shape:** either await the cancelled connection tasks before the lifespan (not ours to change — it is uvicorn), or make the step 2/2b snapshot tolerant of concurrent mutation and assert that tolerance in a test. | Observed during quick-260812-hsx in the FIX-234 flag experiments (two independent runs, marker timestamps above). Not chased further because it is out of ISS-088's scope and the observed impact is nil; recorded so the next reader does not have to re-derive the ordering. |
| ISS-106 | minor | infra | **OPEN** (found during quick-260812-hsx / FIX-234, while correcting the budget comment that FIX-234 was itself fixing) | **The graceful-shutdown budget is bounded only in its drain steps; the final pool close is unbounded, so the documented worst case is not a worst case.** `close_checkpointer()` awaits `pool_to_close.close()` with no timeout (`checkpointer.py:140-144`). It is step 4 of `shutdown_run_infrastructure()`, i.e. it runs AFTER the 24s of bounded waits FIX-234 documented, and it sits between the application and process exit. A Postgres pool that is slow to close (an in-flight statement, a wedged socket, a server that stops responding) therefore pushes the total past `stop_grace_period` (30s, `docker-compose.yml:75`) and docker SIGKILLs mid-teardown — the exact failure the budget exists to prevent, with the shutdown summary never logged. `TEST-018`'s `test_production_teardown_budget_fits_inside_stop_grace_period` cannot catch this: it sums the configured drains, and there is no knob for this wait to appear in. **Fix shape:** wrap step 4 in `asyncio.wait_for(..., timeout=<new setting>)` and include that setting in the arithmetic guard, so the budget test covers the whole path rather than the part that happens to be configurable. The close is already non-raising and latches `_closed` before touching the pool, so a timeout would leave consistent process state. | Read at `9df9c1c8` during quick-260812-hsx: `checkpointer.py:140-144` has no timeout wrapper, and `run_shutdown.py:189-207` awaits it directly. Recorded rather than fixed because bounding it is a behaviour change to the production shutdown path, which is not what ISS-088 authorised; the config comment and the budget test now both state the limitation explicitly instead of implying a total. |
| ISS-107 | minor | frontend | **OPEN** (found during quick-260812-iqh / FIX-235) | **`hook_run`'s payload never reaches the reducer, so the only live consumer of `hookRuns` is structurally dead.** `dashboard/page.tsx:915` dispatches `{ type: msg.type, ...(msg.data || {}) }` — it FLATTENS `data` onto the frame — while the `hook_run` case reads `const hookData = (msg.data as Record<string, unknown>) \|\| {}` (`useWorkflow.ts:827`), which is therefore always `{}`. Every `HookRunEntry` collapses to its defaults. `RunChatLane.deriveSecurityBullets` (`:428-441`) filters `h.outcome === "block" && /secret\|scan/i.test(h.hook)`, so it can **never** match. The store path cannot compensate: `hook_run` is absent from `useRunStateStore`'s `pipelineFrameTypes` (`:396-405`), so it returns early and the reducer is reached ONLY through the flattening path. **This refutes the ISS-082 investigation's stated blast radius** ("a repeated 'Secret scan blocked a write...' line") — the duplication was real, that consequence is not; today it is zero. Fix is a one-line shape reconciliation, but decide deliberately WHICH shape is canonical: every other reducer case is written against the flattened frame, so the `hook_run` case is the outlier. | Empirically verified 2026-08-12 by driving the real reducer with both shapes: FLAT (production) -> `{hook:"audit_logger", event:"", outcome:"continue", detail:{}}` for a `secret_scan`/`before_write`/`block` frame; NESTED (the case's declared contract) -> the correct values. |
| ISS-108 | trivial | frontend | **OPEN** (found during quick-260812-iqh / FIX-235) | **`clarifications` accumulates with no identity and no per-run reset outside `startPipeline`.** `useWorkflow.ts:168` does `clarifications: [...(prev.clarifications ?? []), round]` inside `retainClarifyRound`, a direct UI call made when the questionnaire panel unmounts. Because it carries no frame, ISS-082's frame-identity gate **cannot** reach it — it is registered in the accumulator registry as `driver: "ui"` precisely so GUARD-1 still sees it, but its multiplicity cases are not driven. The per-run boundary is `startPipeline`'s explicit `clarifications: []`; `pipeline_start` spreads `...prev` and never clears it, so a path that reaches a new run without `startPipeline` (a history reopen, a chained run) would carry the prior run's rounds. Not observed — flagged, not claimed. | Source `useWorkflow.ts:69` (the reset), `:165-170` (the accumulate), `:330-370` (`pipeline_start` spreads prev); registry row in `useWorkflow.accumulators.test.ts`. |
| ISS-109 | minor | frontend | **OPEN** (found during quick-260812-iqh / FIX-235; **ISS-087 neighbour — the ISS-087 fixer needs this before designing anything**) | **`validator_result` and `gate_passed` have ZERO producers in non-test backend code, so `validationIssues` may be dead on the wire and `AgentDetailPanel`'s `checksPassed === true` may be structurally unreachable.** `gate_blocked` has 3 real producers and sets `validationPassed: false`; nothing emits the two frames that would set it `true` or populate `validationIssues` (`useWorkflow.ts:986-1021`). `AgentDetailPanel.tsx:144-145` reads `agent.validationIssues?.length` and `agent.validationPassed === true && ...`. The ISS-082 fix repairs the `validator_result` accumulator anyway, for consistency and so the family rule holds uniformly — but if the frame has no producer, the surface is DEAD rather than merely un-versioned, which changes what ISS-087 should do. Needs its own investigation: either wire the producer or delete the consumer (INV-12), not both. | Two independent methods agreeing: enumeration of every literal `"type": "..."` across `backend/agents` + `backend/app` (82 frame types, neither present) and targeted greps (0 hits each in non-test code; only a doc-comment at `capabilities.py:37`). |
| ISS-110 | trivial | frontend | **OPEN** (found during quick-260812-iqh / FIX-235; **unobserved — flagged, not claimed**) | **`hookRuns` has no per-run boundary in the reducer.** `pipeline_start` spreads `...prev` and never clears it; only `startPipeline` / `resetPipeline` do. So run A's hook rows can survive into run B's `useWorkflow.pipelineState`, and the FIX-201 bridge at `page.tsx:1665` copies that whole object into the NEW run's store entry. Containment today comes from the per-run store, and ISS-082 added a per-run reset for the frame-identity cursor on the same `isSameRunReannounce` predicate — so the shape of the fix is already in place beside it and this is a one-line addition. Deliberately NOT folded into FIX-235: unobserved, and ISS-107 means the field currently drives nothing on screen, so fixing it blind would be unverifiable. Sequence it AFTER ISS-107. | Source `useWorkflow.ts:330-370` (`pipeline_start` spreads prev), `:835-838` (the accumulate), `:59-70` (`startPipeline`, the only reset). |
| ISS-111 | minor | frontend + backend | **OPEN** (found during quick-260812-jn9 / FIX-236; **this is the REAL ISS-083 finding and it outlives the prop deletion**) | **The `spec_revision` narrator card kind is unreachable end-to-end, and its branch OUTRANKS every other card kind.** `chat_narrator._classify` evaluates `attempt = _revision_index(data); if attempt > 0:` at `:151-159` — **before** the clarify/gate/pipeline/deliverable branches, and by its own comment "regardless of the carrying event". But `_revision_index` (`:106-118`) keys on `spec_revision_attempt` / `revision_index`, and **nothing anywhere writes either key into an event payload**: those two strings appear as quoted dict keys **only at `:112` and `:114` — the reader itself**. The engine holds `spec_revision_attempt` as a Python local (`engine.py:3376, 3481, 3610, 4547, 7048`) and passes `revision_index=` as a kwarg into `_consume_update_specs` / `_run_spec_revision_sub_pipeline`; the value reaches `ectx.revision_attempt` and is consumed at exactly three places — the `:rev{N}` checkpoint thread suffix and two in-flight booleans — and **never enters an event `data` dict** (`agent_start`'s payload is fixed at `{agent_id, name, role, icon, index, total}`). So `attempt` is always 0, the branch never fires, and the card has **never been emitted**: measured over **232,023 durable `run_events` across 14 runs — including 52 rows mentioning `update_specs`, i.e. spec revisions demonstrably happened** — persisted cards are gate 52 / clarify 43 / pipeline 17 / deliverable 12 / null 6 and `spec_revision` is **ZERO**. **The danger is the precedence, not the dormancy.** Because the branch runs first and ignores the carrying event type, ANY future change that adds `revision_index` or `spec_revision_attempt` to an event payload silently converts **every** card emitted during a revision pass into a `spec_revision` card — **including the gate cards the user must approve**. This was a near miss once already: `IMPLEMENTATION-REGISTER.md:4214` records FIX-220 naming its keys `revision_cycle` / `revision_in_flight` **specifically** to avoid it. **Decide, deliberately:** arm the kind (and add a precedence guard so it cannot outrank `gate`), or delete it from both sides of the contract. Deleting is NOT a cleanup — the kind is declared and asserted in four places (`chat_narrator.CARD_KINDS:44-46`, `types/index.ts:79`, `useRunChat.ts:297-303`, `test_chat_narrator.py:122` "all five card kinds are the documented set") and its dormancy is a locked decision, so it is an owner's call with its own brief. **The family lesson to record with it:** ISS-083 was the **fifth** dead counter here (ISS-039, FIX-164, FIX-163/ISS-063/FIX-221, FIX-225, ISS-083), and the generator is one design choice — *the frontend keeps re-deriving a number the backend already computed as a string*, which `chat_narrator.py:157` has emitted since Phase 29. Three properties make it rot-prone: the authoritative value never reaches the FE **as data**; its correct value is 0 on most runs, so broken is indistinguishable from correct; and every incarnation had zero **wiring** coverage — *a test that supplies a component's input itself proves nothing about wiring*. The ending move (put the cycle on the wire and delete the FE derivation) is a real design change needing its own brief — arming `_revision_index` is precisely the landmine above — and must NOT be smuggled into a trivial fix. Related: `app/api/runs.py` reuses the name `revision_index` for the run-FAMILY v-number, a different concept; a rename would defuse the collision cheaply. | Found by the ISS-083 deep investigation; grep + 232,023-row read-only `dev.db` query re-run and confirmed during quick-260812-jn9. |
| ISS-112 | minor | frontend | **OPEN** (found during quick-260812-jn9 / FIX-236 dormant-prop sweep; **user-facing**) | **`attachmentRefs` is dormant on `StartingPointCard`, so the "image not retained" placeholder can NEVER render.** Declared `StartingPointCard.tsx:44`, destructured `:47`, consumed `:55-57` as `(attachmentRefs ?? []).filter(...)` → `notRetainedImageCount`. The **sole production mount**, `AgentThinkingTab.tsx:222-226`, passes only `input`, `originalBriefRootRunId` and `revisionParentVersion` — verified by reading the mount, and `grep -rn "attachmentRefs=" src` returns **only** `StartingPointCard.test.tsx` (4 sites, all feeding the prop to itself — the same self-feeding blind spot that hid ISS-083). `notRetainedImageCount` is therefore always **0**, so the placeholder at `:246-250` ("Image not retained" / "N images not retained") is dead, **and** — the part that makes this more than cosmetic — it also gates `hasContent` at `:59-64`, whose false value early-returns `null` at `:78`. This is the honest signal that an attached image was dropped, which connects directly to the known image-input gap; a user whose image was silently dropped is told nothing. **Fix shape:** thread the refs from wherever the run's attachment metadata already lives to `AgentThinkingTab`'s mount — but **first confirm the data exists on the FE at all**, since a prop with no available source is the ISS-083 shape and would want deletion instead. Whichever way it goes, the closing test must start from a frame or a fixture and end at the pixel — feeding `attachmentRefs` directly proves nothing. | Dormant-prop sweep during quick-260812-jn9; declaration, consumption and the sole mount all re-read at `f12d99ae`. |
| ISS-113 | minor | frontend | **OPEN** (found during quick-260812-jn9 / FIX-236 dormant-prop sweep) | **`runMeta` is dormant on `AuditTab`, so the audit header's owner and workspace are permanently `null`.** Declared `AuditTab.tsx:59`, destructured `:443`, consumed `:649-655` — `runMeta?.startedAt ?? first`, `runMeta?.durationMs ?? derived`, `runMeta?.owner ?? null`, `runMeta?.workspace ?? null`. **Neither** mount passes it: `PreviewPanel.tsx:1172-1176` sends `hookRuns` / `workflowRunId` / `isRunning`, and `WorkflowHistory.tsx:796` sends `workflowRunId` only. `grep -rn "runMeta=" src` returns **nothing at all** — not even a test. So owner and workspace are always null and the timings always fall back to values derived from the rows. Unlike ISS-083 this is a genuine data gap rather than a duplicate: nothing else supplies owner/workspace to that header, so the honest fix is to **wire it**, not delete it. **A second, opposite defect on the same component, found in the same read:** `hookRuns` is declared at `AuditTab.tsx:51` and **passed** by `PreviewPanel.tsx:1173`, but it is **not destructured at `:443`** — so it is supplied and silently ignored. That is the mirror image of the dormant-prop shape and worth fixing in the same pass. (Note the neighbouring ISS-110 / FIX-235 finding that `hook_run`'s payload never reaches the reducer at all — settle that first, or a wired `hookRuns` will carry defaults.) | Dormant-prop sweep during quick-260812-jn9; both mounts and the destructure re-read at `f12d99ae`. |
| ISS-114 | trivial | test-infra | **OPEN** (found during quick-260812-jn9 / FIX-236; **deliberately NOT repaired inside that change — filed rather than smuggled in**) | **`ResultCard.test.tsx`'s LOCK-F case is RED and belongs to no register row, and the underlying LOCK-F guarantee may actually be broken in the product.** The test `labels the deliverable output 'Deliverable' (LOCK-F) and links to Preview` throws `TestingLibraryElementError: Unable to find an element with the text: Deliverable` on its `getByText("Deliverable")` assertion. Cause: **KAN-154** moved `deliverable` into the inline-render branch at `ResultCard.tsx:141`, which renders **no title** — only `message.content` plus the link — so the literal string "Deliverable" appears nowhere in the DOM for that path. ISS-076 covers the **Playwright** 33-red baseline, not vitest, and no row covers this one. **The open question decides whether this is a test fix or a CODE fix, and it should not be answered by whichever is cheaper:** LOCK-F says the run output is labelled "Deliverable", never a workflow-specific noun. If that guarantee still stands, the product is violating it and the test is right; if KAN-154 deliberately retired the label for the inline path, the comment at `ResultCard.tsx:125-140` never says so — it explains the inline change without mentioning LOCK-F. Either way LOCK-F is **currently unasserted** for the deliverable path. **Do not "fix" this by deleting or loosening the assertion.** Baseline for whoever takes it: at `f12d99ae` and at `0312a0af` the file reads `1 failed / 10 passed (11)` and `1 failed / 12 passed (13)` respectively, the single red being this case (line `:56` before FIX-236, `:57` after — the shift is one added comment line, same test ID, same throw). | Measured at `f12d99ae` during the ISS-083 investigation and re-measured at `0312a0af` after FIX-236; the failing ID was the change's own no-regression gate. |
| ISS-115 | trivial | frontend | **OPEN** (found during quick-260812-kpb / FIX-237; **deliberately NOT folded into that change**) | **Two non-equivalent `## Task N:` parsers still ship (INV-12 debt).** FIX-237 extracted `parseTasks` (`artifactPreview.tsx:80-104`) and pointed the settled tasks CARD at it, but `useWorkflow.ts:1020` keeps its own copy — `/^##\s+Task\s+(\d+)\s*:\s*(.+)$/gm` — which is **line-anchored and tolerates whitespace before the colon**, while the extracted one is neither. They are NOT interchangeable, so unifying them would silently change `protoPlannedTasks`, which feeds `ConstructionBlock` via `AgentThinkingTab.tsx:195-202`. Unifying needs its own change with its own before/after on the construction card — which is exactly why it was filed instead of absorbed. | Named as out-of-scope in owner decision D1 and in the FIX-237 plan; the divergence was read from both regexes, not assumed. |
| ISS-116 | minor | frontend + backend | **OPEN** (found during quick-260812-kpb / FIX-237; **settled a question two prior investigations left open**) | **The governance-checks verdict badge is DEAD CODE, not merely un-versioned.** `checksPassed` requires `validationPassed === true` (`AgentDetailPanel.tsx:145`), which only `case "gate_passed"` sets (`useWorkflow.ts:1078`). **`gate_passed` has ZERO producers in the entire backend — including tests.** The GATE_PASS path calls `write_gate_event` (`agents/capabilities/gates/write.py:20-30`), which writes a `gate_events` DB row and returns `GateOutcome(outcome=GATE_PASS)` carrying **no `events=[]` list**, so nothing reaches the wire; there is no dynamic `f"gate_{...}"` construction either. `validator_result` likewise has ZERO producers (one docstring mention, `app/api/capabilities.py:37`), so `checksIssueCount` is always 0. **Net effect: the green "Passed" chip can never render and the amber chip always reads "Review"** — a constant string on every analysis agent of every run. Only `gate_blocked` is real (3 producers: `gates/validation.py:133`, `gates/security.py:108`/`:174`), and `run_events` holds **0 rows** of any of the three across 232,023 events. Decide whether to wire a `gate_passed` emission or delete the branch; do NOT version-thread a field nothing populates. | ISS-087 asked for an "honest affordance" here; building one was declined because the surface carries no version-specific information to be honest about. Producer counts verified by grep over all backend `*.py`; row counts by read-only SELECT. |
| ISS-117 | minor | frontend | **OPEN** (found during quick-260812-kpb / FIX-237; **upgraded from "suspected" to CONFIRMED**) | **Validation frames are dropped on replay, so no run reopened from history can ever show a gate verdict.** `useRunStateStore.ts:395-403` lists `pipelineFrameTypes` and **omits `validator_result`, `gate_passed` and `gate_blocked`**; `:405` is an explicit `if (!pipelineFrameTypes.includes(msg.type)) return;` early return. So even the one reachable path (`gate_blocked`, which has 3 real producers) cannot rehydrate `validationPassed` when a run is opened from Run History — `ValidationResultCard` (`AgentDetailPanel.tsx:839`) self-gates on `passed === undefined` and renders nothing. This is independent of ISS-116: fixing the producer gap alone would still leave history blind. | The ISS-087 investigation flagged this as suspected without tracing the replay path; quick-260812-kpb read the filter and its early return directly. Not observed on screen — the DB has no such rows to replay. |
| ISS-118 | minor | test | **OPEN** (found during quick-260812-lfv / FIX-238; **deliberately NOT folded into that change — filed, not silently patched**) | **Eight offline tests CONSTRUCT a real LLM provider client. They cost nothing today only because none of them invokes it — one added `.ainvoke`/`.astream` in any of them is live spend.** They are currently exempted by exact nodeid in `_CONSTRUCTS_BUT_NEVER_INVOKES` (`backend/tests/conftest.py`), which is what keeps FIX-238's guard from turning 8 green tests red; the dict is the visible inventory, not a fix. **The list:** `tests/unit/test_brief_max_chars.py::{test_full_brief_reaches_planner_uncut, test_ceiling_still_enforced_above_cap}`; `tests/unit/test_cached_invoke.py::{test_bedrock_call_places_cache_control_on_stable_prefix, test_cache_control_suppressed_when_flag_off}`; `tests/agents/test_iss056_clarify_quality_fixes.py::TestH3SmartPlannerDesignNote::{test_design_note_present_when_design_context_supplied, test_design_note_absent_when_design_context_none, test_design_note_absent_when_both_names_missing}`; `tests/agents/test_mcp_client.py::test_bound_mcp_tool_augments_a_deepagents_agent`. **Five of the eight go through `SmartPlanner._build_llm` (`agents/planner/smart_planner.py:167-201`), which builds its client WITHOUT `build_model()`** — the same bypass that made the provider classes, not the factory, the correct chokepoint. The eighth is different in kind: `create_deep_agent(model="anthropic:claude-3-5-haiku-latest")` makes langchain's `init_chat_model` build a `ChatAnthropic`, so the live client is constructed by the LIBRARY from a string the test passes. **Fix shape:** each should stub its construction seam the way `tests/unit/test_model_factory.py:77/83/89` already does (replace the class attribute on the SOURCE module) so the exemption can be deleted; the MCP one may instead pass a scripted `BaseChatModel` instance, since `create_deep_agent` accepts one. Do them as one deliberate pass with a before/after per test — **not** as a drive-by inside another change. | Enumerated empirically 2026-08-12 by running a prototype guard (patching `__init__` on the three provider classes) over `tests/unit`, `tests/agents`, `tests/properties` and `tests/integration` and recording every trip; cross-checked by the suite deltas, which moved by exactly the number of trips in each tree (+4 unit, +4 agents) and returned to zero once the eight were allow-listed. All eight were confirmed to produce **no** `ExpiredTokenException`/`ConverseStream` line, i.e. construct-only. |
| ISS-119 | minor | test | **OPEN** (found during quick-260812-lfv / FIX-238; **an owner decision, deliberately not taken by a money fix**) | **Two `TestRouting` tests assert a routing contract the product deliberately superseded, and they will stay red until someone decides which side is right.** `tests/unit/test_chat_messages_endpoint.py::TestRouting::test_clarify_waiting_routes_to_answers_seam` asserts `channel == "answers"` and `test_gate_paused_routes_to_gate_seam_approve` asserts `channel == "gate"` with "default action approve". Both POST bare `text=` with no `action` and no structured `responses`. `app/api/chat_router.py::route_chat_turn` now routes exactly that shape to **`CHANNEL_CONCIERGE`** in both phases, on purpose and with the reasoning in-comment: at `PHASE_CLARIFY_WAITING` "a plain text turn with NO structured responses ... must NOT be auto-submitted as a freeform clarify answer — the user may be asking a question"; at `PHASE_GATE_PAUSED` "a plain free-text turn with no action ... must NOT default to 'approve'". Both are **safety** behaviours: the old contract could silently answer a questionnaire or approve a gate on the user's behalf. So the tests encode the pre-safety contract and the product is almost certainly right — but **reconciling a safety-critical routing contract is not a side effect of a money fix**, which is why FIX-238 left the assertions untouched and the tests red. Since FIX-238 they fail cleanly offline (`JSONDecodeError` on `resp.json()`, because the Concierge branch returns `text/event-stream`) instead of on a swallowed Bedrock error. **Decide:** reconcile the two cases to assert `concierge` (and add cases pinning that a *structured* clarify answer still reaches `answers` and an *explicit* gate action still reaches `gate`, which nothing currently proves for the bare-text neighbours), or establish that the routing change was wrong. Do not delete or loosen them. A third red in the same class, `test_terminal_routes_to_revision`, is unrelated and still open on `AttributeError: '_FakeUser' object has no attribute 'tier'` (`run_commands.py:2497`, the KAN-161/ISS-055 entitlement gate reading `user.tier` off a stale test double at `:33`). | Read directly from `app/api/chat_router.py::route_chat_turn` (both branches and their comments) during quick-260812-lfv, and confirmed by the observed failure reason before and after FIX-238: the two tests failed on `JSONDecodeError` both times, but the pre-fix body came from a live Bedrock error and the post-fix body comes from the scripted concierge — same red, different cause. |
| ISS-120 | minor | backend | **OPEN** (split out of ISS-034 item (d) during quick-260812-mq5, 2026-08-12) | **Bedrock prompt caching is a single process-wide on/off switch, and on short runs it is a MEASURED net cost INCREASE.** `BEDROCK_PROMPT_CACHE_ENABLED` (`app/core/config.py:158`, default `True`) is one boolean read at call time by exactly two sites — `app/agents/deep_agent_runner.py:227` (`_BedrockCachePointsMiddleware.wrap_model_call`) and `app/agents/cached_invoke.py:71` (`_bedrock_cache_control`). There is **no per-workflow, per-step or per-agent override anywhere** (all four line numbers re-verified at `c05906c0`). Measured read-only against the live `backend/dev.db`, re-priced through the real shared `estimate_cost_usd` with 0 reconciliation mismatches across all 11 rows: **6 of 11 persisted runs cost MORE with caching on (-4.98% .. -8.86%), 1 is exactly neutral, 4 save (+58% .. +83%).** **The discriminator is run LENGTH, not workflow type** — `ec7fb984` is an `od_prototype` cancelled early (`cache_read=11,225`, `cache_write=148,379`) at **-4.98%**, while every `user_stories` run has `cache_read=0` with a non-zero `cache_write`. Mechanism: a cache entry bills at `cache_write_5m = 1.25x` the input rate (`agents/capabilities/model_catalog.py:82`, `Pricing(1e-6, 5e-6, 0.1e-6, 1.25e-6, 2e-6)`; the same 1.25x ratio holds for every catalog tier), so any turn that writes a cache entry nothing later re-reads is a pure 25% surcharge on that content. The aggregate over all 11 runs is still **+80.34%** because two long prototype runs dominate — **the fleet number hides a real per-run regression**, which is precisely what FIX-239's `estimated_cost_full_usd` now makes visible. Candidate shapes, ascending cost: (a) leave global, document it; (b) suppress the cache point when the step is declared single-turn (manifest-declared capability flag, no kernel edit, SC-001); (c) a heuristic that only writes cache points once a run has exceeded N model calls. **Do NOT key it on workflow name (INV-1).** | Split out of ISS-034 item (d) so a behaviour change to the model-call path is not folded into a telemetry-only change. It has its own risk surface and its own proof obligation (a real cost measurement, not a unit test). Now *measurable* via FIX-239 — which was the argument for landing the telemetry first. |
| ISS-121 | major | engine | **FIXED** by FIX-240 (quick-260812-ppu, 2026-08-12, commit `88521920`) — filed 2026-08-12 by the orchestrator's own LIVE verification pass of FIX-232, on real Bedrock | **A gate-rejected run reopens from history looking ALIVE: header "Awaiting approval" and a "Stop" button.** **SYMPTOM CORRECTED 2026-08-12 (quick-260812-wir):** this row also listed a third symptom — a stale clarify line *"The run is paused and waiting for you to answer clarification questions"*. **That is NOT a bug and never was.** The string exists NOWHERE in `frontend/src` or `backend/` (grep: 0 hits; it appears only in this register and the ISS-121/surface knowledge cards, i.e. it was propagated from this row). On `808612bf` it is the Concierge's own `chat_reply` row at seq 8 — genuine historical chat content, correctly replayed into the transcript. Chasing it as a rendering defect would have sent a fixer into the clarify panel, which is not involved. The run is genuinely finished — DB `workflow_runs.status = 'cancelled'`, and only 1 of 6 agents ever started. **ROOT CAUSE — CORRECTED IN PLACE 2026-08-12. THE ORIGINAL DIAGNOSIS IN THIS ROW WAS WRONG.** It read: *"`pipeline_cancelled` is NEVER WRITTEN to the durable `run_events` log … the fix is to make the reopen path consult `GET /api/runs/{id}`"*. **Every MEASUREMENT below is correct; the causal claim built on top of them was not.** `pipeline_cancelled` IS written on every one of the engine's eight terminal emitters. On this run the INSERT was **attempted, rejected by the `uq_run_events_scope_seq` UNIQUE constraint, and silently swallowed**. **The real defect is general, not gate-specific: every app-layer `run_events` row written while a run is live destroyed exactly one subsequent engine event.** Two writers shared ONE per-run seq space with TWO algorithms — `ScopedStore.append_event_next_seq` (`agents/authz.py:433`) allocates `max(seq)+1` and **retries past `IntegrityError`**, while the engine's `_RunEventSink.persist` (`engine.py:429-462`) used a fixed in-memory counter and **swallowed the collision**: it catches `SQLAlchemyError`, `IntegrityError` is one, so a production seq collision took the branch written for "the offline harness has no schema" and was indistinguishable in the logs. At a gate the engine is idle, so the victim is **deterministically the terminal frame** — which is exactly why the symptom LOOKED like "cancelled is never persisted". `authz.py:462-465` documented this as an accepted trade-off and called the collision **"rare"**; it fires on **every chat turn during a live run**. **MEASUREMENTS (unchanged, all verified):** full type tally for live run `808612bf-777a-4ab1-b188-6561a23eb12a` (user_stories, gate on `domain-analyst`, rejected): `agent_chunk 262, chat_reply 6, chat_message 2, workflow_validated 1, review_gate_ready 1, questionnaire_ready 1, planner_start 1, planner_complete 1, pipeline_start 1, gate_status 1, chat_usage 1, agent_start 1, agent_input 1, agent_complete 1` — no `pipeline_cancelled`, no `pipeline_complete`. **The fingerprint that broke it open:** the log does NOT end at `review_gate_ready` (seq 278) — seq 279 is a narrator `chat_reply` card, 280 the user's `chat_message` "Not what I wanted — stop.", and **281 is a `chat_reply` milestone card reading "Cancelled by you" — the card projected FROM the very `pipeline_cancelled` event whose row is missing.** A milestone card whose preceding row is a `chat_message` is structurally impossible unless the engine event that produced it was dropped. Also corrected: the rejection arrived through **`POST /api/runs/{id}/messages`** (the chat lane, the only one of the three that writes a durable row), NOT `POST /gate`; `POST /gate` and `POST /cancel` write none and never reproduced it. **FIX (FIX-240):** `ScopedStore.append_event_at_or_after` re-appends past the racing writer and returns the seq the row actually landed on; both engine drive loops re-stamp `data["seq"]` with it (mandatory — `run_stream.py:198` renders the SSE `id:` cursor from `row.seq` on replay and `:264` from `data["seq"]` live, so a divergence corrupts `Last-Event-ID`). The rejected "consult the run record instead" option was correctly identified as insufficient AND as an INV-12 second source of truth for terminal state; it survives only as a narrow app-boundary guard on the SSE gate re-arm. **RELATION TO FIX-232 — unchanged and still honest:** FIX-232 is CORRECT and untouched; it did not create the durability gap, it changed which wrong thing the user saw. **Residual, NOT fixed by FIX-240: ISS-123** (rows already destroyed are not retroactively recoverable) and **ISS-126** (a fresh reopen still replays the dangling gate row). | Found during the end-of-batch live verification pass (hex-uki SSO, Bedrock Haiku 4.5, eu-central-1), NOT by a subagent and NOT offline — the three offline probes for ISS-091 read the YIELDED stream, where `pipeline_cancelled` is present, so none of them could have seen this. **Why it shipped:** `tests/unit/test_run_events.py::test_append_event_next_seq_retries_past_a_raced_seq` proved only that the chat writer WINS the race and never asked what happened to the loser — that missing half is now TEST-024. Evidence: DB status query, the full tally above, the seq 279/280/281 tail, an in-process reproduction against the real production classes, and a Playwright reopen-from-history capture (`scratchpad/batch2/cancelled-run-view.png`). Total live spend for the proof: $0.0069. |
| ISS-122 | minor | frontend | **OPEN** (found 2026-08-12 by the orchestrator's LIVE verification of FIX-239) | **The cache-delta line is invisible in practice, because an unmetered-legacy window dilutes the percentage below the zero-suppression threshold — and the `metered_runs` signal that would explain this is fetched but never shown.** Measured live on `GET /api/analytics/summary`: `spend=17.744173`, `spend_full=17.741358`, `metered_runs=2`. The BACKEND IS CORRECT and the FIX-239 legacy guard is PROVEN — 8 unmetered completed runs contributed exactly zero delta, so the aggregate difference (**−$0.002815**) equals the single metered run's delta to the cent, instead of the **−$17.70 / −100%** a naive sum would have produced. But as a share of a window holding $17.74 of unmetered spend, that delta is **−0.016%**, which rounds to 0% and hits the by-design "zero ⇒ render nothing" branch. Net effect: the line renders NOTHING even though a genuine negative delta exists, and it will stay invisible until the 30-day window turns over to metered runs. The FE already receives `metered_runs` and does not surface it, so a user cannot tell the difference between "no caching effect" and "not measured yet". **Suggested fix (not attempted):** compute the percentage over the METERED subset only (the denominator should be metered spend, not window spend), and/or always render the "Measured on N of M runs" note whenever `metered_runs < completed_runs`, even when the delta suppresses. **NOT a defect in FIX-239's arithmetic** — its guard is what prevented the catastrophic number, and that guard is verified. | Found during the end-of-batch live pass. Evidence: the API payload above; a completed live run `58f5df10-2024-45be-b7ee-1b8fc5ef2d73` carrying `estimated_cost_usd=0.04754` vs `estimated_cost_full_usd=0.044725` (**caching cost 6.3% MORE** — `cache_read=0`, `cache_write=10237`, the single-turn write-never-read pattern ISS-034 predicted); and a Playwright capture of the Analytics view showing no caching line (`scratchpad/batch2/analytics-view.png`). Live spend for the proof: $0.0475. |
| ISS-123 | major | engine | **CLOSED — audited, deliberately NOT repaired** by FIX-246 (quick-260812-wir, 2026-08-12) | **COUNT + REASONING CORRECTED 2026-08-12 (FIX-246). The damage is 15 destroyed engine events across 9 runs, NOT ~29 across 10** — `agent_chunk` 11, `questionnaire_complete` 2, `review_gate_approved` 1, `pipeline_cancelled` 1. The 29 counted seq-STEALING app rows, which is an upper bound, not the loss: a steal destroys an engine event only when the engine counter actually WALKS onto it, and 12 steals hit runs with no live drive on that seq while 2 were absorbed by a narrator-card counter jump. Re-derived independently by `backend/scripts/audit_lost_run_events.py` (read-only, `mode=ro`, zero write verbs). **The stated reason for refusing back-fill was half wrong and is corrected:** "a dropped row has no provenance to reconstruct from" is FALSE for 11 of the 15 — they are `agent_chunk` fragments and `artifact_refs.content` is an independent authoritative record of the same text; the audit sizes each hole against it and reproduces the lost strings exactly by length (1 char = the document`s leading `#`, 4, 14, 26, 27). **The correct reason is narrower and stronger: the CONTENT is recoverable, the IDENTITY is not.** `event_id` is the key the whole replay contract rests on (`replayed_event_ids`, `append_event_next_seq` idempotency, the FE `seenRef`), so a back-filled row must mint an `event_id` that never existed — making the log assert the engine emitted a frame it did not, with a de-dup key no live frame can match. The four structural losses are unrecoverable outright: `workflow_clarifications` is EMPTY corpus-wide and `gate_events` records NO approve outcome for any run. **User impact: exactly 1 run was user-visible-broken (`808612bf`) and that symptom is now fixed by FIX-245 (ISS-126); the other 8 are silently lossy** (a reopened transcript short by 1–27 characters of one agent`s text; the DELIVERABLE is unaffected — Preview/Files read `artifact_refs`/`output`). **FIX-240 stops NEW losses; it cannot recover the rows already destroyed.** The pre-fix defect ran for as long as the chat lane has shared the engine's seq space, and a dropped `run_events` row has no provenance to reconstruct from. Corpus-wide upper bound measured read-only over `backend/dev.db`: **29 seq-stealing app-layer rows across 10 runs** (`chat_message` 19, concierge `chat_reply` 8, `chat_usage` 2) against 233,325 rows in 16 runs — i.e. up to **29 silently destroyed engine events**. Three identified by name: `questionnaire_complete` in runs `808612bf` and `f36ab26c`, one `pipeline_cancelled` (`808612bf`), and an `agent_chunk` in `ec7fb984` (seq 3581). Consumers that silently mis-read a holed log: SSE durable replay, `derive_open_gate`, `_reconcile_terminal_status` (a dropped terminal makes a user-resume fall through to `new_status="failed"`), `_apply_terminal_output_columns`, the conversation context provider, the Concierge, and `GET /runs/{id}/events`. **Deliberately NOT fixed:** back-filling synthetic terminal rows was considered and REJECTED — fabricating durable events with no `event_id` provenance is worse than the hole, and it would make the log lie about what the engine actually emitted. **Money is NOT exposed:** the startup restore scan filters on `WorkflowRun.status`, which is correct, so a holed cancelled run is never re-driven. | Measured by the ISS-121 root-cause investigation (read-only SQL, `mode=ro`). Candidate disposition: accept as historical data loss and record it, or write a one-off read-only audit that lists affected runs so support can recognise them. **2026-08-12 (FIX-246):** shipped `backend/scripts/audit_lost_run_events.py` — a read-only forensic audit so the damage is nameable and re-measurable. It simulates the seq allocator (a gap proves nothing: the steal OCCUPIES the seq) and reports two signatures, WALKED-ONTO and ORPHAN-CARD (a narrator card whose source `event_id` has no row — a tombstone that NAMES the destroyed event). Three self-corrections are recorded in the script: post-FIX-240 runs show the same shape but lost nothing (only creation time discriminates -> `--fix240-utc`), a destroyed `review_gate_approved` sits TWO rows after its `review_gate_ready` because the narrator card is between them, and sizing a hole against the SUM of `artifact_refs` versions reported a 1-char hole as 18,718. |
| ISS-124 | minor | backend | **FIXED** by FIX-244 (quick-260812-wir, 2026-08-12, commit `e3c38594`) | **SEVERITY WAS UNDERSTATED — this was not minor.** Beyond the durable hole, `stop_run_driver` runs `_reconcile_terminal_status` once the driver is provably gone, and that decides from the durable tail ALONE: with no `pipeline_cancelled` row it computes `cancelled=[]`/`completes=[]`/`failed=[]` -> the D2 fail-safe `"failed"`, written through an UNCONDITIONAL `_persist_resume_status` that OVERWRITES the `"cancelled"` the driver had just set. **So the owner`s Stop was recorded as a FAILURE.** Observed directly as `assert 'failed' == 'cancelled'`. **Two app-layer synthesised `pipeline_cancelled` frames never persist a durable row at all** — a different cause with the same reopen symptom as ISS-121. `run_commands.py:2435` (`_drive_launch_to_queue`) and `:2666` (`_drive_revision_to_queue`) push a terminal frame straight onto the SSE queue with no `seq`, no `event_id` and no `run_events` write. They fire only on a genuine destructive `task.cancel()` where the engine did not already emit a terminal, so the frequency is low — but a run terminated that way has the same durable hole, and FIX-240 does not touch it (FIX-240 fixes the ENGINE sink; these bypass the engine entirely). Both line numbers re-verified at commit `88521920`. Fix shape: give both sites an additive `append_event_next_seq` write, which is the allocator an app-layer writer should already be using. | Enumerated by the ISS-121 emitter audit (8 engine emitters all persist; these two are the only non-persisting terminals). Filed rather than folded, so a durable-write change to the REST drivers is not smuggled into an engine-sink fix. **FIXED 2026-08-12 (FIX-244):** both branches now append through `_record_cancellation_in_the_durable_tail` — FIX-243`s collision-safe writer — EXTENDED with a `reason` parameter rather than copied (INV-12: one durable-terminal writer, three callers); the default keeps FIX-243`s payload byte-identical. No duplicate-suppression guard, deliberately: the `pipeline_cancelled` FRAME is already emitted unconditionally on both branches, so this only makes the row agree with the frame that already ships, and every verified consumer is `any()`/`max()`-shaped. 4 new tests in `test_rest_answers_cancel.py` (20 -> 24), all seen RED. The discriminating one drives a REAL `task.cancel()`, because the append sits inside an `except asyncio.CancelledError` block and had to be OBSERVED completing under genuine cancellation — that test also corrected a wrong assumption of mine (the driver deliberately CONVERTS the cancellation into a terminal instead of re-raising). |
| ISS-125 | minor | engine | **OPEN** (filed 2026-08-12 during quick-260812-ppu, out of FIX-240's scope) | **`_stamp_resume_marker` still allocates its seq the slow, unprotected way.** `engine.py:6309-6311` reads the ENTIRE durable log (`store.read_events(run_id, after_seq=0)`) to compute `max(seq)+1`, then calls the non-retrying `append_event` at that fixed seq. Two consequences: (a) it materialises every row of the run — the local corpus holds a 26k-row and a 36k-row run — to derive one integer, which is the exact cost profile that ruled `append_event_next_seq` out for the engine sink; (b) it carries the same lost-write exposure ISS-121 describes, though the blast radius is far smaller (the marker is a best-effort audit row wrapped in its own `try/except`, and losing it degrades an audit trail, not run state). Fix shape: reuse the `_max_event_seq` index probe FIX-240 added, and append through `append_event_at_or_after`. Line numbers verified at commit `88521920`. | Surfaced by the ISS-121 allocator sweep. Not folded into FIX-240 because the marker needs no seq RE-STAMP (it yields no frame), so it is a genuinely separate change with a separate proof obligation. |
| ISS-126 | major | frontend,backend | **FIXED** by FIX-245 (quick-260812-wir, 2026-08-12, commit `2b25d3d0`) | **LOCUS AND SCOPE BOTH CORRECTED 2026-08-12 (FIX-245).** (1) **The stated locus is wrong and it changed the fix.** A terminal run NEVER opens an SSE stream — `page.tsx:2171` gates `attachRun` on non-terminal — so `run_stream.py` is never entered on this path. The frames come from `GET /api/runs/{id}/events` replayed at `page.tsx:2234-2247`. Both backend options rejected in this row were therefore aimed at a code path the symptom does not traverse; their rejection stands, but neither would have worked. (2) **"Affects only runs corrupted BEFORE FIX-240" was FALSE** — ISS-124`s two app-layer driver terminals were still minting fresh corrupted runs until FIX-244 landed the same day, so ISS-124 was a prerequisite, not a sibling. **FIX-240's data-repair guard only covers the CURSORED reattach; a fresh reopen of an already-corrupted run still renders an open gate.** The guard added at `run_stream.py:255` suppresses the D-14g gate re-arm when the persisted status is terminal — but that branch is reached only when `after_seq > 0`. Opening a run from history sends no `Last-Event-ID`, so `after_seq == 0` and the symptom instead comes from **step 1, the durable replay itself** (`run_stream.py:196-200`), which faithfully yields the orphaned `review_gate_ready` row that is genuinely in the log. The FE rebuilds "Awaiting approval" + Stop from that frame. **Deliberately not fixed here:** filtering rows out of the durable replay would make the SSE stream disagree with the log it is replaying and break `Last-Event-ID` semantics, and synthesising a terminal frame on the wire would install the second source of truth INV-12 exists to prevent. The honest fix is front-end: `dashboard/page.tsx` already reads `fullRun.status` (:2399-2405) and already clears `reviewGateData`/`activePipelineRunId` on `pipeline_cancelled` (:1192-1200, :1527-1561) — it should apply the same clearing when the fetched run status is terminal, regardless of what the replay contained. Affects only runs corrupted BEFORE FIX-240 (see ISS-123); new runs persist their terminal correctly. | Identified while scoping FIX-240's part (b) and verified by reading the replay path — the guard was measured, not assumed, to leave this half open. Sibling of the known post-c72 residual ("cancel-at-clarify doesn't repaint"), which is also FE-side but a live-path defect, not a replay one. **FIXED 2026-08-12 (FIX-245), front-end, and TWO of the three pieces were found only by driving a real browser AFTER the unit tests were green.** (a) `terminalMarkers()` is now the single definition of what a terminal status does to run state and the three live terminal event cases route through it, so the places deciding "what terminal looks like" go 3 -> 1 — the fix REDUCES truth sources (INV-12); the gate reuses the EXISTING `REOPEN_TERMINAL_STATUSES` and a test pins the two sets equal so they cannot drift into a fifth list. (b) **Patching the store is TRANSIENT**: the FIX-201 bridge (`page.tsx:1663-1672`) copies the LEGACY `useWorkflow.pipelineState` into the store WHOLESALE once the viewport switches, and that legacy state accumulated the same terminal-less replay, so it clobbered the patch a few hundred ms later — see ISS-138. (c) **Resolving `isRunning` is not sufficient**: `laneClarifyOpen` (`DashboardLayout.tsx:1862`) is the ONE lane branch not AND-ed with `isRunning` (deliberately — clarify precedes `pipeline_start`), so the header merely fell from "Awaiting approval" to "Clarifying" with Stop still armed; on `808612bf` that lingering panel is itself ISS-123 damage (its `questionnaire_complete` row is one of the 15 destroyed). `reviewGateData` is deliberately NOT cleared — already masked by the gate branch`s `&& isPipelineRunning`, and clearing it alone is WORSE than nothing. **LIVE A/B on Bedrock dev data:** `808612bf` (corrupted) went Stop-present + "Awaiting approval" -> `lane-run-status` "Cancelled" (tone=neutral), no Stop; `1ea6d262` (clean post-FIX-240 control) read "Cancelled", no Stop, UNCHANGED throughout. |
| ISS-127 | minor | api,validation | **OPEN** (split out of ISS-070, filed 2026-08-12 during quick-260812-sgu) | **`GateCommand.analysis_report` has no length cap anywhere on its path into the composed prompt.** Traced end to end at `e6b24ae5`: `run_commands.py:115` (`analysis_report: str \| None`) → `resolve_gate:264` → `store.set_review_response(instructions=...)` → `engine.py:5788`/`:5840` → `_gate_update_specs` → `engine.py:5511`/`:5552` → `engine.py:5387` `ectx.spec_revision_context = analysis_report` → injected into the composed prompt at `engine.py:9096`. A `grep` for slicing / `MAX_*` / `len(...)` truncation over `agents/` + `app/` on that path returns nothing — so an owner-supplied string of arbitrary size is injected into **every** dispatch of the 3-agent spec-revision sub-pipeline, on a workflow whose measured token ceiling is 37.3M. The sibling field `MessageCommand.analysis_report` (`run_commands.py:698`) shares the gap. **Owner-scoped, so it is a cost-amplification gap rather than a cross-tenant one** (`resolve_gate:222` → `run_engine.py:765` filters on `WorkflowRun.user_id`). | Found by the ISS-070 investigation and named in the ISS-070 row, but **deliberately NOT folded into FIX-241** — it is a different defect with a different fix (a cap plus a 422 at the schema, and an owner decision about what the cap should be), and including it would have obscured the RED/GREEN proof of the discriminator change. Fix shape: a `Field(max_length=...)` on both models plus a decision on the limit; note that unlike the ISS-070 fix this one IS a behaviour change for any client currently sending a long report, so check the FE's `analysis_report` producers first. |
| ISS-128 | trivial | api,validation | **OPEN** (filed 2026-08-12 during quick-260812-sgu, deliberately out of FIX-241's scope) | **A gate REJECTION persists an audit record that says `action="approve"`.** `run_commands.py`'s reject branch calls `store.set_review_response(gate_key, approved=False, edited_content=...)` without passing `action=`, so the store's own default (`store.py:123`) stamps `action="approve"` on the durable record. Measured, not inferred — the same in-process probe that proved ISS-070 printed: `action='reject' → HTTP200 approved=False stored_action='approve'`. **Functionally inert**: the engine keys its terminal decision on `approved`, not `action` (`engine.py:5852`/`:5859`), so a reject still rejects. It is a **forensic** defect — the durable row misdescribes the user's decision, and anyone auditing "who approved this gate" from the store will count rejections as approvals. The same omission exists on the two sibling ingresses' reject branches (`:1101` region and `:1322` region). | Found by the ISS-070 investigation (quick-260812-sgu) while proving the fail-open. FIX-241 fixed the `else` branch's laundering — the approve branch now passes `action="approve"` explicitly and an unknown action is refused before any write — but **deliberately did not touch the reject branch**: changing what a rejection persists is a behaviour change to durable data with real blast radius on the declared-gate outcome computation (`agents/capabilities/gates/human.py:104-141` derives `GATE_BLOCK`/`GATE_WAIT_HUMAN`/`GATE_PASS` from the gate signals), and folding it in would have muddied the RED/GREEN proof of the discriminator change. Fix shape: pass `action="reject"` at all three reject branches, then re-check `human.py`'s outcome computation and any `get_review_response` reader before trusting it. |
| ISS-129 | **major** | engine | **OPEN** (filed 2026-08-12 during quick-260812-syf / FIX-242; deliberately NOT folded — the amplifier, not the instance) | **`run_fanout` swallows EVERY worker event, including terminal ones.** `fanout.py:414-429` iterates `runner.run_worker(...)` and forwards nothing; the loop body reads only `agent_complete` to accumulate `worker_tokens`. The in-file comment justifies this for CHUNK events (D-03, lifecycle-only), but the loop also discards `agent_error`, `pipeline_cancelled` and `review_gate_ready`. Because the durable `_RunEventSink` wraps `execute()`, an event that never leaves `_run_one` is never streamed AND never persisted — so a fan-out worker can fail, be cancelled, or open a HITL gate with no trace anywhere the user or the resume classifier can read. **This is the amplifier that turned ISS-097 from an annoyance into a silent hang**, and it is the same structural gap family as ISS-091. FIX-242 stops the gate instance (workers no longer arm one) but does NOT change what the loop forwards, so the `agent_error` / `pipeline_cancelled` half is untouched and live. | Found 2026-08-12 by the ISS-097 investigation and confirmed at `eb12bc7e` by direct measurement (4 `_run_review_gate` entries, 0 frames on the wire). Filed rather than folded because deciding WHICH worker events must be forwarded or at least recorded is a product/vocabulary decision with real blast radius on the FE event contract and on the 5 characterization goldens — not something to settle inside a gating fix. Fix shape: decide the forwarded set (terminal + error at minimum), or record-without-forwarding into `run_events` so the durable log is complete even when the stream stays lifecycle-only. |
| ISS-130 | minor | engine | **OPEN** (filed 2026-08-12 during quick-260812-syf / FIX-242) | **`KernelServices.run_agent` mutates SHARED `ExecutionContext` scratch while N fan-out workers run concurrently.** `kernel_services.py:1254-1291` (now `:1267-1304`) sets and restores `build_task_number`, `build_task_total`, `current_task_block`, `current_prototype_skeleton` and `current_step` on the ONE per-run `ectx` — but under `run_fanout`'s parallel `asyncio.gather` those N coroutines share that single object, so the save/restore is a classic interleaving hazard: worker A's `finally` can restore values while worker B is still mid-flight. **Contamination was NOT observed** — across five instrumented runs every worker wrote its correct distinct file — and the believed reason is that `_run_agent` consumes the scratch before its first `await`, which would make the mutation atomic w.r.t. the event loop. **That was NOT verified**, and it is an undocumented, unenforced invariant: adding any `await` earlier in `_run_agent` would silently break every parallel fan-out with cross-contaminated task blocks. | Found 2026-08-12 by the ISS-097 investigation while establishing why an argument (not `ectx` state) had to carry `invocation_gated` — that rejection is recorded in FIX-242 and stands on this same hazard. Filed separately because confirming or fixing it is a distinct piece of work: either prove the before-first-`await` invariant and pin it with a test/comment so a future edit cannot break it silently, or move the per-invocation scratch off the shared context entirely. |
| ISS-131 | minor | engine | **OPEN** (filed 2026-08-12 during quick-260812-syf / FIX-242 — the fix's own known residual) | **A fan-out step's DECLARED human gate is deduped against an inline gate that no longer fires.** `engine.py:2470` passes `inline_gated=self._should_gate(spec, ectx)` into `_evaluate_gates` at the step boundary; the dedupe exists so a step that both declares `gates:[human]` and has its agent in `gate_agent_ids` does not double-prompt (pre-step with an empty payload, then post-step with the real output). After FIX-242 the inline gate no longer fires for a fan-out step — workers opt out and the step's own agent is never inline-invoked, because the strategy replaces the L7 dispatch — so `inline_gated=True` there is now a stale prediction and the declared gate is skipped for a gate that never opens: **that step gets no gate at all**. Strictly better than the pre-fix behaviour (which HUNG on the same combination) but still a silent loss of a gate the author declared. **Not reachable from any file manifest today** — verified: the only two fan-out/wave steps in `agents/workflows/` (`sample_fanout`, `sample_wave`) both declare `gates: []` — so it is reachable only via composed selections, which can set `gates` and `strategy: fanout_batch` on the same agent. | Found 2026-08-12 by the ISS-097 fix (quick-260812-syf) as a second-order effect, and deliberately left byte-unchanged so FIX-242's INV-3 dormancy argument stayed exact. Fix shape is a DESIGN decision, not a patch: `:2470` needs to know whether this step runs its agent inline, which the kernel must NOT infer from a strategy-name list (that would be exactly the capability-name branch the Ports-and-Adapters rule forbids). The clean shape is for the strategy capability to declare it — e.g. a `runs_agent_inline` property on the strategy port that the dispatch loop reads generically. |
| ISS-132 | minor | engine | **OPEN** (filed 2026-08-12 during quick-260812-syf / FIX-242; deliberately NOT folded — a product decision, not a defect with one right answer) | **The task-loop sibling of ISS-097: ticking a task-loop agent opens ONE GATE PER TASK.** Same root cause — `_should_gate` is per-INVOCATION on `spec.id` while `gate_agent_ids` is a per-STEP selection — and the task loop is the other strategy that turns one step into N invocations of one agent id. Verified at `eb12bc7e`: `task_loop.py` calls `runner.run_agent(step, ctx, task_number=..., ...)` with NO `invocation_gated`, so it takes the default `True`, and unlike `run_fanout` it FORWARDS every event (`yield event`). **So this is the visible, answerable cousin, not a hang**: N sequential gates, all on the one gate key `f"{run_id}:{agent_id}"`, each surfacing properly on the wire and each requiring a human click. Annoying and almost certainly not what the user meant by ticking one box, but nothing is lost and nothing is invisible. Reachable on a live workflow: `prototype-build` is in the frontend agent library (`AgentLibraryData.ts:25`) with `gate: null`, so it is tickable in the launch wizard's gate picker. **FIX-242 deliberately left this alone.** The mechanism it added (`invocation_gated=False`) would close this in one line at the task-loop call site, but whether a per-task review gate should be offered AT ALL is a product decision — some users may genuinely want to review each task of a build — whereas the fan-out case had no defensible reading because the gates were unreachable. Note ISS-090 already records that FIX-228 handles the task-loop redo case defensively inside `_consume_redo`, so the interaction is partly known. | Found 2026-08-12 by the ISS-097 investigation while sweeping for siblings of the invocation-vs-step confusion, and named in the FIX-242 plan's out-of-scope list. Fix shape, once the product question is answered: either pass `invocation_gated=False` at the `task_loop.py` `run_agent` call (suppress entirely, matching fan-out), or gate only the FINAL task, or leave as-is and change the gate picker's copy so "pauses after they finish" is not a promise the task loop breaks N times. |
| ISS-133 | minor | engine | **OPEN** (filed 2026-08-12 during quick-260812-tni / FIX-243; found by reading the closest analog, deliberately not folded into that fix) | **FIX-240 / ISS-121's collision-unsafe append idiom survives, unfixed, in the resume tier's double-drive guard.** `ExecutionEngine._stamp_resume_marker` (`engine.py:6309`) allocates its seq the naive way — `existing = await store.read_events(run_id, after_seq=0)` then `next_seq = max(seq)+1` then `store.append_event(...)` (`engine.py:6339-6352`) — and its outer `except Exception` (`:6353-6354`) degrades **any** failure to `logger.warning`. `IntegrityError` is one. So a `uq_run_events_scope_seq` rejection from the chat lane, which allocates from the SAME per-run seq space on every turn (`append_event_next_seq`), silently discards the `run_resuming` marker. That is the exact failure mode FIX-240 fixed for the engine's event sink by moving it to `append_event_at_or_after`; this call site was not swept. **What the marker is for, so the impact is not guessed:** it is the **double-drive guard** (T-12-03-DOUBLEDRIVE / Pitfall 3) — written BEFORE the driver task so a crash mid-resume is itself resumable and the run is never driven twice. Losing it weakens exactly that guarantee. A second, **unverified** impact: `_reconcile_terminal_status` treats `run_resuming` as an attempt boundary in its `resume_supersedes` test (`run_commands.py:652-660`), so a lost marker could record a successfully-resumed run as `cancelled` (the KAN-120 / ISS-078 symptom) — except the same test also accepts `pipeline_start`, and I did **not** verify whether a resumed run re-emits `pipeline_start`; if it does, this second impact is nil. **Reachability is narrow but real:** the boot-restore call site runs before any client is attached, but `_drive_user_resume` stamps the marker while the user is on the run screen and can chat. **Fix shape:** one-line — swap `append_event` for `append_event_at_or_after(run_id, max+1, …)`, the same edit FIX-240 made. Consider sweeping every remaining naive `read_events → max+1 → append_event` in one pass rather than one call site at a time. | Found while implementing FIX-243, which needed the correct append idiom and therefore read both. Not folded in: it is a different call site with a different owner (the resume tier), and FIX-243's diff is deliberately one function. Verified at HEAD by reading `engine.py:6309-6354`; the `run_resuming` type and payload were read, not recalled. |
| ISS-134 | minor now, **blocker for the ECS migration** | api,infra | **OPEN** (filed 2026-08-12 during quick-260812-tni / FIX-243 as that fix's explicit, recorded scope boundary) | **No cancel path in the system is multi-instance-safe, and FIX-243 makes that boundary load-bearing rather than academic.** Liveness is decided by `_is_run_live` (`run_engine.py:143`), which reads the **process-local** `_PIPELINE_TASKS` / `_PIPELINE_QUEUES` dicts. Today that is sound: the deployment is single-process — `docker-entrypoint.sh` runs `exec uvicorn` with no `--workers`, and there are no replicas — so "not live in this process" == "not live anywhere". **The locked to-be architecture is ECS Fargate** (see `to-be-architecture.html` / the migration playbook), where it stops being sound in two directions at once: a Stop landing on instance B for a run driven on instance A would (i) take FIX-243's not-live branch and write `status="cancelled"` while instance A **keeps billing**, and (ii) be overwritten later by A's own terminal write — so the owner sees a cancelled run that was not cancelled, then a completed one they had stopped. The pre-FIX-243 behaviour was not better (the Stop reached nothing and the run finished anyway); the difference is that the wrong answer is now written down. **Neither ISS-089 option fixes this** — Option B's durable marker would not stop instance A either. **What it actually needs:** a cross-process liveness fact — a run lease / heartbeat row that instance A renews and instance B can read, plus a durable cancel signal A polls at its cooperative boundary. That is a design item for the migration, not a patch. **Do not let FIX-243 be mistaken for multi-instance-safe**; the constraint is recorded in `_cancel_run_without_a_live_driver`'s docstring as well as here. | Recorded as an explicit scope note during FIX-243 rather than discovered as a failure — nothing multi-instance exists to fail yet. Single-process claim verified at HEAD: `exec uvicorn` in `docker-entrypoint.sh` with no `--workers` flag, no `replicas` key in `docker-compose.yml`. |
| ISS-135 | minor | engine | **OPEN** (filed 2026-08-12 during quick-260812-wir) | **FIX-240 residual — `payload_json.seq` diverges from `run_events.seq` on a re-append.** `_RunEventSink.persist` re-stamps `data["seq"]` AFTER `append_event_at_or_after` returns, but the row was already serialised with the ORIGINAL value. Observed on real data: run `1ea6d262` row **248** carries `"seq": 247` in its payload. Replay yields `id: row.seq` (248) with `data.seq` = 247, while the live frame carried 248. **Harmless TODAY** because every de-dup keys on `event_id`, not `data.seq` — which is why it is filed rather than folded. It is a latent trap for anything that ever keys on `data.seq`. Fix shape: re-stamp before persist, or persist the re-stamped dict. **NOT a reason to touch FIX-240 otherwise.** | Found by the ISS-123/126 investigation and re-confirmed during quick-260812-wir. Deliberately not folded into FIX-244/245: it is an engine-sink change with its own proof obligation, and both of those were app-layer/FE. |
| ISS-136 | minor | frontend | **OPEN** (filed 2026-08-12 during quick-260812-wir) | **Four divergent FE definitions of "terminal", and they disagree.** `page.tsx:64` `{completed,failed,cancelled,degraded}` (correct, matches backend `chat_router.py:62`); `useWorkflow.ts`'s `pipeline_reconnected` arm `["completed","failed","cancelled","error"]` (has a status the backend never persists, MISSING `degraded`); `DashboardLayout.tsx:1869` a marker-triad test; `PreviewPanel.tsx` a derived `!(isStreaming \|\| isRunning)`. **FIX-245 already reduced the marker-mapping count 3 -> 1 and pinned its membership EQUAL to `page.tsx:64` by test, so no fifth list was added** — but the remaining consolidation is a refactor with its own blast radius (`useWorkflow.reconnect.test.ts` pins the divergent arm`s behaviour), not part of a render fix. Fold ISS-137 in when doing it. | Enumerated during the ISS-126 root-cause work; scoped OUT of FIX-245 deliberately ("file, don't fold") to keep that diff to the render defect. |
| ISS-137 | trivial | frontend | **OPEN** (filed 2026-08-12 during quick-260812-wir) | **`pipeline_reconnected` is a DEAD frame with a live handler, a live test suite and a divergent status list.** Verified: NO backend path emits it — `grep` over `backend/app` + `backend/agents` finds it only in comments; the WS->SSE cutover replaced it with `stream_attached` (`run_stream.py:215-227`, documented there as "the new-transport replacement for the legacy `pipeline_reconnected` ack"). The FE still carries the full `case "pipeline_reconnected"` reducer arm plus `useWorkflow.reconnect.test.ts` (6 green tests) pinning behaviour nothing can trigger. Left ALONE by FIX-245 on purpose: routing it through the new shared `terminalMarkers` would silently change an untested `status:"error"` path and add `degraded` handling, for zero user benefit, while breaking a green suite. | Discovered while evaluating whether to consolidate the terminal vocabularies (ISS-136) during quick-260812-wir. |
| ISS-138 | major | frontend | **OPEN** (filed 2026-08-12 during quick-260812-wir) | **The FIX-201 legacy->store bridge overwrites the run store's `pipelineState` WHOLESALE, silently discarding any store-only correction.** `page.tsx:1663-1672` runs `runStore.updatePipelineState(runId, () => pipelineState)` — a total replacement, not a merge — whenever the legacy `useWorkflow.pipelineState` changes and the run is both viewed and actively-building. **This cost a real false-green during FIX-245:** the ISS-126 reconciliation was written to the store, the unit tests passed, and the fix was still INVISIBLE on screen because the bridge copied the stale legacy state over it a few hundred ms later. FIX-245 works around it by reconciling BOTH containers through the same helper, but the hazard remains for the next person who patches store state directly. Fix shape: make the bridge merge rather than replace, or make the store the sole owner of `pipelineState` and retire the legacy copy. | Root-caused in a live browser during quick-260812-wir by instrumenting the store — the store entry was observed flipping back to `isRunning:true` ~4s after being set false, with no `handleFrame` involved. |
| ISS-139 | trivial | frontend | **FIXED** (FIX-247 / TEST-030, quick-260813-1b1, commit `d467ce4f`, 2026-08-13) | **`page.tsx`'s `case "pipeline_cancelled": case "pipeline_failed":` switch arm was UNREACHABLE dead code (~`:1527-1562`).** Both types are members of `pipelineTypes`, and that branch `return`s before the `switch` is ever entered — so the arm's `runStore.update(termSrcRunId, {reviewGateData:null, questionnaireData:null})`, its `setLastCancelledRunId`, its `detachRunRef` release and its `setActivePipelineRunId(null)` never ran. A fix applied there would silently have done nothing. Verified by reading the control flow, and it had been pinned by a test in `terminalReopenReconcile.source.test.ts` asserting the ISS-126 fix is NOT inside it. Deleting it needed care: `setLastCancelledRunId` and the `detachRunRef` release may be genuinely wanted on the LIVE terminal path, in which case they belong in the reachable block, not this one. | Verified during the ISS-126 investigation; the trap is that the arm reads exactly like the right place to fix ISS-126. **FIXED 2026-08-13 (FIX-247):** the entire 36-line arm is DELETED, and the care this row asked for was taken — its two genuinely live-relevant effects (`setLastCancelledRunId`, `detachRunRef.current?.()`) are RESCUED onto the reachable live-handler path, **the first time either has ever executed on a live cancel**. The other effects were not rescued: `questionnaireData` is now cleared by the live block on its own account (ISS-140), `reviewGateData` deliberately is NOT (see ISS-140), and `setActivePipelineRunId(null)` was already being called there. **TWO source-lock tests had pinned this dead arm and were GREEN while pinning code that had never once executed** — `terminalReopenReconcile.source.test.ts` (known, from TEST-029) and `liveRunSwitch.fix201.test.ts` (a SECOND lock on the same dead string, unanticipated, found only mid-execution). Both reconciled TIGHTER, never relaxed. **The general lesson: a source lock satisfied only by unreachable code proves nothing about behaviour.** |
| ISS-140 | **major** (RAISED from minor 2026-08-13 — the filed masking claim was wrong; see below) | frontend | **FIXED** (FIX-247 / TEST-030, quick-260813-1b1, commit `d467ce4f`, 2026-08-13) | **The LIVE terminal path cleared only the legacy React state, never the run store — and it WAS user-visible: a Stop at the clarify gate cancelled the run, but the screen never repainted.** The lane stayed on "Clarifying" with Stop still armed and the clarify questionnaire still submittable, on a run the backend had genuinely already cancelled. `page.tsx:1196-1201` called `setReviewGateData(null)` / `setQuestionnaireData(null)` / `setActivePipelineRunId(null)`, but the lane reads the STORE (`page.tsx:2731`, `questionnaireData={runStore.viewed.questionnaireData}`). **THE FILED TEXT WAS WRONG AND IS CORRECTED HERE.** It claimed the defect was "currently MASKED by the `isRunning` conjunct in `runLaneState`, so not user-visible today". `laneClarifyOpen` (`DashboardLayout.tsx:1862`) has **no `isRunning` conjunct at all** — it is the ONE branch of the `runLaneState` ternary (`DashboardLayout.tsx:1864-1871`) not AND-ed with it, deliberately, because clarify precedes `pipeline_start` — and it is evaluated BEFORE the terminal branch, so a lingering store `questionnaireData` OUTRANKED the terminal markers the same cancelled frame had already applied correctly. The masking claim only ever held for the **`reviewGateData`** half of this row (which correctly IS masked, by the gate branch's own `&& isPipelineRunning`), never for the `questionnaireData` half — the actual live, user-visible cause all along. Disproven twice: once by source reading, once by live browser reproduction. | Verified during the ISS-126 investigation (quick-260812-wir). Not folded into FIX-245: that fix covers the REOPEN path; this is the live path and needs its own live proof. **FIXED 2026-08-13 (FIX-247), with that live proof:** the reachable live block now does `if (frameRunId) runStore.update(frameRunId, { questionnaireData: null })` inside the existing run-scoping `if (!isForeignFrame)` guard (`page.tsx:1206`). **`reviewGateData` is deliberately NOT added to the store patch** — that half genuinely is masked, and clearing it would erase the historical gate card from a reopened transcript, which `terminalReopenReconcile.source.test.ts` already locks against. `activePipelineRunId` likewise not added: no store write for it exists, and none is needed once `questionnaireData` is cleared. Proven by TS-R-05 (seen RED independently twice, identical failure both times: lane stuck at "Clarifying", `data-status-tone="running"`, timeout waiting for /Cancelled/) plus two fresh cheap live Bedrock runs sampled at t+3s/8s/15s/25s after Stop — FIXPROBE (clarify armed) and GATEPROBE (review gate armed, the regression control) both settle to "Cancelled" with no Stop, no clarify submit and no gate actions. |
| ISS-141 | minor | frontend | **OPEN** (filed 2026-08-12 during quick-260812-wir) | **A reopened TERMINAL run still renders an actionable clarify card.** `useRunChat.ts`'s terminal auto-resolve covers only `cardKind === "gate"`, so the narrator CLARIFY card and its "Answer in Steps" affordance stay unresolved and clickable on a finished run. Independent of the destroyed-event damage and cheap (one extra `cardKind` in one existing condition), but it is a transcript-seed concern, not the run-state defect FIX-245 addresses, so it was filed rather than folded. NOTE: FIX-245 clears the run's `questionnaireData` on a terminal reopen, which removes the clarify PANEL; this row is about the chat CARD in the transcript, which is a different surface. | Found during the ISS-126 investigation; re-scoped during quick-260812-wir after FIX-245 changed the adjacent behaviour. |
| ISS-142 | minor | backend,frontend | **OPEN** (filed 2026-08-12 during quick-260812-wir, evaluated and deliberately NOT built) | **The WS->SSE cutover DROPPED `status` from the reconnect ack, breaking parity with locked decision D-13.** `IMPLEMENTATION-REGISTER.md` records for D-13/12-09 that the `pipeline_reconnected` reply carries `live`/`status`/`replayed_through_seq`. Its replacement, `stream_attached` (`run_stream.py:215-227`), is documented in that same file as "the new-transport replacement for the legacy `pipeline_reconnected` ack" — and emits only `{pipeline_run_id, live, replayed_through_seq}`. **Restoring `status` is cheap and is PARITY, not invention:** `run_is_terminal`/`workflow_run.status` is ALREADY computed in request scope for FIX-240's gate-re-arm guard, the handshake is synthesised per attach so it is NOT a durable `run_events` row (sidestepping both objections that killed the wire-synthesis option), and it reuses `replayed_through_seq` as its `id:` so `Last-Event-ID` is untouched. **Evaluated during FIX-245 and NOT built, for two reasons.** (1) It does not fix ISS-126 at all — a terminal run never opens an SSE stream (`page.tsx:2171`), so it cannot subsume the FE reconciliation. (2) The claim that it merely "revives an already-written handler" is **inaccurate**: `useWorkflow.ts`'s handler is keyed on `pipeline_reconnected`, an event NOTHING emits (ISS-137), so this needs a NEW `stream_attached` handler — real work, not a one-field re-add. **Its real value is the cases FIX-245 does not cover:** a reload while the record is still non-terminal, and a cursored reattach. If built, it MUST route through the shared `terminalMarkers`/`applyTerminalStatus` added by FIX-245 rather than introducing a fresh status list (INV-12), and must keep the same one-way directionality. | Evaluated during quick-260812-wir per an explicit instruction to consider it as a possible substitute for the FE fix; rejected as a substitute on evidence (the symptom does not traverse the SSE path) and filed as an independent parity item rather than folded. |
| ISS-143 | minor | frontend | **OPEN** (filed 2026-08-13 during quick-260813-1b1 / FIX-247, deliberately NOT folded — a different defect on a different data path) | **The header "N Running" pill goes stale after a live cancel.** `AppHeader.tsx` derives the pill from `recentRuns` (`LIVE_STATUSES.has(r.status)`, which includes `waiting_for_user`), and `recentRuns` is refreshed by `getWorkflows(...)` at many call sites in `page.tsx` — but there is **NO refresh in the `pipeline_cancelled`/`pipeline_failed` handling arm**, and no polling timer. So after a cancel the pill keeps counting the just-cancelled run as "Running" until the next unrelated `recentRuns` refresh or a page reload. **Confirmed independent of the clarify-repaint defect FIX-247 fixes:** it reproduces identically on a clean review-gate cancel — the GATEPROBE control run in that same task showed a stale "2 Running"/"3 Running" pill even though that run's lane correctly repainted to Cancelled. **Fix shape:** add a `getWorkflows(...)` -> `setRecentRuns(...)` refresh call to the cancelled arm, mirroring the pattern already used at other `page.tsx` call sites (e.g. `page.tsx:1170-1186`). | Found during the live-verification pass of quick-260813-1b1 / FIX-247 — both by the prior investigation that fix was based on, and independently reproduced in that task's own GATEPROBE screenshots. |
| ISS-144 | minor | backend,api | **OPEN** (filed 2026-08-13 during quick-260813-1b1 / FIX-247, deliberately NOT folded — backend, and it carries an undecided contract question) | **`POST /api/runs/{run_id}/answers` accepts a terminal run — no 409 guard, unlike `/gate`.** `backend/app/api/run_commands.py:327-352` (`submit_answers`) checks ownership (`_review_gate_owned_by`) before writing but has **NO terminal-run check**, unlike the sibling `POST /{id}/gate` (`:255-269`) which calls `_review_gate_run_is_terminal(gate_key)` and returns HTTP 409 `pipeline_not_running` — **that exact helper is already imported into the same module (`run_commands.py:82`) and simply not called on the `/answers` path.** Consequence: a clarify answer submitted against an already-cancelled run returns HTTP 200 `{"ok": true, ...}` and silently no-ops — the run does not resume (its queues, tasks and cancel-events were already torn down by `_cleanup_pipeline`), the user's answers vanish with no error shown, and `set_questionnaire_responses` re-creates a small dict entry plus a fresh `asyncio.Event` with no waiter and no eviction path per stray click — a minor per-click leak on a dead run. **Fix shape:** add the same `_review_gate_run_is_terminal(run_id)` -> 409 `pipeline_not_running` guard used by `/gate`, mirroring `run_commands.py:261-269`. **Open contract question, not yet decided:** should this endpoint 409 (matching `/gate`) or degrade some other way — 409 is recommended for consistency, but flag it as a product decision, not purely a bug. | Confirmed by reading both handlers side by side during the quick-260813-1b1 / FIX-247 investigation; independently re-confirmed by the orchestrating agent. |
| ISS-145 | **major** | test-infra | **OPEN** (filed 2026-08-13 during quick-260813-1b1 / FIX-247, explicitly out of that fix's scope) | **Frontend test-suite baselines recorded elsewhere in this repo are stale by roughly an order of magnitude — re-baseline before gating any future fix on suite colour.** Prior handoff/memory documentation records the frontend vitest baseline as "~8 known reds" and the mocked Playwright e2e suite as "132/0". Measured fresh during quick-260813-1b1, independently TWICE (once by the fix's executor, once by the orchestrating agent, both figures identical): **vitest is 147 failed / 894 passed / 1041 total, 29 failed test files out of 126**; **the mocked e2e baseline (page.tsx reverted to pre-fix) is 34 failed / 108 passed / 43 skipped**. Spot-checked one vitest failure (`contentSourceRunScope.source.test.ts`, an assertion about `pipeline_complete`/`isForeignCompletion`, unrelated to FIX-247) and confirmed by direct grep that its expected string is **absent from `page.tsx` both at HEAD and at `d2d2da53`**, two commits before the fix landed — so that failure pre-dates the task entirely. `WorkflowHistory.test.tsx` fails with a live-looking `TypeError: Cannot read properties of undefined (reading 'length')` at `WorkflowHistory.tsx:207` (`runs.length` on an undefined `runs` prop) — a **real, unrelated regression somewhere on this branch**. Several other files fail 100% of their own tests (`PreviewPanel.test.tsx`, `PreviewPanel.genericDeliverable.test.tsx`, `RunConnectionProvider.test.tsx`), which points at a shared module-level/harness cause rather than dozens of independent defects. **Fix shape:** a dedicated investigation pass to re-baseline both suites (ideally diffing against a known-good historical commit to find when and how the drift happened), then update whatever memory/handoff docs currently claim the stale figures. | Measured directly during quick-260813-1b1's regression sweep. Neither caused nor fixed by FIX-247 — that fix's own regression proof used a PAIRED before/after measurement in one session precisely BECAUSE the recorded baseline could not be trusted (see the TEST-030 block in FIX-TEST-REGISTER.md for the paired numbers). |
