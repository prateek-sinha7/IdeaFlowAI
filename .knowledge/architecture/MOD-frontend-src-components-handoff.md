---
id: MOD-frontend-src-components-handoff
type: architecture
kind: state
title: frontend/src/components/handoff — module
path: frontend/src/components/handoff
language: typescript
file_count: 10
code_signature: fa5587a289b2
last_synced: '2026-08-29'
---

<!-- AUTO-GENERATED BELOW THIS LINE — regenerated by tools/knowledge/build_architecture.py -->

### Files (10)
- [frontend/src/components/handoff/DiffView.tsx](../../frontend/src/components/handoff/DiffView.tsx)
- [frontend/src/components/handoff/HandoffAgentPanel.tsx](../../frontend/src/components/handoff/HandoffAgentPanel.tsx)
- [frontend/src/components/handoff/HandoffPreviewPanel.tsx](../../frontend/src/components/handoff/HandoffPreviewPanel.tsx)
- [frontend/src/components/handoff/HandoffWorkflow.test.tsx](../../frontend/src/components/handoff/HandoffWorkflow.test.tsx)
- [frontend/src/components/handoff/HandoffWorkflow.tsx](../../frontend/src/components/handoff/HandoffWorkflow.tsx)
- [frontend/src/components/handoff/IntegrationsCard.copyFeedback.test.tsx](../../frontend/src/components/handoff/IntegrationsCard.copyFeedback.test.tsx)
- [frontend/src/components/handoff/IntegrationsCard.tsx](../../frontend/src/components/handoff/IntegrationsCard.tsx)
- [frontend/src/components/handoff/ReportViews.tsx](../../frontend/src/components/handoff/ReportViews.tsx)
- [frontend/src/components/handoff/types.ts](../../frontend/src/components/handoff/types.ts)
- [frontend/src/components/handoff/useHandoffPipelineState.ts](../../frontend/src/components/handoff/useHandoffPipelineState.ts)

### Depends on
- [MOD-frontend-src-hooks](MOD-frontend-src-hooks.md)
- [MOD-frontend-src-lib](MOD-frontend-src-lib.md)

### Depended on by
- [MOD-frontend-src-app](MOD-frontend-src-app.md)

### External
@testing-library/react, lucide-react, motion, next, react, vitest
<!-- /AUTO-GENERATED -->

## Purpose

Renders the `/flowin-handoff` feature: a standalone page where an IDE-launched coding pipeline
(coding agent → test agent → compliance agent, ending in a draft GitHub PR) streams its
progress. Owns the agent-progress panel, the diff/test/compliance preview tabs, and the GitHub
PAT / API-key onboarding card. Mounted by the app router at the handoff route; nothing else in
the app imports it.

## Shape

- [HandoffWorkflow.tsx](../../frontend/src/components/handoff/HandoffWorkflow.tsx) — page root. Auth-gates on `getToken()`, fetches the handoff session via
  `getHandoff`/`startHandoff`, replays persisted `pipeline_output` into the reducer on reopen,
  and subscribes to `useHandoffSocket` for live frames while running. Renders the two-pane
  layout (`HandoffAgentPanel` + `HandoffPreviewPanel`) or an onboarding/expired/loading state.
- [useHandoffPipelineState.ts](../../frontend/src/components/handoff/useHandoffPipelineState.ts) — a `useReducer` hook, private to this folder, that projects the
  shared `StreamMessage`/`HandoffStreamMessage` envelope (`phase_start`, `agent_thinking`,
  `agent_complete`, `pr_created`, `pipeline_complete`, `handoff_error`, ...) into
  `HandoffPipelineState` (per-agent status, phase log, edit results, PR info).
- [HandoffAgentPanel.tsx](../../frontend/src/components/handoff/HandoffAgentPanel.tsx) — left rail: one `AgentCard` per pipeline agent (coding/test/compliance)
  with status pill, duration, and a scrolling phase/step log.
- [HandoffPreviewPanel.tsx](../../frontend/src/components/handoff/HandoffPreviewPanel.tsx) — right pane: tab bar (Diff / Tests / Compliance) that auto-follows
  the most recently completed agent unless the user has clicked a tab; renders `DiffView` or
  `ReportViews`' `TestReportView`/`ComplianceReportView`; shows the PR strip and pipeline-error
  strip below the tab body.
- [DiffView.tsx](../../frontend/src/components/handoff/DiffView.tsx) — renders proposed file edits as stacked red/green code blocks (no diff library
  — the agent already emits minimal `old_string`/`new_string`), plus tests-added and
  out-of-scope follow-up lists.
- [ReportViews.tsx](../../frontend/src/components/handoff/ReportViews.tsx) — `TestReportView` and `ComplianceReportView`, both
  verdict-pill-plus-sectioned-list layouts.
- [IntegrationsCard.tsx](../../frontend/src/components/handoff/IntegrationsCard.tsx) — self-contained credentials panel (install command, GitHub PAT
  save/delete, VelocityAI API key create/list/revoke), reused both as the standalone
  `/handoff/settings` page and inline as the onboarding state inside `HandoffWorkflow`.
- [types.ts](../../frontend/src/components/handoff/types.ts) — all handoff-local types (`HandoffAgentState`, `HandoffPipelineState`, report
  shapes) plus `HandoffStreamMessage`, a deliberately widened event envelope so the module
  never has to extend the shared `StreamMessage` enum.

## Why this shape

The whole feature is **deliberately self-contained**: its own types, its own reducer, its own
WS-message narrowing at the module boundary — so it can add pipeline-specific events
(`pr_created`, `handoff_error`) without touching the shared workflow/chat `StreamMessage`
contract.

It mirrors the layout of the existing pptx/user-stories preview surfaces (left agent rail,
right tabbed preview) on purpose, so the visual language is familiar even though the domain (a
coding pipeline, not a document pipeline) is new.
