---
id: MOD-frontend-src-lib
type: architecture
kind: state
title: frontend/src/lib — module
path: frontend/src/lib
language: typescript
file_count: 42
code_signature: d1a6e271b258
prose_symbols_signature: da39a3ee5e6b
prose_signature: d1a6e271b258
last_synced: '2026-08-21'
---

<!-- AUTO-GENERATED BELOW THIS LINE — regenerated by tools/knowledge/build_architecture.py -->

### Files (42)
- [frontend/src/lib/__tests__/wsRunScope.test.ts](../../frontend/src/lib/__tests__/wsRunScope.test.ts)
- [frontend/src/lib/api-handoff.test.ts](../../frontend/src/lib/api-handoff.test.ts)
- [frontend/src/lib/api-handoff.ts](../../frontend/src/lib/api-handoff.ts)
- [frontend/src/lib/api.getRunArtifacts.test.ts](../../frontend/src/lib/api.getRunArtifacts.test.ts)
- [frontend/src/lib/api.getRunFamily.test.ts](../../frontend/src/lib/api.getRunFamily.test.ts)
- [frontend/src/lib/api.requestTimeout.test.ts](../../frontend/src/lib/api.requestTimeout.test.ts)
- [frontend/src/lib/api.test.ts](../../frontend/src/lib/api.test.ts)
- [frontend/src/lib/api.ts](../../frontend/src/lib/api.ts)
- [frontend/src/lib/clarifications.test.ts](../../frontend/src/lib/clarifications.test.ts)
- [frontend/src/lib/clarifications.ts](../../frontend/src/lib/clarifications.ts)
- [frontend/src/lib/constants.ts](../../frontend/src/lib/constants.ts)
- [frontend/src/lib/design-system-colors.ts](../../frontend/src/lib/design-system-colors.ts)
- [frontend/src/lib/design-system-preview.ts](../../frontend/src/lib/design-system-preview.ts)
- [frontend/src/lib/entitlements.ts](../../frontend/src/lib/entitlements.ts)
- [frontend/src/lib/env.ts](../../frontend/src/lib/env.ts)
- [frontend/src/lib/exporters/auditExporter.ts](../../frontend/src/lib/exporters/auditExporter.ts)
- [frontend/src/lib/exporters/pptExporter.ts](../../frontend/src/lib/exporters/pptExporter.ts)
- [frontend/src/lib/exporters/prototypeExporter.ts](../../frontend/src/lib/exporters/prototypeExporter.ts)
- [frontend/src/lib/exporters/storyExporter.ts](../../frontend/src/lib/exporters/storyExporter.ts)
- [frontend/src/lib/launchDraft.parity.test.ts](../../frontend/src/lib/launchDraft.parity.test.ts)
- [frontend/src/lib/launchDraft.ts](../../frontend/src/lib/launchDraft.ts)
- [frontend/src/lib/parseFailedAgents.ts](../../frontend/src/lib/parseFailedAgents.ts)
- [frontend/src/lib/parsers/pptParser.ts](../../frontend/src/lib/parsers/pptParser.ts)
- [frontend/src/lib/parsers/streamParser.ts](../../frontend/src/lib/parsers/streamParser.ts)
- [frontend/src/lib/parsers/userStoryParser.ts](../../frontend/src/lib/parsers/userStoryParser.ts)
- [frontend/src/lib/ppt-api.ts](../../frontend/src/lib/ppt-api.ts)
- [frontend/src/lib/prototype-api.ts](../../frontend/src/lib/prototype-api.ts)
- [frontend/src/lib/resizeImage.test.ts](../../frontend/src/lib/resizeImage.test.ts)
- [frontend/src/lib/resizeImage.ts](../../frontend/src/lib/resizeImage.ts)
- [frontend/src/lib/routes.test.ts](../../frontend/src/lib/routes.test.ts)
- [frontend/src/lib/routes.ts](../../frontend/src/lib/routes.ts)
- [frontend/src/lib/runInput.test.ts](../../frontend/src/lib/runInput.test.ts)
- [frontend/src/lib/runInput.ts](../../frontend/src/lib/runInput.ts)
- [frontend/src/lib/runStats.ts](../../frontend/src/lib/runStats.ts)
- [frontend/src/lib/skillIcons.ts](../../frontend/src/lib/skillIcons.ts)
- [frontend/src/lib/sseFrame.test.ts](../../frontend/src/lib/sseFrame.test.ts)
- [frontend/src/lib/sseFrame.ts](../../frontend/src/lib/sseFrame.ts)
- [frontend/src/lib/workflowChaining.test.ts](../../frontend/src/lib/workflowChaining.test.ts)
- [frontend/src/lib/workflowChaining.ts](../../frontend/src/lib/workflowChaining.ts)
- [frontend/src/lib/workflowIcons.ts](../../frontend/src/lib/workflowIcons.ts)
- [frontend/src/lib/wsReplayState.test.ts](../../frontend/src/lib/wsReplayState.test.ts)
- [frontend/src/lib/wsReplayState.ts](../../frontend/src/lib/wsReplayState.ts)

### Depends on
- [MOD-frontend-src-components-workflow](MOD-frontend-src-components-workflow.md)
- [MOD-frontend-src-hooks](MOD-frontend-src-hooks.md)

### Depended on by
- [MOD-frontend-src](MOD-frontend-src.md)
- [MOD-frontend-src-app](MOD-frontend-src-app.md)
- [MOD-frontend-src-components-analytics](MOD-frontend-src-components-analytics.md)
- [MOD-frontend-src-components-catalog](MOD-frontend-src-components-catalog.md)
- [MOD-frontend-src-components-chat](MOD-frontend-src-components-chat.md)
- [MOD-frontend-src-components-handoff](MOD-frontend-src-components-handoff.md)
- [MOD-frontend-src-components-history](MOD-frontend-src-components-history.md)
- [MOD-frontend-src-components-home](MOD-frontend-src-components-home.md)
- [MOD-frontend-src-components-layout](MOD-frontend-src-components-layout.md)
- [MOD-frontend-src-components-library](MOD-frontend-src-components-library.md)
- [MOD-frontend-src-components-preview](MOD-frontend-src-components-preview.md)
- [MOD-frontend-src-components-results](MOD-frontend-src-components-results.md)
- [MOD-frontend-src-components-savedworkflows](MOD-frontend-src-components-savedworkflows.md)
- [MOD-frontend-src-components-settings](MOD-frontend-src-components-settings.md)
- [MOD-frontend-src-components-sidebar](MOD-frontend-src-components-sidebar.md)
- [MOD-frontend-src-components-ui](MOD-frontend-src-components-ui.md)
- [MOD-frontend-src-components-workflow](MOD-frontend-src-components-workflow.md)
- [MOD-frontend-src-hooks](MOD-frontend-src-hooks.md)
- [MOD-frontend-src-providers](MOD-frontend-src-providers.md)

### External
lucide-react, pptxgenjs, vitest
<!-- /AUTO-GENERATED -->

## Purpose

The frontend's non-UI core: everything that talks to the FastAPI backend, and every
piece of parsing, serialization and formatting logic that would otherwise live inside a
React closure. [api.ts](../../frontend/src/lib/api.ts) carries the bulk of it — ~55 endpoint functions covering auth,
chats, workflow runs, run commands, analytics, catalog discovery and the audit tabs.
[sseFrame.ts](../../frontend/src/lib/sseFrame.ts) and [wsReplayState.ts](../../frontend/src/lib/wsReplayState.ts) carry the live-run wire handling that
`frontend/src/hooks` builds its stream state on.

If you are looking for where a backend field name is decided, where a `=== marker ===`
block is parsed, or where a deliverable becomes a download, this is the module. It is
depended on by every `frontend/src/components/*` module and by `frontend/src/hooks`; it
imports almost nothing back — one `DiscoveryAnswers` type from a workflow component
([launchDraft.ts](../../frontend/src/lib/launchDraft.ts)) and one hook import inside a test file.

## Shape

**There is no single API client. Four separate REST clients each own a private fetch
wrapper, and the only thing they genuinely share is [env.ts](../../frontend/src/lib/env.ts).** Only [api.ts](../../frontend/src/lib/api.ts)'s
`request` has the abort timeout and the typed `ApiError`; the other three throw plain
`Error`.

- **Base-URL resolution** — [env.ts](../../frontend/src/lib/env.ts) exports the single `ENV.API_URL` constant, resolved
  as explicit `NEXT_PUBLIC_API_URL` -> same-origin (non-local browser host) ->
  `http://localhost:8000`. Every client above reads it; nothing else in the frontend
  builds a backend URL.

- **The main client** — [api.ts](../../frontend/src/lib/api.ts). `request<T>` wraps `fetch` with an `AbortController`
  (`REQUEST_TIMEOUT_MS`, 30s) and converts failures to `ApiError`; `authHeaders` adds the
  bearer JWT from `localStorage`. `getToken`/`setToken`/`clearToken` and `logout` own the
  token lifecycle. Backend rows arrive snake_case; `ensureUTC` plus a small set of
  `normalize*` helpers convert the chat and workflow-run shapes to the camelCase types in
  `@/types`, but the high-traffic reads (`getRunEvents`, `getRunArtifacts`,
  `getRunFamily`, the audit reads) deliberately return the raw wire interfaces.

- **The satellite clients** — [api-handoff.ts](../../frontend/src/lib/api-handoff.ts) (handoff sessions, GitHub PAT, API keys),
  [ppt-api.ts](../../frontend/src/lib/ppt-api.ts) and [prototype-api.ts](../../frontend/src/lib/prototype-api.ts) (template / design-system catalogs). Each defines
  its own wrapper — `authedJson`, `apiFetch`, `authFetch` respectively. The two catalog
  clients also export unauthenticated URL builders (`getTemplatePreviewUrl`,
  `getPPTTemplateThumbnailUrl`, `getDesignSystemPreviewUrl` and siblings) intended to be
  dropped straight into `<iframe src>` / `<img src>`.

- **Live-run wire handling** — [sseFrame.ts](../../frontend/src/lib/sseFrame.ts) exports `parseSseBlock`, the one SSE block
  parser used by both the SSE down-channel and the streamed POST up-channel; it unwraps
  the `{type, data}` envelope and returns `null` for malformed blocks rather than
  throwing. [wsReplayState.ts](../../frontend/src/lib/wsReplayState.ts) holds the reconnect-idempotency helpers —
  `shouldApplyEvent`, `resetReplayState`, `isForeignRunFrame`, `resolveFrameRunId`,
  `isAgentScopedFrame`.

- **URL parsing/building (spec 015-frontend-routing)** — [routes.ts](../../frontend/src/lib/routes.ts) is the single source for
  both directions: `routes.*()` builder functions construct every URL the app pushes, and
  `parseViewPath(segments)` parses `[...view]/page.tsx`'s catch-all segments back into a
  `ParsedView` discriminated union (`{screen, ...params}`). An unrecognized `/create/{type}`
  segment resolves to `{screen:"create-workflow", pipelineType}` rather than falling through to
  `{screen:"unknown"}` (which 404s) — this is what lets a spec-014 `ex_A*` (or any future
  catalog-only) pipeline's launch URL survive a cold load/refresh, not just an in-app click.

- **Marker and draft serialization** — [runInput.ts](../../frontend/src/lib/runInput.ts) (`parseRunInput`) decomposes a run's
  `input` string into its FE-composed marker families (attachments, existing-artifact
  block, revision request, chain context, preferences); it is format-tolerant and never
  throws. [launchDraft.ts](../../frontend/src/lib/launchDraft.ts) (`buildLaunchDraft`, `buildDiscoveryValue`) serializes the
  wizard-to-dashboard sessionStorage hand-off, with load-bearing key order.

- **Pure domain helpers** — [clarifications.ts](../../frontend/src/lib/clarifications.ts), [workflowChaining.ts](../../frontend/src/lib/workflowChaining.ts),
  [parseFailedAgents.ts](../../frontend/src/lib/parseFailedAgents.ts), [runStats.ts](../../frontend/src/lib/runStats.ts), [entitlements.ts](../../frontend/src/lib/entitlements.ts) (tier-to-pipeline gating),
  [constants.ts](../../frontend/src/lib/constants.ts), [resizeImage.ts](../../frontend/src/lib/resizeImage.ts), and the [design-system-colors.ts](../../frontend/src/lib/design-system-colors.ts) ->
  [design-system-preview.ts](../../frontend/src/lib/design-system-preview.ts) pair (palette extraction feeding
  `generateDesignSystemPreviewHtml`). Small, mostly-pure, each consumed by one or two
  call sites.

- **`parsers/` and `exporters/`** — `parsers/` turns agent output into structures
  (`parsePPTSlideData`, `parseUserStoryMarkdown`, `parseStreamMessage`) and back
  (`serializePPTSlideData`, `formatUserStoryMarkdown`). `exporters/` turns those
  structures into browser Blob downloads (`exportToPptx` via `pptxgenjs`,
  `exportPrototype`, `exportUserStories`, `exportAuditCSV`/`exportAuditJSON`). No
  exporter makes a network call — export is entirely client-side.

- **Co-located vitest suites** — most non-trivial files have a `*.test.ts` sibling, plus
  [launchDraft.parity.test.ts](../../frontend/src/lib/launchDraft.parity.test.ts) and [__tests__/wsRunScope.test.ts](../../frontend/src/lib/__tests__/wsRunScope.test.ts).

## Why this shape

The four-client split is the one thing to know before adding an endpoint. It is not a
layering decision — the docstrings say each satellite was split off "to keep the surface
for the new feature isolated", and [prototype-api.ts](../../frontend/src/lib/prototype-api.ts) / [ppt-api.ts](../../frontend/src/lib/ppt-api.ts) were written to
mirror each other. The cost is that only [api.ts](../../frontend/src/lib/api.ts) gained the timeout hardening: its
comment records that a saturated browser connection pool (six per origin, consumed by
per-run SSE streams) let calls hang forever on a silent screen, which is why `request`
aborts at 30s and rethrows as `ApiError(0, ...)`. The other three wrappers never got that
treatment, so a hung handoff or catalog call still hangs. Whether that was a decision or
an oversight is not recorded.

[api.ts](../../frontend/src/lib/api.ts) is a flat function-per-endpoint file rather than a class or a generated client
because the types are hand-mirrored against backend Pydantic models, cited per interface
in the docstrings. The raw-versus-normalized split follows from that: normalization
exists only where the frontend needs camelCase domain objects (`WorkflowRun`,
`ChatSession`), and the event/artifact/audit reads skip it so consumers can dedupe on the
same `event_id`/`seq` keys the backend logs — the backend's own logs stay greppable
against frontend state.

[env.ts](../../frontend/src/lib/env.ts) resolves to same-origin rather than an absolute URL because production images
are built with `NEXT_PUBLIC_*` empty on purpose — one environment-agnostic image promoted
dev -> stage -> prod, with nginx proxying `/api`. The comment records that the old
absolute `http://localhost:8000` fallback broke every deployed browser: it pointed at the
visitor's own machine and was rejected by the page's `connect-src` CSP.

The parse/serialize/replay logic sits here rather than in components because closures
inside React components cannot be imported or unit-tested. Several of these modules cite
INV-12 ("no dual implementation") explicitly, and each names the duplicate it replaced:
[sseFrame.ts](../../frontend/src/lib/sseFrame.ts) was lifted verbatim out of `useRunStream`'s `dispatchBlock` so the SSE
down-channel and the streamed POST up-channel share one parse; [runInput.ts](../../frontend/src/lib/runInput.ts) replaced
`safeCleanBrief` in `DashboardLayout` and a manual marker scan in `RevisionFamilyView`;
[launchDraft.ts](../../frontend/src/lib/launchDraft.ts) replaced byte-identical inline serialization in two retired route-split
template pages, which is why [launchDraft.parity.test.ts](../../frontend/src/lib/launchDraft.parity.test.ts) asserts the output is
byte-identical to the retired implementation and why the object-literal key order in
`buildLaunchDraft` must not be reordered. [runStats.ts](../../frontend/src/lib/runStats.ts) cites the same rule and records
its own remaining duplicate: the in-panel `WorkflowHistory` detail still carries local
copies of the formatters.
