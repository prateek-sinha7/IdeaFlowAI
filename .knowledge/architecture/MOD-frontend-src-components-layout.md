---
id: MOD-frontend-src-components-layout
type: architecture
kind: state
title: frontend/src/components/layout — module
path: frontend/src/components/layout
language: typescript
file_count: 7
code_signature: 8dc39b81b76a
last_synced: '2026-08-18'
---

<!-- AUTO-GENERATED BELOW THIS LINE — regenerated by tools/knowledge/build_architecture.py -->

### Files (7)
- [frontend/src/components/layout/AppHeader.a11y.test.tsx](../../frontend/src/components/layout/AppHeader.a11y.test.tsx)
- [frontend/src/components/layout/AppHeader.tsx](../../frontend/src/components/layout/AppHeader.tsx)
- [frontend/src/components/layout/DashboardLayout.catalogHome.test.tsx](../../frontend/src/components/layout/DashboardLayout.catalogHome.test.tsx)
- [frontend/src/components/layout/DashboardLayout.fix194.test.tsx](../../frontend/src/components/layout/DashboardLayout.fix194.test.tsx)
- [frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx](../../frontend/src/components/layout/DashboardLayout.laneTitle.test.tsx)
- [frontend/src/components/layout/DashboardLayout.launchLatch.test.tsx](../../frontend/src/components/layout/DashboardLayout.launchLatch.test.tsx)
- [frontend/src/components/layout/DashboardLayout.tsx](../../frontend/src/components/layout/DashboardLayout.tsx)

### Depends on
- [MOD-frontend-src](MOD-frontend-src.md)
- [MOD-frontend-src-components-analytics](MOD-frontend-src-components-analytics.md)
- [MOD-frontend-src-components-catalog](MOD-frontend-src-components-catalog.md)
- [MOD-frontend-src-components-chat](MOD-frontend-src-components-chat.md)
- [MOD-frontend-src-components-history](MOD-frontend-src-components-history.md)
- [MOD-frontend-src-components-library](MOD-frontend-src-components-library.md)
- [MOD-frontend-src-components-preview](MOD-frontend-src-components-preview.md)
- [MOD-frontend-src-components-results](MOD-frontend-src-components-results.md)
- [MOD-frontend-src-components-savedworkflows](MOD-frontend-src-components-savedworkflows.md)
- [MOD-frontend-src-components-settings](MOD-frontend-src-components-settings.md)
- [MOD-frontend-src-components-ui](MOD-frontend-src-components-ui.md)
- [MOD-frontend-src-components-workflow](MOD-frontend-src-components-workflow.md)
- [MOD-frontend-src-context](MOD-frontend-src-context.md)
- [MOD-frontend-src-hooks](MOD-frontend-src-hooks.md)
- [MOD-frontend-src-lib](MOD-frontend-src-lib.md)
- [MOD-frontend-src-providers](MOD-frontend-src-providers.md)

### Depended on by
- [MOD-frontend-src-app](MOD-frontend-src-app.md)

### External
@testing-library/react, @testing-library/user-event, lucide-react, motion, next, react, vitest
<!-- /AUTO-GENERATED -->

## Purpose

The application shell: `AppHeader` (top nav, running-pipeline badge/dropdown, notification bell, profile
menu) and `DashboardLayout`.

**`DashboardLayout` is the composition root** — it owns `mainView` routing and mounts every dashboard-era
page: home, library, history, settings, analytics, saved workflows, the workflow composer, and the live
execution screen. Nearly every other `components/*` page module is depended on by this one.

## Shape

- [AppHeader.tsx](../../frontend/src/components/layout/AppHeader.tsx) — pure presentational nav bar. Takes `currentPage`/`onNavigate` plus live-run props
  (`recentRuns`, `activePipelineRunId`, `runAgentsCompletedMap`) and derives the single-run badge or
  multi-run dropdown from them; the profile dropdown routes to settings/analytics/history via
  `onNavigate`. No data fetching of its own.

- [DashboardLayout.tsx](../../frontend/src/components/layout/DashboardLayout.tsx) — the large stateful root. Holds `mainView`
  (`"home" | "library" | "history" | "settings" | "analytics" | "input" | "execution" | "catalog" |
  "saved-workflows" | "composer"`) and switches on it near the bottom of the render to mount the
  matching page component (`HomeLaunchGrid`, `LibraryPage`, `WorkflowHistory`, `AccountSettings`,
  `AnalyticsPage`, `SavedWorkflowsPage`, `RunChatLane` + `PreviewPanel` for execution, etc.) under a
  shared `AppHeader`. It owns:
  - pipeline-run notification/toast bookkeeping (mapping SSE/WS pipeline events to the
    `useNotifications` store)
  - the revision-launch handlers (`handleRevisePpt`/`handleReviseUserStory`/`handleRevisePrototype`/
    `handleReviseAppBuilder`)
  - the questionnaire/gate wiring passed down to the execution view

  It consumes `useRunConnection` (SSE transport), `useNotifications`, and `SkillsHooksContext`.

- `*.test.tsx` files cover accessibility (`AppHeader.a11y`) and layout-specific regression fixes
  (`catalogHome`, `fix194`, `laneTitle`, `launchLatch`).

## Why this shape

`DashboardLayout` is the **historical single composition root** for the whole dashboard — every
page-level module funnels through its `mainView` switch rather than each owning its own route, which
is why its import list spans nearly all other `components/*` directories.

The size and the dense inline comments (many tagged with `FIX-`/`ISS-`/`BUG-` ids) reflect that it has
absorbed most cross-cutting concerns — concurrent-run notification ownership, SSE reconnect replay,
revision-family linkage — that would otherwise need their own coordination layer. **It is a module that
accretes fixes rather than one that was designed flat.**
