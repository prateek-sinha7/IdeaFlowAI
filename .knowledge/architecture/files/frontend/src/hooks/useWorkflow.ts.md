---
id: FILE-frontend/src/hooks/useWorkflow.ts
type: architecture-file
kind: state
title: useWorkflow.ts — file
file: frontend/src/hooks/useWorkflow.ts
module: MOD-frontend-src-hooks
language: typescript
depends_on_count: 2
depended_on_by_count: 12
code_signature: ecd103626bc5
---

<!-- AUTO-GENERATED BELOW THIS LINE — regenerated by tools/knowledge/build_architecture.py -->

**Source:** [frontend/src/hooks/useWorkflow.ts](../../../../../../frontend/src/hooks/useWorkflow.ts)

**Module:** [MOD-frontend-src-hooks](../../../../MOD-frontend-src-hooks.md)

### Depends on (2)
- [frontend/src/lib/api.ts](../lib/api.ts.md)
- [frontend/src/providers/RunConnectionProvider.tsx](../providers/RunConnectionProvider.tsx.md)

### Depended on by (12)
- [frontend/src/app/dashboard/bridgeTerminality.source.test.ts](../app/dashboard/bridgeTerminality.source.test.ts.md)
- [frontend/src/app/dashboard/page.tsx](../app/dashboard/page.tsx.md)
- [frontend/src/hooks/__fixtures__/reducerHarness.ts](__fixtures__/reducerHarness.ts.md)
- [frontend/src/hooks/__tests__/terminalStatusReconcile.test.ts](__tests__/terminalStatusReconcile.test.ts.md)
- [frontend/src/hooks/__tests__/useWorkflow.pipelineCancelled.test.ts](__tests__/useWorkflow.pipelineCancelled.test.ts.md)
- [frontend/src/hooks/useRunStateStore.ts](useRunStateStore.ts.md)
- [frontend/src/hooks/useWorkflow.clarifyRetention.test.ts](useWorkflow.clarifyRetention.test.ts.md)
- [frontend/src/hooks/useWorkflow.imagePayload.test.ts](useWorkflow.imagePayload.test.ts.md)
- [frontend/src/hooks/useWorkflow.reconnect.test.ts](useWorkflow.reconnect.test.ts.md)
- [frontend/src/hooks/useWorkflow.regenerateReset.test.ts](useWorkflow.regenerateReset.test.ts.md)
- [frontend/src/hooks/useWorkflow.specRevisionCount.test.ts](useWorkflow.specRevisionCount.test.ts.md)
- [frontend/src/lib/__tests__/wsRunScope.test.ts](../lib/__tests__/wsRunScope.test.ts.md)

### External
react
<!-- /AUTO-GENERATED -->

## Purpose
The frontend's pipeline-run state manager: it launches runs over REST (`POST /api/runs` via `RunConnectionProvider`), and reduces the stream of incoming SSE pipeline events (`pipeline_start`, `agent_start/chunk/complete/error`, `task_progress`, `pipeline_complete/failed/cancelled`, etc.) into a single `PipelineRunState` the run screen renders from.

## Shape
- `useWorkflow()` — the exported hook; owns `pipelineState` and exposes `startPipeline`, `resetPipeline`, `handleMessage`, `submitQuestionnaire`, `retainClarifyRound`, `retainAgentEdit`, `reconcileTerminalStatus`.
- `handlePipelineMessage` — the standalone, transport-agnostic reducer (exported so the parent's SSE handler and tests can drive it directly) that switches over `msg.type` and applies each case to `PipelineRunState`.
- `ACCUMULATING_FRAME_TYPES` / `isRedelivery` / `markApplied` — a redelivery guard wrapping every accumulating event type (chunk/thinking/tool_call/hook_run/validator_result) so a durable replay (REST + SSE both delivering the same event) never double-applies it.
- `TERMINAL_MARKER_BY_STATUS` / `terminalMarkers` / `applyTerminalStatus` — the single definition of what a terminal run status (`completed`/`cancelled`/`failed`/`degraded`) does to run state, reused by the `pipeline_complete`/`pipeline_failed`/`pipeline_cancelled` cases and by `reconcileTerminalStatus`.
- `deriveSpecRevisionCount` — derives which spec-revision cycle a run is in from how many times the pipeline's first step has (re)started, scoped positionally rather than to any named agent/workflow.
- `frameIdentity` — the durable identity of one event frame (`event_id` → `seq` → an unstamped positional fallback), used by the redelivery guard.

## Why this shape
The reducer consolidates several previously-duplicated concerns (documented inline as ISS-082, ISS-126, ISS-063) into single definitions — one redelivery gate instead of per-field dedup checks, and one terminal-status vocabulary instead of three open-coded call sites — specifically because a durable run can be replayed over two transports (REST then SSE) and a naive reducer would double-count or desync state across reconnects/resumes.
