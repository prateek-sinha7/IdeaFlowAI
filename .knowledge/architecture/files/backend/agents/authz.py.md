---
id: FILE-backend/agents/authz.py
type: architecture-file
kind: state
title: authz.py — file
file: backend/agents/authz.py
module: MOD-backend-agents
language: python
depends_on_count: 18
depended_on_by_count: 7
code_signature: 5f740e0a4790
---

<!-- AUTO-GENERATED BELOW THIS LINE — regenerated by tools/knowledge/build_architecture.py -->

**Source:** [backend/agents/authz.py](../../../../../backend/agents/authz.py)

**Module:** [MOD-backend-agents](../../../MOD-backend-agents.md)

### Depends on (18)
- [backend/app/__init__.py](../app/__init__.py.md)
- [backend/app/core/__init__.py](../app/core/__init__.py.md)
- [backend/app/core/crypto.py](../app/core/crypto.py.md)
- [backend/app/models/__init__.py](../app/models/__init__.py.md)
- [backend/app/models/artifact_ref.py](../app/models/artifact_ref.py.md)
- [backend/app/models/database.py](../app/models/database.py.md)
- [backend/app/models/exec_runs.py](../app/models/exec_runs.py.md)
- [backend/app/models/gate_events.py](../app/models/gate_events.py.md)
- [backend/app/models/hook_runs.py](../app/models/hook_runs.py.md)
- [backend/app/models/mcp_credential.py](../app/models/mcp_credential.py.md)
- [backend/app/models/repository.py](../app/models/repository.py.md)
- [backend/app/models/run_capabilities.py](../app/models/run_capabilities.py.md)
- [backend/app/models/run_event.py](../app/models/run_event.py.md)
- [backend/app/models/subagent_run.py](../app/models/subagent_run.py.md)
- [backend/app/models/validation_results.py](../app/models/validation_results.py.md)
- [backend/app/models/wave_run.py](../app/models/wave_run.py.md)
- [backend/app/models/workflow.py](../app/models/workflow.py.md)
- [backend/app/models/workspace.py](../app/models/workspace.py.md)

### Depended on by (7)
- [backend/agents/execution_engine/clarify_engine.py](execution_engine/clarify_engine.py.md)
- [backend/agents/execution_engine/engine.py](execution_engine/engine.py.md)
- [backend/app/agents/chat/concierge.py](../app/agents/chat/concierge.py.md)
- [backend/app/api/run_commands.py](../app/api/run_commands.py.md)
- [backend/app/api/run_files.py](../app/api/run_files.py.md)
- [backend/app/api/run_stream.py](../app/api/run_stream.py.md)
- [backend/app/api/runs.py](../app/api/runs.py.md)

### External
(none)
<!-- /AUTO-GENERATED -->

## Purpose
`ScopedStore` — the single enforced default-deny read/write path for the typed-artifact substrate (artifacts, runs, workspaces, events, capabilities, exec/subagent/wave audit rows). It is the relocated-and-grown successor to a formerly pure ownership-predicate module, so that the owner/workspace boundary is enforced in exactly one place rather than scattered across callers.

## Shape
- `ScopedStore(owner_id, workspace_id, session=None)` — constructed with the caller's principal; `_acquire()` either reuses an injected `Session` or opens/closes its own `SessionLocal()`, letting both the request-scoped endpoint path and the session-less engine/SSE path share the same methods.
- `_scope_with_visibility` / `_scope_owner_ws` — the two default-deny filter shapes: owner+visibility for models with a `visibility` column (`ArtifactRef`), plain owner+workspace for everything else (`WorkflowRun`, `RunEvent`, `RunCapabilities`, etc.).
- Per-table read/write method groups, each following the same pattern (stamp the helper principal on write, scope-filter on read): `ArtifactRef` (`write_ref`/`get_ref`/`list_refs`/`lineage`), `RunEvent` (`append_event`, `read_events`, the optimistic-concurrency `append_event_next_seq`/`append_event_at_or_after` seq allocators), `DeepLinkNonce`, `WorkflowRun` (`get_run`, `set_run_scope`), `Repository`, `McpCredential`, plus the audit tables `gate_events`/`validation_results`/`hook_runs`/`exec_runs`/`subagent_runs`/`wave_runs`.
- `assert_owns` / `assert_repo_owned` / `assert_mcp_cred_owned` — explicit-denial variants that look up a row's TRUE owner via an unscoped read and raise `PermissionError` on mismatch, used where a cross-owner reference must fail loudly rather than resolve to a silent `None`.

## Why this shape
The module docstring is explicit about two constraints driving the design: an import-direction rule (this module may import `app.models.*` but never `app.api.*`, enforced by `lint-imports`, so the engine cannot accidentally reach the web layer) and a "DB is fully sync" premise, so every method opens/reuses a synchronous `Session` inside an `async def` via the same `_acquire()`/`finally: close if owned` idiom throughout.
