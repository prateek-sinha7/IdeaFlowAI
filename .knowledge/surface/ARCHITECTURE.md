<!-- GENERATED by scripts/knowledge/build_architecture.py — do not edit. -->

# Current state

Where the project is right now, and what constrains a change to it. Every other file in this store is history; this one is the present.

## Stage

| | |
|---|---|
| **Milestone** | `v3.0` — Top-Tier Resume & Durable Execution |
| **Status** | `verifying` |
| **Phases** | 6/6 complete · 14/14 plans · 100% |
| **Last updated** | 2026-07-19 |

**Current position**

- Phase: ALL COMPLETE — 45 [R0] 4/4 · 46 [R1] 8/8 · 47 [R2] 4/4 · 48 [R3] 4/4 · 49 [R4] 5/5 (KAN-88 green) · 50 [R5] 5/5
- Plan: 14/14 plans complete across 6 phases; register reconciliation batch appended; requirements RESUME-05..18 all Complete
- Status: Milestone v3.0 OFFLINE-COMPLETE — remaining: the consolidated live-Bedrock pass (orchestrator-owned) + /gsd-complete-milestone (user step; v2.0 close-out also still pending)
- Last activity: 2026-08-13 — Completed quick task 260813-as6 (FIX-250/TEST-033, closing ISS-152, filing ISS-153/ISS-154): the revision driver silently dropped 7 WorkflowRun output columns (output/agent_outputs/token_usage/duration/model_id/deliverable_mimetype/deliverable_filename) on EVERY completed revision — the fourth caller BUG-R03 never wired to the shared _apply_terminal_output_columns mapping. Fixed in _drive_revision_to_queue only (run_commands.py), mirroring _drive_launch_to_queue byte-for-byte; RED->GREEN proven in isolation, full regression set (39 pre-existing tests) shows the same 4 pre-existing failures byte-identical against a disposable worktree at the pre-fix commit (never git stash); goldens 10/0 identical, lint-imports 4/0. Live-verified on Bedrock via the deterministic POST /revisions endpoint (the prescribed chat-driven script hit unrelated live-LLM classification variance, not filed): all 7 columns populated live; the Concierge answered "This run used 4,245 total tokens and cost an estimated $0.01" to a revision run for the first time — FIX-249 (same session, commit 7d39a1e1) proven repaired; Analytics rendered non-zero spend where revisions previously forced $0.00. A genuine NEW defect surfaced mid-fix, NOT caused by this fix and NOT fixed here: PreviewPanel.tsx's handleSelectVersion shows a blank preview when switching to an OLDER, non-latest revision whose .output is still NULL (pre-fix rows) — a code path structurally impossible to exercise before this session (no family had 3 versions until this fix's own live test made one); answers the orchestrator's own instruction to investigate before assuming the reopen gate is harmless. Two items surfaced, not silently performed: the wr.error omission (a failed revision gets no error message) filed as ISS-153; the one-row backfill for the pre-existing revision row left as an open decision; the version-picker gap filed as ISS-154. Bookkeeping complete: FIX-250/TEST-033 closes ISS-152, files ISS-153/ISS-154 — four-source sweep confirmed a clean floor, no collisions.

## Enforced boundaries

These are checked by `import-linter` in CI, which makes them the only architecture claims here that cannot silently drift. Breaking one fails the build.

| boundary | source | must not import |
|---|---|---|
| kernel imports only capability ports (scaffold) | `agents.execution_engine.engine` | `app.api` |
| agents.workflows must not import the execution kernel or the web layer | `agents.workflows` | `agents.execution_engine`, `app` |
| agents.capabilities must not import the execution kernel or the web layer | `agents.capabilities` | `agents.execution_engine`, `app` |
| agents.runtime must not import the execution kernel or the web layer | `agents.runtime` | `agents.execution_engine`, `app` |

## Components

| component | what it is | cards | on disk |
|---|---|---:|---|
| `backend/app/api` | HTTP + SSE surface — the only caller of the kernel | 63 | 30 files |
| `backend/app/services` | application services | 2 | 5 files |
| `backend/app/models` | persistence — additive migrations only | 1 | 24 files |
| `backend/agents/execution_engine` | the execution kernel | 44 | 12 files |
| `backend/agents/workflows` | workflow manifests — data, not code paths | 29 | 6 files |
| `backend/agents/capabilities` | capability adapters | 14 | 84 files |
| `backend/agents/runtime` | runtime services | 0 | 2 files |
| `backend/agents/artifact_store` | artifact persistence | 1 | 2 files |
| `backend/agents/guardrails` | policy enforcement | 0 | 0 files |
| `frontend/src/app` | Next.js routes | 63 | 25 files |
| `frontend/src/components` | UI components | 217 | 198 files |
| `frontend/src/hooks` | client state + stream handling | 48 | 32 files |

**18 workflows** registered — `app_builder`, `app_builder_revision`, `chat`, `custom`, `custom_revision`, `dotnet_to_azure`, `mulesoft_to_springboot`, `ppt`, `ppt_revision`, `prototype`, `prototype_revision`, `reverse_engineer`, `sample_brownfield`, `sample_fanout`, `sample_subagents_parallel`, `sample_wave`, `user_stories`, `user_stories_revision`

Per SC-001 these are pure data: adding one is a manifest plus an AGENT.md, with no engine edit and no new pipeline name.

## Decisions in force

| ADR | status | area | decision |
|---|---|---|---|
| `ADR-0001` | accepted | sse, frontend | In the context of SSE streams that the backend closes on purpose, facing a spurious "Reconnecting" banner on every stop, we decided that terminal… |
| `ADR-0002` | deferred | engine, registry, api, frontend, naming | In the context of two live vocabularies for one concept — `workflow` in the manifests and `pipeline` in the engine and registry — facing a… |
| `ADR-0003` | accepted | engine, registry, workflow | In the context of a run's agent roster being built twice — once from the compiled manifest and once from registry membership — facing a run-entry… |
| `ADR-0004` | accepted | engine, workflow | In the context of two competing orderings — the resolver's topo-sort over agent-declared produces/consumes and the compiler's topo-sort over… |
| `ADR-0005` | accepted | loader, registry, workflow | In the context of a hand-maintained frozenset of supported pipeline types that had to be edited by hand for every new workflow, facing SC-001's… |
| `ADR-0006` | accepted | agents, runtime, workflow | In the context of a run sandbox whose skills/ directory is shared by every step, facing a later step reading a SKILL.md staged by an earlier one… |
| `ADR-0007` | accepted | agents, artifacts, workflow | In the context of custom-agent artifact filenames formatted `<instance_id>.<topic>.md`, facing a small local model reading the dot as a path… |
| `ADR-0008` | accepted | engine, api, workflow | In the context of ADR-0003 making the compiled plan the run's roster, facing a composer selection of 3 agents silently executing all 9 steps the… |
| `ADR-0009` | partial | engine, workflow, compiler | In the context of wanting independent steps to run concurrently, facing a `build_waves` that partitions Tasks but not Steps and a plan format… |
| `ADR-0010` | accepted | frontend, api, engine, workflow | In the context of two competing skill-attachment surfaces — a RUN-LEVEL bag applied uniformly to every agent in a launch, and spec 012's PER-AGENT… |
| `ADR-0011` | accepted | engine, factory, workflow | In the context of custom workflows whose steps hand files to one another, facing step prompts that name their own filenames ("save it to… |
| `ADR-0012` | accepted | frontend, factory, workflow | In the context of a Composer offering per-node tool grants, facing four toggles that the runtime cannot honour — two inert, one whose "off"… |

Full text: `ctx.py --show <ID>`. Rules by area: `ctx.py --rules <area>`. Areas carrying history: `agents` (291), `workflow` (259), `sse` (227), `frontend` (225), `backend` (133), `auth` (132), `artifacts` (117), `resume` (81).

## Constraints that bind every phase

10 invariants + SC-001, in `.knowledge/INVARIANTS.md` (~700 tokens). Read it before designing anything; `INV-1`, `INV-8`, `INV-13` and `SC-001` are the ones most often broken by an obvious-looking change.

## What this file does not know

- Runtime topology (what is deployed where) is not derived — see `docs/SIMPLE_AWS_DEPLOYMENT.md`.
- The component table counts files and card hits. It does not verify that a component still does what its description says.
