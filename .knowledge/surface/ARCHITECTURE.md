<!-- GENERATED by scripts/knowledge/build_architecture.py — do not edit. -->

# Current state

Where the project is right now, and what constrains a change to it. Every other file in this store is history; this one is the present.

## Stage

| | |
|---|---|
| **Milestone** | `v3.0` — Top-Tier Resume & Durable Execution |
| **Status** | `verifying` |
| **Phases** | 6/6 complete · 14/14 plans · 100% |
| **Last updated** | 2026-07-19 |

**Current position**

- Phase: ALL COMPLETE — 45 [R0] 4/4 · 46 [R1] 8/8 · 47 [R2] 4/4 · 48 [R3] 4/4 · 49 [R4] 5/5 (KAN-88 green) · 50 [R5] 5/5
- Plan: 14/14 plans complete across 6 phases; register reconciliation batch appended; requirements RESUME-05..18 all Complete
- Status: Milestone v3.0 OFFLINE-COMPLETE — remaining: the consolidated live-Bedrock pass (orchestrator-owned) + /gsd-complete-milestone (user step; v2.0 close-out also still pending)
- Last activity: 2026-08-12 — Completed quick task 260812-sgu: **A human-in-the-loop review gate approved every MISSPELLED DENIAL (ISS-070).** `resolve_gate` dispatched on `action` with `else:  # approve (default)` as the fallback — an exact, case-sensitive, unstripped match — so anything it did not recognise was treated as consent. Observed against the real endpoint, not reasoned: `'Reject'`, `'REJECT'`, `' reject'`, `'reject\n'`, `'rejct'`, `'no'`, `'deny'`, `'Redo'`, `'redo '`, `'update-specs'` **all returned HTTP200 `approved=True` and resolved the pause.** The user believes they stopped the run; it resumes and spends the rest of a 5–21M-token budget. **The row understated it — the record was LAUNDERED:** the `else` never passed `action=`, so the store's own default persisted `action="approve"` while the HTTP response echoed the garbage back (`action='rejct' → HTTP200 approved=True stored_action='approve'`), which is very likely why this never showed in a live log; on the declared-gate path it resolves a formal `GATE_PASS`. **The row's scope was over-generalised and its tag was wrong, both corrected in place:** only **one** of the three `set_review_response` ingresses was defective — `/messages` `CHANNEL_GATE` is fenced by `chat_router.py:234` (verified by reading BOTH `Dispatch(channel=CHANNEL_GATE)` sites, each requiring `turn.action in GATE_ACTIONS`) and the Concierge confirm by `concierge.py:350` plus H1 — and it is **input validation, not authorization** (`resolve_gate:222` → `run_engine.py:765` filters on `WorkflowRun.user_id`, so no third party can approve your gate), so **retagged `api,auth` → `api,validation`**, severity kept **major**. Fixed in two layers, **each mutation-proved independently**: `action` → a four-member `Literal` (422 + an OpenAPI enum) and a fail-closed dispatch `else` raising 400 `unknown_gate_action`. Reverting only A → all ten variants 400; reverting only B → all ten 422. **It REFUSES and never degrades to a reject** — FIX-232 makes rejection terminal, so a typo must not destroy the run; leaving the gate ARMED is the only degrade that preserves every option, mirroring `engine.py:5830-5838`. **The default was KEPT** (`test_attach_replay_matrix.py:551` posts no `action` at all; OpenAPI still reads `required: ['gate_key']` with `"default": "approve"`) and **the if/elif chain was NOT restructured**, because `test_rest_gate_commands.py:362` is a grep-ratchet on the literal source substring `action == "redo"`. **`chat_router.py` deliberately untouched** — its `:234` guard IS the ISS-119 routing contract. INV-12: `GATE_ACTIONS` stays the one authority, pinned by a set-equality test rather than a fifth copy. **Goldens could never have caught this** — `grep -c review_gate_ready` = 0 across all 15 files — so they are the neutrality gate only. **12 of 13 tests seen RED first**; `test_rest_gate_commands` **15 → 28 passed**; goldens **10 passed / 0 files moved**; lint **4 kept / 0 broken**; the 14-id pre-existing-red set across five gate-adjacent suites **byte-identical** to `e6b24ae5`, re-measured by swapping in `git show HEAD:...` (never a stash). Files **ISS-127** (`analysis_report` has no length cap on its path to the composed prompt). No run launched; no token spent. NOT pushed. — previous: 2026-08-12 — Completed quick task 260812-ppu: **every chat turn during a live run silently destroyed one engine event from the durable log (ISS-121), and that row's own root cause was WRONG.** It claimed `pipeline_cancelled` is never written; in fact all eight engine terminal emitters write it, and this one was **attempted, rejected by `uq_run_events_scope_seq`, and swallowed** — `_RunEventSink.persist` catches `SQLAlchemyError`, `IntegrityError` is one, so a real seq collision took the branch meant for the schema-less offline harness. The chat lane retries past a collision; the engine discarded the row. At a gate the engine is idle, so the victim is deterministically the terminal frame. **The fingerprint:** seq 281 is a narrator card reading "Cancelled by you", projected from the very event whose row is missing. Fixed with `append_event_at_or_after` plus a **mandatory** `data["seq"]` re-stamp in both drive loops — the SSE `id:` cursor is `row.seq` on replay and `data["seq"]` live, so a retry without a re-stamp corrupts `Last-Event-ID`. **The discriminator was measured, not assumed**: the golden harness fails with an `IntegrityError` FOREIGN KEY violation, not the `OperationalError` its comment implies, so a naive retry would have re-stamped every golden event and broken `assert_seq_contiguous`. All 3 new tests assert the durable **ROW**, seen RED first and re-proven by mutation. Goldens **10 passed / 0 moved**, lint **4/0**, red sweep **22 F / 176 P identical IDs**. Files **ISS-123..126**. NOT pushed. — previous: 2026-08-12 — Completed quick task 260812-mq5: **prompt caching's dollar effect is now visible, and it is NEGATIVE on most runs (ISS-034).** The row asked for "saved $Y"; re-derivation through the real `estimate_cost_usd` refutes it — **6 of 11 persisted runs cost MORE with caching on** (-4.98%..-8.86%), 1 neutral, 4 save, aggregate +80.34% carried by two long prototype runs. Shipped the counterfactual `estimated_cost_full_usd` at both cost sites (line numbers **re-verified**: `:2873-2880` / `:2171-2178`, all three cited sources had drifted) plus a **SIGNED** Analytics line — never an unconditional "saved". **The -100% catastrophe was reproduced before it was prevented**: with the naive sum, `spend_full = $0.00` against `spend = $15.00`, because 0 of 11 rows carry the key; legacy rows now fold to a zero delta. Golden-neutral **proven both ways** (stripped → 10 passed, 15 files byte-identical; unstripped control → 5 red). D2's footnote corrected: the Concierge IS metered but its event has zero readers, and handoff writes no `WorkflowRun` rows at all. Files **ISS-120** (the global cache flag, keyed on run length not workflow name). NOT pushed. — previous: 2026-08-12 — Completed quick task 260812-lfv: **the test suite can no longer spend the owner's money (ISS-102).** Two `TestRouting` tests made a real, billable Bedrock `ConverseStream` call on every suite run; they were free only because the SSO token was expired, and SSO went live on this machine today. **The row was wrong twice and both corrections matter:** it is **two** tests, not three (`test_terminal_routes_to_revision` never reaches a model — it dies at `run_commands.py:2497` on `AttributeError: '_FakeUser' object has no attribute 'tier'`), and **two** calls, not "four attempts per run" (a grep miscount: 2 ERROR lines plus the same 2 exceptions echoed inside their tracebacks). Both established by per-test log attribution before any edit. `TestRouting` got a class-placed autouse `_resolve_concierge` patch; the real deliverable is a root-conftest autouse guard that makes constructing a live provider client a loud failure naming the offending nodeid. **The chokepoint decision is the whole point and it was ESTABLISHED, not assumed:** the obvious "fail anything that reaches `build_model()`" is **theatre** — a four-tree sweep found **10** offending tests and that guard would have missed **6**, because `agents/planner/smart_planner.py:167-201` builds its client without the factory and `create_deep_agent(model="anthropic:…")` has langchain's `init_chat_model` build one from a string; it would also be defeated by the by-value `build_model` imports at `deep_agent_runner.py:58`/`cached_invoke.py:44`. So the guard covers the **three provider classes**, patching `__init__` and never the class object — verified to keep `isinstance` (`_maybe_apply:234`, `cached_invoke:79`) and `object.__new__` working. Its exception derives from **`BaseException`, not `Exception`**, because `run_commands.py:1513` and `deep_agent_runner.py:586` are bare `except Exception` — **the exact swallow that turned the original `ExpiredTokenException` into a log line instead of a failure**, so an `Exception`-derived guard would have been silent on the very path it polices. Three allowances only, all pre-existing project gates (`RUN_LIVE_BEDROCK=1`, `requires_api_key`, an exact-nodeid allow-list that **fails closed**), each exercised independently. **4 of the 6 new cases seen RED first**, by standing the guard down through its own allowance rather than reverting code — 4 failed/2 passed down, 6 passed up, and **zero network calls either way** because every case constructs without invoking. `ExpiredTokenException|ConverseStream` hits **4 → 0**; `tests/unit` 62/1167 → 62/**1173** (+6 = exactly the new file, FAILED **id set identical**); `tests/agents`+`properties`+`integration` **57/1723/42 unchanged, id set identical, 0 guard trips**; goldens **10 passed / 0 files moved** (15 checksums byte-identical); lint-imports **4 kept / 0 broken**. Environment safety proven before anything ran: `aws sts get-caller-identity` with no profile → exit **254**, `ExpiredToken`; `AWS_PROFILE` never exported, no `aws sso login`. **The fact that makes this permanent:** the suite has **no offline-by-default state** — `AWS_REGION`/`BEDROCK_INFERENCE_PROFILE_ID`/`BEDROCK_MODEL_ID` are hardcoded **code** defaults (`app/core/config.py:130/:123/:118`) with no `backend/.env`, so unsetting env vars can never make `build_model()` offline; the only thing that ever stood between this suite and a bill was whether credentials happened to resolve. Files **ISS-118** (8 more construct-only tests, left green and unmodified rather than silently patched) and **ISS-119** (the two reds assert a routing contract the product deliberately superseded — an owner call, not a side effect of a money fix). NOT pushed. — previous: Completed quick task 260812-kpb: **the settled "Task plan · N planned" card now counts the plan, not the build's progress (ISS-087).** The row is wrong on two of three decision-relevant claims — "one version on most runs" is BACKWARDS (`prototype-plan` is multi-version on **5 of 6** run-groups), and F1/F2 are MIS-CITED as the remedy (neither mentions `protoCompletedTasks` or `dagEdges`; F1's table has no artifact or version column) — and it **understates the defect**: `protoCompletedTasks` is the BUILD agent's *completed*-task stream, so a card labelled "planned" rendered the wrong QUANTITY, **wrong at the latest version with no picker involved** (run `d5dbc9f2`: a 7-task plan rendered "6 planned"). The picker only exposed a category error that was always there. Fixed per owner decision D1 by parsing the `<tasks>` body on screen, with `parseTasks` **EXTRACTED verbatim** from `TasksPreview` (no third parser, INV-12) — it follows the picker for free because the derivation already receives `displayed`, so **no second threading was added**. `useWorkflow.ts` deliberately untouched (its regex is line-anchored and not equivalent; filed ISS-115). Dead props **deleted, not left inert** — the build's completed tasks still show on the ConstructionBlock, where the label matches the data. `dagEdges` **closed as not-a-defect**. **The validation badge is DEAD, not un-versioned** — `gate_passed` has ZERO backend producers (the GATE_PASS path writes a DB row and returns a `GateOutcome` with no `events` list) and `validator_result` zero, so `validationPassed === true` is structurally unreachable and the chip is a constant "Review"; the affordance was declined and filed as ISS-116. **All 7 cases (4 new + 3 reconciled 42-09 pins) seen RED first** on a reverted source (`7 failed | 18 passed`); the pins were reconciled, never deleted or loosened, and one had a fixture **contradicting its own assertion**. Verified on REAL data through the shipped code and a rendered DOM: `d5dbc9f2` now reads "7 planned", `6e38b9a7` v1 → 10 / v2 → 11, `5ecb990f` 8/8 with titles repainting. The investigation's claim that e2e reaches none of this is **refuted** — `ts-n.review-gate` `TS-N-02` renders `TasksPreview`, run 8 passed. Goldens **10 passed / 0 files moved**, lint **4 kept / 0 broken**, tsc unchanged, results failing **ID set identical**. Files ISS-115..117. NOT pushed. — previous: Completed quick task 260812-jn9: **the spec-revision "cycle N" number now has ONE producer, because the second one was deleted rather than wired (ISS-083).** The row's facts are right and its fix is wrong: it asks for `runStore.viewed.specRevisionCount` to be wired in, which would have been a THIRD source for one number — the exact thing `IMPLEMENTATION-REGISTER.md:4214` forbids — threaded into a renderer that **cannot mount**. The entire `spec_revision` card kind is unreachable end-to-end: `chat_narrator._revision_index` keys on `spec_revision_attempt`/`revision_index`, those strings exist as quoted dict keys ONLY at `:112`/`:114` (the reader itself), and across **232,023 durable events over 14 runs — including 52 proving spec revisions happened** — `spec_revision` cards number **ZERO**. The dormancy is deliberate and locked (FIX-220 deliberately misnamed its keys to avoid waking this reader). So `cycle` was never missing data, it was a second CONTRADICTING copy — header "cycle 1" from the FE default above body "cycle 2" from the backend text — and INV-12 wants it **deleted, not extended**. Prop, destructure (`:103`, which the row misses) and ternary all gone; the card KIND is deliberately kept and its unreachability filed as ISS-111. **RED observed twice on a deletion that has no natural failing test**, the second time by the orchestrator on a reverted source: `4 failed | 9 passed (13)`, **T1 receiving exactly 2** — the two copies caught in the act. **The brief's own "ignore Playwright" instruction was wrong** and the planner caught it (`mockSse.chatReply` fabricates the turn client-side, reaching the branch the backend cannot, and its assertion was satisfiable only by the default being deleted) — but its counter-claim that the test was green was also wrong: all 3 tests there are red on an unrelated pre-existing `beforeEach` under ISS-076, so the edit is landmine removal and the gate is **UNVERIFIABLE-GREEN**, proven by before/after identity instead. Gates re-measured, not inherited: ResultCard `1 failed | 10 passed` → `1 failed | 12 passed` with the failing ID unchanged, goldens **10 passed / 0 moved**, lint **4 kept / 0 broken**, narrator **36 passed**, **reducer specs 63 passed — the affirmative evidence nothing was wired**, tsc the same 2 errors. This is the **fifth** dead counter in this family, not the third the row claims, and the generator is one design choice: *the frontend keeps re-deriving a number the backend already computed as a string*. Files ISS-111..114. NOT pushed. — previous: 260812-iqh: **the useWorkflow reducer now keeps its own frame identity, so an accumulating field survives re-delivery (ISS-082).** The register row was right about the concern and wrong about three facts: there are **7** accumulating fields, not 2 (6 unprotected); its cited grep finds **neither field it names** (a comment at `:431` and a local array at `:954` — every real accumulator uses `[...prev.x, y]` or string `+`, which that regex cannot see); and `hook_run` was never protected by anything, because `emit_hook_event` bypasses the engine's stamping chokepoint so the frame arrives unstamped and `wsReplayState.ts:36` exempts unstamped frames **by design**. Three layers: a uuid `event_id` on the transient `hook_run` (kept out of `execute()` so the golden multiset cannot move); ONE identity gate in the reducer, installed by decorating the dispatcher rather than open-coded six times — an O(1) `lastAppliedSeq` cursor (chosen because `agent_chunk` is ~25k frames on one real run, so an immutably-copied id set is O(n²) on replay) plus a small `appliedUnsequencedIds` for the one frame the engine never stamps; and A3, shipped **with** the gate and never after it. A3 un-shadowed `page.tsx`'s `frameRunId` — the shadow made **every** per-agent frame resolve to `undefined` on BOTH the live and the replay path, so the per-run store was never handed one, and FIX-201 had worked around it with a second, UNDEDUPED replay loop that is now DELETED. INV-12: FIX-225's per-field `.includes` guard is deleted, the `agentStartEventIds` **array survives** (it is what `deriveSpecRevisionCount` reads, and ISS-083 depends on it). **12 tests seen RED first** with the investigation's exact observed values (`"Hello worldHello world"`, `toolCalls 2`, `hookRuns 2` — and `hookRuns 2` on a FULL replay too, the only field nothing resets). The deliverable is `useWorkflow.accumulators.test.ts`: a declared registry of all seven fields × three multiplicities, plus GUARD-1, a source assertion keyed on the structural invariant (a property whose value both READS its own previous value and GROWS it) — validated to find exactly 7 with zero false positives before being trusted. **A3's safety is MEASURED, not reasoned:** the run-id decision was extracted to `resolveFrameRunId` in `wsReplayState` precisely so it could be unit-tested, and reverting it to the old behaviour turns 3 tests red. A stale source-guard that locked the deleted bypass was **reconciled and made tighter**, never relaxed. goldens **10 passed / 0 moved**, lint **4 kept / 0 broken**, backend reds **identical ID set**, vitest **147 failed → 147 failed with +36 passing and ZERO newly-red**, mocked Playwright **33/43/108 byte-identical** (the one ID that appeared to swap was chased and proved identical in isolation on the reverted tree). **New finding, empirically verified: `hook_run`'s payload never reaches the reducer** (`page.tsx` flattens `msg.data` away while the handler reads it), so every entry collapses to defaults and `deriveSecurityBullets`' `outcome === "block"` filter can never match — the only live consumer of `hookRuns` is structurally dead, which **refutes** the investigation's claimed user-visible blast radius. NOT pushed. — previous: 260812-hsx: **the graceful-shutdown path is now reachable on a developer machine, and a local restart stops the run (ISS-088).** Without `--timeout-graceful-shutdown` a live SSE stream made uvicorn ignore SIGTERM forever — measured **still alive at 31.25s with only `startup_complete` written**, SIGKILL the only way out; with the flag, **clean exit in 5.82s** and the shutdown body running to completion. Ten dev docs updated (`--reload` dropped: untreated it leaves **two** stuck processes and `watchfiles` is absent so it polls), port kept at 8000 with evidence, both out-of-repo memory notes updated. The double `close_checkpointer()` is deleted (INV-12) so step 4 really is last. **The open question is settled and REVERSED:** `asyncio.run`'s teardown does NOT mark runs cancelled under SIGTERM — uvicorn re-raises the signal inside `serve()` (`server.py:326-330`) so `_cancel_all_tasks` never runs; proven by SIGTERM-vs-SIGINT markers, a no-uvicorn control, and a post-`server.run()` marker that never appears. So `SHUTDOWN_STOP_RUNS` is now env-differentiated (ON in development, OFF in production) and the corrected 24s/30s budget is asserted against a PARSED `stop_grace_period`. 5 tests seen RED first; goldens **10 passed / 0 moved**, lint **4 kept / 0 broken**, pre-existing reds unmoved. **Does NOT close the money hole — a production run still resumes after a restart (ISS-089).** NOT pushed.

## Enforced boundaries

These are checked by `import-linter` in CI, which makes them the only architecture claims here that cannot silently drift. Breaking one fails the build.

| boundary | source | must not import |
|---|---|---|
| kernel imports only capability ports (scaffold) | `agents.execution_engine.engine` | `app.api` |
| agents.workflows must not import the execution kernel or the web layer | `agents.workflows` | `agents.execution_engine`, `app` |
| agents.capabilities must not import the execution kernel or the web layer | `agents.capabilities` | `agents.execution_engine`, `app` |
| agents.runtime must not import the execution kernel or the web layer | `agents.runtime` | `agents.execution_engine`, `app` |

## Components

| component | what it is | cards | on disk |
|---|---|---:|---|
| `backend/app/api` | HTTP + SSE surface — the only caller of the kernel | 52 | 27 files |
| `backend/app/services` | application services | 2 | 5 files |
| `backend/app/models` | persistence — additive migrations only | 1 | 23 files |
| `backend/agents/execution_engine` | the execution kernel | 35 | 11 files |
| `backend/agents/workflows` | workflow manifests — data, not code paths | 23 | 5 files |
| `backend/agents/capabilities` | capability adapters | 14 | 82 files |
| `backend/agents/runtime` | runtime services | 0 | 2 files |
| `backend/agents/artifact_store` | artifact persistence | 1 | 2 files |
| `backend/agents/guardrails` | policy enforcement | 0 | 0 files |
| `frontend/src/app` | Next.js routes | 56 | 23 files |
| `frontend/src/components` | UI components | 209 | 192 files |
| `frontend/src/hooks` | client state + stream handling | 37 | 26 files |

**18 workflows** registered — `app_builder`, `app_builder_revision`, `chat`, `custom`, `dotnet_to_azure`, `mulesoft_to_springboot`, `od_ppt`, `od_ppt_revision`, `ppt`, `ppt_revision`, `prototype`, `prototype_revision`, `reverse_engineer`, `sample_brownfield`, `sample_fanout`, `sample_wave`, `user_stories`, `user_stories_revision`

Per SC-001 these are pure data: adding one is a manifest plus an AGENT.md, with no engine edit and no new pipeline name.

## Decisions in force

| ADR | status | area | decision |
|---|---|---|---|
| `ADR-0001` | accepted | sse, frontend | In the context of SSE streams that the backend closes on purpose, facing a spurious "Reconnecting" banner on every stop, we decided that terminal… |

Full text: `ctx.py --show <ID>`. Rules by area: `ctx.py --rules <area>`. Areas carrying history: `agents` (267), `workflow` (225), `frontend` (201), `sse` (194), `backend` (114), `auth` (108), `artifacts` (106), `resume` (72).

## Constraints that bind every phase

10 invariants + SC-001, in `.knowledge/INVARIANTS.md` (~700 tokens). Read it before designing anything; `INV-1`, `INV-8`, `INV-13` and `SC-001` are the ones most often broken by an obvious-looking change.

## What this file does not know

- Only 1 decision card exists against 266 fixes and bugs. Most rules this project actually follows are still implicit in fix prose — run `knowledge-consolidate` to promote them.
- Runtime topology (what is deployed where) is not derived — see `docs/SIMPLE_AWS_DEPLOYMENT.md`.
- The component table counts files and card hits. It does not verify that a component still does what its description says.
