---
id: FIX-194
type: fix
kind: event
title: 'ISS-060: Completion notification/toast is not run-scoped — finishing run marks
  a DIFFERENT live run as "complete"; toast shows wrong label (KAN-166)'
status: done
applies_to:
  phases:
  - '44'
  modules:
  - DashboardLayout
  - DashboardLayout.fix194.test
  - agents
  globs:
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/layout/DashboardLayout.fix194.test.tsx
  requirements: []
locked_constraints:
- INV-12
- INV-3
verification:
  type: test
  status: passed
  test_files:
  - frontend/src/components/layout/DashboardLayout.fix194.test.tsx
compact_summary: 'DashboardLayout''s completion effect fired on any pipelineState without checking pipelineRunId, letting a reopened run mark the wrong live run complete.'
last_updated: '2026-08-06'
author: 'Prateek Sinha <2000152842@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-155](20260730-2119-FIX-155.md), [FIX-157](20260731-FIX-157.md), [ISS-060](20260806-1934-ISS-060.md)

**Referenced by:** [ISS-060](20260806-1934-ISS-060.md)

<!-- /RELATED -->

# FIX-194

**Phase / trigger:** Phase 44 (DEF-44-12-4 history replay made it reachable), FIX-155, FIX-157, KAN-166

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

ISS-060: Completion notification/toast is not run-scoped — finishing run marks a DIFFERENT live run as "complete"; toast shows wrong label (KAN-166)

## Root cause

DashboardLayout.tsx:441-490 terminal effect fires on any pipelineState with isRunning=false + all agents done, with NO check that pipelineState.pipelineRunId matches the run that owns currentPipelineNotifId. History-reopening a completed user_stories run replays its pipeline_complete into the shared reducer, firing the effect and stamping a gate-paused prototype notification as "completed". Toast label taken from stale shared workflowType (also wrong). Fix: (1) add currentPipelineNotifRunId companion ref (line 373); (2) populate it at every currentPipelineNotifId.current = write site; (3) guard in terminal effect: completingRunId !== currentPipelineNotifRunId.current → return; (4) toast label from notifications.find(notifId)?.workflowType ?? workflowType; (5) clear companion ref in lockstep at every null-assignment.

## Files changed

- `frontend/src/components/layout/DashboardLayout.tsx`
- `frontend/src/components/layout/DashboardLayout.fix194.test.tsx`
