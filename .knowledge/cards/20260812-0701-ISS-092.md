---
id: ISS-092
type: issue
kind: event
title: A single Concierge chat question can feed the model up to 2.3M tokens, and
  not one of them is counted or capped. Two compounding facts, both verified 2026-08...
status: done
severity: major
applies_to:
  phases: []
  modules:
  - agents
  - app
  - chat
  - handoff
  - handoff_pipeline
  - tools
  globs:
  - app/agents/chat/concierge.py
  - concierge.py
  - agents/authz.py
  - handoff_pipeline.py
  - handoff/coder.py
  - run_commands.py
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'concierge.py discarded every usage event and read tools had no row limit, letting a single chat question inject up to 2.3M unmeasured tokens.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-230](20260812-0701-FIX-230.md), [FIX-233](20260812-1232-FIX-233.md), [ISS-033](20260704-2146-ISS-033.md), [ISS-034](20260704-2223-ISS-034.md), [ISS-100](20260812-1232-ISS-100.md)

**Referenced by:** [FIX-230](20260812-0701-FIX-230.md), [ISS-033](20260704-2146-ISS-033.md), [ISS-034](20260704-2223-ISS-034.md), [ISS-100](20260812-1232-ISS-100.md), [ISS-158](20260813-1300-ISS-158.md)

<!-- /RELATED -->

# ISS-092

## Title

**A single Concierge chat question can feed the model up to 2.3M tokens, and not one of them is counted or capped.** Two compounding facts, both verified 2026-08-12. (1) **Uncounted:** `app/agents/chat/concierge.py:365` drains its runner stream with `if event["type"] != "chunk": continue`, so every `usage` event is discarded. Nothing anywhere records a Concierge token — its spend is not merely under-reported, it is *unmeasurable from the data*. (2) **Unbounded input:** `concierge.py:434` calls `scoped_store.read_events(run_id, 0)` and `agents/authz.py:315-327` applies **no LIMIT and no type filter**, projecting every row including 200k+ `agent_chunk` payloads. Measured payload available at each recorded question (6 conversations, 15 `chat_message` events in `dev.db`): `fa66227a`@1322 ≈53.6k tok · `caa5d175`@10519 ≈385.7k · `a7dba362`@7721 ≈267.3k · **`0a27b397`@12123 ≈2,306,800 tok**. On a $0.34 `user_stories` run a single 205k-token Concierge read is ~60% of the run's entire input. The replies are coherent multi-turn answers, so the calls succeeded — what the model actually received is simply unknowable. Same class, same row: the handoff pipeline is **100% invisible** — `handoff_pipeline.py:403`/`:494`/`:514` pass no `usage_sink` (the seams exist, unwired) and `handoff_sessions` has no token column at all; `handoff/coder.py:235` also drops its usage. **Why this was NOT bundled into FIX-230:** `converse` runs from a REST handler (`run_commands.py:1442`) outside `execute()`, often after the run is terminal, so `aux_token_usage` is out of scope AND out of lifetime. Wiring it needs an answer to a product question — *should user-initiated chat spend appear in the run's headline cost, or as a separate line?* — and the handoff half needs a cost surface that does not exist. Both are features, not bug fixes.

## Severity / Tag / Status (original row)

- Sev: major
- Tag: product
- Status: **CLOSED 2026-08-12** (FIX-233 / TEST-017, quick-260812-gsf; commits `47c7672f` + `7b79e426`) — **two claims in this row were REFUTED on measurement and the fix design turned on them: (a) the payload is NOT `agent_chunk` (max row 169 chars) but 18 `agent_input` rows totalling 7,290,638 chars (largest 479,605), so a chunk filter would have fixed almost nothing; (b) there were THREE unbounded tools, not one — `list_refs()` projected artifact bodies inline (5,804,067 chars ≈ 1.45M tok in a single call) and `get_ref()` returned bodies up to 389,651. The '~60% of a $0.34 run's input' figure could not be reproduced under any pairing; the measured reality is worse — 144% and 151% of the runs' entire recorded input. `run_commands.py:1442` is the ctx construction; the `converse` call is at `:1503`. The handoff half was deliberately SPLIT OUT (now ISS-100) rather than bundled.** Resolution: `read_events`/`list_refs`/`get_ref` DELETED (INV-12) and replaced by seven bounded tools closed over `(scoped_store, run_id)`; one new bounded `ScopedStore.read_events_of_types` puts the cap in SQL while leaving the 18-caller replay primitive untouched; `exclude_builtin_tools=True` removes the filesystem WRITE surface the model was holding; `get_artifact` closes a cross-run scope escape; multi-turn preserved by reusing `context_provider:conversation` + `compaction:chat_history`; and one idempotent durable `chat_usage` row per turn makes the spend exist, reported as a SEPARATE line, never estimated when unobserved. **Measured through the shipped tools on real `dev.db` rows: 2,494,799 → 4,710 est. tokens (530×); no fully tool-saturated turn in the corpus exceeds 4,785.**

## Source / Evidence

Split out of ISS-033 during quick-260812-9tq, whose part A (FIX-230) closed the two measurable, safely-foldable sites. Evidence: read-only queries against the live `backend/dev.db` + the cited file:line. **The rejected shortcut, recorded so it is not re-proposed:** estimating Concierge spend from `len(chat_reply)` (or the fix-loop from a fudge factor) puts a fabricated number where a real one is one event-branch away — and would make ISS-034's "saved $Y" a fiction built on a fiction. If a token was not observed it must be reported as unmeasured, never invented. Independent of accounting, the uncapped `read_events` is also a latent context-blowout and latency risk.
