---
id: ISS-098
type: issue
kind: event
title: A mid-wave `CancelledError` leaves its `wave_runs` row stuck at `running` forever.
  `wave_scheduler.py:340` catches `except Exception:` to flip the in-flight ...
status: resolved
severity: trivial
applies_to:
  phases: []
  modules:
  - Base
  globs:
  - wave_scheduler.py
  requirements: []
locked_constraints: []
verification:
  type: test
  status: passed
  test_files:
  - backend/tests/agents/test_wave_scheduler_cancel_row.py
compact_summary: 'wave_scheduler catches except Exception to flip a wave row terminal, but CancelledError inherits BaseException so a cancelled wave stays stuck at running.'
last_updated: '2026-08-31'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-232](20260812-1153-FIX-232.md), [ISS-091](20260812-0654-ISS-091.md)

**Referenced by:** [FIX-425](20260831-1804-FIX-425.md)

<!-- /RELATED -->

# ISS-098

## Title

**A mid-wave `CancelledError` leaves its `wave_runs` row stuck at `running` forever.** `wave_scheduler.py:340` catches `except Exception:` to flip the in-flight wave's row terminal, but `asyncio.CancelledError` has inherited from `BaseException` since Python 3.8, so it is not caught and the row is never updated. Reachable on every Stop-during-a-wave (probe-confirmed that Stop does raise `CancelledError` through `run_fanout`). **Why it is only trivial:** WR-01's stale-row sweep (`wave_scheduler.py:304-313`) flips such rows `superseded` on the next resume, so the durable record self-corrects and no resume decision is made on the stale value. The visible cost is a `wave_runs` row reading `running` for a run that is plainly terminal — misleading to anyone reading the table directly. Fix is one token: `except BaseException:` (re-raising) or an explicit `except asyncio.CancelledError:` arm beside the existing one.

## Severity / Tag / Status (original row)

- Sev: trivial
- Tag: engine
- Status: **RESOLVED** by [FIX-425](20260831-1804-FIX-425.md) (found during quick-260812-g1c / FIX-232; cosmetic, self-healing on resume)

## Source / Evidence

Found 2026-08-12 during quick-260812-g1c; carried over as "N5/Q4" from the ISS-091 investigation, which reached it via probe 3 plus a code read.
