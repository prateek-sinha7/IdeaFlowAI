---
id: FIX-235
type: fix
date: 2026-08-12
status: done
area: [sse, resume, agents]
summary: >-
  A re-delivered frame appended a second copy to every accumulating field in
  handlePipelineMessage — the reducer's correctness was a property of its CALLERS'
  dedup, the exact arrangement ISS-080 died of behind 15 green tests
source: .planning/FIX-REGISTER.md#fix-235
status_detail: "quick-260812-iqh"
relates: [FIX-201]
---

# FIX-235

**Phase / trigger:** The reducer had no identity of its own: six of its cases grow a field out of prev ([...prev.x, y] / string +) with nothing keyed on the frame, so a partial redelivery (an SSE resume from Last-Event-ID with no intervening agent_start) appended a second copy. hook_run compounded it — emit_hook_event bypasses the engine's stamping chokepoint, so the frame reaches the browser with no event_id and wsReplayState.ts:36 exempts unstamped frames by design; no frontend-only fix could touch it. And page.tsx:855 shadowed the run-id parameter, which is why FIX-201 added a deliberately undeduped second replay pass that fed the reducer every durable frame twice.

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

A re-delivered frame appended a second copy to every accumulating field in handlePipelineMessage — the reducer's correctness was a property of its CALLERS' dedup, the exact arrangement ISS-080 died of behind 15 green tests. The register row was right about the concern and wrong about three facts. (1) It names 2 accumulating fields; there are 7, six unprotected. (2) Its cited evidence command finds neither field it names — re-run verbatim at 07197b0e, `grep -nE '\+= 1

## Root cause

\.push\(
