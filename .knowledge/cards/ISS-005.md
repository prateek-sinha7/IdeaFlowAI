---
id: ISS-005
type: issue
status: open
area: [agents]
summary: >-
  declare gates:[validation] — emits validation_warning events → breaks the
  app_builder golden (INV-3)
source: .planning/ISSUES-REGISTER.md#iss-005
severity: ✓
status_detail: "Part A: named live re-confirm (count /api/v1). Part B (durable): a declared api_prefix deliverable-validator (clone of spec_plan_coverage.py), wired as an event-free post_step"
duplicate_rows: 2
---

# ISS-005

## Title

declare gates:[validation] — emits validation_warning events → breaks the app_builder golden (INV-3)

## Source / evidence

S / low (re-confirm); M / low (validator)

## Duplicate register rows (2)

1. ISS-005 | minor | product | FIXED (19-02, structural; live re-confirm deferred) | app-infra-generator ignored its own /api/v1 rule live (0× in 3,083 lines vs api-design 58× / devops 50×) (F5 residual) | REPORT-2026-06-12 §F5; P15 repositioned the prompt; 19-02 added the deterministic pure-stdlib api_prefix validator wired as an event-free post_step on the app_builder infra step (catches missing /api/v1 in a validation_results row, golden byte-identical). Live re-confirm (count /api/v1) deferred to the live pass.

2. ISS-005 | ✓ | prose-only /api/v1 standard: lives only in 3 AGENT.md bodies, zero code enforcement (grep /api/v1 *.py = 0); infra-generator's rule is incidental to its 12-file templating task → lost on live Haiku. P15 repositioned the contract (shipped+pinned) | Part A: named live re-confirm (count /api/v1). Part B (durable): a declared api_prefix deliverable-validator (clone of spec_plan_coverage.py), wired as an event-free post_step | declare gates:[validation] — emits validation_warning events → breaks the app_builder golden (INV-3) | S / low (re-confirm); M / low (validator)
