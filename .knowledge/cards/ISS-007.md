---
id: ISS-007
type: issue
status: open
area: [sse]
summary: >-
  FE clears cards on a timeout / sync ack inside the cancel handler (races) / engine
  special-casing — S / low
source: .planning/ISSUES-REGISTER.md#iss-007
severity: ✓ (repro /tmp/iss007_repro.py)
status_detail: "residual-drain on the cancel path: await (shield+timeout) the bg task, drain event_queue→WS, then raise (reuse the WR-01 residual-drain at websocket.py:2069-2084). FE needs zero ch"
duplicate_rows: 2
---

# ISS-007

## Title

FE clears cards on a timeout / sync ack inside the cancel handler (races) / engine special-casing

## Source / evidence

S / low

## Duplicate register rows (2)

1. ISS-007 | minor | product | FIXED (16-02) | IN-02: pipeline_cancelled ack not delivered on the live wire for cancel_pipeline. UI manifestation (S12): after the real Stop click the header correctly flips to "Pipeline stopped" and the DB row IS cancelled (server-side correct), BUT an agent card stays stuck on "In progress... RUNNING" with no completion toast — inconsistent panel state, because the terminal event never arrives to clear the card. Upgraded trivial→minor: it's user-visible (a stopped run still shows a running agent). | REPORT-2026-06-12 §Known-Info + UI campaign S12 11-after-cancel 2026-06-13 (DB=cancelled, header="Pipeline stopped", card stuck RUNNING)

2. ISS-007 | ✓ (repro /tmp/iss007_repro.py) | SAME root as ISS-002 Defect-2. websocket.py:1718-1722 drainer's except CancelledError runs bg.cancel(); raise and dies before the bg task yields pipeline_cancelled (engine.py:1670-1678); the frame lands on a queue no one drains. DB is correctly cancelled; live wire gets no terminal → card stuck RUNNING. Mirrored in revision drainer :1997-2050 | residual-drain on the cancel path: await (shield+timeout) the bg task, drain event_queue→WS, then raise (reuse the WR-01 residual-drain at websocket.py:2069-2084). FE needs zero change (useWorkflow.ts:403 already resets cards on pipeline_cancelled) | FE clears cards on a timeout / sync ack inside the cancel handler (races) / engine special-casing | S / low
