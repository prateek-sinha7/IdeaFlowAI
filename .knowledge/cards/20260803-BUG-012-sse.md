---
id: BUG-012-sse
type: bug
kind: event
title: BUG-012 — Reopened od_ppt (typed) deck renders BLANK on the shared run screen
  despite valid deck HTML
status: done
severity: major
applies_to:
  phases: []
  modules:
  - App
  - DashboardLayout
  - Match
  - PPTPreview
  - User
  - UserStoryPreview
  - WorkflowRun
  - api
  - app
  - contentSourceRunType.source.test
  globs: []
  requirements: []
locked_constraints: []
verification:
  type: test
  status: passed
  test_files:
  - contentSourceRunType.source.test.ts
compact_summary: 'Render dispatch keyed on stale `workflowType` routed reopened od_ppt/prototype decks to the empty user_stories renderer; fixed by threading a durable `contentSourceRunType` from reopen.'
last_updated: '2026-08-14'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Bilal Arshad <bilala@hexaware.com>'
---

# BUG-012 — Reopened od_ppt (typed) deck renders BLANK on the shared run screen despite valid deck HTML


### BUG-012 — Reopened od_ppt (typed) deck renders BLANK on the shared run screen despite valid deck HTML  [🟠 major] [FIXED ✅]
- **RESOLVED:** FIXED (quick 260716-o6z, commits `9320445f`/`ef4ccd26`) via the **durable** viewed-run-type thread (user's choice over the targeted one-liner): a new `contentSourceRunType` state in `page.tsx` (set = `fullRun.type` on reopen :1244, cleared on the fresh-launch reset :1484, passed to `DashboardLayout` :1507) is folded into `viewedRunType` (`DashboardLayout.tsx:1265-1268`), and the PreviewPanel dispatch is fed the viewed type by changing `:1825` `workflowType={workflowType}` → `workflowType={effectiveReviseType}` — so `detectedType`/`renderType` bind to the real viewed type for ANY reopened run (incl. outside the recents window). `PreviewPanel.tsx` untouched (its dispatch was already correct once fed the right type). RED→GREEN: source-lock `contentSourceRunType.source.test.ts` 7→7; e2e TS-T-04c — `iframe[title="Slide Deck Preview"]` not-found (blank) → renders (od_ppt "Slide Deck Preview" + od_prototype "Prototype Preview", blank count 0). tsc clean, at-risk 42/42, launch→watch byte-identical, SC-001 clean. **LIVE-PROVEN:** the exact run that was blank (od_ppt `cbd46671`, 20 KB deck) now renders the full DogWalk deck with the chrome correctly reading "RENDERS AS Slides" (was "User Stories"); user_stories reopen still renders (no regression). Screenshots `o6z-reopen-odppt.png` (after) vs `n2d-reopen-odppt.png` (before). This closes the stale-`workflowType` family (BUG-006 label / BUG-008 generic gate / BUG-012 typed dispatch).
- **Found:** 2026-07-16 · during the BUG-008 orchestrator live-proof visual pass (reopen an existing completed od_ppt run in the running app) · surface: shared run-screen PreviewPanel.
- **Symptom (VERIFIED, screenshot `n2d-reopen-odppt.png`):** reopening a completed `od_ppt` run whose `output` is **20,183 chars of valid deck HTML** (`GET /api/runs/cbd46671-…` → `type:od_ppt status:completed mime:text/html`, output head `<!DOCTYPE html>…DogWalk — Pitch…`) renders the Preview tab **BLANK** (no slides, `iframe` count 0) — NOT the "Output will appear here" empty state. The render-mode chrome shows **"RENDERS AS Auto / User Stories"** on an od_ppt run, the tell that `renderType` collapsed to the stale default `workflowType="user_stories"`. The left lane is correct (OD_PPT · Done, right title, 3-agent trace, `presentation.pptx` card), so the run bound correctly — only the deliverable render is wrong.
- **CONFIRMED root cause (investigation agent 2026-07-16 — read `.planning/IMPLEMENTATION-REGISTER.md` overview+locked-decisions + FE render phases 16/18/22/32/39/42 + `.planning/SSE-QA-BUG-LOG.md` BUG-003/006/008; traced the full chain in source `page.tsx`/`PreviewPanel.tsx`/`DashboardLayout.tsx`):** classification **(b) renderer-dispatch keyed on the stale `renderType`** — a pure DISPATCH bug, NOT a content-seeding gap (classification (a) is FALSE: the deck IS seeded). Confirmed pin: **`PreviewPanel.tsx:893`** (`FIRST_PARTY_RENDERERS[renderType]?.()`) fed a stale `renderType="user_stories"` from **`PreviewPanel.tsx:536`** (`detectedType = workflowType || …`), whose `workflowType` prop is the stale `DashboardLayout` state passed at **`DashboardLayout.tsx:1825`** (`workflowType={workflowType}`), left stale because the sync binder at **`DashboardLayout.tsx:330-368`** is gated `if (pipelineState?.isRunning)` (`:331`) and never fires on a terminal reopen. Full chain, each link verified in source:
  1. **The deck IS seeded (so (a) is FALSE).** `page.tsx handleSelectWorkflowRun` clears all content (`page.tsx:1225-1228`) then, in the `fullRun.output && isContentTerminal` block (`:1307`), the `else if (fullRun.type === … "od_ppt" …)` branch (`:1310`) runs **`setPptContent(fullRun.output)`** (`:1311`) — the 20 KB deck lands in `pptContent`, passed to `<PreviewPanel pptContent={pptContent || undefined}>` (`DashboardLayout.tsx:1821`). od_ppt does NOT reach the generic-else at `:1314` (that is a non-known-type fallback; od_ppt is a known typed branch), so `genericDeliverable` stays `undefined`.
  2. **`workflowType` is stale on reopen.** `page.tsx` cannot set it (it is `DashboardLayout` state, default `"user_stories"` `DashboardLayout.tsx:250-256`); the only rebinder is the `isRunning`-gated effect at `:330-368` which never runs for a terminal reopen. So the `workflowType` prop at `:1825` is `"user_stories"` (the same stale-`workflowType` root as BUG-003-L2/BUG-006/BUG-008; binder blamed `0590c2448`, 2026-05-19).
  3. **`renderType` collapses to `"user_stories"`.** `PreviewPanel.tsx:536` `detectedType = workflowType || (…)` short-circuits to the truthy stale `"user_stories"`; `:538-545` maps it through unchanged (its `od_ppt → ppt` case at `:540` is DEAD because `detectedType` is never `"od_ppt"`) → `renderType = "user_stories"`. This is exactly the screenshot's **"RENDERS AS Auto / User Stories"** tell (`rendererOptions` pushes `{value: renderType, label: FIRST_PARTY_LABELS[renderType]}` = "User Stories", `:846-847`/`:828`).
  4. **`hasContent` is TRUE → NOT the empty state.** `effPptContent = pptContent` (`:555`, override inactive), so `hasContent = !!(… || effPptContent || …)` = true (`:588`) → the render reaches `renderDeliverable()` (`:1017`/`:1075`), not the "Output will appear here" branch (`:1025-1027`). Matches the symptom: BLANK, not empty-state.
  5. **The dispatch misroutes to the empty user-stories renderer → BLANK.** `renderDeliverable()` (`:877-904`): step-1 `const firstParty = FIRST_PARTY_RENDERERS[renderType]?.()` (`:893`) = `FIRST_PARTY_RENDERERS["user_stories"]()` = `effUserStoryContent ? <UserStoryPreview/> : null` (`:797-800`) → `effUserStoryContent` is empty (reopen seeded only `pptContent`) → **`null`**; step-2 `if (genericDeliverable?.content)` (`:899`) → `undefined` → skip; **`return null` (`:903`) → blank**. The `ppt` renderer that WOULD show the deck (`(effPptContent || pptxCode) ? <PPTPreview content={effPptContent}/>`, `:805-808`) is registered under key `"ppt"` and is NEVER invoked because the dispatch key is the stale `"user_stories"`. The 20 KB deck in `effPptContent` is orphaned. (The `PPTTabActions` block `:972` and the deck-only preview chrome are likewise gated `renderType === "ppt"` → also silent.)
- **Why BUG-008 (n2d) does NOT fix this (confirmed):** BUG-008 decoupled ONLY `hasGenericDeliverable` (`PreviewPanel.tsx:586`) from the stale gate — but od_ppt seeds `pptContent` (a TYPED slot), not `genericDeliverable`, so `hasGenericDeliverable` stays `false` and the BUG-008 change is a provable no-op here. BUG-008 fixed the GENERIC-deliverable render path; BUG-012 is the TYPED-deliverable render dispatch — different seeding channel, different failure.
- **Affected workflow types (reasoned from the reopen seeding branches + the dispatch):** **od_ppt AFFECTED** (blank — content → `pptContent`). **od_prototype AFFECTED** (blank — `page.tsx:1312-1313` seeds `setPrototypeContent(fullRun.output)`; same misroute: `renderType="user_stories"` → the `"prototype"` renderer at `:809-812` never fires). Plain `ppt`/`prototype` and the `*_revision` variants (all route to typed slots at `page.tsx:1308-1313`) are AFFECTED whenever the stale `workflowType`'s `renderType` ≠ the seeded slot. **user_stories is AFFECTED only when the stale `workflowType` ≠ `user_stories`** — in this repro it happened to BE `"user_stories"`, so the reopened user_stories run rendered *coincidentally* (the `n2d-reopen-userstories.png` "renders perfectly" is luck, exactly the BUG-006 coincidence). **app_builder is NOT affected (does not blank):** app_builder is absent from the known-typed reopen branches (`page.tsx:1308/1310/1312` omit it), so it falls to the generic-else at `:1314` → `setGenericDeliverable(…)` → rescued by BUG-008's `:586` fix → renders via `GenericDeliverablePreview` (sandboxed iframe). (Fidelity nuance, separate/lower-sev: reopened app_builder renders as a generic HTML iframe, not the bespoke `AppBuilderIDEPreview` "Code" view — not blank, out of BUG-012 scope.)
- **PRE-EXISTING; the fourth-and-last unlogged consumer of the stale `workflowType` that the BUG-006 sweep (quick 260716-lb6) missed.** BUG-006 enumerated three consumers of the stale `workflowType` and fixed them keyed on `recentRuns.find(r => r.id === contentSourceRunId)?.type`: the lane TITLE (BUG-001), the revise-handler selection (BUG-003-L2), the type LABEL (BUG-006, applied at `DashboardLayout.tsx:1762` `runType={effectiveReviseType || pipelineState?.pipeline_type}`). The **PreviewPanel render-dispatch type** at `DashboardLayout.tsx:1825` is the fourth consumer and still passes raw `workflowType`. Same "run-screen state not bound to the viewed run" class as BUG-001/003/005/006/008. No register lock forbids rebinding `workflowType` on reopen (the binder's own comment `:325-329` states its intent is to keep `workflowType` in sync so PreviewPanel renders the right component; the `isRunning` gate is an incidental limitation, not a locked decision); the fix stays inside the REQ-UXFIX-04/D-21 `renderType`-dispatch table (register 2291/3057/3500 — renderers REUSED, ND-G) and adds no workflow-name literal (SC-001).
- **Proposed MINIMAL fix (NOT applied) — bind PreviewPanel's `workflowType` prop to the VIEWED run, mirroring the applied BUG-006 fix.** One line at `DashboardLayout.tsx:1825`: `workflowType={workflowType}` → `workflowType={effectiveReviseType || pipelineState?.pipeline_type || workflowType}` (reuse the in-scope `effectiveReviseType` = `viewedRunType ?? workflowType`, computed at `:1265-1269`; already consumed 63 lines above at `:1762`). Then reopened od_ppt → `detectedType="od_ppt"` → `renderType="ppt"` (`:540`) → `FIRST_PARTY_RENDERERS["ppt"]` renders `effPptContent`; od_prototype → `"prototype"` renderer. Behaviour: live/launch (`isRunning` true / `contentSourceRunId` null → `viewedRunType` undefined) → `workflowType` (byte-identical, no regression); terminal reopen → the viewed run's real type (fixed). Same known caveat as BUG-006/008: a run OUTSIDE the `recentRuns` window has `viewedRunType===undefined` → falls back to stale `workflowType` → still blank; the durable variant threads `fullRun.type` from `page.tsx handleSelectWorkflowRun` as a dedicated `contentSourceRunType` state (works for any reopened run). Optional hardening: relax the `:331` `isRunning` gate to also rebind `workflowType` from the viewed run on a terminal reopen — a single change that closes this whole stale-`workflowType` family at the source (larger blast radius: `workflowType` also drives revise handlers/chaining/notifications).
- **Classification — TARGETED guard (like BUG-006/008), NOT the systemic per-run-subscriber-scoping fix.** The two mechanisms are orthogonal: BUG-012 is a reopen-time stale-STATE binding feeding the render dispatch (no SSE frame involved), so the systemic per-run subscriber scoping (drop foreign-`pipeline_run_id` frames at `page.tsx:909-912`, the durable fix for the LIVE cross-run poisoning family BUG-005/010/011) would NOT touch it. BUG-012 needs its own targeted viewed-run binding — the direct sibling of the BUG-006 label fix.
- **Distinct from:** BUG-008 (generic/custom empty-state gate — FIXED), BUG-009 (empty deck OUTPUT — this run's output is present/valid, 20KB), BUG-006 (pipeline-type LABEL — FIXED).
- **NOT caused by n2d:** the BUG-008 change is a provable no-op for known types (`isKnownRenderType=true` + `knownContentPresent=true` → `hasGenericDeliverable` stays `false`); this blank-typed-deck reproduces independently of it.
- **Fix:** PENDING (awaiting root-cause + user scope decision).

### BUG-009 recurrence confirmed LIVE  (see BUG-009 entry above)
- **2026-07-16:** the *other* completed od_ppt run `6c6f38c7-…` has `output` length **3** (`"..."`) with `mime:text/html` — a live recurrence of BUG-009 (od_ppt completes with an effectively-empty deck). Distinct from BUG-012 (which has a full 20KB deck that renders blank). Backend fix still gated on the READ-ONLY register sign-off.

---
