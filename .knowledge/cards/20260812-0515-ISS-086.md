---
id: ISS-086
type: issue
kind: event
title: '"Request changes" (`redo`) at an agent''s own gate re-runs that agent WITHOUT
  injecting its prior output, so the agent returns only the delta and the previous...'
status: done
severity: major
applies_to:
  phases: []
  modules:
  - User
  - agents
  - app
  globs: []
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'Redo re-ran an agent with only the user''s instruction and no prior artifact, so ''add 2 more pages'' destroyed the other 6 existing tasks.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-217](20260811-2101-FIX-217.md), [FIX-228](20260812-0617-FIX-228.md)

**Referenced by:** [ISS-090](20260812-0632-ISS-090.md)

<!-- /RELATED -->

# ISS-086

## Title

**"Request changes" (`redo`) at an agent's own gate re-runs that agent WITHOUT injecting its prior output, so the agent returns only the delta and the previous work is destroyed.** Owner asked the Task Planner to "add 2 more pages"; it came back with **one** task. Measured on run `a0d17afa-7292-4bc9-88d9-681437d315bb`: `task_list` **v1 → v2 = 12,542 → 3,495 chars (−72.1%)**, headings **7 → 1**, **0 of 7 kept**. All six real tasks were destroyed (`Task 1: HTML Shell & Navigation Chrome`, `Task 2: Dashboard Overview`, `Task 3: Feature Analytics`, `Task 4: User Cohorts`, `Task 5: System Health`, `Task 6: Settings Page`) and replaced by the single heading `Task 1: Extend Dashboard with Two Additional Pages` — i.e. the model wrote ONLY the requested change, because it could not see what it was changing. **Prompt-level proof:** the redo dispatch (`agent_input` seq 5552, 02:57:09, matching the `gate_events` `redo` row exactly) is 80,306 chars and contains **neither** `Task 2: Dashboard` **nor** `HTML Shell` — versus 80,200 chars on the first dispatch (seq 4099). The redo added **106 characters** — the instruction alone — and no prior artifact. **The contrast that localises the bug:** on the SAME run the `update_specs` path at the analyze gate behaved CORRECTLY — `spec` v1 → v2 went 53,288 → 25,988 chars but kept **12 of 12 headings (100%)** and added 2, i.e. compaction, not loss. So FIX-217's `spec_revision_prior_artifact` injection works for `update_specs` → specify, and the **`redo` path at every agent gate has the same hole FIX-217 closed for one path only**. Judging this by length alone would have mis-read it in both directions — the healthy path shrank 51% and the broken path is the one to fix. **Scope note (owner's framing, and it is correct):** re-running from a specific agent (`redo`) and re-running the specify→plan→analyze sub-pipeline (`update_specs`) are different code paths with different context assembly, and only the second was ever fixed or tested. Every per-agent iteration path needs coverage.

## Severity / Tag / Status (original row)

- Sev: **major**
- Tag: agents,engine
- Status: **FIXED — FIX-228 / TEST-012 (quick-260812-8j4), commits `70eb3636` (RED tests) + `38d0166f` (fix), branch `bugfix/spec-revision-context-loss`, NOT pushed.** Root cause was a *decision*, not a mechanism: P23 modelled a re-run as a REGENERATION and minted a fresh `:redo{N}` thread specifically to stop the model seeing its rejected output; FIX-217 hit the identical fork on the `update_specs` path and resolved it by pairing that fresh thread **with** a prior-artifact injection — **P23 shipped only the fresh-thread half of the pair.** The fix completes the pairing on the path FIX-217 did not cover, reusing FIX-217's `spec_revision_prior_artifact` seam and its renderer (one field, one block, two publishers — INV-12) rather than building a parallel one. Three near-identical `_gate_redo` consumers collapsed into one `_consume_redo`, which also closes the re-opened-gate branch's thin-copy defects (it set only the directive, so it ALSO lost `derived_from` lineage, left a duplicate `results` entry, and wrote no audit row — under-seeding `_seed_gate_reentry_attempts` and risking a colliding `:redo{N}` after a restart). The `PRIOR ARTIFACT` block moved **above** `ADDITIONAL INSTRUCTIONS` (subject first, instructions second) and its prose generalised — it read "the document the analysis report below refers to", and a redo has no report. Withheld for a **blank** redo (the FE's own "leave blank to just regenerate" — the retained P23 semantics) and for a per-task build dispatch (already carries a compacted view). Proven agent-general by parametrizing over registry-read agent ids across two pipelines (SC-001). Verified vs `742f0c6e`: goldens **10/10**, lint-imports **4 kept / 0 broken**, the first (non-redo) dispatch **byte-identical by sha256 `9f84b82e…`**, `test_restart_resume` unchanged at 7 failed / 48 passed with the SAME 7 ids. **Live Bedrock acceptance deferred** to the end-of-milestone pass (recipe in the quick task's BASELINE.md). Original report follows.

## Source / Evidence

Found by the owner during manual testing of the "apple design website" run and reported in their own words ("i tried to change spec in the first iteration to task planner agent to add 2 more pages and it came back with only 1 task"). I confirmed it from `artifact_refs` (version-by-version char/heading diff), `gate_events` (the `redo` row at `prototype-plan`, 02:57:09) and the durable `agent_input` prompts — mechanism proven at the prompt, not inferred.
