---
id: FIX-112
type: fix
date: 2026-07-24
status: done
area: [frontend, workflow, agents]
files:
  - frontend/src/components/workflow/IdeaInputPage.tsx
  - frontend/src/components/workflow/composer/ComposerPage.tsx
summary: >-
  Custom composer (ComposerPage) shows no agent recommendations or companion pipeline
  suggestions — ComposerPage is the custom workflow entry (not IdeaInputPage).
  Brief-based recommendations and companion suggestions existed only in
source: .planning/FIX-REGISTER.md#fix-112
ticket: KAN-121
invariants: [INV-1, INV-3, INV-12, SC-001]
---

# FIX-112

**Phase / trigger:** Phase 22 (KAN-112 custom composer)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Custom composer (ComposerPage) shows no agent recommendations or companion pipeline suggestions

## Root cause

ComposerPage is the custom workflow entry (not IdeaInputPage). Brief-based recommendations and companion suggestions existed only in IdeaInputPage; ComposerPage had no such logic. Fix: export getAgentRecommendations + COMPANION_GROUPS from IdeaInputPage (INV-12 single source), import + render them in ComposerPage keyed on the description field.

## Files changed

- `frontend/src/components/workflow/IdeaInputPage.tsx`
- `frontend/src/components/workflow/composer/ComposerPage.tsx`
