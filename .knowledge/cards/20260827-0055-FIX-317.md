---
id: FIX-317
type: fix
kind: event
title: Agent protocol wrappers are transport markers, not type claims
status: resolved
applies_to:
  phases: []
  modules: [MOD-frontend-src-components-results]
  globs:
    - 'frontend/src/components/results/SandboxTab.tsx'
    - 'frontend/src/components/results/artifactPreview.tsx'
  requirements: []
locked_constraints:
  - 'The <spec>, <tasks> and <analysis> tags name the STEP that produced a payload, not its type — every one carries a markdown document, so all three are unwrapped and rendered as markdown with no test on the body.'
  - 'An <artifact> and a bare <!DOCTYPE html> are whole documents and MUST stay fenced — react-markdown runs without rehype-raw (a security invariant), so unfenced they are escaped tag by tag into one unreadable paragraph.'
  - 'Never reach inside an existing fence still holds, with one narrow exception — a fence with NO info string whose entire body is one prose wrapper is an agent fencing its own markdown.'
  - 'Unmarked pasted source is caught by LONG_LINE_IS_CODE on line length, which is what keeps a large payload out of micromark''s inline tokeniser regardless of which tag delivered it.'
verification:
  type: test
  status: passed
  test_files:
    - 'frontend/src/components/results/SandboxTab.test.tsx'
    - 'frontend/src/components/results/artifactPreview.test.tsx'
compact_summary: 'Fencing keyed on the wrapper TAG rendered a markdown <spec> as raw source while <tasks>/<analysis> rendered fine; wrappers are now unwrapped unconditionally and only real documents are fenced.'
last_updated: 2026-08-27
author: sync
---

## The tell

`02-prototype-plan.md` and `03-prototype-analyze.md` rendered correctly and
`01-prototype-specify.md` did not — not because the spec was different in kind,
but because `spec` was on the alternation and `tasks`/`analysis` were not.

## The second case

The task planner writes `# Task Planner Agent`, then opens a bare ``` fence and
puts `<tasks>…</tasks>` inside it. The "never reach inside a fence" rule — right
for a deliberate ```js block — kept that as code with `<tasks>` on line 1.
