---
id: ADR-0030
type: adr
kind: state
title: Revision pipelines run the analyzer as step 0, declared by produces_solution_plan
status: accepted
applies_to:
  phases: []
  modules: [MOD-backend-agents, MOD-backend-app-agents]
  globs:
    - 'backend/agents/execution_engine/engine.py'
    - 'backend/agents/workflows/manifest.py'
  requirements: []
locked_constraints:
  - The analyzer's promotion to step 0 is DECLARED by the `produces_solution_plan` manifest flag, not by step index or agent id — the engine's post-step hook keys on the flag (SC-001).
  - The extracted Solution Plan rides on ectx.analyzer_solution and surfaces downstream as a Revision Analysis context chip.
  - 'The analyzer step sets `tools: {read_files: false}` so it streams live text instead of tool calls; Position 6 injects the prototype HTML for no-tools agents.'
verification:
  type: test
  status: passed
  test_files:
    - 'backend/tests/agents/test_compiled_plan_runs.py'
compact_summary: 'All three revision pipelines run prototype-revision-analyzer as step 0, declared via a produces_solution_plan manifest flag whose Solution Plan the engine extracts and surfaces downstream.'
last_updated: 2026-08-25
author: sync
---

## Decision

Small, large and feature revision pipelines all begin with the analyzer, so the
downstream agents work from a Solution Plan rather than re-deriving intent from
the brief. The flag is what makes it a manifest decision instead of an engine
special case.
