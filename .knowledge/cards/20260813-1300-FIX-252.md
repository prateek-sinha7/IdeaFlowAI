---
id: FIX-252
type: fix
kind: event
title: FIX-218
status: done
applies_to:
  phases: []
  modules:
  - context
  - engine
  - test_redo_gate_safety
  - test_spec_revision_cycles
  - InlineGateActions
  - agents
  globs:
  - backend/agents/execution_engine/engine.py
  - backend/agents/execution_engine/context.py
  - backend/tests/agents/test_spec_revision_cycles.py
  - backend/tests/agents/test_redo_gate_safety.py
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: test
  status: passed
  test_files:
  - backend/tests/agents/test_redo_gate_safety.py
  - backend/tests/agents/test_spec_revision_cycles.py
  - backend/tests/agents/test_spec_revision_context.py
compact_summary: 'A re-opened analyze gate''s second Update-the-Specs was a silent no-op, and nested revisions minted duplicate :rev1 checkpoint threads while zeroing the outer pass''s counter.'
last_updated: '2026-08-31'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: applies-to-glob
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-217](20260811-2101-FIX-217.md), [FIX-218](20260811-2246-FIX-218.md), [FIX-BUGFIX-NESTED-REVISION](20260811-2031-FIX-BUGFIX-NESTED-REVISION.md), [ISS-050](20260806-1225-ISS-050.md), [ISS-051](20260811-2101-ISS-051.md)

**Referenced by:** [ISS-051](20260811-2101-ISS-051.md), [ISS-064](20260812-0001-ISS-064.md)

<!-- /RELATED -->

# FIX-218

**Phase / trigger:** quick-260811-si4 (successor to FIX-217 / quick-260811-mxg; closes its carry-forward ISS-050 + ISS-051)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

A second "Update the Specs" at the RE-OPENED analyze gate was a silent no-op — the gate closed, instantly re-opened on identical content, no revision ran, and the build proceeded from the unrevised spec (defect B); and a nested revision pass minted DUPLICATE :rev1 checkpoint threads while zeroing the outer pass's counter and analysis report mid-flight (defect A)

## Root cause

(B) Of the three _gate_update_specs branches only two called the sub-pipeline; the third — the pending-re-open short-circuit — merely re-seeded spec_revision_pending_output and breaked. It was written to CLOSE a revision, not to start one, and the FE sends the analyzer's own output as the report (InlineGateActions.tsx:163), so the re-opened gate showed identical content and the no-op was invisible. (A) spec_revision_attempt is a per-_run_agent LOCAL, so the analyze re-run inside a pass — itself gated, and _run_review_gate acts on action == "update_specs" without ever consulting update_specs_eligible — re-entered the consumer with its own counter at 1; both levels published index 1 and minted the same :rev{N}. The sub-pipeline's finally then cleared to zero rather than restoring, stripping the outer pass while it was still on the stack.

## Files changed

- `backend/agents/execution_engine/engine.py`
- `backend/agents/execution_engine/context.py`
- `backend/tests/agents/test_spec_revision_cycles.py`
- `backend/tests/agents/test_redo_gate_safety.py`

## Verification — 2026-08-31 fixer pass (FIX-BUGFIX-NESTED-REVISION)

The bug line handed [FIX-BUGFIX-NESTED-REVISION](20260811-2031-FIX-BUGFIX-NESTED-REVISION.md)
(the brief for defects A and B) to a fixer. **No source change was needed** — this fix is
already in the tree and the brief's two mechanisms were re-read and confirmed at their current
lines:

- Defect B, the root-cause site: `engine.py:6823` — `_consume_gate_update_specs`, "The ONE
  `_gate_update_specs` consumer, shared by all three gate branches". All three branches
  (`:4570`, `:4695`, `:5845`) now route through it, so the pending-re-open short-circuit that
  used to only re-seed and `break` starts a real revision like the other two. Exactly one
  `_run_spec_revision_sub_pipeline` call site remains (INV-12).
- Defect A, distinct threads: `engine.py:6696-6698` — the effective index comes from
  `ectx.revision_high_water` (`context.py:354`), a monotone per-run mark that is never
  restored, so no two passes at any depth mint the same `:rev{N}`.
- Defect A, stack discipline: `engine.py:6788-6797` — the `finally` RESTORES
  `spec_revision_context` / `spec_revision_prior_artifact` / `revision_attempt` to the values
  captured at `:6700-6702` instead of zeroing an outer pass still on the stack. At the outer
  level the saved values are `""`/`""`/`0`, so dormancy (INV-3) is byte-equivalent.

```
TEST COVERAGE — FIX-252 (re-measured 2026-08-31, working tree at 923b0d4fd, no source change)
Unit tests:        5 passed / 1 xfailed in backend/tests/agents/test_spec_revision_cycles.py
                     → the 1 xfail is ISS-072's own strict marker, NOT this fix
                   4 passed in backend/tests/agents/test_spec_revision_context.py  → ALL GREEN
                   7 passed in backend/tests/agents/test_redo_gate_safety.py       → ALL GREEN
Integration tests: N/A — scripted-model harness only; no Bedrock/Postgres/Chromium path
Frontend tests:    N/A — no frontend file in this fix
Goldens:           not re-run — no source file was changed in this pass
lint-imports:      3 kept / 1 broken (`kernel imports only capability ports (scaffold)`)
                     — IDENTICAL to the 84f4bbe3 baseline recorded in the brief; pre-existing
Regression guards:
  - test_reopened_gate_second_update_specs_runs_a_second_cycle: defect B — a second
    "Update the Specs" at the re-opened gate actually starts a revision
  - test_reopened_gate_cycle_keeps_a_flat_stack: the second cycle is a SIBLING, not nested
  - test_update_specs_not_offered_while_a_revision_is_in_flight: the in-pass nesting route
    is fenced (ISS-053)
  - test_nested_revision_keeps_distinct_threads_and_restores_the_outer_pass: defect A —
    distinct :rev{N} at depth, outer scratch restored not zeroed
  - test_single_cycle_shape_is_unchanged: INV-3 dormancy — one cycle unchanged
```

SC-001 re-checked this pass: `grep -rn 'if pipeline_type ==\|spec\.id ==' backend/agents/execution_engine/`
returns nothing. No migration, no new dependency.
