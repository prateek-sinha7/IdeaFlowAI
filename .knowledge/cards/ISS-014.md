---
id: ISS-014
type: issue
status: open
area: [sse, workflow, agents]
summary: >-
  route the legacy composer (resurrects a superseded dual impl w/ a hardcoded 15-agent
  stub) / delete /api/capabilities (regresses API-02)
source: .planning/ISSUES-REGISTER.md#iss-014
severity: ✓ (orphaned dead code)
status_detail: "DELETE WorkflowComposer+CapabilityPalette; relocate AgentModelPicker into the live AgentsPopup + wire model_overrides into the run payload (delivers MODEL-03) or delete + v2-carry;"
duplicate_rows: 2
---

# ISS-014

## Title

route the legacy composer (resurrects a superseded dual impl w/ a hardcoded 15-agent stub) / delete /api/capabilities (regresses API-02)

## Source / evidence

S / low (delete); M / low-med (relocate picker)

## Duplicate register rows (2)

1. ISS-014 | minor | product | FIXED (18-04/05) | The agent-level custom composer IS live and well-built — the "Compose a custom workflow" tile → IdeaInputPage → Advanced → "Workflow configuration" modal (Agents / Skills & Hooks tabs, drag-to-reorder, "+ Add agent (8 left)") → a searchable "Add agent" library (category sidebar + agent cards with descriptions + "+ Add"). What's dead is only the capability-level CapabilityPalette.tsx/WorkflowComposer.tsx (the /api/capabilities allow-listed-palette composer) — mounted by no live route. So users CAN compose custom agent workflows; they CANNOT compose from the capability palette. | UI campaign S07 A-agents-popup.png + B-agent-library.png 2026-06-13 (corrects the earlier "composer is dead" reading)

2. ISS-014 | ✓ (orphaned dead code) | WorkflowComposer.tsx mounted by no route (pre-existing legacy, base commit 4889e3a8); CapabilityPalette/AgentModelPicker consumed only by it; /api/capabilities live but zero FE consumer. Phase-8 API-06 hung new panels into the unrouted legacy composer; Phase-12 logged it a "separate composer-routing concern" → never resolved = INV-3/INV-12 dual-impl smell | DELETE WorkflowComposer+CapabilityPalette; relocate AgentModelPicker into the live AgentsPopup + wire model_overrides into the run payload (delivers MODEL-03) or delete + v2-carry; KEEP /api/capabilities (API-02 registry-reflection contract) | route the legacy composer (resurrects a superseded dual impl w/ a hardcoded 15-agent stub) / delete /api/capabilities (regresses API-02) | S / low (delete); M / low-med (relocate picker)
