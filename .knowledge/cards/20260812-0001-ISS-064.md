---
id: ISS-064
type: issue
kind: event
title: A second "Update the Specs" at the RE-OPENED analyze gate is a SILENT NO-OP.
  `engine.py:3545` re-seeds `ectx.spec_revision_pending_output` and `break`s witho...
status: done
severity: major
applies_to:
  phases: []
  modules:
  - agents
  - app
  globs:
  - engine.py
  - .planning/BUGFIX-NESTED-REVISION.md
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'A second ''Update the Specs'' at the re-opened analyze gate silently no-op''d; closed by routing all 3 gate branches through one shared consumer.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-185](20260806-1225-FIX-185.md), [FIX-218](20260811-2246-FIX-218.md), [FIX-219](20260812-0107-FIX-219.md), [FIX-252](20260813-1300-FIX-252.md), [FIX-BUGFIX-NESTED-REVISION](20260811-2031-FIX-BUGFIX-NESTED-REVISION.md), [ISS-050](20260806-1225-ISS-050.md), [ISS-053](20260811-2101-ISS-053.md), [ISS-062](20260811-2303-ISS-062.md)

**Referenced by:** [ISS-050](20260806-1225-ISS-050.md), [ISS-051](20260811-2101-ISS-051.md), [ISS-052](20260811-2101-ISS-052.md), [ISS-053](20260811-2101-ISS-053.md), [ISS-062](20260811-2303-ISS-062.md), [ISS-066](20260812-0005-ISS-066.md), [ISS-067](20260812-0005-ISS-067.md), [ISS-072](20260812-0131-ISS-072.md)

<!-- /RELATED -->

# ISS-064

## Title

A second "Update the Specs" at the RE-OPENED analyze gate is a SILENT NO-OP. `engine.py:3545` re-seeds `ectx.spec_revision_pending_output` and `break`s without calling `_run_spec_revision_sub_pipeline`; the other two `_gate_update_specs` branches (`:3428`, `:4444`) do call it (`:3444`, `:4480`). The click is accepted, the gate closes, no revision runs, and the build proceeds from the spec the user was trying to change — invisible on screen, one wasted build.

## Severity / Tag / Status (original row)

- Sev: major
- Tag: agents
- Status: **CLOSED 2026-08-12 — FULLY FIXED by FIX-252 (ex-FIX-218).** The "UI half is NOT reachable" clause below was based on ISS-062, which is now closed as a misdiagnosis (see that row): the re-opened analyze gate does advertise `eligible=True`, so the button is NOT hidden there and the engine fix is reachable from the UI. The engine half was re-proven independently rather than taken on FIX-218's word — the pre-fix tree at `30159b36` was exported to a scratch worktree and observed RED (`test_reopened_gate_second_update_specs_runs_a_second_cycle` and `test_reopened_gate_cycle_keeps_a_flat_stack` both FAILED, "the second 'Update the Specs' ran no cycle"), and GREEN at HEAD (5 passed). All three `_gate_update_specs` branches now drive the single shared `_consume_update_specs` consumer, which holds the file's only sub-pipeline call site (INV-12 gate: 1 definition / 3 callers / 0 duplicates). Live corroboration: run `d5dbc9f2` produced spec v3 at 40,052 chars. The one genuinely missing guard — asserting the eligibility vector against the REAL gate rather than a stub — was added as part of FIX-219 (ISS-053), where the three-gate verdict measured honored / REFUSED / honored. Original text follows — (filed 2026-08-11 as ISS-050 and RENUMBERED to 064 — ISS-050 was already taken by a different, unrelated defect: "CR-02 guard blocks prototype_revision and user_stories_revision", closed by FIX-185 / commit `fd2ad8f1`. That id lived only in a commit message, never in either register, so an issues-register-only lookup missed it.) — FIX-218 / quick-260811-si4 fixed the ENGINE half (a real second cycle now runs; live-proven, spec v3 on run d5dbc9f2). The UI half is NOT reachable — see ISS-062: the eligibility fence over-fires so the button is hidden at exactly the re-opened gate. Original resolution note follows — (2026-08-11)

## Source / Evidence

Found during quick-260811-mxg plan-check/verify; brief `.planning/BUGFIX-NESTED-REVISION.md`. NOT observed live — on run 5ecb990f I reached that exact gate and approved instead of re-revising. si4 planner independently reproduced the flat-sibling shape offline. **RESOLVED (FIX-218 / quick-260811-si4):** the re-opened gate now drives the shared `_consume_update_specs` helper as a FLAT SIBLING cycle — the sub-pipeline's `finally` precedes its terminal yield and the driving `async for` exhausts the generator before breaking, so the previous pass is fully unwound and the second cycle sits at the same stack depth (the REDO-GATE F2 shape), threading `:rev2`. Guarded by `test_reopened_gate_second_update_specs_runs_a_second_cycle` + `test_reopened_gate_cycle_keeps_a_flat_stack`, both observed RED first.
