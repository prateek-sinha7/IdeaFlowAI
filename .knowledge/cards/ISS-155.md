---
id: ISS-155
type: issue
status: done
summary: >-
  \ — chainContextBlock?.trim()), and all four staging/consumption guards agree so the
  reload path can no longer reach a different verdict. TEST-034 (3), seen RED — the
  empty brief returned 200 OK and minted a run
source: .planning/ISSUES-REGISTER.md#iss-155
severity: major
status_detail: "FIXED — FIX-255 (2026-08-13, commits 0279fae8 + d0dc1f49). Fixed at all three layers rather than one: the ingress refuses an empty/whitespace message pre-mint (empty_brief), canCon"
---

# ISS-155

## Title

\

## Source / evidence

chainContextBlock?.trim()), and all four staging/consumption guards agree so the reload path can no longer reach a different verdict. TEST-034 (3), seen RED — the empty brief returned 200 OK and minted a run. LIVE-VERIFIED: 400 empty_brief` with run rows 46→46, and the wizard discriminates (no context block → "Add a brief" + Continue disabled; with one → Continue enabled, dev's UX preserved). Side effect: repaired dev's tsc break, 4 errors → 2.
