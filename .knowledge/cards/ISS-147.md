---
id: ISS-147
type: issue
status: open
area: [frontend, sse]
files:
  - frontend/src/hooks/useRunStream.ts
summary: >-
  pipeline_complete is missing from useRunStream.ts's non-live-close terminal list, so
  a completed run's stream reconnects when it should settle quietly — ADR-0001's own
  predicted failure mode, realized
source: .planning/ISSUES-REGISTER.md#iss-147
severity: minor
status_detail: "OPEN (filed 2026-08-13 during quick-260813-3wo / FIX-248, deliberately NOT fixed — the TRIGGER, not the defect FIX-248 closes)"
---

# ISS-147

## Title

pipeline_complete is missing from useRunStream.ts's non-live-close terminal list, so a completed run's stream reconnects when it should settle quietly — ADR-0001's own predicted failure mode, realized. frontend/src/hooks/useRunStream.ts:301 — `if (type === "pipeline_cancelled" \

## Source / evidence

\
