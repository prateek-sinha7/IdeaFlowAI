---
id: ISS-052
type: issue
status: open
area: [agents, artifacts]
summary: >-
  One "Update the Specs" click costs FOUR approvals: the re-run specify gate, the
  re-run plan gate, then the analyze gate RE-OPENS after the sub-pipeline returns
source: .planning/ISSUES-REGISTER.md#iss-052
severity: minor
status_detail: "PARTIALLY FIXED — FIX-220 / quick-260812-1nz (2026-08-12). The ambiguity clause is CLOSED; the extra-approval clause is NOT — it is now ISS-072. This row had two clauses and they h"
relates: [ISS-064]
---

# ISS-052

## Title

One "Update the Specs" click costs FOUR approvals: the re-run specify gate, the re-run plan gate, then the analyze gate RE-OPENS after the sub-pipeline returns. The doubled analyze gate is redundant and the two are visually identical, so the user cannot tell which one they are on. This is the real UX fix behind ISS-064.

## Source / evidence

Observed live driving run 5ecb990f end to end on Bedrock (2026-08-11). Deliberately left OPEN by quick-260811-si4 (which closed ISS-064/051): collapsing the doubled analyze gate means SUPPRESSING a gate — a behaviour change no source artifact analysed, and outside the .planning/BUGFIX-NESTED-REVISION.md brief. It must NOT be done via ectx.revision_attempt alone, because the specify and plan gates also fire inside the pass and those ARE wanted. si4 re-confirmed the shape by probe: 3 gate firings per cycle, [0] outer / [1] in-pass analyze / [2] re-opened.
