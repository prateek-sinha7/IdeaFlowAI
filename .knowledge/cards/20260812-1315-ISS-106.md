---
id: ISS-106
type: issue
kind: event
title: The graceful-shutdown budget is bounded only in its drain steps; the final
  pool close is unbounded, so the documented worst case is not a worst case. `close_...
status: open
severity: minor
applies_to:
  phases: []
  modules:
  - app
  - checkpointer
  - run_shutdown
  globs:
  - checkpointer.py
  - docker-compose.yml
  - run_shutdown.py
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: optional
  test_files: []
compact_summary: 'close_checkpointer()''s pool.close() has no timeout, so a slow Postgres pool close can push teardown past the SIGKILL deadline undetected.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-234](20260812-1312-FIX-234.md), [ISS-088](20260812-0604-ISS-088.md)

**Referenced by:** [ISS-103](20260812-1312-ISS-103.md)

<!-- /RELATED -->

# ISS-106

## Title

**The graceful-shutdown budget is bounded only in its drain steps; the final pool close is unbounded, so the documented worst case is not a worst case.** `close_checkpointer()` awaits `pool_to_close.close()` with no timeout (`checkpointer.py:140-144`). It is step 4 of `shutdown_run_infrastructure()`, i.e. it runs AFTER the 24s of bounded waits FIX-234 documented, and it sits between the application and process exit. A Postgres pool that is slow to close (an in-flight statement, a wedged socket, a server that stops responding) therefore pushes the total past `stop_grace_period` (30s, `docker-compose.yml:75`) and docker SIGKILLs mid-teardown — the exact failure the budget exists to prevent, with the shutdown summary never logged. `TEST-018`'s `test_production_teardown_budget_fits_inside_stop_grace_period` cannot catch this: it sums the configured drains, and there is no knob for this wait to appear in. **Fix shape:** wrap step 4 in `asyncio.wait_for(..., timeout=<new setting>)` and include that setting in the arithmetic guard, so the budget test covers the whole path rather than the part that happens to be configurable. The close is already non-raising and latches `_closed` before touching the pool, so a timeout would leave consistent process state.

## Severity / Tag / Status (original row)

- Sev: minor
- Tag: infra
- Status: **OPEN** (found during quick-260812-hsx / FIX-234, while correcting the budget comment that FIX-234 was itself fixing)

## Source / Evidence

Read at `9df9c1c8` during quick-260812-hsx: `checkpointer.py:140-144` has no timeout wrapper, and `run_shutdown.py:189-207` awaits it directly. Recorded rather than fixed because bounding it is a behaviour change to the production shutdown path, which is not what ISS-088 authorised; the config comment and the budget test now both state the limitation explicitly instead of implying a total.
