---
id: FIX-208
type: fix
kind: event
title: Notification panel agent progress shows 0/N for background concurrent runs
  — only viewed run shows live progress
status: done
applies_to:
  phases:
  - '38'
  modules:
  - AppHeader
  - DashboardLayout
  - page
  - App
  - app
  globs:
  - frontend/src/app/dashboard/page.tsx
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/components/layout/AppHeader.tsx
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'AppHeader''s runningPipelines mapping only read the viewed run''s completedCount and showed 0 for all background runs; computed a runAgentsCompletedMap from runStore per live run instead.'
last_updated: '2026-08-07'
author: 'Prateek Sinha <2000152842@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-202](20260807-1929-FIX-202.md)

<!-- /RELATED -->

# FIX-208

**Phase / trigger:** Phase 38 (notification panel), FIX-202

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Notification panel agent progress shows 0/N for background concurrent runs — only viewed run shows live progress

## Root cause

AppHeader.runningPipelines mapping used pipelineAgentsCompleted (scalar from pipelineState.completedCount, viewed run only) for active run, and 0 for all other runs. Background run completedCount was never read. The data existed in runStore.get(runId).pipelineState.completedCount for every attached run. Fix: compute runAgentsCompletedMap: Record<string, number> in page.tsx from runStore.get() for every live run; thread via DashboardLayout → AppHeader; use runAgentsCompletedMap[r.id] ?? 0 for non-active runs in runningPipelines mapping.

## Files changed

- `frontend/src/app/dashboard/page.tsx`
- `frontend/src/components/layout/DashboardLayout.tsx`
- `frontend/src/components/layout/AppHeader.tsx`
