---
id: ISS-064
type: issue
status: done
area: [agents]
summary: >-
  A second "Update the Specs" at the RE-OPENED analyze gate is a SILENT NO-OP.
  engine.py:3545 re-seeds ectx.spec_revision_pending_output and breaks without calling
  _run_spec_revision_sub_pipeline
source: .planning/ISSUES-REGISTER.md#iss-064
severity: major
status_detail: "CLOSED 2026-08-12 — FULLY FIXED by FIX-252 (ex-FIX-218). The "UI half is NOT reachable" clause below was based on ISS-062, which is now closed as a misdiagnosis (see that row): the"
relates: [FIX-218]
---

# ISS-064

## Title

A second "Update the Specs" at the RE-OPENED analyze gate is a SILENT NO-OP. engine.py:3545 re-seeds ectx.spec_revision_pending_output and breaks without calling _run_spec_revision_sub_pipeline; the other two _gate_update_specs branches (:3428, :4444) do call it (:3444, :4480). The click is accepted, the gate closes, no revision runs, and the build proceeds from the spec the user was trying to change — invisible on screen, one wasted build.

## Source / evidence

Found during quick-260811-mxg plan-check/verify; brief .planning/BUGFIX-NESTED-REVISION.md. NOT observed live — on run 5ecb990f I reached that exact gate and approved instead of re-revising. si4 planner independently reproduced the flat-sibling shape offline. RESOLVED (FIX-218 / quick-260811-si4): the re-opened gate now drives the shared _consume_update_specs helper as a FLAT SIBLING cycle — the sub-pipeline's finally precedes its terminal yield and the driving async for exhausts the generator before breaking, so the previous pass is fully unwound and the second cycle sits at the same stack depth (the REDO-GATE F2 shape), threading :rev2. Guarded by test_reopened_gate_second_update_specs_runs_a_second_cycle + test_reopened_gate_cycle_keeps_a_flat_stack, both observed RED first.
