---
id: FIX-193
type: fix
date: 2026-08-06
status: done
area: [frontend, agents]
files:
  - frontend/src/components/chat/RunChatLane.tsx
  - frontend/src/components/chat/RunChatLane.test.tsx
summary: >-
  ISS-059: Concierge free-text ask during an active ("building") run silently no-ops —
  every message typed while agents are running returns HTTP 200 {"channel":"steering"}
  with no reply (KAN-165)
source: .planning/FIX-REGISTER.md#fix-193
invariants: [INV-3, INV-12, SC-001]
---

# FIX-193

**Phase / trigger:** Phase 33 (Concierge D-05), Phase 43 (43-02 classify-and-route), KAN-165

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

ISS-059: Concierge free-text ask during an active ("building") run silently no-ops — every message typed while agents are running returns HTTP 200 {"channel":"steering"} with no reply (KAN-165)

## Root cause

handleFreeText in RunChatLane.tsx had a runState === "complete" branch that classifies intent via classifyIntent LLM and routes "ask" → {concierge: true}. Every other runState — including "building" (active run) — fell straight to the unconditional fallback sendMessage(text, attachments) with no options (no concierge key). Backend's route_chat_turn receives concierge=false, dispatches to CHANNEL_STEERING (a fire-and-forget steering note with no reply mechanism). HTTP 200 returned, user sees their message echoed, then silence — no error, no chat_reply, no Concierge. Fix: add runState === "building" branch, structurally parallel to "complete", reusing the same classifyIntent + addOptimisticMessage + {concierge: true} pattern. Non-"ask" fallback is byte-identical to pre-fix "building" behavior (steering). Locked: "clarify" / "gate" / "terminal" NOT touched (Group-C decision, 42-03).

## Files changed

- `frontend/src/components/chat/RunChatLane.tsx`
- `frontend/src/components/chat/RunChatLane.test.tsx`
