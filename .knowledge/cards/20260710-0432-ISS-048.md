---
id: ISS-048
type: issue
kind: event
title: 'Phase-38 review (verifier PASS 11/11, 0 crit/high): (MD-1) `useNotifications.ts:99`/`DashboardLayout.tsx:489`
  gate notification never reverts — `markGatePaus...'
status: done
severity: minor
applies_to:
  phases: []
  modules:
  - AnalyticsPage
  - DashboardLayout
  - Home
  - HomeLaunchGrid
  - HomeLaunchGrid.test
  - LIBRARY_AGENTS
  - ReviewGatesSection
  - ReviewGatesSection.test
  - SavedWorkflowsPage
  globs:
  - useNotifications.ts
  - DashboardLayout.tsx
  - AnalyticsPage.tsx
  - analytics.py
  - ReviewGatesSection.test.tsx
  - HomeLaunchGrid.test.tsx
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'Phase-38 review: gate notifications never reverted ''paused''->''running'' on resolve, and avgTokens divided by completed-run count; fixed with markGateResumed and corrected denominator.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [ISS-045](20260709-0610-ISS-045.md)

<!-- /RELATED -->

# ISS-048

## Title

Phase-38 review (verifier PASS 11/11, 0 crit/high): (**MD-1**) `useNotifications.ts:99`/`DashboardLayout.tsx:489` gate notification never reverts — `markGatePaused` sets `"gate"` and nothing returns it to `"running"` on gate-resolve → a resumed run shows "paused" until a terminal mark (real feed UX bug); (**MD-2**) `AnalyticsPage.tsx:183` KPI `avgTokens = totalTokens/completedCount` divides an all-run numerator by a completed-run denominator + inconsistent labels (display-correctness); (MD-3) estimate key divergence (`analytics.py` keys `type_avg_duration_sec` on `run.type` vs card `[row.id]`) — WORKING AS SPECIFIED (mandated tolerant fallback; SC-2 holds) → optional enhancement. Pre-existing test reds (NOT 38-caused, byte-identical at base): `ReviewGatesSection.test.tsx` 3 (LIBRARY_AGENTS data-drift) + `HomeLaunchGrid.test.tsx` 5 (Phase-21 "Your workflows" moved to SavedWorkflowsPage — dup of ISS-045).

## Severity / Tag / Status (original row)

- Sev: minor
- Tag: product
- Status: **FIXED** (feat/ui-2, 2026-07-10 — MD-1 `markGateResumed` reverts gate→running off the generic reviewGateData+isRunning markers + hook pin (both directions) `ce0ac16a`; MD-2 `avgTokens = totalTokens/totalCount` + "tokens per run" label + pin (fail-before proven) `352d6614`; MD-3 left AS-SPECIFIED (tolerant fallback, SC-2 holds); ReviewGatesSection 3 reds reconciled — stale `PIPELINE_CATEGORIES` prototype=4/all=54 → 5/55 (invariant-preserving) + uncheck-all-3-gates test `4d683ffa`, now 19/19. HomeLaunchGrid 5 reds = dup of ISS-045, resolved there. tsc 0, lint-imports 4/0, 5 backend goldens byte-identical, retired-palette 0)

## Source / Evidence

38-REVIEW.md + 38-VERIFICATION.md (2026-07-10)
