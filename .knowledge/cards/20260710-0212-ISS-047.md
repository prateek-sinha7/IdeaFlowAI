---
id: ISS-047
type: issue
kind: event
title: '37-07 unified-launch rebuild re-review (verifier PASSED 3/3, 0 blockers, contract
  byte-identical, 0 dropped behaviors): (WR-05, VISIBLE bug) `LaunchWizard.ts...'
status: done
severity: minor
applies_to:
  phases: []
  modules:
  - AgentModelPicker
  - AgentsPopup
  - IdeaInputPage
  - LaunchWizard
  - app
  globs:
  - LaunchWizard.tsx
  requirements: []
locked_constraints:
- INV-12
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: '37-07 re-review found MODE_CONFIG[initialMode] froze header chrome after toggling; fixed to key off current mode, plus 6 other findings across 7 commits.'
last_updated: '2026-08-14'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

# ISS-047

## Title

37-07 unified-launch rebuild re-review (verifier PASSED 3/3, 0 blockers, contract byte-identical, 0 dropped behaviors): (**WR-05**, VISIBLE bug) `LaunchWizard.tsx:125` `cfg = MODE_CONFIG[initialMode]` freezes header/brief/placeholder/save-modal chrome to the INITIAL family — after the Web/Deck toggle the page reads "prototype" while building/saving a ppt deck (Phase-36 stale-chrome-on-toggle class) → `MODE_CONFIG[mode]` + a chrome-after-toggle test; (**WR-06**) ppt custom-template branch (`customTemplateBody` + `designSystemId:null`) UNCOVERED by golden + component test (byte-correct by construction, unproven) → add golden + `pick-deck-custom` test; (**WR-07**) chaining launch (`finalBrief=contextBlock`+`sourceRunId`) untested; (**IN-04**) dead `onModelOverridesChange` prop + vestigial `modelOverrides` draft field → divergent-model-on-restore risk; (**IN-05**) orphaned `prototype/discovery` route + dropped template-input discovery (INV-12 smell); (IN-06) WR-04 FE gate keys on `workflowId` not the resolved base pipeline_type (latent); (IN-07) redundant double auth gate. **FIXED** FE-only on feat/ui-2 in 7 atomic commits (all 7 items): WR-05 `b7f957e9` (cfg→`MODE_CONFIG[mode]` + chrome-after-toggle test), WR-06 `98190930` (ppt custom-template golden + `pick-deck-custom` component test, additive), WR-07 `0e6d86b9` (chaining Continue → `finalBrief=contextBlock`+`sourceRunId` test), IN-04 `b4633ab0` (removed the `modelOverridesRef` mirror from launch-draft + save payload + restore + dead prop → `selections[id].model` single source; pin test; AgentsPopup interface prop kept for IdeaInputPage), IN-05 `4004fc5c` (deleted the orphan `prototype/discovery` route + its dead sole-consumer `getPrototypeTemplate`/`PrototypeTemplateDetail` + the unreachable `template inputs only` golden → one discovery impl), IN-06 `060dcafd` (Configure gate keys on resolved base `detail.id` not `workflowId`; discriminating test), IN-07 `e4dd37c7` (dropped the wrapper's redundant auth gate; LaunchWizard owns it). Verified offline: touched suites 39/39 (parity+LaunchWizard+ConfigureScreen+IdeaInputPage.modelOverrides), tsc --noEmit 0 (identity), retired-palette grep 0 on all touched files, 5 backend goldens byte-identical (git-clean), lint-imports 4/0, SC-001 mode-keyed (no per-workflow-name branch), AgentModelPicker not mounted. Mocked Playwright live-deferred.

## Severity / Tag / Status (original row)

- Sev: minor
- Tag: product
- Status: **FIXED** (feat/ui-2, 2026-07-10)

## Source / Evidence

37-REVIEW.md re-review (2026-07-10)
