---
id: FIX-029
type: fix
kind: event
title: 'byv: Prototype "Thinking" tab missing the StartingPoint + Clarifications preamble,
  and the build checklist flashes "all complete" between tasks'
status: done
applies_to:
  phases:
  - '03'
  modules:
  - AgentThinkingTab
  - PrototypePipelineView
  - Card
  - ClarificationsCard
  - StartingPointCard
  globs:
  - frontend/src/components/results/AgentThinkingTab.tsx
  - frontend/src/components/results/PrototypePipelineView.tsx
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'AgentThinkingTab''s prototype early-return skipped the StartingPoint/Clarifications preamble, and buildStatus==''done'' briefly true between tasks flashed the checklist all-complete; fixed both.'
last_updated: '2026-07-03'
author: 'Prateek Sinha <2000152842@hexaware.com>'
author_source: code-commit
---

# FIX-029

**Phase / trigger:** Phase 3 (Thinking-view) · quick-260703-byv

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

byv: Prototype "Thinking" tab missing the StartingPoint + Clarifications preamble, and the build checklist flashes "all complete" between tasks

## Root cause

AgentThinkingTab's isPrototypePipeline early-return rendered PrototypePipelineView alone, skipping the StartingPointCard/ClarificationsCard shown on the main render path; separately PrototypePipelineView derived all-done from buildStatus==="done" alone, which is briefly true between tasks so the checklist flashed all-complete mid-loop. Fix: render the two existing cards as a preamble above the pipeline view; add a buildTrulyDone gate (true only once build is done AND validation has started or the run has stopped) at the terminal sites; currentTaskIndex falls back to realtimeCompletedCount during the transient-done window so completed tasks stay checked. Backend untouched (FE-only).

## Files changed

- `frontend/src/components/results/AgentThinkingTab.tsx`
- `frontend/src/components/results/PrototypePipelineView.tsx`
