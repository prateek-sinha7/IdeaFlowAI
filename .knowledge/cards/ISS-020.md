---
id: ISS-020
type: issue
status: open
area: [workflow, artifacts, harness]
summary: >-
  ws_rewrite scenarios (sample_wave/fanout/ui_custom_proto — no product launch
  surface) show "User Stories" FE labels + the user_stories questionnaire, because the
  rewrite only changes the backend pipeline_type
source: .planning/ISSUES-REGISTER.md#iss-020
severity: trivial
status_detail: "NOTE"
duplicate_rows: 2
---

# ISS-020

## Title

ws_rewrite scenarios (sample_wave/fanout/ui_custom_proto — no product launch surface) show "User Stories" FE labels + the user_stories questionnaire, because the rewrite only changes the backend pipeline_type, not the FE's displayed workflow type. Harness artifact, not a product bug; means FE pipeline-specific labels aren't faithfully exercised for the fixture workflows.

## Source / evidence

UI campaign 2026-06-13

## Duplicate register rows (2)

1. ISS-020 | trivial | harness | NOTE | ws_rewrite scenarios (sample_wave/fanout/ui_custom_proto — no product launch surface) show "User Stories" FE labels + the user_stories questionnaire, because the rewrite only changes the backend pipeline_type, not the FE's displayed workflow type. Harness artifact, not a product bug; means FE pipeline-specific labels aren't faithfully exercised for the fixture workflows. | UI campaign 2026-06-13

2. ISS-020 | ✓ (harness, direct-fill) | ws_rewrite mutates only the backend pipeline_type, not the FE's displayed workflow type | none — harness artifact; the fixture workflows have no product launch surface anyway (see ISS-015) | — | —
