---
id: FIX-074
type: fix
date: 2026-07-21
status: done
area: [frontend, sse]
files:
  - frontend/src/components/workflow/AgentsPopup.tsx
summary: >-
  Config levers always show "Default" — fix by giving ConfigLeversFlat its own local
  selections state
source: .planning/FIX-REGISTER.md#fix-074
invariants: [INV-1, INV-3, INV-12, SC-001]
---

# FIX-074

**Phase / trigger:** Phase 37/41 (B3/B7)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Config levers always show "Default" — fix by giving ConfigLeversFlat its own local selections state

## Root cause

Display was driven by selections PROP passed through effectiveSelections chain. Prop updates require parent re-render + prop drill, introducing a render cycle where displayLabel could read stale/empty data. Fixed by giving ConfigLeversFlat its own localSel useState seeded from props on mount; updateLever calls setLocalSel immediately so display is in sync. Still propagates up via onSelectionsChange.

## Files changed

- `frontend/src/components/workflow/AgentsPopup.tsx`
