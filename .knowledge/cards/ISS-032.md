---
id: ISS-032
type: issue
status: done
area: [sse, agents, auth, product]
summary: >-
  Run cost not yet cache-discounted — the runner did not surface cache_read_tokens /
  cache_write_tokens from LangChain usage_metadata.input_token_details, so both cost
  sites passed 0 for the cache args
source: .planning/ISSUES-REGISTER.md#iss-032
severity: minor
status_detail: "RESOLVED (quick-260704-ttk)"
relates: [FIX-035, FIX-034, FIX-036, ISS-031]
---

# ISS-032

## Title

Run cost not yet cache-discounted — the runner did not surface cache_read_tokens / cache_write_tokens from LangChain usage_metadata.input_token_details, so both cost sites passed 0 for the cache args. estimate_cost_usd was already cache-ready (cache_read / cache_write_5m / cache_write_1h tiers, FIX-035), but with FIX-034's Bedrock prompt caching ON in prod a large share of billed input is cache-read (≈0.1x) and was priced at full input rate → cost OVER-reported for cached runs. Resolution (FIX-036): the shared runner surfaces input_token_details (cache_read/cache_creation) into the usage event + text-only TokenUsage → the engine accumulates per-agent → results + agent_complete + run totals on pipeline_complete → BOTH cost sites price the UNCACHED split (input_tokens=max(0, total − cache_read − cache_write) + cache_read/write tiers + cache_ttl) via the ONE shared estimate_cost_usd; golden-neutral via additive _VOLATILE_STRIP_KEYS (no regen).

## Source / evidence

Resolved by FIX-036 (quick-260704-ttk); companion to FIX-034/ISS-031 (Bedrock cachePoints ON). Offline proof lands now (test_iss032_cache_tokens green; 5 goldens byte/event-identical; lint 4/0). Live cache_read>0 confirmation DEFERRED to the end-of-milestone Bedrock pass.
