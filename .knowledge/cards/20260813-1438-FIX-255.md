---
id: FIX-255
type: fix
kind: event
title: A run with no brief could be launched, and the pipeline it could launch is
  the 5-21M-token one. LaunchCommand.message was a bare str and launch_run never che...
status: done
applies_to:
  phases: []
  modules:
  - LaunchWizard
  globs: []
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'LaunchCommand.message was a bare, unchecked str — a chain of 3 individually-defensible gaps let an empty brief launch the 5-21M-token pipeline; RED test proved a 200 OK on empty input.'
last_updated: '2026-08-13'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-217](20260811-2101-FIX-217.md)

**Referenced by:** [ISS-155](20260813-1300-ISS-155.md)

<!-- /RELATED -->

# FIX-255

**Phase / trigger:** brief.trim() — waiving the brief while chaining on the assumption a context block would stand in, without ever checking the block exists. (2) handleLaunch falls through to brief.trim(), the empty string, when that block is absent — and dev's FIX-217 made "absent" MORE reachable by clearing chain.context_block up front while a failed getChainContext stays non-fatal. (3) dev's FIX-216c removed the last net, the !draft.brief` staging guard, citing a wizard guard that does not cover the chained case. Nothing server-side ever looked.

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

A run with no brief could be launched, and the pipeline it could launch is the 5-21M-token one. LaunchCommand.message was a bare str and launch_run never checked it. Proven by a RED test on 785e40c1: an empty brief returned 200 OK and minted a run.

## Root cause

Not one bug but a chain of three, each individually defensible. (1) LaunchWizard.canContinue validated a PROXY — `isChaining \
