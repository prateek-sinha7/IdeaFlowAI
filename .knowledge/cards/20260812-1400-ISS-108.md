---
id: ISS-108
type: issue
kind: event
title: '`clarifications` accumulates with no identity and no per-run reset outside
  `startPipeline`. `useWorkflow.ts:168` does `clarifications: [...(prev.clarificatio...'
status: resolved
severity: trivial
applies_to:
  phases: []
  modules:
  - useWorkflow
  - useWorkflow.accumulators.test
  globs:
  - useWorkflow.ts
  - useWorkflow.accumulators.test.ts
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'retainClarifyRound pushes clarification rounds with no identity key and no reset outside startPipeline, risking carryover across runs.'
last_updated: '2026-09-02'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-235](20260812-1400-FIX-235.md), [ISS-082](20260812-0351-ISS-082.md)

**Referenced by:** [FIX-475](20260902-1200-FIX-475.md)

<!-- /RELATED -->

# ISS-108

## Title

**`clarifications` accumulates with no identity and no per-run reset outside `startPipeline`.** `useWorkflow.ts:168` does `clarifications: [...(prev.clarifications ?? []), round]` inside `retainClarifyRound`, a direct UI call made when the questionnaire panel unmounts. Because it carries no frame, ISS-082's frame-identity gate **cannot** reach it — it is registered in the accumulator registry as `driver: "ui"` precisely so GUARD-1 still sees it, but its multiplicity cases are not driven. The per-run boundary is `startPipeline`'s explicit `clarifications: []`; `pipeline_start` spreads `...prev` and never clears it, so a path that reaches a new run without `startPipeline` (a history reopen, a chained run) would carry the prior run's rounds. Not observed — flagged, not claimed.

## Severity / Tag / Status (original row)

- Sev: trivial
- Tag: frontend
- Status: **OPEN** (found during quick-260812-iqh / FIX-235)

## Source / Evidence

Source `useWorkflow.ts:69` (the reset), `:165-170` (the accumulate), `:330-370` (`pipeline_start` spreads prev); registry row in `useWorkflow.accumulators.test.ts`.
