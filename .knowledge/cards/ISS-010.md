---
id: ISS-010
type: issue
status: done
area: [agents, product]
summary: >-
  OTLP span export verified deterministically OFFLINE: a new test_hooks.py otel test
  injects an InMemorySpanExporter via the hook's OWN _build_span_processor factory (+
  reset of the module-private _TRACER) and proves the real
source: .planning/ISSUES-REGISTER.md#iss-010
severity: trivial
status_detail: "FIXED (17-02, by-test)"
duplicate_rows: 2
---

# ISS-010

## Title

OTLP span export verified deterministically OFFLINE: a new test_hooks.py otel test injects an InMemorySpanExporter via the hook's OWN _build_span_processor factory (+ reset of the module-private _TRACER) and proves the real OtelTracingHook.handle exports exactly 1 span with flowin.hook/flowin.event/flowin.agent_id attrs under scope flowin.agents.hooks.otel_tracing; a parallel test pins the OTLP-pkg-absent → console-processor degrade. Live OTLP-collector export remains an OPTIONAL end-of-milestone smoke, not a blocker.

## Source / evidence

REPORT-2026-06-12 §Phase-8 sweep; closed by 17-02 in-memory-exporter test

## Duplicate register rows (2)

1. ISS-010 | trivial | product | FIXED (17-02, by-test) | OTLP span export verified deterministically OFFLINE: a new test_hooks.py otel test injects an InMemorySpanExporter via the hook's OWN _build_span_processor factory (+ reset of the module-private _TRACER) and proves the real OtelTracingHook.handle exports exactly 1 span with flowin.hook/flowin.event/flowin.agent_id attrs under scope flowin.agents.hooks.otel_tracing; a parallel test pins the OTLP-pkg-absent → console-processor degrade. Live OTLP-collector export remains an OPTIONAL end-of-milestone smoke, not a blocker. | REPORT-2026-06-12 §Phase-8 sweep; closed by 17-02 in-memory-exporter test

2. ISS-010 | ✓ (verification gap, not a code gap) | "declaration-driven hooks → legacy pipelines emit no spans" is true & proven (all 18 manifests declare no hooks:). Given a collector + a hook-declaring step, spans do export — proven by running the real OtelTracingHook.handle with an injected InMemorySpanExporter (1 span, correct attrs) | add a deterministic offline InMemorySpanExporter test (monkeypatch otel._build_span_processor + reset the module-private _TRACER) → close DEFERRED | stand up a live OTLP collector / a global set_tracer_provider test (misses the hook's own _TRACER → false green) | S / low
