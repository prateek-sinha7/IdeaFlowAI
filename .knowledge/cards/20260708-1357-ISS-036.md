---
id: ISS-036
type: issue
kind: event
title: 'Latent (SSE-ON only, inert today: flag OFF + `RunConnectionProvider` unmounted):
  `useRunChat.runId` bound to clarify-only `activePipelineRunId` (null while "...'
status: done
severity: minor
applies_to:
  phases: []
  modules:
  - RunConnectionProvider
  - api
  - runtime
  - useRunChat
  globs:
  - dashboard/page.tsx
  - 32-VERIFICATION.md
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: '`useRunChat.runId` was bound to clarify-only `activePipelineRunId`, which would spawn a new run per message once SSE activates; fixed by threading `pipelineState.pipelineRunId`.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [ISS-035](20260708-1357-ISS-035.md)

<!-- /RELATED -->

# ISS-036

## Title

Latent (SSE-ON only, inert today: flag OFF + `RunConnectionProvider` unmounted): `useRunChat.runId` bound to clarify-only `activePipelineRunId` (null while "building") → with `NEXT_PUBLIC_SSE_TRANSPORT` ON, `sendCommand` would `POST /api/runs` and spawn a NEW run per in-run message instead of targeting the live run. Fix: thread `pipelineState.pipelineRunId`. **CLOSED in Phase 32 plan 32-05** (register row stayed stale — same tracking gap as ISS-035): `useRunChat({ runId: pipelineState.pipelineRunId ?? activePipelineRunId, … })` at the `dashboard/page.tsx` call site so the REST command path targets the live building run; pure additive FE prop change — LOCK-B held (SSE stays dormant, `RunConnectionProvider` NOT mounted, no env/transport file touched), so the latent bug is closed in code with zero runtime effect until SSE is activated. VERIFIED in `32-VERIFICATION.md` (LOCK-B, grep-confirmed).

## Severity / Tag / Status (original row)

- Sev: minor
- Tag: product
- Status: **RESOLVED** (Phase 32 / 32-05; reconciled 42-11)

## Source / Evidence

31-REVIEW.md HI-01 (2026-07-08); closed 32-05 (`03997db3`), `32-VERIFICATION.md`; reconciled 42-11
