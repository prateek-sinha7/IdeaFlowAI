---
id: ADR-0002
type: adr
kind: state
title: 'That `workflow` is the only vocabulary the system knows and `pipeline` is removed rather than deprecated'
status: deferred
applies_to:
  phases: []
  modules: []
  globs:
  - backend/agents/loader.py
  - backend/agents/registry.py
  - backend/agents/execution_engine/engine.py
  - backend/CLAUDE.md
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: pending
  test_files:
compact_summary: 'That `workflow` is the only vocabulary the system knows and `pipeline` is removed rather than deprecated, to achieve one name per concept enforced by a grep gate'
last_updated: '2026-08-11'
author: 'unknown'
author_source: 'imported-from-decision-card'
---

# ADR-0002

## Decision

In the context of two live vocabularies for one concept — `workflow` in the manifests
and `pipeline` in the engine and registry — facing a "temporary" migration alias that
outgrew the term it was aliasing, we decided that `workflow` is the only vocabulary the
system knows and `pipeline` is removed rather than deprecated, to achieve one name per
concept enforced by a grep gate, accepting a breaking wire-protocol rename and a
coordinated frontend/backend release.

**Area:** engine, registry, api, frontend, naming

## Files this governs

- [backend/agents/loader.py](../../backend/agents/loader.py)
- [backend/agents/registry.py](../../backend/agents/registry.py)
- [backend/agents/execution_engine/engine.py](../../backend/agents/execution_engine/engine.py)
- [backend/CLAUDE.md](../../backend/CLAUDE.md)

## Derived from

- `specs/003-workflow-engine-decoupling/plan.md#q1`
