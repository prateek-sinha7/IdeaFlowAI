---
id: ISS-012
type: issue
status: done
area: [sse, docs]
summary: >-
  113 REQ-IDs present in REQUIREMENTS.md body but missing from its Traceability table
  (project-wide doc drift) — surfaced by /gsd-progress, noted in PROJECT.md Phase-14
  evolution
source: .planning/ISSUES-REGISTER.md#iss-012
severity: cosmetic
status_detail: "FIXED (2026-06-14)"
relates: [ISS-014]
duplicate_rows: 2
---

# ISS-012

## Title

113 REQ-IDs present in REQUIREMENTS.md body but missing from its Traceability table (project-wide doc drift)

## Source / evidence

surfaced by /gsd-progress, noted in PROJECT.md Phase-14 evolution. Root cause: the table used grouped/comma rows (SAFE-01, SAFE-02, …), so the coverage check (first table cell must be EXACTLY one REQ-ID) recognized only 9 of 122 → 113 flagged. Fix: rewrote .planning/REQUIREMENTS.md Traceability table to one row per REQ-ID (117 v1 + 6 v2 = 123 rows), each mapped to its owning phase + that phase's NN-VERIFICATION.md status: passed verdict; stale Pending/In-progress/Partial statuses refreshed to verified-Complete; CTX-04 kept as VOIDED/DEFERRED; API-06 keeps the ISS-014 deletion note; v2 IDs listed explicitly as deferred. Verify: body↔table diff = 0 missing / 0 stray under both the strict single-cell parse and the simple [A-Z]+-[0-9]+ parse (was 113). Requirement *definitions* untouched (body byte-identical to HEAD).

## Duplicate register rows (2)

1. ISS-012 | cosmetic | docs | FIXED (2026-06-14) | 113 REQ-IDs present in REQUIREMENTS.md body but missing from its Traceability table (project-wide doc drift) | surfaced by /gsd-progress, noted in PROJECT.md Phase-14 evolution. Root cause: the table used grouped/comma rows (SAFE-01, SAFE-02, …), so the coverage check (first table cell must be EXACTLY one REQ-ID) recognized only 9 of 122 → 113 flagged. Fix: rewrote .planning/REQUIREMENTS.md Traceability table to one row per REQ-ID (117 v1 + 6 v2 = 123 rows), each mapped to its owning phase + that phase's NN-VERIFICATION.md status: passed verdict; stale Pending/In-progress/Partial statuses refreshed to verified-Complete; CTX-04 kept as VOIDED/DEFERRED; API-06 keeps the ISS-014 deletion note; v2 IDs listed explicitly as deferred. Verify: body↔table diff = 0 missing / 0 stray under both the strict single-cell parse and the simple [A-Z]+-[0-9]+ parse (was 113). Requirement *definitions* untouched (body byte-identical to HEAD).

2. ISS-012 | ✓ (docs, direct-fill) | REQUIREMENTS.md Traceability table not regenerated as 113 REQ-IDs were appended to the body | regenerate the table from the REQ-ID definitions; ideally a generator + a CI drift-check | hand-patch a few rows (re-drifts on the next REQ) | S / low
