---
id: FIX-076
type: fix
date: 2026-07-21
status: done
area: [frontend]
files:
  - frontend/src/components/workflow/AgentsPopup.tsx
summary: >-
  Definitively fix Config lever selections — replace broken custom dropdown with
  native <select>
source: .planning/FIX-REGISTER.md#fix-076
invariants: [INV-1, INV-3, INV-12, SC-001]
---

# FIX-076

**Phase / trigger:** Phase 37/41 (B3/B7)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Definitively fix Config lever selections — replace broken custom dropdown with native <select>

## Root cause

All custom dropdown attempts failed due to browser event sequencing (mousedown dismiss fires before click). Replaced ConfigLeverSelect entirely with native <select> + appearance-none + ChevronDown overlay. Native select onChange always fires reliably. Deleted the ConfigLeverSelect component and the outside-click useEffect entirely.

## Files changed

- `frontend/src/components/workflow/AgentsPopup.tsx`
