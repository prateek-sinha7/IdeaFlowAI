---
id: FIX-161
type: fix
date: 2026-07-31
status: done
area: [agents]
summary: >-
  Remove no-op logs:DescribeLogGroups from instance-role CloudWatch IAM policy —
  logs:DescribeLogGroups has no AWS IAM resource type (AWS Service Reference: no
  "Resources" entry)
source: .planning/FIX-REGISTER.md#fix-161
ticket: KAN-149
invariants: [INV-1, INV-3, INV-12, SC-001]
---

# FIX-161

**Phase / trigger:** quick-260731-fx3 (infrastructure, least-privilege hardening)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Remove no-op logs:DescribeLogGroups from instance-role CloudWatch IAM policy

## Root cause

logs:DescribeLogGroups has no AWS IAM resource type (AWS Service Reference: no "Resources" entry), so scoping it to arn:...:log-group:/velocityai/<env>/* was a silent no-op. Agent never calls it in this config (gate: target.Retention > 0 at pusher/target.go:79; default -1; no config sets it). Deleted statement + documented why via HCL comment so it's not re-added. Widening to Resource:"*" was rejected: would grant unnecessary account-wide cross-env log-group enumeration to internet-facing host for a call path that doesn't execute.
