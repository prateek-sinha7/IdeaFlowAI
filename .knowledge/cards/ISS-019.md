---
id: ISS-019
type: issue
status: open
area: [workflow, agents]
summary: >-
  clamp the agent panel / shrink the wave panel to a fixed height — clips content,
  viewport-fragile (the 122px-vs-13px drift) / reorder wave above agent (pushes agent
  below fold)
source: .planning/ISSUES-REGISTER.md#iss-019
severity: ✓
status_detail: "flex-budget split (CSS-only, DashboardLayout.tsx): column flex flex-col overflow-hidden; agent wrapper flex-1 min-h-0 overflow-hidden; wave wrapper flex-shrink-0 max-h-[40%] overfl"
duplicate_rows: 2
---

# ISS-019

## Title

clamp the agent panel / shrink the wave panel to a fixed height — clips content, viewport-fragile (the 122px-vs-13px drift) / reorder wave above agent (pushes agent below fold)

## Source / evidence

S / low

## Duplicate register rows (2)

1. ISS-019 | cosmetic | product | FIXED (18-02; live fold proof in Playwright pass) | WaveTreePanel renders correctly (mounts, "WAVE / SUBAGENT TREE" heading + "No waves running." empty state + populated Wave 0/Wave 1 groups during active waves — backend wave data perfect) but sits below the left execution-column fold at 1440×950 (heading at y=963, ~13px below the fold; reachable by scrolling the column). A headline Phase-12 capability is invisible at default viewport without scrolling. Matches 12-UAT test-5 cosmetic note; persists. Worth a follow-up: mount it higher or collapse the agent panel. | UI campaign S09 wave-tree-scrolled.png + DOM probe 2026-06-13

2. ISS-019 | ✓ | DashboardLayout.tsx:1158-1190: AgentProgressPanel (flex h-full, designed to fill 100% + scroll internally) is the first of two block siblings in an overflow-y-auto column, so it eats the full height and WaveTreePanel starts at/after the fold; the column (not the panel) owns the only scroll. Test stubs the agent panel → never caught | flex-budget split (CSS-only, DashboardLayout.tsx): column flex flex-col overflow-hidden; agent wrapper flex-1 min-h-0 overflow-hidden; wave wrapper flex-shrink-0 max-h-[40%] overflow-y-auto | clamp the agent panel / shrink the wave panel to a fixed height — clips content, viewport-fragile (the 122px-vs-13px drift) / reorder wave above agent (pushes agent below fold) | S / low
