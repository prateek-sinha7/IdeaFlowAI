---
id: FIX-222
type: fix
kind: event
title: No UI surfaced PER-ARTIFACT versions inside a run. artifact_refs durably stores
  every version and GET /api/runs/{id}/artifacts?kind=X&include=content already...
status: done
applies_to:
  phases: []
  modules:
  - AgentDetailPanel
  - AgentDetailPanel.artifactVersions.test
  - AgentThinkingTab
  - ArtifactVersionPicker
  - ArtifactVersionPicker.test
  - WorkflowHistory
  - api
  - useWorkflow
  globs:
  - frontend/src/components/results/ArtifactVersionPicker.tsx
  - frontend/src/components/results/AgentDetailPanel.tsx
  - frontend/src/components/results/AgentThinkingTab.tsx
  - frontend/src/components/results/ArtifactVersionPicker.test.tsx
  - frontend/src/components/results/AgentDetailPanel.artifactVersions.test.tsx
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: test
  status: passed
  test_files:
  - frontend/src/components/results/AgentDetailPanel.artifactVersions.test.tsx
  - frontend/src/components/results/ArtifactVersionPicker.test.tsx
compact_summary: 'Added an artifact version picker onto the already-existing versioned artifacts endpoint, since useWorkflow reset agent.output on every agent_start and destroyed prior copies.'
last_updated: '2026-08-12'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-039](20260707-0125-FIX-039.md), [FIX-217](20260811-2101-FIX-217.md), [FIX-221](20260812-0158-FIX-221.md), [ISS-063](20260811-2303-ISS-063.md), [ISS-065](20260812-0005-ISS-065.md)

**Referenced by:** [FIX-221](20260812-0158-FIX-221.md), [FIX-225](20260812-0345-FIX-225.md), [ISS-063](20260811-2303-ISS-063.md), [ISS-065](20260812-0005-ISS-065.md), [ISS-075](20260812-0212-ISS-075.md), [ISS-077](20260812-0212-ISS-077.md), [ISS-085](20260812-0505-ISS-085.md)

<!-- /RELATED -->

# FIX-222

**Phase / trigger:** quick-260812-2ci (closes ISS-065; shipped together with FIX-221/ISS-063 as ONE change)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

No UI surfaced PER-ARTIFACT versions inside a run. artifact_refs durably stores every version and GET /api/runs/{id}/artifacts?kind=X&include=content already returns each one WITH its body — verified live on :8010 (run d5dbc9f2: spec v1/v2/v3 at 30081/36729/40052 chars, one call, HTTP 200) — but the only FE caller was WorkflowHistory.tsx:303, pinned to kind="clarifications". Worse, the evidence is actively destroyed in FE state: useWorkflow.ts resets an agent's output to "" on every agent_start (FIX-039), so the moment the spec agent re-runs during an update_specs cycle the only in-memory copy of v1 is gone and the panel shows v3 with no label and no way back. This invisibility is why FIX-217 shipped undetected.

## Root cause

A gap between two axes that were never distinguished on screen: the only version control users had is the run-header chip, which walks the run FAMILY (parent_run_id) per locked decision D2. Three in-run revision cycles move that chip not at all, so the product taught users that "Version" means something it does not, and the axis that DID change (artifact_refs.version, per (run_id, kind)) had no control at all. Data, API and read-scope all already existed; only the consumer was missing.

## Files changed

- `frontend/src/components/results/ArtifactVersionPicker.tsx`
- `frontend/src/components/results/AgentDetailPanel.tsx`
- `frontend/src/components/results/AgentThinkingTab.tsx`
- `frontend/src/components/results/ArtifactVersionPicker.test.tsx`
- `frontend/src/components/results/AgentDetailPanel.artifactVersions.test.tsx`
