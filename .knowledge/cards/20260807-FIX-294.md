---
id: FIX-294
type: fix
kind: event
title: Auth-critical timestamps were naive, so a token-revocation comparison could silently
  compare across timezones and fail to revoke
status: done
applies_to:
  phases: []
  modules: [MOD-backend-app-models, MOD-backend-app-core]
  globs:
    - backend/alembic/versions/0035_timezone_aware_auth_timestamps.py
    - backend/app/models/user.py
  requirements: []
locked_constraints:
  - tokens_valid_from, password_changed_at, mfa_verified_at and roles_synced_at are
    timezone-aware. A naive datetime must never be compared against them.
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'Auth-critical timestamp columns were naive; migration 0035 makes them timezone-aware so token-revocation comparisons cannot silently mis-order across zones'
last_updated: 2026-08-24
author: 'dev branch (recorded at merge)'
---

Token revocation compares a token's issued-at against `tokens_valid_from`. With naive
columns that comparison depended on the process timezone, so a revocation could fail to
take effect. Migration 0035 converts the auth-critical columns to timezone-aware.
