---
id: ISS-137
type: issue
kind: event
title: '`pipeline_reconnected` is a DEAD frame with a live handler, a live test suite
  and a divergent status list. Verified: NO backend path emits it — `grep` over `...'
status: open
severity: trivial
applies_to:
  phases: []
  modules:
  - agents
  - app
  - run_stream
  - terminalMarkers
  - useWorkflow
  - useWorkflow.reconnect.test
  globs:
  - run_stream.py
  - useWorkflow.reconnect.test.ts
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: optional
  test_files: []
compact_summary: 'pipeline_reconnected is a dead frame — no backend path emits it since stream_attached replaced it — yet a live reducer arm and 6-test suite still guard it.'
last_updated: '2026-08-14'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-245](20260813-0005-FIX-245.md), [ISS-136](20260813-0005-ISS-136.md)

**Referenced by:** [ISS-136](20260813-0005-ISS-136.md), [ISS-142](20260813-0005-ISS-142.md)

<!-- /RELATED -->

# ISS-137

## Title

**`pipeline_reconnected` is a DEAD frame with a live handler, a live test suite and a divergent status list.** Verified: NO backend path emits it — `grep` over `backend/app` + `backend/agents` finds it only in comments; the WS->SSE cutover replaced it with `stream_attached` (`run_stream.py:215-227`, documented there as "the new-transport replacement for the legacy `pipeline_reconnected` ack"). The FE still carries the full `case "pipeline_reconnected"` reducer arm plus `useWorkflow.reconnect.test.ts` (6 green tests) pinning behaviour nothing can trigger. Left ALONE by FIX-245 on purpose: routing it through the new shared `terminalMarkers` would silently change an untested `status:"error"` path and add `degraded` handling, for zero user benefit, while breaking a green suite.

## Severity / Tag / Status (original row)

- Sev: trivial
- Tag: frontend
- Status: **OPEN** (filed 2026-08-12 during quick-260812-wir)

## Source / Evidence

Discovered while evaluating whether to consolidate the terminal vocabularies (ISS-136) during quick-260812-wir.
