---
id: ISS-135
type: issue
kind: event
title: FIX-240 residual — `payload_json.seq` diverges from `run_events.seq` on a re-append.
  `_RunEventSink.persist` re-stamps `data["seq"]` AFTER `append_event_at_o...
status: open
severity: minor
applies_to:
  phases: []
  modules:
  - RunEvent
  - app
  globs: []
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: optional
  test_files: []
compact_summary: 'payload_json.seq gets re-stamped after append_event_at_or_after returns, so the payload''s seq can lag the row''s real seq by one; harmless since dedup keys on event_id, not seq.'
last_updated: '2026-08-14'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-240](20260812-1852-FIX-240.md), [FIX-244](20260813-0005-FIX-244.md), [ISS-123](20260812-1852-ISS-123.md)

<!-- /RELATED -->

# ISS-135

## Title

**FIX-240 residual — `payload_json.seq` diverges from `run_events.seq` on a re-append.** `_RunEventSink.persist` re-stamps `data["seq"]` AFTER `append_event_at_or_after` returns, but the row was already serialised with the ORIGINAL value. Observed on real data: run `1ea6d262` row **248** carries `"seq": 247` in its payload. Replay yields `id: row.seq` (248) with `data.seq` = 247, while the live frame carried 248. **Harmless TODAY** because every de-dup keys on `event_id`, not `data.seq` — which is why it is filed rather than folded. It is a latent trap for anything that ever keys on `data.seq`. Fix shape: re-stamp before persist, or persist the re-stamped dict. **NOT a reason to touch FIX-240 otherwise.**

## Severity / Tag / Status (original row)

- Sev: minor
- Tag: engine
- Status: **OPEN** (filed 2026-08-12 during quick-260812-wir)

## Source / Evidence

Found by the ISS-123/126 investigation and re-confirmed during quick-260812-wir. Deliberately not folded into FIX-244/245: it is an engine-sink change with its own proof obligation, and both of those were app-layer/FE.
