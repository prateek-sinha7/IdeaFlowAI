---
id: FIX-158
type: fix
date: 2026-07-30
status: done
area: [agents, auth]
summary: >-
  CloudWatch agent duplicate log stream configuration — audit.log and
  unattended-upgrades.log collide on /velocityai/*/system:{instance_id}
source: .planning/FIX-REGISTER.md#fix-158
ticket: KAN-142
invariants: [INV-1, INV-3, INV-12, SC-001]
---

# FIX-158

**Phase / trigger:** quick-260730-k8x (infrastructure, no schema/migration, infra-only)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

CloudWatch agent duplicate log stream configuration — audit.log and unattended-upgrades.log collide on /velocityai/*/system:{instance_id}

## Root cause

Two entries in the collect_list both targeted identical (logGroupName, logStreamName) pair (/velocityai/${ENVIRONMENT}/system, {instance_id}), violating CloudWatch's unique stream constraint per CreateLogStream API. Root: config materialized verbatim from documentation in 2026-07-01 prefix-rename pass (commit e1a3a495); uniqueness never validated. Collision latent due to B1 (agent crash-loop on missing logfile perms, FIX-149). Fix: apply uniform stream-naming scheme across all 8 entries — every entry now uses {instance_id}/<source-slug> for unique, self-documenting stream names (nginx-access, nginx-error, postgres, audit, auth, unattended-upgrades, letsencrypt, docker). Added durability comment documenting the naming rule and INV-12 contract.
