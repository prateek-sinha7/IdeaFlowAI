---
id: FIX-042
type: fix
kind: event
title: Reject & cancel pipeline leaves user stranded on execution view — onRejectReview
  had no navigation
status: done
applies_to:
  phases:
  - 08
  modules:
  - DashboardLayout
  - Home
  - app
  globs:
  - frontend/src/components/layout/DashboardLayout.tsx
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'onRejectReview only sent the WS message and cleared gate data with no navigation, stranding the user on the execution view; added a wrapper that resets state and navigates home.'
last_updated: '2026-07-06'
author: 'Prateek Sinha <2000152842@hexaware.com>'
author_source: applies-to-glob
---

<!-- RELATED -->

## Related

**Referenced by:** [FIX-042b](20260708-FIX-042b.md)

<!-- /RELATED -->

# FIX-042

> **Reused id.** The register uses `FIX-042` for more than one unrelated
> fix. This card is one of them; the suffix exists only here, so that one card
> means one fix. The register itself is unchanged — see `source:`.

**Phase / trigger:** Phase 8/23 (GATE/REDO-GATE)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Reject & cancel pipeline leaves user stranded on execution view — onRejectReview had no navigation

## Root cause

onRejectReview in page.tsx only sent the WS message and cleared reviewGateData; no setMainView("home") or onResetPipeline() call. cancelNavigatingHomeRef was never set so pipelineState.isRunning effect would snap back to execution. Added handleRejectReview wrapper in DashboardLayout (mirrors handleCancelWorkflow pattern from KAN-90): sets cancelNavigatingHomeRef, calls setMainView("home"), calls onResetPipeline(), then delegates to onRejectReview for WS send + reviewGateData clear.

## Files changed

- `frontend/src/components/layout/DashboardLayout.tsx`
