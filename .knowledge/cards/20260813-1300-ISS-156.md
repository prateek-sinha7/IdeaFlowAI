---
id: ISS-156
type: issue
kind: event
title: '`dev` shipped FIX-218 with five red tests, which the merge inherits. `backend/tests/unit/test_run_commands_fix218.py`
  asserts the Concierge prompt contains `...'
status: open
severity: major
applies_to:
  phases: []
  modules:
  - ChatAttachments
  - ChatAttachments.test
  - app
  - chat
  - useChatAttachments
  - useChatAttachments.test
  globs:
  - backend/tests/unit/test_run_commands_fix218.py
  - frontend/src/hooks/useChatAttachments.test.ts
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: optional
  test_files: []
compact_summary: 'dev merged FIX-218 with 5 red tests: 2 assert a prompt string dev''s code no longer emits, 2 more in useChatAttachments; 4 remain dev''s to fix, 1 was reconciled in this merge.'
last_updated: '2026-08-14'
author: 'Bilal Arshad <bilala@hexaware.com>'
author_source: applies-to-glob
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-218](20260811-2246-FIX-218.md)

<!-- /RELATED -->

# ISS-156

## Title

**`dev` shipped FIX-218 with five red tests, which the merge inherits.** `backend/tests/unit/test_run_commands_fix218.py` asserts the Concierge prompt contains `## Attached files` while dev's own code emits `## PRIORITY OVERRIDE — File attached this turn` (2 failures); `frontend/src/hooks/useChatAttachments.test.ts` has 2 more. A third, `test_chat_messages_endpoint.py::TestPersist::test_attachments_are_not_retained`, was green on this branch and red after the merge — **that one is RECONCILED** in this merge commit (the exact-dict assertion was over-strict; it now pins the ND-10 invariant that the bytes never persist). The remaining four are dev's to fix.

## Severity / Tag / Status (original row)

- Sev: major
- Tag: test-infra
- Status: **OPEN** (filed 2026-08-13 during the `dev` merge audit (merge of `origin/dev` `04fa2a5b` into `bugfix/spec-revision-context-loss`))

## Source / Evidence

All five reproduced on a PURE `origin/dev` worktree with no merge applied, which is what attributes them to dev rather than to the merge.

## 4-test-writer note (2026-08-31)

Re-ran all three files this card names on the current branch (`feat/conditional-gates`):
`backend/tests/unit/test_run_commands_fix218.py` — 24/24 passed;
`frontend/src/hooks/useChatAttachments.test.ts` — 6/6 passed;
`frontend/src/components/chat/ChatAttachments.test.tsx` — 3/3 passed.
The backend assertions (`test_fix218_attached_files_injected`,
`test_fix218_extraction_error_in_prompt`) already assert the string
`PRIORITY OVERRIDE — File attached this turn` — the same string the card says dev's
code emits — so the "asserts `## Attached files`, code emits `PRIORITY OVERRIDE`"
mismatch this card describes is not present on this branch today. No red to pin a
regression test to. Not reproducible; could not write a test per §4 ("do not ship a
test you never saw fail").
