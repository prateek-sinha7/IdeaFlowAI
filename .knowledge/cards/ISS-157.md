---
id: ISS-157
type: issue
status: done
area: [frontend]
files:
  - frontend/src/hooks/useRunStateStore.ts
summary: >-
  syncPipelineStateOnly is added to the run store, exported on the interface, and
  never called — dev's FIX-220 register row says "for future use". The rAF fix it
  ships alongside still calls runStore.updatePipelineState
source: .planning/ISSUES-REGISTER.md#iss-157
severity: minor
status_detail: "FIXED — FIX-256 (2026-08-13, commit d0dc1f49). syncPipelineStateOnly DELETED — never called, a second mechanism for what the rAF coalescing already does (INV-12), and its stated pr"
relates: [FIX-220]
---

# ISS-157

## Title

syncPipelineStateOnly is added to the run store, exported on the interface, and never called — dev's FIX-220 register row says "for future use". The rAF fix it ships alongside still calls runStore.updatePipelineState. A dead abstraction is exactly what INV-12 forbids.

## Source / evidence

frontend/src/hooks/useRunStateStore.ts:225 (interface), :463 (impl), :516 (export). A repo-wide grep finds no call site.
