---
id: FIX-165
type: fix
date: 2026-08-03
status: done
area: [frontend]
files:
  - frontend/src/app/dashboard/page.tsx
summary: >-
  Analyze gate approve requires 2 clicks — first click disables button but does
  nothing visible
source: .planning/FIX-REGISTER.md#fix-165
invariants: [INV-1, INV-3, INV-12, SC-001]
---

# FIX-165

**Phase / trigger:** Phase 8 (GATE-01/02), Phase 42 (inline gate)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Analyze gate approve requires 2 clicks — first click disables button but does nothing visible

## Root cause

onApproveReview in page.tsx never called setReviewGateData(null) after the POST, so InlineGateActions stayed mounted with unchanged props, useEffect([output,gateKey]) never fired, submitted stayed true, button remained disabled. All other gate handlers (reject/redo/update_specs) had the clear — approve was missing it

## Files changed

- `frontend/src/app/dashboard/page.tsx`
