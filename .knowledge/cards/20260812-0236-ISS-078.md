---
id: ISS-078
type: issue
kind: event
title: 7 reds in `backend/tests/agents/test_restart_resume.py` (7 failed / 48 passed),
  and at least two of them may be describing a LIVE PRODUCTION RESUME BUG rathe...
status: done
severity: major
applies_to:
  phases: []
  modules:
  - ArtifactRef
  - Base
  - RunEvent
  - agents
  - app
  - chat
  - chat_narrator
  globs:
  - backend/tests/agents/test_restart_resume.py
  - engine.py
  - test_restart_resume.py
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'Carved out of ISS-068; six of the seven reds proved to be stale fixtures and one a genuine defect (a rejected-then-resumed run recorded completed).'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-121](20260727-1259-FIX-121.md), [FIX-175](20260805-1853-FIX-175.md), [FIX-223](20260812-0226-FIX-223.md), [FIX-227](20260812-0557-FIX-227.md), [FIX-229](20260812-0653-FIX-229.md), [ISS-029](20260703-2055-ISS-029.md), [ISS-068](20260812-0005-ISS-068.md), [ISS-091](20260812-0654-ISS-091.md)

**Referenced by:** [FIX-229](20260812-0653-FIX-229.md), [FIX-232](20260812-1153-FIX-232.md), [ISS-068](20260812-0005-ISS-068.md), [ISS-084](20260812-0505-ISS-084.md), [ISS-091](20260812-0654-ISS-091.md), [ISS-094](20260812-1120-ISS-094.md), [ISS-133](20260812-2148-ISS-133.md)

<!-- /RELATED -->

# ISS-078

## Title

**7 reds in `backend/tests/agents/test_restart_resume.py` (7 failed / 48 passed), and at least two of them may be describing a LIVE PRODUCTION RESUME BUG rather than test rot.** Carved out of ISS-068 (which owned the golden + import halves and is now FIXED); measured identically at `f5f2f7c6` and after FIX-223/224, same 7 ids, twice in one session — **not flaky, and not environment-gated** (55 collected, **0 skipped**, whole file runs offline in ~5 s; no Postgres/Bedrock/Chromium gate fires). All 7 exist on `dev` too. They were green once: commit `3e80fcc4` (2026-07-19) records "restart_resume 39 -> 41/0" with all 7 already present, so these are **regressions since 2026-07-19**, not red-by-design pins. **Failing ids:** `test_midwave_resume_does_not_reinvoke_completed_workers` (:472), `test_open_gate_override_is_noop_without_review_gate` (:1204), `test_partial_task_loop_build_reenters_step_not_skipped` (:1575), `test_completed_task_loop_build_stays_complete_no_rerun` (:1603), `test_resumed_run_is_wired_live_ectx_and_milestone_cards` (:2663), `test_failed_run_resumes_skips_completed_tasks` (:3081), `test_failed_run_with_open_gate_resumes_into_gate` (:3173). **LEAD (high confidence, UNPROVEN):** `c71f3d9c` — a commit titled `fix(frontend)` — is the only commit that has ever touched `_first_incomplete_step`'s skip predicate, and it inverted it from `or` to `and` (`engine.py:7882-7885`: `complete_by_artifact = agent_id in produced_agents and agent_id in agent_complete_events`), strictly tightening what counts as "already complete". Three of the reds (#2/#3/#4) fail with the identical `idx=0` signature — exactly what a stricter predicate produces when a test seeds durable artifact rows but no `agent_complete` rows. **The same commit also added `resume_offset`, which was ISS-068 cause C1 — it is implicated in both halves.** **TWO LIVE HYPOTHESES — do not guess:** *H1 (test stale)* the tightening is correct and the hand-seed idiom now needs `agent_complete` rows; *H2 (code regressed)* the tightening over-corrects and really does re-invoke completed work on resume in production. **H2 has real support:** #1 and #6 are **end-to-end** — they drive the real engine, let it write its own durable rows, and still observe `part_a` re-invoked (`{'a': 4, 'b': 4, 'c': 2, 'd': 2}`). A hand-seeding artefact cannot explain those two. **RUN THIS FIRST:** in a scratch worktree revert *only* the `c71f3d9c` predicate hunk and re-run the 7; if ≥3 go green, H2 is live and this is a production resume regression, not test rot. **One red is PROVEN trivial and independent (#5):** `2d5b9fcc` (FIX-175) updated the unpack at `test_restart_resume.py:2665` to a 4-tuple (`_created, _card_seq, _, _reply_eid`) but left its own stub at `:2642` appending a 3-tuple (`emitted_cards.append((created, card_seq, card))`) → `ValueError: not enough values to unpack (expected 4, got 3)`. One line, zero production risk. **DO NOT delete, xfail or loosen any of the 7** — at least two are end-to-end assertions that completed work is not re-invoked on resume; if they are right they are describing a live bug. Estimated multi-day; plausibly ≥3 distinct causes.

## Severity / Tag / Status (original row)

- Sev: major
- Tag: engine
- Status: **FIXED — FIX-229 / TEST-013 (quick-260812-97f), commits `1c7569db` (RED tests) + `d3184e10` (fix), 2026-08-12. `test_restart_resume.py` 7 failed / 48 passed -> **60 passed / 0 failed**. Offline-proven; deliberately NOT live-proven (one build is 5-21M tokens).** **BOTH register hypotheses were WRONG, and the answer was a third thing.** *H1 (test stale)* and *H2 (code regressed)* both assumed ONE cause; there were **three**, from three commits. **Six of the seven reds were TEST defects; exactly one was a product defect — and it was not the one the row was watching.** **(1) Five reds = fixture drift, NOT a resume regression (#1 #2 #3 #4 #6).** Every one of those fixtures persists **zero** `run_events`, so the `agent_complete` evidence `_first_incomplete_step` requires cannot exist in them by construction. #1/#6 — the two the row called "end-to-end" and treated as H2's strongest support — drove `engine._execute_impl` **directly** (`:431`, `:3049`); the durable `_RunEventSink` is built in the PUBLIC entry (`engine.py:1020`, persisted `:1065`) and `_execute_impl` persists nothing, so instance A wrote artifacts and not one event row. They called themselves end-to-end while skipping the durability layer their own premise depends on. Driving `execute()` makes them pass **with the engine untouched** — which is the disproof of H2. #2/#3/#4 hand-seed `ArtifactRef` only; #3/#4 were failing at index 0 on the PLAN step and never reaching the `task_loop` branch under test. **(2) THE PREDICATE WAS NOT LOOSENED — and that is the load-bearing decision here.** The investigation recommended replacing `c71f3d9c`'s conjunction with an interruption test (`in agent_start_events and not in agent_complete_events`). **Rejected on measured evidence** (read-only against live `dev.db`): its differential vs HEAD is exactly *artifact producer with NO `agent_start` AND NO `agent_complete`* = **27 rows, all `deep-planner`/`clarify-agent`** — and **neither appears in any `workflow.yaml` step list nor in `registry.PIPELINE_AGENTS`** (grep: 0 hits), so neither is ever in `ordered_agents`. Meanwhile the case it was argued from (a lost `agent_complete` write) has **count 0** live **and the proposal is a no-op for it anyway** — `agent_start` present + `agent_complete` absent is `_interrupted=True`, i.e. still re-run, identical to HEAD. **Net: zero production effect, sole observable consequence = five broken fixtures go green.** That is loosening a predicate to satisfy a test. **`engine.py` is byte-identical to `99fcf4a2`.** **(3) #5 confirmed exactly as this row stated** — 3-tuple stub (`:2642`) vs the 4-tuple contract (`chat_narrator.persist_milestone_card:287-289`). It was breaking the resumed **drive** (`engine.py:8509`, swallowed), not just the test. One line. **(4) #7 = the one REAL product defect, and it is a status LIE (FIX-229).** A user who resumes a run parked at a gate and REJECTS had it recorded `completed`. Probe-captured tail: `...5:review_gate_ready / 6:pipeline_cancelled / 7:pipeline_complete`. `resume_supersedes` (`run_commands.py:602`, `bac1e556`/KAN-120) asked *"higher seq?"* and read it as *"later attempt succeeded"* — two different questions; on a rejection both events are ONE attempt. Now requires a `run_resuming`/`pipeline_start` boundary between them. **Coverage gaps closed (ISS-029 discipline):** FIX-121 and `resume_supersedes` **both shipped with no test at all** — `grep resume_supersedes tests/` returned zero. Both are now pinned, each seen RED under the mutation it guards. **Goldens 10/10 and lint-imports 4 kept / 0 broken, both identical to `99fcf4a2`; no golden regenerated.** Protected suites 52 passed and the resume/cancel/shutdown adjacents 44 passed, both identical to baseline. **Filed, not folded in: ISS-091** — gate rejection does not STOP the pipeline (measured at HEAD, i.e. *with* FIX-227: the full fan-out still runs after `pipeline_cancelled`). FIX-229 stops the record lying about it; the wasted work is ISS-091's. **Also observed, unregistered: 3 pre-existing reds in `tests/agents/test_gates.py`** (`test_raising_hitl_gate_blocks_and_stops_gate_evaluation`, `test_engine_sentinel_carries_human_gate_edit_detail`, `test_apply_declared_gate_edit_rewrites_upstream_artifact_and_result`) — present at `99fcf4a2`, unrelated to ISS-078, in no register row I could find. ORIGINAL REPORT:

## Source / Evidence

Carved out of ISS-068 during quick-260812-35u, 2026-08-12. Baseline re-measured before and after FIX-223/224: unchanged at 7 failed / 48 passed with the same 7 ids. Root-cause detail in the ISS-068 investigation report §6.
