---
id: ISS-622
type: issue
kind: event
title: window.confirm() was reintroduced in four places, defeating the in-app confirm dialog the product standardised on
status: open
applies_to:
  phases: []
  modules: [MOD-frontend-src-app]
  globs:
    - "frontend/src/components/settings/AccountSettings.tsx"
    - "frontend/src/components/layout/DashboardLayout.tsx"
    - "frontend/src/components/workflow/composer/ComposerPage.tsx"
locked_constraints: []
verification:
  type: test
  status: failed
  test_files:
    - frontend/src/components/settings/AccountSettings.tsx
    - frontend/src/components/layout/DashboardLayout.tsx
    - frontend/src/components/workflow/composer/ComposerPage.tsx
compact_summary: 'window.confirm is back in AccountSettings, DashboardLayout (x2) and ComposerPage; feat/conditional-gates had zero, d027f16bd added all four'
last_updated: 2026-08-31
author: 'Bilal Arshad <BilalA@hexaware.com>'
---

## Symptom

```
S-19-17 test_download_failures_are_the_only_native_dialogs_in_the_product
AssertionError: native confirm/prompt reintroduced: [
  'components/settings/AccountSettings.tsx: window.confirm(',
  'components/layout/DashboardLayout.tsx: confirm(',
  'components/layout/DashboardLayout.tsx: window.confirm(',
  'components/workflow/composer/ComposerPage.tsx: confirm(']
```

S-19-17 is a source assertion that exists for exactly this — destructive
confirmation is done with the in-app dialog, and there is no runtime surface
that could prove an absence.

## Provenance — this one IS ours

```
                       602296cc8   a35e8fb2a   merged
AccountSettings.tsx        0           1          1
DashboardLayout.tsx        0           2          2
ComposerPage.tsx           0           1          1
```

Added by `d027f16bd` — *"bug-hunter: close 80 bugs from the parallel
validate/analyze/test/fix/verify batch"*. A fixer agent reached for the native
dialog while closing something else.

## What to do

Replace all four with the in-app confirm dialog used elsewhere — find it via
the destructive-delete flow S-19-16 covers. Ranked highest of the open work:
it is a real UX regression introduced by this branch, not a stale test.
