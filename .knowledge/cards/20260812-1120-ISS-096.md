---
id: ISS-096
type: issue
kind: event
title: 'Stale prompt-contract pin: `tests/agents/test_prompt_contracts.py::test_od_ppt_validator_deck_reemission_contract`
  asserts a sentence that is no longer in th...'
status: open
severity: trivial
applies_to:
  phases: []
  modules:
  - agents
  globs:
  - test_prompt_contracts.py
  - backend/agents/prompts/od-ppt-validator/AGENT.md
  - test_live_contract.py
  - test_phase3_token_delta_live.py
  - live_harness.py
  requirements: []
locked_constraints: []
verification:
  type: test
  status: not_reproducible
  test_files: []
compact_summary: 'A prompt-contract test still asserts a sentence no longer present in the od-ppt-validator prompt; unclear whether the prompt or the pin regressed.'
last_updated: '2026-08-14'
author: 'Bilal Arshad <bilala@hexaware.com>'
author_source: applies-to-glob
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-001](20260622-FIX-001.md), [FIX-014](20260617-FIX-014.md), [FIX-109](20260724-1055-FIX-109.md), [FIX-231](20260812-1120-FIX-231.md)

<!-- /RELATED -->

# ISS-096

## Title

**Stale prompt-contract pin: `tests/agents/test_prompt_contracts.py::test_od_ppt_validator_deck_reemission_contract` asserts a sentence that is no longer in the agent's prompt.** `test_prompt_contracts.py:63` asserts `"exactly ONE <artifact>" in body`, where `body` is `load_agent_spec("od-ppt-validator").prompt_body`. That substring is absent from the current `backend/agents/prompts/od-ppt-validator/AGENT.md`. **Proven pre-existing:** reproduced identically at `fab9b646` in a clean detached worktree, so it is not a FIX-231 regression. The rest of that file is green (44 passed / 1 skipped alongside `test_live_contract.py` + `test_phase3_token_delta_live.py`). **Decide which side is stale before editing either** — this file is a deliberate durable pin (Phase 15-02) whose whole job is to fail when a prompt contract silently changes, so the correct fix may be to restore the prompt line rather than relax the pin. The `od-ppt-validator` output contract has been hardened three times (FIX-001, FIX-014, FIX-109) precisely because losing it makes the live deck render as raw QA checklist text, so treat a missing contract sentence as a possible product regression until the AGENT.md history says otherwise.

## Severity / Tag / Status (original row)

- Sev: trivial
- Tag: test-infra
- Status: **OPEN** (pre-existing; found while sweeping `live_harness` consumers)

## Source / Evidence

Observed 2026-08-12 by quick-260812-fbk while running every consumer of the changed `live_harness.py` to bound FIX-231's blast radius; confirmed pre-existing at `fab9b646` via `git worktree add --detach`.

## Re-check (4-test-writer, 2026-08-31)

Ran `backend/tests/agents/test_prompt_contracts.py` (whole file and the single
`test_od_ppt_validator_deck_reemission_contract` case) with
`venv/bin/python3 -m pytest` — **14 passed**, including the named test. The
assertion the card names, `"exactly ONE \`<artifact>\` block" in body`, is
present in the loaded `ppt-validator` prompt today. The card's own
`applies_to.globs` names `backend/agents/prompts/od-ppt-validator/AGENT.md`,
but no `od-ppt-validator` directory exists in this repo — only
`backend/agents/prompts/ppt-validator/`. Whatever regression triggered this
card either never reproduced against the current prompt/pin pairing, or was
already resolved upstream (matching FIX-001/FIX-014/FIX-109 hardening this
card lists as dependencies). No red state to pin a test to — could not write
a failing test for this card as described.
