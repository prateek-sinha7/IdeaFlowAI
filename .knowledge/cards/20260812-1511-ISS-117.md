---
id: ISS-117
type: issue
kind: event
title: Validation frames are dropped on replay, so no run reopened from history can
  ever show a gate verdict. `useRunStateStore.ts:395-403` lists `pipelineFrameType...
status: resolved
severity: minor
applies_to:
  phases: []
  modules:
  - Card
  - ResultCard
  - RunState
  - ValidationResult
  - useRunStateStore
  globs:
  - useRunStateStore.ts
  - AgentDetailPanel.tsx
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: optional
  test_files: []
compact_summary: 'useRunStateStore''s replay allowlist omits validator_result/gate_passed/gate_blocked, so no reopened run can ever show a gate verdict.'
last_updated: '2026-08-14'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-237](20260812-1511-FIX-237.md), [ISS-087](20260812-0533-ISS-087.md), [ISS-116](20260812-1511-ISS-116.md)

**Referenced by:** [FIX-454](20260901-1600-FIX-454.md)

<!-- /RELATED -->

# ISS-117

## Title

**Validation frames are dropped on replay, so no run reopened from history can ever show a gate verdict.** `useRunStateStore.ts:395-403` lists `pipelineFrameTypes` and **omits `validator_result`, `gate_passed` and `gate_blocked`**; `:405` is an explicit `if (!pipelineFrameTypes.includes(msg.type)) return;` early return. So even the one reachable path (`gate_blocked`, which has 3 real producers) cannot rehydrate `validationPassed` when a run is opened from Run History — `ValidationResultCard` (`AgentDetailPanel.tsx:839`) self-gates on `passed === undefined` and renders nothing. This is independent of ISS-116: fixing the producer gap alone would still leave history blind.

## Severity / Tag / Status (original row)

- Sev: minor
- Tag: frontend
- Status: **OPEN** (found during quick-260812-kpb / FIX-237; **upgraded from "suspected" to CONFIRMED**)

## Source / Evidence

The ISS-087 investigation flagged this as suspected without tracing the replay path; quick-260812-kpb read the filter and its early return directly. Not observed on screen — the DB has no such rows to replay.
