---
id: ISS-006
type: issue
status: open
area: [agents]
summary: >-
  rename one side only (other side re-drifts) / build a tsc gate now (TS + exec OFF
  until N3 + golden-pipeline) — S / low
source: .planning/ISSUES-REGISTER.md#iss-006
severity: ✓
status_detail: "thread a shared canonical accessor literal (getDatabase) through both prompts (13-03 shared-literal pattern) + offline acceptance-grep pin"
duplicate_rows: 2
---

# ISS-006

## Title

rename one side only (other side re-drifts) / build a tsc gate now (TS + exec OFF until N3 + golden-pipeline)

## Source / evidence

S / low

## Duplicate register rows (2)

1. ISS-006 | minor | product | FIXED (19-01; live re-confirm deferred) | app_builder contract drift: tests/setup.ts imports getDb but impl exports getDatabase — one TS2305 in the Jest global setup | REPORT-2026-06-12 §F5 re-audit, residual 1; FIXED 19-01: a shared canonical getDatabase named-export contract threaded through the producer (app-code-generator) + consumer (app-test-implementation, incl. the Jest global-setup) prompts + a 2-way grep pin. Live re-confirm (tsc/grep no getDb mismatch) deferred to the live pass.

2. ISS-006 | ✓ | prompt-contract gap: producer app-code-generator (order 8) exports getDatabase; consumer app-test-implementation (order 12) imports getDb in tests/setup.ts; no shared source of truth + contract-fidelity rule excludes the Jest global-setup file (symbols are model-invented: grep=0 in prompts/golden) | thread a shared canonical accessor literal (getDatabase) through both prompts (13-03 shared-literal pattern) + offline acceptance-grep pin | rename one side only (other side re-drifts) / build a tsc gate now (TS + exec OFF until N3 + golden-pipeline) | S / low
