---
id: ISS-049
type: issue
status: done
area: [sse, workflow, agents, auth, test-infra]
summary: >-
  Surfaced by the 37-07 rebuild agent's careful re-analysis (corrects the earlier "~10
  pre-existing" hand-wave): the FE suite has 2 DETERMINISTIC failures (both
  pre-existing, unrelated to 37-07) + ~8 FLAKY
source: .planning/ISSUES-REGISTER.md#iss-049
severity: trivial
status_detail: "FIXED (be620716, test-only — both were STALE tests, ZERO product bugs; #2 SURF-03 confirmed WORKING [strip is on the AgentsPopup "Workflow" tab; the test only opened "Agents"]; the"
---

# ISS-049

## Title

Surfaced by the 37-07 rebuild agent's careful re-analysis (corrects the earlier "~10 pre-existing" hand-wave): the FE suite has 2 DETERMINISTIC failures (both pre-existing, unrelated to 37-07) + ~8 FLAKY. (1, TEST bug) AgentProgressPanel.test.tsx "_revision same as base filter" — PRODUCT is correct (a ppt_revision correctly hides the Presentation chain button) but the loose queryByText("Presentation") catches the run's display-name header label; fix to queryByRole("button", {name:/^presentation/i}) per its sibling. (2, PROBABLE REAL REGRESSION) IdeaInputPage.declaredCapabilities (SURF-03) — the "Declared by this workflow" strip does NOT render after opening Advanced; the fetch fires but the strip is gone (IdeaInputPage last changed by 30-04 image-resize, AFTER the SURF-03 test) — investigate real-regression vs mock-mismatch, fix the correct side. (3, test-infra) ~8 tests (ReviewGatesSection/HomeLaunchGrid) pass in ISOLATION but leak global state (IntersectionObserver/sessionStorage/singletons) → fail under parallel load; harness weakness, not product.

## Source / evidence

37-07 rebuild agent re-analysis (2026-07-10)
