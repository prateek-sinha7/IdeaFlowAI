---
id: FIX-030
type: fix
kind: event
title: 'd4v: POST /api/prototype/run rejected any brief over 8000 chars (422) — blocked
  large briefs and attached-file content'
status: done
applies_to:
  phases:
  - '04'
  modules:
  - prototype_templates
  - test_prototype_run_request
  - api
  - app
  globs:
  - backend/app/api/prototype_templates.py
  - backend/tests/unit/test_prototype_run_request.py
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: test
  status: passed
  test_files:
  - backend/tests/unit/test_prototype_run_request.py
compact_summary: 'POST /api/prototype/run capped brief at a legacy 8000 chars while the app-wide BRIEF_MAX_CHARS is ~450k, 422ing long briefs and attached files; sourced the cap from settings instead.'
last_updated: '2026-07-03'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: applies-to-glob
---

# FIX-030

**Phase / trigger:** Phase 4 · quick-260703-d4v

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

d4v: POST /api/prototype/run rejected any brief over 8000 chars (422) — blocked large briefs and attached-file content

## Root cause

RunRequest.brief was declared max_length=8000, a legacy straggler from the prototype era; the app-wide BRIEF_MAX_CHARS (450k, ~150k tokens) already governs the other ingest paths but this endpoint was never aligned, so long briefs 422'd before reaching the engine. Fix: source the field cap from settings.BRIEF_MAX_CHARS instead of the hardcoded 8000; added a unit test pinning the cap to the setting.

## Files changed

- `backend/app/api/prototype_templates.py`
- `backend/tests/unit/test_prototype_run_request.py`
