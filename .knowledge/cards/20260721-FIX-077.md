---
id: FIX-077
type: fix
kind: event
title: Restore styled custom dropdown using onPointerDown+preventDefault to beat mousedown
  dismiss — Native select options cannot be CSS styled (OS renders them). C...
status: done
applies_to:
  phases:
  - '37'
  modules:
  - AgentsPopup
  globs:
  - frontend/src/components/workflow/AgentsPopup.tsx
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'Native select couldn''t be styled, so the custom dropdown was restored using `onPointerDown`+`preventDefault`, which fires before the mousedown dismiss.'
last_updated: '2026-07-21'
author: 'Prateek Sinha <2000152842@hexaware.com>'
author_source: applies-to-glob
---

# FIX-077

**Phase / trigger:** Phase 37/41 (B3/B7)

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Restore styled custom dropdown using onPointerDown+preventDefault to beat mousedown dismiss

## Root cause

Native select options cannot be CSS styled (OS renders them). Custom dropdown needed but all previous attempts failed because onClick fires after mousedown dismiss. Fix: use onPointerDown+preventDefault on both trigger and options — pointer events fire before mousedown, so selection lands before the outside-click dismiss.

## Files changed

- `frontend/src/components/workflow/AgentsPopup.tsx`
