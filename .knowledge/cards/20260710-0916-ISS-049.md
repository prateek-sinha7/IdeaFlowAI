---
id: ISS-049
type: issue
kind: event
title: 'Surfaced by the 37-07 rebuild agent''s careful re-analysis (corrects the earlier
  "~10 pre-existing" hand-wave): the FE suite has 2 DETERMINISTIC failures (bot...'
status: done
severity: trivial
applies_to:
  phases: []
  modules:
  - AgentsPopup
  - Home
  - HomeLaunchGrid
  - IdeaInputPage
  - ReviewGatesSection
  globs:
  - AgentProgressPanel.test.tsx
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'Re-analysis found only 2 real FE test failures (stale text query, mock/tab mismatch) vs ~10 assumed; fixing them also cleared ~8 flaky failures from parallel-run state leakage.'
last_updated: '2026-08-14'
author: 'unknown'
author_source: 'unknown'
recorded_by: 'Imran Yousaf <imrany@hexaware.com>'
---

# ISS-049

## Title

Surfaced by the 37-07 rebuild agent's careful re-analysis (corrects the earlier "~10 pre-existing" hand-wave): the FE suite has **2 DETERMINISTIC failures** (both pre-existing, unrelated to 37-07) + ~8 FLAKY. (1, TEST bug) `AgentProgressPanel.test.tsx` "_revision same as base filter" — PRODUCT is correct (a `ppt_revision` correctly hides the Presentation chain button) but the loose `queryByText("Presentation")` catches the run's display-name header label; fix to `queryByRole("button", {name:/^presentation/i})` per its sibling. (2, PROBABLE REAL REGRESSION) `IdeaInputPage.declaredCapabilities` (REQ-SURF-03) — the "Declared by this workflow" strip does NOT render after opening Advanced; the fetch fires but the strip is gone (`IdeaInputPage` last changed by 30-04 image-resize, AFTER the REQ-SURF-03 test) — investigate real-regression vs mock-mismatch, fix the correct side. (3, test-infra) ~8 tests (`ReviewGatesSection`/`HomeLaunchGrid`) pass in ISOLATION but leak global state (`IntersectionObserver`/`sessionStorage`/singletons) → fail under parallel load; harness weakness, not product.

## Severity / Tag / Status (original row)

- Sev: trivial
- Tag: test-infra
- Status: **FIXED** (be620716, test-only — both were STALE tests, ZERO product bugs; #2 REQ-SURF-03 confirmed WORKING [strip is on the AgentsPopup "Workflow" tab; the test only opened "Agents"]; the ~8 "flaky" were collateral parallel-timing damage from the 2 stale tests — now gone; full vitest suite 585/585 green ×5 runs, tsc 0. Two independent analyses concurred.)

## Source / Evidence

37-07 rebuild agent re-analysis (2026-07-10)
