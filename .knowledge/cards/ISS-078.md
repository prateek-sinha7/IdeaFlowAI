---
id: ISS-078
type: issue
status: open
area: [backend, sse, resume, workflow, agents, auth, artifacts, engine]
files:
  - backend/tests/agents/test_restart_resume.py
summary: >-
  7 reds in backend/tests/agents/test_restart_resume.py (7 failed / 48 passed), and at
  least two of them may be describing a LIVE PRODUCTION RESUME BUG rather than test
  rot
source: .planning/ISSUES-REGISTER.md#iss-078
severity: major
relates: [ISS-068, FIX-223, FIX-175]
---

# ISS-078

## Title

7 reds in backend/tests/agents/test_restart_resume.py (7 failed / 48 passed), and at least two of them may be describing a LIVE PRODUCTION RESUME BUG rather than test rot. Carved out of ISS-068 (which owned the golden + import halves and is now FIXED); measured identically at f5f2f7c6 and after FIX-223/224, same 7 ids, twice in one session — not flaky, and not environment-gated (55 collected, 0 skipped, whole file runs offline in ~5 s; no Postgres/Bedrock/Chromium gate fires). All 7 exist on dev too. They were green once: commit 3e80fcc4 (2026-07-19) records "restart_resume 39 -> 41/0" with all 7 already present, so these are regressions since 2026-07-19, not red-by-design pins. Failing ids: test_midwave_resume_does_not_reinvoke_completed_workers (:472), test_open_gate_override_is_noop_without_review_gate (:1204), test_partial_task_loop_build_reenters_step_not_skipped (:1575), test_completed_task_loop_build_stays_complete_no_rerun (:1603), test_resumed_run_is_wired_live_ectx_and_milestone_cards (:2663), test_failed_run_resumes_skips_completed_tasks (:3081), test_failed_run_with_open_gate_resumes_into_gate (:3173). LEAD (high confidence, UNPROVEN): c71f3d9c — a commit titled fix(frontend) — is the only commit that has ever touched _first_incomplete_step's skip predicate, and it inverted it from or to and (engine.py:7882-7885: complete_by_artifact = agent_id in produced_agents and agent_id in agent_complete_events), strictly tightening what counts as "already complete". Three of the reds (#2/#3/#4) fail with the identical idx=0 signature — exactly what a stricter predicate produces when a test seeds durable artifact rows but no agent_complete rows. The same commit also added resume_offset, which was ISS-068 cause C1 — it is implicated in both halves. TWO LIVE HYPOTHESES — do not guess: *H1 (test stale)* the tightening is correct and the hand-seed idiom now needs agent_complete rows; *H2 (code regressed)* the tightening over-corrects and really does re-invoke completed work on resume in production. H2 has real support: #1 and #6 are end-to-end — they drive the real engine, let it write its own durable rows, and still observe part_a re-invoked ({'a': 4, 'b': 4, 'c': 2, 'd': 2}). A hand-seeding artefact cannot explain those two. RUN THIS FIRST: in a scratch worktree revert *only* the c71f3d9c predicate hunk and re-run the 7; if ≥3 go green, H2 is live and this is a production resume regression, not test rot. One red is PROVEN trivial and independent (#5): 2d5b9fcc (FIX-175) updated the unpack at test_restart_resume.py:2665 to a 4-tuple (_created, _card_seq, _, _reply_eid) but left its own stub at :2642 appending a 3-tuple (emitted_cards.append((created, card_seq, card))) → ValueError: not enough values to unpack (expected 4, got 3). One line, zero production risk. DO NOT delete, xfail or loosen any of the 7 — at least two are end-to-end assertions that completed work is not re-invoked on resume; if they are right they are describing a live bug. Estimated multi-day; plausibly ≥3 distinct causes.

## Source / evidence

Carved out of ISS-068 during quick-260812-35u, 2026-08-12. Baseline re-measured before and after FIX-223/224: unchanged at 7 failed / 48 passed with the same 7 ids. Root-cause detail in the ISS-068 investigation report §6.
