---
id: ISS-140
type: issue
status: open
area: [frontend]
summary: >-
  The LIVE terminal path clears only the legacy React state, never the run store.
  page.tsx:1196-1201 calls setReviewGateData(null) / setQuestionnaireData(null) /
  setActivePipelineRunId(null)
source: .planning/ISSUES-REGISTER.md#iss-140
severity: minor
status_detail: "OPEN (filed 2026-08-12 during quick-260812-wir)"
relates: [ISS-126, FIX-245]
---

# ISS-140

## Title

The LIVE terminal path clears only the legacy React state, never the run store. page.tsx:1196-1201 calls setReviewGateData(null) / setQuestionnaireData(null) / setActivePipelineRunId(null), but the lane reads the STORE (runStore.viewed.reviewGateData, runStore.viewed.activePipelineRunId) — and no runStore.update(..., {activePipelineRunId: null}) exists anywhere in the tree. Currently MASKED by the isRunning conjunct in runLaneState, so not user-visible today, but it is the same class of defect as the known unfixed residual "cancel-at-clarify doesn't repaint" and a plausible cause of it.

## Source / evidence

Verified during the ISS-126 investigation (quick-260812-wir). Not folded into FIX-245: that fix covers the REOPEN path; this is the live path and needs its own live proof.
