---
id: ISS-068
type: issue
kind: event
title: 'Measured repeatedly 2026-08-11 across quick-260811-mxg and -si4. NOTE: an
  older memory recorded "10 failed / 6 passed" — that was stale `feat/ui-2` data;
  the...'
status: done
severity: major
applies_to:
  phases: []
  modules:
  - agents
  - app
  - od_loader
  - services
  globs: []
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: '5 characterization goldens and 1 import-linter contract were red on dev, so INV-3 parity could only be checked by diffing baselines, not a clean run.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-185](20260806-1225-FIX-185.md), [FIX-190](20260806-1715-FIX-190.md), [FIX-217](20260811-2101-FIX-217.md), [FIX-223](20260812-0226-FIX-223.md), [FIX-224](20260812-0227-FIX-224.md), [ISS-029](20260703-2055-ISS-029.md), [ISS-078](20260812-0236-ISS-078.md), [ISS-079](20260812-0236-ISS-079.md)

**Referenced by:** [ISS-076](20260812-0212-ISS-076.md), [ISS-078](20260812-0236-ISS-078.md), [ISS-079](20260812-0236-ISS-079.md)

<!-- /RELATED -->

# ISS-068

## Title

Measured repeatedly 2026-08-11 across quick-260811-mxg and -si4. NOTE: an older memory recorded "10 failed / 6 passed" — that was stale `feat/ui-2` data; the current figure is 5/5.

## Severity / Tag / Status (original row)

- Sev: major
- Tag: test-infra
- Status: **FIXED** (2026-08-12, quick-260812-35u; FIX-223 + FIX-224 + TEST-008; INV-3 + Ports & Adapters restored) — **goldens 5 failed/5 passed → 10 passed/0 failed; lint-imports 3 kept/1 broken → 4 kept/0 broken.** The 5 red goldens reduced to exactly **3 causes**, proven exhaustive by counterfactual, each adjudicated separately per the **ISS-029** method (which explicitly REJECTED regen-only). **C1 (all 5) — STALE NORMALIZER, neither golden nor engine wrong:** `c71f3d9c` added an additive `pipeline_start.resume_offset`; 0 on every non-resumed run, but a 0-valued key is still a NEW key and the snapshot compares whole canonical-JSON dicts. Fixed by adding it to `_VOLATILE_STRIP_KEYS` (the 7th instance of a 6-precedent convention) — **zero golden bytes touched**; `_REQUIRED_DATA_KEYS` has no `pipeline_start` entry so nothing is weakened, and resume-offset behaviour is pinned directly by `test_restart_resume.py`. **C2 (`prototype`, `od_prototype`) — a REAL REGRESSION; the golden was RIGHT:** `f608e4bc` (FIX-190) appended an `accessibility` guardrail to 4 prototype-family agents and in `prototype-build/AGENT.md` the `+` line landed ON TOP OF `icon: "🏗️"` (the other 3 files are pure `1 +`; this one is `2 +-`), so the agent fell through to the loader's `🤖` default — a user-visible UI regression in `pipeline_start.agents[]` + both `agent_start`s. Sweep: **exactly 1 of 86** AGENT.md files was missing an icon. Restored byte-identical to `f608e4bc^` — **zero golden bytes touched**. Regenerating here would have permanently pinned the regression. **C3 (`prototype_revision`) — a genuinely STALE GOLDEN:** `fd2ad8f1` (FIX-185) deliberately set `planner: skip`. Regenerated **ONLY** this file, in its own revertable commit `a6b2097e`, **strictly after C1** so `resume_offset` could not be baked in; diff fully accounted — **33 deletions / 0 insertions**, 17→14 events, removed exactly {`planner_start`, `planner_complete`, `gate_status`}, the 14 survivors byte-identical and same-order. **Import contract:** the single violation `task_loop.py:562` was a one-off bypass of the already-existing `KernelServices.template_example()` port (already consumed correctly by `context_providers/opendesign.py:128-135`); fixed by breaking the import, NOT by amending the contract. **2 coverage gaps closed (ISS-029 discipline):** every AGENT.md must now declare an explicit `icon:`, and the capability import boundary now fails **pytest**, not only the separately-invoked `lint-imports` binary — the reason a one-line violation survived ~3 weeks. Carve-outs filed, not dropped: **ISS-078** (the 7 `test_restart_resume.py` reds, unchanged at 7F/48P) and **ISS-079** (2 `test_phase6_frontend_consistency.py` reds). ORIGINAL REPORT: Shared red baseline on `dev` blocks the goldens from serving as a CI gate: **5 of 5 characterization event-snapshot tests fail** (`prototype`, `od_prototype`, `od_ppt`, `app_builder`, `prototype_revision`) and **1 import-linter contract is broken** (`agents.capabilities` → `execution_engine.od_context` → `app.services.od_loader`). Also 7 reds in `test_restart_resume.py`. Re-measured independently in a detached worktree at `edc44daa` and again at `84f4bbe3` — identical failing ids both times, so it is genuinely pre-existing and shared with `dev`, not introduced by FIX-217/218. While it stands, INV-3 dormancy can only be checked by diffing counts and ids against a hand-captured baseline rather than by a clean green run.

## Source / Evidence

(none recorded)
