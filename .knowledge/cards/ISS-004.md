---
id: ISS-004
type: issue
status: open
area: [agents, artifacts]
summary: >-
  tick closed on the offline pin (scripted model ignores prompts) / strip XML in the
  FE — S / low (re-confirm); M / med (belt)
source: .planning/ISSUES-REGISTER.md#iss-004
severity: ✓ (live re-confirm pending)
status_detail: "Part 1: the named live re-confirm (PHASE-15-RECHECK.md:40-43). Part 2 (optional belt): apply the sanitizer to the streamed agent_chunk path with chunk-straddle buffering (engine-to"
duplicate_rows: 2
---

# ISS-004

## Title

tick closed on the offline pin (scripted model ignores prompts) / strip XML in the FE

## Source / evidence

S / low (re-confirm); M / med (belt)

## Duplicate register rows (2)

1. ISS-004 | minor | product | FIXED (19-03, structural; live re-confirm deferred) | sdlc-governance agent family emits fabricated tool-call XML preamble (<function_calls>/<invoke name="read_file">) on live Haiku before content (F4 residual) | REPORT-2026-06-12 §F5-residual; P15 defused the prompt trigger; 19-03 added the deterministic engine agent_chunk sanitizer (chunk-straddle buffer, tool-less) so fabricated XML can't reach the live UI stream. Live re-confirm (0 tool-XML in chunks) deferred to the live pass.

2. ISS-004 | ✓ (live re-confirm pending) | Two layers. (A) prompt: tool-less aggregator's "Read … from context" framing elicits simulated <invoke read_file> on live Haiku — P15 defused the trigger (shipped+pinned). (B) structural: the 13-02 sanitizer by design does not touch the UI chunk stream — engine.py:2330-2332 yields raw chunks before sanitize_output() at :2480-2492, so it's UI-stream-cosmetic only (downstream context + deliverable are clean) | Part 1: the named live re-confirm (PHASE-15-RECHECK.md:40-43). Part 2 (optional belt): apply the sanitizer to the streamed agent_chunk path with chunk-straddle buffering (engine-touching → separate phase) | tick closed on the offline pin (scripted model ignores prompts) / strip XML in the FE | S / low (re-confirm); M / med (belt)
