---
id: ISS-171
type: issue
kind: event
title: '`read_files: false` is not enforced at runtime — a step denied read access still executes read_file/ls/grep against the run sandbox'
status: done
applies_to:
  phases: []
  modules: [MOD-backend-app-agents, MOD-backend-agents]
  globs:
  - backend/app/agents/deep_agent_runner.py
  - backend/agents/factory.py
  - backend/agents/workflows/ex_A1_loop/workflow.yaml
  requirements: []
locked_constraints:
  - A step whose compiled ToolPermissions say `read_files: false` must not be able to call read_file, ls, glob or grep. The permission is a security boundary, not a hint.
verification:
  type: test
  status: passed
  test_files:
    - backend/tests/agents/test_tool_grant_invariants.py
compact_summary: 'Denial was applied at OFFER time (wrap_model_call strips request.tools) but never at execution, so a model naming a stripped tool still reached a live implementation; fixed with wrap_tool_call/awrap_tool_call refusals'
last_updated: 2026-08-25
author: 'Bilal Arshad <BilalA@hexaware.com>'
---

<!-- RELATED -->

## Related

**Referenced by:** [ISS-174](20260825-0956-ISS-174.md)

<!-- /RELATED -->

Found while taking spec-014's `ex_A1_loop` fixture through repeated live runs on AWS
Bedrock (`eu.anthropic.claude-haiku-4-5`).

## Symptom

Every step of `ex_A1_loop` declares `read_files: false` (verified at compile time — all
four steps report `read/write/exec = False/False/False`). Yet a single run emitted **11
`tool_call` events**, and they executed:

```
greet  read_file  /emoji-say-hello-to-me.md
check  ls         /   ->  ['/.logs/', '/emoji-say-hello-to-me.md', '/greet-say-hello-to-me.md']
check  read_file  /greet-say-hello-to-me.md
done   grep, read_file, ls
```

The `ls` result is a REAL directory listing returned to the model, so this is enforcement
failing — not the model narrating an intention it could not act on.

## Why it also breaks the fixture

The files those steps discover are the per-step `<step>-<slug>.md` artifacts
`artifact_fallback` writes. Each step spends its turn hunting for and reading the previous
step's artifact and narrating that hunt, which pollutes its output:

```
greet -> 'I need to read the file first. Let me check the directory:👋 Hello there!'
check -> 'Let me read the greeting file:I need to find the file first...'
```

`check` must emit one line of JSON. The narration means it does not, the conditional gate
fails closed, and — via the separate defect in
[ISS-170](20260824-1610-ISS-170.md) — the run advances anyway and reports success.

This is why `ex_A1_loop` fails consistently while A2/A3/A4 pass: A1 is the only fixture
whose prompts reference prior output ("Look at the greeting above", "Respond with the
greeting above"), which is what invites the file hunt.

## Where the break is

Traced end to end, and the first three layers are CORRECT:

- the compiler applies the cap and produces the right `ToolPermissions`
- `current_step` is bound correctly (`kernel_services.py`)
- `denied_tools(ctx.step_tools)` computes the right denial set (`factory.py:330`)
- it is passed to `DeepAgentRunner(denied_tools=...)` (`factory.py:389`)

**The break is inside `DeepAgentRunner`, past that hand-off** — the denial set arrives and
does not take effect. `PERMISSION_TOOLS["read_files"]` covers `read_file`, `ls`, `glob`
and `grep`, all four of which were observed executing.

## Severity

Security-adjacent. A step deliberately denied read access can enumerate and read the run
sandbox, including artifacts written by other steps in the same run. No cross-run or
cross-user access was observed — the sandbox is still per-run — but the per-step boundary
is not holding.

TEST COVERAGE — ISS-171
Unit tests:        none — not fixed
Integration tests: none
Regression guards: none. `ex_A1_loop`'s runner fails on the downstream symptom (missing
                   joke / wrong deliverable), not on the permission breach itself; a
                   direct assertion would be "zero tool_call events on a fully-denied step".


## Resolution — 2026-08-25

**The card's "the break is inside DeepAgentRunner, past the hand-off" is not quite right,
and the correction matters for anyone reading this later.** The denial arrives intact and
IS applied — at the wrong seam. `_ToolFilterMiddleware._filter` strips tools from the model
REQUEST via `wrap_model_call`, which decides what the model is OFFERED. Nothing unregisters
the tool from the graph, and deepagents' own system prompt documents the filesystem suite,
so a model names a tool it was not offered and reaches a live implementation. Offer-time
filtering is UX narrowing; a withheld permission needs an enforcement point at EXECUTION.

Added `wrap_tool_call` / `awrap_tool_call` returning a refusal
`ToolMessage(status="error")` without calling `handler`. Verified against the installed
LangChain 1.3.4 that this is the sanctioned contract — the signature is literally
`-> ToolMessage | Command[Any]`, and skipping `handler` short-circuits execution.

### Blast radius — wider than this card's A1 framing

Measured by compiling all 27 workflows and computing `denied_tools` per step. **All 119
compiled steps have a non-empty exclusion set**, so the new arm is live on every
deep-agent invocation. Three tiers:

| denial set | steps | effect |
|---|---|---|
| `execute` only | 40 | inert — `execute` is already a no-op under `FilesystemBackend` |
| `+ write_file, edit_file` | 57 | REAL — production: app_builder 10, custom 9, dotnet_to_azure 9, mulesoft_to_springboot 8, chat 7, user_stories 6, prototype 3, sc001-test-fixture 2, ppt_revision/sample_brownfield/user_stories_revision 1 each |
| `+ read_file, ls, glob, grep` | 22 | REAL — the `ex_A*` fixtures, the case this card reports |

`excluded` is never empty even before a manifest denial — the baseline is always at least
`{task}` (`_LIBRARY_SUBAGENT_TOOL`), so `task` also moves from un-offered to
execution-refused, which is ADR-0012's "spawn_subagents always off" now actually holding.

TEST COVERAGE — ISS-171
Unit tests:        23 in backend/tests/agents/test_tool_grant_invariants.py → ALL GREEN
                   (4 new §2b tests over PERMISSION_TOOLS["read_files"])
                   144 across the permission surface → ALL GREEN
                   (test_tool_permissions 9, test_create_runner 22, test_hooks 26,
                    test_concierge_capability 38, test_text_only_prompt_hygiene 14,
                    test_banned_patterns 18, test_engine_runner_error_arm 6,
                    test_integration_scopes 6, test_runner_output_fallback 3,
                    test_skills_text_only_deliverable 2)
                   253 across the non-workflow denied_tools paths → ALL GREEN
                   (test_no_baseagent 193, test_handoff_agents 17,
                    test_concierge_escalation 13, test_chat_contract 11,
                    test_chat_runner 8, test_handoff_coder_hardening 6,
                    test_chat_event_neutrality 5)
Integration tests: N/A
Frontend tests:    N/A — backend-only change
Goldens:           0 failed / 10 passed across all 5 characterization suites — IDENTICAL
                   to be1533e11. NOTE these run a SCRIPTED model, so they prove the normal
                   path is unchanged; they do NOT exercise the refusal path, because a
                   scripted model never calls write_file.
lint-imports:      not run — the one new import (`langchain_core.messages.ToolMessage`)
                   is within an already-imported package
Regression guards:
  - the 4 new §2b tests assert every PERMISSION_TOOLS["read_files"] member is refused at
    execution, not merely unbound.
  - direct middleware smoke test (sync AND async): ls/read_file/grep refused with
    status='error'; write_file/execute pass through; an empty exclusion set is a pure
    pass-through.

**Live verification against a real model is still outstanding** and is the one genuinely
open risk: the 57 write-denied production steps change behaviour only when a model
actually calls `write_file`, which no offline harness reproduces. A `prototype` and an
`app_builder` run are the honest confirmation.
