---
id: ISS-013
type: issue
status: done
area: [harness]
summary: >-
  UI driver generic selectors didn't match real Flowin controls (submit is "Run with
  defaults"/"Skip all & run directly"; options are radio-card buttons) → fell back to
  WS on S01
source: .planning/ISSUES-REGISTER.md#iss-013
severity: minor
duplicate_rows: 2
---

# ISS-013

## Title

UI driver generic selectors didn't match real Flowin controls (submit is "Run with defaults"/"Skip all & run directly"; options are radio-card buttons) → fell back to WS on S01

## Source / evidence

UI campaign S01 02-questionnaire.png; fixed in driver v2 with the real selector reference

## Duplicate register rows (2)

1. ISS-013 | minor | harness | FIXED | UI driver generic selectors didn't match real Flowin controls (submit is "Run with defaults"/"Skip all & run directly"; options are radio-card buttons) → fell back to WS on S01 | UI campaign S01 02-questionnaire.png; fixed in driver v2 with the real selector reference

2. ISS-013 | ✓ FIXED | driver v1 generic selectors didn't match real Flowin controls | driver v2 with the real FE selector reference (shipped) | — | —
