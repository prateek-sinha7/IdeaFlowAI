---
id: FIX-092
type: fix
kind: event
title: 'Bug 3 (definitive): pass _display_title in extraParams from all chain/revision
  call sites so page.tsx never needs to re-parse complex nested context blocks'
status: done
applies_to:
  phases:
  - '25'
  modules:
  - DashboardLayout
  - page
  - app
  - parseRunInput
  globs:
  - frontend/src/components/layout/DashboardLayout.tsx
  - frontend/src/app/dashboard/page.tsx
  requirements: []
locked_constraints:
- INV-1
- INV-12
- INV-3
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: '`parseRunInput` failed on complex nested chain context; call sites now pass an already-clean `_display_title` in extraParams instead of re-parsing.'
last_updated: '2026-07-22'
author: 'Sharad Gajera <gajerasharadn@hexaware.com>'
author_source: applies-to-glob
---

# FIX-092

**Phase / trigger:** Phase 25/36

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Bug 3 (definitive): pass _display_title in extraParams from all chain/revision call sites so page.tsx never needs to re-parse complex nested context blocks

## Root cause

parseRunInput failed on complex nested context; fix passes the already-clean chainBrief/instruction as _display_title in extraParams — no re-parsing needed

## Files changed

- `frontend/src/components/layout/DashboardLayout.tsx`
- `frontend/src/app/dashboard/page.tsx`
