---
id: ISS-033
type: issue
status: open
area: [workflow, agents, auth, product]
summary: >-
  SmartPlanner + ClarifyEngine + the handoff Test/Compliance agents call the model
  DIRECTLY (not via the shared cached deep-agent runner)
source: .planning/ISSUES-REGISTER.md#iss-033
severity: minor
status_detail: "OPEN (deferred; scoped-out of quick-260704-ttk)"
relates: [FIX-034, FIX-036, ISS-032]
---

# ISS-033

## Title

SmartPlanner + ClarifyEngine + the handoff Test/Compliance agents call the model DIRECTLY (not via the shared cached deep-agent runner) → those calls get NO Bedrock prompt caching (FIX-034 middleware runs only in the runner path) AND their tokens are UNCOUNTED in run cost (they never emit usage events into the engine's per-agent accumulation). Affects ~every workflow; each direct call can be up to ~150k tokens. Action: a shared cached-invoke helper that routes direct model calls through the same cachePoints + usage_metadata path, and fold their tokens into run-cost accounting.

## Source / evidence

Companion to FIX-036/ISS-032, which fixed ONLY the shared runner path; the direct-call paths are out of scope for quick-260704-ttk.
