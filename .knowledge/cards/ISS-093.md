---
id: ISS-093
type: issue
status: open
area: [backend, sse, auth, test-infra]
files:
  - backend/tests/unit/test_execution_engine.py
summary: >-
  3 stale clarify-round tests in backend/tests/unit/test_execution_engine.py assert an
  event vocabulary the product no longer emits
source: .planning/ISSUES-REGISTER.md#iss-093
severity: minor
status_detail: "OPEN (pre-existing; found while verifying FIX-230, deliberately not fixed there)"
relates: [ISS-027, FIX-230]
---

# ISS-093

## Title

3 stale clarify-round tests in backend/tests/unit/test_execution_engine.py assert an event vocabulary the product no longer emits. test_clarify_engine_asks_once_then_limit_reached (:152), test_clarify_engine_rounds_knob_allows_multi_round (:186) and test_clarify_engine_empty_submit_proceeds_after_one_round (:309) expect clarification_limit_reached; the engine now emits ['questionnaire_ready', 'questionnaire_complete']. Reads as fallout from ISS-027's "Option A force-proceed" change, which replaced the limit-reached path — the tests were not reconciled with it. Classify before fixing: this is *changed behaviour* (reconcile the assertions to the current vocabulary), not selector drift, and the tests must not simply be deleted.

## Source / evidence

Verified pre-existing, with a SHA: re-measured at d24c576a in a detached worktree containing none of FIX-230's changes — the identical 3 fail there (3 failed / 11 passed), and the same 3 fail after (3 failed / 78 passed across the wider selection). Not previously registered as its own row. Left unfixed because reconciling them means deciding what the clarify round-limit contract now IS, which is ISS-027's territory, not a token-accounting fix.
