---
id: FIX-009
type: fix
date: 2026-06-16
status: done
area: [backend, agents]
files:
  - backend/agents/prompts/prototype-plan/AGENT.md
  - backend/agents/prompts/prototype-build/AGENT.md
summary: >-
  Task Planner produces plan without ## Task N: headers — build agent gets empty task,
  streams HTML as text
source: .planning/FIX-REGISTER.md#fix-009
invariants: [INV-1, INV-3, INV-12, SC-001]
---

# FIX-009

**Phase / trigger:** Phase 15

> No detail section exists in the register for this entry — only the
> summary-table row below. Nothing has been invented to fill the gap.

## Description

Task Planner produces plan without ## Task N: headers — build agent gets empty task, streams HTML as text

## Root cause

Even after FIX-008, prototype-plan produces 1696-char plan without ## Task N: headers (uses bullets/prose); heading_tasks parser finds 0 tasks → empty fallback task → build agent has no instructions → streams HTML as text, never writes prototype.html; strengthened output contract with concrete example + explicit format warning; added build agent guard for empty task block + mandatory write_file reminder

## Files changed

- `backend/agents/prompts/prototype-plan/AGENT.md`
- `backend/agents/prompts/prototype-build/AGENT.md`
