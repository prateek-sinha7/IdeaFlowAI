---
id: ISS-063
type: issue
kind: event
title: The violet "Spec Revision Cycle N" banner (FIX-163) is LIVE-SESSION-ONLY state
  and never back-fills from durable events, so reopening a run that was revised ...
status: done
severity: minor
applies_to:
  phases: []
  modules:
  - app
  globs:
  - dashboard/page.tsx
  - page.tsx
  requirements: []
locked_constraints: []
verification:
  type: manual
  status: passed
  test_files: []
compact_summary: 'The revision-cycle banner armed on an unsatisfiable predicate (every agent done while still running), so it never left 0 even live.'
last_updated: '2026-08-14'
author: 'Imran Yousaf <imrany@hexaware.com>'
author_source: code-commit
---

<!-- RELATED -->

## Related

**Depends on:** [FIX-163](20260802-2348-FIX-163.md), [FIX-164](20260802-2348-FIX-164.md), [FIX-217](20260811-2101-FIX-217.md), [FIX-221](20260812-0158-FIX-221.md), [FIX-222](20260812-0158-FIX-222.md), [ISS-065](20260812-0005-ISS-065.md)

**Referenced by:** [FIX-221](20260812-0158-FIX-221.md), [FIX-222](20260812-0158-FIX-222.md), [FIX-236](20260812-1444-FIX-236.md), [FIX-254](20260813-1300-FIX-254.md), [ISS-065](20260812-0005-ISS-065.md), [ISS-075](20260812-0212-ISS-075.md), [ISS-080](20260812-0247-ISS-080.md), [ISS-111](20260812-1444-ISS-111.md)

<!-- /RELATED -->

# ISS-063

## Title

The violet "Spec Revision Cycle N" banner (FIX-163) is LIVE-SESSION-ONLY state and never back-fills from durable events, so reopening a run that was revised shows no sign a revision ever happened. `dashboard/page.tsx:357` holds `specRevisionCount` in `useState(0)` and `:907-912` increments it only when `revisionCycleArmedRef` is armed by the FE's own click AND a matching live event arrives. Same invisibility family as FIX-217: a revision occurred and the screen does not say so. **CORRECTIONS ON INVESTIGATION (2026-08-12) — this row was wrong twice.** (1) *"armed by the FE's own click"* is **wrong**: arming was a derived-state condition in a `useEffect` (`page.tsx:1646`), never a click. (2) Far more importantly, the defect was **not** "live works, reload does not" — it was **dead on every path** for `od_prototype`, live included. FIX-164's arming predicate required EVERY agent to be `"done"` while the run was still `isRunning`, but the update_specs gate sits on `prototype-analyze`, agent 2 of 5; the only sites that mark the rest done also set `isRunning:false`. The arm was structurally unsatisfiable, so the counter never left 0 in ANY session, on the flagship workflow, since `219702a4` (2026-08-03). Severity accordingly raised minor → **major**. Two further sufficient mechanisms sat behind it (a post-commit ref frozen during the synchronous replay loop; the trailing resume `pipeline_start` zeroing the counter last on every replay) — all three are one root cause: FIX-163/164 modelled "a revision is happening" as assembled live-session state instead of a property derivable from accumulated events. **Closed by deriving it in the reducer** (`agentStartCounts` → `deriveSpecRevisionCount`), with the arm/consume machinery DELETED, not shadowed.

## Severity / Tag / Status (original row)

- Sev: ~~minor~~ **major**
- Tag: frontend,product
- Status: **FIXED** — FIX-221 (2026-08-12, quick-260812-2ci)

## Source / Evidence

Filed from the live Playwright pass on run `d5dbc9f2`. Root-caused 2026-08-12 by a dedicated read-only investigation that read the durable `run_events` log and simulated the FE logic against it (predicted 0, correct value 2). Fixed the same day, shipped as ONE change with ISS-065/FIX-222 — see that row for why. 15 regression tests, all seen RED first; there had been **zero** coverage before, which is exactly how FIX-164 silently killed the feature FIX-163 had just shipped.
