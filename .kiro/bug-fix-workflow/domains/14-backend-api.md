# Domain 14 — backend·api

29 cards, 4 batches.

| card | status | batch | round | fix site | tier | phases | model |
|---|---|---|---|---|---|---|---|
| ISS-134 | ESCALATED | B1 | 1 | backend·api (mixed) | A′ (ISS-471 sibling of ISS-319) + C | mixed | **sonnet** (backend core) |
| ISS-182 | CLOSED | B1 | 1 | backend·api (mixed) | A′ (ISS-471 sibling of ISS-319) + C | mixed | **sonnet** (backend core) |
| ISS-419 | ESCALATED | B1 | 1 | backend·api (mixed) | A′ (ISS-471 sibling of ISS-319) + C | mixed | **sonnet** (backend core) |
| ISS-470 | FIXED | B1 | 1 | backend·api (mixed) | A′ (ISS-471 sibling of ISS-319) + C | mixed | **sonnet** (backend core) |
| ISS-471 | FIXED | B1 | 1 | backend·api (mixed) | A′ (ISS-471 sibling of ISS-319) + C | mixed | **sonnet** (backend core) |
| BUG-004 | FIXED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| BUG-017 | TESTED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-100 | TESTED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-104 | TESTED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-119 | ESCALATED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-127 | TESTED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-128 | TESTED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-133 | ESCALATED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-144 | TESTED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-154 | TESTED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-161 | ANALYZED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-398 | ANALYZED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-418 | ANALYZED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-422 | ANALYZED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-435 | ANALYZED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-436 | ANALYZED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-437 | ANALYZED | B2 | 2 | `backend/app/api/run_commands.py` | A′ (ISS-398 sibling of ISS-292; ISS-422 sibling of ISS-312; ISS-435-437 sibling of ISS-316) + B (stale-state + silent-discard) + C | full; one read | **sonnet** (17 cards, load-bearing API file) |
| ISS-180 | ANALYZED | B3 | 5 | `backend/app/api/user_workflows.py` | A′ (ISS-263 sibling of ISS-225) + C | mixed | **sonnet** |
| ISS-181 | ANALYZED | B3 | 5 | `backend/app/api/user_workflows.py` | A′ (ISS-263 sibling of ISS-225) + C | mixed | **sonnet** |
| ISS-263 | ANALYZED | B3 | 5 | `backend/app/api/user_workflows.py` | A′ (ISS-263 sibling of ISS-225) + C | mixed | **sonnet** |
| ISS-381 | ANALYZED | B3 | 5 | `backend/app/api/user_workflows.py` | A′ (ISS-263 sibling of ISS-225) + C | mixed | **sonnet** |
| BUG-015-016 | ANALYZED | B4 | 5 | `backend/app/api/run_stream.py` | C | full | **sonnet** (SSE transport surface) |
| ISS-105 | ANALYZED | B4 | 5 | `backend/app/api/run_stream.py` | C | full | **sonnet** (SSE transport surface) |
| ISS-137 | ANALYZED | B4 | 5 | `backend/app/api/run_stream.py` | C | full | **sonnet** (SSE transport surface) |

Notes:
- B2 `run_commands.py` is the **second-heaviest batch** (17 cards in one file). It is
  the REST up-channel: gate, answers, cancel, messages, revisions. Dominant defects:
  missing terminal-run guards (ISS-144), uncapped analysis_report in spec-revision
  prompts (ISS-127), false-approve on gate reject (ISS-128), stale line-number
  references (ISS-104). Tier-A′ siblings (ISS-398/422/435-437) replicate validated
  diffs — fast.
- B1: ISS-134 cancel-liveness process-local dict (architecture concern for ECS
  migration; may be escalate rather than fix). ISS-470/471 race on concurrent
  PUT/refresh. ISS-182 stale custom-agent:<id> rows.
- B1 ISS-470 FIXED 2026-08-31 by 5-fixer: `upsert_github_pat`'s 200 body now reports the
  local `gh_user`/`scopes_header` this request validated instead of a post-`db.refresh(row)`
  re-read (`backend/app/api/settings.py:168-181`) — the ISS-319/FIX-382 shape, sibling fix.
  `backend/tests/unit/test_settings_github_pat_race.py` XPASS(strict); FIX-439.
- B1 ISS-419 ESCALATED 2026-08-31 by 5-fixer: card was a body-less stub (no root cause,
  no proposed fix), and its test
  (`frontend/src/components/workflow/ReviewGatesSection.test.tsx:234,247`) asserts a
  manifest `approval`/`security` gate seeds the Review-gates checkbox CHECKED. That
  reverses FIX-377's locked constraint, and once seeded the next toggle writes
  `gates: [approval, human]` — rejected at `backend/agents/workflows/compiler.py:806-811`
  (ADR-0013), with no runtime dedupe for `approval`
  (`backend/agents/execution_engine/engine.py:6491`). Both gates are `user_allowed=False`
  (engineer-only). Needs a design decision; nothing edited, test left as written. Full
  reasoning + 3 options on the ISS-419 card.
- B2 ISS-119 ESCALATED 2026-08-31 by test-writer: the two `TestRouting` tests the card
  names (`test_clarify_waiting_routes_to_answers_seam` /
  `test_gate_paused_routes_to_gate_seam_approve`) no longer exist under those names —
  someone already reconciled them to `test_clarify_waiting_plain_text_routes_to_concierge`
  / `test_gate_paused_plain_text_routes_to_concierge`, both asserting `CHANNEL_CONCIERGE`.
  `test_terminal_routes_to_revision` (`_FakeUser.tier` AttributeError) is also fixed.
  Ran `backend/tests/unit/test_chat_messages_endpoint.py` whole file offline: 21 passed,
  0 red. Wrote and ran a candidate coverage test for the card's remaining ask
  (structured clarify answer → `answers` at the endpoint level) — it passed on the
  first try, so it is not a defect proof and was not committed (would XPASS an
  `xfail(strict=True)` and fail the suite for the wrong reason). No red test exists to
  write against this card; the defect is already resolved in code. Recommend closing
  ISS-119 or re-scoping to a plain (non-xfail) coverage addition. Full note on the
  ISS-119 card.
- B2 ISS-133 ESCALATED 2026-08-31 by test-writer: the card's defect —
  `ExecutionEngine._stamp_resume_marker`'s collision-unsafe
  `read_events → max+1 → append_event` seq allocation — is already fixed at HEAD.
  `engine.py:7694-7755` uses `store._max_event_seq(run_id)` + `append_event_at_or_after`
  (the collision-safe pair), and its own docstring says so: "ISS-125: it now rides the
  SAME collision-safe pair the engine sink and the durable-cancel writer use". ISS-125's
  existing test (`backend/tests/agents/test_resume_marker_seq_allocation.py`) already
  covers this call site and passes at HEAD (ran offline, 1 passed). No red test exists
  to write against ISS-133 — writing an `xfail(strict=True)` here would XPASS
  immediately and fail the suite for the wrong reason. ISS-133 is a stale duplicate of
  the already-shipped ISS-125 fix; recommend closing. Full note on the ISS-133 card.
- B2 BUG-004 FIXED 2026-08-31 by 5-fixer: **no source change needed — the fix was already
  shipped.** Its real fix site is `backend/app/api/run_stream.py:465-468` (session-less
  generator `ScopedStore`) + `backend/app/models/database.py:19-33` (Postgres pool sizing),
  NOT the `run_commands.py` this batch's fix-site column names — BUG-004 landed in the B2
  batch only because `run_commands.py` is in its card globs (as a SAFE sibling it must not
  touch). Both hunks verified present; landed 2026-07-16 as `e0e4c3e88` (red test) →
  `d418b5a1f` (primary) → `56deb5c46` (secondary). Oracle re-run:
  `backend/tests/unit/test_run_stream_pool_leak.py` → 3 passed;
  `tests/agents/test_wire_parity.py` → 5 passed (INV-3). No xfail marker on that file —
  it was authored red before the fix, so it is green-today by construction. Recorded as
  [FIX-440](../../../.knowledge/cards/20260831-2002-FIX-440.md).
- B2 ISS-144 TESTED 2026-08-31 by test-writer: already fixed. `run_commands.py:358`
  (`submit_answers`) already calls `_review_gate_run_is_terminal(run_id)` -> 409
  `pipeline_not_running`, tagged `# API-001 (task.md R-05)`, landed in a prior
  bug-hunter batch commit (`d027f16bd`). `backend/tests/unit/test_rest_answers_cancel.py::test_answers_on_a_terminal_run_is_rejected`
  (5 parametrized statuses) + `test_answers_on_a_non_terminal_run_still_succeeds`
  already cover this contract and pass today (ran offline: 5 passed). No new xfail
  test written — code is already correct, so an xfail would XPASS. Card status ->
  `fixed`, verification -> `passed`, pointing at the existing test file.
- B1 ISS-471 FIXED 2026-08-31 by 5-fixer: `admin.py::update_user_tier` / `update_user_role`
  built the 200 body from `_to_response(user, db)` AFTER `db.refresh(user)`, so a concurrent
  admin write landing between this request's own `commit()` and `refresh()` was echoed back as
  this request's own privilege change. Both returns now override the mutated field with the
  request's committed value (`backend/app/api/admin.py:330, 398`). Fix card FIX-441; test
  `backend/tests/unit/test_admin_privilege_race.py` 2 XPASS(strict) (the pass signal, xfail
  markers left for 6-verifier). Pure `.py` change — `--reload` picks it up, no restart.
- B4 `run_stream.py`: SSE transport. ISS-105 (uvicorn cancels SSE tasks, shutdown
  snapshot races), ISS-137 (pipeline_reconnected dead frame), BUG-015-016 (completed
  run reconnects forever). All correctness-critical SSE behaviour.
- All backend batches tagged sonnet — a wrong edit in any of these API files is a
  runtime regression.

- B2 ISS-154 TESTED 2026-08-31 by test-writer: defect is actually FRONTEND
  (`frontend/src/components/preview/PreviewPanel.tsx`, `handleSelectVersion` /
  `versionViewFrom`), not `run_commands.py` — the row's fix-site column is the
  batch's shared default, not this card's real location. Root cause confirmed:
  the terminal-empty affordance (`showFailureAffordance`) gates on
  `reopenedRunStatus` (failed/cancelled/degraded), which reflects the FAMILY's
  currently-loaded run, not the per-version run `handleSelectVersion` fetches —
  so an older COMPLETED revision with a NULL `.output` falls through to the
  generic "Output will appear here" placeholder instead of a distinct message.
  Test: `frontend/src/components/preview/PreviewPanel.olderVersionNullOutput.test.tsx`,
  `it.fails(...)`, observed red today (asserts the placeholder is NOT shown for
  a null-output older version; currently it is).

Collision note: each batch owns ONE API file. Secondary globs into engine.py
(domain 15), frontend hooks (domain 11) — never edit those.
Status: the `status` column above is authoritative — it is what the line
reads and writes. A whole-file status could only drift from it.
