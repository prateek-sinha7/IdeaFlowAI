# Domain 15 — backend·other

31 cards, 4 batches.

| card | status | batch | round | fix site | tier | phases | model |
|---|---|---|---|---|---|---|---|
| ISS-096 | CLOSED (already-resolved — od-ppt-validator path doesn't exist on this branch; test passes 14/14; not reproducible) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: not reproducible, `test_od_ppt_validator_deck_reemission_contract` passes today (14/14 in file); card's `od-ppt-validator` path doesn't exist, only `ppt-validator`; no red to pin a test to |
| ISS-098 | CLOSED | B1 | 1 | `backend/agents/capabilities/strategies/wave_scheduler.py` | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 5-fixer: FIX-425, narrow `except asyncio.CancelledError` arm at wave_scheduler.py:346 flips the row `cancelled` and re-raises (NOT `except BaseException` — that would swallow GeneratorExit at the `yield` in the same try); test_wave_scheduler_cancel_row.py XPASS(strict), marker left for 6-verifier; test_wave_scheduler 10/10, test_restart_resume 69/69, test_fanout_cancel 9/9, lint-imports 3 kept/1 broken (baseline), SC-001 grep 0 hits |
| ISS-101 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) |
| ISS-106 | CLOSED | B1 | 1 | `backend/app/agents/checkpointer.py` | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 5-fixer: FIX-428, `pool_to_close.close()` now runs under `asyncio.wait_for(settings.SHUTDOWN_CHECKPOINTER_CLOSE_SECONDS=1.0)` inside `close_checkpointer()` (the one function all callers route through), with an `except asyncio.TimeoutError` arm ahead of the broad one; new setting added to the config.py budget arithmetic (24s → 25s) and to `_teardown_budget_seconds()`; test_checkpointer_shutdown_budget.py XPASS(strict) — marker left for 6-verifier; test_shutdown_reachability.py 6/6, test_run_shutdown.py 9/9, lint-imports 3 kept/1 broken (baseline), SC-001 grep 0 hits — 6-verifier: re-ran test_checkpointer_shutdown_budget.py (XPASS strict confirmed, xfail marker removed, re-run plain green 1 passed), test_shutdown_reachability.py 6/6, test_run_shutdown.py 9/9, lint-imports 3 kept/1 broken (identical baseline contract), backend :8000/docs 200. No UI-reproducible repro exists for this defect (process-teardown-only, code-review-found per ISS-106 card's Source/Evidence, no register browser steps) — verification is by the reachability suite, which per FIX-428's card drives a real uvicorn subprocess + real SIGTERM |
| ISS-118 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 5-fixer: FIX-429, `_CONSTRUCTS_BUT_NEVER_INVOKES` emptied to `{}` (backend/tests/conftest.py:206) — all 9 exempted tests now stub their own construction seam instead of disarming the ISS-102 guard by nodeid. Three seams: new non-autouse `stub_provider_classes` fixture in conftest MagicMocks ChatAnthropic/ChatBedrockConverse on their SOURCE modules for the 5 `SmartPlanner._build_llm` sites (test_brief_max_chars ×2, test_iss056 TestH3SmartPlannerDesignNote ×3); `_RecordingBedrock.model_construct(...)` in test_cached_invoke.py::_make_recording_bedrock — the shared helper all 3 sites route through — keeps isinstance real while ChatBedrockConverse.__init__ (boto client) never runs; local `_NeverInvokedChatModel()` replaces the `"anthropic:..."` string handed to create_deep_agent in test_mcp_client.py. No production source changed. test_iss118_construct_only_exemptions.py XPASS(strict) — marker left for 6-verifier; test_iss102_live_model_guard 6/6, test_brief_max_chars 4/4, test_cached_invoke 11 passed + 1 XPASS (that XPASS is ISS-120/FIX-427's, not this change), test_iss056 36/36, test_mcp_client 5/5; lint-imports 3 kept/1 broken (baseline 923b0d4fd), SC-001 grep 0 hits |
| ISS-120 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: `backend/tests/unit/test_cached_invoke.py::test_cached_invoke_supports_per_call_cache_override`, xfail(strict) — observed red today: `TypeError: cached_invoke() got an unexpected keyword argument 'enable_cache'` (no per-call override exists, only the process-wide `BEDROCK_PROMPT_CACHE_ENABLED` flag) — 5-fixer: FIX-427, `enable_cache: bool = True` keyword-only opt-out on `cached_invoke` (`app/agents/cached_invoke.py:129`) gating the cache point at `:184` (`cc = _bedrock_cache_control(llm) if enable_cache else None`) — placed in the shared helper all 5 direct-invoke callers route through (smart_planner:405, clarify_engine:554, classifier:50, test_agent:143, compliance_agent:159), NOT in `_bedrock_cache_control` (would break its INV-12 'identical to the middleware' contract). Opt-OUT only: the global flag stays the master kill switch, every existing caller byte-identical. test_cached_invoke.py 11 passed + 1 XPASS(strict), marker left for 6-verifier; lint-imports 3 kept/1 broken (baseline, no new imports); SC-001 grep 0 hits. Middleware half (`deep_agent_runner.py:283`) deferred as ISS-640 — it needs a manifest-declared per-step flag (ISS-120 shape (b)), a product decision the card leaves open. — **Verified:** 6-verifier — `pytest tests/unit/test_cached_invoke.py` XPASS(strict) confirmed (11 passed + 1 xfail-strict fail), xfail marker removed, re-run 12/12 plain green; manually confirmed `inspect.signature(cached_invoke)` now exposes keyword-only `enable_cache: bool = True` (the original `TypeError: cached_invoke() got an unexpected keyword argument 'enable_cache'` no longer reproduces); backend `/docs` 200; `lint-imports` from `backend/` unchanged at 3 kept/1 broken (pre-existing, unrelated to `cached_invoke.py`); frontend `npx tsc --noEmit` clean (no UI surface touched by this fix). |
| ISS-130 | CLOSED (already-resolved — hazard closed upstream by KRN-005 scratch_lock, landed 7a8530627; both witness tests pass today) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: not reproducible, hazard already closed upstream by KRN-005's full-invocation `scratch_lock` (context.py:489, kernel_services.py:1677-1724, landed 7a8530627); `test_scratch_lock_fanout_race.py`'s two tests already pin the invariant and both PASS today, no red to xfail |
| ISS-132 | CLOSED (FIX — Option A: invocation_gated=False added at task_loop.py run_agent call site; mirrors ISS-097/FIX-242 fan-out pattern; SC-001 0 hits, AST clean) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: card explicitly names this a product decision, not a defect with one right answer (task_loop's N sequential per-task gates fire correctly and visibly, just more often than a user may expect); card itself lists three mutually exclusive fix shapes (suppress via invocation_gated=False, gate only final task, or leave as-is and reword gate-picker copy) with no chosen resolution, and FIX-424 explicitly declined to touch it for the same reason. No single "correct behaviour" exists to assert against, so no xfail test was written — doing so would mean the test-writer picking the product decision, not proving one |
| ISS-156 | CLOSED (already-resolved — mismatch not present on this branch; all 3 named test files green today) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: not reproducible, all three named files green today (`test_run_commands_fix218.py` 24/24, `useChatAttachments.test.ts` 6/6, `ChatAttachments.test.tsx` 3/3); the "asserts `## Attached files`, code emits `PRIORITY OVERRIDE`" mismatch is not present on this branch, no red to pin a test to |
| ISS-185 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: `backend/tests/unit/test_skill_staging.py::test_staged_skill_carries_its_referenced_sibling_files`, xfail(strict) — observed red: staging the real `systematic-debugging` catalog skill (whose SKILL.md body says "See `root-cause-tracing.md` in this directory") never copies that sibling file into the sandbox next to SKILL.md; `stage_skills` writes only the synthesized SKILL.md, confirming the ppt-specific card's root cause generically — 5-fixer: FIX-431, `stage_skills` now `shutil.copytree`s the catalog folder (`skills/global/<id>/`, SKILL.md excluded) into the staged skill dir (skill_staging.py:190) so a body's skill-relative sibling reference resolves at the mount point the agent was told to use; new public `skill_source_dir()` in skills_catalog.py:92 keeps the folder layout in the catalog module (NOT a `GlobalSkillEntry` field — app/api/skills.py:38 `asdict`s entries and would leak the host path). Copy runs AFTER `staged.append` in its own try, so a copy failure records an error and leaves the skill staged. test_skill_staging.py 10 passed + 1 XPASS(strict), marker left for 6-verifier; regressions green: test_create_runner 22, test_composer_skills_delivery 9, test_skill_directive_block 4, test_skill_prompt_baseline 2, test_skills_text_only_deliverable 2, test_skills 26, test_skill_content_guard 11, test_skills_catalog_hygiene 6, test_skills_catalog_compatible_agents 6, test_engine_skill_resolution 4; lint-imports 3 kept/1 broken (baseline), SC-001 grep 0 hits. ppt/workflow.yaml left untouched — `html-ppt` is not in `backend/skills/global/` on this branch, so its `injects:` workaround stays — **Verified:** 6-verifier — `pytest tests/unit/test_skill_staging.py` XPASS(strict) confirmed (10 passed + 1 xfail-strict fail), xfail marker removed, re-run 11/11 plain green; manual repro by hand (no browser surface — this is a backend-internal staging path) — invoked `stage_skills` on the real `systematic-debugging` catalog skill via a standalone script: staged dir now contains `root-cause-tracing.md` plus every other sibling file (`condition-based-waiting.md`, `defense-in-depth.md`, etc.) alongside `SKILL.md`, where before only `SKILL.md` existed; regressions green: test_create_runner 22, test_composer_skills_delivery 9, test_skills_catalog_hygiene 6, test_engine_skill_resolution 4 (41/41); lint-imports from `backend/` unchanged at 3 kept/1 broken (baseline); backend `/docs` 200; frontend `npx tsc --noEmit` clean |
| ISS-357 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: `backend/tests/unit/test_register_password_echo_fixture_mounts_real_app.py::test_password_echo_harness_observes_app_mains_redaction_fix`, xfail(strict) — observed red today: bare-`FastAPI()`+`auth_router` harness (mirrors test_register_password_echo.py's fixture) falls back to FastAPI's default `RequestValidationError` handler, which still embeds `short7!` in `detail[].input`; app.main's FIX-332 redacting handler is never mounted on this object — 5-fixer: FIX-430, both fixtures (the ISS-257 test and its ISS-357 mirror) now do a late `from app.main import app` instead of `FastAPI()` + `include_router(auth_router)`, and the client is NOT entered as a context manager (`TestClient(app)`, not `with …`) so the lifespan — and `restore_non_terminal_runs()` against the real DB — never runs; orphaned `FastAPI` / `auth_router` imports removed, assertions and xfail markers untouched. test_register_password_echo_fixture_mounts_real_app.py XPASS(strict) (was `1 xfailed`) and test_register_password_echo.py XPASS(strict) — markers left for 6-verifier; the two files plus test_login_timing_and_pii.py together 2 failed (both XPASS) / 5 passed; harness now discriminating (handler popped off the real app → `'input': 'short7!'` reappears). lint-imports 3 kept/1 broken (baseline), SC-001 grep 0 hits. — **Verified:** 6-verifier — `pytest tests/unit/test_register_password_echo_fixture_mounts_real_app.py` XPASS(strict) confirmed, xfail marker removed, re-run 1/1 plain green; `test_register_password_echo.py` XPASS(strict) (ISS-257's own marker, untouched — not this card's), `test_login_timing_and_pii.py` 5/5; manual repro — `POST /api/auth/register` with `short7!` against the live backend returns 422 with no `input` key (matches card's documented FIX-332 behaviour); backend `/docs` 200 (no restart needed, test-fixture-only change); `lint-imports` from `backend/` unchanged at 3 kept/1 broken (pre-existing); frontend `npx tsc --noEmit` clean (no UI surface touched). |
| ISS-630 | CLOSED (ISS-630 — integration test rewrite: added _stub_bedrock_construction autouse fixture; removed analyzer_solution kwarg assertions; +1 agent_count → len(rev_agents); revision_analyzer_complete removed; 9 xfail markers removed; AST clean; 0 production files changed) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent)
| ISS-631 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: pre-existing tests already reproduce the roster/manifest drift for all 3 revision pipelines; marked the exact failing parametrize cases `@pytest.mark.issue("ISS-631")` + xfail(strict) in test_registry.py (6 cases), test_manifest_coverage.py (3), test_id_alias_resolver.py (3), test_wire_parity.py (1 — prototype_revision only), test_characterization_prototype_revision.py (1); all 5 files re-run green (xfailed, not error) — 5-fixer: FIX-433, `prototype-revision-analyzer/AGENT.md:13` swapped its private `pipeline_type: prototype_revision_analyzer` (a leftover from when it was an app-layer pre-pipeline agent) for the three real revision types, so `list_agent_ids` finally returns the manifest steps; `order` re-spaced to open a slot at 2 (analyzer stays 1; revision-agent/planner/feature-specify 1→2, prototype-plan 2→3, analyze 3→4, build 4→5, validate 5→6) — relative order byte-identical everywhere, `order` is only ever a sort key. Root cause is named verbatim in the production workaround at engine.py:7942; no engine/loader/registry code changed, the fix is data. Two `prototype_revision` goldens were separately clobbered by merge 5d9885150 taking 732adc245's `agent_count: 2` side over e0f6beeed's correct 3 — 6 goldens regenerated with SNAPSHOT_UPDATE=1 and re-CRLF'd so the diffs show only the semantic delta (1 changed entry each for prototype/large/feature; 16→21 entries for prototype_revision). All 14 ISS-631 xfail(strict) cases XPASS — test_registry.py 6F(XPASS)/19P, test_manifest_coverage.py 3F(XPASS)/28P, test_id_alias_resolver.py 3F(XPASS)/113P/2xf(ISS-635), test_wire_parity.py 1F(XPASS)/4P, test_characterization_prototype_revision.py 1F(XPASS)/1P — markers LEFT for 6-verifier. **6 stale assertions re-pointed, NOT weakened** (flagged for review): test_registry.py::TestRevisionPipelineRosters ×3 hardcoded rosters, test_registry_discovery.py pinned count 2→3, and test_revision_analyzer.py's `frontmatter_pipeline_type` + `revision_agent_order_is_two` — all six froze the pre-e0f6beeed design that the card's own witness tests contradict. Regressions green: test_loader 97, test_registry_helpers 27, test_registry_discovery 10, test_revision_analyzer 20, test_manifest_parity 54P/2xf, test_compiled_plan_runs 27, test_restart_resume 69, test_routing_parity 5, test_phase3_cutover_verify 6, test_entitlement_parity 51P/34S, test_workflows_api 43P/1xf, test_phase6_frontend_consistency 16P/1xf, characterization prototype/large/feature 2/2 each. lint-imports 3 kept/1 broken (identical to 923b0d4fd), SC-001 grep 0 hits, no migrations. Side effect: test_phase5_revision_validation.py::test_fix_message_targets_regression_not_baseline_and_reinjects_instruction now XPASSes for ISS-638 — marker left, ISS-638 NOT claimed here | **Verified (6-verifier):** re-ran all 5 witness files individually — test_registry.py XPASS(strict)x6 confirmed (6 failed/19 passed), test_manifest_coverage.py XPASS(strict)x3 confirmed (3 failed/28 passed), test_id_alias_resolver.py XPASS(strict)x3 confirmed (3 failed/113 passed/2xf ISS-635), test_wire_parity.py XPASS(strict)x1 confirmed (1 failed/4 passed), test_characterization_prototype_revision.py XPASS(strict)x1 confirmed (1 failed/1 passed) — `@pytest.mark.issue("ISS-631")` xfail markers removed (issue markers kept), re-run plain green: test_registry.py 25 passed, test_manifest_coverage.py 31 passed, test_wire_parity.py 5 passed, test_characterization_prototype_revision.py 2 passed; test_id_alias_resolver.py 116 passed/2 failed (the 2 failures are pre-existing unrelated ISS-635 XPASS(strict) on `playwright_smoke_test`, not touched, not ISS-631's). Manual repro: no browser UI surface for this defect (data-only AGENT.md pipeline_type fix, sibling of ISS-633's note); re-ran `list_agent_ids` directly for all 3 revision pipelines and confirmed `prototype-revision-analyzer` now leads each roster, matching workflow.yaml. Health: backend `:8000/docs` 200 (no restart needed — pure `.py`/`AGENT.md` data fix, `--reload` already live), `npx tsc --noEmit` clean, `lint-imports` (from `backend/`) unchanged at 3 kept/1 broken (identical baseline contract). Regression: `test_registry_discovery.py` 10/10 (the file whose pinned count 2→3 the fixer's note flagged for review) re-confirmed green.
| ISS-632 | CLOSED (ISS-632 — test harness rewrite: removed [:2] roster slice from _drive_engine + _drive_execute; updated count assertions to len(specs); ADR-0008 intentional; AST clean across 2 files) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: pre-existing `backend/tests/unit/test_pipeline_failure_semantics.py` (5 tests) + `backend/tests/agents/test_engine_runner_error_arm.py` (2 tests) already reproduce the defect verbatim (`assert 5 == 1`, `assert 6 == 2`, extra `workflow_validated`/`agent_input`/etc. events riding alongside pipeline_failed); marked all 7 `@pytest.mark.issue("ISS-632")` + `@pytest.mark.xfail(strict=True)`; re-run confirms 5 xfailed / 2 xfailed (6/6 in second file, 4 unrelated tests still pass) — 5-fixer: ESCALATED, no application defect exists. Root cause is the ADR-0008 roster seam at `engine.py:1720-1723` (`_roster_is_partial`, landed `bab17c0b9` with ppt_v2): a caller roster that is a STRICT SUBSET of the compiled plan is filled in from the plan. Both harnesses slice `get_pipeline_agents("user_stories")[:2]` — a strict subset of the 6-step plan — so the engine correctly runs all 6 and every count/`fail_ids` is computed against the stale 2-agent slice (`assert 5 == 1`/`6 == 2` are `agents_completed`; the 0-`pipeline_failed` case is 4 of 6 completing, i.e. a partial not a collapse). `workflow_validated` is NOT a new event — it is the unconditional Step-1 DAG event at `engine.py:1904` since `4889e3a85`, only visible because the failing assert dumps the whole `events` list. Probe with the SAME stubs + SAME assertions driving the full 6-agent roster, ZERO app/test edits: all-fail → 1 `pipeline_failed`, 0 complete, state `failed`, completed 0/6, `error="no agent completed"`; 1-of-6 → `degraded`, `agents_failed=['domain-analyst']`, 5/6; clean → NEITHER `status` NOR `agents_failed`, 6/6; recovered timeout → neither key, 6/6. Every F3 contract (a/b/c + WR-05) holds in production today and the card's "clean run carrying degraded keys" worry is disproved. Honouring a subset roster would revert ADR-0008 and re-break ppt_v2 (partial `get_pipeline_agents("ppt_v2")` → DAG resolver rejects the run); loosening the counts is bending the test to the code. Resolution is a harness rewrite (4-test-writer): drop the `[:2]` at test_pipeline_failure_semantics.py:104 and test_engine_runner_error_arm.py:67, derive `fail_ids`/`error_ids` + expected counts from the full roster. ISS-632 card updated in place with root cause + evidence and linked to ADR-0008 both ways; no FIX card minted; no source or test file touched |
| ISS-633 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: all 6 named tests reproduce today across the 4 files, each marked `@pytest.mark.issue("ISS-633")` + xfail(strict); `test_revision_intelligence.py` 17/19 pass, 2 xfailed (exact-kind ref count 0 not 1); `test_run_revision_fe_contract.py` 2 xfailed (`list index out of range` in `state_restoration_failed`); `test_concierge_proposal_channels.py` 1 xfailed (mints `prototype_large_output` not `prototype_output`); `test_revision_gating.py` 1 xfailed (stray `.uploads/manifest.json` in `runner.reads`) | — 5-fixer: FIX-432, one line — `producer_agent=agents[-1].id` at engine.py:8058 read the roster `_handle_revision` deliberately empties (`agents: list = []`, engine.py:7962, so execute() rebuilds it from the manifest via _specs_from_plan); the IndexError was swallowed by the write block's own `except Exception` into `state_restoration_failed {'error': 'list index out of range'}`, so NO revision ever persisted its FR-014 chain-link-1 exact-kind ref. Now reads `_compiled_revision.steps[-1].agent_id` (the same compiled plan execute() rebuilds from) — fixed at the one chokepoint all 3 revision entry points share (WS run_revision / REST /runs/{id}/revisions / Concierge, all via run_commands.py:3835). 4 of 6 witnesses XPASS(strict), markers left for 6-verifier: test_revision_intelligence.py 2 XPASS/17 passed, test_run_revision_fe_contract.py 2 XPASS. Regressions green: test_rest_revisions 15, test_revision_file_editing 6, test_runs_api_artifacts 8, test_characterization_prototype_{revision 1+1xfail(ISS-631), large_revision 2, feature_revision 4}, test_phase5_revision_validation 6+2xfail. lint-imports 3 kept/1 broken (baseline 923b0d4fd), SC-001 grep 0 hits, no imports added, no migration. The other 2 witnesses (test_concierge_proposal_channels TestRevisionDisposal, test_revision_gating test_declared_revision_with_parent_seeds_after_assert_owns) are STALE HARNESSES asserting the pre-revision-pipeline-refactor contract — split out as ISS-642 for 4-test-writer, NOT bent to pass; both files still green (18+1xfail / 6+1xfail) | **Verified (6-verifier):** `test_revision_intelligence.py` XPASS(strict)x2 confirmed (17 passed/2 xfailed) then xfail markers removed, re-run 19 passed plain green; `test_run_revision_fe_contract.py` XPASS(strict)x2 confirmed (2 failed/0 passed) then xfail removed, re-run 2 passed plain green; `@pytest.mark.issue("ISS-633")` kept on all 4. `test_concierge_proposal_channels.py` (18 passed/1 xfailed) and `test_revision_gating.py` (6 passed/1 xfailed) re-confirmed untouched — ISS-642 scope, left xfailed. Regression: `test_rest_revisions.py` 15 passed. `lint-imports` (from backend/) 3 kept/1 broken, same 2 edges as baseline. `npx tsc --noEmit` clean. `:8000/docs` 200, no restart needed (pure engine.py fix, no reload required for this check). No frontend surface to this bug — manual repro is the real-dispatch unit tests themselves (scripted models, real `execute()` path, real DB writes via StaticPool SQLite), which is what the register's "reproduction" actually consists of; no browser UI steps exist for this defect
| ISS-634 | CLOSED (ISS-634 — test harness rewrite: _drive_single_agent now passes compiled_override with single-step plan, bypassing ADR-0008 _roster_is_partial expansion; 4 xfail markers removed; AST clean) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: pre-existing `backend/tests/agents/test_chunk_sanitizer.py` already reproduces all 4 named symptoms; marked `test_split_tool_xml_untouched_for_tooluse_agent`, `test_clean_stream_passes_through_unchanged_for_toolless_agent`, `test_lone_lt_and_html_at_boundaries_chunk_identical_for_toolless_agent`, `test_benign_held_tail_survives_flush_no_content_loss` `@pytest.mark.issue("ISS-634")` + xfail(strict); re-run 3 passed, 4 xfailed (whole file green) — 5-fixer: **card root cause is WRONG, no code changed.** `_ChunkStreamSanitizer` driven directly returns all 4 cases byte- AND chunk-identical (no content loss, no perturbation of a clean stream). Real cause: `engine.py:1720-1722` `_roster_is_partial` (added 2026-08-27 `bab17c0b9` for ppt_v2) replaces the harness's `agents=[spec]` strict-subset roster with the FULL compiled plan, so `_drive_single_agent` runs 6 (`user_stories`) / 3 (`prototype_revision`) agents and `emitted` is N copies of the deltas — each per-agent slice matches the expectation exactly. Engine fix = design decision (ppt_v2 is also a strict subset, no signal to key on); test fix = 4 red tests edited with zero prod change on a card that does not say the expectation was misstated. Needs 3-analyzer/4-test-writer re-decision; `execute(compiled_override=…)` is the viable test-only seam. Observed: `backend/tests/agents/test_chunk_sanitizer.py` 4 failed / 3 passed under `--runxfail`, 3 passed / 4 xfailed normally (unchanged). xfail markers LEFT in place. |
| ISS-635 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: pre-existing parametrized tests already reproduce the parity break for `playwright_smoke_test` (planner: skip where parity requires run; clarify.defaults `[]` where the engine dict expects the pinned list); marked the 4 exact failing parametrize cases `@pytest.mark.issue("ISS-635")` + xfail(strict) — `test_planner_run_everywhere`/`test_clarify_defaults_match_engine` in test_manifest_parity.py (54 passed, 2 xfailed) and `test_compiled_planner_is_run_everywhere`/`test_compiled_clarify_defaults_match_engine_dict` in test_id_alias_resolver.py (114 passed, 5 xfailed incl. 3 pre-existing ISS-631) — 5-fixer: FIX-435, the smoke fixture IS genuinely exempt and the exemption is now EXPLICIT where the parity tests already keep every other one: `playwright_smoke_test` unioned into `_PLANNER_SKIP_IDS` (test_manifest_parity.py:150, test_id_alias_resolver.py:302) and pinned as `[]` in both clarify-defaults dicts (test_manifest_parity.py:198, test_id_alias_resolver.py:94), each carrying the same rationale comment `sample_subagents_parallel` and the `ex_A*` family carry — the identical repair ISS-174 got. `agents/workflows/playwright_smoke_test/workflow.yaml` deliberately UNCHANGED: flipping a one-step “under a minute” smoke fixture to `planner: run` + the 4-item `_CUSTOM_DEFAULTS` would satisfy the pins by destroying the only property it exists to have, and it matches every exempt sibling on PROPERTY (user_launchable: true, not run_revision-dispatched, clarify.mode: skip, 1 step) — nothing distinguishes it from ex_A1_loop. All 4 ISS-635 xfail(strict) cases XPASS: test_manifest_parity.py 2 failed (BOTH XPASS)/54 passed (baseline immediately before the edit on the same file: 54 passed/2 xfailed), test_id_alias_resolver.py 2 failed (BOTH XPASS)/116 passed — markers LEFT for 6-verifier. No production source touched, so no backend restart needed. Regressions green: test_compiled_plan_runs 27 passed, test_manifest_coverage 31 passed. lint-imports 3 kept/1 broken (unchanged — no import added or moved), SC-001 grep 0 hits under agents/execution_engine/, no migrations | **Verified (6-verifier):** re-ran both witness files individually — test_manifest_parity.py XPASS(strict)x2 confirmed (2 failed/54 passed), test_id_alias_resolver.py XPASS(strict)x2 confirmed (2 failed/116 passed); xfail markers removed from both `playwright_smoke_test` parametrize cases in each file (`@pytest.mark.issue("ISS-635")` kept), re-run plain green: test_manifest_parity.py 56 passed, test_id_alias_resolver.py 118 passed. Manual repro: `agents/workflows/playwright_smoke_test/workflow.yaml` confirmed unchanged (`planner: skip`, `clarify.defaults: []`), matching the now-explicit `_PLANNER_SKIP_IDS`/clarify-defaults exemption — no browser UI surface for this defect (test-fixture parity fix only, no production source touched). Health: backend `:8000/docs` 200 (no restart needed, no source changed), `npx tsc --noEmit` clean, `lint-imports` (from `backend/`) unchanged at 3 kept/1 broken (identical baseline contract). Regression: test_compiled_plan_runs.py 27 passed, test_manifest_coverage.py 31 passed. |
| ISS-637 | CLOSED | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — 4-test-writer: pre-existing `backend/tests/unit/test_alembic.py::TestAlembicMigrations::test_upgrade_then_check_reports_no_drift` already reproduces the defect; marked `@pytest.mark.issue("ISS-637")` + xfail(strict) — observed red: `alembic.util.exc.AutogenerateDiffsDetected: New upgrade operations detected: [('remove_index', Index('uq_workflow_runs_share_token', ...))]`, i.e. the model declares the unique index but migration 0040 never creates it; re-run confirms 4 passed, 1 xfailed — 5-fixer: FIX-434, **the register paraphrase had the direction backwards** — migration 0040 DOES create the sparse unique index (`0040_workflow_run_share_token.py:44-51`, with a `share_token IS NOT NULL` WHERE clause); the MODEL never declared it, so `Base.metadata` was missing an index every chain-built DB has and autogenerate proposed `remove_index` (verbatim repro re-run by hand before editing). Fixed at the one place all consumers route through — `WorkflowRun.__table_args__` (`app/models/workflow.py:23-32`) now carries the index mirroring 0040 exactly, both dialect WHERE clauses included, following the KAN-131/0030 precedent already in that tuple; `text` added to the existing sqlalchemy import line. **NO migration added or altered** — the index already exists in every chain-built DB, and the other direction autogenerate offered (dropping it) would delete the O(log n) lookup behind `GET /s/{token}`. test_alembic.py `[XPASS(strict)] ISS-637 unfixed` (1 failed/4 passed), marker LEFT for 6-verifier. Regressions green across the `Base.metadata.create_all` consumers: test_migrations 7, test_runs_api_summary 8, test_runs_api_artifacts 8, test_revision_intelligence 19, test_sse_stream 53; `import app.main` clean. lint-imports 3 kept/1 broken (identical 923b0d4fd baseline), SC-001 grep 0 hits, migrations untouched. Flagged not fixed: `test_alembic.py:186-189`'s docstring states the inverse of the real defect — the assertion is correct, only the prose is backwards; left as found since no card asked for it — 6-verifier: test_alembic.py re-confirmed XPASS(strict)->plain green (5 passed) after xfail removed, marker.issue kept; manual repro re-run by hand (fresh sqlite `alembic upgrade head` + `alembic check` outside pytest) shows "No new upgrade operations detected"; source-confirmed uq_workflow_runs_share_token index in WorkflowRun.__table_args__ (app/models/workflow.py); regression test_migrations.py 7 passed; lint-imports 3 kept/1 broken identical baseline; backend 200, import app.main clean |
| ISS-638 | CLOSED (ISS-638 — 6 stale assertions fixed across 5 test files; 0 production files changed; xfail markers removed; AST clean) | B1 | 1 | backend·other (19 scattered) | A′ (ISS-132 sibling of ISS-097) + C | full | **sonnet** (scattered backend, test infra, engine-adjacent) — fixed: (1) test_tool_grant_invariants.py narrowed heuristic from any-tools to fs-write-tools-only so ppt-code-generator's pptx tool no longer false-positives; (2) test_phase5_revision_validation.py agent_start==2→3 + starts list updated for FIX-433 3-step roster; (3) test_text_only_prompt_hygiene.py removed [:2] slice + full roster; (4) test_workflows_api.py prototype.chained_from added ppt_v2 entry; (5) test_phase5 test_fix_message row xfail removed (FIX-437 already cured); (6) test_phase8_live xfail removed (FIX-437 already cured) FIX-437 — `previous_run._seed_existing_artifact` (previous_run.py:308) is re-entered per AGENT (the generic injector calls `provider.load(ectx)` at engine.py:10721, not just at run entry engine.py:2549); step 0 permanently slims the `=== EXISTING PROTOTYPE HTML ===` markers out of `runner.user_message`, so step 1's invocation reads "no markers", takes the Concierge parent-read branch and OVERWRITES the just-edited `prototype.html` with the PARENT run's copy (`edit_file → "String not found in file: '<h1>Dashboard</h1>'"`, deliverable = parent HTML) plus clobbers the `revision_original_html` baseline. Guard: `if getattr(ctx, "revision_original_html", None): return` at the top of the one helper both `load` call sites route through — seeds once per run, first invocation byte-identical, L16 gate untouched. Blast radius = all 7 manifests declaring `context_providers: [previous_run]`. test_phase8_live.py (offline) 1F(XPASS strict)/18P/16S — marker LEFT for 6-verifier; same node `--runxfail` 1 passed. Regressions green: test_context_providers 20P, test_revision_gating 6P/1xf, test_revision_file_editing 6P, test_characterization_prototype_revision 2P, test_phase5_revision_validation 1F(the FIX-433 XPASS)/6P/1xf. SC-001 grep 0 hits, lint-imports 3 kept/1 broken (identical baseline, no import added), no migrations. **ESCALATED because 4 of the 6 rows are STALE TESTS, not defects — the app is deliberately right in each and I will not edit an assertion to make it green:** (1) tool_grant_invariants flags `ppt_v2:ppt-code-generator`, but `tools: [pptx]` is not a filesystem tool set and `ppt_v2/workflow.yaml:74-77` sets `write_files: false` with the reason in a comment — the test treats ANY non-empty `tools:` frontmatter as filesystem, a heuristic written at ec1ea70f5 before `planning`/`playwright`/`pptx`/`prototype_emit_only` existed; (2) phase5 `no_phantom_second_agent` asserts `agent_start == 2` but the roster is 3 since e0f6beeed/FIX-433 made the analyzer step 0 — its real invariant (no extra start from the `:fix` sub-agent) still holds; (3) text_only_prompt_hygiene drives `execute(agents=get_pipeline_agents('user_stories')[:2])` and the ADR-0008 roster seam (engine.py:1720-1722) expands a strict-subset roster back to all 6 steps, so `backlog-compiler` is last — both sanitizer assertions the test exists for PASS; (4) workflows_api pins `prototype.chained_from == {user_stories, ppt}` but `prototype/workflow.yaml:15` authors `ppt_v2` deliberately (bab17c0b9). Re-pointing those four is a decision the card does not carry — needs an owner. 6th row (phase5 TestFixPolicyEndToEnd) was already cured by FIX-433 and passes; its marker is also still on. ISS-638 updated in place with the per-row verdicts (status stays `open`, verification `failed` — 4 rows unresolved); FIX-437 linked both ways to ISS-638 and FIX-309 |
| ISS-079 | ESCALATED | B2 | 1 | `backend/tests/agents/test_phase6_frontend_consistency.py` | A′ (ISS-636 sibling of ISS-617/FIX-415) + C | mixed | haiku | — 4-test-writer: not reproducible, both named tests (`test_prototype_real_ids_and_human_gate`, `test_ppt_and_prototype_counts_reconciled`) pass today (17 items, 1 unrelated failure); the file's only live red — `test_gate_values_match_real_registry[prototype]` — is a different defect already tracked and test-linked by ISS-636, no red left to pin a new test to |
| ISS-636 | CLOSED | B2 | 1 | `backend/tests/agents/test_phase6_frontend_consistency.py` | A′ (ISS-636 sibling of ISS-617/FIX-415) + C | mixed | haiku — 4-test-writer: pre-existing tests already reproduce all 3 named symptoms; marked `test_prototype_endpoint_gate_values` + `test_library_returns_200_with_gate_and_real_prototype_ids` in test_agents_api_real_registry.py, and `test_gate_values_match_real_registry[prototype]` (conditional xfail via `request.node.add_marker`, since it shares a class-level parametrize with a passing sibling test `test_id_set_matches_real_registry` and the `[ppt]` case) in test_phase6_frontend_consistency.py, all `@pytest.mark.issue("ISS-636")` + xfail(strict); re-run confirms 24 passed, 3 xfailed — 5-fixer: FIX-436, root cause `frontend/src/components/workflow/AgentLibraryData.ts:38-40` (the generated mirror still declared `gate: "Human_Gate"` on prototype-specify/plan/analyze after FIX-323 stripped it from the AGENT.md files) → regenerated to `gate: null`; the two API assertions + the frozen frontend-data pin were INVERTED (FIX-415 pattern) to assert the ABSENCE of a static gate, and `test_prototype_real_ids_and_human_gate` renamed `..._and_no_static_gate`; all 3 xfail(strict) cases now XPASS (test_agents_api_real_registry 2 XPASS/8 passed, test_phase6_frontend_consistency 1 XPASS/16 passed), markers left for 6-verifier; guard test_declared_gate_streaming.py 3 passed; SC-001 grep 0 hits, zero imports added, no migrations — **Verified:** 6-verifier re-ran both files: 2 XPASS(strict)+8 passed and 1 XPASS(strict)+16 passed confirmed the pass signal, xfail markers removed, re-run plain green 27/27; source-confirmed `AgentLibraryData.ts:38-40` reads `gate: null` for prototype-specify/plan/analyze; regression guard test_declared_gate_streaming.py 3/3; backend :8000/docs 200 (no restart needed, no engine.py/.py source touched); frontend `tsc --noEmit` clean; lint-imports 3 kept/1 broken unchanged from baseline (923b0d4fd); no live browser repro exists for this defect — `AgentLibraryData.ts` is an unimported/dead mirror file, the fix is entirely test-assertion + dead-data-file drift |
| ISS-173 | ESCALATED | B3 | 2 | `backend/agents/workflows/ex_A3_divert/workflow.yaml` | C | full | haiku — 4-test-writer: not reproducible, both named tests pass today (`test_conditional_divert_v4_validation.py` 1/1, `test_conditional_human_gate_source_t26.py` 2/2); both files already assert the current `language`/`spanish`/`dutch` fixture shape, no red left to pin a test to |
| ISS-174 | ESCALATED | B3 | 2 | `backend/agents/workflows/ex_A3_divert/workflow.yaml` | C | full | haiku | — 4-test-writer: not reproducible, option 1 (exempt the `ex_*` family) already applied upstream in `test_manifest_parity.py`'s `_PLANNER_SKIP_IDS`/`_ENGINE_PIPELINE_DEFAULTS` and their `test_id_alias_resolver.py` twins; all 14 + 28 `ex_A*` parametrize cases pass today (168 passed/7 xfailed across both files, the 7 xfails belong to ISS-635), no red to pin a test to |
| ADR-0002 | CLOSED (Option 3 accepted 2026-09-03 — pipeline_type is the permanent on-wire/on-disk name; DB column renames non-additive, blocked by additive-only migrations invariant; vocabulary gate converted from xfail to skip) | B4 | 4 | `backend/agents/execution_engine/engine.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) |
| FIX-BUGFIX-NESTED-REVISION | CLOSED | B4 | 4 | `backend/agents/execution_engine/engine.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) |
| ISS-072 | CLOSED | B4 | 4 | `backend/agents/execution_engine/engine.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) — verified: 3 test files re-confirmed green (XPASS->pass, xfail removed), source-confirmed suppress_review_gate publish/honour/restore live at engine.py:6811/6240/6864, backend 200 (prior outage recovered), lint-imports/tsc/test_restart_resume.py (69/69) all clean; see verifier note |
| ISS-090 | CLOSED | B4 | 4 | `backend/agents/execution_engine/engine.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) — verified: test_redoable_eligibility_fence.py XPASS(strict)->plain green (xfail removed), test_redo_gate_safety.py 7/7, test_gates.py 46/46, lint-imports 3 kept/1 broken identical to 923b0d4fd, tsc clean, backend 200; source-confirmed no `redoable=True` literal remains at any of the 3 gate sites, `_redoable` predicate + enforcement present (engine.py:4537,4659,5800,7211,8380) |
| ISS-094 | CLOSED (already-resolved — fixed upstream in engine.py, not reproducible) | B4 | 4 | `backend/agents/execution_engine/engine.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) — already fixed upstream, no red reproducible; see card note |
| ISS-125 | CLOSED | B4 | 4 | `backend/agents/execution_engine/engine.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) |
| ISS-131 | CLOSED | B4 | 4 | `backend/agents/execution_engine/engine.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) |
| ISS-402 | CLOSED | B4 | 4 | `backend/agents/factory.py` | C | full | **sonnet** (engine.py, 8 cards, load-bearing) |

Notes:
- B1 is the "long tail" — 19 backend cards across test infra, capabilities,
  planner, checkpointer, factory, model catalog, and recently-opened test failures
  (ISS-630 through ISS-638). Many are stale-test or test-fixture drift. The high
  count is why it's sonnet — each is small but there are 19 files to touch.
- B4 `engine.py` — the CRITICAL batch. 8 cards in the busiest, most load-bearing file
  in the runtime. ADR-0002 (vocabulary rename), ISS-072 (redundant in-pass analyze gate),
  ISS-090 (hardcoded redoable=True), ISS-094 (hand-rolled double drift),
  ISS-125 (_stamp_resume_marker unbounded read + non-retrying append),
  ISS-131 (declared human gate silently deduped away on a fan-out step), ISS-402 (_inject_constitution
  full injection every run), FIX-BUGFIX-NESTED-REVISION (deferred nested-revision
  defect). **Every edit must re-run its test and confirm SC-001 + import-linter.**
- B3 workflow yaml fixes (step-id renames, planner/clarify defaults).
- ADR-0002 ESCALATED (5-fixer, round 4). Not fixable inside its 4 governed
  globs. `pipeline_type` / `pipeline_run_id` / `SUPPORTED_PIPELINE_TYPES` /
  `PIPELINE_AGENTS` are public identifiers, not local names: 94 AGENT.md
  frontmatter keys, 122 backend .py + 93 frontend .ts/.tsx files, the WS/SSE
  event payload key (`engine.py:134`), and 3 DB columns (`base_pipeline_type`,
  `overrides_pipeline_type`, `pipeline_output`). The column renames are NOT
  additive -> violates the migrations invariant. The ADR itself is
  `status: deferred` and its own text accepts "a breaking wire-protocol rename
  and a coordinated frontend/backend release" — a release decision, not a
  fixer's. Needs a human go/no-go before any code moves.

- ISS-402 FIXED by FIX-423 (5-fixer, round 4). Fix site is `backend/agents/factory.py`,
  NOT engine.py — the fix-site column above is corrected in place. ISS-402's own
  "Where the fix belongs" section names `_inject_constitution` (factory.py:673) as the
  one place every governed agent's Constitution block is assembled; the engine only
  pre-warms the value (engine.py:1821) and needed no edit. A `_CONSTITUTION_MAX_CHARS
  = 32_000` ceiling plus a truncation marker now bounds the injected block. Two
  heads-ups for the verifier: (a) this card does NOT serialize against the other B4
  cards — factory.py is owned by no domain register (`grep -rn "agents/factory.py"
  .kiro/bug-fix-workflow/domains/` is empty), so it did not collide with the engine.py
  work; (b) `bug-hunter/OPEN-ISSUES.md:201` still lists ISS-402 as `open` — that file
  is not this line's register and was left untouched.

- **ISS-402 Verified CLOSED (6-verifier).** `backend/tests/agents/test_constitution_size_guard.py`
  confirmed `[XPASS(strict)] ISS-402 unfixed` (`1 failed, 1 warning`) before the change; `xfail`
  marker removed, re-run -> plain green (`1 passed`, `@pytest.mark.issue("ISS-402")` kept).
  Regressions re-run one file at a time, both green: `test_constitution_prod.py` (3 passed),
  `test_guardrails.py` (13 passed). `lint-imports` (run from `backend/`) unchanged at 3 kept / 1
  broken, same two edges as pre-change. `npx tsc --noEmit` clean. `:8000/docs` 200 (no restart
  needed — pure `.py` fix, `--reload` already picked it up).
  **Manual repro (browser, lane4, qa-admin):** the frontend Settings > Constitution tab now
  enforces FIX-364's 4000-char cap, so an oversized value can no longer be *saved* through the
  UI — expected, since that gap is what FIX-364 already closed. To exercise the same DB state
  the card describes (an oversized value already on a user's row, e.g. from before FIX-364 or a
  direct API write), PUT `http://localhost:8000/api/settings/constitution` with an 80,000-char
  body directly (200 OK, confirming the store still accepts up to its real 1,048,576-char
  ceiling). Launching a live pipeline run (`Spanish Greeter`, 1 agent) failed at the model call
  ("The model rejected this request" — a Bedrock-side rejection, not this fix; the run's
  `input_prompt` field only records the user brief, not the composed system prompt, so it does
  not evidence the guard either way). To confirm the fix on the exact production path instead:
  called `agents.workflow_memory.get_workflow_memory().get_constitution(user.id)` for the real
  qa-admin row (now holding the 80,000-char value just saved) followed by the real
  `agents.factory._inject_constitution`, both unmodified production code — result: composed
  block `32,249` chars, containing `[... Constitution truncated — 48000 chars omitted ...]`,
  matching `_CONSTITUTION_MAX_CHARS = 32_000` exactly. Constitution restored to its original
  44-char value afterward. Screenshot:
  `bug-hunter/evidence/ISS-402/after-verified-constitution-restored.png`. No new console errors
  on `/settings/constitution`.

- ISS-131 CLOSED by FIX-424 (5-fixer, round 4; verified 6-verifier).
  - **Root cause:** `engine.py:2815`/`:3043` passed `inline_gated=self._should_gate(spec, ectx)`
    into `_evaluate_gates`. `_should_gate` answers "is this AGENT selected", not "will the
    inline `_run_agent` -> `_run_review_gate` path actually run". After FIX-242 a fan-out
    step's invocations are all `run_fanout` children carrying `invocation_gated=False`, so
    the inline gate never fires and the stale `True` made the dedupe skip the step's
    DECLARED `gates:[human]` too -- no gate at all.
  - **Fix:** `runs_agent_inline` on the `ExecutionStrategy` port (`capabilities/base.py`),
    `False` on `fanout_batch` and `wave_scheduler`; `engine._strategy_runs_agent_inline`
    resolves the capability and reads the attribute generically, ANDed into both
    `inline_gated` reads. No strategy-name branch in the kernel (SC-001 guard
    `test_banned_patterns.py` green, 18 passed). No new imports; lint-imports unchanged at
    3 kept / 1 broken (both broken edges present at 923b0d4fd).
  - **Verified:** `test_composed_fanout_declared_gate.py` xfailed before, XPASS(strict)
    after; the xfail marker is LEFT IN PLACE for the 6-verifier. Regressions green:
    test_gates (46), test_registry_capabilities (98), test_fanout (24), test_strategies
    (11), test_wave_scheduler (10), test_composed_fanout (6),
    test_conditional_human_gate_source_t26 (2).
  - **Heads-up for the verifier:** the register's batch note called ISS-131 "stale
    line-number citations in comments" -- that was wrong and has been corrected above;
    the ISS-131 CARD is the authority. `wave_scheduler` was cured by the same edit
    (same spawn-only shape) although only fan-out was reported.
  - **6-verifier Verified (2026-08-31):** re-ran `test_composed_fanout_declared_gate.py`
    with xfail still in place -> `1 failed` on `[XPASS(strict)] ISS-131 unfixed`, confirming
    the fixer's XPASS signal directly. Removed the `xfail` marker (kept `@pytest.mark.issue`),
    re-ran -> `1 passed`. Regression files re-run one at a time and all green as reported:
    test_gates (46), test_composed_fanout (6), test_conditional_human_gate_source_t26 (2),
    test_fanout (24), test_registry_capabilities (98), test_strategies (11),
    test_wave_scheduler (10), test_banned_patterns (18). `lint-imports` (run from `backend/`)
    confirms 3 kept / 1 broken, same broken contract, and `git diff --stat` on the four
    touched files shows zero import-line changes. `python3 -c "import app.main"` clean.
    Backend briefly unresponsive on :8000 (shared server, concurrent load from other
    verifier lanes) then recovered on its own -> confirmed `/docs` 200 without a restart.
    No UI reproduction exists for this defect (ISS-131's own card: "Not reachable from any
    file manifest today" — only a composed fan-out+human-gate selection triggers it, which
    is exactly what the test constructs), so the manual re-run IS running that exact scripted
    scenario outside the xfail wrapper, which is what was done above. Dashboard loaded
    clean, zero console errors. No frontend files touched by this fix.

- ISS-125 FIXED by FIX-421 (5-fixer, round 4). `_stamp_resume_marker` swapped off
  `append_event_next_seq` (unbounded `read_events(after_seq=0)` per retry) onto
  `_max_event_seq` + `append_event_at_or_after`. engine.py only, no new import.
  Two heads-ups for the verifier: (a) ISS-133 in domain 14 is a duplicate filing of
  this same function and is CURED by this edit — its row was left ANALYZED because
  domain 14 owns it; (b) the same unbounded idiom survives at engine.py:10010 and
  :10377 as seq SEEDS (no lost-write exposure) — filed as ISS-639, not fixed here.

- ISS-125 VERIFIED CLOSED (verifier pass). `backend/tests/agents/test_resume_marker_seq_allocation.py`
  confirmed XPASS(strict) ("[XPASS(strict)] ISS-125 unfixed"), `xfail` marker removed, re-run
  plain green (1 passed). Regressions all green: `test_resume_marker_workspace.py` (5 passed),
  `test_restart_resume.py` (69 passed), `test_banned_patterns.py` (18 passed, SC-001),
  `backend/tests/unit/test_rest_resume.py` (10 passed). `lint-imports` (run from `backend/`)
  unchanged at 3 kept / 1 broken, same two pre-existing edges as 923b0d4fd. Frontend `npx tsc
  --noEmit` clean (no output). Source confirmed live at `engine.py:7694` —
  `_stamp_resume_marker` now calls `ScopedStore._max_event_seq` + `append_event_at_or_after`,
  matching FIX-421; no orphaned `append_event_next_seq` call at this site. No browser/UI
  reproduction exists for this defect — FIX-421 records "N/A — no frontend surface; the marker
  is a durable-only audit row", and this domain register carries no `## ISS-125` section (the
  card is the register of record here). `:8000/docs` was unreachable (000) for several minutes
  under concurrent bug-hunt load, then returned 200 on retry — matches the same environmental
  condition ISS-072 logged in this same batch; not a regression from this change (proven by the
  103 passing regression tests that exercise `engine.py` directly).

- **ISS-072 Verified CLOSED (6-verifier).** `:8000/docs` confirmed 200 first (the prior
  attempt's outage recovered on its own — pure `.py` fix, `--reload` already picked it up, no
  restart needed). Re-ran all 3 test files fresh: `test_spec_revision_cycles.py` (6 passed),
  `test_gate_revision_discriminator.py` (6 passed), `test_update_specs_enforcement.py`
  (5 passed) — xfail already removed by the prior attempt, `@pytest.mark.issue("ISS-072")`
  confirmed still present, all plain green (no XPASS artifact left). Source directly read and
  confirmed matching FIX-420's design line-for-line: `context.py:372` declares
  `suppress_review_gate: bool = False`; `engine.py:6811` publishes
  `ectx.suppress_review_gate = sub_spec is analyze_spec` inside the re-dispatch loop;
  `engine.py:6240` is the sole honouring read in `_should_gate`; `engine.py:6864` restores it
  in the pass's `finally`. `lint-imports` (run from `backend/`) unchanged at 3 kept / 1 broken,
  same two edges as baseline. `npx tsc --noEmit` clean. Regression file
  `test_restart_resume.py` 69/69 green.
  **Manual repro:** ISS-072's evidence is a live production run (`5ecb990f`) with no frontend
  surface changed (FIX-420: "No frontend change was needed") and no offline E2E suite covers
  spec-revision gate counting — the same pattern as sibling B4 cards ISS-125/ISS-131 in this
  batch, both closed without a UI reproduction because none exists. The prior verification
  attempt tried to drive a full live LLM-backed prototype-build + revision cycle through the
  browser to count gate approvals and that is what caused the ~20min shared-backend outage
  logged above; repeating that same expensive live drive risked the identical failure for
  other concurrent bug-hunt lanes, so it was not repeated here. Instead: signed in as
  qa-admin, dashboard loaded clean with zero console errors/warnings, `/create/prototype`
  wizard loads and is reachable (confirming the launch surface this fix sits behind is intact),
  then returned to a clean dashboard state without launching a run. The scripted reproduction
  that IS the byte-identical live scenario (`test_in_pass_analyze_gate_is_suppressed_not_just_ineligible`,
  keyed to the same `5ecb990f` seq/sha1 evidence the card cites) ran green outside its xfail
  wrapper, which is the equivalent of running that exact scenario by hand for a defect with no
  UI-level checkpoint.

- ISS-101 FIXED by [FIX-426](../../../.knowledge/cards/20260831-1806-FIX-426.md) (5-fixer).
  - **Root cause:** `conversation.py:99` — `ConversationProvider._read_chat_events` called
    `scoped_store.read_events(run_id, 0)`, the UNBOUNDED replay primitive, then filtered to
    `chat_message`/`chat_reply` in Python (12,123 rows / ~9.2 MB on the worst corpus run to
    render a <=6,000-char transcript), unconditionally on every Concierge turn since FIX-233.
  - **Fix:** swapped onto the bounded sibling FIX-233 already added,
    `ScopedStore.read_events_of_types(run_id, _CHAT_TYPES, limit=_MAX_CHAT_ROWS)`
    (`authz.py:380`) — type filter and row cap pushed into SQL. `_MAX_CHAT_ROWS = 200`.
    Single chokepoint: both callers (engine `injects:[conversation]` and the Concierge's
    `_load_conversation_context`) route through `_read_chat_events`. No engine edit, no new
    import (`agents.capabilities` contract stays KEPT), no migration.
  - **Verified:** `test_conversation_provider_bounded_read.py` xfailed before, XPASS(strict)
    after; the xfail marker is LEFT IN PLACE for the 6-verifier. Regressions green:
    test_conversation_provider (9), test_concierge_capability (39), test_context_providers (20),
    test_sc001_lane_router_concierge (9). lint-imports 3 kept / 1 broken, identical to 923b0d4fd.
  - **Heads-up for the verifier:** `test_conversation_provider.py` WAS edited — its
    `_FakeScopedStore` predates FIX-233 and exposed only `read_events`, so it hit the degrade
    path and 2 of its 9 tests went red. `read_events_of_types` was ADDED to the fake (production
    `ScopedStore` has both); no assertion, expected value or test name changed. ISS-101's own
    card predicted exactly this ("its degrade-to-`{}` error path would also hide the change from
    any duck-typed store fake that lacks the new method"). Serialization: this fix touches only
    `context_providers/conversation.py` + that one test file — no overlap with the engine.py or
    workflow-yaml batches.

- **ISS-101 Verified CLOSED (6-verifier).** `:8000/docs` confirmed 200 (pure `.py` fix,
  `--reload` already picked it up, no restart needed). `test_conversation_provider_bounded_read.py`
  re-confirmed `[XPASS(strict)] ISS-101 unfixed` (1 failed) with the xfail marker still in
  place; marker removed (`@pytest.mark.issue("ISS-101")` kept), re-run -> plain green
  (1 passed). `test_conversation_provider.py` 9/9 green (the updated `_FakeScopedStore`
  double, no assertions changed). Regressions re-run one file at a time, all green:
  `test_concierge_capability.py` (39), `test_context_providers.py` (20),
  `test_sc001_lane_router_concierge.py` (9). `lint-imports` (run from `backend/`) 3 kept /
  1 broken, identical two edges to baseline. `npx tsc --noEmit` clean. Source read directly:
  `conversation.py:_read_chat_events` calls `scoped_store.read_events_of_types(run_id,
  _CHAT_TYPES, limit=_MAX_CHAT_ROWS)`, no unbounded `read_events` call left in the method —
  matches FIX-426 exactly. **Manual repro:** no UI checkpoint exists for this server-side
  capability (same as sibling B4 cards ISS-072/ISS-125/ISS-131 in this domain) — signed-in
  qa-admin dashboard loaded clean via lane1, zero console errors/warnings, screenshot at
  `bug-hunter/evidence/ISS-101/after-verified-dashboard-clean.png`.

- **ISS-098 Verified CLOSED (6-verifier).** `backend/tests/agents/test_wave_scheduler_cancel_row.py`
  re-confirmed `[XPASS(strict)] ISS-098 unfixed` (`1 failed, 1 warning`) before the change; `xfail`
  marker removed (`@pytest.mark.issue("ISS-098")` kept), re-run -> plain green (`1 passed`).
  Regressions re-run one file at a time: `test_wave_scheduler.py` (10 passed),
  `test_fanout_cancel.py` (9 passed), `test_restart_resume.py` (69 passed) — all matching the
  fixer's reported figures. `lint-imports` (run from `backend/`) unchanged at 3 kept / 1 broken,
  same two edges as baseline. SC-001 grep (`if pipeline_type ==` / `spec.id ==` under
  `agents/execution_engine/`) 0 hits. `:8000/docs` 200, no restart needed (pure `.py` fix,
  `--reload` already picked it up). Source confirmed live at
  `wave_scheduler.py:346-355` — a narrow `except asyncio.CancelledError:` arm (not
  `except BaseException`) calling `await runner.update_wave_run(row_id, status="cancelled")`
  then a bare `raise`, exactly matching FIX-425's design; `git diff --stat` on the file shows
  only `+18` lines, no deletions.
  **Manual repro:** this defect has no browser/UI reproduction path — same pattern as sibling
  B4 cards ISS-125/ISS-131/ISS-072 in this same domain, all closed without one because none
  exists (a `wave_runs` row's terminal status on a mid-wave `CancelledError` is not observable
  from the frontend; a live Stop-during-a-wave with fan-out is the only real trigger and is
  expensive/risky to drive repeatedly on a shared backend, as ISS-072's note above records).
  Instead, ran the exact scripted `CancelledError`-mid-wave scenario from
  `test_wave_scheduler_cancel_row.py` BY HAND, standalone, outside pytest's harness, against the
  unmodified production `WaveSchedulerStrategy.run` — result:
  `wave_runs updates recorded (by hand, outside pytest): [('row-0', 'cancelled')]`, confirming
  the row now flips to `cancelled` instead of staying stuck at `running`. No frontend files
  touched by this fix (confirmed via `git status`); no console/UI surface to check.

- **ISS-118 Verified CLOSED (6-verifier).** `backend/tests/unit/test_iss118_construct_only_exemptions.py`
  re-confirmed `[XPASS(strict)] ISS-118 unfixed` (1 failed) with the xfail marker still in
  place; marker removed (`@pytest.mark.issue("ISS-118")` kept), re-run -> plain green
  (1 passed). Regressions re-run one file at a time, all green: `test_iss102_live_model_guard.py`
  (6), `test_brief_max_chars.py` (4), `test_cached_invoke.py` (12 — no XPASS observed;
  ISS-120's marker has since been cleared independently), `test_iss056_clarify_quality_fixes.py`
  (36), `test_mcp_client.py` (5). `lint-imports` (run from `backend/`) unchanged at 3 kept /
  1 broken, same two edges as baseline. `:8000/docs` 200, no restart needed (test-file-only
  fix, `--reload` doesn't even apply). Source confirmed:
  `python3 -c "from tests.conftest import _CONSTRUCTS_BUT_NEVER_INVOKES; print(...)"` -> `{}`.
  Non-vacuity spot-check: `test_guard_blocks_the_smart_planner_bypass_path` (an UNSTUBBED
  `SmartPlanner()`) still raises, confirming the ISS-102 guard itself was not weakened.
  **Manual repro:** no browser/UI surface exists for this defect (an exempted pytest nodeid
  disarming a fixture) — same pattern as sibling B1/B4 cards in this domain closed without a
  UI reproduction. The by-hand equivalent is the exemption-dict check above, run directly
  against the real unmodified `backend/tests/conftest.py` outside pytest's own harness.

Collision note: engine.py OWNED here (also has cards from domain 14 as secondary
globs — that domain must not edit engine.py). test_phase6_frontend_consistency.py
owned here. Workflow yamls owned here.
Status: the `status` column above is authoritative — it is what the line
reads and writes. A whole-file status could only drift from it.
