# Refinement Run Report — RFN-001 · Per-Agent Model Chip

**Date:** 2026-09-07  
**Session:** Kiro main session (orchestrator role)  
**Refinement card:** `.kiro/bug-fix-workflow/refinements/RFN-001-agent-model-chip.md`  
**Status:** CLOSED ✅

---

## Summary

Delivered the per-agent model chip on the Steps tab → AgentDetailPanel header. The chip shows which model each agent is using — visible immediately when the agent starts running (via a new `model_id` on `agent_start`), persists after pipeline completion, and is shown when reopening completed runs from run history.

---

## Cards

| card | result | note |
|------|--------|------|
| RFN-001 | CLOSED | All gaps fixed, tests written, all suites green |

No cards reopened. No escalations.

---

## Changes Applied

### Phase 1 — Frontend gaps (6 files)

| File | Change |
|------|--------|
| `frontend/src/types/index.ts` | Added `modelId?: string` to `AgentRunState` |
| `frontend/src/hooks/useWorkflow.ts` | `agent_complete` reducer writes `modelId`; `agent_start` reset now writes `modelId` from event (not just clears to `undefined`) |
| `frontend/src/lib/api.ts` | Added `model_id?: string` to `RunSummaryAgent` |
| `frontend/src/components/history/WorkflowHistory.tsx` | `detailAgentOutputs` inline type gains `model_id?`; dedup branch adds keep-last; `thinkingAgents` map carries `modelId` |
| `frontend/src/components/results/AgentDetailPanel.tsx` | Added `MODEL_LABELS` map + `resolveModelLabel()` helper; chip `{agent.modelId && …}` inserted after Done/Live/Failed chips |

### Phase 2 — Backend engine change (1 file)

| File | Change |
|------|--------|
| `backend/agents/execution_engine/engine.py` | `_resolve_model` called before `yield agent_start`; result stored as `_start_model_id`; included as `"model_id": _start_model_id` in the `agent_start` event payload |

INV-3 safe: `model_id` is already in `_VOLATILE_STRIP_KEYS` — prototype characterization tests (2 passed) confirmed byte-identical goldens.

---

## Tests Written

| File | Count | What it pins |
|------|-------|-------------|
| `backend/tests/agents/test_rfn001_model_id_on_agent_start.py` | 8 | Source-pin: engine.py emits `model_id` on `agent_start` before the yield; resolve-before-yield ordering; `agent_complete` unchanged (ISS-165 regression guard); normalizer strips from both events (INV-3); end-to-end `_drive("prototype")` confirms live path; `run_commands.py` persistence unchanged |
| `frontend/src/components/results/AgentDetailPanel.modelChip.test.tsx` | 15 | Chip visible while running; persists on done; absent on idle; fallback label for unknown IDs; absent `modelId` no crash; all 9 catalog IDs resolve to correct mixed-case labels; Live badge + chip coexist |
| `frontend/src/components/history/WorkflowHistory.modelChip.test.tsx` | 4 | `model_id` threaded from `agent_outputs` → `thinkingAgents` → `AgentRunState.modelId` via capture-spy on `AgentThinkingTab`; absent `model_id` graceful degrade; dedup keep-last semantics; no crash when `agentOutputs` null |

**Total new tests: 27**

---

## Test Results

| Suite | Result | Count |
|-------|--------|-------|
| `tsc --noEmit` | ✅ EXIT_CODE=0 | 0 errors |
| `AgentDetailPanel` vitest (existing) | ✅ EXIT_CODE=0 | 33/33 |
| `WorkflowHistory` vitest (existing) | ✅ EXIT_CODE=0 | 51/51 + 1 pre-existing xfail |
| `test_rfn001_model_id_on_agent_start.py` (new) | ✅ EXIT_CODE=0 | 8/8 |
| `AgentDetailPanel.modelChip.test.tsx` (new) | ✅ EXIT_CODE=0 | 15/15 |
| `WorkflowHistory.modelChip.test.tsx` (new) | ✅ EXIT_CODE=0 | 4/4 |
| `test_characterization_prototype.py` (INV-3 guard) | ✅ EXIT_CODE=0 | 2/2 |

---

## Invariants Checked

| Invariant | Status |
|-----------|--------|
| SC-001 / INV-1 — no workflow-name or agent-id literals in chip logic | ✅ Chip keyed on `agent.modelId` (data value only) |
| INV-3 goldens — byte-identical after `agent_start` model_id addition | ✅ `model_id` in `_VOLATILE_STRIP_KEYS`; prototype characterization 2/2 passed |
| import-linter | ✅ No new imports; `Cpu` already imported in `AgentDetailPanel` |
| Additive-only migrations | ✅ No migration; Python + TypeScript only |
| Backend restart required | ✅ Yes — `engine.py` changed (pure `.py`; `--reload` picks it up automatically) |

---

## Knowledge Rebuild

```
Stage 1 (index):   SUCCESS — INDEX.md + state.yaml updated, 1165 cards indexed
Stage 2 (context): FAILED  — pre-existing UnicodeDecodeError cp1252 0x90 on one
                             MOD-*.md card (documented pre-existing Windows issue;
                             CONTEXT.md stays stale — see DISPATCH.md note)
Stage 3 (links):   SKIPPED — aborted at stage 2
Architecture:      NOT REBUILT — no source-graph change required
```

No new `.knowledge/cards/` entries created. RFN-001 lives in `.kiro/bug-fix-workflow/refinements/` (the new refinements folder introduced this session).

---

## Ready to Commit

All changes are in the working tree. Suggested commit groupings:

**Commit 1 — Feature: per-agent model chip (frontend + backend)**
```
feat: show per-agent model chip on Steps tab (RFN-001)

- engine.py: emit model_id on agent_start (chip visible while running)
- useWorkflow.ts: write modelId from agent_start + agent_complete
- AgentDetailPanel.tsx: model chip + resolveModelLabel helper
- WorkflowHistory.tsx: thread model_id through detailAgentOutputs/thinkingAgents
- types/index.ts: AgentRunState.modelId
- lib/api.ts: RunSummaryAgent.model_id
```
Files:
- `backend/agents/execution_engine/engine.py`
- `frontend/src/types/index.ts`
- `frontend/src/hooks/useWorkflow.ts`
- `frontend/src/lib/api.ts`
- `frontend/src/components/history/WorkflowHistory.tsx`
- `frontend/src/components/results/AgentDetailPanel.tsx`

**Commit 2 — Tests: RFN-001 model chip coverage**
```
test: RFN-001 model chip — backend source-pin + frontend unit tests
```
Files:
- `backend/tests/agents/test_rfn001_model_id_on_agent_start.py`
- `frontend/src/components/results/AgentDetailPanel.modelChip.test.tsx`
- `frontend/src/components/history/WorkflowHistory.modelChip.test.tsx`

**Commit 3 — Workflow: refinements card + knowledge + report**
```
chore: RFN-001 refinement card, closer report, knowledge index update
```
Files:
- `.kiro/bug-fix-workflow/refinements/RFN-001-agent-model-chip.md`
- `.kiro/bug-fix-workflow/reports/20260907-1200-rfn001-model-chip.md`
- `.kiro/bug-fix-workflow/STATE.md`
- `.knowledge/INDEX.md`
- `.knowledge/state.yaml`

---

## Restart Required

`backend/agents/execution_engine/engine.py` was changed. The dev server runs with `--reload` (watches `*.py`), so it will auto-reload. For production or a manually-started server: **manual restart required before trusting live `agent_start` model_id events**.

---

## Pre-existing Issues (not fixed)

- `build_context.py` Stage 2 Windows encoding failure: `UnicodeDecodeError cp1252 0x90` on one MOD-*.md architecture card at position 7187. Pre-existing on this machine; documented in `DISPATCH.md`. `CONTEXT.md` stays stale. No action taken.

---

## Needs a Human

None. No escalations, no wontfix candidates, no design decisions deferred.

---

## Unexpected Changes

None. All modified files are within RFN-001's declared scope.
