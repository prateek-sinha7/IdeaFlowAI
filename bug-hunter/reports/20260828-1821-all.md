# Bug-hunt run report — 2026-08-28 18:21 UTC

**Phases run:** validate → analyze → test → fix → verify
**Batch size:** 3
**Run status:** **PAUSED** (see "Pause" at the end — this was not a failure)

---

## Per-bug results

Grouped by furthest phase reached this run. Within each group, in the order results arrived.

### verify (closed this run)

| Bug ID | Verdict | Cards | Tests | Fix summary |
|---|---|---|---|---|
| `BUG-20260828-012900-runs-id-files` | **CLOSED** (verify passed) | ISS-199 (root), ISS-214, ISS-215, FIX-327 | 3 xfail tests removed, all green after fix; regression suite (5 files, 42/42) green | FilesTab's "Final output" row now resolves against the run's real workspace listing via a shared `resolveRunDeliverable` (extracted from FIX-318) instead of regex-parsing streamed text; `ppt_v2` now joins the `ppt` deliverable slot everywhere. Manual repro confirmed: run now shows `presentation.pptx` (159.9 KB) not the old tmp/ HTML. Deferred, filed not fixed: ISS-251 (PreviewPanel still holds its own inline copy of the rule), ISS-252 (a 4th unnormalized ppt check in DashboardLayout). |

### fix (implemented, not yet verified)

| Bug ID | Cards | Fix cards | Summary |
|---|---|---|---|
| `BUG-20260828-090732-runs-id-versions` | ISS-230 (root), ISS-294 | FIX-328, ISS-325 | The `/versions/{v}` pin now survives a tab switch and is actually applied to the fetch. `routes.run{Detail,Steps,Files,Workspace,Audit}` take an optional version and emit `/runs/{id}/versions/{v}/{tab}`; the tab-switch chokepoint (`handlePreviewPanelTabSelect`) preserves it; `page.tsx` stops discarding `parsed.version`; `PreviewPanel` resolves it against the family and sets `viewingVersion`, which previously lost to the family-root `liveRunId`. 3× XPASS(strict) on the ISS-230 test, xfail markers left for verify. Regression: e2e 35 passed/12 skipped, frontend 140 passed, 0 new tsc/eslint errors. **Card correction filed** (not silent): ISS-230's claimed mechanism ("pushState drives the refetch") was wrong — the real chokepoint is `PreviewPanel`'s `activeRunId` derivation, not the page-level fetch effect. **Serialization note:** this fixer also touched `page.tsx` and `PreviewPanel.tsx`, which are in ISS-294/296/300's globs — any fixer picking those up must not run concurrently with this change (ISS-294 already resolved as a byproduct; ISS-296/300 deliberately left open). One deferred/unrelated test flake filed as ISS-325 (S-21-05 family-count filter chip, touches `WorkflowHistory.tsx` which another agent is concurrently editing). |

### test (red tests written, not yet fixed)

| Bug ID | Cards | Test file(s) | Observed red |
|---|---|---|---|
| `BUG-20260828-090732-runs-id-versions` | ISS-230 | `tests/integration/e2e/suites/21_run_families_and_versions/test_iss_230_version_pin_survives_tab_switch.py` | Yes — URL collapsed off `/versions/2` on tab switch; app re-fetched the root run, never the pinned revision. (Superseded by the fix above in the same run.) |

### analyze (root-caused, not yet coded)

| Bug ID | Root cause (one line) | Cards minted |
|---|---|---|
| `BUG-20260828-015500-runs-failed` | "Edit brief & run again" navigates via a bare `router.push` that never sets `pendingHomeBrief`, so the composer textarea renders empty despite the handler's own comment claiming a pre-fill. | ISS-218 (root, CONFIRMED), ISS-235 (sibling: stale Home-draft bleed, INFERRED) |
| `BUG-20260828-015930-runs-cancelled` | Resume (`handleResumeRun`) triggers a `router.push` remount that tears down the page's one SSE-subscriber effect; `RunConnectionProvider`'s fanout has zero buffering, so a frame (e.g. a fast `pipeline_failed`) emitted mid-resubscribe is lost until manual reload. | ISS-220 (root, augmented CONFIRMED), ISS-236 (sibling: failed-run "Reopen & fix" shares the handler, INFERRED), ISS-237 (sibling: degraded-run "Run again", INFERRED) |
| `BUG-20260828-020430-runs-diverted` | The shared `relaunch(label)` helper in `RunChatLane.tsx` renders only a Button, never the `relaunchError` banner — hand-duplicated only into the cancelled/failed branches — so a diverted run's 409 resume rejection is silently dropped. | ISS-221 (root, CONFIRMED), ISS-233 (sibling: degraded branch shares the gap, INFERRED) |
| `BUG-20260828-025521-library-hooks` | Hooks (and Agents/Skills) card grids attach `onClick` to the shared, deliberately non-semantic `Card` `<div>` with no `tabIndex`/`role`/`onKeyDown`, so Tab skips every card and Enter/Space can't activate one. | ISS-222 (root, pre-existing, augmented), ISS-270 (Agents sibling, INFERRED), ISS-271 (Skills sibling, INFERRED), ISS-272 (adjacent: both detail modals have no Escape/focus handling, INFERRED) |
| `BUG-20260828-034200-handoff-settings` | No app-wide `RequestValidationError` handler in `backend/app/main.py`, so any length-constrained Pydantic field's 422 echoes the rejected value verbatim; two independent frontend parsers (`authedJson`, `ApiError`) both fall back to raw `JSON.stringify(body)` and render it unparsed on-page. | ISS-224 (root, CONFIRMED, pre-existing), ISS-264 (sibling: API-key name, INFERRED), ISS-256 (sibling: `api.ts`'s independent duplicate, INFERRED), ISS-257 (sibling: register-password field, INFERRED) |
| `BUG-20260828-051530-create` | `selectionsRef` is never seeded from the manifest's declared `gates`; "Save as my version" serializes it verbatim, so a route-declaring built-in 422s on save (compiler's R-03 requires `gates:[conditional]` for any `route:` step). | ISS-225 (root, CONFIRMED, pre-existing), ISS-254 (sibling: every other route-declaring example, INFERRED), ISS-263 (sibling: plain "Save workflow" silently drops the route instead of erroring, INFERRED) |
| `BUG-20260828-052800-create-ppt-r2` | `handleSaveAsOverride` (in both `LaunchWizard.tsx` and `IdeaInputPage.tsx`) hardcodes name/description string literals and never threads the live brief/template/design-system state into the save payload — the sibling "Save workflow" path already does this correctly. | ISS-226 (root, CONFIRMED, pre-existing), ISS-279 (sibling: prototype/ppt_v2 share the same function, INFERRED), ISS-280 (sibling: `IdeaInputPage`'s own copy, INFERRED) |
| `BUG-20260828-060000-workflow-create-prototype` | `frontend/src/proxy.ts` interpolates the raw, unvalidated `mode` query param into `new URL()`; standard dot-segment normalization lets `mode=../admin` escape the `/create` namespace entirely, reaching `/admin` with zero authorization check of its own. | ISS-227 (root, CONFIRMED, pre-existing), ISS-281 (sibling: non-admin-tier exposure via the same hole, unverified/INFERRED) |
| `BUG-20260828-073900-workflows-id-run-r2` | Same-commit React effect-ordering race in `LaunchWizard.tsx`: a "restore session draft" effect correctly sets `pipelineAgents` from the saved override, but a sibling "re-derive once real agents arrive" effect declared right after it reads a stale pre-update `pipelineAgents.length` in the same render and always overwrites it with the generic default roster. | ISS-228 (root, CONFIRMED, augmented with the confirmed mechanism), ISS-283 (sibling: click-through Run from Saved-workflows shares the race, INFERRED), ISS-284 (sibling: Save/Save-as-override persist the race-clobbered roster, data-loss consequence, INFERRED) |
| `BUG-20260828-081200-analytics` | KPI tiles, Daily Activity chart, Success Rate, and token breakdown are all computed from the single unfiltered `/api/analytics/summary` response; the backend accepts no pipeline/model filter param at all, so only the two client-side `.filter()`'d breakdown arrays (By Pipeline / By Model) ever reflect the selected filter. | ISS-229 (root, CONFIRMED), ISS-288 (sibling: model filter, INFERRED), ISS-289 (sibling: cross-filter ignored, INFERRED) |
| `BUG-20260828-090732-runs-id-versions` | (See fix above — this bug moved all the way through analyze→test→fix this run.) No consumer ever applies `ParsedView.version`; the Version-menu pin is never written to the URL either — a feature with no single (rootId, version) → run-id resolver. | ISS-230 (root, CONFIRMED — resolved this run), ISS-296 (sibling: menu pin never writes URL, INFERRED, left open), ISS-294 (sibling: cold-mount discards version, resolved as byproduct), ISS-300 (sibling: FilesTab ignores the pin, INFERRED, left open) |

### validate (this run's largest phase — CONFIRMED unless noted)

| Bug ID | Verdict | Card | One-line trigger |
|---|---|---|---|
| `BUG-20260828-091300-library-skills-r2` | CONFIRMED | ISS-232 | Any skill id renders the catalog listing instead of the detail view — cold-mount seed effect's guard is never satisfied for any id now. |
| `BUG-20260828-104200-workflows-nonexistent-r2` | CONFIRMED | ISS-234 | Composer sends `currentIds=[]` for a dead-link workflow with `dispatchType="custom"`; backend treats empty `agent_ids` as falsy and falls back to the full 9-agent custom-utility pool instead of rejecting. |
| `BUG-20260827-221730-library` | CONFIRMED | ISS-231 | Non-matching search on any Library tab renders zero markup with no empty-state message (bare `.map()`, no `length===0` branch). |
| `BUG-20260827-222300-settings-profile` | CONFIRMED | ISS-244 | "New passwords do not match" banner never clears on input — only computed inside the submit handler, never on change. |
| `BUG-20260827-222750-login` | CONFIRMED | ISS-245 | Rapid triple-click on Sign-in fires 3 concurrent logins — `disabled={isLoading}` reads a React state commit that hasn't landed synchronously yet. |
| `BUG-20260827-231407-register` | CONFIRMED | ISS-238 | `/register/<sub-path>` for an authenticated user parses to the same `{screen:'register'}` as the real route but has no handler in the catch-all, falling through to the dashboard render. |
| `BUG-20260827-232305-root` | **DUPLICATE** | — (of ISS-217) | Same bfcache/`back_forward` zero-hydration mechanism ISS-217 already covers, on a different route. |
| `BUG-20260828-000100-create-app` | CONFIRMED | ISS-247 | Review-gates checklist and the per-agent Gate combobox write to two entirely unsynced state slots; `gate_agent_ids` is never even sent for composer-launched workflows. |
| `BUG-20260828-000600-create-user-stories` | CONFIRMED | ISS-246 | `LaunchWizard` never forwards `onRunConfigChange`/`onCapabilitiesChange` for a built-in launch, so all 6 Workflow-tab controls render disabled with zero lock icon/tooltip explaining why. |
| `BUG-20260828-001100-create-ex-a2-branch` | CONFIRMED | ISS-258 | Modal's Cancel button only closes the popup; live-propagated edits to `selectionsRef.current` are never reverted, so a cancelled edit persists on reopen. |
| `BUG-20260828-001420-workflow-create-ppt` | CONFIRMED | ISS-250 | `CreateRoute()` only canonicalizes the URL for `raw==="ppt"/"ppt_v2"`; a null/empty `mode` never gets a `router.replace`, so the legacy URL never changes. |
| `BUG-20260828-001720-workflow-create-prototype` | CONFIRMED | ISS-253 | Any unrecognized slug 404s `getWorkflowDetail()`, which is silently swallowed; composer renders 0 agents, all action buttons lock permanently, no visible error. |
| `BUG-20260828-003800-workflows-new` | CONFIRMED | ISS-275 | Composer's Back button discards unsaved name+agent state instantly via a shared `handleBackNav` with zero dirty-state check (5 call sites share it). |
| `BUG-20260828-005500-workflows-id-edit` | CONFIRMED | ISS-274 | Simple view's "Configure →" panel opens `AdvancedExpander`, which never imports `CanvasConfigRail` — the Tools tab (Read/Write/Execute) is structurally unreachable from Simple view. |
| `BUG-20260828-010600-workflows-id-run` | CONFIRMED | ISS-273 | A 404 on cold-mount `/workflows/{id}/run` falls through to rendering Dashboard content with no `router.replace` — URL stays on the bogus `/run` path. |
| `BUG-20260828-003300-workflows` | **UNREPRODUCIBLE** | — none minted | Reproduced 0/3 via genuine clicks; only reproducible via a raw `page.evaluate` JS click that skips native focus — likely an artifact of the original hunter's script, not a real defect. |
| `BUG-20260828-011700-runs-id` | CONFIRMED | ISS-276 | Header's relative-age label reads client-hydration-time `pipelineState.createdAt` instead of the run's real `created_at`, so it always shows "just now" on a completed run. |
| `BUG-20260828-012500-runs-id-steps` | CONFIRMED | ISS-278 | Token-usage "input" label shows cache-adjusted `uncachedInput` while "total" uses the full input figure — the tooltip already carries the correct full value. |
| `BUG-20260828-012700-runs-id-steps-agent` | CONFIRMED | ISS-277 | Deep-link agentId is parsed by `routes.ts` but discarded before reaching the tab-open request; manual in-page agent selection also never updates the URL — both halves broken. |
| `BUG-20260828-013500-runs-id-workspace` | CONFIRMED, **routed to WONTFIX candidates** | none | Code view is genuinely editable (contentEditable) but a source comment documents this as intentional for Find&Replace, with zero persistence. Missing-affordance framing recorded for human ruling; see WONTFIX section below. |
| `BUG-20260828-014416-runs-id-preview-full` | CONFIRMED | ISS-285 | A failed run's default-tab effect silently auto-selects Audit with no fallback banner explaining why Preview is absent. |
| `BUG-20260828-014937-runs-id-stream` | CONFIRMED | ISS-282 | `reopenFailureSignal` enumerates failed/cancelled/degraded but omits "diverted", so a diverted run falls through to the generic "Output will appear here" placeholder instead of a status-aware message. |
| `BUG-20260828-020900-runs-id-versions` | CONFIRMED | ISS-286 | `parsed.version` from the URL is captured by `parseViewPath` but never consumed anywhere downstream — cold-load of `/versions/2` always labels/resolves as v1. |
| `BUG-20260828-025950-library-agents-id` | CONFIRMED | ISS-287 | Library's agent drawer mounts `AgentsPopup` with no `onSelectionsChange`; the Config tab's "Save agent" click is a documented no-op there. |
| `BUG-20260828-030430-library-skills-id` | **UNREPRODUCIBLE** | none | Report is a misreading of a documented "drawer over the still-visible list" pattern — the "failure" screenshots show correct rendering. |
| `BUG-20260828-030700-library-hooks-id` | CONFIRMED | ISS-290 | Seed-data defect: `compatible_agents` in `HOOK.md` frontmatter is loaded verbatim with no validation against the live agent catalog — a data-integrity issue, not a rendering bug. |
| `BUG-20260828-031200-settings-ai-model` | CONFIRMED | ISS-292 | Neither `PUT /api/settings/preferences` nor the UI apply any tier filter to premium models, despite the app having an established gating pattern elsewhere (`/api/capabilities`). |
| `BUG-20260828-031600-settings-usage` | CONFIRMED | ISS-291 | "Manage plan" button has no `onClick` at all — a fully static element, not a swallowed handler. |
| `BUG-20260828-031900-settings-constitution` | CONFIRMED | ISS-293 | Backend's `max_length` is 1 MiB, textarea has no `maxLength`, Save has no length gate — the UI's "4000 chars" label is display-only, backed by nothing. |
| `BUG-20260828-032424-settings-security` | CONFIRMED | ISS-293* (see note) | Security tab shows "credentials managed outside the app" for local/break-glass accounts too, even though the component's own comment distinguishes Cognito vs local `auth_provider`. *Ledger note used ISS-293 in the card field — likely a copy/paste slip against the constitution bug filed at the same timestamp; flagged for reconciliation below. |
| `BUG-20260828-033400-admin` | CONFIRMED | ISS-295 | "Create User" enables and the backend persists on any non-empty email with zero format validation at either layer. |
| `BUG-20260828-040400-workflows-nonexistent` | CONFIRMED | ISS-299 | Composer's `getWorkflowDetail()` 404 is swallowed identically to ISS-253's sibling, but here Save-as-copy stays fully functional, persisting a phantom workflow. |
| `BUG-20260828-040915-preview-fullscreen-quota` | CONFIRMED | ISS-298 | `error==="quota"` branch returns before ever reading `sessionStorage["__app_preview__"]` — fires 100% of the time the param is present, masking a valid preview. |
| `BUG-20260828-042900-handoff-invalid` | CONFIRMED | ISS-301 | Any network-layer fetch failure (not just a real 404) on `GET /api/handoff/{token}` renders the identical "Handoff not found" message with no retry, no distinction from a genuine dead link. |
| `BUG-20260828-053745-create-app-r2` | CONFIRMED | ISS-312 | Text-file attachments over 450,000 chars are silently truncated with no "[Content truncated]" marker, unlike the binary-file path which shows one. |
| `BUG-20260828-054500-create-user-stories` | CONFIRMED | ISS-311 | Add-agent dialog defaults to the one category tab (User Stories) guaranteed empty for this pipeline's own default workflow, indistinguishable from "empty catalog." |
| `BUG-20260828-055300-create-ex-a2-branch` | CONFIRMED | ISS-306 | Review-gates checklist and the Gate combobox both fail to represent the backend-declared `gates:["before-human","conditional"]` on zero-interaction fresh load. |
| `BUG-20260828-060700-workflows-new-r2` | CONFIRMED | ISS-315 | Empty-name "Save workflow" click is silently swallowed — focuses the input and returns before `handleSave`, no toast, no red border, no aria-invalid. |
| `BUG-20260828-084621-runs-id-preview-full` | CONFIRMED | ISS-314 | Full-screen toolbar has no `flex-wrap`/overflow handling; below ~493px content overflows with no wrap or scroll mechanism. |
| `BUG-20260828-085047-runs-id-stream` | CONFIRMED | ISS-313 | Deliverable exists and downloads via the file panel's own ZIP button, but the toolbar Download gate (per FIX-318) can never resolve because the backend never populated `deliverable_filename`. |
| `BUG-20260828-085400-runs-failed` | CONFIRMED | ISS-317 | Resuming a terminal run while a stale per-agent "error" status lingers pins the Steps panel on "Run failed" for the entire live-progress duration, contradicting the correct "Running" header badge. |
| `BUG-20260828-085830-runs-cancelled` | CONFIRMED | ISS-316 | `resume_supersedes` only treats a later `pipeline_complete` as superseding an earlier cancel; a resumed pipeline that instead fails leaves status stuck at "cancelled" forever. |
| `BUG-20260828-092630-library-agents-id` | CONFIRMED | ISS-318 | Library's agent drawer enables the full interactive skills picker against its own documented "nowhere to write back" contract; writes only to a component-lifetime ref, wiped on reload. |
| `BUG-20260828-094120-settings-ai-model-r2` | CONFIRMED | ISS-319 | Real backend race: `update_preferences` commits then `db.refresh()`s — a concurrent request's commit landing in between causes this request's own write to be silently overwritten and misreported as successful. |
| `BUG-20260828-094937-settings-usage-r2` | CONFIRMED | ISS-321 | Direct URL nav to `/create/prototype` as a locked tier bypasses the dashboard's correctly-locked catalog card entirely — the route handler never gates on `can_run_pipeline`. |
| `BUG-20260828-095800-settings-constitution-r2` | CONFIRMED | ISS-320 | "Clear" fires `DELETE` immediately on click with zero confirmation, regardless of saved-vs-draft textarea state. |
| `BUG-20260828-101500-login-expired-true-r2` | CONFIRMED | ISS-322 | Mid-session 401 redirect to `/login?expired=true` omits the `redirect` param the login page already knows how to consume — one line at the single call site. |
| `BUG-20260828-103434-admin` | CONFIRMED | ISS-324 | Duplicate-email 409 is genuinely caught and a toast call is genuinely made, but the toast's own DOM marker never mounts — a render/mount defect in the toast state update itself, not a swallowed catch. |
| `BUG-20260828-105300-preview-fullscreen-r2` | CONFIRMED | ISS-323 | Markdown parser's file-detection regex matches any `### heading` containing a dot-plus-word-chars anywhere in the text (e.g. "Via Node.js"), not just a trailing extension — creates phantom files with malformed download names. |
| `BUG-20260828-audit-export-r2` | **DUPLICATE** | — (of ISS-211/FIX-326) | Already fixed by FIX-326 before this hunt ran; verified live across 3 cycles, matches the already-applied fix exactly. |
| `BUG-20260827-221400-dashboard` | CONFIRMED | ISS-326 | `WorkflowDialog` declares `role="dialog" aria-modal="true"` but has zero Escape/keydown handling — third instance of the app's known "Escape doesn't dismiss an overlay" class, in a component not previously covered. |

---

## Needs a human

Everything below requires a person's ruling — verdicts, escalations, and duplicates left as noted, not silently resolved.

### UNREPRODUCIBLE (3 total, no card minted for any)
- `BUG-20260828-003300-workflows` — "Workflow actions" Escape-dismiss. Only reproduces via a synthetic JS `.click()` that bypasses native focus; a real user gesture closes the menu correctly on 3/3. Likely a harness artifact in the original hunt, not a live defect.
- `BUG-20260828-030430-library-skills-id` — misreads the documented drawer-over-list pattern as a fallback failure; the app's own `velocity.json` describes this exact behavior as intended.
- (Pre-existing, carried forward from prior runs per `hunt-state.md`) `BUG-20260828-091300-library-skills-r2` and `BUG-20260828-030430-library-skills-id` were both flagged STALE at run start — the second was independently re-validated UNREPRODUCIBLE this run and should now be considered settled; the first (`...-091300`) was re-validated CONFIRMED this run (ISS-232) — **these two facts conflict with the standing STALE note and need reconciling**, see the "register/cards agreement" section below.

### DUPLICATE — human should confirm the pointer is right, not re-investigate
- `BUG-20260827-232305-root` → points at ISS-217's bfcache mechanism.
- `BUG-20260828-audit-export-r2` → already covered by FIX-326.

### WONTFIX candidate (mirrored to `bug-hunter/wontfix-candidates.md`, no Status change made)
- `BUG-20260828-013500-runs-id-workspace` — Workspace Code view is freely editable with no persistence; a source comment documents this as intentional (Find&Replace needs a writable doc). The agent left Status untouched and filed the entry with a narrower open question: even if editability is by design, there is zero UI signal (no lock icon, no "editing" badge, no discard warning) that keystrokes are silently accepted and then dropped. **A human should rule on whether that narrower affordance gap alone is worth a fix**, separate from the editability itself.

### Escalations already on the register (pre-existing, not touched this run — reported, not re-routed)
- `BUG-20260827-235402-create-prototype` — ESCALATED (blank-page-on-Forward-nav)
- `BUG-20260828-005700-workflows-ppt-canvas` — ESCALATED (Save-as-copy drops manifest)
- `BUG-20260828-011500-runs` — ESCALATED (50-run cap mislabeled as total)

### Card-ID collision pressure (operational, not a bug — flagged by multiple agents)
Under this run's parallel-lane concurrency, `max+1` ISS-id allocation raced repeatedly across concurrent validator/analyzer/fixer agents — collisions were self-detected and repaired in every case (renumbered off the live max before the final rebuild), but at least a dozen agents independently flagged this as a structural risk worth a locking/reservation step in the pipeline. Worth a look before the next large parallel wave, not a code bug.

### Fix reopened / not fully closed
None — the one bug that reached `verify` this run (`BUG-20260828-012900-runs-id-files`) passed cleanly and is CLOSED. `BUG-20260828-090732-runs-id-versions` reached `fix` but has not yet been through `verify` — normal in-flight state, not a reopen.

---

## WONTFIX candidates mirrored to `bug-hunter/wontfix-candidates.md`

`bug-hunter/wontfix-candidates.md` already contains the `BUG-20260828-013500-runs-id-workspace` entry from this run's validate pass (full detail: symptom, why it may be intentional, the narrower open question, repro, evidence, register-entry pointer). No new entries were added by this report — confirmed the file already carries it verbatim from the validator's own write. No other bug this run was judged a WONTFIX candidate.

---

## Register / cards agreement check

Spot-checked rather than exhaustively re-diffed (94 rows in `ledger-index.md`, several hundred cards) — flagging discrepancies found, not fixing them silently:

1. **`BUG-20260828-032424-settings-security` (ISS-293 reference).** The validate result for this bug names its issue card as `ISS-293`, but `ISS-293` is the constitution-textarea `max_length` bug (`BUG-20260828-031900-settings-constitution`), filed in the same batch at a very similar timestamp. This looks like a copy/paste slip in the agent's own report rather than a genuine dual-purpose card — **needs a human to check `bug-hunter/ledger.md`'s actual entry and `.knowledge/cards/` for the real card id minted for the security-tab bug** (the agent's evidence and root-cause description are specific to Security tab / `SecuritySection.tsx`, not Constitution).
2. **STALE-flag reconciliation.** `hunt-state.md` (as of run start) flagged both `BUG-20260828-091300-library-skills-r2` and `BUG-20260828-030430-library-skills-id` STALE, instructing validators not to confirm either without fresh evidence. This run did exactly that on both: `...-091300` was re-validated **CONFIRMED** (ISS-232, 3/3 fresh reproduction) and `...-030430` was re-validated **UNREPRODUCIBLE** (0/3, code-read confirms intended behavior). Both results are internally well-evidenced and independent of each other, so this is not a contradiction — but a human should update the STALE note in `hunt-state.md`'s "Live run notes" block accordingly, since it currently reads as if neither should currently reproduce.
3. **Two-Status-line risk.** Given the volume of concurrent card writes reported (ID collisions repaired in-flight by numerous agents), a full `validate_links.py`-style pass over `ledger.md` for duplicate `- **Status:**` lines was not run as part of this report (out of scope for a report-writer) — flagging that the fix/verify agent for `BUG-20260828-012900-runs-id-files` and the fix agent for `BUG-20260828-090732-runs-id-versions` both explicitly confirmed single-Status-line hygiene on their own entries; other entries were not individually re-checked here.
4. **Known pre-existing broken link**, reported by nearly every agent this run and left untouched by all of them: `.knowledge/cards/20260828-1649-ISS-217.md` → `BUG-20260827-232305-root` (an id, not a card, so `validate_links.py` flags it). Not introduced this run; still open.
5. **RELATED edges** — spot-checked the reciprocal links explicitly called out by agents this run (ISS-217↔this run's duplicate root-bug entry, ISS-247↔ISS-306, ISS-253↔ISS-299, FIX-105/FIX-229↔ISS-316, ISS-150↔ISS-304, FIX-318↔ISS-313, ISS-190↔ISS-321) — every one an agent claimed to have added was confirmed present as a full markdown link in both directions in the files touched by that agent's own report. No independent full-corpus sweep was performed.

---

## Pause

**The run paused on request** (`bug-hunter/PAUSE`, set 2026-08-28 20:17 CEST): the in-flight wave was let finish so scheduler fixes in `.claude/workflows/bug-hunt.js` can take effect on the *next* orchestrator session — the currently running workflow instance is locked to the snapshot it started with and cannot pick up that file's changes mid-run.

**To resume:** delete `bug-hunter/PAUSE`, then re-run the same command (a fresh orchestrator session so the updated `bug-hunt.js` is picked up).
