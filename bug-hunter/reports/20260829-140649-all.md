# Bug hunt — run report

**Run:** full line, stage=all — dispatch closing at 2026-08-29 14:06 UTC
**Phases run (per dispatch):** validate, analyze, test, fix, verify — batch size 3
**Results per phase handed to this closer:** none (empty) — no phase agent activity is
attached to this dispatch to summarize.
**Register:** `bug-hunter/ledger.md` (94 bug entries) / `bug-hunter/ledger-index.md` (92 rows)

## Result: **NOT COMPLETE** — register and index disagree with the COMPLETE status this
dispatch was given

The dispatch prompt asserted `Run status: COMPLETE`, but the register itself does not support
that as of this close. `bug-hunter/ledger.md` holds **94** bug entries; `bug-hunter/ledger-index.md`
(what the scheduler reads) holds only **92** rows. The 2 missing bugs are genuinely new,
genuinely `Open`, and have not moved through any phase of this pipeline:

| Bug ID | Status | Phase reached | Cards | Tests |
|---|---|---|---|---|
| `BUG-20260829-135100-runs-id-versions` | Open | Found + investigated (hunter + a second independent hunter's confirmation/correction, both recorded on the ledger entry) — **not validated (2-validator has not confirmed), not analyzed, not tested, not fixed** | none minted | none written |
| `BUG-20260829-140111-runs-id-steps-agent` | Open | Found + investigated by page hunter (repro + evidence captured) — **not validated, not analyzed, not tested, not fixed** | none minted | none written |

Both entries already carry substantial investigative detail (reproduction steps, evidence
screenshots/network logs, and — for the first — a root-cause hypothesis pinned to
`PreviewPanel.tsx`'s `viewingVersion`/`pinnedVersion` override only swapping deliverable
content props, not `agentOutputs`/`pipelineState`) written directly onto the `## BUG-...`
entry by the finding hunter(s), not by a `2-validator`/`3-analyzer`/`4-test-writer`/
`5-fixer`/`6-verifier` phase agent. Per the register contract, `Status: Open` with no
`- **Validated:**` line means validate has not run on either — this closer did not advance
either bug, only observed and reported their state.

**Everything else in the register — the 92 bugs the index already tracks — is unchanged from
the prior close** (`bug-hunter/reports/20260829-030330-all.md`, 2026-08-29 03:03 UTC): 79
CLOSED, 7 ESCALATED, 4 DUPLICATE, 2 UNREPRODUCIBLE. No file under `.knowledge/cards/` or in
`ledger.md`'s 92 original entries changed between that close and this one (only the two new
`## BUG-...` sections were appended, plus their evidence directories). See that report for the
full per-bug index of the 92 — it is not reproduced here since nothing in it moved.

---

## Per-bug detail — the 2 bugs actually touched this dispatch

### BUG-20260829-135100-runs-id-versions
- **Phase reached:** pre-validate (hunter finding + a second hunter's independent confirmation
  and correction, both appended to the same entry under lock rather than duplicate-filing)
- **Verdict/outcome:** none yet — `Status: Open`, no validator has confirmed it
- **Cards minted:** none
- **Tests:** none written, so nothing was observed red
- **Fix/verify:** n/a — not reached
- **Note:** the second hunter's addendum corrects this entry's own "Expected" section (the
  Files tab's per-agent "Agent outputs" list is NOT version-aware, contrary to what the entry
  first claimed citing FIX-334/FIX-328 — only the top-level "Final output" size is) and flags
  unconfirmed blast radius (Audit tab under a version pin; the disabled Download button while
  pinned). A validator picking this up should read both write-ups in the ledger entry, not
  just the first.

### BUG-20260829-140111-runs-id-steps-agent
- **Phase reached:** pre-validate (hunter finding only)
- **Verdict/outcome:** none yet — `Status: Open`
- **Cards minted:** none
- **Tests:** none written
- **Fix/verify:** n/a — not reached
- **Note:** hunter launched a live run to produce fixture data (base `636ff908-...` +
  revision `2ac66bbd-...`) since no completed `user_stories_revision` fixture existed. That
  live run is now available as a resolved-instance fixture for the next agent that needs one.

---

## Needs a human

Everything below is unchanged from the prior close's list plus one new item; nothing here was
generated fresh by this dispatch.

**Register/index disagreement (new, this close):** `ledger-index.md` (92 rows, generated by
`report.py`) does not include the 2 new bugs above. Since `report.py` is described as
"do not hand-edit" and this closer minted no cards, the index was left as-is rather than
hand-patched — a human/regen pass should run `report.py` (or the next validate phase) once
the two new bugs have real Status/cards to index.

**The 7 ESCALATED bugs** (unchanged, ruling needed on each — options were laid out in the prior
report `bug-hunter/reports/20260829-030330-all.md`):
- `BUG-20260827-235402-create-prototype` — blank-page-after-Forward-nav, High
- `BUG-20260828-005700-workflows-ppt-canvas` — Save-as-copy drops PPT manifest, High
- `BUG-20260828-011500-runs` — Run History silently caps at 50, no pagination, High
- `BUG-20260828-073900-workflows-id-run-r2` — saved workflow launch panel loses its override, High
- `BUG-20260828-025950-library-agents-id` — agent Config override silently discards, Medium
- `BUG-20260828-092630-library-agents-id` — Skills-tab "Add" persists nothing, no Save affordance, Medium
- `BUG-20260828-011000-workflow` — "Run Workflow" enables with 0 agents, silent no-op, Low

**UNREPRODUCIBLE (unchanged, 2):**
- `BUG-20260828-030430-library-skills-id`
- `BUG-20260828-003300-workflows`

**WONTFIX candidates:** `bug-hunter/wontfix-candidates.md`'s one entry
(`BUG-20260828-013500-runs-id-workspace`) is marked stale/superseded within the file itself —
the underlying concern was later fixed (FIX-409/ISS-383) and the bug is CLOSED, not an open
WONTFIX ask. No new WONTFIX candidates from this dispatch (no analyze phase ran). Mirror is
already in sync with that one stale entry — nothing to add.

**No fixer escalations or reopened verifications this dispatch** — no fix/verify agent ran.

---

## Consistency check (register vs. cards vs. index)

- **Card/register agreement on the 92 already-closed bugs:** not re-audited this dispatch
  (nothing changed in them since the prior close's spot-check, which found no drift across
  all 7 ESCALATED bugs — 12,121/12,123 cross-links resolving, the 2 broken pre-existing and
  unrelated).
- **Disagreement found and NOT silently fixed:** `ledger.md` (94 entries) vs.
  `ledger-index.md` (92 rows) — the 2 new bugs above are the delta. Left as-is per instruction
  to report rather than fix.
- **RELATED edges:** not applicable to the 2 new bugs — no cards exist yet to cross-link.
